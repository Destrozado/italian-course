---
phase: 03-variedad-de-ejercicios-ergonom-a-de-teclado
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
# B4 revisión iteración 1 — requirement closure attribution:
# Este plan es un CHECKPOINT humano (UAT) sin artefactos de código. La closure formal
# de los 3 requisitos de Phase 3 NO depende exclusivamente de este plan, sino de la
# combinación PLAN PASS + UAT PASS:
#   - EXTYPE-02 closure depends on: 03-01 PASS + 03-03 UAT PASS
#   - EXTYPE-03 closure depends on: 03-02 PASS + 03-03 UAT PASS
#   - SESSION-06 closure depends on: 03-01 PASS + 03-02 PASS + 03-03 UAT PASS
# Por eso `requirements: []` en el frontmatter: este plan VERIFICA pero no IMPLEMENTA
# ninguno de los 3 requisitos por sí solo. Atribuir requirements aquí sin artefactos
# crearía ambigüedad de imputación si UAT termina en NEEDS-PATCH o FAIL — la
# verificación humana evidencia closure pero no la realiza ni la representa como
# implementación.
requirements: []
user_setup: []

must_haves:
  truths:
    - "El autor confirma que una sesión de 20 ejercicios mezclando los 3 tipos se completa sin tocar el ratón."
    - "El autor confirma que tras un fallo en cualquiera de los 3 tipos, recargar la página NO permite escapar la cascada (D-54 / D-61 exploit-proof)."
    - "El autor confirma que las transiciones entre los 3 tipos en la misma sesión son fluidas — sin saltos de UI, sin estados residuales del ejercicio anterior."
    - "El autor confirma que el comportamiento de teclado es predecible: 1-4 multi-choice; 1-9 dinámicos word-buttons; 1-9 izq + a-i der match; Enter/Space tras fallo avanza; Backspace word-buttons quita última."
    - "El autor confirma que el listener `@keydown.window` se desmonta limpio al cambiar `currentScreen` y que el foco queda en el body al montar/avanzar sesión (D-72) — paso 5 (Pitfall #1) + paso 12 (`document.activeElement`) del UAT cubren ambos lados de la decisión."
    - "El autor confirma que el summary final agrega correctamente aciertos/fallos por categoría tras una sesión mixta."
  artifacts: []
  key_links: []
---

<objective>
Checkpoint final de Phase 3 — verificación humana exhaustiva (UAT) que cierra los 3 requisitos (EXTYPE-02, EXTYPE-03, SESSION-06) y los 4 criterios de éxito de ROADMAP.

Este plan NO escribe código. Es un checkpoint formal en el que el autor (Claude Code spawnea el plan en modo `checkpoint:human-verify`) recorre una checklist exhaustiva que:
1. Confirma los 4 criterios de éxito de ROADMAP § "Phase 3".
2. Confirma los 5 hechos observables del bloque `must_haves.truths` (que sintetizan el cierre de los 3 requisitos).
3. Comprueba los 8 pitfalls de RESEARCH.md no se manifiestan en uso real.
4. Decide si Phase 3 está lista para `/gsd:complete-phase 3` o si emerge una corrección bloqueante.

Purpose: ningún check automatizado puede sustituir un humano resolviendo una sesión real de 20 ejercicios con teclado y observando que la experiencia es la prometida ("te obliga a no olvidar" sin fricción mecánica). El executor de este plan solo orquesta la verificación; el sign-off final es del autor.

**Atribución de closure (B4 revisión iteración 1):** este plan NO carga `requirements` en su frontmatter porque la closure formal de EXTYPE-02, EXTYPE-03 y SESSION-06 ocurre por la combinación PLAN PASS + UAT PASS, no por este plan en aislamiento:
- **EXTYPE-02** closure = 03-01 PASS (entrega los artefactos word-buttons end-to-end) + 03-03 UAT PASS (confirma que funciona en uso real con teclado).
- **EXTYPE-03** closure = 03-02 PASS (entrega los artefactos match end-to-end + D-61 + D-66) + 03-03 UAT PASS.
- **SESSION-06** closure = 03-01 PASS (rama multi-choice 1-4 + word-buttons 1-9 + Enter/Space + Backspace + foco al body) + 03-02 PASS (rama match 1-9 izq + a-i der) + 03-03 UAT PASS (confirma que una sesión completa sin ratón es viable).

Output: este checkpoint produce, tras la aprobación humana, un SUMMARY 03-03 que documenta la sesión UAT, cualquier ajuste observado, y un veredicto explícito (PASS / FAIL / NEEDS-PATCH). Si NEEDS-PATCH, el orquestador puede invocar `/gsd:plan-phase 3 --gaps` para abrir una iteración de cierre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-CONTEXT.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-RESEARCH.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-UI-SPEC.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-01-SUMMARY.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT exhaustivo de Phase 3 — sesión mixta de 20 ejercicios con teclado</name>
  <files>(ninguno — checkpoint humano; no se modifica código)</files>
  <read_first>
    - .planning/ROADMAP.md sección "Phase 3" (los 4 criterios de éxito)
    - .planning/REQUIREMENTS.md (los 3 IDs EXTYPE-02, EXTYPE-03, SESSION-06)
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-01-SUMMARY.md
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-02-SUMMARY.md
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-RESEARCH.md sección "Common Pitfalls" (los 8 pitfalls)
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-UI-SPEC.md sección "Keyboard shortcuts contrato exhaustivo"
  </read_first>
  <what-built>
    Phase 3 entrega cerrada por los planes 03-01 y 03-02:
    - Tipo word-buttons (EXTYPE-02): banco + área respuesta + Comprobar; teclado 1-9 dinámicos + Backspace + Enter/Space.
    - Tipo match (EXTYPE-03): dos columnas, validación instantánea por pareja, cascada D-61 inmediata + idempotente, parpadeo rojo 300ms, duplicados D-66 soportados; teclado 1-9 izq + a-i der.
    - Ergonomía teclado (SESSION-06): 1-4 multi-choice, Enter/Space tras fallo avanza, Backspace word-buttons quita última, foco al body via `@keydown.window` con cleanup automático.
    - Helper canónico `applyResultToSession` con exactamente 2 call-sites de `applyImmediateFailure` (decisión final + primer-fallo-match con guard).
    - Seed avere.json extendido: 12 multi-choice + 2 word-buttons + 3 match (uno con duplicados D-66).
    - Tests: 65+ verdes en `node --test tests/*.test.js` (incluyendo W3 idempotencia + W5 smoke handlers — sea como instancia real o como grep-presence verificada).
  </what-built>
  <action>
    El ejecutor de este checkpoint NO escribe código. Orquesta la verificación humana siguiendo el bloque `<how-to-verify>` paso a paso:

    1. Confirmar que `node --test tests/*.test.js` sigue verde tras los planes 03-01 y 03-02 (último check automatizable antes del UAT humano).
    2. Confirmar que `npx serve` corre y `http://localhost:3000` carga sin errores (smoke).
    3. Presentar al autor los 15 pasos del `<how-to-verify>` en orden, esperar la confirmación de cada uno o anotar issues.
    4. Recoger el veredicto final del autor: PASS / NEEDS-PATCH / FAIL con justificación.
    5. Escribir el SUMMARY 03-03 con: veredicto, lista de los 15 pasos con resultado (✓/✗/N-A), observaciones de UX no bloqueantes (candidatas a Phase 5 polish), confirmación o discrepancia con cada uno de los 5 `must_haves.truths`.

    Si el veredicto es PASS, el SUMMARY recomienda al orquestador ejecutar `/gsd:complete-phase 3` (que marcará EXTYPE-02, EXTYPE-03, SESSION-06 como Complete en REQUIREMENTS.md y actualizará STATE.md). **El comando `complete-phase` es quien materializa el cambio de estado de los 3 requisitos — este plan solo emite la señal PASS que lo habilita.**

    Si NEEDS-PATCH, el SUMMARY describe el patch puntual con la suficiente especificidad para que `/gsd:plan-phase 3 --gaps` pueda generar un Plan 03-04.

    Si FAIL, el SUMMARY describe el fallo bloqueante (qué decisión / qué archivo / qué pitfall se manifestó) y recomienda replanificar la pieza afectada (probablemente 03-01 o 03-02 reabierto).
  </action>
  <how-to-verify>
    **Preparación**:
    - Limpiar localStorage: en DevTools Console ejecutar `localStorage.removeItem('italianCourse.v1')`.
    - Recargar `http://localhost:3000` con `npx serve` corriendo.
    - Tener DevTools Application panel abierto para inspeccionar localStorage cuando se indique.

    **Checklist de verificación humana — 4 criterios de éxito de ROADMAP + 11 verificaciones complementarias**:

    **Criterio 1 (cubierto en Phase 1+2 — re-verificar no-regresión):**
    1. Abrir la app: home muestra Avere con badge ●/✓/★ + racha + total ejercicios + última fecha. Sin errores en consola. Sin banner `#error-banner` visible.

    **Criterio 2 (cubierto en Phase 2 — re-verificar no-regresión):**
    2. Pulsar "Repaso de 20" → picker; marcar Avere; pulsar Empezar. Sesión arranca con un ejercicio del tipo que el sampler elija primero (probablemente multi-choice por su mayor masa).

    **Criterio 3 (NUEVO — sesión mixta):**
    3. Completar los 20 ejercicios de la sesión. Anotar mentalmente si aparecieron los 3 tipos. Si NO aparecieron los 3 tipos (sampler ponderado puede dejar fuera alguno), abandonar (volver al home con confirmación) Y reiniciar Repaso hasta obtener una sesión que incluya al menos 1 ejercicio de cada tipo. NOTA: con 12 + 2 + 3 = 17 ejercicios en el pool, una sesión de 20 incluye TODOS los ejercicios (con repeticiones). En la práctica, los 3 tipos aparecen en cada Repaso a partir del segundo arranque (timesShown ponderando).

    **Criterio 4 (NUEVO — sin ratón):**
    4. En una sesión nueva (cuando aparezcan los 3 tipos), completar TODA la sesión usando SOLO el teclado:
       - Multi-choice: pulsar `1`, `2`, `3`, o `4` según la opción correcta. Tras acierto, esperar el auto-avance 600ms. Tras fallo, pulsar Enter para avanzar.
       - Word-buttons: pulsar `1`, `2`, `3`, ... sobre las palabras del banco en el orden de la traducción italiana. Si te equivocas con una palabra, pulsar `Backspace` para quitar la última. Pulsar `Enter` cuando esté lista. Tras acierto: auto-avance 600ms. Tras fallo: leer la frase correcta literal en la línea inferior, pulsar Enter para avanzar.
       - Match: pulsar `1` para seleccionar el primer item izq, luego pulsar la letra `a`-`i` correspondiente al item der que emparejas. Repetir hasta consumir todas las parejas. Si fallas una pareja, parpadeo rojo + deshacer; el flag `matchHadFailure` activo internamente. Al consumir la última pareja correcta, auto-avance 600ms (si no había fallo) o el ejercicio pasa marcado fallado.

       Anotar **cualquier** instante en que sintieras la NECESIDAD de tocar el ratón. Si alguno, registrarlo en el SUMMARY como NEEDS-PATCH.

    **Verificaciones complementarias** (smoke checks que cubren los 8 pitfalls de RESEARCH):

    5. **Pitfall #1 (listener huérfano) — D-72 cleanup automático al cambiar `currentScreen`**: tras completar la sesión y ver el summary, volver al home. Pulsar `1` en home → NO debe pasar nada (ni error en consola, ni efecto secundario). Pulsar `Space` en home → NO debe scrollear la página ni disparar acción. Esto valida que el `@keydown.window` se desmontó automáticamente con el outer `<template x-if="currentScreen === 'session'">` (D-72).

    6. **Pitfall #2 (saveState redundante en match)**: arrancar otro Repaso. Cuando aparezca un match, hacer 2-3 emparejamientos INCORRECTOS consecutivos en el mismo ejercicio. En DevTools Application panel localStorage, observar `italianCourse.v1` — el valor debe quedarse estable tras el PRIMER fallo (no se observa "Modified" en cada click subsiguiente). Confirmar que `categoryProgress.avere.status === 'no-hecha'` se setea en el primer fallo y NO cambia con los siguientes.

    7. **Pitfall #3 (sub-estado residual entre ejercicios)**: en un Repaso que mezcle 2 word-buttons consecutivos, verificar que el segundo word-buttons arranca con el banco completo (no parcial del anterior). Idem para 2 match consecutivos: el segundo arranca con `matchPairsConsumed: []` y ningún item con `.match-consumed`.

    8. **Pitfall #4 (setTimeout match-flash huérfano)**: en un match, hacer un emparejamiento incorrecto (parpadeo rojo activo). Durante los 300ms del parpadeo, pulsar inmediatamente el botón `← Volver al home` y confirmar el descarte. Esperar 1 segundo. Verificar que NO hay errores en consola (el setTimeout fue cancelado por `cancelMatchFlash` en `resetSession`).

    9. **Pitfall #5 (renumeración word-buttons >9)**: si el seed contiene un word-buttons con >9 palabras visibles (`avere-101` tiene 3 answer + 2 distractors = 5 visibles; los 2 nuevos no exceden 9). Para verificar el cap, **opcionalmente** editar manualmente avere.json añadiendo distractors hasta tener >9 palabras en un ejercicio; recargar; observar que las posiciones 10+ NO llevan sufijo Unicode `¹⁰`/`¹¹`/`¹²`. Revertir el cambio tras la verificación. **Esta verificación es opcional** — el cap `idx < 9` está cubierto por test unitario en 03-01.

    10. **Pitfall #6 (validator rechaza match malformado)**: editar manualmente avere.json corrompiendo un ejercicio match (ej. cambiar `pairs: [["a","b"]]` a `pairs: [["a"]]` — pair de length 1). Recargar. El `#error-banner` debe mostrar el error con `file: 'avere.json'`, `exerciseId: 'avere-XXX'`, `reason` explicativa. Revertir el JSON tras la verificación.

    11. **Pitfall #7 (read de payload tras unmount)**: en un word-buttons fallido, antes de pulsar Siguiente, observar la consola. Pulsar Siguiente → avance limpio sin TypeError en consola.

    12. **Pitfall #8 (foco residual) — D-72 foco al body al montar/avanzar sesión**: tras una sesión completa, abrir DevTools Console y ejecutar `document.activeElement` — debería ser `<body>` o un elemento sin focus visible (no un botón retenido). Esto valida D-72 "foco al body" — ningún botón debe retener focus visualmente tras la sesión.

    13. **D-54 exploit-proof multi-choice (no-regresión Phase 2)**: en multi-choice, pulsar tecla `1` sobre una opción incorrecta → rojo. Recargar página sin pulsar Siguiente → categoría Avere en `no-hecha`.

    14. **D-61 exploit-proof match (NUEVO Phase 3)**: en un match, hacer 1 emparejamiento incorrecto → parpadeo rojo. Cerrar pestaña inmediatamente (NO pulsar Siguiente, NO completar las parejas restantes). Recargar página → categoría Avere en `no-hecha`. Si Repaso, debería ser un "Repaso abandonado" que se descartó (los aciertos hipotéticos no quedan), pero la cascada del fallo SÍ quedó (D-54/D-61 prevalece sobre "abandono descarta").

    15. **D-66 duplicados visuales (NUEVO Phase 3)**: si el ejercicio `avere-202` aparece en una sesión, observar que la columna derecha tiene 2 items `'ha'` visibles (sin distintivo visual entre ellos). Emparejar `lui → primer 'ha'` (el de arriba). El primer 'ha' queda `.match-consumed`. Ahora emparejar `lei → segundo 'ha'` (el de abajo, único 'ha' visible que queda). Debería funcionar también. Idem en orden inverso: en otra sesión, primero `lei → segundo 'ha'`, luego `lui → primer 'ha'` (ahora único disponible). El grading consume por índice — cualquier orden funciona.

    **Veredicto final (el autor responde EXPLÍCITAMENTE):**
    - **PASS**: los 4 criterios de ROADMAP confirmados + 11 verificaciones complementarias sin issues bloqueantes. Phase 3 lista para `/gsd:complete-phase 3` — `complete-phase` marcará EXTYPE-02, EXTYPE-03 y SESSION-06 como Complete en REQUIREMENTS.md.
    - **NEEDS-PATCH**: un issue puntual emergió (ej. un atajo de teclado se sintió incómodo; un visual quedó raro). Describir el patch necesario; el orquestador invocará `/gsd:plan-phase 3 --gaps` con un plan 03-04 puntual.
    - **FAIL**: un bug bloqueante emergió (ej. cascada D-61 no persiste; sub-estado residual entre ejercicios; listener huérfano). Describir el fail; replanificación profunda necesaria.
  </how-to-verify>
  <verify>
    <human-check>Veredicto explícito del autor recogido en SUMMARY 03-03: "PASS" / "NEEDS-PATCH: &lt;descripción&gt;" / "FAIL: &lt;descripción&gt;".</human-check>
  </verify>
  <acceptance_criteria>
    - El autor proporciona un veredicto explícito (PASS / NEEDS-PATCH / FAIL) — el `<resume-signal>` no se acepta vacío ni ambiguo.
    - Los 15 pasos del `<how-to-verify>` se ejecutaron al menos una vez (incluso si el autor descubre un FAIL al paso 4 y aborta — los pasos 1-3 quedan documentados como PASS / N-A en el SUMMARY).
    - El SUMMARY 03-03 existe en `.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-03-SUMMARY.md` con: veredicto + lista de 15 pasos con resultado + cualquier observación UX no bloqueante + confirmación de los 5 `must_haves.truths` (uno por uno).
    - Si PASS: el SUMMARY recomienda explícitamente `/gsd:complete-phase 3` como siguiente acción Y enumera los 3 requisitos a cerrar (EXTYPE-02, EXTYPE-03, SESSION-06).
    - Si NEEDS-PATCH o FAIL: el SUMMARY describe el problema con suficiente especificidad (qué decisión / qué archivo / qué pitfall) para que la siguiente iteración tenga señal accionable.
  </acceptance_criteria>
  <done>
    El autor pronuncia un veredicto explícito tras recorrer los 15 pasos del UAT. El SUMMARY 03-03 documenta el veredicto, los resultados paso a paso, y las observaciones UX. Phase 3 queda lista para `/gsd:complete-phase 3` (PASS), iteración de cierre puntual (NEEDS-PATCH), o replanificación (FAIL).
  </done>
  <resume-signal>Escribir "PASS", "NEEDS-PATCH: &lt;descripción del patch&gt;", o "FAIL: &lt;descripción del fallo bloqueante&gt;"</resume-signal>
</task>

</tasks>

<threat_model>
Phase 3 plan 03 es un checkpoint humano — NO escribe código, NO toca archivos. Sin amenazas nuevas. Las mitigaciones de 03-01 y 03-02 ya están en sitio.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Repudiation | UAT inconcluso por falta de pasos claros | mitigate | Checklist exhaustivo (15 verificaciones numeradas) en `<how-to-verify>`; veredicto explícito requerido como `<resume-signal>`. |

Sin amenazas HIGH-severity.
</threat_model>

<verification>
Verificación humana exhaustiva — no automatizable. Los 15 pasos del `<how-to-verify>` cubren:
- Los 4 criterios de éxito de ROADMAP § "Phase 3".
- Los 8 pitfalls de RESEARCH.md aplicados a escenarios concretos de uso.
- Los 2 exploit-proof checks (D-54 multi-choice + D-61 match) — el núcleo del core value "te obliga a no olvidar".
- D-66 duplicados visuales (caso emblemático de la decisión de Phase 3).
</verification>

<success_criteria>
- [ ] Veredicto explícito del autor: PASS, NEEDS-PATCH, o FAIL.
- [ ] Si PASS: Phase 3 completa; el orquestador puede ejecutar `/gsd:complete-phase 3` para actualizar STATE.md y marcar los 3 requisitos (EXTYPE-02, EXTYPE-03, SESSION-06) como Complete (la closure formal ocurre en ese comando, no en este plan).
- [ ] Si NEEDS-PATCH: especificación clara del patch puntual; el orquestador abre Plan 03-04 vía `/gsd:plan-phase 3 --gaps`.
- [ ] Si FAIL: especificación clara del fallo; replanificación de la pieza afectada.
- [ ] El SUMMARY 03-03 documenta cualquier hallazgo, observación de UX, o lección aprendida durante el UAT (incluso si el veredicto es PASS — las lecciones se acumulan para Phase 4+).
</success_criteria>

<output>
Crear `.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-03-SUMMARY.md` al finalizar el checkpoint. Incluir:
- Veredicto final (PASS / NEEDS-PATCH / FAIL) con justificación.
- Lista de los 15 pasos de verificación con resultado individual (✓ ✗ N/A).
- Observaciones de UX que NO bloquearon el PASS pero podrían convertirse en Phase 5 polish (ej. "el parpadeo rojo a veces se siente muy corto", "preferiría ver el contador de parejas consumidas").
- Notas sobre la sensación general: ¿se cumple la promesa "sin ratón"? ¿la cascada inmediata D-61 se sintió justa o draconiana? ¿el sufijo Unicode `¹²³ᵃᵇᶜ` se lee bien en dark mode?
- Si PASS: confirmar que los 3 requisitos (EXTYPE-02, EXTYPE-03, SESSION-06) se marcarán Complete en REQUIREMENTS.md tras `/gsd:complete-phase 3`. Recordar la atribución: EXTYPE-02 closure = 03-01 + 03-03 UAT; EXTYPE-03 closure = 03-02 + 03-03 UAT; SESSION-06 closure = 03-01 + 03-02 + 03-03 UAT.
</output>
