---
phase: 03-variedad-de-ejercicios-ergonom-a-de-teclado
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/exercise-types/match.js
  - src/exercise-types/index.js
  - src/data/schema-validator.js
  - src/screens/app.js
  - index.html
  - styles.css
  - tests/exercise-types.test.js
  - content/exercises/avere.json
autonomous: true
requirements:
  - EXTYPE-03
  - SESSION-06
# SESSION-06 partition note (revisión iteración 1):
# SESSION-06 completion: este plan AÑADE la rama match (1-9 izq + a-i der) sobre el helper
# `handleSessionKey` instalado en 03-01. Closure final de SESSION-06 ocurre en UAT 03-03
# (la suma de 03-01 + 03-02 + UAT cierra el requisito; ninguno de los planes individualmente
# lo cierra). Esta duplicación de SESSION-06 entre 03-01 y 03-02 es INTENCIONAL — ambos
# contribuyen, ninguno satisface el requisito por sí solo.
user_setup: []

must_haves:
  truths:
    - "Tras cargar la app, los nuevos ejercicios match en avere.json se validan sin errores."
    - "Lanzar Repaso sobre Avere puede mostrar un ejercicio match: 2 columnas con sufijos numéricos 1..9 (izq) y alfabéticos a..i (der) (D-70)."
    - "Click sobre un item izq lo marca visualmente como seleccionado (outline azul Pico primary); click subsiguiente sobre un item der dispara la validación instantánea (acierto o fallo de la pareja) (D-60)."
    - "Cuando el usuario empareja correctamente dos items, ambos quedan visualmente apagados (muted) y dejan de ser clickeables; cuando el emparejamiento es incorrecto, los dos items parpadean en rojo durante 300ms y la selección se deshace, dejando ambos clickeables de nuevo (D-60)."
    - "Tras dos o más intentos erróneos consecutivos en el mismo ejercicio match, el contenido de `localStorage` (`italianCourse.v1`) se modifica EXACTAMENTE UNA VEZ — el primer fallo persiste la cascada D-61, los siguientes son no-op observables (DevTools Application panel)."
    - "Tras el primer intento erróneo en un ejercicio match, la categoría Avere queda inmediatamente en `status: 'no-hecha'` con `streakDays: 0` en localStorage, ANTES de que el usuario complete el ejercicio."
    - "El ejercicio match no se interrumpe tras un fallo: el usuario debe completar todas las parejas correctamente para avanzar; al consumir la última pareja, si hubo cualquier fallo previo, el ejercicio se contabiliza como fallado en el `sessionResults` final. Tamaño variable 2..~10 parejas; sin auto-completar la pareja final (forced-last-pair se hace por click manual) (D-63)."
    - "Cerrar la pestaña inmediatamente tras el primer fallo de un ejercicio match y reabrir la app: la categoría Avere sigue en `no-hecha` (D-61 exploit-proof — la regresión persistió aunque el usuario no llegó a pulsar Siguiente)."
    - "Teclado match: pulsar dígito `1..9` selecciona el item izq de esa posición visible; pulsar letra `a..i` con un izq ya seleccionado dispara la validación de la pareja {izq, der}; pulsar una letra sin haber pulsado un número antes no hace nada; pulsar otro dígito antes de la letra reemplaza la selección anterior (D-70)."
    - "Pulsar Enter o Space tras un fallo (en cualquier tipo) avanza al siguiente ejercicio; en match, Enter/Space tras consumir la última pareja con fallos previos también avanza tras el auto-advance habitual (D-71)."
    - "El grading de match es case-insensitive (toLowerCase + NFC) — `pair[0]` y `pair[1]` se comparan con los textos seleccionados sin distinguir mayúsculas (D-67)."
    - "Si la columna derecha contiene duplicados textuales (ej. dos items 'la' visibles), el usuario puede emparejar cualquier sustantivo válido con cualquiera de los dos: el grading consume por índice interno y ambos órdenes (primer 'la' luego segundo, o viceversa) producen progreso correcto del ejercicio (D-66)."
    - "Una sesión Repaso 20 sobre Avere que mezcle los 3 tipos (multiple-choice + word-buttons + match) se completa de principio a fin sin errores en consola y termina en un summary con el delta correcto por categoría."
    - "Editar avere.json a mano con un payload match malformado (pairs no-array, pairs con menos de 2 o más de 10 entradas, un pair sin 2 elementos, un pair con string vacío) hace que la app muestre el banner `#error-banner` con `file`, `exerciseId` y motivo legible en español antes de montar Alpine."
    - "Foco al body al montar/avanzar sesión: ningún botón retiene focus visualmente; el listener `@keydown.window` (instalado en 03-01) cubre las 18 teclas match (1-9 + a-i) y se desmonta automáticamente al cambiar `currentScreen` (D-72)."
  artifacts:
    - path: "src/exercise-types/match.js"
      provides: "Handler match con grade() que devuelve {correct, pairIdx?} consumiendo el primer pair libre que coincide case-insensitive (D-67) sobre payload {prompt, pairs: [[izq,der]]} (D-65); duplicados D-66 soportados por consumedPairIdx"
      exports: ["match"]
      min_lines: 25
    - path: "src/exercise-types/index.js"
      provides: "Registry con las 3 entradas finales: multiple-choice, word-buttons, match"
      contains: "'match': match"
    - path: "src/data/schema-validator.js"
      provides: "validateMatchPayload reemplaza el stub creado en 03-01 con validación completa de pairs (D-65 schema: 2..10 tuples de strings no vacíos)"
      contains: "validateMatchPayload"
    - path: "src/screens/app.js"
      provides: "Sub-estados match completos + handlers matchSelectLeft/matchPickRight + flashMatchPair (D-60 parpadeo 300ms) + handleSessionKey extendido con ramas 1-9 (left) y a-i (right) (D-70) + initSubStateForExercise extendido con rama match (shuffle Fisher-Yates de ambas columnas — D-62)"
      contains: "matchPickRight"
    - path: "index.html"
      provides: "Sub-template Alpine type === 'match' con .match-grid + 2 columnas (D-63 layout variable) + letterFor mapping para los sufijos alfabéticos (D-70)"
      contains: "sessionCurrentExercise.type === 'match'"
    - path: "styles.css"
      provides: ".match-grid, .match-col, .match-selected, .match-consumed, .match-flash + @keyframes match-flash-red (300ms iteración única WCAG-safe) (D-60 estados visuales + D-63 dos columnas + D-70 sufijos)"
      contains: "@keyframes match-flash-red"
    - path: "tests/exercise-types.test.js"
      provides: "Tests adicionales para match.grade (pareja correcta, incorrecta, duplicados D-66, case-insensitive D-67, índice consumido se ignora) + validator match (negative cases — D-65) + smoke test idempotencia matchPickRight (W3 revisión)"
      min_lines: 180
    - path: "content/exercises/avere.json"
      provides: "Al menos 2 ejercicios type match añadidos al seed para validar la mecánica end-to-end (un ejercicio sin duplicados, otro con duplicados en columna derecha para verificar D-66; payload D-65)"
      contains: "\"type\": \"match\""
  key_links:
    - from: "src/screens/app.js matchPickRight (rama !correct, primer fallo del ejercicio)"
      to: "applyImmediateFailure + saveState"
      via: "guard if (!this.matchHadFailure)"
      pattern: "if \\(!this\\.matchHadFailure\\)"
    - from: "src/screens/app.js matchPickRight (rama correct, al completar todas las parejas)"
      to: "applyResultToSession(ex, !this.matchHadFailure)"
      via: "branch que detecta this.matchPairsConsumed.length === ex.payload.pairs.length"
      pattern: "applyResultToSession\\(ex,"
    - from: "index.html match-col items izquierda"
      to: "handleSessionKey ramas 1-9 + matchSelectLeft"
      via: "sufijo numérico + click + tecla (D-70)"
      pattern: "matchSelectLeft"
    - from: "index.html match-col items derecha"
      to: "handleSessionKey ramas a-i + matchPickRight"
      via: "sufijo alfabético + letterFor mapping + click + tecla (D-70)"
      pattern: "matchPickRight"
    - from: "src/screens/app.js flashMatchPair"
      to: "cancelMatchFlash + clase CSS .match-flash"
      via: "setTimeout 300ms + cleanup en resetSession/destroy (D-72 cleanup)"
      pattern: "matchFlashHandle"
---

<objective>
Slice vertical 2/3 de Phase 3 — `match` end-to-end con ergonomía de teclado completa (1-9 izq + a-i der) y cascada inmediata D-61 idempotente en el primer fallo.

Tras este plan, el autor puede:
1. Lanzar Repaso 20 sobre Avere y encontrar ejercicios de los **3 tipos mezclados** (multiple-choice + word-buttons + match) en la misma sesión (3er criterio de éxito de ROADMAP cumplido a falta de UAT humano que confirma el flujo).
2. Resolver match con click (izq → der) o con teclado (1..9 + a..i).
3. Observar la cascada D-61 inmediata + idempotencia: el primer intento erróneo persiste la regresión a localStorage; intentos erróneos posteriores en el mismo ejercicio son no-op sobre el state (Pitfall #2 evitado).
4. Beneficiarse del exploit-proof: cerrar pestaña tras el primer fallo de match NO permite escapar la cascada.

Este plan REUSA sin modificar los 3 helpers establecidos por 03-01:
- `applyResultToSession(ex, correct)` — invocado UNA SOLA VEZ por ejercicio match: al completar la última pareja (con `correct = !this.matchHadFailure`).
- `initSubStateForExercise(ex)` — se le añade la rama `match` que baraja ambas columnas con `fisherYates`.
- `handleSessionKey(event)` — se le rellenan las ramas placeholder de match: dígitos 1-9 sobre `matchLeft` + letras a-i sobre `matchRight`.

Purpose: cerrar EXTYPE-03 + completar SESSION-06 a su extensión total (D-68..D-72). Habilitar el 3er criterio de éxito de ROADMAP (sesión mixta). Garantizar exploit-proof tras primer fallo match (D-61 + Pitfall #2).

Output: app funcional con los 3 tipos + teclado completo; sesión mixta verificable; cascada match exploit-proof; tests verdes incluyendo cobertura específica de D-66 duplicados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-CONTEXT.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-RESEARCH.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-PATTERNS.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-UI-SPEC.md
@.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-01-SUMMARY.md

<interfaces>
<!-- Heredados de 03-01, NO se modifican: -->

`src/exercise-types/index.js` ya contiene 2 entradas (`multiple-choice`, `word-buttons`); este plan añade `'match': match` como 3ª y última.

`src/screens/app.js` provee tras 03-01:
```
appShell()._instance = {
  // sub-estados match ya DECLARADOS en 03-01 (defaults vacíos):
  matchLeft: [], matchRight: [], matchSelectedLeftIdx: null,
  matchPairsConsumed: [], matchHadFailure: false,
  matchFlashIdx: null, matchFlashHandle: null,

  // helpers existentes a REUSAR sin modificar:
  applyResultToSession(ex, correct),    // único call-site de applyImmediateFailure para "decisión final"
  initSubStateForExercise(exercise),    // tiene comentario "// exercise.type === 'match' → 03-02 añade la rama"
  handleSessionKey(event),              // tiene comentarios "// match letras a-i llegan en 03-02" y "// ramas match llegan en 03-02"
  cancelMatchFlash(),                   // stub idempotente listo para flashMatchPair
  // resetSession() ya limpia los sub-estados match
  // destroy() ya invoca cancelMatchFlash
}
```

Desde `src/exercise-types/multiple-choice.js` + `src/exercise-types/word-buttons.js` (analogs canónicos del registry):
- Layer purity D-02 invariante (sin DOM/storage/fetch).
- `grade(exercise, response)` puro.

Forma del payload match (D-65):
```
{ prompt: string, pairs: Array<[string, string]> }   // 2 ≤ pairs.length ≤ 10
```

Forma de `response` que pasa `matchPickRight` a `match.grade(ex, response)` (D-66):
```
{ leftWord: string, rightWord: string, consumedPairIdx: number[] }
```

Forma del retorno enriquecido (D-66):
```
{ correct: boolean, pairIdx?: number }   // correct false → pairIdx ausente
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear match handler + reemplazar stubs del validator + extender registry + tests cubriendo D-66 duplicados + smoke test idempotencia matchPickRight</name>
  <files>src/exercise-types/match.js, src/exercise-types/index.js, src/data/schema-validator.js, tests/exercise-types.test.js</files>
  <read_first>
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-CONTEXT.md (D-65, D-66, D-67)
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-RESEARCH.md sección "Pattern 7" + "Code Examples · Example 4" (algoritmo de consumo por índice)
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-PATTERNS.md sección "src/exercise-types/match.js (NEW)"
    - src/exercise-types/multiple-choice.js (analog del registry)
    - src/exercise-types/word-buttons.js (analog Phase 3 plan 01 — mismo header/JSDoc estilo)
    - src/exercise-types/index.js (resultado de 03-01: tiene 2 entradas)
    - src/data/schema-validator.js (resultado de 03-01: dispatch table con stub `validateMatchPayload`)
    - tests/exercise-types.test.js (resultado de 03-01: estructura describe; añadir tests al final)
  </read_first>
  <behavior>
    - `match.grade({payload:{pairs:[['casa','la'],['libro','il']]}}, {leftWord:'casa', rightWord:'la', consumedPairIdx:[]})` retorna `{correct: true, pairIdx: 0}`.
    - `match.grade(... pairs:[['casa','la'],['libro','il']]}, {leftWord:'casa', rightWord:'il', consumedPairIdx:[]})` retorna `{correct: false}` (sin `pairIdx`).
    - `match.grade(... pairs:[['CASA','LA']]}, {leftWord:'casa', rightWord:'la', consumedPairIdx:[]})` retorna `{correct: true, pairIdx: 0}` (case-insensitive D-67).
    - **D-66 duplicados**: con `pairs:[['casa','la'],['porta','la']]`, `match.grade(ex, {leftWord:'casa', rightWord:'la', consumedPairIdx:[]})` retorna `{correct: true, pairIdx: 0}`. Después, `match.grade(ex, {leftWord:'porta', rightWord:'la', consumedPairIdx:[0]})` retorna `{correct: true, pairIdx: 1}` (el primer 'la' está consumed, encuentra el segundo).
    - **D-66 orden inverso**: con `pairs:[['casa','la'],['porta','la']]`, `match.grade(ex, {leftWord:'porta', rightWord:'la', consumedPairIdx:[]})` retorna `{correct: true, pairIdx: 0}` — busca el PRIMER pair libre que coincide textualmente; `porta`+`la` coincide tanto con pair 0 (NO, su izq es `casa`) como con pair 1 (sí, izq `porta`). El test debe verificar que matchea por texto AMBOS lados, no solo por derecha.
    - Índice consumido se ignora: con `pairs:[['casa','la']]` y `consumedPairIdx:[0]`, `match.grade(ex, {leftWord:'casa', rightWord:'la', ...})` retorna `{correct: false}` (no quedan pairs libres).
    - `response.consumedPairIdx` undefined: tratado como `[]`.
    - `validateContent` con 1 ejercicio match válido (`pairs:[['a','b'],['c','d']]`) retorna `{ok:true}`.
    - Negative cases para validator: `prompt` vacío, `pairs` no-array, `pairs` con `length < 2`, `pairs` con `length > 10`, pair sin array, pair con `length !== 2`, pair con string vacío en `[0]` o `[1]`, pair con no-string. Cada uno produce error con `file` + `exerciseId` + `reason` específica.
    - **W3 — Idempotencia matchPickRight**: smoke test al nivel del módulo (W3 revisión iteración 1). Si `appShell` puede instanciarse bajo node sin DOM (factory plana D-25), invocar dos veces consecutivas `matchPickRight(idxIncorrecto)` con `matchSelectedLeftIdx` reseteado entre invocaciones (simulando dos fallos consecutivos) y verificar que la rama `applyImmediateFailure` solo se ejecuta UNA vez. Aceptable proxy: spy/mock sobre `applyImmediateFailure` importado, contando invocaciones (`assert.equal(spy.calls.length, 1)`). Si el factory NO es testeable headlessly (depende de Alpine/document), DOCUMENTAR explícitamente el gap en `<threat_model>` (T-03-02-03 ya describe la mitigación; añadir nota "gap: idempotencia verificada solo por UAT humano paso 8") y al menos verificar por grep que la cadena `if (!this.matchHadFailure)` aparece en `src/screens/app.js` antes del call-site de `applyImmediateFailure` dentro de `matchPickRight`.
  </behavior>
  <action>
    Crear `src/exercise-types/match.js` con header de comentarios estilo `multiple-choice.js`/`word-buttons.js` citando "Decisiones aplicadas: D-65 (payload), D-66 (duplicados consumibles por índice), D-67 (case-insensitive), CONT-06 (NFC ya aplicado), Layer purity D-02 (sin DOM/storage/fetch)". Exportar `match` como objeto con `grade(exercise, response)`:

    - `const { pairs } = exercise.payload;`
    - `const consumed = new Set(response.consumedPairIdx ?? []);`
    - `const lwLower = (response.leftWord ?? '').toLowerCase();`
    - `const rwLower = (response.rightWord ?? '').toLowerCase();`
    - Recorrer `for (let i = 0; i < pairs.length; i++)`: si `consumed.has(i)` skip; `const [pl, pr] = pairs[i];` y si `pl.toLowerCase() === lwLower && pr.toLowerCase() === rwLower` retornar `{ correct: true, pairIdx: i }`.
    - Tras el bucle, retornar `{ correct: false }` (NO incluir `pairIdx: null` — usar la presencia del campo como señal, coherente con el JSDoc de RESEARCH).

    JSDoc explícito con `@param` + `@returns {{correct: boolean, pairIdx?: number}}` + explicación inline de la asimetría con multiple-choice: "el retorno enriquecido permite al caller marcar el `pairIdx` consumed para D-66 — no es por inconsistencia con el contrato del registry, sino por necesidad funcional".

    Modificar `src/exercise-types/index.js`: añadir `import { match } from './match.js';` y añadir la entrada `'match': match` al registry. Comentario header pasa a "Phase 3 completo: registry expone los 3 tipos finales".

    Modificar `src/data/schema-validator.js`: reemplazar la implementación stub de `validateMatchPayload` (creada en 03-01) por la implementación completa, manteniendo la firma `(ex, file, push)`:
    - `const { prompt, pairs } = ex.payload;`
    - `prompt` debe ser string no vacío (`typeof prompt !== 'string' || !prompt.trim()` → push "`payload.prompt` debe ser string no vacío").
    - `pairs` debe ser `Array.isArray`; si no, push "`payload.pairs` debe ser array de tuples [izq, der]" y `return` (no seguir).
    - `pairs.length < 2 || pairs.length > 10` → push "`payload.pairs` debe tener entre 2 y 10 entradas (encontrado: N)".
    - `pairs.forEach((p, idx) => { ... })`: si `p` no es array o `p.length !== 2`, push "`payload.pairs[idx]` debe ser tuple de exactamente 2 strings" y `return` del forEach iter; si `typeof p[0] !== 'string' || !p[0].trim()` push "`payload.pairs[idx][0]` debe ser string no vacío"; idem para `p[1]`.
    - Acumular errores (D-08): NO early return tras el primer error global; SÍ early return dentro de `forEach` cuando una pair no es array (para no crashear al traversar `p[0]` con `p = null`).

    Extender `tests/exercise-types.test.js` añadiendo AL FINAL del archivo (no entremezclar con los describes existentes del Task 1 de 03-01):
    - Import adicional: `import { match } from '../src/exercise-types/match.js';`.
    - `describe('exercise-types/match', ...)` con los 7 casos del bloque `<behavior>` arriba (correct, incorrect, case-insensitive, duplicado consumido, duplicado orden inverso textual, índice consumido, response sin consumedPairIdx).
    - Ampliar el describe `'exercise-types/index — registry'` añadiendo un test que verifica las 3 entradas finales: `'multiple-choice'`, `'word-buttons'`, `'match'` con identidades correctas.
    - `describe('data/schema-validator — match payload', ...)` con happy path + 7 negative cases (prompt vacío, prompt no-string, pairs no-array, pairs length 1, pairs length 11, pair sin array, pair length 1, pair[0] vacío, pair[1] no-string) — cada uno asserta `result.ok === false` Y error con sub-string esperado.
    - Verificar que el test "dispatch unknown type" de 03-01 SIGUE pasando — el mensaje de error ahora menciona los 3 tipos sin cambios.
    - **W3 — Idempotencia matchPickRight (revisión iteración 1)**: añadir un `describe('appShell.matchPickRight — D-61 idempotencia (W3)', ...)`. Estrategia:
      1. **Si `appShell` es importable y instanciable bajo node sin Alpine ni DOM**: importar `appShell` desde `'../src/screens/app.js'` y `applyImmediateFailure` desde `'../src/domain/progress.js'`. Usar `import.meta` / `module.children` interception o un wrapper de test que pase un `state` mock + `content` mock. Inicializar un sub-estado válido (`sessionFeedback = null`, `sessionCurrentExercise` apunta a un ejercicio match con `pairs:[['a','b'],['c','d']]`, `matchLeft = ['a','c']`, `matchRight = ['b','d']`, `matchSelectedLeftIdx = 0`, `matchPairsConsumed = []`, `matchHadFailure = false`, `state = {categoryProgress: {...}}`). Wrap `applyImmediateFailure` en un spy (o monkey-patch su import temporalmente). Invocar `matchPickRight(1)` (incorrecto — empareja 'a' con 'd'). Resetear `matchSelectedLeftIdx = 0` (simulando que el usuario re-seleccionó tras el deshacer). Invocar `matchPickRight(1)` de nuevo (segundo incorrecto). Assert: `spy.callCount === 1` (W3 — guard funciona).
      2. **Si `appShell` NO es importable bajo node** (depende de `document` global o de import-time side-effects): documentar el gap en el comentario del test (`test.skip` con razón explícita) Y verificar por grep que el guard está presente: `const src = readFileSync('src/screens/app.js', 'utf8'); assert.match(src, /if \(!this\.matchHadFailure\)\s*\{/);` con un mensaje explicativo "este test verifica la presencia textual del guard porque el factory no es instanciable headlessly; el comportamiento idempotente se valida por UAT humano paso 8 (03-03)".
    - **Test adicional — match.grade puro (sin appShell)**: invocar `match.grade(ex, {leftWord:'a', rightWord:'d', consumedPairIdx:[]})` dos veces consecutivas — ambas retornan `{correct: false}` deterministamente (verificación trivial; documenta que la idempotencia de `applyImmediateFailure` NO está en `match.grade` sino en el caller `matchPickRight`).
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/exercise-types/match.js` existe Y contiene `export const match` Y la propiedad `grade` es una función con `for` + `consumed.has(i)` + `toLowerCase()` aplicado a AMBOS lados.
    - `src/exercise-types/match.js` NO contiene ningún import (layer purity D-02) — verificable con `grep -c "^import" src/exercise-types/match.js` retorna 0.
    - `src/exercise-types/index.js` contiene literal `'match': match` Y `from './match.js'`.
    - `src/data/schema-validator.js` ya NO contiene el mensaje stub "aún no soportado" — verificable con `grep -F 'aún no soportado' src/data/schema-validator.js` retorna nada después de este Task. (Coordinado con 03-01 acceptance criterion B3 revisión: el mensaje stub en 03-01 ya NO contiene "en este plan (llegará en 03-02)" — pasa a `'type "match" aún no soportado'` estable, sin referencia a plan ID.)
    - `tests/exercise-types.test.js` contiene `describe('exercise-types/match'` Y `describe('data/schema-validator — match payload'` Y `describe('appShell.matchPickRight — D-61 idempotencia (W3)'` (W3 revisión).
    - `node --test tests/exercise-types.test.js` retorna exit 0 con al menos 26 tests passing (12+ de 03-01 + 7 grade match + 1 registry extendido + 8+ validator match + 1+ W3 idempotencia, sea como instancia real o como grep-presence).
    - `node --test tests/*.test.js` retorna exit 0 globalmente.
    - El test específico que cubre D-66 (duplicados con `consumedPairIdx:[0]`) está presente y verde — verificable con `grep -F "consumedPairIdx: [0]" tests/exercise-types.test.js` retorna al menos 1 línea.
    - El test específico de W3 (idempotencia) está presente: `grep -F "matchHadFailure" tests/exercise-types.test.js` retorna al menos 1 línea (sea como assertion sobre spy o como regex match sobre el código fuente).
  </acceptance_criteria>
  <done>
    Registry expone los 3 tipos finales. Validator dispatch table completa (sin stubs). Tests verdes con cobertura específica de D-66 duplicados Y de W3 idempotencia `matchHadFailure`. `match.grade` puro, layer D-02 invariante.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rellenar ramas match en appShell (handleSessionKey + initSubStateForExercise + handlers matchSelectLeft/matchPickRight + flashMatchPair) + sub-template match en index.html + CSS match + seed match en avere.json + smoke test UI behaviors (W5)</name>
  <files>src/screens/app.js, index.html, styles.css, content/exercises/avere.json, tests/exercise-types.test.js</files>
  <read_first>
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-CONTEXT.md (D-60, D-61, D-62, D-63, D-66, D-70, D-72)
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-RESEARCH.md secciones "Pattern 2" + "Pattern 3" (rama match) + "Pattern 8" + "Pattern 9" (ramas dígitos/letras de match) + "Pattern 11" + "Pitfall 2" + "Pitfall 4"
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-PATTERNS.md sección "src/screens/app.js (MODIFIED) — Analog 1/2/3" + "index.html (MODIFIED)" + "styles.css (MODIFIED)"
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-UI-SPEC.md secciones "Color (CSS overrides — match)" + "Estados visuales por componente — match" + "Keyboard shortcuts contrato exhaustivo" + "Comportamiento forced last pair"
    - .planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-01-SUMMARY.md (los stubs que dejó 03-01 — leer para no romper su shape)
    - src/screens/app.js (resultado de 03-01 — ramas match con comentarios "llegan en 03-02")
    - index.html líneas 226-285 aprox (resultado de 03-01 — tiene el sub-template multiple-choice anidado + sub-template word-buttons; añadir el de match como 3er hermano)
    - styles.css (resultado de 03-01 — tiene las clases .wb-* al final; añadir .match-* después)
    - content/exercises/avere.json (resultado de 03-01 — añadir 2-3 ejercicios match al final)
  </read_first>
  <action>
    **(a) Extender `src/screens/app.js` con las ramas match faltantes (D-62 shuffle ambas columnas; D-60 validación instantánea por pareja; D-70 teclas 1-9 izq + a-i der; D-72 foco al body y cleanup automático del listener):**

    Reemplazar los comentarios placeholder de 03-01 (`// exercise.type === 'match' → 03-02 añade la rama` en `initSubStateForExercise`, `// ramas match llegan en 03-02` y `// match letras a-i llegan en 03-02` en `handleSessionKey`) por implementaciones completas.

    En `initSubStateForExercise(exercise)`, añadir la rama después de la rama word-buttons:
    ```
    if (exercise.type === 'match') {
      this.matchLeft = fisherYates(exercise.payload.pairs.map(p => p[0]));
      this.matchRight = fisherYates(exercise.payload.pairs.map(p => p[1]));
      this.matchSelectedLeftIdx = null;
      this.matchPairsConsumed = [];
      this.matchHadFailure = false;
    }
    ```
    (la limpieza universal anterior ya pone todo a default; este bloque rellena con los shuffles. D-62 explícita el mismo RNG que el sampler — `fisherYates` ya importada en 03-01 desde `src/domain/session.js`.)

    Añadir GETTERS reactivos junto a los existentes:
    - `matchLeftIsConsumed(idx)` — método (NO getter, recibe argumento): `return this.matchPairsConsumed.some(p => p.leftIdx === idx);`. Defensivo: si `this.matchPairsConsumed` no es array (no debería pasar), tratar como `[]`.
    - `matchRightIsConsumed(idx)` — simétrico.
    - `letterFor(idx)` — método helper: `return idx < 9 ? String.fromCharCode(97 + idx) : '';` (mapea 0→'a', 8→'i', resto a vacío).

    Añadir MÉTODOS:
    - `matchSelectLeft(idx)` — guard: `if (this.sessionFeedback !== null) return; if (this.matchLeftIsConsumed(idx)) return;` y luego `this.matchSelectedLeftIdx = idx;`. (D-70: pulsar otro número antes de la letra reemplaza la selección — esta asignación cubre eso naturalmente.)
    - `matchPickRight(rightIdx)` — implementación canónica per RESEARCH Pattern 3 (rama match):
      1. Guards: `if (this.sessionFeedback !== null) return; if (this.matchSelectedLeftIdx === null) return; if (this.matchRightIsConsumed(rightIdx)) return;`.
      2. `const ex = this.sessionCurrentExercise; const leftIdx = this.matchSelectedLeftIdx; const leftWord = this.matchLeft[leftIdx]; const rightWord = this.matchRight[rightIdx];`.
      3. `const handler = registry['match'];`.
      4. `const result = handler.grade(ex, { leftWord, rightWord, consumedPairIdx: this.matchPairsConsumed.map(p => p.pairIdx) });`.
      5. Si `result.correct === true`:
         - Append `{ leftIdx, rightIdx, pairIdx: result.pairIdx }` a `this.matchPairsConsumed` (inmutable, asignación nueva): `this.matchPairsConsumed = [...this.matchPairsConsumed, { leftIdx, rightIdx, pairIdx: result.pairIdx }];`.
         - `this.matchSelectedLeftIdx = null;`.
         - Si `this.matchPairsConsumed.length === ex.payload.pairs.length` → completaste el ejercicio → `this.applyResultToSession(ex, !this.matchHadFailure);`. **Nota crítica**: `applyResultToSession` SIEMPRE se llama UNA vez al completar match (con `correct = false` si hubo cualquier intento erróneo). El `applyImmediateFailure` ya disparó en el primer fallo; aquí se contribuye a `sessionResults` para que `applySessionResult` al final marque `timesShown`/`timesFailed` (D-09 monotonicidad). La cascada al final es idempotente sobre el state ya reseteado (no-op para `categoryProgress`).
      6. Si `result.correct === false`:
         - **Guard Pitfall #2**: `if (!this.matchHadFailure) { const newState = applyImmediateFailure(this.state, ex, this.content, todayLocal()); saveState(newState); this.state = newState; this.matchHadFailure = true; if (this.sessionMode === 'test-completo') { this.persistInFlightTest(); } }`. La rama `this.matchHadFailure === true` es no-op respecto a localStorage — el state ya está reseteado, escribir de nuevo sería ruido (Pitfall #2).
         - Disparar el parpadeo: `this.flashMatchPair(leftIdx, rightIdx);`.
         - Deshacer la selección: `this.matchSelectedLeftIdx = null;`.
    - `flashMatchPair(leftIdx, rightIdx)` — implementación canónica per Pattern 11:
      ```
      this.cancelMatchFlash();   // defensivo si hay uno previo
      this.matchFlashIdx = { left: leftIdx, right: rightIdx };
      this.matchFlashHandle = setTimeout(() => {
        this.matchFlashIdx = null;
        this.matchFlashHandle = null;
      }, 300);
      ```
      `cancelMatchFlash` ya existe desde 03-01 y limpia ambos.

    Extender `handleSessionKey(event)` reemplazando los placeholders (D-70 teclas match: 1-9 izq + a-i der; D-72 cleanup automático del listener al cambiar `currentScreen`):
    - Rama dígitos `'1'..'9'` para `ex.type === 'match'`: `if (idx < this.matchLeft.length && idx < 9 && !this.matchLeftIsConsumed(idx)) { this.matchSelectLeft(idx); }`. Reemplaza el `// ramas match llegan en 03-02`.
    - Rama letras `'a'..'i'` (NUEVA — el placeholder de 03-01 era un comentario; ahora se añade la rama completa):
      ```
      if (key >= 'a' && key <= 'i') {
        if (this.sessionFeedback !== null) return;
        if (ex.type !== 'match') return;
        if (this.matchSelectedLeftIdx === null) return;   // letra sin número previo: ignorar (D-70)
        const idx = key.charCodeAt(0) - 97;
        if (idx < this.matchRight.length && idx < 9 && !this.matchRightIsConsumed(idx)) {
          this.matchPickRight(idx);
        }
        return;
      }
      ```
      Colocar esta rama tras la rama dígitos `'1'..'9'`, antes del fallthrough final.

    Verificar que `resetSession()` (creado en 03-01) ya limpia `matchPairsConsumed`, `matchHadFailure`, `matchSelectedLeftIdx` Y llama `cancelMatchFlash()`. Si en 03-01 quedaron en `[]`/`null`/`false` defaults sin limpiar, completar aquí.

    Verificar que `destroy()` invoca `cancelMatchFlash()`. (Ya debería desde 03-01.)

    **(b) Extender `index.html` con el sub-template match (3er hermano dentro del `<article>` session):**

    Tras los `<template x-if="sessionCurrentExercise.type === 'word-buttons'">` añadidos en 03-01, añadir un nuevo sibling `<template x-if="sessionCurrentExercise.type === 'match'">` con:
    ```
    <div>
      <div class="match-grid">
        <div class="match-col">
          <template x-for="(item, idx) in matchLeft" :key="'L_' + idx + '_' + item">
            <button type="button"
                    @click="matchSelectLeft(idx)"
                    :disabled="matchLeftIsConsumed(idx) || sessionFeedback !== null"
                    :class="{
                      'match-selected': matchSelectedLeftIdx === idx && !matchLeftIsConsumed(idx),
                      'match-consumed': matchLeftIsConsumed(idx),
                      'match-flash': matchFlashIdx && matchFlashIdx.left === idx
                    }"
                    :aria-label="'Sustantivo ' + (idx + 1) + ': ' + item">
              <span x-text="item"></span><sup class="kbd-hint" x-text="idx < 9 ? (idx + 1) : ''"></sup>
            </button>
          </template>
        </div>
        <div class="match-col">
          <template x-for="(item, idx) in matchRight" :key="'R_' + idx + '_' + item">
            <button type="button"
                    @click="matchPickRight(idx)"
                    :disabled="matchRightIsConsumed(idx) || sessionFeedback !== null"
                    :class="{
                      'match-consumed': matchRightIsConsumed(idx),
                      'match-flash': matchFlashIdx && matchFlashIdx.right === idx
                    }"
                    :aria-label="'Letra ' + letterFor(idx) + ': ' + item">
              <span x-text="item"></span><sup class="kbd-hint" x-text="letterFor(idx)"></sup>
            </button>
          </template>
        </div>
      </div>
      <button type="button" x-show="sessionFeedback === 'incorrect'" @click="sessionAdvance">Siguiente</button>
    </div>
    ```

    **NO duplicar** el botón `← Volver al home` ni el `<hr>` — quedan UNA sola vez fuera de los 3 sub-templates type, al final del `<article>` (ya estaban allí desde antes de Phase 3).

    El sub-template match double-defense Alpine: el outer x-if `currentScreen === 'session' && sessionCurrentExercise` ya guarda contra null; los bindings internos usan `matchFlashIdx && matchFlashIdx.left === idx` (no `?.` en la primera traversa — Pattern 1 + double-defense canónica). El `aria-label` de la derecha usa `letterFor(idx)` que retorna `''` para idx ≥ 9 — el screen reader anuncia "Letra : <item>" en ese caso, aceptable (poco común para A1/A2).

    **(c) Extender `styles.css` añadiendo al FINAL (tras las clases .wb-* de 03-01):**

    Sección comentada `/* ─── Phase 3 — match (Plan 03-02) ─── */` con:
    ```
    .match-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin: 1.5rem 0;
    }
    .match-col { display: flex; flex-direction: column; gap: 0.5rem; }

    .match-selected {
      outline: 2px solid var(--pico-primary, #1095c1);
      outline-offset: 2px;
    }
    .match-consumed {
      color: var(--pico-muted-color, #6c757d);
      background: transparent;
      border-color: var(--pico-muted-border-color, #e0e0e0);
      opacity: 0.55;
      cursor: not-allowed;
    }

    /*
     * Parpadeo rojo único (300ms ease-out, iteración 1 — NO loop).
     * WCAG 2.1 §2.3.1 safe (max 3 flashes/sec, este es 1 único).
     * El setTimeout en flashMatchPair desactiva el flag matchFlashIdx tras 300ms
     * (cancelable vía cancelMatchFlash en resetSession/destroy — Pitfall #4).
     */
    @keyframes match-flash-red {
      0%, 100% {
        background-color: transparent;
        border-color: var(--pico-primary-border, currentColor);
      }
      50% {
        background-color: var(--pico-color-red-500, #d32f2f);
        border-color: var(--pico-color-red-600, #b71c1c);
        color: white;
      }
    }
    .match-flash { animation: match-flash-red 300ms ease-out 1; }
    ```

    Comentario inline sobre cada bloque citando el D-NN aplicable (D-60 estados visuales; D-61 cascada inmediata pero sin señal visual extra; D-63 dos columnas variables; D-70 sufijos numéricos/alfabéticos).

    **(d) Añadir 2-3 ejercicios match al final de `content/exercises/avere.json`:**

    Aunque "match" es semánticamente más natural para categorías como género/número o profesiones, para Phase 3 lo añadimos a la categoría Avere usando un emparejamiento natural: persona italiana ↔ forma de avere conjugada. Esto valida el tipo end-to-end sin esperar a Phase 4. Los ejercicios:

    - `avere-200`: type `match`, categoryIds `["avere"]`, payload `{ prompt: "Empareja el sujeto con la forma de avere.", pairs: [["io", "ho"], ["tu", "hai"], ["lui", "ha"], ["noi", "abbiamo"]] }`, notes `"Match de conjugaciones avere — 4 parejas, sin duplicados."`.
    - `avere-201`: type `match`, categoryIds `["avere"]`, payload `{ prompt: "Empareja la persona con la forma de avere correspondiente.", pairs: [["voi", "avete"], ["loro", "hanno"], ["io", "ho"], ["lei", "ha"]] }`, notes `"Match con 'ha' en derecha — verifica que duplicados textuales serían posibles si existieran; aquí cada izq es distinta."`.
    - `avere-202`: type `match`, categoryIds `["avere"]`, payload `{ prompt: "Empareja sujeto con forma de avere (con duplicados).", pairs: [["lui", "ha"], ["lei", "ha"], ["noi", "abbiamo"]] }`, notes `"Match con DUPLICADOS textuales en columna derecha (D-66): dos 'ha' válidos — el grading los consume por índice; el usuario puede emparejar 'lui'→primer 'ha' o 'lui'→segundo 'ha' indistintamente."`.

    Verificar `JSON.parse` con `node -e 'JSON.parse(require("fs").readFileSync("content/exercises/avere.json","utf8"))'`.

    **(e) W5 — Smoke test UI behaviors (revisión iteración 1):**

    Añadir al final de `tests/exercise-types.test.js` un `describe('appShell match handlers — smoke (W5)', ...)` que intenta instanciar `appShell` y ejercitar `matchSelectLeft(0)` + `matchPickRight(0)` directamente:
    1. **Si `appShell` es instanciable bajo node sin Alpine/document**: importar `appShell` desde `'../src/screens/app.js'`. Crear instancia con `const inst = appShell();` y mínimo state mock (categoryProgress, exerciseProgress vacíos, schemaVersion 2). Setear `inst.content = { exerciseById: { 'm1': { id:'m1', type:'match', categoryIds:['avere'], payload:{ prompt:'p', pairs:[['a','b'],['c','d']] } } } };`. Llamar `inst.initSubStateForExercise(inst.content.exerciseById['m1'])`. Setear `inst.sessionCurrentExercise = inst.content.exerciseById['m1'];` (o equivalente getter mock). Llamar `inst.matchSelectLeft(0)` y asertar `inst.matchSelectedLeftIdx === 0`. Llamar `inst.matchPickRight(0)`. Asertar: si el par 0 era correcto, `inst.matchPairsConsumed.length === 1`; si no, `inst.sessionFeedback === null` (todavía no completó) Y `inst.matchHadFailure === true`. Capturar errores y fallar el test con mensaje claro si la instanciación lanza.
    2. **Si NO es instanciable** (lanza por dependencia de Alpine/DOM): el test debe usar `test.skip` con un mensaje claro: "appShell no es instanciable bajo node — UI behaviors de matchPickRight/matchSelectLeft se validan por UAT humano paso 4-8 (03-03). Test marcado como gap explícito (W5 revisión iteración 1)." En este caso, hacer al menos una verificación de presencia textual: `grep` en `src/screens/app.js` de `matchSelectLeft(`, `matchPickRight(`, `flashMatchPair(`, asertando que existen (`assert.ok(src.includes('matchSelectLeft('), ...)`).

    El test debe ejecutarse limpio bajo `node --test` sin fallar — `skip` es aceptable y deseable si la instanciación real no es factible.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -5</automated>
    <human-check>
      1. Arrancar `npx serve`, abrir `http://localhost:3000`. Verificar que NO hay errores en `#error-banner` (los nuevos ejercicios match validan).
      2. Verificar que la home muestra Avere con 17 ejercicios (12 multi-choice + 2 word-buttons + 3 match).
      3. Lanzar Repaso 20 sobre Avere. Reintentar hasta encontrar un ejercicio match (es probable que en 20 entren los 3 tipos, dado el sampler ponderado).
      4. Verificar visualmente el sub-template match: 2 columnas lado a lado (grid 1fr 1fr); cada item con sufijo `¹..⁹` en izq y `ᵃ..ⁱ` en der; outline azul Pico cuando se selecciona un item izq con click; al hacer click después en un item der CORRECTO ambos quedan muted con opacity 0.55 (clase `.match-consumed`); al hacer click en un item der INCORRECTO los dos parpadean en rojo durante 300ms (clase `.match-flash`) y se deshacen.
      5. Probar teclado: pulsar `1` → outline en primer item izq. Pulsar `a` → si la pareja es correcta, ambos quedan consumed; si no, parpadeo rojo + deshacer. Pulsar otro `1` antes de letra → reemplaza la selección anterior. Pulsar `a` sin haber pulsado número → ignorado silenciosamente.
      6. **D-61 exploit-proof**: pulsar `1` luego `a` (asumiendo pareja incorrecta) → parpadeo rojo. Abrir DevTools console: `JSON.parse(localStorage.getItem('italianCourse.v1')).categoryProgress.avere` debería mostrar `status: 'no-hecha', streakDays: 0, clearedExerciseIds: []` INMEDIATAMENTE. Cerrar pestaña + recargar → la categoría Avere sigue en `no-hecha` en home (cascada persistió).
      7. **D-66 duplicados** (ejercicio `avere-202`): si aparece en la sesión, verificar que `lui → primer 'ha'` y `lui → segundo 'ha'` ambos funcionan (no importa cuál de los dos 'ha' visibles eliges; el grading consume el primer libre). Empareja `lui` con uno de los 'ha' → consumed. Empareja `lei` con el OTRO 'ha' → consumed. Empareja `noi` con `abbiamo` → consumed. El ejercicio se completa y dispara `applyResultToSession`.
      8. **Idempotencia Pitfall #2**: en un match con N≥3 pairs, hacer 3 emparejamientos incorrectos consecutivos. Tras cada parpadeo rojo, verificar en DevTools que `localStorage.getItem('italianCourse.v1')` se escribe SOLO la primera vez (a partir del 2º error, el guard `matchHadFailure` evita el saveState redundante).
      9. **Sesión mixta** (criterio 3 de ROADMAP): lanzar Repaso 20 sobre Avere; intentar que la sesión incluya los 3 tipos (reintentar si la mezcla aleatoria deja fuera alguno). Completar de principio a fin sin errores en consola. El summary final muestra el delta correcto (Avere antes → Avere después, con racha y pendientes).
      10. **Sin ratón** (criterio 4 de ROADMAP, parcial — el UAT final de 03-03 lo verifica completo): pulsar Tab/Shift+Tab antes para que el body tenga foco (Pico aplica outline en focus-visible); pulsar números y letras según corresponda. Una sesión de 20 ejercicios debe ser completable sin tocar el ratón.
      11. Volver al home tras la sesión. Pulsar `1` en home → ningún error en consola, ningún efecto secundario.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `src/screens/app.js` ya NO contiene los comentarios `'// ramas match llegan en 03-02'` ni `'// match letras a-i llegan en 03-02'` ni `"// exercise.type === 'match' → 03-02 añade la rama"` — verificable con `grep -F "03-02" src/screens/app.js` retorna nada (sin ocurrencias).
    - `src/screens/app.js` contiene los identificadores: `matchSelectLeft(`, `matchPickRight(`, `flashMatchPair(`, `matchLeftIsConsumed(`, `matchRightIsConsumed(`, `letterFor(`.
    - `src/screens/app.js` contiene EXACTAMENTE 2 invocaciones de `applyImmediateFailure(`: una dentro de `applyResultToSession` (desde 03-01) y otra dentro de `matchPickRight` con guard `if (!this.matchHadFailure)`. Verificable con `grep -c "applyImmediateFailure(this.state" src/screens/app.js` retorna `2`.
    - `src/screens/app.js` contiene la cadena exacta `if (!this.matchHadFailure)` (Pitfall #2 guard).
    - `src/screens/app.js` invoca `applyResultToSession` desde `matchPickRight` solo cuando `this.matchPairsConsumed.length === ex.payload.pairs.length` (verificable inspeccionando el código).
    - `index.html` contiene la cadena `sessionCurrentExercise.type === 'match'` Y `.match-grid` Y `matchSelectLeft(idx)` Y `matchPickRight(idx)` Y `letterFor(idx)`.
    - **B1 revisión** — `index.html` NO añade nuevos botones "Volver al home" en sub-templates de tipo (la única instancia legítima vive FUERA de los 3 sub-templates type, en el outer `<article>` del session screen + las dos pre-existentes Phase 1+2): verificable con `grep -c "@click=\"requestReturnToHome\"" index.html` retorna `2` (idéntico al estado pre-Phase 3: picker + session). Cualquier valor distinto a 2 indica que se duplicó o se eliminó accidentalmente un botón legítimo. La cuenta de la frase literal `"Volver al home"` puede ser 3 (las 2 con `requestReturnToHome` + 1 con `returnToHomeFromSummary` en el summary screen) — esta cuenta NO debe cambiar.
    - `styles.css` contiene los selectores `.match-grid`, `.match-col`, `.match-selected`, `.match-consumed`, `.match-flash` Y la regla `@keyframes match-flash-red` con `0%, 100%` y `50%` keyframes Y `animation: match-flash-red 300ms ease-out 1` (iteración única, NO loop — WCAG-safe).
    - `content/exercises/avere.json` parsea como JSON válido Y contiene al menos 2 ejercicios cuyo `type` es exactamente `"match"` Y al menos uno de ellos contiene un duplicado textual en columna derecha (verificable con `node -e 'const ex = JSON.parse(require("fs").readFileSync("content/exercises/avere.json","utf8")).exercises; const matches = ex.filter(e => e.type === "match"); const withDup = matches.filter(m => { const rights = m.payload.pairs.map(p => p[1].toLowerCase()); return new Set(rights).size < rights.length; }); console.log("match count:", matches.length, "with-dup:", withDup.length);'` imprime `match count: 2` o `3` Y `with-dup: 1` o más).
    - **W5 revisión** — `tests/exercise-types.test.js` contiene el describe `'appShell match handlers — smoke (W5)'` Y ejecutarse con `node --test tests/exercise-types.test.js` no produce fallos (skip explícito es aceptable y documentado con mensaje).
    - `node --test tests/*.test.js` exit 0 con todos los tests verdes (incluyendo los 25+ de `tests/exercise-types.test.js`).
    - Verificación humana (11 pasos del `<human-check>`) aprobada, especialmente: D-61 exploit-proof, D-66 duplicados consumibles, Pitfall #2 idempotencia (`saveState` no se invoca redundantemente en errores match consecutivos).
  </acceptance_criteria>
  <done>
    Match end-to-end con click y teclado. Cascada D-61 inmediata + idempotente. Sesión Repaso 20 puede mezclar los 3 tipos sin saltos de UI. Listener `@keydown.window` cubre las 18 teclas (1-9 + a-i + Enter + Space + Backspace) con cleanup automático. CSS WCAG-safe (1 flash único). Seed con duplicados D-66 listo para UAT. W5 smoke test de matchSelectLeft/matchPickRight (real o skipped+grep) en place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Sin cambios respecto a 03-01: autor → JSON file system, JSON → Alpine render.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | JSON payload `match` (autor escribe `pairs: [["<script>","</script>"]]` o similar) | mitigate | T-02-01 ya activo desde Phase 1: TODO el render usa `x-text` exclusivamente; `<span x-text="item">` escapa cualquier markup. Cobertura por validator (Task 1 negative tests). |
| T-03-02-02 | Tampering | Schema validator stub→real (autor escribe `pairs: null` o `pair = "not-array"`) | mitigate | `validateMatchPayload` (Task 1) reemplaza el stub de 03-01 con validación exhaustiva. Banner visible `#error-banner` con `file` + `exerciseId` + `reason` antes de Alpine boot — el x-data no monta si el validator falla. |
| T-03-02-03 | Repudiation | `applyImmediateFailure` doble en match — escribe `saveState` redundantemente | mitigate | Guard `if (!this.matchHadFailure) { ... matchHadFailure = true; }` en `matchPickRight` (Pitfall #2). Verificación automatizada (W3 revisión iteración 1) en `tests/exercise-types.test.js`: smoke test que cuenta invocaciones de `applyImmediateFailure` tras 2 fallos consecutivos (1 sola vez); si `appShell` no es instanciable headlessly, fallback a verificación textual del guard. UAT humano paso 8 (03-03) cierra el gap residual. |
| T-03-02-04 | DoS local | setTimeout del flash match sobrevive al unmount de la sesión | mitigate | `cancelMatchFlash` se invoca en `resetSession`, `initSubStateForExercise`, `destroy` y `flashMatchPair` (cleanup previo defensivo) — Pitfall #4. |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | Sin paquetes nuevos (idéntico a 03-01). |

Sin amenazas HIGH-severity nuevas. Phase 3 plan 02 reusa el motor de validación y persistencia de Phase 1+2 sin alterar su arquitectura.
</threat_model>

<verification>
**Verificación local del plan completo** (después de ambos tasks):

1. `node --test tests/*.test.js` → exit 0, ≥65 tests passing (58 Phase 2 + 12 word-buttons 03-01 + ≥15 match 03-02 + W3 idempotencia + W5 smoke).
2. `npx serve` → `http://localhost:3000` arranca sin errores; los 3 ejercicios match nuevos pasan el schema validator.
3. Smoke test cascada D-61: provocar 1 fallo en match, recargar página inmediatamente sin completar el ejercicio → categoría Avere en `no-hecha` con `streakDays: 0` (exploit-proof).
4. Smoke test idempotencia Pitfall #2: 3 fallos consecutivos en el MISMO match → `saveState` solo se observa la primera vez (DevTools Application panel) Y test W3 en verde.
5. Smoke test D-66 duplicados: en `avere-202`, el primer 'la' y el segundo 'la' (visualmente idénticos) ambos sirven para emparejar — el grading consume por índice automáticamente.
6. Sesión mixta de 3 tipos termina sin errores; summary muestra el delta por categoría (4-5 mins).

**Queda para 03-03 (UAT humano completo):**
- Confirmación humana del 4º criterio de éxito: "sesión de 20 ejercicios sin necesidad de ratón **incluyendo word-buttons y match**" en una sola pasada.
- Confirmación humana del 3er criterio: la sesión mixta no tiene "saltos de UI" inesperados (transiciones entre tipos suaves).
- Confirmación humana de "foco al body" D-72: ningún botón retiene focus visualmente tras la sesión.
- Cierre formal de SESSION-06 (depende de 03-01 PASS + 03-02 PASS + 03-03 UAT PASS).
</verification>

<success_criteria>
- [ ] EXTYPE-03 (match) operativo end-to-end con click y con teclado (1-9 izq + a-i der).
- [ ] SESSION-06 cubierto en su extensión total (D-68 multi-choice + D-69 word-buttons + D-70 match + D-71 Enter/Space + D-72 cleanup automático) — **closure final via 03-03 UAT**.
- [ ] Cascada D-61 inmediata + idempotente: primer fallo persiste; fallos posteriores en mismo ejercicio son no-op para localStorage.
- [ ] Pitfall #2 (saveState redundante) evitado por guard `matchHadFailure` Y verificado en test W3.
- [ ] Pitfall #4 (setTimeout match-flash huérfano) evitado por `cancelMatchFlash` en lifecycle hooks.
- [ ] D-66 duplicados textuales en columna derecha funcionan (verificable con el ejercicio seed `avere-202`).
- [ ] Sesión Repaso/Test completo mixto (3 tipos) corre sin errores de consola.
- [ ] Tests verdes: `node --test tests/*.test.js` exit 0 con ≥65 tests passing.
- [ ] `applyImmediateFailure` invocado exactamente desde 2 call-sites (uno en `applyResultToSession`, otro en `matchPickRight` con guard) — arquitectura clara, no propenso a duplicarse.
- [ ] Phase 2 sigue intacta (home/picker/session/summary/banner inFlightTest), Phase 3 plan 01 (word-buttons) sigue intacto.
</success_criteria>

<output>
Crear `.planning/phases/03-variedad-de-ejercicios-ergonom-a-de-teclado/03-02-SUMMARY.md` al finalizar. Incluir explícitamente:
- Confirmación de que los stubs de 03-01 fueron reemplazados (validator + handleSessionKey + initSubStateForExercise) sin tocar el shape de los helpers compartidos.
- Verificación de que `applyImmediateFailure` tiene exactamente 2 call-sites con guards diferentes (una decisión final, una primer-fallo-match).
- Confirmación del comportamiento de "forced last pair" (NO auto-completar) — la decisión del UI-SPEC se respetó: el usuario hace el click final manualmente.
- Notas sobre la mecánica match-flash bajo carga real (¿300ms se siente lento/rápido?); recomendación post-UAT si el autor lo pide.
- Reporte explícito sobre W3 (idempotencia): ¿el test corrió como instancia real o quedó skipped + grep-verificado? Si skipped, documentar la razón técnica concreta y reservar la verificación al UAT 03-03 paso 8.
- Reporte explícito sobre W5 (UI behaviors smoke): mismo formato — instancia real o skip+grep.
- Reserva de capacidad para 03-03 UAT: lista de los 4 criterios de éxito de ROADMAP + las 11 verificaciones humanas que el plan 03 hará un checkpoint formal.
</output>
