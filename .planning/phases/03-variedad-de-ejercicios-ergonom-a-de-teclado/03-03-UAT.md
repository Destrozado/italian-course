---
phase: 03-variedad-de-ejercicios-ergonom-a-de-teclado
plan: 03
type: uat-checklist
status: pending-human-verification
generated: 2026-05-23
tests_baseline: 105/105 verdes
covers:
  - ROADMAP Phase 3 success criteria 1-4
  - 8 pitfalls de 03-RESEARCH.md
  - D-54 exploit-proof multi-choice (no-regresión Phase 2)
  - D-61 exploit-proof match (NUEVO Phase 3)
  - D-66 duplicados visuales (NUEVO Phase 3)
  - W2 Phase 2 regression smoke (sessionSelectOption refactor)
  - W3 idempotencia runtime via DevTools localStorage
  - D-72 listener cleanup + foco al body
---

# Phase 3 — Plan 03-03 UAT Checklist

> **UAT (User Acceptance Test) — verificación humana exhaustiva**
>
> Tu trabajo: recorrer los 15 pasos abajo, marcar cada uno como `[PASS] / [FAIL] / [N/A]`, anotar cualquier observación de UX no bloqueante, y al final emitir un veredicto explícito **PASS / NEEDS-PATCH / FAIL**.
>
> El executor automatizado ya confirmó (pre-UAT readiness):
> - 105/105 tests verdes (`node --test tests/*.test.js`)
> - Commit chain Phase 3 intacto (10 commits: cb17a97 → 6a10bed)
> - 2 call-sites de `applyImmediateFailure(this.state` en `src/screens/app.js` (invariante D-61)
> - avere.json: 17 ejercicios (12 multi-choice + 2 word-buttons + 3 match)
> - Archivos clave de Phase 3 presentes y no vacíos
>
> **Tiempo estimado del UAT:** 20-30 minutos.

---

## How to Run

### 1. Arranca el servidor local

```bash
# En la raíz del proyecto:
cd /home/vcompanyb/italian-course
npx serve
```

`npx serve` te dirá el puerto exacto (típicamente `http://localhost:3000`). Bookmark esa URL.

### 2. Abre el navegador

- Abre `http://localhost:3000` (o el puerto que `npx serve` indique).
- **Abre DevTools** (F12 o Cmd+Opt+I). Necesitarás:
  - **Console tab** — para verificar ausencia de errores y ejecutar comandos puntuales (`localStorage.removeItem`, `document.activeElement`).
  - **Application tab → Local Storage → `http://localhost:3000`** — para inspeccionar la key `italianCourse.v1` y observar las escrituras.

### 3. Reset inicial (limpia estado de pruebas previas)

En **Console**, ejecuta:

```js
localStorage.removeItem('italianCourse.v1');
location.reload();
```

Tras la recarga deberías ver la home con Avere sin progreso (estado inicial limpio).

### 4. Ejecuta los 15 pasos en orden

Sigue los pasos abajo. Después de cada paso marca su `Status` como `[PASS]`, `[FAIL]`, o `[N/A]` con notas si quieres.

### 5. Emite el veredicto

Al final del archivo hay una sección `## Veredicto`. Escribe **PASS**, **NEEDS-PATCH: ...**, o **FAIL: ...** ahí. Esa es la señal que el orquestador necesita para decidir el siguiente paso.

---

## Cobertura del UAT — visión rápida

| Bloque | Pasos | Cubre |
|--------|-------|-------|
| **ROADMAP Criterio 1** (no-regresión Phase 1+2) | 1 | Home, Avere visible, badges, racha, total ejercicios, sin errores |
| **ROADMAP Criterio 2** (no-regresión Phase 2) | 2 | Picker → "Repaso de 20" arranca sesión |
| **ROADMAP Criterio 3** (NUEVO) — sesión mixta | 3 | Los 3 tipos aparecen en una sesión |
| **ROADMAP Criterio 4** (NUEVO) — sin ratón | 4 | Sesión completa solo con teclado (1-4 + 1-9 + Backspace + Enter + 1-9 izq + a-i der) |
| **W2 Phase 2 regression smoke** | 4 (inline en multi-choice) | sessionSelectOption refactor no regresionado |
| **Pitfall #1** — listener huérfano D-72 | 5 | `@keydown.window` se desmonta al cambiar `currentScreen` |
| **Pitfall #2** — saveState redundante match | 6 | DevTools localStorage observable: 1 write tras N fallos |
| **Pitfall #3** — sub-estado residual entre ejercicios | 7 | 2 word-buttons o 2 match consecutivos arrancan limpios |
| **Pitfall #4** — setTimeout match-flash huérfano | 8 | Abandonar mid-flash sin errores en consola |
| **Pitfall #5** — renumeración word-buttons >9 (opcional) | 9 | Posiciones 10+ renderizan sin sufijo |
| **Pitfall #6** — validator rechaza match malformado | 10 | Banner `#error-banner` con archivo + exerciseId + reason |
| **Pitfall #7** — read de payload tras unmount | 11 | Avance limpio tras fallo word-buttons (sin TypeError) |
| **Pitfall #8** — foco residual D-72 | 12 | `document.activeElement` es `<body>` tras la sesión |
| **D-54 exploit-proof multi-choice** | 13 | Cascada persiste tras recarga sin pulsar Siguiente |
| **D-61 exploit-proof match (NUEVO)** | 14 | Cascada persiste tras cerrar pestaña en primer fallo match |
| **D-66 duplicados visuales (NUEVO)** | 15 | avere-202 acepta `lui→primer 'ha'` Y `lui→segundo 'ha'` |

---

## Checklist — 15 Pasos

### Criterio 1 (ROADMAP) — no-regresión Phase 1+2

#### Paso 1 — Home arranca limpia

**Acción:**

1. Reset: en Console, `localStorage.removeItem('italianCourse.v1'); location.reload();`
2. Observar la home tras la recarga.

**Resultado esperado:**

- Se ve la categoría **Avere** con badge **● (no-hecha)** (sin ✓ ni ★).
- Se muestra la racha (probablemente `0 días`), total de ejercicios (`17` — porque 03-01 añadió 2 word-buttons y 03-02 añadió 3 match al seed Avere original de 12), última fecha (probablemente vacía o "—").
- **Console SIN errores** rojos. Sin warnings extraños.
- **NO se ve** el banner `#error-banner` (el de "Error cargando contenido").

**Status:** `[pending]`

**Notas:**

---

### Criterio 2 (ROADMAP) — no-regresión Phase 2

#### Paso 2 — Repaso de 20 arranca sobre Avere

**Acción:**

1. Pulsar **"Repaso de 20"** en la home.
2. Aparece el picker de categorías.
3. Marcar **Avere** (checkbox).
4. Pulsar **"Empezar"** (o equivalente).

**Resultado esperado:**

- La sesión arranca, mostrando un ejercicio (probablemente multi-choice por su mayor masa: 12 de 17).
- Indicador "Ejercicio 1 / 20" visible.
- Botón "← Volver al home" visible.

**Status:** `[pending]`

**Notas:**

---

### Criterio 3 (ROADMAP) — sesión mixta de los 3 tipos

#### Paso 3 — Los 3 tipos aparecen en una sesión

**Acción:**

1. Completar la sesión iniciada en el Paso 2 (cualquier modo: con ratón vale para este paso).
2. Anotar mentalmente si aparecieron los 3 tipos (multi-choice + word-buttons + match).
3. Si NO aparecieron los 3 tipos, abandonar (volver al home con confirmación "Descartar esta sesión...") Y reiniciar Repaso hasta obtener una sesión que incluya **al menos 1 ejercicio de cada tipo**.

**Resultado esperado:**

- Eventualmente aparecen los 3 tipos en una sola pasada de 20 ejercicios.
- **NOTA**: con 17 ejercicios en el pool y 20 slots, el sampler PRÁCTICAMENTE garantiza los 3 tipos al segundo intento (timesShown ponderando empuja los menos vistos hacia arriba).

**Status:** `[pending]`

**Notas:** (anotar cuántos ejercicios de cada tipo viste, si percibiste algún salto raro de UI entre tipos)

---

### Criterio 4 (ROADMAP) — sin ratón

#### Paso 4 — Sesión completa solo con teclado (W2 Phase 2 regression smoke inline)

> **Este paso cubre simultáneamente Criterio 4 ROADMAP + W2 Phase 2 regression smoke** (el smoke smoke de `sessionSelectOption` refactor de 03-01: si las teclas 1-4 en multi-choice se comportan exactamente como pre-refactor, el regression smoke está OK).

**Acción (NO TOCAR EL RATÓN):**

1. Reset suave si quieres empezar limpio: `localStorage.removeItem('italianCourse.v1'); location.reload();`
2. Pulsar "Repaso de 20" → marcar Avere → "Empezar" (estos clicks SÍ con ratón, son orquestación; el contrato "sin ratón" aplica DURANTE el ejercicio).
3. Para cada uno de los 20 ejercicios:
   - **Multi-choice:** pulsar `1`, `2`, `3`, o `4` según la opción correcta.
     - Tras acierto: auto-avance 600ms (no toques nada).
     - Tras fallo: leer la respuesta correcta, pulsar **Enter** para avanzar.
   - **Word-buttons:** pulsar las teclas `1`-`9` sobre las palabras del banco en el orden correcto de la traducción italiana.
     - Si te equivocas con una palabra: pulsar **Backspace** para quitar la última.
     - Cuando esté listo: pulsar **Enter** (= botón "Comprobar").
     - Tras acierto: auto-avance 600ms.
     - Tras fallo: leer la respuesta correcta literal, pulsar Enter para avanzar.
   - **Match:** pulsar `1` para seleccionar el primer item izq, luego pulsar la letra `a`-`i` correspondiente al item der que emparejas. Repetir.
     - Si fallas una pareja: ves parpadeo rojo 300ms; sigue intentando.
     - Al consumir la última pareja correctamente: auto-avance 600ms (o el ejercicio queda marcado fallado si `matchHadFailure=true`).
4. Completar los 20 ejercicios.

**Resultado esperado:**

- **Cero veces** debiste tocar el ratón durante un ejercicio.
- Las teclas 1-4 multi-choice se comportan IDÉNTICAMENTE a como lo hacían en Phase 2 antes del refactor de 03-01 (W2 regression).
- En multi-choice de 3 opciones, pulsar `4` no hace nada (D-68).
- Backspace en word-buttons quita la última palabra colocada (no la primera, no una aleatoria).
- En match, pulsar una letra `a-i` SIN número previo es ignorado silenciosamente (no crashea, no hace nada).
- En match, pulsar otro número antes de la letra REEMPLAZA la selección anterior (no la añade).
- Tras acierto, Enter/Space NO hacen nada (auto-avance 600ms es lo único que avanza).
- Tras fallo, Enter Y Space avanzan equivalentemente.
- Sin errores en la Console.

**Status:** `[pending]`

**Notas:** (si echaste de menos el ratón en algún momento, anota cuándo y por qué; eso es NEEDS-PATCH potencial)

---

### Pitfall #1 — listener huérfano D-72

#### Paso 5 — `@keydown.window` se desmonta al cambiar `currentScreen`

**Acción:**

1. Tras completar la sesión del Paso 4, deberías ver la pantalla de **summary**.
2. Pulsar el botón para **volver al home** (Continuar / Volver / Aceptar — el copy del summary).
3. **YA EN LA HOME:** pulsar la tecla `1`.
4. Pulsar la tecla `Space`.

**Resultado esperado:**

- Pulsar `1` en la home **NO hace absolutamente nada**: no aparece ningún error en consola, no se inicia un ejercicio, no cambia ningún visual.
- Pulsar `Space` en la home **NO scrollea la página** (esto requeriría que el handler global hubiese seguido activo con `e.preventDefault()`; si la página scrollea es OK porque el listener YA NO ESTÁ; si la página NO scrollea Y hay un error en Console, es FAIL).
- **Mejor caso (PASS):** pulsar Space scrollea la página (= el navegador hace lo suyo porque el listener Alpine ya se desmontó). Sin errores en Console.

**Status:** `[pending]`

**Notas:**

---

### Pitfall #2 — saveState redundante en match (W3 idempotencia runtime)

#### Paso 6 — DevTools localStorage observable tras N fallos match

**Acción:**

1. Arrancar otro "Repaso 20" → Avere → Empezar (los clicks de orquestación están permitidos).
2. **Abrir DevTools → Application tab → Local Storage → `http://localhost:3000`**. Tenerlo visible.
3. Avanzar hasta que aparezca un ejercicio **match** (puedes pulsar Enter tras los multi-choice y word-buttons rápidamente).
4. En el match: hacer **3 emparejamientos INCORRECTOS consecutivos** en el mismo ejercicio (pulsar `1` + una letra incorrecta, ver parpadeo rojo, pulsar otra letra incorrecta, etc.).
5. Observar el valor de `italianCourse.v1` en el Application panel **EN TIEMPO REAL**.

**Resultado esperado:**

- **TRAS EL PRIMER FALLO**: la key `italianCourse.v1` se MODIFICA — el campo `categoryProgress.avere.status` pasa a `'no-hecha'` y `streakDays: 0`. **Visible**: el valor del item en Application panel se actualiza.
- **TRAS LOS FALLOS SIGUIENTES (2do, 3er, etc.)**: la key NO se vuelve a modificar (no "Modified" visible en cada click subsiguiente; el contenido sigue idéntico al primer fallo). **El guard `if (!this.matchHadFailure)` impide la cascada redundante** (Pitfall #2 D-61 idempotencia).

**Status:** `[pending]`

**Notas:** (si dudas, mira el campo `categoryProgress.avere.lastUpdateTimestamp` o equivalente — si lo hubiera; pero más práctico es ver si el item del Application panel se "highlights" en gris/azul tras cada fallo (señal de write) o no.)

---

### Pitfall #3 — sub-estado residual entre ejercicios

#### Paso 7 — 2 word-buttons consecutivos arrancan limpios (Y idem 2 match)

**Acción:**

1. Continuar la sesión del Paso 6, o reiniciar Repaso. Avanzar.
2. Si aparecen **2 word-buttons consecutivos** (avere-100 y avere-101 son los únicos word-buttons; quizá no son consecutivos en una sesión típica — paso opcional):
   - Verificar que el SEGUNDO word-buttons arranca con el banco COMPLETO (todas las palabras + distractoras visibles), área respuesta VACÍA con su placeholder "Construye la frase...".
3. Si aparecen **2 match consecutivos** (3 match en el pool — más probable):
   - Verificar que el SEGUNDO match arranca con NINGÚN item con clase `.match-consumed` (= "apagado"), el `matchSelectedLeftIdx` interno es null (= ningún item con outline azul), todas las parejas pendientes.

**Resultado esperado:**

- Sub-estado limpio. El banco del 2do word-buttons NO contiene palabras del 1ero. El 2do match NO tiene `.match-consumed` heredado del 1ero.

**Status:** `[pending]`

**Notas:** (si NO te aparecen 2 del mismo tipo consecutivos, marca este paso `[N/A]` y muévete — el cubrimiento estructural de `initSubStateForExercise` está cubierto por el test W5 smoke "rama match con shuffle de ambas columnas".)

---

### Pitfall #4 — setTimeout match-flash huérfano

#### Paso 8 — Abandonar mid-flash sin errores

**Acción:**

1. En un match (sigue el flow desde el Paso 6 o reinicia Repaso).
2. Hacer un emparejamiento INCORRECTO → parpadeo rojo activo (dura 300ms).
3. **DURANTE los 300ms del parpadeo** (rápido — tienes que ser ágil), pulsar el botón "← Volver al home".
4. Aparece la confirmación "¿Descartar esta sesión...?". Confirmar **Descartar**.
5. Esperar **1 segundo** (más que los 300ms del flash).
6. Mirar la Console.

**Resultado esperado:**

- **CERO errores en Console** durante o tras el descarte.
- En particular, sin `TypeError: Cannot read properties of null` ni `Cannot read property 'left' of null` ni similar — esto sería el síntoma de que el setTimeout del flash se ejecutó tras el unmount intentando leer `matchFlashIdx` que ya fue limpiado.
- **El listener `@keydown.window` también está limpio**: pulsar 1 / a / Backspace en la home NO hace nada (re-verificación cruzada con Paso 5).

**Status:** `[pending]`

**Notas:**

---

### Pitfall #5 — renumeración word-buttons >9 (OPCIONAL)

#### Paso 9 — Posiciones 10+ renderizan sin sufijo

**Acción (OPCIONAL — el cap idx<9 está cubierto por test unitario en 03-01):**

1. **Solo si te apetece testarlo manualmente:** editar `content/exercises/avere.json` añadiendo distractores a `avere-101` hasta tener `>9` palabras visibles (banco + answer + distractors).
2. Recargar `http://localhost:3000`.
3. Cuando aparezca el `avere-101` en una sesión, **observar visualmente** los sufijos del banco.
4. **Revertir el cambio** tras la verificación (`git checkout content/exercises/avere.json`).

**Resultado esperado:**

- Las primeras 9 palabras del banco llevan sufijo `¹` `²` ... `⁹`.
- Las palabras 10+ NO llevan sufijo (sin `¹⁰`, sin `¹¹` — `bankWithKeys` retorna `key: ''` para idx ≥ 9, D-69).

**Status:** `[pending]` `[N/A si se salta]`

**Notas:** (si lo saltas, márcalo `[N/A]` con la nota "cap unit-tested")

---

### Pitfall #6 — validator rechaza match malformado

#### Paso 10 — Banner `#error-banner` con archivo + exerciseId + reason

**Acción:**

1. Editar `content/exercises/avere.json` manualmente — corromper un ejercicio match (ej. cambiar `"pairs": [["io", "ho"], ...]` en `avere-200` a `"pairs": [["io"], ...]` — quitar el segundo string del primer pair, dejando un array de longitud 1).
2. Recargar `http://localhost:3000`.
3. Observar la home.
4. **Revertir el cambio** tras la verificación (`git checkout content/exercises/avere.json`).
5. Recargar para confirmar que el banner desaparece.

**Resultado esperado:**

- **Aparece el banner `#error-banner` en la UI** (no solo en consola).
- El banner muestra texto en español con: `file: 'avere.json'` (o equivalente), `exerciseId: 'avere-200'`, y una `reason` explicativa (ej. "payload.pairs[0] debe ser tuple de exactamente 2 strings").
- Tras revertir y recargar, el banner desaparece y la app vuelve a funcionar normalmente.

**Status:** `[pending]`

**Notas:**

---

### Pitfall #7 — read de payload tras unmount

#### Paso 11 — Avance limpio tras fallo word-buttons

**Acción:**

1. En una sesión, llegar a un word-buttons (o reiniciar Repaso hasta que aparezca uno).
2. Fallar deliberadamente (poner las palabras en orden incorrecto, pulsar Enter / Comprobar).
3. Ver el feedback rojo + frase correcta literal aparecer.
4. **ANTES de pulsar Siguiente / Enter**, observar la Console (debería estar limpia).
5. Pulsar `Enter` (o el botón "Siguiente") para avanzar al siguiente ejercicio.
6. Observar la Console **durante la transición**.

**Resultado esperado:**

- **NINGÚN `TypeError`** o error de "Cannot read properties of null/undefined" durante la transición.
- En particular, los getters null-safe (double-defense Alpine) deben proteger contra el tick de unmount donde `sessionCurrentExercise.payload` podría ser leído antes de que el sub-template se desmonte.

**Status:** `[pending]`

**Notas:**

---

### Pitfall #8 — foco residual D-72

#### Paso 12 — `document.activeElement` es `<body>` tras la sesión

**Acción:**

1. Tras completar una sesión (cualquiera), en la pantalla de summary o tras volver al home:
2. Abrir DevTools **Console** y ejecutar:
   ```js
   document.activeElement
   ```
3. Observar lo que devuelve.

**Resultado esperado:**

- Devuelve `<body>` (o un elemento sin focus visible — ej. `<html>` o un wrapper genérico).
- **NO devuelve** un `<button>` que retenga focus (eso sería un foco residual = violación D-72).
- Visualmente, **ningún botón en la pantalla tiene anillo de focus visible**.

**Status:** `[pending]`

**Notas:**

---

### D-54 exploit-proof multi-choice — no-regresión Phase 2

#### Paso 13 — Cascada multi-choice persiste tras recarga sin pulsar Siguiente

**Acción:**

1. Reset suave: `localStorage.removeItem('italianCourse.v1'); location.reload();`
2. Pulsar "Repaso de 20" → Avere → Empezar.
3. En el primer ejercicio **multi-choice**, pulsar la tecla de una opción **INCORRECTA**.
4. Ver el feedback rojo + respuesta correcta.
5. **NO pulses Siguiente / Enter**.
6. **Recargar la página** (F5 o Cmd+R) directamente.
7. Tras la recarga, mirar la home.

**Resultado esperado:**

- En la home tras la recarga, **Avere muestra badge `●` (no-hecha)** y `racha: 0 días`.
- La cascada D-54 ya persistió en `localStorage` ANTES de pulsar Siguiente (es la promesa "te obliga a no olvidar" — exploit-proof).
- **Verificación adicional en DevTools localStorage:** `italianCourse.v1` → `state.categoryProgress.avere.status === 'no-hecha'` Y `state.categoryProgress.avere.streakDays === 0`.

**Status:** `[pending]`

**Notas:**

---

### D-61 exploit-proof match — NUEVO Phase 3

#### Paso 14 — Cascada match persiste tras cerrar pestaña en primer fallo

**Acción:**

1. Reset suave: `localStorage.removeItem('italianCourse.v1'); location.reload();`
2. Pulsar "Repaso de 20" → Avere → Empezar.
3. Avanzar hasta que aparezca un ejercicio **match** (puedes resolver los anteriores rápidamente).
4. En el match: hacer **1 emparejamiento INCORRECTO** (pulsar `1` + una letra incorrecta).
5. Ver el parpadeo rojo.
6. **CERRAR LA PESTAÑA del navegador inmediatamente** (Ctrl+W / Cmd+W). NO completar las parejas restantes. NO pulsar Siguiente.
7. Abrir una pestaña nueva → `http://localhost:3000`.
8. Observar la home.

**Resultado esperado:**

- En la home tras reabrir, **Avere muestra badge `●` (no-hecha)** y `racha: 0 días`.
- La cascada D-61 ya persistió en `localStorage` EN EL PRIMER PARPADEO ROJO (antes de completar el ejercicio).
- **Verificación adicional en DevTools localStorage:** `state.categoryProgress.avere.status === 'no-hecha'`.
- **NOTA sobre el banner Repaso abandonado**: si era una sesión Repaso, los aciertos hipotéticos se descartan (los aciertos antes del primer fallo no quedan), pero la cascada del primer fallo SÍ quedó. D-54/D-61 prevalece sobre "abandono descarta".
- Si la sesión era **Test completo** en lugar de Repaso, además debería ofrecer reanudar; no es este flujo (el plan exige Repaso 20, no Test completo).

**Status:** `[pending]`

**Notas:**

---

### D-66 duplicados visuales — NUEVO Phase 3

#### Paso 15 — avere-202 acepta `lui → primer 'ha'` Y `lui → segundo 'ha'`

**Acción:**

1. Arrancar otro Repaso. Avanzar hasta que aparezca **`avere-202`** (es el match con 3 parejas: `io↔ho, lui↔ha, lei↔ha, noi↔abbiamo` — espera... el pool del plan dice 3 parejas con `ha` duplicado en derecha: `lui↔ha + lei↔ha + noi↔abbiamo`).
2. **Observar la columna derecha**: debería haber **2 items textuales `ha`** visibles (uno arriba, otro abajo, indistinguibles excepto por la letra `ᵃ` vs `ᵇ` vs `ᶜ`).
3. **Probar prueba A** (orden natural):
   - Pulsar `1` (selecciona `lui`).
   - Pulsar la letra del primer `ha` visible (por ejemplo `a` si el primer `ha` está arriba).
   - Esperado: pareja correcta, primer `ha` queda `.match-consumed` (apagado).
   - Pulsar `2` (selecciona `lei`).
   - Pulsar la letra del segundo `ha` (ahora único `ha` disponible).
   - Esperado: pareja correcta, segundo `ha` queda apagado.
   - Completar el ejercicio (la pareja restante).
4. Abandonar la sesión y volver. Reiniciar Repaso hasta que vuelva a aparecer `avere-202`.
5. **Probar prueba B** (orden inverso):
   - Pulsar `2` (lei).
   - Pulsar la letra del segundo `ha`.
   - Esperado: pareja correcta también.
   - Pulsar `1` (lui).
   - Pulsar la letra del primer `ha` (único `ha` ahora disponible).
   - Esperado: pareja correcta.

**Resultado esperado:**

- **Ambas pruebas A y B completan el ejercicio sin error.**
- El grading consume por **índice** (no por texto literal): cualquier orden de asignación funciona porque el algoritmo busca el primer `pair` no consumido que coincide textualmente.
- Visualmente NO hay distintivo entre los 2 `ha` aparte de la letra `ᵃ`/`ᵇ`/`ᶜ`.

**Status:** `[pending]`

**Notas:** (si una de las dos pruebas falla, registra exactamente qué pasos hiciste y qué viste — D-66 sería un FAIL crítico)

---

## Verificación de los 5 `must_haves.truths` del plan

Tras los 15 pasos, confirmar uno por uno los 5 hechos observables del plan (sintetizan el cierre de los 3 requisitos):

| # | Truth | Confirmado |
|---|-------|------------|
| 1 | Una sesión de 20 ejercicios mezclando los 3 tipos se completa sin tocar el ratón. | `[pending]` |
| 2 | Tras un fallo en cualquiera de los 3 tipos, recargar la página NO permite escapar la cascada (D-54 / D-61 exploit-proof). | `[pending]` |
| 3 | Las transiciones entre los 3 tipos en la misma sesión son fluidas — sin saltos de UI, sin estados residuales del ejercicio anterior. | `[pending]` |
| 4 | El comportamiento de teclado es predecible: 1-4 multi-choice; 1-9 dinámicos word-buttons; 1-9 izq + a-i der match; Enter/Space tras fallo avanza; Backspace word-buttons quita última. | `[pending]` |
| 5 | El listener `@keydown.window` se desmonta limpio al cambiar `currentScreen` y el foco queda en el body al montar/avanzar sesión (D-72) — paso 5 (Pitfall #1) + paso 12 (`document.activeElement`) cubren ambos lados. | `[pending]` |
| 6 | El summary final agrega correctamente aciertos/fallos por categoría tras una sesión mixta. | `[pending]` |

Marca cada uno como `[CONFIRMED]` o `[DISCREPANCY: ...]` con notas si hay desajuste con lo observado.

---

## Observaciones de UX no bloqueantes (Phase 5 polish candidates)

> Espacio libre para anotar cosas que NO bloquean el PASS pero podrían convertirse en mejoras de Phase 5. Ejemplos posibles:
>
> - "El parpadeo rojo de match a veces se siente muy corto / muy largo a 300ms".
> - "Preferiría ver un contador de parejas consumidas".
> - "El sufijo `¹²³ᵃᵇᶜ` se lee bien en dark mode pero quizá un poco apagado en light mode".
> - "El forced last pair se siente trivial — preferiría auto-completar".
> - "Echo de menos un atajo para abandonar Repaso (Esc?)".

(Anota aquí tus observaciones)

---

## Veredicto

> **Una vez completados los 15 pasos, escribe AQUÍ tu veredicto explícito:**

**Veredicto:** `[pending]`

Opciones válidas (escoge UNA y reemplaza `[pending]` arriba):

- **PASS** — los 4 criterios de ROADMAP confirmados + 11 verificaciones complementarias sin issues bloqueantes. Phase 3 lista para `/gsd:complete-phase 3` (que marcará EXTYPE-02, EXTYPE-03 y SESSION-06 como Complete en REQUIREMENTS.md).
- **NEEDS-PATCH: \<descripción del patch puntual\>** — un issue puntual emergió (ej. un atajo de teclado se sintió incómodo; un visual quedó raro). El orquestador invocará `/gsd:plan-phase 3 --gaps` con un plan 03-04 puntual.
- **FAIL: \<descripción del bug bloqueante\>** — un bug bloqueante emergió (ej. cascada D-61 no persiste; sub-estado residual entre ejercicios; listener huérfano). Replanificación profunda necesaria.

---

## Notas finales del autor

(Cualquier observación general, lección aprendida, idea futura — espacio libre.)

---

*Plan: 03-03*
*Generated: 2026-05-23 by sequential executor of plan 03-03 (UAT checkpoint)*
*Pre-UAT readiness: 105/105 tests verdes, commit chain intact (cb17a97 → 6a10bed), 2 call-sites de applyImmediateFailure verificados*
