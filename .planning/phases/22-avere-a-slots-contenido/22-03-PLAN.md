---
phase: 22-avere-a-slots-contenido
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [AVE-01]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Avere (expected: 23 en exercise-types.test.js:1268, slot-variants-integration.test.js:167 y run-validation-271.mjs:95) estan sincronizados contra el conteo REAL final de slots leido del JSON (no una estimacion)"
    - "TOTAL_EXPECTED en run-validation-271.mjs esta recalculado = 323 - 23 + n_slots_final_avere"
    - "El smoke parametrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto/Articoli/Partitivi; Avere convertido a slots pasa coverage/R1/R2/smart-quotes/markdown sin tocar la logica del test, solo el count"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Avere en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots"
      contains: "avere.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de avere sincronizado al n de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected avere + TOTAL_EXPECTED recalculado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de avere.json"
      via: "sincronizacion contra el conteo REAL del JSON, no estimacion"
      pattern: "avere"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Avere (`expected: 23`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `avere.json` leido del JSON tras 22-01/22-02 (NO una estimacion), cerrando AVE-01 (cierre del count). El smoke parametrico YA es shape-agnostic (la bifurcacion `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17, se reuso en Articoli Phase 19 y Partitivi Phase 20, y esta en el codebase — verificado en exercise-types.test.js getExplanation/getPrompts), asi que Avere convertido a slots pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la logica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]). El blindaje APPEND-ONLY D-88 ya quedo consistente (re-basado) en 22-01/22-02 — este plan NO lo toca.

Purpose: convertir Avere rompe los 3 counts hoy verdes (23 replicado en 3 archivos + TOTAL_EXPECTED=323 que incluye articoli=34 y partitivos=19). AVE-01 (cierre) ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase (primera de las 3 conversiones de verbos de v1.6).

Output: los 3 test/script files con el count de Avere + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validacion PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/avere.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-05). El smoke YA bifurca por shape (no requiere extension de logica, solo count). articoli ya esta en expected:34 y partitivos en 19 — NO tocarlos. -->

1. tests/exercise-types.test.js:
   - linea 1268: `{ file: 'content/exercises/avere.json', expected: 23 }` en CATEGORIES_WITH_EXPLANATIONS <- cambiar 23 al n real de slots
   - lineas 1273/1274: articoli expected:34 / partitivos expected:19 — YA sincronizados (Phase 19/20), NO tocar
   - getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation — YA bifurcado por shape (heredado del piloto/Articoli/Partitivi, NO tocar)
   - getPrompts YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar)
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido -> se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:167: `{ slug: 'avere', expected: 23 }` en REAL_CATEGORIES <- cambiar 23 al n real de slots (articoli linea 174 en 34, partitivos linea 175 en 19, NO tocar)

3. scripts/run-validation-271.mjs:
   - linea 95: `{ slug:'avere', file:'...', expected: 23 }` <- cambiar 23 al n real
   - linea 104: `const TOTAL_EXPECTED = 323;` <- recalcular = 323 - 23 + n_slots_final_avere
   - lineas 64-95: comentario explicativo del historial del total <- anadir la conversion de Avere a slots (precedente: el comentario narra cada cambio que movio el total; la conversion de Partitivi en 20-03 esta documentada ahi como modelo a espejar: "TOTAL = 348 - 44 + 19 = 323"; anadir una linea analoga "TOTAL = 323 - 23 + <count> = <nuevo>")

El count `expected` de avere = n de SLOTS final (data.exercises.length tras 22-01+22-02) — sincronizar contra el JSON REAL, NO contra una estimacion. La fusion por regla REDUCE el count (23 -> ~19 tras reagrupar: 6 presente + 1 sensaciones + 1 passato + 2 wb + 3 match + 6 cruces); los slots/variantes nuevos de 22-02 lo suben un poco (las variantes NO suben el count de slots; solo los slots NUEVOS lo suben). Net = leer del JSON.
Comando: `node -e "console.log(require('./content/exercises/avere.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
Leccion 19-03/20-03: la estimacion fue ~25 (Articoli) y el real fue 34; leer SIEMPRE del JSON, nunca estimar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Avere en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (las lineas alrededor de 1265-1310 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + el assert de conteo; y el bloque VAL_07_STRICT alrededor de 1410-1430)
    - content/exercises/avere.json (estado final tras 22-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/milestones/v1.5-phases/20-partitivi-a-slots-contenido/20-03-PLAN.md (Task 1 analogo de Partitivi — el mismo patron de sync de count contra el JSON real) y .planning/milestones/v1.5-phases/19-articoli-a-slots-contenido/19-03-PLAN.md (Articoli, segundo analogo)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de avere.json (`data.exercises.length` tras 22-02, via `node -e "console.log(require('./content/exercises/avere.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry avere en CATEGORIES_WITH_EXPLANATIONS (linea 1268) a ese numero. NO tocar las entries de articoli (linea 1273, en 34) ni partitivos (linea 1274, en 19). NO tocar la logica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto/Articoli/Partitivi) -> Avere slot pasa coverage/R1/R2/smart-quotes/markdown automaticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level -> no cambia salvo heredar el count sincronizado (todo slot/variante nuevo de 22-02 ya esta validated por el quorum con validation top-level D-19-09 -> la gate pasa; los slots de cruces 300..305 y los slots movidos conservan su validation fuente). NO tocar los counts de las otras 8 categorias.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de avere en CATEGORIES_WITH_EXPLANATIONS == n REAL de slots: `node -e "const d=require('./content/exercises/avere.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en linea 1268
    - Las otras 8 categorias siguen verdes con sus counts originales (articoli 34, partitivos 19, preposiciones 49, genero-numero 40, sustantivos-irregulares 31, verbos-movimiento 37, essere 39, profesiones 51) — no se tocaron
    - La logica del test (getExplanation/getPrompts/bifurcacion) NO se modifico: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Avere en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-avere intactas (articoli sigue en 34, partitivos en 19); logica del smoke (ya shape-agnostic) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED)</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (las lineas alrededor de 166-202 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (las lineas alrededor de 63-110 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/avere.json (conteo REAL final de slots)
    - .planning/milestones/v1.5-phases/20-partitivi-a-slots-contenido/20-03-PLAN.md (Task 2 analogo de Partitivi — mismo patron de 3 hardcodes + TOTAL_EXPECTED + comentario del historial) y 19-03-PLAN.md (Articoli)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'avere', expected: 23 }` (linea 167) a `expected` = n REAL de slots. NO tocar las entries de articoli (linea 174, en 34) ni partitivos (linea 175, en 19). El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 23` de la entry avere (linea 95) a n REAL de slots; recalcular `TOTAL_EXPECTED` (linea 104) = `323 - 23 + n_slots_final_avere`; actualizar el comentario explicativo del historial del total (lineas 64-95) registrando la conversion de Avere a slots (precedente: el comentario narra cada cambio historico que movio el total — el de Partitivi de 20-03 esta ahi como modelo a espejar; anadir una linea analoga: "-> <nuevo> (v1.6 Phase 22, AVE-01): Avere se convirtio al shape slot+variantes. ... TOTAL = 323 - 23 + <count>"). Leer el conteo real del JSON, NO usar una estimacion. Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO numero.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de avere en ambos archivos == n REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/avere.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 323 - 23 + n_slots_final_avere (verificable aritmeticamente contra el conteo real; articoli sigue contribuyendo 34 y partitivos 19)
    - El reporter de run-validation-271 no emite warning `total != esperado` para avere
    - El comentario del historial del total en run-validation-271.mjs menciona la conversion de Avere a slots
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Avere (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el n real de slots; TOTAL_EXPECTED recalculado (articoli=34 y partitivos=19 intactos); reporter PASS sin warnings de count; comentario del historial actualizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido -> smoke parametrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Tampering | count desincronizado entre los 3 hardcodes -> gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leido del JSON (`data.exercises.length`), no estimaciones (leccion 19-03/20-03: estimo ~25, real 34). El acceptance asserta que coinciden y que run-validation-271 PASS. |
| T-22-03 | Tampering | explanation/prompt de slot con HTML/markdown/smart-quotes pasa sin escanear | mitigate | El smoke YA bifurca por shape (heredado del piloto/Articoli/Partitivi): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots — la cobertura de seguridad de contenido NO se pierde al convertir Avere. |
| T-22-08 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 22-02 paso el quorum (status validated) y todo slot nuevo lleva validation top-level (D-19-09) antes de integrarse -> la gate pasa solo si 22-02 cumplio. Si algo quedo disputed o sin validation top-level, la gate FALLA visible. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v9 de Phase 21, integration, exercise-types, articoli/partitivos de Phase 19/20)
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exits 0
- `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (blindaje D-88 sigue consistente — re-basado en 22-01/22-02, este plan no lo toca)
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de avere.json
</verification>

<success_criteria>
- Los 3 hardcodes de count de Avere + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacia — verificado por el smoke shape-agnostic)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); logica del smoke NO tocada (ya shape-agnostic); articoli=34 y partitivos=19 intactos
</success_criteria>

<output>
Create `.planning/phases/22-avere-a-slots-contenido/22-03-SUMMARY.md` when done
</output>
