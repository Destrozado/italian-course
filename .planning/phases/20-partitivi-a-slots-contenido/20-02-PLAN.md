---
phase: 20-partitivi-a-slots-contenido
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - content/exercises/partitivos.json
  - .planning/phases/20-partitivi-a-slots-contenido/20-VARIANTES-NUEVAS.md
autonomous: false
requirements: [PART-02]
must_haves:
  truths:
    - "Cada variante NUEVA y cada slot NUEVO autorado pasa el quorum cross-vendor R1-R7 (Gemini + DeepSeek via validate-ai-pass + Claude Opus+Sonnet via skill gsd-validate-exercise) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot"
    - "Las celdas pobres de del-formas (dello+s-impura, degli+s-impura, degli+vocal, degli+z) quedan engordadas a >=2 variantes donde la regla admite reformulacion natural (D-19-05); las celdas con sustantivo unico no se inflan artificialmente"
    - "Los huecos de del-formas detectados (dello+gn/ps/x, degli+gn/ps) se anaden como slots nuevos SOLO donde existe un sustantivo italiano A1 natural que dispara la serie, con la contraccion di+articolo verificada (R6, D-19-06)"
    - "Las variantes nuevas se autoran SOLO donde la regla admite reformulacion natural; ninguna lleva explanation propia (comparten la del slot); cada slot nuevo lleva validation.status top-level (D-19-09)"
  artifacts:
    - path: "content/exercises/partitivos.json"
      provides: "slots nuevos de huecos de del-formas + variantes nuevas validated integradas en variants[]"
      contains: "\"variants\""
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quorum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS donde la regla admite reformulacion natural (PART-02, D-19-05 arrastrado) priorizando engordar las celdas pobres de del-formas que quedaron con 1 variante tras el split por sub-disparador en 20-01 (dello+s-impura, degli+s-impura, degli+vocal, degli+z) a >=2 variantes, y anadir los slots NUEVOS para los huecos de disparador de la serie de suoni speciali en el partitivo masc (D-19-06 arrastrado: dello+gn / dello+ps / dello+x, degli+gn / degli+ps) SOLO donde existe un sustantivo italiano A1 natural. Cada superficie NUEVA pasa el quorum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched, con revision del autor por variante (D-85). Las superficies movidas intactas en 20-01 NO se re-validan.

Purpose: las variantes nuevas materializan el dolor anti-memorizacion (re-hacer un slot puede tocar una superficie distinta). Los huecos de del-formas cierran la serie de suoni speciali del partitivo masc (completitud de la regla por encima de acotar el alcance, segun los Specific Ideas del autor — la misma linea purista de Articoli). El quorum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazo 8 bugs en Articoli que el human-verify aprobo). R6 es critico aqui: las contracciones di+articolo de los suoni speciali (dello gnomo? dello psicologo? degli psicologi?) son terreno de error facil — verificar el sustantivo italiano, su genero/numero y la contraccion correcta antes de meterlos. Pitfalls de Partitivi a vigilar: trampa de genero de `latte`, calco del plural espanol en `qualche` (siempre singular), errores de contraccion di+articolo.

Output: `partitivos.json` con los slots nuevos de huecos de del-formas + las variantes nuevas validated integradas; cada superficie nueva con su quorum verde en `validation.passes[]`; cada slot nuevo con validation.status top-level (D-19-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-partitivi-a-slots-contenido/20-CONTEXT.md
@.planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/partitivos.json

<interfaces>
<!-- Pipeline de quorum cross-vendor. Verificado en validate-ai-pass.mjs + skill (PATTERNS §Cross-vendor quorum pipeline). NO modificar estos scripts/skill. Mismo flujo ejecutado con exito en Phase 19 (8 superficies, 0 excluidas). -->
Quorum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Mitad 1 — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondio; >=2 by distintos para el quorum; status re-derivado por deriveStatus, NO hand-editar status)

Mitad 2 — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus->Sonnet):
  /gsd-validate-exercise <id>
  (claude-opus-4-7 + claude-sonnet-4-6 literales; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio)
  Fallback cuando Task no esta disponible al executor (precedente Phase 19, D-19-08): la mitad Claude se corre con `claude -p --model claude-opus-4-7` y `--model claude-sonnet-4-6` headless, 1 proceso por modelo por superficie, con el 09-VALIDATION-PROMPT verbatim y el ejercicio SIN su campo validation (no sesgar). Equivalente funcional 1-por-1 NUNCA batched.

Gate D-17-07: el passes[] final de cada superficie nueva = >=4x "correcta" (gemini + deepseek + opus + sonnet, by distintos), CERO "incorrecta". deriveStatus -> "validated"; cualquier "incorrecta" -> "disputed" (sticky, bloquea, NO entra al slot).

Pitfall (PATTERNS): el VALIDATION-PROMPT es legacy-payload-centrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/partitivos.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quorum, mover la superficie al variants[] del slot (sin re-validacion) y eliminar el id temporal legacy.

Alcance D-19-05 (priorizar celdas pobres, sin cuota rigida):
- Celdas pobres a engordar a >=2 variantes (reformular el MISMO sub-disparador con otro sustantivo del mismo disparador): dello+s-impura (otro sust. s-impura masc incontable), degli+s-impura (otro sust. masc pl s-impura), degli+vocal (otro sust. masc pl con vocal), degli+z (otro sust. masc pl con z-). Verificar que el sustantivo italiano existe y dispara realmente esa serie.
- NO inflar slots cuya regla no admite reformulacion natural (sustantivo idiomatico unico). Pragmatico como el piloto/Articoli.

Slots NUEVOS de huecos de del-formas (D-19-06, R6 critico):
- `dello` + gn / ps / x (sing masc incontable): dello gnocco? dello psicologo? dello xilofono? -> slot(s) nuevo(s) SOLO si existe sustantivo A1 natural. VERIFICAR contraccion di+lo = dello (R6).
- `degli` + gn / ps (pl masc contable): degli gnocchi? degli psicologi? -> slot(s) nuevo(s) SOLO si natural. VERIFICAR di+gli = degli (R6).
  categoryIds=["partitivos"]; explanation de slot nueva (la serie del/dello + suono speciale dispara di+lo->dello, di+gli->degli); rule-first (D-19-07).
- El planner/autor decide en plan-time/checkpoint que huecos son naturales. Si un hueco no tiene sustantivo A1 natural, NO se materializa (alcance conservador).

Validation top-level para la gate (D-19-09): cada slot NUEVO debe portar el bloque `validation` top-level ({status:"validated", passes[]}) ademas del por-variante implicito — el gate VAL_07_STRICT lee `ex.validation?.status` a nivel de slot. Reflejar el quorum cross-vendor de sus variantes en el bloque top-level (NO inventar validacion; elevar la del nivel variante al nivel slot). Esta fue la correccion Rule-2 de 19-03.

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quorum, un scan ASCII/tildes (RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial.
Gloss ES (R7 canon): mantener "(en espanol: ...)" donde desambigue doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de politica (base de aprobacion = Claude Opus+Sonnet).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (celdas pobres de del-formas) + slots de huecos de suoni speciali</name>
  <files>.planning/phases/20-partitivi-a-slots-contenido/20-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/partitivos.json (estado tras 20-01 — los slots ya reagrupados, para saber que slots existen, cuales son celdas pobres y que admite variante nueva)
    - .planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md (las celdas pobres marcadas como candidatas + los huecos de del-formas anotados en 20-01)
    - .planning/phases/20-partitivi-a-slots-contenido/20-CONTEXT.md §decisions (D-19-05 alcance/celdas pobres arrastrado, D-19-06 huecos arrastrado, D-17-07 quorum, R7 gloss, pitfalls de Partitivi)
    - .planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md §Cross-vendor quorum (R6 critico para contracciones di+articolo, invocacion, scan de acentos, pitfalls Partitivi)
    - .planning/phases/19-articoli-a-slots-contenido/19-02-PLAN.md (Task 1 analogo de Articoli) y .planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md (el artefacto analogo ya producido — formato a espejar)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6 si accesible) o la seccion R1-R7 del 09-VALIDATION-PROMPT
  </read_first>
  <action>
    Proponer un set CONSERVADOR de superficies nuevas (D-19-05). (1) Para cada celda pobre marcada en 20-01 (dello+s-impura, degli+s-impura, degli+vocal, degli+z): redactar 1 variante nueva (superficie `{prompt con ___, options 3-4, correctIndex}`) que reformule el MISMO sub-disparador con otro sustantivo italiano que realmente dispare esa serie y misma contabilidad/numero (dello/degli = incontable sing / contable pl masc). (2) Redactar los slots NUEVOS de huecos de suoni speciali (D-19-06): dello+gn/ps/x y degli+gn/ps SOLO donde exista un sustantivo A1 italiano natural que dispare la serie — con su explanation de slot nueva (rule-first: el suono speciale dispara di+lo->dello / di+gli->degli). VERIFICAR explicitamente (R6) que la contraccion di+articolo es correcta, el sustantivo italiano existe y su genero/numero es el correcto para cada hueco. NO materializar un hueco sin sustantivo A1 natural (alcance conservador). Cumplir R1-R7 (sin leak de la respuesta en el prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigue doble-validez). Pasar un scan de acentos/ASCII (tildes RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. Producir `.planning/phases/20-partitivi-a-slots-contenido/20-VARIANTES-NUEVAS.md`: lista de cada superficie nueva con su id temporal (para el quorum aislado), slot destino (existente o nuevo), prompt/options/correctIndex/explanation, y la justificacion de por que la regla admite reformulacion / por que la contraccion italiana es correcta (R6). Reportar el conteo de superficies nuevas propuestas (driver del coste de quorum y del count final de 20-03).
  </action>
  <verify>
    <automated>test -f .planning/phases/20-partitivi-a-slots-contenido/20-VARIANTES-NUEVAS.md && grep -ci "dello\|degli\|s-impura\|gn\|ps" .planning/phases/20-partitivi-a-slots-contenido/20-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 20-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + superficie + explanation
    - Las celdas pobres de del-formas (dello+s-impura, degli+s-impura, degli+vocal, degli+z) tienen al menos una variante nueva propuesta cada una (engorde a >=2, D-19-05), con misma contabilidad/numero que el slot
    - Los slots de huecos de suoni speciali (dello+gn/ps/x, degli+gn/ps) estan redactados SOLO donde existe sustantivo A1 natural, con verificacion R6 explicita de la contraccion di+articolo + genero/numero del sustantivo italiano
    - Ninguna superficie nueva se propone en una regla que no admite reformulacion natural (alcance conservador D-19-05); ningun hueco se materializa sin sustantivo A1 natural
    - Cada explanation/prompt nueva pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers
    - El conteo de superficies nuevas propuestas esta reportado
  </acceptance_criteria>
  <done>
    20-VARIANTES-NUEVAS.md lista el set conservador de superficies nuevas (celdas pobres de del-formas engordadas + slots de huecos de suoni speciali con R6 verificada) con superficies R1-R7-conformes y scan de acentos pasado; conteo reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (20-VARIANTES-NUEVAS.md) — variantes para engordar celdas pobres de del-formas + los slots nuevos de huecos de suoni speciali (dello/degli + gn/ps/x), cumpliendo R1-R7 y con scan de acentos. AUN NO validadas por quorum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/20-partitivi-a-slots-contenido/20-VARIANTES-NUEVAS.md`.
    2. Revisa cada variante nueva de celda pobre: el sustantivo dispara DE VERDAD ese sub-sonido (s-impura/z/vocal) y es de la contabilidad/numero correcta (dello=incontable masc sing; degli=contable masc pl)? La respuesta correcta es inequivoca (sin doble-validez R7)?
    3. Revisa los slots de huecos: `dello gnocco`/`dello psicologo`/`dello xilofono`, `degli gnocchi`/`degli psicologi` son la contraccion correcta (di+lo->dello, di+gli->degli)? El sustantivo existe en italiano A1 y dispara la serie? (R6 — terreno de error facil; las contracciones de suoni speciali confunden).
    4. Confirma el alcance: es conservador (no infla con variantes artificiales en reglas que no se reformulan; no materializa huecos sin sustantivo natural)?
    5. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quorum.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las superficies aprobadas al quorum cross-vendor, o lista las que descartar/reformular.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quorum cross-vendor (1-por-1) e integrarla al slot</name>
  <files>content/exercises/partitivos.json</files>
  <read_first>
    - .planning/phases/20-partitivi-a-slots-contenido/20-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales claude-opus-4-7/claude-sonnet-4-6, 1 commit por ejercicio; fallback `claude -p` headless si no hay Task, D-19-08)
    - .planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md §Cross-vendor quorum pipeline (invocacion exacta de validate-ai-pass.mjs + validar como legacy aislado + gate D-17-07 + R6/acentos/R7 gloss + validation top-level D-19-09)
    - content/exercises/partitivos.json (slots destino donde integrar las superficies tras pasar el quorum)
    - .planning/phases/19-articoli-a-slots-contenido/19-02-PLAN.md (Task 2 analogo de Articoli) y .planning/phases/19-articoli-a-slots-contenido/19-02-SUMMARY.md (el flujo real ejecutado: 8 superficies, disputed resuelto, validation top-level)
  </read_first>
  <action>
    Para CADA superficie nueva aprobada, ejecutar el ciclo completo de quorum 1-por-1 (NUNCA batched): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/partitivos.json como entry temporal o tests/fixtures/*.json — el VALIDATION-PROMPT es legacy-payload-centrico, asi ve el shape que espera). (2) Mitad 1 externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write`. (3) Mitad 2 Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus->Sonnet secuencial, 1 spawn); fallback `claude -p --model claude-opus-4-7` / `--model claude-sonnet-4-6` headless con el ejercicio SIN su campo validation si no hay Task (D-19-08). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4x "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` -> la superficie queda disputed y se resuelve (calidad > tokens; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de politica — NO override-atajo; si un externo alucina un acento sobre texto correcto, re-run sobre el MISMO texto, precedente Phase 19 `lo iodio`) o se descarta — NO entra al slot disputed. (5) Tras pasar el quorum, mover la superficie validada al variants[] del slot destino (existente, para celdas pobres) o crear el slot nuevo `{id, type:"multiple-choice", categoryIds:["partitivos"], explanation, variants:[...], validation}` (para huecos de suoni speciali); eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Para cada slot NUEVO, anadir el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum cross-vendor aprobado de sus variantes (D-19-09 — el gate VAL_07_STRICT lo lee a nivel slot; NO inventar validacion, elevar la aprobada). Hacer 1 commit por superficie validada. NUNCA validar mas de una superficie en un mismo contexto/spawn.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` exits 0
    - Las celdas pobres engordadas tienen >=2 variantes (verificable contando variants[] de los slots dello+s-impura/degli+s-impura/degli+vocal/degli+z en el JSON)
    - Los slots de huecos creados (si los hay) tienen variants[] no vacio y explanation top-level + validation.status=="validated" top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); console.log('slots variants sin validation top-level:', d.exercises.filter(e=>Array.isArray(e.variants)&&e.validation?.status!=='validated').length)"` imprime `0`
    - Cada superficie NUEVA paso el quorum con >=4x "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se valido como legacy)
    - Ningun id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 20-01 NO se re-validaron (# invocaciones de quorum == # superficies nuevas)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/partitivos.json` da 0 matches
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas pasaron el quorum cross-vendor (>=4x correcta) e integradas a sus slots; slots de huecos de suoni speciali creados con R6 verificada + validation top-level (D-19-09); celdas pobres de del-formas engordadas a >=2 variantes; validateContent verde; sin ids temporales residuales; 1 commit por superficie.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio -> evaluador del quorum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podria intentar dirigir al evaluador |
| JSON de contenido editado a mano -> loader/validator | superficies nuevas redactadas a mano cruzan a validateContent y al render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | superficie nueva con respuesta ambigua (doble-validez R7), contraccion di+articolo incorrecta (R6, huecos de suoni speciali) o acento incorrecto entra al slot | mitigate | Quorum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); R6 verificada en Task 1 + checkpoint para los huecos de del-formas; pre-scan de acentos; gate de >=4x "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-20-06 | Tampering | prompt-injection en el quorum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instruccion; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-20-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown); el smoke de 20-03 re-asserta sobre variants[].prompt + explanation. |
| T-20-08 | Tampering | slot nuevo sin validation.status top-level cuela como `status: absent` en VAL_07_STRICT (la regresion de 19-03 Rule-2) | mitigate | Cada slot NUEVO porta el bloque `validation` top-level (status validated + passes[]) reflejando el quorum aprobado de sus variantes (D-19-09); acceptance asserta `slots variants sin validation top-level == 0`. NO se inventa validacion: se eleva la del nivel variante. |
| T-20-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` exits 0
- Cada superficie nueva: passes[] con >=4x "correcta", cero "incorrecta" (registrado en SUMMARY)
- Celdas pobres de del-formas engordadas a >=2 variantes; slots de huecos (si los hay) con validation top-level (D-19-09)
- `node --test tests/*.test.js` — el count de Partitivi sigue rojo a proposito hasta 20-03 (el conteo final ya es determinable tras este plan)
</verification>

<success_criteria>
- Variantes nuevas autoradas solo donde la regla admite reformulacion (D-19-05, conservador, priorizando celdas pobres de del-formas)
- Slots nuevos de huecos de suoni speciali creados SOLO donde existe sustantivo A1 natural con R6 verificada (D-19-06) + validation top-level (D-19-09)
- Cada superficie nueva paso el quorum cross-vendor R1-R7 (>=4x correcta) antes de entrar
- Superficies existentes movidas NO re-validadas
- validateContent verde; conteo final de slots determinable para 20-03
</success_criteria>

<output>
Create `.planning/phases/20-partitivi-a-slots-contenido/20-02-SUMMARY.md` when done
</output>
