---
phase: 17-piloto-preposiciones-contenido
plan: 03
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - content/exercises/preposiciones.json
autonomous: false
requirements: [PILOT-02, PILOT-03]
must_haves:
  truths:
    - "Cada variante NUEVA autorada pasa el quórum cross-vendor R1-R7 (Gemini + DeepSeek vía validate-ai-pass + Claude Opus+Sonnet vía skill) — todas 'correcta', cero 'incorrecta', antes de entrar al slot"
    - "Existe el slot locativo fijo `in spiaggia / in montagna / in campagna` (in-locativo, 3 variantes) que antes no estaba en ninguna categoría"
    - "Existe el slot `al mare` (slot-de-1, forma `al` distinta)"
    - "Las variantes nuevas se autoran solo donde la regla admite reformulación natural (D-17-06); las excepciones idiomáticas únicas quedan slot-de-1 sin variantes artificiales"
  artifacts:
    - path: "content/exercises/preposiciones.json"
      provides: "slots locativos nuevos + variantes nuevas validated integradas en variants[]"
      contains: "spiaggia"
  key_links:
    - from: "cada variante nueva"
      to: "validation.passes[]"
      via: "quórum cross-vendor (4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS donde la regla admite reformulación natural (PILOT-02, D-17-06) y añadir los dos slots locativos nuevos (PILOT-03, D-17-04): slot `in`-locativo-fijo (`in spiaggia`, `in montagna`, `in campagna` = 3 variantes) y slot `al mare` (slot-de-1). Cada variante con superficie NUEVA pasa el quórum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched, con revisión del autor por variante (D-85). Las superficies movidas intactas en 17-02 NO se re-validan.

Purpose: las variantes nuevas son lo que materializa el dolor anti-memorización (re-hacer un slot puede tocar una superficie distinta). El quórum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente) que los human-verify no pillan. El alcance es conservador (D-17-06) para no inflar el coste de tokens.

Output: `preposiciones.json` con los 2 slots locativos nuevos + las variantes nuevas validated integradas; cada variante nueva con su quórum verde en `validation.passes[]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-piloto-preposiciones-contenido/17-CONTEXT.md
@.planning/phases/17-piloto-preposiciones-contenido/17-RESEARCH.md
@.planning/phases/17-piloto-preposiciones-contenido/17-PATTERNS.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/preposiciones.json

<interfaces>
<!-- Pipeline de quórum cross-vendor. Verificado en validate-ai-pass.mjs + skill. NO modificar estos scripts/skill. -->
Quórum por cada variante NUEVA = 2 mitades, AMBAS deben dar "correcta":

Mitad 1 — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place; el `by` registrado es el modelo que respondió; ≥2 by distintos para el quórum)

Mitad 2 — Claude (skill gsd-validate-exercise, 1 spawn por variante, secuencial Opus→Sonnet):
  /gsd-validate-exercise <id>
  (claude-opus-4-7 + claude-sonnet-4-6 literales; 1-por-1 fresh context NUNCA batched)

Gate D-17-07: el passes[] final de cada variante nueva = 4× "correcta" (gemini + deepseek + opus + sonnet, by distintos), CERO "incorrecta". deriveStatus → "validated"; cualquier "incorrecta" → "disputed" (sticky, bloquea).

Pitfall 2 / Assumption A2 (RESEARCH §Cross-Vendor Quórum): el VALIDATION-PROMPT es legacy-payload-céntrico. Validar cada variante nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}`) ANTES de moverla al slot — así el prompt C1-C5 ve el shape que espera. Tras pasar el quórum, mover la superficie al `variants[]` del slot (movimiento sin re-validación). El validate-ai-pass.mjs escanea content/exercises/*.json + tests/fixtures/*.json buscando el id exacto.

Alcance D-17-06 (RESEARCH §Reagrupación Mapping — columna "¿Admite variante nueva?"): los slots cuya regla admite reformulación (di=posesión, mezzo di trasporto, articolata con otro sustantivo del mismo artículo, etc.) son candidatos; las excepciones idiomáticas únicas (`vado a casa` S-A-casa, `al mare`) quedan slot-de-1. Target conservador: NO inflar.

Slots locativos nuevos (PILOT-03, D-17-04):
- S-LOC-IN (`in` locativo fijo sin artículo): in spiaggia, in montagna, in campagna (3 variantes nuevas — gap confirmado, todas pasan quórum)
- S-AL-MARE (`al`=a+il locativo fijo): al mare (slot-de-1, 1 variante nueva)

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quórum, un scan ASCII/tildes sobre las explanations/prompts nuevos evita gastar un pase en un fallo de tilde trivial. Apóstrofes ASCII U+0027.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer el set de variantes nuevas + redactar superficies y slots locativos</name>
  <files>.planning/phases/17-piloto-preposiciones-contenido/17-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/preposiciones.json (estado tras 17-02 — los slots ya reagrupados, para saber qué slots existen y cuáles admiten variante nueva)
    - .planning/phases/17-piloto-preposiciones-contenido/17-RESEARCH.md §Reagrupación Mapping (columna "¿Admite variante nueva?" por slot + Bloque C slots locativos + Open Q #2 cuántas variantes por slot)
    - .planning/phases/17-piloto-preposiciones-contenido/17-CONTEXT.md §decisions (D-17-04 locativos, D-17-06 alcance, D-17-07 quórum)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R7 si accesible) o la sección R1-R7 del 09-VALIDATION-PROMPT
  </read_first>
  <action>
    Proponer un set CONSERVADOR de variantes nuevas (D-17-06): para los slots cuya regla admite reformulación natural, redactar 1 variante nueva (superficie `{prompt con ___, options 3-4, correctIndex}`) que reformule la MISMA regla con otro ejemplo (di=posesión otro poseedor, mezzo di trasporto otro vehículo, articolata otro sustantivo del mismo artículo). NO forzar variantes en excepciones idiomáticas únicas (S-A-casa, etc.). Redactar las 4 superficies de los slots locativos nuevos: in spiaggia / in montagna / in campagna (S-LOC-IN) y al mare (S-AL-MARE), con sus explanations de slot. Cumplir R1-R7 (sin leak de la respuesta en el prompt, sin refs #NNN en explanations, options con 3+ valores distintos, verificar artículo/noun italiano). Pasar un scan de acentos/ASCII (tildes RAE correctas, apóstrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. Producir `.planning/phases/17-piloto-preposiciones-contenido/17-VARIANTES-NUEVAS.md`: lista de cada variante nueva con su id temporal (para el quórum aislado), el slot destino, prompt/options/correctIndex/explanation, y la justificación de por qué la regla admite reformulación. Reportar el conteo de variantes nuevas propuestas.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-piloto-preposiciones-contenido/17-VARIANTES-NUEVAS.md && grep -ci "spiaggia\|montagna\|campagna\|mare" .planning/phases/17-piloto-preposiciones-contenido/17-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 17-VARIANTES-NUEVAS.md existe y lista cada variante nueva con id temporal + slot destino + superficie + explanation
    - Las 4 superficies locativas (in spiaggia, in montagna, in campagna, al mare) están redactadas
    - Ninguna variante nueva se propone en una excepción idiomática única (S-A-casa) — alcance conservador D-17-06
    - Cada explanation/prompt nueva pasa un scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers
    - El conteo de variantes nuevas propuestas está reportado (driver del coste de quórum y del count final de 17-04)
  </acceptance_criteria>
  <done>
    17-VARIANTES-NUEVAS.md lista el set conservador de variantes nuevas (incluidos los 4 locativos) con superficies R1-R7-conformes y scan de acentos pasado; conteo reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de variantes nuevas (17-VARIANTES-NUEVAS.md) — superficies redactadas para los slots que admiten reformulación + los 4 locativos, cumpliendo R1-R7 y con scan de acentos. AÚN NO validadas por quórum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/17-piloto-preposiciones-contenido/17-VARIANTES-NUEVAS.md`.
    2. Revisa cada variante nueva: ¿la regla del slot admite de verdad esta reformulación? ¿el ejemplo es natural (no artificial)? ¿la respuesta correcta es inequívoca (sin doble-validez R7)?
    3. Confirma el alcance: ¿es conservador (no infla con variantes artificiales en excepciones idiomáticas)?
    4. Revisa las 4 superficies locativas y sus explanations.
    5. Marca cualquier variante a descartar/reformular antes de gastar tokens de quórum.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las variantes aprobadas al quórum cross-vendor, o lista las variantes a descartar/reformular.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada variante nueva por quórum cross-vendor (1-por-1) e integrarla al slot</name>
  <files>content/exercises/preposiciones.json</files>
  <read_first>
    - .planning/phases/17-piloto-preposiciones-contenido/17-VARIANTES-NUEVAS.md (las variantes aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales, retry budget 1)
    - .planning/phases/17-piloto-preposiciones-contenido/17-RESEARCH.md §Cross-Vendor Quórum Pipeline (invocación exacta + Pitfall 2 validar como legacy aislado + gate D-17-07)
    - content/exercises/preposiciones.json (slots destino donde integrar las superficies tras pasar el quórum)
  </read_first>
  <action>
    Para CADA variante nueva aprobada, ejecutar el ciclo completo de quórum 1-por-1 (NUNCA batched): (1) materializar la variante como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/preposiciones.json como entry temporal o tests/fixtures/*.json — Pitfall 2/A2: el VALIDATION-PROMPT es legacy-payload-céntrico, así el prompt ve el shape que espera). (2) Mitad 1 externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write`. (3) Mitad 2 Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus→Sonnet secuencial, 1 spawn). (4) Verificar el gate D-17-07: el `passes[]` resultante tiene 4× "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` → la variante queda disputed y se resuelve (calidad > tokens; incluir gloss español para desambiguar doble-validez R7) o se descarta — NO entra al slot disputed. (5) Tras pasar el quórum, mover la superficie validada al `variants[]` del slot destino en preposiciones.json (sin re-validación — cambio de contenedor) y para los slots locativos nuevos crear el slot `{id, type, categoryIds, explanation, variants:[...]}`. Eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation propia ni validation propia (el slot la lleva). Hacer 1 commit por variante validada (granularidad del skill). NUNCA validar más de una variante en un mismo contexto/spawn.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exits 0
    - El slot in-locativo existe con 3 variantes (in spiaggia / in montagna / in campagna): verificable por grep `spiaggia`, `montagna`, `campagna` en preposiciones.json
    - El slot al mare existe (grep `mare` + es slot-de-1)
    - Cada variante NUEVA pasó el quórum con 4× "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se validó como legacy)
    - Ningún id temporal legacy queda en preposiciones.json: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `0`
    - Las superficies existentes movidas en 17-02 NO se re-validaron (solo las nuevas pasaron quórum — # invocaciones de quórum == # variantes nuevas)
  </acceptance_criteria>
  <done>
    Todas las variantes nuevas aprobadas pasaron el quórum cross-vendor (4× correcta) e integradas a sus slots; slots locativos S-LOC-IN (3 variantes) + al-mare (1) creados; validateContent verde; sin ids temporales residuales; 1 commit por variante.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio → evaluador del quórum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt malicioso podría intentar dirigir al evaluador |
| JSON de contenido editado a mano → loader/validator | superficies nuevas redactadas a mano cruzan a validateContent y al render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | prompt-injection en el quórum | mitigate | El VALIDATION-PROMPT §6 trata el contenido del ejercicio como DATA, no instrucción. El contenido es autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-17-08 | Tampering | variante nueva con respuesta ambigua (doble-validez R7) o acento incorrecto entra al slot | mitigate | Quórum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); pre-scan de acentos; gate de 4× "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched (cierra el bug class de los 4 bugs post-v1.0). |
| T-17-04 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes | mitigate | Scan de acentos/ASCII en Task 1 (apóstrofes U+0027, sin markdown); el smoke de 17-04 re-asserta sobre variants[].prompt + explanation. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. `.env` (GEMINI_API_KEY/DEEPSEEK_API_KEY) solo se lee, no se instala nada. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exits 0
- Cada variante nueva: passes[] con 4× "correcta", cero "incorrecta" (registrado en SUMMARY)
- Slots locativos presentes (in spiaggia/montagna/campagna + al mare)
- `node --test tests/*.test.js` — los counts de Preposiciones siguen rojos a propósito hasta 17-04 (el conteo final ya es determinable tras este plan)
</verification>

<success_criteria>
- Variantes nuevas autoradas solo donde la regla admite reformulación (D-17-06, conservador)
- Cada variante nueva pasó el quórum cross-vendor R1-R7 (4× correcta) antes de entrar
- Slot in-locativo (3 variantes) + slot al-mare (1) creados (PILOT-03)
- Superficies existentes movidas NO re-validadas
- validateContent verde; conteo final de slots determinable para 17-04
</success_criteria>

<output>
Create `.planning/phases/17-piloto-preposiciones-contenido/17-03-SUMMARY.md` when done
</output>
