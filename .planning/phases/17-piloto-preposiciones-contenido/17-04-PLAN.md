---
phase: 17-piloto-preposiciones-contenido
plan: 04
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [PILOT-05]
must_haves:
  truths:
    - "El smoke paramétrico entiende el shape slot+variantes de Preposiciones: cuenta slots, lee explanation top-level para cobertura, escanea variants[].prompt para R1 y explanation para R2"
    - "Las otras 8 categorías legacy siguen leyéndose por payload sin cambios (bifurcación por shape, no reemplazo)"
    - "Los 3 hardcodes de count (52) están sincronizados contra el conteo REAL final de slots leído del JSON, no una estimación"
    - "node --test tests/*.test.js y node scripts/run-validation-271.mjs y VAL_07_STRICT=1 node --test tests/*.test.js pasan verdes"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "CATEGORIES_WITH_EXPLANATIONS bifurcado por shape (slot/legacy) + count Preposiciones sincronizado"
      contains: "Array.isArray"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de preposiciones sincronizado al nº de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected preposiciones + TOTAL_EXPECTED recalculado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "tests/exercise-types.test.js CATEGORIES_WITH_EXPLANATIONS"
      to: "ex.explanation / ex.variants[].prompt (slot) vs ex.payload.* (legacy)"
      via: "bifurcación Array.isArray(ex.variants)"
      pattern: "Array\\.isArray\\(ex\\.variants\\)"
---

<objective>
Sincronizar los 4 lugares que rompen al convertir Preposiciones a slot+variantes (PILOT-05): bifurcar el smoke paramétrico por shape (slot vs legacy) y actualizar los 3 hardcodes de count (`expected: 52`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots leído del JSON (NO una estimación). El validator y el loader NO se tocan (ya aceptan variants[]). Hacer el smoke shape-agnostic deja el camino abierto a CONV-01 (futuras categorías convertidas) sin reescribir el test.

Purpose: convertir Preposiciones rompe 4 checks hoy verdes porque asumen `ex.payload.*` y `expected: 52` replicado en 4 archivos. PILOT-05 ES arreglar estos 4 + extender la cobertura al shape slot, preservando la cobertura de explanations por slot. Es el gate final del piloto: estructura final pasa validator + smoke.

Output: los 3 test/script files sincronizados + bifurcados por shape; suite completa verde + reporter de validación PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-piloto-preposiciones-contenido/17-RESEARCH.md
@.planning/phases/17-piloto-preposiciones-contenido/17-PATTERNS.md
@content/exercises/preposiciones.json

<interfaces>
<!-- Los 4 lugares exactos a tocar. Verificado por grep en el codebase. -->

1. tests/exercise-types.test.js:
   - línea 1265-1274: `CATEGORIES_WITH_EXPLANATIONS` array con `{ file:'content/exercises/preposiciones.json', expected: 52 }` (las otras 8 cats con sus counts)
   - línea 1286: test `${expected}/${expected} ejercicios con explanation válida` con assert `data.exercises.length === expected`
   - línea 1294: coverage lee `ex.payload?.explanation`
   - línea 1307: smart-quotes sobre `ex.payload?.explanation`
   - línea 1323: markdown sobre `ex.payload?.explanation`
   - línea 1343: R1 leak sobre `ex.payload?.prompt`
   - línea 1362: R2 #NNN sobre `ex.payload?.explanation`
   - línea 1397-1415: bloque VAL_07_STRICT — lee `ex.validation?.status` (top-level → SOBREVIVE sin cambio); hereda el `expected` del array compartido (se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:169: `{ slug: 'preposiciones', expected: 52 }` en REAL_CATEGORIES

3. scripts/run-validation-271.mjs:75: `{ slug:'preposiciones', file:'...', expected: 52 }`; :86: `const TOTAL_EXPECTED = 373;`

Patrón de bifurcación por shape (RESEARCH §Validator + Smoke + PATTERNS §Shared Patterns):
```
const hasVariants = Array.isArray(ex.variants);
// hasVariants → coverage sobre ex.explanation (top-level); R1 sobre cada ex.variants[].prompt; R2/smart-quotes/markdown sobre ex.explanation
// else (legacy) → ruta actual: ex.payload.explanation / ex.payload.prompt (otras 8 cats sin cambio)
```
El count `expected` de preposiciones = nº de SLOTS final (data.exercises.length tras la reagrupación) — sincronizar contra el JSON REAL, NO contra la estimación 49-50.
`TOTAL_EXPECTED` = 373 − 52 + nº_slots_final (recalcular).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bifurcar CATEGORIES_WITH_EXPLANATIONS por shape (slot/legacy) en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (líneas 1255-1416 — el bloque CATEGORIES_WITH_EXPLANATIONS completo + VAL_07_STRICT)
    - content/exercises/preposiciones.json (estado final tras 17-03 — para leer el conteo REAL de slots y el shape de cada slot)
    - .planning/phases/17-piloto-preposiciones-contenido/17-PATTERNS.md §tests/exercise-types.test.js (qué rompe + patrón de extensión bifurcado)
    - .planning/phases/17-piloto-preposiciones-contenido/17-RESEARCH.md §Validator + Smoke (tabla de los 4 lugares + qué assertar sobre la estructura final)
  </read_first>
  <action>
    En tests/exercise-types.test.js: (1) leer el conteo REAL de slots de preposiciones.json (`data.exercises.length` tras 17-03) y actualizar `expected` de la entry preposiciones en `CATEGORIES_WITH_EXPLANATIONS` (línea 1266) a ese número. (2) Dentro del loop de coverage/R1/R2/smart-quotes/markdown, bifurcar por shape: `const hasVariants = Array.isArray(ex.variants)`. Si `hasVariants` → coverage sobre `ex.explanation` (top-level, no vacío), R1 (leak) sobre cada `ex.variants[].prompt`, R2 (#NNN) + smart-quotes + markdown sobre `ex.explanation`. Si NO (legacy) → ruta actual sin cambios (`ex.payload?.explanation` / `ex.payload?.prompt`) para las otras 8 categorías. Mantener el assert de cobertura: todo slot con `variants[]` tiene explanation top-level no vacía (cobertura preservada PILOT-05). El bloque VAL_07_STRICT (línea 1397+) lee `ex.validation?.status` que es top-level → NO cambia salvo heredar el count sincronizado. Hacer la bifurcación shape-agnostic (lista para CONV-01) — NO hardcodear "preposiciones" en la rama slot.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 (bifurcación presente)
    - El `expected` de preposiciones en CATEGORIES_WITH_EXPLANATIONS == nº REAL de slots (== `data.exercises.length` del JSON; verificable comparando)
    - Las otras 8 categorías siguen verdes con sus counts originales (40/23/31/37/39/51/56/44) — no se tocaron
    - La rama slot lee `ex.explanation` y `ex.variants[].prompt` (no `ex.payload`), verificable por grep en el bloque bifurcado
  </acceptance_criteria>
  <done>
    CATEGORIES_WITH_EXPLANATIONS bifurca coverage/R1/R2/smart-quotes/markdown por shape (slot→explanation+variants[].prompt; legacy→payload); count de Preposiciones sincronizado al nº real de slots; VAL_07_STRICT verde; las 8 cats legacy intactas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (líneas 166-202 — REAL_CATEGORIES + assert de conteo)
    - scripts/run-validation-271.mjs (líneas 63-104 — CATEGORIES array + TOTAL_EXPECTED + gates VAL-04/06/08, y el comentario explicativo 63-73)
    - content/exercises/preposiciones.json (conteo REAL final de slots)
    - .planning/phases/17-piloto-preposiciones-contenido/17-PATTERNS.md §slot-variants-integration + §run-validation-271
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'preposiciones', expected: 52 }` (línea 169) a `expected` = nº REAL de slots. El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 52` de la entry preposiciones (línea 75) a nº REAL de slots; recalcular `TOTAL_EXPECTED` (línea 86) = `373 - 52 + nº_slots_final`; actualizar el comentario explicativo (líneas 63-73) registrando la conversión de Preposiciones a slots (precedente: el comentario narra cada alta histórica que movió el total). Leer el conteo real del JSON, NO usar la estimación 49-50.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de preposiciones en ambos archivos == nº REAL de slots (== el usado en Task 1; los 3 hardcodes coinciden)
    - `TOTAL_EXPECTED` == 373 − 52 + nº_slots_final (verificable aritméticamente contra el conteo real)
    - El reporter de run-validation-271 no emite warning `total ≠ esperado` para preposiciones
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count (slot-variants-integration + run-validation-271 expected + TOTAL_EXPECTED) sincronizados contra el conteo real de slots; reporter PASS sin warnings de count; comentario de run-validation-271 actualizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido → smoke paramétrico | El smoke lee el shape del JSON; un shape inesperado (ni payload ni variants) debe fallar visible, no pasar silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Tampering | count desincronizado entre los 3 hardcodes → gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes (exercise-types, slot-variants-integration, run-validation-271 + TOTAL_EXPECTED) se sincronizan contra el MISMO conteo real leído del JSON (`data.exercises.length`), no estimaciones. El acceptance criteria asserta que coinciden y que run-validation-271 PASS. |
| T-17-04 | Tampering | explanation/prompt de slot con HTML/markdown/smart-quotes pasa sin escanear | mitigate | La bifurcación por shape mueve los escaneos smart-quotes/markdown/R1/R2 a `ex.explanation` + `ex.variants[].prompt` para slots — la cobertura de seguridad de contenido NO se pierde al convertir el shape. |
| T-17-10 | Spoofing/Tampering | shape inesperado (ni payload ni variants) pasa el smoke silenciosamente | mitigate | La bifurcación tiene dos ramas explícitas (hasVariants / payload); un ejercicio sin ninguno cae fuera de cobertura y falla el assert de coverage (explanation ausente) → visible. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup de 17-01, integration, exercise-types)
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exits 0
- `node scripts/assert-avere-prefix-unchanged.mjs` exits 0 (otras categorías intactas)
</verification>

<success_criteria>
- Smoke bifurcado por shape: slots → explanation top-level + variants[].prompt; legacy → payload (8 cats intactas)
- Los 3 hardcodes de count + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacía)
- Suite completa + reporter + smoke estricto verdes
- Shape-agnostic (listo para CONV-01)
</success_criteria>

<output>
Create `.planning/phases/17-piloto-preposiciones-contenido/17-04-SUMMARY.md` when done
</output>
