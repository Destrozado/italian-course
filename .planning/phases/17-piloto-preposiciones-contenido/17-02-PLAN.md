---
phase: 17-piloto-preposiciones-contenido
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/preposiciones.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PILOT-01]
must_haves:
  truths:
    - "Los 52 ejercicios de Preposiciones quedan reagrupados en slots por regla/forma; los que entrenan la misma forma exacta son variantes del mismo slot"
    - "Cada slot tiene una explanation top-level no vacía (cobertura preservada); las variantes NO llevan explanation propia"
    - "Las superficies existentes movidas intactas a variants[] NO se re-validan; el JSON sigue pasando validateContent"
    - "REQUIREMENTS.md y ROADMAP.md dicen 52 (no 57) ejercicios de Preposiciones"
  artifacts:
    - path: "content/exercises/preposiciones.json"
      provides: "Preposiciones reagrupado a shape slot+variantes (payload eliminado, variants[]+explanation top-level)"
      contains: "\"variants\""
    - path: ".planning/phases/17-piloto-preposiciones-contenido/17-REAGRUPACION-MAP.md"
      provides: "Mapa id-fuente→slot revisado por el autor (artefacto de auditoría)"
  key_links:
    - from: "content/exercises/preposiciones.json"
      to: "validateContent / normalizeExerciseToSlot"
      via: "shape slot+variantes (payload XOR variants[])"
      pattern: "\"variants\"\\s*:\\s*\\["
---

<objective>
Reagrupar los 52 ejercicios reales de Preposiciones en slots por regla pedagógica (D-17-01) / 1-slot-por-forma para articolate (D-17-02), fusionando los duplicados de forma confirmados (D-17-03) como variantes de un slot, y produciendo la explanation de slot vía elegir-la-más-completa + injertar matices (D-17-05). Las superficies existentes se MUEVEN intactas a `variants[]` (cambio de contenedor → NO re-validación). Corregir el hecho "57"→"52" en REQUIREMENTS.md y ROADMAP.md. Este plan NO autora variantes nuevas (eso es 17-03) ni añade los slots locativos nuevos (eso es 17-03, PILOT-03).

Purpose: es la columna vertebral del piloto — convierte el contenido real al modelo slot+variantes que el motor de Phase 16 ya consume. La fusión de duplicados de forma y la explanation mergeada son donde el dolor anti-memorización empieza a materializarse.

Output: `preposiciones.json` reescrito a slot+variantes (sin payload, con variants[]+explanation top-level por slot), validando verde; mapa de reagrupación auditado por el autor; fact correction 57→52.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-piloto-preposiciones-contenido/17-CONTEXT.md
@.planning/phases/17-piloto-preposiciones-contenido/17-RESEARCH.md
@.planning/phases/17-piloto-preposiciones-contenido/17-PATTERNS.md
@content/exercises/preposiciones.json

<interfaces>
<!-- Shape de slot con variantes que enforça el validator. Verificado en schema-validator.js / content-loader.js. -->
Shape autorado de un slot con variantes:
- `id`: string id de slot (= exercise id; esquema = Claude's Discretion del planner — usa renumeración semántica limpia tipo `preposiciones-al`, `preposiciones-sul`, …)
- `type`: "multiple-choice" a NIVEL de slot (todos los 52 son multiple-choice)
- `categoryIds`: ["preposiciones"] a nivel de slot, array no vacío
- `explanation`: string no vacío OBLIGATORIO a nivel de slot (SLOT-02)
- `variants`: array NO vacío; cada variante = objeto plano `{prompt (con ___), options (3-4 strings), correctIndex (en rango)}`. La variante NO lleva `explanation`.
- `validation`: top-level opcional `{status, passes[]}` — se preserva del/los ejercicio(s) fuente.

Reglas hard del validator (verificadas):
- `payload` XOR `variants[]` — nunca ambos, nunca ninguno. Al convertir, se ELIMINA `payload`.
- `explanation` top-level string no vacío cuando hay `variants[]`.
- `variants[]` no vacío; superficie validada por SURFACE_VALIDATORS[type].

Mapa de reagrupación (RESEARCH §Reagrupación Mapping Bloques A/B/C — leer íntegro). Fusiones confirmadas:
- SUL `su+il`: 006 + 013 + 043 → 1 slot, 3 variantes (D-17-03)
- AL `a+il`: 011 + 015 → 1 slot, 2 variantes (D-17-03)
- DI-posesso: 010 + 012 → 1 slot (misma regla posesión nombre propio)
- TRA/FRA tempo futuro: 008 + 049 → 1 slot 2 variantes (lectura D-17-01 — tra/fra son sinónimos eufónicos de la MISMA preposición, no articolate distintas; CONFIRMAR con el autor en el checkpoint, Open Q A1)
- El resto de los 52 → slots-de-1 (singletons), a la espera de variantes nuevas (17-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Producir el mapa de reagrupación auditable (id-fuente → slot)</name>
  <files>.planning/phases/17-piloto-preposiciones-contenido/17-REAGRUPACION-MAP.md</files>
  <read_first>
    - content/exercises/preposiciones.json (íntegro — los 52 ejercicios con su campo `notes` que declara la regla/forma de cada uno, y su `validation.passes[]`)
    - .planning/phases/17-piloto-preposiciones-contenido/17-RESEARCH.md §Reagrupación Mapping (Bloques A/B/C — el mapa propuesto completo, fusiones confirmadas, y Open Q A1 tra/fra)
    - .planning/phases/17-piloto-preposiciones-contenido/17-CONTEXT.md §decisions (D-17-01..05)
  </read_first>
  <action>
    Crear `.planning/phases/17-piloto-preposiciones-contenido/17-REAGRUPACION-MAP.md` como artefacto de auditoría: una tabla por cada slot resultante con columnas (slot-id propuesto, regla/forma, ids-fuente que entran como variantes, ¿fusión?, explanation-base elegida + matices a injertar D-17-05). Aplicar D-17-01 (regla pedagógica = 1 slot), D-17-02 (articolate 1-slot-por-forma), D-17-03 (fusiones confirmadas SUL 006/013/043, AL 011/015, DI-posesso 010/012). Para TRA/FRA (008/049) proponer la lectura D-17-01 (1 slot 2 variantes) y MARCARLO explícitamente como punto de confirmación del autor (Open Q A1). Para cada slot fusionado, comparar las explanations fuente y registrar cuál es la base (la más completa/general) y qué matices únicos de las descartadas se injertan (un contraste tipo "distinto de Nel", un pitfall del hispanohablante) — NO reescritura total. Usar esquema de id semántico limpio (`preposiciones-{forma}` o `preposiciones-{regla}`) respetando unicidad. NO autorar variantes nuevas aquí. NO incluir los slots locativos nuevos (van a 17-03). Reportar el conteo de slots resultante de SOLO esta reagrupación (sin slots nuevos ni variantes nuevas).
  </action>
  <verify>
    <automated>test -f .planning/phases/17-piloto-preposiciones-contenido/17-REAGRUPACION-MAP.md && grep -c "preposiciones-" .planning/phases/17-piloto-preposiciones-contenido/17-REAGRUPACION-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo 17-REAGRUPACION-MAP.md existe con una tabla que cubre los 52 ids fuente (cada id 001-052 aparece exactamente una vez en alguna columna de ids-fuente)
    - Las fusiones confirmadas D-17-03 están reflejadas: SUL agrupa 006/013/043, AL agrupa 011/015, DI-posesso agrupa 010/012
    - TRA/FRA (008/049) marcado como punto de confirmación del autor (Open Q A1)
    - Cada slot fusionado registra explanation-base + matices a injertar (D-17-05)
    - El conteo de slots de esta reagrupación está reportado (sin slots nuevos ni variantes nuevas)
  </acceptance_criteria>
  <done>
    17-REAGRUPACION-MAP.md cubre los 52 ids→slots con fusiones confirmadas, plan de explanation por slot fusionado, y el punto de confirmación tra/fra explícito; conteo de slots reportado.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Aprobar el mapa de reagrupación de Preposiciones y la lectura tra/fra antes de reescribir el JSON</decision>
  <context>
    El mapa (17-REAGRUPACION-MAP.md) determina la estructura final de slots y por tanto el conteo que los tests de 17-04 sincronizan. La única ambigüedad real es tra/fra (008/049): bajo D-17-01 (regla pedagógica) son la MISMA regla → 1 slot 2 variantes; bajo una lectura por forma serían 2 slots. La investigación recomienda 1 slot 2 variantes. Además, las explanations mergeadas (D-17-05) requieren revisión del autor (D-85) para no perder matices (Pitfall 5).
  </context>
  <options>
    <option id="tra-fra-fusion">
      <name>tra/fra fusionados (1 slot, 2 variantes) — recomendado</name>
      <pros>Coherente con D-17-01 (misma regla pedagógica = tempo futuro); maximiza el "no olvidar" por regla; son sinónimos eufónicos de la misma preposición simple, no articolate distintas.</pros>
      <cons>Mezcla dos formas de superficie (tra/fra) en un slot — aceptable porque la regla es idéntica.</cons>
    </option>
    <option id="tra-fra-split">
      <name>tra/fra separados (2 slots de 1)</name>
      <pros>Una superficie por slot, lectura estricta por forma.</pros>
      <cons>Contradice D-17-01 (regla pedagógica); el motor re-verificaría la misma regla dos veces.</cons>
    </option>
  </options>
  <resume-signal>Selecciona: tra-fra-fusion o tra-fra-split; y confirma/ajusta el mapa de slots y los merges de explanation. Escribe "aprobado" cuando el mapa esté listo para reescribir el JSON.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reescribir preposiciones.json a slot+variantes + fact correction 57→52</name>
  <files>content/exercises/preposiciones.json, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/17-piloto-preposiciones-contenido/17-REAGRUPACION-MAP.md (el mapa aprobado por el autor — fuente de verdad de la estructura)
    - content/exercises/preposiciones.json (íntegro — superficies y explanations fuente a mover/mergear)
    - .planning/phases/17-piloto-preposiciones-contenido/17-PATTERNS.md §content/exercises/preposiciones.json (Before/After de fusión real S-AL = 011+015)
    - .planning/REQUIREMENTS.md (línea ~30, "57 ejercicios") y .planning/ROADMAP.md (líneas ~69, 104, 108, "57")
  </read_first>
  <action>
    Reescribir `content/exercises/preposiciones.json` según el mapa aprobado: por cada slot, emitir `{id (semántico), type:"multiple-choice", categoryIds:["preposiciones"], explanation (merge D-17-05), variants:[...]}` ELIMINANDO `payload` de cada ejercicio fuente y MOVIENDO su superficie `{prompt, options, correctIndex}` intacta a `variants[]` (cambio de contenedor → NO re-validación). La `explanation` de cada slot fusionado = la base elegida en el mapa + los matices injertados; para singletons, la explanation existente sube a top-level tal cual. Preservar el campo `validation` top-level (de la fusión: combinar los `passes[]` o conservar el del ejercicio cuya superficie/explanation domina — registrar el criterio en el SUMMARY). NO autorar variantes nuevas. NO añadir slots locativos nuevos. Aparte, corregir el hecho en REQUIREMENTS.md ("57"→"52" en la descripción de PILOT-01) y en ROADMAP.md (cada "57" de la sección Phase 17 / success criteria → "52"). NO tocar los counts de tests/scripts aquí (eso es 17-04, que sincroniza contra el conteo REAL final tras 17-03).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exits 0
    - `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); const bad=d.exercises.filter(e=>e.payload); console.log('con payload:', bad.length)"` imprime `con payload: 0` (ningún ejercicio conserva payload)
    - Todo slot con `variants[]` tiene `explanation` top-level no vacía: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); const bad=d.exercises.filter(e=>Array.isArray(e.variants)&&(!e.explanation||!e.explanation.trim())); console.log('slots sin explanation:', bad.length)"` imprime `0`
    - Ninguna variante lleva explanation propia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); const bad=d.exercises.flatMap(e=>e.variants||[]).filter(v=>v.explanation!==undefined); console.log('variantes con explanation:', bad.length)"` imprime `0`
    - SUL tiene 3 variantes, AL tiene 2 variantes, DI-posesso 1 slot (asserts contra el JSON)
    - `grep -c "57" .planning/REQUIREMENTS.md` no cuenta referencias a 57-ejercicios-Preposiciones; `grep -rn "57 ejercicios" .planning/ROADMAP.md .planning/REQUIREMENTS.md` da 0 matches (filtrar comentarios con `grep -v '^#'` donde aplique)
  </acceptance_criteria>
  <done>
    preposiciones.json reescrito a slot+variantes (sin payload, variants[]+explanation top-level por slot, variantes sin explanation propia, fusiones SUL/AL/DI-posesso aplicadas), validateContent verde; "57"→"52" corregido en REQUIREMENTS.md + ROADMAP.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano → loader/validator al boot | El `preposiciones.json` reescrito a mano es input que cruza a validateContent y al render; explanation/prompt se renderizan como texto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | explanation/prompt con HTML/markdown | mitigate | Render plain-text; el smoke (17-04) asserta `/<[^>]+>/` y markdown markers ausentes; apóstrofes ASCII U+0027. Al mergear explanations (D-17-05) no introducir markup ni smart-quotes. |
| T-17-05 | Tampering / data-integrity | reagrupación que pierde un matiz de explanation o duplica/pierde una superficie fuente | mitigate | El mapa auditado (Task 1 + checkpoint) garantiza que los 52 ids fuente se mapean 1:1 (sin pérdida ni duplicado). D-17-05 elegir-la-más-completa + injertar evita perder matices (Pitfall 5); el acceptance criteria asserta cobertura de explanation por slot. |
| T-17-06 | Tampering | malformed JSON no crashea el loader | mitigate | `validateContent` (existente, no modificado) rechaza con banner visible slot sin variantes / variante sin superficie válida / explanation ausente. El acceptance criteria corre el validador. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exits 0
- `node --test tests/*.test.js` — nota: los counts de Preposiciones (52 hardcoded) FALLARÁN tras esta reescritura porque ahora hay menos slots y shape slot; esto es ESPERADO y se arregla en 17-04 (sync de hardcodes + bifurcación por shape). Documentar el rojo esperado en el SUMMARY; NO arreglar los counts aquí.
- El mapa 17-REAGRUPACION-MAP.md aprobado por el autor (checkpoint)
</verification>

<success_criteria>
- 52 ejercicios fuente reagrupados a slots por regla/forma; fusiones SUL/AL/DI-posesso (+ tra/fra según decisión del autor) aplicadas
- Cada slot: explanation top-level no vacía (merge D-17-05), variantes sin explanation propia, payload eliminado
- validateContent verde
- "57"→"52" corregido en REQUIREMENTS.md + ROADMAP.md
- (Los counts de tests quedan rojos a propósito hasta 17-04)
</success_criteria>

<output>
Create `.planning/phases/17-piloto-preposiciones-contenido/17-02-SUMMARY.md` when done
</output>
