---
phase: 24-verbi-di-movimento-a-slots-contenido
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [MOV-01]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Verbi di movimento (expected: 37 en exercise-types.test.js:1270, slot-variants-integration.test.js:170 y run-validation-271.mjs:117) estan sincronizados contra el conteo REAL final de slots leido del JSON (no una estimacion)"
    - "TOTAL_EXPECTED en run-validation-271.mjs:120 esta recalculado = 307 - 37 + n_slots_final_verbos_movimiento"
    - "El smoke parametrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto/Articoli/Partitivi/Avere/Essere; Verbi di movimento convertido a slots pasa coverage/R1/R2/smart-quotes/markdown sin tocar la logica del test, solo el count"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Verbi di movimento en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots"
      contains: "verbos-movimiento.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de verbos-movimiento sincronizado al n de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected verbos-movimiento + TOTAL_EXPECTED recalculado + comentario del historial actualizado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de verbos-movimiento.json"
      via: "sincronizacion contra el conteo REAL del JSON, no estimacion"
      pattern: "verbos-movimiento"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Verbi di movimento (`expected: 37`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `verbos-movimiento.json` leido del JSON tras 24-01/24-02 (NO una estimacion), cerrando MOV-01 (cierre del count). El smoke parametrico YA es shape-agnostic (la bifurcacion `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17, se reuso en Articoli Phase 19, Partitivos Phase 20, Avere Phase 22 y Essere Phase 23, y esta en el codebase — verificado en exercise-types.test.js getExplanation/getPrompts), asi que Verbi di movimento convertido a slots pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la logica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]). Verbi di movimento NO tiene snapshot/assert APPEND-ONLY (avere-only) ni cruces multi-cat -> este plan no corre ningun script de snapshot.

Purpose: convertir Verbi di movimento rompe los 3 counts hoy verdes (37 replicado en 3 archivos + TOTAL_EXPECTED=307 que incluye avere=20, articoli=34, partitivos=19 y essere=26). MOV-01 (cierre) ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase (tercera y ULTIMA de las 3 conversiones de verbos de v1.6).

Output: los 3 test/script files con el count de Verbi di movimento + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validacion PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/verbos-movimiento.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-08). El smoke YA bifurca por shape (no requiere extension de logica, solo count). avere ya esta en expected:20, articoli en 34, partitivos en 19 y essere en 26 — NO tocarlos. -->

1. tests/exercise-types.test.js:
   - linea 1270: `{ file: 'content/exercises/verbos-movimiento.json', expected: 37 }` en CATEGORIES_WITH_EXPLANATIONS <- cambiar 37 al n real de slots
   - avere expected:20 / articoli expected:34 / partitivos expected:19 / essere expected:26 — YA sincronizados (Phase 19/20/22/23), NO tocar
   - getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation — YA bifurcado por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere, NO tocar)
   - getPrompts YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar)
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido -> se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:170: `{ slug: 'verbos-movimiento', expected: 37 }` en REAL_CATEGORIES <- cambiar 37 al n real de slots (avere/articoli/partitivos/essere en sus lineas, NO tocar)

3. scripts/run-validation-271.mjs:
   - linea 117: `{ slug: 'verbos-movimiento', file: 'content/exercises/verbos-movimiento.json', expected: 37 }` <- cambiar 37 al n real
   - linea 120: `const TOTAL_EXPECTED = 307;` <- recalcular = 307 - 37 + n_slots_final_verbos_movimiento
   - lineas 64-103: comentario explicativo del historial del total <- anadir la conversion de Verbi di movimento a slots. El comentario narra cada cambio que movio el total; la ultima linea (lineas 95-103) documenta Essere ("-> 307 (v1.6 Phase 23, ESS-01): ... TOTAL = 320 - 39 + 26 = 307"). Anadir una linea analoga DESPUES: "-> <nuevo> (v1.6 Phase 24, MOV-01): Verbi di movimento se convirtio al shape slot+variantes. La reagrupacion fundio 37 ejercicios payload por REGLA DE AUXILIAR (D-24-01); la concordancia quedo en 1 SOLO slot (D-24-03, divergencia vs Essere) + correre como slot propio (D-24-04) + slot excepcioni->avere + 3 word-buttons = <count> slots reales. TOTAL = 307 - 37 + <count> = <nuevo>". OJO: a diferencia de Essere (que SUBIO por el passato en 4 slots + ser/estar), aqui la concordancia es 1 solo slot y NO hay cruces -> el net probablemente BAJA respecto a 37; pero LEER el count real, no asumir (los 4 ejes de huecos de 24-02 anaden VARIANTES, que NO suben el count; solo SLOTS nuevos lo suben).

El count `expected` de verbos-movimiento = n de SLOTS final (data.exercises.length tras 24-01+24-02) — sincronizar contra el JSON REAL, NO contra una estimacion.
Comando: `node -e "console.log(require('./content/exercises/verbos-movimiento.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
Leccion 19-03/20-03/22-03/23-03: leer SIEMPRE del JSON, nunca estimar. Las VARIANTES nuevas (los 4 ejes de 24-02) NO suben el count de slots; solo los SLOTS nuevos lo suben (24-02 recomienda NO crear slots nuevos -> el net = slots de reagrupacion de 24-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Verbi di movimento en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (las lineas alrededor de 1265-1310 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + el assert de conteo; y el bloque VAL_07_STRICT)
    - content/exercises/verbos-movimiento.json (estado final tras 24-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/phases/23-essere-a-slots-contenido/23-03-PLAN.md (Task 1 analogo de Essere — el mismo patron de sync de count contra el JSON real)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de verbos-movimiento.json (`data.exercises.length` tras 24-02, via `node -e "console.log(require('./content/exercises/verbos-movimiento.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry verbos-movimiento en CATEGORIES_WITH_EXPLANATIONS (linea 1270) a ese numero. NO tocar las entries de avere (en 20), articoli (en 34), partitivos (en 19) ni essere (en 26). NO tocar la logica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto/Articoli/Partitivi/Avere/Essere) -> Verbi di movimento slot pasa coverage/R1/R2/smart-quotes/markdown automaticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level -> no cambia salvo heredar el count sincronizado (todo slot/variante nuevo de 24-02 ya esta validated por el quorum; los slots movidos conservan su validation fuente, incluidos los disputed->override 011/024/026). NO tocar los counts de las otras 8 categorias.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de verbos-movimiento en CATEGORIES_WITH_EXPLANATIONS == n REAL de slots: `node -e "const d=require('./content/exercises/verbos-movimiento.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en linea 1270
    - Las otras 8 categorias siguen verdes con sus counts originales (avere 20, articoli 34, partitivos 19, essere 26, preposiciones 49, genero-numero 40, sustantivos-irregulares 31, profesiones 51) — no se tocaron
    - La logica del test (getExplanation/getPrompts/bifurcacion) NO se modifico: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Verbi di movimento en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-verbos-movimiento intactas (avere 20, articoli 34, partitivos 19, essere 26); logica del smoke (ya shape-agnostic) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED)</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (las lineas alrededor de 160-200 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (las lineas alrededor de 63-120 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/verbos-movimiento.json (conteo REAL final de slots)
    - .planning/phases/23-essere-a-slots-contenido/23-03-PLAN.md (Task 2 analogo de Essere — mismo patron de 3 hardcodes + TOTAL_EXPECTED + comentario del historial)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'verbos-movimiento', expected: 37 }` (linea 170) a `expected` = n REAL de slots. NO tocar las entries de avere (en 20), articoli (en 34), partitivos (en 19) ni essere (en 26). El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 37` de la entry verbos-movimiento (linea 117) a n REAL de slots; recalcular `TOTAL_EXPECTED` (linea 120) = `307 - 37 + n_slots_final_verbos_movimiento`; actualizar el comentario explicativo del historial del total (lineas 64-103) anadiendo una linea DESPUES de la de Essere (lineas 95-103) que registre la conversion de Verbi di movimento a slots: "-> <nuevo> (v1.6 Phase 24, MOV-01): Verbi di movimento se convirtio al shape slot+variantes. La reagrupacion fundio 37 ejercicios payload por REGLA DE AUXILIAR (D-24-01); la concordancia quedo en 1 SOLO slot (D-24-03, divergencia deliberada vs Essere) + correre como slot propio (D-24-04, alterna por destino) + slot excepcioni->avere + 3 word-buttons = <count> slots reales. Las 37 superficies fuente se preservaron como variantes (+N superficies nuevas de los 4 ejes D-24-06: mas verbos essere + mas excepciones avere + mas test-de-destino + matriz de concordancia). TOTAL = 307 - 37 + <count> = <nuevo>". Leer el conteo real del JSON, NO usar una estimacion (a diferencia de Essere que SUBIO por el passato en 4 slots + ser/estar, aqui la concordancia es 1 solo slot y no hay cruces -> el net probablemente BAJA; pero leer, no asumir). Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO numero.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de verbos-movimiento en ambos archivos == n REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/verbos-movimiento.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 307 - 37 + n_slots_final_verbos_movimiento (verificable aritmeticamente contra el conteo real; avere sigue contribuyendo 20, articoli 34, partitivos 19, essere 26)
    - El reporter de run-validation-271 no emite warning `total != esperado` para verbos-movimiento
    - El comentario del historial del total en run-validation-271.mjs menciona la conversion de Verbi di movimento a slots (incluida la concordancia en 1 solo slot D-24-03 y correre como slot propio D-24-04)
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Verbi di movimento (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el n real de slots; TOTAL_EXPECTED recalculado (avere=20, articoli=34, partitivos=19, essere=26 intactos); reporter PASS sin warnings de count; comentario del historial actualizado con la conversion de Verbi di movimento (concordancia 1 slot + correre propio).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido -> smoke parametrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

> Nota: esta fase edita hardcodes de count en tests/scripts de tooling de una app local single-user sin backend; no introduce nueva superficie de ataque. No se esperan amenazas HIGH.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Tampering | count desincronizado entre los 3 hardcodes -> gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leido del JSON (`data.exercises.length`), no estimaciones (leccion 19-03/20-03/22-03/23-03). El acceptance asserta que coinciden y que run-validation-271 PASS. La concordancia en 1 slot + sin cruces hacen que el net probablemente baje -> leer, no asumir. |
| T-24-03 | Tampering | explanation/prompt de slot con HTML/markdown/smart-quotes pasa sin escanear | mitigate | El smoke YA bifurca por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots — la cobertura de seguridad de contenido NO se pierde al convertir Verbi di movimento. |
| T-24-08 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 24-02 paso el quorum (status validated) y cualquier slot nuevo lleva validation top-level (D-19-09) antes de integrarse -> la gate pasa solo si 24-02 cumplio. Si algo quedo disputed o sin validation top-level, la gate FALLA visible. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v9 de Phase 21, integration, exercise-types, articoli/partitivos/avere/essere de Phase 19/20/22/23). Usar el glob `tests/*.test.js`; el path desnudo falla en Node 22.20 (MEMORY test_command_node_glob).
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json` exits 0
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de verbos-movimiento.json
- (NO se corre ningun assert de snapshot — avere-only, no existe para Verbi di movimento)
</verification>

<success_criteria>
- Los 3 hardcodes de count de Verbi di movimento + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacia — verificado por el smoke shape-agnostic)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); logica del smoke NO tocada (ya shape-agnostic); avere=20, articoli=34, partitivos=19 y essere=26 intactos
</success_criteria>

<output>
Create `.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-03-SUMMARY.md` when done
</output>
