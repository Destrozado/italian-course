---
phase: 48-traducci-n-paradigma-fare-4-categor-as
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - docs/TRANSLATION-VALIDATION-PROMPT.md
  - content/exercises/fare-indicativo.json
  - .planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md
autonomous: true
requirements: [TRAD-03]

user_setup:
  - service: deepseek + gemini
    why: "El quórum cross-vendor de traducción (D-46-13) llama a los dos proveedores por HTTP; aquí se consumen ~98 llamadas para las 49 variantes restantes, más las que imponga el alcance de re-validación decidido en 48-01."
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: ".env en la raíz del repo (ya presente desde la Phase 46)"
      - name: GEMINI_API_KEY
        source: ".env en la raíz del repo (ya presente desde la Phase 46)"

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "La decisión que el autor tomó en el checkpoint de 48-01 queda EJECUTADA, no solo registrada: si eligió amendar, la excepción vive en `docs/TRANSLATION-VALIDATION-PROMPT.md` —el único fichero que el evaluador lee— y no en un SUMMARY; si eligió cambiar de juez, el juez nuevo se aplica al bloque `fare` ENTERO y queda declarado en el ledger; si eligió no aplica, queda la evidencia que lo respalda."
    - "Las 54 variantes `multiple-choice` de `fare-indicativo` tienen `translationES.text` no vacío y status derivado `validated` con ≥2 `by` distintos de dos VENDORS distintos, comprobado ejecutando `deriveStatus` sobre el disco y no leyendo el campo `status` escrito."
    - "Los ocho tiempos del indicativo se distinguen ENTRE SÍ en español: las 48 variantes de los ocho slots de paradigma no colapsan en menos de ocho formas verbales españolas distintas por persona. Esa es la afirmación operativa del Success Criterion 2 y se cuenta del disco, no se declara."
    - "Ningún `disputed` se cierra con atajo: se resuelve con trabajo (leer el concern, arreglar el acento si el tag es de acentos, reescribir y re-correr el quórum COMPLETO desde cero si el concern es correcto), y si sobrevive dos rondas se escala con la dirección compuesta y los veredictos literales en lugar de overridearse."
    - "El sub-gate TRAD-COV imprime `fare-indicativo` en `54/54` y las tres categorías anteriores intactas; el rojo residual del reporter, si lo hay, viene SOLO de las tres categorías `fare` aún sin enganchar, no de esta."
    # ── Edge probe · TRAD-03 · adjacency (explicit, con sujeto en esta categoría) ──
    - "Dentro de `fare-indicativo` hay DOS formas italianas que se repiten en slots distintos (`facciamo` entre presente y otras casillas del bloque, y las coincidencias de persona entre passato remoto y passato prossimo): sus traducciones se escriben cada una en su variante, sin compartir ni referenciar, y si dos textos españoles salen byte-idénticos se cuenta y se declara la cifra en lugar de deduplicar."
  prohibitions:
    - statement: "Una excepción de criterios descubierta por el quórum MUST NOT quedarse en el SUMMARY, en el `notes` de un fichero de contenido ni en un comentario de código: se escribe en `docs/TRANSLATION-VALIDATION-PROMPT.md`, que es el único fichero que el evaluador lee. El síntoma de haberlo hecho mal es que el modelo marca un patrón y aprueba otro idéntico; este proyecto ya lo pagó tres veces."
      status: unresolved
      verification: judgment
    - statement: "Una excepción escrita MUST NOT omitir su parte de «qué se sigue vigilando». Las tres excepciones de la Phase 47 y las dos de la Phase 46 la llevan, y sin ella una excepción es un cheque en blanco que rebaja el criterio general para arreglar un caso particular."
      status: unresolved
      verification: judgment
    - statement: "Un `disputed` MUST NOT cerrarse reescribiendo la traducción para complacer al juez cuando el juez está equivocado. Si el concern pide aplanar el tiempo verbal, obedecerlo BORRA la casilla que el ejercicio existe para enseñar: es el Success Criterion 2 en negativo. La salida correcta es trabajo adversarial, y si no basta, escalar."
      status: unresolved
      verification: judgment
    - statement: "Un override de autor MUST NOT fabricar quórum. `deriveStatus` admite `by: autor` con `override: true` y el `incorrecta` se QUEDA en `passes[]`, pero la barra estricta de este proyecto exige que la variante ya tuviera ≥2 pases `correcta` de MODELOS distintos ANTES del override — la diferencia exacta entre los dos overrides que hoy existen en el corpus (ledger ids 35 y 39)."
      status: unresolved
      verification: judgment
    - statement: "Las traducciones MUST NOT generarse con el mismo pozo de modelos que después las juzga. La autoría es de Claude; la validación es DeepSeek + Gemini por script. Si el generador y el validador salen del mismo pozo, el quórum cross-vendor deja de ser independiente justo donde más valor tiene."
      status: unresolved
      verification: judgment
  artifacts:
    - path: "content/exercises/fare-indicativo.json"
      provides: "TRAD-03 — las 54 traducciones de la categoría más grande del bloque, todas validadas"
      contains: "translationES"
    - path: "docs/TRANSLATION-VALIDATION-PROMPT.md"
      provides: "La excepción que la evidencia del plan 48-01 exija, escrita donde el evaluador la lee — o cero líneas de diff con la evidencia que lo justifica"
      contains: "italianoResuelto"
    - path: ".planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md"
      provides: "La nota de enmienda fechada de D-46-12, si el alcance elegido no fue el cumplimiento literal"
      contains: "D-46-12"
  key_links:
    - from: "docs/TRANSLATION-VALIDATION-PROMPT.md"
      to: "scripts/validate-translation-pass.mjs"
      via: "`composePrompt` concatena el doc entero delante del payload: lo que no está en el doc, el evaluador no lo sabe"
      pattern: "PROMPT_PATH"
    - from: "content/exercises/fare-indicativo.json"
      to: "scripts/run-validation-271.mjs"
      via: "TRAD-COV cuenta las variantes con traducción `validated` contra el `expected` derivado de la categoría"
      pattern: "TRANSLATION_COVERAGE"
---

<objective>
Ejecutar la decisión que el autor tomó en el checkpoint de 48-01 y cerrar la categoría más grande del
bloque: las 54 variantes de `fare-indicativo`, los ocho tiempos del indicativo de `fare` más las dos
casillas de contraste, todas traducidas y `validated`.

Purpose: `fare-indicativo` es el 44% del bloque y contiene los ocho tiempos que el Success Criterion 2
nombra por su nombre. Si el español distingue los ocho, el criterio está probado sobre el sujeto que lo
motiva; si colapsa alguno, se sabe aquí y no en la fase de cierre.

Output: `docs/TRANSLATION-VALIDATION-PROMPT.md` en el estado que la decisión del autor exija (amendado
con su parte de «qué se sigue vigilando», o byte-intacto con la evidencia que lo respalda), las 54
traducciones de `fare-indicativo` en disco y `validated`, y TRAD-COV imprimiendo esa categoría completa.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-traducci-n-paradigma-fare-4-categor-as/48-01-SUMMARY.md
@docs/TRANSLATION-VALIDATION-PROMPT.md
</context>

## Artifacts this phase produces

Este plan no crea ninguna función, ningún flag de CLI ni ningún fichero de test. Lo que nace:

| Símbolo nuevo | Dónde | Task |
|---|---|---|
| Sección de excepción de criterios (nombre a fijar con los veredictos delante) — SOLO si la decisión del autor fue amendar | `docs/TRANSLATION-VALIDATION-PROMPT.md` | Task 1 |
| Nota de enmienda fechada y firmada de D-46-12 — SOLO si el alcance elegido no fue el cumplimiento literal | `.planning/phases/46-…/46-CONTEXT.md` | Task 1 |
| 49 sub-objetos `translationES` nuevos (hasta completar los 54 de la categoría) | `content/exercises/fare-indicativo.json` | Task 2 |
| `48-02-SUMMARY.md` | `.planning/phases/48-…/` | ambos |

**Cero símbolos nuevos en `src/`.** Motor byte-intacto, `schemaVersion` en 13.

## Lo que este plan hereda y NO puede re-decidir

El alcance de la re-validación lo decidió el autor en el checkpoint bloqueante del plan 48-01. Este
plan lo **ejecuta**; no lo re-abre, no lo recorta y no lo amplía. Si la opción elegida implica un
volumen de llamadas que no cabe en el presupuesto de este plan, **se declara como bloqueo con la cifra
estimada** y se pide un plan de continuación — recortar el alcance en silencio es exactamente el modo
de fallo que la gobernanza de excepciones existe para impedir.

## La cuenta de la categoría, derivada del disco al planificar (2026-08-15)

| Slot | Variantes | Qué casilla es | Riesgo declarado |
|---|---|---|---|
| `fare-indicativo-presente` | 6 | presente | ninguno especial |
| `fare-indicativo-imperfetto` | 6 | imperfetto | ninguno especial |
| `fare-indicativo-futuro-semplice` | 6 | futuro semplice | ninguno especial |
| `fare-indicativo-passato-remoto` | 6 | passato remoto | contraste con passato prossimo y con trapassato remoto |
| `fare-indicativo-passato-prossimo` | 6 | passato prossimo | `#3` lleva el adverbial de comida (carve-out ya escrito, id 37) |
| `fare-indicativo-trapassato-prossimo` | 6 | trapassato prossimo | ninguno especial |
| `fare-indicativo-futuro-anteriore` | 6 | futuro anteriore | forma española compuesta poco frecuente |
| `fare-indicativo-trapassato-remoto` | 6 | trapassato remoto | **el caso del tracer**: la forma española correcta es poco frecuente y la natural aplana el tiempo |
| `fare-indicativo-300` | 3 | selección de auxiliar | los 3 `prompt` llevan gloss de FRASE COMPLETA que coincide con la traducción |
| `fare-indicativo-301` | 3 | contraste con otros verbos | **el hueco NO es una forma de `fare`**: la traducción rinde las dos oraciones |
| **Total** | **54** | | |

Cinco de las 54 quedaron hechas en el plan 48-01 (el tracer más los cuatro de la batería de evidencia).
**Este plan cierra las 49 restantes.** Si al ejecutar la cuenta de hechas no fuera 5, **gana el disco** y
la discrepancia se declara antes de seguir.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ejecutar la decisión del autor sobre el doc de criterios y su alcance de re-validación</name>
  <reversibility rating="costly">Amendar `docs/TRANSLATION-VALIDATION-PROMPT.md` es `costly` por D-46-12: cambiar los criterios obliga a re-validar lo ya `validated` con el prompt viejo, y hoy son 206 traducciones en tres categorías. Por eso se hace TEMPRANO —cuando lo ya validado del bloque `fare` es mínimo— y por eso su alcance lo decidió el autor en 48-01 y no este task. Si la decisión fue cambiar de juez en vez de amendar, la reversibilidad baja a `reversible` en el doc y sube a `costly` en el corpus (los pases del juez retirado se retiran, y su contenido literal solo sobrevive en el histórico de git).</reversibility>
  <precondition>El plan 48-01 dejó registrada en su SUMMARY una decisión explícita del autor (`opcion-a`, `opcion-b`, `opcion-c`, `opcion-d` o `no-aplica`) con su motivo; sin esa línea este task no tiene mandato y debe detenerse en lugar de elegir por su cuenta.</precondition>
  <read_first>
    - `.planning/phases/48-traducci-n-paradigma-fare-4-categor-as/48-01-SUMMARY.md` — la decisión literal del autor, los 8 veredictos transcritos de la batería de evidencia, su clasificación en las tres lecturas, y el dictamen sobre si la excepción candidata es carve-out puro o introduce un imperativo.
    - `docs/TRANSLATION-VALIDATION-PROMPT.md` — §2 los criterios completos; §3 las tres excepciones léxicas ya escritas (da + PERSONA, PARTITIVO, ADVERBIAL DE COMIDA) y la excepción estructural del `prompt` metalingüístico: son el MOLDE EXACTO de cómo se redacta una excepción en este proyecto (qué NO marcar, qué NO exigir, y qué SÍ se sigue vigilando); y §3 «Gobernanza de excepciones».
    - `.planning/phases/47-traducci-n-bloque-art-culos-articoli-partitivos/47-02-SUMMARY.md` — la prueba de dos condiciones del carve-out y cómo se ejecutó el cumplimiento literal de D-46-12 sobre 32 variantes en vuelo, con el resultado medido (31 verdes, 1 `disputed` por un motivo que la enmienda ni tocaba).
    - `.planning/phases/47-traducci-n-bloque-art-culos-articoli-partitivos/47-03-SUMMARY.md` — cómo se ejecutó el cambio de juez sobre una categoría ENTERA (opción B+), por qué se aplicó a las 62 y no a las 9 que fallaban, y por qué los pases del juez retirado se retiraron en lugar de convivir.
    - `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md` — D-46-12 con sus cuatro notas de enmienda existentes: el formato exacto (fecha, decisión del autor citada, motivo) que una quinta nota tendría que seguir.
    - `scripts/lib/pass-guard.mjs` — cuándo lanza `assertNoBorraIncorrectaEnSilencio` y qué exige `--adjudicar`. Una re-validación masiva sobre contenido con `incorrecta` históricos lo va a encontrar.
  </read_first>
  <files>docs/TRANSLATION-VALIDATION-PROMPT.md, .planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md</files>
  <action>
Materializar la decisión del autor. **Cinco ramas, mutuamente excluyentes, y la rama se lee del
SUMMARY de 48-01 — no se elige aquí.**

**Rama `no-aplica`.** No se toca ningún fichero. Se registra en el SUMMARY que la excepción NO hizo
falta, con los veredictos literales que lo respaldan. Una excepción escrita «por si acaso» relaja el
criterio sin sujeto que lo justifique. Se pasa directo al Task 2.

**Rama `opcion-a` o `opcion-b` (amendar el doc).** Escribir la excepción **en el doc**, en la sección de
fronteras de §3, usando como molde las cuatro excepciones ya escritas. Debe decir tres cosas y ninguna
más:
  - **qué NO marcar** — la forma verbal española concreta que el juez objetó, nombrada con precisión y
    con el motivo por el que es correcta;
  - **qué NO exigir** — que la traducción se reescriba en la forma más frecuente cuando la más
    frecuente aplana el tiempo del italiano;
  - **qué SÍ se sigue vigilando** — que la traducción siga sujeta a S2 con todo el rigor, incluida la
    viñeta que declara `false` cambiar el tiempo verbal, y a S1/S4/S6 sin rebaja. **Una excepción sin
    esta tercera parte es un cheque en blanco.**

  Después, ejecutar el alcance de re-validación que el autor eligió: en `opcion-a`, las 206 ya
  `validated` bajo el prompt viejo, con quórum completo desde cero, contando del disco cuántas cambian
  de veredicto y cuántas no; en `opcion-b`, solo el bloque `fare`, **más** la entrada de deuda en el
  ledger y la nota de enmienda fechada de D-46-12 en `46-CONTEXT.md` **antes** de continuar. La nota
  cita la decisión del autor literalmente y explica por qué la direccionalidad absolutoria se sostiene.

**Rama `opcion-c` (cambiar de juez).** `docs/TRANSLATION-VALIDATION-PROMPT.md` queda con **cero líneas
de diff**. El juez nuevo se declara para el bloque `fare` ENTERO —las cuatro categorías, no solo
`fare-indicativo`— y se registra en el ledger como segunda desviación de homogeneidad del corpus,
apuntando a la primera (id 38) para que las dos sean legibles juntas. Las variantes ya validadas del
bloque bajo el juez anterior reciben el pase nuevo igual que las que fallaron: re-juzgar solo las que
fallan es re-tirar los dados con un dado distinto hasta que pasen. Los pases del juez retirado se
retiran, porque `deriveStatus` hace sticky cualquier `incorrecta` y un juez retirado con objeciones
bloquearía el `disputed` para siempre salvo override; su contenido literal se transcribe en el SUMMARY
antes de retirarlo.

**Rama `opcion-d` (reescribir las traducciones).** Se reescriben las variantes candidatas y se
re-validan desde cero, con quórum completo. **Se transcribe en el SUMMARY, para cada una, la forma
verbal que se abandona y la que se adopta**, porque esta rama es la única que puede reducir la
legibilidad del tiempo y el autor tiene que poder verlo sin leer el JSON.

**En todas las ramas menos `no-aplica`:** si durante la re-validación el `pass-guard` lanza porque un
modelo ya votó `incorrecta` sobre esa variante, **no se sortea el guard**: se pasa `--adjudicar` con un
motivo escrito de verdad, que queda grabado en el JSON, o se detiene. El guard existe porque el disenso
no se borra en silencio.
  </action>
  <verify>
    <automated>node --test tests/translation-validator.test.js tests/schema-translation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - La rama ejecutada es EXACTAMENTE la que el SUMMARY de 48-01 registra: la línea de decisión del autor se cita literalmente en el SUMMARY de este plan antes de describir lo que se hizo.
    - Si se amendó el doc: la sección nueva contiene explícitamente su parte de «qué se sigue vigilando», verificable leyéndola, y menciona que S2 sigue declarando `false` el cambio de tiempo verbal. Y NO vive en ningún otro sitio: `git diff` muestra que la regla se añadió al doc, no solo al SUMMARY ni a un comentario.
    - Si NO se amendó el doc: `git diff --stat docs/TRANSLATION-VALIDATION-PROMPT.md` está vacío y el SUMMARY transcribe los veredictos que justifican dejarlo intacto.
    - El alcance de re-validación ejecutado coincide con el elegido, y su resultado está CONTADO del disco: cuántas variantes se re-validaron, cuántas conservaron veredicto, cuántas cambiaron, cuántos caracteres de español se modificaron y cuántos overrides nuevos se crearon. Las cinco cifras derivadas, no afirmadas.
    - Si el alcance fue `opcion-b`: existen tanto la entrada en el ledger como la nota de enmienda fechada en `46-CONTEXT.md`, ambas verificables por grep, y ambas ANTES del commit que cierra este task.
    - Si el alcance fue `opcion-c`: el juez nuevo está declarado para las CUATRO categorías del bloque en el SUMMARY, y la entrada de ledger apunta explícitamente a la desviación previa de homogeneidad.
    - Cero pases pre-enmienda supervivientes donde el alcance los alcanzaba: para cada variante re-validada, el `by` y la fecha de sus pases son posteriores a la enmienda, comprobado recorriendo el disco.
    - Reporter: `node scripts/run-validation-271.mjs` sigue sin emitir ninguna violación de ancla, y VAL-04/06/09 siguen en PASS. Si TRAD-COV está en FAIL, la causa está atribuida por escrito a la cobertura parcial del bloque `fare` y no a esta operación.
    - `git diff --stat src/domain/ src/screens/app.js` vacío; `schemaVersion` en 13; suite con los mismos 4 fallos pre-existentes y ni uno más.
  </acceptance_criteria>
  <done>La decisión del autor está ejecutada, no solo registrada: el doc quedó amendado con su parte de vigilancia, o byte-intacto con la evidencia que lo respalda, o el juez nuevo aplicado al bloque entero — y el alcance de re-validación que el autor eligió está ejecutado y contado del disco. Committeado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cerrar `fare-indicativo` — las 49 traducciones restantes, los ocho tiempos distinguidos</name>
  <reversibility rating="reversible">49 traducciones y sus pases. El texto de cada una se reescribe en su sitio; nada estructural cambia y el enganche al array y al ancla ya lo hizo 48-01.</reversibility>
  <precondition>El Task 1 dejó `docs/TRANSLATION-VALIDATION-PROMPT.md` en su estado final para esta fase; autorar 49 traducciones bajo un doc que aún puede cambiar las condenaría a re-validarse por D-46-12.</precondition>
  <read_first>
    - `content/exercises/fare-indicativo.json` — los 10 slots ENTEROS con sus `explanation` y sus `prompt`. Leer slot por slot antes de traducir cada uno: las seis variantes de un slot son las seis personas de la MISMA casilla y tienen que ser coherentes entre hermanas. Leer también el `notes` de cabecera: documenta 0-GLOSS (D-41-05), la lista negra de formas atestiguadas (D-41-08) y por qué la categoría no tiene slots `match` ni `word-buttons`.
    - `.planning/phases/48-traducci-n-paradigma-fare-4-categor-as/48-01-SUMMARY.md` — las 5 traducciones ya hechas y sus veredictos: son el patrón de registro y de forma verbal que las 49 restantes tienen que seguir para que la categoría sea coherente.
    - `docs/TRANSLATION-VALIDATION-PROMPT.md` — en su estado FINAL tras el Task 1, con especial atención a la viñeta de S2 sobre el tiempo verbal, a la GUARDIA DE FIDELIDAD de S6, y a la frontera con el `gloss` (los 3 `prompt` de `fare-indicativo-300` llevan gloss de frase completa).
    - `scripts/validate-translation-pass.mjs` — la cabecera de uso y los cuatro exit codes; en una tanda de ~98 llamadas la diferencia entre el 1 (no se gastó nada) y el 3 (se gastó la llamada y el veredicto va impreso en stdout, recuperable a mano) decide si se re-lanza o se aplica el pase manualmente.
    - `src/data/validation-state.js` — `deriveStatus` y el reconocedor del override de autor.
  </read_first>
  <files>content/exercises/fare-indicativo.json</files>
  <action>
Cerrar la categoría entera: autorar y validar las 49 traducciones que faltan, slot por slot.

**1. Recomputar la línea base del disco.** Contar cuántas variantes `multiple-choice` tiene la
categoría y cuántas ya llevan traducción. Si no son 54 y 5, gana el disco y se declara la
discrepancia antes de seguir.

**2. Autorar slot por slot, no variante por variante.** Ver el slot ENTERO —su `explanation` y sus
seis hermanas— antes de escribir la primera. Las seis variantes de un slot de paradigma son las seis
personas de la misma casilla: la forma verbal española tiene que ser **la misma en las seis, conjugada
en la persona que toca**. Una que se salga del molde enseña mal justo el contraste que el slot existe
para enseñar, y es además la señal más fácil de detectar de que se tradujo sin mirar a los lados.

**El trabajo de esta categoría, dicho con precisión:** ocho slots son ocho tiempos distintos del mismo
verbo. Cada uno tiene en español una forma verbal propia. La traducción de cada casilla usa **esa** y
no la de al lado — aunque la de al lado suene más frecuente en el habla. Que el tiempo se reconozca es
consecuencia de haber elegido bien la forma, jamás de haberla etiquetado: **ninguna traducción nombra
ningún tiempo ni ningún modo**, ni en español ni en italiano.

Los dos slots que no son de paradigma piden otra cosa: en `fare-indicativo-300` el hueco es la
selección del auxiliar y el `prompt` ya lleva un gloss español de frase completa, así que la traducción
coincidirá con él casi palabra por palabra —**y eso es correcto y esperable según el doc**, no es
repetir el gloss; en `fare-indicativo-301` el hueco NO es una forma de `fare`, y la traducción tiene
que rendir **las dos oraciones enteras**, la que lleva la forma de `fare` ya resuelta y la del hueco.

Forma en disco: la de siempre (D-46-02), hermana de `prompt`/`options`/`correctIndex`, con `validation`
inicializado a `pending` y `passes` vacío. **El `validation` a nivel de SLOT no se toca.** Español
acentuado RAE, mayúscula inicial y punto final cuando el italiano los tiene, apóstrofes ASCII.

**3. Validar 1-por-1 con los dos jueces declarados** (VAL-03: cada invocación ve una sola traducción,
fresh context). Dos vendors, `--avoid` alimentado con el `by` REAL del primer pase, concurrencia 1,
`--write`. Verificar la cola de fallbacks contra el proveedor antes de empezar la tanda, sabiendo que
eso es necesario y no suficiente. Trabajar por lotes de un slot y hacer `git diff` entre lotes:
cualquier cambio fuera de los bloques esperados detiene el plan.

**4. Resolver los `disputed` CON TRABAJO, en este orden.**
   - Si el tag es de acentos: **es un bug REAL**, se arregla la tilde en el texto y se re-valida. En
     este proyecto un flag de acento sobre el español nunca se cierra como falso positivo.
   - Si el concern dice que la traducción cambió el tiempo verbal: comprobarlo. Si tiene razón, el
     defecto es de la traducción y se reescribe.
   - Si el concern pide **aplanar** el tiempo por naturalidad: **no obedecer**. Hacer trabajo
     adversarial —segunda muestra independiente del mismo modelo para descartar ruido de muestreo, y
     juez más estricto del MISMO vendor que el objetor— y contar del disco si el propio objetor aprobó
     la estructura idéntica en otra variante. Si el trabajo adversarial lo refuta, queda documentado.
   - Si un `disputed` sobrevive dos rondas completas de trabajo: **DETENER** y devolver un bloqueo con
     la dirección compuesta, los veredictos y el concern literal. El override de autor existe y es de
     primera clase, pero exige adjudicación adversarial previa y motivo escrito, y **no fabrica
     quórum**: el `incorrecta` se queda en `passes[]`.

**5. Contar la distinción de tiempos antes de cerrar.** Extraer del disco las 48 traducciones de los
ocho slots de paradigma y comprobar que, para una misma persona, los ocho slots dan ocho formas
verbales españolas distintas. Es la afirmación operativa del Success Criterion 2 y se cuenta, no se
declara. Si dos slots colapsan en la misma forma para alguna persona, se declara la colisión con las
dos direcciones compuestas y el motivo lingüístico, y se lleva al plan de cierre.
  </action>
  <verify>
    <automated>node --test tests/schema-translation.test.js tests/translation-validator.test.js tests/count-arrays-lockstep.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Las 54 variantes `multiple-choice` de `content/exercises/fare-indicativo.json` tienen `translationES.text` no vacío: recorrido propio del disco, con la cifra 54/54 transcrita.
    - Las 54 tienen status derivado `validated` con ≥2 `by` distintos de dos VENDORS distintos, comprobado ejecutando `deriveStatus` sobre `passes[]` leído del disco — no leyendo el campo `status` escrito. Cero `pending`, cero `disputed` residuales.
    - Cero traducciones arrastran el hueco: un grep del patrón del marcador de hueco dentro de un valor `text` sobre el fichero devuelve 0.
    - Ninguna traducción nombra un tiempo ni un modo verbal: un grep sobre los 54 valores `text` buscando términos de metalenguaje gramatical devuelve cero coincidencias.
    - Coherencia intra-slot verificada por lectura: para cada uno de los ocho slots de paradigma, las seis traducciones usan la MISMA forma verbal española conjugada en las seis personas. Transcribir en el SUMMARY, para cada slot, la forma verbal elegida — ocho líneas, una por casilla.
    - Distinción inter-slot CONTADA: para una persona fija, los ocho slots de paradigma dan ocho formas verbales españolas distintas. Si alguna pareja colapsa, se listan las dos direcciones compuestas y el motivo, y se declara como hallazgo para el plan de cierre en lugar de silenciarlo.
    - TRAD-COV imprime `fare-indicativo` en `54/54`: la línea literal del reporter transcrita en el SUMMARY, copiada de la salida y no afirmada. Las tres categorías anteriores siguen en sus cifras de la línea base (96, 48, 62).
    - Cero violaciones de ancla en la corrida del reporter, y `content/translation-coverage.lock.json` **sin tocar** en este plan: `git diff --stat` sobre el fichero vacío. La categoría ya estaba anclada desde 48-01 y el ancla es un suelo, no una igualdad — crecer no la enrojece.
    - Overrides nuevos: cero, salvo que alguno esté explícitamente autorizado por un bloqueo escalado y resuelto por el autor. El recuento de entradas de pase con autor `autor` en el corpus se compara contra `HEAD` y se transcribe.
    - El `validation` a nivel de SLOT de los 10 slots queda idéntico al de `HEAD`, comparado como dos fotos distintas del mismo dato.
    - `git diff --stat src/domain/ src/screens/app.js` vacío; `schemaVersion` en 13; suite con los mismos 4 fallos pre-existentes y ni uno más, sobre un total ≥ 1360.
  </acceptance_criteria>
  <done>Las 54 variantes de `fare-indicativo` tienen traducción `validated` por quórum cross-vendor real, los ocho tiempos del indicativo se distinguen en español y esa distinción está contada del disco, y TRAD-COV imprime la categoría completa. Committeado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenido del corpus → modelos externos (DeepSeek / Gemini) | ~98 llamadas de este plan, más las que imponga el alcance de re-validación decidido en 48-01 (hasta 412 en el peor caso). |
| respuesta del modelo → `content/exercises/fare-indicativo.json` | Texto no confiable de un tercero vuelve parseado y se escribe en el corpus, 49 veces. |
| edición del doc de criterios → todo el corpus ya validado | `docs/TRANSLATION-VALIDATION-PROMPT.md` gobierna el veredicto de las 260 traducciones del milestone: tocarlo cambia el criterio bajo el que TODO fue juzgado. |
| `.env` → proceso del script | Claves de API en memoria y en cabeceras HTTP. |

## STRIDE Threat Register (ASVS L1 · block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-09 | Tampering (relajar el criterio para todo el corpus arreglando un caso) | la sección de excepción escrita en el doc | **high** | mitigate | La excepción se redacta con el molde de las cuatro existentes y DEBE llevar su parte de «qué se sigue vigilando», que reafirma S2 sin rebaja. Su alcance de re-validación lo decidió el autor en un checkpoint bloqueante, no este task. Una excepción sin la tercera parte es un fallo del criterio de aceptación, no un detalle de estilo. |
| T-48-10 | Repudiation (perder el disenso al re-validar en masa) | `passes[]` de las variantes re-validadas | **high** | mitigate | El `pass-guard` (CR-01) lanza si un modelo que ya votó `incorrecta` va a re-emitir pase, y solo cede con `--adjudicar` y motivo escrito grabado en el JSON. Los pases de un juez retirado se transcriben literalmente en el SUMMARY antes de retirarse, y su contenido sobrevive en el histórico de git. |
| T-48-11 | Tampering (corrupción del corpus en una tanda larga) | `writePass` sobre 49 variantes anidadas | **high** | mitigate | `withFileLock` serializa el read-modify-write y el objetivo se re-estrecha hasta la variante ANTES de ramificar; los 10 slots ya tienen su propio `validation` y confundirlos corrompería 10 registros de auditoría. `git diff` entre lotes de un slot: cualquier cambio fuera de los bloques esperados detiene el plan. Temporales `*.json.tmp-*` ignorados desde WR-06. |
| T-48-12 | Tampering (prompt injection vía contenido del corpus) | el prompt compuesto | **high** | mitigate | Guard §6 del doc: `prompt`, `options`, `correctIndex` y `translationES.text` son DATA, nunca directiva. Vector específico: los 3 `prompt` de `fare-indicativo-300` llevan una frase española completa entre paréntesis. Contenido autorado por el autor y por Claude, no importado. |
| T-48-13 | Information Disclosure (fuga de claves) | `loadEnv()` / `callModel` | **high** | mitigate | Clave en cabecera, jamás en URL ni en log. El objeto de pase escrito contiene solo autor, fecha, veredicto y concerns. Fichero de entorno ignorado por git. |
| T-48-14 | Denial of Service | rate limits en una tanda de ~98 llamadas | medium | mitigate | Concurrencia 1, cola de fallbacks verificada antes de empezar, auto-fallback con reintentos, y el exit code 3 que distingue «llamada gastada, veredicto recuperable en stdout» de «no se gastó nada». El lock de fichero garantiza atomicidad si la tanda se corta. |
| T-48-15 | Spoofing / Elevation of Privilege | n/a | low | accept | Scripts locales del autor sobre su máquina; sin servidor, sin sesiones, sin roles. |

## Gate de legitimidad de paquetes

**No aplica:** cero instalaciones de paquetes. `T-48-SC` no aplicable por ausencia de instalaciones.
</threat_model>

<verification>
1. La decisión del autor de 48-01 citada literalmente y EJECUTADA en su rama correspondiente.
2. Si se amendó el doc: la excepción está en el doc (no en un SUMMARY) y lleva su parte de «qué se
   sigue vigilando». Si no: cero líneas de diff con la evidencia que lo respalda.
3. 54/54 variantes de `fare-indicativo` con traducción y con status derivado `validated`, dos vendors.
4. Las ocho formas verbales españolas, una por casilla del paradigma, transcritas; la distinción entre
   ellas contada del disco.
5. TRAD-COV imprime `fare-indicativo` 54/54 y cero violaciones de ancla.
6. Cero `disputed` residuales; cero overrides nuevos no autorizados.
7. `git diff --stat src/domain/ src/screens/app.js` vacío; `schemaVersion` en 13.
8. Suite con exactamente los 4 fallos pre-existentes, sobre un total ≥ 1360.
</verification>

<success_criteria>
- La categoría más grande del bloque está completa y validada, y su calidad está contada del disco en
  vez de afirmada.
- El doc de criterios quedó en el estado que el autor decidió, con la evidencia que lo respalda escrita
  donde el evaluador la lee.
- Los ocho tiempos del indicativo de `fare` se distinguen en español, que es el Success Criterion 2
  medido sobre su sujeto principal.
- Motor byte-intacto, `schemaVersion` en 13, cero regresiones nuevas.
</success_criteria>

<output>
Create `.planning/phases/48-traducci-n-paradigma-fare-4-categor-as/48-02-SUMMARY.md` when done
</output>
