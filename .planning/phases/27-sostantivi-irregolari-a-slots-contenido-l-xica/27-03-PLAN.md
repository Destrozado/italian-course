---
phase: 27-sostantivi-irregolari-a-slots-contenido-l-xica
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [SOST-01]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Sostantivi irregolari (expected: 31 en exercise-types.test.js:1269, slot-variants-integration.test.js:171 y run-validation-271.mjs:152) estan sincronizados contra el conteo REAL final de slots leido del JSON (data.exercises.length), no una estimacion"
    - "TOTAL_EXPECTED en run-validation-271.mjs:156 esta recalculado = 209 - 31 + n_slots_final_sustantivos"
    - "El smoke parametrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero/Professioni; Sostantivi irregolari convertido a slots (100% MC) pasa coverage/R1/R2/smart-quotes/markdown sin tocar la logica del test, solo el count"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada; CONV-01 cerrado (9/9 categorias de gramatica en slot+variantes) -> fin de v1.6"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Sostantivi irregolari en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots"
      contains: "sustantivos-irregulares.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de sustantivos-irregulares sincronizado al n de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected sustantivos-irregulares + TOTAL_EXPECTED recalculado + comentario del historial actualizado (cierre CONV-01)"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de sustantivos-irregulares.json"
      via: "sincronizacion contra el conteo REAL del JSON, no estimacion"
      pattern: "sustantivos-irregulares"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Sostantivi irregolari (`expected: 31`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `sustantivos-irregulares.json` leido del JSON tras 27-01/27-02 (NO una estimacion), cerrando SOST-01 (cierre del count) y con ello CONV-01 (9/9 categorias de gramatica en slot+variantes) -> FIN del milestone v1.6. El smoke parametrico YA es shape-agnostic (la bifurcacion `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17, se reuso en Articoli Phase 19, Partitivos Phase 20, Avere Phase 22, Essere Phase 23, Verbi di movimento Phase 24, Genere e numero Phase 25 y Professioni Phase 26, y esta en el codebase — verificado en exercise-types.test.js getExplanation/getPrompts), asi que Sostantivi irregolari convertido a slots (100% MC) pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la logica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]). Sostantivi irregolari NO tiene snapshot/assert APPEND-ONLY (avere-only) ni cruces multi-cat -> este plan no corre ningun script de snapshot. Este set es 100% MC (sin match ni word-buttons) -> no toca las ramas match/word-buttons del smoke (ya existen para otras categorias y siguen verdes).

Purpose: convertir Sostantivi irregolari rompe los 3 counts hoy verdes (31 replicado en 3 archivos + TOTAL_EXPECTED=209 que incluye preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, profesiones=11, verbos-movimiento=7). SOST-01 (cierre) ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase Y del milestone (tercera y ultima conversion lexica de v1.6; cierra CONV-01).

Output: los 3 test/script files con el count de Sostantivi irregolari + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validacion PASS + smoke estricto verde; CONV-01 cerrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/sustantivos-irregulares.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-09). El smoke YA bifurca por shape (no requiere extension de logica, solo count). preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, profesiones=11, verbos-movimiento=7 — NO tocarlos. -->

1. tests/exercise-types.test.js:
   - linea 1269: `{ file: 'content/exercises/sustantivos-irregulares.json', expected: 31 }` en CATEGORIES_WITH_EXPLANATIONS <- cambiar 31 al n real de slots
   - las otras 8 categorias (preposiciones 49, articoli 34, avere 20, essere 26, genero-numero 12, partitivos 19, profesiones 11, verbos-movimiento 7) — YA sincronizadas, NO tocar
   - getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation — YA bifurcado por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero/Professioni, NO tocar)
   - getPrompts YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar); Sostantivi irregolari es 100% MC -> pasa por la rama MC sin tocar logica
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido -> se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:171: `{ slug: 'sustantivos-irregulares', expected: 31 }` en REAL_CATEGORIES <- cambiar 31 al n real de slots (las otras 8 en sus lineas, NO tocar)

3. scripts/run-validation-271.mjs:
   - linea 152: `{ slug: 'sustantivos-irregulares', file: 'content/exercises/sustantivos-irregulares.json', expected: 31 }` <- cambiar 31 al n real
   - linea 156: `const TOTAL_EXPECTED = 209;` <- recalcular = 209 - 31 + n_slots_final_sustantivos
   - lineas del comentario explicativo del historial del total (alrededor de 64-155) <- anadir la conversion de Sostantivi irregolari a slots. El comentario narra cada cambio que movio el total; la ultima linea documenta Professioni (Phase 26, PROF-01). Anadir una linea analoga DESPUES: "-> <nuevo> (v1.6 Phase 27, SOST-01, CIERRE CONV-01): Sostantivi irregolari se convirtio al shape slot+variantes HIBRIDO. La reagrupacion fundio 31 ejercicios payload (100% MC) a <count> slots reales: bloque REGLA por sub-regla (D-27-02: sovrabbondanti -o->-a, invariabili-accentate, invariabili-straniere) + bloque LEXICO PURO cambio-radice sin variantes (uomo/dio/bue/tempio, duplicado #008==#025 como 2 variantes) + bloque CONTRASTE plurali-regolari sin engorde (D-27-05). Las 31 superficies fuente se preservaron como variantes (+N superficies nuevas SOLO del bloque regla D-27-03: sovrabbondanti + invariabili acentuadas/extranjeras — variantes, NO slots, asi que NO suben el count; el bloque lexico y el contraste no recibieron variantes, SOST-01). TOTAL = 209 - 31 + <count> = <nuevo>. Con esta conversion CONV-01 queda cerrado: 9/9 categorias de gramatica en slot+variantes -> fin de v1.6". OJO: el net BAJA (31 payload -> menos slots por la fusion a slot+variantes); pero LEER el count real, no asumir (las VARIANTES nuevas de 27-02 NO suben el count; solo SLOTS nuevos lo suben, y 27-02 recomienda NO crear slots nuevos -> el net = slots de reagrupacion de 27-01).

El count `expected` de sustantivos-irregulares = n de SLOTS final (data.exercises.length tras 27-01+27-02) — sincronizar contra el JSON REAL, NO contra una estimacion.
Comando: `node -e "console.log(require('./content/exercises/sustantivos-irregulares.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
Leccion 19-03/20-03/22-03/23-03/24-03/25-03/26-03: leer SIEMPRE del JSON, nunca estimar. Las VARIANTES nuevas (los ejes del bloque regla de 27-02) NO suben el count de slots; solo los SLOTS nuevos lo suben (27-02 recomienda NO crear slots nuevos -> el net = slots de reagrupacion de 27-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Sostantivi irregolari en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (las lineas alrededor de 1260-1290 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + el assert de conteo; y el bloque VAL_07_STRICT)
    - content/exercises/sustantivos-irregulares.json (estado final tras 27-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/phases/26-professioni-a-slots-contenido-l-xica/26-03-PLAN.md (Task 1 analogo de Professioni — el mismo patron de sync de count contra el JSON real)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de sustantivos-irregulares.json (`data.exercises.length` tras 27-02, via `node -e "console.log(require('./content/exercises/sustantivos-irregulares.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry sustantivos-irregulares en CATEGORIES_WITH_EXPLANATIONS (linea 1269) a ese numero. NO tocar las entries de preposiciones (49), articoli (34), avere (20), essere (26), genero-numero (12), partitivos (19), profesiones (11) ni verbos-movimiento (7). NO tocar la logica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero/Professioni) -> Sostantivi irregolari slot (100% MC) pasa coverage/R1/R2/smart-quotes/markdown automaticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level -> no cambia salvo heredar el count sincronizado (todo slot/variante nuevo de 27-02 ya esta validated por el quorum; los slots movidos conservan su validation fuente). NO tocar los counts de las otras 8 categorias.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de sustantivos-irregulares en CATEGORIES_WITH_EXPLANATIONS == n REAL de slots: `node -e "const d=require('./content/exercises/sustantivos-irregulares.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en linea 1269
    - Las otras 8 categorias siguen verdes con sus counts originales (preposiciones 49, articoli 34, avere 20, essere 26, genero-numero 12, partitivos 19, profesiones 11, verbos-movimiento 7) — no se tocaron
    - La logica del test (getExplanation/getPrompts/bifurcacion) NO se modifico: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Sostantivi irregolari en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-sustantivos intactas; logica del smoke (ya shape-agnostic) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED) y cerrar CONV-01</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (las lineas alrededor de 165-200 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (las lineas alrededor de 64-160 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/sustantivos-irregulares.json (conteo REAL final de slots)
    - .planning/phases/26-professioni-a-slots-contenido-l-xica/26-03-PLAN.md (Task 2 analogo de Professioni — mismo patron de 3 hardcodes + TOTAL_EXPECTED + comentario del historial)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'sustantivos-irregulares', expected: 31 }` (linea 171) a `expected` = n REAL de slots. NO tocar las entries de preposiciones (49), articoli (34), avere (20), essere (26), genero-numero (12), partitivos (19), profesiones (11) ni verbos-movimiento (7). El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 31` de la entry sustantivos-irregulares (linea 152) a n REAL de slots; recalcular `TOTAL_EXPECTED` (linea 156) = `209 - 31 + n_slots_final_sustantivos`; actualizar el comentario explicativo del historial del total (alrededor de lineas 64-155) anadiendo una linea DESPUES de la de Professioni (Phase 26) que registre la conversion de Sostantivi irregolari a slots y el CIERRE de CONV-01: "-> <nuevo> (v1.6 Phase 27, SOST-01, CIERRE CONV-01): Sostantivi irregolari se convirtio al shape slot+variantes HIBRIDO. La reagrupacion fundio 31 ejercicios payload (100% MC) a <count> slots reales: bloque REGLA por sub-regla (D-27-02: sovrabbondanti -o->-a, invariabili-accentate, invariabili-straniere) + bloque LEXICO PURO cambio-radice sin variantes (uomo/dio/bue/tempio, duplicado #008==#025 como 2 variantes) + bloque CONTRASTE plurali-regolari sin engorde (D-27-05). Las 31 superficies fuente se preservaron como variantes (+N superficies nuevas SOLO del bloque regla D-27-03: sovrabbondanti + invariabili acentuadas/extranjeras — variantes, NO slots; el bloque lexico y el contraste no recibieron variantes, SOST-01). TOTAL = 209 - 31 + <count> = <nuevo>. Con esta conversion CONV-01 queda cerrado: 9/9 categorias de gramatica en slot+variantes -> fin de v1.6". Leer el conteo real del JSON, NO usar una estimacion (el net BAJA por la fusion a slot+variantes; pero leer, no asumir). Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO numero.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de sustantivos-irregulares en ambos archivos == n REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/sustantivos-irregulares.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 209 - 31 + n_slots_final_sustantivos (verificable aritmeticamente contra el conteo real; preposiciones sigue contribuyendo 49, articoli 34, avere 20, essere 26, genero-numero 12, partitivos 19, profesiones 11, verbos-movimiento 7)
    - El reporter de run-validation-271 no emite warning `total != esperado` para sustantivos-irregulares
    - El comentario del historial del total en run-validation-271.mjs menciona la conversion de Sostantivi irregolari a slots (incluida la naturaleza HIBRIDA D-27-01, el duplicado #008==#025 como 2 variantes y el CIERRE de CONV-01: 9/9 categorias)
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Sostantivi irregolari (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el n real de slots; TOTAL_EXPECTED recalculado (preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, profesiones=11, verbos-movimiento=7 intactos); reporter PASS sin warnings de count; comentario del historial actualizado con la conversion HIBRIDA de Sostantivi irregolari y el cierre de CONV-01 (9/9 categorias).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido -> smoke parametrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

> Nota: esta fase edita hardcodes de count en tests/scripts de tooling de una app local single-user, offline, sin backend; no introduce nueva superficie de ataque. No se esperan amenazas HIGH/MEDIUM.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Tampering | count desincronizado entre los 3 hardcodes -> gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leido del JSON (`data.exercises.length`), no estimaciones (leccion 19-03/20-03/22-03/23-03/24-03/25-03/26-03). El acceptance asserta que coinciden y que run-validation-271 PASS. El net baja por la fusion a slot+variantes -> leer, no asumir. |
| T-27-03 | Tampering | explanation/prompt de slot con HTML/markdown/smart-quotes pasa sin escanear (rompe T-02-01 x-text) | mitigate | El smoke YA bifurca por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero/Professioni): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots (Sostantivi irregolari es 100% MC -> rama MC) — la cobertura de seguridad de contenido NO se pierde al convertir. |
| T-27-09 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 27-02 paso el quorum (status validated) y cualquier slot nuevo lleva validation top-level (D-19-09) antes de integrarse -> la gate pasa solo si 27-02 cumplio. Si algo quedo disputed o sin validation top-level, la gate FALLA visible. |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. No hay tabla de Package Legitimacy porque no hay installs. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v9 de Phase 21, integration, exercise-types, articoli/partitivos/avere/essere/verbos-movimiento/genero-numero/profesiones de Phase 19/20/22/23/24/25/26). Usar el glob `tests/*.test.js`; el path desnudo falla en Node 22.20 (MEMORY test_command_node_glob).
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs sustantivos-irregulares content/exercises/sustantivos-irregulares.json` exits 0
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de sustantivos-irregulares.json
- (NO se corre ningun assert de snapshot — avere-only, no existe para Sostantivi irregolari)
</verification>

<success_criteria>
- Los 3 hardcodes de count de Sostantivi irregolari + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacia — verificado por el smoke shape-agnostic; Sostantivi irregolari es 100% MC -> rama MC)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); logica del smoke NO tocada (ya shape-agnostic); preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, profesiones=11 y verbos-movimiento=7 intactos
- CONV-01 cerrado: 9/9 categorias de gramatica en formato slot+variantes unificado -> fin del milestone v1.6
</success_criteria>

<output>
Create `.planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-03-SUMMARY.md` when done
</output>
</content>
