---
phase: 26-professioni-a-slots-contenido-l-xica
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - content/exercises/profesiones.json
  - .planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md
autonomous: false
requirements: [PROF-02]
must_haves:
  truths:
    - "Cada variante NUEVA autorada pasa el quorum cross-vendor R1-R7 (base canonica Claude Opus + Sonnet via skill gsd-validate-exercise; refuerzo Gemini + DeepSeek via validate-ai-pass) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot; 1-por-1, fresh context, NUNCA batched (VAL-03)"
    - "Las variantes nuevas se autoran SOLO en los slots de feminizacion del BLOQUE REGLA (D-26-04): (a) contraste -tore/-trice vs -e/-ore->-essa (scrittore/cantautore? -> -trice vs poeta?/eroe? -> -essa — el contraste de sufijos, la trampa A1 estrella); (b) invariables -ista/-ante (artista/autista/regista/turista / comandante/agente) — mata el calco la-dentistessa/la-cantantessa; (c) -o/-a regular y -iere/-iera variedad. El BLOQUE LEXICO PURO (comprension + 3 match) NO recibe NINGUNA variante nueva — documentado como cumplimiento explicito de PROF-01/D-26-02"
    - "Cada palabra asignada a una sub-regla de feminizacion se VERIFICO que toma realmente esa forma femenina en italiano (D-26-04): el quorum cross-vendor caza cualquier feminizacion inventada o sufijo erroneo antes de entrar (NO inventar -trice donde va -essa ni viceversa; NO feminizar una invariable)"
    - "Las variantes nuevas se autoran con ambicion GENEROSA sin cuota fija (D-26-04, hereda D-25-04/D-24-05); ninguna lleva explanation propia (comparten la del slot); cualquier slot NUEVO (recomendado: ninguno) lleva validation.status top-level (D-19-09)"
    - "Las superficies existentes movidas intactas en 26-01 NO se re-validan (D-26-10); ninguna explanation nueva/tocada referencia las categorias Articoli/Essere/Genere por id ni prosa (D-26-05); los 3 match siguen type:match (D-26-02/D-04) y los 5 WB siguen type:word-buttons (D-26-06) sin tocarse"
  artifacts:
    - path: "content/exercises/profesiones.json"
      provides: "variantes nuevas validated integradas en variants[] de los slots de feminizacion, + cualquier slot nuevo con validation top-level"
      contains: "\"variants\""
    - path: ".planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md"
      provides: "set propuesto de superficies nuevas (SOLO feminizacion) con id temporal + slot destino + superficie + justificacion R1-R7 + forma femenina verificada; seccion explicita de que el bloque lexico no recibe variantes (PROF-01)"
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quorum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS con ambicion GENEROSA (PROF-02, D-26-04) donde la regla admite reformulacion natural, materializando los EJES DE HUECOS PRIORIZADOS de Professioni — SOLO en los slots de feminizacion del BLOQUE REGLA (D-26-01/D-26-04). El BLOQUE LEXICO PURO (comprension 039/040 + match lugar/herramienta/accion 200/201/202) NO recibe NINGUNA variante nueva: PROF-01 prohibe forzar variantes artificiales y D-26-02 lo documenta como cumplimiento explicito (la pista del bloque lexico es por significado/campo lexico, no derivable por raiz -> no hay reformulacion natural de "regla con variantes intercambiables").

Ejes de huecos PRIORIZADOS (D-26-04, el autor los marca/afina en el checkpoint), SOLO en feminizacion:

1. CONTRASTE -tore/-trice vs -e/-ore->-essa (engorde de los slots `profesiones-femminile-trice` y `profesiones-femminile-essa`): mas contraste de sufijos de feminizacion — agentes derivados en -tore (scrittore->scrittrice, lettore->lettrice, lavoratore->lavoratrice, pittore — ya en legacy, no duplicar) que toman -trice vs -e/-ore que toman -essa (poeta->poetessa, eroe->eroina? OJO eroe->eroina NO es -essa, verificar; principe->principessa). La trampa A1 estrella: SABER que sufijo toca. VERIFICAR el femenino real de cada palabra (no inventar: scrittore->scrittrice, poeta->poetessa; descartar las que no tengan femenino A1 natural o sean dudosas).

2. INVARIABLES -ista/-ante (engorde del slot `profesiones-invariabili`): mas casos -ista (artista, autista, regista, turista, barista, analista) + -ante/-nte (comandante, agente, dirigente) -> mata el calco "la dentistessa"/"la cantantessa"/"la artistessa". Trampa A1 FUERTE del hispanohablante. VERIFICAR que cada palabra es realmente invariable (-ista/-ante/-nte; solo el articulo distingue il/la).

3. -o/-a REGULAR y -iere/-iera (engorde de los slots `profesiones-femminile-o-a` y `profesiones-femminile-iera`): variedad para que el examen no memorice por palabra (cuoco/impiegato/avvocato ya en legacy — no duplicar; nuevos: maestro->maestra, operaio->operaia, infermiere ya en legacy; nuevos -iere: giardiniere->giardiniera, banchiere->banchiera, pasticciere->pasticciera). VERIFICAR el femenino real.

Cada superficie NUEVA pasa el quorum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched (VAL-03), con revision del autor por variante (D-85). Las superficies movidas intactas en 26-01 NO se re-validan (D-26-10). Sin cuota fija de densidad (D-26-04 hereda D-25-04/D-24-05): proponer variantes donde la regla lo pida, no por cumplir un minimo (evita variantes de relleno). RESTRICCION D-26-05: ninguna explanation nueva/tocada referencia las categorias Articoli/Essere/Genere por id ni prosa (el articulo/essere/plural como contenido del ejercicio es valido). NO autorar variantes en el bloque lexico (PROF-01: comprension + 3 match quedan como estan). NO tocar los 3 match (D-26-02/D-04) ni los 5 word-buttons (D-26-06) de 26-01.

El planner/autor decide en plan-time/checkpoint si los huecos materializan VARIANTES de slots existentes (lo natural — los ejes engordan los slots de feminizacion de 26-01) o algun SLOT NUEVO. Recomendacion del planner: NO se necesitan slots nuevos — los ejes engordan los slots existentes (femminile-trice, femminile-essa, invariabili, femminile-o-a, femminile-iera). Si el autor crea algun slot nuevo, este requiere validation.status top-level (D-19-09).

Purpose: las variantes nuevas materializan el dolor anti-memorizacion (re-hacer un slot puede tocar una palabra distinta) y cierran los huecos pedagogicos de la feminizacion de Professioni, todos centrados en las trampas A1 estrella del hispanohablante: el contraste de sufijos -tore/-trice vs -e/-essa, y el calco la-dentistessa en invariables. El quorum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazo 8 bugs en Articoli, 3 en Avere, 2 en Essere que el human-verify aprobo) y la unica garantia de que cada palabra toma realmente la forma femenina reclamada (D-26-04 — saber si va -trice o -essa es la trampa central). R1/R5 criticos: el prompt no debe leak la forma correcta; las options deben tener 3+ valores distintos (distractoras = feminizacion erronea plausible, p.ej. cruzar -essa/-trice o el calco -istessa).

PROF-02 SE CUMPLE ASI: hay autoria de variantes donde hay regla (feminizacion); se documenta explicitamente en 26-VARIANTES-NUEVAS.md que el bloque lexico NO admite autoria de variantes (cumplimiento de PROF-01).

NO existe snapshot/assert APPEND-ONLY para Professioni (avere-only, verificado: 0 refs activas; assert-multi-cat-cross.mjs:19 es comentario de ejemplo). NO ejecutar ningun script de snapshot ni assert de prefijo. NO existen cruces multi-cat (no hay profesiones-300..305); NO crearlos. A diferencia de 22-02 (que re-basaba el snapshot de Avere), aqui NO hay paso de re-base — igual que 23-02/24-02/25-02.

Output: `profesiones.json` con las variantes nuevas de los ejes de feminizacion integradas; cada superficie nueva con su quorum verde en `validation.passes[]`; cualquier slot nuevo con validation.status top-level (D-19-09); el bloque lexico intacto (PROF-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/profesiones.json

<interfaces>
<!-- Pipeline de quorum cross-vendor. Verificado en validate-ai-pass.mjs + skill. NO modificar estos scripts/skill. Mismo flujo ejecutado con exito en Phase 19 (8 superficies), Phase 20 (6), Phase 22 (14), Phase 23 (14), Phase 24 y Phase 25 (20), 0 excluidas. -->
Quorum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Base canonica (D-26-11) — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus->Sonnet):
  /gsd-validate-exercise <id>
  (model IDs literales que emite el skill: claude-opus-4-7 + claude-sonnet-4-6; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio. Mantener los IDs literales del skill por consistencia del audit trail con el contenido existente — confirmado en Phase 23 Open Q #1.)
  Fallback cuando Task no esta disponible al executor (precedente Phase 19/20/22/23/24/25, D-19-08): la mitad Claude se corre con `claude -p --model claude-opus-4-7` y `--model claude-sonnet-4-6` headless, 1 proceso por modelo por superficie, con el 09-VALIDATION-PROMPT verbatim y el ejercicio SIN su campo validation (no sesgar). Equivalente funcional 1-por-1 NUNCA batched.

Refuerzo — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondio; >=2 by distintos para el quorum; status re-derivado por deriveStatus, NO hand-editar status. Si Gemini agota cuota 429/503, DeepSeek cubre los 2 externos con by distintos chat + reasoner — precedente 22-VERIFICATION / 23-02 / 24-02 / 25-02.)

Gate D-17-07: el passes[] final de cada superficie nueva = >=4x "correcta" (opus + sonnet + 2 externos by distintos), CERO "incorrecta". deriveStatus -> "validated"; cualquier "incorrecta" -> "disputed" (sticky, bloquea, NO entra al slot).

Pitfall: el VALIDATION-PROMPT es legacy-payload-centrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/profesiones.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quorum, mover la superficie al variants[] del slot (sin re-validacion) y eliminar el id temporal legacy.

Alcance D-26-04 (ambicion generosa, hereda D-25-04/D-24-05) — SOLO feminizacion, sin cuota fija:
- Engordar las celdas pobres marcadas en 26-01 + materializar los ejes de huecos de FEMINIZACION. NO inflar slots cuya regla no admite reformulacion natural; sin minimo por slot. NO autorar NADA en el bloque lexico (comprension + 3 match) ni en articolo-suono ni en word-buttons (D-26-02/D-26-05/D-26-06: esos slots quedan como en 26-01; solo feminizacion recibe variantes nuevas).

Los ejes de huecos PRIORIZADOS (D-26-04, R1/R5 criticos, VERIFICAR la forma femenina real de cada palabra):
1. CONTRASTE -tore/-trice vs -e/-essa (slots femminile-trice y femminile-essa): -tore->-trice (scrittore->scrittrice, lettore->lettrice, lavoratore->lavoratrice) vs -e/-ore->-essa (poeta->poetessa, principe->principessa). El prompt fija el masculino; respuesta = el femenino; distractoras = feminizacion cruzada (-essa donde va -trice y viceversa, o -a generico). VERIFICAR que cada palabra toma realmente su sufijo (NO scrittoressa, NO poetatrice). NO duplicar las del legacy (attore/direttore/programmatore/allenatore/pittore/traduttore/ricercatore en -trice; dottore/professore/studente en -essa).
2. INVARIABLES -ista/-ante (slot invariabili): artista, autista, regista, turista, barista, comandante, agente, dirigente. Prompt fija el masculino (il artista); respuesta = la MISMA forma (la artista); distractoras = el calco -istessa/-antessa (artistessa, cantantessa) + feminizacion inventada (artista->artisto?). VERIFICAR (R1) que el prompt no leak la respuesta; (R5) options con 3+ valores distintos; que cada palabra es realmente invariable. NO duplicar las del legacy (cantante/insegnante/collega/cliente/dentista/pianista/farmacista/giornalista/tassista/pilota/manager).
3. -o/-a REGULAR y -iere/-iera (slots femminile-o-a y femminile-iera): maestro->maestra, operaio->operaia (-o->-a); giardiniere->giardiniera, banchiere->banchiera, pasticciere->pasticciera (-iere->-iera). Distractoras = -essa/-trice mal aplicadas (no son -tore ni -e) + plural fem. VERIFICAR el femenino real. NO duplicar las del legacy.
- El planner/autor decide en plan-time/checkpoint que huecos son naturales. Si un hueco no tiene femenino A1 natural (dudoso/literario) o la palabra es ambigua, NO se materializa.

Validation top-level para la gate (D-19-09): si el autor crea algun slot NUEVO, debe portar el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum cross-vendor de sus variantes (el gate VAL_07_STRICT lee `ex.validation?.status` a nivel de slot). Si todos los huecos engordan slots existentes (recomendado), esos slots ya tienen su validation top-level de 26-01 (de las superficies fuente movidas) — NO degradarla; el passes[] de las variantes nuevas se anade donde el flujo lo registre, pero la gate de slot ya esta cubierta. NO inventar validacion: elevar la aprobada.

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quorum, un scan ASCII/tildes (italiano: acentos donde aplique; espanol RAE en explanations; apostrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial.
Gloss ES (R7 canon): mantener "(en espanol: ...)" donde desambigue doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de politica (base de aprobacion = Claude Opus+Sonnet, D-26-11).
D-26-05: ninguna explanation nueva/tocada referencia las categorias Articoli/Essere/Genere por id ni prosa; el articulo/essere/plural como contenido del ejercicio es valido.

BLOQUE LEXICO INTACTO (PROF-01/D-26-02): la comprension (039/040) y los 3 match (200/201/202) NO reciben variantes nuevas; documentar en 26-VARIANTES-NUEVAS.md una seccion explicita "El bloque lexico puro no admite autoria de variantes (PROF-01) — solo se autoran variantes en feminizacion".

NO snapshot: Professioni no tiene snapshot/assert APPEND-ONLY (avere-only, verificado). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs ni buscar .profesiones-prefix-snapshot.json. NO crear cruces 300..305 (no existen; fuera de scope). NO tocar los word-buttons ni los match de 26-01. A diferencia de 22-02 NO hay re-base de snapshot aqui.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (SOLO feminizacion, los ejes de huecos D-26-04) con forma femenina verificada</name>
  <files>.planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/profesiones.json (estado tras 26-01 — los slots ya reagrupados HIBRIDO, para saber que slots de feminizacion existen, cuales son celdas pobres y que admite variante nueva; y para NO duplicar palabras del legacy)
    - .planning/phases/26-professioni-a-slots-contenido-l-xica/26-REAGRUPACION-MAP.md (las celdas pobres del bloque regla marcadas como candidatas + los ejes de huecos anotados en 26-01 + la seccion de la decision hibrida)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints del quorum: 1-por-1, fresh context, NUNCA batched; model IDs literales)
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-02-PLAN.md (Task 1 analogo — formato de 25-VARIANTES-NUEVAS.md a espejar) y .planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md (la plantilla concreta, si existe; si no, 23-VARIANTES-NUEVAS.md)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6/R7 si accesible) y gloss_es_desambiguacion_canon.md (gloss ES canon)
  </read_first>
  <action>
    Proponer un set de superficies nuevas con ambicion GENEROSA (D-26-04) pero sin cuota fija, materializando los EJES DE HUECOS PRIORIZADOS SOLO de feminizacion: (1) CONTRASTE -tore/-trice vs -e/-essa para los slots femminile-trice y femminile-essa: -tore->-trice (scrittore->scrittrice, lettore->lettrice, lavoratore->lavoratrice) vs -e/-ore->-essa (poeta->poetessa, principe->principessa); prompt fija el masculino, respuesta = el femenino, distractoras = feminizacion cruzada (-essa donde va -trice y viceversa); VERIFICAR que cada palabra toma realmente su sufijo (no scrittoressa, no poetatrice). (2) INVARIABLES -ista/-ante para el slot invariabili: artista/autista/regista/turista/barista (-ista) + comandante/agente/dirigente (-ante/-nte); prompt fija el masculino, respuesta = la MISMA forma, distractoras = el calco -istessa/-antessa + feminizacion inventada; VERIFICAR que la palabra es realmente invariable. (3) -o/-a REGULAR y -iere/-iera para los slots femminile-o-a y femminile-iera: maestro->maestra, operaio->operaia (-o); giardiniere->giardiniera, banchiere->banchiera, pasticciere->pasticciera (-iere); VERIFICAR el femenino real. VERIFICAR explicitamente por superficie: (R1) el prompt no leak la forma correcta; (R5) las options tienen 3+ valores distintos; que la palabra toma realmente el femenino reclamado en italiano A1 (D-26-04 — no inventar feminizaciones; descartar las dudosas/literarias). Cumplir R1-R7 (sin leak en prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigue doble-validez). RESTRICCION D-26-05: ninguna explanation referencia las categorias Articoli/Essere/Genere por id ni prosa; el articulo/essere/plural como contenido del ejercicio es valido. NO PROPONER NINGUNA variante para el bloque lexico (comprension 039/040 + match 200/201/202) ni para articolo-suono ni para word-buttons — esos slots quedan como en 26-01 (D-26-02/PROF-01). Pasar un scan de acentos/ASCII (tildes italianas/espanolas donde apliquen; apostrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. NO duplicar palabras ya presentes en el legacy (verificar contra el JSON tras 26-01). Producir `.planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md` (espejar el formato de 25-VARIANTES-NUEVAS.md): lista de cada superficie nueva con su id temporal (para el quorum aislado), slot destino (existente — recomendado que los ejes engorden los slots de feminizacion de 26-01; marcar cualquier slot NUEVO si el autor lo pide), eje al que pertenece (1-3), prompt/options/correctIndex/explanation, la justificacion de por que la regla admite reformulacion, y la VERIFICACION de la forma femenina real de la palabra. Incluir una SECCION EXPLICITA "Bloque lexico puro: SIN autoria de variantes (PROF-01/D-26-02)" que documente que la comprension y los 3 match no reciben variantes nuevas (cumplimiento explicito de PROF-01). Recomendar en una seccion que NO se necesitan slots nuevos (los ejes engordan los slots existentes) — surfacear al autor. Reportar el conteo de superficies nuevas propuestas por eje Y cuantas (si alguna) son SLOTS NUEVOS (los slots nuevos suben el count de 26-03; las variantes no).
  </action>
  <verify>
    <automated>test -f .planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md && grep -ci "trice\|essa\|invariab\|ista\|iera\|o-a\|lexic\|PROF-01" .planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 26-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + eje (1-3) + superficie + explanation
    - EJE 1 (contraste -tore/-trice vs -e/-essa) cubierto: variantes -tore->-trice (scrittore/lettore/lavoratore...) y -e/-essa (poeta/principe...) para sus dos slots, con la verificacion del sufijo real (no cruzados)
    - EJE 2 (invariables -ista/-ante) cubierto: variantes -ista (artista/autista/regista/turista...) y -ante/-nte (comandante/agente...) para el slot invariabili, cada una con la verificacion de invariabilidad y con la distractora del calco -istessa/-antessa
    - EJE 3 (-o/-a regular y -iere/-iera) cubierto: variantes regulares para los slots femminile-o-a y femminile-iera con el femenino verificado
    - Hay una SECCION EXPLICITA "Bloque lexico puro: SIN autoria de variantes (PROF-01/D-26-02)" que documenta que comprension + 3 match no reciben variantes nuevas
    - Cada superficie nueva cumple R1 (sin leak de la forma correcta en el prompt) y R5 (options con 3+ valores distintos); cada explanation/prompt pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers; ninguna referencia las categorias Articoli/Essere/Genero por id/prosa (D-26-05)
    - Cada palabra lleva su VERIFICACION de forma femenina real (D-26-04) — no se inventan feminizaciones; ninguna palabra duplica una ya presente en el legacy tras 26-01
    - Hay una seccion que recomienda NO crear slots nuevos (los ejes engordan los slots existentes), surfaceada al autor
    - El conteo de superficies nuevas propuestas por eje Y de slots nuevos (si alguno) esta reportado
  </acceptance_criteria>
  <done>
    26-VARIANTES-NUEVAS.md lista el set generoso de superficies nuevas de los 3 ejes de feminizacion D-26-04 (contraste -trice/-essa + invariables -ista/-ante + -o/-a y -iere/-iera) con superficies R1-R7-conformes, scan de acentos pasado, sin refs a Articoli/Essere/Genero, sin duplicar palabras del legacy, y la verificacion de la forma femenina real de cada palabra; seccion explicita de que el bloque lexico NO recibe variantes (PROF-01/D-26-02); recomendacion de no crear slots nuevos surfaceada; conteo de superficies por eje y de slots nuevos reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (26-VARIANTES-NUEVAS.md) — SOLO los ejes de feminizacion D-26-04: contraste -tore/-trice vs -e/-essa (scrittore->scrittrice vs poeta->poetessa), invariables -ista/-ante (artista/regista/comandante), -o/-a regular y -iere/-iera (maestro->maestra, giardiniere->giardiniera) — cumpliendo R1-R7, con scan de acentos y la verificacion de la forma femenina real de cada palabra. El bloque lexico puro (comprension + 3 match) NO recibe variantes (PROF-01). AUN NO validadas por quorum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md`.
    2. EJE 1 (contraste de sufijos): scrittore->scrittrice, lettore->lettrice, lavoratore->lavoratrice (-tore->-trice) vs poeta->poetessa, principe->principessa (-e/-essa). Confirma que cada palabra toma su sufijo correcto (NO scrittoressa, NO poetatrice). Las distractoras cruzan -essa/-trice? El prompt NO contiene la respuesta (R1)?
    3. EJE 2 (invariables): artista/autista/regista/turista (-ista) y comandante/agente/dirigente (-ante/-nte) son realmente invariables en genero? La respuesta = la misma forma? Las options incluyen el calco -istessa/-antessa (artistessa/comandantessa) como distractora (R5)?
    4. EJE 3 (-o/-a y -iere): maestro->maestra, operaio->operaia, giardiniere->giardiniera, banchiere->banchiera. Femenino correcto?
    5. Confirma que NINGUNA palabra duplica una ya presente en el legacy (cuoco/avvocato/attore/dottore/dentista... ya estan).
    6. Confirma que NINGUNA explanation referencia las categorias Articoli/Essere/Genero por id ni prosa (D-26-05) — el articulo/essere/plural como contenido del ejercicio SI es valido.
    7. Confirma que el bloque lexico puro (comprension 039/040 + match lugar/herramienta/accion) NO recibe variantes nuevas (PROF-01) — la seccion explicita esta presente.
    8. Confirma el alcance: generoso pero sin variantes artificiales en reglas que no se reformulan (D-26-04); decide si algun eje crea un slot nuevo (recomendado: NO, todos engordan slots existentes).
    9. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quorum (especialmente cualquier feminizacion cuyo sufijo dudes — el contraste -trice/-essa es la trampa central; y cualquier invariable de la que no estes seguro).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las superficies aprobadas al quorum cross-vendor, o lista las que descartar/reformular (y cualquier palabra cuyo femenino quieras que el quorum confirme con prioridad — especialmente el contraste -trice/-essa del eje 1 y las invariables del eje 2).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quorum cross-vendor (1-por-1) e integrarla al slot de feminizacion</name>
  <files>content/exercises/profesiones.json</files>
  <read_first>
    - .planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales claude-opus-4-7/claude-sonnet-4-6, 1 commit por ejercicio; fallback `claude -p` headless si no hay Task, D-19-08)
    - content/exercises/profesiones.json (slots de feminizacion destino donde integrar las superficies tras pasar el quorum; NO tocar los slots lexicos/match/wb/articolo-suono)
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-02-PLAN.md (Task 2 analogo — el flujo real: superficies por eje, disputes resueltas por reformulacion, fallback DeepSeek reasoner cuando Gemini 429) y .planning/phases/23-essere-a-slots-contenido/23-02-PLAN.md (precedente con slot nuevo + validation top-level)
  </read_first>
  <action>
    Para CADA superficie nueva aprobada (SOLO feminizacion), ejecutar el ciclo completo de quorum 1-por-1 (NUNCA batched, VAL-03): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/profesiones.json como entry temporal o tests/fixtures/*.json). (2) Base canonica Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus->Sonnet secuencial, 1 spawn; model IDs literales del skill claude-opus-4-7/claude-sonnet-4-6); fallback `claude -p --model claude-opus-4-7` / `--model claude-sonnet-4-6` headless con el ejercicio SIN su campo validation si no hay Task (D-19-08). (3) Refuerzo externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write` (si Gemini agota cuota 429/503, usar deepseek-chat + deepseek-reasoner como 2 by externos distintos, precedente 22-VERIFICATION / 23-02 / 24-02 / 25-02). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4x "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` -> la superficie queda disputed y se resuelve por REFORMULACION (calidad > tokens, NO override-atajo; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de politica; si un externo alucina un acento sobre texto correcto, re-run sobre el MISMO texto, precedente Phase 19/22/23/25) o se descarta — NO entra al slot disputed. PRESTAR ESPECIAL ATENCION (D-26-04) a que el quorum confirme la FORMA FEMENINA REAL: si una superficie de -tore/-essa resulta `incorrecta` por usar el sufijo cruzado (scrittoressa en vez de scrittrice, poetatrice en vez de poetessa), reformular o descartar; idem si una invariable usa el calco -istessa como respuesta correcta por error, o una -o/-a aplica un sufijo prestigioso indebido. (5) Tras pasar el quorum, mover la superficie validada al variants[] del slot destino (existente, para los ejes: femminile-trice, femminile-essa, invariabili, femminile-o-a, femminile-iera) o crear el slot NUEVO `{id, type:"multiple-choice", categoryIds:["profesiones"], explanation, variants:[...], validation}` SOLO si el autor lo aprobo; eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Para cualquier slot NUEVO, anadir el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum aprobado de sus variantes (D-19-09). Para los slots EXISTENTES engordados, NO degradar su validation top-level de 26-01 (ya cubre la gate de slot). NO re-validar las superficies movidas intactas en 26-01 (D-26-10). RESTRICCION D-26-05: ninguna explanation tocada referencia las categorias Articoli/Essere/Genero por id ni prosa. NO autorar NI tocar NADA en el bloque lexico (comprension 039/040 + match 200/201/202), en articolo-suono ni en word-buttons (PROF-01/D-26-02/D-26-05/D-26-06: esos slots quedan exactamente como en 26-01). Hacer 1 commit por superficie validada. NUNCA validar mas de una superficie en un mismo contexto/spawn (VAL-03). NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Professioni; avere-only). NO crear cruces 300..305. NO convertir ningun match a MC ni ningun WB a MC (los de 26-01 se preservan intactos).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs profesiones content/exercises/profesiones.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs profesiones content/exercises/profesiones.json` exits 0
    - Los slots de feminizacion engordaron: femminile-trice, femminile-essa, invariabili y/o femminile-o-a/femminile-iera tienen mas variantes que tras 26-01 (verificable contando variants[] de cada slot)
    - El eje 1 incluye el contraste -trice/-essa como variantes validadas: existe al menos una -tore->-trice nueva (p.ej. scrittrice) en el slot femminile-trice y al menos una -e/-essa nueva (p.ej. poetessa) en el slot femminile-essa: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); const t=d.exercises.find(e=>/trice/.test(e.id)); const s=d.exercises.find(e=>/essa/.test(e.id)); console.log('trice variants:', t?t.variants.length:0, 'essa variants:', s?s.variants.length:0)"` ambos > el conteo de 26-01
    - Cada superficie NUEVA paso el quorum con >=4x "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se valido como legacy)
    - Todo slot con variants[] sigue con validation.status=="validated" top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); console.log('slots variants sin validation top-level:', d.exercises.filter(e=>Array.isArray(e.variants)&&e.validation?.status!=='validated').length)"` imprime `0`
    - Ningun id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 26-01 NO se re-validaron (# invocaciones de quorum == # superficies nuevas)
    - El bloque lexico esta INTACTO (PROF-01): los 3 match siguen type:match y NO se convirtieron a MC ni recibieron variantes nuevas: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); console.log('match slots:', d.exercises.filter(e=>e.type==='match').length)"` imprime el mismo numero que tras 26-01 (>=1); idem el slot de comprension no engordo
    - Los word-buttons siguen type:word-buttons y no se tocaron (D-26-06): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); console.log('wb slots:', d.exercises.filter(e=>e.type==='word-buttons').length)"` imprime el mismo numero que tras 26-01
    - NO se crearon cruces: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); console.log('cruces:', d.exercises.filter(e=>/-30[0-5]$/.test(e.id)).length)"` imprime `cruces: 0`; todos los slots categoryIds=["profesiones"]
    - Ninguna explanation referencia las categorias Articoli/Essere/Genero por id/prosa: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); console.log('refs cross-cat:', d.exercises.filter(e=>/articoli-\d|essere-\d|genero-numero-\d|ver Articoli|ver Essere|ver G[eé]nero/i.test(e.explanation||'')).length)"` imprime `0` (D-26-05)
    - El disputed->override del autor de collega (020) sigue presente verbatim en el slot invariabili (no se degrado al engordar): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/profesiones.json','utf8')); console.log('override autor presente:', d.exercises.some(e=>JSON.stringify(e.validation||{}).includes('override')&&JSON.stringify(e.validation||{}).includes('autor')))"` imprime `true`
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/profesiones.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Professioni
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas de los 3 ejes de feminizacion D-26-04 pasaron el quorum cross-vendor (>=4x correcta) e integradas a sus slots de feminizacion (contraste -trice/-essa + invariables -ista/-ante + -o/-a y -iere/-iera); forma femenina de cada palabra confirmada por el quorum (D-26-04); el bloque lexico (comprension + 3 match) INTACTO sin variantes nuevas (PROF-01/D-26-02); los 5 word-buttons intactos (D-26-06); validateContent verde; sin ids temporales residuales; sin cruces; sin refs a Articoli/Essere/Genero; disputed->override de collega preservado; 1 commit por superficie; sin snapshot (no aplica a Professioni).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio -> evaluador del quorum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podria intentar dirigir al evaluador |
| JSON de contenido editado a mano -> loader/validator (x-text, nunca x-html, T-02-01) | superficies nuevas redactadas a mano cruzan a validateContent y al render como texto plano |

> Nota: esta fase autora contenido JSON en una app local single-user, offline, sin backend; el unico acceso externo son las API keys de `.env` (leidas, no instaladas) para el quorum. Sin red entrante, auth, ni input de terceros. No se esperan amenazas HIGH/MEDIUM.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering | superficie nueva con forma femenina incorrecta (sufijo cruzado scrittoressa/poetatrice, calco -istessa como respuesta correcta en invariable, feminizacion inventada de una invariable), respuesta ambigua (doble-validez R7) o acento incorrecto entra al slot | mitigate | Quorum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); verificacion de la forma femenina real en Task 1 + checkpoint (D-26-04, no inventar); atencion especial al contraste de sufijos -tore/-trice vs -e/-essa (la trampa central); pre-scan de acentos; gate de >=4x "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-26-08 | Tampering / governance | se autoran variantes artificiales en el bloque lexico puro (comprension/match), violando PROF-01/D-26-02 | mitigate | Task 1 + checkpoint solo proponen variantes en feminizacion; seccion explicita "bloque lexico sin variantes"; acceptance de Task 2 asserta que los match siguen type:match con el mismo conteo y que la comprension no engordo. |
| T-26-09 | Tampering | prompt-injection en el quorum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instruccion; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-26-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes (rompe T-02-01 x-text); leak de la forma correcta en el prompt (R1) | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown); verificacion R1 (sin leak) en Task 1 + checkpoint; el smoke de 26-03 re-asserta sobre variants[].prompt + explanation. Todo string permanece plain text -> x-text seguro. |
| T-26-05 | Information disclosure / governance | una explanation nueva referencia las CATEGORIAS Articoli/Essere/Genero por id o prosa, violando D-26-05 | mitigate | Verificacion en Task 1 + checkpoint; acceptance de Task 2 asserta 0 refs a `articoli-`/`essere-`/`genero-numero-` ni "ver X"; el articulo/essere/plural como contenido del ejercicio es valido. |
| T-26-06 | Tampering | el disputed->override del autor de collega (020) se degrada al engordar el slot invariabili | mitigate | NO se re-valida ni degrada la validation top-level de 26-01; acceptance asserta que el override sigue presente tras 26-02. |
| T-26-10 | Tampering | slot NUEVO (si lo hubiera) sin validation.status top-level cuela como `status: absent` en VAL_07_STRICT | mitigate | Cualquier slot NUEVO porta el bloque `validation` top-level reflejando el quorum aprobado (D-19-09); acceptance asserta `slots variants sin validation top-level == 0`. Si todos los ejes engordan slots existentes, su validation de 26-01 ya cubre la gate. |
| T-26-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. No hay tabla de Package Legitimacy porque no hay installs. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs profesiones content/exercises/profesiones.json` exits 0
- Cada superficie nueva: passes[] con >=4x "correcta", cero "incorrecta" (registrado en SUMMARY); forma femenina de cada palabra confirmada por el quorum (D-26-04)
- Los ejes de feminizacion D-26-04 materializados (contraste -trice/-essa, invariables -ista/-ante, -o/-a y -iere/-iera); bloque lexico INTACTO (PROF-01); 3 match siguen type:match (D-26-02/D-04); 5 WB intactos (D-26-06); sin cruces; sin refs a Articoli/Essere/Genero (D-26-05)
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Professioni)
- `node --test tests/*.test.js` — el count de Professioni sigue rojo a proposito hasta 26-03 (el conteo final ya es determinable tras este plan; usar el glob `tests/*.test.js`, path desnudo falla en Node 22.20)
</verification>

<success_criteria>
- Variantes nuevas autoradas con ambicion generosa donde la regla admite reformulacion (D-26-04, sin cuota fija), SOLO en feminizacion: contraste -tore/-trice vs -e/-essa + invariables -ista/-ante + -o/-a y -iere/-iera
- Cada palabra verificada por el quorum como forma femenina real (D-26-04, no se inventan feminizaciones)
- Cada superficie nueva paso el quorum cross-vendor R1-R7 (>=4x correcta) antes de entrar (base canonica Opus+Sonnet, D-26-11)
- Bloque lexico puro (comprension + 3 match) SIN variantes nuevas (PROF-01/D-26-02) — PROF-02 cumplido: autoria donde hay regla, documentado que el lexico no la admite
- Superficies existentes movidas NO re-validadas; 3 match siguen type:match (D-26-02/D-04); 5 WB intactos (D-26-06); ninguna explanation referencia Articoli/Essere/Genero (D-26-05)
- validateContent verde; conteo final de slots determinable para 26-03; sin snapshot (no aplica a Professioni)
</success_criteria>

<output>
Create `.planning/phases/26-professioni-a-slots-contenido-l-xica/26-02-SUMMARY.md` when done
</output>
</content>
