---
phase: 23-essere-a-slots-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/essere.json
  - .planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md
autonomous: false
requirements: [ESS-01]
must_haves:
  truths:
    - "Los 39 ejercicios de Essere quedan reagrupados en slots por regla: presente indicativo por persona (sono/sei/e/siamo/siete/sono como slots por forma, D-23-02; io-sono 001 y loro-sono 007 como 2 slots por persona segun la nota de discrecion); identidad/nacionalidad/profesion/estado/copula agrupados por regla (D-23-01, pocos slots, agrupar lo facil); explanation a nivel de slot (merge D-23-10)"
    - "El passato prossimo essere-026..029 (stato/stata/stati/state) queda en 4 SLOTS SEPARADOS por forma de concordancia (D-23-03, drillear lo dificil), NO en un unico slot con 4 variantes; la explanation de cada uno enfatiza la regla de concordancia genero/numero del participio de essere"
    - "Los 4 word-buttons (essere-100..103) quedan como slots-de-1 type:word-buttons sin variantes forzadas (D-23-12); Essere NO tiene match en el set legacy y no se inventa"
    - "Los 6 cruces multi-cat (essere-300..305) quedan slots-de-1 con id ESTABLE legacy intacto y sus categoryIds de 2 ids preservados (D-23-09); la cascada D-54 sigue intacta (no se renumera ningun cruce)"
    - "Cada slot tiene una explanation top-level no vacia (merge elegir-la-mas-completa + injertar matices, D-23-10); ninguna variante lleva explanation propia; payload eliminado de todos los ejercicios; el patron distractoras essere/avere se preserva donde aplica; ids semanticos para slots essere-only RESERVANDO el rango essere-300..305"
    - "Las superficies existentes movidas intactas a variants[] NO se re-validan (D-23-11, cosmetico); los validation.passes[] se mueven verbatim; el JSON sigue pasando validateContent"
    - "NO existe ni se necesita snapshot/assert APPEND-ONLY para Essere (los scripts snapshot-/assert-avere-prefix son avere-only, hardcoded a avere.json); NO se replica la tarea de re-base D-88 de 22-01"
  artifacts:
    - path: "content/exercises/essere.json"
      provides: "Essere reagrupado a shape slot+variantes (payload eliminado, variants[]+explanation top-level por slot)"
      contains: "\"variants\""
    - path: ".planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md"
      provides: "Mapa id-fuente -> slot revisado por el autor (artefacto de auditoria); cubre los 39 ids 1:1; reserva el rango 300..305; documenta los 4 slots separados del passato"
  key_links:
    - from: "content/exercises/essere.json"
      to: "validateContent / normalizeExerciseToSlot"
      via: "shape slot+variantes (payload XOR variants[])"
      pattern: "\"variants\"\\s*:\\s*\\["
    - from: "los 6 cruces essere-300..305"
      to: "cobertura de avere / profesiones / verbos-movimiento / genero-numero / sustantivos-irregulares / preposiciones (clearedExerciseIds + cascada D-54)"
      via: "id estable + categoryIds de 2 ids preservados"
      pattern: "\"essere-30[0-5]\""
---

<objective>
Reagrupar los 39 ejercicios reales de Essere en slots por regla replicando el patron del plan 22-01 (Avere) / 19-01 (Articoli) / 20-01 (Partitivi). Reglas propias de Essere: presente indicativo por persona (sono io 001, sei 002, e 003+004, siamo 005, siete 006, sono loro 007 — un slot por persona/forma siguiendo la granularidad D-23-02; io-sono y loro-sono comparten la forma `sono` pero son 2 slots distintos por persona, espiritu literal de D-23-02, ver nota de discrecion); identidad/nacionalidad/profesion/estado/copula agrupados por REGLA (D-23-01, agrupar lo facil, pocos slots: ~5-6 slots candidatos — identidad/parentesco 009/010/011, nacionalidad+concordancia+di-origen 013/014/015, profesion/rol sin articulo 012/016/017/018/019, estado/emocion 008/020/021/022/023, copula clasificatoria 024/025); passato prossimo participio (essere-026..029) en 4 SLOTS SEPARADOS por concordancia (D-23-03 — stato masc sing, stata fem sing, stati masc pl, state fem pl — drillear lo dificil; la concordancia del participio de essere con el sujeto es la regla distintiva frente a Avere); 4 word-buttons (100..103) como slots-de-1 type:word-buttons (D-23-12); 6 cruces (essere-300..305) como slots-de-1 con id ESTABLE + categoryIds de 2 ids (D-23-09). Producir la explanation de cada slot via elegir-la-mas-completa + injertar matices (D-23-10), preservando el patron distractoras essere/avere donde aplica. Las superficies existentes se MUEVEN intactas a `variants[]` (cambio de contenedor -> NO re-validacion, D-23-11; los validation.passes[] se mueven verbatim). Renumeracion de ids essere-only LIBRE RESERVANDO el rango 300..305 (D-23-09 + esquema semantico: `essere-sono`, `essere-sei`, `essere-e`, `essere-passato-prossimo-stato`, etc.).

DIVERGENCIA CRITICA #1 frente a Avere: el passato prossimo NO es 1 slot con 4 variantes (como fue Avere) — son 4 SLOTS SEPARADOS por forma de concordancia (D-23-03). Un mapa con un solo slot `essere-passato-prossimo` es un ERROR de replica ciega; D-23-03 lo prohibe explicitamente.

DIVERGENCIA CRITICA #2 frente a Avere: Essere NO tiene snapshot/assert APPEND-ONLY. Los scripts `snapshot-avere-prefix.mjs` / `assert-avere-prefix-unchanged.mjs` / `.avere-prefix-snapshot.json` estan hardcodeados a `content/exercises/avere.json` y NO leen essere.json (verificado: 0 refs a essere). NO existe `.essere-prefix-snapshot.json`. Este plan OMITE toda la columna de snapshot/re-base D-88 que tenia 22-01. NO ejecutar ningun script de snapshot ni assert de prefijo. Documentar explicitamente "Essere no tiene snapshot — no aplica re-base D-88" en el SUMMARY para que no se copie a ciegas.

Este plan NO autora variantes nuevas (eso es 23-02) ni anade slots de huecos de regla (eso es 23-02 — nacionalidad concordancia, localizacion, slot ser/estar D-23-07). NO toca los counts de tests/scripts (eso es 23-03, que sincroniza contra el conteo REAL final tras 23-02).

Purpose: es la columna vertebral de la fase — convierte el contenido real de Essere al modelo slot+variantes que el motor de Phase 16 ya consume. Segunda de las 3 categorias de verbos de v1.6 (Avere completada en Phase 22).

Output: `essere.json` reescrito a slot+variantes (sin payload, con variants[]+explanation top-level por slot), validando verde; mapa de reagrupacion auditado por el autor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/essere.json
@content/exercises/avere.json

<interfaces>
<!-- Shape de slot con variantes que enforza el validator. NO modificar loader ni validator: ya aceptan variants[] desde Phase 15. avere.json (20 slots, todos validated) es la referencia AFTER exacta a espejar (categoria hermana, ya convertida en Phase 22). articoli.json (34 slots) es referencia secundaria de agrupacion multi-semantica. -->
Shape autorado de un slot con variantes (verificado contra avere.json/articoli.json):
- `id`: string id de slot (= exercise id). Esquema = renumeracion semantica limpia para slots essere-only (p.ej. `essere-sono`, `essere-sei`, `essere-e`, `essere-siamo`, `essere-siete`, `essere-sono-loro`, `essere-identidad`, `essere-nacionalidad`, `essere-profesion`, `essere-stato`, `essere-copula`, `essere-passato-prossimo-stato`, `essere-passato-prossimo-stata`, `essere-passato-prossimo-stati`, `essere-passato-prossimo-state`, `essere-wb-identidad`, etc.); RESERVAR/excluir el rango `essere-300..305` para no colisionar con los cruces (D-23-09). Los 6 cruces CONSERVAN su id legacy intacto (essere-300..305). Unicidad obligatoria.
- `type`: "multiple-choice" | "word-buttons" a NIVEL de slot (los slots essere-only MC; 100..103 word-buttons; cruces MC). Essere NO tiene match (no se inventa).
- `categoryIds`: array no vacio a nivel de slot. `["essere"]` para todos los slots essere-only. Los 6 cruces conservan SUS DOS ids: `["essere","avere"]` (300), `["essere","profesiones"]` (301), `["essere","verbos-movimiento"]` (302), `["essere","genero-numero"]` (303), `["essere","sustantivos-irregulares"]` (304), `["essere","preposiciones"]` (305). [VERIFIED: leidos del JSON]
- `explanation`: string no vacio OBLIGATORIO a nivel de slot. Merge D-23-10: base = la explanation fuente mas completa/general del grupo + injertar los matices unicos de las descartadas. Para los slots del passato (stato/stata/stati/state) la explanation enfatiza la regla de concordancia genero/numero del participio de essere (el unico lugar donde el participio italiano concuerda visiblemente). Explanations RULE-FIRST (lideran con la regla/disparador, no con un sustantivo concreto). El campo `notes` legacy es autor-internal (NO se mueve a variants[]); su contenido pedagogico relevante se injerta en la explanation del slot si aporta un matiz (D-23-10).
- `variants`: array NO vacio. Cada variante MC = objeto plano `{prompt (con ___), options (3-4 strings), correctIndex (en rango)}`. Cada variante word-buttons = `{prompt, answer, distractors}`. La variante NO lleva `explanation` ni `validation`.
- `validation`: top-level `{status, passes[]}` — se preserva del/los ejercicio(s) fuente verbatim (D-23-11).

Reglas hard del validator (verificadas contra scripts/validate-content-fixture.mjs):
- `payload` XOR `variants[]` — nunca ambos, nunca ninguno. Al convertir se ELIMINA `payload`.
- `explanation` top-level string no vacio cuando hay `variants[]`.
- `variants[]` no vacio; superficie validada por el SURFACE_VALIDATOR del `type` (MC: prompt con `___`, 3-4 options, correctIndex en rango; word-buttons: answer no vacio).
- Apostrofes ASCII U+0027 si aparecen; sin smart-quotes (el smoke FALLA con smart-quotes). El legacy de Essere ya usa apostrofes ASCII (l'amico, trent'anni, d'acqua) — preservar verbatim.

Mapa de reagrupacion PROPUESTO (verificado contra los `notes`/options/correctIndex/categoryIds de los 39 ids; el planner lo deriva, el autor lo refina en el checkpoint). Predccion rough ~21 slots de reagrupacion (antes de slots nuevos de 23-02):

PRESENTE INDICATIVO POR PERSONA (D-23-02, 1 slot por persona/forma) -> 6 slots:
- `essere-sono` (io, 1a sing): 001 -> slot-de-1 (celda pobre -> candidata a variante en 23-02). Matiz: `sono` tambien es 3a pl.
- `essere-sei` (tu, 2a sing): 002 -> slot-de-1 (celda pobre)
- `essere-e` (lui/lei, 3a sing): 003 + 004 -> slot (misma forma `e`, 2 variantes; matiz: acento grave e != conjuncion e)
- `essere-siamo` (noi, 1a pl): 005 -> slot-de-1 (celda pobre; localizacion, falso amigo ser/estar)
- `essere-siete` (voi, 2a pl): 006 -> slot-de-1 (celda pobre; estado, concordancia adjetival)
- `essere-sono-loro` (loro, 3a pl): 007 -> slot-de-1 (celda pobre; misma forma que io, contexto desambigua)
  NOTA DE DISCRECION (A2): io-sono 001 y loro-sono 007 comparten la forma `sono` pero entrenan personas distintas (verificado: 001 "Io ___ Maria" correct=sono; 007 "Loro ___ in vacanza" correct=sono). Recomendacion: 2 slots separados por persona (espiritu literal D-23-02 — cada persona es una trampa distinta). El autor confirma en el checkpoint si los funde en 1 slot `essere-sono` con 2 variantes o los deja como 2 slots (afecta el count en 1 slot).

IDENTIDAD / NACIONALIDAD / PROFESION / ESTADO / COPULA (D-23-01, agrupar por regla, pocos slots) -> ~5-6 slots:
- `essere-identidad`: identidad/parentesco con essere (nombre, amigo, hermano) <- 009 (presentacion), 010 (parentesco), 011 (amigo di Luca). Merge D-23-10. (Open Q #2: el autor afina si nacionalidad+profesion se separan o se funden.)
- `essere-nacionalidad`: nacionalidad con essere + concordancia + di-origen <- 013 (masc sing), 014 (fem sing + di), 015 (pl masc). HUECO PEDAGOGICO (D-23-04): concordancia italiano/italiana -> variantes nuevas en 23-02.
- `essere-profesion`: profesion/rol con essere (sin articulo) <- 012 (rol pl), 016 (medico), 017 (avvocata fem), 018 (rol pl), 019 (disyuntiva). Contraste essere/avere (D-23-06).
- `essere-stato`: estado/emocion con essere (stanca/felice/tristi) <- 008 (non stanca), 020 (stanca), 021 (felice), 022 (contenti), 023 (tristi). Falso amigo estar->essere; distractoras avere (D-23-06).
- `essere-copula`: copula clasificatoria (X es de la clase Y) <- 024 (Roma e citta), 025 (gatto e animale). Uso esencial puro.

PASSATO PROSSIMO PARTICIPIO (D-23-03, 4 SLOTS SEPARADOS por concordancia — DIVERGENCIA frente a Avere) -> 4 slots:
- `essere-passato-prossimo-stato`: masc sing (stato) <- 026 (io sono stato)
- `essere-passato-prossimo-stata`: fem sing (stata) <- 027 (Maria e stata)
- `essere-passato-prossimo-stati`: masc pl (stati) <- 028 (noi siamo stati)
- `essere-passato-prossimo-state`: fem pl (state) <- 029 (le ragazze sono state)
  Cada slot-de-1; explanation enfatiza la concordancia genero/numero (D-23-03). NO fundir en 1 slot.

WORD-BUTTONS (D-23-12, 4 slots-de-1 type:word-buttons) -> 4 slots:
- `essere-wb-identidad` <- 100 (identidad/presentacion multiple)
- `essere-wb-nacionalidad` <- 101 (nacionalidad pl + di-origen)
- `essere-wb-profesion` <- 102 (profesion fem + in-lugar)
- `essere-wb-passato` <- 103 (passato pl masc + a-ciudad + per-duracion)

CRUCES (D-23-09, id ESTABLE) -> 6 slots:
- 300 (essere+avere, e medico + ha trent'anni) / 301 (essere+profesiones, avvocata) / 302 (essere+verbos-movimiento, e andata) / 303 (essere+genero-numero, italiani di Milano) / 304 (essere+sustantivos-irregulares, braccia stanche) / 305 (essere+preposiciones, di Milano) -> 6 slots-de-1, ids intactos, categoryIds de 2 ids intactos.

Este mapa propuesto da ~21 slots (6 presente + ~5 identidad/etc + 4 passato + 4 wb + 6 cruces = ~25 si io/loro-sono son 2 slots y los del bloque 2 quedan en 5; ~21 si se fusionan agresivamente). El conteo final lo determina el autor en el checkpoint + 23-02 (slot ser/estar nuevo D-23-07 lo sube). El conteo se REPORTA, NO se hardcodea en tests aqui (eso es 23-03, que lee data.exercises.length REAL).

NO snapshot: Essere no tiene snapshot/assert APPEND-ONLY (avere-only, verificado). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs. NO buscar ni crear .essere-prefix-snapshot.json (no existe).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Producir el mapa de reagrupacion auditable (id-fuente -> slot)</name>
  <files>.planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md</files>
  <read_first>
    - content/exercises/essere.json (integro — los 39 ejercicios con su campo `notes` que declara la regla/persona/uso de cada uno, su `payload.explanation`, su `payload.prompt`/`options`/`correctIndex` (MC), su `payload.answer`/`distractors` (word-buttons 100..103), sus `categoryIds` de 2 ids (300..305) y su `validation.passes[]`)
    - content/exercises/avere.json (el output canonico de Avere ya convertido — la referencia AFTER exacta de la categoria hermana: shape de slot, variants[], explanation/validation top-level, ids semanticos, cruces 300..305 con id+2 cats)
    - .planning/phases/22-avere-a-slots-contenido/22-01-PLAN.md (el plan analogo de Avere que esta fase replica — Task 1: metodologia de reagrupacion por regla, merge D-17-05/D-23-10, manejo de cruces)
    - .planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md (la plantilla del formato del mapa old-id->slot a espejar cambiando categoria)
    - .planning/phases/23-essere-a-slots-contenido/23-CONTEXT.md y 23-RESEARCH.md (decisiones locked D-23-01..13 + la reagrupacion propuesta del researcher con la tabla por bloques)
  </read_first>
  <action>
    Crear `.planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md` como artefacto de auditoria (espejar el formato de 22-REAGRUPACION-MAP.md): una tabla por cada slot resultante con columnas (slot-id propuesto, regla/persona/uso, ids-fuente que entran como variantes, type [MC|word-buttons], categoryIds, explanation-base elegida + matices a injertar D-23-10, celda pobre candidata a variante nueva en 23-02?). Aplicar el mapa propuesto en <interfaces>: presente indicativo por persona (001 io, 002 tu, 003+004 lui/lei, 005 noi, 006 voi, 007 loro como slots por persona, D-23-02; anotar EXPLICITAMENTE la nota de discrecion io-sono 001 vs loro-sono 007 — 2 slots por persona [recomendado] o 1 slot `essere-sono` con 2 variantes — para confirmacion del autor); identidad/nacionalidad/profesion/estado/copula agrupados por regla (D-23-01, ~5-6 slots: identidad 009/010/011, nacionalidad 013/014/015, profesion 012/016/017/018/019, estado 008/020/021/022/023, copula 024/025 — anotar Open Q #2 para el autor: si nacionalidad+profesion se separan o se funden, si estado va con copula); passato prossimo en 4 SLOTS SEPARADOS por concordancia (026 stato / 027 stata / 028 stati / 029 state, D-23-03 — NO fundir en 1 slot; cada explanation enfatiza la concordancia genero/numero del participio de essere); 100..103 word-buttons slots-de-1 (D-23-12); cruces 300..305 slots-de-1 con id ESTABLE + categoryIds de 2 ids (D-23-09). Essere NO tiene match — no inventar. Para cada slot essere-only proponer un id semantico limpio RESERVANDO el rango 300..305. Para cada slot con varias variantes (essere-e 003+004, identidad, nacionalidad, profesion, estado, copula), comparar las explanations fuente y registrar cual es la base (la mas completa/general) y que matices unicos de las descartadas se injertan (D-23-10), incluido el patron distractoras essere/avere donde aplica (D-23-06). Marcar las celdas pobres (slots de presente con 1 variante) como candidatas a engordar en 23-02 y anotar los huecos de regla a evaluar en 23-02 (concordancia nacionalidad italiano/italiana, localizacion con essere, slot NUEVO ser/estar D-23-07, contraste essere/avere agresivo D-23-06). Incluir una SECCION EXPLICITA "Snapshot/re-base D-88: NO APLICA a Essere" documentando que los scripts snapshot-/assert-avere-prefix son avere-only (hardcoded a avere.json, 0 refs a essere) y que NO existe ni se necesita un snapshot para Essere — para que el ejecutor de Task 2 NO copie la tarea de re-base de 22-01/22-02. NO autorar variantes nuevas aqui. NO incluir los slots de huecos nuevos (van a 23-02). Reportar el conteo de slots resultante de SOLO esta reagrupacion (sin slots nuevos ni variantes nuevas).
  </action>
  <verify>
    <automated>test -f .planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md && grep -c "essere-" .planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo 23-REAGRUPACION-MAP.md existe con una tabla que cubre los 39 ids fuente (cada id 001-029, 100-103, 300-305 aparece exactamente una vez en alguna columna de ids-fuente)
    - El presente indicativo esta por persona (D-23-02): 001(io), 002(tu), 003+004(lui/lei), 005(noi), 006(voi), 007(loro); la nota de discrecion io-sono vs loro-sono (2 slots por persona [recomendado] vs 1 slot por forma) esta anotada para confirmacion del autor
    - El passato prossimo esta en 4 SLOTS SEPARADOS (essere-passato-prossimo-stato/stata/stati/state <- 026/027/028/029), NO en 1 slot con 4 variantes (D-23-03); cada uno con explanation que enfatiza la concordancia genero/numero
    - El bloque identidad/nacionalidad/profesion/estado/copula esta agrupado por regla en ~5-6 slots (D-23-01) con Open Q #2 anotada para el autor
    - 100..103 marcados como slots-de-1 type:word-buttons; NO hay ningun slot type:match (Essere no tiene match)
    - 300..305 marcados como slots-de-1 con id ESTABLE + categoryIds de 2 ids verificados (300 essere+avere, 301 essere+profesiones, 302 essere+verbos-movimiento, 303 essere+genero-numero, 304 essere+sustantivos-irregulares, 305 essere+preposiciones); el rango 300..305 esta RESERVADO/excluido de la renumeracion de slots essere-only
    - Todos los slots essere-only tienen categoryIds=["essere"]; los ids propuestos son unicos y semanticos
    - Cada slot multi-variante registra explanation-base + matices a injertar (D-23-10); el patron distractoras essere/avere esta anotado donde aplica; las celdas pobres estan marcadas como candidatas a 23-02 y los huecos anotados (nacionalidad concordancia, localizacion, slot ser/estar D-23-07)
    - Hay una seccion EXPLICITA "Snapshot/re-base D-88: NO APLICA a Essere" (los scripts son avere-only; no replicar la tarea de re-base de 22-01)
    - El conteo de slots de esta reagrupacion esta reportado (sin slots nuevos ni variantes nuevas)
  </acceptance_criteria>
  <done>
    23-REAGRUPACION-MAP.md cubre los 39 ids->slots con presente indicativo por persona, bloque identidad/nac/prof/estado/copula por regla, passato prossimo en 4 SLOTS SEPARADOS por concordancia (D-23-03), 100..103 word-buttons slots-de-1 (sin match), cruces 300..305 con id estable + 2 cats; ids semanticos libres reservando 300..305; plan de explanation por slot multi-variante con matices y patron essere/avere; seccion explicita "snapshot no aplica a Essere"; conteo de slots reportado.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Aprobar el mapa de reagrupacion de Essere (granularidad de slots del bloque identidad/estado/copula, io-sono vs loro-sono, agrupacion de essere-e 003+004, merges de explanation) antes de reescribir el JSON. Confirmar los 4 slots separados del passato prossimo y que NO aplica snapshot.</decision>
  <context>
    El mapa (23-REAGRUPACION-MAP.md) determina la estructura final de slots y por tanto el conteo que los tests de 23-03 sincronizan. Decisiones cargadas de esta fase: (1) presente indicativo por persona (D-23-02) — cada forma (sono/sei/e/siamo/siete/sono) es una trampa distinta para el hispanohablante; nota de discrecion: io-sono 001 y loro-sono 007 comparten forma `sono` -> 2 slots por persona [recomendado, espiritu literal D-23-02] o 1 slot por forma con 2 variantes (afecta el count en 1 slot); (2) el bloque identidad/nacionalidad/profesion/estado/copula agrupado por REGLA (D-23-01, agrupar lo facil, ~5-6 slots) — Open Q #2: si nacionalidad+profesion se separan o se funden, si estado va con copula; (3) el passato prossimo (026..029) en 4 SLOTS SEPARADOS por concordancia (D-23-03, drillear lo dificil) — DIVERGENCIA deliberada frente a Avere (que fue 1 slot con N variantes): la concordancia del participio de essere con el sujeto es la regla distintiva frente a Avere y el punto duro para el hispanohablante -> el loop debe obligar a acertar stato/stata/stati/state por separado; (4) Essere NO tiene snapshot APPEND-ONLY (avere-only, verificado) -> NO se replica la tarea de re-base D-88 de 22-01. Las explanations mergeadas (D-23-10) requieren revision del autor (D-85) para no perder matices (concordancia adjetival/participio, falso amigo ser/estar->essere, edad con avere no essere, posesion vs identidad).
  </context>
  <options>
    <option id="por-regla-passato-4-slots">
      <name>Presente por persona + bloque por regla + passato en 4 slots separados + sin snapshot (recomendado/locked)</name>
      <pros>Coherente con D-23-02 (cada persona una trampa), D-23-01 (agrupar lo facil, pocos slots de identidad/estado/copula) y D-23-03 (drillear lo dificil: 4 slots de concordancia del participio). El passato en 4 slots fuerza la re-verificacion de cada forma de concordancia — la regla distintiva de Essere. No replicar el snapshot evita una tarea inexistente. Espeja el patron de Avere/Articoli/Partitivos salvo en las 2 divergencias deliberadas.</pros>
      <cons>El passato sube el count (4 slots en vez de 1). Algunas celdas pobres de presente con 1 variante hasta engordarlas en 23-02.</cons>
    </option>
    <option id="passato-1-slot">
      <name>Passato prossimo como 1 slot con 4 variantes (replica ciega de Avere)</name>
      <pros>Menos slots.</pros>
      <cons>Contradice D-23-03 EXPLICITAMENTE: mezcla las 4 formas de concordancia en un slot -> el motor no obliga a acertar stato/stata/stati/state por separado, perdiendo el drilling de la regla distintiva de Essere. Descartado salvo override explicito del autor.</cons>
    </option>
  </options>
  <resume-signal>Confirma la granularidad del bloque identidad/estado/copula (cuantos slots), la decision io-sono vs loro-sono y la agrupacion de essere-e (003+004), confirma los 4 slots separados del passato prossimo (D-23-03) y que NO aplica snapshot, ajusta los slot-ids/merges de explanation y marca las celdas pobres + huecos a evaluar en 23-02. Escribe "aprobado" cuando el mapa este listo para reescribir el JSON.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reescribir essere.json a slot+variantes segun el mapa aprobado</name>
  <files>content/exercises/essere.json</files>
  <read_first>
    - .planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md (el mapa aprobado por el autor — fuente de verdad de la estructura)
    - content/exercises/essere.json (integro — superficies, explanations, prompt/options/correctIndex, answer/distractors (100..103) y validation.passes[] fuente a mover/mergear)
    - content/exercises/avere.json (el output canonico de Avere — shape multi-variante, slot-de-1 word-buttons real, 6 cruces con id estable + 2 cats; la referencia AFTER exacta a espejar de la categoria hermana)
    - .planning/phases/22-avere-a-slots-contenido/22-01-PLAN.md (Task 2 analogo de Avere — el manejo de los cruces 300..305 con id+2 cats, el move de superficies intactas a variants[], el merge de explanations y validation)
  </read_first>
  <action>
    Reescribir `content/exercises/essere.json` segun el mapa aprobado. Por cada slot, emitir `{id, type, categoryIds, explanation, variants:[...], validation}` ELIMINANDO `payload` de cada ejercicio fuente y MOVIENDO su superficie intacta a `variants[]`: para MC `{prompt, options, correctIndex}`; para word-buttons (100..103) `{prompt, answer, distractors}`. La `explanation` de cada slot multi-variante (essere-e, identidad, nacionalidad, profesion, estado, copula) = la base elegida en el mapa + los matices injertados (D-23-10); para slots-de-1 (incluidos los 4 del passato y los word-buttons y los cruces) la explanation existente sube a top-level tal cual; rule-first. Para los 4 slots del passato (essere-passato-prossimo-stato/stata/stati/state) la explanation enfatiza la regla de concordancia genero/numero del participio de essere (D-23-03). Preservar el patron distractoras essere/avere donde aplica (D-23-06) — NO eliminar las distractoras de avere existentes en las options. El campo `notes` legacy es autor-internal: NO se mueve a variants[]; su matiz pedagogico relevante se injerta en la explanation del slot si aporta (D-23-10). Mover el texto intacto NO requiere re-validacion (cambio de contenedor, D-23-11); si al mergear cambia la SUPERFICIE de algun prompt/options existente, eso cuenta como variante nueva -> quorum en 23-02 (NO re-validar aqui; documentar en el mapa/SUMMARY cualquier superficie tocada). Para los 6 cruces (300..305): conservar id, type, categoryIds de 2 ids intactos; convertir su payload a un unico variant + explanation top-level (slot-de-1). Para los slots essere-only usar el id semantico del mapa RESERVANDO 300..305. Todos los slots essere-only con categoryIds=["essere"]. Preservar el campo `validation` top-level moviendo los passes[] verbatim (D-23-11); para slots multi-variante combinar segun el criterio del mapa (la del ejercicio cuya superficie/explanation domina o la fusion de passes[]); registrar el criterio en el SUMMARY. Aplicar un scan de acentos sobre las superficies/explanations MOVIDAS (leccion Phase 19 WR-01/WR-02: el legacy puede arrastrar tildes ausentes; DeepSeek es estricto en acentos; corregir defectos legacy de acento es correccion ortografica, no cambio de superficie). Apostrofes ASCII U+0027 (el legacy ya usa l'amico/trent'anni/d'acqua — preservar); sin smart-quotes. NO autorar variantes nuevas. NO anadir slots de huecos (van a 23-02). NO tocar counts de tests/scripts (van a 23-03). NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Essere; avere-only). Documentar en el SUMMARY que "Essere no tiene snapshot APPEND-ONLY — no aplica re-base D-88".
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs essere content/exercises/essere.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs essere content/exercises/essere.json` exits 0
    - Ningun ejercicio conserva payload: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Todo slot con variants[] tiene explanation top-level no vacia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('slots sin explanation:', d.exercises.filter(e=>Array.isArray(e.variants)&&(!e.explanation||!e.explanation.trim())).length)"` imprime `0`
    - Ninguna variante lleva explanation propia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('variantes con explanation:', d.exercises.flatMap(e=>e.variants||[]).filter(v=>v.explanation!==undefined).length)"` imprime `0`
    - Los 6 cruces conservan id + 2 categoryIds: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); const c=d.exercises.filter(e=>/^essere-30[0-5]$/.test(e.id)); console.log('cruces:', c.length, 'con 2 cats:', c.filter(e=>e.categoryIds.length===2).length)"` imprime `cruces: 6 con 2 cats: 6`
    - Existen exactamente 4 slots de passato prossimo separados (D-23-03): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('passato slots:', d.exercises.filter(e=>/^essere-passato-prossimo-/.test(e.id)).map(e=>e.id).sort().join(','))"` imprime los 4 ids `...stata,...state,...stati,...stato` (ninguno con 4 variantes en un solo slot)
    - Los word-buttons existen y NO hay match: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('wb:', d.exercises.filter(e=>e.type==='word-buttons').length, 'match:', d.exercises.filter(e=>e.type==='match').length)"` imprime `wb: 4 match: 0`
    - Los slots essere-only tienen categoryIds=["essere"]: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('essere-only mal-cat:', d.exercises.filter(e=>!/^essere-30[0-5]$/.test(e.id)&&!(e.categoryIds.length===1&&e.categoryIds[0]==='essere')).length)"` imprime `0`
    - Ningun id de slot essere-only colisiona con 300..305 (rango reservado)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/essere.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Essere (no existe `.essere-prefix-snapshot.json`)
  </acceptance_criteria>
  <done>
    essere.json reescrito a slot+variantes (sin payload, variants[]+explanation top-level por slot, variantes sin explanation propia; presente por persona; bloque identidad/estado/copula por regla; passato prossimo en 4 SLOTS SEPARADOS por concordancia con explanation de concordancia; 100..103 word-buttons slots-de-1 sin match; cruces 300..305 con id+2 cats intactos; ids semanticos libres con categoryIds=["essere"]; patron distractoras essere/avere preservado), validateContent verde, scan de acentos aplicado a superficies movidas, sin smart-quotes; documentado que Essere no tiene snapshot (no aplica re-base D-88).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano -> loader/validator al boot | El `essere.json` reescrito a mano cruza a validateContent y al render; explanation/prompt se renderizan como texto plano |
| Estado de slots cross-cat -> clearedExerciseIds de otras categorias | Renumerar essere-300..305 desincronizaria clearedExerciseIds de las categorias cruzadas (preposiciones NO fue reseteada en Phase 21 -> es la mas sensible, via essere-305) y romperia la cascada D-54 |

> Nota: esta fase edita un archivo JSON de contenido estatico en una app local single-user sin backend; no introduce nueva superficie de ataque (sin red, auth, input de usuario, ni deserializacion de datos no confiables). El unico endurecimiento adyacente (anti prototype-pollution) vive en la capa de migracion, intacta aqui. No se esperan amenazas HIGH.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering / data-integrity | reagrupacion que pierde/duplica una superficie fuente, o renumera un cruce rompiendo su cobertura multi-cat | mitigate | El mapa auditado (Task 1 + checkpoint) garantiza los 39 ids fuente mapeados 1:1; el acceptance asserta cruces=6 con id estable + 2 categoryIds (renumerarlos haria stale clearedExerciseIds de avere/profesiones/verbos-movimiento/genero-numero/sustantivos-irregulares/preposiciones + romperia la cascada D-54), wb=4, match=0, payload==0, 4 slots de passato. Rango 300..305 reservado evita colision. |
| T-23-02 | Tampering | reagrupacion pierde un matiz de explanation al mergear (D-23-10) o pierde el drilling del passato (D-23-03) | mitigate | Merge elegir-la-mas-completa + injertar matices (no reescritura); 4 slots separados del passato asegurados por acceptance (4 ids distintos, no 1 slot con 4 variantes); revision del autor en el checkpoint (D-85); acceptance asserta cobertura de explanation por slot (slots sin explanation == 0). |
| T-23-03 | Tampering | explanation/prompt con HTML/markdown/smart-quotes; defecto de acento legacy arrastrado (Phase 19 WR-01/02) | mitigate | Acceptance asserta 0 smart-quotes; scan de acentos sobre superficies MOVIDAS (DeepSeek estricto en acentos); el smoke (23-03) re-asserta markdown/HTML sobre variants[].prompt + explanation. Apostrofes ASCII U+0027 preservados del legacy. |
| T-23-04 | Tampering | malformed JSON crashea el loader | mitigate | `validateContent` (existente, no modificado) rechaza con banner visible slot sin variantes / variante sin superficie valida / explanation ausente. El acceptance corre el validador. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que instalar/auditar (proyecto estatico offline single-user). |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs essere content/exercises/essere.json` exits 0
- `node --test tests/*.test.js` — nota: el count de Essere (39 hardcoded en 3 sitios) FALLARA tras esta reescritura porque ahora hay otro nº de slots; esto es ESPERADO y se arregla en 23-03. Documentar el rojo esperado en el SUMMARY; NO arreglar los counts aqui.
- El mapa 23-REAGRUPACION-MAP.md aprobado por el autor (checkpoint), incluida la confirmacion de los 4 slots del passato y que no aplica snapshot
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Essere)
</verification>

<success_criteria>
- 39 ejercicios fuente reagrupados a slots por regla: presente por persona (D-23-02); bloque identidad/nac/prof/estado/copula por regla (D-23-01); passato prossimo en 4 SLOTS SEPARADOS por concordancia (D-23-03); 100..103 word-buttons slots-de-1 sin match (D-23-12); cruces 300..305 id estable + 2 cats (D-23-09); ids semanticos libres reservando 300..305, categoryIds=["essere"] para essere-only
- Cada slot: explanation top-level no vacia (merge D-23-10, matices injertados, concordancia enfatizada en el passato), variantes sin explanation propia, payload eliminado; patron distractoras essere/avere preservado
- validateContent verde; sin smart-quotes; scan de acentos aplicado a superficies movidas
- Essere no tiene snapshot APPEND-ONLY (avere-only) -> NO se replica la tarea de re-base D-88; documentado en el SUMMARY
- (Los counts de tests quedan rojos a proposito hasta 23-03)
</success_criteria>

<output>
Create `.planning/phases/23-essere-a-slots-contenido/23-01-SUMMARY.md` when done
</output>
