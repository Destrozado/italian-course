---
phase: 23-essere-a-slots-contenido
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - content/exercises/essere.json
  - .planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md
autonomous: false
requirements: [ESS-02]
must_haves:
  truths:
    - "Cada variante NUEVA y cada slot NUEVO autorado pasa el quorum cross-vendor R1-R7 (base canonica Claude Opus + Sonnet via skill gsd-validate-exercise; refuerzo Gemini + DeepSeek via validate-ai-pass) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot; 1-por-1, fresh context, NUNCA batched"
    - "Las celdas pobres de presente indicativo (sono/sei/siamo/siete/sono-loro con 1 variante tras 23-01) quedan engordadas a >=2 variantes donde la regla admite reformulacion natural, con misma persona/forma y otro sujeto/contexto A1"
    - "Los huecos pedagogicos prioritarios de Essere se materializan (D-23-04, ambicion generosa): concordancia de nacionalidad (italiano/italiana/italiani/italiane y otras nacionalidades) en el slot essere-nacionalidad; localizacion con essere (a casa, in ufficio, al mare) como variantes o slot; y un SLOT NUEVO dedicado a ser-vs-estar (D-23-07) que contrasta estado (Maria e stanca) vs identidad (Maria e medico) con explanation del calco espanol"
    - "El contraste essere/avere se refuerza agresivamente (D-23-06): distractoras avere donde tiene sentido + explicaciones del calco (edad con avere ho trent'anni no essere; ser/estar -> essere; posesion con avere vs identidad/estado/localizacion con essere)"
    - "Las variantes nuevas se autoran SOLO donde la regla admite reformulacion natural (sin cuota fija D-23-05); ninguna lleva explanation propia (comparten la del slot); cada slot NUEVO lleva validation.status top-level (D-19-09)"
  artifacts:
    - path: "content/exercises/essere.json"
      provides: "slots/variantes nuevas validated integradas en variants[], incluido el slot nuevo ser/estar (D-23-07)"
      contains: "\"variants\""
    - path: ".planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md"
      provides: "set propuesto de superficies nuevas con id temporal + slot destino + superficie + justificacion R1-R7"
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quorum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS con ambicion generosa (ESS-02, D-23-04) donde la regla admite reformulacion natural, priorizando los huecos pedagogicos de Essere para el hispanohablante: (1) engordar las celdas pobres de presente indicativo que quedaron con 1 variante tras 23-01 (sono/sei/siamo/siete/sono-loro; essere-e ya tiene 2) a >=2 variantes; (2) HUECO concordancia de nacionalidad (italiano/italiana/italiani/italiane, spagnolo/spagnola, tedesco/tedesca...) en el slot `essere-nacionalidad` — el error de concordancia mas frecuente con essere; (3) HUECO localizacion con essere (sono a casa, e in ufficio, siamo al mare) — falso amigo estar->essere; como variantes de `essere-siamo`/`essere-e` o un slot de localizacion si el autor separa; (4) SLOT NUEVO dedicado ser-vs-estar (D-23-07): `essere-ser-estar` que contrasta estado (Maria e stanca) vs identidad (Maria e medico), ambos con essere, explanation explicita del calco espanol (italiano usa un solo verbo para ser y estar) — se re-verifica como tema propio en el loop; (5) reforzar agresivamente el contraste essere/avere (D-23-06): distractoras avere (ho/hai/ha...) donde tenga sentido + explicaciones del calco (edad con avere `ho trent'anni` no essere; ser/estar -> essere; posesion con avere vs identidad/estado/localizacion con essere). Cada superficie NUEVA pasa el quorum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched, con revision del autor por variante (D-85). Las superficies movidas intactas en 23-01 NO se re-validan (D-23-11). Sin cuota fija de densidad (D-23-05): proponer variantes donde la regla lo pida, no por cumplir un minimo.

Purpose: las variantes nuevas materializan el dolor anti-memorizacion (re-hacer un slot puede tocar una superficie distinta) y cierran los huecos pedagogicos mas grandes de Essere: la concordancia de nacionalidad, la localizacion con essere (no estar), y sobre todo el slot ser/estar — el calco que mas confunde al hispanohablante (un verbo italiano para dos espanoles). El contraste essere/avere agresivo refuerza la otra confusion clasica (edad/sensaciones con avere, identidad/estado con essere). El quorum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazo 8 bugs en Articoli y 3 en Avere que el human-verify aprobo). R1/R5 criticos: el prompt no debe leak la forma de essere; las options deben tener 3+ valores distintos. Pitfall de Essere a vigilar: la concordancia adjetival/participial (stanca/stanco/stanchi/stanche; stato/stata/stati/state); el calco ser/estar; la edad y las sensaciones se TIENEN con avere, no se SON con essere.

NO existe snapshot/assert APPEND-ONLY para Essere (avere-only, hardcoded a avere.json). NO ejecutar ningun script de snapshot ni assert de prefijo, ni siquiera "por seguridad": no aplica a esta categoria (a diferencia de 22-02, que re-basaba el snapshot de Avere).

Output: `essere.json` con las variantes nuevas + el slot nuevo ser/estar (D-23-07) + huecos de nacionalidad/localizacion integrados; cada superficie nueva con su quorum verde en `validation.passes[]`; cada slot nuevo con validation.status top-level (D-19-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/essere.json

<interfaces>
<!-- Pipeline de quorum cross-vendor. Verificado en validate-ai-pass.mjs + skill. NO modificar estos scripts/skill. Mismo flujo ejecutado con exito en Phase 19 (8 superficies), Phase 20 (6 superficies) y Phase 22 (14 superficies), 0 excluidas. -->
Quorum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Base canonica (D-23-13) — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus->Sonnet):
  /gsd-validate-exercise <id>
  (model IDs literales que emite el skill: claude-opus-4-7 + claude-sonnet-4-6; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio)
  Fallback cuando Task no esta disponible al executor (precedente Phase 19/20/22, D-19-08): la mitad Claude se corre con `claude -p --model claude-opus-4-7` y `--model claude-sonnet-4-6` headless, 1 proceso por modelo por superficie, con el 09-VALIDATION-PROMPT verbatim y el ejercicio SIN su campo validation (no sesgar). Equivalente funcional 1-por-1 NUNCA batched.

Refuerzo — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondio; >=2 by distintos para el quorum; status re-derivado por deriveStatus, NO hand-editar status. Si Gemini agota cuota 429, DeepSeek cubre los 2 externos con by distintos chat + reasoner — precedente 22-VERIFICATION.)

Gate D-17-07: el passes[] final de cada superficie nueva = >=4x "correcta" (opus + sonnet + 2 externos by distintos), CERO "incorrecta". deriveStatus -> "validated"; cualquier "incorrecta" -> "disputed" (sticky, bloquea, NO entra al slot).

Pitfall: el VALIDATION-PROMPT es legacy-payload-centrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/essere.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quorum, mover la superficie al variants[] del slot (sin re-validacion) y eliminar el id temporal legacy.

Alcance D-23-04 (ambicion generosa) + D-23-05 (sin cuota fija):
- Celdas pobres a engordar a >=2 variantes (reformular la MISMA persona/forma con otro sujeto/contexto A1): sono-io (otro predicado de identidad), sei (otra pregunta/atributo de tu), siamo (otro complemento de noi), siete (otra pregunta/estado de voi), sono-loro (otro complemento de loro). essere-e (003+004) ya tiene 2 variantes -> opcional. Verificar que la forma sigue siendo inequivoca para esa persona.
- NO inflar slots cuya regla no admite reformulacion natural; sin minimo por slot (D-23-05).

Huecos pedagogicos prioritarios (D-23-04, R1/R5 criticos):
1. CONCORDANCIA DE NACIONALIDAD (slot `essere-nacionalidad`): variantes masc/fem sing/pl con essere (italiano/italiana/italiani/italiane, spagnolo/spagnola, tedesco/tedesca...). El prompt fija sujeto+genero+numero; la respuesta es la nacionalidad concordada. VERIFICAR (R1) que el prompt no leak la concordancia; (R5) options con 3+ valores distintos (incluir las concordancias incorrectas como distractoras).
2. LOCALIZACION CON ESSERE: sono a casa, e in ufficio, siamo al mare. Variantes de `essere-siamo`/`essere-e`/`essere-sono` o un slot de localizacion si el autor separa. Falso amigo estar->essere (en italiano la localizacion es essere, no un verbo aparte). categoryIds=["essere"].
3. SLOT NUEVO ser-vs-estar (D-23-07): `essere-ser-estar`, type:multiple-choice, categoryIds=["essere"]. Variantes que contrastan estado (Maria e stanca) vs identidad (Maria e medico), ambos con essere; explanation explicita del calco (el italiano usa essere para lo que el espanol parte en ser y estar). VERIFICAR que la respuesta no es ambigua (el contraste es conceptual, pero la forma correcta de essere debe ser inequivoca). Slot NUEVO -> requiere validation.status top-level (D-19-09).
4. CONTRASTE ESSERE/AVERE AGRESIVO (D-23-06): distractoras avere (ho/hai/ha...) en options donde tenga sentido + explicaciones del calco: edad con avere (`ho trent'anni`, NO `sono trent'anni`); ser/estar -> essere; posesion con avere vs identidad/estado/localizacion con essere. Tejido en explanations + distractoras. Si se redacta una variante explicita "edad con avere", VERIFICAR que la respuesta correcta es la forma de avere (no essere) y que el slot destino tiene sentido (p.ej. el cruce 300 ya contrasta e-medico + ha-trent'anni).
- El planner/autor decide en plan-time/checkpoint que huecos son naturales. Si un hueco no tiene construccion A1 natural, NO se materializa.

Validation top-level para la gate (D-19-09): el slot NUEVO ser/estar (y cualquier otro slot nuevo) debe portar el bloque `validation` top-level ({status:"validated", passes[]}) ademas del por-variante implicito — el gate VAL_07_STRICT lee `ex.validation?.status` a nivel de slot. Reflejar el quorum cross-vendor de sus variantes en el bloque top-level (NO inventar validacion; elevar la aprobada). Esta fue la correccion Rule-2 de 19-03.

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quorum, un scan ASCII/tildes (RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial.
Gloss ES (R7 canon): mantener "(en espanol: ...)" donde desambigue doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de politica (base de aprobacion = Claude Opus+Sonnet, D-23-13).

NO snapshot: Essere no tiene snapshot/assert APPEND-ONLY (avere-only, verificado). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs ni buscar .essere-prefix-snapshot.json. A diferencia de 22-02 (que re-basaba el snapshot de Avere), aqui NO hay paso de re-base.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (celdas pobres de presente) + huecos (nacionalidad, localizacion, slot ser/estar, contraste essere/avere)</name>
  <files>.planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/essere.json (estado tras 23-01 — los slots ya reagrupados, para saber que slots existen, cuales son celdas pobres y que admite variante nueva)
    - .planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md (las celdas pobres marcadas como candidatas + los huecos de regla anotados en 23-01)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints del quorum: 1-por-1, fresh context, NUNCA batched; model IDs literales)
    - .planning/phases/22-avere-a-slots-contenido/22-02-PLAN.md (Task 1 analogo de Avere — formato de 22-VARIANTES-NUEVAS.md a espejar) y .planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md (la plantilla concreta)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6/R7 si accesible) y gloss_es_desambiguacion_canon.md (gloss ES canon)
  </read_first>
  <action>
    Proponer un set de superficies nuevas con ambicion GENEROSA (D-23-04) pero sin cuota fija (D-23-05). (1) Para cada celda pobre de presente indicativo con 1 variante tras 23-01 (sono-io, sei, siamo, siete, sono-loro): redactar >=1 variante nueva (superficie `{prompt con ___, options 3-4, correctIndex}`) que reformule la MISMA persona/forma con otro sujeto/contexto A1 natural y respuesta inequivoca. (2) HUECO concordancia de nacionalidad (slot essere-nacionalidad): variantes masc/fem sing/pl con essere (italiano/italiana/italiani/italiane, spagnolo/spagnola, tedesco/tedesca...); el prompt fija sujeto+genero+numero, la respuesta es la nacionalidad concordada; distractoras = concordancias incorrectas. (3) HUECO localizacion con essere (sono a casa, e in ufficio, siamo al mare): variantes de los slots de presente correspondientes o un slot de localizacion. (4) SLOT NUEVO ser/estar (D-23-07): `essere-ser-estar`, variantes que contrastan estado (Maria e stanca) vs identidad (Maria e medico), explanation explicita del calco espanol; este slot NUEVO requiere validation top-level (D-19-09) tras el quorum. (5) CONTRASTE essere/avere agresivo (D-23-06): distractoras avere en options donde tenga sentido + explicaciones del calco (edad con avere, ser/estar->essere, posesion vs identidad/estado/localizacion). VERIFICAR explicitamente por superficie: (R1) el prompt no leak la forma correcta de essere; (R5) las options tienen 3+ valores distintos; que la nacionalidad/estado/localizacion/identidad se expresa con essere (no avere) y, donde la variante contraste edad/posesion, que esos usan avere (no essere). Cumplir R1-R7 (sin leak en prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigue doble-validez). Pasar un scan de acentos/ASCII (tildes RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. Producir `.planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md` (espejar el formato de 22-VARIANTES-NUEVAS.md): lista de cada superficie nueva con su id temporal (para el quorum aislado), slot destino (existente o nuevo, marcando el slot NUEVO ser/estar y cualquier otro slot nuevo), prompt/options/correctIndex/explanation, y la justificacion de por que la regla admite reformulacion / por que la construccion italiana es correcta. Incluir una SECCION de la decision de model IDs (Open Q #1, ver checkpoint): recomendar mantener los IDs literales del skill (claude-opus-4-7/claude-sonnet-4-6) por consistencia del audit trail con el contenido existente, surfacear al autor. Reportar el conteo de superficies nuevas propuestas Y cuantas son SLOTS NUEVOS (los slots nuevos suben el count de 23-03; las variantes no).
  </action>
  <verify>
    <automated>test -f .planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md && grep -ci "nacionalidad\|ser.estar\|localizaci\|essere\|avere" .planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 23-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + superficie + explanation
    - Las celdas pobres de presente indicativo (sono-io/sei/siamo/siete/sono-loro) tienen al menos una variante nueva propuesta cada una (engorde a >=2), con la misma persona/forma y respuesta inequivoca
    - El hueco de concordancia de nacionalidad esta cubierto con variantes masc/fem sing/pl con essere para el slot essere-nacionalidad (D-23-04)
    - El hueco de localizacion con essere (a casa/in ufficio/al mare) esta cubierto como variantes o slot, con la nota del falso amigo estar->essere
    - El SLOT NUEVO ser/estar (D-23-07) esta propuesto: `essere-ser-estar` con variantes que contrastan estado vs identidad y explanation explicita del calco espanol; marcado como slot NUEVO que requiere validation top-level (D-19-09)
    - El contraste essere/avere agresivo (D-23-06) esta presente: distractoras avere + explicaciones del calco (edad con avere, ser/estar->essere, posesion vs identidad/estado/localizacion), con verificacion de que la respuesta correcta usa el verbo correcto (essere para identidad/estado/localizacion; avere para edad/posesion donde aplique)
    - Cada superficie nueva cumple R1 (sin leak de la forma de essere en el prompt) y R5 (options con 3+ valores distintos); cada explanation/prompt pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers
    - Hay una seccion que surfacea la decision de model IDs (Open Q #1) recomendando mantener los IDs literales del skill por consistencia del audit trail
    - El conteo de superficies nuevas propuestas Y de slots nuevos esta reportado
  </acceptance_criteria>
  <done>
    23-VARIANTES-NUEVAS.md lista el set generoso de superficies nuevas (celdas pobres de presente engordadas + concordancia de nacionalidad + localizacion + slot NUEVO ser/estar D-23-07 + contraste essere/avere agresivo D-23-06) con superficies R1-R7-conformes y scan de acentos pasado; decision de model IDs surfaceada; conteo de superficies y de slots nuevos reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (23-VARIANTES-NUEVAS.md) — variantes para engordar las celdas pobres de presente + concordancia de nacionalidad + localizacion con essere + el SLOT NUEVO ser/estar (D-23-07) + contraste essere/avere agresivo (D-23-06), cumpliendo R1-R7 y con scan de acentos. AUN NO validadas por quorum. Incluye la decision pendiente de model IDs del quorum (Open Q #1).</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md`.
    2. Revisa cada variante nueva de celda pobre de presente: la forma de essere es inequivoca para esa persona (sono=io, sei=tu, siamo=noi, siete=voi, sono=loro)? El prompt NO contiene la respuesta (R1)? Las options tienen 3+ valores distintos (R5)?
    3. Revisa la concordancia de nacionalidad: italiano/italiana/italiani/italiane (y otras) concuerdan correctamente con el sujeto del prompt? Las distractoras son concordancias incorrectas (no formas de otro verbo)?
    4. Revisa la localizacion con essere: `sono a casa`/`e in ufficio`/`siamo al mare` usan essere (no estar/un verbo aparte)? La explanation avisa del calco estar->essere?
    5. Revisa el SLOT NUEVO ser/estar (D-23-07): los contrastes (Maria e stanca [estado] vs Maria e medico [identidad]) son A1 correctos, ambos con essere? La explanation explica el calco (un verbo italiano para ser y estar) sin volver ambigua la respuesta? El slot porta validation top-level tras el quorum?
    6. Revisa el contraste essere/avere: las distractoras avere refuerzan el error tipico sin volver ambigua la respuesta? Donde una variante use edad/posesion, la respuesta correcta es avere (no essere)?
    7. DECIDE la Open Q #1 (model IDs del quorum): D-23-13 nombra "Opus 4.8 + Sonnet 4.6" pero el skill emite literalmente `claude-opus-4-7`/`claude-sonnet-4-6` y todo el contenido previo (avere.json, essere.json legacy) usa esos IDs. Recomendacion del planner: mantener los IDs literales del skill por consistencia del audit trail (cambiarlos seria un cambio al skill, fuera del scope de contenido). Confirma "mantener IDs del skill" o indica que se actualicen.
    8. Confirma el alcance: generoso pero sin variantes artificiales en reglas que no se reformulan (D-23-05)?
    9. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quorum.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" (e indica la decision de model IDs: "mantener IDs del skill" recomendado) para enviar las superficies aprobadas al quorum cross-vendor, o lista las que descartar/reformular.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quorum cross-vendor (1-por-1) e integrarla al slot</name>
  <files>content/exercises/essere.json</files>
  <read_first>
    - .planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor + la decision de model IDs confirmada en el checkpoint)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales claude-opus-4-7/claude-sonnet-4-6, 1 commit por ejercicio; fallback `claude -p` headless si no hay Task, D-19-08)
    - content/exercises/essere.json (slots destino donde integrar las superficies tras pasar el quorum)
    - .planning/phases/22-avere-a-slots-contenido/22-02-PLAN.md (Task 2 analogo de Avere — el flujo real: 14 superficies, 3 disputes resueltas por reformulacion, slot nuevo con validation top-level, fallback DeepSeek reasoner cuando Gemini 429) y .planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md
  </read_first>
  <action>
    Para CADA superficie nueva aprobada, ejecutar el ciclo completo de quorum 1-por-1 (NUNCA batched): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/essere.json como entry temporal o tests/fixtures/*.json). (2) Base canonica Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus->Sonnet secuencial, 1 spawn; model IDs literales del skill segun la decision del checkpoint); fallback `claude -p --model claude-opus-4-7` / `--model claude-sonnet-4-6` headless con el ejercicio SIN su campo validation si no hay Task (D-19-08). (3) Refuerzo externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write` (si Gemini agota cuota 429, usar deepseek-chat + deepseek-reasoner como 2 by externos distintos, precedente 22-VERIFICATION). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4x "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` -> la superficie queda disputed y se resuelve por REFORMULACION (calidad > tokens, NO override-atajo; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de politica; si un externo alucina un acento sobre texto correcto, re-run sobre el MISMO texto, precedente Phase 19/22) o se descarta — NO entra al slot disputed. (5) Tras pasar el quorum, mover la superficie validada al variants[] del slot destino (existente, para celdas pobres, nacionalidad, localizacion absorbida) o crear el slot NUEVO `{id, type:"multiple-choice", categoryIds:["essere"], explanation, variants:[...], validation}` (ser/estar D-23-07, y cualquier otro slot nuevo que el autor aprobo); eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Para cada slot NUEVO, anadir el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum cross-vendor aprobado de sus variantes (D-19-09 — el gate VAL_07_STRICT lo lee a nivel slot; NO inventar validacion, elevar la aprobada). Reforzar el contraste essere/avere (D-23-06) en las explanations/distractoras de los slots tocados donde aplique, sin re-validar las superficies movidas intactas. Hacer 1 commit por superficie validada. NUNCA validar mas de una superficie en un mismo contexto/spawn. NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Essere; avere-only — a diferencia de 22-02 NO hay re-base de snapshot aqui).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs essere content/exercises/essere.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs essere content/exercises/essere.json` exits 0
    - Las celdas pobres de presente engordadas tienen >=2 variantes (verificable contando variants[] de los slots de presente en el JSON)
    - El slot NUEVO ser/estar existe con variants[] no vacio, explanation top-level del calco y validation.status=="validated" top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); const s=d.exercises.find(e=>e.id==='essere-ser-estar'); console.log('ser-estar ok:', !!s && Array.isArray(s.variants) && s.variants.length>0 && !!s.explanation && s.validation?.status==='validated')"` imprime `ser-estar ok: true`
    - Todos los slots con variants[] tienen validation.status top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('slots variants sin validation top-level:', d.exercises.filter(e=>Array.isArray(e.variants)&&e.validation?.status!=='validated').length)"` imprime `0`
    - Cada superficie NUEVA paso el quorum con >=4x "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se valido como legacy)
    - Ningun id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 23-01 NO se re-validaron (# invocaciones de quorum == # superficies nuevas)
    - Los 6 cruces siguen con id estable + 2 cats (no se tocaron): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/essere.json','utf8')); const c=d.exercises.filter(e=>/^essere-30[0-5]$/.test(e.id)); console.log('cruces:', c.length, 'con 2 cats:', c.filter(e=>e.categoryIds.length===2).length)"` imprime `cruces: 6 con 2 cats: 6`
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/essere.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Essere
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas pasaron el quorum cross-vendor (>=4x correcta) e integradas a sus slots; celdas pobres de presente engordadas a >=2 variantes; concordancia de nacionalidad + localizacion integradas; slot NUEVO ser/estar (D-23-07) creado con validation top-level (D-19-09); contraste essere/avere reforzado (D-23-06); validateContent verde; sin ids temporales residuales; cruces intactos; 1 commit por superficie; sin snapshot (no aplica a Essere).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio -> evaluador del quorum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podria intentar dirigir al evaluador |
| JSON de contenido editado a mano -> loader/validator | superficies nuevas redactadas a mano cruzan a validateContent y al render |

> Nota: esta fase autora contenido JSON en una app local single-user sin backend; el unico acceso externo son las API keys de `.env` (leidas, no instaladas) para el quorum. Sin red entrante, auth, ni input de terceros. No se esperan amenazas HIGH.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering | superficie nueva con respuesta ambigua (doble-validez R7), concordancia de nacionalidad/participio incorrecta, calco ser/estar mal contrastado, construccion avere/essere incorrecta o acento incorrecto entra al slot | mitigate | Quorum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); verificacion essere-no-avere (y avere-no-essere para edad/posesion) en Task 1 + checkpoint; pre-scan de acentos; gate de >=4x "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-23-06 | Tampering | prompt-injection en el quorum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instruccion; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-23-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes; leak de la forma de essere en el prompt (R1) | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown); verificacion R1 (sin leak) en Task 1 + checkpoint; el smoke de 23-03 re-asserta sobre variants[].prompt + explanation. |
| T-23-08 | Tampering | slot NUEVO (ser/estar) sin validation.status top-level cuela como `status: absent` en VAL_07_STRICT (regresion de 19-03 Rule-2) | mitigate | El slot NUEVO ser/estar (y cualquier otro) porta el bloque `validation` top-level (status validated + passes[]) reflejando el quorum aprobado de sus variantes (D-19-09); acceptance asserta `ser-estar ok: true` y `slots variants sin validation top-level == 0`. NO se inventa validacion: se eleva la del nivel variante. |
| T-23-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs essere content/exercises/essere.json` exits 0
- Cada superficie nueva: passes[] con >=4x "correcta", cero "incorrecta" (registrado en SUMMARY)
- Celdas pobres de presente engordadas a >=2 variantes; slot NUEVO ser/estar con validation top-level (D-19-09); concordancia de nacionalidad + localizacion integradas
- Cruces essere-300..305 intactos (id estable + 2 cats)
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Essere)
- `node --test tests/*.test.js` — el count de Essere sigue rojo a proposito hasta 23-03 (el conteo final ya es determinable tras este plan)
</verification>

<success_criteria>
- Variantes nuevas autoradas con ambicion generosa donde la regla admite reformulacion (D-23-04, sin cuota fija D-23-05), priorizando celdas pobres de presente
- Huecos pedagogicos cubiertos: concordancia de nacionalidad + localizacion con essere + SLOT NUEVO ser/estar (D-23-07) con validation top-level (D-19-09)
- Contraste essere/avere reforzado agresivamente (D-23-06): distractoras avere + explicaciones del calco (edad con avere, ser/estar->essere, posesion vs identidad/estado/localizacion)
- Cada superficie nueva paso el quorum cross-vendor R1-R7 (>=4x correcta) antes de entrar (base canonica Opus+Sonnet, D-23-13)
- Superficies existentes movidas NO re-validadas; cruces intactos
- validateContent verde; conteo final de slots determinable para 23-03; sin snapshot (no aplica a Essere)
</success_criteria>

<output>
Create `.planning/phases/23-essere-a-slots-contenido/23-02-SUMMARY.md` when done
</output>
