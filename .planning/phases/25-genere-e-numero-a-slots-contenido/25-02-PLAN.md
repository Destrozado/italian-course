---
phase: 25-genere-e-numero-a-slots-contenido
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - content/exercises/genero-numero.json
  - .planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md
autonomous: false
requirements: [GEN-02]
must_haves:
  truths:
    - "Cada variante NUEVA y cada slot NUEVO autorado pasa el quorum cross-vendor R1-R7 (base canonica Claude Opus + Sonnet via skill gsd-validate-exercise; refuerzo Gemini + DeepSeek via validate-ai-pass) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot; 1-por-1, fresh context, NUNCA batched (VAL-03)"
    - "Los 4 ejes de huecos PRIORIZADOS (D-25-04) se materializan con ambicion generosa sin cuota fija: (1) INVARIABLES — mas acentuados (virtu/citta/universita/caffe) + extranjerismos (film/sport/bar/computer) al slot invariabili, matando el calco -s; (2) PLURAL -co/-go sonido duro — la EXCEPCION amico->amici (PIERDE el sonido) + greco/nemico vs parco/medico/lago al slot sonido-duro; (3) GENERO -tore/-trice vs -e/-essa — engordar el contraste de feminizacion en sus dos slots; (4) PLURAL base -o/-a/-e — mas variantes de las reglas regulares"
    - "Cada palabra asignada a una micro-regla se VERIFICO que toma realmente esa forma de plural/femenino en italiano (D-25-04): el quorum cross-vendor caza cualquier excepcion inventada o forma erronea antes de entrar; NO se autoran irregulares puros (uomo/uomini, mano/mani -> Sostantivi irregolari Phase 27, OUT of scope)"
    - "Las variantes nuevas se autoran SOLO donde la regla admite reformulacion natural (sin cuota fija); ninguna lleva explanation propia (comparten la del slot); cualquier slot NUEVO lleva validation.status top-level (D-19-09)"
    - "Las superficies existentes movidas intactas en 25-01 NO se re-validan (D-25-08); ninguna explanation nueva/tocada referencia la categoria Articoli por id ni prosa (D-25-02)"
  artifacts:
    - path: "content/exercises/genero-numero.json"
      provides: "variantes nuevas validated integradas en variants[] de los slots por micro-regla, + cualquier slot nuevo con validation top-level"
      contains: "\"variants\""
    - path: ".planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md"
      provides: "set propuesto de superficies nuevas con id temporal + slot destino + superficie + justificacion R1-R7 + forma de plural/femenino verificada"
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quorum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS con ambicion GENEROSA (GEN-02, D-25-04) donde la regla admite reformulacion natural, materializando los 4 EJES DE HUECOS PRIORIZADOS de Genere e numero (D-25-04, el autor marco los 4):

1. INVARIABLES (trampa A1 estrella, engorde del slot `genero-numero-invariabili`): mas acentuados (virtu, citta, universita, caffe, liberta, qualita) + extranjerismos (film, sport, bar, computer, autobus). No cambian en plural -> mata el calco espanol -s (cittas/films/sports). Trampa A1 directa del hispanohablante. VERIFICAR (D-25-04) que cada palabra es realmente invariable (acentuada final O extranjerismo terminado en consonante).

2. PLURAL -co/-go sonido duro CON SU EXCEPCION (engorde del slot `genero-numero-plurale-co-chi` o nombre analogo): casos de CONSERVACION (parco->parchi, medico->? OJO medico->medici PIERDE el sonido, gioco->giochi, lago->laghi, albergo->alberghi) vs PERDIDA (amico->amici, greco->greci, nemico->nemici, medico->medici). La EXCEPCION amico->amici (pierde el sonido /k/) es la trampa real y NO esta en el set legacy. VERIFICAR el plural real de cada palabra (la regla de conservacion/perdida del sonido duro NO es totalmente predecible -> el quorum confirma cada forma; cuidado: amico/greco/nemico/medico/porco PIERDEN, parco/gioco/lago/albergo/fuoco CONSERVAN).

3. GENERO -tore/-trice vs -e/-ore -> -essa (engorde de los slots `genero-numero-femminile-trice` y `genero-numero-femminile-essa`): mas contraste de sufijos de feminizacion — attore/scrittore/pittore/lavoratore/cantante? (-tore->-trice) vs dottore/professore/studente/poeta? (-e/-ore->-essa) — cuando cada uno. VERIFICAR que cada palabra toma realmente -trice o -essa (NO inventar: lavoratore->lavoratrice, dottore->dottoressa).

4. PLURAL base -o/-a/-e (engorde de los slots `genero-numero-plurale-o-i`, `-a-e`, `-e-i`): mas variantes de las reglas regulares para dar variedad y que el examen no memorice por palabra (libro->libri, tavolo->tavoli, porta->porte, finestra->finestre, fiore->fiori, chiave->chiavi). VERIFICAR el plural real y el genero.

Cada superficie NUEVA pasa el quorum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched (VAL-03), con revision del autor por variante (D-85). Las superficies movidas intactas en 25-01 NO se re-validan (D-25-08). Sin cuota fija de densidad (D-25-04 hereda D-24-05): proponer variantes donde la regla lo pida, no por cumplir un minimo (evita variantes de relleno). RESTRICCION D-25-02: ninguna explanation nueva/tocada referencia la categoria Articoli por id ni prosa (el articulo como contenido del ejercicio es valido). NO autorar irregulares puros (uomo/uomini, mano/mani, uovo/uova -> pertenecen a Sostantivi irregolari Phase 27, OUT of scope aqui).

El planner/autor decide en plan-time/checkpoint si los huecos materializan VARIANTES de slots existentes (lo natural — los 4 ejes engordan los slots de 25-01) o algun SLOT NUEVO. Recomendacion del planner: NO se necesitan slots nuevos — los 4 ejes engordan los slots existentes (invariabili, sonido-duro, femminile-trice, femminile-essa, plurale-o-i/a-e/e-i). Si el autor crea algun slot nuevo, este requiere validation.status top-level (D-19-09).

Purpose: las variantes nuevas materializan el dolor anti-memorizacion (re-hacer un slot puede tocar una palabra distinta) y cierran los huecos pedagogicos de Genere e numero, todos centrados en las trampas A1 estrella del hispanohablante: el calco -s en invariables, la (im)prediccion del sonido duro en -co/-go, y el contraste de sufijos de feminizacion. El quorum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazo 8 bugs en Articoli, 3 en Avere, 2 en Essere que el human-verify aprobo) y la unica garantia de que cada palabra toma realmente la forma de plural/femenino reclamada (D-25-04 — la regla del sonido duro es la mas tramposa: amico PIERDE, parco CONSERVA). R1/R5 criticos: el prompt no debe leak la forma correcta; las options deben tener 3+ valores distintos (distractoras = formas erroneas plausibles, p.ej. el calco -s).

NO existe snapshot/assert APPEND-ONLY para Genere e numero (avere-only, verificado: 0 refs). NO ejecutar ningun script de snapshot ni assert de prefijo. NO existen cruces multi-cat (no hay genero-numero-300..305); NO crearlos. NO crear word-buttons (no existen en esta categoria). NO convertir ningun match a MC (los 3 match preservados son de 25-01, D-25-03/D-04). A diferencia de 22-02 (que re-basaba el snapshot de Avere), aqui NO hay paso de re-base — igual que 23-02/24-02.

Output: `genero-numero.json` con las variantes nuevas de los 4 ejes integradas; cada superficie nueva con su quorum verde en `validation.passes[]`; cualquier slot nuevo con validation.status top-level (D-19-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/genero-numero.json

<interfaces>
<!-- Pipeline de quorum cross-vendor. Verificado en validate-ai-pass.mjs + skill. NO modificar estos scripts/skill. Mismo flujo ejecutado con exito en Phase 19 (8 superficies), Phase 20 (6), Phase 22 (14), Phase 23 (14) y Phase 24, 0 excluidas. -->
Quorum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Base canonica (D-25-09) — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus->Sonnet):
  /gsd-validate-exercise <id>
  (model IDs literales que emite el skill: claude-opus-4-7 + claude-sonnet-4-6; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio. Mantener los IDs literales del skill por consistencia del audit trail con el contenido existente — confirmado en Phase 23 Open Q #1.)
  Fallback cuando Task no esta disponible al executor (precedente Phase 19/20/22/23/24, D-19-08): la mitad Claude se corre con `claude -p --model claude-opus-4-7` y `--model claude-sonnet-4-6` headless, 1 proceso por modelo por superficie, con el 09-VALIDATION-PROMPT verbatim y el ejercicio SIN su campo validation (no sesgar). Equivalente funcional 1-por-1 NUNCA batched.

Refuerzo — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondio; >=2 by distintos para el quorum; status re-derivado por deriveStatus, NO hand-editar status. Si Gemini agota cuota 429/503, DeepSeek cubre los 2 externos con by distintos chat + reasoner — precedente 22-VERIFICATION / 23-02 / 24-02.)

Gate D-17-07: el passes[] final de cada superficie nueva = >=4x "correcta" (opus + sonnet + 2 externos by distintos), CERO "incorrecta". deriveStatus -> "validated"; cualquier "incorrecta" -> "disputed" (sticky, bloquea, NO entra al slot).

Pitfall: el VALIDATION-PROMPT es legacy-payload-centrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/genero-numero.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quorum, mover la superficie al variants[] del slot (sin re-validacion) y eliminar el id temporal legacy.

Alcance D-25-04 (ambicion generosa, hereda D-24-05) — los 4 ejes priorizados, sin cuota fija:
- Engordar las celdas pobres marcadas en 25-01 + materializar los 4 ejes de huecos. NO inflar slots cuya regla no admite reformulacion natural; sin minimo por slot.

Los 4 ejes de huecos PRIORIZADOS (D-25-04, R1/R5 criticos, VERIFICAR la forma real de plural/femenino de cada palabra):
1. INVARIABLES (slot `genero-numero-invariabili`): acentuados (virtu, citta, universita, caffe, liberta, qualita) + extranjerismos (film, sport, bar, computer, autobus). Prompt fija "Un/una X, due ___"; respuesta = la MISMA forma; distractoras = el calco -s (cittas, films) + plurales inventados (citte, filmi). VERIFICAR (R1) que el prompt no leak la respuesta; (R5) options con 3+ valores distintos; que cada palabra es realmente invariable.
2. PLURAL -co/-go sonido duro CON EXCEPCION (slot sonido-duro): pares de CONSERVACION (parco->parchi, lago->laghi, gioco->giochi, albergo->alberghi) vs PERDIDA (amico->amici, greco->greci, nemico->nemici, medico->medici). La excepcion amico->amici es la trampa estrella. VERIFICAR el plural real de cada palabra (regla no totalmente predecible; el quorum confirma; cuidado: NO meter el plural equivocado — amici NO amichi, parchi NO parci).
3. GENERO -tore/-trice vs -e/-essa (slots femminile-trice y femminile-essa): mas contraste — attore/scrittore/pittore/lavoratore (-tore->-trice) vs dottore/professore/studente (-e/-ore->-essa). El prompt fija el masculino; respuesta = el femenino; distractoras = la feminizacion erronea (cruzar -essa/-trice, o -a generico). VERIFICAR que cada palabra toma realmente su sufijo.
4. PLURAL base -o/-a/-e (slots plurale-o-i, -a-e, -e-i): libro->libri, tavolo->tavoli (masc -o), porta->porte, finestra->finestre (fem -a), fiore->fiori, chiave->chiavi (-e). Distractoras = el calco -s y la regla cruzada (libre/libra). VERIFICAR el plural y el genero.
- El planner/autor decide en plan-time/checkpoint que huecos son naturales. Si un hueco no tiene construccion A1 natural o la palabra es un irregular puro (Phase 27), NO se materializa.

Validation top-level para la gate (D-19-09): si el autor crea algun slot NUEVO, debe portar el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum cross-vendor de sus variantes (el gate VAL_07_STRICT lee `ex.validation?.status` a nivel de slot). Si todos los huecos engordan slots existentes (recomendado), esos slots ya tienen su validation top-level de 25-01 (de las superficies fuente movidas) — NO degradarla; el passes[] de las variantes nuevas se anade donde el flujo lo registre, pero la gate de slot ya esta cubierta. NO inventar validacion: elevar la aprobada.

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quorum, un scan ASCII/tildes (italiano: città/università/caffè/virtù/libertà/qualità acentuados; espanol RAE en explanations; apostrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial.
Gloss ES (R7 canon): mantener "(en espanol: ...)" donde desambigue doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de politica (base de aprobacion = Claude Opus+Sonnet, D-25-09).
D-25-02: ninguna explanation nueva/tocada referencia la categoria Articoli por id ni prosa; el articulo como contenido del ejercicio es valido.

NO snapshot: Genere e numero no tiene snapshot/assert APPEND-ONLY (avere-only, verificado). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs ni buscar .genero-numero-prefix-snapshot.json. NO crear cruces 300..305 (no existen; fuera de scope). NO crear word-buttons. NO convertir match a MC. A diferencia de 22-02 NO hay re-base de snapshot aqui.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (los 4 ejes de huecos D-25-04) con forma de plural/femenino verificada</name>
  <files>.planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/genero-numero.json (estado tras 25-01 — los slots ya reagrupados por micro-regla, para saber que slots existen, cuales son celdas pobres y que admite variante nueva)
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md (las celdas pobres marcadas como candidatas + los 4 ejes de huecos anotados en 25-01)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints del quorum: 1-por-1, fresh context, NUNCA batched; model IDs literales)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-02-PLAN.md (Task 1 analogo — formato de 24-VARIANTES-NUEVAS.md a espejar) y .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md (la plantilla concreta, si existe; si no, 23-VARIANTES-NUEVAS.md)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6/R7 si accesible) y gloss_es_desambiguacion_canon.md (gloss ES canon)
  </read_first>
  <action>
    Proponer un set de superficies nuevas con ambicion GENEROSA (D-25-04) pero sin cuota fija, materializando los 4 EJES DE HUECOS PRIORIZADOS: (1) INVARIABLES para el slot `genero-numero-invariabili`: variantes con acentuados (virtu/citta/universita/caffe/liberta/qualita) + extranjerismos (film/sport/bar/computer/autobus); prompt "Un/una X, due ___", respuesta = misma forma, distractoras = el calco -s (cittas, films) + plurales inventados; VERIFICAR por superficie que la palabra es realmente invariable (acentuada final O extranjerismo en consonante). (2) PLURAL -co/-go sonido duro CON EXCEPCION para el slot sonido-duro: pares de CONSERVACION (parco->parchi, lago->laghi, gioco->giochi) vs PERDIDA (amico->amici, greco->greci, nemico->nemici, medico->medici); VERIFICAR el plural real de cada (la regla NO es totalmente predecible — confirmar amico->amici PIERDE, parco->parchi CONSERVA; NO meter formas inventadas como amichi/parci). (3) GENERO -tore/-trice vs -e/-essa para los slots femminile-trice y femminile-essa: attore/scrittore/pittore/lavoratore (-tore->-trice) vs dottore/professore/studente (-e/-ore->-essa); prompt fija el masculino, respuesta = el femenino, distractoras = feminizacion cruzada (-essa donde va -trice y viceversa); VERIFICAR que cada palabra toma realmente su sufijo. (4) PLURAL base -o/-a/-e para los slots plurale-o-i/a-e/e-i: libro/tavolo (masc -o->-i), porta/finestra (fem -a->-e), fiore/chiave (-e->-i); VERIFICAR el plural y el genero. VERIFICAR explicitamente por superficie: (R1) el prompt no leak la forma correcta; (R5) las options tienen 3+ valores distintos; que la palabra toma realmente la forma reclamada en italiano A1 (D-25-04 — no inventar excepciones; NO irregulares puros uomo/mano/uovo, esos son Phase 27). Cumplir R1-R7 (sin leak en prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigue doble-validez). RESTRICCION D-25-02: ninguna explanation referencia la categoria Articoli por id ni prosa; el articulo como contenido del ejercicio es valido. Pasar un scan de acentos/ASCII (tildes italianas città/università/caffè/virtù acentuadas; RAE en explanations espanolas; apostrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. Producir `.planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md` (espejar el formato de 24-VARIANTES-NUEVAS.md): lista de cada superficie nueva con su id temporal (para el quorum aislado), slot destino (existente — recomendado que los 4 ejes engorden los slots de 25-01; marcar cualquier slot NUEVO si el autor lo pide), eje al que pertenece (1-4), prompt/options/correctIndex/explanation, la justificacion de por que la regla admite reformulacion, y la VERIFICACION de la forma real de plural/femenino de la palabra. Recomendar en una seccion que NO se necesitan slots nuevos (los 4 ejes engordan los slots existentes) — surfacear al autor. Reportar el conteo de superficies nuevas propuestas por eje Y cuantas (si alguna) son SLOTS NUEVOS (los slots nuevos suben el count de 25-03; las variantes no).
  </action>
  <verify>
    <automated>test -f .planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md && grep -ci "invariab\|amico\|parco\|trice\|essa\|plural\|extranjer\|sonido" .planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 25-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + eje (1-4) + superficie + explanation
    - EJE 1 (invariables) cubierto: variantes con acentuados (virtu/citta/universita/caffe/...) + extranjerismos (film/sport/bar/...) para el slot invariabili, cada una con la verificacion de que la palabra es invariable y con distractora del calco -s
    - EJE 2 (sonido duro -co/-go con excepcion) cubierto: pares CONSERVACION (parco->parchi) vs PERDIDA (amico->amici, greco->greci, nemico->nemici, medico->medici) para el slot sonido-duro, con la verificacion del plural real de cada palabra (la excepcion amico->amici explicitamente presente)
    - EJE 3 (genero -tore/-trice vs -e/-essa) cubierto: variantes -tore->-trice (attore/scrittore/pittore/lavoratore) y -e/-essa (dottore/professore/studente) para sus dos slots, con la verificacion del sufijo real
    - EJE 4 (plural base -o/-a/-e) cubierto: variantes regulares para los slots plurale-o-i/a-e/e-i con el plural y genero verificados
    - Cada superficie nueva cumple R1 (sin leak de la forma correcta en el prompt) y R5 (options con 3+ valores distintos); cada explanation/prompt pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers; ninguna referencia la categoria Articoli por id/prosa (D-25-02)
    - Cada palabra lleva su VERIFICACION de forma real de plural/femenino (D-25-04) — no se inventan excepciones; NO hay irregulares puros (uomo/mano/uovo — Phase 27)
    - Hay una seccion que recomienda NO crear slots nuevos (los 4 ejes engordan los slots existentes), surfaceada al autor
    - El conteo de superficies nuevas propuestas por eje Y de slots nuevos (si alguno) esta reportado
  </acceptance_criteria>
  <done>
    25-VARIANTES-NUEVAS.md lista el set generoso de superficies nuevas de los 4 ejes D-25-04 (invariables + sonido duro con excepcion amico->amici + genero -trice/-essa + plural base) con superficies R1-R7-conformes, scan de acentos pasado, sin refs a la categoria Articoli, y la verificacion de la forma real de plural/femenino de cada palabra (sin irregulares puros); recomendacion de no crear slots nuevos surfaceada; conteo de superficies por eje y de slots nuevos reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (25-VARIANTES-NUEVAS.md) — los 4 ejes de huecos D-25-04: invariables (acentuados virtu/citta/universita + extranjerismos film/sport/bar/computer), plural -co/-go sonido duro con su excepcion (amico->amici PIERDE vs parco->parchi CONSERVA), genero -tore/-trice vs -e/-essa, plural base -o/-a/-e — cumpliendo R1-R7, con scan de acentos y la verificacion de la forma real de plural/femenino de cada palabra. AUN NO validadas por quorum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md`.
    2. EJE 1 (invariables): virtu/citta/universita/caffe (acentuados) y film/sport/bar/computer/autobus (extranjerismos) son realmente invariables en plural? El prompt NO contiene la respuesta (R1)? Las options incluyen el calco -s (cittas/films) como distractora (R5)?
    3. EJE 2 (sonido duro con excepcion): amico->amici PIERDE el sonido /k/ (NO amichi), greco->greci, nemico->nemici, medico->medici PIERDEN; parco->parchi, lago->laghi, gioco->giochi CONSERVAN. Confirma cada plural — esta es la regla mas tramposa (no totalmente predecible). Las distractoras son formas erroneas plausibles (amichi/parci)?
    4. EJE 3 (genero): attore->attrice, scrittore->scrittrice, pittore->pittrice, lavoratore->lavoratrice (-tore->-trice); dottore->dottoressa, professore->professoressa, studente->studentessa (-e/-ore->-essa). Confirma que cada palabra toma su sufijo (no cruzados). Las distractoras cruzan -essa/-trice?
    5. EJE 4 (plural base): libro->libri, tavolo->tavoli, porta->porte, finestra->finestre, fiore->fiori, chiave->chiavi. Plural y genero correctos?
    6. Confirma que NINGUNA explanation referencia la categoria Articoli por id ni prosa (D-25-02) — el articulo como contenido del ejercicio SI es valido.
    7. Confirma que NO hay irregulares puros (uomo/uomini, mano/mani, uovo/uova — esos son Sostantivi irregolari Phase 27, fuera de scope).
    8. Confirma el alcance: generoso pero sin variantes artificiales en reglas que no se reformulan (D-25-04); decide si algun eje crea un slot nuevo (recomendado: NO, todos engordan slots existentes).
    9. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quorum (especialmente cualquier plural de -co/-go cuya forma dudes — la regla del sonido duro es la mas dificil de predecir).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las superficies aprobadas al quorum cross-vendor, o lista las que descartar/reformular (y cualquier palabra cuyo plural/femenino quieras que el quorum confirme con prioridad — especialmente los -co/-go del eje 2).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quorum cross-vendor (1-por-1) e integrarla al slot</name>
  <files>content/exercises/genero-numero.json</files>
  <read_first>
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales claude-opus-4-7/claude-sonnet-4-6, 1 commit por ejercicio; fallback `claude -p` headless si no hay Task, D-19-08)
    - content/exercises/genero-numero.json (slots destino donde integrar las superficies tras pasar el quorum)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-02-PLAN.md (Task 2 analogo — el flujo real: superficies por eje, disputes resueltas por reformulacion, fallback DeepSeek reasoner cuando Gemini 429) y .planning/phases/23-essere-a-slots-contenido/23-02-PLAN.md (precedente con slot nuevo + validation top-level)
  </read_first>
  <action>
    Para CADA superficie nueva aprobada, ejecutar el ciclo completo de quorum 1-por-1 (NUNCA batched, VAL-03): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/genero-numero.json como entry temporal o tests/fixtures/*.json). (2) Base canonica Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus->Sonnet secuencial, 1 spawn; model IDs literales del skill claude-opus-4-7/claude-sonnet-4-6); fallback `claude -p --model claude-opus-4-7` / `--model claude-sonnet-4-6` headless con el ejercicio SIN su campo validation si no hay Task (D-19-08). (3) Refuerzo externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write` (si Gemini agota cuota 429/503, usar deepseek-chat + deepseek-reasoner como 2 by externos distintos, precedente 22-VERIFICATION / 23-02 / 24-02). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4x "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` -> la superficie queda disputed y se resuelve por REFORMULACION (calidad > tokens, NO override-atajo; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de politica; si un externo alucina un acento sobre texto correcto, re-run sobre el MISMO texto, precedente Phase 19/22/23) o se descarta — NO entra al slot disputed. PRESTAR ESPECIAL ATENCION (D-25-04) a que el quorum confirme la FORMA REAL de plural/femenino: si una superficie de -co/-go resulta `incorrecta` por usar un plural inventado (amichi en vez de amici, parci en vez de parchi), reformular o descartar; idem si una invariable usa el calco -s como respuesta correcta por error, o un femenino cruza -trice/-essa. (5) Tras pasar el quorum, mover la superficie validada al variants[] del slot destino (existente, para los 4 ejes: invariabili, sonido-duro, femminile-trice, femminile-essa, plurale-o-i/a-e/e-i) o crear el slot NUEVO `{id, type:"multiple-choice", categoryIds:["genero-numero"], explanation, variants:[...], validation}` SOLO si el autor lo aprobo; eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Para cualquier slot NUEVO, anadir el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum aprobado de sus variantes (D-19-09). Para los slots EXISTENTES engordados, NO degradar su validation top-level de 25-01 (ya cubre la gate de slot). NO re-validar las superficies movidas intactas en 25-01 (D-25-08). RESTRICCION D-25-02: ninguna explanation tocada referencia la categoria Articoli por id ni prosa. Hacer 1 commit por superficie validada. NUNCA validar mas de una superficie en un mismo contexto/spawn (VAL-03). NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Genere e numero; avere-only). NO crear cruces 300..305. NO crear word-buttons. NO convertir ningun match a MC (los 3 match de 25-01 se preservan intactos).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs genero-numero content/exercises/genero-numero.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs genero-numero content/exercises/genero-numero.json` exits 0
    - Los 4 ejes engordaron sus slots: invariabili, sonido-duro, femminile-trice, femminile-essa y los plurale-o-i/a-e/e-i tienen mas variantes que tras 25-01 (verificable contando variants[] de cada slot)
    - El eje 2 incluye la excepcion amico->amici (PIERDE el sonido) como variante validada del slot sonido-duro: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); const s=d.exercises.find(e=>/co-chi|sonido|duro|co-go/.test(e.id)); const has=s&&s.variants.some(v=>/amici/.test(JSON.stringify(v))); console.log('amico->amici presente:', !!has)"` imprime `true`
    - Cada superficie NUEVA paso el quorum con >=4x "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se valido como legacy)
    - Todo slot con variants[] sigue con validation.status=="validated" top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('slots variants sin validation top-level:', d.exercises.filter(e=>Array.isArray(e.variants)&&e.validation?.status!=='validated').length)"` imprime `0`
    - Ningun id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 25-01 NO se re-validaron (# invocaciones de quorum == # superficies nuevas)
    - Los 3 match SIGUEN siendo match y NO se convirtieron a MC (D-25-03/D-04): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('match slots:', d.exercises.filter(e=>e.type==='match').length)"` imprime el mismo numero que tras 25-01 (>=1)
    - NO se crearon cruces ni word-buttons: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('cruces:', d.exercises.filter(e=>/-30[0-5]$/.test(e.id)).length, 'wb:', d.exercises.filter(e=>e.type==='word-buttons').length)"` imprime `cruces: 0 wb: 0`; todos los slots categoryIds=["genero-numero"]
    - Ninguna explanation referencia la categoria Articoli por id/prosa: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('refs Articoli-cat:', d.exercises.filter(e=>/articoli-0\d\d|ver Articoli|categor.a Articoli/i.test(e.explanation||'')).length)"` imprime `0` (D-25-02)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/genero-numero.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Genere e numero
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas de los 4 ejes D-25-04 pasaron el quorum cross-vendor (>=4x correcta) e integradas a sus slots; invariables + sonido duro con excepcion amico->amici + genero -trice/-essa + plural base integrados; forma de plural/femenino de cada palabra confirmada por el quorum (D-25-04); 3 match siguen siendo match (D-25-03/D-04); validateContent verde; sin ids temporales residuales; sin cruces ni word-buttons; sin refs a la categoria Articoli; 1 commit por superficie; sin snapshot (no aplica a Genere e numero).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio -> evaluador del quorum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podria intentar dirigir al evaluador |
| JSON de contenido editado a mano -> loader/validator (x-text, nunca x-html, T-02-01) | superficies nuevas redactadas a mano cruzan a validateContent y al render como texto plano |

> Nota: esta fase autora contenido JSON en una app local single-user, offline, sin backend; el unico acceso externo son las API keys de `.env` (leidas, no instaladas) para el quorum. Sin red entrante, auth, ni input de terceros. No se esperan amenazas HIGH/MEDIUM.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering | superficie nueva con forma de plural/femenino incorrecta (plural inventado amichi/parci en -co/-go, calco -s como respuesta correcta en invariable, femenino cruzado -trice/-essa, irregular puro fuera de scope), respuesta ambigua (doble-validez R7) o acento incorrecto entra al slot | mitigate | Quorum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); verificacion de la forma real de cada palabra en Task 1 + checkpoint (D-25-04, no inventar excepciones; NO irregulares puros); atencion especial a la regla no-predecible del sonido duro -co/-go (amico PIERDE, parco CONSERVA); pre-scan de acentos; gate de >=4x "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-25-06 | Tampering | prompt-injection en el quorum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instruccion; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-25-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes (rompe T-02-01 x-text); leak de la forma correcta en el prompt (R1) | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown); verificacion R1 (sin leak) en Task 1 + checkpoint; el smoke de 25-03 re-asserta sobre variants[].prompt + explanation. Todo string permanece plain text -> x-text seguro. |
| T-25-02b | Information disclosure / governance | una explanation nueva referencia la CATEGORIA Articoli por id o prosa, violando D-25-02 | mitigate | Verificacion en Task 1 + checkpoint; acceptance de Task 2 asserta 0 refs a "articoli-0XX"/"ver Articoli"/"categoria Articoli"; el articulo como contenido del ejercicio es valido. |
| T-25-08 | Tampering | slot NUEVO (si lo hubiera) sin validation.status top-level cuela como `status: absent` en VAL_07_STRICT | mitigate | Cualquier slot NUEVO porta el bloque `validation` top-level reflejando el quorum aprobado (D-19-09); acceptance asserta `slots variants sin validation top-level == 0`. Si todos los ejes engordan slots existentes, su validation de 25-01 ya cubre la gate. |
| T-25-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. No hay tabla de Package Legitimacy porque no hay installs. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs genero-numero content/exercises/genero-numero.json` exits 0
- Cada superficie nueva: passes[] con >=4x "correcta", cero "incorrecta" (registrado en SUMMARY); forma de plural/femenino de cada palabra confirmada por el quorum (D-25-04)
- Los 4 ejes de huecos D-25-04 materializados (incluida la excepcion amico->amici); 3 match siguen siendo match (D-25-03/D-04); sin cruces ni word-buttons; sin refs a la categoria Articoli (D-25-02)
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Genere e numero)
- `node --test tests/*.test.js` — el count de Genere e numero sigue rojo a proposito hasta 25-03 (el conteo final ya es determinable tras este plan; usar el glob `tests/*.test.js`, path desnudo falla en Node 22.20)
</verification>

<success_criteria>
- Variantes nuevas autoradas con ambicion generosa donde la regla admite reformulacion (D-25-04, sin cuota fija), materializando los 4 ejes priorizados: invariables + sonido duro con excepcion (amico->amici) + genero -tore/-trice vs -e/-essa + plural base -o/-a/-e
- Cada palabra verificada por el quorum como forma real de plural/femenino (D-25-04, no se inventan excepciones; sin irregulares puros — Phase 27)
- Cada superficie nueva paso el quorum cross-vendor R1-R7 (>=4x correcta) antes de entrar (base canonica Opus+Sonnet, D-25-09)
- Superficies existentes movidas NO re-validadas; 3 match siguen siendo match (D-25-03/D-04); sin cruces ni word-buttons; ninguna explanation referencia la categoria Articoli (D-25-02)
- validateContent verde; conteo final de slots determinable para 25-03; sin snapshot (no aplica a Genere e numero)
</success_criteria>

<output>
Create `.planning/phases/25-genere-e-numero-a-slots-contenido/25-02-SUMMARY.md` when done
</output>
