---
phase: 25-genere-e-numero-a-slots-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/genero-numero.json
  - .planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md
autonomous: false
requirements: [GEN-01]
must_haves:
  truths:
    - "Los 40 ejercicios de Genere e numero quedan reagrupados a 1 SLOT POR MICRO-REGLA (D-25-01, granularidad fina, NO por familia gruesa): cada terminacion concreta de plural/genero es un slot propio (p.ej. plural -o/-i, plural -a/-e, plural -e/-i, plural sonido duro -co/-go->-chi/-ghi, invariables, femenino -o/-a, femenino -tore/-trice, femenino -e/-ore->-essa, articulo por sonido) — ~10-13 slots rough; el examen elige 1 variante al azar DENTRO de cada micro-regla"
    - "Los duplicados literales se colapsan a variantes del MISMO micro-slot (D-25-01): 001==011 (ragazzo->ragazzi), 002==012 (casa->case), 003==030 (cuoco->cuochi), 004==031 (albergo->alberghi), 005==032 (amica->amiche), 008==035 (caffe) — no slots duplicados"
    - "Las invariables (acentuados citta/caffe/universita + extranjerismos film) quedan en UN slot dedicado invariabili (D-25-01): no cambian en plural; es la trampa A1 estrella del calco espanol -s (cittas/films)"
    - "El plural sonido duro -co/-go va a su slot propio separado del plural regular -o/-i (D-25-01): la insercion de H para mantener /k//g/ (cuoco->cuochi, parco->parchi, amica->amiche) es una regla con trampa propia; la excepcion amico->amici (que PIERDE el sonido) NO esta en el set legacy y se reserva al engorde de 25-02"
    - "El articulo definido por sonido (009 l'amico, 010 lo zaino MC + los 3 match 207/208/209) se queda (boundary fijo 40 ejercicios) en su(s) slot(s) propio(s) (D-25-02); los 3 match PRESERVAN type:match (D-25-03/D-04: articulo-por-sonido NO es derivable por raiz); las explanation NO referencian la categoria Articoli por id ni por prosa (D-25-02, analogo a D-159) — usar el articulo como contenido del ejercicio es valido e inevitable"
    - "Cada slot tiene una explanation top-level no vacia (merge elegir-la-mas-completa + injertar matices, D-25-07), rule-first; ninguna variante lleva explanation propia; payload eliminado de los 40 ejercicios; ids TODOS semanticos {categoria}-{regla} (D-25-06) — NO hay cruces cross-cat (no existen genero-numero-300..305, verificado)"
    - "Las superficies existentes movidas intactas a variants[] NO se re-validan (D-25-08, cosmetico); los validation.passes[] se mueven verbatim (incluidas resoluciones disputed con override del autor); el JSON sigue pasando validateContent"
    - "NO existe ni se necesita snapshot/assert APPEND-ONLY para Genere e numero (los scripts snapshot-/assert-avere-prefix son avere-only, 0 refs a genero-numero, verificado); NO se replica la tarea de re-base D-88 (D-25-10)"
  artifacts:
    - path: "content/exercises/genero-numero.json"
      provides: "Genere e numero reagrupado a shape slot+variantes (payload eliminado, variants[]+explanation top-level por slot, 3 match preservados)"
      contains: "\"variants\""
    - path: ".planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md"
      provides: "Mapa id-fuente -> micro-slot revisado por el autor (artefacto de auditoria); cubre los 40 ids 1:1; colapsa los 6 pares de duplicados literales a variantes; reporta el conteo de slots que fija el sync de 25-03 ANTES del rewrite"
  key_links:
    - from: "content/exercises/genero-numero.json"
      to: "validateContent / normalizeExerciseToSlot"
      via: "shape slot+variantes (payload XOR variants[])"
      pattern: "\"variants\"\\s*:\\s*\\["
    - from: "los 3 match 207/208/209"
      to: "slot(s) de articulo por sonido con type:match (D-25-03/D-04)"
      via: "preservacion del tipo match en shape slot+variantes (variant = {prompt, pairs})"
      pattern: "\"type\"\\s*:\\s*\"match\""
---

<objective>
Reagrupar los 40 ejercicios reales de Genere e numero a 1 SLOT POR MICRO-REGLA (D-25-01) replicando el patron de los plans 24-01 (Verbi di movimento) / 23-01 (Essere) / 22-01 (Avere), pero con el eje organizador propio de esta categoria. A diferencia de Verbi di movimento (UNA sola regla essere-vs-avere -> pocos slots), Genere e numero es RULE-RICH: tres familias de regla en paralelo (formacion de plural por terminacion, formacion de femenino por terminacion, articulo definido por sonido), cada una con varias micro-reglas que tienen trampa propia. La granularidad FINA (1 slot por micro-regla, NO por familia gruesa) es una DIVERGENCIA CONSCIENTE vs el "pocos slots por sub-regla" de Verbi di movimento (D-24-02): aqui mezclar `-o/-i` con `-co/-go` en un mismo slot difuminaria dos reglas con trampa distinta. El examen elige 1 variante al azar DENTRO de cada micro-regla -> mata la memorizacion por palabra sin mezclar reglas.

Micro-reglas a aislar en slots (el mapa las concreta y nombra; ids semanticos D-25-06):

1. PLURAL `-o/-i` (masc regular): genero-numero-plurale-o-i. Superficies fuente: 001 (ragazzo->ragazzi), 011 (ragazzo, dup de 001), 013 (libro->libri). [El autor decide en el checkpoint si 037 psicologo->psicologi entra aqui o en el slot sonido-duro — psicologo CONSERVA el sonido /g/ via -gi, ojo: psicologi NO inserta H; ver nota abajo.]

2. PLURAL `-a/-e` (fem regular): genero-numero-plurale-a-e. Superficies fuente: 002 (casa->case), 012 (casa, dup de 002), 014 (mela->mele).

3. PLURAL `-e/-i` (ambos generos, terminacion -e): genero-numero-plurale-e-i. Superficies fuente: 015 (padre->padri), 016 (madre->madri), 017 (studente->studenti), 018 (cane->cani).

4. PLURAL sonido duro `-co/-go -> -chi/-ghi`: genero-numero-plurale-co-chi (o nombre analogo). Insercion de H para mantener /k//g/. Superficies fuente: 003 (cuoco->cuochi), 030 (cuoco, dup de 003), 004 (albergo->alberghi), 031 (albergo, dup de 004), 005 (amica->amiche), 032 (amica, dup de 005), 029 (parco->parchi). NOTA: en el set legacy TODOS conservan el sonido (insertan H); la excepcion amico->amici (que PIERDE el sonido) NO esta en el legacy -> se reserva al engorde de 25-02 (D-25-04 eje 2). El autor decide si 037 (psicologo->psicologi, que conserva sonido via -gi sin H) va aqui o al slot -o/-i; recomendacion del planner: 037 combina dos reglas (articulo `lo` ante ps- + plural -go->-gi) -> es candidato a slot propio o a este slot con nota; anotar para confirmacion del autor.

5. INVARIABLES (acentuados + extranjerismos): genero-numero-invariabili. No cambian en plural. Superficies fuente: 008 (caffe->caffe), 035 (caffe, dup de 008), 033 (citta->citta), 034 (universita->universita), 036 (film->film). Trampa A1 estrella (calco -s: cittas/films) -> engorde generoso en 25-02 (D-25-04 eje 1).

6. FEMENINO `-o/-a` (masc->fem regular): genero-numero-femminile-o-a. Superficies fuente: 019 (amico->amica), 020 (maestro->maestra), 021 (ragazzo->ragazza), 022 (gatto->gatta).

7. FEMENINO `-tore/-trice`: genero-numero-femminile-trice. Superficies fuente: 006 (attore->attrice), 023 (scrittore->scrittrice), 024 (pittore->pittrice), 025 (lavoratore->lavoratrice).

8. FEMENINO `-e/-ore -> -essa`: genero-numero-femminile-essa. Superficies fuente: 007 (dottore->dottoressa), 028 (dottore, dup de 007), 026 (professore->professoressa), 027 (studente->studentessa).

9. ARTICULO definido por sonido (MC): genero-numero-articolo-suono. Superficies fuente: 009 (l'amico, elision ante vocal), 010 (lo zaino, s+cons/z/ps/gn). El autor decide si son 1 slot o 2 (D-25-02 / Claude's Discretion). 037 (lo psicologo) combina articulo + plural -> ver nota arriba.

10/11. MATCH articulo por sonido (D-25-03/D-04, PRESERVAN type:match): 207 (sing sustantivo<->articulo il/la/l'), 208 (masc sing zaino/amico/studente/ragazzo -> lo/l'/lo/il), 209 (plural sustantivo<->articulo i/le/gli). El autor decide (Claude's Discretion) si quedan como 3 slots-de-1 type:match o se agrupan en menos slots de match (los que entrenan la MISMA regla reformulada pueden ser variantes del mismo slot match). 207 y 208 son singular (il/lo/l'/la) -> candidatos a fundirse; 209 es plural (i/gli/le) -> regla distinta -> slot propio. Recomendacion del planner: 207+208 fusionables a 1 slot match-singular, 209 slot match-plural -> 2 match slots; el autor confirma.

DUPLICADOS LITERALES (D-25-01, colapsar a variantes del mismo micro-slot, NO slots duplicados): 001==011 (ragazzo), 002==012 (casa), 003==030 (cuoco), 004==031 (albergo), 005==032 (amica), 008==035 (caffe). Las dos superficies del par (con distinto orden de distractoras) son VARIANTES INTERCAMBIABLES del mismo slot.

Producir la explanation de cada slot via elegir-la-mas-completa + injertar matices (D-25-07), rule-first (lidera con la regla/disparador, no con un sustantivo). RESTRICCION LOCKED D-25-02 (analoga a D-159 de Essere/Verbi di movimento): las explanation NO referencian la CATEGORIA Articoli por id ni por prosa (cero "ver Articoli", cero ids `articoli-0XX`); usar el articulo (il/lo/l'/i/gli/le) como CONTENIDO del ejercicio SI es valido e inevitable (el articulo concuerda con genero/numero — es la regla del slot de articulo). Las superficies existentes se MUEVEN intactas a `variants[]` (cambio de contenedor -> NO re-validacion, D-25-08; los validation.passes[] se mueven verbatim, incluidas resoluciones disputed->override del autor si las hubiera). Ids TODOS semanticos (D-25-06 — NO hay cruces cross-cat en esta categoria, no existen genero-numero-300..305, verificado).

DIVERGENCIA CRITICA #1 vs Verbi di movimento: la granularidad es FINA (D-25-01, 1 slot por micro-regla), NO gruesa. Un mapa que mete `-o/-i`, `-a/-e`, `-co/-go` e invariables en un solo slot "plural" es un ERROR; D-25-01 exige aislar cada micro-regla con trampa propia.

DIVERGENCIA CRITICA #2 (preservar match, D-25-03/D-04): los 3 match (207/208/209) NO se convierten a multiple-choice. Articulo-por-sonido (lo/l'/gli dependen del sonido inicial, no de la terminacion) es regla NO derivable por raiz -> match preservado por DESIGN RULE D-04. Referencia de shape: articoli.json:articoli-049 es un slot match en shape slot+variantes (`variant = {prompt, pairs}`, explanation+validation top-level).

SIMPLIFICACION vs Avere/Essere: Genere e numero NO tiene word-buttons (verificado: 37 MC + 3 match, 0 word-buttons) NI cruces multi-cat (no hay genero-numero-300..305) NI snapshot APPEND-ONLY (avere-only, verificado). TODOS los ids semanticos, categoryIds=["genero-numero"] en todos los slots.

Este plan NO autora variantes nuevas (eso es 25-02) ni anade slots de huecos de regla (eso es 25-02). NO toca los counts de tests/scripts (eso es 25-03, que sincroniza contra el conteo REAL final tras 25-02).

Purpose: es la columna vertebral de la fase — convierte el contenido real de Genere e numero al modelo slot+variantes que el motor de Phase 16 ya consume. Primera de las 3 categorias de morfologia/lexico de v1.6.

Output: `genero-numero.json` reescrito a slot+variantes (sin payload, con variants[]+explanation top-level por slot, 3 match preservados), validando verde; mapa de reagrupacion auditado por el autor con el conteo de slots reportado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/genero-numero.json
@content/exercises/articoli.json

<interfaces>
<!-- Shape de slot con variantes que enforza el validator. NO modificar loader ni validator: ya aceptan variants[] desde Phase 15 y bifurcan por shape. articoli.json (slot match-en-variantes articoli-049 = referencia EXACTA del match convertido) + essere.json/avere.json (MC slots) son las referencias AFTER. OJO: ni articoli ni genero-numero tienen cruces 300..305 aqui -> NO copiar esa columna de Essere/Avere. -->
Shape autorado de un slot con variantes (verificado contra articoli.json/essere.json):
- `id`: string id de slot (= exercise id). Esquema = renumeracion semantica limpia TODOS los slots (D-25-06: NO hay cruces cross-cat -> NO hay rango reservado): p.ej. `genero-numero-plurale-o-i`, `genero-numero-plurale-a-e`, `genero-numero-plurale-e-i`, `genero-numero-plurale-co-chi`, `genero-numero-invariabili`, `genero-numero-femminile-o-a`, `genero-numero-femminile-trice`, `genero-numero-femminile-essa`, `genero-numero-articolo-suono`, `genero-numero-match-singolare`, `genero-numero-match-plurale`. Unicidad obligatoria. Reusar el patron `{categoria}-{regla}` (D-15-09).
- `type`: "multiple-choice" | "match" a NIVEL de slot. Los slots de plural/femenino/articulo-MC = "multiple-choice"; los 207/208/209 = "match" (PRESERVADO, D-25-03/D-04). Genere e numero NO tiene word-buttons (verificado).
- `categoryIds`: array no vacio a nivel de slot. `["genero-numero"]` para TODOS los slots (no hay cruces). [VERIFIED: los 40 ejercicios fuente son categoryId unico genero-numero]
- `explanation`: string no vacio OBLIGATORIO a nivel de slot. Merge D-25-07: base = la explanation fuente mas completa/general del grupo + injertar los matices unicos de las descartadas. RESTRICCION D-25-02: cero refs a la CATEGORIA Articoli (ni "ver Articoli" ni "articoli-0XX"); el articulo como contenido del ejercicio SI. Rule-first (lidera con la micro-regla: "-o masc -> -i plural", "-co/-go inserta H para mantener el sonido duro", "invariables: acentuados y extranjerismos NO cambian", "-tore -> -trice", "-e/-ore -> -essa", "articulo por sonido inicial"). El campo `notes` legacy es autor-internal (NO se mueve a variants[]); su matiz pedagogico relevante se injerta en la explanation del slot si aporta (D-25-07).
- `variants`: array NO vacio. Cada variante MC = objeto plano `{prompt (con ___), options (3-4 strings), correctIndex (en rango)}`. Cada variante match = `{prompt, pairs[]}` (como articoli-049). La variante NO lleva `explanation` ni `validation`.
- `validation`: top-level `{status, passes[]}` — se preserva del/los ejercicio(s) fuente verbatim (D-25-08); incluye cualquier disputed->override del autor. Para slots multi-variante combinar segun el criterio del mapa (la del ejercicio cuya superficie/explanation domina o la fusion de passes[]); registrar el criterio en el SUMMARY.

Reglas hard del validator (verificadas contra scripts/validate-content-fixture.mjs):
- `payload` XOR `variants[]` — nunca ambos, nunca ninguno. Al convertir se ELIMINA `payload`.
- `explanation` top-level string no vacio cuando hay `variants[]`.
- `variants[]` no vacio; superficie validada por el SURFACE_VALIDATOR del `type` (MC: prompt con `___`, 3-4 options, correctIndex en rango; match: pairs[] no vacio).
- Apostrofes ASCII U+0027 si aparecen (l'amico, l' attore); sin smart-quotes (el smoke FALLA con smart-quotes). El legacy ya usa apostrofes ASCII y tildes (caffe->caffè, citta->città, universita->università) — preservar verbatim; scan de acentos sobre superficies movidas (Phase 19 WR-01/02).

Inventario fuente verificado (40 ejercicios, 37 MC + 3 match, categoryId unico, 40 con payload / 0 con variants):
- PLURAL -o/-i: 001, 011(dup), 013 | -a/-e: 002, 012(dup), 014 | -e/-i: 015, 016, 017, 018 | sonido duro -co/-go: 003, 030(dup), 004, 031(dup), 005, 032(dup), 029, [037 combina articulo+plural -go]
- INVARIABLES: 008, 035(dup), 033, 034, 036
- FEMENINO -o/-a: 019, 020, 021, 022 | -tore/-trice: 006, 023, 024, 025 | -e/-essa: 007, 028(dup), 026, 027
- ARTICULO MC: 009, 010 [, 037 articulo+plural] | MATCH: 207, 208, 209

Mapa de reagrupacion PROPUESTO (verificado contra los notes/prompt/options/correctIndex de los 40 ids; el planner lo deriva, el autor lo refina en el checkpoint). Prediccion rough D-25-01: ~10-13 slots ANTES de slots nuevos de 25-02. Eje = 1 SLOT POR MICRO-REGLA (granularidad fina). Ver las 11 micro-reglas enumeradas en <objective>.

NO cruces: NO hay genero-numero-300..305 (verificado). NO crearlos. TODOS los ids semanticos.
NO word-buttons: 0 en el legacy (verificado). NO inventar.
NO snapshot: Genere e numero no tiene snapshot/assert APPEND-ONLY (avere-only, verificado: 0 refs; no existe .genero-numero-prefix-snapshot.json). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs.
PRESERVAR match: 207/208/209 siguen type:match (D-25-03/D-04, articulo-por-sonido NO derivable por raiz).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Producir el mapa de reagrupacion auditable (id-fuente -> micro-slot) por micro-regla</name>
  <files>.planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md</files>
  <read_first>
    - content/exercises/genero-numero.json (integro — los 40 ejercicios con su campo `notes` que declara la regla/uso de cada uno, su `payload.explanation`, su `payload.prompt`/`options`/`correctIndex` (MC), su `payload.pairs` (match 207/208/209) y su `validation.passes[]`)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md (la plantilla del formato del mapa old-id->slot a espejar cambiando categoria y eje organizador; AQUI el eje DIVERGE: granularidad FINA por micro-regla, NO pocos slots)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-01-PLAN.md (el plan analogo — metodologia de reagrupacion por regla, merge D-24-10, las divergencias; AQUI la granularidad DIVERGE en sentido opuesto: Verbi di movimento agrupo en pocos slots, aqui se aisla 1 slot por micro-regla D-25-01)
    - content/exercises/articoli.json (output canonico con match convertido — articoli-049 es la referencia EXACTA del shape de un slot type:match en slot+variantes: variant={prompt,pairs}, explanation/validation top-level, id semantico) y content/exercises/essere.json (MC slots de referencia)
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-CONTEXT.md (decisiones locked D-25-01..11 + Claude's Discretion sobre la asignacion exacta ejercicio->micro-slot, si los match son 3 slots-de-1 o agrupados, y el numero final de slots)
  </read_first>
  <action>
    Crear `.planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md` como artefacto de auditoria (espejar el formato de 24-REAGRUPACION-MAP.md): una tabla por cada slot resultante con columnas (slot-id propuesto, micro-regla/uso, ids-fuente que entran como variantes, type [multiple-choice|match], categoryIds, explanation-base elegida + matices a injertar D-25-07, celda pobre candidata a variante nueva en 25-02?). Aplicar el mapa propuesto en <interfaces> con el eje 1 SLOT POR MICRO-REGLA (D-25-01, granularidad FINA, NO por familia gruesa): aislar cada micro-regla en su slot propio: (1) plural -o/-i [001/011/013]; (2) plural -a/-e [002/012/014]; (3) plural -e/-i [015/016/017/018]; (4) plural sonido duro -co/-go->-chi/-ghi [003/030/004/031/005/032/029]; (5) invariables [008/035/033/034/036]; (6) femenino -o/-a [019/020/021/022]; (7) femenino -tore/-trice [006/023/024/025]; (8) femenino -e/-ore->-essa [007/028/026/027]; (9) articulo por sonido MC [009/010]; (10/11) los 3 match [207/208/209] PRESERVAN type:match (D-25-03/D-04) — el autor decide (Claude's Discretion) si son 3 slots-de-1 o se agrupan (recomendado: 207+208 singular a 1 slot, 209 plural a slot propio = 2 match slots; surfacear al autor). COLAPSAR EXPLICITAMENTE los 6 pares de duplicados literales a variantes del mismo micro-slot (D-25-01): 001==011, 002==012, 003==030, 004==031, 005==032, 008==035 — verificar contra el prompt/correctIndex de cada par que son la MISMA regla con distinto orden de distractoras. DECIDIR la ubicacion de 037 (lo psicologo->psicologi: combina articulo `lo` ante ps- + plural -go->-gi que conserva sonido): anotar la opcion recomendada (slot propio de combinacion O al slot -o/-i con nota O al slot sonido-duro) para confirmacion del autor; verificar la forma real: psicologo->psicologi (conserva /g/, NO inserta H porque ya hay i; cuidado: es -go->-gi, distinto de albergo->alberghi). Para cada slot proponer un id semantico limpio (D-25-06, TODOS semanticos, sin rango reservado porque no hay cruces). Para cada slot multi-variante comparar las explanations fuente y registrar cual es la base (la mas completa/general) y que matices unicos de las descartadas se injertan (D-25-07). VERIFICAR que ninguna explanation propuesta referencia la CATEGORIA Articoli por id ni por prosa (D-25-02; el articulo como contenido del ejercicio es valido). Marcar las celdas pobres como candidatas a engordar en 25-02 y anotar los 4 EJES DE HUECOS PRIORIZADOS (D-25-04): (a) invariables — mas acentuados (virtu, citta, universita) + extranjerismos (film, sport, bar, computer) al slot invariabili; (b) plural -co/-go sonido duro — la EXCEPCION amico->amici (pierde sonido), greco->greci, nemico->nemici vs parco->parchi, medico->medici, lago->laghi al slot sonido-duro; (c) genero -tore->-trice vs -e->-essa — engordar el contraste (attore/scrittore/pittore/lavoratore vs dottore/professore/studente) en sus dos slots; (d) plural base -o/-a/-e — mas variantes de las reglas regulares. Anotar que cada palabra asignada a una micro-regla debe VERIFICARSE que toma realmente esa forma (verificar el plural/femenino real del sustantivo italiano, R-MEMORY exercise_authoring_rules); NO inventar excepciones ni meter irregulares puros (uomo/uomini, mano/mani -> son de Sostantivi irregolari Phase 27, OUT of scope). Incluir una SECCION EXPLICITA "Snapshot/re-base D-88: NO APLICA a Genere e numero" (los scripts snapshot-/assert-avere-prefix son avere-only, hardcoded a avere.json, 0 refs a genero-numero; D-25-10) y una SECCION EXPLICITA "Sin cruces multi-cat ni word-buttons: no existen genero-numero-300..305 ni word-buttons; NO crear; todos los ids semanticos, categoryIds=['genero-numero']" para que el ejecutor de Task 2 NO copie esas columnas de Essere/Avere/Verbi-movimiento. NO autorar variantes nuevas aqui. NO incluir los slots de huecos nuevos (van a 25-02). Reportar el CONTEO de slots resultante de SOLO esta reagrupacion (sin slots nuevos ni variantes nuevas) — es el numero que 25-03 sincronizara.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md && grep -c "genero-numero-" .planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo 25-REAGRUPACION-MAP.md existe con una tabla que cubre los 40 ids fuente (cada id 001-037, 207/208/209 aparece exactamente una vez en alguna columna de ids-fuente)
    - La reagrupacion es POR MICRO-REGLA con granularidad FINA (D-25-01): existen slots SEPARADOS para plural -o/-i, plural -a/-e, plural -e/-i, plural sonido duro -co/-go, invariables, femenino -o/-a, femenino -tore/-trice, femenino -e/-essa — NO un unico slot "plural" ni un unico slot "femenino"
    - Las invariables (008/035/033/034/036: caffe/citta/universita/film) estan en UN slot dedicado invariabili, separado de los plurales regulares
    - El plural sonido duro -co/-go (003/030/004/031/005/032/029) esta en su slot propio separado del plural -o/-i regular
    - Los 6 pares de duplicados literales estan colapsados a variantes del mismo slot (001==011, 002==012, 003==030, 004==031, 005==032, 008==035) — verificado contra prompt/correctIndex
    - Los 3 match (207/208/209) estan marcados como type:match PRESERVADO (D-25-03/D-04) — NO convertidos a multiple-choice; el mapa documenta si son 3 slots-de-1 o agrupados
    - La ubicacion de 037 (lo psicologo->psicologi) esta decidida con la forma real verificada (psicologo->psicologi, -go->-gi conserva /g/, NO inserta H) y anotada para confirmacion del autor
    - TODOS los slots tienen categoryIds=["genero-numero"]; los ids propuestos son unicos y semanticos; hay una seccion explicita "sin cruces 300..305 ni word-buttons — todos semanticos"
    - Cada slot multi-variante registra explanation-base + matices a injertar (D-25-07); ninguna explanation propuesta referencia la CATEGORIA Articoli por id ni por prosa (D-25-02) — verificado en el mapa
    - Las celdas pobres estan marcadas como candidatas a 25-02 y los 4 ejes de huecos PRIORIZADOS (D-25-04: invariables; -co/-go con la excepcion amico->amici; -tore/-trice vs -e/-essa; plural base) estan anotados con la nota "verificar el plural/femenino real de cada palabra; NO irregulares puros (Phase 27)"
    - Hay una seccion EXPLICITA "Snapshot/re-base D-88: NO APLICA a Genere e numero" (los scripts son avere-only; no replicar la tarea de re-base de 22-01)
    - El CONTEO de slots de esta reagrupacion esta reportado (sin slots nuevos ni variantes nuevas) — es el numero base para 25-03
  </acceptance_criteria>
  <done>
    25-REAGRUPACION-MAP.md cubre los 40 ids->micro-slots con granularidad FINA (D-25-01): slots separados por cada terminacion de plural/femenino + invariables + sonido duro + articulo por sonido; 6 pares de duplicados colapsados a variantes; 3 match PRESERVADOS (D-25-03/D-04); 037 ubicado con forma real verificada; ids TODOS semanticos categoryIds=["genero-numero"]; plan de explanation por slot sin refs a la categoria Articoli (D-25-02); secciones explicitas "sin cruces/word-buttons" y "snapshot no aplica"; 4 ejes de huecos D-25-04 anotados; conteo de slots reportado.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Aprobar el mapa de reagrupacion de Genere e numero (granularidad FINA 1 slot por micro-regla, colapso de los 6 duplicados, ubicacion de 037, agrupacion de los 3 match, merges de explanation) antes de reescribir el JSON. Confirmar que la granularidad es FINA (D-25-01, NO familia gruesa), que los 3 match siguen siendo match (D-25-03/D-04) y que NO aplican snapshot ni cruces ni word-buttons.</decision>
  <context>
    El mapa (25-REAGRUPACION-MAP.md) determina la estructura final de slots y por tanto el conteo que los tests de 25-03 sincronizan. Decisiones cargadas de esta fase (LOCKED en CONTEXT.md, no se re-litigan, solo se afinan los detalles abiertos): (1) eje organizador = 1 SLOT POR MICRO-REGLA, granularidad FINA (D-25-01) — DIVERGENCIA CONSCIENTE vs Verbi di movimento (pocos slots, una regla): aqui hay TRES familias de regla en paralelo (plural, femenino, articulo) y cada micro-regla concreta tiene trampa propia; mezclar -o/-i con -co/-go difuminaria dos trampas distintas; (2) los 6 pares de duplicados literales (001==011, 002==012, 003==030, 004==031, 005==032, 008==035) se colapsan a VARIANTES intercambiables del mismo micro-slot, NO slots duplicados; (3) los 3 match (207/208/209) PRESERVAN type:match (D-25-03/D-04: articulo-por-sonido NO es derivable por raiz) — Claude's Discretion: el autor decide si son 3 slots-de-1 o se agrupan (recomendado: 207+208 singular a 1 slot, 209 plural a slot propio); (4) Genere e numero NO tiene cruces multi-cat (no existen 300..305) ni word-buttons ni snapshot APPEND-ONLY (avere-only) -> NO se replican esas columnas; (5) RESTRICCION D-25-02: las explanation NO referencian la categoria Articoli por id ni por prosa (usar el articulo como contenido del ejercicio es valido e inevitable). Claude's Discretion a afinar: la asignacion exacta ejercicio->micro-slot (cuantos slots resultan, ~10-13 rough), la ubicacion de 037 (lo psicologo: articulo+plural), si los match son 3 slots-de-1 o agrupados, y el numero final de slots (se fija AQUI, antes del rewrite, para planear el sync de counts). Las explanations mergeadas (D-25-07) requieren revision del autor (D-85) para no perder matices (la trampa del calco -s en invariables, la insercion de H en sonido duro, el contraste -trice vs -essa).
  </context>
  <options>
    <option id="granularidad-fina-1-slot-por-micro-regla">
      <name>Granularidad FINA: 1 slot por micro-regla + duplicados colapsados + 3 match preservados + sin snapshot/cruces/word-buttons (recomendado/locked)</name>
      <pros>Coherente con D-25-01 (cada terminacion de plural/femenino es una trampa propia que merece su slot; el examen elige 1 variante al azar dentro de la micro-regla -> mata la memorizacion por palabra sin mezclar reglas), D-25-02 (articulo en sitio sin remitir a Articoli), D-25-03/D-04 (match preservado por no derivable por raiz). No replicar snapshot/cruces/word-buttons evita tareas inexistentes. Aisla las trampas A1 estrella (invariables, sonido duro) en slots dedicados para engorde generoso en 25-02.</pros>
      <cons>Mas slots que Verbi di movimento (categoria rule-rich con 3 familias paralelas). Algunos slots quedan delgados (2-4 variantes) hasta el engorde de 25-02.</cons>
    </option>
    <option id="familia-gruesa">
      <name>Familia gruesa (1 slot "plural", 1 slot "femenino", 1 slot "articulo")</name>
      <pros>Menos slots.</pros>
      <cons>Contradice D-25-01 EXPLICITAMENTE: mezclar -o/-i con -co/-go e invariables en un mismo slot "plural" difumina reglas con trampa distinta; el examen podria no re-verificar la micro-regla concreta donde esta la trampa. Descartado salvo override explicito del autor.</cons>
    </option>
  </options>
  <resume-signal>Confirma la granularidad FINA (1 slot por micro-regla, D-25-01), el colapso de los 6 duplicados a variantes, la ubicacion de 037, la agrupacion de los 3 match (preservados type:match D-25-03/D-04), que NO aplican snapshot/cruces/word-buttons, ajusta los slot-ids/merges de explanation y marca las celdas pobres + los 4 ejes de huecos a evaluar en 25-02. Escribe "aprobado" cuando el mapa este listo para reescribir el JSON.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reescribir genero-numero.json a slot+variantes segun el mapa aprobado</name>
  <files>content/exercises/genero-numero.json</files>
  <read_first>
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-REAGRUPACION-MAP.md (el mapa aprobado por el autor — fuente de verdad de la estructura)
    - content/exercises/genero-numero.json (integro — superficies, explanations, prompt/options/correctIndex (MC), prompt/pairs (match 207/208/209) y validation.passes[] fuente a mover/mergear)
    - content/exercises/articoli.json (el output canonico con match convertido — articoli-049 es la referencia EXACTA del shape de un slot type:match en slot+variantes: variant={prompt,pairs}, explanation/validation top-level) y content/exercises/essere.json (MC slots, agrupacion por regla, slots-de-1)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-01-PLAN.md (Task 2 analogo — el move de superficies intactas a variants[], el merge de explanations y validation; AQUI ademas hay match a preservar, ver articoli-049)
  </read_first>
  <action>
    Reescribir `content/exercises/genero-numero.json` segun el mapa aprobado. Por cada slot, emitir `{id, type, categoryIds, explanation, variants:[...], validation}` ELIMINANDO `payload` de cada ejercicio fuente y MOVIENDO su superficie intacta a `variants[]`: para MC `{prompt, options, correctIndex}`; para match (207/208/209) `{prompt, pairs}` (shape de articoli-049). La `explanation` de cada slot multi-variante = la base elegida en el mapa + los matices injertados (D-25-07); para slots-de-1 la explanation existente sube a top-level tal cual; rule-first. Por micro-regla la explanation lidera con la regla concreta: plural -o/-i ("masculino -o -> -i"); plural -a/-e ("femenino -a -> -e"); plural -e/-i ("terminacion -e -> -i, ambos generos"); sonido duro ("-co/-go inserta H para mantener el sonido duro /k//g/: cuoco->cuochi, parco->parchi; amica->amiche"); invariables ("acentuados y extranjerismos NO cambian en plural: una citta -> due citta, un film -> due film; NUNCA cittas/films" — la trampa del calco espanol -s como hilo conductor); femenino -o/-a ("masculino -o -> femenino -a"); femenino -tore/-trice ("-tore -> -trice: attore->attrice"); femenino -e/-essa ("-e/-ore -> -essa: dottore->dottoressa"); articulo por sonido ("el articulo depende del SONIDO inicial: l' ante vocal, lo ante s+cons/z/ps/gn, il en el resto"). Para los slots match (207/208/209) la explanation cubre la regla de articulo por sonido en singular/plural sin remitir a otra categoria. RESTRICCION D-25-02: NINGUNA explanation referencia la CATEGORIA Articoli por id ni por prosa (cero "ver Articoli", cero "articoli-0XX"); el articulo (il/lo/l'/i/gli/le) como CONTENIDO del ejercicio SI (es la regla del slot de articulo) — las explanations legacy ya cumplen esto, preservarlo al mergear. El campo `notes` legacy es autor-internal: NO se mueve a variants[]; su matiz pedagogico relevante se injerta en la explanation del slot si aporta (D-25-07). Mover el texto intacto NO requiere re-validacion (cambio de contenedor, D-25-08); si al mergear cambia la SUPERFICIE de algun prompt/options existente, eso cuenta como variante nueva -> quorum en 25-02 (NO re-validar aqui; documentar en el mapa/SUMMARY cualquier superficie tocada). Para los slots usar el id semantico del mapa (D-25-06, TODOS semanticos; NO hay rango reservado porque no hay cruces). TODOS los slots con categoryIds=["genero-numero"]. Preservar el campo `validation` top-level moviendo los passes[] verbatim (D-25-08); para slots multi-variante combinar segun el criterio del mapa (la del ejercicio cuya superficie/explanation domina o la fusion de passes[]); preservar cualquier disputed->override del autor tal cual (no re-validar); registrar el criterio en el SUMMARY. Aplicar un scan de acentos sobre las superficies/explanations MOVIDAS (leccion Phase 19 WR-01/WR-02: el legacy puede arrastrar tildes ausentes; DeepSeek es estricto en acentos; corregir defectos legacy de acento es correccion ortografica, no cambio de superficie; las tildes italianas caffè/città/università/perché y las espanolas RAE). Apostrofes ASCII U+0027 (el legacy ya usa l'amico — preservar); sin smart-quotes. NO autorar variantes nuevas. NO anadir slots de huecos (van a 25-02). NO tocar counts de tests/scripts (van a 25-03). NO crear ningun cruce 300..305 (no existen; fuera de scope). NO crear word-buttons (no existen). NO convertir los 3 match a multiple-choice (D-25-03/D-04). NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Genere e numero; avere-only). Documentar en el SUMMARY que "Genere e numero no tiene snapshot APPEND-ONLY ni cruces multi-cat ni word-buttons — no aplica re-base D-88".
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs genero-numero content/exercises/genero-numero.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs genero-numero content/exercises/genero-numero.json` exits 0
    - Ningun ejercicio conserva payload: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Todo slot con variants[] tiene explanation top-level no vacia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('slots sin explanation:', d.exercises.filter(e=>Array.isArray(e.variants)&&(!e.explanation||!e.explanation.trim())).length)"` imprime `0`
    - Ninguna variante lleva explanation propia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('variantes con explanation:', d.exercises.flatMap(e=>e.variants||[]).filter(v=>v.explanation!==undefined).length)"` imprime `0`
    - Los 3 match estan PRESERVADOS como type:match (D-25-03/D-04, NO convertidos a MC): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); const m=d.exercises.filter(e=>e.type==='match'); console.log('match slots:', m.length, 'todos con variants[].pairs:', m.every(e=>Array.isArray(e.variants)&&e.variants.every(v=>Array.isArray(v.pairs))))"` imprime `match slots:` >=1 (segun agrupacion aprobada: 2 o 3) `todos con variants[].pairs: true`
    - Existe el slot invariabili con las invariables (caffe/citta/universita/film): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('invariabili:', d.exercises.filter(e=>/invariab/.test(e.id)).length)"` imprime al menos `1`
    - Existen slots SEPARADOS por micro-regla de plural (granularidad fina D-25-01): hay slots distintos cuyos ids matchean plurale-o-i, plurale-a-e, plurale-e-i, y un slot de sonido duro -co/-go — verificable: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); const ids=d.exercises.map(e=>e.id).join(' '); console.log('o-i:', /o-i/.test(ids), 'a-e:', /a-e/.test(ids), 'e-i:', /e-i/.test(ids), 'co/go:', /co-|go-|chi|ghi|duro|suono-duro/.test(ids))"` los cuatro `true`
    - Existen slots SEPARADOS por micro-regla de femenino: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); const ids=d.exercises.map(e=>e.id).join(' '); console.log('femminile:', /femminile|femenino|trice|essa/.test(ids))"` imprime `true` (hay slots -o/-a, -trice, -essa)
    - NO hay word-buttons: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('wb:', d.exercises.filter(e=>e.type==='word-buttons').length)"` imprime `wb: 0`
    - TODOS los slots tienen categoryIds=["genero-numero"] (no hay cruces): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('mal-cat:', d.exercises.filter(e=>!(e.categoryIds.length===1&&e.categoryIds[0]==='genero-numero')).length)"` imprime `0`
    - NO existe ningun id genero-numero-30[0-5] (no se crearon cruces): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('cruces:', d.exercises.filter(e=>/-30[0-5]$/.test(e.id)).length)"` imprime `0`
    - Ninguna explanation referencia la categoria Articoli por id o prosa: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/genero-numero.json','utf8')); console.log('refs Articoli-cat:', d.exercises.filter(e=>/articoli-0\d\d|ver Articoli|categor.a Articoli/i.test(e.explanation||'')).length)"` imprime `0` (D-25-02)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/genero-numero.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Genere e numero (no existe `.genero-numero-prefix-snapshot.json`)
  </acceptance_criteria>
  <done>
    genero-numero.json reescrito a slot+variantes (sin payload, variants[]+explanation top-level por slot, variantes sin explanation propia; granularidad FINA 1 slot por micro-regla D-25-01: plural -o/-i, -a/-e, -e/-i, sonido duro, invariables, femenino -o/-a, -trice, -essa, articulo por sonido; 6 duplicados colapsados a variantes; 3 match PRESERVADOS type:match con variant={prompt,pairs} D-25-03/D-04; ids TODOS semanticos categoryIds=["genero-numero"], sin cruces ni word-buttons; explanations sin refs a la categoria Articoli D-25-02; validation/disputed-override verbatim), validateContent verde, scan de acentos aplicado a superficies movidas, sin smart-quotes; documentado que Genere e numero no tiene snapshot ni cruces ni word-buttons (no aplica re-base D-88).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano -> loader/validator al boot | El `genero-numero.json` reescrito a mano cruza a validateContent y al render; explanation/prompt/pairs se renderizan como texto plano (x-text, nunca x-html — invariante T-02-01) |
| Estado de slots -> motor slot-aware / cascada D-54 | Los ids de slot alimentan clearedExerciseIds + la cascada; renumerar a ids semanticos es seguro porque Phase 21 ya reseteo el progreso de genero-numero y NO hay cruces que desincronicen otras categorias |

> Nota: esta fase es una conversion de contenido JSON hand-authored (sin cruces multi-cat, sin word-buttons, sin nuevo runtime, motor/sampler/loader/validator intactos) en una app local single-user, offline, sin backend; NO introduce nueva superficie de ataque (sin red entrante, auth, input de usuario, ni deserializacion de datos no confiables). No se esperan amenazas HIGH/MEDIUM. El threat surface realista = JSON malformado/invalido que rompa el loader al boot, reagrupacion que pierda una superficie fuente, y smart-quotes/markup que rompan el invariante anti-XSS x-text (T-02-01).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering / data-integrity | reagrupacion que pierde/duplica una superficie fuente, mete dos micro-reglas en un mismo slot (familia gruesa) rompiendo D-25-01, o asigna una palabra a una micro-regla que no le corresponde (p.ej. invariable al slot -o/-i) | mitigate | El mapa auditado (Task 1 + checkpoint) garantiza los 40 ids fuente mapeados 1:1 y los 6 duplicados colapsados a variantes; el acceptance asserta slots SEPARADOS por micro-regla (o-i/a-e/e-i/sonido-duro distintos), slot invariabili existe, payload==0, categoryIds=["genero-numero"] en todos, wb==0, cruces==0. |
| T-25-02 | Tampering | reagrupacion pierde un matiz de explanation al mergear (D-25-07), o convierte un match a MC rompiendo D-25-03/D-04 | mitigate | Merge elegir-la-mas-completa + injertar matices (no reescritura); acceptance asserta los 3 match preservados como type:match con variant.pairs (count>=1 segun agrupacion, todos con pairs); revision del autor en el checkpoint (D-85); acceptance asserta cobertura de explanation por slot (slots sin explanation == 0). |
| T-25-03 | Tampering | explanation/prompt/pairs con HTML/markdown/smart-quotes rompe el invariante anti-XSS x-text (T-02-01); defecto de acento legacy arrastrado (Phase 19 WR-01/02) | mitigate | Acceptance asserta 0 smart-quotes; scan de acentos sobre superficies MOVIDAS (tildes italianas caffè/città/università + RAE espanol; DeepSeek estricto en acentos); el smoke (25-03) re-asserta markdown/HTML sobre variants[].prompt + explanation. Apostrofes ASCII U+0027 preservados del legacy (l'amico). Todo string permanece plain text (sin markup) -> x-text lo renderiza seguro. |
| T-25-04 | Tampering | malformed JSON crashea el loader | mitigate | `validateContent` (existente, no modificado) rechaza con banner visible slot sin variantes / variante sin superficie valida (MC sin ___/options/correctIndex; match sin pairs) / explanation ausente. El acceptance corre el validador. |
| T-25-02b | Information disclosure / governance | una explanation referencia la CATEGORIA Articoli por id o prosa, violando D-25-02 | mitigate | Acceptance asserta 0 refs a "articoli-0XX"/"ver Articoli"/"categoria Articoli" en explanations; el merge preserva las explanations legacy que ya cumplen; el articulo como contenido del ejercicio es valido e inevitable (no se penaliza). |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que instalar/auditar (proyecto estatico offline single-user). No hay tabla de Package Legitimacy porque no hay installs. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs genero-numero content/exercises/genero-numero.json` exits 0
- `node --test tests/*.test.js` — nota: el count de Genere e numero (40 hardcoded en 3 sitios) FALLARA tras esta reescritura porque ahora hay otro nº de slots; esto es ESPERADO y se arregla en 25-03 (path desnudo falla en Node 22.20 — usar el glob `tests/*.test.js`). Documentar el rojo esperado en el SUMMARY; NO arreglar los counts aqui.
- El mapa 25-REAGRUPACION-MAP.md aprobado por el autor (checkpoint), incluida la confirmacion de la granularidad fina, los 3 match preservados y que no aplica snapshot/cruces/word-buttons
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Genere e numero)
</verification>

<success_criteria>
- 40 ejercicios fuente reagrupados a 1 SLOT POR MICRO-REGLA (D-25-01, granularidad fina): slots separados de plural -o/-i, -a/-e, -e/-i, sonido duro -co/-go, invariables, femenino -o/-a, -tore/-trice, -e/-essa, articulo por sonido; 6 duplicados colapsados a variantes; 3 match preservados type:match (D-25-03/D-04); ids TODOS semanticos (D-25-06), categoryIds=["genero-numero"], sin cruces ni word-buttons
- Cada slot: explanation top-level no vacia (merge D-25-07, rule-first, trampa del calco -s en invariables, insercion de H en sonido duro), variantes sin explanation propia, payload eliminado; ninguna explanation referencia la categoria Articoli por id ni prosa (D-25-02); validation/disputed-override verbatim
- validateContent verde; sin smart-quotes; scan de acentos aplicado a superficies movidas
- Genere e numero no tiene snapshot APPEND-ONLY ni cruces multi-cat ni word-buttons (avere-only) -> NO se replica la re-base D-88 ni se crean cruces/word-buttons; documentado en el SUMMARY
- (Los counts de tests quedan rojos a proposito hasta 25-03)
</success_criteria>

<output>
Create `.planning/phases/25-genere-e-numero-a-slots-contenido/25-01-SUMMARY.md` when done
</output>
