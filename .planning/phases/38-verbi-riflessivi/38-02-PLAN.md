---
phase: 38-verbi-riflessivi
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - content/exercises/riflessivi.json
autonomous: true
requirements: [REFLEX-01, REFLEX-02, REFLEX-03, REFLEX-04, REFLEX-05]
must_haves:
  truths:
    - "Cada variante de riflessivi.json queda con el SELLO canonico base Opus+Sonnet (>=2 passes correcta, >=2 by distintos, con claude-opus-4-8 y claude-sonnet-4-6 entre los by) O el quorum estructural reforzado equivalente cuando el sello Sonnet no fue estampable, sin degradar ninguna variante ya validated (SC#5, INT-04)."
    - "El MAGNET riflessivi-pp-concordanza y el cruce riflessivi-301 llevan la ronda EXTRA DeepSeek obligatoria (>=1 by deepseek entre los passes), con las 4 terminaciones -o/-a/-i/-e auditadas explicitamente en concerns[] (D-31-08, Pitfall 6)."
    - "Ninguna disputa se resuelve por override-atajo: toda variante disputada se REESCRIBE hasta que ambas IAs dan correcta (memoria feedback_disputed_resolution); un C4-accent en espanol se arregla con la tilde, no se override (Pitfall 10)."
    - "El motor v1.4 sigue intacto (applyImmediateFailure = 2 call-sites; git diff del motor vacio); riflessivi sigue registrada order 14 sin origen; el 0-match y el scope-gate reciprocos siguen documentados en notes."
  artifacts:
    - path: "content/exercises/riflessivi.json"
      provides: "riflessivi.json con el sello canonico Opus+Sonnet estampado (o el refuerzo cross-vendor equivalente) en cada variante y la ronda EXTRA DeepSeek en pp-concordanza + riflessivi-301; toda variante validation.status validated con >=2 by distintos"
      contains: "\"status\": \"validated\""
  key_links:
    - from: "content/exercises/riflessivi.json (cada slot)"
      to: "quorum canonico base (skill gsd-validate-exercise: Opus + Sonnet, top-level)"
      via: "1 subagent fresh por ejercicio, NUNCA batched, C1-C5 -> estampa el pase Sonnet canonico"
      pattern: "claude-sonnet-4-6"
    - from: "content/exercises/riflessivi.json riflessivi-pp-concordanza + riflessivi-301"
      to: "scripts/validate-ai-pass.mjs (ronda EXTRA DeepSeek)"
      via: "--write auto-fallback 429 -> deepseek-reasoner como by adicional; estricto en concordancia/acentos"
      pattern: "deepseek"
---

<objective>
Rematar la validacion canonica de `content/exercises/riflessivi.json` que el Plan 01 dejo con quorum ESTRUCTURAL (Opus-inline + DeepSeek) por la limitacion del executor. Este plan corre TOP-LEVEL (no dentro de gsd-executor), donde el skill canonico `gsd-validate-exercise` SI puede spawnear Task subagents: estampa el pase base Opus+Sonnet en cada variante y aplica la ronda EXTRA DeepSeek OBLIGATORIA en el MAGNET (riflessivi-pp-concordanza) y en el cruce riflessivi-301. Cierra INT-04 / SC#5 con el quorum canonico R1-R7.

Purpose: memoria executor_cannot_run_task_quorum — el quorum base canonico Opus+Sonnet (que spawnea Task subagents) NO esta disponible dentro del subagent del executor, asi que Plan 01 solo pudo cumplir la barra estructural (Opus-inline + DeepSeek). El sello Sonnet canonico y la disciplina D-31-08 de las 4 terminaciones del MAGNET requieren esta pasada top-level. Sin ella, SC#5 (quorum R1-R7 canonico, >=2 by distintos) no queda honrado con el rigor que exige el MAGNET de concordancia (Pitfall 6: DeepSeek caza el -o para sujeto femenino que human-verify aprueba).

Output: `content/exercises/riflessivi.json` con cada variante sellada por el quorum base Opus+Sonnet (o el refuerzo cross-vendor equivalente si Sonnet no fuera estampable) y la ronda EXTRA DeepSeek en los 2 nodos de concordancia; toda disputa resuelta por REESCRITURA. NO se toca categories.json (ya registrada en Plan 01), ni el motor, ni los counts (Phase 39).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-verbi-riflessivi/38-CONTEXT.md
@.planning/phases/38-verbi-riflessivi/38-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/38-verbi-riflessivi/38-01-SUMMARY.md

<execution_note>
IMPORTANTE: este plan DEBE correr TOP-LEVEL (no via gsd-executor). El skill `gsd-validate-exercise` spawnea Task subagents (Opus + Sonnet) que NO existen dentro de un subagent. Si por cualquier razon este plan se ejecutase dentro del executor, el pase Sonnet canonico NO seria estampable; en ese caso, reforzar con un segundo vendor distinto (deepseek-reasoner ademas de deepseek-chat) para mantener >=2 by distintos NO-Claude-inline y DEJAR CONSTANCIA en el SUMMARY de que el sello Sonnet canonico queda pendiente de una pasada humana/top-level. El pase DeepSeek del MAGNET es obligatorio en cualquier caso.
</execution_note>

<validation_infra>
- Skill `gsd-validate-exercise` (SKILL.md): quorum base canonico Opus + Sonnet, criterios C1-C5, NUNCA batched, 1 subagent fresh por ejercicio. Es la via canonica del pase Sonnet — disponible SOLO top-level.
- scripts/validate-ai-pass.mjs (Gemini/DeepSeek, --write, auto-fallback 429 -> deepseek-reasoner; claves en .env): refuerzo cross-vendor. Ronda EXTRA DeepSeek OBLIGATORIA en riflessivi-pp-concordanza y riflessivi-301.
- Resolucion de disputas: SIEMPRE por REESCRITURA hasta que ambas IAs den correcta (memoria feedback_disputed_resolution); calidad > tokens; NUNCA override-atajo. Un C4-accent en espanol = arreglar la tilde (Pitfall 10, memoria explanations_must_be_accented). El gloss R7 `(en español: ...)` marcado C5 por Gemini/DeepSeek = falso-positivo de politica -> mantener (memoria gloss_es_desambiguacion_canon), EXCEPTO en pp-concordanza/riflessivi-301 donde por D-38-01 NO debe haber gloss.
</validation_infra>

<canon_recheck>
Al re-validar, verificar que Plan 01 respeto el canon (si no, arreglar y re-validar):
- riflessivi-pp-concordanza y riflessivi-301: CERO auxiliar avere en las keys; las 4 terminaciones -o/-a/-i/-e con cue por nombres propios (Marco -o / Maria -a / i ragazzi -i / le ragazze -e); SIN gloss ES (D-38-01).
- riflessivi-mismatch: gloss ES obligatorio donde difiere (ammalarsi/dimenticarsi (di)/salire); sin mismatch fabricado.
- word-buttons de colocacion: banco con el orden-distractor `sveglio mi`.
- 0-match + scope-gate reciprocos documentados en notes; sin reciprocos (si amano/ci scriviamo) en ninguna variante.
</canon_recheck>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sello canonico Opus+Sonnet + ronda EXTRA DeepSeek del MAGNET (top-level)</name>
  <read_first>
    - content/exercises/riflessivi.json (el archivo a re-validar y estampar: revisar que slots quedaron con quorum estructural en Plan 01 y cuales ya tienen sello Sonnet)
    - .planning/phases/38-verbi-riflessivi/38-01-SUMMARY.md (que variantes quedaron con quorum estructural Opus-inline + DeepSeek y esperan el pase Sonnet canonico)
    - .claude/skills/gsd-validate-exercise (SKILL.md: quorum base Opus+Sonnet, C1-C5, NUNCA batched, 1 subagent fresh por ejercicio -- la via del pase Sonnet)
    - scripts/validate-ai-pass.mjs (refuerzo cross-vendor Gemini/DeepSeek, --write, auto-fallback 429 -> deepseek-reasoner; ronda EXTRA DeepSeek del MAGNET)
    - .planning/phases/38-verbi-riflessivi/38-CONTEXT.md (D-38-01 CERO avere + NO gloss en pp; D-38-04 ronda extra DeepSeek; canon; validation caveat)
    - .planning/research/PITFALLS.md (Pitfall 6 el MAGNET, disciplina D-31-08 4 terminaciones; Pitfall 10 acentos = bug real; Pitfall 9 gloss R7 mantener)
    - content/exercises/presente-regolare.json (presente-regolare-301 lineas 472-543: el concerns[] modelo D-31-08 de las 4 terminaciones)
  </read_first>
  <action>
    Correr TOP-LEVEL (fuera de gsd-executor) para tener acceso a los Task subagents del skill.
    Para CADA variante/slot de riflessivi.json que el SUMMARY de Plan 01 marque con quorum ESTRUCTURAL (Opus-inline + DeepSeek), estampar el pase base CANONICO via el skill `gsd-validate-exercise` (Opus + Sonnet, 1 subagent fresh por ejercicio, NUNCA batched, criterios C1-C5). Anadir el pase `claude-sonnet-4-6` (verdict correcta) al bloque validation de la variante, de modo que quede con >=2 by distintos canonicos (opus + sonnet). NO degradar ni borrar passes ya presentes; solo AGADIR el sello que faltaba.
    MAGNET (riflessivi-pp-concordanza) y cruce riflessivi-301: aplicar la ronda EXTRA DeepSeek OBLIGATORIA via scripts/validate-ai-pass.mjs --write (auto-fallback 429 -> deepseek-reasoner). DeepSeek es estricto en concordancia/acentos y caza un -o para sujeto femenino. En el concerns[] de estos 2 nodos AUDITAR EXPLICITAMENTE las 4 terminaciones -o/-a/-i/-e contra su sujeto (D-31-08, mirror de presente-regolare.json:529-533) y confirmar CERO auxiliar avere en las keys y AUSENCIA de gloss ES (D-38-01).
    RESOLUCION DE DISPUTAS: si cualquier vendor marca una variante como incorrecta/dudosa, RESOLVER POR REESCRITURA (editar prompt/options/explanation hasta que ambas IAs den correcta), NUNCA por override-atajo (memoria feedback_disputed_resolution). Un C4-accent en espanol = arreglar la tilde (Pitfall 10). El gloss R7 marcado C5 en los slots donde el gloss ES canonico = falso-positivo, mantener; pero en pp-concordanza/riflessivi-301 NO debe haber gloss (D-38-01) — si Plan 01 dejo uno, quitarlo. Tras reescribir, re-validar esa variante por el quorum completo.
    Si (y solo si) este plan corriese dentro del executor y el pase Sonnet no fuera estampable, reforzar con deepseek-reasoner ADEMAS de deepseek-chat (>=2 by NO-inline distintos) y DEJAR CONSTANCIA en el SUMMARY de que el sello Sonnet canonico queda pendiente.
    NO tocar categories.json (ya en Plan 01), NO tocar el motor, NO anadir origen ni sincronizar counts (Phase 39).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/exercises/riflessivi.json'); const s=c.exercises; s.forEach(e=>{ if(!e.validation||e.validation.status!=='validated') throw new Error(e.id+' no validated'); const bys=[...new Set(e.validation.passes.map(p=>p.by))]; if(bys.length<2) throw new Error(e.id+' <2 by distintos'); if(!e.validation.passes.every(p=>p.verdict==='correcta')) throw new Error(e.id+' pass no correcta'); }); console.log('OK todas las variantes validated con >=2 by distintos:', s.length, 'slots')"</automated>
    <automated>node -e "const c=require('./content/exercises/riflessivi.json'); const s=c.exercises; ['riflessivi-pp-concordanza','riflessivi-301'].forEach(id=>{ const e=s.find(x=>x.id===id); if(!e) throw new Error('falta '+id); const bys=[...new Set(e.validation.passes.map(p=>p.by))]; if(!bys.some(b=>/deepseek/i.test(b))) throw new Error(id+' sin ronda EXTRA DeepSeek'); const auditsAllFour=e.validation.passes.some(p=>(p.concerns||[]).some(cc=>/-o.*-a.*-i.*-e|4 terminaciones|D-31-08/i.test(cc))); if(!auditsAllFour) throw new Error(id+' concerns[] no audita las 4 terminaciones (D-31-08)'); e.variants.forEach(v=>{ const k=v.options[v.correctIndex]; if(/\b(ho|hai|ha|abbiamo|avete|hanno)\b/.test(k)) throw new Error(id+' CERO-avere violado en key: '+k); }); }); console.log('OK MAGNET + riflessivi-301: ronda DeepSeek, 4 terminaciones auditadas, CERO avere')"</automated>
    <automated>node -e "const c=require('./content/exercises/riflessivi.json'); const s=c.exercises; [ 'riflessivi-pp-concordanza','riflessivi-301'].forEach(id=>{ const e=s.find(x=>x.id===id); if(!e) return; e.variants.forEach(v=>{ if(/\(en espa/i.test(v.prompt)) throw new Error(id+' tiene gloss ES prohibido (D-38-01): '+v.prompt); }); }); console.log('OK sin gloss ES en los nodos de concordancia (D-38-01)')"</automated>
    <automated>node scripts/validate-content-fixture.mjs riflessivi content/exercises/riflessivi.json</automated>
    <automated>node --test tests/*.test.js 2>&1 | tail -5</automated>
    <automated>grep -c "applyImmediateFailure(this.state" src/screens/app.js | grep -qx 2 && echo "D-54 OK (2 call-sites)"</automated>
    <automated>test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "engine diff vacio OK"</automated>
  </verify>
  <acceptance_criteria>
    - Cada variante de riflessivi.json tiene validation.status "validated", >=2 passes verdict correcta, >=2 by distintos. En condiciones top-level, el pase `claude-sonnet-4-6` figura entre los by de los slots que Plan 01 dejo estructurales (o el SUMMARY documenta por que no fue estampable).
    - riflessivi-pp-concordanza y riflessivi-301 llevan >=1 pase DeepSeek y su concerns[] audita EXPLICITAMENTE las 4 terminaciones -o/-a/-i/-e (D-31-08).
    - CERO auxiliar avere en las keys de pp-concordanza/riflessivi-301; SIN gloss ES en esos 2 nodos (D-38-01).
    - Ninguna variante degradada: los passes de Plan 01 se conservan; solo se AGADEN sellos.
    - Toda disputa resuelta por REESCRITURA (no override); acentos ES corregidos con tilde donde el quorum marco C4.
    - `node scripts/validate-content-fixture.mjs riflessivi ...` sale 0; `node --test tests/*.test.js` pasa (salvo el fail preexistente ajeno genero-numero 12<->13).
    - `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2; `git diff src/screens/app.js src/domain/progress.js` vacio.
  </acceptance_criteria>
  <done>
    riflessivi.json queda con el sello canonico base Opus+Sonnet estampado (o el refuerzo cross-vendor equivalente documentado en el SUMMARY) en cada variante, y la ronda EXTRA DeepSeek con auditoria de las 4 terminaciones en el MAGNET pp-concordanza y en riflessivi-301. Disputas resueltas por reescritura. Motor intacto, categoria intacta (order 14). INT-04 / SC#5 honrados con el quorum canonico R1-R7.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Re-validacion de contenido JSON estatico ya existente (editado in-place para estampar passes y reescribir disputadas). Sin codigo ejecutable nuevo, sin input de usuario, sin red de runtime, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02 | Tampering | content/exercises/riflessivi.json (edicion in-place de validation + reescritura de disputadas) | accept | Solo se AGADEN passes y se reescriben prompts disputados; el smoke `node --test tests/*.test.js` (readdir auto-cubre riflessivi.json) + `validate-content-fixture.mjs` verifican integridad estructural. Cero codigo nuevo, cero superficie ejecutable, motor intacto (D-54 = 2 call-sites). |
| T-38-SC | Tampering | scripts/validate-ai-pass.mjs (DeepSeek, claves .env) | accept | Script existente ya en uso; sin instalacion de paquetes nuevos; claves en .env fuera de git. 0 installs en esta fase. |

No hay amenazas high/medium introducidas: re-validacion de contenido, motor intacto, invariante anti-XSS `x-text` preservado.
</threat_model>

<verification>
- SC#5 / INT-04: cada variante de riflessivi.json validated con >=2 by distintos por el quorum canonico R1-R7 (Opus+Sonnet top-level; DeepSeek de refuerzo).
- Pitfall 6 / D-38-04: ronda EXTRA DeepSeek en pp-concordanza + riflessivi-301, con las 4 terminaciones auditadas (D-31-08) y CERO avere.
- D-38-01: sin gloss ES en los 2 nodos de concordancia.
- Disputas resueltas por REESCRITURA (memoria feedback_disputed_resolution); acentos corregidos con tilde (Pitfall 10).
- D-54: EXACTAMENTE 2 call-sites; git diff del motor vacio. categories.json intacta (order 14).
</verification>

<success_criteria>
- Todas las variantes de riflessivi.json quedan con el sello canonico base Opus+Sonnet (o el refuerzo equivalente documentado), >=2 by distintos.
- El MAGNET y riflessivi-301 llevan la ronda EXTRA DeepSeek con auditoria D-31-08 de las 4 terminaciones y CERO avere; sin gloss ES (D-38-01).
- Ninguna variante degradada; disputas resueltas por reescritura.
- Motor v1.4 intacto (git diff vacio); categoria order 14 intacta; counts en rojo hasta Phase 39 = esperado.
</success_criteria>

<output>
Create `.planning/phases/38-verbi-riflessivi/38-02-SUMMARY.md` when done
</output>
