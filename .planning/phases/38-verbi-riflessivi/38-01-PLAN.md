---
phase: 38-verbi-riflessivi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/riflessivi.json
  - content/categories.json
autonomous: false
requirements: [REFLEX-01, REFLEX-02, REFLEX-03, REFLEX-04, REFLEX-05]
must_haves:
  truths:
    - "El autor es examinado sobre el presente reflexivo (mi chiamo/ti chiami/si chiama) en todas las personas (REFLEX-01)."
    - "El autor es examinado sobre la colocacion del pronombre reflexivo ANTES del verbo conjugado via >=1 slot word-buttons cuyo banco incluye el orden-distractor *sveglio mi (REFLEX-02, SC#1 duro)."
    - "El autor es examinado sobre el reflexivo construido sobre terminaciones regulares (si alza/ci laviamo/vi vestite) enganchado a presente-regolare (REFLEX-03)."
    - "El autor es examinado sobre el passato prossimo reflexivo con essere + concordancia -o/-a/-i/-e (slot riflessivi-pp-concordanza dedicado, las 4 terminaciones como variantes contrastantes, cue por nombres propios, CERO auxiliar avere, sin gloss ES) (REFLEX-04, el MAGNET, SC#3)."
    - "El autor es examinado sobre 2-3 desajustes reflexivos ES<->IT genuinos (ammalarsi/dimenticarsi (di)/salire) con gloss ES obligatorio donde difiere la reflexividad, sin fabricar trampas falsas (REFLEX-05, SC#2)."
    - "Los DOS cruces multi-cat riflessivi-300 (categoryIds riflessivi+presente-regolare) y riflessivi-301 (categoryIds riflessivi+essere) existen como contenido, al final del array (SC#4)."
    - "El motor v1.4 queda intacto: applyImmediateFailure sigue en EXACTAMENTE 2 call-sites (app.js:1642, app.js:1969); git diff del motor vacio (D-54, SC#4)."
    - "riflessivi es 0-match (D-04/D-38-03): pronombre<->persona derivable -> solo MC + word-buttons, sin slot match; el 0-match documentado en notes."
    - "riflessivi queda registrada en categories.json con order 14 (append), EXACTAMENTE id/name/order (sin origen), sin romper el display del home (SC#5)."
  artifacts:
    - path: "content/exercises/riflessivi.json"
      provides: "Categoria riflessivi nacida en slot+variantes: presente reflexivo (REFLEX-01), word-buttons de colocacion con orden-distractor (REFLEX-02), reflexivo sobre terminacion regular (REFLEX-03), MAGNET pp-concordanza -o/-a/-i/-e sin avere (REFLEX-04), trio mismatch ES<->IT (REFLEX-05), cruces riflessivi-300 y riflessivi-301; 0-match + scope-gate reciprocos documentados en notes"
      contains: "\"id\": \"riflessivi-"
    - path: "content/categories.json"
      provides: "Entrada riflessivi order 14 (append al final, solo id/name/order, sin origen)"
      contains: "\"id\": \"riflessivi\""
  key_links:
    - from: "content/exercises/riflessivi.json riflessivi-300 / riflessivi-301"
      to: "cascada D-54 (applyResultToSession existente)"
      via: "categoryIds de 2 (riflessivi+presente-regolare / riflessivi+essere) -> el motor cascadea a ambas categorias sin call-site nuevo"
      pattern: "presente-regolare|essere"
    - from: "content/exercises/riflessivi.json"
      to: "content-loader.js loadContent()"
      via: "fetch generico por slug + validateContent (permisivo)"
      pattern: "riflessivi"
    - from: "content/categories.json entry riflessivi"
      to: "app.js categoriesForDisplay"
      via: "itera content.categories en orden de array -> fila nueva en el home sin codigo nuevo"
      pattern: "riflessivi"
---

<objective>
Dar de alta la categoria `riflessivi` (order 14) nacida DIRECTAMENTE en slot+variantes (clon del patron v1.7 `presente-regolare` + Phases 36/37) — la MAS layered de las 4 altas del milestone. Autorar los 5 slots REFLEX (presente reflexivo, colocacion del pronombre via word-buttons, reflexivo sobre terminacion regular, passato prossimo reflexivo con essere + concordancia = el MAGNET, y el trio de desajustes ES<->IT) + los DOS cruces multi-cat `riflessivi-300` (`["riflessivi","presente-regolare"]`) y `riflessivi-301` (`["riflessivi","essere"]`), validando 1-por-1. Registrar la categoria en `categories.json` (order 14). Cubre REFLEX-01..05.

Purpose: Es la 12a categoria de gramatica del proyecto y cierra las 4 altas de v1.9. Sin estos slots el autor no re-verifica los reflexivos; sin el word-buttons de colocacion (REFLEX-02/SC#1) no se ejercita `mi sveglio` vs `*sveglio mi`; sin el MAGNET de concordancia (REFLEX-04/SC#3, las 4 terminaciones -o/-a/-i/-e sin auxiliar avere) el hispanohablante falla en silencio porque el espanol no concuerda el participio; sin los 2 cruces (SC#4) la cascada D-54 no engancha con presente-regolare ni essere.

Output: `content/exercises/riflessivi.json` (5 slots REFLEX + >=1 word-buttons + 2 cruces) con validacion IN-EXECUTOR (Opus-inline + DeepSeek, la barra estructural) y `content/categories.json` con la entrada order 14. El SELLO canonico Opus+Sonnet + la ronda EXTRA DeepSeek del MAGNET los remata el Plan 02 (top-level, tras execute-phase; ver caveat abajo). El campo `origen`/PROV-01 y la sincronizacion de counts son de Phase 39 (rojo esperado hasta entonces).

Este plan NO toca el motor v1.4 (D-54 = EXACTAMENTE 2 call-sites; sampler; slot-engine; schema-validator; migracion; backup). `git diff src/screens/app.js src/domain/progress.js` DEBE quedar vacio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-verbi-riflessivi/38-CONTEXT.md
@.planning/phases/38-verbi-riflessivi/38-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<slug_contract>
El slug es `riflessivi` (contrato transversal INVIOLABLE de Phase 35, D-35-01/02): (a) `id` en categories.json, (b) fichero `content/exercises/riflessivi.json`, (c) prefijo de TODOS los ids de ejercicio (`riflessivi-*`) y de los DOS cruces (`riflessivi-300` / `riflessivi-301`), (d) ya presente en `RESET_PREFIXES_V12` (storage.js:1168) con `delete categoryProgress['riflessivi']` (storage.js:1215), verificado, sin colision `startsWith`. NO usar `verbi-riflessivi` (eso es solo el nombre del directorio de la fase). TODOS los ids de slot DEBEN empezar por `riflessivi-`.
</slug_contract>

<interfaces>
Shape de la categoria (analogo EXACTO `content/exercises/presente-regolare.json`, verificado):
- Top-level: `{ "notes": "...", "exercises": [ ...slots... ] }`. Sin categoryId top-level; pertenencia por-slot en `categoryIds:["riflessivi"]`.
- Slot MC: `{ id, type:"multiple-choice", categoryIds:["riflessivi"], explanation, variants:[{prompt,options,correctIndex}, ...>=2], validation }`.
- Slot word-buttons: `{ id, type:"word-buttons", categoryIds:["riflessivi"], explanation, variants:[{prompt, answer:[tokens], distractors:[tokens]}, ...>=2], validation }`. Usa `answer:[tokens]`+`distractors:[tokens]` (NO options/correctIndex).
- Cruce: `{ id:"riflessivi-300"|"riflessivi-301", type:"multiple-choice", categoryIds:[2 slugs], explanation, variants:[...>=2], validation }`. La UNICA diferencia estructural vs slot normal: `categoryIds` con 2 slugs. El motor cascadea a ambas categorias automaticamente (D-54). CERO edits de motor. Colocar los cruces -300 y -301 al FINAL del array (convencion: cruces numerados -300+ van al tail; -300 antes de -301).
- Bloque validation: `{ status:"validated", passes:[{by,date,verdict:"correcta",concerns:[]}, ... >=2 con >=2 by distintos] }`. `by` vistos en repo: `claude-opus-4-8`, `claude-sonnet-4-6`, `deepseek-chat`, `deepseek-reasoner`.
- riflessivi es 0-MATCH (D-04/D-38-03): NO hay slot `type:"match"`. Pronombre<->persona (mi<->io) es asociacion mecanica derivable = exactamente lo que D-04/R3 prohiben -> solo MC + word-buttons.

Entrada categories.json (analogo linea de modali order 13): `{ "id":"riflessivi", "name":"<display italiano>", "order":14 }` -- APPEND al final (despues de modali order 13). Solo id/name/order; NADA de origen (Phase 39). `order` es documental (content-loader NO re-ordena; categoriesForDisplay itera el array en orden de insercion).

Mecanica del MAGNET y de riflessivi-301 (analogo DIRECTO `presente-regolare-301`, presente-regolare.json lineas ~476-543):
- 4 variantes contrastantes, una por sujeto/terminacion: Marco -> -o (`si e svegliato`), Maria -> -a (`si e svegliata`), i ragazzi -> -i (`si sono alzati`), le ragazze -> -e (`si sono alzate`). El sujeto (nombre propio / sujeto explicito) porta genero+numero de forma NATURAL -> una sola terminacion correcta, sin nombrar la terminacion ni la regla (sin R1-leak).
- Auxiliar SIEMPRE essere; el auxiliar avere (`si ha svegliato` / `mi ho svegliato`) es distractora OBLIGATORIA, NUNCA key. Las otras distractoras = concordancia incorrecta para el sujeto.
- El bloque concerns[] audita las 4 terminaciones explicitamente (disciplina D-31-08; ver presente-regolare.json:529-533).
</interfaces>

<canon_editorial>
LOCKED (aplica a CADA slot, prompt y note en riflessivi.json):
- Explanations en espanol ACENTUADO RAE (a/e/i/o/u/n correctas). Un C4-accent flag del quorum = arreglar la tilde, NO override (Pitfall 10, memoria explanations_must_be_accented).
- Apostrofe ASCII U+0027, plain text (sin markdown), sin smart-quotes.
- Italianismos citados en ortografia italiana literal: `mi sveglio`, `si e svegliata`, `ci laviamo`, `vi vestite`. Preservar los acentos italianos donde son parte de la forma (`si e`, `si e svegliata`).
- R1 NO-leak: el prompt = frase + hueco (+ gloss ES donde desambigua). NUNCA nombrar la forma objetivo, la persona, la terminacion, ni etiquetas como `(reflexivo)`/`(femenino)`. La regla vive en `explanation` (mostrada tras fallo) + `notes` (autor-interno).
- Gloss `(en español: ...)` dentro del prompt: canon R7, NO es leak. Gemini/DeepSeek lo marcan C5 falso-positivo -> mantener (memoria gloss_es_desambiguacion_canon).
</canon_editorial>

<gloss_policy>
Gloss ES `(en español: ...)`:
- OBLIGATORIO en el slot REFLEX-05 mismatch (ammalarsi / dimenticarsi (di) / salire): es el desambiguador legitimo donde la reflexividad ES<->IT difiere (Pitfall 6/9). Verificar cada verbo italiano contra su contraparte espanola.
- PERMITIDO en los slots donde persona/significado es defendible (presente, su-regolari, cruce -300).
- PROHIBIDO en `riflessivi-pp-concordanza` (D-38-01): el espanol (`me he despertado`) NO concuerda el participio con el sujeto -> un gloss confundiria en vez de desambiguar. Esta es la UNICA excepcion deliberada al habito del gloss. (El cruce riflessivi-301 tambien es de concordancia; usar el mismo criterio D-38-01 alli.)
</gloss_policy>

<scope_gate>
HARD (Fuera de scope, CONTEXT lineas 24-28): NINGUNA variante (prompt, options, answer, distractors) puede contener reflexivos RECIPROCOS (`si amano`, `ci scriviamo`, "each other") — semantica A2+/B1 distinta, diferida a un milestone de pronombres. Tampoco clitico-como-contenido ni modal+clitico. Documentar OUT-OF-SCOPE en `notes` (mirror del 0-match). El passato prossimo reflexivo SI es IN scope (REFLEX-04, es el MAGNET) — no confundir con el scope-gate de reciprocos.
</scope_gate>

<validation_caveat>
CRITICO para este plan (memoria executor_cannot_run_task_quorum): si este plan corre via execute-phase -> gsd-executor, el quorum base CANONICO Opus+Sonnet (skill `gsd-validate-exercise`, que spawnea Task subagents) NO esta disponible dentro del subagent. En ese caso, validar IN-EXECUTOR con Opus-inline + DeepSeek (via scripts/validate-ai-pass.mjs) = cumple la barra ESTRUCTURAL (>=2 by distintos, verdict correcta). El SELLO canonico Opus+Sonnet + la ronda EXTRA DeepSeek del MAGNET (riflessivi-pp-concordanza + riflessivi-301) se rematan en el Plan 02 (top-level, tras execute-phase). El pase DeepSeek es OBLIGATORIO en cualquier caso para los nodos de concordancia. Marcar en el SUMMARY que variantes quedaron con quorum estructural (Opus-inline + DeepSeek) para que Plan 02 estampe el pase Sonnet canonico.
</validation_caveat>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Aprobar el mapa de slots de riflessivi antes de autorar</name>
  <read_first>
    - .planning/phases/38-verbi-riflessivi/38-CONTEXT.md (D-38-01 cue nombres propios; D-38-02 trio ES<->IT; D-38-03 guia+checkpoint del mapa; D-38-04 los 2 cruces; canon; scope-gate reciprocos)
    - .planning/phases/38-verbi-riflessivi/38-PATTERNS.md (ids semanticos propuestos: riflessivi-presente/collocazione-wb/su-regolari/pp-concordanza/mismatch; 0-match; word-buttons obligatorio REFLEX-02; los 2 cruces -300/-301)
    - .planning/research/FEATURES.md (lineas 33-35 presente + colocacion + reflexivo-sobre-regular; 48 pp con essere+agreement; 49/51 mismatch con los pares concretos; 108/119 match NOT justified -> MC + word-buttons; 136 REFLEX-04 IN scope)
    - content/exercises/presente-regolare.json (molde: ids semanticos por regla, notes documentando 0-match, mezcla MC + -wb + cruces -300/-301)
  </read_first>
  <decision>Aprobar el mapa de ids/tipo de riflessivi antes de autorar (patron v1.6/v1.7/Phase 36 D-36-01/Phase 37 D-37-02). El nº final de slots, los ids exactos (prefijo `riflessivi-`), y consolidar-vs-partir son discrecion del autor dentro de la cobertura minima; D-38-03 fija 0-match (solo MC + word-buttons) y word-buttons OBLIGATORIO para la colocacion (REFLEX-02).</decision>
  <context>
    Cobertura MINIMA obligatoria = las 5 capas REFLEX + los 2 cruces (satisface REFLEX-01..05 + SC#1..#4):
      1. REFLEX-01 presente reflexivo (chiamarsi + personas): mi chiamo/ti chiami/si chiama/ci chiamiamo/vi chiamate/si chiamano. MC.
      2. REFLEX-02 colocacion del pronombre ANTES del verbo conjugado (mi sveglio, nunca *sveglio mi): WORD-BUTTONS OBLIGATORIO, banco que incluye el orden-distractor `*sveglio mi`.
      3. REFLEX-03 reflexivo sobre terminacion regular (si alza / ci laviamo / vi vestite): engancha con presente-regolare. MC o word-buttons.
      4. REFLEX-04 passato prossimo reflexivo con essere + concordancia -o/-a/-i/-e = el MAGNET: slot dedicado riflessivi-pp-concordanza, las 4 terminaciones como variantes contrastantes, cue por nombres propios (Marco/Maria/i ragazzi/le ragazze), CERO avere, SIN gloss ES (D-38-01). MC.
      5. REFLEX-05 desajustes ES<->IT genuinos: SOLO el trio del research (ammalarsi vs "enfermar", dimenticarsi (di) vs "olvidarse (de)", salire NO-reflexivo vs "subirse"), con gloss ES obligatorio donde difiere. NO fabricar mismatch con verbos que SI coinciden (chiamarsi/svegliarsi/alzarsi/arrabbiarsi). MC.
    Mas los 2 cruces (D-38-04, al final del array): riflessivi-300 (["riflessivi","presente-regolare"], presente reflexivo sobre terminacion regular) y riflessivi-301 (["riflessivi","essere"], pp con essere + concordancia, analogo directo de presente-regolare-301).
    Mapa PROPUESTO (ids exactos = discrecion; el autor puede consolidar o partir dentro de la cobertura): `riflessivi-presente` (MC), `riflessivi-collocazione-wb` (word-buttons, OBLIGATORIO), `riflessivi-su-regolari` (MC/word-buttons), `riflessivi-pp-concordanza` (MC MAGNET), `riflessivi-mismatch` (MC), + `riflessivi-300` / `riflessivi-301`.
  </context>
  <options>
    <option id="approve-proposed">
      <name>Aprobar el mapa propuesto (5 slots REFLEX + word-buttons de colocacion + 2 cruces)</name>
      <pros>Un slot por capa REFLEX = re-verificacion aislada por concepto; espejo de como presente-regolare separa por regla. word-buttons dedicado cumple REFLEX-02. MAGNET pp-concordanza aislado para la ronda extra DeepSeek. 2 cruces al tail.</pros>
      <cons>~7 slots (la mas layered del milestone); mas volumen de quorum. Si el autor prefiere menos, elegir consolidar.</cons>
    </option>
    <option id="approve-consolidated">
      <name>Consolidar capas afines (p.ej. presente + su-regolari en 1 slot) manteniendo pp-concordanza y mismatch aislados + word-buttons + 2 cruces</name>
      <pros>Menos slots; menos volumen de quorum. Mantiene aislados el MAGNET (ronda extra) y el mismatch (alta senal).</pros>
      <cons>Menos separacion pedagogica; la re-verificacion no aisla que capa se fallo.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - El autor ha aprobado un mapa que cubre las 5 capas REFLEX-01..05 y los 2 cruces (riflessivi-300 + riflessivi-301).
    - El mapa incluye >=1 slot word-buttons para la colocacion del pronombre (REFLEX-02) con el orden-distractor en el banco.
    - El mapa incluye un slot pp-concordanza dedicado (MAGNET, REFLEX-04) con las 4 terminaciones -o/-a/-i/-e.
    - Todos los ids empiezan por `riflessivi-`.
    - El mapa es 0-match (sin ningun `type:"match"`, D-04/D-38-03).
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" (mapa propuesto), "consolidar" (opcion consolidada), o indica ajustes al mapa.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Autorar riflessivi.json (5 slots REFLEX + word-buttons + cruces -300/-301, 1-por-1 por quorum estructural)</name>
  <read_first>
    - content/exercises/presente-regolare.json (molde EXACTO: top-level notes+exercises; slot MC lineas 4-60; slot word-buttons presente-regolare-are-wb lineas 360-410 con answer[]+distractors[]; cruce presente-regolare-300 lineas 411-471; cruce presente-regolare-301 lineas 472-543 = el MOLDE del MAGNET y de riflessivi-301: 4 terminaciones -o/-a/-i/-e, cue nombres propios, avere como distractora, concerns[] D-31-08; notes documentando el 0-match)
    - content/exercises/essere.json (la categoria con la que cascadea riflessivi-301; verificar el shape de la matriz de concordancia stato/stata/stati/state, lineas ~621-740)
    - content/exercises/modali.json (twin fresco Phase 37: mismo canon/fechas; bloque validation simple y reforzado 3-by)
    - .planning/phases/38-verbi-riflessivi/38-CONTEXT.md (D-38-01 cue nombres propios + NO gloss en pp; D-38-02 trio; D-38-04 los 2 cruces; canon; scope-gate reciprocos; validation caveat)
    - .planning/phases/38-verbi-riflessivi/38-PATTERNS.md (shapes verbatim MC/word-buttons/MAGNET/cruces/validation; gloss policy; CERO avere; orden-distractor *sveglio mi)
    - .planning/research/PITFALLS.md (Pitfall 6 el MAGNET pp essere-agreement 4-way + mismatch ES<->IT; Pitfall 8 R1 leak; Pitfall 9 gloss ES R7 mantener; Pitfall 10 acentos = bug real; linea 248 cruces content-only D-54)
    - .planning/research/FEATURES.md (los pares mismatch concretos; hook reflexivo->presente-regolare/essere para los cruces; 0-match confirmado)
    - scripts/validate-ai-pass.mjs (refuerzo cross-vendor Gemini/DeepSeek, --write, auto-fallback 429 -> deepseek-reasoner; claves en .env)
  </read_first>
  <action>
    Crear `content/exercises/riflessivi.json` con top-level `{notes, exercises:[]}` copiando la estructura de presente-regolare.json. En `notes` (autor-interno, espanol acentuado, apostrofe ASCII, plain text) documentar DOS cosas, mirror de como presente-regolare documenta su 0-match: (1) riflessivi es 0-match por D-04/D-38-03 (pronombre<->persona derivable; un match mi<->io seria arrastrar sin pensar) -> solo MC + word-buttons; (2) los reflexivos RECIPROCOS (`si amano`/`ci scriviamo`, "each other") quedan HARD OUT-OF-SCOPE (semantica A2+/B1, diferida a un milestone de pronombres) -> ninguna variante los contiene. (El passato prossimo reflexivo SI es IN scope: es REFLEX-04, el MAGNET.)
    Autorar los slots del mapa aprobado (Task 1), cada slot MC con >=2 variantes:
      - REFLEX-01 presente reflexivo (chiamarsi + personas): mi chiamo/ti chiami/si chiama/ci chiamiamo/vi chiamate/si chiamano; distractoras = pronombre-persona mal casado (si chiamo para io) y el orden invertido (chiamo mi). Gloss ES donde desambigua la persona.
      - REFLEX-02 colocacion: >=1 slot WORD-BUTTONS (answer:[tokens]+distractors:[tokens]) donde el pronombre reflexivo va ANTES del verbo conjugado; el banco INCLUYE el orden-distractor `sveglio mi` (verbo antes del pronombre) para forzar `mi sveglio`. Prompt en espanol, tokens en italiano.
      - REFLEX-03 reflexivo sobre terminacion regular: si alza / ci laviamo / vi vestite; la explanation nombra el enganche con presente-regolare (ya sabes la terminacion, lo nuevo es el pronombre pre-puesto).
      - REFLEX-04 riflessivi-pp-concordanza (el MAGNET): clonar VERBATIM la mecanica de presente-regolare-301 -> las 4 terminaciones -o/-a/-i/-e como variantes contrastantes, una por sujeto (Marco -o / Maria -a / i ragazzi -i / le ragazze -e), auxiliar SIEMPRE essere, `si ha svegliato`/`mi ho svegliato` (avere) como distractora OBLIGATORIA nunca key, otras distractoras = concordancia incorrecta. SIN gloss ES (D-38-01: el espanol no concuerda el participio). concerns[] audita las 4 terminaciones explicitamente (D-31-08).
      - REFLEX-05 riflessivi-mismatch: SOLO el trio ammalarsi (vs "enfermar"), dimenticarsi (di) (vs "olvidarse (de)"), salire NO-reflexivo (vs "subirse"), con gloss ES `(en español: ...)` OBLIGATORIO donde difiere la reflexividad. NO fabricar mismatch con verbos que SI coinciden. Verificar cada verbo italiano contra su contraparte espanola.
    Cruces (AL FINAL del array, -300 antes de -301, cada uno >=2 variantes):
      - `riflessivi-300`: `categoryIds: ["riflessivi","presente-regolare"]` — el reflexivo PRESENTE sobre terminacion regular (mi sveglio / ci laviamo). Gloss ES permitido.
      - `riflessivi-301`: `categoryIds: ["riflessivi","essere"]` — el pp con essere + concordancia -o/-a/-i/-e (analogo directo de presente-regolare-301). Misma mecanica del MAGNET (cue nombres propios, CERO avere, SIN gloss D-38-01). concerns[] audita las 4 terminaciones.
    INVARIANTES antes del quorum (verificar): (a) CERO auxiliar avere como key en cualquier variante reflexiva del pp (pp-concordanza y riflessivi-301); (b) ninguna terminacion -o para sujeto femenino ni -a para masculino, etc. (una sola concordancia correcta por variante); (c) 0 reciprocos (si amano/ci scriviamo). Preservar los acentos italianos literales (`si e svegliata`).
    Validar 1-por-1 con el quorum ESTRUCTURAL disponible en el executor (Opus-inline + DeepSeek via scripts/validate-ai-pass.mjs): >=2 passes / >=2 by distintos / verdict correcta por variante/slot. El pase DeepSeek es OBLIGATORIO en pp-concordanza y en riflessivi-301 (nodos de concordancia). Resolver disputadas por REESCRITURA (ambas IAs correcta), nunca override-atajo; C4-accent en espanol = arreglar tilde; gloss R7 marcado C5 = falso-positivo, mantener. Bloque validation por variante/slot con concerns[] etiquetados [D-38-01/02/04]/[R1-R7] y la verificacion de las 4 terminaciones donde aplique.
    NOTA (validation caveat): el SELLO canonico Opus+Sonnet lo remata Plan 02 (top-level). Registrar en el SUMMARY que slots quedaron con quorum estructural (Opus-inline + DeepSeek) para que Plan 02 estampe el pase Sonnet canonico + la ronda EXTRA DeepSeek del MAGNET.
    NO tocar el motor. NO anadir slot match (0-match). NO registrar en categories.json (Task 3). NO anadir origen ni sincronizar counts (Phase 39).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/exercises/riflessivi.json'); const s=c.exercises; if(!s.every(e=>e.id.startsWith('riflessivi-'))) throw new Error('id sin prefijo riflessivi-'); if(s.some(e=>e.type==='match')) throw new Error('riflessivi debe ser 0-match (D-04/D-38-03)'); if(!s.some(e=>e.type==='word-buttons')) throw new Error('falta slot word-buttons (REFLEX-02)'); s.forEach(e=>{ if((e.variants||[]).length<2) throw new Error(e.id+' <2 variantes'); if(!e.validation||e.validation.status!=='validated') throw new Error(e.id+' no validated'); if(new Set(e.validation.passes.map(p=>p.by)).size<2) throw new Error(e.id+' <2 by'); if(!e.validation.passes.every(p=>p.verdict==='correcta')) throw new Error(e.id+' pass no correcta'); }); console.log('OK riflessivi:', s.length, 'slots')"</automated>
    <automated>node -e "const c=require('./content/exercises/riflessivi.json'); const s=c.exercises; const c300=s.find(e=>e.id==='riflessivi-300'), c301=s.find(e=>e.id==='riflessivi-301'); if(!c300||!c301) throw new Error('falta uno de los 2 cruces (SC#4 exige 2)'); if(JSON.stringify(c300.categoryIds.slice().sort())!==JSON.stringify(['presente-regolare','riflessivi'])) throw new Error('riflessivi-300 categoryIds mal'); if(JSON.stringify(c301.categoryIds.slice().sort())!==JSON.stringify(['essere','riflessivi'])) throw new Error('riflessivi-301 categoryIds mal'); const last2=s.slice(-2).map(e=>e.id); if(!(last2.includes('riflessivi-300')&&last2.includes('riflessivi-301'))) throw new Error('los cruces no estan al final del array'); [c300,c301].forEach(x=>{ if(![...new Set(x.validation.passes.map(p=>p.by))].some(b=>/deepseek/i.test(b))) throw new Error(x.id+' sin pase DeepSeek'); }); console.log('OK 2 cruces con categoryIds de 2 y pase DeepSeek')"</automated>
    <automated>node -e "const c=require('./content/exercises/riflessivi.json'); const s=c.exercises; const mag=s.find(e=>e.id==='riflessivi-pp-concordanza'); if(!mag) throw new Error('falta el MAGNET riflessivi-pp-concordanza (REFLEX-04)'); if(![...new Set(mag.validation.passes.map(p=>p.by))].some(b=>/deepseek/i.test(b))) throw new Error('MAGNET sin pase DeepSeek'); const keys=[mag,s.find(e=>e.id==='riflessivi-301')].filter(Boolean).flatMap(e=>e.variants.map(v=>v.options[v.correctIndex])); keys.forEach(k=>{ if(/\b(ho|hai|ha|abbiamo|avete|hanno)\b/.test(k)) throw new Error('CERO-avere VIOLADO: auxiliar avere como key en pp reflexivo: '+k); }); console.log('OK MAGNET: pase DeepSeek + CERO avere en las keys de concordancia')"</automated>
    <automated>node -e "const c=require('./content/exercises/riflessivi.json'); const blob=JSON.stringify(c).toLowerCase(); if(/\b(si amano|ci scriviamo|si scrivono|ci amiamo)\b/.test(blob)) throw new Error('SCOPE-GATE VIOLADO: reflexivo reciproco'); if(!c.notes||!/(recipro|si amano|ci scriviamo)/i.test(c.notes)) throw new Error('notes no documenta el scope-gate reciprocos'); if(!/match/i.test(c.notes)) throw new Error('notes no documenta el 0-match'); ['ammalarsi','dimenticarsi','salire'].forEach(v=>{ if(!blob.includes(v.replace('rsi','')) && !blob.includes(v)) throw new Error('falta el verbo mismatch: '+v); }); console.log('OK scope-gate reciprocos + notes (0-match + reciprocos) + trio mismatch presente')"</automated>
    <automated>node scripts/validate-content-fixture.mjs riflessivi content/exercises/riflessivi.json</automated>
    <automated>grep -c "applyImmediateFailure(this.state" src/screens/app.js | grep -qx 2 && echo "D-54 OK (2 call-sites)"</automated>
    <automated>test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "engine diff vacio OK"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/riflessivi.json` parsea como JSON; todos los ids empiezan por `riflessivi-`.
    - NINGUN slot es `type:"match"` (0-match, D-04/D-38-03); existe >=1 slot `type:"word-buttons"` para la colocacion (REFLEX-02) con el orden-distractor `sveglio mi` en el banco.
    - Las 5 capas REFLEX estan cubiertas: presente reflexivo (REFLEX-01), colocacion word-buttons (REFLEX-02), reflexivo sobre terminacion regular (REFLEX-03), MAGNET pp-concordanza (REFLEX-04), trio mismatch ES<->IT (REFLEX-05).
    - `riflessivi-pp-concordanza` existe con las 4 terminaciones -o/-a/-i/-e como variantes contrastantes, cue por nombres propios, SIN auxiliar avere en ninguna key (grep-verificable), SIN gloss ES (D-38-01), y pase DeepSeek.
    - El slot mismatch cubre SOLO el trio ammalarsi/dimenticarsi (di)/salire con gloss ES donde difiere; sin fabricar mismatch con verbos que coinciden.
    - `riflessivi-300` (["riflessivi","presente-regolare"]) y `riflessivi-301` (["riflessivi","essere"]) existen, al FINAL del array, cada uno con >=2 variantes y pase DeepSeek; riflessivi-301 tambien CERO avere.
    - Cada slot: variants[].length >= 2.
    - SCOPE-GATE: NINGUNA variante contiene reflexivos reciprocos (si amano/ci scriviamo); `notes` documenta el scope-gate reciprocos + el 0-match.
    - Cada slot: validation.status "validated", >=2 passes, >=2 by distintos, verdict correcta; pase DeepSeek en pp-concordanza y en riflessivi-301.
    - `node scripts/validate-content-fixture.mjs riflessivi ...` sale 0.
    - `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2; `git diff src/screens/app.js src/domain/progress.js` vacio.
  </acceptance_criteria>
  <done>
    riflessivi.json existe con las 5 capas REFLEX (presente, colocacion word-buttons con orden-distractor, sobre-regular, MAGNET pp-concordanza -o/-a/-i/-e sin avere, trio mismatch) y los 2 cruces riflessivi-300 + riflessivi-301, todo autorado 1-por-1 por quorum ESTRUCTURAL (Opus-inline + DeepSeek) y validated, con pase DeepSeek en los nodos de concordancia. Scope-gate reciprocos verificado (0 ocurrencias) y documentado en notes junto al 0-match. Motor intacto. El SUMMARY marca que slots esperan el sello canonico Opus+Sonnet de Plan 02. Listo para que Task 3 registre la categoria.
  </done>
</task>

<task type="auto">
  <name>Task 3: Registrar riflessivi en categories.json (order 14)</name>
  <read_first>
    - content/categories.json (el archivo a MODIFICAR: 13 entradas, ultima modali order 13; APPEND riflessivi order 14 al final)
    - .planning/phases/38-verbi-riflessivi/38-PATTERNS.md (seccion categories.json lineas 157-176: append al final, order 14, solo id/name/order, NO origen; el order es documental)
    - content/exercises/riflessivi.json (para el content-fixture validator de la nueva categoria)
    - .planning/research/PITFALLS.md (order duplicado; prefix-collision startsWith -- id `riflessivi` ya en RESET_PREFIXES_V12 sin colision)
  </read_first>
  <action>
    Anadir al FINAL del array `categories` de `content/categories.json` la entrada `{ "id": "riflessivi", "name": "Verbi riflessivi (mi chiamo/si alza)", "order": 14 }`, DESPUES de la entrada modali order 13. APPEND (NO insertar a mitad; anadir coma a la linea de modali, luego insertar). Tres campos EXACTOS: `id` (= slug `riflessivi`, matchea el nombre de fichero, el prefijo de ids de ejercicio, y RESET_PREFIXES_V12), `name` (display italiano; el estilo con parentesis-desambiguador espeja a modali/possessivi -- redaccion exacta a discrecion), `order` 14 (unico, siguiente libre tras 1-13). NO anadir `origen`/`source` (Phase 39, PROV-01).
    Los 3 arrays de counts hardcoded + TOTAL_EXPECTED + TOTAL_EXPECTED_BASELINE quedaran en rojo hasta Phase 39 (lockstep) -- rojo ESPERADO (patron v1.6/v1.7/Phase 36/37), NO es fallo de este plan. El planner opto por dejar el sync a Phase 39.
  </action>
  <verify>
    <automated>node -e "const c=require('./content/categories.json'); const cats=c.categories; const r=cats.find(x=>x.id==='riflessivi'); const m=cats.find(x=>x.id==='modali'); if(!r) throw new Error('falta entrada riflessivi'); if(!m) throw new Error('falta entrada modali (Phase 37)'); if(r.order!==14) throw new Error('riflessivi order != 14'); if(m.order!==13) throw new Error('modali order != 13'); if(Object.keys(r).sort().join(',')!=='id,name,order') throw new Error('campos extra (origen?) o faltantes: '+Object.keys(r)); if(cats[cats.length-1].id!=='riflessivi') throw new Error('riflessivi no esta al final del array'); const orders=cats.map(x=>x.order); if(new Set(orders).size!==orders.length) throw new Error('order duplicado'); console.log('OK categories.json:', cats.length, 'entradas; modali 13, riflessivi 14')"</automated>
    <automated>node scripts/validate-content-fixture.mjs riflessivi content/exercises/riflessivi.json && echo "riflessivi valida contra categories.json"</automated>
  </verify>
  <acceptance_criteria>
    - `content/categories.json` parsea; contiene `{id:"riflessivi", name, order:14}` al FINAL del array.
    - La entrada riflessivi tiene EXACTAMENTE id/name/order (sin `origen`/`source`).
    - La entrada modali (order 13, Phase 37) sigue presente e intacta.
    - No hay `order` duplicado; las 14 entradas tienen orders unicos 1-14.
    - `node scripts/validate-content-fixture.mjs riflessivi content/exercises/riflessivi.json` sale 0.
  </acceptance_criteria>
  <done>
    riflessivi registrada en categories.json (order 14, append). La categoria aparece en home/picker/Repaso/Examen genericamente (categoriesForDisplay itera el array). La sincronizacion de los 3 count arrays + TOTAL_EXPECTED + baseline queda para Phase 39 (rojo esperado).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Contenido JSON estatico + 1 entrada de indice; consumido por el content-loader validado existente. Sin codigo ejecutable nuevo, sin input de usuario, sin red, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | content/exercises/riflessivi.json + content/categories.json (contenido nuevo) | accept | Sin superficie de ataque ejecutable: JSON estatico consumido por el content-loader ya validado; la unica preocupacion de integridad es JSON malformado, mitigada por el `validateContent` permisivo existente + el smoke `node --test tests/*.test.js` (readdir auto-cubre riflessivi.json porque tiene >=1 ejercicio con categoryIds.length>=2). Invariante anti-XSS `x-text`-only preservado. Cero codigo nuevo, cero input de usuario, cero red, cero auth. |
| T-38-SC | Tampering | scripts/validate-ai-pass.mjs (Gemini/DeepSeek, claves .env) | accept | Script existente ya en uso (v1.4-Phases 36/37); sin instalacion de paquetes nuevos (npm/pip/cargo); claves en .env fuera de git. Sin gate de legitimidad de paquetes: 0 installs en esta fase. |

No hay amenazas high/medium introducidas: content-only, motor intacto (D-54 = 2 call-sites), invariante anti-XSS `x-text` preservado. Threat register minimo por naturaleza (brownfield puro de contenido).
</threat_model>

<verification>
- REFLEX-01 (presente reflexivo, personas) cubierto por el slot riflessivi-presente.
- REFLEX-02 (colocacion del pronombre) cubierto por >=1 slot word-buttons con el orden-distractor `sveglio mi`.
- REFLEX-03 (reflexivo sobre terminacion regular) cubierto por riflessivi-su-regolari.
- REFLEX-04 (pp con essere + concordancia -o/-a/-i/-e) cubierto por el MAGNET riflessivi-pp-concordanza (4 terminaciones, cue nombres propios, CERO avere, sin gloss, pase DeepSeek).
- REFLEX-05 (trio mismatch ES<->IT) cubierto por riflessivi-mismatch (ammalarsi/dimenticarsi/salire, gloss ES).
- SC#4: los 2 cruces riflessivi-300 (riflessivi+presente-regolare) y riflessivi-301 (riflessivi+essere) con categoryIds de 2, al final, validated, pase DeepSeek; D-54 = EXACTAMENTE 2 call-sites; git diff del motor vacio.
- SC#5: riflessivi carga en boot y aparece en home/picker/Repaso/Examen genericamente (registrada order 14).
- 0-match (D-04/D-38-03): sin slot match; documentado en notes junto al scope-gate reciprocos.
- INT-04 / SC#5: variantes validadas por quorum ESTRUCTURAL (Opus-inline + DeepSeek); el sello canonico Opus+Sonnet + la ronda EXTRA DeepSeek del MAGNET los remata Plan 02.
</verification>

<success_criteria>
- REFLEX-01..05 ejercitables por los slots autorados de riflessivi.
- Todas las variantes `validated` por quorum estructural (>=2 by distintos), con pase DeepSeek en el MAGNET y en riflessivi-301.
- riflessivi es 0-match (sin match) con >=1 word-buttons de colocacion (REFLEX-02, orden-distractor en banco).
- El MAGNET pp-concordanza tiene las 4 terminaciones -o/-a/-i/-e, CERO avere, sin gloss ES (D-38-01).
- Los 2 cruces (SC#4) propagan la cascada sin anadir call-sites (D-54 = 2).
- La categoria riflessivi aparece en el home (registrada order 14).
- Motor v1.4 intacto (git diff vacio). Counts en rojo hasta Phase 39 = esperado, no es fallo.
- El SUMMARY marca que slots esperan el sello canonico Opus+Sonnet de Plan 02.
</success_criteria>

<output>
Create `.planning/phases/38-verbi-riflessivi/38-01-SUMMARY.md` when done
</output>
