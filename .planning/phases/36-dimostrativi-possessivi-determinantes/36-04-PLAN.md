---
phase: 36-dimostrativi-possessivi-determinantes
plan: 04
type: execute
wave: 3
depends_on: [36-03, 36-02]
files_modified:
  - content/exercises/possessivi.json
  - content/categories.json
autonomous: true
requirements: [POSS-05, POSS-01, POSS-02]
must_haves:
  truths:
    - "El autor es examinado sobre la ambiguedad de suo (his/her, concuerda con lo poseido, con variantes poseedor != poseido)."
    - "El autor es examinado sobre loro posesivo INVARIABLE que conserva articulo (il loro / la loro / i loro / le loro)."
    - "Los cruces possessivi-300 (categoryIds: possessivi+articoli) y possessivi-301 (categoryIds: possessivi+genero-numero) existen como contenido, propagan la cascada D-54 y NO anaden call-sites (siguen EXACTAMENTE 2)."
    - "La categoria possessivi queda registrada en categories.json con order 12 sin romper el display del home."
  artifacts:
    - path: "content/categories.json"
      provides: "Entrada possessivi order 12 (append al final del array)"
      contains: "\"id\": \"possessivi\""
    - path: "content/exercises/possessivi.json"
      provides: "Slots A2 (suo his/her, loro invariable) + cruces possessivi-300/301, todos validated"
      contains: "possessivi-301"
  key_links:
    - from: "content/exercises/possessivi.json possessivi-300 / possessivi-301"
      to: "cascada D-54 (applyResultToSession existente)"
      via: "categoryIds de 2 -> el motor cascadea a ambas categorias sin call-site nuevo"
      pattern: "genero-numero"
    - from: "content/categories.json entry possessivi"
      to: "app.js categoriesForDisplay"
      via: "itera content.categories en orden de array -> fila nueva en el home sin codigo nuevo"
      pattern: "possessivi"
---

<objective>
Cerrar la categoria `possessivi`: autorar los DIFERENCIADORES A2 (POSS-05: `suo` his/her ambiguo + `loro` invariable con articulo), REGISTRAR la categoria en `categories.json` (order 12, solo id/name/order), y autorar los cruces multi-cat `possessivi-300` (`categoryIds: ["possessivi","articoli"]`) y `possessivi-301` (`categoryIds: ["possessivi","genero-numero"]`) (D-36-04). Todo por quorum R1-R7.

Purpose: POSS-05 es requisito obligatorio; el registro hace que possessivi aparezca en la UI genericamente; los 2 cruces cierran SC#4 de la fase (dimostrativi-300 lo hizo 36-02; aqui van los 2 de possessivi). Sin este plan la categoria no aparece en el home y el diferenciador A2 + los cruces de possessivi quedan sin cubrir.

Output: `content/exercises/possessivi.json` completado (A2 + 2 cruces) y `content/categories.json` con la entrada order 12. El campo `origen`/PROV-01 y la sincronizacion de counts son de Phase 39.

Este plan depende de 36-03 (continua possessivi.json) y de 36-02 (que ya escribio en categories.json -> evita conflicto de fichero secuenciando este a Wave 3). NO toca el motor v1.4; `applyImmediateFailure` sigue en EXACTAMENTE 2 call-sites; `git diff src/screens/app.js src/domain/progress.js` DEBE quedar vacio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-CONTEXT.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<prior_plans>
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-03-SUMMARY.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-02-SUMMARY.md
Este plan CONTINUA `content/exercises/possessivi.json` (nucleo de 36-03: concordanza, articolo, parentela). Anade a `exercises[]` sin reescribir. El `notes` ya documenta MC-only + suo/loro pendientes. Sobre `categories.json`: 36-02 ya anadio dimostrativi order 11 al final; este plan hace APPEND de possessivi order 12 DESPUES.
</prior_plans>

<interfaces>
Cruces multi-cat (analogo EXACTO `presente-regolare-300`):
- `{ id:"possessivi-300", type:"multiple-choice", categoryIds:["possessivi","articoli"], explanation, variants:[...>=2], validation }` -- el posesivo lleva articulo (enganche con articoli).
- `{ id:"possessivi-301", type:"multiple-choice", categoryIds:["possessivi","genero-numero"], explanation, variants:[...>=2], validation }` -- concordancia con la cosa poseida (enganche con genero-numero).
- La UNICA diferencia estructural vs slot normal: `categoryIds` con 2 slugs. El motor cascadea a ambas categorias automaticamente (D-54). CERO edits de motor.

Entrada categories.json (analogo linea 12 order 10): `{ "id":"possessivi", "name":"Possessivi", "order":12 }` -- APPEND al final (despues de la entrada dimostrativi order 11 de 36-02). Solo id/name/order; NADA de origen (Phase 39).

suo/loro son slots MC ordinarios (>=2 variantes), shape presente-regolare-are. possessivi sigue MC-only (D-36-03): sin match.
</interfaces>

<canon_editorial>
Igual que 36-03 (LOCKED): explanations espanol acentuado RAE (C4-accent = arreglar tilde, no override), apostrofe ASCII U+0027, plain text, R1 no-leak, gloss R7 canon (C5 falso-positivo mantener). Italianismos en ortografia italiana (`il loro`, `la sua`).
</canon_editorial>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autorar diferenciadores A2 (suo his/her + loro invariable) + los cruces possessivi-300/301</name>
  <read_first>
    - content/exercises/possessivi.json (el archivo a MODIFICAR: nucleo de 36-03; append a exercises[], respetar el notes existente)
    - content/exercises/presente-regolare.json (cruces presente-regolare-300..303 lineas 411-647: shape del cruce con categoryIds de 2 + convencion concerns[])
    - content/exercises/articoli.json (para autorar possessivi-300: articolo + posesivo)
    - content/exercises/genero-numero.json (para autorar possessivi-301: concordancia genero/numero de la cosa poseida)
    - .planning/research/PITFALLS.md (Pitfall 4 suo concuerda con lo poseido, poseedor!=poseido; Pitfall 12 cruce content-only 0 call-sites; Pitfall 8 R1; Pitfall 9 R7)
    - .planning/research/FEATURES.md (suo ambiguity his/her; loro INVARIABLE + keeps article il loro/la loro/i loro/le loro)
    - .claude/skills/gsd-validate-exercise (SKILL.md: quorum 1-por-1, NUNCA batched)
    - scripts/validate-ai-pass.mjs (refuerzo cross-vendor)
  </read_first>
  <action>
    Anadir a `content/exercises/possessivi.json` (append a exercises[], sin tocar el nucleo ni el notes existente), todo MC (D-36-03 MC-only):
      - `possessivi-suo` (MC, >=2 variantes): ambiguedad de `suo` (his/her/its) que concuerda con la COSA POSEIDA. Autorar >=1 variante con genero del poseedor != genero de lo poseido ("Marco lava ___ macchina" -> la sua) para ejercer la trampa real (Pitfall 4). El gloss ES ("su coche") NO expone la trampa -> el frame italiano la carga; usar gloss donde desambigua his-vs-her.
      - `possessivi-loro` (MC, >=2 variantes): `loro` posesivo INVARIABLE que SIEMPRE conserva articulo (il loro / la loro / i loro / le loro), incluso con parentesco (il loro padre). Distractoras = intentos de flexionar loro (*loro/*lori) o de dropear el articulo.
      - `possessivi-300` (cruce, MC, >=2 variantes): `categoryIds: ["possessivi","articoli"]` -- el posesivo lleva articulo determinado (enganche con articoli). Id estable con prefijo de la categoria primaria.
      - `possessivi-301` (cruce, MC, >=2 variantes): `categoryIds: ["possessivi","genero-numero"]` -- concordancia del posesivo con el genero/numero de la cosa poseida (enganche con genero-numero).
    CERO edits de motor: la cascada ya rutea por los 2 call-sites existentes para los cruces (categoryIds de 2).
    Validar 1-por-1 via skill `gsd-validate-exercise` (Opus+Sonnet, C1-C5, NUNCA batched, 1 subagent fresh por ejercicio). suo (Pitfall 4, trampa poseedor!=poseido) -> anadir pase DeepSeek via validate-ai-pass.mjs. Resolver disputadas por reescritura (ambas IAs correcta), nunca override; C4-accent = arreglar tilde; gloss R7 marcado C5 = falso-positivo, mantener. Bloque validation >=2 passes / >=2 by distintos / verdict correcta; concerns[] con tags [D-36-02]/[D-36-04]/[R1-R7].
    NO tocar el motor. NO sincronizar counts (Phase 39). NO anadir origen (Phase 39). NO anadir match.
  </action>
  <verify>
    <automated>node -e "const c=require('./content/exercises/possessivi.json'); const s=c.exercises; if(s.some(e=>e.type==='match')) throw new Error('possessivi debe seguir MC-only'); const suo=s.find(e=>e.id==='possessivi-suo'); const loro=s.find(e=>e.id==='possessivi-loro'); const x300=s.find(e=>e.id==='possessivi-300'); const x301=s.find(e=>e.id==='possessivi-301'); if(!suo||!loro||!x300||!x301) throw new Error('falta suo/loro/300/301'); if(JSON.stringify(x300.categoryIds.slice().sort())!==JSON.stringify(['articoli','possessivi'])) throw new Error('300 categoryIds mal'); if(JSON.stringify(x301.categoryIds.slice().sort())!==JSON.stringify(['genero-numero','possessivi'])) throw new Error('301 categoryIds mal'); [suo,loro,x300,x301].forEach(e=>{ if((e.variants||[]).length<2) throw new Error(e.id+' <2 variantes'); if(e.validation.status!=='validated') throw new Error(e.id+' no validated'); if(new Set(e.validation.passes.map(p=>p.by)).size<2) throw new Error(e.id+' <2 by'); if(!e.validation.passes.every(p=>p.verdict==='correcta')) throw new Error(e.id+' pass no correcta'); }); if(![...new Set(suo.validation.passes.map(p=>p.by))].some(b=>/deepseek/i.test(b))) throw new Error('suo (Pitfall 4) sin pase DeepSeek'); const lb=JSON.stringify(loro).toLowerCase(); ['il loro','la loro','i loro','le loro'].forEach(f=>{ if(!lb.includes(f)) throw new Error('loro: falta forma '+f); }); if(!s.every(e=>e.id.startsWith('possessivi-'))) throw new Error('id sin prefijo'); console.log('OK A2+cruces, total slots:', s.length)"</automated>
    <automated>node scripts/validate-content-fixture.mjs possessivi content/exercises/possessivi.json</automated>
    <automated>grep -c "applyImmediateFailure(this.state" src/screens/app.js | grep -qx 2 && echo "D-54 OK"</automated>
    <automated>test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "engine diff vacio OK"</automated>
  </verify>
  <acceptance_criteria>
    - `possessivi-suo` existe con >=2 variantes, incluida >=1 con poseedor != poseido; pase DeepSeek presente.
    - `possessivi-loro` existe con >=2 variantes cubriendo las 4 formas invariables il loro / la loro / i loro / le loro (siempre con articulo).
    - `possessivi-300` con `categoryIds` == `["possessivi","articoli"]`; `possessivi-301` con `categoryIds` == `["possessivi","genero-numero"]`; ambos >=2 variantes.
    - possessivi sigue sin ningun slot `type:"match"` (MC-only, D-36-03).
    - Los 4 slots: validation.status "validated", >=2 passes, >=2 by distintos, verdict correcta.
    - `node scripts/validate-content-fixture.mjs possessivi ...` sale 0.
    - `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2; git diff del motor vacio.
  </acceptance_criteria>
  <done>
    possessivi.json completo: nucleo (36-03) + suo + loro + cruces 300/301 con categoryIds de 2. POSS-05 cubierto; los cruces propagan la cascada sin call-site nuevo. Motor intacto.
  </done>
</task>

<task type="auto">
  <name>Task 2: Registrar possessivi en categories.json (order 12)</name>
  <read_first>
    - content/categories.json (el archivo a MODIFICAR: 10 entradas legacy + dimostrativi order 11 anadida por 36-02; APPEND possessivi order 12 al final)
    - .planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md (seccion categories.json lineas 211-231: append al final, order 12, solo id/name/order, NO origen)
    - .planning/research/PITFALLS.md (Pitfall 14 order duplicado/colision)
  </read_first>
  <action>
    Anadir al FINAL del array `categories` de `content/categories.json` la entrada `{ "id": "possessivi", "name": "Possessivi", "order": 12 }`, DESPUES de la entrada dimostrativi order 11 (que anadio 36-02). APPEND (Anti-Pattern 2: NO insertar a mitad). Tres campos: id (= slug, matchea prefijo de ids de ejercicio Y RESET_PREFIXES_V12), name (nombre italiano de display), order 12 (unico). NO anadir `origen`/`source` (Phase 39).
    Los conteos hardcoded quedaran en rojo hasta Phase 39 (lockstep) -- rojo ESPERADO (patron v1.6/v1.7), no es fallo de este plan.
  </action>
  <verify>
    <automated>node -e "const c=require('./content/categories.json'); const cats=c.categories; const p=cats.find(x=>x.id==='possessivi'); const d=cats.find(x=>x.id==='dimostrativi'); if(!p) throw new Error('falta entrada possessivi'); if(!d) throw new Error('falta entrada dimostrativi (36-02)'); if(p.order!==12) throw new Error('possessivi order != 12'); if(d.order!==11) throw new Error('dimostrativi order != 11'); if(Object.keys(p).sort().join(',')!=='id,name,order') throw new Error('campos extra (origen?) o faltantes: '+Object.keys(p)); if(cats[cats.length-1].id!=='possessivi') throw new Error('possessivi no esta al final'); const orders=cats.map(x=>x.order); if(new Set(orders).size!==orders.length) throw new Error('order duplicado'); console.log('OK categories.json:', cats.length, 'entradas; dimostrativi 11, possessivi 12')"</automated>
    <automated>node scripts/validate-content-fixture.mjs possessivi content/exercises/possessivi.json && node scripts/validate-content-fixture.mjs dimostrativi content/exercises/dimostrativi.json && echo "ambas categorias validan contra categories.json"</automated>
  </verify>
  <acceptance_criteria>
    - `content/categories.json` parsea; contiene `{id:"possessivi", name, order:12}` al final del array.
    - La entrada possessivi tiene EXACTAMENTE id/name/order (sin `origen`/`source`).
    - La entrada dimostrativi (order 11, de 36-02) sigue presente e intacta.
    - No hay `order` duplicado; las 12 entradas tienen orders unicos 1-12.
    - Ambas categorias nuevas validan contra categories.json (content-fixture validator sale 0 para las dos).
  </acceptance_criteria>
  <done>
    possessivi registrada en categories.json (order 12, append). Las 2 categorias de la fase (dimostrativi 11 + possessivi 12) aparecen en home/picker/Repaso/Examen genericamente. La sincronizacion de counts queda para Phase 39 (rojo esperado).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Contenido JSON estatico + entrada de indice; loader validado existente; sin input de usuario, sin red, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | content/exercises/possessivi.json (A2+cruces), content/categories.json (indice) | accept | Sin superficie nueva: JSON estatico via loader validado; invariante anti-XSS `x-text`-only preservado; los cruces reusan la cascada D-54 existente (0 call-sites nuevos). Sin input de usuario, sin red, sin auth. |

No hay amenazas high/medium introducidas: content-only, engine untouched, invariante anti-XSS `x-text` preservado.
</threat_model>

<verification>
- POSS-05 cubierto: suo his/her (poseedor!=poseido, DeepSeek) + loro invariable (4 formas), ambos validated.
- Cruces possessivi-300 (possessivi+articoli) y possessivi-301 (possessivi+genero-numero) con categoryIds de 2, validated.
- possessivi sigue MC-only (sin match).
- categories.json: dimostrativi 11 + possessivi 12, append, sin origen, sin order duplicado.
- D-54: 2 call-sites; git diff del motor vacio.
</verification>

<success_criteria>
- POSS-05 ejercitable (suo + loro validados).
- La categoria possessivi aparece en el home (registrada order 12); ambas categorias de la fase visibles.
- Los cruces possessivi-300/301 propagan la cascada sin anadir call-sites (D-54 = 2) -> cierra SC#4 junto con dimostrativi-300 (36-02).
- Motor v1.4 intacto.
</success_criteria>

<output>
Create `.planning/phases/36-dimostrativi-possessivi-determinantes/36-04-SUMMARY.md` when done
</output>
