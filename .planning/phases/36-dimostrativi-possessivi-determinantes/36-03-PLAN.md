---
phase: 36-dimostrativi-possessivi-determinantes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/possessivi.json
autonomous: false
requirements: [POSS-01, POSS-02, POSS-03, POSS-04]
must_haves:
  truths:
    - "El autor es examinado sobre la concordancia de la forma posesiva (il mio / la mia / i miei / le mie, x tuo/suo) con la COSA POSEIDA, no con el poseedor (incluidas variantes donde genero del poseedor != genero de lo poseido)."
    - "El autor es examinado sobre que el posesivo EXIGE articulo determinado (la mia casa, nunca *mia casa), contraste directo con el espanol mi casa, via MC con la distractora del calco."
    - "El autor es examinado sobre la excepcion de parentesco singular sin modificar que ELIMINA el articulo (mia madre, tuo fratello)."
    - "El autor es examinado sobre el RETORNO del articulo con plural o alteracion (le mie sorelle, la mia mamma, il loro padre) via variantes contrastantes en un slot dedicado."
    - "possessivi es MC-ONLY (D-36-03): el unico match de la fase es el de dimostrativi quello."
  artifacts:
    - path: "content/exercises/possessivi.json"
      provides: "Categoria possessivi nacida en slot+variantes: concordancia con la cosa poseida, articulo obligatorio (calco distractor), excepcion parentesco (carve-outs), todos validated, MC-only"
      contains: "\"id\": \"possessivi-"
  key_links:
    - from: "content/exercises/possessivi.json (slot parentesco)"
      to: "las 4 carve-outs de la frontera"
      via: "variantes contrastantes mia madre (drop) / la mia mamma (alterado) / le mie sorelle (plural) / il loro padre (loro)"
      pattern: "loro"
    - from: "content/exercises/possessivi.json"
      to: "content-loader.js loadContent()"
      via: "fetch generico por slug + validateContent"
      pattern: "possessivi"
---

<objective>
Dar de alta la categoria `possessivi` (order 12) nacida DIRECTAMENTE en slot+variantes (clon del patron v1.7), autorando su NUCLEO por quorum cross-vendor R1-R7 y MC-ONLY (D-36-03): concordancia de la forma posesiva con la COSA POSEIDA (no el poseedor), articulo determinado OBLIGATORIO (via MC con la distractora del calco `mi casa`->`*mia casa`), excepcion de parentesco singular sin modificar que ELIMINA el articulo, y el RETORNO del articulo con plural/`loro`/alteracion. Cubre POSS-01, POSS-02, POSS-03, POSS-04.

Purpose: Es la mitad "possessivi" del enganche con el articolo/genero-numero. Sin estos slots el autor no re-verifica el posesivo, y el magnet #2 (excepcion de parentesco con sus 4 carve-outs) queda sin ejercitar. La trampa real del hispanohablante (posesivo SIN articulo, calco de "mi casa") es el core pedagogico.

Output: `content/exercises/possessivi.json` con los slots del nucleo autorados y `validation.status: validated`. El registro en `categories.json` (order 12) y los diferenciadores A2 (`suo` his/her, `loro` invariable) + los cruces `possessivi-300`/`possessivi-301` los hace el plan 36-04.

Este plan NO toca el motor v1.4 (D-54 = 2 call-sites). `git diff src/screens/app.js src/domain/progress.js` DEBE quedar vacio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-CONTEXT.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<slug_contract>
El slug es `possessivi` (contrato transversal de Phase 35: id en categories.json + fichero `content/exercises/possessivi.json` + prefijo de TODOS los ids de ejercicio + prefijo en `RESET_PREFIXES_V12` storage.js:1168). TODOS los ids de slot DEBEN empezar por `possessivi-`.
</slug_contract>

<interfaces>
Shape de la categoria (analogo EXACTO `content/exercises/presente-regolare.json`):
- Top-level: `{ "notes": "...", "exercises": [ ...slots... ] }`. Sin categoryId top-level; pertenencia por-slot en `categoryIds:["possessivi"]`.
- Slot MC: `{ id, type:"multiple-choice", categoryIds:["possessivi"], explanation, variants:[{prompt,options,correctIndex}, ...>=2], validation }`.
- possessivi es MC-ONLY (D-36-03): NO hay slot `match` en esta categoria. La seleccion de forma posesiva es derivable por raiz (genero/numero del sustantivo) -> MC con distractoras; la ausencia/presencia de articulo se ensena con MC + la distractora del calco, NO con match.
- Bloque validation: `{ status:"validated", passes:[{by,date,verdict:"correcta",concerns:[]}, ... >=2 con >=2 by distintos] }`.
</interfaces>

<canon_editorial>
LOCKED (aplica a CADA slot): explanations espanol ACENTUADO RAE (C4-accent flag = arreglar tilde, NO override -- Pitfall 10), apostrofe ASCII U+0027, plain text (sin markdown), R1 no-leak (nada de `(parentesco)`, `(plural)`, ni la forma-objetivo nombrada -- la regla vive en explanation/notes), gloss `(en espanol: ...)` canon R7 (C5 falso-positivo mantener). Italianismos en ortografia italiana (`mia madre`, `le mie sorelle`, `il loro padre`).
</canon_editorial>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Aprobar el mapa de slots del NUCLEO possessivi</name>
  <read_first>
    - .planning/research/FEATURES.md (Per-category slot summary linea ~106: POSS ~4 table-stakes, match NOT justified; Pitfalls 3/4)
    - .planning/phases/36-dimostrativi-possessivi-determinantes/36-CONTEXT.md (D-36-03 MC-only; excepcion parentesco magnet #2 con las 4 carve-outs)
    - .planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md (sub-slot analogs: possessivi core MC + calco distractor; family-exception slot analogo presente-regolare-301)
  </read_first>
  <decision>Aprobar el mapa de ids/tipo del nucleo possessivi antes de autorar (patron v1.6/v1.7). Propuesta (ids exactos = discrecion del executor, prefijo `possessivi-`; D-36-03 fija MC-only):</decision>
  <context>
    D-36-03 (LOCKED): possessivi es MC-ONLY. El unico match de la fase es el de dimostrativi quello. NO anadir el match familia-vs-comun (considerado y DESCARTADO): la seleccion de forma es derivable por raiz -> MC; la ausencia/presencia de articulo se ensena con MC + distractora del calco.
    Mapa propuesto para el NUCLEO (este plan; los A2 suo/loro + los cruces 300/301 los hace 36-04):
      - `possessivi-concordanza` (MC) -> forma il mio/la mia/i miei/le mie (x tuo/suo) concordando con la COSA POSEIDA. INCLUIR >=1 variante donde genero del poseedor != genero de lo poseido (p.ej. "Marco lava ___ macchina" -> la sua) para ejercer la trampa real (Pitfall 4; el gloss ES no la expone). POSS-01.
      - `possessivi-articolo` (MC) -> el posesivo EXIGE articulo (la mia casa vs *mia casa). Distractora = el calco espanol SIN articulo (mi casa -> *mia casa). POSS-02.
      - `possessivi-parentela` (MC) -> excepcion de parentesco: MAGNET #2, slot dedicado con variantes que abarcan DELIBERADAMENTE ambos lados de la frontera: mia madre (drop) / la mia mamma (alterado->keep) / le mie sorelle (plural->keep) / il loro padre (loro->keep). Cubre POSS-03 (drop) Y POSS-04 (retorno) en un slot que ensena el limite (R6: 1 punto por ejercicio, pero el SLOT ensena la frontera via variantes contrastantes).
    Opcional (discrecion): partir parentela en 2 slots (drop puro POSS-03 + retorno POSS-04) si el executor lo prefiere, mientras se cubran ambos requisitos y las 4 carve-outs.
  </context>
  <options>
    <option id="approve-3slots">
      <name>Aprobar el mapa propuesto (3 slots de nucleo: concordanza, articolo, parentela con 4 carve-outs)</name>
      <pros>Coherente con D-36-03 (MC-only), con presente-regolare-301 (4 variantes contrastantes en 1 slot), y con los magnets/carve-outs de la research.</pros>
      <cons>parentela concentra drop+retorno; si el autor prefiere separarlos, elegir la otra opcion.</cons>
    </option>
    <option id="approve-4slots">
      <name>Partir parentela en 2 (drop POSS-03 + retorno POSS-04) -> 4 slots de nucleo</name>
      <pros>Un punto por slot mas explicito.</pros>
      <cons>Dos slots que ensenan las dos caras de la misma frontera; el magnet #2 se reparte.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - El autor ha aprobado un mapa que cubre: concordancia con la cosa poseida (con variante poseedor!=poseido), articulo obligatorio (con distractora del calco), y la excepcion de parentesco con las 4 carve-outs (mia madre / la mia mamma / le mie sorelle / il loro padre).
    - Todos los ids empiezan por `possessivi-`.
    - El mapa es MC-only (sin `match`, D-36-03).
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" (opcion 3-slots) o "partir parentela" (opcion 4-slots) o indica ajustes.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Autorar los slots del nucleo possessivi (1-por-1 por quorum, MC-only)</name>
  <read_first>
    - content/exercises/presente-regolare.json (molde: top-level notes+exercises, slot MC -are lineas 4-60, slot con 4 variantes contrastantes presente-regolare-301 lineas 472-543, bloque validation)
    - .planning/research/PITFALLS.md (Pitfall 3 excepcion parentesco carve-outs = magnet #2 DeepSeek; Pitfall 4 concordancia con la cosa poseida; Pitfall 8 R1 leak; Pitfall 9 R7; Pitfall 10 acentos)
    - .planning/research/FEATURES.md (dependency notes: POSS requiere genero-numero + articoli; el calco distractor mi casa->*mia casa)
    - .claude/skills/gsd-validate-exercise (SKILL.md: quorum Opus+Sonnet, C1-C5, NUNCA batched, 1 subagent fresh por ejercicio)
    - scripts/validate-ai-pass.mjs (refuerzo cross-vendor Gemini/DeepSeek, --write; claves en .env)
  </read_first>
  <action>
    Crear `content/exercises/possessivi.json` con top-level `{notes, exercises:[]}` copiando la estructura de presente-regolare.json. En `notes` (autor-interno): documentar que possessivi es MC-ONLY por D-36-03 (el match familia-vs-comun se considero y DESCARTO: forma derivable por raiz -> MC con distractora del calco), espejo de como presente-regolare documenta su 0-match. Documentar tambien que `suo`/`loro` (A2) los autora 36-04.
    Autorar los slots del mapa aprobado (Task 1), cada uno MC con >=2 variantes:
      - `possessivi-concordanza`: forma il mio/la mia/i miei/le mie (x tuo/suo) concordando con la COSA POSEIDA. INCLUIR >=1 variante donde genero del poseedor != genero de lo poseido ("Marco lava ___ macchina" -> la sua). La explanation dice la regla ("concuerda con la cosa poseida, no con el poseedor"). NO apoyarse en el gloss ES para ensenar esto (el espanol no lo expone; el frame italiano carga el contraste). Distractoras = formas posesivas reales gendered al poseedor (la trampa).
      - `possessivi-articolo`: el posesivo EXIGE articulo (la mia casa). Distractora = el calco SIN articulo (*mia casa), espejo del espanol "mi casa". La explanation nombra el calco como el error tipico.
      - `possessivi-parentela` (MAGNET #2): variantes contrastantes que abarcan las 4 carve-outs -> mia madre (drop: singular, sin modificar, sin loro), la mia mamma (alterado/afectivo -> articulo retorna), le mie sorelle (plural -> articulo retorna), il loro padre (loro SIEMPRE lleva articulo). Verificar CADA key contra las 4 carve-outs ANTES del quorum (Pitfall 3). Si Task 1 eligio 4-slots, repartir drop (POSS-03) y retorno (POSS-04) en dos slots cubriendo las mismas carve-outs.
    Validar 1-por-1 via skill `gsd-validate-exercise` (base Opus+Sonnet, C1-C5, NUNCA batched). En `possessivi-parentela` (y `possessivi-concordanza` por la trampa poseedor!=poseido) anadir OBLIGATORIAMENTE una ronda EXTRA con `by` DeepSeek via validate-ai-pass.mjs (Pitfall 3: DeepSeek caza los drops de articulo en family plural que human-verify aprueba). Resolver disputadas por REESCRITURA (ambas IAs correcta), nunca override; C4-accent = arreglar tilde; gloss R7 marcado C5 = falso-positivo, mantener. Bloque validation >=2 passes / >=2 by distintos / verdict correcta; concerns[] con tags [D-36-03]/[R1-R7] y la verificacion de las 4 carve-outs.
    NO tocar el motor. NO anadir slot match (MC-only). NO registrar en categories.json (36-04). NO autorar suo/loro/cruces (36-04).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/exercises/possessivi.json'); const s=c.exercises; if(!s.every(e=>e.id.startsWith('possessivi-'))) throw new Error('id sin prefijo possessivi-'); if(s.some(e=>e.type==='match')) throw new Error('possessivi debe ser MC-only (D-36-03)'); s.forEach(e=>{ if((e.variants||[]).length<2) throw new Error(e.id+' <2 variantes'); if(e.validation.status!=='validated') throw new Error(e.id+' no validated'); if(new Set(e.validation.passes.map(p=>p.by)).size<2) throw new Error(e.id+' <2 by'); if(!e.validation.passes.every(p=>p.verdict==='correcta')) throw new Error(e.id+' pass no correcta'); }); const par=s.filter(e=>/parent/.test(e.id)); if(par.length<1) throw new Error('falta slot parentela'); const blob=JSON.stringify(par).toLowerCase(); ['mia madre','la mia mamma','le mie sorelle','il loro padre'].forEach(cv=>{ if(!blob.includes(cv)) throw new Error('carve-out ausente: '+cv); }); par.forEach(e=>{ if(![...new Set(e.validation.passes.map(p=>p.by))].some(b=>/deepseek/i.test(b))) throw new Error(e.id+' MAGNET #2 sin pase DeepSeek'); }); console.log('OK possessivi nucleo:', s.length, 'slots MC')"</automated>
    <automated>node scripts/validate-content-fixture.mjs possessivi content/exercises/possessivi.json</automated>
    <automated>grep -c "applyImmediateFailure(this.state" src/screens/app.js | grep -qx 2 && echo "D-54 OK"</automated>
    <automated>test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "engine diff vacio OK"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/possessivi.json` parsea; todos los ids empiezan por `possessivi-`.
    - NINGUN slot es `type:"match"` (MC-only, D-36-03).
    - Cada slot tiene `variants[].length >= 2`.
    - El slot de parentesco (parentela) contiene las 4 carve-outs literales: `mia madre`, `la mia mamma`, `le mie sorelle`, `il loro padre`.
    - possessivi-concordanza incluye >=1 variante donde genero del poseedor != genero de lo poseido.
    - possessivi-articolo incluye la distractora del calco (posesivo sin articulo).
    - Cada slot: validation.status "validated", >=2 passes, >=2 by distintos, verdict correcta.
    - MAGNET #2: el/los slot(s) de parentesco incluyen un pase con `by` DeepSeek (INT-04).
    - `notes` documenta MC-only (match descartado) + suo/loro pendientes (36-04).
    - `node scripts/validate-content-fixture.mjs possessivi ...` sale 0.
    - `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2; git diff del motor vacio.
  </acceptance_criteria>
  <done>
    possessivi.json existe con el nucleo (concordancia cosa-poseida, articulo obligatorio con calco distractor, excepcion parentesco con 4 carve-outs) autorado MC-only y validado 1-por-1, con pase DeepSeek en el magnet parentesco. Motor intacto. Listo para que 36-04 anada suo/loro, registre la categoria y autore los cruces 300/301.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Contenido JSON estatico cargado por el loader validado existente; sin input de usuario, sin red, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | content/exercises/possessivi.json (contenido nuevo) | accept | Sin superficie nueva: JSON estatico via loader validado (schema-validator permisivo pero validado); invariante anti-XSS `x-text`-only preservado. Sin input de usuario, sin red, sin auth. |
| T-36-SC | Tampering | validate-ai-pass.mjs (Gemini/DeepSeek con claves .env) | accept | Script existente ya en uso; sin instalacion de paquetes nuevos; claves en .env fuera de git. |

No hay amenazas high/medium introducidas: content-only, engine untouched, invariante anti-XSS `x-text` preservado.
</threat_model>

<verification>
- POSS-01 (concordancia cosa-poseida, con poseedor!=poseido), POSS-02 (articulo obligatorio + calco distractor), POSS-03 (drop parentesco), POSS-04 (retorno) cubiertos por el nucleo.
- MC-only (sin match, D-36-03).
- MAGNET #2: pase DeepSeek en el slot de parentesco; 4 carve-outs verificadas.
- D-54: 2 call-sites; git diff del motor vacio.
</verification>

<success_criteria>
- POSS-01..04 ejercitables por el nucleo autorado de possessivi.
- Todas las variantes `validated` (>=2 by distintos), con pase DeepSeek en el magnet parentesco.
- possessivi MC-only (sin match).
- Motor v1.4 intacto (2 call-sites, diff vacio).
</success_criteria>

<output>
Create `.planning/phases/36-dimostrativi-possessivi-determinantes/36-03-SUMMARY.md` when done
</output>
