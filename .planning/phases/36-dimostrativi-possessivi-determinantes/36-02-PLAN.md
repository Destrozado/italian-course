---
phase: 36-dimostrativi-possessivi-determinantes
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - content/exercises/dimostrativi.json
  - content/categories.json
autonomous: false
requirements: [DEMOS-05, DEMOS-03]
must_haves:
  truths:
    - "El autor es examinado sobre el pronombre neutro cio (slot lexico single-variant documentado sin autoria en notes)."
    - "El autor es examinado sobre las formas pronominales de questo/quello (questo/questa/questi/queste, quello/quella/quelli/quelle)."
    - "El cruce dimostrativi-300 (categoryIds de 2: dimostrativi + articoli) existe como contenido, propaga la cascada D-54 y NO anade call-sites de applyImmediateFailure (siguen EXACTAMENTE 2)."
    - "La categoria dimostrativi queda registrada en categories.json con order 11 sin romper el display del home."
  artifacts:
    - path: "content/categories.json"
      provides: "Entrada dimostrativi order 11 (append al final del array)"
      contains: "\"id\": \"dimostrativi\""
    - path: "content/exercises/dimostrativi.json"
      provides: "Slots A2 (cio single-variant, pronominales) + cruce dimostrativi-300, todos validated"
      contains: "dimostrativi-300"
  key_links:
    - from: "content/exercises/dimostrativi.json dimostrativi-300"
      to: "cascada D-54 (applyResultToSession existente)"
      via: "categoryIds:[dimostrativi, articoli] -> el motor cascadea a ambas categorias sin call-site nuevo"
      pattern: "\"articoli\""
    - from: "content/categories.json entry dimostrativi"
      to: "app.js categoriesForDisplay"
      via: "itera content.categories en orden de array -> fila nueva en el home sin codigo nuevo"
      pattern: "dimostrativi"
---

<objective>
Cerrar la categoria `dimostrativi`: autorar los DIFERENCIADORES A2 (DEMOS-05) con el patron hibrido PROF-01/SOST-01 (`cio` = slot lexico single-variant documentado "sin autoria"; formas pronominales de questo/quello = 1 slot con variantes), REGISTRAR la categoria en `categories.json` (order 11, solo id/name/order), y autorar el cruce multi-cat `dimostrativi-300` (`categoryIds: ["dimostrativi", "articoli"]`, D-36-04). Todo por quorum R1-R7.

Purpose: DEMOS-05 es requisito obligatorio (no diferible); el registro hace que la categoria aparezca en home/picker/Repaso/Examen genericamente; el cruce 300 propaga la cascada D-54 al articolo (quello espeja lo/gli) reforzando el enganche. Sin este plan la categoria no aparece en la UI y el diferenciador A2 queda sin cubrir.

Output: `content/exercises/dimostrativi.json` completado (A2 + cruce) y `content/categories.json` con la entrada order 11. El campo `origen`/PROV-01 y la sincronizacion de counts son de Phase 39 (NO en este plan).

Este plan NO toca el motor v1.4. `git diff src/screens/app.js src/domain/progress.js` DEBE quedar vacio; `applyImmediateFailure` sigue en EXACTAMENTE 2 call-sites (`:1642`, `:1969`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-CONTEXT.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<prior_plan>
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-01-SUMMARY.md
Este plan CONTINUA el archivo `content/exercises/dimostrativi.json` creado por 36-01 (nucleo: questo, quest', quello MC+match, colapso ES). Anade a `exercises[]`; NO reescribe lo existente. El `notes` ya documenta cio single-variant + codesto OOS.
</prior_plan>

<interfaces>
Cruce multi-cat (analogo EXACTO `presente-regolare-300`, verificado en repo):
`{ id:"dimostrativi-300", type:"multiple-choice", categoryIds:["dimostrativi","articoli"], explanation:"...(en espanol: ...)", variants:[{prompt,options,correctIndex}, ...>=2], validation:{ status:"validated", passes:[{by:"claude-opus-4-8",...},{by:"deepseek-chat"|"claude-sonnet-4-6",...}] } }`
- La UNICA diferencia estructural vs un slot normal: `categoryIds` tiene DOS slugs. El motor cascadea el fallo a AMBAS categorias automaticamente (D-54). CERO edits de motor.

Entrada de categories.json (analogo linea 12 `presente-regolare` order 10):
`{ "id": "dimostrativi", "name": "Dimostrativi", "order": 11 }` -- APPEND al final del array (Anti-Pattern 2: NO insertar a mitad). Solo id/name/order; NADA de origen (Phase 39).

cio single-variant (analogo `genero-numero-articolo-plurale-logo` lineas 459-477): mismo shape MC, solo 1 entrada en variants[]. Documentado en el notes del archivo (ya escrito por 36-01).
</interfaces>

<canon_editorial>
Igual que 36-01 (LOCKED): explanations espanol acentuado RAE (C4-accent flag = arreglar tilde, no override), apostrofe ASCII U+0027, plain text, R1 no-leak, gloss R7 canon (C5 falso-positivo mantener). Italianismos en ortografia italiana.
</canon_editorial>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autorar diferenciadores A2 (cio + pronominales) + el cruce dimostrativi-300</name>
  <read_first>
    - content/exercises/dimostrativi.json (el archivo a MODIFICAR: nucleo de 36-01; append a exercises[], respetar el notes existente)
    - content/exercises/presente-regolare.json (cruces presente-regolare-300..303 lineas 411-647: shape del cruce con categoryIds de 2 + convencion concerns[] con tags)
    - content/exercises/genero-numero.json (genero-numero-articolo-plurale-logo lineas 459-477: shape del slot MC single-variant legitimo con validation)
    - content/exercises/articoli.json (familia il/i/lo/gli/l'/le para autorar el cruce dimostrativi-300 quello<->articolo)
    - .planning/research/PITFALLS.md (Pitfall 7 no forzar variantes sobre lexico -> cio; Pitfall 12 cruce = content-only, 0 call-sites; Pitfall 8 R1; Pitfall 9 R7)
    - .claude/skills/gsd-validate-exercise (SKILL.md: quorum 1-por-1, NUNCA batched)
    - scripts/validate-ai-pass.mjs (refuerzo cross-vendor)
  </read_first>
  <action>
    Anadir a `content/exercises/dimostrativi.json` (append a exercises[], sin tocar el nucleo ni el notes existente):
      - `dimostrativi-cio` (MC, SINGLE-VARIANT hibrido D-36-02): 1 sola variante sobre el pronombre neutro `cio` (p.ej. "Non capisco ___ che dici" con gloss ES). NO forzar variantes sinteticas (cio che / per cio / e cio che como padding = Pitfall 7). El notes del archivo (ya escrito por 36-01) documenta que es lexico single-variant "sin autoria de variantes" para que un reviewer futuro no lo marque incompleto.
      - `dimostrativi-pronominali` (MC, >=2 variantes): formas PRONOMINALES de questo/quello (questo/questa/questi/queste, quello/quella/quelli/quelle) usadas como pronombre (mas simples que el quello adjetival). Distractoras = otras formas pronominales reales; solo una concuerda con el referente.
      - `dimostrativi-300` (cruce, MC, >=2 variantes): `categoryIds: ["dimostrativi", "articoli"]`. Ejercer el paralelismo quello<->articolo (quel<->il, quello<->lo, quell'<->l', quei<->i, quegli<->gli, quelle<->le). Id estable con prefijo de la categoria primaria. CERO edits de motor: la cascada ya rutea por los 2 call-sites existentes.
    Validar 1-por-1 via skill `gsd-validate-exercise` (Opus+Sonnet, C1-C5, NUNCA batched, 1 subagent fresh por ejercicio). El cruce dimostrativi-300 toca `quei/quegli` (magnet #1) -> anadir pase DeepSeek via validate-ai-pass.mjs y verificar la key contra la tabla fonetica. Resolver disputadas por reescritura (ambas IAs correcta), nunca override; C4-accent = arreglar tilde; gloss R7 marcado C5 = falso-positivo, mantener. Bloque validation con >=2 passes / >=2 by distintos / verdict correcta; concerns[] con tags [D-36-02]/[D-36-04]/[R1-R7].
    NO tocar el motor. NO sincronizar counts (Phase 39). NO anadir origen (Phase 39).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/exercises/dimostrativi.json'); const s=c.exercises; const cio=s.find(e=>e.id==='dimostrativi-cio'); if(!cio||cio.variants.length!==1) throw new Error('cio no es single-variant'); const pr=s.find(e=>e.id==='dimostrativi-pronominali'); if(!pr||pr.variants.length<2) throw new Error('pronominali <2 variantes'); const x=s.find(e=>e.id==='dimostrativi-300'); if(!x) throw new Error('falta cruce 300'); const set=JSON.stringify(x.categoryIds.slice().sort()); if(set!==JSON.stringify(['articoli','dimostrativi'])) throw new Error('300 categoryIds != [dimostrativi,articoli]'); [cio,pr,x].forEach(e=>{ if(e.validation.status!=='validated') throw new Error(e.id+' no validated'); if(new Set(e.validation.passes.map(p=>p.by)).size<2) throw new Error(e.id+' <2 by'); if(!e.validation.passes.every(p=>p.verdict==='correcta')) throw new Error(e.id+' pass no correcta'); }); if(![...new Set(x.validation.passes.map(p=>p.by))].some(b=>/deepseek/i.test(b))) throw new Error('cruce 300 (magnet quello) sin pase DeepSeek'); if(!s.every(e=>e.id.startsWith('dimostrativi-'))) throw new Error('id sin prefijo'); console.log('OK A2+cruce, total slots:', s.length)"</automated>
    <automated>node scripts/validate-content-fixture.mjs dimostrativi content/exercises/dimostrativi.json</automated>
    <automated>grep -c "applyImmediateFailure(this.state" src/screens/app.js | grep -qx 2 && echo "D-54 OK"</automated>
    <automated>test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "engine diff vacio OK"</automated>
  </verify>
  <acceptance_criteria>
    - `dimostrativi-cio` existe con EXACTAMENTE 1 variante (single-variant hibrido, documentado en notes).
    - `dimostrativi-pronominali` existe con >=2 variantes.
    - `dimostrativi-300` existe con `categoryIds` == `["dimostrativi","articoli"]` (2 slugs) y >=2 variantes.
    - Los 3 slots: validation.status "validated", >=2 passes, >=2 by distintos, verdict correcta.
    - El cruce dimostrativi-300 (toca quello, magnet #1) incluye un pase DeepSeek.
    - `node scripts/validate-content-fixture.mjs dimostrativi ...` sale 0.
    - `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2; git diff del motor vacio.
  </acceptance_criteria>
  <done>
    dimostrativi.json completo: nucleo (36-01) + cio single-variant + pronominales + cruce 300 con categoryIds de 2. DEMOS-05 cubierto; el cruce propaga la cascada sin call-site nuevo. Motor intacto.
  </done>
</task>

<task type="auto">
  <name>Task 2: Registrar dimostrativi en categories.json (order 11)</name>
  <read_first>
    - content/categories.json (el archivo a MODIFICAR: 10 entradas existentes order 1-10; presente-regolare es order 10; APPEND al final)
    - .planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md (seccion categories.json lineas 211-231: append al final, order 11, solo id/name/order, NO origen)
    - .planning/research/PITFALLS.md (Pitfall 14 order colisiones/duplicados; el array define el display, order es documental)
  </read_first>
  <action>
    Anadir al FINAL del array `categories` de `content/categories.json` la entrada `{ "id": "dimostrativi", "name": "Dimostrativi", "order": 11 }`. APPEND (Anti-Pattern 2: NO insertar a mitad por afinidad semantica -> reordenaria el display de las 10 filas existentes). Tres campos unicamente: id (= slug, matchea el prefijo de los ids de ejercicio Y el RESET_PREFIXES_V12 de Phase 35), name (nombre italiano de display, estilo consistente con las filas existentes), order 11 (unico, continua la secuencia). NO anadir campo `origen`/`source` (Phase 39, PROV-01).
    Los conteos hardcoded (los 3 arrays + TOTAL_EXPECTED + baseline) quedaran en rojo hasta Phase 39 (lockstep de cierre) -- es rojo ESPERADO, no es un fallo de este plan (mismo patron v1.6/v1.7).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/categories.json'); const cats=c.categories; const d=cats.find(x=>x.id==='dimostrativi'); if(!d) throw new Error('falta entrada dimostrativi'); if(d.order!==11) throw new Error('order != 11'); if(Object.keys(d).sort().join(',')!=='id,name,order') throw new Error('campos extra (origen?) o faltantes: '+Object.keys(d)); if(cats[cats.length-1].id!=='dimostrativi' && !cats.slice(-2).some(x=>x.id==='dimostrativi')) throw new Error('no esta al final (append)'); const orders=cats.map(x=>x.order); if(new Set(orders).size!==orders.length) throw new Error('order duplicado'); console.log('OK categories.json: dimostrativi order 11,', cats.length, 'entradas')"</automated>
  </verify>
  <acceptance_criteria>
    - `content/categories.json` parsea; contiene una entrada `{id:"dimostrativi", name, order:11}`.
    - La entrada tiene EXACTAMENTE los campos id/name/order (sin `origen`/`source`).
    - Esta al final del array (append, no insertada a mitad).
    - No hay `order` duplicado en el array.
  </acceptance_criteria>
  <done>
    dimostrativi registrada en categories.json (order 11, append, id/name/order); aparecera en home/picker/Repaso/Examen genericamente sin codigo nuevo. La sincronizacion de counts queda para Phase 39 (rojo esperado).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Contenido JSON estatico + entrada de indice; loader validado existente; sin input de usuario, sin red, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | Tampering | content/exercises/dimostrativi.json (A2+cruce), content/categories.json (indice) | accept | Sin superficie nueva: JSON estatico via loader validado; invariante anti-XSS `x-text`-only preservado; el cruce reusa la cascada D-54 existente (0 call-sites nuevos). Sin input de usuario, sin red, sin auth. |

No hay amenazas high/medium introducidas: content-only, engine untouched, invariante anti-XSS `x-text` preservado.
</threat_model>

<verification>
- DEMOS-05 cubierto: cio single-variant + formas pronominales, ambos validated.
- Cruce dimostrativi-300 con categoryIds de 2 (dimostrativi+articoli), validated con pase DeepSeek.
- categories.json: entrada dimostrativi order 11, append, sin origen, sin order duplicado.
- D-54: 2 call-sites; git diff del motor vacio.
</verification>

<success_criteria>
- DEMOS-05 ejercitable (cio + pronominales validados).
- La categoria dimostrativi aparece en el home (registrada order 11).
- El cruce dimostrativi-300 propaga la cascada al articolo sin anadir call-sites (D-54 = 2).
- Motor v1.4 intacto.
</success_criteria>

<output>
Create `.planning/phases/36-dimostrativi-possessivi-determinantes/36-02-SUMMARY.md` when done
</output>
