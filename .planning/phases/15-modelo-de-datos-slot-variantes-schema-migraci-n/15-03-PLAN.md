---
phase: 15-modelo-de-datos-slot-variantes-schema-migraci-n
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - tests/fixtures/slot-variants-integration.test.js
  - content/exercises/_fixtures/slot-demo.json
autonomous: false
requirements: [SLOT-01, SLOT-03, SLOT-06]
must_haves:
  truths:
    - "El pipeline loadContent end-to-end acepta un fixture real con un slot multi-variante + un slot de 1 variante: valida ok, normaliza a slotById, y NO altera exerciseById."
    - "Los 8 archivos de categoría legacy + el bloque Canciones siguen cargando y validando sin re-autoría (back-compat SLOT-06); el snapshot append-only de avere sigue verde."
    - "La app arranca en el navegador mostrando las 9 categorías exactamente como hoy (verificación humana)."
    - "La suite completa (node --test) y el assert de avere pasan tras los cambios de Plan 01 + Plan 02 combinados."
  artifacts:
    - path: "content/exercises/_fixtures/slot-demo.json"
      provides: "Fixture canónico de autoría slot+variantes (1 slot multi-variante + 1 slot de 1 variante) usado por el test de integración"
      contains: "variants"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "Test de integración del pipeline completo (validate -> loadContent shape -> slotById/exerciseById) + back-compat de las 9 categorías reales"
  key_links:
    - from: "content/exercises/_fixtures/slot-demo.json"
      to: "validateContent + normalización slotById"
      via: "pipeline de loadContent ejecutado sobre el fixture en el test de integración"
      pattern: "slotById"
    - from: "las 9 categorías reales en content/exercises/"
      to: "validateContent"
      via: "validate-content-fixture.mjs por archivo (back-compat SLOT-06)"
      pattern: "validateContent"
---

<objective>
Verificar end-to-end que el modelo slot+variantes (Plan 01) y la migración v6 (Plan 02) integran sin romper nada: un fixture real slot+variantes recorre el pipeline completo, las 9 categorías reales siguen cargando intactas (SLOT-06), el snapshot de avere sigue verde, y la app arranca mostrando las 9 categorías como hoy.

Purpose: Cerrar la fase con la garantía de back-compat verificable — "la app arranca con las 9 categorías visibles como hoy" (criterio de éxito 5 del roadmap) — y demostrar el shape de autoría slot+variantes con un fixture canónico que sirve de contrato para Phase 16/17.
Output: Fixture slot-demo.json, test de integración del pipeline, confirmación humana del booteo, todas las verificaciones automáticas verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-CONTEXT.md
@.planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-PATTERNS.md
@.planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-01-SUMMARY.md
@.planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-02-SUMMARY.md

<interfaces>
<!-- Contratos de Plan 01/02 que este plan ejercita. -->

loadContent (post-Plan-01) retorna `{ categories, exerciseById, slotById }`. exerciseById intacto (app.js lo consume vía .payload). slotById expone cada ejercicio como `{id, type, categoryIds, explanation, variants[]}` (legacy → slot de 1).

validateContent({categories, exercisesByFile}) (post-Plan-01) acepta payload XOR variants[]; firma real (NO toma slug). El helper scripts/validate-content-fixture.mjs <slug> <path> invoca con la firma correcta (exit 0 = válido).

Shape de autoría del fixture (CONTEXT D-15-02, ejemplo canónico §Specifics):
  { "id": "...", "type": "multiple-choice", "categoryIds": ["..."], "explanation": "...",
    "variants": [ {prompt, options, correctIndex}, {prompt, options, correctIndex} ] }
  + un segundo ejercicio slot de 1 variante (SLOT-03).

Las 9 categorías reales: content/exercises/{avere,essere,...,articoli,partitivos,preposiciones}.json — leídas vía categoryRegistry. scripts/assert-avere-prefix-unchanged.mjs debe seguir exit 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture canónico slot+variantes + test de integración del pipeline completo</name>
  <files>content/exercises/_fixtures/slot-demo.json, tests/fixtures/slot-variants-integration.test.js</files>
  <read_first>
    - .planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-CONTEXT.md (§Specifics — ejemplo canónico de slot multi-variante + slot de 1 `in spiaggia`; D-15-02/03/04)
    - src/data/content-loader.js (loadContent + el slotById añadido en Plan 01; cómo construye exercisesByFile y los índices)
    - src/data/schema-validator.js (validateContent firma {categories, exercisesByFile})
    - tests/fixtures/slot-variants.test.js (los tests unitarios de Plan 01 — para no duplicar; este test es de INTEGRACIÓN del pipeline, no de unidad del validator)
    - tests/exercise-types.test.js (líneas 1278-1399 — patrón "Categorías con explanation coverage" que recorre archivos reales y los pasa por validateContent; reusar el patrón de carga real)
    - scripts/validate-content-fixture.mjs (firma de invocación correcta de validateContent)
  </read_first>
  <action>
    Crear content/exercises/_fixtures/slot-demo.json con `{ "exercises": [ ... ] }` conteniendo: (1) un slot multi-variante (type multiple-choice, categoryIds válidas, explanation a nivel de slot, variants[] con 2 variantes {prompt con hueco ___, options 3-4, correctIndex en rango}) siguiendo el ejemplo canónico del CONTEXT §Specifics; (2) un slot de 1 variante (SLOT-03) — modelar el caso `in spiaggia` como type multiple-choice o word-buttons con variants[] de longitud 1 y su explanation. Contenido en español acentuado correcto, plain text sin markdown, apóstrofes ASCII (canon editorial D-129/D-135/D-137). NO es contenido de producción — vive en _fixtures/ y NO entra en categoryRegistry. Crear tests/fixtures/slot-variants-integration.test.js que: (a) lee slot-demo.json del disco, lo pasa por validateContent con categorías sintéticas válidas → assert ok true; (b) construye el slotById replicando la normalización del loader (o invocando un helper exportado por content-loader si Plan 01 lo expuso) y afirma que el slot multi-variante preserva sus 2 variantes y el slot de 1 normaliza correctamente; (c) afirma que un ejercicio legacy con payload normaliza a slot de 1 con explanation desde payload.explanation y que exerciseById NO se muta (referencia al objeto original intacta). NO usar HTML en ningún string del fixture (T-15-XSS).
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/validate-content-fixture.mjs slot-demo content/exercises/_fixtures/slot-demo.json</automated>
  </verify>
  <done>El fixture valida ok; el test de integración afirma el shape de slotById para multi-variante + slot-de-1 + legacy, y la no-mutación de exerciseById. validate-content-fixture.mjs sobre el fixture sale exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Back-compat gate — las 9 categorías reales + Canciones + avere snapshot intactos</name>
  <files>tests/fixtures/slot-variants-integration.test.js</files>
  <read_first>
    - scripts/assert-avere-prefix-unchanged.mjs (invariante D-88/D-178 append-only de avere)
    - scripts/validate-content-fixture.mjs (validar cada archivo real)
    - tests/exercise-types.test.js (1278-1399 — recorrido de archivos reales por validateContent; VAL-07 close gate)
    - .planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-CONTEXT.md (D-15-06/07 — los 8 archivos no se tocan byte-a-byte, 372 validation blocks no se re-tocan)
  </read_first>
  <action>
    Añadir al test de integración (o como bloque describe separado en el mismo archivo) una verificación de back-compat SLOT-06: cargar cada uno de los 9 archivos reales de content/exercises/ (el registry actual) y afirmar que validateContent los acepta tal cual (ok true) con el validator extendido de Plan 01 — demostrando que un ejercicio legacy con payload sigue siendo válido (ruta XOR rama "solo payload"). Afirmar que el conteo de ejercicios por categoría no cambia. NO modificar ningún archivo de content/exercises/ (D-15-06). El snapshot append-only de avere se verifica vía el script existente en <verify>, no se duplica en JS.
  </action>
  <verify>
    <automated>node --test && node scripts/assert-avere-prefix-unchanged.mjs</automated>
  </verify>
  <done>La suite completa node --test pasa (incluyendo el nuevo bloque que valida las 9 categorías reales con el validator extendido); assert-avere-prefix-unchanged.mjs sale exit 0; ningún archivo de content/exercises/ fue modificado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Booteo de las 9 categorias como hoy + state v6 + backup round-trip (gate seguridad ASVS L1)</name>
  <what-built>El modelo slot+variantes (validator + loader slotById, Plan 01), la migración a schemaVersion 6 (Plan 02), y el fixture+tests de integración (Tasks 1-2). El render de app.js NO se tocó: las 9 categorías deben arrancar exactamente como hoy (back-compat SLOT-06). Esta es la verificación de seguridad del gate (ASVS L1, block-on-high) + la confirmación del criterio de éxito 5 del roadmap.</what-built>
  <action>Pausa para verificación humana. NO hay implementación autónoma en esta tarea: el executor presenta los pasos de <how-to-verify>, espera la <resume-signal> del autor, y solo continúa al recibir "approved". Es el gate de seguridad bloqueante (ASVS L1, block-on-high) de la fase.</action>
  <verify><human-check>El autor confirma en navegador: 9 categorias arrancan como hoy, state persiste en schemaVersion 6 sin reset de progreso, backup round-trip importa sin error de version, banners de validacion como texto plano.</human-check></verify>
  <done>El autor responde "approved" tras confirmar los 6 pasos de how-to-verify; cualquier discrepancia (categoria que no carga, progreso reseteado, fallo de import, inyeccion HTML) bloquea el cierre de la fase.</done>
  <how-to-verify>
    1. Arranca el servidor local: `npx serve` (o VS Code Live Server) en la raíz del proyecto y abre http://localhost:3000 (o el puerto que indique).
    2. Confirma que la pantalla home muestra las 9 categorías gramaticales como hasta ahora (Avere, Essere, Verbos-movimiento, Sustantivos-irregulares, Género-número, Profesiones, Articoli, Partitivos, Preposiciones) + el bloque Canciones, sin banner de error de validación.
    3. Abre una sesión de 1-2 categorías cualesquiera (p.ej. Avere y Preposiciones) y resuelve un par de ejercicios — multiple-choice, word-buttons y/o match — confirmando que se renderizan y puntúan igual que antes (el render sigue leyendo .payload intacto).
    4. (DevTools → Application → Local Storage) confirma que tras una sesión el estado persiste con `"schemaVersion":6` y que el progreso previo (si lo había) NO se ha perdido ni reseteado.
    5. (Backup round-trip) usa el botón de export de backup, luego re-importa el .json descargado: debe importar sin el error "versión más nueva".
    6. (Seguridad del gate) confirma que NO aparece ningún comportamiento inesperado de renderizado (no se inyecta HTML desde mensajes de error; los banners de validación, si los provocas editando un JSON, salen como texto plano en español).
  </how-to-verify>
  <resume-signal>Escribe "approved" si las 9 categorías arrancan como hoy, el state persiste en schemaVersion 6 sin reset, y el backup round-trip funciona; o describe cualquier discrepancia (categoría que no carga, banner de error, progreso reseteado, fallo de import).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Web estática single-user, fully-local, sin servidor/red/auth. Este plan es de integración/verificación; no introduce código de producción nuevo más allá de un fixture de test y assertions. Los inputs son los mismos de Plan 01/02 (contenido JSON en disco + state en localStorage).

| Boundary | Description |
|----------|-------------|
| Contenido JSON real (9 categorías) → loadContent → render | Carga de los archivos del autor por el pipeline extendido. |
| State en localStorage v6 → render/persistencia | El bump a v6 debe preservar el progreso del autor (sin reset en esta fase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-REGR | Tampering | Regresión de back-compat en las 8 categorías legacy / avere | mitigate | Gate automático: node --test sobre las 9 categorías reales + assert-avere-prefix-unchanged.mjs (D-88/D-178) deben pasar; checkpoint humano confirma que la app arranca con las 9 categorías como hoy. |
| T-15-XSS | XSS / Information Disclosure | Mensajes de error del validator renderizados en el banner | mitigate | Confirmación humana (paso 6) de que los banners salen como texto plano en español sin inyección de HTML; el fixture no contiene HTML. |
| T-15-DATALOSS | Tampering | Pérdida/reset de progreso del autor al migrar a v6 | mitigate | El bump 5→6 es nominal (D-15-09, sin reset — Preposiciones reset es Phase 17); checkpoint humano (paso 4) confirma que el progreso previo persiste y el state queda en schemaVersion 6. |
</threat_model>

<verification>
- `node --test` — suite completa verde (unitarios Plan 01/02 + integración Plan 03 + 9 categorías reales).
- `node scripts/validate-content-fixture.mjs slot-demo content/exercises/_fixtures/slot-demo.json` — exit 0.
- `node scripts/assert-avere-prefix-unchanged.mjs` — exit 0 (back-compat append-only).
- Checkpoint humano: app arranca con 9 categorías como hoy, state v6 sin reset, backup round-trip ok.
</verification>

<success_criteria>
- El pipeline end-to-end acepta un fixture slot+variantes real y lo normaliza a slotById sin mutar exerciseById.
- Las 9 categorías reales + Canciones cargan y validan intactas; avere snapshot verde (SLOT-06).
- La app arranca mostrando las 9 categorías como hoy; el state persiste en schemaVersion 6 sin reset; backup round-trip v6 funciona (confirmación humana).
</success_criteria>

<output>
Create `.planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-03-SUMMARY.md` when done
</output>
