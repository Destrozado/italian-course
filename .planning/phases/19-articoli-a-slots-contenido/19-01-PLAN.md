---
phase: 19-articoli-a-slots-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/articoli.json
  - .planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md
autonomous: false
requirements: [ART-01, ART-03]
must_haves:
  truths:
    - "Los 56 ejercicios de Articoli quedan reagrupados en slots por regla/forma: determinativi por forma (sing/plural separados, D-19-01) con un slot por sub-disparador en la serie lo/gli (D-19-02) y un slot con variantes para las formas invariables (la, le, una; D-17-01)"
    - "Los indeterminativi (un/uno/una/un') quedan como slots propios dentro de Articoli (categoryIds=[\"articoli\"]), misma granularidad por forma/disparador (ART-03)"
    - "Los 2 match (049, 050) quedan como slots-de-1 type:match sin variantes forzadas (D-19-03); los 6 cruces (articoli-300..305) quedan slots-de-1 con id ESTABLE intacto y categoryIds de 2 ids (D-19-04)"
    - "Cada slot tiene una explanation top-level no vacía (merge elegir-la-más-completa + injertar matices, D-17-05); ninguna variante lleva explanation propia; payload eliminado de todos los ejercicios"
    - "Las superficies existentes movidas intactas a variants[] NO se re-validan; el JSON sigue pasando validateContent"
  artifacts:
    - path: "content/exercises/articoli.json"
      provides: "Articoli reagrupado a shape slot+variantes (payload eliminado, variants[]+explanation top-level por slot)"
      contains: "\"variants\""
    - path: ".planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md"
      provides: "Mapa id-fuente→slot revisado por el autor (artefacto de auditoría); reserva el rango 300..305"
  key_links:
    - from: "content/exercises/articoli.json"
      to: "validateContent / normalizeExerciseToSlot"
      via: "shape slot+variantes (payload XOR variants[])"
      pattern: "\"variants\"\\s*:\\s*\\["
    - from: "los 6 cruces articoli-300..305"
      to: "cobertura de genero-numero / sustantivos-irregulares (clearedExerciseIds)"
      via: "id estable + categoryIds de 2 ids preservados"
      pattern: "\"articoli-30[0-5]\""
---

<objective>
Reagrupar los 56 ejercicios reales de Articoli en slots por regla/forma (D-19-01 sing/plural separados, D-19-02 un slot por sub-disparador en la serie lo/gli, D-17-01 un slot con variantes para las formas invariables la/le/una), modelando los indeterminativi como slots propios dentro de Articoli (ART-03), los 2 match como slots-de-1 type:match (D-19-03) y los 6 cruces como slots-de-1 con id ESTABLE (D-19-04). Producir la explanation de cada slot vía elegir-la-más-completa + injertar matices (D-17-05). Las superficies existentes se MUEVEN intactas a `variants[]` (cambio de contenedor → NO re-validación).

Este plan NO autora variantes nuevas (eso es 19-02) ni añade los slots de huecos y/i+vocal (eso es 19-02, D-19-06). NO toca los counts de tests/scripts (eso es 19-03, que sincroniza contra el conteo REAL final tras 19-02).

Purpose: es la columna vertebral de la fase — convierte el contenido real de Articoli al modelo slot+variantes que el motor de Phase 16 ya consume, replicando exactamente el patrón del plan 17-02 del piloto. La fusión por forma/sub-disparador y la explanation mergeada son donde el dolor anti-memorización empieza a materializarse. El split por sub-disparador (D-19-02) en la serie lo/gli es la diferencia clave vs el piloto.

Output: `articoli.json` reescrito a slot+variantes (sin payload, con variants[]+explanation top-level por slot), validando verde; mapa de reagrupación auditado por el autor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-articoli-a-slots-contenido/19-CONTEXT.md
@.planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md
@content/exercises/articoli.json

<interfaces>
<!-- Shape de slot con variantes que enforça el validator. Verificado en schema-validator.js / content-loader.js (PATTERNS §Mechanical mapping). NO modificar loader ni validator: ya aceptan variants[] desde Phase 15. -->
Shape autorado de un slot con variantes:
- `id`: string id de slot (= exercise id). Esquema = Claude's Discretion: renumeración semántica limpia para slots articoli-only (p.ej. `articoli-il-cons`, `articoli-lo-scons`, `articoli-gli-z`, `articoli-uno-ps`); RESERVAR/excluir el rango `articoli-300..305` para no colisionar con los cruces (D-19-04). Los 6 cruces CONSERVAN su id legacy intacto (articoli-300..305).
- `type`: "multiple-choice" o "match" a NIVEL de slot (54 MC + 2 match)
- `categoryIds`: array no vacío a nivel de slot. `["articoli"]` para articoli-only e indeterminativi (ART-03 → siguen siendo categoria articoli, NO categoria nueva). Los 6 cruces conservan SUS DOS ids: `["articoli","genero-numero"]` (300/301/302) y `["articoli","sustantivos-irregulares"]` (303/304/305).
- `explanation`: string no vacío OBLIGATORIO a nivel de slot (D-15-02). Merge D-17-05: base = la explanation fuente más completa/general del grupo + injertar los matices únicos de las descartadas (contrastes masc/fem, pitfalls del hispanohablante) — NO reescritura total a regla generalizada, NI quedarse con una sola perdiendo matices.
- `variants`: array NO vacío. Cada variante MC = objeto plano `{prompt (con ___), options (3-4 strings), correctIndex (en rango)}`. Cada variante match = `{prompt, pairs}` (array de 2-tuplas `[noun, articolo]`, 3+ valores distintos en columna derecha R3, sin correctIndex). La variante NO lleva `explanation` ni `validation`.
- `validation`: top-level `{status, passes[]}` — se preserva del/los ejercicio(s) fuente.

Reglas hard del validator (verificadas, PATTERNS §GOTCHAS):
- `payload` XOR `variants[]` — nunca ambos, nunca ninguno. Al convertir se ELIMINA `payload`.
- `explanation` top-level string no vacío cuando hay `variants[]`.
- `variants[]` no vacío; superficie validada por el SURFACE_VALIDATOR del `type`.
- Apóstrofes ASCII U+0027 en `l'` / `un'` (el legacy ya los usa — preservar; el smoke FALLA con smart-quotes).

Mapa de reagrupación PROPUESTO (el planner lo deriva de los `notes` de cada id; el autor lo refina en el checkpoint). Granularidad por forma/disparador:

DETERMINATIVI:
- `il` + cons simple (sing): 001,002,003,004 → 1 slot (4 variantes)
- `lo` + s+cons (sing): 005,006,007 → 1 slot (3)
- `lo` + z (sing): 008,009,010 → 1 slot (3)
- `lo` + ps (sing): 011 → slot-de-1 (celda pobre → candidata a variante en 19-02)
- `lo` + gn (sing): 012 → slot-de-1 (celda pobre)
- `lo` + x (sing): 013 → slot-de-1 (celda pobre)
- `l'` masc + vocal (sing): 014,015,016,017 → 1 slot (4)
- `la` fem invariable (sing, D-17-01 "el fem no cambia"): 018,019,020 → 1 slot (3, refuerzan la invariabilidad y el contraste con la serie masc)
- `l'` fem + vocal (sing): 021,022,023 → 1 slot (3)
- `i` masc plural: 024,025 → 1 slot (2)
- serie `gli` masc plural, un slot POR SUB-DISPARADOR (D-19-02): gli+s-cons 026; gli+z 027; gli+ps 028; gli+gn 029; gli+vocal 030,031 → 5 slots
- `le` fem plural invariable (D-17-01): 032,033,034 → 1 slot (3)

INDETERMINATIVI (slots propios, categoryIds=["articoli"], ART-03):
- `un` masc + cons: 035,036 → 1 slot (2)
- `un` masc + vocal SIN apostrofo: 037,038 → 1 slot (2)
- `uno` + s+cons: 039 → slot-de-1 (celda pobre)
- `uno` + z: 040 → slot-de-1 (celda pobre)
- `uno` + ps: 041 → slot-de-1 (celda pobre)
- `uno` + gn: 042 → slot-de-1 (celda pobre)
- `una` fem invariable: 043,044,045 → 1 slot (3)
- `un'` fem + vocal CON apostrofo: 046,047,048 → 1 slot (3)

MATCH (D-19-03): 049 (serie lo/gli/i) → slot-de-1 type:match; 050 (serie uno/un'/una/un) → slot-de-1 type:match.
CRUCES (D-19-04, id ESTABLE): 300,301,302 (articoli+genero-numero) + 303,304,305 (articoli+sustantivos-irregulares) → 6 slots-de-1, ids intactos.

Este mapa propuesto da ~25 slots (sin contar variantes nuevas ni huecos de 19-02). El conteo final lo determina el autor en el checkpoint + 19-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Producir el mapa de reagrupación auditable (id-fuente → slot)</name>
  <files>.planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md</files>
  <read_first>
    - content/exercises/articoli.json (íntegro — los 56 ejercicios con su campo `notes` que declara la regla/celda/disparador de cada uno, su `payload.explanation`, y su `validation.passes[]`)
    - .planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md §content/exercises/articoli.json (Before/After del shape legacy→slot, mechanical mapping table, ID renumbering rules, GOTCHAS)
    - .planning/phases/19-articoli-a-slots-contenido/19-CONTEXT.md §decisions (D-19-01..06, D-17-05 arrastrado)
    - .planning/milestones/v1.4-phases/17-piloto-preposiciones-contenido/17-02-PLAN.md (el plan análogo del piloto que esta fase replica)
  </read_first>
  <action>
    Crear `.planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md` como artefacto de auditoría: una tabla por cada slot resultante con columnas (slot-id propuesto, regla/forma/disparador, ids-fuente que entran como variantes, type [MC|match], categoryIds, explanation-base elegida + matices a injertar D-17-05, ¿celda pobre candidata a variante nueva en 19-02?). Aplicar el mapa propuesto en <interfaces>: D-19-01 (sing/plural slots separados), D-19-02 (un slot por sub-disparador en la serie lo/gli: s-cons/z/ps/gn separados, gli+vocal su propio slot), D-17-01 (la/le/una invariables = un slot con variantes), ART-03 (indeterminativi como slots propios con categoryIds=["articoli"]), D-19-03 (049/050 slots-de-1 type:match), D-19-04 (cruces 300..305 slots-de-1 con id ESTABLE + categoryIds de 2 ids). Para cada slot articoli-only proponer un id semántico limpio RESERVANDO el rango 300..305. Para cada slot con varias variantes, comparar las explanations fuente y registrar cuál es la base (la más completa/general) y qué matices únicos de las descartadas se injertan. Marcar explícitamente las celdas pobres (lo+ps 011, lo+gn 012, lo+x 013, uno+ps 041, uno+gn 042, uno+x si existe) como candidatas a engordar en 19-02 (D-19-05). NO autorar variantes nuevas aquí. NO incluir los slots de huecos y/i+vocal (van a 19-02). Reportar el conteo de slots resultante de SOLO esta reagrupación (sin slots nuevos ni variantes nuevas).
  </action>
  <verify>
    <automated>test -f .planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md && grep -c "articoli-" .planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo 19-REAGRUPACION-MAP.md existe con una tabla que cubre los 56 ids fuente (cada id 001-050 + 300-305 aparece exactamente una vez en alguna columna de ids-fuente)
    - La serie lo/gli está dividida por sub-disparador (D-19-02): lo+s-cons, lo+z, lo+ps, lo+gn, lo+x son slots distintos; gli tiene un slot por sub-disparador (gli+s-cons, gli+z, gli+ps, gli+gn, gli+vocal)
    - Las formas invariables (la, le, una) están como UN slot con variantes (D-17-01), no divididas por disparador
    - Los indeterminativi tienen categoryIds=["articoli"] (slots propios dentro de Articoli, NO categoria nueva, ART-03)
    - 049 y 050 marcados como slots-de-1 type:match (D-19-03); 300..305 marcados como slots-de-1 con id estable + categoryIds de 2 ids (D-19-04)
    - El rango de ids 300..305 está RESERVADO/excluido de la renumeración de slots articoli-only (sin colisión)
    - Cada slot multi-variante registra explanation-base + matices a injertar (D-17-05); las celdas pobres están marcadas como candidatas a 19-02
    - El conteo de slots de esta reagrupación está reportado (sin slots nuevos ni variantes nuevas)
  </acceptance_criteria>
  <done>
    19-REAGRUPACION-MAP.md cubre los 56 ids→slots con split por sub-disparador en lo/gli, formas invariables agrupadas, indeterminativi como slots propios, match y cruces como slots-de-1 con id estable, plan de explanation por slot multi-variante, y rango 300..305 reservado; conteo de slots reportado.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Aprobar el mapa de reagrupación de Articoli (granularidad de slots, split por sub-disparador, merges de explanation) antes de reescribir el JSON</decision>
  <context>
    El mapa (19-REAGRUPACION-MAP.md) determina la estructura final de slots y por tanto el conteo que los tests de 19-03 sincronizan. La granularidad de D-19-02 (un slot por sub-disparador en lo/gli) es la decisión más cargada de la fase: maximiza la re-verificación por trampa (z vs gn vs ps son confusiones distintas para el hispanohablante) a costa de más slots. Las formas invariables (la/le/una) bajo D-17-01 quedan como un slot con variantes. Además, las explanations mergeadas (D-17-05) requieren revisión del autor (D-85) para no perder matices (contraste masc/fem, pitfalls). Confirmar también qué celdas pobres se engordarán en 19-02.
  </context>
  <options>
    <option id="split-por-subdisparador">
      <name>Split por sub-disparador en lo/gli (D-19-02) — recomendado/locked</name>
      <pros>Coherente con D-19-02 (locked en CONTEXT): cada sub-sonido (s+cons, z, gn, ps, x) es una trampa distinta → el motor re-verifica cada celda; maximiza el "no olvidar". Respeta la línea purista-por-forma del autor del piloto.</pros>
      <cons>Más slots (celdas pobres con 1 variante hasta engordarlas en 19-02).</cons>
    </option>
    <option id="suoni-speciali-unico">
      <name>Un slot único "suoni speciali" para toda la serie lo/gli</name>
      <pros>Menos slots.</pros>
      <cons>Contradice D-19-02 (locked); mezcla trampas distintas en un slot → el motor no re-verifica cada celda. Descartado salvo override explícito del autor.</cons>
    </option>
  </options>
  <resume-signal>Confirma la granularidad (split por sub-disparador recomendado), ajusta los slot-ids/merges de explanation y marca las celdas pobres a engordar en 19-02. Escribe "aprobado" cuando el mapa esté listo para reescribir el JSON.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reescribir articoli.json a slot+variantes según el mapa aprobado</name>
  <files>content/exercises/articoli.json</files>
  <read_first>
    - .planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md (el mapa aprobado por el autor — fuente de verdad de la estructura)
    - content/exercises/articoli.json (íntegro — superficies, explanations y validation.passes[] fuente a mover/mergear)
    - .planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md §content/exercises/articoli.json (Before/After MC + Before/After match articoli-049; mechanical mapping table; ID renumbering rules; GOTCHAS de apóstrofes/categoryIds/payload-XOR)
    - .planning/milestones/v1.4-phases/17-piloto-preposiciones-contenido/17-02-PLAN.md (Task 2 análogo) y content/exercises/preposiciones.json (el output canónico del piloto — shape multi-variante y slot-de-1 reales)
  </read_first>
  <action>
    Reescribir `content/exercises/articoli.json` según el mapa aprobado. Por cada slot, emitir `{id, type, categoryIds, explanation, variants:[...], validation}` ELIMINANDO `payload` de cada ejercicio fuente y MOVIENDO su superficie intacta a `variants[]`: para MC `{prompt, options, correctIndex}`; para match (049/050) `{prompt, pairs}` (sin correctIndex). La `explanation` de cada slot multi-variante = la base elegida en el mapa + los matices injertados (D-17-05); para slots-de-1 la explanation existente sube a top-level tal cual. Mover el texto intacto NO requiere re-validación (cambio de contenedor); si al mergear/reformular cambia la SUPERFICIE de algún prompt/options existente, eso cuenta como variante nueva → quórum en 19-02 (NO re-validar aquí; documentar en el mapa/SUMMARY cualquier superficie tocada). Para los 6 cruces (300..305): conservar id, type, categoryIds de 2 ids intactos; convertir su payload a un único variant + explanation top-level (slot-de-1). Para los slots articoli-only usar el id semántico del mapa reservando 300..305. Preservar el campo `validation` top-level (de la fusión: combinar los passes[] o conservar el del ejercicio cuya superficie/explanation domina — registrar el criterio en el SUMMARY). Apóstrofes ASCII U+0027 en l'/un'/gli/le (preservar los del legacy). NO autorar variantes nuevas. NO añadir slots de huecos y/i+vocal. NO tocar counts de tests/scripts.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` exits 0
    - Ningún ejercicio conserva payload: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Todo slot con variants[] tiene explanation top-level no vacía: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); console.log('slots sin explanation:', d.exercises.filter(e=>Array.isArray(e.variants)&&(!e.explanation||!e.explanation.trim())).length)"` imprime `0`
    - Ninguna variante lleva explanation propia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); console.log('variantes con explanation:', d.exercises.flatMap(e=>e.variants||[]).filter(v=>v.explanation!==undefined).length)"` imprime `0`
    - Los 6 cruces conservan id + 2 categoryIds: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); const c=d.exercises.filter(e=>/^articoli-30[0-5]$/.test(e.id)); console.log('cruces:', c.length, 'con 2 cats:', c.filter(e=>e.categoryIds.length===2).length)"` imprime `cruces: 6 con 2 cats: 6`
    - Los 2 slots match existen: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); console.log('match slots:', d.exercises.filter(e=>e.type==='match').length)"` imprime `match slots: 2`
    - Ningún id de slot articoli-only colisiona con 300..305 (rango reservado)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/articoli.json` da 0 matches
  </acceptance_criteria>
  <done>
    articoli.json reescrito a slot+variantes (sin payload, variants[]+explanation top-level por slot, variantes sin explanation propia; lo/gli split por sub-disparador; la/le/una invariables agrupadas; indeterminativi slots propios; 049/050 match slots-de-1; cruces 300..305 con id+2 cats intactos), validateContent verde, apóstrofes ASCII preservados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano → loader/validator al boot | El `articoli.json` reescrito a mano cruza a validateContent y al render; explanation/prompt se renderizan como texto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering / data-integrity | reagrupación que pierde/duplica una superficie fuente o renumera un cruce rompiendo su cobertura | mitigate | El mapa auditado (Task 1 + checkpoint) garantiza los 56 ids fuente mapeados 1:1; el acceptance criteria asserta cruces=6 con id estable + 2 categoryIds (renumerarlos haría stale clearedExerciseIds de genero-numero/sustantivos-irregulares, regresión a no-hecha). Rango 300..305 reservado evita colisión. |
| T-19-02 | Tampering | reagrupación pierde un matiz de explanation al mergear (D-17-05) | mitigate | Merge elegir-la-más-completa + injertar matices (no reescritura); revisión del autor en el checkpoint (D-85); acceptance asserta cobertura de explanation por slot (slots sin explanation == 0). |
| T-19-03 | Tampering | explanation/prompt con HTML/markdown/smart-quotes o apóstrofe no-ASCII | mitigate | Acceptance asserta 0 smart-quotes; el smoke (19-03) re-asserta markdown/HTML/U+0027 sobre variants[].prompt + explanation. El legacy ya usa l'/un' U+0027 — preservar. |
| T-19-04 | Tampering | malformed JSON crashea el loader | mitigate | `validateContent` (existente, no modificado) rechaza con banner visible slot sin variantes / variante sin superficie válida / explanation ausente. El acceptance criteria corre el validador. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que instalar/auditar (proyecto estático offline). |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` exits 0
- `node --test tests/*.test.js` — nota: el count de Articoli (56 hardcoded en 3 sitios) FALLARÁ tras esta reescritura porque ahora hay menos slots; esto es ESPERADO y se arregla en 19-03. Documentar el rojo esperado en el SUMMARY; NO arreglar los counts aquí.
- El mapa 19-REAGRUPACION-MAP.md aprobado por el autor (checkpoint)
</verification>

<success_criteria>
- 56 ejercicios fuente reagrupados a slots por forma/sub-disparador; lo/gli split por sub-disparador (D-19-02); la/le/una invariables agrupadas (D-17-01); indeterminativi slots propios (ART-03); 049/050 match slots-de-1 (D-19-03); cruces 300..305 id estable + 2 cats (D-19-04)
- Cada slot: explanation top-level no vacía (merge D-17-05), variantes sin explanation propia, payload eliminado
- validateContent verde; sin smart-quotes
- (Los counts de tests quedan rojos a propósito hasta 19-03)
</success_criteria>

<output>
Create `.planning/phases/19-articoli-a-slots-contenido/19-01-SUMMARY.md` when done
</output>
