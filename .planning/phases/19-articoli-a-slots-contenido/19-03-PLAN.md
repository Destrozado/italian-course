---
phase: 19-articoli-a-slots-contenido
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [ART-04]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Articoli (expected: 56 en exercise-types.test.js, slot-variants-integration.test.js y run-validation-271.mjs) están sincronizados contra el conteo REAL final de slots leído del JSON (no una estimación)"
    - "TOTAL_EXPECTED en run-validation-271.mjs está recalculado = 370 - 56 + nº_slots_final_articoli"
    - "El smoke paramétrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto; Articoli convertido a slots pasa coverage/R1/R2/smart-quotes/markdown sin tocar la lógica del test, solo el count"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Articoli en CATEGORIES_WITH_EXPLANATIONS sincronizado al nº real de slots"
      contains: "articoli.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de articoli sincronizado al nº de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected articoli + TOTAL_EXPECTED recalculado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de articoli.json"
      via: "sincronización contra el conteo REAL del JSON, no estimación"
      pattern: "articoli"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Articoli (`expected: 56`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `articoli.json` leído del JSON tras 19-01/19-02 (NO una estimación), cerrando ART-04. El smoke paramétrico YA es shape-agnostic (la bifurcación `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17 y está en el codebase — verificado en exercise-types.test.js:1285,1288), así que Articoli convertido a slots pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la lógica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]).

Purpose: convertir Articoli rompe los 3 counts hoy verdes (56 replicado en 3 archivos + TOTAL_EXPECTED=370). ART-04 ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase: estructura final pasa validator + smoke paramétrico.

Output: los 3 test/script files con el count de Articoli + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validación PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md
@content/exercises/articoli.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-04). El smoke YA bifurca por shape (no requiere extensión de lógica, solo count). -->

1. tests/exercise-types.test.js:
   - línea 1273: `{ file: 'content/exercises/articoli.json', expected: 56 }` en CATEGORIES_WITH_EXPLANATIONS ← cambiar 56 al nº real de slots
   - línea 1285: `getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation` — YA bifurcado por shape (heredado del piloto, NO tocar)
   - línea 1288: `getPrompts` YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar)
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido → se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:174: `{ slug: 'articoli', expected: 56 }` en REAL_CATEGORIES ← cambiar 56 al nº real de slots

3. scripts/run-validation-271.mjs:
   - línea 81: `{ slug:'articoli', file:'...', expected: 56 }` ← cambiar 56 al nº real
   - línea 91: `const TOTAL_EXPECTED = 370;` ← recalcular = 370 - 56 + nº_slots_final_articoli
   - líneas 63-77: comentario explicativo del historial del total ← añadir la conversión de Articoli a slots (precedente: el comentario narra cada cambio que movió el total)

El count `expected` de articoli = nº de SLOTS final (data.exercises.length tras 19-01+19-02) — sincronizar contra el JSON REAL, NO contra una estimación. La fusión por forma REDUCE el count (56 → ~25 tras reagrupar); los slots nuevos y/i+vocal de 19-02 lo SUBEN un poco. Net = leer del JSON.
Comando: `node -e "console.log(require('./content/exercises/articoli.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Articoli en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (líneas 1265-1300 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + el assert de conteo; y el bloque VAL_07_STRICT)
    - content/exercises/articoli.json (estado final tras 19-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md §tests/exercise-types.test.js (por qué el count es el ÚNICO cambio: el test ya es shape-agnostic; los accessors getExplanation/getPrompts ya bifurcan)
    - .planning/milestones/v1.4-phases/17-piloto-preposiciones-contenido/17-04-PLAN.md (Task 1 análogo del piloto — el piloto SÍ introdujo la bifurcación; aquí ya existe, solo se sincroniza el count)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de articoli.json (`data.exercises.length` tras 19-02, vía `node -e "console.log(require('./content/exercises/articoli.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry articoli en CATEGORIES_WITH_EXPLANATIONS (línea 1273) a ese número. NO tocar la lógica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto) → Articoli slot pasa coverage/R1/R2/smart-quotes/markdown automáticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level → no cambia salvo heredar el count sincronizado (toda variante/slot nuevo de 19-02 ya está validated por el quórum → la gate pasa). NO tocar los counts de las otras 8 categorías.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de articoli en CATEGORIES_WITH_EXPLANATIONS == nº REAL de slots: `node -e "const d=require('./content/exercises/articoli.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en línea 1273
    - Las otras 8 categorías siguen verdes con sus counts originales (preposiciones 49, genero-numero 40, avere 23, sustantivos-irregulares 31, verbos-movimiento 37, essere 39, profesiones 51, partitivos 44) — no se tocaron
    - La lógica del test (getExplanation/getPrompts/bifurcación) NO se modificó: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Articoli en CATEGORIES_WITH_EXPLANATIONS sincronizado al nº real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-articoli intactas; lógica del smoke (ya shape-agnostic) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED)</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (líneas 166-202 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (líneas 63-104 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/articoli.json (conteo REAL final de slots)
    - .planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md §tests (GOTCHA: el test cuenta SLOTS, no variantes; fusión baja el count, huecos lo suben)
    - .planning/milestones/v1.4-phases/17-piloto-preposiciones-contenido/17-04-PLAN.md (Task 2 análogo del piloto — mismo patrón de 3 hardcodes + TOTAL_EXPECTED)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'articoli', expected: 56 }` (línea 174) a `expected` = nº REAL de slots. El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 56` de la entry articoli (línea 81) a nº REAL de slots; recalcular `TOTAL_EXPECTED` (línea 91) = `370 - 56 + nº_slots_final_articoli`; actualizar el comentario explicativo del historial del total (líneas 63-77) registrando la conversión de Articoli a slots (precedente: el comentario narra cada cambio histórico que movió el total — el del piloto Preposiciones está en líneas 73). Leer el conteo real del JSON, NO usar una estimación. Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO número.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de articoli en ambos archivos == nº REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/articoli.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 370 − 56 + nº_slots_final_articoli (verificable aritméticamente contra el conteo real)
    - El reporter de run-validation-271 no emite warning `total ≠ esperado` para articoli
    - El comentario del historial del total en run-validation-271.mjs menciona la conversión de Articoli a slots
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Articoli (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el nº real de slots; TOTAL_EXPECTED recalculado; reporter PASS sin warnings de count; comentario del historial actualizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido → smoke paramétrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | count desincronizado entre los 3 hardcodes → gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leído del JSON (`data.exercises.length`), no estimaciones. El acceptance criteria asserta que coinciden y que run-validation-271 PASS. |
| T-19-03 | Tampering | explanation/prompt de slot con HTML/markdown/smart-quotes pasa sin escanear | mitigate | El smoke YA bifurca por shape (heredado del piloto): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots — la cobertura de seguridad de contenido NO se pierde al convertir Articoli. Verificado en exercise-types.test.js:1285,1288. |
| T-19-08 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 19-02 pasó el quórum (status validated) antes de integrarse → la gate pasa solo si 19-02 cumplió. Si algo quedó disputed, la gate FALLA visible. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v8 de Phase 18, integration, exercise-types)
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` exits 0
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de articoli.json
</verification>

<success_criteria>
- Los 3 hardcodes de count de Articoli + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacía — verificado por el smoke shape-agnostic)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); lógica del smoke NO tocada (ya shape-agnostic)
</success_criteria>

<output>
Create `.planning/phases/19-articoli-a-slots-contenido/19-03-SUMMARY.md` when done
</output>
