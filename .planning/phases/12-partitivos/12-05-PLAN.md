---
phase: 12-partitivos
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified:
  - content/exercises/partitivos.json
autonomous: false
requirements: [PART-07]
must_haves:
  truths:
    - "Todos los ejercicios de partitivos.json tienen validation.status === validated con >=2 by DISTINTOS en passes[] (validación por quórum cross-vendor), pool = deepseek-v4-flash + claude-sonnet-4-6 (D-10) — D-15"
    - "Cada entry de passes[] lleva el model-id COMPLETO con tier (deepseek-v4-flash, claude-sonnet-4-6), nunca una clave genérica de vendor (deepseek) — el invariante >=2 by distintos no se puede burlar con dos tiers del mismo modelo (D-12)"
    - "El canon ortográfico de acentos (D-135) se aplicó como REGLA EXPLÍCITA del prompt de validación, no como scan posterior ni a discreción de la IA — ambas IAs verifican que las explanations cumplen español acentuado + italianismos preservados (D-11)"
    - "El reporter node scripts/run-validation-271.mjs sale exit 0 incluyendo todos los ejercicios nuevos de partitivos (328+N validated)"
    - "El smoke test estricto VAL_07_STRICT=1 node --test tests/*.test.js sigue verde con las constantes de conteo actualizadas (328+N)"
  artifacts:
    - path: "content/exercises/partitivos.json"
      provides: "Todos los ejercicios de partitivos con validation.status validated y passes[] de >=2 modelos distintos cross-vendor (deepseek-v4-flash + claude-sonnet-4-6)"
      contains: "validated"
  key_links:
    - from: "content/exercises/partitivos.json (validation.passes)"
      to: "scripts/run-validation-271.mjs (sub-gates VAL-04/VAL-06/VAL-08)"
      via: "deriveStatus exige >=2 correcta con by distintos; el reporter verifica status validated por ejercicio y el conteo 328+N"
      pattern: "\"status\": \"validated\""
---

<objective>
Validar por QUÓRUM CROSS-VENDOR (≥2 IAs distintas de vendors diferentes) todos los ejercicios nuevos de `content/exercises/partitivos.json`, llevándolos de `validation.status: "pending"` a `"validated"` con ≥2 `by` distintos en `passes[]`, y cerrar el GATE VERDE de la fase: `node scripts/run-validation-271.mjs` exit 0 + `VAL_07_STRICT=1 node --test tests/*.test.js` verde.

Purpose: PART-07 — la definición de "done" editorial del proyecto exige quórum multi-IA por ejercicio (heredado de v1.1, R1-R7 → C1-C5). Cierra el último criterio de éxito del ROADMAP (reporter exit 0 + smoke estricto verde). El pool de Phase 12 es **DeepSeek Flash + Sonnet 4.6** (D-10): cross-vendor real (DeepSeek + Anthropic), elegido por el autor tras descartar dos tiers del mismo vendor — la diversidad de vendor es lo que cazó 8 bugs en Articoli (Phase 11). Distinto del pool de v1.1 (Opus+Sonnet) y del de P11 (Opus+DeepSeek), conserva la diversidad de criterio.

Output: `content/exercises/partitivos.json` con todos los ejercicios en `status: "validated"`, cada uno con ≥2 `by` distintos (`deepseek-v4-flash` + `claude-sonnet-4-6`) y model-id con tier (D-12), audit trail en git, y ambos comandos del gate en verde (reporter 328+N validated; smoke estricto verde).

IMPORTANTE — este plan es NO AUTÓNOMO y se ejecuta INTERACTIVAMENTE por el operador (el autor) en la sesión principal, NO por un subagente autónomo. Razón arquitectónica documentada en la skill `gsd-validate-batch`: usa `AskUserQuestion` (checkpoints por categoría + cola disputed con 4 caminos), y `AskUserQuestion` NO está disponible en subagentes spawneados vía Task() (D-VAL-19/D-VAL-20, anthropics/claude-code#18721). Las tareas de abajo son guiones para que el OPERADOR ejecute la validación y verifique el gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-partitivos/12-CONTEXT.md
@.claude/skills/gsd-validate-batch/SKILL.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@docs/VALIDACION-QUORUM.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Checkpoint (operador) — validar partitivos por quórum cross-vendor DeepSeek Flash + Sonnet 4.6 (interactivo, no autónomo)</name>
  <what-built>
    Hasta aquí (planes 12-01..12-04): el temario, todos los ejercicios de partitivos.json con explanations curadas y validation.status "pending", y los 3 puntos de integración cableados (categories.json order 9 + reporter TOTAL_EXPECTED 328+N + test) con conteos en lockstep. Lo que FALTA es la validación por quórum: flipar cada ejercicio de "pending" a "validated" con ≥2 by distintos (pool cross-vendor deepseek-v4-flash + claude-sonnet-4-6).
  </what-built>
  <how-to-verify>
    Esta es una acción del operador (NO automatizable por subagente, porque el flujo usa AskUserQuestion). Pasos:

    1. Asegúrate de estar en la sesión principal de Claude Code (no en un subagente). Confirma que `.env` tiene `DEEPSEEK_API_KEY` (lo añadió Phase 11; `.env.example` lo documenta).

    2. POOL LOCKED para Phase 12 (D-10): **deepseek-v4-flash + claude-sonnet-4-6** — cross-vendor (DeepSeek + Anthropic). NO usar Opus (el canon v1.1 era Opus+Sonnet; P11 usó Opus+DeepSeek; Phase 12 cambia a Flash+Sonnet a petición del autor, conservando diversidad de vendor). El invariante es ≥2 by DISTINTOS, y cada by DEBE llevar el tier completo (D-12): `deepseek-v4-flash` y `claude-sonnet-4-6` — NUNCA una clave genérica `deepseek` o `claude`.
       - Pase DeepSeek Flash: vía `node scripts/validate-ai-pass.mjs <id> --model=deepseek-v4-flash --write` (multi-provider, auto-fallback en 429; el `by` registrado es SIEMPRE el modelo que respondió de verdad). Ver docs/VALIDACION-QUORUM.md.
       - Pase Sonnet 4.6: vía Task subagent con model sonnet (el skill gsd-validate-exercise corre el pase Claude por Task). El by resultante debe ser `claude-sonnet-4-6`.

    3. ACENTOS COMO REGLA EXPLÍCITA DEL PROMPT (D-11): el canon ortográfico de acentos (D-135: español acentuado á/é/í/ó/ú + ñ RAE; italianismos preservados) debe figurar como REGLA EXPLÍCITA en el prompt de validación (09-VALIDATION-PROMPT.md, criterio C4 de explanation), NO como un scan posterior ni a discreción de la IA. Antes de lanzar, verifica que el prompt incluye esa regla; si no, añádela (es la lección de Phase 11: DeepSeek fue estricto en acentos donde Opus fue lenient — el detalle de acentos se recuerda en las reglas para que no se escape).

    4. Lanza la validación apuntando a la categoría nueva. NOTA CRÍTICA (feedback de Phase 11): el slug `partitivos` NO está en la lista lockeada D-VAL-22 de la skill gsd-validate-batch (que enumera solo los 7 slugs de v1.1) — igual que pasó con `articoli`. Por eso:
       - Intenta primero: /gsd-validate-batch partitivos
       - Si la skill RECHAZA `partitivos` como scope no reconocido (lo más probable, dado el precedente de articoli), usa el MODO CSV de IDs: pasa la lista de ids partitivos-001..partitivos-0NN separados por coma (la skill los resuelve por archivo). En Phase 11 funcionó el modo CSV; documenta en el SUMMARY cuál de los dos modos funcionó.
       - Alternativa equivalente: correr el pase DeepSeek Flash con scripts/validate-ai-pass.mjs por id y el pase Sonnet por gsd-validate-exercise por id, ejercicio a ejercicio (1-por-1, NUNCA batched — D-VAL-20).

    5. El flujo itera 1-por-1 (un subagente fresh-context por ejercicio, NUNCA batched — D-VAL-20): por cada ejercicio corre el pase DeepSeek Flash y el pase Sonnet, parsea verdicts, escribe passes[] con los by CON tier, deriva status y commitea. Verás un checkpoint AskUserQuestion al cierre y, si hay disputed, la cola con 4 caminos (accept fix / reject+override / rewrite / skip).

    6. Resuelve cada disputed con el criterio de la memoria feedback_disputed_resolution.md: calidad > tokens, NO override-atajo; ambas IAs deben acabar dando "correcta" (salvo override consciente camino b). Incluye traducción española en el prompt si hay doble-validez que desambiguar (esto es especialmente relevante en Partitivos: las alternativas qualche/un po' di/alcuni son casi sinónimos — la restricción gramatical de D-03 debe garantizar UNA correcta; si una IA marca doble-validez, revisar que el ejercicio fuerza la restricción).

    7. Objetivo: 0 disputed sin resolver y 0 deferred (la sección ## Deferred-disputed de STATE.md vacía o ausente). PART-07 / VAL-06 impiden cerrar con deferred.
  </how-to-verify>
  <resume-signal>Escribe "validación completa" cuando todos los ejercicios de partitivos.json estén en status "validated" (≥2 by distintos con tier, 0 disputed, 0 deferred), o describe qué disputed quedan y por qué.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verificar quórum por ejercicio (≥2 by distintos CON tier) y cerrar el gate verde</name>
  <read_first>
    - content/exercises/partitivos.json — comprobar que todos los ejercicios tienen validation.status "validated" y passes[] con ≥2 by DISTINTOS, cada by con tier completo (deepseek-v4-flash / claude-sonnet-4-6)
    - scripts/run-validation-271.mjs (líneas 80-110 y 290-310) — el helper effectiveStatus y los sub-gates VAL-04/VAL-06/VAL-08 que el reporter verifica, y el chequeo totalValidated/totalActual === TOTAL_EXPECTED (328+N)
    - src/data/validation-state.js — deriveStatus: "validated" exige ≥2 correcta con by DISTINTOS y cero incorrecta (sticky disputed)
  </read_first>
  <action>
    Esta tarea verifica el resultado de la validación interactiva (la acción del checkpoint anterior) y cierra el gate. Es la parte automatizable del plan.

    1. Verificar quórum por ejercicio: cada ejercicio de partitivos.json debe tener validation.status === "validated" y passes[] con al menos 2 entries de `by` DISTINTOS, todas verdict "correcta" (o un override consciente by:"autor" "correcta" si hubo camino b — el reporter lo acepta vía effectiveStatus). NINGÚN ejercicio en "pending" ni "disputed". VERIFICAR D-12: los `by` distintos llevan el tier completo (esperados `deepseek-v4-flash` y `claude-sonnet-4-6`); NINGUNA entry con clave genérica de vendor (`deepseek`/`claude` a secas). El pool esperado es cross-vendor (DeepSeek + Anthropic), no dos tiers del mismo vendor.

    2. Ejecutar el reporter del gate: node scripts/run-validation-271.mjs — debe salir exit 0, con los sub-gates VAL-06 (todos validated), VAL-08 (cero disputed) y VAL-04 (≥2 by distintos) en PASS, e incluir el conteo de partitivos (TOTAL_EXPECTED = 328+N, totalValidated == totalActual == 328+N).

    3. Ejecutar el smoke test estricto: VAL_07_STRICT=1 node --test tests/*.test.js — debe salir exit 0 (todos los tests verdes, incluyendo los 3 invariantes editoriales de partitivos del plan 12-04 y cualquier check VAL_07 estricto).

    Si el reporter o el smoke fallan: NO es trabajo de re-autoría; lo normal es (a) algún ejercicio quedó en pending/disputed (volver al checkpoint anterior con los ids concretos), (b) un by sin tier o un solo by distinto (volver a validar ese ejercicio asegurando los 2 vendors), o (c) un conteo se desincronizó (revisar lockstep de 12-04: TOTAL_EXPECTED debe ser 328+N y las 3 cuentas idénticas). Diagnosticar con el output del reporter (lista por categoría) y remediar en el plan correspondiente, no parcheando aquí.

    NO modificar el contenido pedagógico de los ejercicios en esta tarea (prompts/options/explanations ya aprobados en 12-02/12-03). Esta tarea SOLO verifica el estado de validación y el gate.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); const bad=d.exercises.filter(e=>!e.validation||e.validation.status!=='validated'); if(bad.length){console.error('No validados: '+bad.map(e=>e.id+':'+(e.validation&&e.validation.status)).join(', '));process.exit(1)} const lowQuorum=d.exercises.filter(e=>{const by=new Set((e.validation.passes||[]).filter(p=>p&&p.verdict==='correcta').map(p=>p.by)); return by.size<2}); if(lowQuorum.length){console.error('Quorum <2 by distintos: '+lowQuorum.map(e=>e.id).join(', '));process.exit(1)} const genericBy=d.exercises.filter(e=>(e.validation.passes||[]).some(p=>p&&['deepseek','claude','gemini','openai'].includes(p.by))); if(genericBy.length){console.error('by generico sin tier (viola D-12): '+genericBy.map(e=>e.id).join(', '));process.exit(1)} console.log('OK '+d.exercises.length+' ejercicios validated con quorum >=2 by distintos CON tier')" && node scripts/run-validation-271.mjs && VAL_07_STRICT=1 node --test tests/*.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TODOS los ejercicios de partitivos.json tienen validation.status === "validated".
    - Cada ejercicio tiene ≥2 `by` DISTINTOS con verdict "correcta" en passes[] (quórum VAL-04), o un override consciente by:"autor" que el reporter acepta.
    - Los `by` distintos llevan tier completo (esperados `deepseek-v4-flash` + `claude-sonnet-4-6`); NINGUNA entry usa clave genérica de vendor (`deepseek`/`claude`/`gemini`) — D-12 verificado.
    - `node scripts/run-validation-271.mjs` sale exit 0 con VAL-06/VAL-08/VAL-04 en PASS y el conteo de partitivos incluido (TOTAL_EXPECTED = 328+N).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` sale exit 0 (smoke estricto verde).
    - La sección ## Deferred-disputed de .planning/STATE.md está vacía o ausente (0 deferred).
  </acceptance_criteria>
  <done>Todos los ejercicios de partitivos validados por quórum cross-vendor (deepseek-v4-flash + claude-sonnet-4-6, by con tier); reporter exit 0 (328+N); smoke estricto verde. Phase 12 lista para verifier.</done>
</task>

</tasks>

<verification>
- PART-07 cubierto: 100% de partitivos.json en status "validated" con ≥2 by distintos CON tier (deepseek-v4-flash + claude-sonnet-4-6, D-10/D-12).
- El canon de acentos (D-135) se aplicó como regla explícita del prompt (D-11), no como scan posterior.
- Gate verde: `node scripts/run-validation-271.mjs` exit 0 (incluye partitivos, 328+N) + `VAL_07_STRICT=1 node --test tests/*.test.js` exit 0.
- 0 disputed, 0 deferred.
</verification>

<success_criteria>
- PART-07 satisfecho; los 5 criterios de éxito del ROADMAP Phase 12 quedan cubiertos en conjunto con los planes 12-01..12-04.
- El gate del milestone v1.2 (para Phase 12) en verde; lista para `/gsd:verify-phase 12` o el cierre que corresponda, y tras él `/gsd:complete-milestone v1.2`.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation.passes ↔ gate del reporter | El status validated controla si el gate pasa; lo escribe el flujo de quórum (pase DeepSeek vía validate-ai-pass.mjs + pase Sonnet vía Task aislado por ejercicio). |
| validate-ai-pass.mjs ↔ proveedor externo (DeepSeek) | El script llama a api.deepseek.com con la API key de .env; el by registrado es el modelo que respondió de verdad (auto-fallback en 429). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing / Repudiation | passes[].by (identidad del validador) | mitigate | El by registra el model-id COMPLETO con tier (deepseek-v4-flash / claude-sonnet-4-6), nunca una clave genérica de vendor (D-12) — así el invariante ≥2 by distintos no se burla con dos tiers del mismo modelo bajo una clave. deriveStatus exige ≥2 by DISTINTOS, evita auto-aprobación con un solo validador. Override del autor queda registrado con by:"autor" + razón + timestamp ISO. El <verify> de Task 2 rechaza by genéricos. |
| T-12-05b | Tampering | validation.status escrito a mano (bypass del quórum) | accept | El reporter (effectiveStatus + sub-gates) es la verificación fail-closed: un status "validated" sin ≥2 by distintos y sin override del autor sería detectado por VAL-04. Autor único es el oracle final por diseño. |
| T-12-05c | Information Disclosure | API key de DeepSeek en .env | mitigate | .env está git-ignored (precedente Phase 11; .env.example documenta las variables sin valores). validate-ai-pass.mjs lee la clave solo de .env/process.env, no la imprime ni la commitea. Solo se envía el ejercicio + prompt de validación al proveedor (contenido educativo público, sin PII). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). validate-ai-pass.mjs usa solo https/fs nativos; el pase Sonnet usa Task nativo. No aplica audit de legitimidad de paquetes. |

Sin amenazas HIGH: la validación refuerza la integridad editorial; el gate es fail-closed; el quórum cross-vendor (vendors distintos) evita aprobación unilateral y puntos ciegos compartidos; la API key vive solo en .env git-ignored.
</threat_model>

<output>
Create `.planning/phases/12-partitivos/12-05-SUMMARY.md` when done (INCLUIR: qué modo de scope funcionó (slug 'partitivos' o lista CSV de ids — feedback para futuras categorías); que el pool fue cross-vendor deepseek-v4-flash + claude-sonnet-4-6 con tier; número de disputed resueltos y bugs de autoría capturados; resultado del gate reporter 328+N + smoke estricto)
</output>
