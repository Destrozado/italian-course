---
phase: 12-partitivos
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - content/categories.json
  - scripts/run-validation-271.mjs
  - tests/exercise-types.test.js
autonomous: true
requirements: [PART-01]
must_haves:
  truths:
    - "La categoría partitivos existe en content/categories.json con order 9 y la app la surfacea como 9ª fila del home + botón Examen (sin código UI, vía categoriesForDisplay)"
    - "Los TRES números de conteo están en lockstep: partitivos.json length == expected en CATEGORIES de run-validation-271.mjs == expected en CATEGORIES_WITH_EXPLANATIONS de exercise-types.test.js"
    - "El conteo N propagado a los 3 sitios es el número final (~30-40) de la categoría (D-09), determinado por la longitud real de partitivos.json según el temario (D-13) — no un número inventado"
    - "TOTAL_EXPECTED en run-validation-271.mjs subió de 328 a 328+N de forma consistente con la suma de expected (las 9 entries suman TOTAL_EXPECTED)"
    - "La entry de CATEGORIES[] (con clave slug) y la línea de CATEGORIES_WITH_EXPLANATIONS (SIN clave slug) usan las shapes CORRECTAS de cada array — no se mezclan"
  artifacts:
    - path: "content/categories.json"
      provides: "Entry de la categoría partitivos (id/name/order) que auto-surfacea el home row + Examen"
      contains: "partitivos"
    - path: "scripts/run-validation-271.mjs"
      provides: "Entry partitivos en CATEGORIES[] (con slug) + TOTAL_EXPECTED bumpeado a 328+N"
      contains: "slug: 'partitivos'"
    - path: "tests/exercise-types.test.js"
      provides: "Entry partitivos en CATEGORIES_WITH_EXPLANATIONS (sin slug) con su cuenta exacta"
      contains: "content/exercises/partitivos.json"
  key_links:
    - from: "content/categories.json"
      to: "src/screens/app.js (categoriesForDisplay ~2141)"
      via: "añadir entry auto-surfacea fila home + Examen (examenEnabled = totalCount > 0), sin código UI"
      pattern: "id.*partitivos"
    - from: "scripts/run-validation-271.mjs (CATEGORIES + TOTAL_EXPECTED)"
      to: "content/exercises/partitivos.json (length)"
      via: "los 3 números en lockstep; el reporter falla si la suma de expected != TOTAL_EXPECTED o != ejercicios en disco"
      pattern: "TOTAL_EXPECTED"
---

<objective>
Cablear la categoría `partitivos` en los TRES puntos de integración que un alta de categoría requiere (sin tocar engine ni UI), con los tres números de conteo en LOCKSTEP: (1) entry en `content/categories.json` (order 9), (2) entry en `CATEGORIES[]` + bump de `TOTAL_EXPECTED` en `scripts/run-validation-271.mjs`, (3) entry en `CATEGORIES_WITH_EXPLANATIONS` en `tests/exercise-types.test.js`. Es la tarea BLOCKING de integración: los 3 conteos deben coincidir con la longitud real de `content/exercises/partitivos.json`.

Purpose: añadir la entry a categories.json auto-surfacea la 9ª fila del home + botón Examen vía `categoriesForDisplay` (cero código UI) — cubre PART-01. Los conteos en lockstep son condición para que el reporter y el smoke test puedan pasar (el reporter falla si la suma de `expected` != `TOTAL_EXPECTED` o != ejercicios en disco). Calca exactamente el plan 11-04 (que cableó articoli y dejó TOTAL_EXPECTED en 328).

Output: los 3 archivos editados con el conteo final N de partitivos.json (leído del archivo, NO inventado) idéntico en los 3 sitios, y `TOTAL_EXPECTED` = 328 + N. Tras este plan: el content-fixture y el coverage test de partitivos pasan; el reporter dará exit distinto de 0 SOLO por status (los nuevos ejercicios están en `pending` hasta 12-05) — eso es esperado y lo cierra 12-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-partitivos/12-PATTERNS.md

<interfaces>
Shapes EXACTAS de los 3 puntos de integración. Extraídas del repo vivo (confirmadas el 2026-05-28). El ejecutor usa estas directamente, sin exploración. Las DOS arrays de conteo tienen shapes DISTINTAS — NO mezclarlas.

1) content/categories.json — array actual con 8 entries (la última: id "articoli", name "Articoli (artículos)", order 8). Añadir partitivos como 9ª entry, dentro del array, antes del cierre del array, añadiendo una coma tras la línea de articoli. La entry nueva (alineación de columnas como las demás): id "partitivos", name "Partitivos", order 9. (El name "Partitivos" plano es el default lean — el slug ya se lee como español; alternativa analog-consistente "Partitivos (partitivo)" si se prefiere el gloss tipo "Articoli (artículos)". Claude's Discretion D — elegir uno.)

2) scripts/run-validation-271.mjs — array CATEGORIES (líneas 70-79) + TOTAL_EXPECTED (línea 81, actualmente 328). Shape de cada entry: TRES claves slug + file + expected, con `slug`. Orden documentado (comentario línea 63): "riesgo-first (preposiciones) + alfabético resto". Alfabéticamente `partitivos` va entre `genero-numero` y `profesiones` → insertar la entry nueva JUSTO tras la línea de genero-numero (línea 75) y antes de profesiones (línea 76). TOTAL_EXPECTED pasa de 328 a 328+N (escribir el número entero resuelto, p.ej. si N=34 escribir 362). El comentario de cabecera (líneas 63-69) explica el 328: extenderlo para mencionar la 9ª categoría partitivos (328 + N).

3) tests/exercise-types.test.js — array CATEGORIES_WITH_EXPLANATIONS (líneas 1265-1275). Shape de cada línea: DOS claves file + expected, SIN `slug`. Este array NO está ordenado alfabéticamente y NO usa slug — se APPENDEA al final, antes del cierre del array (tras la línea de articoli, línea 1273). Actualizar el comentario de cierre (línea 1274) que dice "...= 328 con explanation curada" a "...+ N partitivos (9ª categoría, v1.2 Phase 12) = 328+N con explanation curada.".

El valor N es IDÉNTICO en los 3 sitios y es la longitud real de partitivos.json (leída del archivo, reportada por 12-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver el conteo N de partitivos.json y añadir la entry a categories.json (order 9)</name>
  <read_first>
    - content/categories.json — el array completo actual (8 entries, la última articoli order 8) para insertar la 9ª conservando el formato alineado por columnas
    - .planning/phases/12-partitivos/12-PATTERNS.md §3 "content/categories.json" (líneas 197-227) — la inserción exacta, el dato de que esto auto-surfacea el home row sin código UI, y la nota de Claude's Discretion sobre name plano vs gloss
    - .planning/phases/12-partitivos/12-03-SUMMARY.md — el conteo final N reportado por el plan 12-03 (cross-check con el archivo real)
  </read_first>
  <action>
    Primero, resolver el conteo REAL N de partitivos.json (NO inventar) leyendo el archivo y contando exercises (node oneliner que parsea el archivo y lee .exercises.length). Cross-checar con el N reportado en 12-03-SUMMARY.md; si difieren, el ARCHIVO manda: usar la longitud real y anotarlo en el SUMMARY de este plan. Registrar N porque las Tasks 2 y 3 lo reutilizan idéntico.

    Editar `content/categories.json`: añadir la coma final a la línea de articoli (order 8) y añadir como ÚLTIMO elemento del array la entry partitivos con id "partitivos", name "Partitivos", order 9 (o name "Partitivos (partitivo)" con gloss si se prefiere el patrón de Articoli — Claude's Discretion D, elegir uno). Respetar el alineamiento de columnas del archivo (valores padded con espacios como las demás entries). NO tocar las otras 8 entries ni sus order.

    NO escribir código UI: la entry auto-surfacea la fila del home + botón Examen vía categoriesForDisplay (src/screens/app.js ~2141, examenEnabled = totalCount > 0). Confirmado en PATTERNS.md §3 y en CONTEXT §Reusable Assets; no requiere edición de app.js.
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')); const p=c.categories.find(x=>x.id==='partitivos'); if(!p){console.error('Falta entry partitivos');process.exit(1)} if(p.order!==9){console.error('order != 9, es '+p.order);process.exit(1)} if(c.categories.length!==9){console.error('Esperaba 9 categorias, hay '+c.categories.length);process.exit(1)} const a=c.categories.find(x=>x.id==='articoli'); if(!a||a.order!==8){console.error('articoli order 8 alterado');process.exit(1)} console.log('OK partitivos order 9, 9 categorias, articoli intacto')" && node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json</automated>
  </verify>
  <acceptance_criteria>
    - El conteo N de partitivos.json queda resuelto leyendo el archivo real y registrado en el SUMMARY.
    - content/categories.json parsea y contiene exactamente 9 categorías.
    - La entry partitivos tiene id "partitivos", order 9, name "Partitivos" (o "Partitivos (partitivo)").
    - Las otras 8 entries quedan intactas (mismos id/order; articoli sigue order 8).
    - `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` sigue exit 0 (la nueva categoría no rompe la validación de contenido).
  </acceptance_criteria>
  <done>La categoría partitivos existe en categories.json con order 9; la app la surfacea como 9ª fila + Examen sin código UI; N resuelto del archivo real.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir partitivos a CATEGORIES[] (con slug, alfabético tras genero-numero) y bumpear TOTAL_EXPECTED a 328+N</name>
  <read_first>
    - scripts/run-validation-271.mjs (líneas 63-81) — el comentario del orden (riesgo-first + alfabético, línea 63), el array CATEGORIES (70-79) y TOTAL_EXPECTED (81, actualmente 328). La suma de los 8 expected actuales es 328 (51+56+23+39+40+51+31+37)
    - .planning/phases/12-partitivos/12-PATTERNS.md §4 "scripts/run-validation-271.mjs" (líneas 230-266) — las 2 ediciones (CATEGORIES + TOTAL_EXPECTED), la regla de lockstep, y la posición alfabética de partitivos (entre genero-numero y profesiones)
  </read_first>
  <action>
    Usar el N resuelto en Task 1 (la longitud real de partitivos.json). Editar scripts/run-validation-271.mjs:

    Edición 1 — array CATEGORIES (líneas 70-79): insertar la entry nueva en orden alfabético del resto, JUSTO tras la línea de genero-numero (línea 75) y antes de profesiones (línea 76). Shape EXACTA con TRES claves (con slug): slug 'partitivos', file 'content/exercises/partitivos.json', expected N. Respetar el alineamiento de columnas del array (padding de espacios como las demás entries).

    Edición 2 — TOTAL_EXPECTED (línea 81): cambiar el literal de 328 a 328+N ya resuelto como número entero (p.ej. si N=34, escribir 362). NO dejar una expresión "328 + N"; escribir el número calculado.

    Edición 3 — comentario de cabecera (líneas 63-69): actualizar la explicación para que mencione que el total ahora incluye los N ejercicios de partitivos (9ª categoría, v1.2 Phase 12), no solo los 328 de v1.0/v1.1/articoli.

    El reporter exige que: suma de expected de CATEGORIES == TOTAL_EXPECTED, y que ambos == ejercicios encontrados en disco. Por eso N debe ser idéntico al de Task 1 y al de Task 3.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('scripts/run-validation-271.mjs','utf8'); const N=JSON.parse(fs.readFileSync('content/exercises/partitivos.json','utf8')).exercises.length; if(!/slug:\s*'partitivos'/.test(src)){console.error('Falta entry slug partitivos en CATEGORIES');process.exit(1)} const te=src.match(/TOTAL_EXPECTED\s*=\s*(\d+)/); if(!te){console.error('No encuentro TOTAL_EXPECTED');process.exit(1)} if(Number(te[1])!==328+N){console.error('TOTAL_EXPECTED='+te[1]+' esperado '+(328+N)+' (328+N, N='+N+')');process.exit(1)} const m=src.match(/expected:\s*(\d+)[^\n]*content\/exercises\/partitivos\.json|partitivos\.json[^\n]*expected:\s*(\d+)/); const exp=m?Number(m[1]||m[2]):null; if(exp!==N){console.error('expected de la entry partitivos='+exp+' != N='+N);process.exit(1)} console.log('OK CATEGORIES tiene partitivos expected='+N+', TOTAL_EXPECTED='+te[1]+' (328+'+N+')')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run-validation-271.mjs contiene una entry con slug 'partitivos' y file 'content/exercises/partitivos.json' en CATEGORIES, insertada alfabéticamente entre genero-numero y profesiones.
    - El expected de esa entry == N (longitud real de partitivos.json).
    - TOTAL_EXPECTED == 328 + N (número entero literal, no expresión).
    - El comentario de cabecera menciona la 9ª categoría partitivos.
    - La suma de expected de las 9 entries == TOTAL_EXPECTED (consistencia interna del reporter).
  </acceptance_criteria>
  <done>El reporter conoce la categoría partitivos con su cuenta exacta (entry con slug, alfabética) y TOTAL_EXPECTED está en lockstep (328+N).</done>
</task>

<task type="auto">
  <name>Task 3: Añadir partitivos a CATEGORIES_WITH_EXPLANATIONS (sin slug) en exercise-types.test.js</name>
  <read_first>
    - tests/exercise-types.test.js (líneas 1255-1320) — el comentario de los 3 invariantes editoriales (coverage exacto, apóstrofes ASCII, plain text sin markdown), el array CATEGORIES_WITH_EXPLANATIONS (1265-1275, SIN clave slug) y cómo se consume en el describe (1277+)
    - .planning/phases/12-partitivos/12-PATTERNS.md §5 "tests/exercise-types.test.js" (líneas 270-305) — la inserción de 1 línea (shape sin slug), la regla de lockstep, y la nota de que este array alimenta 3 invariantes (coverage + apóstrofes + no-markdown)
  </read_first>
  <action>
    Usar el N resuelto en Task 1. Editar tests/exercise-types.test.js: añadir al array CATEGORIES_WITH_EXPLANATIONS (líneas 1265-1275) una línea nueva con shape SIN slug (DOS claves): file 'content/exercises/partitivos.json', expected N, antes del cierre del array (tras la línea de articoli, línea 1273). Respetar el estilo de las líneas existentes. NO añadir clave `slug` (este array no la usa — distinto del array de run-validation-271.mjs). Actualizar el comentario de cierre (línea 1274) que dice "...= 328 con explanation curada." para que mencione "+ N partitivos (9ª categoría, v1.2 Phase 12) = 328+N con explanation curada.".

    Este array se consume por los describe (líneas 1277+ y ~1393) que verifican 3 invariantes por cada categoría: (1) data.exercises.length === expected Y toda explanation no vacía; (2) apóstrofes ASCII U+0027 (sin smart quotes); (3) plain text sin markers markdown. Con N correcto y las explanations curadas de 12-02/12-03, los 3 invariantes pasan para partitivos.

    NO tocar la lógica de los describe ni los otros entries del array.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('tests/exercise-types.test.js','utf8'); const N=JSON.parse(fs.readFileSync('content/exercises/partitivos.json','utf8')).exercises.length; const re=new RegExp(\"content/exercises/partitivos\\\\.json'\\\\s*,\\\\s*expected:\\\\s*(\\\\d+)\"); const m=src.match(re); if(!m){console.error('Falta entry partitivos en CATEGORIES_WITH_EXPLANATIONS o formato inesperado');process.exit(1)} if(Number(m[1])!==N){console.error('expected='+m[1]+' != N='+N);process.exit(1)} console.log('OK CATEGORIES_WITH_EXPLANATIONS partitivos expected='+N)" && node --test tests/exercise-types.test.js 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - tests/exercise-types.test.js contiene una entry con file 'content/exercises/partitivos.json' y expected N en CATEGORIES_WITH_EXPLANATIONS, SIN clave slug.
    - El expected == N (idéntico a Task 1 y Task 2).
    - `node --test tests/exercise-types.test.js` pasa para los invariantes editoriales de partitivos (coverage exacto + apóstrofes ASCII + plain text sin markdown) — los 3 números están en lockstep.
  </acceptance_criteria>
  <done>El smoke test paramétrico cubre partitivos.json con su cuenta exacta (shape sin slug); los 3 números (categories/reporter/test) en lockstep verificado.</done>
</task>

</tasks>

<verification>
- Los TRES números en lockstep: longitud real de partitivos.json == expected en run-validation-271.mjs CATEGORIES == expected en exercise-types.test.js CATEGORIES_WITH_EXPLANATIONS; y TOTAL_EXPECTED == 328 + N.
- content/categories.json tiene 9 categorías; partitivos order 9; la app surfacea la fila + Examen sin código UI.
- `node --test tests/exercise-types.test.js` pasa los invariantes editoriales de partitivos.
- NOTA esperada: `node scripts/run-validation-271.mjs` dará exit distinto de 0 en este punto SOLO porque los ejercicios de partitivos están en `pending` (no validados aún). Eso es correcto: el gate verde lo cierra el plan 12-05 tras la validación por quórum. NO intentar arreglar el status en este plan.
</verification>

<success_criteria>
- PART-01 cubierto: categoría partitivos existe en categories.json y carga como 9ª en la app.
- Los 3 puntos de integración cableados con conteos en lockstep; el smoke test de coverage editorial verde para partitivos.
- El único item pendiente para exit 0 del reporter es el status validated (plan 12-05).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| categories.json ↔ runtime de la app | La entry de categoría se lee al boot y mapea a la UI vía categoriesForDisplay. id es slug ASCII validado por el schema. |
| constantes de conteo ↔ reporter/test | Números de configuración en scripts de build/test, no entran al DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | content/categories.json (id/name renderizado en home) | mitigate | El name se renderiza vía x-text (T-02-01 LOCKED), sin interpretación de HTML/markdown. id es slug ASCII validado por ID_SLUG_RE en schema-validator. Autor único, edición a mano. |
| T-12-04b | Tampering | constantes de conteo en reporter/test | accept | Son números de configuración en scripts Node de validación; no entran al navegador. Un valor erróneo solo hace fallar el gate (fail-closed), no introduce ejecución. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). |

Sin amenazas HIGH: edits de configuración/registro; el único valor renderizado (name de categoría) pasa por x-text.
</threat_model>

<output>
Create `.planning/phases/12-partitivos/12-04-SUMMARY.md` when done (INCLUIR el valor N usado en los 3 sitios)
</output>
