---
phase: 12-partitivos
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - content/exercises/partitivos.json
autonomous: false
requirements: [PART-05, PART-06]
must_haves:
  truths:
    - "Existe al menos un ejercicio match sustantivo↔forma partitiva (pane→del, zucchero→dello, acqua→dell', studenti→degli, mele→delle, libri→dei) que cumple la DESIGN RULE — el pareo depende de género + disparador fonético + número, NO de raíz compartida (D-08)"
    - "Existen ejercicios de CLASIFICACIÓN de función (multiple-choice meta-lingüístico) que ejercitan la distinción partitivo (algo de) vs preposizione articolata (de el) sobre formas di-based que comparten forma del/della, con options [\"partitivo\",\"preposición\"] y grading por índice (PART-05, D-05)"
    - "Los ejercicios de clasificación usan SOLO formas di-based (del/dello/della/dell'/dei/degli/delle) — las únicas que solapan con la preposizione articolata; nel/sul/al/dal/col quedan fuera (D-06)"
    - "Los ejercicios de clasificación mantienen categoryIds:[\"partitivos\"] (sin bridge a Preposiciones, D-06/D-14) y su explanation deja claro que la función prepositiva vive en la categoría Preposiciones, sin re-enseñarla ni duplicar lo que preposiciones.json ya cubre (PART-05, criterio 3 del ROADMAP)"
    - "El bloque match + clasificación deja el archivo en su conteo FINAL N (~30-40, D-09), registrado en el SUMMARY para que el plan 12-04 lo propague en lockstep a los 3 puntos de integración"
    - "Cada ejercicio match y de clasificación tiene explanation pedagógica curada (canon D-15) en status pending para la validación por quórum posterior"
  artifacts:
    - path: "content/exercises/partitivos.json"
      provides: "Bloque match sustantivo↔forma partitiva + bloque de clasificación partitivo/preposición (PART-05) añadidos al archivo de la categoría; conteo final N fijado"
      contains: "\"type\": \"match\""
  key_links:
    - from: "content/exercises/partitivos.json"
      to: "src/data/schema-validator.js"
      via: "match valida contra validateMatchPayload (pairs array 2-10, cada pair 2 strings no vacíos); clasificación contra validateMultipleChoicePayload"
      pattern: "\"type\": \"match\""
    - from: "content/exercises/partitivos.json (clasificación PART-05)"
      to: "content/exercises/preposiciones.json (preposizioni articolate)"
      via: "los ejercicios de clasificación CONTRASTAN la función prepositiva pero NO la duplican ni resetean (categoryIds solo partitivos, D-06)"
      pattern: "\"partitivo\""
---

<objective>
Completar `content/exercises/partitivos.json` con (a) el **bloque `match` sustantivo↔forma partitiva** (D-08) — único tipo donde el pareo NO es derivable por raíz (DESIGN RULE: depende de género + disparador fonético + número), y (b) los **ejercicios de CLASIFICACIÓN de función** (PART-05, D-05/D-06) que obligan al autor a distinguir la función partitiva ("algo de") de la preposizione articolata ("de el") cuando comparten la misma forma `del/della`. Todos con explanation curada. Con esto el archivo queda en su conteo FINAL `<N>` (~30-40), que el plan 12-04 propagará a los 3 puntos de integración.

Purpose: El `match` ejercita el reconocimiento de qué forma partitiva pide cada sustantivo según su género/disparador/número (único tipo que cumple la DESIGN RULE). Los ejercicios de clasificación cubren PART-05 (criterio #3 del ROADMAP): atacan de frente la confusión partitivo/preposición, dejando claro que la función prepositiva vive en Preposiciones — sin duplicarla ni cruzar categorías (sin bridge, D-06/D-14). Calca el bloque match + bridges de Articoli (11-03), pero sustituyendo los bridges por la clasificación PART-05 (Partitivos NO lleva bridges).

Output: el bloque match (1-2 ejercicios `type: "match"`) + los ejercicios de clasificación PART-05 (`type: "multiple-choice"`, options `["partitivo","preposición"]`) añadidos por APPEND a `content/exercises/partitivos.json`, todos `categoryIds: ["partitivos"]` con ids `partitivos-0..` (sin serie -300.., D-14), cada uno con `validation.status: "pending"`. Con esto el archivo queda en su conteo FINAL `<N>` — número que el plan 12-04 propagará en lockstep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-partitivos/12-CONTEXT.md
@.planning/phases/12-partitivos/12-PATTERNS.md
@.planning/phases/12-partitivos/12-TEMARIO-PARTITIVOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir el bloque match sustantivo↔forma partitiva (DESIGN RULE: pareo por género+disparador+número, no por raíz)</name>
  <read_first>
    - .planning/phases/12-partitivos/12-PATTERNS.md §2 "match exercise shape" (líneas 128-157) — la shape exacta (pairs como tuples [sustantivo,forma], duplicados textuales en columna derecha permitidos D-66, regla R3 ≥3 valores distintos, DESIGN RULE) y el ejemplo concreto de pairs para Partitivos
    - content/exercises/articoli.json (articoli-049, el bloque match articolo↔sustantivo) — el modelo a calcar (shape de pairs, notes documentando R3 + D-66 + DESIGN RULE)
    - src/data/schema-validator.js (validateMatchPayload) — constraints: pairs array de longitud 2-10, cada pair exactamente 2 strings no vacíos, explanation opcional no vacía si presente
    - .planning/phases/12-partitivos/12-TEMARIO-PARTITIVOS.md — para elegir sustantivos cuyos disparadores/género/número cubran del/dello/della/dell'/dei/degli/delle
  </read_first>
  <action>
    APPEND a `content/exercises/partitivos.json` 1-2 ejercicios `type: "match"`, `categoryIds: ["partitivos"]` (sin bridges, D-14), continuando la numeración de ids del bloque base (NO en rango 300 — Partitivos no usa rango 300). Cada uno con `payload.prompt` (consigna de empareje en español), `payload.pairs` (array de tuples `[sustantivo, forma partitiva]`, longitud 2-10), y `payload.explanation` curada.

    DISEÑO OBLIGATORIO (D-08): pairs = sustantivo↔forma partitiva. Ejemplo concreto de pairs (PATTERNS.md): `["pane","del"]`, `["zucchero","dello"]`, `["acqua","dell'"]`, `["studenti","degli"]`, `["mele","delle"]`, `["libri","dei"]`. La columna derecha DEBE tener ≥3 valores DISTINTOS (R3 / regla de autoría); las seis formas del ejemplo dan 6 distintas → cumple. Los duplicados textuales en la columna derecha están PERMITIDOS (D-66): varios sustantivos pueden compartir `del`/`degli`/`delle` (p.ej. añadir `["vino","del"]`, `["amici","degli"]`, `["case","delle"]`); el grading consume por índice. Si se hacen 2 bloques match, el segundo puede agrupar otro conjunto de sustantivos cubriendo formas no repetidas del primero.

    La explanation debe enseñar la regla: la forma partitiva de un sustantivo depende de su GÉNERO + DISPARADOR FONÉTICO + NÚMERO (pane masc cons → del; zucchero masc s-impura/z → dello; acqua fem vocal → dell'; studenti masc plural s+cons/vocal → degli; mele fem plural → delle; libri masc plural cons → dei) — exactamente los mismos disparadores que el artículo determinativo (del = di + il). Canon: español acentuado, italianismos preservados, apóstrofes ASCII U+0027, plain text sin markdown, sin refs `#NNN`. `notes` top-level con rationale (referenciar D-08, D-66, R3, DESIGN RULE). `validation: {status:"pending", passes:[]}`.

    Respetar la DESIGN RULE (D-08): usar `match` SOLO porque sustantivo↔forma partitiva NO es derivable por raíz compartida (la forma depende de la fonética/género/número del sustantivo, no de su raíz). NO meter pares que sí sean derivables por raíz.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); const m=d.exercises.filter(e=>e.type==='match'); if(m.length<1){console.error('Falta bloque match');process.exit(1)} for(const e of m){if(!Array.isArray(e.categoryIds)||e.categoryIds.length!==1||e.categoryIds[0]!=='partitivos'){console.error(e.id+': categoryIds debe ser [partitivos] (D-14)');process.exit(1)} const right=e.payload.pairs.map(p=>p[1]); const distinct=new Set(right); if(distinct.size<3){console.error(e.id+': solo '+distinct.size+' formas distintas en columna derecha (R3 pide >=3)');process.exit(1)} const isPartitive=right.every(f=>/^(del|dello|della|dell'|dei|degli|delle)\$/.test(f)); if(!isPartitive){console.error(e.id+': la columna derecha debe ser formas partitivas del/dello/della/dell\\047/dei/degli/delle');process.exit(1)}} console.log('OK match: '+m.length+' bloque(s), R3 cumplida, formas partitivas correctas')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` sale exit 0 tras el append.
    - Existe ≥1 ejercicio `type:"match"` con `pairs` de longitud 2-10, cada pair 2 strings no vacíos, `categoryIds:["partitivos"]`.
    - El/los match tienen ≥3 valores DISTINTOS en la columna derecha (R3) y la columna derecha son formas partitivas (`del/dello/della/dell'/dei/degli/delle`).
    - Los duplicados textuales en columna derecha (varios sustantivos → misma forma) están presentes y son intencionales (D-66).
    - Cada match tiene explanation curada (canon) que enseña la dependencia género+disparador+número, y `validation:{status:"pending",passes:[]}`.
  </acceptance_criteria>
  <done>El bloque match sustantivo↔forma partitiva existe, cumple la DESIGN RULE + R3 + D-66, con explanation curada y sin bridges.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir los ejercicios de CLASIFICACIÓN partitivo vs preposizione articolata (PART-05) y fijar el conteo final N</name>
  <read_first>
    - .planning/phases/12-partitivos/12-CONTEXT.md §D-05 (clasificación de función: dada una frase con del/della, elegir "partitivo (algo de)" vs "preposición (de el)"; modelos "Ho mangiato del pane"→partitivo, "Il sapore del caffè"→preposición) y §D-06 (restricciones LOCKED: solo formas di-based; categoryIds solo partitivos sin bridge; la explanation dice que la función prepositiva vive en Preposiciones) y §specifics (pares mínimos de clasificación)
    - .planning/phases/12-partitivos/12-PATTERNS.md §S4 "PART-05 differentiation from Preposiciones" (líneas 329-331) — lo que preposiciones.json YA enseña (del/della como preposizione articolata) y por qué NO duplicar; clasificar/contrastar con options ["partitivo","preposición"], grading por índice
    - content/exercises/preposiciones.json — el archivo a DIFERENCIAR: ahí del/della aparecen SOLO como preposizioni articolate (función prepositiva). Los ejercicios de clasificación NO deben re-enseñar esa función, solo clasificarla/contrastarla
    - content/exercises/partitivos.json — el archivo con match ya añadido (Task 1); para APPEND y para contar el total final N
  </read_first>
  <action>
    APPEND a `content/exercises/partitivos.json` los ejercicios de CLASIFICACIÓN de función (PART-05), ~5-8 ejercicios, `type: "multiple-choice"`, `categoryIds: ["partitivos"]` (SIN bridge a Preposiciones — D-06/D-14), continuando la numeración de ids.

    DISEÑO OBLIGATORIO (D-05/D-06): cada ejercicio presenta una FRASE COMPLETA con una forma `di`-based (`del/dello/della/dell'/dei/degli/delle`) en `payload.prompt`, y `payload.options` = `["partitivo", "preposición"]` (exactamente esas dos clases; grading por índice → `correctIndex` 0 o 1). El autor clasifica la función:
    - Función PARTITIVA ("algo de"): "Ho mangiato del pane" (del = algo de pan), "Bevo dell'acqua", "Ho comprato delle mele". correctIndex → "partitivo".
    - Función PREPOSITIVA ("de el"): "Il sapore del caffè" (del = del café, complemento del nombre), "La casa del nonno", "Le pagine del libro". correctIndex → "preposición".

    RESTRICCIONES LOCKED (D-06):
    - SOLO formas `di`-based (`del/dello/della/dell'/dei/degli/delle`) — son las únicas que solapan en forma con la preposizione articolata. NUNCA usar `nel/sul/al/dal/col` (esas NUNCA son partitivo y quedan fuera de estos ejercicios).
    - `categoryIds: ["partitivos"]` siempre (NO añadir "preposiciones" como segundo slug — sin bridge; el ejercicio referencia el uso prepositivo como contraste pero NO resetea la categoría Preposiciones, D-06/D-14).
    - La `payload.explanation` DEBE dejar claro que la función prepositiva (de + artículo = preposizione articolata) vive en la categoría Preposiciones, NO aquí (criterio #3 del ROADMAP). NO re-enseñar la preposizione articolata (no duplicar lo que preposiciones.json ya cubre): el ejercicio CLASIFICA/contrasta, distinguiendo "algo de" (partitivo, esta categoría) de "de el" (preposición, otra categoría).

    Para cada ejercicio de clasificación: `notes` top-level con rationale (referenciar D-05/D-06, citar la forma di-based usada y por qué es partitivo o preposición). `validation: {status:"pending", passes:[]}`. Canon de explanation idéntico (español acentuado, italianismos preservados, ASCII U+0027, plain text sin markdown, sin refs `#NNN`).

    Al terminar el APPEND: resolver el conteo TOTAL FINAL N del archivo (node oneliner: `JSON.parse(...).exercises.length`). Verificar que N está en el rango ~30-40 (D-09) y que coincide con la cobertura del temario (cada celda ≥1 ejercicio). REGISTRAR N de forma destacada en el SUMMARY — el plan 12-04 lo consume para propagarlo en lockstep a los 3 puntos de integración.

    PROHIBIDO: NO crear ningún ejercicio con `categoryIds` de 2 slugs (sin bridges, D-14). NO crear serie `partitivos-300..`. NO usar formas `nel/sul/al/dal/col` en la clasificación (solo di-based, D-06). NO duplicar el contenido de preposiciones.json (solo clasificar/contrastar).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); const cls=d.exercises.filter(e=>e.type==='multiple-choice'&&Array.isArray(e.payload.options)&&e.payload.options.length===2&&e.payload.options.includes('partitivo')&&e.payload.options.some(o=>/preposici/.test(o))); if(cls.length<5){console.error('Esperaba >=5 ejercicios de clasificación PART-05, hay '+cls.length);process.exit(1)} const bothClasses=cls.some(e=>e.payload.correctIndex===e.payload.options.indexOf('partitivo'))&&cls.some(e=>e.payload.correctIndex!==e.payload.options.indexOf('partitivo')); if(!bothClasses){console.error('La clasificación debe tener ejemplos de AMBAS clases (partitivo y preposición)');process.exit(1)} const N=d.exercises.length; const allPart=d.exercises.every(e=>Array.isArray(e.categoryIds)&&e.categoryIds.length===1&&e.categoryIds[0]==='partitivos'); if(!allPart){console.error('Algún ejercicio tiene categoryIds != [partitivos] (sin bridges, D-14)');process.exit(1)} const no300=d.exercises.every(e=>!/^partitivos-3\d\d\$/.test(e.id)); if(!no300){console.error('Hay ids en rango 300 (prohibido, D-14)');process.exit(1)} if(N<28||N>44){console.error('Conteo final N='+N+' fuera del rango ~30-40 (D-09) — revisar cobertura del temario');process.exit(1)} console.log('OK clasificación PART-05: '+cls.length+' ejercicios, ambas clases; conteo FINAL N='+N+' (registrar en SUMMARY para 12-04)')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` sale exit 0.
    - Existen ≥5 ejercicios de clasificación `type:"multiple-choice"` con `options` = exactamente `["partitivo","preposición"]` (o equivalente con "preposición" acentuado) y grading por índice.
    - Hay ejemplos de AMBAS clases: ≥1 frase clasificada como partitivo ("algo de") y ≥1 como preposición ("de el").
    - Todas las frases de clasificación usan formas `di`-based (`del/dello/della/dell'/dei/degli/delle`); NINGUNA usa `nel/sul/al/dal/col` (D-06).
    - Cada ejercicio de clasificación tiene `categoryIds:["partitivos"]` (sin bridge, D-06/D-14) y su explanation menciona que la función prepositiva vive en la categoría Preposiciones.
    - El conteo TOTAL FINAL N del archivo está en ~30-40 (D-09), todos los ejercicios `categoryIds:["partitivos"]`, ningún id en rango 300, y N queda registrado en el SUMMARY para el plan 12-04.
  </acceptance_criteria>
  <done>Los ejercicios de clasificación partitivo/preposición (PART-05) usan solo formas di-based, options ["partitivo","preposición"], sin bridge, con explanation que remite la función prepositiva a Preposiciones; el conteo final N (~30-40) queda fijado y registrado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — el autor revisa match + clasificación PART-05 y confirma el conteo final N</name>
  <what-built>
    El bloque `match` sustantivo↔forma partitiva (pareo por género+disparador+número, cumple DESIGN RULE) y los ejercicios de CLASIFICACIÓN partitivo vs preposizione articolata (PART-05): frases con del/della donde el autor clasifica "partitivo (algo de)" vs "preposición (de el)", con la explanation remitiendo la función prepositiva a la categoría Preposiciones. Cada uno con explanation curada. Con esto `content/exercises/partitivos.json` queda en su conteo FINAL.
  </what-built>
  <how-to-verify>
    1. Revisa el/los match: ¿agrupan bien los sustantivos con su forma partitiva (pane→del, zucchero→dello, acqua→dell', studenti→degli, mele→delle, libri→dei)? ¿la explanation enseña que la forma depende del género+disparador+número (los mismos disparadores que el artículo, del=di+il)?
    2. Revisa los ejercicios de clasificación PART-05 uno a uno: ¿cada frase deja clara su función? Partitivo ("Ho mangiato del pane" = algo de pan) vs preposición ("Il sapore del caffè" = del café). ¿Te obligan de verdad a pensar la función, no solo la forma?
    3. Confirma que TODAS las frases de clasificación usan formas di-based (del/dello/della/dell'/dei/degli/delle) y NINGUNA usa nel/sul/al/dal/col (esas nunca son partitivo).
    4. Confirma que la explanation de cada clasificación deja claro que la función prepositiva ("de el") vive en la categoría Preposiciones, y que aquí solo se clasifica/contrasta — sin re-enseñar la preposición ni duplicar lo que ya practicas en Preposiciones.
    5. Confirma que NINGÚN ejercicio cruza categorías (todos categoryIds solo "partitivos" — Partitivos no lleva bridges en v1.2).
    6. Revisa las explanations de match y clasificación con el canon (español acentuado, italianismos, apóstrofes ASCII, plain text, sin refs #NNN).
    7. Verifica el conteo total del archivo (te lo dará el ejecutor) — debe estar en ~30-40 y será el número N que se propague a los 3 puntos de integración en el plan 12-04.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" y CONFIRMA el conteo total final N del archivo (lo necesita el plan 12-04 para los 3 puntos de integración), o indica qué match/clasificación reformular (p.ej. más ejemplos de preposición, otras frases mínimas, otros sustantivos en el match).</resume-signal>
</task>

</tasks>

<verification>
- `content/exercises/partitivos.json` valida (exit 0 en `validate-content-fixture.mjs`) con el bloque match + clasificación PART-05 añadidos.
- PART-05 cubierto: ejercicios de clasificación partitivo/preposición con options ["partitivo","preposición"], solo formas di-based, sin bridge, explanation remitiendo la función prepositiva a Preposiciones (criterio #3 del ROADMAP).
- Cobertura match (D-08) + clasificación + explanations curadas (PART-06) en match y clasificación.
- El conteo final `<N>` del archivo (~30-40) queda fijado y registrado en el SUMMARY.
</verification>

<success_criteria>
- PART-05 (clasificación partitivo/preposición) + PART-06 (explanations match/clasificación) cubiertos; el match (D-08) cumple la DESIGN RULE.
- `content/exercises/partitivos.json` en su forma de contenido FINAL (solo falta flip de validation status en 12-05); conteo `<N>` confirmado por el autor.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido ↔ DOM de la app | Prompts/explanations de match y clasificación se renderizan al usuario. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering / Injection | `payload.explanation`/`prompt` de match y clasificación renderizados | mitigate | Igual que T-12-02: render vía `x-text` exclusivo (T-02-01 LOCKED), tokens markdown/HTML mostrados literales. Test plain-text + ASCII-apostrophe en exercise-types.test.js refuerza. Autor único, contenido a mano. Las options ["partitivo","preposición"] son texto plano. |
| T-12-03b | Tampering | `categoryIds` de los ejercicios de clasificación (NO debe cruzar a preposiciones) | accept | Decisión LOCKED D-06/D-14: categoryIds = solo ["partitivos"], sin bridge. Un categoryIds con 2 slugs activaría la cascada D-54 sobre Preposiciones erróneamente — el <verify> de Task 2 y el checkpoint lo previenen. El schema valida que categoryIds son slugs ASCII. Sin código nuevo en esta fase. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). |

Sin amenazas HIGH: contenido estático renderizado por `x-text`; sin bridges (categoryIds de 1 solo slug) → no se toca la cascada D-54.
</threat_model>

<output>
Create `.planning/phases/12-partitivos/12-03-SUMMARY.md` when done (INCLUIR el conteo total final `<N>` de partitivos.json de forma destacada — lo consume el plan 12-04 para los 3 puntos de integración en lockstep)
</output>
