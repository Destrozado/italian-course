---
phase: 12-partitivos
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - content/exercises/partitivos.json
autonomous: false
requirements: [PART-03, PART-04, PART-06]
must_haves:
  truths:
    - "Existe content/exercises/partitivos.json con un bloque de ejercicios multiple-choice (columna vertebral, grading por índice — D-07) que cubren cada forma del partitivo determinativo en su disparador fonético: incontable singular del/dello/della/dell' y contable plural dei/degli/delle (PART-03, D-04), heredando los disparadores de Articoli (del = di + il)"
    - "Existen ejercicios que cubren las alternativas partitivas tratadas como TRAMPA POR RESTRICCIÓN GRAMATICAL (D-03): qualche + SIEMPRE singular, un po' di + SOLO incontable, alcuni/alcune + SOLO plural — cada uno con exactamente UNA opción correcta inequívoca (PART-04)"
    - "Existe un mini-bloque de omisión que contrasta afirmativa (usar el partitivo: Compro del pane) vs negativa (omitirlo: Non compro pane) con la opción ∅ / sin partitivo (D-02), y la explanation insiste en que lo difícil para el hispanohablante es USAR el partitivo en afirmativa"
    - "El uso de word-buttons es mínimo o nulo en este bloque — montar la frase completa solaparía con la concordancia de género-número, así que la columna vertebral es multiple-choice (D-07)"
    - "Todos los ejercicios usan categoryIds:[\"partitivos\"] con ids partitivos-001.. (sin serie -300.. de bridges, D-14), cada uno con explanation pedagógica curada (canon D-15) y validation.status pending para la validación por quórum posterior (D-15)"
  artifacts:
    - path: "content/exercises/partitivos.json"
      provides: "Archivo de ejercicios de la categoría partitivos con la base multiple-choice (del-formas incontable/contable + alternativas-por-restricción + mini-bloque omisión) y sus explanations"
      contains: "\"id\": \"partitivos-001\""
  key_links:
    - from: "content/exercises/partitivos.json"
      to: "src/data/schema-validator.js"
      via: "cada ejercicio valida contra validateMultipleChoicePayload (prompt con ___, options 3-4 strings, correctIndex en rango)"
      pattern: "\"type\": \"multiple-choice\""
    - from: "content/exercises/partitivos.json"
      to: ".planning/phases/12-partitivos/12-TEMARIO-PARTITIVOS.md"
      via: "cada celda del temario (incontable/contable/alternativa/omisión) tiene ≥1 ejercicio en este archivo"
      pattern: "partitivos-0"
---

<objective>
Crear `content/exercises/partitivos.json` y autorizar el **bloque base de ejercicios multiple-choice** que cubre: (a) las formas del partitivo determinativo por disparador fonético en sus dos caras semánticas — incontable singular (`del/dello/della/dell'`) y contable plural (`dei/degli/delle`) (D-01/D-04, PART-03); (b) las alternativas (`qualche`/`un po' di`/`alcuni/alcune`) tratadas como TRAMPA POR RESTRICCIÓN GRAMATICAL (D-03, PART-04); y (c) un mini-bloque de omisión afirmativa↔negativa con opción `∅ / sin partitivo` (D-02). Cada ejercicio lleva una **explanation pedagógica curada** (canon editorial heredado D-15).

Purpose: Es la columna vertebral de la categoría (D-07): multiple-choice rápido, grading por índice, la dificultad real en el disparador fonético + la distinción incontable/contable + la restricción de las alternativas. Cubre PART-03 (formas), PART-04 (alternativas) y PART-06 (explanations) para el grueso de los ejercicios. Calca el bloque base de Articoli (11-02) pero reorganizado sobre el eje incontable/contable y SIN serie de bridges.

Output: `content/exercises/partitivos.json` con el envelope `{ "exercises": [...] }` y ~24-32 ejercicios `partitivos-001..` de tipo `multiple-choice`, cada uno con `validation.status: "pending"` y `passes: []` (la validación por quórum la hace el plan 12-05). El bloque `match` y la clasificación PART-05 los añade el plan 12-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-partitivos/12-CONTEXT.md
@.planning/phases/12-partitivos/12-PATTERNS.md
@.planning/phases/12-partitivos/12-TEMARIO-PARTITIVOS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear partitivos.json y autorizar el bloque de del-formas multiple-choice (incontable del/dello/della/dell' + contable dei/degli/delle)</name>
  <read_first>
    - .planning/phases/12-partitivos/12-TEMARIO-PARTITIVOS.md — la fuente de verdad de qué celdas incontable/contable cubrir (cada celda → ≥1 ejercicio); APROBADO por el autor en 12-01
    - .planning/phases/12-partitivos/12-PATTERNS.md §2 "content/exercises/partitivos.json" (líneas 75-194) — la shape EXACTA del ejercicio multiple-choice (id/type/categoryIds/payload con prompt+options+correctIndex+explanation/notes/validation), el canon de explanation (líneas 178-194), y el DIFF clave: ids partitivos-001.. SIN serie -300.. (D-14)
    - content/exercises/articoli.json (líneas 1-31, ejercicio articoli-001) — ejemplo real de la shape multiple-choice con bloque validation; el partitivo HEREDA los mismos disparadores (del=di+il)
    - src/data/schema-validator.js (validateMultipleChoicePayload) — constraints enforced: prompt debe contener "___", options array de 3 o 4 strings no vacíos, correctIndex entero en rango [0, options.length)
  </read_first>
  <action>
    Crear el archivo nuevo `content/exercises/partitivos.json` con el envelope raíz `{ "exercises": [ ... ] }` (una sola clave top-level `exercises` con un array; sin metadata de categoría — eso vive en categories.json).

    Autorizar el bloque de del-formas (partitivo determinativo), ids consecutivos empezando en `partitivos-001`. Todos `type: "multiple-choice"`, `categoryIds: ["partitivos"]` (NUNCA un segundo slug — sin bridges, D-14). Cobertura obligatoria (≥1 ejercicio por celda del temario, varios contextos léxicos en los disparadores frecuentes — densidad D-09):

    INCONTABLE singular (D-01, "algo de"):
    - `del` ante consonante simple, sustantivos incontables masc (del pane, del vino, del latte, del formaggio).
    - `dello` ante s+consonante / z / gn / ps / x, incontables masc (dello zucchero, dello zenzero). Disparador heredado de Articoli (di+lo).
    - `della` ante consonante, incontables fem (della carne, della frutta, della pasta).
    - `dell'` ante vocal / elisión, incontables (dell'acqua, dell'olio, dell'aria). Trampa de elisión obligatoria del temario.

    CONTABLE plural (D-01, "unos/algunos"):
    - `dei` ante consonante simple, plurales masc (dei libri, dei ragazzi, dei panini).
    - `degli` ante s+consonante / z / vocal, plurales masc (degli studenti, degli amici, degli zii, degli gnocchi). Trampas obligatorias del temario: `degli studenti` (s+cons), `degli amici` (vocal).
    - `delle` plural fem, siempre (delle mele, delle case, delle amiche, delle ragazze).

    Crear al menos UN ejercicio de contraste que explote el EJE incontable/contable (D-01, specifics): mismo verbo, sustantivo incontable vs contable → "Ho comprato ___ pane" (del) vs "Ho comprato ___ mele" (delle). La explanation debe enseñar que la elección incontable/contable determina singular (del-formas) vs plural (dei/degli/delle).

    Para cada ejercicio: `payload.prompt` lleva el sustantivo disparador y el hueco `___` (el hueco literal "___" es OBLIGATORIO o el validador falla). `payload.options` = 3 o 4 formas del partitivo plausibles (la correcta + distractoras = las OTRAS formas plausibles ante ese disparador/número, p.ej. `["del","dello","della","dell'"]` para incontable masc/fem; `["dei","degli","delle","del"]` para contable plural — Claude's Discretion en CONTEXT). La dificultad real es el disparador fonético + la distinción contable/incontable (D-Discretion). `payload.correctIndex` selecciona la forma correcta (grading POR ÍNDICE). Apóstrofes en options/prompt ASCII U+0027 (`dell'`).

    Campo `notes` (hermano top-level de payload, NO renderizado): rationale breve referenciando la celda del temario y la decisión aplicada (ej. "Incontable masc + s+consonante → dello (di+lo, disparador heredado de Articoli). Distractoras del/della/dell' = errores ante otros disparadores.").

    Campo `validation`: escribir `{ "status": "pending", "passes": [] }` en CADA ejercicio. NO inventar pases — la validación por quórum la ejecuta el plan 12-05 (interactivo).

    Estrategia de distractoras (Claude's discretion, CONTEXT): las otras formas del partitivo plausibles ante el mismo disparador/número, priorizando el error típico del hispanohablante (omitir el partitivo, o confundir singular/plural). Cada ejercicio debe tener UNA sola opción correcta inequívoca (regla C2 del canon de validación).

    Explanation curada (canon D-15, idéntico a Articoli): español acentuado (á/é/í/ó/ú + ñ RAE), italianismos preservados con ortografía italiana, apóstrofes ASCII U+0027, plain text SIN markdown (sin `**`/`__`/`##`/backtick), SIN referencias `#NNN` a otros ejercicios (regla de memoria R2). Explotar la derivación `del = di + il` como ancla pedagógica donde aplique.

    NO escribir el bloque `match` ni la clasificación PART-05 en este plan (van en 12-03). NO crear serie de bridges `partitivos-300..` (D-14). NO tocar categories.json, run-validation-271.mjs ni tests/exercise-types.test.js (van en 12-04).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); const mc=d.exercises.filter(e=>e.type==='multiple-choice'); if(mc.length<14){console.error('Esperaba >=14 mc de del-formas, hay '+mc.length);process.exit(1)} const bad=d.exercises.filter(e=>!Array.isArray(e.categoryIds)||e.categoryIds.length!==1||e.categoryIds[0]!=='partitivos'); if(bad.length){console.error('categoryIds mal (debe ser solo [\"partitivos\"], D-14): '+bad.map(e=>e.id).join(','));process.exit(1)} const noExpl=d.exercises.filter(e=>typeof e.payload.explanation!=='string'||!e.payload.explanation.trim()); if(noExpl.length){console.error('Sin explanation: '+noExpl.map(e=>e.id).join(','));process.exit(1)} const txt=JSON.stringify(d); const forms=['del','dello','della',\"dell'\",'dei','degli','delle']; const missing=forms.filter(f=>!new RegExp('\"'+f.replace(/[.*+?^\${}()|[\\]\\\\]/g,'\\\\$&')+'\"').test(txt)); if(missing.length){console.error('Faltan formas en options: '+missing.join(','));process.exit(1)} console.log('OK '+d.exercises.length+' ejercicios, 7 del-formas presentes, todos categoryIds=[partitivos]')"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/partitivos.json` parsea como JSON y tiene la clave top-level `exercises` (array).
    - `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` sale exit 0 (todos los multiple-choice cumplen: prompt con `___`, options 3-4 strings, correctIndex en rango).
    - Existen ≥14 ejercicios `multiple-choice` que cubren las 7 del-formas (`del/dello/della/dell'/dei/degli/delle`) con sus disparadores; cada forma del temario tiene ≥1 ejercicio.
    - Las trampas de elisión/disparador del temario (`dell'` ante vocal, `dello` ante s+cons/z, `degli` ante vocal/s+cons) aparecen cada una en ≥1 prompt.
    - Existe ≥1 ejercicio de contraste incontable/contable (mismo verbo, sustantivo incontable→del vs contable→delle/dei/degli).
    - TODOS los ejercicios tienen `categoryIds: ["partitivos"]` (exactamente 1 slug — sin bridges, D-14) y `validation: {status:"pending", passes:[]}`.
    - Cero comillas tipográficas en el archivo (apóstrofes ASCII U+0027).
  </acceptance_criteria>
  <done>El bloque de del-formas multiple-choice cubre las 7 formas del partitivo en sus disparadores y en el eje incontable/contable, con explanations curadas, ids partitivos-001.. sin bridges, validando contra el schema.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Añadir el bloque de ALTERNATIVAS (qualche/un po' di/alcuni-alcune) por restricción gramatical + el mini-bloque de OMISIÓN (∅)</name>
  <read_first>
    - .planning/phases/12-partitivos/12-TEMARIO-PARTITIVOS.md §Alternativas y §Omisión — las celdas a cubrir (qualche+singular, un po' di+incontable, alcuni/alcune+plural; afirmativa vs negativa con ∅)
    - .planning/phases/12-partitivos/12-CONTEXT.md §D-03 (alternativas como restricción gramatical, NO sinonimia — la justificación de doble-validez) y §D-02 (omisión, el matiz pedagógico clave: lo difícil es USAR el partitivo en afirmativa) y §specifics (opción `∅ / sin partitivo`)
    - .planning/phases/12-partitivos/12-PATTERNS.md §S3 "Grading" (líneas 324-327) — el "Doble-validez guard" (D-03): multiple-choice por índice exige UNA sola correcta; forzar la restricción gramatical (qualche+sing, un po' di+incontable, alcuni+plural) es lo que impide que dei libri ≈ alcuni libri ≈ qualche libro den >1 correcta
    - content/exercises/partitivos.json — el archivo ya creado en Task 1 (para APPEND, conservando el envelope y los ids ya usados)
  </read_first>
  <action>
    APPEND al array `exercises` de `content/exercises/partitivos.json` (sin reescribir lo de Task 1) dos sub-bloques, continuando la numeración consecutiva de ids (si Task 1 terminó en partitivos-0XX, seguir en partitivos-0(XX+1)). Todos `type: "multiple-choice"`, `categoryIds: ["partitivos"]`.

    SUB-BLOQUE A — Alternativas como TRAMPA POR RESTRICCIÓN GRAMATICAL (D-03, PART-04), ~6-9 ejercicios. CADA ejercicio testea LA REGLA de una alternativa de forma que haya exactamente UNA opción correcta inequívoca (esto es lo que blinda la doble-validez — D-03):
    - `qualche` + SIEMPRE singular: prompt con sustantivo en SINGULAR donde qualche es válido y el plural NO ("Ho letto qualche ___" con options que fuerzan singular, o "___ libro" con options `["qualche","alcuni","dei","un po' di"]` → correcta `qualche` porque el sustantivo está en singular contable; `alcuni`/`dei` exigirían plural y `un po' di` exige incontable). Crear también al menos uno que ataque el error "qualche libri" como distractora invalidada por la regla "qualche SIEMPRE singular".
    - `un po' di` + SOLO incontable: prompt con sustantivo INCONTABLE ("Vorrei ___ acqua" / "___ pane") con options `["un po' di","alcuni","dei","qualche"]` → correcta `un po' di` (o `del`/`dell'`) porque el sustantivo es incontable; `alcuni`/`dei` exigen plural contable. La restricción "un po' di NO va con contables (no un po' di libri)" es la trampa.
    - `alcuni/alcune` + SOLO plural: prompt con sustantivo en PLURAL contable ("Ho comprato ___ mele" / "___ libri") con options que incluyan `alcune`/`alcuni` como correcta y singular/incontable como distractoras invalidadas.

    Para CADA ejercicio del sub-bloque A, la explanation debe enseñar LA RESTRICCIÓN GRAMATICAL explícitamente (por qué qualche pide singular, por qué un po' di pide incontable, por qué alcuni/alcune piden plural), NO presentar las alternativas como meros sinónimos. Es el corazón pedagógico de PART-04.

    SUB-BLOQUE B — Mini-bloque de OMISIÓN afirmativa↔negativa (D-02), ~3-5 ejercicios. Contrastar USAR el partitivo en afirmativa vs OMITIRLO en negativa:
    - Afirmativa que SÍ usa partitivo: "Compro ___ pane" → correcta `del` (NO ∅). Distractora `∅ / sin partitivo` es el error del hispanohablante (que en español no pondría artículo).
    - Negativa que OMITE el partitivo: "Non compro ___ pane" → correcta `∅ / sin partitivo` (en italiano la negativa omite el partitivo: "Non compro pane", nunca "del pane"). La opción `∅ / sin partitivo` es una string literal de options (D-specifics).
    - Incluir la opción `"∅ / sin partitivo"` como una de las options (string literal) en estos ejercicios; en afirmativa es distractora, en negativa es la correcta.
    - La explanation debe insistir en el MATIZ PEDAGÓGICO CLAVE (D-02): para un hispanohablante omitir en negativa es intuitivo (= "no compro pan"); lo realmente ajeno y difícil es USAR el partitivo en afirmativa donde el español no pone nada. Enfocar la explanation ahí.

    Cada ejercicio (ambos sub-bloques): `validation: {status:"pending", passes:[]}`, `categoryIds: ["partitivos"]` (sin bridges, D-14), `notes` con rationale + celda del temario + D-ref, explanation curada (canon: español acentuado, italianismos preservados, apóstrofes ASCII U+0027, plain text SIN markdown, SIN referencias `#NNN`). word-buttons mínimo/nulo (D-07): NO montar la frase completa; el partitivo es el foco vía multiple-choice.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); const ex=d.exercises; const txt=JSON.stringify(d); const hasQualche=ex.some(e=>e.payload.options.includes('qualche')); const hasPoDi=ex.some(e=>e.payload.options.some(o=>/un po' di/.test(o))); const hasAlcuni=ex.some(e=>e.payload.options.some(o=>/alcun[ie]/.test(o))); const hasOmission=ex.some(e=>e.payload.options.some(o=>/∅|sin partitivo/.test(o))); if(!hasQualche){console.error('Falta alternativa qualche en options');process.exit(1)} if(!hasPoDi){console.error(\"Falta alternativa un po' di en options\");process.exit(1)} if(!hasAlcuni){console.error('Falta alternativa alcuni/alcune en options');process.exit(1)} if(!hasOmission){console.error('Falta opción ∅ / sin partitivo (mini-bloque omisión D-02)');process.exit(1)} const pend=ex.filter(e=>e.validation&&e.validation.status==='pending'&&Array.isArray(e.validation.passes)&&e.validation.passes.length===0); if(pend.length!==ex.length){console.error('No todos pending/passes[]: '+(ex.length-pend.length));process.exit(1)} const br=ex.filter(e=>!Array.isArray(e.categoryIds)||e.categoryIds.length!==1||e.categoryIds[0]!=='partitivos'); if(br.length){console.error('categoryIds mal (sin bridges, D-14): '+br.map(e=>e.id).join(','));process.exit(1)} console.log('OK alternativas+omisión, '+ex.length+' totales, todos pending y categoryIds=[partitivos]')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` sigue exit 0 tras el append.
    - Existen ejercicios para las 3 alternativas con su restricción gramatical: `qualche` (+singular), `un po' di` (+incontable), `alcuni/alcune` (+plural) — cada una presente en options y con explanation que enseña LA RESTRICCIÓN (no sinonimia).
    - Existe el mini-bloque de omisión con la opción literal `"∅ / sin partitivo"` en options, con ≥1 afirmativa (correcta = forma del partitivo) y ≥1 negativa (correcta = ∅ / sin partitivo).
    - La explanation de la omisión insiste en que lo difícil es USAR el partitivo en afirmativa (matiz D-02).
    - TODOS los ejercicios del archivo (del-formas + alternativas + omisión) tienen `categoryIds: ["partitivos"]` y `validation:{status:"pending",passes:[]}`.
    - Cada explanation es plain text sin tokens markdown y sin referencias `#NNN`.
  </acceptance_criteria>
  <done>El bloque de alternativas por restricción gramatical (qualche/un po' di/alcuni-alcune) y el mini-bloque de omisión (∅) están añadidos con explanations curadas que enseñan la restricción y el matiz afirmativa/negativa, todos pending, sin bridges, validando contra el schema.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — el autor revisa por bloques las explanations del bloque base (del-formas + alternativas + omisión)</name>
  <what-built>
    El bloque base multiple-choice de Partitivos (~24-32 ejercicios: del-formas incontable/contable + alternativas por restricción gramatical + mini-bloque de omisión) en `content/exercises/partitivos.json`, cada uno con prompt+opciones+respuesta correcta y una explanation pedagógica curada (canon: español acentuado, italianismos, apóstrofes ASCII, plain text). Todos en `validation.status: "pending"` (la validación por quórum vendrá en 12-05).
  </what-built>
  <how-to-verify>
    1. Revisa las explanations por bloques (revisión humana por lote, patrón Phase 11): abre `content/exercises/partitivos.json` y lee primero las del-formas (incontable del/dello/della/dell', luego contable dei/degli/delle), después las alternativas, después la omisión.
    2. Para cada explanation comprueba el canon: ¿español con acentos correctos (á/é/í/ó/ú/ñ)? ¿términos italianos preservados (`dello`, `degli`, `un po' di`)? ¿apóstrofes ASCII (no tipográficos)? ¿plain text sin markdown? ¿sin referencias a "#NNN" de otros ejercicios?
    3. del-formas: ¿la explanation enseña la regla (disparador fonético heredado de Articoli, del=di+il) Y la distinción incontable/contable? ¿Te ayuda a no confundir singular (del-formas) con plural (dei/degli/delle)?
    4. Alternativas: ¿cada explanation enseña LA RESTRICCIÓN gramatical (qualche siempre singular, un po' di solo incontable, alcuni/alcune solo plural) en vez de tratarlas como sinónimos? ¿Te convence que en cada ejercicio hay UNA sola opción correcta por la restricción?
    5. Omisión: ¿queda claro el contraste afirmativa (usar del) vs negativa (omitir, ∅)? ¿La explanation insiste en que lo difícil para ti es USAR el partitivo en afirmativa, no omitirlo en negativa?
    6. Comprueba que la respuesta correcta (correctIndex) es inequívoca en cada ejercicio y que las distractoras son errores plausibles del hispanohablante (omitir el partitivo, confundir singular/plural, usar la alternativa con el número/contabilidad equivocados).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para fijar el bloque base, o indica qué explanations reescribir, qué ejercicios reformular, o qué celdas del temario (del-formas / alternativas / omisión) aún no están cubiertas.</resume-signal>
</task>

</tasks>

<verification>
- `content/exercises/partitivos.json` existe, parsea, y `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` sale exit 0.
- Cobertura PART-03 (del-formas incontable/contable) + PART-04 (alternativas por restricción) + el mini-bloque de omisión (D-02) en el bloque multiple-choice base; cada celda del temario con ≥1 ejercicio.
- Cada ejercicio tiene explanation curada (PART-06), `categoryIds:["partitivos"]` (sin bridges D-14) y `validation.status: "pending"`.
- El commit de partitivos.json es POSTERIOR en git al commit del temario (12-01).
</verification>

<success_criteria>
- El grueso de PART-03/04 cubierto con multiple-choice; explanations curadas (PART-06) aprobadas por el autor.
- Archivo listo para que 12-03 añada el bloque match + clasificación PART-05, y 12-05 valide por quórum.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido ↔ DOM de la app | Las explanations y prompts se renderizan al usuario. La superficie es content-injection/XSS si el contenido se interpretara como HTML/markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering / Injection | `payload.explanation` y `payload.prompt` renderizados al usuario | mitigate | Mitigado project-wide por la decisión LOCKED T-02-01: el contenido se renderiza vía `x-text` exclusivo (nunca x-html), así que tokens markdown/HTML (incl. el carácter ∅ U+2205 y el apóstrofo ASCII) se muestran literales, no se interpretan. El test `tests/exercise-types.test.js` (plain-text + ASCII-apostrophe assertions) refuerza que las explanations no contienen `**`/`__`/`##`/backtick ni smart-quotes. Autor único, contenido editado a mano → sin entrada no confiable. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). No aplica audit de legitimidad. |

Sin amenazas HIGH: contenido educativo estático, renderizado por `x-text` (anti-XSS por construcción), un solo autor de confianza. La opción `∅ / sin partitivo` es texto plano que x-text muestra literal.
</threat_model>

<output>
Create `.planning/phases/12-partitivos/12-02-SUMMARY.md` when done (anota el conteo parcial de ejercicios tras este plan — lo consume 12-03 para llegar al conteo final N)
</output>
