---
phase: 33-pantallas-de-ejercicio
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - index.html
  - app.css
autonomous: true
requirements: [EX-03]
must_haves:
  truths:
    - "Tapping a multiple-choice option grades INSTANTLY (1-step) — no Comprobar button, no pre-check green-selection state, keyboard 1-4 still grades instantly"
    - "After grading: the correct option shows green-tint fill + green 2px border + green-on-tint text + ✓"
    - "After grading: the chosen-incorrect option shows red-tint fill + red 2px border + red-text + ✗"
    - "After grading: all other options dim to opacity 0.5"
    - "A feedback box appears (green-tint on correct / red-tint on wrong) with serif Italian title ¡Esatto! / Quasi… and the existing pedagogical explanation"
    - "Advance is manual: the bottom CTA reads Continuar → and calls sessionAdvance"
    - "The ¿Por qué? reveal affordance on correct answers is preserved"
  artifacts:
    - path: "index.html"
      provides: "Repainted multiple-choice sub-template (instant-grade button row + feedback box + Continuar CTA)"
      contains: "sessionSelectOption(perm)"
    - path: "app.css"
      provides: "Editoriale repaint of .correcta/.incorrecta + option-row + ✓/✗ marks + opacity-dim rule in the Phase 33 session section"
      contains: "button.correcta"
  key_links:
    - from: "index.html option button"
      to: "sessionSelectOption / grade / applyResultToSession"
      via: "@click instant-grade (unchanged)"
      pattern: "@click=\"sessionSelectOption\\(perm\\)\""
    - from: "app.css button.correcta repaint"
      to: "--ed-green-tint tokens"
      via: "tint fill + border + glyph"
      pattern: "--ed-green-tint"
---

<objective>
Repaint the multiple-choice sub-template into the Editoriale comprobado states (EX-03) on top of the intact 1-step grading flow.

Purpose: EX-03 visual states (green-tint ✓ / red-tint ✗ / rest dim + titled feedback box + Continuar CTA) recreate the handoff's *comprobado* look. CONSCIOUS DEVIATION (D-01): the flow stays 1-STEP — tap grades instantly. We do NOT add the handoff 2-step "Comprobar" CTA nor a pre-check `green-selection` state nor a pre-grading gap fill. A plan that "fixes" EX-03 back to the 2-step model is WRONG.
Output: Repainted MC sub-template (index.html ~417-470) + MC CSS (`.correcta`/`.incorrecta` repaint, option row, marks, dim) appended to the Phase 33 section in app.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md
@.planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md
@.planning/phases/33-pantallas-de-ejercicio/33-01-SUMMARY.md

<interfaces>
<!-- Reuse from 33-01 (foundation) and existing engine state — DO NOT re-derive. -->
From 33-01: shared classes `.session-feedback`(`.correct`/`.wrong`), `.session-feedback-title`, `.session-cta`, gap-slot classes, tint tokens `--ed-green-tint`/`--ed-green-tint-border`/`--ed-green-on-tint`/`--ed-red-tint`/`--ed-red-tint-border`/`--ed-red-text`.
Existing bindings to PRESERVE verbatim (index.html 417-470):
  - `<template x-for="(perm, idx) in multiChoiceOrder" :key="perm">` — shuffle indirection (D-181), keep.
  - `@click="sessionSelectOption(perm)"` — 1-step instant grade (D-01), UNCHANGED.
  - `:disabled="sessionFeedback !== null"`.
  - `:class` toggles `'correcta': sessionFeedback !== null && perm === ...correctIndex` and `'incorrecta': sessionFeedback === 'incorrect' && perm === sessionSelectedIndex`.
  - `x-text="sessionCurrentExercise.payload.options[perm]"`.
  - `.session-explanation` <p> (460-462), `.session-why` ¿Por qué? button (464-467, `revealSessionExplanation()`), `sessionExplanationRevealed` logic — preserve.
  - `<button x-show="sessionFeedback !== null" @click="sessionAdvance">` (468) — becomes the Continuar CTA (label hardcoded).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint the multiple-choice sub-template markup (instant-grade + feedback box + Continuar CTA)</name>
  <files>index.html</files>
  <read_first>
    - index.html MC sub-template lines 417-470 (button row 430-441, "Respuesta correcta" line 444-447, explanation 460-462, ¿Por qué? 464-467, Siguiente 468).
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Multiple-choice (EX-03, 1-STEP, D-01/D-02/D-04), §Copywriting (Continuar → / ¡Esatto! / Quasi…), §Conscious Deviations #1.
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §Multiple-choice sub-template.
    - 33-01-SUMMARY.md — exact names of the shared `.session-feedback`/`.session-cta` classes created in the foundation.
  </read_first>
  <action>
    In `index.html` MC sub-template (~417-470), repaint while keeping the 1-step flow and every binding:
    - Keep the `multiChoiceOrder` `x-for`, `:key="perm"`, `@click="sessionSelectOption(perm)"`, `:disabled`, the `'correcta'`/`'incorrecta'` `:class` toggles, and `x-text="...options[perm]"` EXACTLY (D-01: NO Comprobar button, NO pre-check selection state). Add the ✓/✗ glyph as a non-data hardcoded element shown via the `.correcta`/`.incorrecta` state (CSS `::after` or a hardcoded `<span aria-hidden>` toggled by the class) so it carries meaning non-chromatically.
    - Wrap the feedback area into a `.session-feedback` box (from 33-01) toggled `.correct` when `sessionFeedback === 'correct'` and `.wrong` when `'incorrect'`. Inside it: a hardcoded serif title — `¡Esatto!` shown `x-show="sessionFeedback === 'correct'"` and `Quasi…` shown `x-show="sessionFeedback === 'incorrect'"` (D-09; labels hardcoded, NOT x-text) — followed by the EXISTING `.session-explanation` <p> (keep its dual-guard `x-show` + `x-text`) and the EXISTING `.session-why` ¿Por qué? button (keep `revealSessionExplanation()`). The legacy "Respuesta correcta:" line (444-447) may be folded into the feedback box or kept (planner's call; copy stays Spanish, `<strong x-text>` preserved if kept).
    - Convert the `<button x-show="sessionFeedback !== null" @click="sessionAdvance">` (468) into the bottom primary CTA `.session-cta` with hardcoded label `Continuar →` (D-04 manual advance; `sessionAdvance` unchanged). No auto-advance added (600ms stays song-mode only).
    - x-text ONLY for data; all titles/CTA labels hardcoded. No x-html.
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8'); const keep=['sessionSelectOption(perm)','multiChoiceOrder','revealSessionExplanation()','sessionAdvance']; const okKeep=keep.every(k=>h.includes(k)); const okCopy=h.includes('¡Esatto!')&&h.includes('Quasi')&&h.includes('Continuar'); const noComprobarMC=!/green-selection/.test(h); const noHtml=(h.match(/x-html/g)||[]).length===0; if(okKeep&&okCopy&&noComprobarMC&&noHtml){console.log('PASS')}else{console.error('FAIL keep='+okKeep+' copy='+okCopy+' noPreSel='+noComprobarMC+' noHtml='+noHtml);process.exit(1)}"</automated>
    <human-check>Run `npx serve`, do a multiple-choice exercise. Confirm: tapping an option grades instantly (no Comprobar step); correct option → green tint + ✓, chosen-wrong → red tint + ✗, others fade; a feedback box shows ¡Esatto!/Quasi… + explanation; CTA reads "Continuar →"; ¿Por qué? appears on correct.</human-check>
  </verify>
  <acceptance_criteria>
    - MC sub-template keeps `@click="sessionSelectOption(perm)"`, the `multiChoiceOrder` `x-for`/`:key="perm"`, the `'correcta'`/`'incorrecta'` `:class` toggles, and `x-text="...options[perm]"` byte-equivalent.
    - NO "Comprobar" button and NO `green-selection`/pre-check selection class exist in the MC sub-template (D-01).
    - A `.session-feedback` box toggles `.correct`/`.wrong` and contains a hardcoded serif title `¡Esatto!`/`Quasi…` plus the preserved `.session-explanation` and `.session-why` elements.
    - The advance control is the bottom `.session-cta` with hardcoded label `Continuar →` calling `sessionAdvance`.
    - `grep -c 'x-html' index.html` = 0.
  </acceptance_criteria>
  <done>MC markup repainted with instant-grade flow intact, titled feedback box, and Continuar CTA; automated check prints PASS; human-check confirms the comprobado visuals.</done>
</task>

<task type="auto">
  <name>Task 2: Editoriale CSS for multiple-choice options + marks + dim</name>
  <files>app.css</files>
  <read_first>
    - styles.css `button.correcta` (38-42) and `button.incorrecta` (44-48) — the legacy full-fill green/red to be overridden.
    - app.css Phase 33 session section created in 33-01 (append here) + the tint tokens.
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Multiple-choice (option specs: 4 stacked gap 10px, serif 18, resting `--ed-surface` + `--ed-border-soft` 1.5px + radius 14, padding 15×18, justify space-between; correct/incorrect/dim states), §Spacing (verbatim 10px gap, 15×18 padding), §Focus/disabled.
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §styles.css legacy `.correcta`/`.incorrecta`.
  </read_first>
  <action>
    In `app.css` (in the Phase 33 session section, AFTER 33-01's rules — app.css wins the cascade over styles.css by source order, same specificity), ADD the MC option styling:
    - Resting option button: serif `--ed-font-serif` 18/500, `--ed-surface` fill, `--ed-border-soft` 1.5px border, radius 14 (`--ed-radius-feedback`), padding 15px×18px (`/* exception verbatim */`), `display:flex; justify-content: space-between` (text left, mark right). Option-row stack gap 10px (`/* exception verbatim */`). Hover (desktop): border darken toward `--ed-ink` ~120ms, no fill change.
    - Override `button.correcta` → `--ed-green-tint` fill + 2px `--ed-green` border + `--ed-green-on-tint` text + a `✓` mark (via `::after` content or styling the hardcoded glyph).
    - Override `button.incorrecta` → `--ed-red-tint` fill + 2px `--ed-red` border + `--ed-red-text` text + a `✗` mark.
    - Post-grading dim of the non-correct/non-chosen options to `opacity: 0.5` (target the disabled options that carry neither `.correcta` nor `.incorrecta` while `sessionFeedback !== null`; scope it to the MC option row so it does not bleed into match/word-buttons).
    - Color/border/opacity transitions ~150-200ms ease. Focus-visible: 2px `--ed-green` outline offset 2px. Consume only `--ed-*`; no Pico, no palette redefinition.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app.css','utf8'); const ok=s.includes('button.correcta')&&s.includes('button.incorrecta')&&s.includes('--ed-green-tint')&&s.includes('--ed-red-tint')&&/opacity:\s*0?\.5/.test(s); const noPico=!/--pico-[a-z-]+:\s/.test(s.split('Phase 33')[1]||''); if(ok){console.log('PASS')}else{console.error('FAIL ok='+ok);process.exit(1)}"</automated>
    <automated>node --test tests/*.test.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `app.css` Phase 33 section overrides `button.correcta` to `--ed-green-tint` fill + `--ed-green` 2px border + `--ed-green-on-tint` text + ✓, and `button.incorrecta` to `--ed-red-tint` + `--ed-red` 2px border + `--ed-red-text` + ✗.
    - A post-grading `opacity: 0.5` rule dims the other MC options, scoped to the MC option row (no bleed into match/word-buttons).
    - Resting options: serif 18, `--ed-surface`, `--ed-border-soft` 1.5px, radius 14, padding 15×18, gap 10px (verbatim values marked `/* exception verbatim */`).
    - Focus-visible 2px green outline present; only `--ed-*` tokens used; no new `--pico-*` definition.
    - `node --test tests/*.test.js` shows only the known preexisting `genero-numero` failure.
  </acceptance_criteria>
  <done>MC option/marks/dim CSS appended consuming tint tokens; legacy full-fill overridden; tests show only the known preexisting failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exercise content (options/explanation) → DOM | Hand-authored option/explanation strings rendered in the MC sub-template. Only untrusted-input surface (static, single-user, local, no backend/network/auth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering (DOM injection) | MC option/explanation rendering (index.html 417-470) | mitigate | `x-text` exclusive for `options[perm]` and `payload.explanation`; feedback titles (¡Esatto!/Quasi…) and CTA (Continuar →) and ✓/✗ marks are hardcoded, never x-html. Acceptance asserts `grep -c x-html` = 0. |
| T-33-04 | (no new surface) | CSS repaint + markup | accept | Pure presentation; no network/storage/privilege/auth surface added. The grading flow (`sessionSelectOption`/`applyResultToSession`/cascade) is unchanged. |

No server/auth surface in this architecture — none invented.
</threat_model>

<verification>
- `node --test tests/*.test.js` → only the known preexisting `genero-numero` failure.
- `grep -c 'x-html' index.html` → 0.
- Manual: instant-grade MC, comprobado tint states + ✓/✗ + dim, titled feedback box, Continuar CTA, ¿Por qué? on correct.
</verification>

<success_criteria>
- EX-03 comprobado states recreated (green-tint ✓ / red-tint ✗ / rest dim, titled feedback box, Continuar → CTA) over the intact 1-step instant-grade flow (D-01/D-02/D-04).
- Engine untouched; anti-XSS x-text-only invariant preserved.
- Conscious deviation honored: NO 2-step Comprobar flow, NO pre-check green-selection state.
</success_criteria>

<output>
Create `.planning/phases/33-pantallas-de-ejercicio/33-02-SUMMARY.md` when done.
</output>
