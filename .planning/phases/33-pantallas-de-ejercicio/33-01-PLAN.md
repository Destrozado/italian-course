---
phase: 33-pantallas-de-ejercicio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.css
  - src/screens/app.js
  - index.html
autonomous: true
requirements: [EX-01, EX-02]
must_haves:
  truths:
    - "Every exercise screen shows the Editoriale top bar: circular back button (‹), green progress bar (% of set), NN/NN counter in Space Grotesk"
    - "When Contrarreloj is active, a timer chip (remaining seconds, Space Grotesk) shows AND the depleting progress bar stays"
    - "The back button calls the existing requestReturnToHome (confirmation preserved) and carries aria-label='Volver al home'"
    - "The question block shows the category overline (uppercase, Hanken 11) reusing sessionContextLabel"
    - "The prompt renders serif 30px with the literal ___ gap shown as a styled inline slot (empty pre-grading)"
    - "Post-grading the gap fills with the answer: green underline if correct, red strikethrough if wrong"
    - "Reiniciar ejercicios + ← Volver al home stay at the bottom under the <hr>"
  artifacts:
    - path: "app.css"
      provides: "Post-grading tint tokens (--ed-green-tint family, --ed-red-tint family) + session Editoriale CSS section (top bar, overline, prompt, gap slot, feedback box scaffold, bottom CTA, footer)"
      contains: "--ed-green-tint"
    - path: "src/screens/app.js"
      provides: "Two read-only presentational getters: progress percent/counter + prompt gap-split on ___"
      contains: "sessionProgressPercent"
    - path: "index.html"
      provides: "Repainted top bar markup + gap-split prompt markup inside the session <template>"
      contains: "aria-label=\"Volver al home\""
  key_links:
    - from: "index.html top bar"
      to: "requestReturnToHome"
      via: "@click on circular back button"
      pattern: "@click=\"requestReturnToHome\""
    - from: "index.html prompt"
      to: "sessionPromptParts getter"
      via: "x-for over gap-split parts"
      pattern: "sessionPromptParts"
    - from: "app.css top progress bar"
      to: "sessionProgressPercent getter"
      via: ":style width binding"
      pattern: "sessionProgressPercent"
---

<objective>
Lay the Editoriale foundation for all three exercise screens: the unified top bar (EX-01), the question block with styled gap (EX-02), the post-grading tint tokens, the two new read-only presentational getters, and the shared session CSS scaffold (feedback box, bottom CTA, footer) that the three per-type repaint plans (33-02/03/04) build on.

Purpose: EX-01 (top bar) and EX-02 (question block + gap render) are shared by all three exercise types. Defining the contracts (tokens, getters, top-bar markup, gap-split markup, shared CSS classes) once, first, prevents the three downstream plans from each re-deriving them. This is the interface-first foundation.
Output: New tint tokens + session CSS section in `app.css`; two getters in `app.js`; repainted top bar + gap-split prompt + preserved footer in `index.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md
@.planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md
@.planning/phases/33-pantallas-de-ejercicio/33-CONTEXT.md

<interfaces>
<!-- Getters/state the executor reuses verbatim (extracted from src/screens/app.js — DO NOT re-derive). -->

Existing read-only getters to REUSE (no logic change):
- `sessionContextLabel` (app.js ~2654) → returns "Examen: Avere" / "Repaso (20 ejercicios)" / "Canción: <title>". The EX-02 overline binds to this. CSS uppercases it (text-transform), NOT JS.
- `sessionProgressLabel` (app.js ~2600) → returns `Ejercicio ${sessionCursor + 1} / ${sessionExerciseIds.length}`.

Existing state the NEW getters derive from (read-only):
- `this.sessionCursor` (0-based cursor), `this.sessionExerciseIds` (array; `.length` = set size).
- `this.sessionCurrentExercise` (current exercise object; `.payload.prompt` holds the literal `___`). Null during the unmount tick → guard required.
- `this.sessionFeedback` ('correct' | 'incorrect' | null), `this.sessionCurrentExercise.payload.correctIndex`, `.options[]` (used by the gap-fill answer text downstream — this plan only splits the prompt).

Getter guard precedent to COPY (from `bankWithKeys` ~2710 / `sessionContextLabel` ~2655):
  every new getter starts with `if (!this.sessionCurrentExercise) return <safe default>;`

CSS token precedents already in app.css :root (~132-171) — REUSE, do not redefine:
  --ed-green #2f7d56 · --ed-red #b5412e · --ed-surface #fbfaf6 · --ed-paper #f4f0e8
  --ed-border-soft · --ed-hairline · --ed-faint-2 · --ed-muted · --ed-ink
  --ed-radius-feedback 14px · --ed-radius-cta 16px · --ed-radius-pill 999px
  --ed-space-screen 22px · --ed-shadow-cta · --ed-font-serif|sans|mono
CSS component precedents to model on (Phase 32 Home section, app.css 409-656):
  `.home-overline` (435-443) → the EX-02 overline · `.home-cta` (455-468) → the bottom "Continuar →" CTA · `.status-dot` currentColor technique (546-552) · base `<progress>` green-fill restyle (~358-378).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add post-grading tint tokens + two presentational getters</name>
  <files>app.css, src/screens/app.js</files>
  <read_first>
    - app.css `:root` block (lines 126-171) — where the `--ed-*` tokens live; append the new tint tokens in the same `:root` style.
    - src/screens/app.js `sessionProgressLabel` (~2600-2602), `sessionContextLabel` (~2654-2674), `bankWithKeys` (~2704-2721) — the read-only getter shape + the double-defense early-return to copy.
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Color ("Post-grading tint pairs") and §Gap rendering & fill (D-08).
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §`app.css` :root tint tokens + §`src/screens/app.js` presentational getters.
  </read_first>
  <behavior>
    - sessionProgressPercent: with sessionExerciseIds.length = 20 and sessionCursor = 0 → returns 5 (the (cursor+1)/length percentage, integer 0..100). With cursor = 19 → returns 100. With sessionExerciseIds empty → returns 0 (no divide-by-zero).
    - sessionProgressCounter: with cursor 0 / length 20 → returns the string "1/20". With cursor 19 / length 20 → "20/20".
    - sessionPromptParts: given payload.prompt "Lui ___ ventidue anni." → returns ["Lui ", " ventidue anni."] (split on the literal "___", the gap is the join boundary). A prompt with no "___" → returns the single-element array [prompt] (gap still renderable or absent — never throws). With sessionCurrentExercise null → returns [] (unmount-tick guard).
  </behavior>
  <action>
    In `app.css` `:root` (after line 167, alongside the existing `--ed-*` tokens), ADD the six post-grading tint tokens VERBATIM from UI-SPEC §Color: `--ed-green-tint: #e8f1ea;` `--ed-green-tint-border: #cfe3d6;` `--ed-green-on-tint: #23603f;` `--ed-red-tint: #f6e9e6;` `--ed-red-tint-border: #ecd3cc;` `--ed-red-text: #8f3322;`. Do NOT redefine any existing `--ed-*` token.

    In `src/screens/app.js`, ADD three read-only getters next to `sessionProgressLabel` (~2602), each starting with the double-defense guard pattern: (1) `sessionProgressPercent` — derive `Math.round(((this.sessionCursor + 1) / this.sessionExerciseIds.length) * 100)` with an early-return `0` when `sessionExerciseIds.length` is 0; (2) `sessionProgressCounter` — return `` `${this.sessionCursor + 1}/${this.sessionExerciseIds.length}` ``; (3) `sessionPromptParts` — guard `if (!this.sessionCurrentExercise) return [];` then `return String(this.sessionCurrentExercise.payload.prompt ?? '').split('___');`. These are pure derived reads — touch NO engine state, NO `sessionSelectOption`/`applyResultToSession`/grading/cascade. No `x-html` anywhere.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app.css','utf8'); const g=require('fs').readFileSync('src/screens/app.js','utf8'); const okT=['--ed-green-tint','--ed-green-tint-border','--ed-green-on-tint','--ed-red-tint','--ed-red-tint-border','--ed-red-text'].every(t=>s.includes(t)); const okG=['sessionProgressPercent','sessionProgressCounter','sessionPromptParts'].every(t=>g.includes(t)); const noHtml=!/x-html/.test(g); if(okT&&okG&&noHtml){console.log('PASS')}else{console.error('FAIL tokens='+okT+' getters='+okG+' noHtml='+noHtml);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `app.css :root` contains all six tokens: `--ed-green-tint`, `--ed-green-tint-border`, `--ed-green-on-tint`, `--ed-red-tint`, `--ed-red-tint-border`, `--ed-red-text` with the exact hex values from UI-SPEC §Color.
    - No existing `--ed-*` token is redefined or removed (the six are net-new additions).
    - `src/screens/app.js` defines three getters: `sessionProgressPercent` (integer 0..100, returns 0 when length 0), `sessionProgressCounter` (string `N/M`), `sessionPromptParts` (array from splitting prompt on `___`, `[]` when no current exercise).
    - Each new getter begins with the double-defense early-return guard.
    - `grep -c 'sessionSelectOption\|applyResultToSession\|applyImmediateFailure' src/screens/app.js` is unchanged from baseline (engine call-sites untouched); no `x-html` introduced.
  </acceptance_criteria>
  <done>Tint tokens present in app.css; three read-only getters present in app.js with guards; automated check prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Repaint the top bar (EX-01) + question block with styled gap (EX-02)</name>
  <files>index.html, app.css</files>
  <read_first>
    - index.html session block lines 390-410 (top bar: `.session-context` h2, `<header>` progress, `.session-timer` block, prompt `<p>`) and lines 637-668 (the `<hr>` + bottom `.button-row` footer — KEEP verbatim).
    - app.css Phase 32 Home section (409-656): `.home-overline` (435-443), `.home-cta` (455-468), `.status-dot` currentColor (546-552), base `<progress>` restyle (~358-378). Append the new session section AFTER line 656.
    - styles.css `.session-context` (487-493), `.session-timer*` (503-522 — note `::-webkit-progress-value`/`::-moz-progress-bar` at 514-515 force the timer bar red and win by specificity).
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Top bar (EX-01), §Question block (EX-02), §Gap rendering & fill (D-08), §Spacing (gutter 22px verbatim), §Typography (overline Hanken 11/700 ls 2-2.5px uppercase faint-2; prompt Spectral 30/500 lh1.3; counter+timer Space Grotesk 13/700).
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §index.html top bar + §No Analog Found (NN/NN counter chip, styled gap).
  </read_first>
  <action>
    In `index.html`, inside `<template x-if="currentScreen === 'session' && sessionCurrentExercise">` (~390), repaint the region lines 391-410 into the EX-01 top bar while PRESERVING every existing binding:
    - ADD a circular icon-only back button (glyph `‹`) at top-left with `@click="requestReturnToHome"` and a static `aria-label="Volver al home"` (D-10; the existing confirmation inside `requestReturnToHome` is untouched).
    - ADD a green top progress bar whose fill width binds to the new `sessionProgressPercent` getter (e.g. an inner fill `:style="'width:' + sessionProgressPercent + '%'"`), and a `NN/NN` counter bound `x-text="sessionProgressCounter"` (Space Grotesk via CSS).
    - KEEP the `.session-context` h2 `x-text="sessionContextLabel"` (now styled as the EX-02 overline via CSS — D-06). KEEP the existing `<header x-text="sessionProgressLabel">` OR fold its role into the counter (planner's call; if removed, the counter must carry the NN/NN). KEEP the `.session-timer` block VERBATIM (`x-show`, `:max`, `:value`, the seconds `x-text`) — D-11 only restyles it into a chip; add a chip wrapper/class around `.session-timer-secs`, do NOT touch the timer expressions.
    - Replace the flat prompt `<p x-text="...payload.prompt">` (line 410) with a serif-30 prompt that renders `sessionPromptParts` via `x-for`, inserting a styled inline gap `<span>` slot between the two parts. The gap span is empty pre-grading; post-grading it shows the chosen/correct answer with green underline (correct) or red strikethrough (wrong) — drive this with a class toggle on `sessionFeedback`. Use `x-text` ONLY for any data; the gap fill text is `sessionCurrentExercise.payload.options[sessionCurrentExercise.payload.correctIndex]` for the correct answer (downstream MC plan refines per-type; here render the correct answer text in the gap post-grading as the shared default). Hardcode no data via `x-html`.
    - KEEP lines 637-668 (the `<hr>` + bottom `.button-row` with `restartRepaso` + `requestReturnToHome`) VERBATIM — this is the EX-01 footer (D-10, UX-01 preserved).

    In `app.css`, AFTER line 656, ADD a new banner-commented section "Phase 33 — Pantallas de ejercicio (EX-01/EX-02 foundation)" consuming `--ed-*` only:
    - `.session` layout wrapper: full-height column, screen gutter `--ed-space-screen` (22px verbatim — mark `/* exception verbatim */`), bottom secondary row pinned at the bottom (technique planner's discretion).
    - Circular back button: `--ed-radius-pill`, `--ed-surface` fill, `--ed-border-soft` border; focus-visible 2px `--ed-green` outline offset 2px.
    - Top progress bar: track on a faint tint, fill `--ed-green`, height 4px (xs).
    - `.session-context` (the overline) repaint: COPY `.home-overline` — Hanken 11/700, letter-spacing 2.5px, `text-transform: uppercase`, color `--ed-faint-2` (D-06; uppercase via CSS, NOT JS).
    - Prompt: serif `--ed-font-serif` 30px/500 line-height 1.3.
    - Gap slot: a styled inline underline-placeholder when empty; `.gap-correct` → `--ed-green` text + underline; `.gap-wrong` → `--ed-red` text + line-through.
    - Counter + timer chip: Space Grotesk (`--ed-font-mono`) 13/700; timer chip = green-tint pill background. Override the legacy red timer value-color (styles.css 514-515) by repainting `.session-timer-bar::-webkit-progress-value` / `::-moz-progress-bar` here (app.css wins by source order) toward the Editoriale palette — keep it readable as "time depleting".
    - Bottom CTA scaffold `.session-cta` (used by 33-02/03/04): COPY `.home-cta` — `--ed-radius-cta`, `--ed-green` fill, `--ed-surface` text, `--ed-shadow-cta`; focus-visible green outline.
    - Feedback box scaffold `.session-feedback` (used by 33-02/03/04): margin-top 16, radius 14 (`--ed-radius-feedback`); `.session-feedback.correct` → `--ed-green-tint` fill + `--ed-green-tint-border`; `.session-feedback.wrong` → `--ed-red-tint` + `--ed-red-tint-border`. Feedback title `.session-feedback-title` serif 17/600. Repaint `.session-explanation` → Hanken 13/400 `--ed-muted` line-height 1.5.
    Off-grid verbatim values (gutter 22px) marked `/* exception verbatim */` exactly like the Phase 32 section did. No `x-html`, no Pico reintroduction, no palette redefinition.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -5</automated>
    <human-check>Run `npx serve`, start a Repaso. Confirm: (1) top-left circular ‹ back button (aria-label Volver al home), (2) green progress bar grows as you advance, (3) NN/NN counter in monospace, (4) with Contrarreloj on, a seconds chip shows AND the bar depletes, (5) the prompt renders serif ~30px with a visible styled gap where `___` was, (6) Reiniciar + ← Volver al home remain at the bottom under the hr.</human-check>
  </verify>
  <acceptance_criteria>
    - `index.html` session block contains a back button with `@click="requestReturnToHome"` and static `aria-label="Volver al home"`.
    - The top progress bar fill binds to `sessionProgressPercent`; a counter binds `x-text="sessionProgressCounter"`.
    - The `.session-timer` block keeps its existing `x-show="sessionTimed && sessionFeedback === null"`, `:max="sessionTimeLimitMs(...)"`, `:value="sessionTimeRemainingMs"`, and seconds `x-text` expressions byte-identical (only wrapped/classed for the chip).
    - The prompt renders via `x-for` over `sessionPromptParts` with a styled gap `<span>`; the gap shows green-underline on correct / red-strikethrough on wrong via a `sessionFeedback`-driven class.
    - The `<hr>` + bottom `.button-row` (lines 637-668: `restartRepaso` + `requestReturnToHome`) are preserved verbatim.
    - `app.css` has a new "Phase 33" session section consuming only `--ed-*` tokens, defining at least `.session-context` (overline), the back button, top progress bar, gap-slot classes, `.session-cta`, and `.session-feedback`(`.correct`/`.wrong`) scaffolds.
    - `grep -c 'x-html' index.html` returns 0; no `--pico-*` token is newly DEFINED in app.css; no `<link>` to Pico is added.
    - `node --test tests/*.test.js` shows no NEW failures vs the known single preexisting `genero-numero` failure.
  </acceptance_criteria>
  <done>Top bar + question block repainted in index.html reusing all bindings; session CSS scaffold + shared feedback/CTA classes present in app.css; tests show only the known preexisting failure; human-check confirms the EX-01/EX-02 visuals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exercise content (JSON) → DOM | Hand-authored prompt/options/explanation strings rendered into the session DOM. This is the ONLY untrusted-input surface in a static, single-user, local-only app (no backend, no network, no auth, no server-side data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering (DOM injection) | prompt/gap-split rendering in index.html (`sessionPromptParts`, overline, counter) | mitigate | `x-text` is the EXCLUSIVE data binding (invariant T-02-01); all content (prompt parts, label) flows via `x-text`, never `x-html`. The new gap-split getter returns plain strings rendered with `x-text`. Static button labels are hardcoded. The acceptance check asserts `grep -c x-html` = 0. |
| T-33-02 | (no new surface) | tokens/getters/CSS | accept | A UI repaint introduces NO network, storage, privilege, or auth surface. The two new getters are read-only derived reads of existing session state; they touch no engine/persistence path. No new threat. |

Single paper palette (dark mode off, Phase 32 D-03). No server/auth threats exist in this architecture — none invented.
</threat_model>

<verification>
- `node --test tests/*.test.js` → only the known preexisting `genero-numero` failure; zero new failures.
- `grep -c 'x-html' index.html` → 0.
- The two engine-call-site grep counts (`applyResultToSession`, `sessionSelectOption`) unchanged vs baseline.
- Manual: top bar (back/progress/counter/timer chip), serif prompt with styled gap, preserved footer.
</verification>

<success_criteria>
- EX-01 top bar present (circular back, green progress bar, NN/NN counter, timer chip + depleting bar when timed) reusing existing bindings.
- EX-02 question block present (overline via sessionContextLabel, serif-30 prompt, styled `___` gap with post-grading green-underline / red-strikethrough fill).
- Tint tokens + shared `.session-cta` / `.session-feedback` scaffolds available for the three per-type plans.
- Engine untouched; anti-XSS x-text-only invariant preserved.
</success_criteria>

<output>
Create `.planning/phases/33-pantallas-de-ejercicio/33-01-SUMMARY.md` when done.
</output>
