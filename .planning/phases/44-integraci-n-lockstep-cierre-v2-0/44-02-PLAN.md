---
phase: 44-integraci-n-lockstep-cierre-v2-0
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - content/exercises/fare-indicativo.json
  - content/exercises/fare-indefiniti.json
  - tests/content-fare-indicativo.test.js
  - tests/content-fare-indefiniti.test.js
  - .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md
autonomous: false
requirements: [INT-03, INT-04]

estimate:
  tokens: 135000
  raw_tokens: 135000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # --- INT-03 / D-44-01 : los 3 cruces, exactamente 3 ---
    - "Existen EXACTAMENTE 3 slots de cruce nuevos en el corpus, con estos ids literales y estas parejas: `fare-indicativo-300` con `categoryIds: [\"fare-indicativo\", \"avere\"]`, `fare-indicativo-301` con `[\"fare-indicativo\", \"presente-regolare\"]` y `fare-indefiniti-300` con `[\"fare-indefiniti\", \"modali\"]`. `fare-congiuntivo` y `fare-cond-imperativo` se quedan SIN cruce por decisión, no por olvido (INT-03, D-44-01)."
    - "El segundo `categoryId` es `modali`, byte a byte, NUNCA `verbi-modali`: `src/data/schema-validator.js` exige que todo valor de `categoryIds` referencie una categoría registrada y `verbi-modali` no existe, así que el slug equivocado que escribe INT-03 en REQUIREMENTS.md haría rechazar el fichero entero (INT-03, D-44-01)."
    - "El prefijo de cada id de cruce es el slug COMPLETO de su categoría, byte a byte: `fare-indicativo-300` / `-301` y `fare-indefiniti-300`. `fare-ind` es prefijo ambiguo entre las dos categorías (D-40-03), así que ninguna comprobación, ningún regex y ningún `RESET_PREFIXES_V13` se apoya en la forma truncada (D-40-07, D-44-12)."
    # --- INT-03 / D-44-02 : el sentido invertido, que es lo que hace que el cruce valga algo ---
    - "En los 3 cruces la key vive en la categoría VECINA y la forma de `fare` va ESCRITA en el enunciado como contexto: `fare-indicativo-300` pregunta el auxiliar `avere` con el participio `fatto` escrito, `fare-indicativo-301` pregunta el presente de un verbo REGULAR con una forma de `fare` escrita, y `fare-indefiniti-300` pregunta el modal conjugado con `fare` escrito en infinitivo. Ninguna key de los 3 cruces es una forma de `fare` (INT-03, D-44-02)."
    - "Ninguno de los 3 cruces duplica un slot que ya existe: en particular `fare-indefiniti-300` NO pregunta el infinitivo tras modal, porque `fare-indefiniti-infinito-presente` variante 3 ya es literalmente `Io devo ___ il letto` con key `fare` (D-43-12) y un cruce en ese sentido sería el mismo ejercicio con otra etiqueta — el propio quórum lo marcaría redundante (D-44-02)."
    # --- INT-03 / D-44-03 : el volumen ---
    - "Cada cruce tiene EXACTAMENTE 3 variantes y el eje es la PERSONA, 3 personas distintas por cruce: 9 variantes nuevas en total, el volumen medio de los cruces del proyecto (`modali-300` lleva 3, `presente-regolare-300` lleva 3). El paradigma ya lo cubren los 22 slots del milestone, así que enumerar 6 personas aquí sería repetir (INT-03, D-44-03)."
    - "Los 3 cruces son `type: \"multiple-choice\"` con `options` de longitud 4 sin duplicados internos, y `correctIndex` es un entero en `[0,4)` que NO es constante dentro de un mismo cruce — si lo fuera, el autor aprendería la posición y no la forma. 0-match y 0-word-buttons son decisión razonada (DESIGN RULE D-04, D-41-13)."
    # --- G1 : fare-indicativo-300 ↔ avere ---
    - "**G1** — `fare-indicativo-300`: el participio `fatto` va escrito en los 3 `prompt` y es SIEMPRE invariable, de modo que ninguno de los 3 `prompt` contiene los pronombres objeto antepuestos `lo`, `la`, `li` ni `le` como palabra suelta: con un pronombre objeto antepuesto la concordancia del participio sería obligatoria y la frase abriría una segunda lectura defendible. Es D-43-16 aplicado al revés (G1, D-44-04)."
    - "**G1** — las 4 `options` de cada variante de `fare-indicativo-300` son formas conjugadas de `avere` más una forma de `essere` como distractora de auxiliar, y NINGUNA opción contiene una forma de `fare` (verificable por campo sobre `variants[].options[]`): el hueco es SOLO el auxiliar, así que un `fatto` dentro de una opción convertiría el ejercicio en otro (G1, D-44-04)."
    # --- G2 : fare-indicativo-301 ↔ presente-regolare ---
    - "**G2** — `fare-indicativo-301`: los 3 `prompt` llevan sujeto pronominal explícito en las DOS cláusulas (la que trae la forma de `fare` escrita y la del hueco), y los dos sujetos son personas DISTINTAS, de modo que la persona del hueco queda fijada sin ambigüedad (G2, D-41-07, D-44-04)."
    - "**G2** — en cada variante de `fare-indicativo-301` las 4 `options` son 4 personas del MISMO verbo regular, nunca verbos distintos: si el pool mezclara verbos, más de una opción sería defendible por la regla de las LECTURAS. El verbo regular candidato es UNO solo, fijado por el objeto literal del conjunto cerrado (G2, D-44-04)."
    # --- G3 : fare-indefiniti-300 ↔ modali ---
    - "**G3** — `fare-indefiniti-300`: cada uno de los 3 `prompt` lleva un complemento explícito que EXCLUYE dos de los tres modales dejando solo obligación, permiso o voluntad. `devo`, `posso` y `voglio` son intercambiables en una frase genérica; sin ese complemento las tres serían defendibles y la variante tendría tres respuestas correctas. Es el análogo exacto del vocativo de D-43-05 (G3, D-44-04)."
    - "**G3** — las 4 `options` de cada variante de `fare-indefiniti-300` son formas conjugadas de modales y nada más; NINGUNA contiene una forma de `fare`. Esta es la divergencia OBLIGATORIA respecto al molde `modali-300`, que sí mete `parlo`/`prendiamo`/`dormi` (formas del verbo gobernado) como distractora: clonarlo literalmente pondría un `fare` en `options` y violaría G3 (G3, D-44-04)."
    # --- D-41-06 / D-44-12 : el SCOPE-GATE y el canon heredados, sin excepción ---
    - "Cada uno de los 9 `prompt` contiene exactamente un objeto del conjunto literal CERRADO de 7 —`i compiti`, `un errore`, `il lavoro`, `una torta`, `il letto`, `tutto`, `una foto`— y un escaneo POR CAMPO sobre `variants[].prompt` y `variants[].options[]` devuelve 0 coincidencias para los marcadores de perífrasis y modismos (`colazione`, `spesa`, `freddo`, `farcel`, `far fare`, `causativo`). El SCOPE-GATE HARD de D-41-06 rige en los 3 cruces SIN excepción: la de `facente funzione` (D-43-18) es local al slot de participio presente y no se extiende aquí."
    - "Las 3 explanations están en español acentuado RAE, plain text de un solo párrafo sin markdown ni backticks, con apóstrofes ASCII U+0027 al citar italiano y sin comillas tipográficas. Un flag C4-accent del quórum sobre español sin tildes es bug REAL y se arregla poniendo las tildes, nunca con override (D-43-21, D-44-12)."
    - "Ninguna de las 3 explanations afirma que una construcción italiana es «agramatical» cuando la lengua la admite, ninguna habla del italiano fuera de lo que examina su hueco, ninguna describe la estructura del propio ejercicio y ninguna explica por qué falla cada distractora una por una: son las 3 prohibiciones de una explanation, destiladas de las 7 rondas de quórum de Phase 43 (D-44-12)."
    - "Ningún `prompt` de los 3 cruces filtra la respuesta: no nombra la regla, la persona ni la terminación que el hueco examina (R1 / C5). El gloss ES entre paréntesis SÍ está permitido en los cruces porque ahí DESAMBIGUA sin filtrar —la key vive en la categoría vecina, así que glosar el sentido del enunciado no regala la forma—, y el gate 0-gloss de la categoría queda acotado a los slots base con su decisión escrita en el `notes` (D-41-05, D-42-13, D-44-04)."
    # --- la ROTURA estructural de los gates existentes: el trabajo obligatorio ---
    - "`tests/content-fare-indicativo.test.js` y `tests/content-fare-indefiniti.test.js` quedan VERDES con los cruces en disco porque sus gates de paradigma están re-apuntados a una partición explícita —`CROSS_IDS`, `BASE_SLOTS`, `CROSS_SLOTS` y `allVariants()` iterando solo `BASE_SLOTS`—, clonando el idiom `SIMPLE_SLOTS`/`COMPOUND_SLOTS` que ya vive en `tests/content-fare-indicativo.test.js:69-70`. NINGÚN gate se borra ni se debilita: cada uno pasa a gobernar la partición que de verdad le corresponde."
    - "El gate que reservaba el espacio `-300`+ queda INVERTIDO en los dos ficheros, no borrado: pasa de exigir que ningún id lleve sufijo numérico de 3 cifras a exigir que los ids con ese sufijo sean EXACTAMENTE `CROSS_IDS`, y su comentario —que ya anunciaba esta fase— se actualiza en vez de eliminarse (D-40-07, D-41-14)."
    - "Los cruces tienen su propio `describe` en cada fichero de test, con los gates que SIGUEN rigiendo sobre ellos re-asertados explícitamente (hueco `___`, `options` de 4 sin duplicados, SCOPE-GATE de perífrasis, objeto del conjunto cerrado, blacklist de formas atestiguadas, y la ausencia de participio concordado) MÁS los predicados de G1/G2/G3. Un cruce no queda fuera de cobertura por el hecho de estar fuera de `BASE_SLOTS`."
    - "El gate de `categoryIds` de `tests/content-fare-indefiniti.test.js:540` queda acotado a `BASE_SLOTS` (longitud 1 con el slug completo) y gana su espejo para el cruce: longitud EXACTAMENTE 2, con `[0] === 'fare-indefiniti'` y `[1] === 'modali'`. El equivalente se escribe para los dos cruces de `fare-indicativo`."
    # --- D-44-11 / VAL-03 : el hand-off, y el rojo honesto ---
    - "Al cerrar este plan los 3 cruces llevan `validation` con `status: \"pending\"` y `passes: []`: el executor NO fabrica pases ni estampa `validated`, porque el quórum base canónico Opus+Sonnet vive en una pasada TOP-LEVEL posterior —`gsd-executor` no puede spawnear los Task subagents que el skill `gsd-validate-exercise` necesita— y un pase inventado destruye la única evidencia que el autor tiene de que una variante fue revisada (D-44-11, VAL-03)."
    - "`node scripts/run-validation-271.mjs` cierra el plan en `VAL-06 (250/250 validated): FAIL (247/250 — pending=3, missing=0, disputed=0)` y `Milestone gate FAIL`, y `VAL_07_STRICT=1 node --test tests/*.test.js` falla EXACTAMENTE en los tests de `content/exercises/fare-indicativo.json` y `content/exercises/fare-indefiniti.json` nombrando sus slots `pending`. **Ese rojo es el entregable, no un fallo:** es el marcador diciendo la verdad entre que los cruces aterrizan y el quórum termina. Nadie lo «arregla» tocando contenido ni relajando un gate (D-44-10, D-44-11)."
    - "`node --test tests/*.test.js` (sin `VAL_07_STRICT`) cierra con `fail 0`: los gates de `deriveStatus(passes) === status` son verdes con `passes: []` porque `deriveStatus([])` devuelve `pending`, y esa igualdad es justo lo que impide que un `validated` escrito a mano pase por bueno (D-44-11)."
    # --- edge probe INT-02 : la mitad «después» del boundary ---
    - "Con los 3 cruces en disco, `TOTAL_EXPECTED` (Σ de los `expected` de `CATEGORIES`) y `TOTAL_EXPECTED_BASELINE` (Σ de `slotCountOf` del disco) siguen siendo exactamente iguales y valen 250, así que el reporter termina de cargar sin disparar su `process.exit(1)`: los `expected` de las 4 entradas de `fare` son dinámicos y siguen al disco solos (edge: boundary + precision, INT-02, D-31-06)."
    # --- el motor sigue intacto ---
    - "`git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` no produce salida y `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` sigue dando 2 con los 3 cruces en disco: `applyResultToSession` ya propaga la cascada D-54 sobre TODOS los `categoryIds`, y `pickVariantIndex` (`src/domain/session.js:232`) sirve las 3 variantes de cada cruce sin una línea nueva (INT-03, SC#3, D-44-09)."
    - "Fallar `fare-indicativo-300` resetea `fare-indicativo` Y `avere` entera por la cascada D-54, exactamente como ya hace `presente-regolare-300` hoy — es el Core Value funcionando y no requiere call-site nuevo. Los ids de cruce llevan el prefijo correcto para que `RESET_PREFIXES_V13` (`src/data/storage.js:1345`, que este plan NO toca) los alcance."
    # --- INT-04 : la excepción va al prompt, no solo al notes ---
    - "`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` —la ruta REAL que el skill lee; `docs/09-VALIDATION-PROMPT.md` no existe— gana una sección `### 7.6` que declara los cruces `-300`+ y los gates G1/G2/G3 con su frontera, porque el subagent del quórum arranca con contexto vacío y NUNCA ve el `notes` de un fichero de contenido (D-44-12, INT-04)."
    - "El `notes` de los dos ficheros de contenido gana un bloque APENDIDO (nunca una re-edición de lo que ya declara) con: por qué `fare-congiuntivo` y `fare-cond-imperativo` no llevan cruce (D-44-01), por qué el sentido está invertido citando el solape de `Io devo ___ il letto` (D-44-02), G1/G2/G3 verbatim, y el gloss ES del cruce frente al 0-gloss de la categoría. Los gates `CONTENT.notes.includes(...)` que ya existen siguen verdes porque el append no borra nada."

  artifacts:
    - path: "content/exercises/fare-indicativo.json"
      provides: "los 2 cruces de `fare-indicativo`, 6 variantes nuevas"
      contains: "`fare-indicativo-300` (↔ `avere`, G1) y `fare-indicativo-301` (↔ `presente-regolare`, G2), 3 variantes cada uno, ambos con `validation.status: \"pending\"` y `passes: []`, más el bloque apendido de `notes`"
    - path: "content/exercises/fare-indefiniti.json"
      provides: "el cruce de `fare-indefiniti`, 3 variantes nuevas"
      contains: "`fare-indefiniti-300` (↔ `modali`, G3), 3 variantes, `validation.status: \"pending\"` y `passes: []`, más el bloque apendido de `notes`"
    - path: "tests/content-fare-indicativo.test.js"
      provides: "los gates de la categoría re-apuntados a la partición base/cruce, más los invariantes permanentes de G1 y G2"
      contains: "`CROSS_IDS`, `BASE_SLOTS`, `CROSS_SLOTS`, `allVariants()` sobre `BASE_SLOTS`, el gate del sufijo de 3 cifras INVERTIDO, y un `describe` propio de cruces"
    - path: "tests/content-fare-indefiniti.test.js"
      provides: "lo mismo para `fare-indefiniti`, más los invariantes permanentes de G3"
      contains: "la partición, el gate de `categoryIds` acotado a base con su espejo de longitud 2 para el cruce, el gate del sufijo invertido, y un `describe` propio de cruce"
    - path: ".planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md"
      provides: "los gates G1/G2/G3 declarados para el subagent del quórum"
      contains: "sección `### 7.6` con qué NO es falso positivo, la frontera de lo que SÍ sigue mordiendo, y el cierre remitiendo a `### 7.3`"

  key_links:
    - "`categoryIds` de 2 → `applyResultToSession` → cascada D-54: fallar un cruce resetea las DOS categorías que nombra. Es el enlace que hace que el cruce valga algo y también el que lo hace peligroso: una variante con dos respuestas defendibles no cuesta un ejercicio, cuesta 20 slots de `avere` o 12 de `modali`. Por eso los gates van declarados ANTES de escribir."
    - "`categoryIds` → `src/data/schema-validator.js`: exige que todo valor referencie una categoría registrada. `avere`, `modali` y `presente-regolare` las tres están en `content/categories.json`, así que no hay prerequisito nuevo — pero `verbi-modali` haría rechazar el fichero ENTERO y `tests/domain.test.js` se pondría rojo."
    - "`SLOTS` (= `CONTENT.exercises` entero) → los gates de paradigma de los dos ficheros de test: es EL enlace que rompe al apendir un cruce. Los ficheros iteran el array completo, no una lista base, así que el conteo de variantes, la igualdad de ids, el gate de un solo pronombre y el 0-gloss se ponen rojos en el mismo commit que aterriza el primer cruce. La partición es la precondición de un suite verde, no un refactor opcional."
    - "`variants[]` → `pickVariantIndex` (`src/domain/session.js:232`): la longitud del array es lo que produce «una casilla distinta cada pasada». Es `Pure` y axis-agnostic, así que un cruce de 3 variantes funciona sin motor nuevo — la prueba de que `git diff src/screens/app.js src/domain/` queda vacío."
    - "`validation.passes[]` → `deriveStatus` (`src/data/validation-state.js`): `validated` exige ≥2 pases `correcta` con `by` distintos y cero `incorrecta`; cualquier `incorrecta` da `disputed` STICKY. El gate `status === deriveStatus(passes)` de los dos ficheros de test es lo que hace imposible cerrar el plan con un `validated` fabricado."
    - "el complemento del `prompt` de `fare-indefiniti-300` → la unicidad de la respuesta: `devo`, `posso` y `voglio` son intercambiables en frases genéricas. El complemento es el ÚNICO desambiguador, así que un marco flojo no produce un ejercicio mediocre: produce tres respuestas defendibles, `disputed` sticky, y el reset de `fare-indefiniti` y `modali` a la vez."
    - "el `notes` → los gates `CONTENT.notes.includes(...)` que ya existen (`tests/content-fare-indicativo.test.js:377`, `tests/content-fare-indefiniti.test.js:909-970`): el `notes` es contrato EJECUTABLE, no prosa. Por eso el bloque nuevo se APENDE y nunca se re-edita: borrar un marcador declarado pone la suite roja."
    - "el `notes` → el subagent del quórum: **no hay enlace.** El subagent arranca con contexto vacío y solo lee el prompt de validación, así que toda excepción nueva a R1-R7 tiene que vivir en `### 7.6` de `09-VALIDATION-PROMPT.md`. Una excepción que solo viva en el `notes` produce el síntoma conocido: el modelo marca un patrón y aprueba otro idéntico."

  prohibitions:
    - "MUST NOT escribir `validation.status: \"validated\"` ni ninguna entrada de `passes[]` que no venga de un pase de modelo real: el audit trail es la única evidencia que el autor tiene de que una variante fue revisada, y un pase inventado la destruye sin dejar rastro. Los 3 cruces cierran este plan en `pending` con `passes: []`, y el rojo del reporter y del estricto es el entregable."
    - "MUST NOT relajar, borrar ni debilitar un gate existente para que el contenido nuevo quepa. El comentario que precede al gate 0-gloss de `tests/content-fare-indicativo.test.js` lo dice literalmente: si algún día hace falta un gloss, hay que cambiar la decisión en el `notes` y re-pasar el quórum de esa variante, NO relajar el test. Re-apuntar un gate a la partición que de verdad gobierna es legítimo; bajarle la barra no lo es, y la diferencia es si el gate sigue mordiendo sobre lo que antes cubría."
    - "MUST NOT dar de alta una variante en la que más de una opción sea defendiblemente correcta para ese prompt: el quórum la marca `disputed`, `disputed` es STICKY, y por la cascada D-54 un solo ejercicio injusto resetea DOS categorías enteras y le enseña al autor que su respuesta correcta es incorrecta. Es el daño exacto que esta herramienta existe para evitar."
    - "MUST NOT afirmar en una explanation que una construcción italiana es «agramatical» cuando la lengua la admite: enseñar una regla más rígida que el idioma es un bug de contenido, no una simplificación pedagógica. Pasó tres veces en Phase 43 y dos de ellas las introdujo el arreglo anterior."
    - "MUST NOT explicar en una explanation por qué falla cada distractora una por una, ni describir la estructura del propio ejercicio, ni hablar del italiano fuera de lo que el hueco examina: son las 3 prohibiciones destiladas de 7 rondas de quórum donde cada arreglo de prosa engendró el siguiente. «Di menos» no basta como instrucción; el criterio es el alcance, no la longitud."
    - "MUST NOT hacer override de un flag C4-accent del quórum sobre español sin tildes: en este proyecto ese flag es un bug REAL y se arregla poniendo los acentos. El override de autor existe para adjudicaciones razonadas con trabajo previo, no para ahorrarse una tilde."
    - "MUST NOT presentar una forma regional, dialectal, arcaica o de registro literario como si fuera la norma del italiano estándar, ni al revés: el autor no puede distinguirlas y la interiorizaría como regla."
    - "MUST NOT examinar vocabulario que el autor no ha estudiado sin glosarlo: fallar un cruce por no conocer una locución —no por no saber el auxiliar, el presente regular o el modal— resetea DOS categorías por la cascada D-54. Es la misma injusticia por otra puerta."
    - "MUST NOT renombrar ni renumerar un id de cruce después de darlo de alta: los ids viven en el `exerciseStats` real del autor y en el alcance de `RESET_PREFIXES_V13`, y un renombrado deja su progreso huérfano en silencio."
    - "MUST NOT autorar un cuarto cruce, un cruce imperativo ↔ congiuntivo, ni un slot o variante nuevos del paradigma de `fare`: la lista queda CERRADA en 3 (D-44-05) y los 22 slots están cerrados desde Phase 43. Tampoco perífrasis ni modismos de `fare` (SCOPE-GATE HARD de D-41-06, sin excepción en los cruces)."
    - "MUST NOT reducir el alcance de una decisión de 44-CONTEXT.md a una versión «v1», «simplificada», «estática por ahora», «placeholder» o «pendiente de cablear»: si algo no cabe, se parte la fase y se pide aprobación, no se entrega menos de lo decidido."
    - "MUST NOT tocar `src/screens/app.js`, `src/domain/` ni `src/data/`, ni añadir dependencia, `package.json` de runtime o paso de build: el proyecto es web estática de dependencias cero (CLAUDE.md §Constraints) y si el contenido exigiera cambiar el motor, el diseño del contenido está mal."
    - "MUST NOT editar `docs/09-VALIDATION-PROMPT.md`: ese fichero NO EXISTE y crearlo produce un huérfano que el quórum nunca lee, con la apariencia de que la excepción quedó declarada. La ruta real es `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md`."
    - "MUST NOT re-editar el `notes` existente de ninguno de los dos ficheros: el bloque nuevo se APENDE. Los gates `CONTENT.notes.includes(...)` convierten cada marcador declarado en contrato ejecutable, así que reescribir un párrafo puede poner la suite roja por el texto que una decisión anterior exige que exista."
    - "MUST NOT tocar `scripts/run-validation-271.mjs`, `tests/fixtures/slot-variants-integration.test.js` ni `tests/count-arrays-lockstep.test.js`: son propiedad exclusiva del plan 44-01 y sus `expected` dinámicos ya siguen al disco solos."
---

<objective>
Autorar los **3 cruces multi-categoría de `fare`** (INT-03) y, como precondición inseparable de que
puedan aterrizar, **re-apuntar los gates de paradigma de los dos ficheros de test a una partición
explícita base/cruce**.

La primera tarea es un **tracer**: `fare-indicativo-300` de punta a punta —la partición del fichero de
test, los gates que rompen re-apuntados, el `describe` de cruces con los predicados de G1, el slot con
sus 3 variantes, el bloque apendido de `notes` y la sección `### 7.6` del prompt del quórum—, verificado
con la suite entera en verde antes de que exista un segundo cruce. Se elige `fare-indicativo-300` porque
es el que atraviesa TODAS las capas de esta fase a la vez y porque su gate (G1: `fatto` invariable →
prohibidos los pronombres objeto antepuestos) es el mismo patrón de razonamiento que gobierna G2 y G3: si
está mal, está mal una vez y no tres.

Cubre INT-03 (los cruces con `categoryIds` de 2, propagando la cascada D-54 sin call-sites nuevos) y la
mitad activa de INT-04 (los 3 cruces nacen `pending` y se entregan al quórum TOP-LEVEL).

Purpose: **el cruce solo se gana su segundo `categoryId` si la key vive en la categoría vecina.** El
scout de la discusión encontró que el sentido literal del roadmap produciría duplicados: `Io devo ___ il
letto` con key `fare` YA EXISTE en `fare-indefiniti-infinito-presente` variante 3, `fare-indicativo-passato-prossimo`
ya lleva `sono fatto` como distractora, y `fare-indicativo-presente` ya examina `faccio` con distractoras
regularizadas. Invirtiendo el sentido —la forma de `fare` escrita como contexto, la pregunta en la
casilla vecina— el solape es cero y el autor practica `avere`, `presente-regolare` y `modali` bajo la
presión de un verbo irregular en el mismo enunciado. Y el riesgo real de estos 3 slots no es doble
validez de forma: es que la frase admita **otra respuesta defendible**, que se cierra con gates escritos
antes de escribir y no con presupuesto de quórum.

Output: 3 slots / 9 variantes en `validation.status: "pending"`, los dos ficheros de test particionados
con los cruces bajo su propio `describe`, dos bloques de `notes` apendidos, y `### 7.6` en el prompt de
validación. El reporter pasa de `247/247 PASS` a `VAL-06 (250/250 validated): FAIL (247/250 — pending=3)`
y **ese rojo es el entregable**: es el marcador diciendo la verdad hasta que el quórum top-level termine.
Ningún fichero de `src/` cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-CONTEXT.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-PATTERNS.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Código canon a clonar, verificado contra el disco el 2026-08-11.

**El molde LITERAL del cruce invertido** — `content/exercises/presente-regolare.json`, slot
`presente-regolare-302`: el compuesto va ESCRITO como contexto temporal y el hueco pide la otra casilla.
Es exactamente la forma que D-44-02 adopta. Nótese el **gloss ES entre paréntesis** y el key set.

```json
{
  "id": "presente-regolare-302",
  "type": "multiple-choice",
  "categoryIds": ["presente-regolare", "avere"],
  "explanation": "Aquí hay que formar el presente indicativo regular, no el pasado. El enunciado ya trae escrita la forma compuesta del passato prossimo con avere (ho parlato, abbiamo mangiato), que sirve solo como contexto temporal de lo que pasó ayer; el hueco pide la forma del presente del mismo verbo […] Las distractoras son otras personas del presente del mismo verbo: solo una concuerda con el sujeto.",
  "variants": [
    { "prompt": "Ieri ho parlato spagnolo, ma di solito io ___ italiano. (en español: pero normalmente yo hablo)", "options": ["parlo", "parli", "parla", "parlano"], "correctIndex": 0 },
    { "prompt": "La settimana scorsa abbiamo mangiato al ristorante, ma di solito noi ___ a casa. (en español: pero normalmente nosotros comemos)", "options": ["mangia", "mangio", "mangiamo", "mangiano"], "correctIndex": 2 }
  ]
}
```

> Key set del slot al ordenar: `['categoryIds','explanation','id','type','validation','variants']`; de la
> variante: `['correctIndex','options','prompt']` (gate en `tests/content-fare-indicativo.test.js:208-219`).
> **`validation` nace `{"status": "pending", "passes": []}`** — NO se copia el bloque `validated` del molde.

**El pool del eje AUXILIAR** — `presente-regolare-300`, variantes 1-3 (auxiliar equivocado + participio
mal formado). En `fare-indicativo-300` el hueco es SOLO el auxiliar, así que el pool son formas
conjugadas de `avere` más `essere` como distractora, **sin participio dentro de la opción**:

```json
["ho parlato", "sono parlato", "ho parlate", "ho parlando"]
["siamo lavorato", "abbiamo lavorato", "abbiamo lavorate", "abbiamo lavorando"]
```

**El pool de G2 — «4 personas del MISMO verbo regular»** (verbatim de `presente-regolare-302`/`-303`):

```json
{ "options": ["parlo", "parli", "parla", "parlano"], "correctIndex": 0 }
{ "options": ["mangia", "mangio", "mangiamo", "mangiano"], "correctIndex": 2 }
{ "options": ["parto", "parti", "parte", "partono"], "correctIndex": 0 }
```

**El molde de volumen y de tono del cruce ↔ modali** — `content/exercises/modali.json`, `modali-300`:

```json
{
  "id": "modali-300",
  "type": "multiple-choice",
  "categoryIds": ["modali", "presente-regolare"],
  "variants": [
    { "prompt": "Io ___ parlare con il direttore. (en español: yo tengo que / debo hablar)", "options": ["dovo", "devo", "devi", "parlo"], "correctIndex": 1 },
    { "prompt": "Noi ___ prendere il treno delle otto. (en español: nosotros podemos coger)", "options": ["possiamo", "possono", "prendiamo", "posso"], "correctIndex": 0 },
    { "prompt": "Tu ___ dormire ancora un po'. (en español: tú puedes dormir)", "options": ["dormi", "puoi", "può", "posso"], "correctIndex": 1 }
  ]
}
```

> **DIVERGENCIA OBLIGATORIA (G3):** `modali-300` mete `parlo`/`prendiamo`/`dormi` —forma conjugada del
> verbo GOBERNADO— como distractora. En `fare-indefiniti-300` eso sería una forma de `fare` en `options`
> y G3 lo prohíbe: el pool son 4 formas de modales y nada más. Y `modali-300` usa marcos genéricos donde
> los 3 modales serían intercambiables, salvado solo por el gloss; **G3 exige además el complemento
> explícito** que excluya dos de los tres. Se clona la FORMA, no el pool ni el marco.

**El idiom de partición que YA existe** — `tests/content-fare-indicativo.test.js:69-70`, a clonar:

```js
const SIMPLE_SLOTS = IDS.slice(0, 4);
const COMPOUND_SLOTS = IDS.slice(4);
```

Forma a añadir (slug COMPLETO, nunca `fare-ind*` — D-40-03):

```js
const CROSS_IDS = ['fare-indicativo-300', 'fare-indicativo-301'];
const BASE_SLOTS = SLOTS.filter((s) => !CROSS_IDS.includes(s.id));
const CROSS_SLOTS = SLOTS.filter((s) => CROSS_IDS.includes(s.id));
const allVariants = () => BASE_SLOTS.flatMap((s) => s.variants.map((v, k) => ({ slot: s, v, k })));
const crossVariants = () => CROSS_SLOTS.flatMap((s) => s.variants.map((v, k) => ({ slot: s, v, k })));
```

**El gate a INVERTIR** (`tests/content-fare-indicativo.test.js:221-225`, gemelo en
`tests/content-fare-indefiniti.test.js:577-580`). Su comentario ya anuncia esta fase: se actualiza, no se
borra.

```js
  test('los unicos ids con sufijo de 3 cifras son los cruces declarados (D-40-07, INT-03)', () => {
    const usados = SLOTS.map((s) => s.id).filter((id) => /-\d{3}$/.test(id));
    assert.deepEqual(usados, CROSS_IDS, 'D-40-07: el espacio -300+ aloja EXACTAMENTE los cruces de Phase 44');
  });
```

**Escaneo POR CAMPO, forma canónica** (`tests/content-fare-indicativo.test.js:307-325`). Nunca sobre el
fichero completo ni sobre `notes`: el `notes` nombra a propósito cada forma de la blacklist con su audit
trail, así que un grep de fichero entero se auto-invalidaría.

```js
    for (const { slot, v, k } of allVariants()) {
      const campos = [v.prompt, ...v.options];
      …
      assert.ok(OBJECTS.some((o) => v.prompt.includes(o)), `D-41-06: ${slot.id}#${k} no usa ningun objeto del conjunto cerrado`);
    }
```

**El conjunto CERRADO de objetos** (`tests/content-fare-indicativo.test.js:124`,
`tests/content-fare-indefiniti.test.js:373`):

```js
const OBJECTS = ['i compiti', 'un errore', 'il lavoro', 'una torta', 'il letto', 'tutto', 'una foto'];
```

**Los gates que SOBREVIVEN con los cruces `pending`** (`tests/content-fare-indicativo.test.js:600-651`) —
el permiso explícito para cerrar este plan en rojo honesto:

```js
  // ESTOS ASSERTS SON VERDES CON passes: [] — deriveStatus([]) devuelve
  // 'pending', asi que la igualdad se cumple antes de que corra el quorum.
  test('status coincide con deriveStatus(passes) …: no se puede forjar un validated', () => {
```

**El molde de una excepción bien escrita** — `### 7.4` de
`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` (líneas
313-321): declara la indulgencia y a continuación **«La frontera, y aquí NO hay indulgencia»** con el
caso inverso que sí muerde, cerrando con remisión a `### 7.3`. Tras la `### 7.5` del plan 44-01, la nueva
es `### 7.6`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarea 1 (tracer): `fare-indicativo-300` ↔ `avere` de punta a punta, con la partición base/cruce</name>

  <precondition>El plan 44-01 cerró: `node scripts/run-validation-271.mjs` imprime `VAL-06 (247/247 validated)` con `PASS` y `Milestone gate PASS`, y `node --test tests/*.test.js` cierra con `fail 0`. Si el reporter sigue diciendo `225/225`, los conteos no están enganchados y el rojo honesto de este plan no se distinguiría de la ceguera anterior.</precondition>

  <reversibility rating="costly">D-44-02 (el sentido invertido del cruce) es costly: invertirlo después reescribe las 3 explanations, los 3 pools de `options` y exige re-pase de quórum de los 3 slots. No es one-way, así que no lleva checkpoint de decisión — se registra y se sigue.</reversibility>

  <files>tests/content-fare-indicativo.test.js, content/exercises/fare-indicativo.json, .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md</files>

  <read_first>
    - `tests/content-fare-indicativo.test.js` COMPLETO (700 líneas). Es obligatorio leerlo entero: la partición toca constantes de la cabecera (`:41`, `:69-70`, `:181`) y gates repartidos por 12 `describe`, y los comentarios de `:278-289` y `:600-607` son instrucciones explícitas para quien venga a «arreglar» un gate.
    - `content/exercises/fare-indicativo.json` — el `notes` completo (para apender sin re-editar) y los slots `fare-indicativo-passato-prossimo` (ya lleva `sono fatto` como distractora, relevante para G1) y `fare-indicativo-presente`.
    - `content/exercises/presente-regolare.json` — los slots `presente-regolare-300` (el pool del eje auxiliar) y `-302`/`-303` (el molde del cruce invertido, con su gloss ES).
    - `content/exercises/avere.json` — el paradigma de `avere` en presente, para que el pool de `options` use formas reales.
    - `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` §7 completo, en especial `### 7.3` y `### 7.4`.
    - `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-PATTERNS.md` §«Rotura estructural que el planner DEBE presupuestar» — la tabla línea a línea de los gates que rompen.
  </read_first>

  <behavior>
Los tests se escriben ANTES del contenido, y en este orden, porque la partición es lo que hace posible
que el fichero de contenido crezca sin poner roja la suite:

  - La partición existe y discrimina: `CROSS_IDS` contiene solo `fare-indicativo-300`, `BASE_SLOTS` tiene
    8 elementos y `CROSS_SLOTS` tiene 1; `allVariants()` devuelve 48 entradas y `crossVariants()` 3.
  - Con el cruce en disco, el gate de ids exactos sigue verde porque compara `BASE_SLOTS` con `IDS`.
  - El gate de conteo sigue verde porque suma variantes de `BASE_SLOTS` y da 48.
  - El gate del sufijo de 3 cifras, invertido, pasa a exigir `deepEqual(usados, CROSS_IDS)` y es rojo si
    alguien añade un `-302` no declarado.
  - El gate de «exactamente un pronombre sujeto» y los dos gates 0-gloss («ningún prompt con paréntesis»,
    «ningún prompt menciona el español») siguen verdes sobre los 48 prompts base y NO se aplican al cruce.
  - El `describe` de cruces es rojo si: el cruce no tiene 3 variantes; `categoryIds` no es exactamente
    `['fare-indicativo','avere']` con longitud 2; algún `prompt` no lleva el hueco `___`; algún `prompt`
    no lleva un objeto del conjunto cerrado de 7; algún `prompt` contiene `lo`, `la`, `li` o `le` como
    palabra suelta (G1); alguna opción contiene una forma de `fare` (G1); alguna opción es una forma de la
    blacklist de atestiguadas; algún `options` no tiene 4 elementos distintos; o `correctIndex` es
    constante en el cruce.
  - Los gates de `validation` siguen verdes con `passes: []`, porque `deriveStatus([])` da `pending`.
  </behavior>

  <action>
Tres capas, en este orden: primero la partición del fichero de test, luego el contenido, luego el prompt
del quórum.

**Capa 1 — partición de `tests/content-fare-indicativo.test.js`.** Añadir junto al idiom
`SIMPLE_SLOTS`/`COMPOUND_SLOTS` de la cabecera las constantes `CROSS_IDS` (en esta tarea, un solo
elemento: `fare-indicativo-300`), `BASE_SLOTS` y `CROSS_SLOTS`, derivadas de `SLOTS` por pertenencia a
`CROSS_IDS` con el id COMPLETO —nunca por prefijo, que es ambiguo (D-40-03)—. Redefinir `allVariants()`
para que itere `BASE_SLOTS` en vez de `SLOTS`, con lo que los 14 call-sites existentes quedan acotados a
la base de una sola edición, y añadir `crossVariants()` para el `describe` nuevo. Re-apuntar a
`BASE_SLOTS` los tres gates de `SLOTS` que rompen: el de igualdad de ids con `IDS`, el `reduce` del
conteo total de 48 variantes, y —invirtiéndolo, no borrándolo— el del sufijo numérico de 3 cifras, que
pasa a exigir que los ids con ese sufijo sean exactamente `CROSS_IDS`; actualizar su comentario, que ya
anuncia esta fase. Dejar sobre `SLOTS` los gates que legítimamente gobiernan todo el fichero: tipo
`multiple-choice`, key set de slot y de variante, claves peligrosas del JSON, explanations acentuadas y
los cuatro de coherencia del audit trail de `validation`.

Escribir a continuación un `describe` propio de los cruces que re-aserte los gates que SIGUEN rigiendo
sobre ellos —hueco `___`, `options` de 4 sin duplicados internos, `correctIndex` no constante,
SCOPE-GATE de perífrasis por campo, objeto del conjunto cerrado de 7, blacklist de formas atestiguadas y
ausencia de participio concordado— más los predicados de G1: `categoryIds` de longitud exactamente 2 con
`[0]` el slug completo de la categoría y `[1]` el slug vecino exacto; ausencia de los pronombres objeto
antepuestos como palabra suelta en los `prompt`; y ninguna opción que contenga una forma de `fare`,
verificada por campo sobre `variants[].options[]`. Añadir a la cabecera del fichero una nota que explique
la partición y por qué existe, en la línea de la ADVERTENCIA DE ESCANEO que ya tiene.

**Capa 2 — `content/exercises/fare-indicativo.json`.** Apender el slot `fare-indicativo-300` al final del
array `exercises`, con `categoryIds: ["fare-indicativo", "avere"]`, `type: "multiple-choice"`, 3 variantes
y `validation: { "status": "pending", "passes": [] }`. El marco es un compuesto de `fare` con el
participio `fatto` ESCRITO en el enunciado y el hueco en el auxiliar: queda a discreción cuál compuesto
(passato prossimo, trapassato u otro) mientras `fatto` quede invariable. Las 3 variantes muestrean 3
personas distintas con sujeto pronominal explícito, y cada `prompt` lleva un objeto del conjunto cerrado
de 7 más el gloss ES entre paréntesis, que aquí desambigua sin filtrar porque la key está en la categoría
vecina. Las 4 `options` de cada variante son formas conjugadas de `avere` del tiempo que el marco pide
más una forma de `essere` como distractora de auxiliar; ninguna opción lleva participio dentro ni ninguna
forma de `fare`. Los `prompt` no contienen los pronombres objeto antepuestos, porque con uno de ellos la
concordancia del participio sería obligatoria y la frase tendría segunda lectura defendible.

La explanation va en español acentuado RAE, plain text de un párrafo, apóstrofes ASCII, y explica lo que
el hueco examina —que en los compuestos de `fare` el auxiliar es `avere` y se conjuga, y que el participio
`fatto` no cambia— sin hablar del italiano que el hueco no examina, sin describir la estructura del
ejercicio y sin recorrer las distractoras una por una. No afirma que ninguna construcción sea
«agramatical».

Apender al `notes` un bloque nuevo —nunca re-editar el existente, que es contrato ejecutable— con: por
qué `fare-congiuntivo` y `fare-cond-imperativo` se quedan sin cruce (D-44-01), por qué el sentido está
invertido citando el solape concreto de `Io devo ___ il letto` que ya vive en `fare-indefiniti` (D-44-02),
G1 y G2 verbatim, y la decisión del gloss ES del cruce frente al 0-gloss de la categoría, con la nota de
que el gate 0-gloss queda acotado a los slots base y no relajado.

**Capa 3 — el prompt del quórum.** Añadir `### 7.6` a
`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` (la ruta REAL;
`docs/09-VALIDATION-PROMPT.md` no existe), clonando la estructura de `### 7.4`: título nombrando los
cruces `-300`+ de v2.0 Phase 44 con sus ids; qué NO es falso positivo (que la key viva en la categoría
vecina y que la forma de `fare` vaya escrita en el enunciado ES el diseño, no un leak ni un fuera de tema;
y que el gloss ES entre paréntesis es canon R7 donde desambigua); la frontera con lo que SÍ sigue
mordiendo (C5 sobre la forma verbal del hueco, C2 si el marco no excluye las lecturas alternativas, y el
gloss si llega a cubrir la palabra del blank); y cierre remitiendo a `### 7.3`. Declarar los tres gates
G1, G2 y G3 aquí aunque dos de ellos aterricen en tareas posteriores: el subagent arranca con contexto
vacío y una excepción declarada solo en el `notes` produce el síntoma conocido de marcar un patrón y
aprobar otro idéntico.
  </action>

  <verify>
    <automated>node --test tests/content-fare-indicativo.test.js 2>&amp;1 | tail -6 &amp;&amp; node --test tests/*.test.js 2>&amp;1 | tail -6 &amp;&amp; node scripts/run-validation-271.mjs; echo "reporter-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-indicativo.test.js` cierra con `fail 0`.
    - `node --test tests/*.test.js` cierra con `fail 0` y `pass` estrictamente mayor que el conteo con que cerró el plan 44-01.
    - `node scripts/run-validation-271.mjs` sale con código distinto de 0 y su salida contiene `VAL-06 (248/248 validated): FAIL (247/248 — pending=1` y `Milestone gate FAIL`. **Es el resultado esperado**, no un fallo de la tarea.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla EXACTAMENTE en el test de `content/exercises/fare-indicativo.json` nombrando `fare-indicativo-300(pending)`, y en ningún otro fichero de contenido.
    - Estructura del slot: `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-300'); console.log(JSON.stringify(s.categoryIds), s.variants.length, s.validation.status, s.validation.passes.length, Object.keys(s).sort().join(','))"` imprime `[\"fare-indicativo\",\"avere\"] 3 pending 0 categoryIds,explanation,id,type,validation,variants`.
    - G1, pronombre objeto antepuesto ausente: `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-300'); const re=/(^|[^\p{L}])(lo|la|li|le)([^\p{L}]|$)/iu; const sucio=s.variants.filter(v=>re.test(v.prompt)).map(v=>v.prompt); console.log(sucio.length, sucio.join(' | '))"` imprime `0` y nada más.
    - G1, ninguna forma de `fare` en `options`: `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-300'); const re=/^(f|fa|fe)/i; const sucio=s.variants.flatMap(v=>v.options).filter(o=>o.split(/\s+/).some(w=>re.test(w))); console.log(sucio.length, sucio.join(' | '))"` imprime `0` y nada más.
    - G1, el participio va escrito en el enunciado: `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-300'); console.log(s.variants.every(v=>/(^|\W)fatto(\W|$)/.test(v.prompt)), s.variants.every(v=>v.prompt.includes('___')))"` imprime `true true`.
    - Objeto del conjunto cerrado en las 3 variantes: `node -e "const O=['i compiti','un errore','il lavoro','una torta','il letto','tutto','una foto']; const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-300'); console.log(s.variants.every(v=>O.some(o=>v.prompt.includes(o))))"` imprime `true`.
    - `options` de 4 sin duplicados y `correctIndex` no constante: `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-300'); console.log(s.variants.every(v=>v.options.length===4&&new Set(v.options).size===4), new Set(s.variants.map(v=>v.correctIndex)).size>1)"` imprime `true true`.
    - La partición existe con el id completo: `grep -c "CROSS_IDS" tests/content-fare-indicativo.test.js` da al menos `4`, `grep -c "BASE_SLOTS" tests/content-fare-indicativo.test.js` da al menos `4`, y `grep -cF "'fare-indicativo-300'" tests/content-fare-indicativo.test.js` da al menos `1`.
    - `allVariants()` está acotado a la base: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8'); console.log(/const allVariants = \(\) => BASE_SLOTS\./.test(s))"` imprime `true`.
    - El gate del sufijo quedó invertido y no borrado: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8'); console.log(/-\\\\d\{3\}\$/.test(s), /deepEqual\(usados, CROSS_IDS\)/.test(s))"` imprime `true true`.
    - Ningún gate desapareció: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8'); console.log((s.match(/^\s*test\(/gm)||[]).length)"` da un número estrictamente mayor que el del fichero antes de la tarea (registrar el valor previo en el SUMMARY).
    - El `notes` creció por append: `git diff --numstat content/exercises/fare-indicativo.json` muestra 0 líneas borradas en la región del `notes` (verificable con `git diff -U0 content/exercises/fare-indicativo.json | grep -c '^-[^-]'` dando `0`).
    - `grep -c '### 7.6' .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` da `1`, y esa sección nombra los tres ids de cruce y remite a `7.3`.
    - `ls docs/09-VALIDATION-PROMPT.md 2>/dev/null | wc -l` da `0`.
    - `git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` no produce salida y `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` da `2`.
  </acceptance_criteria>

  <done>`fare-indicativo-300` está en disco con 3 variantes `pending`, la partición base/cruce está montada, TODOS los gates del fichero de test siguen mordiendo sobre la partición que les corresponde, el cruce tiene su propio bloque de invariantes con G1, y el prompt del quórum declara los 3 gates. El suite está verde y el reporter está rojo diciendo la verdad.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: `fare-indicativo-301` ↔ `presente-regolare` con el gate G2</name>

  <files>content/exercises/fare-indicativo.json, tests/content-fare-indicativo.test.js</files>

  <read_first>
    - `tests/content-fare-indicativo.test.js` — la partición y el `describe` de cruces creados en la Tarea 1
    - `content/exercises/fare-indicativo.json` — el slot `fare-indicativo-300` recién escrito (para clonar tono, key set y forma del `notes`) y el slot `fare-indicativo-presente` (que ya examina `faccio`, para no solaparse)
    - `content/exercises/presente-regolare.json` — el paradigma de los verbos regulares y los pools de `-302`/`-303`, que son literalmente el patrón de G2
    - `content/categories.json` — para confirmar que `presente-regolare` está registrada (`order` 10)
  </read_first>

  <behavior>
    - `CROSS_IDS` pasa a tener 2 elementos y `crossVariants()` devuelve 6; `BASE_SLOTS` sigue en 8 y `allVariants()` en 48, así que ningún gate de paradigma se mueve.
    - El gate invertido del sufijo exige ahora los DOS ids y es rojo si falta uno.
    - Los gates del `describe` de cruces se aplican a los 2 sin edición (iteran `crossVariants()`), incluido el de `categoryIds` de longitud 2, que ahora comprueba la pareja correcta POR CRUCE y no una pareja única para todos.
    - Gates propios de G2, rojos si se violan: algún `prompt` de `-301` no lleva sujeto pronominal explícito en las DOS cláusulas; los dos sujetos de una misma frase son la misma persona; las 4 `options` de una variante no son 4 personas del MISMO verbo regular (es decir, comparten raíz y difieren solo en terminación); o alguna opción es una forma de `fare`.
  </behavior>

  <action>
Apender `fare-indicativo-301` al array `exercises` de `content/exercises/fare-indicativo.json`, con
`categoryIds: ["fare-indicativo", "presente-regolare"]`, `type: "multiple-choice"`, 3 variantes y
`validation: { "status": "pending", "passes": [] }`. El marco es una frase de dos cláusulas: en la
primera va ESCRITA una forma de `fare` en presente y en la segunda está el hueco, que pide el presente de
un verbo REGULAR. Las 3 variantes muestrean 3 personas distintas para el hueco.

G2 al pie de la letra: sujeto pronominal explícito en las DOS cláusulas y con personas DISTINTAS entre sí,
de modo que la persona del hueco quede fijada sin ambigüedad; UN SOLO verbo regular candidato, fijado por
el objeto literal del conjunto cerrado de 7 que lleva la cláusula del hueco; y las 4 `options` son 4
personas del MISMO verbo regular, nunca verbos distintos —si el pool mezclara verbos, más de uno sería
defendible por la regla de las LECTURAS ratificada el 2026-08-03—. Queda a discreción qué verbo regular
concreto examina cada variante dentro del paradigma de `presente-regolare` (`-are` / `-ere` / `-ire` /
`-isc-` / velar / palatal), siempre que el objeto lo fije. Cada `prompt` lleva el gloss ES entre
paréntesis, que aquí desambigua el sentido de la segunda cláusula sin regalar la persona.

La explanation, en el tono de `presente-regolare-302`: dice que el enunciado ya trae escrita la forma
irregular de `fare` como contexto y que el hueco pide el presente regular del otro verbo, y cierra con la
frase que hace de contrato del pool —las distractoras son otras personas del presente del mismo verbo, y
solo una concuerda con el sujeto—. Español acentuado RAE, plain text, apóstrofes ASCII, sin hablar del
italiano que el hueco no examina, sin describir la estructura del ejercicio, sin recorrer distractoras una
por una y sin afirmar que nada es «agramatical».

En `tests/content-fare-indicativo.test.js`: añadir `fare-indicativo-301` a `CROSS_IDS` y ampliar el
`describe` de cruces con los gates propios de G2 —sujeto pronominal explícito en las dos cláusulas con
personas distintas, y las 4 opciones como personas del mismo verbo regular—, aplicados solo al cruce que
los pide. Si el gate de `categoryIds` de la Tarea 1 fijaba una pareja única, generalizarlo a un mapa de
id de cruce a pareja esperada, para que los dos queden congelados con la pareja que de verdad les toca.

Apender al `notes` la línea que le falte sobre G2 si la Tarea 1 no lo dejó completo; no re-editar nada.
  </action>

  <verify>
    <automated>node --test tests/content-fare-indicativo.test.js 2>&amp;1 | tail -6 &amp;&amp; node --test tests/*.test.js 2>&amp;1 | tail -6 &amp;&amp; node scripts/run-validation-271.mjs | grep -F 'VAL-06 (249/249 validated)'; echo "grep-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/*.test.js` cierra con `fail 0`.
    - `node scripts/run-validation-271.mjs` imprime `VAL-06 (249/249 validated): FAIL (247/249 — pending=2` y `Milestone gate FAIL` — el rojo esperado, con el conteo subiendo solo porque los `expected` son dinámicos.
    - Estructura: `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-301'); console.log(JSON.stringify(s.categoryIds), s.variants.length, s.validation.status, s.validation.passes.length)"` imprime `[\"fare-indicativo\",\"presente-regolare\"] 3 pending 0`.
    - G2, sujeto explícito en las DOS cláusulas con personas distintas: `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-301'); const RE=/\b(io|tu|lui|lei|noi|voi|loro)\b/gi; const r=s.variants.map(v=>(v.prompt.match(RE)||[]).map(x=>x.toLowerCase())); console.log(r.every(h=>h.length===2), r.every(h=>h[0]!==h[1]), JSON.stringify(r))"` imprime `true true` seguido del detalle.
    - G2, 4 personas del MISMO verbo regular (mismo prefijo de raíz de al menos 3 caracteres en las 4 opciones): `node -e "const s=require('./content/exercises/fare-indicativo.json').exercises.find(e=>e.id==='fare-indicativo-301'); const raiz=o=>o.slice(0,3).toLowerCase(); console.log(s.variants.every(v=>new Set(v.options.map(raiz)).size===1), s.variants.map(v=>v.options.join('/')).join(' | '))"` imprime `true` seguido de los pools.
    - Ninguna forma de `fare` en `options`: el mismo comando de la Tarea 1 aplicado a `-301` imprime `0`.
    - Objeto del conjunto cerrado y hueco presentes en las 3 variantes: `true true` con los comandos equivalentes de la Tarea 1.
    - `CROSS_IDS` tiene los 2 ids completos: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8'); const m=s.match(/const CROSS_IDS = \[[^\]]*\]/)[0]; console.log(m)"` muestra `'fare-indicativo-300'` y `'fare-indicativo-301'` y nada más.
    - `node -e "const c=require('./content/exercises/fare-indicativo.json'); console.log(c.exercises.length, c.exercises.filter(e=>/-\d{3}$/.test(e.id)).length)"` imprime `10 2`.
    - `git diff -U0 content/exercises/fare-indicativo.json | grep -c '^-[^-]'` da `0` en la región del `notes` (append puro).
    - `git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` no produce salida.
  </acceptance_criteria>

  <done>`fare-indicativo-301` está en disco con 3 variantes `pending` que cumplen G2 de forma verificable —dos sujetos explícitos distintos por frase y 4 personas del mismo verbo regular por pool—, sus invariantes están congelados en el `describe` de cruces, y la suite sigue verde con el reporter en `247/249`.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: `fare-indefiniti-300` ↔ `modali` con el gate G3, y la partición del segundo fichero de test</name>

  <reversibility rating="costly">D-44-01 (la lista cerrada en 3 cruces, alojados por contenido) es costly: quitar un cruce después exige borrar variantes ya validadas por quórum, y si el autor ya las ha fallado sus ids viven en su `exerciseStats`. No es one-way; se registra y se sigue.</reversibility>

  <files>tests/content-fare-indefiniti.test.js, content/exercises/fare-indefiniti.json</files>

  <read_first>
    - `tests/content-fare-indefiniti.test.js` COMPLETO (1939 líneas). Es el fichero de test más grande del proyecto y tiene 13 call-sites de `allVariants()` y 8 de `SLOTS`, así que la partición no se puede hacer a ciegas: hay gates de POOL CERRADO de formas no personales (líneas 985 y 991) que el cruce viola por diseño, porque su key es un modal conjugado.
    - `content/exercises/fare-indefiniti.json` — el `notes` completo (los gates `CONTENT.notes.includes(...)` de las líneas 909-970 lo convierten en contrato ejecutable: apender, nunca re-editar) y el slot `fare-indefiniti-infinito-presente`, cuya variante 3 es literalmente `Io devo ___ il letto` y es EL solape que motiva D-44-02.
    - `content/exercises/modali.json` — el slot `modali-300` completo, y el paradigma de `dovere`, `potere` y `volere` en presente.
    - `tests/content-fare-indicativo.test.js` — la partición y el `describe` de cruces de las Tareas 1 y 2, como molde a replicar.
    - `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-PATTERNS.md` §«Rotura estructural» — la tabla de `content-fare-indefiniti.test.js` (líneas 504, 534, 540, 578 y los escaneos de 1413 en adelante).
  </read_first>

  <behavior>
    - La partición de `tests/content-fare-indefiniti.test.js` deja `CROSS_IDS` con un elemento, `BASE_SLOTS` con 6 y `CROSS_SLOTS` con 1; `allVariants()` devuelve 18 y `crossVariants()` 3.
    - Con el cruce en disco siguen verdes: el gate de ids exactos, el de CONTEO DESIGUAL 3/3/4/2/3/3 con 18 en total, el de `categoryIds` de longitud 1 (ahora sobre `BASE_SLOTS`), y los dos gates de POOL CERRADO de formas no personales de las líneas 985 y 991.
    - El gate del sufijo de 3 cifras, invertido, exige `deepEqual(usados, CROSS_IDS)`.
    - Gates propios de G3, rojos si se violan: `categoryIds` no es exactamente `['fare-indefiniti','modali']` con longitud 2; el cruce no tiene 3 variantes; alguna `option` contiene una forma de `fare`; las 4 `options` de una variante no son todas formas conjugadas de un modal; algún `prompt` no lleva el hueco, o no lleva objeto del conjunto cerrado, o no lleva el complemento explícito que excluye dos de los tres modales (declarado como conjunto cerrado de marcadores en el propio test, no juzgado a ojo).
    - Los gates de `validation` y los de `CONTENT.notes.includes(...)` siguen verdes.
  </behavior>

  <action>
**Capa 1 — partición de `tests/content-fare-indefiniti.test.js`.** Clonar exactamente la forma de la
Tarea 1: `CROSS_IDS` con `fare-indefiniti-300` (slug COMPLETO; `fare-ind` es ambiguo, D-40-03),
`BASE_SLOTS`, `CROSS_SLOTS`, `allVariants()` redefinido sobre `BASE_SLOTS` —una sola edición en la línea
del helper acota los 13 call-sites de golpe— y `crossVariants()` nuevo. Re-apuntar a `BASE_SLOTS` los
cuatro gates de `SLOTS` que rompen: la igualdad de ids con `IDS`, el `reduce` del conteo total contra
`TOTAL_VARIANTES`, el de `categoryIds` de longitud 1 con el slug completo, y —invirtiéndolo— el del
sufijo numérico de 3 cifras. Dejar sobre `SLOTS` los que gobiernan todo el fichero: tipo
`multiple-choice`, key set, `options` de 4 sin duplicados con `correctIndex` en rango y no constante,
explanations, claves peligrosas y los cuatro de coherencia de `validation`. Verificar uno a uno los ocho
gates de `SLOTS` de la segunda mitad del fichero (a partir de la línea 1400) y decidir para cada uno si
gobierna la base o todo: los de `validation` sobreviven con `passes: []`, los de contenido específico del
paradigma no personal van a `BASE_SLOTS`.

Escribir el `describe` propio del cruce con los gates que siguen rigiendo —hueco, `options` de 4 sin
duplicados, SCOPE-GATE de perífrasis por campo (el conjunto de este fichero incluye `far fare` y
`causativo`), objeto del conjunto cerrado de 7, blacklist— más los predicados de G3: `categoryIds` de
longitud exactamente 2 con la pareja exacta; ninguna opción con una forma de `fare`; las 4 opciones como
formas conjugadas de modales; y la presencia, en cada `prompt`, de un marcador del conjunto CERRADO de
complementos desambiguadores que el propio test declara como constante, para que el gate sea mecánico y
no un juicio.

**Capa 2 — `content/exercises/fare-indefiniti.json`.** Apender el slot `fare-indefiniti-300` con
`categoryIds: ["fare-indefiniti", "modali"]` —`modali`, NUNCA `verbi-modali`, que haría rechazar el
fichero entero—, `type: "multiple-choice"`, 3 variantes y `validation: { "status": "pending", "passes": [] }`.
El marco lleva `fare` ESCRITO en infinitivo, gobernado por el modal, y el hueco está en el modal
conjugado. Las 3 variantes muestrean 3 personas distintas con sujeto pronominal explícito.

G3 es el gate más delicado de la fase y es fácil de subestimar: `devo`, `posso` y `voglio` son
intercambiables en una frase genérica, así que **cada `prompt` necesita un complemento explícito que
excluya dos de los tres** dejando solo obligación, permiso o voluntad. El gloss ES solo no basta como red
única —es el punto donde el molde `modali-300` se queda corto—. Queda a discreción qué complemento
concreto lo hace en cada variante y qué objeto del conjunto cerrado de 7 lleva cada frase; la exigencia es
que el complemento esté en el enunciado y que uno de los tres marcadores del conjunto declarado en el test
lo reconozca. Las 4 `options` son formas conjugadas de modales y nada más: **ninguna forma de `fare`**, que
es la divergencia obligatoria respecto a `modali-300`, el cual sí mete la forma conjugada del verbo
gobernado como distractora.

La explanation dice lo que el hueco examina —que el modal se conjuga y gobierna el infinitivo, que aquí el
infinitivo `fare` ya viene dado, y que el complemento de la frase decide cuál de los tres modales encaja—
en español acentuado RAE, plain text, apóstrofes ASCII, sin hablar del italiano ajeno al hueco, sin
describir la estructura del ejercicio, sin recorrer distractoras una por una y sin afirmar que nada es
«agramatical».

Apender al `notes` un bloque nuevo —los gates `CONTENT.notes.includes(...)` hacen del `notes` contrato
ejecutable, así que no se re-edita ni una línea— con: el cruce y su pareja, G3 verbatim, por qué el sentido
está invertido citando que `Io devo ___ il letto` con key `fare` ya existe en este mismo fichero (D-44-02,
D-43-12), la decisión del gloss ES, y por qué `fare-congiuntivo` y `fare-cond-imperativo` se quedan sin
cruce.
  </action>

  <verify>
    <automated>node --test tests/content-fare-indefiniti.test.js 2>&amp;1 | tail -6 &amp;&amp; node --test tests/*.test.js 2>&amp;1 | tail -6 &amp;&amp; node scripts/run-validation-271.mjs | grep -F 'VAL-06 (250/250 validated)'; echo "grep-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-indefiniti.test.js` cierra con `fail 0`.
    - `node --test tests/*.test.js` cierra con `fail 0`.
    - `node scripts/run-validation-271.mjs` sale con código distinto de 0 y su salida contiene `VAL-06 (250/250 validated): FAIL (247/250 — pending=3, missing=0, disputed=0)` y `Milestone gate FAIL`. **Es el estado esperado y es el entregable del plan.**
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla EXACTAMENTE en los tests de `content/exercises/fare-indicativo.json` y `content/exercises/fare-indefiniti.json`, nombrando los 3 ids de cruce como `pending`, y en ningún otro fichero.
    - Estructura: `node -e "const s=require('./content/exercises/fare-indefiniti.json').exercises.find(e=>e.id==='fare-indefiniti-300'); console.log(JSON.stringify(s.categoryIds), s.variants.length, s.validation.status, s.validation.passes.length, Object.keys(s).sort().join(','))"` imprime `[\"fare-indefiniti\",\"modali\"] 3 pending 0 categoryIds,explanation,id,type,validation,variants`.
    - El slug vecino es el real: `grep -cF '"verbi-modali"' content/exercises/fare-indefiniti.json` da `0`, y el `notes` no lo cita tampoco entre comillas dobles.
      <!-- planner-discipline-allow: verbi-modali -->
    - G3, ninguna forma de `fare` en `options`: `node -e "const s=require('./content/exercises/fare-indefiniti.json').exercises.find(e=>e.id==='fare-indefiniti-300'); const re=/^(f|fa|fe)/i; const sucio=s.variants.flatMap(v=>v.options).filter(o=>o.split(/\s+/).some(w=>re.test(w))); console.log(sucio.length, sucio.join(' | '))"` imprime `0` y nada más.
    - G3, las 4 opciones son modales conjugados: `node -e "const M=/^(dev|dov|poss|pu|vogl|vuo|vol)/i; const s=require('./content/exercises/fare-indefiniti.json').exercises.find(e=>e.id==='fare-indefiniti-300'); console.log(s.variants.every(v=>v.options.every(o=>M.test(o))), s.variants.map(v=>v.options.join('/')).join(' | '))"` imprime `true` seguido de los pools.
    - G3, `fare` va escrito en el enunciado y el hueco existe: `node -e "const s=require('./content/exercises/fare-indefiniti.json').exercises.find(e=>e.id==='fare-indefiniti-300'); console.log(s.variants.every(v=>/(^|\W)fare(\W|$)/.test(v.prompt)), s.variants.every(v=>v.prompt.includes('___')))"` imprime `true true`.
    - G3, el complemento desambiguador es mecánicamente reconocible: el `describe` de cruce declara una constante con el conjunto CERRADO de marcadores y su test correspondiente pasa — `grep -cE "complement|desambigu|EXCLU" tests/content-fare-indefiniti.test.js` da al menos `1` y el nombre del test lo menciona.
    - Objeto del conjunto cerrado en las 3 variantes: `node -e "const O=['i compiti','un errore','il lavoro','una torta','il letto','tutto','una foto']; const s=require('./content/exercises/fare-indefiniti.json').exercises.find(e=>e.id==='fare-indefiniti-300'); console.log(s.variants.every(v=>O.some(o=>v.prompt.includes(o))))"` imprime `true`.
    - Las tres personas son distintas: `node -e "const s=require('./content/exercises/fare-indefiniti.json').exercises.find(e=>e.id==='fare-indefiniti-300'); const RE=/\b(io|tu|lui|lei|noi|voi|loro)\b/gi; const p=s.variants.map(v=>(v.prompt.match(RE)||[])[0].toLowerCase()); console.log(new Set(p).size===3, p.join(','))"` imprime `true` seguido de las personas.
    - `node -e "const c=require('./content/exercises/fare-indefiniti.json'); console.log(c.exercises.length, c.exercises.filter(e=>/-\d{3}$/.test(e.id)).length)"` imprime `7 1`.
    - Partición presente y `allVariants()` acotado: `node -e "const s=require('fs').readFileSync('tests/content-fare-indefiniti.test.js','utf8'); console.log(/const allVariants = \(\) => BASE_SLOTS\./.test(s), /deepEqual\(usados, CROSS_IDS\)/.test(s))"` imprime `true true`.
    - Ningún gate desapareció: el conteo de `test(` de `tests/content-fare-indefiniti.test.js` es estrictamente mayor que antes de la tarea (valor previo registrado en el SUMMARY).
    - El `notes` creció por append: `git diff -U0 content/exercises/fare-indefiniti.json | grep -c '^-[^-]'` da `0`.
    - Los 3 cruces existen y ninguno está `validated`: `node -e "const ids=['fare-indicativo-300','fare-indicativo-301','fare-indefiniti-300']; const f={'fare-indicativo':require('./content/exercises/fare-indicativo.json'),'fare-indefiniti':require('./content/exercises/fare-indefiniti.json')}; const all=[...f['fare-indicativo'].exercises,...f['fare-indefiniti'].exercises]; const s=ids.map(i=>all.find(e=>e.id===i)); console.log(s.length, s.every(x=>x&&x.validation.status==='pending'&&x.validation.passes.length===0), s.reduce((a,x)=>a+x.variants.length,0))"` imprime `3 true 9`.
    - `git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` no produce salida y `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` da `2`.
  </acceptance_criteria>

  <done>Los 3 cruces están en disco (9 variantes, todas `pending` con `passes: []`), los dos ficheros de test están particionados con TODOS sus gates re-apuntados y ninguno debilitado, los cruces tienen sus invariantes G1/G2/G3 congelados, la suite está verde y el reporter está en `VAL-06 (250/250): FAIL (247/250 — pending=3)`, que es el rojo honesto del hand-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 4 (checkpoint): hand-off del quórum TOP-LEVEL sobre los 3 cruces</name>

  <what-built>
Los 3 cruces multi-categoría de `fare` están en disco con 9 variantes, sus gates G1/G2/G3 congelados como
invariantes permanentes en los dos ficheros de test, el `notes` de los dos ficheros de contenido apendido
con las decisiones, y `### 7.6` del prompt de validación declarando los tres gates para el subagent del
quórum.

Los 3 cruces están en `validation.status: "pending"` con `passes: []`. **Esto es deliberado y no se puede
arreglar desde aquí:** `gsd-executor` no puede spawnear los Task subagents que el skill
`gsd-validate-exercise` necesita (D-44-11, invariante heredado de D-41-15 / D-42-04 / D-43-02), y un pase
inventado destruiría la única evidencia que tienes de que una variante fue revisada.

**El estado ROJO del marcador es el entregable, no un fallo.** Ahora mismo y por diseño:
`node scripts/run-validation-271.mjs` → `VAL-06 (250/250 validated): FAIL (247/250 — pending=3)` y
`Milestone gate FAIL`; `VAL_07_STRICT=1 node --test tests/*.test.js` → rojo en los dos ficheros de `fare`
tocados. `node --test tests/*.test.js` sigue en `fail 0`. Nadie debe «arreglar» ese rojo tocando contenido
ni relajando un gate: se cierra estampando el quórum.
  </what-built>

  <how-to-verify>
**1. Comprueba el estado del hand-off** (debe salir `3 true 9`):

```
node -e "const all=[...require('./content/exercises/fare-indicativo.json').exercises,...require('./content/exercises/fare-indefiniti.json').exercises]; const s=['fare-indicativo-300','fare-indicativo-301','fare-indefiniti-300'].map(i=>all.find(e=>e.id===i)); console.log(s.length, s.every(x=>x.validation.status==='pending'&&x.validation.passes.length===0), s.reduce((a,x)=>a+x.variants.length,0))"
```

**2. Lee las 9 variantes** y decide si el contenido te sirve como ejercicio antes de gastar quórum en él:

```
node -e "for(const f of ['fare-indicativo','fare-indefiniti']){for(const e of require('./content/exercises/'+f+'.json').exercises.filter(x=>/-\d{3}$/.test(x.id))){console.log('==='+e.id, JSON.stringify(e.categoryIds)); console.log(e.explanation); e.variants.forEach((v,k)=>console.log(k, v.prompt, '->', v.options.join(' | '), 'key='+v.options[v.correctIndex]));}}"
```

Fíjate en particular en `fare-indefiniti-300`: ¿el complemento de cada frase EXCLUYE de verdad dos de los
tres modales, o los tres siguen siendo defendibles? Es el gate más delicado de la fase (G3) y el que un
quórum caro debería no tener que descubrir.

**3. Cuando el contenido te valga, lanza el quórum base TOP-LEVEL** — desde el nivel superior, NO dentro
de un executor, **un ejercicio por contexto fresco y NUNCA en lote** (VAL-03), Opus + Sonnet:

```
/gsd-validate-exercise fare-indicativo-300
/gsd-validate-exercise fare-indicativo-301
/gsd-validate-exercise fare-indefiniti-300
```

`deriveStatus` exige ≥2 pases `correcta` con `by` DISTINTOS y cero `incorrecta` para `validated`;
cualquier `incorrecta` da `disputed` STICKY. No hay ronda extra cross-vendor prevista (D-44-04 pone la red
en los gates), pero `scripts/validate-ai-pass.mjs` está disponible si algo queda `disputed`.

**4. Gate de cierre del milestone**, cuando los 3 estén `validated`:

```
node --test tests/*.test.js                                    # fail 0
VAL_07_STRICT=1 node --test tests/*.test.js                     # fail 0
node scripts/run-validation-271.mjs                             # VAL-06 (250/250): PASS + Milestone gate PASS
git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/        # sin salida  (NO añadir src/data/)
grep -c 'applyImmediateFailure(this.state' src/screens/app.js   # 2
```

Y después, ya fuera de esta fase: `/gsd-complete-milestone v2.0`.
  </how-to-verify>

  <resume-signal>Escribe "aprobado" para cerrar el plan con los 3 cruces en `pending` y el hand-off registrado, o describe qué variante hay que reescribir antes de gastar quórum en ella.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

La superficie real de este proyecto: web estática monousuario, sin servidor, sin red en runtime, sin
cuentas y sin entrada de usuario más allá de `localStorage`. No hay inyección, ni authz, ni transporte que
proteger. Lo que sí es riesgo real y demostrado es la **integridad del contenido y de la evidencia de
revisión**: un ejercicio injusto y un audit trail que no se puede creer.

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano → `content-loader.js` → DOM (`prompt`, `options`, `explanation`) | contenido de confianza pero renderizado: markdown, smart-quotes o HTML crudo degradarían el render |
| `validation.passes[]` → `deriveStatus` → decisión del autor de dar una categoría por revisada | el audit trail es la ÚNICA evidencia de revisión; falsificarlo es indetectable a ojo |
| `categoryIds` → `applyResultToSession` → cascada D-54 | un cruce amplía el radio de daño de un ejercicio injusto de 1 categoría a 2 |
| `localStorage` → `hydrateV13` → estado | única persistencia; este plan NO la cruza (no toca `src/data/`) |

## STRIDE Threat Register

ASVS nivel 1 · bloqueo en `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-07 | Repudiation | `validation.passes[]` de los 3 cruces | high | mitigate | los 3 cierran en `pending` con `passes: []` (Tareas 1-3); el gate `status === deriveStatus(passes)` de los dos ficheros de test hace imposible commitear un `validated` fabricado; el quórum real se estampa en la pasada top-level del checkpoint de la Tarea 4 |
| T-44-08 | Tampering | los gates verdes existentes de `tests/content-fare-indicativo.test.js` y `tests/content-fare-indefiniti.test.js` | high | mitigate | la partición re-apunta cada gate a la partición que gobierna sin bajarle la barra, se verifica que el conteo de `test(` de cada fichero CRECE, y el `describe` de cruces re-aserta explícitamente los gates que siguen rigiendo sobre los cruces; prohibición explícita de relajar un gate para que el contenido quepa |
| T-44-09 | Tampering | `explanation` / `prompt` / `options` renderizados en el DOM | medium | mitigate | los cruces entran automáticamente en el smoke de `tests/exercise-types.test.js` (`CATEGORIES_WITH_EXPLANATIONS` ya incluye los dos ficheros), que prohíbe markdown y smart-quotes (T-02-01, anti-XSS) y exige apóstrofes ASCII |
| T-44-10 | Tampering | claves peligrosas en el JSON de contenido (`__proto__`, `constructor`, `prototype`) | medium | mitigate | el gate de claves peligrosas de los dos ficheros de test itera `SLOTS` completo y por tanto cubre los cruces sin edición. Nota de canon: prototype-pollution es canon de seguridad — cubierto por `/gsd-secure-phase` y por ese gate; no se acuña prohibición nueva |
| T-44-11 | Denial of service | ejercicio con más de una respuesta defendible → `disputed` sticky → reset de 2 categorías | medium | mitigate | G1/G2/G3 declarados ANTES de escribir y convertidos en gates mecánicos por cruce (pronombre objeto ausente, 4 personas del mismo verbo regular, complemento desambiguador del conjunto cerrado); el quórum es la red, no el mecanismo |
| T-44-12 | Information disclosure | datos del autor | low | accept | no hay datos personales, ni red, ni cuentas, ni telemetría: `localStorage` de una sola persona en `http://localhost` |
| T-44-13 | Elevation of privilege | superficie de ejecución | low | accept | sin backend, sin autenticación y sin roles: no existe privilegio que elevar |
| T-44-SC | Tampering | instalación de paquetes npm/pip/cargo | n/a | accept | **este plan no instala ningún paquete.** Dependencias cero por constraint (CLAUDE.md §Constraints: web estática, sin `npm install`), sin `package.json` de runtime, y ninguna tarea ejecuta un gestor de paquetes. La puerta de legitimidad de paquetes no aplica; si una tarea futura la necesitara, exige antes la tabla `## Package Legitimacy Audit` en RESEARCH.md |
</threat_model>

<artifacts_produced>
## Artefactos que produce este plan

**Ids de ejercicio nuevos** (los 3 únicos de la fase; la lista queda CERRADA, D-44-05)
- `fare-indicativo-300` — `categoryIds: ["fare-indicativo", "avere"]` · 3 variantes · gate G1
- `fare-indicativo-301` — `categoryIds: ["fare-indicativo", "presente-regolare"]` · 3 variantes · gate G2
- `fare-indefiniti-300` — `categoryIds: ["fare-indefiniti", "modali"]` · 3 variantes · gate G3

**Constantes y helpers nuevos, en los DOS ficheros de test**
- `CROSS_IDS` — los ids de cruce del fichero, con el slug COMPLETO (nunca `fare-ind*`)
- `BASE_SLOTS` / `CROSS_SLOTS` — la partición de `SLOTS`
- `crossVariants()` — el iterador del cruce, gemelo de `allVariants()`
- `allVariants()` — REDEFINIDO para iterar `BASE_SLOTS` en vez de `SLOTS` (misma firma, mismo nombre, 14 y 13 call-sites acotados de una edición)
- En `tests/content-fare-indefiniti.test.js`, la constante del conjunto CERRADO de marcadores de complemento desambiguador de G3

**`describe` nuevos**
- `tests/content-fare-indicativo.test.js` — el bloque de invariantes de los cruces `-300`+ con G1 y G2
- `tests/content-fare-indefiniti.test.js` — el bloque de invariantes del cruce `-300` con G3

**Gates existentes MODIFICADOS (re-apuntados o invertidos, nunca borrados)**
- `tests/content-fare-indicativo.test.js`: igualdad de ids con `IDS`, `reduce` del conteo de 48, sufijo numérico de 3 cifras (INVERTIDO), «exactamente un pronombre sujeto», y los dos gates 0-gloss (paréntesis y mención del español)
- `tests/content-fare-indefiniti.test.js`: igualdad de ids, `reduce` contra `TOTAL_VARIANTES`, `categoryIds` de longitud 1, sufijo numérico de 3 cifras (INVERTIDO), y los gates de POOL CERRADO de formas no personales

**Bloques de `notes` apendidos**
- `content/exercises/fare-indicativo.json` y `content/exercises/fare-indefiniti.json` — la decisión de omisión de los cruces de `fare-congiuntivo`/`fare-cond-imperativo`, el sentido invertido con el solape citado, G1/G2/G3 verbatim, y el gloss ES del cruce

**Secciones nuevas de documento**
- `### 7.6` en `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md`

**Lo que este plan NO crea**
- Ninguna entrada nueva en `content/categories.json`, en `CATEGORIES`, en `REAL_CATEGORIES` ni en `CATEGORIES_WITH_EXPLANATIONS`: `avere`, `modali` y `presente-regolare` ya están registradas y los conteos son dinámicos, así que suben solos
- Ningún cuarto cruce, ningún cruce imperativo ↔ congiuntivo, ningún slot ni variante del paradigma de `fare`
- Ningún pase de `validation`: los 3 cruces se entregan `pending` al quórum top-level
</artifacts_produced>

<flagged_assumptions>
## Supuestos marcados (probe spec-less, sin resolver)

Esta fase no tiene SPEC.md, así que la sonda de edge corrió sobre INT-01..INT-04. De sus 8 filas, 5 (las
de INT-02) están authoradas como predicados en `must_haves.truths` del plan 44-01. Las 3 restantes
salieron `unclassified` y se quedan **`unresolved`**: se marcan y NUNCA se descartan en silencio. INT-01
está marcada en 44-01; aquí van las otras dos.

- **INT-03 — `unclassified`.** La sonda no pudo clasificar el requisito, cuyo texto además contiene dos
  datos falsos: escribe el slug `verbi-modali` (el real es `modali`) y describe el sentido del cruce al
  revés del que se implementa. **Supuestos de este plan:** (1) el slug correcto es `modali` y el requisito
  se lee corregido (D-44-08 corrige el texto en el plan 44-01); (2) «reusando `applyResultToSession`» se
  verifica por la vía negativa —`grep -c` de `applyImmediateFailure` sigue en 2 y el diff del motor sigue
  vacío—, **NO** ejecutando una sesión real y comprobando que fallar `fare-indicativo-300` resetea `avere`.
  Esa comprobación de comportamiento es UAT humano y queda fuera del plan; el precedente vivo es
  `presente-regolare-300`, que ya hace exactamente eso en producción.
- **INT-04 — `unclassified`.** El requisito pide «todas las variantes nuevas validadas 1-por-1 por quórum
  cross-vendor R1-R7 con rondas EXTRA en los magnets». **Supuestos:** (1) su mitad documental (declarar los
  4 magnets, que ya tienen su ronda extra ejecutada en disco) se cierra en el plan 44-01; (2) su mitad
  activa —los 3 cruces— **NO se cierra dentro de este plan**: los cruces se entregan `pending` y el quórum
  se estampa en la pasada TOP-LEVEL del checkpoint de la Tarea 4, porque `gsd-executor` no puede spawnear
  los Task subagents del skill (D-44-11). Por tanto **INT-04 NO estará cumplido al terminar 44-02**, y el
  rojo del reporter es la prueba honesta de ello. Quien verifique la fase debe contar el quórum top-level
  como parte del cierre, no como trabajo posterior opcional.

**Igualdad de no-descarte-silencioso (fase completa):** 8 filas de la sonda = 5 authoradas en
`must_haves.truths` (44-01) + 3 marcadas como supuesto explícito (INT-01 en 44-01, INT-03 e INT-04 aquí).
</flagged_assumptions>

<verification>
Al cerrar el plan, este estado exacto —parte verde y parte roja, y la parte roja es deliberada:

1. `node --test tests/*.test.js` → `fail 0`. **Verde.**
2. `node --test tests/content-fare-indicativo.test.js` y `node --test tests/content-fare-indefiniti.test.js` → `fail 0` cada uno, con el conteo de tests de ambos ficheros CRECIDO respecto al inicio del plan. **Verde.**
3. `node scripts/run-validation-271.mjs` → `VAL-06 (250/250 validated): FAIL (247/250 — pending=3, missing=0, disputed=0)` y `Milestone gate FAIL`. **ROJO ESPERADO.** No se arregla tocando contenido: se arregla estampando el quórum top-level.
4. `VAL_07_STRICT=1 node --test tests/*.test.js` → rojo EXACTAMENTE en los dos ficheros de contenido tocados, nombrando los 3 ids de cruce como `pending`. **ROJO ESPERADO.**
5. Los 3 cruces existen, suman 9 variantes y ninguno tiene un pase: `pending` con `passes: []` los tres.
6. `git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` → sin salida. **No añadir `src/data/`** al comando: divergió legítimamente en Phases 40 y 42 y el gate fallaría por diseño (D-44-09).
7. `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` → `2`.
8. `git diff --stat content/categories.json scripts/run-validation-271.mjs tests/fixtures/slot-variants-integration.test.js` → sin salida: este plan no toca lo que es del plan 44-01.
9. El checkpoint de la Tarea 4 está registrado con el hand-off y la respuesta del autor.
</verification>

<success_criteria>
- INT-03 entregado en su forma decidida: 3 cruces, 9 variantes, `categoryIds` de 2 con las parejas exactas y el slug `modali`, la key en la categoría vecina y la forma de `fare` escrita como contexto.
- G1, G2 y G3 son gates mecánicos y permanentes, no intenciones: cada uno tiene su assert por cruce en el fichero de test de su categoría.
- Los dos ficheros de test están particionados y NINGÚN gate quedó debilitado ni sin dueño: el conteo de tests de ambos creció y los cruces tienen cobertura propia.
- Los 3 cruces cierran en `pending` con `passes: []`, y el hand-off al quórum TOP-LEVEL (Opus + Sonnet, 1 ejercicio por contexto fresco, VAL-03) está escrito y visible en el checkpoint.
- El rojo del reporter (`247/250`) y del estricto está declarado como el estado honesto y esperado, con instrucción explícita de no «arreglarlo».
- Cero líneas de motor tocadas; INT-04 queda pendiente del quórum top-level y así está dicho.
</success_criteria>

<output>
Crear `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-02-SUMMARY.md` al terminar, recogiendo:
las 9 variantes con su key y su gate, el conteo de `test(` de los dos ficheros de test ANTES y DESPUÉS de
la partición (la prueba de que ningún gate se perdió), la lista de gates re-apuntados e invertidos, la
salida literal del reporter en su estado rojo, la comprobación de que los 3 cruces están `pending` con
`passes: []`, y el hand-off al quórum top-level con los 3 comandos exactos a lanzar.
</output>

