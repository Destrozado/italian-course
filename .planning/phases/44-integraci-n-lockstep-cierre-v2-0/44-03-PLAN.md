---
phase: 44-integraci-n-lockstep-cierre-v2-0
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - tests/count-arrays-lockstep.test.js
autonomous: true
gap_closure: true
gap_ids: [G-44-3-WR01]
requirements: [INT-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- G-44-3-WR01, vía 1: la entrada COMENTADA ---
    - "El ancla de `slugsCiegos` exige que el slug aparezca al INICIO de una entrada del array —línea que abre con `{`, con flag `m`— y no en cualquier posición del fichero, de modo que una entrada comentada con `//` deja de satisfacerla y la categoría se devuelve como CIEGA. Comentar «temporalmente» una línea del array es el gesto más plausible de todos los que producen el bug histórico (G-44-3-WR01, D-44-06)."
    - "El golden que congela esa vía está COMMITEADO y opera sobre una cadena literal del propio fichero de test, igual que los tres goldens que ya viven ahí: el gate no se prueba solo en verde, porque un gate probado solo en verde es una afirmación y no una garantía (D-44-06)."
    # --- G-44-3-WR01, vía 2: el par slug ↔ file cruzado ---
    - "Existe un gate nuevo que extrae los pares `{ slug, file }` declarados en `scripts/run-validation-271.mjs` y exige que todo `file` sea EXACTAMENTE `content/exercises/<slug>.json`. Sin él, una entrada con `slug: 'fare-indefiniti'` y el `file` de `fare-indicativo` —el copia-pega entre los dos slugs que comparten el prefijo `fare-ind`— hace que `expected` y el baseline dinámico lean el MISMO fichero: el guard de coherencia del reporter cuadra, el gate anti-ceguera está verde, `fare-indicativo` se cuenta dos veces y los 7 slots de `fare-indefiniti` desaparecen del total (G-44-3-WR01, D-40-03, D-31-06)."
    - "El gate del par NO puede quedarse verde por no haber encontrado nada: el número de pares extraídos se confronta con `SLUGS_REGISTRADOS.length`, que se DERIVA de `content/categories.json` en disco. Un extractor que deja de ver el array falla con un mensaje que lo dice, en vez de comparar dos listas vacías y pasar (D-44-06, D-31-06)."
    - "La segunda fuente de conteo —`tests/fixtures/slot-variants-integration.test.js`— no declara `file` por entrada sino que DERIVA la ruta del slug, así que el gate la trata explícitamente por esa vía en vez de dejarla fuera de cobertura en silencio: una fuente declara el fichero por entrada (y entonces el par tiene que cuadrar) o lo deriva del slug (y entonces es inmune por construcción). Una fuente que no haga ninguna de las dos cosas es un canal de ceguera nuevo y el gate lo dice."
    # --- la propiedad que distingue este arreglo de CR-01 ---
    - "Los dos gates se han VISTO ROJOS por mutación antes de cerrarse: se introduce en `scripts/run-validation-271.mjs` el defecto exacto que cada uno existe para cazar (la entrada comentada; el `file` del hermano), se comprueba el rojo con su mensaje verdadero, se revierte con `git checkout --` y se vuelve a verde. Un gate que nunca se ha visto rojo no está verificado: el CR-01 de esta misma fase era una suite en verde certificando una cifra obsoleta (G-44-3-WR01, `[[gate_congela_literal_debe_anclar_disco]]`)."
    - "Además de la mutación transitoria queda constancia PERMANENTE en el repo: cada predicado nuevo es una función pura exportada con sus goldens positivo y negativo committeados, de modo que la prueba de que el gate sabe morder sobrevive a la sesión que la produjo (D-44-06)."
    # --- lo que NO cambia ---
    - "`scripts/run-validation-271.mjs`, `tests/fixtures/slot-variants-integration.test.js` y `content/` terminan el plan BYTE-IDÉNTICOS: `git status --porcelain` no menciona ninguno de los tres y el único fichero del diff es `tests/count-arrays-lockstep.test.js`. Las mutaciones son transitorias, se hacen sobre un árbol ya committeado y se revierten con `git checkout --`."
    - "`node --test tests/*.test.js` termina con `fail 0` y con un número de tests ESTRICTAMENTE MAYOR que el medido antes de tocar nada en la misma sesión (medido, no transcrito de ningún `notes` ni de esta prosa), y `node scripts/run-validation-271.mjs` sigue imprimiendo su `Milestone gate PASS` — que es la prueba de que los reverts quedaron limpios (INT-02)."

  artifacts:
    - path: "tests/count-arrays-lockstep.test.js"
      provides: "el gate anti-ceguera endurecido por las dos vías que el code review encontró abiertas"
      contains: "`slugsCiegos` con el ancla a inicio de entrada y flag `m`; el helper nuevo `paresSlugFile` exportado; los goldens committeados de la entrada comentada, del par cruzado, de la fuente alineada por columnas y de la fuente que deriva la ruta; el gate D-40-03 del par `slug` ↔ `file` con su cláusula de no-vacuidad; y el gate de cobertura de las dos fuentes"

  key_links:
    - "`content/categories.json` → `SLUGS_REGISTRADOS` → los dos gates: la lista de referencia se lee del DISCO y nunca se escribe a mano en el test, porque una lista escrita aquí se compararía consigo misma y sería verde para siempre, incluida el alta de la categoría 19. Ese enlace es la razón de que el gate valga algo y no se toca."
    - "el array `CATEGORIES` de `scripts/run-validation-271.mjs` → `expected` + `TOTAL_EXPECTED_BASELINE`: los dos lados de la resta del guard dinámico se calculan sobre `c.file`, así que un `file` cruzado desplaza LOS DOS lados a la vez y la resta cuadra. Es exactamente por eso que el rojo tiene que venir del test y no del guard del reporter (D-31-06)."
    - "`slugsCiegos` / `paresSlugFile` → sus goldens: los predicados son funciones puras exportadas justamente para que se puedan probar sobre cadenas literales deterministas. Es el idiom que este fichero ya estableció y el que hace que el fail-first quede committeado."
    - "el `process.exit(1)` que `scripts/run-validation-271.mjs` dispara AL CARGARSE → la decisión de leer las fuentes como TEXTO y nunca importarlas (D-44-07): importar el reporter desde un test mata el runner sin mensaje útil. Los gates nuevos siguen esa vía sin excepción."

  prohibitions:
    - "MUST NOT tocar ningún fichero de `content/`: los tres gaps de este cierre son arreglos SOLO de test y el autor lo dejó escrito en la decisión del UAT. Si un assert nuevo pareciera exigir un cambio de contenido, el assert está mal — se reporta, no se edita el contenido."
    - "MUST NOT dejar `scripts/run-validation-271.mjs` ni `tests/fixtures/slot-variants-integration.test.js` modificados al cerrar el plan. La mutación es transitoria y el orden es obligatorio: primero se COMMITEA el gate, después se muta, y se revierte con `git checkout -- <fichero>`. Revertir con `git checkout` sobre trabajo sin committear lo borra."
    - "MUST NOT cerrar un gate nuevo sin haberlo visto rojo. El precedente es de esta misma fase: CR-01 era un test que congelaba una cifra escrita en un `notes` sin derivarla del disco, y certificaba en verde un número obsoleto. La verificación por mutación no es una formalidad de proceso, es la única evidencia de que el gate muerde."
    - "MUST NOT escribir en un assert ninguna cifra transcrita de un `notes`, de un SUMMARY, de este plan o del 44-REVIEW.md: toda cifra que un test afirme se DERIVA del disco en el momento de correr (`SLUGS_REGISTRADOS.length`, `exercises.length`). Es la lección literal de CR-01."
    - "MUST NOT relajar, borrar ni reescribir a la baja ninguno de los tres goldens que ya viven en el fichero: se AÑADEN goldens, y los tres existentes tienen que seguir verdes con el ancla nueva (sus entradas literales abren la línea con `{`, así que lo están)."
    - "MUST NOT importar `scripts/run-validation-271.mjs` desde un test: su guard de coherencia llama a `process.exit(1)` al cargar el módulo y mata el runner sin mensaje útil. Las dos fuentes se leen como TEXTO (D-44-07)."
    - "MUST NOT ampliar el alcance a WR-04…WR-09 ni a IN-01…IN-03: el autor los aceptó como deuda documentada en `44-REVIEW.md` y ninguno abre plan en esta fase. En particular NO se toca el conteo de pronombres de los cruces (WR-04), que vive en otros ficheros."
    - "MUST NOT añadir dependencia, `package.json` de runtime ni paso de build: el proyecto es web estática de dependencias cero (CLAUDE.md §Constraints) y el arnés de test corre con el runner nativo de Node."
---

<objective>
Cerrar **G-44-3-WR01**: el gate anti-ceguera de `tests/count-arrays-lockstep.test.js` se queda VERDE ante
dos gestos que devuelven al reporter a la ceguera histórica, y el autor decidió arreglarlo antes de
`/gsd-complete-milestone v2.0` en vez de aceptarlo como deuda.

Las dos vías, verbatim del code review:

1. **La entrada comentada.** `slugsCiegos` solo exige que el TEXTO `slug: '<slug>'` exista en algún sitio
   del fichero, así que `// { slug: 'fare-indefiniti', ... },` sigue satisfaciendo la regex. Comentar una
   línea «temporalmente» es el gesto más plausible de todos los que producen el bug.
2. **El `file` del hermano.** Una entrada con `slug: 'fare-indefiniti'` y
   `file: 'content/exercises/fare-indicativo.json'` —el copia-pega entre los dos slugs que comparten el
   prefijo `fare-ind`— hace que `expected` y el baseline dinámico lean el MISMO fichero: el guard de
   coherencia del reporter cuadra, el gate anti-ceguera está verde, `fare-indicativo` se cuenta dos veces
   y los 7 slots de `fare-indefiniti` desaparecen del total. El bloque 4 comprueba que
   `content/exercises/<slug>.json` existe y no está vacío, pero nunca que la entrada del array apunte a
   ese fichero.

Purpose: **este gate es lo ÚNICO que impide la cuarta repetición del bug que ya ocurrió tres veces.**
225/225 `PASS` mientras 22 slots de `fare` estaban en disco, validados y sin contar (Phases 41, 42 y 43),
y antes `presente-regolare` ausente de los arrays desde Phase 30 hasta Phase 31. WR-01 no es contenido en
absoluto: es que el arnés de test se queda verde ante el mismo patrón que ya mordió en CR-01.

Output: `tests/count-arrays-lockstep.test.js` con el ancla endurecida, el gate D-40-03 del par
`slug` ↔ `file`, sus goldens fail-first committeados, y las dos vías VISTAS ROJAS por mutación sobre el
reporter real y revertidas. Cero cambios de contenido, cero cambios en `src/`, y las dos fuentes de conteo
byte-idénticas al cerrar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-UAT.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-REVIEW.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Verificado contra el disco el 2026-08-12. Lee ESTAS regiones y no el fichero entero:

**`tests/count-arrays-lockstep.test.js`** (298 líneas) — el fichero que se edita. Regiones:
- 62-87: `escapeRe` y `slugsCiegos` (el helper a endurecer, ya exportado).
- 89-163: el `describe` de los TRES goldens sobre cadenas literales (`SRC_VACIO`, `SRC_SOLO_HERMANO`,
  `SRC_SOLO_EL_OTRO_HERMANO`, `SRC_COMPLETO`). Es el idiom a clonar para los goldens nuevos.
- 165-182: el gate real sobre las dos fuentes (`COUNT_ARRAY_SOURCES`, `readSrc`, bucle con `SRC`).
- 184-225: el bloque 4 (fichero de cada categoría presente / parseable / no vacío) — el que comprueba el
  fichero derivado del slug pero NUNCA que la entrada apunte a él.

**La forma REAL de las entradas de las dos fuentes** — el gate nuevo tiene que parsear esto tal cual:

`scripts/run-validation-271.mjs` (18 entradas, líneas 173-197) declara `file` por entrada y ALINEA las
columnas con varios espacios:

```js
const CATEGORIES = [
  { slug: 'preposiciones',            file: 'content/exercises/preposiciones.json',            expected: 50 },
  { slug: 'fare-indefiniti',          file: 'content/exercises/fare-indefiniti.json',          expected: slotCountOf('content/exercises/fare-indefiniti.json') },
];
```

`tests/fixtures/slot-variants-integration.test.js` (18 entradas, líneas 170-195) NO declara `file`: lo
DERIVA del slug en el bucle de consumo (línea 198), así que es inmune al par cruzado por construcción:

```js
  const REAL_CATEGORIES = [
    { slug: 'avere', expected: 20 },
    { slug: 'fare-indicativo', expected: readJson('content/exercises/fare-indicativo.json').exercises.length },
  ];

  for (const { slug, expected } of REAL_CATEGORIES) {
    const file = `content/exercises/${slug}.json`;
```

**El fix que el reviewer dejó escrito** (`44-REVIEW.md` § WR-01), como punto de partida y no como copia
literal — le falta la cláusula de no-vacuidad y el trato explícito de la segunda fuente:

```js
// 1) el ancla no puede estar comentada: se exige que la linea empiece por `{`
const anclado = new RegExp(`^\\s*\\{[^\\n]*slug:\\s*(['"\`])${escapeRe(slug)}\\1`, 'm');

// 2) NUEVO gate: en el reporter, slug y file tienen que ser el mismo slug
const pares = [...SRC.matchAll(/\{\s*slug:\s*'([^']+)'\s*,\s*file:\s*'([^']+)'/g)];
const cruzados = pares
  .filter(([, slug, file]) => file !== `content/exercises/${slug}.json`)
  .map(([, slug, file]) => `${slug} -> ${file}`);
assert.deepEqual(cruzados, [], 'D-40-03: una entrada declara el fichero de OTRA categoria');
```

> Ojo a dos detalles del fichero real: las entradas del reporter llevan **varios espacios** entre
> `slug: '...',` y `file:` (columnas alineadas), y el ancla nueva necesita el flag `m` para que `^`
> signifique «inicio de línea» y no «inicio de fichero».
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarea 1 (tracer): el ancla de `slugsCiegos` a inicio de entrada — la vía de la línea comentada, vista roja</name>
  <files>tests/count-arrays-lockstep.test.js</files>
  <behavior>
    Goldens NUEVOS que se escriben ANTES de tocar `slugsCiegos` y que con el helper actual fallan:
    - `SRC_COMENTADO`: array donde la entrada de `fare-indefiniti` va precedida de `//` en la misma
      línea → `slugsCiegos(SRC_COMENTADO, ['fare-indefiniti'])` devuelve `['fare-indefiniti']`.
    - `SRC_SLUG_EN_PROSA`: fichero donde el texto `slug: 'fare-indefiniti'` aparece dentro de un
      comentario de prosa que NO abre la línea con `{` → devuelve `['fare-indefiniti']`.
    Goldens EXISTENTES que tienen que seguir verdes sin retocarlos (sus entradas literales abren la
    línea con `{` tras la indentación): el negativo simple, el de colisión de prefijo en las dos
    direcciones, y el positivo.
    Y la mutación sobre la fuente real: con la entrada de `fare-indefiniti` comentada en
    `scripts/run-validation-271.mjs`, el gate real del bloque 3 se pone ROJO nombrando `fare-indefiniti`.
  </behavior>
  <action>
    Paso 1 — REPRODUCIR el bug antes de arreglarlo (el árbol está limpio, así que la mutación es
    reversible sin riesgo). Comenta la entrada de `fare-indefiniti` del array `CATEGORIES` de
    `scripts/run-validation-271.mjs` anteponiendo `//` a la línea, corre solo este fichero de test y
    comprueba que pasa en VERDE: eso es el bug de G-44-3-WR01 vía 1, presenciado. Revierte con
    `git checkout -- scripts/run-validation-271.mjs`. Anota en el SUMMARY que se observó verde.

    Paso 2 — Añade los dos goldens nuevos al `describe` de goldens existente (el del bloque 2). El nombre
    del primero tiene que contener la palabra `comentada`, porque el criterio de aceptación lo localiza por
    ahí en la salida del runner. Clona
    el idiom que ya está ahí: constantes de cadena literal declaradas en el propio fichero, nunca lectura
    de disco, porque es lo que las hace deterministas y lo que convierte el fail-first en algo
    committeado. `SRC_COMENTADO` lleva la entrada de `fare-indefiniti` precedida de `//` y la de
    `fare-indicativo` sana; `SRC_SLUG_EN_PROSA` mete el texto de la clave dentro de un comentario de
    prosa que no abre línea con llave. Corre el fichero y comprueba que los dos goldens nuevos están
    ROJOS con el helper actual: es el fail-first, y si alguno sale verde el golden no está reproduciendo
    la vía.

    Paso 3 — Endurece `slugsCiegos`: el ancla pasa a exigir que el slug aparezca en una línea que ABRE
    una entrada del array, es decir `^\s*\{` seguido de lo que sea que no contenga salto de línea y
    luego `slug:` con el slug completo entrecomillado, con flag `m` para que el ancla signifique inicio
    de línea. Mantén `escapeRe` y mantén el requisito del slug COMPLETO byte a byte, que es lo que
    resuelve la colisión `fare-ind` de D-40-03: se endurece el ancla, no se relaja la identidad del slug.
    Actualiza el JSDoc del helper y el comentario que lo precede para que declaren el invariante nuevo
    —el ancla es el inicio de una ENTRADA, de modo que una entrada comentada no la satisface— y por qué
    (comentar una línea temporalmente es el gesto que produce el bug histórico). Corre el fichero: los
    cinco goldens y los gates reales, todos verdes.

    Paso 4 — COMMITEA el fichero de test antes de mutar nada, porque el revert de la mutación va por
    `git checkout --` y eso destruye cualquier trabajo sin committear.

    Paso 5 — MUTACIÓN de verificación, ahora con el gate endurecido: vuelve a comentar la entrada de
    `fare-indefiniti` en `scripts/run-validation-271.mjs`, corre el fichero de test y comprueba que el
    gate del bloque 3 se pone ROJO nombrando `fare-indefiniti` en el mensaje. Revierte con
    `git checkout -- scripts/run-validation-271.mjs`, corre otra vez y comprueba verde, y comprueba que
    `git status --porcelain` no menciona el reporter. Pega en el SUMMARY el mensaje de assert rojo
    literal: es la evidencia de que el gate muerde, y sin ella el gate no está verificado.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/count-arrays-lockstep.test.js 2>&amp;1 | tail -8</automated>
    <automated>cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/count-arrays-lockstep.test.js 2>&amp;1 | grep -c "^ok .*COMENTADA\|^ok .*comentada"</automated>
    <automated>node -e "const fs=require('fs'),p='scripts/run-validation-271.mjs';fs.writeFileSync(p,fs.readFileSync(p,'utf8').replace(\"  { slug: 'fare-indefiniti',\",\"  // { slug: 'fare-indefiniti',\"))" &amp;&amp; node --test tests/count-arrays-lockstep.test.js > /tmp/mut1.txt 2>&amp;1; echo "exit-esperado-no-cero=$?"; grep -c "fare-indefiniti" /tmp/mut1.txt; git checkout -- scripts/run-validation-271.mjs &amp;&amp; node --test tests/count-arrays-lockstep.test.js 2>&amp;1 | grep -c "^# fail 0"; git status --porcelain scripts/ content/ | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/count-arrays-lockstep.test.js` cierra con `fail 0`.
    - El número de tests del fichero es estrictamente mayor que el medido antes de la tarea (registrar el valor previo en el SUMMARY, medido en la misma sesión, nunca transcrito de un `notes`).
    - El ancla es de inicio de línea: `node -e "const s=require('fs').readFileSync('tests/count-arrays-lockstep.test.js','utf8');const m=s.match(/const anclado = new RegExp\(([\s\S]*?)\);/);console.log(Boolean(m), m ? /'m'/.test(m[1]) : false)"` imprime `true true`.
    - MUTACIÓN 1 (entrada comentada), con el gate endurecido: tras comentar la entrada de `fare-indefiniti` del array del reporter, `node --test tests/count-arrays-lockstep.test.js` sale con código distinto de 0 y su salida contiene `fare-indefiniti`. **Ese rojo es el entregable de la tarea**, y su mensaje literal va pegado en el SUMMARY.
    - Tras `git checkout -- scripts/run-validation-271.mjs`, `node --test tests/count-arrays-lockstep.test.js` vuelve a `fail 0` y `git status --porcelain scripts/ content/` no imprime ninguna línea.
    - `git status --porcelain content/` no imprime ninguna línea en ningún momento de la tarea: este cierre es SOLO de test.
  </acceptance_criteria>

  <done>Los cinco goldens del helper están verdes y committeados, `slugsCiegos` ancla al inicio de entrada con flag `m`, la vía de la entrada comentada se observó VERDE antes del arreglo y ROJA después sobre el reporter real, el reporter quedó byte-idéntico tras el revert, y el mensaje de assert rojo está pegado en el SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: el gate D-40-03 del par `slug` ↔ `file`, con cláusula de no-vacuidad y las dos fuentes cubiertas</name>
  <files>tests/count-arrays-lockstep.test.js</files>
  <behavior>
    Helper puro nuevo, exportado, con goldens committeados sobre cadenas literales:
    - `paresSlugFile(SRC_PAR_COHERENTE)` → los pares tal cual, y ninguno cruzado.
    - `paresSlugFile(SRC_PAR_CRUZADO)` → el par de `fare-indefiniti` apuntando al fichero de
      `fare-indicativo`, que es el copia-pega de D-40-03.
    - `paresSlugFile(SRC_ALINEADO)` → parsea entradas con varios espacios entre `slug:` y `file:`,
      que es la forma REAL del reporter; si no, el gate sería vacuo sobre la fuente que gobierna.
    - `paresSlugFile(SRC_SIN_FILE)` → lista vacía, que es la forma de la segunda fuente.
    Gates reales:
    - `scripts/run-validation-271.mjs`: el número de pares extraídos es igual a `SLUGS_REGISTRADOS.length`
      (derivado de `content/categories.json`), y todo `file` es exactamente `content/exercises/<slug>.json`.
    - `tests/fixtures/slot-variants-integration.test.js`: no declara `file` por entrada, así que la
      cobertura se afirma por la otra vía — su texto DERIVA la ruta del slug.
    Mutaciones: con el `file` de `fare-indefiniti` apuntando al JSON de `fare-indicativo`, el gate se
    pone rojo nombrando el par; con la clave `file` renombrada en una entrada, el gate se pone rojo por
    la cláusula de no-vacuidad y no por la comparación.
  </behavior>
  <action>
    Paso 1 — Escribe los cuatro goldens NUEVOS y el helper `paresSlugFile(src)` exportado, en ese orden
    (goldens primero, rojos, después el helper). `paresSlugFile` devuelve la lista de pares
    `{ slug, file }` que el texto declara: casa la apertura de entrada, la clave del slug y la clave del
    fichero tolerando varios espacios de alineación y las tres formas de entrecomillado que el proyecto
    usa. Vive junto a `slugsCiegos` y se exporta por el mismo motivo: un predicado puro es lo único que
    se puede probar sobre cadenas literales deterministas.

    Paso 2 — Añade el gate real de coherencia sobre `scripts/run-validation-271.mjs` con DOS cláusulas y
    en este orden. Primero la de no-vacuidad: el número de pares extraídos tiene que ser igual a
    `SLUGS_REGISTRADOS.length`, que se deriva de `content/categories.json` en disco; el mensaje dice que
    el extractor dejó de ver el array del reporter. Sin esta cláusula, un extractor que no encuentra nada
    compara dos listas vacías y pasa en verde, que es el modo de fallo de la especie de CR-01. Después la
    de coherencia: para cada par, `file` tiene que ser exactamente `content/exercises/<slug>.json`, y el
    mensaje nombra el par cruzado en la forma `slug -> file` para que el diagnóstico sea verdadero.
    Explica en el comentario del gate por qué el guard dinámico del reporter no puede ver esto: con
    `expected` derivado del propio `file`, un `file` cruzado desplaza LOS DOS lados de la resta a la vez
    y la resta cuadra.

    Paso 3 — Cierra la segunda fuente en el mismo gate en vez de dejarla fuera de cobertura en silencio.
    Recorre `COUNT_ARRAY_SOURCES` y aplica la disyuntiva: si la fuente declara la clave del fichero por
    entrada, sus pares tienen que cuadrar; si no la declara, se exige que su texto DERIVE la ruta del
    slug, comprobando que contiene la expresión de plantilla con la que la construye en el bucle de
    consumo. Deja escrito en el comentario que una fuente que no haga ninguna de las dos cosas es un
    canal de ceguera nuevo, que es exactamente lo que este gate existe para nombrar.

    Paso 4 — Corre el fichero: todo verde. COMMITEA antes de mutar.

    Paso 5 — MUTACIÓN A (la vía del review): en `scripts/run-validation-271.mjs`, cambia el `file` de la
    entrada de `fare-indefiniti` por el JSON de `fare-indicativo`, corre el fichero de test y comprueba
    el ROJO con el par nombrado. Revierte con `git checkout --` y comprueba verde.

    Paso 6 — MUTACIÓN B (la cláusula de no-vacuidad): en la misma entrada del reporter, renombra la clave
    del fichero a un nombre distinto para que el extractor pierda ese par, corre el test y comprueba que
    el rojo llega por la cláusula del conteo y no por la de coherencia. Revierte con `git checkout --`,
    comprueba verde, y comprueba que `git status --porcelain` no menciona `scripts/` ni `content/`. Pega
    los DOS mensajes de assert rojo en el SUMMARY.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/count-arrays-lockstep.test.js 2>&amp;1 | tail -8</automated>
    <automated>node -e "const fs=require('fs'),p='scripts/run-validation-271.mjs';fs.writeFileSync(p,fs.readFileSync(p,'utf8').replace(\"file: 'content/exercises/fare-indefiniti.json'\",\"file: 'content/exercises/fare-indicativo.json'\"))" &amp;&amp; node --test tests/count-arrays-lockstep.test.js > /tmp/mut2.txt 2>&amp;1; echo "exit-esperado-no-cero=$?"; grep -c "fare-indefiniti" /tmp/mut2.txt; git checkout -- scripts/run-validation-271.mjs &amp;&amp; node --test tests/count-arrays-lockstep.test.js 2>&amp;1 | grep -c "^# fail 0"</automated>
    <automated>node -e "const fs=require('fs'),p='scripts/run-validation-271.mjs';fs.writeFileSync(p,fs.readFileSync(p,'utf8').replace(\"file: 'content/exercises/fare-indefiniti.json'\",\"fichero: 'content/exercises/fare-indefiniti.json'\"))" &amp;&amp; node --test tests/count-arrays-lockstep.test.js > /tmp/mut3.txt 2>&amp;1; echo "exit-esperado-no-cero=$?"; git checkout -- scripts/run-validation-271.mjs &amp;&amp; node --test tests/count-arrays-lockstep.test.js 2>&amp;1 | grep -c "^# fail 0"; git status --porcelain scripts/ content/ | wc -l</automated>
    <automated>node --test tests/*.test.js 2>&amp;1 | tail -8; node scripts/run-validation-271.mjs 2>&amp;1 | tail -4; echo "reporter-exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/count-arrays-lockstep.test.js` y `node --test tests/*.test.js` cierran los dos con `fail 0`, y el `pass` de la suite completa es estrictamente mayor que el medido antes de empezar el plan (medido en la misma sesión).
    - El helper nuevo está exportado y es puro: `node -e "import('./tests/count-arrays-lockstep.test.js').then(m=>console.log(typeof m.slugsCiegos, typeof m.paresSlugFile))"` imprime `function function`. (Si importar el fichero de test hiciera correr sus tests dos veces en la suite, exportar el predicado desde un módulo hermano es alternativa aceptable: lo que no es negociable es que el predicado sea puro y tenga goldens.)
    - La cláusula de no-vacuidad existe y se apoya en el disco: `node -e "const s=require('fs').readFileSync('tests/count-arrays-lockstep.test.js','utf8');console.log(/SLUGS_REGISTRADOS\.length/.test(s))"` imprime `true`.
    - MUTACIÓN A (par cruzado): con el `file` de `fare-indefiniti` apuntando al JSON de `fare-indicativo`, el fichero de test sale con código distinto de 0 y su mensaje NOMBRA el par cruzado. Mensaje literal en el SUMMARY.
    - MUTACIÓN B (no-vacuidad): con la clave del fichero renombrada en esa entrada, el fichero de test sale con código distinto de 0 y el rojo llega por la cláusula del conteo de pares, no por la de coherencia. Mensaje literal en el SUMMARY.
    - Tras los dos reverts, `git status --porcelain scripts/ content/` no imprime ninguna línea y `node scripts/run-validation-271.mjs` vuelve a imprimir su `Milestone gate PASS` — la prueba de que el reporter quedó intacto.
    - `git diff --name-only HEAD~1..HEAD` (o el diff acumulado del plan) lista `tests/count-arrays-lockstep.test.js` y ningún fichero de `content/`, `src/` ni `scripts/`.
  </acceptance_criteria>

  <done>El gate D-40-03 del par `slug` ↔ `file` está en la suite con su cláusula de no-vacuidad derivada del disco, las dos fuentes de conteo están cubiertas explícitamente (la que declara el fichero por su par, la que lo deriva por su derivación), los cuatro goldens nuevos están committeados, las dos mutaciones se vieron rojas con mensajes verdaderos y revertidas, y el reporter cierra en `Milestone gate PASS` byte-idéntico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichero fuente autorado a mano → arnés de test (parseo por TEXTO) | Los dos ficheros de conteo se leen como texto y se asertan por regex. Nada se importa ni se evalúa: el `process.exit(1)` del reporter al cargarse es la razón (D-44-07). |
| árbol de git → mutación transitoria de verificación | La tarea escribe deliberadamente un defecto en un fichero versionado y lo revierte. Es la única frontera con riesgo real de este plan. |
| (sin frontera de red, sin entrada de usuario, sin instalación de paquetes) | Proyecto de dependencias cero, web estática, single-user local (CLAUDE.md §Constraints). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-03-01 | Tampering | `paresSlugFile` sobre `scripts/run-validation-271.mjs` | high | mitigate | Un extractor por regex que deja de casar devuelve lista vacía y el `deepEqual` contra `[]` pasa en VERDE: es la especie exacta de CR-01. Cláusula de no-vacuidad obligatoria contra `SLUGS_REGISTRADOS.length` derivado del disco, MÁS golden con la forma alineada real del reporter, MÁS mutación B que renombra la clave y exige el rojo por esa cláusula. |
| T-44-03-02 | Tampering | mutación transitoria de `scripts/run-validation-271.mjs` | high | mitigate | Orden obligatorio: commit del gate ANTES de mutar, revert por `git checkout -- <fichero>`, y `git status --porcelain scripts/ content/` vacío como criterio de aceptación. Revertir con `git checkout` sobre trabajo sin committear lo destruye — de ahí el orden. |
| T-44-03-03 | Denial of Service | la regex anclada de `slugsCiegos` | low | accept | El ancla usa `[^\n]*` (acotado a la línea) y no anida cuantificadores, así que no hay retroceso catastrófico. Las fuentes son de cientos de líneas y el fichero corre en decenas de milisegundos. |
| T-44-03-04 | Spoofing | un `slug` que se hace pasar por otro (`fare-ind`) | high | mitigate | La identidad del slug sigue exigiéndose COMPLETA byte a byte (D-40-03) y los goldens de colisión de prefijo existentes mordiendo en las dos direcciones se mantienen intactos. El endurecimiento es del ancla, nunca de la identidad. |
| T-44-03-05 | Information disclosure | — | low | accept | Herramienta local single-user sin red, sin cuentas y sin datos de terceros: no hay superficie. |
| T-44-03-SC | Tampering | npm/pip/cargo installs | high | accept | Este plan NO instala nada: el proyecto es de dependencias cero y el arnés corre con el runner nativo de Node. No hay tabla de legitimidad de paquetes que consultar porque no hay paquete. Si una tarea futura introdujera un install, aplica el gate de legitimidad con checkpoint humano bloqueante. |
</threat_model>

<verification>
1. `node --test tests/count-arrays-lockstep.test.js` → `fail 0`.
2. `node --test tests/*.test.js` → `fail 0`, con `pass` estrictamente mayor que el medido al abrir el plan.
3. `node scripts/run-validation-271.mjs` → `Milestone gate PASS` (prueba de que los reverts quedaron limpios).
4. `git status --porcelain` → sin líneas para `content/`, `src/` ni `scripts/`.
5. Las TRES mutaciones (entrada comentada; par cruzado; clave del fichero renombrada) documentadas en el SUMMARY con su mensaje de assert rojo literal.
6. `git diff` del plan → un único fichero tocado: `tests/count-arrays-lockstep.test.js`.
</verification>

<success_criteria>
- G-44-3-WR01 cerrado por las DOS vías que el code review encontró: la entrada comentada ya no satisface el ancla, y el par `slug` ↔ `file` del reporter tiene gate propio con cláusula de no-vacuidad.
- Cada gate nuevo se ha visto ROJO por mutación sobre la fuente real y VERDE tras el revert, con la evidencia literal en el SUMMARY.
- Cada predicado nuevo es puro, exportado y con goldens positivo y negativo COMMITEADOS: la prueba de que el gate muerde sobrevive a la sesión.
- Ninguna cifra asertada está transcrita de prosa o de un `notes`: todas se derivan del disco al correr.
- Cero cambios en `content/`, `src/` y `scripts/`. La suite entera verde y el reporter en `Milestone gate PASS`.
</success_criteria>

<output>
Create `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-03-SUMMARY.md` when done
</output>