---
phase: 44-integraci-n-lockstep-cierre-v2-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run-validation-271.mjs
  - tests/fixtures/slot-variants-integration.test.js
  - tests/count-arrays-lockstep.test.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md
autonomous: true
requirements: [INT-01, INT-02, INT-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- INT-02 / D-44-06 : los dos arrays que faltaban ---
    - "`scripts/run-validation-271.mjs` tiene 18 entradas en `CATEGORIES` y `tests/fixtures/slot-variants-integration.test.js` tiene 18 entradas en `REAL_CATEGORIES`: las 4 nuevas son `fare-cond-imperativo`, `fare-congiuntivo`, `fare-indefiniti` y `fare-indicativo`, con el slug COMPLETO byte a byte (D-40-03: `fare-ind` es prefijo ambiguo, nunca se trunca) (INT-02, D-44-06)."
    - "`node scripts/run-validation-271.mjs` imprime `VAL-06 (247/247 validated): PASS (247/247)`, `VAL-08 (cero disputed): PASS`, `VAL-04 (≥2 distinct AIs por validated): PASS` y `Milestone gate PASS`, y sale con código 0: el reporter deja de estar ciego a los 22 slots de `fare` que llevaba tres fases sin contar (INT-02, D-44-06)."
    # --- edge probe INT-02 : boundary + precision ---
    - "`TOTAL_EXPECTED` (Σ de los `expected` literales de `CATEGORIES`) y `TOTAL_EXPECTED_BASELINE` (Σ de `slotCountOf(c.file)` leído del disco en el mismo arranque) son exactamente iguales y valen 247 al cerrar este plan, de modo que el bloque de coherencia de `scripts/run-validation-271.mjs` NO dispara su `process.exit(1)` y el módulo termina de cargar (edge: boundary + precision, INT-02)."
    - statement: "Ninguna de las 8 entradas nuevas de los dos arrays de conteo lleva un `expected` numérico literal: las 4 del reporter derivan de `slotCountOf('content/exercises/<slug>.json')` y las 4 del back-compat de `readJson('content/exercises/<slug>.json').exercises.length`, de modo que un alta o una baja de slot mueve `expected`, `TOTAL_EXPECTED` y el baseline a la vez sin edición manual (D-31-06, dynamic-count)."
      verification: backstop
    # --- edge probe INT-02 : empty ---
    - "El gate anti-ceguera se pone ROJO si una categoría registrada en `content/categories.json` (a) no aparece en alguno de los dos arrays de conteo, (b) no tiene fichero `content/exercises/<slug>.json` en disco, o (c) tiene ese fichero con `exercises` de longitud 0 — los tres casos vacíos que el guard dinámico de `TOTAL_EXPECTED_BASELINE` NO puede distinguir de un cero legítimo, porque con `expected` dinámico ambos lados de la resta valen cero (edge: empty, D-44-06)."
    # --- edge probe INT-02 : adjacency ---
    - "El gate anti-ceguera ancla por slug COMPLETO y no por substring: sobre un texto fuente golden-negative que contiene `fare-indefiniti` y NO contiene `fare-indicativo`, el helper devuelve exactamente `['fare-indicativo']`; la colisión de prefijo `fare-ind` de D-40-03 no puede producir un falso verde en ninguno de los dos sentidos (edge: adjacency, D-40-03)."
    # --- edge probe INT-02 : ordering ---
    - "Los 18 `order` de `content/categories.json` son únicos y contiguos de 1 a 18, y para cada entrada `entradas.indexOf(cat) === cat.order - 1`: el ORDEN DEL ARRAY es lo que `categoriesForDisplay` recorre para pintar home, picker, Repaso y Examen, y el `order` es documental — si los dos divergen, el display deja de coincidir con el número escrito (edge: ordering, INT-01)."
    # --- INT-01 : ya cumplido en disco, aquí se verifica y se congela ---
    - "`content/categories.json` tiene 18 entradas, las 4 de `fare` con `order` 15, 16, 17 y 18 y `origen: \"ia-quorum\"` (PROV-01), y las 4 claves exactas `id`, `name`, `order`, `origen`; este plan NO añade ni edita ninguna entrada — INT-01 ya está cumplido de facto por las Phases 41/42/43 y aquí se verifica y se congela como invariante permanente (INT-01, D-44-06)."
    # --- INT-04 : la mitad documental ---
    - "Los CUATRO magnets de doble validez tienen su ronda EXTRA registrada en disco y verificable: `fare-cond-imperativo-imperativo`, los slots de `fare-congiuntivo`, `fare-indefiniti-participio-passato` y `fare-indefiniti-infinito-passato` — el cuarto, el par `aver fatto` / `avere fatto`, con 3 pases incluido un `by` que empieza por `deepseek` (INT-04, D-43-17)."
    - "`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` — la ruta REAL que el skill `gsd-validate-exercise` lee, no `docs/09-VALIDATION-PROMPT.md`, que no existe — gana una sección `### 7.5` que declara el CUARTO magnet, porque el subagent del quórum arranca con contexto vacío y nunca ve el `notes` (INT-04, D-44-12)."
    # --- INT-04 / D-44-08 : los documentos dejan de mentir ---
    - "`.planning/REQUIREMENTS.md` describe la realidad: INT-02 dice **2** arrays hardcoded (no 3) más el gate anti-ceguera, INT-03 escribe el slug real `modali` (no `verbi-modali`, que `src/data/schema-validator.js` rechazaría) e INT-04 declara **4** magnets incluido el par `aver fatto` / `avere fatto` (D-44-08, D-43-17)."
    - "`.planning/ROADMAP.md` §Phase 44 declara **22** slots nuevos, **247** en disco tras Phase 43 y **250** con los 3 cruces (SC#2), los ids concretos de los 3 cruces con el sentido invertido de D-44-02 (SC#3) y **113** variantes con **4** magnets (SC#4); y §v2.0 dice **22 slots / 113 variantes** en cabecera y pie. La edición va por el skill `gsd-phase`, NUNCA con Write ni Edit (D-44-08, anti-pattern #15 de GSD)."
    # --- el motor sigue intacto ---
    - "`git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` no produce salida y `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` sigue dando 2: este plan no toca una sola línea de motor. El scope del gate es EXACTAMENTE esas dos rutas — `src/data/` divergió legítimamente en Phases 40 y 42 y meterlo en el gate lo rompería por diseño (D-44-09)."
    - "`node --test tests/*.test.js` cierra con `fail 0` y `pass` ≥ 1034 (baseline verificado 2026-08-11: 1026), y `VAL_07_STRICT=1 node --test tests/*.test.js` cierra con `fail 0`: al no añadirse contenido nuevo en este plan, las 18 categorías siguen `validated` y el estricto sigue verde."

  artifacts:
    - path: "tests/count-arrays-lockstep.test.js"
      provides: "el gate anti-ceguera de D-44-06/07 — el único artefacto de la fase que impide la CUARTA repetición del lockstep diferido"
      contains: "el helper puro `slugsCiegos(src, slugs)`, sus tres goldens (negativo simple, negativo de colisión de prefijo, positivo), `COUNT_ARRAY_SOURCES` con las dos rutas de fuente, el gate de fichero presente y no vacío, y el gate de `order` único, contiguo e igual a `indexOf + 1`"
    - path: "scripts/run-validation-271.mjs"
      provides: "el reporter del milestone contando las 18 categorías"
      contains: "`CATEGORIES` con 18 entradas, las 4 de `fare` con `expected: slotCountOf(...)` y ordenadas alfabéticamente entre `essere` y `genero-numero`"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "el gate back-compat sobre las 18 categorías reales"
      contains: "`REAL_CATEGORIES` con 18 entradas, las 4 de `fare` con `expected: readJson(...).exercises.length`"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INT-02, INT-03 e INT-04 describiendo el estado real"
      contains: "2 arrays + gate anti-ceguera, el slug `modali`, y los 4 magnets"
    - path: ".planning/ROADMAP.md"
      provides: "§Phase 44 SC#2/SC#3/SC#4 y §v2.0 con los números reales"
      contains: "22 slots, 113 variantes, 247 en disco, 250 con los cruces, 4 magnets, ids de los 3 cruces"
    - path: ".planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md"
      provides: "la declaración del CUARTO magnet para el subagent del quórum"
      contains: "sección `### 7.5` con el par `aver fatto` / `avere fatto` y su frontera"

  key_links:
    - "`content/categories.json` → los dos arrays de conteo: es EL enlace que llevaba tres fases roto. El reporter no lee `categories.json`; construye su propia lista, así que una categoría registrada y no enganchada desaparece del total sin que nada se ponga rojo. El gate anti-ceguera es lo único que cierra ese lazo."
    - "`CATEGORIES` → `TOTAL_EXPECTED` → `TOTAL_EXPECTED_BASELINE`: el guard corre AL CARGAR el módulo y hace `process.exit(1)` si divergen. Por eso el gate anti-ceguera NO puede importar `scripts/run-validation-271.mjs` desde un test — importarlo es una bomba — y va por source-assert sobre el texto fuente (D-44-07)."
    - "`slug` → `content/exercises/<slug>.json`: en `REAL_CATEGORIES` la ruta se DERIVA del slug en el bucle, así que un slug mal escrito no da un error de nombre sino un `ENOENT` en runtime del test. El gate de fichero presente lo convierte en un rojo con mensaje."
    - "`content/categories.json` (orden del array) → `categoriesForDisplay` (`src/screens/app.js`): el array define el display; `order` es documental. Las 18 filas salen en home, picker, Repaso y Examen sin una línea de motor nueva, que es la prueba de que INT-01 está cumplido."
    - "`tests/exercise-types.test.js:1338` (`CATEGORIES_WITH_EXPLANATIONS`) → el smoke paramétrico `VAL_07_STRICT` de `:1503`: los dos comparten array y las 4 entradas de `fare` YA están, así que este plan no los toca. Cualquier edición ahí sería trabajo duplicado."

  prohibitions:
    - "MUST NOT emitir `Milestone gate PASS` sobre un conteo que el disco no respalde: ningún `expected` nuevo puede ser un número escrito a mano ni un valor que ignore el JSON real. Un PASS que miente es peor que un FAIL, porque el autor cierra el milestone creyendo que 22 slots están verificados cuando el reporter ni siquiera los ha mirado — es exactamente lo que pasó durante tres fases."
    - "MUST NOT hacer vacuo el gate anti-ceguera: su lista de referencia se lee de `content/categories.json` en tiempo de test, NUNCA de una lista de slugs escrita a mano dentro del propio fichero de test. Un gate que compara una lista consigo misma es verde para siempre y no protege de nada, que es la forma más cara de fingir cobertura."
    - "MUST NOT probar el gate anti-ceguera solo en verde: tiene que llevar su golden-negative committeado, es decir la demostración mecánica de que SABE ponerse rojo. Sin fail-first probado, un gate nuevo es una afirmación, no una garantía."
    - "MUST NOT mutar `.planning/ROADMAP.md` con las herramientas Write o Edit: es el anti-pattern #15 de GSD y una escritura de fichero completo destruye las entradas de fase que queden fuera de la ventana del diff. La corrección va por el skill `gsd-phase`."
    - "MUST NOT añadir `src/data/` al scope del gate del motor ni cambiar el commit base `0a9a2e5` por `HEAD~1`: `src/data/` divergió legítimamente (Phase 40 metió `migrate12to13`/`hydrateV13`, Phase 42 amplió `validation-state.js`) y un diff contra `HEAD~1` sale vacío siempre, así que sería un gate vacuo que no prueba nada."
    - "MUST NOT tocar `src/screens/app.js`, `src/domain/` ni `src/data/`, ni añadir dependencia, `package.json` de runtime o paso de build: el proyecto es web estática de dependencias cero (CLAUDE.md §Constraints) y el claim del milestone es que el motor v1.4 no se tocó."
    - "MUST NOT autorar contenido, cruces multi-categoría ni ids `-300`+, ni editar `content/exercises/fare-indicativo.json` o `content/exercises/fare-indefiniti.json`: son propiedad exclusiva del plan 44-02 y el orden de D-44-10 existe para que el marcador diga la verdad en cada commit."
    - "MUST NOT editar `docs/09-VALIDATION-PROMPT.md`: ese fichero NO EXISTE. Crearlo produce un huérfano que el quórum nunca lee, con la apariencia de que la excepción quedó declarada. La ruta real es `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md`."
---

<objective>
Cerrar la mitad **mecánica y documental** del milestone: enganchar las 4 categorías de `fare` a los DOS
arrays de conteo que quedaron ciegos, levantar el **gate anti-ceguera** que impide la cuarta repetición
del fallo, y corregir los documentos que ya no describen la realidad.

La primera tarea es un **tracer**: `fare-indicativo` de punta a punta por las tres capas que este plan
toca —el array del reporter, el array del back-compat y el gate nuevo—, verificado antes de que exista
una segunda entrada. Se elige `fare-indicativo` porque es la categoría con el prefijo ambiguo (`fare-ind`
también prefija `fare-indefiniti`, D-40-03), así que si el anclaje del gate está mal, está mal ahí
primero y no en las cuatro a la vez.

Cubre INT-01 (verificación y congelación del registro, ya cumplido en disco), INT-02 (los 2 arrays + el
gate) e INT-04 en su mitad documental (declarar los 4 magnets, que ya tienen su ronda extra ejecutada).

Purpose: **el reporter lleva tres fases emitiendo `Milestone gate PASS` estando ciego a 22 slots, y nada
lo detectó.** No es un descuido de Phase 43: es el mismo lockstep diferido que v1.7 ya documentó con
`presente-regolare`. El append son 8 líneas; lo que de verdad entrega este plan es el gate que hace
estructuralmente imposible que v2.1 vuelva a quedarse ciega, y unos documentos cuyos criterios de éxito
se pueden verificar porque describen lo que hay.

Output: `tests/count-arrays-lockstep.test.js` nuevo, 8 líneas nuevas repartidas entre
`scripts/run-validation-271.mjs` y `tests/fixtures/slot-variants-integration.test.js`, `REQUIREMENTS.md`
y `ROADMAP.md` corregidos, y `### 7.5` en el prompt de validación. El reporter pasa de `225/225 PASS`
(mintiendo) a `247/247 PASS` (verde honesto). Ningún fichero de `src/` ni de `content/` cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-CONTEXT.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-PATTERNS.md
@CLAUDE.md

<interfaces>
Código canon a clonar, verificado contra el disco el 2026-08-11.

`scripts/run-validation-271.mjs:170-188` — el helper y la forma de las 5 entradas dinámicas ya
presentes. El array está ordenado por slug: las 4 nuevas van entre `essere` y `genero-numero`.

```js
const slotCountOf = (file) =>
  JSON.parse(readFileSync(resolve(projectRoot, file), 'utf8')).exercises.length;

const CATEGORIES = [
  { slug: 'preposiciones',            file: 'content/exercises/preposiciones.json',            expected: 50 },
  { slug: 'articoli',                 file: 'content/exercises/articoli.json',                 expected: 34 },
  { slug: 'avere',                    file: 'content/exercises/avere.json',                    expected: 20 },
  { slug: 'dimostrativi',             file: 'content/exercises/dimostrativi.json',             expected: slotCountOf('content/exercises/dimostrativi.json') },
  { slug: 'essere',                   file: 'content/exercises/essere.json',                   expected: 26 },
  { slug: 'genero-numero',            file: 'content/exercises/genero-numero.json',            expected: 13 },
  …
];
```

`scripts/run-validation-271.mjs:204-214` — el guard que corre AL CARGAR el módulo. **No tocar.** Es la
razón técnica de que el gate anti-ceguera vaya por source-assert (D-44-07).

```js
{
  const TOTAL_EXPECTED_BASELINE = CATEGORIES.reduce((s, c) => s + slotCountOf(c.file), 0);
  if (TOTAL_EXPECTED !== TOTAL_EXPECTED_BASELINE) {
    console.error(`Incoherencia de conteo: TOTAL_EXPECTED (Σ literales)=${TOTAL_EXPECTED} != …`);
    process.exit(1);
  }
}
```

`tests/fixtures/slot-variants-integration.test.js:179-184` — las 4 entradas de Phase 39, dentro del
callback del `describe` que empieza en `:163` (por eso **no es importable**). El bucle de `:187` deriva
la ruta del slug: `content/exercises/${slug}.json`.

```js
    // v1.9 Phase 39 (INT-02): 4 categorías nuevas con expected DINÁMICO (D-31-06)
    // = exercises.length real del JSON, NO número mágico.
    { slug: 'dimostrativi', expected: readJson('content/exercises/dimostrativi.json').exercises.length },
    { slug: 'possessivi', expected: readJson('content/exercises/possessivi.json').exercises.length },
    { slug: 'modali', expected: readJson('content/exercises/modali.json').exercises.length },
    { slug: 'riflessivi', expected: readJson('content/exercises/riflessivi.json').exercises.length }
```

`tests/exercise-types.test.js:1534-1535` — el patrón **source-assert** a clonar: leer el TEXTO FUENTE de
un fichero que no se puede importar y asertar sobre él.

```js
describe('quick-260615-hr0: ver explicación al acertar', () => {
  const APP_SRC = readFileSync(new URL('../src/screens/app.js', import.meta.url), 'utf8');
```

`tests/content-fare-indicativo.test.js:36-39, 655, 693-700` — cómo se lee `categories.json` desde un
test de contenido y el gate estable de posición (WR-05: índice derivado, nunca número mágico).

```js
const CATEGORIES = JSON.parse(
  readFileSync(new URL('../content/categories.json', import.meta.url), 'utf-8')
);
…
const entradas = CATEGORIES.categories;
assert.equal(entradas.indexOf(cat), cat.order - 1, '…el array define el display (indice = order - 1)…');
```

`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md:313-321` — la
sección `### 7.4`, el molde de una excepción bien escrita: declara la indulgencia y a continuación
**«La frontera, y aquí NO hay indulgencia»** con el caso inverso que sí muerde. Las secciones existentes
llegan hasta `### 7.4`, así que la nueva es `### 7.5`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Tarea 1 (tracer): `fare-indicativo` de punta a punta por las tres capas de conteo</name>

  <files>scripts/run-validation-271.mjs, tests/fixtures/slot-variants-integration.test.js, tests/count-arrays-lockstep.test.js</files>

  <read_first>
    - `scripts/run-validation-271.mjs` líneas 155-215 (el comentario de procedencia, `slotCountOf`, `CATEGORIES`, `TOTAL_EXPECTED` y el bloque del baseline con su `process.exit(1)`)
    - `tests/fixtures/slot-variants-integration.test.js` líneas 35-50 (el helper `readJson` y `realCategories`) y 159-223 (el `describe` back-compat completo con `REAL_CATEGORIES` y su bucle)
    - `tests/exercise-types.test.js` líneas 1525-1548 (el patrón source-assert `APP_SRC` y el comentario que lo justifica)
    - `tests/content-fare-indicativo.test.js` líneas 34-44 y 650-700 (cómo se lee `content/categories.json` desde un test y el gate `indexOf === order - 1`)
    - `content/categories.json` (las 18 entradas, para conocer el key set y los `order`)
    - `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-PATTERNS.md` §«NUEVO — gate anti-ceguera»
  </read_first>

  <action>
Enganchar UNA sola categoría, `fare-indicativo`, por las tres capas que este plan modifica, y levantar
el gate nuevo con su prueba de fail-first. Es el slice fino: si el anclaje por slug está mal, se ve aquí.

Capa 1 — `scripts/run-validation-271.mjs`: añadir a `CATEGORIES` una entrada
`{ slug: 'fare-indicativo', file: 'content/exercises/fare-indicativo.json', expected: slotCountOf('content/exercises/fare-indicativo.json') }`,
colocada por orden alfabético de slug (el array va ordenado; `fare-indicativo` cae entre `essere` y
`genero-numero`), clonando el alineado de columnas de las entradas vecinas. Añadir encima un comentario
de una o dos líneas en la forma de los de Phase 31 y Phase 39: cita la fase, el requisito INT-02 y por
qué el `expected` es dinámico (D-31-06). NO tocar `TOTAL_EXPECTED` ni el bloque del
`TOTAL_EXPECTED_BASELINE`: los dos son `reduce` sobre `CATEGORIES` y se re-suman solos.

Capa 2 — `tests/fixtures/slot-variants-integration.test.js`: añadir a `REAL_CATEGORIES` la entrada
`{ slug: 'fare-indicativo', expected: readJson('content/exercises/fare-indicativo.json').exercises.length }`
al final del array, con su comentario de fase. El bucle de abajo deriva la ruta del slug, así que no hay
más código que escribir. Actualizar además los tres literales del texto que dicen «las 10 categorías
reales» —el título del `describe`, el título del test de bundle y el mensaje intermedio— a «las 18
categorías reales», porque hoy ya mienten con 14.

Capa 3 — fichero NUEVO `tests/count-arrays-lockstep.test.js`, que entra en el glob `tests/*.test.js` sin
configuración. Es un test ESM con `node:test` + `node:assert/strict` + `readFileSync`, en la línea de los
demás ficheros de `tests/`. Contiene:

  (1) Un helper PURO exportado dentro del módulo, `slugsCiegos(src, slugs)`, que recibe el TEXTO FUENTE
      de un fichero y una lista de slugs y devuelve la sublista de los que NO aparecen anclados en ese
      texto. El ancla es el slug COMPLETO dentro de una cadena entrecomillada precedida de la clave
      `slug:` — nunca `src.includes(slug)` a pelo, que daría verde a `fare-indicativo` con solo
      `fare-indefiniti` presente por compartir el prefijo `fare-ind` (D-40-03). Construir el patrón
      escapando el slug antes de meterlo en el `RegExp`.

  (2) Los TRES goldens, que son la prueba mecánica de fail-first y quedan committeados como invariante:
      un golden-negativo simple (texto fuente sintético sin la categoría probada → el helper la devuelve),
      un golden-negativo de COLISIÓN DE PREFIJO (texto sintético que contiene el slug hermano y no el
      probado → el helper sigue devolviendo el probado, en los dos sentidos de la pareja) y un
      golden-positivo (texto sintético con todos → devuelve lista vacía). Los tres operan sobre cadenas
      literales del propio test, no sobre el disco: es lo que hace la prueba determinista.

  (3) El gate real sobre las dos fuentes, por SOURCE-ASSERT: leer con `readFileSync(new URL(...))` el
      texto de `../scripts/run-validation-271.mjs` y de `./fixtures/slot-variants-integration.test.js`
      —NUNCA importarlos: el primero hace `process.exit(1)` al cargarse y el segundo tiene su array
      dentro del callback de un `describe`— y afirmar que `slugsCiegos` devuelve lista vacía. En esta
      tarea el gate se aplica a una lista TRACER de un solo elemento, `['fare-indicativo']`, declarada en
      una constante con un comentario que dice que la Tarea 2 la sustituye por las 18 de
      `content/categories.json`.

Escribir la cabecera del fichero en el estilo de los demás tests de contenido: qué invariante congela,
por qué va por source-assert (citando el `process.exit(1)` del reporter y el `const` dentro del
`describe`), y la advertencia de que este gate es lo único que impide la cuarta repetición del lockstep
diferido. Sin acentos en los comentarios de código si el fichero vecino tampoco los lleva; las
explanations acentuadas son cosa del contenido, no de los tests.
  </action>

  <verify>
    <automated>node --test tests/count-arrays-lockstep.test.js &amp;&amp; node --test tests/*.test.js 2>&amp;1 | tail -6 &amp;&amp; node scripts/run-validation-271.mjs | grep -F 'VAL-06 (233/233 validated)'</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/count-arrays-lockstep.test.js` cierra con `fail 0` y al menos 4 tests: los 3 goldens y el gate real sobre las dos fuentes.
    - `node --test tests/*.test.js` cierra con `fail 0` y `pass` estrictamente mayor que 1026 (baseline verificado 2026-08-11).
    - `node scripts/run-validation-271.mjs` sale con código 0 y su salida contiene la subcadena `VAL-06 (233/233 validated)` seguida de `PASS` — 225 previos + los 8 slots de `fare-indicativo`.
    - `node -e "const s=require('fs').readFileSync('scripts/run-validation-271.mjs','utf8'); const m=s.match(/const CATEGORIES = \[[\s\S]*?\n\];/)[0]; console.log(m.split('\n').filter(l=>/slug:/.test(l)).length)"` imprime `15`.
    - `node -e "const s=require('fs').readFileSync('scripts/run-validation-271.mjs','utf8'); const m=s.match(/const CATEGORIES = \[[\s\S]*?\n\];/)[0]; const l=m.split('\n').find(x=>/'fare-indicativo'/.test(x)); console.log(/expected:\s*slotCountOf\(/.test(l))"` imprime `true` — el `expected` es dinámico, no literal.
    - `node -e "const s=require('fs').readFileSync('tests/fixtures/slot-variants-integration.test.js','utf8'); const m=s.match(/const REAL_CATEGORIES = \[[\s\S]*?\n  \];/)[0]; console.log(m.split('\n').filter(l=>/slug:/.test(l)).length, /'fare-indicativo'[\s\S]{0,120}exercises\.length/.test(m))"` imprime `15 true`.
    - El literal desfasado desapareció del CÓDIGO (no de los comentarios, que pueden citarlo con su audit trail): `grep -v '^\s*//' tests/fixtures/slot-variants-integration.test.js | grep -c 'las 10 categorías reales'` da `0`, y `grep -c 'las 18 categorías reales' tests/fixtures/slot-variants-integration.test.js` da al menos `1`.
      <!-- planner-discipline-allow: las 10 categorías reales -->
    - El golden-negativo de colisión de prefijo existe y es específico: `grep -c 'fare-indefiniti' tests/count-arrays-lockstep.test.js` da al menos `1` (el slug hermano aparece en la cadena sintética del golden).
    - El gate NO importa las fuentes: `grep -cE "^\s*import .*(run-validation-271|slot-variants-integration)" tests/count-arrays-lockstep.test.js` da `0`.
    - `git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` no produce salida.
  </acceptance_criteria>

  <done>`fare-indicativo` está enganchada a las dos capas de conteo, el reporter la cuenta (233/233 PASS) y existe un gate nuevo que, con su golden-negative committeado, ha demostrado mecánicamente que sabe ponerse rojo y que no se deja engañar por el prefijo compartido `fare-ind`.</done>
</task>

<task type="auto">
  <name>Tarea 2: las 3 categorías restantes y el gate generalizado a las 18 registradas</name>

  <files>scripts/run-validation-271.mjs, tests/fixtures/slot-variants-integration.test.js, tests/count-arrays-lockstep.test.js</files>

  <read_first>
    - `tests/count-arrays-lockstep.test.js` (el fichero creado en la Tarea 1, entero)
    - `scripts/run-validation-271.mjs` líneas 170-215 (el array ya con 15 entradas)
    - `tests/fixtures/slot-variants-integration.test.js` líneas 159-223
    - `content/categories.json` (las 18 entradas con sus `order` y `origen`)
    - `tests/content-fare-indicativo.test.js` líneas 650-700 (el `describe` §12 de registro de categoría: el gate `indexOf === order - 1` y el key set de 4 claves, que aquí se generaliza a las 18)
  </read_first>

  <action>
Completar el lockstep y convertir el gate tracer en el gate real de las 18 categorías registradas.

Arrays: añadir las 3 entradas que faltan —`fare-cond-imperativo`, `fare-congiuntivo` y
`fare-indefiniti`— a `CATEGORIES` de `scripts/run-validation-271.mjs` y a `REAL_CATEGORIES` de
`tests/fixtures/slot-variants-integration.test.js`, con la misma forma dinámica de la Tarea 1. En el
reporter respetar el orden alfabético por slug: las cuatro de `fare` quedan agrupadas entre `essere` y
`genero-numero` en el orden `fare-cond-imperativo`, `fare-congiuntivo`, `fare-indefiniti`,
`fare-indicativo`. Declarar los slugs COMPLETOS byte a byte; no truncar nunca a `fare-ind` (D-40-03).

Gate: sustituir la constante tracer de un solo slug por la lista REAL, leída de `content/categories.json`
en tiempo de test (`.categories.map(c => c.id)`) — nunca una lista de slugs escrita a mano dentro del
test, que sería un gate vacuo comparándose consigo mismo. Los tres goldens de la Tarea 1 se quedan como
están: son la prueba de fail-first y no dependen del disco.

Añadir al mismo fichero los dos gates que cierran los casos vacíos y de orden que el guard dinámico del
reporter no puede ver:

  (a) Presencia y no vacuidad: para cada categoría registrada, `content/exercises/<slug>.json` existe,
      parsea y tiene `exercises` con longitud ≥ 1. Con `expected` dinámico un fichero de cero ejercicios
      da cero en los DOS lados del guard de coherencia y pasa desapercibido, así que el rojo tiene que
      venir de aquí. El mensaje de fallo nombra el slug y la ruta que faltó.

  (b) Orden y display: los 18 `order` son únicos, cubren el rango contiguo de 1 al número de entradas, y
      para cada entrada `entradas.indexOf(cat) === cat.order - 1`. Es la forma estable que WR-05 ya fijó
      en `tests/content-fare-indicativo.test.js` para una categoría, generalizada a las 18: el ARRAY es
      lo que `categoriesForDisplay` recorre y `order` es documental; si divergen, el display deja de
      coincidir con el número escrito. Añadir en el mismo bloque el key set exacto de 4 claves por
      entrada y `origen: 'ia-quorum'` en las 8 categorías que lo declaran, para que INT-01 quede
      congelado como invariante y no como observación de una vez.
  </action>

  <verify>
    <automated>node --test tests/*.test.js 2>&amp;1 | tail -6 &amp;&amp; node scripts/run-validation-271.mjs | grep -F 'VAL-06 (247/247 validated)'</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/*.test.js` cierra con `fail 0` y `pass` ≥ 1034 (1026 de baseline + 8 tests del bucle de `REAL_CATEGORIES`, 2 por categoría nueva, más los del gate).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` cierra con `fail 0`: este plan no añade contenido, así que las 18 categorías siguen `validated`.
    - `node scripts/run-validation-271.mjs` sale con código 0 y su salida contiene `VAL-06 (247/247 validated)` con `PASS`, `VAL-08 (cero disputed): PASS`, `VAL-04 (≥2 distinct AIs por validated): PASS` y `Milestone gate PASS`.
    - `node -e "const s=require('fs').readFileSync('scripts/run-validation-271.mjs','utf8'); const m=s.match(/const CATEGORIES = \[[\s\S]*?\n\];/)[0]; const l=m.split('\n').filter(x=>/slug:/.test(x)); console.log(l.length, l.filter(x=>/expected:\s*\d/.test(x)).length)"` imprime `18 9` — 18 entradas y exactamente los 9 `expected` literales preexistentes, ninguno nuevo.
    - `node -e "const s=require('fs').readFileSync('tests/fixtures/slot-variants-integration.test.js','utf8'); const m=s.match(/const REAL_CATEGORIES = \[[\s\S]*?\n  \];/)[0]; console.log(m.split('\n').filter(l=>/slug:/.test(l)).length)"` imprime `18`.
    - Los 4 slugs completos están en las dos fuentes: `for f in scripts/run-validation-271.mjs tests/fixtures/slot-variants-integration.test.js; do for s in fare-cond-imperativo fare-congiuntivo fare-indefiniti fare-indicativo; do grep -qF "'$s'" "$f" || echo "FALTA $s en $f"; done; done` no imprime nada.
    - El gate lee la lista real del disco: `grep -cE "categories\.json" tests/count-arrays-lockstep.test.js` da al menos `1`, y `grep -cE "\.categories\.map\(" tests/count-arrays-lockstep.test.js` da al menos `1`.
    - Prueba de mordida en caliente (se ejecuta y se revierte, sin commitear): al borrar la línea de `fare-congiuntivo` del array del reporter en una copia scratch, `slugsCiegos` sobre ese texto devuelve `['fare-congiuntivo']` — verificable con `node -e "..."` sobre el fichero copiado a `/tmp`; el fichero del repo queda intacto (`git diff --stat scripts/run-validation-271.mjs` refleja solo las 4 líneas nuevas).
    - Gates nuevos presentes: `node --test tests/count-arrays-lockstep.test.js` lista un test cuyo nombre menciona el fichero ausente o vacío y otro que menciona el orden de display.
    - `git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` no produce salida y `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` da `2`.
  </acceptance_criteria>

  <done>Las 18 categorías registradas están enganchadas a los dos arrays de conteo, el reporter dice la verdad (`247/247 PASS`) y el gate anti-ceguera cubre las tres formas de ceguera —slug no enganchado, fichero ausente o vacío, y orden de display divergente— leyendo su lista de referencia del disco.</done>
</task>

<task type="auto">
  <name>Tarea 3: los documentos dejan de mentir — REQUIREMENTS, ROADMAP y el prompt del quórum</name>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` líneas 55-61 (INT-01..INT-04) y 112-125 (§Mapping rationale y §Estado del codebase al fijar el roadmap)
    - `.planning/ROADMAP.md` línea 17 (§Milestones, la entrada de v2.0) y líneas 251-263 (§Phase 44 con Goal, Requirements y los 4 Success Criteria)
    - `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` §7 completo, y en especial `### 7.3` («Lo que estas excepciones NO relajan») y `### 7.4` (el molde de redacción)
    - `content/exercises/fare-indefiniti.json` — el bloque `validation` del slot `fare-indefiniti-infinito-passato` (el CUARTO magnet) y su `notes`
    - `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-CONTEXT.md` §D-44-08 (la tabla de correcciones exactas)
  </read_first>

  <action>
Poner los tres documentos a describir la realidad, y verificar en disco lo que INT-01 e INT-04 declaran.

`.planning/REQUIREMENTS.md` — edición DIRECTA permitida (no es ROADMAP ni STATE). Tres correcciones por
D-44-08: en INT-02 sustituir «los 3 arrays hardcoded de count» por **2** arrays, y nombrar el gate
anti-ceguera como parte del requisito (los otros arrays los engancharon las Phases 41/42/43); en INT-03
sustituir `verbi-modali` por el slug real `modali`, que es el único que `src/data/schema-validator.js`
acepta; en INT-04 sustituir «los 3 magnets» por **4**, añadiendo el par `aver fatto` / `avere fatto`
(D-43-17). Actualizar además §Estado del codebase, que sigue diciendo 14 categorías, 225 slots y «225 +
los 21 slots nuevos»: hoy son 18 categorías, 247 slots en disco y 22 slots nuevos. Marcar INT-01..INT-04
como completados en la tabla de §Traceability cuando el plan cierre, no antes.

`.planning/ROADMAP.md` — **NO se edita con Write ni con Edit**: es el anti-pattern #15 de GSD y una
escritura de fichero completo destruye las fases que queden fuera del diff. La corrección va por el skill
`gsd-phase` (CRUD de fases del roadmap), invocado con la edición de §Phase 44. Correcciones: SC#2 pasa a
decir **22** slots nuevos, **247** en disco tras Phase 43 y **250** con los 3 cruces, y menciona que los
arrays que faltaban eran 2, no 3; SC#3 nombra los ids concretos `fare-indicativo-300`,
`fare-indicativo-301` y `fare-indefiniti-300` con sus parejas de `categoryIds` y con el sentido de
D-44-02 (la key vive en la categoría VECINA y la forma de `fare` va escrita como contexto), y cambia
`verbi-modali` por `modali`; SC#4 pasa a **113** variantes y **4** magnets. Y la entrada de §Milestones
de v2.0 (cabecera del fichero) pasa de «21 slots ≈ 107 variantes» a **22 slots / 113 variantes**. Si el
skill no permite tocar la línea de §Milestones, dejar constancia del intento y de la línea exacta en el
SUMMARY en vez de forzar una edición directa.

`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` —
**esta es la ruta REAL** que el skill `gsd-validate-exercise` lee en su Paso 1; `docs/09-VALIDATION-PROMPT.md`
no existe y crearlo produciría un huérfano que el quórum nunca abre. Añadir una sección `### 7.5` (las
existentes llegan a `7.4`) que declare el CUARTO magnet: el par `aver fatto` / `avere fatto` del slot
`fare-indefiniti-infinito-passato`, es decir que la apócope y la forma plena son ambas norma y que ver
las dos en el corpus no es una incoherencia que haya que marcar. Clonar la estructura de `### 7.4`: qué
NO es falso positivo, luego el bloque de la frontera con lo que SÍ sigue mordiendo, y cierre remitiendo a
`### 7.3`. El subagent del quórum arranca con contexto vacío y NUNCA ve el `notes`, así que una excepción
que solo viva en el `notes` no existe para él.

Verificación de INT-01 e INT-04 sobre el disco (no son ediciones, son comprobaciones que el SUMMARY
recoge): que `content/categories.json` tiene 18 entradas con las 4 de `fare` en `order` 15-18 y
`origen: "ia-quorum"`, y que los cuatro magnets tienen su ronda extra registrada en `validation.passes`.
  </action>

  <verify>
    <automated>node -e "const c=require('./content/categories.json').categories; const f=c.filter(x=>x.id.startsWith('fare-')); if(c.length!==18||f.length!==4) throw new Error('categorias '+c.length+'/'+f.length); for(const x of f){ if(x.origen!=='ia-quorum') throw new Error('origen '+x.id); if(c.indexOf(x)!==x.order-1) throw new Error('order '+x.id);} console.log('INT-01 OK', f.map(x=>x.id+':'+x.order).join(' '))" &amp;&amp; grep -c '### 7.5' .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md &amp;&amp; node --test tests/*.test.js 2>&amp;1 | tail -4</automated>
  </verify>

  <acceptance_criteria>
    - INT-01 en disco: el comando de `<verify>` imprime `INT-01 OK fare-indicativo:15 fare-congiuntivo:16 fare-cond-imperativo:17 fare-indefiniti:18` y no lanza.
    - INT-04 en disco, los 4 magnets con ronda extra: `node -e "const j=p=>require(p); const n=(f,id)=>j('./content/exercises/'+f+'.json').exercises.find(e=>e.id===id).validation.passes.length; console.log(n('fare-cond-imperativo','fare-cond-imperativo-imperativo'), n('fare-indefiniti','fare-indefiniti-participio-passato'), n('fare-indefiniti','fare-indefiniti-infinito-passato'))"` imprime tres números, todos ≥ 3.
    - El cuarto magnet lleva su pase cross-vendor: `node -e "const s=require('./content/exercises/fare-indefiniti.json').exercises.find(e=>e.id==='fare-indefiniti-infinito-passato'); console.log(s.validation.passes.some(p=>String(p.by).toLowerCase().startsWith('deepseek')))"` imprime `true`.
    - `grep -c 'verbi-modali' .planning/REQUIREMENTS.md` da `0` — el slug equivocado desaparece del documento, no se anota al lado del correcto.
      <!-- planner-discipline-allow: verbi-modali -->
    - `grep -cE 'INT-02.*\*\*2\*\* arrays|2 arrays hardcoded' .planning/REQUIREMENTS.md` da al menos `1`, y la línea de INT-04 contiene la subcadena `aver fatto`: `grep -c 'aver fatto' .planning/REQUIREMENTS.md` da al menos `1`.
    - `grep -c '225 + los 21 slots nuevos' .planning/REQUIREMENTS.md` da `0`.
      <!-- planner-discipline-allow: 225 + los 21 slots nuevos -->
    - `grep -c '21 slots ≈ 107 variantes' .planning/ROADMAP.md` da `0`; `grep -c '113' .planning/ROADMAP.md` y `grep -c '22 slots' .planning/ROADMAP.md` dan al menos `1` cada uno.
      <!-- planner-discipline-allow: 21 slots ≈ 107 variantes -->
    - §Phase 44 del ROADMAP nombra los tres ids: `grep -c 'fare-indicativo-300' .planning/ROADMAP.md`, `grep -c 'fare-indicativo-301' .planning/ROADMAP.md` y `grep -c 'fare-indefiniti-300' .planning/ROADMAP.md` dan al menos `1` cada uno.
    - El ROADMAP se editó por skill, no por herramienta de fichero: el SUMMARY declara la invocación de `gsd-phase` y `git log -1 --name-only` no muestra una escritura de fichero completo de `.planning/ROADMAP.md` fuera de esa vía.
    - `grep -c '### 7.5' .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` da `1`, y esa sección contiene tanto `avere fatto` como una línea de frontera que remite a `7.3`.
    - `ls docs/09-VALIDATION-PROMPT.md 2>/dev/null | wc -l` da `0` — no se creó el fichero huérfano.
    - `node --test tests/*.test.js` sigue con `fail 0` y `node scripts/run-validation-271.mjs` sigue en `Milestone gate PASS`.
  </acceptance_criteria>

  <done>REQUIREMENTS, ROADMAP y el prompt del quórum describen el estado real del milestone (18 categorías, 22 slots, 113 variantes, 247 en disco, 250 con los cruces, 4 magnets, slug `modali`), INT-01 e INT-04 están verificados contra el disco, y la corrección del roadmap pasó por el skill `gsd-phase`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Honestidad sobre la superficie real: esta es una app web estática, monousuario, sin servidor, sin red en
runtime, sin cuentas y sin entrada de usuario más allá de `localStorage`. La superficie de ataque clásica
(inyección, authz, transporte) **no existe aquí**. Lo que sí es un riesgo real y demostrado es la
**integridad de los datos y de la evidencia**: un conteo que miente y un audit trail que no se puede
creer. El modelo de amenazas de esta fase es ese, y no otro.

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano → `content-loader.js` → DOM | contenido de confianza pero frágil: un id duplicado, un fichero ausente o un conteo desincronizado degradan en silencio |
| `localStorage` → `hydrateV13` → estado de sesión | única persistencia; este plan NO la cruza (no toca `src/data/`) |
| arrays de conteo del reporter y de los tests → decisión humana de cerrar el milestone | el reporter es la EVIDENCIA sobre la que el autor decide; si emite un PASS ciego, la decisión se toma sobre datos falsos |

## STRIDE Threat Register

ASVS nivel 1 · bloqueo en `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Repudiation | `scripts/run-validation-271.mjs` como evidencia de cierre de milestone | high | mitigate | Tarea 1 + Tarea 2: las 4 entradas dinámicas hacen que `TOTAL_EXPECTED` siga al disco, y `tests/count-arrays-lockstep.test.js` falla si una categoría registrada en `content/categories.json` no aparece en los dos arrays — el modo de fallo que produjo tres fases de `PASS` mentiroso |
| T-44-02 | Tampering | los dos arrays de conteo (`CATEGORIES`, `REAL_CATEGORIES`) | high | mitigate | prohibición de `expected` literal en las entradas nuevas, verificada con un conteo de literales (`18 9`); el guard de coherencia del reporter (`process.exit(1)`) se mantiene intacto como segunda red |
| T-44-03 | Tampering | `content/categories.json` (fichero ausente, `exercises` vacío, `order` divergente del índice del array) | medium | mitigate | Tarea 2: gates de presencia y no vacuidad, y de `indexOf === order - 1` sobre las 18 entradas — los tres casos que el guard dinámico no puede distinguir de un cero legítimo |
| T-44-04 | Tampering | `.planning/ROADMAP.md` | medium | mitigate | la edición va por el skill `gsd-phase`; prohibición explícita de Write/Edit, que sobre este fichero destruye las fases fuera de la ventana del diff (anti-pattern #15) |
| T-44-05 | Information disclosure | datos del autor | low | accept | no hay datos personales, ni red, ni cuentas, ni telemetría: la app corre en `http://localhost` sobre `localStorage` de una sola persona. No hay nada que revelar |
| T-44-06 | Elevation of privilege | superficie de ejecución | low | accept | sin backend, sin autenticación y sin roles: no existe privilegio que elevar |
| T-44-SC | Tampering | instalación de paquetes npm/pip/cargo | n/a | accept | **este plan no instala ningún paquete.** El proyecto es de dependencias cero por constraint (CLAUDE.md §Constraints: web estática, sin `npm install`), no hay `package.json` de runtime y ninguna tarea ejecuta un gestor de paquetes. La puerta de legitimidad de paquetes no aplica; si una tarea futura la necesitara, exige antes la tabla `## Package Legitimacy Audit` en RESEARCH.md |
</threat_model>

<artifacts_produced>
## Artefactos que produce este plan

**Ficheros nuevos**
- `tests/count-arrays-lockstep.test.js`

**Símbolos nuevos**
- `slugsCiegos(src, slugs)` — helper puro del gate anti-ceguera
- `COUNT_ARRAY_SOURCES` — las dos rutas de fuente leídas por source-assert
- La constante de la lista de categorías registradas, derivada de `content/categories.json` (en la Tarea 1 es una lista tracer de un solo slug y la Tarea 2 la sustituye)
- Los tres goldens del helper: negativo simple, negativo de colisión de prefijo `fare-ind`, y positivo

**Entradas nuevas en arrays existentes**
- `CATEGORIES` (`scripts/run-validation-271.mjs`): `fare-cond-imperativo`, `fare-congiuntivo`, `fare-indefiniti`, `fare-indicativo` — 14 → 18
- `REAL_CATEGORIES` (`tests/fixtures/slot-variants-integration.test.js`): las mismas 4 — 14 → 18

**Secciones nuevas de documento**
- `### 7.5` en `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` — el CUARTO magnet

**Lo que este plan NO crea** (para que nadie lo busque ni lo duplique)
- Ninguna entrada nueva en `content/categories.json` — INT-01 ya está cumplido en disco
- Ninguna entrada nueva en `CATEGORIES_WITH_EXPLANATIONS` ni en el smoke `VAL_07_STRICT` — las 4 de `fare` ya están (`tests/exercise-types.test.js:1338` y `:1503`)
- Ningún id de ejercicio, ningún slot, ninguna variante — son del plan 44-02
</artifacts_produced>

<flagged_assumptions>
## Supuestos marcados (probe spec-less, sin resolver)

Esta fase no tiene SPEC.md, así que la sonda de edge corrió sobre INT-01..INT-04. De sus 8 filas, 5
(las de INT-02: `boundary`, `precision`, `empty`, `adjacency`, `ordering`) están authoradas como
predicados en `must_haves.truths` de este plan. Las 3 restantes salieron `unclassified` y se quedan
**`unresolved`**: se marcan aquí y NUNCA se descartan en silencio.

- **INT-01 — `unclassified`.** La sonda no pudo clasificar el requisito («4 entradas nuevas en
  `categories.json` … sin romper el display de la tabla del home»). **Supuesto de este plan:** «sin
  romper el display» se interpreta como *el array de `content/categories.json` es lo que
  `categoriesForDisplay` recorre, así que basta con que las 18 entradas existan, tengan las 4 claves y
  su índice coincida con `order - 1`*; se verifica con el gate de orden de la Tarea 2 y con el comando
  de `<verify>` de la Tarea 3, pero **NO se verifica visualmente** que las 18 filas se pinten bien en
  home, picker, Repaso y Examen. Si el autor quiere esa prueba, es UAT humano, no un test.
- **INT-03 y INT-04 — `unclassified`.** Se marcan en el plan 44-02, que es donde vive su trabajo.

**Igualdad de no-descarte-silencioso (fase completa):** 8 filas de la sonda = 5 authoradas en
`must_haves.truths` (44-01) + 3 marcadas como supuesto explícito (INT-01 aquí, INT-03 e INT-04 en 44-02).
</flagged_assumptions>

<verification>
Al cerrar el plan, todo esto en verde a la vez:

1. `node --test tests/*.test.js` → `fail 0`, `pass` ≥ 1034.
2. `VAL_07_STRICT=1 node --test tests/*.test.js` → `fail 0`.
3. `node scripts/run-validation-271.mjs` → código de salida 0, con `VAL-06 (247/247 validated): PASS (247/247)`, `VAL-08 (cero disputed): PASS`, `VAL-04 (≥2 distinct AIs por validated): PASS` y `Milestone gate PASS`.
4. `git diff 0a9a2e5..HEAD -- src/screens/app.js src/domain/` → sin salida. **No añadir `src/data/` a este comando:** divergió legítimamente en Phases 40 y 42 y el gate fallaría por diseño (D-44-09).
5. `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` → `2`.
6. Los dos arrays con 18 entradas y cero `expected` literales nuevos.
7. `content/categories.json` sin cambios: `git diff --stat content/` no produce salida.
</verification>

<success_criteria>
- INT-01 verificado y congelado: 18 entradas, las 4 de `fare` en `order` 15-18 con `origen: "ia-quorum"`, y el orden del array coherente con el `order` en las 18.
- INT-02 cerrado: los 2 arrays enganchados con `expected` dinámico y el gate anti-ceguera activo en cada `node --test tests/*.test.js`, con su fail-first probado por golden-negative committeado.
- INT-04 en su mitad documental: los 4 magnets verificados en disco y declarados en REQUIREMENTS y en `### 7.5` del prompt de validación.
- Los documentos describen la realidad y el ROADMAP se corrigió por el skill `gsd-phase`.
- El reporter pasa de mentir (`225/225 PASS` ciego a 22 slots) a decir la verdad (`247/247 PASS`).
- Cero líneas de motor tocadas.
</success_criteria>

<output>
Crear `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-01-SUMMARY.md` al terminar, recogiendo:
los conteos antes/después del reporter, la salida literal de las 3 líneas de sub-gates, la lista de
tests nuevos del gate anti-ceguera, la constancia de que el ROADMAP se editó por `gsd-phase`, y la
verificación en disco de los 4 magnets.
</output>
