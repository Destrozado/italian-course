---
phase: 40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/storage.js
  - src/data/backup.js
  - tests/data-storage.test.js
  - tests/backup.test.js
autonomous: false
requirements: [MIG-01, MIG-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # --- SC-1 / MIG-01 : el reset selectivo ---
    - "Tras `migrate12to13`, los 4 slugs `fare-indicativo`, `fare-congiuntivo`, `fare-cond-imperativo` y `fare-indefiniti` están ausentes de `categoryProgress` (`Object.prototype.hasOwnProperty.call(cp, slug) === false`, no solo `undefined`) y ninguna clave de `exerciseStats` que empiece por esos prefijos sobrevive — arrancan `no-hecha` con racha 0 (D-40-01, D-40-04, MIG-01, SC-1)."
    # --- SC-2 / MIG-01 : no-regresión de las 14 legacy ---
    - "Las 14 categorías legacy (`avere`, `essere`, `preposiciones`, `verbos-movimiento`, `sustantivos-irregulares`, `genero-numero`, `profesiones`, `articoli`, `partitivos`, `presente-regolare`, `dimostrativi`, `possessivi`, `modali`, `riflessivi`) y `songProgress` quedan byte-intactas: snapshot `deepEqual` pre/post por categoría en `categoryProgress` y en `exerciseStats` (D-40-03, MIG-01, SC-2)."
    # --- edge: adjacency (MIG-01) — resuelto COVERED ---
    - "Un fixture v12 que contiene simultáneamente `fare-indicativo-001` y `fare-indefiniti-001` (los dos slugs que comparten el prefijo textual `fare-ind`) deja AMBAS claves ausentes tras `migrate12to13`, y ninguna clave legacy se elimina como efecto colateral de ese solape (D-40-03, D-40-05; edge: adjacency)."
    # --- edge: empty (MIG-01) — resuelto COVERED ---
    - "`migrate12to13` con `categoryProgress`, `exerciseStats`, `dailyLog` o `songProgress` puestos a `null`, `'x'` o `42` devuelve `{}` en ese sub-dict sin lanzar; un input sin `inFlightTest` devuelve `inFlightTest === undefined`; y `blankState().schemaVersion === 13` (D-40-08; edge: empty)."
    # --- edge: ordering (MIG-01) — resuelto COVERED ---
    - "`migrate12to13` es idempotente y pura: `deepEqual(migrate12to13(migrate12to13(x)), migrate12to13(x))` y el fixture v12 original conserva los 4 slugs en ambos dicts tras la llamada; el root devuelto tiene el mismo key set en las dos pasadas (D-40-08; edge: ordering)."
    # --- SC-2 : anti-prototype-pollution ---
    - "`migrate12to13` y `hydrateV13`, aplicados a un state construido con `JSON.parse` de un literal que declara `__proto__` como own-property, dejan `({}).polluted === undefined` y devuelven `schemaVersion === 13` (MIG-01, SC-2, T-40-01)."
    # --- D-40-09 : hydrate es shape-only ---
    - "`hydrateV13` sobre un state v13 que contiene `fare-indicativo` en `categoryProgress` y `fare-indicativo-001` en `exerciseStats` los PRESERVA (no repite la poda), y devuelve cada sub-dict deep-clonado (`assert.notEqual` por referencia frente al input) (D-40-09)."
    # --- SC-3 / MIG-02 : backup ---
    - "`buildBackupWrapper` de un state v13 seguido de `parseBackupFile` devuelve `ok: true` con `state.schemaVersion === 13` y el progreso de las categorías legacy `deepEqual` al de partida (MIG-02, SC-3)."
    - "Un wrapper `schemaVersion: 12` cuyo state tiene progreso bajo `fare-indicativo` importa con `ok: true`, sale en `schemaVersion === 13` con `fare-indicativo` ausente de `categoryProgress` y de `exerciseStats`, y con `preposiciones` byte-intacta (D-40-11, MIG-02, SC-3)."
    # --- edge: boundary (MIG-02) — resuelto COVERED ---
    - "`parseBackupFile` acepta wrappers con `schemaVersion` 12 y 13 (ambos salen en 13) y rechaza 14 con `ok: false` y `reason` que casa `/versión más nueva/i` — un paso a cada lado del nuevo máximo (MIG-02, SC-3; edge: boundary)."
    # --- edge: empty (MIG-02) — resuelto COVERED ---
    - "`parseBackupFile` con `state.schemaVersion` ausente, `null` o no-numérico (p. ej. la cadena `'13'`) devuelve `ok: false` con `reason` que casa `/falta o no es número/` — el guard preexistente de `src/data/backup.js` sigue disparando después del bump (edge: empty)."
    # --- edge: precision (MIG-02) — resuelto COVERED ---
    - "`parseBackupFile` con `state.schemaVersion` no entero o menor que 1 (`12.5`, `13.5`, `NaN`, `0`, `-1`) devuelve `ok: false` con `reason` que casa `/schemaVersion inválido/`; nunca alcanza el dispatcher de migración ni se hidrata silenciosamente a 13 (edge: precision)."
    # --- edge: encoding (MIG-02) — resuelto BACKSTOP ---
    - statement: "La comparación de prefijo de `RESET_PREFIXES_V13` es por unidades de código UTF-16 (`String.prototype.startsWith`), y los 4 slugs son ASCII puro sin marcas combinantes, así que ninguna forma de normalización Unicode cambia qué claves se resetean: una clave que solo casaría el prefijo tras normalizar NO se resetea y el progreso del autor se preserva."
      verification: backstop
    # --- SC-3 : constante espejada ---
    - "`CURRENT_SCHEMA_VERSION` vale 13 tanto en `src/data/storage.js` como en `src/data/backup.js` — las dos constantes nunca quedan desincronizadas (D-40-09, SC-3)."
    # --- SC-4 : suite + valla del motor ---
    - "`node --test tests/*.test.js` termina con 0 fails (baseline 672 pass más los tests v13 nuevos) y `git status --porcelain` no lista ningún fichero fuera de los 4 declarados en `files_modified` — en particular `src/screens/app.js`, `src/domain/` y `content/` quedan sin tocar (D-40-06, SC-4)."

  artifacts:
    - "`src/data/storage.js` — contiene `RESET_PREFIXES_V13` con su comentario de bloque, `export function migrate12to13`, `export function hydrateV13`, y `CURRENT_SCHEMA_VERSION = 13`."
    - "`src/data/backup.js` — importa `migrate12to13` y `hydrateV13` de `./storage.js`, tiene `CURRENT_SCHEMA_VERSION = 13` y la frase de Phase 40 apendizada a su comentario de bloque acumulativo."
    - "`tests/data-storage.test.js` — bloque `describe` v13 con los tests de `migrate12to13` / `hydrateV13`, incluido el fixture de no-regresión de 14 legacy."
    - "`tests/backup.test.js` — bloque `describe` v13 con round-trip, import `v12→v13`, el test tracer end-to-end y los tests de frontera 12/13/14."

  key_links:
    - "Dispatcher de `migrate()` en `src/data/storage.js`: `if (s.schemaVersion === 12) s = migrate12to13(s);` seguido de `if (s.schemaVersion === 13) return hydrateV13(s);` — si el segundo falta, un state v13 cae al `console.warn` y el autor pierde su progreso al arrancar."
    - "Cadena de import en `src/data/backup.js`: `if (migrated.schemaVersion === 12) migrated = migrate12to13(migrated);` y la cola incondicional `migrated = hydrateV13(migrated);` — es el único punto por el que entra JSON no confiable al state."
    - "Espejo `CURRENT_SCHEMA_VERSION` `src/data/storage.js` ↔ `src/data/backup.js` — si derivan, un export propio se auto-rechaza como 'versión más nueva'."
    - "`RESET_PREFIXES_V13` es consumido por DOS sitios: la poda de `exerciseStats` y la invalidación de `inFlightTest`. Actualizar uno solo deja un test en vuelo apuntando a ids ya borrados."

  prohibitions:
    - "No se introduce ninguna dependencia nueva, ni `package.json`, ni paso de build: el proyecto es web estática con ES modules nativos y `node --test` sin instalar nada (CLAUDE.md §Constraints)."
    - "El predicado del reset no usa el prefijo paraguas de familia ni prefijos truncados — los 4 slugs se declaran completos y literales (D-40-04)."
    - "`hydrateV13` no ejecuta la poda por prefijo ni ningún borrado de clave: es shape-only, espejo de `hydrateV12` (D-40-09)."
    - "No se tocan `content/categories.json`, `content/exercises/`, `TOTAL_EXPECTED`, los counts del reporter ni los cruces multi-categoría — pertenecen a las Phases 41-44 (D-40-06)."
    - "No se 'arreglan de paso' las discrepancias de conteo VAL-06 preexistentes de `genero-numero` y `preposiciones` (D-40-12)."
    - "No se reescribe el historial de los comentarios de bloque acumulativos de `src/data/backup.js` ni de los JSDoc de migración: la convención del codebase es APENDIZAR una frase nueva que nombre la fase y la decisión (D-40-05)."
    - "No se modifican las aserciones de los `describe` de versiones intermedias (v1..v11) que comprueban su PROPIA versión — solo bumpean las de fin-de-cadena y las de `blankState()`."
---

<objective>
Subir el state del proyecto a `schemaVersion 13` con un eslabón `migrate12to13` + `hydrateV13` que
resetea selectivamente, por prefijo de id, el progreso de las 4 categorías nuevas de v2.0
(`fare-indicativo`, `fare-congiuntivo`, `fare-cond-imperativo`, `fare-indefiniti`), dejando el
terreno limpio para que nazcan sin estado espurio en las Phases 41-43. Es un mirror casi verbatim de
`migrate11to12` / `RESET_PREFIXES_V12` (v1.9, Phase 35) una versión más arriba.

Cubre MIG-01 (`migrate12to13` + `hydrateV13` idempotentes, puros, anti-prototype-pollution, con reset
selectivo por prefijo) y MIG-02 (`backup.js` con round-trip v13, import `v12→v13` y rechazo de
wrappers `> 13`).

Purpose: es la invariante del proyecto desde v1.5 — la migración con reset selectivo va ANTES de dar
de alta contenido, para que las categorías nuevas nazcan sin progreso espurio y sin renumerar ids con
progreso vivo. Además congela el contrato de slugs (D-40-01/D-40-02) que las Phases 41-44 consumen.

Output: `src/data/storage.js` y `src/data/backup.js` en `schemaVersion 13`, más los bloques de tests
v13 en `tests/data-storage.test.js` y `tests/backup.test.js`. Ningún fichero de `src/screens/`,
`src/domain/` ni `content/` cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-CONTEXT.md
@.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-PATTERNS.md
</context>

## Artifacts this phase produces

Símbolos e identificadores **creados** por esta fase (no existen hoy en el codebase — el pase de
source-grounding de revisión debe excluirlos de la verificación de drift):

| Símbolo | Fichero | Tipo |
|---------|---------|------|
| `RESET_PREFIXES_V13` | `src/data/storage.js` | `const` módulo-privado (array de 4 strings) |
| `migrate12to13` | `src/data/storage.js` | función exportada |
| `hydrateV13` | `src/data/storage.js` | función exportada |
| `RESET_NEW_V13` | `tests/data-storage.test.js` | constante de test (los 4 slugs nuevos) |
| `CATORCE_LEGACY` | `tests/data-storage.test.js` | constante de test (las 14 categorías legacy) |
| `v12WithNewFour` | `tests/data-storage.test.js` | factory de fixture |
| `stateV13` | `tests/backup.test.js` | factory de fixture |
| `stateV12WithFare` | `tests/backup.test.js` | factory de fixture (state v12 con progreso de `fare`) |

Strings de dominio nuevos (los 4 slugs de D-40-01, contrato transversal del milestone v2.0):
`fare-indicativo`, `fare-congiuntivo`, `fare-cond-imperativo`, `fare-indefiniti`.

## Multi-source coverage audit

| Fuente | Item | Plan | Estado |
|--------|------|------|--------|
| GOAL | ROADMAP §Phase 40 Goal — state sube a 13 con reset selectivo de las 4 de `fare` | 40-01 T2 | COVERED |
| GOAL | SC-1 — las 4 arrancan `no-hecha` racha 0, `startsWith` sin colisión con las 14 | 40-01 T2, T3 | COVERED |
| GOAL | SC-2 — 14 legacy + `songProgress` byte-intactas; idempotente/pura/anti-pollution | 40-01 T3 | COVERED |
| GOAL | SC-3 — round-trip v13, import `v12→v13`, rechazo `>13`, constante espejada | 40-01 T2, T4 | COVERED |
| GOAL | SC-4 — suite sin fails nuevos y `git diff` del motor vacío | 40-01 T2, T3, T4 | COVERED |
| REQ | MIG-01 | 40-01 T2, T3 | COVERED |
| REQ | MIG-02 | 40-01 T2, T4 | COVERED |
| RESEARCH | — sin RESEARCH.md (`--skip-research`); el analog lo aportan CONTEXT.md §canonical_refs y 40-PATTERNS.md | n/a | N/A por diseño |
| CONTEXT | D-40-01 los 4 slugs verbatim (one-way) | 40-01 T1 (checkpoint) + T2 | COVERED |
| CONTEXT | D-40-02 contrato transversal del milestone | 40-01 T2 (comentario de bloque) | COVERED |
| CONTEXT | D-40-03 gate de no-colisión con las 14 legacy | 40-01 T2 (comentario) + T3 (fixture) | COVERED |
| CONTEXT | D-40-04 prefijos completos, nunca truncados (costly) | 40-01 T2 | COVERED |
| CONTEXT | D-40-05 comentario de bloque con el párrafo `fare-ind` | 40-01 T2 | COVERED |
| CONTEXT | D-40-06 solo 4 ficheros; `categories.json` fuera | 40-01 `files_modified` + T4 gate | COVERED |
| CONTEXT | D-40-07 convención de ids de cruce, escrita donde Phase 44 la vea (costly) | 40-01 T2 (comentario) | COVERED |
| CONTEXT | D-40-08 `migrate12to13` = 3 pasos + deep-clone, pura, idempotente | 40-01 T2, T3 | COVERED |
| CONTEXT | D-40-09 `hydrateV13` shape-only + constante espejada | 40-01 T2, T3 | COVERED |
| CONTEXT | D-40-10 reset preventivo / forward-compat (documentado en JSDoc) | 40-01 T2 | COVERED |
| CONTEXT | D-40-11 `backup.js` round-trip + import + rechazo | 40-01 T2, T4 | COVERED |
| CONTEXT | D-40-12 VAL-06 preexistente fuera de scope | 40-01 `must_haves.prohibitions` | COVERED |

**0 items MISSING.** No se requiere split de fase.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Congelar el contrato de slugs de `fare` antes de escribirlo en la migración (D-40-01)</name>
  <decision>Los 4 ids/slugs que `RESET_PREFIXES_V13` va a declarar, y que las Phases 41-44 usarán como nombre de fichero de contenido, `id` en `categories.json` y prefijo de ids de slot y de cruce.</decision>
  <context>
    D-40-01 está LOCKED en 40-CONTEXT.md y este checkpoint NO lo reabre: es el gate de
    irreversibilidad que exige confirmarlo una última vez antes de que quede escrito en el state.

    Por qué es one-way: en cuanto v13 se shippee, el slug será simultáneamente (a) elemento de
    `RESET_PREFIXES_V13`, (b) nombre de `content/exercises/<slug>.json`, (c) `id` en
    `content/categories.json` (Phase 44, order 15-18) y (d) prefijo de los ids de cruce multi-cat ya
    escritos en el `exerciseStats` real del autor. Renombrar después de las Phases 41-43 exige OTRO
    bump de `schemaVersion` con su propio reset — no es una edición local.

    Los 4 slugs propuestos, verbatim del ROADMAP y de REQUIREMENTS.md:
    `fare-indicativo`, `fare-congiuntivo`, `fare-cond-imperativo`, `fare-indefiniti`.

    Nota sobre el tercero: se mantiene la abreviatura `cond` (no `condizionale`) precisamente para que
    no haya deriva entre ROADMAP.md / REQUIREMENTS.md y el código.

    Nota sobre el par `fare-indicativo` / `fare-indefiniti`: comparten el prefijo textual `fare-ind`.
    Es inocuo porque los dos se resetean, pero es la razón de D-40-04 (prefijos completos, nunca
    truncados) y quedará documentado en el comentario de bloque.
  </context>
  <options>
    <option id="confirmar">
      <name>Confirmar los 4 slugs tal cual (recomendado — es la decisión ya lockeada)</name>
      <pros>Cero deriva entre roadmap, requisitos y código. Es el precedente literal de D-35-01 (v1.9) con el prefijo de verbo `fare-` como espacio de nombres del milestone.</pros>
      <cons>Ninguno conocido. El coste de renombrar se paga solo si el autor cambia de idea DESPUÉS de las Phases 41-43.</cons>
    </option>
    <option id="enmendar">
      <name>Enmendar el set de slugs ahora, antes de escribirlo</name>
      <pros>Es el único momento barato para cambiarlos: todavía no existen ni contenido, ni entradas en `categories.json`, ni progreso del autor bajo esos ids.</pros>
      <cons>Obliga a actualizar ROADMAP.md §Phases 40-44 y REQUIREMENTS.md en el mismo movimiento, o se abre exactamente la deriva que D-40-01 evita.</cons>
    </option>
  </options>
  <resume-signal>Selecciona: `confirmar` o `enmendar` (si es `enmendar`, indica los 4 slugs exactos).</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Eslabón `12→13` end-to-end — `storage.js` + `backup.js` + un test tracer que atraviesa toda la cadena</name>

  <files>src/data/storage.js, src/data/backup.js, tests/data-storage.test.js, tests/backup.test.js</files>

  <read_first>
    - `src/data/storage.js` líneas 30-40 (`KEY` + `CURRENT_SCHEMA_VERSION`), 145-172 (el dispatcher `migrate()`), 1154-1294 (`RESET_PREFIXES_V12` + su comentario de bloque, el JSDoc de `migrate11to12`, `migrate11to12`, `hydrateV12`). `hydrateV12` termina en la línea 1294, que es el final del fichero: el código nuevo se APENDIZA ahí.
    - `src/data/backup.js` líneas 26 (la línea plana de imports de la cadena), 28-56 (el comentario de bloque acumulativo y `CURRENT_SCHEMA_VERSION`), 120-146 (el rechazo de versión futura y la cadena de migración del import).
    - `.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-PATTERNS.md` — lleva los extractos reales de los 5 sitios de edición de `storage.js` y los 4 de `backup.js`, la tabla comparativa `migrate12to13` vs `hydrateV13`, y el inventario de las aserciones hardcodeadas a 12.
    - `.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-CONTEXT.md` §decisions — D-40-02 (contrato transversal), D-40-04 (predicado), D-40-05 (comentario), D-40-07 (convención de cruces), D-40-08 (los 3 pasos), D-40-09 (hydrate shape-only), D-40-10 (reset preventivo), D-40-11 (backup).
    - `tests/backup.test.js` líneas 630-722 (el `describe` v12 análogo) y 760-780 (el test de versión futura que hay que mover de frontera).
  </read_first>

  <precondition>`node --test tests/*.test.js` reporta 672 pass / 0 fail en el árbol limpio ANTES de cualquier edición — es el baseline contra el que se mide "sin fails nuevos". Si no da 672/0, parar y reportar en vez de seguir.</precondition>

  <reversibility rating="one-way">Escribe los 4 slugs de D-40-01 en `RESET_PREFIXES_V13`, que a partir de aquí es simultáneamente predicado de reset, nombre de fichero de contenido, `id` de `categories.json` y prefijo de ids de cruce. Revertir tras las Phases 41-43 exige un eslabón `13→14` con su propio reset. Por eso Task 1 es un checkpoint bloqueante.</reversibility>

  <behavior>
    El test tracer (uno solo, en `tests/backup.test.js`, nombre que empiece por `tracer`) atraviesa
    las dos capas de la fase en una sola llamada — `parseBackupFile` hace parse, guards, cadena de
    migración de `storage.js` y `hydrateV13`:

    - Dado un wrapper `{ kind: 'italian-course-backup', exportedAt, schemaVersion: 12, state }` cuyo
      `state` v12 tiene progreso bajo `fare-indicativo` Y bajo `fare-indefiniti` (el par que comparte
      `fare-ind`) más progreso bajo al menos `preposiciones` y `presente-regolare`:
      → `parseBackupFile(JSON.stringify(wrapper))` devuelve `ok: true`.
      → `r.state.schemaVersion === 13`.
      → `fare-indicativo` y `fare-indefiniti` ausentes de `r.state.categoryProgress`.
      → `fare-indicativo-001` y `fare-indefiniti-001` ausentes de `r.state.exerciseStats`.
      → `preposiciones` y `presente-regolare` `deepEqual` a como entraron, en ambos dicts.
  </behavior>

  <action>
    Cinco ediciones en `src/data/storage.js`, tres en `src/data/backup.js`, más el re-apuntado
    mecánico de los tests y el test tracer. Todo en una sola tarea porque el bump de la constante y el
    re-apuntado de las aserciones son inseparables: separarlos dejaría la suite en rojo entre commits.

    **`src/data/storage.js`**

    1. `CURRENT_SCHEMA_VERSION` pasa de 12 a 13. `blankState()` lee la constante, así que sigue sola.

    2. En el dispatcher `migrate()`: la línea que despacha `s.schemaVersion === 11` se deja como está;
       tras ella se inserta el despacho de la versión 12 a `migrate12to13`, y a continuación el
       retorno de la versión 13 a través de `hydrateV13`. El `console.warn` de versión desconocida y
       su `return blankState()` NO se tocan — siguen siendo la cola del dispatcher.

    3. Al final del fichero (después de `hydrateV12`, línea 1294) se declara `RESET_PREFIXES_V13`
       como un array de exactamente 4 strings, en este orden y sin truncar: `fare-indicativo`,
       `fare-congiuntivo`, `fare-cond-imperativo`, `fare-indefiniti` (D-40-04). Encima va un
       comentario de bloque, espejo estructural del de `RESET_PREFIXES_V12` (líneas 1154-1167), que
       DEBE cubrir cuatro puntos:
       (a) que son las 4 categorías nuevas del milestone v2.0, que nacen en las Phases 41-43, y que
           esos 4 strings son el contrato transversal del milestone (D-40-02): cada uno es a la vez
           `id` en `categories.json`, nombre de `content/exercises/<slug>.json`, elemento de
           `RESET_PREFIXES_V13` y prefijo de los ids de slot y de cruce, de modo que las Phases 41-44
           DEBEN usar exactamente estos 4 strings;
       (b) el gate de no-colisión de D-40-03 verificado el 2026-08-03: ninguna de las 14 categorías
           registradas (`avere`, `essere`, `preposiciones`, `verbos-movimiento`,
           `sustantivos-irregulares`, `genero-numero`, `profesiones`, `articoli`, `partitivos`,
           `presente-regolare`, `dimostrativi`, `possessivi`, `modali`, `riflessivi`) empieza por
           `fare` ni es prefijo de un slug nuevo, así que el filtro preserva las 14 byte a byte;
       (c) un párrafo NUEVO respecto a Phase 35 (D-40-05): `fare-indicativo` y `fare-indefiniti`
           comparten el prefijo textual `fare-ind`, ninguno es prefijo del otro, y el solape es
           inocuo porque los dos se resetean — es la razón de declarar los prefijos completos y de
           rechazar tanto el prefijo paraguas de familia (barrería una futura `fare-modismi`, hoy
           Out of Scope) como la igualdad exacta (no barrería los ids de slot como
           `fare-indicativo-001`, que es justo lo que hay que limpiar);
       (d) la convención de ids de cruce multi-categoría de D-40-07, vinculante para Phase 44: los
           cruces viven en el fichero de la categoría de `fare`, su `id` lleva prefijo de `fare`
           (`fare-indicativo-300`, …) y `categoryIds` es `[slug de fare, slug legacy]` con el de
           `fare` PRIMERO — un cruce autorado bajo el prefijo legacy quedaría fuera del reset y
           dejaría estado huérfano.

    4. `export function migrate12to13(v12)`, espejo literal de `migrate11to12` (líneas 1207-1249) con
       los slugs y la versión cambiados. Tres pasos, cada uno tras deep-clone defensivo por sub-dict
       (`JSON.parse(JSON.stringify(x))` guardado por `typeof x === 'object' && x !== null`, con caída
       a `{}`), nunca un clon único del root:
       (1) clon de `categoryProgress` y `delete` en notación de corchetes de los 4 slugs;
       (2) clon de `exerciseStats` y reconstrucción quedándose solo con las claves para las que
           `RESET_PREFIXES_V13.some(p => k.startsWith(p))` es falso;
       (3) `inFlightTest` reconstruido condicionalmente sin mutar el input: si es objeto, tiene
           `exerciseIds` array, y algún elemento es string cuyo prefijo casa alguno de los 4, pasa a
           `undefined`; en cualquier otro caso se pasa tal cual.
       Devuelve un literal de objeto FRESCO con `schemaVersion: 13` primero, luego `exerciseStats`,
       `categoryProgress`, `dailyLog` y `songProgress` deep-clonados con la misma guarda,
       `lastBackupAt` y `firstUsedAt` normalizados con `typeof … === 'string' ? … : null`, e
       `inFlightTest`. Su JSDoc espeja el de `migrate11to12` (líneas 1170-1206): cuál es el eslabón
       análogo, el gate de colisión, por qué prefijo y no igualdad, idempotencia y pureza, la razón
       del deep-clone, "exportada para testabilidad", y el punto de D-40-10 — el reset es preventivo
       y forward-compat aunque hoy sea no-op sobre datos reales, porque cubre un re-autorado o
       renumerado futuro que deje estado espurio.

    5. `export function hydrateV13(parsed)`, espejo literal de `hydrateV12` (líneas 1274-1294) con la
       versión a 13. Es shape-only (ver la tabla comparativa de 40-PATTERNS.md): mantiene la guarda
       de root `const p = (parsed && typeof parsed === 'object') ? parsed : {}`, deep-clona cada
       sub-dict con la misma guarda de tipo, pasa `inFlightTest` tal cual, y NO borra claves ni poda
       por prefijo — un state que llega aquí ya viene v13-shaped por la cadena, o es un import
       directo v13 que debe preservarse íntegro (D-40-09).

    **`src/data/backup.js`**

    6. A la línea plana de imports (línea 26) se le añaden al final, en orden cronológico,
       `migrate12to13` y `hydrateV13`.

    7. Al comentario de bloque acumulativo (líneas 28-55) se le APENDIZA una frase de Phase 40 en la
       misma voz que la de Phase 35, nombrando la fase y la decisión (`Phase 40 (D-40 / MIG-02)`),
       diciendo que el bump 12 a 13 resetea el progreso de las CUATRO categorías nuevas de v2.0
       (`fare-indicativo`, `fare-congiuntivo`, `fare-cond-imperativo`, `fare-indefiniti`) que nacen en
       las Phases 41-43, que hoy es no-op y es forward-compat de un backup futuro que ya las
       contenga, y que el set de sub-dicts sigue sin cambiar. NO se reescribe ninguna frase anterior.
       Después, `CURRENT_SCHEMA_VERSION` pasa de 12 a 13.

    8. En la cadena de migración del import (líneas 133-146): tras el despacho de la versión 11 se
       inserta el despacho de la versión 12 a `migrate12to13`, y la cola incondicional deja de ser
       `hydrateV12` para ser `hydrateV13` (D-40-11).

    9. El bloque de rechazo de versión futura (líneas 126-131) NO se toca: está escrito genéricamente
       contra la constante y empieza a rechazar `> 13` solo con el bump del punto 7.

    **Re-apuntado mecánico de los tests (inseparable del bump)**

    10. `tests/data-storage.test.js`: a la línea de import (línea 28) se le añaden `migrate12to13` y
        `hydrateV13`. Las 6 aserciones de fin-de-cadena y de `blankState()` de las líneas 39, 52-53,
        639, 1495, 1782 y 2063 pasan a comparar contra 13. Los `describe` de las versiones
        intermedias (v1..v11) comprueban su PROPIA versión y NO se tocan; en particular todos los
        `12` internos del bloque de Phase 35 (líneas 1799-2060) son correctos tal cual, porque ese
        bloque testea `migrate11to12`, que sigue aterrizando en 12.

    11. `tests/backup.test.js`: las 17 aserciones de fin-de-cadena de las líneas 93, 121, 155, 175,
        198, 246, 294, 345, 386, 446, 488, 527, 575, 615, 668, 671 y 711 pasan a comparar contra 13,
        y el fixture de la línea 645 se declara en 13. En el test de versión futura (líneas 764-775)
        la frontera sube un escalón: su comentario pasa a nombrar Phase 40 y
        `CURRENT_SCHEMA_VERSION = 13`, el nombre del test pasa a decir `rejects future schemaVersion
        > 13`, y las dos apariciones de la versión en el wrapper y el state del fixture pasan de 13 a
        14. Sin este cambio el test se vuelve rojo, porque 13 deja de ser una versión futura.

    12. El test tracer descrito en `<behavior>`, en `tests/backup.test.js`, con nombre que empiece por
        `tracer` para poder aislarlo con `--test-name-pattern`. Su mensaje de aserción sobre las dos
        claves de `fare` debe nombrar explícitamente el solape `fare-ind`.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test --test-name-pattern="tracer" tests/backup.test.js && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "CURRENT_SCHEMA_VERSION = 13" src/data/storage.js` devuelve `1`.
    - `grep -c "CURRENT_SCHEMA_VERSION = 13" src/data/backup.js` devuelve `1`.
    - `grep -c "export function migrate12to13" src/data/storage.js` devuelve `1`.
    - `grep -c "export function hydrateV13" src/data/storage.js` devuelve `1`.
    - `grep -c "RESET_PREFIXES_V13" src/data/storage.js` devuelve `3` o más: la declaración, la poda de `exerciseStats` y la invalidación de `inFlightTest`.
    - Cada uno de los 4 slugs aparece en `src/data/storage.js`: `grep -c "fare-indicativo"`, `grep -c "fare-congiuntivo"`, `grep -c "fare-cond-imperativo"` y `grep -c "fare-indefiniti"` son todos `>= 1`.
    - `grep -c "fare-ind" src/data/storage.js` es `>= 3` — la declaración de los dos slugs más el párrafo de D-40-05 que documenta el solape.
    - `grep -c "migrate12to13" src/data/backup.js` devuelve `2`: el import y el despacho de la cadena.
    - `grep -c "hydrateV13" src/data/backup.js` devuelve `2`: el import y la cola de la cadena.
    - `grep -c "schemaVersion > 13" tests/backup.test.js` devuelve `1`.
    - `node --test --test-name-pattern="tracer" tests/backup.test.js` sale con código 0 y reporta al menos 1 test pasado y 0 fallados.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con un `pass` de al menos 673.
    - `git status --porcelain src/screens/ src/domain/ content/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    `parseBackupFile` sobre un wrapper v12 con progreso de `fare-indicativo` y `fare-indefiniti`
    devuelve un state v13 con esas dos categorías reseteadas y `preposiciones` / `presente-regolare`
    byte-intactas; la suite completa sigue en 0 fails; `src/screens/`, `src/domain/` y `content/`
    están sin modificar.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bloque de tests v13 de la cadena de migración en `tests/data-storage.test.js`</name>

  <files>tests/data-storage.test.js</files>

  <read_first>
    - `tests/data-storage.test.js` líneas 1786-2065 — el `describe` de Phase 35 completo, que es el molde a espejar: banner ASCII, las dos constantes de slugs, la factory de fixture, los 14 tests en orden, y el test de no-regresión con snapshot (líneas 2014-2060).
    - `src/data/storage.js` — `migrate12to13`, `hydrateV13` y `RESET_PREFIXES_V13` tal como quedaron en Task 2.
    - `.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-PATTERNS.md` §`tests/data-storage.test.js` — describe la estructura y los nombres de los 14 tests, y el hecho verificado nº1 (el fixture de no-regresión necesita 14 legacy, no 10).
    - `.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-CONTEXT.md` §Claude's Discretion — los 5 puntos (a)-(e) que la cobertura debe cumplir.
  </read_first>

  <behavior>
    Un `describe` nuevo al final del fichero, espejo del de Phase 35. Comportamientos a cubrir:

    - `migrate12to13` borra `categoryProgress` de los 4 slugs nuevos y deja las 14 legacy intactas —
      por cada slug reseteado se asserta tanto `=== undefined` como
      `Object.prototype.hasOwnProperty.call(cp, slug) === false`.
    - `migrate12to13` poda `exerciseStats` con los 4 prefijos nuevos y preserva las 14 legacy.
    - El caso del solape: `fare-indicativo-001` y `fare-indefiniti-001` conviven en el mismo fixture y
      los dos desaparecen; el mensaje de aserción nombra `fare-ind` (CONTEXT.md discreción (e)).
    - `inFlightTest` que mezcla un id de slug nuevo con uno legacy queda `undefined`.
    - `inFlightTest` que solo toca categorías legacy se preserva íntegro.
    - Un input sin `inFlightTest` no crashea y devuelve `undefined`.
    - Idempotencia: `deepEqual(migrate12to13(migrate12to13(x)), migrate12to13(x))`.
    - Pureza: tras la llamada, el fixture v12 original sigue teniendo los 4 slugs en ambos dicts.
    - Anti-prototype-pollution: el fixture malicioso se construye con `JSON.parse` de un literal
      string que declara `__proto__` (un literal de objeto NO crearía la own-property y el test sería
      vacuo); después `({}).polluted === undefined` y el resultado sale en `schemaVersion: 13`.
    - Sub-dicts corruptos: en bucle sobre `null`, `'x'` y `42`, cada sub-dict cae a `{}`.
    - `hydrateV13` es espejo de `hydrateV12` en versión 13, SIN poda: preserva un slug nuevo si está
      presente, y `assert.notEqual(out.exerciseStats, entrada.exerciseStats)` prueba el deep-clone.
    - `hydrateV13` sobre un v13 con sub-dicts ausentes los normaliza a `{}`.
    - `hydrateV13` anti-prototype-pollution.
    - Cadena `v12 → v13` y cadena end-to-end desde v8 hasta v13 preservando `preposiciones`.
    - Test de no-regresión (el que sostiene SC-2): snapshot `JSON.parse(JSON.stringify(...))` de
      `categoryProgress`, `exerciseStats` y `songProgress` ANTES de migrar; después, (a) los 4 slugs
      ausentes de ambos dicts, (b) `deepEqual` por categoría de las 14 legacy pre/post, (c)
      `songProgress` byte-intacto. Este fixture siembra DOS claves de stats por categoría (`-001` y
      `-002`), a diferencia de la factory.
    - `blankState()` devuelve `schemaVersion` 13.
  </behavior>

  <action>
    Añadir al final de `tests/data-storage.test.js` un `describe` nuevo, espejo estructural del bloque
    de Phase 35 (líneas 1786-2065). El import ya se amplió en Task 2.

    Encabezar con un banner de comentario ASCII en el mismo formato que el de las líneas 1786-1797:
    identifica el bloque como `data/storage v13 — migrate12to13 reset selectivo de las 4 categorías
    nuevas (Phase 40)`, dice que es clon del bloque anterior con la desviación funcional (los slugs de
    `fare` y el solape `fare-ind`), y nombra la obligación de no-regresión sobre 14 legacy.

    Dentro del `describe`, declarar dos constantes:
    - `RESET_NEW_V13` con los 4 slugs de D-40-01 en el mismo orden que `RESET_PREFIXES_V13`.
    - `CATORCE_LEGACY` con las 14 categorías registradas, en el orden de `content/categories.json`:
      `avere`, `essere`, `preposiciones`, `verbos-movimiento`, `sustantivos-irregulares`,
      `genero-numero`, `profesiones`, `articoli`, `partitivos`, `presente-regolare`, `dimostrativi`,
      `possessivi`, `modali`, `riflessivi`. El nombre cambia respecto a `DIEZ_LEGACY` porque el
      recuento ya no es 10: las 4 categorías de v1.9 son legacy para v2.0.

    Declarar la factory `v12WithNewFour()`, espejo de `v11WithNewFour()` (líneas 1805-1823): recorre
    `[...RESET_NEW_V13, ...CATORCE_LEGACY]` con un contador `n` que da a cada categoría valores
    distintos en `timesShown` / `timesCorrect` / `streakDays`, de modo que las comparaciones byte a
    byte sean significativas; siembra una clave `<cat>-001` en `exerciseStats` y una entrada en
    `categoryProgress` por categoría; y devuelve un root con `schemaVersion: 12`, un `dailyLog` con
    una fecha, un `songProgress` con una canción, `lastBackupAt` y `firstUsedAt`. Como los 4 slugs
    nuevos se recorren en bucle, el solape `fare-ind` queda cubierto por construcción — pero hay que
    hacerlo explícito en el nombre o el mensaje de aserción del test correspondiente.

    Escribir los tests de `<behavior>` en ese orden, con los nombres del molde de Phase 35 adaptados
    (`migrate12to13 …`, `hydrateV13 …`) y con los recuentos de categorías actualizados de 10 a 14 en
    los nombres que los mencionan.

    Para el test de no-regresión, usar un fixture propio que siembre DOS claves de stats por
    categoría (sufijos `-001` y `-002`), no la factory.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/data-storage.test.js && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "migrate12to13" tests/data-storage.test.js` es `>= 12`.
    - `grep -c "hydrateV13" tests/data-storage.test.js` es `>= 4`.
    - `grep -c "CATORCE_LEGACY" tests/data-storage.test.js` es `>= 4`.
    - `grep -c "fare-ind" tests/data-storage.test.js` es `>= 2` — al menos los dos slugs que solapan.
    - `grep -c "presente-regolare" tests/data-storage.test.js` es `>= 1` — prueba que el array legacy llega más allá de las 10 de Phase 35; y `grep -c "riflessivi" tests/data-storage.test.js` es `>= 1` — prueba que incluye las 4 de v1.9.
    - `grep -c "hasOwnProperty" tests/data-storage.test.js` es `>= 1`.
    - `node --test tests/data-storage.test.js` sale con código 0 y reporta `fail 0`.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - El recuento total de `pass` de `node --test tests/*.test.js` es al menos 686 (672 de baseline, más el tracer de Task 2, más los 13 o más tests de este bloque).
    - `git status --porcelain src/ content/` no produce ninguna línea de salida (esta tarea es solo de tests).
  </acceptance_criteria>

  <done>
    El bloque v13 de `tests/data-storage.test.js` cubre los 5 puntos (a)-(e) de la discreción de
    CONTEXT.md, el test de no-regresión compara las 14 categorías legacy con `deepEqual` pre/post, y
    la suite completa sigue en 0 fails.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Bloque de tests v13 de backup — round-trip, import `v12→v13` y las fronteras de versión</name>

  <files>tests/backup.test.js</files>

  <read_first>
    - `tests/backup.test.js` líneas 630-722 — el `describe` de Phase 35 (`stateV12()` más tres tests), que es el molde; y 760-780, el test de versión futura ya movido de frontera en Task 2.
    - `src/data/backup.js` líneas 80-146 — los guards de `parseBackupFile` en orden: `kind`, `state` objeto, `typeof state.schemaVersion !== 'number'`, `!Number.isInteger(...) || < 1`, coherencia wrapper ↔ state, rechazo de versión futura, y la cadena. Los mensajes exactos de `reason` salen de aquí.
    - `.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-PATTERNS.md` §`tests/backup.test.js`.
    - `.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-CONTEXT.md` — D-40-11.
  </read_first>

  <behavior>
    Un `describe` nuevo, espejo del de Phase 35, más los tests de frontera que cierran los edge
    probes de MIG-02:

    - `stateV13()` — factory de un state v13 cuyo progreso vive SOLO en categorías legacy.
    - Round-trip v13: `buildBackupWrapper(state, ...)` produce un wrapper con `schemaVersion` 13;
      `parseBackupFile(JSON.stringify(wrapper))` devuelve `ok: true` con `state.schemaVersion` 13 y
      sin el reason de "versión más nueva".
    - Round-trip v13 preserva el progreso legacy: `deepEqual` por categoría.
    - `stateV12WithFare()` — factory de un state v12 con progreso bajo `fare-indicativo` (y, para el
      solape, bajo `fare-indefiniti`) además de `preposiciones`. Import de un wrapper v12 construido
      a mano: sale en 13, los slugs de `fare` ausentes de `categoryProgress` y de `exerciseStats`, y
      `preposiciones` byte-intacta.
    - Frontera de versión (edge: boundary) — un paso a cada lado del nuevo máximo: wrapper 12 →
      `ok: true` saliendo en 13; wrapper 13 → `ok: true` saliendo en 13; wrapper 14 → `ok: false`
      con `reason` que casa `/versión más nueva/i`. El caso 14 ya lo cubre el test movido en Task 2;
      aquí se añaden explícitamente los casos 12 y 13 como frontera si no están ya cubiertos.
    - `schemaVersion` ausente o no numérico (edge: empty) — en bucle sobre `undefined` (campo
      ausente), `null` y la cadena `'13'`: `ok: false` con `reason` que casa `/falta o no es número/`.
      Es un guard preexistente; el test prueba que el bump no lo esquiva.
    - `schemaVersion` no entero o fuera de rango bajo (edge: precision) — en bucle sobre `12.5`,
      `13.5`, `NaN`, `0` y `-1`: `ok: false` con `reason` que casa `/schemaVersion inválido/`. Cubre
      que un `12.5` no cae al dispatcher (que no casa ninguna rama) para salir hidratado a 13 como si
      fuera un backup válido.
  </behavior>

  <action>
    Añadir a `tests/backup.test.js` un `describe` nuevo con el título en la voz del codebase
    (`data/backup v13 — round-trip + import v12→v13 (Phase 40)`), espejo del de las líneas 630-722.

    Declarar `stateV13()` como espejo de `stateV12()` (líneas 643-663) con la versión a 13 y el
    progreso repartido solo entre categorías legacy, y `stateV12WithFare()` como el state v12 de
    entrada del test de import: progreso bajo `fare-indicativo`, `fare-indefiniti` y `preposiciones`,
    con claves de slot `<slug>-001` en `exerciseStats`.

    Escribir los tests de `<behavior>`. Para el test de import, construir el wrapper como literal a
    mano (`kind`, `exportedAt`, `schemaVersion: 12`, `state`), no vía `buildBackupWrapper`, igual que
    hace el análogo de Phase 35 — el punto del test es simular un fichero real de una versión
    anterior.

    Los tres tests de frontera y de guards usan `assert.match` contra los mensajes literales que
    produce `src/data/backup.js`, no contra paráfrasis: leer los `reason` exactos de las líneas
    100-131 antes de escribir los patrones. Los casos de `undefined`, `null`, cadena y no-entero se
    escriben en bucle sobre un array de valores para que añadir un caso más sea una línea.

    En los wrappers de estos tests de guard, `wrapper.schemaVersion` debe igualar
    `state.schemaVersion` cuando se quiera llegar al guard que se está probando: el guard de
    coherencia wrapper ↔ state dispara ANTES que el de versión futura, así que un desajuste
    accidental haría que el test verificase el mensaje equivocado. Comprobar en cada caso qué `reason`
    devuelve realmente antes de fijar el patrón.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/backup.test.js && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "stateV13" tests/backup.test.js` es `>= 3`.
    - `grep -c "stateV12WithFare" tests/backup.test.js` es `>= 2`.
    - `grep -c "versión más nueva" tests/backup.test.js` es `>= 2`.
    - `grep -c "schemaVersion inválido" tests/backup.test.js` es `>= 1`.
    - `grep -c "falta o no es número" tests/backup.test.js` es `>= 1`.
    - `grep -c "fare-indefiniti" tests/backup.test.js` es `>= 1`.
    - `node --test tests/backup.test.js` sale con código 0 y reporta `fail 0`.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - El recuento total de `pass` de `node --test tests/*.test.js` es al menos 693.
    - `git status --porcelain` lista exactamente los 4 ficheros de `files_modified` y nada más — en particular ninguna entrada bajo `src/screens/`, `src/domain/` ni `content/`.
    - `git diff --stat src/screens/app.js src/domain/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    El backup hace round-trip en v13, un wrapper v12 con progreso de `fare` importa a v13 con ese
    progreso reseteado y `preposiciones` intacta, las fronteras 12/13/14 se comportan como especifica
    SC-3, los guards de `schemaVersion` ausente / no numérico / no entero siguen disparando con sus
    mensajes, y la suite completa está en 0 fails con el motor sin tocar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichero `.json` elegido por el autor → `parseBackupFile` (`src/data/backup.js`) | Único punto por el que entra JSON **no confiable** al state. El contenido puede haber sido editado a mano o venir de otra máquina. Cruza hacia la cadena de migración de `storage.js`. |
| blob de `localStorage` → `migrate()` (`src/data/storage.js`) | Semi-confiable: es el propio state del autor, pero es editable desde DevTools y puede estar corrupto o traer una versión desconocida. |
| `migrate12to13` / `hydrateV13` → resto de la app | Todo lo que sale de aquí se trata como state válido por el motor, screens y persistencia. Una own-property `__proto__` que sobreviva contamina `Object.prototype` para toda la sesión. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | `migrate12to13` y `hydrateV13` en `src/data/storage.js` | high | mitigate | Deep-clone defensivo **por sub-dict** (`JSON.parse(JSON.stringify(x))` guardado por `typeof x === 'object' && x !== null`, con caída a `{}`), nunca un clon único del root, más reconstrucción del root como literal fresco con `schemaVersion` primero. `JSON.parse` crea `__proto__` como own-property y `JSON.stringify` no la serializa, así que el ciclo la neutraliza junto con cualquier getter. Es el patrón establecido (T-21-01 / T-29-01 / T-35-01); no se inventa mitigación nueva. Verificado por los tests anti-prototype-pollution de Task 3, que construyen el fixture con `JSON.parse` de un literal string y assertan `({}).polluted === undefined`. |
| T-40-02 | Tampering | guards de versión de `parseBackupFile`, `src/data/backup.js:100-131` | medium | mitigate | Los cuatro guards preexistentes se mantienen intactos y se re-assertan contra la nueva frontera: `typeof state.schemaVersion !== 'number'`, `!Number.isInteger(...) || < 1`, coherencia `wrapper.schemaVersion === state.schemaVersion`, y el rechazo `> CURRENT_SCHEMA_VERSION` (genérico contra la constante, por lo que empieza a rechazar `> 13` sin editarlo). Tests de frontera 12/13/14 y de `schemaVersion` ausente / no numérico / no entero en Task 4. |
| T-40-03 | Tampering | predicado de reset `RESET_PREFIXES_V13` en `src/data/storage.js` | high | mitigate | Un predicado demasiado ancho (prefijo paraguas de familia) o truncado borraría progreso real del autor de forma silenciosa e irrecuperable. Mitigación: los 4 slugs se declaran completos y literales (D-40-04), el comentario de bloque documenta el gate de no-colisión contra las 14 categorías registradas (D-40-03/D-40-05), y el test de no-regresión de Task 3 hace snapshot `deepEqual` pre/post de las 14 legacy y de `songProgress`. |
| T-40-04 | Denial of Service | `JSON.parse(rawStr)` en `parseBackupFile` con un fichero enorme o profundamente anidado | low | accept | Herramienta local monousuario sin servidor, sin red y sin sincronización (CLAUDE.md §Constraints). El fichero lo elige el propio autor de su propio disco vía `<input type="file">`; no existe ruta de atacante remoto. El `try/catch` ya existente evita que un parse fallido rompa la app. |
| T-40-05 | Information Disclosure | el JSON exportado por `buildBackupWrapper` contiene el progreso completo del autor | low | accept | El contenido es el progreso de estudio del propio autor, sin PII ni credenciales, y el fichero nunca sale de su máquina (sin backend, sin cuentas, sin sync). |
| T-40-06 | Repudiation | pérdida de progreso por un import equivocado, sin traza de qué se sobrescribió | low | accept | El flujo de import ya exige un confirm inline con `summary` (categorías y ejercicios con progreso, `exportedAt`) antes de aplicar — D-76. Esta fase no cambia ese flujo. |
| T-40-SC | Tampering | instalaciones de npm / pip / cargo | n/a | n/a — gate no disparado | Esta fase no ejecuta ninguna instalación de gestor de paquetes ni añade dependencias: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node. El Package Legitimacy Gate no aplica y no se requiere tabla de auditoría en RESEARCH.md. |

**ASVS L1, block on high.** Las tres amenazas `high`/`medium` (T-40-01, T-40-02, T-40-03) están todas
en disposición `mitigate` con verificación automatizada en Tasks 3 y 4. No queda ninguna amenaza
`high` en `accept`.

### Candidatos de prohibición canon descartados (breadcrumbs)

- **prototype-pollution** — es canon de seguridad en este proyecto y vive en `<threat_model>` T-40-01, no se acuña como prohibición.
- **OWASP injection / XSS** — sin construcción de queries ni inyección de HTML en esta fase; descartado.
- **path-traversal** — sin acceso a sistema de ficheros: el import va por `<input type="file">` y el runtime es el navegador; descartado.
- **GDPR / privacidad de datos personales** — el state solo contiene el progreso de estudio del propio autor en su máquina; descartado.
- **fairness / sesgo genérico** — sin componente de decisión automatizada sobre personas; descartado.

### Assumption flags (spec-less probe fallback)

Los 7 edge probes aplicables se resolvieron así, sin descartes silenciosos:
**6 `covered`** (adjacency/MIG-01, empty/MIG-01, ordering/MIG-01, boundary/MIG-02, empty/MIG-02,
precision/MIG-02) → escritos como `must_haves.truths` en texto plano.
**1 `backstop`** (encoding/MIG-02) → marcador estructurado con `verification: backstop`, porque los 4
slugs son ASCII puro y afirmar el contrato Unicode de `startsWith` codificaría una propiedad
incidental en lugar de una decisión de diseño; si el verificador no puede confirmarlo con evidencia
explícita, debe abstenerse (`human_needed`), nunca dar pase silencioso.
**0 `unresolved`.** Igualdad de no-descarte-silencioso: 7 = 7 escritos en `must_haves` + 0 flagged.
</threat_model>

<verification>
Comprobaciones de fase, tras completar las 4 tareas:

1. `node --test tests/*.test.js` → código de salida 0, `fail 0`, `pass` >= 693 (baseline 672 más el
   tracer más los bloques v13 de Tasks 3 y 4).
2. `grep -c "CURRENT_SCHEMA_VERSION = 13" src/data/storage.js src/data/backup.js` → `1` en cada
   fichero; las dos constantes espejadas y sin derivar.
3. `git diff --stat src/screens/app.js src/domain/` → salida vacía. Es el gate de cierre declarado en
   el ROADMAP (SC-4) y en D-40-06.
4. `git status --porcelain` → exactamente los 4 ficheros de `files_modified`, ninguno bajo `content/`.
5. Los 12 items de CONTEXT.md (D-40-01 .. D-40-12) están presentes: los del comentario de bloque de
   `RESET_PREFIXES_V13` se comprueban leyéndolo (debe nombrar el gate de las 14, el solape `fare-ind`
   y la convención de ids de cruce de D-40-07).
</verification>

<success_criteria>
- MIG-01: `migrate12to13` y `hydrateV13` existen, son idempotentes, puras y anti-prototype-pollution
  por deep-clone, con `CURRENT_SCHEMA_VERSION = 13` y reset selectivo por prefijo de exactamente los
  4 slugs de `fare`.
- MIG-02: `backup.js` hace round-trip v13, migra un import `v12→v13` aplicando el reset, y rechaza
  wrappers `> 13` con el mensaje existente.
- SC-1: las 4 categorías de `fare` arrancan `no-hecha` con racha 0 tras migrar; el predicado cubre los
  4 slugs completos y no colisiona con las 14 registradas.
- SC-2: las 14 legacy y `songProgress` byte-intactas, verificado con snapshot `deepEqual` pre/post.
- SC-3: round-trip, import cross-version y rechazo de versión futura, con la constante espejada.
- SC-4: la suite pasa sin fails nuevos y el motor (`src/screens/app.js`, `src/domain/`) queda sin
  tocar.
</success_criteria>

<output>
Crear `.planning/phases/40-migraci-n-12-13-reset-selectivo-preventivo-de-las-4-categor-/40-01-SUMMARY.md` al terminar.
</output>
