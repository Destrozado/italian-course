---
phase: 18-migraci-n-7-8-reset-selectivo-articoli-partitivos
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/data/storage.js
  - src/data/backup.js
  - tests/data-storage.test.js
  - tests/backup.test.js
autonomous: true
requirements: [MIG-01, MIG-02]

must_haves:
  truths:
    - "Tras migrate7to8 el progreso de articoli queda reseteado (categoryProgress borrado, exerciseStats podado, racha 0)"
    - "Tras migrate7to8 el progreso de partitivos queda reseteado (categoryProgress borrado, exerciseStats podado, racha 0)"
    - "Las otras 7 categorías (avere, essere, preposiciones, verbos-movimiento, sustantivos-irregulares, genero-numero, profesiones) conservan categoryProgress y exerciseStats byte-idénticos pre/post migración"
    - "migrate7to8 invalida inFlightTest si contiene algún id que empieza por articoli o partitivos; lo preserva si no"
    - "migrate7to8 es idempotente y puro (no muta el input) y no contamina el prototipo global"
    - "backup.js exporta v8 y un export v8 se reimporta sin error de versión más nueva"
    - "Un backup v7 importado migra a v8 reseteando articoli+partitivos; un backup con schemaVersion > 8 se rechaza"
    - "La suite completa (baseline + nuevos tests v8) pasa con node --test tests/*.test.js"
  artifacts:
    - path: "src/data/storage.js"
      provides: "migrate7to8 (reset selectivo articoli+partitivos) + hydrateV8 (espejo sin poda) + CURRENT_SCHEMA_VERSION = 8 + eslabón v7→v8 en migrate()"
      contains: "export function migrate7to8"
    - path: "src/data/backup.js"
      provides: "CURRENT_SCHEMA_VERSION espejo = 8 + eslabón migrate7to8/hydrateV8 en parseBackupFile"
      contains: "migrate7to8"
    - path: "tests/data-storage.test.js"
      provides: "bloque describe v8 (reset selectivo, idempotencia, pureza, anti-prototype-pollution, inFlightTest, hydrateV8, cadena, no-regresión 9 categorías)"
      contains: "migrate7to8"
    - path: "tests/backup.test.js"
      provides: "bloque round-trip v8 + import v7→v8 con reset + sync de asserts de versión a 8"
      contains: "schemaVersion === 8"
  key_links:
    - from: "src/data/storage.js migrate()"
      to: "migrate7to8 + hydrateV8"
      via: "eslabón fall-through sobre s.schemaVersion === 7 / === 8"
      pattern: "schemaVersion === 7\\) s = migrate7to8"
    - from: "src/data/backup.js parseBackupFile"
      to: "migrate7to8 + hydrateV8 (importados de storage.js)"
      via: "cadena de parse antes del hydrate final"
      pattern: "migrate7to8\\(migrated\\)"
---

<objective>
Migrar el state de `schemaVersion 7` a `8` clonando LITERALMENTE el patrón validado `migrate6to7`/`hydrateV7` (plan 17-01), con UNA sola desviación funcional: el reset selectivo opera sobre DOS prefijos de id de categoría (`articoli` + `partitivos`) en lugar de uno (`preposiciones`). Extender `backup.js` al round-trip v8 (espejo de la constante + eslabón en la cadena de parse).

Purpose: Dejar libre la renumeración de ids que harán las fases de contenido (19/20) — no se puede renumerar con progreso vivo. El progreso de articoli+partitivos se resetea por diseño (Core Value: re-verificación), las otras 7 categorías quedan byte-intactas.

Output: `migrate7to8`/`hydrateV8` + `CURRENT_SCHEMA_VERSION = 8` en `storage.js`; `backup.js` round-trip v8; bloques de test v8 en ambos test files. Cubre MIG-01 (storage) y MIG-02 (backup) en una sola unidad atómica (ambas comparten el bump de versión), igual que hizo el plan canónico 17-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-CONTEXT.md
@.planning/phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-PATTERNS.md
@.planning/milestones/v1.4-phases/17-piloto-preposiciones-contenido/17-01-SUMMARY.md

<interfaces>
<!-- Contratos exactos del analog a clonar. El executor NO debe explorar el codebase: estos son los identificadores y posiciones. -->

storage.js (líneas verificadas):
- CURRENT_SCHEMA_VERSION (línea 35) = 7 → bump a 8.
- blankState() (línea 57): usa CURRENT_SCHEMA_VERSION; NO edita código, solo JSDoc.
- migrate() chain (línea 138): fall-through sobre s.schemaVersion; el eslabón terminal actual es `if (s.schemaVersion === 7) return hydrateV7(s);`.
- migrate6to7(v6) (línea 604): ANALOG a clonar → migrate7to8(v7). Devuelve objeto literal { schemaVersion: 7, ... } con deep-clone defensivo JSON.parse(JSON.stringify(...)) por sub-dict; delete categoryProgress.preposiciones; filtro exerciseStats por prefijo 'preposiciones'; invalidación condicional de inFlightTest.
- hydrateV7(parsed) (línea 666): ANALOG → hydrateV8(parsed). Espejo SIN poda, solo type-guards de shape, schemaVersion: 7.

backup.js (líneas verificadas):
- import (línea 26): `{ migrate1to2, ..., migrate6to7, hydrateV7 }` → añadir migrate7to8, hydrateV8.
- CURRENT_SCHEMA_VERSION (línea 39) = 7 → 8 (espejo inline, NO importa de storage).
- forward-compat reject (línea 109): `state.schemaVersion > CURRENT_SCHEMA_VERSION` → ya cubre > 8 al bumpear; SIN cambio de código.
- parseBackupFile cadena (líneas 123-124): `if (migrated.schemaVersion === 6) migrated = migrate6to7(migrated); migrated = hydrateV7(migrated);` → insertar eslabón v7 + cambiar hydrate final a hydrateV8.
- buildBackupWrapper (línea 148): espeja state.schemaVersion dinámico, SIN cambio.

tests/data-storage.test.js:
- import (línea 28): añadir migrate7to8, hydrateV8.
- bloque analog: describe('data/storage v7 — migrate6to7 reset selectivo de Preposiciones (Phase 17)') líneas 687-849.
- helper analog: v6WithPreposiciones() líneas 688-707.
- OJO: el test de línea 721 usa partitivos-001 como ASSERT-preservado; en v8 partitivos se RESETEA → cambiar ese assert a avere/essere.

tests/backup.test.js:
- bloque analog: describe('data/backup v7 — round-trip + import v6→v7 (Phase 17)') líneas 212-286.
- helper analog: stateV7() líneas 215-232.
- asserts de versión de current-version a sincronizar a 8: líneas 121, 155, 175, 196, 240, 277 (cubren round-trip / fin de cadena de current-version).
- ADEMÁS (críticos, NO en la lista de arriba):
  - línea 93: el test `blankState()` (líneas 91-104) asserta `s.schemaVersion === 7`. Tras el bump de Task 1 a 8, blankState() devuelve 8 → este assert FALLA si no se sincroniza a 8. Sincronizar línea 93 a 8 (y, opcional, refrescar el nombre/comment del test "shape v7" → "shape v8").
  - línea 331+: el test forward-compat `'rejects future schemaVersion > 7'` (líneas 331-339) usa fixture `schemaVersion: 8` (líneas 334-335) y asserta `r.ok === false`. Tras bumpear backup.js a 8, un fixture de 8 ya NO es `> 8` → la reject guard no dispara → el test flipea a ok:true y el assert FALLA. Avanzar el fixture de `8` a `9` (ambas keys: wrapper.schemaVersion y state.schemaVersion) y renombrar el test de `> 7` a `> 8`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: migrate7to8 + hydrateV8 + bump CURRENT_SCHEMA_VERSION (storage.js) — MIG-01</name>
  <files>src/data/storage.js, tests/data-storage.test.js</files>
  <read_first>
    - src/data/storage.js líneas 35, 37-68 (CURRENT_SCHEMA_VERSION + blankState + JSDoc), 138-153 (cadena migrate()), 604-643 (migrate6to7 — analog), 666-685 (hydrateV7 — analog)
    - tests/data-storage.test.js líneas 25-28 (imports), 673-849 (bloque describe v7 completo + helper v6WithPreposiciones)
    - .planning/phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-PATTERNS.md secciones "migrate7to8", "hydrateV8", "CURRENT_SCHEMA_VERSION/blankState/cadena", "tests/data-storage.test.js"
  </read_first>
  <behavior>
    Tests a clonar del bloque v7 (renombrados 6to7→7to8, v6→v7, v7→v8), en un nuevo describe('data/storage v8 — migrate7to8 reset selectivo de articoli + partitivos (Phase 18)'):
    - Test: migrate7to8 borra categoryProgress.articoli Y categoryProgress.partitivos; deja avere intacto.
    - Test: migrate7to8 poda exerciseStats con prefijo articoli Y partitivos; preserva avere/essere (NO usar partitivos como "preservado" — ahora se resetea).
    - Test: migrate7to8 invalida inFlightTest cuyos exerciseIds contienen algún id articoli-* o partitivos-*.
    - Test: migrate7to8 preserva inFlightTest cuyos ids son de avere/essere (ajenos a ambos prefijos).
    - Test: migrate7to8 sin inFlightTest no crashea y preserva undefined.
    - Test: migrate7to8 es idempotente (migrate7to8(migrate7to8(x)) deep-equals migrate7to8(x)).
    - Test: migrate7to8 es puro (input v7 conserva articoli/partitivos tras la llamada).
    - Test: anti-prototype-pollution: fixture con __proto__ own-property (schemaVersion:7) no contamina el global (({}).polluted === undefined).
    - Test: con sub-dict no-objeto (corrupto) cae a {} (type-guards).
    - Test: hydrateV8 es espejo SIN poda — preserva articoli/partitivos si llegan presentes; schemaVersion 8.
    - Test: hydrateV8 sobre v8 con sub-dicts ausentes los normaliza a {}.
    - Test: cadena v7→v8: hydrateV8(migrate7to8(...)) sale v8 con articoli/partitivos reseteados y el resto preservado.
    - Test REFORZADO (D-04): fixture con progreso en las 9 categorías (avere, essere, preposiciones, verbos-movimiento, sustantivos-irregulares, genero-numero, profesiones, articoli, partitivos) en categoryProgress Y exerciseStats → migrate7to8 → asertar (a) articoli+partitivos ausentes de ambos dicts; (b) las 7 restantes deep-equal byte a byte pre/post (assert.deepEqual por categoría preservada).
    Helper nuevo v7WithArticoliPartitivos(): schemaVersion 7, exerciseStats con articoli-*, partitivos-*, avere-*, essere-*; categoryProgress con articoli+partitivos (status hecha/dominada) + avere/essere preservados.
  </behavior>
  <action>
    RED: añadir el import de migrate7to8, hydrateV8 en tests/data-storage.test.js línea 28. Escribir el bloque describe v8 con los 13 tests del bloque <behavior> + el helper v7WithArticoliPartitivos. Ejecutar `node --test tests/data-storage.test.js` — DEBE fallar (funciones no existen aún). Commit test.
    GREEN: en src/data/storage.js: (1) bump CURRENT_SCHEMA_VERSION (línea 35) de 7 a 8; (2) clonar migrate6to7 → export function migrate7to8(v7) — firma (v6)→(v7), todas las refs v6.X→v7.X, schemaVersion de retorno 7→8, y las 3 desviaciones de DOS prefijos: reemplazar `delete categoryProgress.preposiciones;` por DOS deletes (delete categoryProgress.articoli; delete categoryProgress.partitivos;); reemplazar el guard del filtro `if (!k.startsWith('preposiciones'))` por `if (!k.startsWith('articoli') && !k.startsWith('partitivos'))`; reemplazar el predicado de inFlightTest `id.startsWith('preposiciones')` por `(id.startsWith('articoli') || id.startsWith('partitivos'))`. Mantener el deep-clone defensivo JSON.parse(JSON.stringify(...)) por sub-dict idéntico, y timestamps/songProgress/dailyLog idénticos byte a byte; (3) clonar hydrateV7 → export function hydrateV8(parsed) con schemaVersion: 8, SIN poda (solo type-guards de shape, idéntico al espejo); (4) en la cadena migrate() (línea 138) insertar `if (s.schemaVersion === 7) s = migrate7to8(s);` y `if (s.schemaVersion === 8) return hydrateV8(s);` ANTES del eslabón terminal actual, dejando que `if (s.schemaVersion === 7) return hydrateV7(s);` deje de ser terminal (pasa a migrate7to8); (5) actualizar el JSDoc de blankState (líneas 37-55) para citar Phase 18 / D-18 (articoli + partitivos) además de Phase 17 — blankState NO cambia de código (el bump es nominal, la versión sale de la constante). Mantener migrate6to7/hydrateV7 exportados por backward-compat (igual que hydrateV2/V3 se conservan). Actualizar el comentario JSDoc de migrate7to8 enumerando que las otras 7 categorías no empiezan por articoli/partitivos (D-03). Ejecutar `node --test tests/data-storage.test.js` — DEBE pasar. Commit feat.
    Si algún test existente de blankState asume schemaVersion 7, sincronizar a 8 (consecuencia directa del bump, precedente 17-01 deviation 1).
  </action>
  <verify>
    <automated>node --test tests/data-storage.test.js 2>&1 | grep -E "^# (pass|fail)" | grep -q "fail 0" && grep -q "export function migrate7to8" src/data/storage.js && grep -q "export function hydrateV8" src/data/storage.js && grep -v '^[[:space:]]*//' src/data/storage.js | grep -q "CURRENT_SCHEMA_VERSION = 8" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/data/storage.js contiene `export function migrate7to8(` y `export function hydrateV8(`
    - CURRENT_SCHEMA_VERSION === 8 en src/data/storage.js (verificado ignorando comentarios)
    - migrate7to8 hace dos deletes (articoli + partitivos) y filtra exerciseStats por ambos prefijos; verificable por el test "borra categoryProgress.articoli y partitivos" y "poda exerciseStats con ambos prefijos"
    - El test de no-regresión D-04 (fixture 9 categorías) pasa: articoli+partitivos ausentes, las 7 restantes deep-equal byte a byte
    - La cadena migrate() contiene el eslabón `s.schemaVersion === 7) s = migrate7to8` y `s.schemaVersion === 8) return hydrateV8`
    - `node --test tests/data-storage.test.js` exits 0 (fail 0)
    - El commit de test precede al commit de feat (TDD gate)
  </acceptance_criteria>
  <done>migrate7to8 + hydrateV8 implementadas y testadas; CURRENT_SCHEMA_VERSION = 8; cadena migrate() con eslabón v7→v8→hydrate; reset de articoli+partitivos + preservación byte-idéntica de las otras 7; node --test tests/data-storage.test.js verde.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: backup.js round-trip v8 + import v7→v8 con reset (MIG-02)</name>
  <files>src/data/backup.js, tests/backup.test.js</files>
  <read_first>
    - src/data/backup.js líneas 26 (import), 28-39 (JSDoc + CURRENT_SCHEMA_VERSION), 108-114 (forward-compat reject), 116-124 (cadena de migración en parseBackupFile), 148-155 (buildBackupWrapper)
    - tests/backup.test.js líneas 23-29 (imports), 89-104 (test blankState v7 — assert línea 93), 121, 137, 155, 175, 196, 204-286 (bloque round-trip v7 + helper stateV7 + asserts de versión), 240, 277, 328-339 (forward-compat reject — fixture línea 334-335, name línea 331)
    - src/data/storage.js (resultado de Task 1: migrate7to8, hydrateV8 exportadas)
    - .planning/phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-PATTERNS.md secciones "src/data/backup.js" y "tests/backup.test.js"
  </read_first>
  <behavior>
    Tests a clonar del bloque round-trip v7 (renombrados a v8), en un nuevo describe('data/backup v8 — round-trip + import v7→v8 (Phase 18)'):
    - Test: round-trip v8 — buildBackupWrapper → parseBackupFile sin error "versión más nueva"; wrapper.schemaVersion === 8 y r.state.schemaVersion === 8.
    - Test: round-trip v8 preserva avere/essere progreso intacto.
    - Test: import de backup v7 → state v8 con articoli/partitivos reseteados (clon de "import v6→v7"): fixture v7 con progreso en articoli + partitivos + avere; tras parseBackupFile, articoli/partitivos podados de categoryProgress y exerciseStats, avere preservado (cubre D-06: progreso de articoli/partitivos en backup viejo se pierde por diseño).
    - Verificación forward-compat (test existente actualizado): un backup con schemaVersion 9 (> 8) se rechaza con motivo "versión más nueva". El fixture pre-existente usaba 8 (> 7); tras el bump a CURRENT=8, debe avanzar a 9.
    Helper nuevo stateV8(): schemaVersion 8 con progreso en avere/essere (preservados) — clon de stateV7.
    Asserts de versión existentes que cubren el fin de cadena de current-version (líneas 121, 155, 175, 196, 240, 277) deben actualizarse de 7 a 8 porque la cadena ahora termina en v8. ADEMÁS: el assert de blankState() (línea 93) y el fixture forward-compat (líneas 331-335) también dependen del valor de current y deben sincronizarse (ver <action>).
  </behavior>
  <action>
    RED: en tests/backup.test.js: (a) escribir el bloque describe v8 con los tests del bloque <behavior> + helper stateV8; (b) actualizar a 8 los asserts de versión de fin-de-cadena de current-version en las líneas 121, 155, 175, 196, 240, 277; (c) CRÍTICO — sincronizar línea 93: el test blankState() (líneas 91-104) asserta `s.schemaVersion === 7` → cambiar a `=== 8` (tras el bump de Task 1, blankState() devuelve 8; sin este cambio el verify falla). Opcionalmente refrescar el nombre del test "shape v7" → "shape v8" y su comentario para citar Phase 18; (d) CRÍTICO — actualizar el test forward-compat de líneas 331-339: el fixture usa `schemaVersion: 8` en las DOS keys (wrapper.schemaVersion línea 334 y state.schemaVersion línea 335) y asserta `r.ok === false`; tras bumpear backup.js a CURRENT=8, un fixture de 8 ya NO es `> 8` → la guard no dispara y el test flipea a ok:true. Avanzar AMBAS keys del fixture de `8` a `9`, y renombrar el test de `'rejects future schemaVersion > 7'` a `'rejects future schemaVersion > 8'` (y actualizar el comentario de líneas 328-330 a CURRENT=8 / fixture 9). Ejecutar `node --test tests/backup.test.js` — DEBE fallar (cadena aún termina en v7). Commit test.
    GREEN: en src/data/backup.js: (1) añadir migrate7to8, hydrateV8 al import de línea 26 (mantener migrate6to7/hydrateV7 importados por backward-compat de la cadena); (2) bump CURRENT_SCHEMA_VERSION (línea 39) de 7 a 8 (espejo inline de storage.js, NO importar); (3) en parseBackupFile (líneas 123-124) insertar `if (migrated.schemaVersion === 7) migrated = migrate7to8(migrated);` ANTES del hydrate final, y cambiar `migrated = hydrateV7(migrated);` por `migrated = hydrateV8(migrated);`; (4) actualizar el JSDoc de la constante (líneas 28-38) y el comentario de la cadena (línea 52-53) añadiendo el eslabón Phase 18 (migrate7to8 → hydrateV8). El reject forward-compat (línea 109) y buildBackupWrapper (línea 148) NO cambian de código — cubren > 8 y espejan dinámicamente al bumpear la constante. Ejecutar `node --test tests/backup.test.js` — DEBE pasar. Commit feat.
    Verificación final de toda la suite: `node --test tests/*.test.js` debe seguir verde (baseline + nuevos v8).
  </action>
  <verify>
    <automated>node --test tests/backup.test.js 2>&1 | grep -E "^# (pass|fail)" | grep -q "fail 0" && grep -q "migrate7to8" src/data/backup.js && grep -v '^[[:space:]]*\*' src/data/backup.js | grep -v '^[[:space:]]*//' | grep -q "CURRENT_SCHEMA_VERSION = 8" && node --test tests/*.test.js 2>&1 | grep -E "^# fail" | grep -q "fail 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/data/backup.js importa migrate7to8 y hydrateV8 de ./storage.js
    - CURRENT_SCHEMA_VERSION === 8 en src/data/backup.js (espejo, verificado ignorando comentarios)
    - parseBackupFile contiene el eslabón `migrated.schemaVersion === 7) migrated = migrate7to8(migrated)` y el hydrate final es hydrateV8
    - Test round-trip v8: wrapper.schemaVersion === 8 y r.state.schemaVersion === 8
    - Test import v7→v8: articoli/partitivos reseteados, avere preservado tras parseBackupFile
    - El test blankState() (línea ~93) asserta schemaVersion === 8 (sincronizado tras el bump)
    - El test forward-compat usa fixture schemaVersion 9 (> 8) y se rechaza con "versión más nueva"
    - `node --test tests/backup.test.js` exits 0 (fail 0) Y `node --test tests/*.test.js` exits 0 (suite completa verde)
    - El commit de test precede al commit de feat (TDD gate)
  </acceptance_criteria>
  <done>backup.js extendido a round-trip v8 (constante espejo, eslabón en la cadena, import); import de backup v7 migra a v8 reseteando articoli+partitivos; backups > 8 rechazados; asserts de versión y blankState sincronizados a 8; suite completa verde con node --test tests/*.test.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archivo de backup JSON → parseBackupFile | El backup importado es entrada no confiable (puede venir manipulado o corrupto); cruza a la lógica de migración como objeto parseado |
| localStorage state → migrate() | El state persistido puede haber sido editado a mano en DevTools; cruza a la cadena de migración al arrancar |

Nota de proporcionalidad: herramienta single-user local-only, sin red, sin auth, sin servidor. La única superficie real es el parseo de JSON arbitrario (state propio o backup importado).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | migrate7to8 / hydrateV8 (JSON con `__proto__` own-property crafted) | mitigate | Preservar el deep-clone defensivo `JSON.parse(JSON.stringify(...))` por sub-dict del analog migrate6to7 (neutraliza `__proto__` como own-property y getters); reconstruir el root como objeto literal fresco. Cubierto por el test anti-prototype-pollution clonado (({}).polluted === undefined) |
| T-18-02 | Tampering | poda sobre-amplia o que muta el input | mitigate | La poda (deletes + filtro) opera SOLO sobre el clon, nunca sobre el input → garantiza pureza. Cubierto por test de pureza + test de no-regresión D-04 (las 7 categorías deep-equal byte a byte) + test de idempotencia |
| T-18-03 | Denial of Service | inFlightTest stale tras reset (crash al reanudar Test con ids de articoli/partitivos ya inexistentes) | mitigate | migrate7to8 invalida inFlightTest si algún exerciseId empieza por articoli o partitivos (no se confía del guard de UI). Cubierto por test "invalida inFlightTest que toca articoli/partitivos" + "preserva el que no" |
| T-18-04 | Elevation of Privilege | import de backup con schemaVersion > 8 (versión futura no soportada) | mitigate | El reject `state.schemaVersion > CURRENT_SCHEMA_VERSION` ya cubre > 8 automáticamente al bumpear la constante a 8 (forward-compat, D-05). Cubierto por test de rechazo de backup futuro (fixture schemaVersion 9) |
| T-18-05 | Tampering | sub-dict corrupto (no-objeto) en state/backup | mitigate | Type-guards `typeof x === 'object' && x !== null ? clone : {}` por sub-dict (heredados del analog). Cubierto por test "sub-dict no-objeto cae a {}" |
| T-18-SC | Tampering | instalaciones npm/pip/cargo | accept | N/A — zero-deps, no hay instalaciones de paquetes en esta fase (proyecto static web sin build) |
</threat_model>

<verification>
Verificación de toda la fase:
- `node --test tests/*.test.js` verde (baseline 342 + los nuevos tests v8 de ambos archivos).
- `grep -q "export function migrate7to8" src/data/storage.js` y `grep -q "export function hydrateV8" src/data/storage.js`.
- `CURRENT_SCHEMA_VERSION === 8` en storage.js Y backup.js (espejo coherente).
- Reset selectivo verificado: articoli + partitivos reseteados, las otras 7 byte-idénticas (test D-04).
- Round-trip backup v8 + import v7→v8 con reset + rechazo > 8.
</verification>

<success_criteria>
- MIG-01 cubierto: migrate7to8 + hydrateV8 idempotentes, deep-clone defensivo, reset SOLO de articoli + partitivos (categoryProgress + exerciseStats por prefijo + inFlightTest), racha 0; las otras 7 categorías byte-intactas.
- MIG-02 cubierto: backup.js round-trip v8 (export v8 reimportable); import de backup v7 migra a v8 reseteando ambas categorías; backups > 8 rechazados.
- La app arranca limpia sobre el state migrado y la suite completa sigue verde.
</success_criteria>

<output>
Create `.planning/phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-01-SUMMARY.md` when done
</output>
