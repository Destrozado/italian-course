---
phase: 11-articoli
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - content/exercises/articoli.json
autonomous: false
requirements: [ART-05, ART-06, ART-07]
must_haves:
  truths:
    - "Existe al menos un ejercicio match articolo↔sustantivo que agrupa varios nombres bajo su disparador fonético, más profundo que el match básico de genero-numero (incluye lo/gli/uno + plurales + trampas)"
    - "Existen ~6 ejercicios bridge multiple-choice con categoryIds:[\"articoli\", X] cruzando articoli↔genero-numero y articoli↔sustantivos-irregulares (ids articoli-300..)"
    - "Cada ejercicio match y bridge tiene explanation pedagógica curada (canon español acentuado, apóstrofes ASCII U+0027, plain text)"
  artifacts:
    - path: "content/exercises/articoli.json"
      provides: "Bloque match articolo↔sustantivo + ~6 bridges multi-categoría articoli-300.. añadidos al archivo de la categoría"
      contains: "\"id\": \"articoli-300\""
  key_links:
    - from: "content/exercises/articoli.json"
      to: "src/data/schema-validator.js"
      via: "match valida contra validateMatchPayload (pairs array 2-10, cada pair 2 strings no vacíos); bridges contra validateMultipleChoicePayload"
      pattern: "\"type\": \"match\""
    - from: "content/exercises/articoli.json (bridges articoli-300..)"
      to: "cascada D-54 (engine, heredado)"
      via: "categoryIds:[A,B] → fallar un bridge resetea ambas categorías (sin código nuevo)"
      pattern: "\"categoryIds\""
---

<objective>
Completar `content/exercises/articoli.json` con (a) el **bloque `match` articolo↔sustantivo** que va más profundo que el de género-número, y (b) los **~6 bridges multi-categoría** `articoli-300..` que cruzan Articoli con género-número y con sustantivos-irregulares (patrón avere-300..). Todos con explanation curada.

Purpose: El `match` ejercita el reconocimiento agrupado de disparadores (varios nombres comparten `lo`/`gli`) — único tipo donde el pareo NO es derivable por raíz (DESIGN RULE, articolo↔sustantivo cumple). Los bridges cubren ART-06: fallar uno cascada D-54 a ambas categorías (sin código nuevo, engine heredado).

Output: el bloque match (1-2 ejercicios `type: "match"`) + los ~6 bridges `articoli-300..` (`type: "multiple-choice"`, `categoryIds` de 2 slugs) añadidos por APPEND a `content/exercises/articoli.json`, cada uno con `validation.status: "pending"`. Con esto el archivo queda en su conteo FINAL `<N>` (~45-55) — número que el plan 11-04 propagará a los 3 puntos de integración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-articoli/11-CONTEXT.md
@.planning/phases/11-articoli/11-PATTERNS.md
@.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir el bloque match articolo↔sustantivo (más profundo que génnumero — lo/gli/uno + plurales + trampas)</name>
  <read_first>
    - .planning/phases/11-articoli/11-PATTERNS.md §"(b) match exercise" (líneas 84-116) — shape exacta (pairs como tuples [izq,der], duplicados en columna derecha permitidos D-66, regla R3 ≥3 valores distintos, DESIGN RULE) y la directriz "ir MÁS PROFUNDO que génnumero"
    - content/exercises/genero-numero.json (genero-numero-208 líneas 1111-1142, genero-numero-209 líneas 1144-1178) — el modelo a DIFERENCIAR (génnumero ya cubre il/la/l' singular básico; articoli NO lo re-hace)
    - src/data/schema-validator.js (validateMatchPayload, líneas 249-287) — constraints: pairs array de longitud 2-10, cada pair exactamente 2 strings no vacíos, explanation opcional no vacía si presente
    - .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md — para elegir nombres cuyos disparadores cubran lo/gli/uno + plurales + trampas
  </read_first>
  <action>
    APPEND a `content/exercises/articoli.json` 1-2 ejercicios `type: "match"`, `categoryIds: ["articoli"]`, continuando la numeración de ids del bloque base (NO en el rango 300 — eso es solo para bridges). Cada uno con `payload.prompt` (consigna de empareje en español), `payload.pairs` (array de tuples `[sustantivo, artículo]`, longitud 2-10), y `payload.explanation` curada.

    DISEÑO OBLIGATORIO (D-06, va MÁS PROFUNDO que génnumero-208 que solo cubre il/la/l' singular básico):
    - Bloque match 1 (determinativi avanzado): agrupar nombres cuyos artículos incluyan `lo`, `gli`, y plurales — NO re-hacer el `il/la/l'` singular básico. Ejemplo de pairs: `["studente","lo"]`, `["zio","lo"]`, `["psicologo","lo"]`, `["gnocchi","gli"]`, `["amici","gli"]`, `["libri","i"]`. Duplicados textuales en la columna derecha son INTENCIONALES y permitidos (D-66 — `lo` aparece varias veces; grading consume por índice). Cumplir R3: ≥3 valores DISTINTOS en la columna derecha (ej. `lo`, `gli`, `i` → 3 distintos).
    - (Opcional) Bloque match 2 (indeterminativi): agrupar nombres con `un`/`uno`/`una`/`un'` si aporta cobertura no redundante (ej. `["studente","uno"]`, `["amico","un"]`, `["amica","un'"]`, `["casa","una"]` → 4 distintos). Solo si añade valor sobre los multiple-choice del bloque base.

    La explanation debe enseñar la regla agrupada: por qué z-, s+cons-, ps-, gn- disparan TODOS la misma serie (`lo`/`uno` en singular, `gli` en plural), y la elisión `l'`/`un'` ante vocal. Canon: español acentuado, italianismos preservados, apóstrofes ASCII U+0027, plain text sin markdown, sin refs `#NNN`. `notes` top-level con rationale (referenciar D-06, D-66, R3). `validation: {status:"pending", passes:[]}`.

    Respetar la DESIGN RULE: usar `match` SOLO porque artículo↔sustantivo NO es derivable por raíz compartida (el artículo depende de la fonética inicial del nombre). NO meter pares singular↔plural ni masc↔fem con raíz compartida en el match (esos van como multiple-choice, ya cubiertos en 11-02).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); const m=d.exercises.filter(e=>e.type==='match'); if(m.length<1){console.error('Falta bloque match');process.exit(1)} for(const e of m){const right=e.payload.pairs.map(p=>p[1]); const distinct=new Set(right); if(distinct.size<3){console.error(e.id+': solo '+distinct.size+' artículos distintos en columna derecha (R3 pide >=3)');process.exit(1)} const deep=right.some(a=>['lo','gli','uno',\"un'\"].includes(a)); if(!deep){console.error(e.id+': el match no va más profundo que génnumero (falta lo/gli/uno/un\\047)');process.exit(1)}} console.log('OK match: '+m.length+' bloque(s), R3 y profundidad cumplidas')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` sale exit 0 tras el append.
    - Existe ≥1 ejercicio `type:"match"` con `pairs` de longitud 2-10, cada pair 2 strings no vacíos.
    - El/los match tienen ≥3 valores DISTINTOS en la columna derecha (R3) e incluyen al menos uno de `lo`/`gli`/`uno`/`un'` (va más profundo que génnumero, D-06).
    - Los duplicados textuales en columna derecha (varios nombres → mismo artículo) están presentes y son intencionales (D-66).
    - Cada match tiene explanation curada (canon) y `validation:{status:"pending",passes:[]}`.
  </acceptance_criteria>
  <done>El bloque match articolo↔sustantivo existe, va más profundo que génnumero (lo/gli/uno+plurales), cumple R3 y D-66, con explanation curada.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir ~6 bridges multi-categoría articoli-300.. (articoli↔genero-numero + articoli↔sustantivos-irregulares)</name>
  <read_first>
    - .planning/phases/11-articoli/11-PATTERNS.md §"(c) Multi-category bridge" (líneas 120-156) — shape del bridge (id en rango 300+, type multiple-choice, categoryIds de 2 slugs) y el modelo concreto articoli↔irregulares ("___ braccia sono stanche." options ["le","i","gli","la"] correcta le)
    - content/exercises/avere.json (avere-302 líneas 738-772) — modelo EXACTO del bridge irregular ya verificado en el repo (categoryIds:["avere","sustantivos-irregulares"])
    - .planning/phases/11-articoli/11-CONTEXT.md §D-08/D-09/D-10 — los 2 cruces permitidos (articoli↔genero-numero ~3, articoli↔sustantivos-irregulares ~3) y la prohibición de abrir a más cruces (profesiones/preposiciones FUERA)
  </read_first>
  <action>
    APPEND a `content/exercises/articoli.json` ~6 ejercicios bridge, ids en el rango `articoli-300`, `articoli-301`, ... `articoli-305`. Todos `type: "multiple-choice"`. Espejo EXACTO del patrón avere-300..305. SOLO los DOS cruces decididos (D-09/D-10):

    Cruce 1 — `articoli↔genero-numero` (~3 ejercicios), `categoryIds: ["articoli", "genero-numero"]`: el artículo correcto combinado con plural/género. Ejemplos: prompt con plural fem que pide `le` y distingue de `i/gli/la`; prompt con plural masc que pide `i` o `gli` según disparador. La dificultad cruza la regla del artículo (articoli) con la concordancia de número/género (génnumero).

    Cruce 2 — `articoli↔sustantivos-irregulares` (~3 ejercicios), `categoryIds: ["articoli", "sustantivos-irregulares"]`: el artículo de un sustantivo de plural irregular. Modelo concreto del CONTEXT/PATTERNS: prompt "___ braccia sono stanche." con options `["le","i","gli","la"]`, correctIndex → `le` (braccia = plural fem irregular de braccio). Otros: `lo zio → gli zii` (prompt que pide `gli` para el plural irregular), `l'uovo → le uova` (prompt que pide `le` para uova). Cada uno cruza la regla del artículo con la irregularidad del plural del sustantivo.

    Para cada bridge: `payload.prompt` con hueco `___`, `payload.options` 3-4 formas de artículo plausibles, `payload.correctIndex` la correcta. `payload.explanation` curada que explica AMBAS dimensiones (la regla del artículo + la concordancia/irregularidad) — patrón de la explanation de avere-302. `notes` top-level referenciando el cruce y la decisión (ej. "Cruce Articoli + Sustantivos Irregulares (D-09). Plural irregular braccia (sing masc braccio → plural fem braccia) → artículo le."). `validation: {status:"pending", passes:[]}`.

    Canon de explanation/apóstrofes idéntico (español acentuado, italianismos preservados, ASCII U+0027, plain text sin markdown, sin refs `#NNN`).

    PROHIBIDO (D-10): NO crear bridges con profesiones (`l'avvocato`/`lo psicologo` como cruce de categoría) ni con preposiciones (`nello zaino`) — esos cruces están diferidos. SOLO los dos cruces de arriba. NO escribir código de cascada — D-54 es heredada y se activa sola por `categoryIds:[A,B]`.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); const br=d.exercises.filter(e=>/^articoli-3\d\d$/.test(e.id)); if(br.length<6){console.error('Esperaba >=6 bridges articoli-300.., hay '+br.length);process.exit(1)} const bad=br.filter(e=>!(Array.isArray(e.categoryIds)&&e.categoryIds.length===2&&e.categoryIds[0]==='articoli')); if(bad.length){console.error('Bridges con categoryIds mal formados: '+bad.map(e=>e.id).join(','));process.exit(1)} const crosses=new Set(br.map(e=>e.categoryIds[1])); const allowed=['genero-numero','sustantivos-irregulares']; const illegal=[...crosses].filter(c=>!allowed.includes(c)); if(illegal.length){console.error('Cruces no permitidos (D-10): '+illegal.join(','));process.exit(1)} if(!crosses.has('genero-numero')||!crosses.has('sustantivos-irregulares')){console.error('Faltan los 2 cruces obligatorios');process.exit(1)} console.log('OK '+br.length+' bridges, cruces: '+[...crosses].join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` sale exit 0.
    - Existen ≥6 ejercicios con id `articoli-300..` (regex `^articoli-3\d\d$`), todos `type:"multiple-choice"`.
    - Cada bridge tiene `categoryIds` de exactamente 2 slugs con `articoli` primero.
    - Los slugs cruzados son EXACTAMENTE `genero-numero` y `sustantivos-irregulares` (ambos presentes, ningún otro — D-10).
    - Cada bridge tiene explanation curada que cubre ambas dimensiones, y `validation:{status:"pending",passes:[]}`.
    - El conteo TOTAL del archivo queda fijado (`<N>` final, ~45-55) — anótalo en el SUMMARY para el plan 11-04.
  </acceptance_criteria>
  <done>Los ~6 bridges articoli-300.. cruzan solo articoli↔genero-numero y articoli↔sustantivos-irregulares (patrón avere-302), con explanations curadas; el conteo final del archivo queda fijado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — el autor revisa match + bridges y confirma el conteo final del archivo</name>
  <what-built>
    El bloque `match` articolo↔sustantivo (agrupado por disparador, más profundo que génnumero) y los ~6 bridges multi-categoría `articoli-300..` (articoli↔genero-numero + articoli↔sustantivos-irregulares, patrón avere-302), cada uno con explanation curada. Con esto `content/exercises/articoli.json` queda en su conteo FINAL.
  </what-built>
  <how-to-verify>
    1. Revisa el/los match: ¿agrupan bien los nombres bajo su disparador? ¿van más profundo que el match de género-número (incluyen `lo`/`gli`/`uno` + plurales, no solo `il/la/l'` básico)? ¿la explanation enseña la regla agrupada?
    2. Revisa los 6 bridges uno a uno: ¿el cruce articoli↔genero-numero ejercita artículo + plural/género de forma natural? ¿el cruce articoli↔sustantivos-irregulares usa plurales irregulares reales (braccia, zii, uova) con el artículo correcto?
    3. Confirma que cada bridge tiene EXACTAMENTE 2 categoryIds y que el segundo es genero-numero o sustantivos-irregulares (no profesiones ni preposiciones — esos están diferidos).
    4. Revisa las explanations de match y bridges con el canon (español acentuado, italianismos, apóstrofes ASCII, plain text, sin refs #NNN). En los bridges, la explanation debe cubrir las DOS dimensiones del cruce.
    5. Verifica el conteo total del archivo (te lo dará el ejecutor) — debe estar en el rango ~45-55 y será el número que se propague a los 3 puntos de integración en el plan siguiente.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" y CONFIRMA el conteo total final del archivo (lo necesita el plan 11-04 para los 3 puntos de integración), o indica qué match/bridge reformular.</resume-signal>
</task>

</tasks>

<verification>
- `content/exercises/articoli.json` valida (exit 0 en `validate-content-fixture.mjs`) con el bloque match + ~6 bridges añadidos.
- ART-06 cubierto: bridges con `categoryIds:[A,B]` para los 2 cruces; cascada D-54 se activará sola (sin código nuevo).
- Cobertura match de las trampas agrupadas (ART-05) + explanations curadas (ART-07) en match y bridges.
- El conteo final `<N>` del archivo queda fijado y registrado en el SUMMARY.
</verification>

<success_criteria>
- ART-05 (trampas vía match agrupado) + ART-06 (bridges multi-cat) + ART-07 (explanations match/bridges) cubiertos.
- `content/exercises/articoli.json` en su forma de contenido FINAL (solo falta flip de validation status en 11-05); conteo `<N>` confirmado por el autor.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido ↔ DOM de la app | Prompts/explanations de match y bridges se renderizan al usuario. |
| bridge categoryIds ↔ cascada D-54 (engine) | `categoryIds:[A,B]` dispara reset de ambas categorías al fallar — comportamiento heredado, sin código nuevo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering / Injection | `payload.explanation`/`prompt` de match y bridges renderizados | mitigate | Igual que T-11-02: render vía `x-text` exclusivo (T-02-01 LOCKED), tokens markdown/HTML mostrados literales. Test plain-text en exercise-types.test.js refuerza. Autor único, contenido a mano. |
| T-11-03b | Tampering | `categoryIds` de bridges (activa cascada D-54) | accept | La cascada es lógica heredada del engine ya validada en v1.0 (avere-300..). Un categoryIds mal formado solo afecta a qué categorías resetea, no introduce ejecución de código. El schema valida que categoryIds son slugs ASCII. Sin código nuevo en esta fase. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). |

Sin amenazas HIGH: contenido estático renderizado por `x-text`; los bridges reusan cascada heredada sin código nuevo.
</threat_model>

<output>
Create `.planning/phases/11-articoli/11-03-SUMMARY.md` when done (INCLUIR el conteo total final `<N>` de articoli.json — lo consume el plan 11-04)
</output>
