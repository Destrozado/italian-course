---
phase: 11-articoli
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - content/exercises/articoli.json
autonomous: false
requirements: [ART-08]
must_haves:
  truths:
    - "Todos los ejercicios de articoli.json tienen validation.status === validated con >=2 by distintos en passes[] (validacion por quorum multi-IA via gsd-validate-batch, R1-R7->C1-C5) — D-15"
    - "El reporter node scripts/run-validation-271.mjs sale exit 0 incluyendo todos los ejercicios nuevos de articoli"
    - "El smoke test estricto VAL_07_STRICT=1 node --test tests/*.test.js sigue verde con las constantes de conteo actualizadas"
  artifacts:
    - path: "content/exercises/articoli.json"
      provides: "Todos los ejercicios de articoli con validation.status validated y passes[] de >=2 modelos distintos"
      contains: "validated"
  key_links:
    - from: "content/exercises/articoli.json (validation.passes)"
      to: "scripts/run-validation-271.mjs (sub-gates VAL-04/VAL-06/VAL-08)"
      via: "deriveStatus exige >=2 correcta con by distintos; el reporter verifica status validated por ejercicio"
      pattern: "\"status\": \"validated\""
---

<objective>
Validar por QUORUM (>=2 IAs distintas) todos los ejercicios nuevos de `content/exercises/articoli.json`, llevandolos de `validation.status: "pending"` a `"validated"` con >=2 `by` distintos en `passes[]`, y cerrar el GATE VERDE de la fase: `node scripts/run-validation-271.mjs` exit 0 + `VAL_07_STRICT=1 node --test tests/*.test.js` verde.

Purpose: ART-08 — la definicion de "done" editorial del proyecto exige quorum multi-IA por ejercicio (heredado de v1.1, R1-R7 -> C1-C5). Cierra el ultimo criterio de exito del ROADMAP (reporter exit 0 + smoke estricto verde).

Output: `content/exercises/articoli.json` con todos los ejercicios en `status: "validated"`, audit trail en git (1 commit por ejercicio en el path normal + commits extra por cada disputed resuelto), y ambos comandos del gate en verde.

IMPORTANTE — este plan es NO AUTONOMO y se ejecuta INTERACTIVAMENTE por el operador (el autor) en la sesion principal, NO por un subagente autonomo. La razon es arquitectonica y esta documentada en la skill: `gsd-validate-batch` usa `AskUserQuestion` (checkpoints por categoria + cola disputed con 4 caminos), y `AskUserQuestion` NO esta disponible en subagentes spawneados via Task() (ver D-VAL-19/D-VAL-20 en la skill, anthropics/claude-code#18721). Un ejecutor autonomo NO puede correr esta validacion. Las tareas de abajo son guiones para que el OPERADOR ejecute la skill y verifique el gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-articoli/11-CONTEXT.md
@.claude/skills/gsd-validate-batch/SKILL.md
@.claude/skills/gsd-validate-exercise/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Checkpoint (operador) — validar articoli por quorum con gsd-validate-batch (interactivo, no autonomo)</name>
  <what-built>
    Hasta aqui (planes 11-01..11-04): el temario, todos los ejercicios de articoli.json con explanations curadas y validation.status "pending", y los 3 puntos de integracion cableados (categories.json + reporter + test) con conteos en lockstep. Lo que FALTA es la validacion por quorum: flipar cada ejercicio de "pending" a "validated" via la skill interactiva gsd-validate-batch.
  </what-built>
  <how-to-verify>
    Esta es una accion del operador (NO automatizable por subagente, porque la skill usa AskUserQuestion). Pasos:

    1. Asegurate de estar en la sesion principal de Claude Code (no en un subagente).
    2. Confirma el pool de IAs del quorum. v1.1 uso claude-opus-4-7 + claude-sonnet-4-6 (el invariante es >=2 by distintos). Por defecto usa el mismo pool; si quieres variarlo, es decision tuya antes de lanzar. La skill gsd-validate-exercise tiene los model IDs literales fijados (claude-opus-4-7 Pase 1, claude-sonnet-4-6 Pase 2) — coherente con v1.1.
    3. Lanza la skill apuntando a la categoria nueva:  /gsd-validate-batch articoli
       (NOTA: el slug 'articoli' no estaba en la lista lockeada D-VAL-22 de la skill, que enumera los 7 slugs de v1.1. Si la skill rechaza 'articoli' como scope no reconocido, usa el modo CSV de IDs en su lugar: pasa la lista de ids articoli-001..articoli-3NN separados por coma, que la skill resuelve por archivo. Documenta en el SUMMARY cual de los dos modos funciono — es feedback util para Phase 12.)
    4. La skill itera 1-por-1 (un subagente fresh-context por ejercicio, NUNCA batched — D-VAL-20): por cada ejercicio spawnea Opus, luego Sonnet, parsea verdicts, escribe passes[], deriva status y commitea. Tu veras un checkpoint AskUserQuestion al cierre de la categoria y, si hay disputed, la cola con 4 caminos (accept fix / reject+override / rewrite / skip).
    5. Resuelve cada disputed con el criterio de la memoria feedback_disputed_resolution.md: calidad > tokens, NO override-atajo; ambas IAs deben acabar dando "correcta" (salvo override consciente camino b). Incluye traduccion espanola en el prompt si hay doble-validez que desambiguar.
    6. Objetivo: 0 disputed sin resolver y 0 deferred (la seccion ## Deferred-disputed de STATE.md vacia o ausente). Si difieres alguno conscientemente, anotalo — pero ART-08 / VAL-06 impiden cerrar con deferred.
  </how-to-verify>
  <resume-signal>Escribe "validacion completa" cuando todos los ejercicios de articoli.json esten en status "validated" (0 disputed, 0 deferred), o describe que disputed quedan y por que.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verificar quorum por ejercicio y cerrar el gate verde</name>
  <read_first>
    - content/exercises/articoli.json — comprobar que todos los ejercicios tienen validation.status "validated" y passes[] con >=2 by distintos
    - scripts/run-validation-271.mjs (lineas 80-100) — el helper effectiveStatus y los sub-gates VAL-04/VAL-06/VAL-08 que el reporter verifica
    - src/data/validation-state.js — deriveStatus: "validated" exige >=2 correcta con by DISTINTOS y cero incorrecta (sticky disputed)
  </read_first>
  <action>
    Esta tarea verifica el resultado de la validacion interactiva (la accion del checkpoint anterior) y cierra el gate. Es la parte automatizable del plan.

    1. Verificar quorum por ejercicio: cada ejercicio de articoli.json debe tener validation.status === "validated" y passes[] con al menos 2 entries de `by` DISTINTOS, todas verdict "correcta" (o un override consciente "by":"autor" "correcta" si hubo camino b — el reporter lo acepta via effectiveStatus). NINGUN ejercicio en "pending" ni "disputed".

    2. Ejecutar el reporter del gate: node scripts/run-validation-271.mjs — debe salir exit 0, con los sub-gates VAL-06 (todos validated), VAL-08 (cero disputed) y VAL-04 (>=2 by distintos) en PASS, e incluir el conteo de articoli (272+N).

    3. Ejecutar el smoke test estricto: VAL_07_STRICT=1 node --test tests/*.test.js — debe salir exit 0 (todos los tests verdes, incluyendo los 3 invariantes editoriales de articoli del plan 11-04 y cualquier check VAL_07 estricto).

    Si el reporter o el smoke fallan: NO es trabajo de re-autoria; lo normal es (a) algun ejercicio quedo en pending/disputed (volver a la skill via el checkpoint anterior con los ids concretos), o (b) un conteo se desincronizo (revisar lockstep de 11-04). Diagnosticar con el output del reporter (lista por categoria) y remediar en el plan correspondiente, no parcheando aqui.

    NO modificar el contenido pedagogico de los ejercicios en esta tarea (prompts/options/explanations ya aprobados en 11-02/11-03). Esta tarea SOLO verifica el estado de validacion y el gate.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); const bad=d.exercises.filter(e=>!e.validation||e.validation.status!=='validated'); if(bad.length){console.error('No validados: '+bad.map(e=>e.id+':'+(e.validation&&e.validation.status)).join(', '));process.exit(1)} const lowQuorum=d.exercises.filter(e=>{const by=new Set((e.validation.passes||[]).filter(p=>p&&p.verdict==='correcta').map(p=>p.by)); return by.size<2}); if(lowQuorum.length){console.error('Quorum <2 by distintos: '+lowQuorum.map(e=>e.id).join(', '));process.exit(1)} console.log('OK '+d.exercises.length+' ejercicios validated con quorum >=2')" && node scripts/run-validation-271.mjs && VAL_07_STRICT=1 node --test tests/*.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TODOS los ejercicios de articoli.json tienen validation.status === "validated".
    - Cada ejercicio tiene >=2 `by` DISTINTOS con verdict "correcta" en passes[] (quorum VAL-04), o un override consciente "by":"autor" que el reporter acepta.
    - `node scripts/run-validation-271.mjs` sale exit 0 con VAL-06/VAL-08/VAL-04 en PASS y el conteo de articoli incluido (272+N).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` sale exit 0 (smoke estricto verde).
    - La seccion ## Deferred-disputed de .planning/STATE.md esta vacia o ausente (0 deferred).
  </acceptance_criteria>
  <done>Todos los ejercicios de articoli validados por quorum; reporter exit 0; smoke estricto verde. Phase 11 lista para verifier.</done>
</task>

</tasks>

<verification>
- ART-08 cubierto: 100% de articoli.json en status "validated" con >=2 by distintos.
- Gate verde: `node scripts/run-validation-271.mjs` exit 0 (incluye articoli) + `VAL_07_STRICT=1 node --test tests/*.test.js` exit 0.
- 0 disputed, 0 deferred.
</verification>

<success_criteria>
- ART-08 satisfecho; los 5 criterios de exito del ROADMAP Phase 11 quedan cubiertos en conjunto con los planes 11-01..11-04.
- El gate del milestone v1.2 (para Phase 11) en verde; lista para `/gsd:verify-phase 11` o el cierre que corresponda.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation.passes ↔ gate del reporter | El status validated controla si el gate pasa; lo escribe la skill via subagentes aislados (fresh context por ejercicio). |
| skill gsd-validate-batch ↔ subagentes Task() | La skill orquesta inline en la sesion principal (usa AskUserQuestion); los subagentes solo emiten verdicts, no heredan contexto del autor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Spoofing / Repudiation | passes[].by (identidad del validador) | mitigate | Model IDs literales fijados en la skill (claude-opus-4-7 / claude-sonnet-4-6), no aliases remapeables — audit trail estable (D-VAL-02). deriveStatus exige >=2 by DISTINTOS, evita auto-aprobacion con un solo validador. Override del autor queda registrado con by:"autor" + razon + timestamp ISO. |
| T-11-05b | Tampering | validation.status escrito a mano (bypass del quorum) | accept | El reporter (effectiveStatus + sub-gates) es la verificacion fail-closed: un status "validated" sin >=2 by distintos y sin override del autor seria detectado por VAL-04. Autor unico es el oracle final por diseno. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). La skill usa solo herramientas nativas + Node builtins. |

Sin amenazas HIGH: la validacion refuerza la integridad editorial; el gate es fail-closed y el quorum multi-IA evita aprobacion unilateral.
</threat_model>

<output>
Create `.planning/phases/11-articoli/11-05-SUMMARY.md` when done (INCLUIR: que modo de scope funciono en gsd-validate-batch — slug 'articoli' o lista CSV de ids; numero de disputed resueltos; resultado del gate)
</output>
