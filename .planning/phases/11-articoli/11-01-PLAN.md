---
phase: 11-articoli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md
autonomous: false
requirements: [ART-02]
must_haves:
  truths:
    - "Existe un documento de temario exhaustivo de Articoli en la carpeta de la fase"
    - "El temario enumera TODAS las celdas (forma de artículo × disparador fonético × género/número × trampa canónica) que los ejercicios deben cubrir"
    - "El temario está escrito y revisado por el autor ANTES de que exista ningún ejercicio de la categoría — el orden temario-antes-de-ejercicios es verificable en el historial git"
  artifacts:
    - path: ".planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md"
      provides: "Checklist de cobertura exhaustivo de Articoli (det+indet × disparadores × trampas) que precede a articoli.json en git"
      contains: "## Determinativi"
  key_links:
    - from: ".planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md"
      to: "content/exercises/articoli.json"
      via: "cada celda del temario → ≥1 ejercicio en planes posteriores (mapeo de cobertura)"
      pattern: "Determinativi.*Indeterminativi"
---

<objective>
Producir el **temario exhaustivo de Articoli** como PRIMER entregable de la fase (ART-02 / D-14), ANTES de redactar ningún ejercicio. El documento enumera cada celda que los ejercicios deberán cubrir: cada forma del artículo (determinativo `il/lo/l'/la/i/gli/le` + indeterminativo `un/uno/una/un'`) cruzada con cada disparador fonético (consonante simple, s+consonante, z, gn, ps, pn, x, y/semiconsonante, vocal) y cada trampa canónica obligatoria (D-04). Sirve de checklist de cobertura para los planes 11-02 y 11-03.

Purpose: El orden temario→ejercicios es un hard requirement verificable en git (ART-02, success criterion #1 del ROADMAP). El temario es la única fuente que fija cuántas y cuáles celdas existen → de él se deriva el número final de ejercicios (~45-55, D-12/D-13).

Output: `.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md` — documento markdown revisado y aprobado por el autor, commiteado ANTES de que exista `content/exercises/articoli.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-articoli/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redactar el documento de temario exhaustivo de Articoli</name>
  <read_first>
    - .planning/phases/11-articoli/11-CONTEXT.md — `<decisions>` D-01..D-04 (alcance, disparadores en/fuera, trampas obligatorias), D-12/D-13 (densidad ~45-55), `<specifics>` (espejo `il→lo→l'` vs `un→uno→un'`)
    - .planning/ROADMAP.md §Phase 11 success criterion #1 (la lista literal de formas y disparadores que el temario debe enumerar) y #2 (las trampas canónicas: `lo zio`/`gli gnocchi`/`lo psicologo`/`uno studente`/`l'amico`/`l'amica`/`un'amica` vs `un amico`)
    - .planning/REQUIREMENTS.md §Articoli ART-02 (la definición literal de "done" del temario) y §Out of Scope (formas raras/literarias `gli dei`/`lo iato` quedan FUERA)
  </read_first>
  <action>
    Crear `.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md`. Estructurar el documento con DOS secciones de encabezado de nivel 2 obligatorias: `## Determinativi` y `## Indeterminativi`. Dentro de cada sección, una tabla (o lista estructurada) que cruce explícitamente cada forma con su disparador fonético y dé al menos un sustantivo de ejemplo por celda.

    Cobertura mínima OBLIGATORIA del temario (cada fila es una celda que los planes 11-02/11-03 mapearán a ≥1 ejercicio):

    Determinativi — masculino singular: `il` (consonante simple: il libro, il ragazzo, il cane), `lo` (s+consonante: lo studente / lo studio; z: lo zio / lo zaino; ps: lo psicologo; gn: lo gnomo; pn: lo pneumatico SOLO si se decide incluir — por defecto FUERA por D-03; x: lo xilofono; y/semiconsonante: lo yogurt SOLO si se decide incluir — por defecto FUERA por D-03), `l'` (vocal, elisión masc: l'amico, l'occhio, l'uomo).
    Determinativi — masculino plural: `i` (plural de il: i libri, i ragazzi), `gli` (plural de lo y l': gli studenti, gli zii, gli psicologi, gli gnocchi, gli amici, gli occhi).
    Determinativi — femenino singular: `la` (consonante o s+cualquier cosa: la casa, la studentessa, la zia), `l'` (vocal, elisión fem: l'amica, l'opera).
    Determinativi — femenino plural: `le` (SIEMPRE, sin variación fonética: le case, le amiche, le studentesse).

    Indeterminativi — masculino: `un` (consonante simple Y vocal — SIN apóstrofo en masc: un libro, un amico, un occhio), `uno` (s+consonante, z, ps, gn, x: uno studente, uno zaino, uno psicologo, uno gnomo).
    Indeterminativi — femenino: `una` (consonante: una casa, una zia), `un'` (vocal, elisión fem CON apóstrofo: un'amica, un'opera).

    Incluir una sub-sección explícita `### Trampas canónicas` (o columna marcada) que liste las trampas obligatorias D-04, cada una etiquetada con la regla que ejercita: `lo zio` (z→lo), `lo studente`/`uno studente` (s+cons→lo/uno), `lo psicologo` (ps→lo), `lo zaino`/`uno zaino` (z→lo/uno), `gli gnocchi` (gn plural→gli), `l'amico`/`l'amica` (elisión en AMBOS géneros), `un'amica` (apóstrofo SOLO femenino) vs `un amico` (sin apóstrofo masculino — contraste explícito).

    Resaltar el espejo pedagógico (`<specifics>`): la serie determinativa masc `il→lo→l'` refleja la indeterminativa `un→uno→un'` ante el MISMO disparador. Documentarlo como nota para que los planes posteriores puedan crear ejercicios de contraste.

    Reglas de redacción: español acentuado (interfaz del autor es español). Términos italianos preservados con su ortografía. Apóstrofes ASCII U+0027 (`l'`, `un'`) — NUNCA comillas tipográficas. Plain text / markdown estándar de documento (este es un doc de planificación, no un payload — markdown SÍ permitido aquí, a diferencia de las explanations). FUERA de alcance (NO incluir como celdas obligatorias, por D-03 y Out of Scope): `gli dei`, `lo iato`, y los disparadores variables/discutibles `lo pneumatico` vs `il pneumatico` / `lo yogurt` vs `lo iogurt` (si se mencionan, marcarlos explícitamente como "A2 / fuera de v1.2"). NO escribir ningún ejercicio ni JSON en este plan — solo el temario.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md && grep -q '## Determinativi' .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md && grep -q '## Indeterminativi' .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md && grep -q "un'amica" .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md && grep -q 'gli gnocchi' .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md && grep -qv "[‘’“”]" .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md && echo "TEMARIO OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md` existe.
    - Contiene encabezados `## Determinativi` y `## Indeterminativi`.
    - Enumera las 7 formas determinativas (`il/lo/l'/la/i/gli/le`) y las 4 indeterminativas (`un/uno/una/un'`), cada una con su disparador fonético y ≥1 sustantivo de ejemplo.
    - Las 6 trampas canónicas D-04 aparecen explícitas y etiquetadas con su regla: `lo zio`, `uno studente`, `lo psicologo`, `gli gnocchi`, `l'amico`/`l'amica`, `un'amica` vs `un amico`.
    - Cero comillas tipográficas (`grep` de `[‘’“”]` no encuentra nada); apóstrofes ASCII U+0027.
    - NO existe `content/exercises/articoli.json` todavía (orden temario-antes-de-ejercicios; se verifica en el checkpoint y en el commit).
  </acceptance_criteria>
  <done>El temario exhaustivo existe, cubre todas las formas × disparadores × trampas D-04, y precede a cualquier ejercicio de la categoría.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — el autor revisa y aprueba el temario antes de redactar ejercicios</name>
  <what-built>
    El documento de temario exhaustivo de Articoli (`.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md`) — el checklist de cobertura que enumera cada forma de artículo × disparador fonético × trampa canónica. Es el PRIMER entregable de la fase (D-14) y la fuente de la que se deriva el número final de ejercicios.
  </what-built>
  <how-to-verify>
    1. Abre `.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md`.
    2. Confirma que cada forma que esperas practicar está presente: determinativi `il/lo/l'/la/i/gli/le`, indeterminativi `un/uno/una/un'`.
    3. Confirma que cada disparador fonético que te importa tiene su fila/celda: consonante simple, s+consonante, z, gn, ps, x, vocal (elisión).
    4. Confirma que las trampas que más te cuestan aparecen explícitas: `lo zio`, `uno studente`, `lo psicologo`, `gli gnocchi`, `l'amico`/`l'amica`, y el contraste `un'amica` (con apóstrofo) vs `un amico` (sin apóstrofo).
    5. Decide si quieres incluir o dejar fuera los casos variables A2 (`lo pneumatico`, `lo yogurt`) — por defecto están fuera (D-03); dilo si quieres añadirlos.
    6. Verifica que NO se ha escrito ningún ejercicio todavía (no existe `content/exercises/articoli.json`) — el temario debe preceder a los ejercicios en git.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para que el temario se commitee (y quede registrado en git ANTES de cualquier ejercicio), o describe qué celdas añadir/quitar/reorganizar.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md` existe, cubre todas las formas × disparadores × trampas D-04, revisado y aprobado por el autor.
- El commit del temario precede en `git log` a cualquier commit que cree `content/exercises/articoli.json` (verificable: `git log --oneline -- .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md content/exercises/articoli.json`).
</verification>

<success_criteria>
- ART-02 satisfecho: temario exhaustivo documentado y revisado ANTES de redactar ejercicios.
- El documento sirve de checklist de cobertura sin ambigüedad para los planes 11-02 (base mc) y 11-03 (match + bridges).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor ↔ documento markdown | El temario es un documento de planificación interno editado por el autor; no entra en el runtime de la app ni se renderiza en el DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `11-TEMARIO-ARTICOLI.md` (contenido editorial) | accept | Documento de planificación, no código ni payload de runtime. No se renderiza al usuario. Sin superficie de inyección. Riesgo nulo. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala ningún paquete (zero-deps invariant del proyecto, CLAUDE.md). No aplica audit de legitimidad de paquetes. |

Sin amenazas HIGH: el entregable es un documento markdown de planificación que nunca toca el navegador.
</threat_model>

<output>
Create `.planning/phases/11-articoli/11-01-SUMMARY.md` when done
</output>
