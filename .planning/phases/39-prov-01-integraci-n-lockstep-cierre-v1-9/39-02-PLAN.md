---
phase: 39-prov-01-integraci-n-lockstep-cierre-v1-9
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - tests/exercise-types.test.js
  - scripts/run-validation-271.mjs
  - tests/fixtures/slot-variants-integration.test.js
autonomous: true
requirements: [INT-02, INT-03, INT-04]
must_haves:
  truths:
    - "Los 3 arrays de conteo incluyen las 4 cats nuevas con expected DINÁMICO (slotCountOf/.exercises.length), nunca número mágico"
    - "genero-numero está a 13 en los 3 arrays (arreglado el red preexistente 12→13)"
    - "TOTAL_EXPECTED se re-suma solo y el baseline-guard ya no throwea (magic 183 eliminado/reframeado)"
    - "preposiciones está a 50 en run-validation (skew 49→50 corregido para reporter honesto)"
    - "node --test tests/*.test.js exits 0 (0 fail) sobre las 14 categorías"
    - "VAL_07_STRICT=1 node --test tests/*.test.js exits 0 (las 4 nuevas + magnets validadas)"
    - "node scripts/run-validation-271.mjs exits 0 (reporter coherente)"
    - "applyImmediateFailure(this.state permanece en EXACTAMENTE 2 call-sites (D-54)"
    - "git diff src/screens/app.js src/domain/progress.js está vacío (motor intacto)"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "CATEGORIES_WITH_EXPLANATIONS con +4 dinámicas + genero-numero 13"
      contains: "dimostrativi.json"
    - path: "scripts/run-validation-271.mjs"
      provides: "CATEGORIES +4 + baseline reframeado + preposiciones 50 + genero-numero 13"
      contains: "dimostrativi"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES con +4 dinámicas + genero-numero 13"
      contains: "dimostrativi"
  key_links:
    - from: "scripts/run-validation-271.mjs"
      to: "content/exercises/*.json"
      via: "slotCountOf(c.file) suma dinámica en baseline-guard"
      pattern: "slotCountOf"
    - from: "tests/exercise-types.test.js CATEGORIES_WITH_EXPLANATIONS"
      to: "VAL_07_STRICT gate + coverage loop"
      via: "reutilización del array (D-VAL-18)"
      pattern: "CATEGORIES_WITH_EXPLANATIONS"
---

<objective>
Re-sincronizar en LOCKSTEP los conteos del milestone v1.9 (INT-02): añadir las 4 cats nuevas a los 3 arrays hardcoded con expected DINÁMICO, re-sumar TOTAL_EXPECTED, reframear el baseline-guard (matar el mágico 183), arreglar el red preexistente genero-numero 12→13 en los 3 arrays, y corregir el skew preposiciones 49→50 en run-validation. Verificar (INT-03) que la cascada D-54 sigue en EXACTAMENTE 2 call-sites y el motor está byte-intacto, y confirmar (INT-04) que las 28 variantes nuevas + los 3 magnets siguen `validated`. Dejar la suite completa (incl. VAL_07_STRICT=1) VERDE sobre las 14 categorías.

Purpose: El cierre honesto del milestone — ningún conteo rojo en ningún gate, motor sin tocar, procedencia + counts coherentes.
Output: 3 arrays sincronizados dinámicamente; suite + reporter verdes; motor verificado intacto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-RESEARCH.md
@.planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-01-SUMMARY.md

<interfaces>
<!-- Conteos on-disk VERIFICADOS (usar slotCountOf, NUNCA hardcodear estos números): -->
<!-- dimostrativi=8, possessivi=7, modali=6, riflessivi=7 (total nuevo=28); genero-numero=13; preposiciones=50 -->

Touchpoint 1 — tests/exercise-types.test.js:1273-1287 (CATEGORIES_WITH_EXPLANATIONS)
  helper slotCountOf @1270-1271. Precedente dinámico @1285:
  { file: 'content/exercises/presente-regolare.json', expected: slotCountOf('content/exercises/presente-regolare.json') },
  genero-numero @1275 dice 12 (disk=13). preposiciones @1274 dice 50 (correcto, NO tocar aquí).
  Consumido por: coverage loop @1304 (smoke ASCII/R1/explanation) Y VAL_07_STRICT gate @1424-1442 (reutiliza el mismo array, D-VAL-18).

Touchpoint 2 — scripts/run-validation-271.mjs:173-203 (CATEGORIES + TOTAL_EXPECTED + baseline-guard)
  helper slotCountOf @170-171. Precedente dinámico @180 (presente-regolare).
  genero-numero @178 dice 12 (disk=13). preposiciones @174 dice 49 (disk=50 → skew, corregir).
  TOTAL_EXPECTED @188 = reduce sum (auto).
  BASELINE-GUARD @193-203: const TOTAL_EXPECTED_BASELINE = 183 + PRESENTE_REGOLARE_SLOTS; — el mágico 183 hay que matarlo.
  Loop consumidor @251.

Touchpoint 3 — tests/fixtures/slot-variants-integration.test.js:168-179 (REAL_CATEGORIES)
  helper readJson(...).exercises.length. Precedente dinámico @178 (presente-regolare).
  genero-numero @174 dice 12 (disk=13).

D-54 invariante: applyImmediateFailure(this.state en src/screens/app.js:1642 y :1969 (EXACTAMENTE 2).
  Tests guard: exercise-types.test.js:791, screen-canciones.test.js:587, screen-home-editorial.test.js:41.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lockstep de los 3 arrays de conteo (+4 dinámicas, genero-numero 12→13, preposiciones 49→50, baseline reframe)</name>
  <files>tests/exercise-types.test.js, scripts/run-validation-271.mjs, tests/fixtures/slot-variants-integration.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (CATEGORIES_WITH_EXPLANATIONS 1273-1287; coverage loop 1304; VAL_07 gate 1424-1442)
    - scripts/run-validation-271.mjs (CATEGORIES 173-184; TOTAL_EXPECTED 188; baseline-guard 193-203; loop 251)
    - tests/fixtures/slot-variants-integration.test.js (REAL_CATEGORIES 168-179)
    - .planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-RESEARCH.md (§"Count Sync Touchpoints" lines 72-143; §"Pitfall 1" baseline; §"Pitfall 2" genero-numero; §"Pitfall 3" preposiciones)
  </read_first>
  <action>
    LOCKSTEP en un solo pase — los 3 arrays deben quedar coherentes juntos o cualquiera flipa a rojo (RESEARCH Pitfall 2):

    (A) tests/exercise-types.test.js CATEGORIES_WITH_EXPLANATIONS (1273-1287): añade 4 entradas nuevas clonando el precedente dinámico de presente-regolare @1285 — entrada { file: 'content/exercises/dimostrativi.json', expected: slotCountOf('content/exercises/dimostrativi.json') } y análogas para possessivi/modali/riflessivi. Cambia genero-numero @1275 de expected: 12 a expected: 13 (red preexistente, RESEARCH Pitfall 2). NO toques preposiciones aquí (ya está 50, correcto). Usa slotCountOf, NUNCA un entero literal para las 4 nuevas (D-31-06).

    (B) scripts/run-validation-271.mjs CATEGORIES (173-184): añade las 4 nuevas con slug + file + expected: slotCountOf(...) clonando el precedente de presente-regolare @180. Cambia genero-numero @178 de 12 a 13. Cambia preposiciones @174 de 49 a 50 (skew reporter, RESEARCH Pitfall 3 / A1 — SC-5 requiere el reporter verde). TOTAL_EXPECTED @188 se re-suma solo (no lo toques). BASELINE-GUARD (193-203): REFRAMEA para matar el mágico 183 (RESEARCH Pitfall 1, opción b recomendada) — deriva el baseline sumando slotCountOf(c.file) sobre TODAS las CATEGORIES y compáralo con TOTAL_EXPECTED (que suma los .expected); si divergen, el mismo console.error(...) + process.exit(1). Actualiza el comentario para reflejar la suma dinámica (sin el literal 183 ni 195). Esto hace el guard inmune a futuros adds y honesto respecto a genero-numero=13 y las 4 nuevas.

    (C) tests/fixtures/slot-variants-integration.test.js REAL_CATEGORIES (168-179): añade las 4 nuevas con entrada { slug, expected: readJson('content/exercises/<slug>.json').exercises.length } clonando el precedente @178. Cambia genero-numero @174 de 12 a 13.

    NO añadas un array de conteo nuevo (tests/domain.test.js ya auto-descubre vía readdirSync — no requiere edición). NO toques ningún .js del motor.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "dimostrativi.json\|possessivi.json\|modali.json\|riflessivi.json" tests/exercise-types.test.js == 4
    - grep -c "dimostrativi\|possessivi\|modali\|riflessivi" scripts/run-validation-271.mjs >= 4 (las 4 slugs añadidas)
    - grep -c "dimostrativi\|possessivi\|modali\|riflessivi" tests/fixtures/slot-variants-integration.test.js >= 4
    - No magic number: las 4 entradas nuevas usan slotCountOf/.exercises.length en los 3 arrays (grep NO encuentra expected: 8 / expected: 7 / expected: 6 literales para las nuevas)
    - genero-numero a 13 en run-validation: grep -v '^\s*//' scripts/run-validation-271.mjs | grep -c "genero-numero.*expected: 12" == 0
    - preposiciones a 50 en run-validation: grep -v '^\s*//' scripts/run-validation-271.mjs | grep -c "preposiciones.*expected: 49" == 0
    - baseline-guard sin mágico 183: grep -c "183" scripts/run-validation-271.mjs == 0
    - node scripts/run-validation-271.mjs exits 0 (sin "Incoherencia de conteo", sin genero-numero/preposiciones FAIL)
    - node --test tests/exercise-types.test.js tests/fixtures/slot-variants-integration.test.js exits 0
  </acceptance_criteria>
  <done>Los 3 arrays incluyen las 4 nuevas dinámicas; genero-numero=13 y preposiciones=50 sincronizados; baseline reframeado sin 183; reporter y tests de conteo verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Gate de cierre — motor intacto (INT-03), quórum confirmado (INT-04), suite completa verde (SC-5)</name>
  <files>(verificación — no modifica archivos; si un gate falla, corrige la causa en los archivos de Task 1 o reverte un cambio de motor accidental)</files>
  <read_first>
    - .planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-RESEARCH.md (§"D-54 two-call-site invariant" 194-201; §"INT-03 cruces" 180-192; §"INT-04 magnet verification" 264-274)
    - src/screens/app.js (call-sites 1642, 1969 — solo lectura, NO editar)
  </read_first>
  <action>
    Verificación de cierre pura (INT-03 verify-only + INT-04 confirm-only + SC-5 gate). NO re-autores contenido ni toques el motor. Corre los 5 gates y confirma que pasan:
    (1) node --test tests/*.test.js — debe dar # fail 0 sobre las 14 categorías (el genero-numero red ya no existe tras Task 1; también corre el test nuevo del enum origen del Plan 01).
    (2) VAL_07_STRICT=1 node --test tests/*.test.js — debe dar # fail 0 (INT-04: las 4 nuevas + los 3 magnets quei/quegli de quello, parentesco de possessivi, concordancia essere-PP reflexivo siguen validated; el gate reutiliza CATEGORIES_WITH_EXPLANATIONS que ya incluye las 4 nuevas tras Task 1).
    (3) node scripts/run-validation-271.mjs — exit 0, reporter coherente.
    (4) grep -c "applyImmediateFailure(this.state" src/screens/app.js — debe ser exactamente 2 (D-54, INT-03).
    (5) git diff --stat src/screens/app.js src/domain/progress.js — debe estar VACÍO (motor byte-intacto, INT-03).
    Si alguno falla: normalmente NO es por este task — investiga si Task 1 dejó un array desincronizado (fix en los archivos de Task 1) o si algún .js del motor se tocó por accidente (revert). Los cruces multi-cat (dimostrativi-300, possessivi-300/301, riflessivi-300/301, modali-300) ya existen en disco y cascada por los 2 call-sites existentes — NO añadas plumbing.
  </action>
  <verify>
    <automated>node --test tests/*.test.js && VAL_07_STRICT=1 node --test tests/*.test.js && node scripts/run-validation-271.mjs && test "$(grep -c 'applyImmediateFailure(this.state' src/screens/app.js)" = "2" && test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "ALL 5 GATES PASS"</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/*.test.js exits 0 con # fail 0 (14 categorías)
    - VAL_07_STRICT=1 node --test tests/*.test.js exits 0 con # fail 0 (INT-04: 4 nuevas + 3 magnets validated)
    - node scripts/run-validation-271.mjs exits 0
    - grep -c "applyImmediateFailure(this.state" src/screens/app.js == 2 (D-54)
    - git diff src/screens/app.js src/domain/progress.js VACÍO (motor intacto)
    - El comando compuesto imprime "ALL 5 GATES PASS"
  </acceptance_criteria>
  <done>Suite completa + VAL_07_STRICT verdes sobre 14 cats; reporter coherente; D-54 en 2 call-sites; motor byte-intacto; INT-04 confirmado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | Solo se sincronizan constantes de test/reporter y se verifican invariantes; sin runtime nuevo, sin inputs, sin red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02 | Tampering | motor (app.js/progress.js) | mitigate | Gate explícito git diff src/screens/app.js src/domain/progress.js vacío + D-54 grep==2 + 4 tests guard; el motor NO se toca en esta fase. |

Zero-deps: sin instalaciones de package-manager → sin T-39-SC.
</threat_model>

<verification>
- node --test tests/*.test.js exits 0 (# fail 0) — SC-5
- VAL_07_STRICT=1 node --test tests/*.test.js exits 0 — SC-5 / INT-04
- node scripts/run-validation-271.mjs exits 0 — reporter honesto (A1)
- grep -c "applyImmediateFailure(this.state" src/screens/app.js == 2 — D-54 / INT-03 / SC-4
- git diff src/screens/app.js src/domain/progress.js vacío — motor intacto / SC-4
- Sin nuevos packages (zero-deps): package.json/lock sin cambios
</verification>

<success_criteria>
- INT-02: 3 arrays +4 dinámicas + TOTAL_EXPECTED auto + baseline reframeado + genero-numero 12→13 + preposiciones 49→50 — cubre SC-3
- INT-03: D-54 en 2 call-sites, motor diff vacío, cruces existentes — cubre SC-4
- INT-04: 4 nuevas + 3 magnets validated (VAL_07_STRICT verde) — cubre SC-5
- Suite completa verde sobre 14 cats (node --test + VAL_07_STRICT) — cubre SC-5
</success_criteria>

<output>
Create `.planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-02-SUMMARY.md` when done
</output>
