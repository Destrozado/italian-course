---
phase: 46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/preposiciones.json
  - src/data/schema-validator.js
  - index.html
  - app.css
  - tests/schema-translation.test.js
  - tests/screen-translation.test.js
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03, REND-01, REND-02, REND-03, REND-04, REND-05]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 2
  confidence: high

must_haves:
  truths:
    # ── Goal-backward: la verdad de la fase, a escala de UNA variante ──────────
    - "El autor resuelve la variante canónica de `preposiciones-di-origen` (`Paolo è ___ Napoli di nascita.`) y ve `Paolo es de Nápoles de nacimiento.` dentro de la caja de feedback, tanto al acertar como al fallar."
    - "La misma traducción reaparece en la card de «Errores cometidos» del resumen cuando esa variante se falló."
    - "El corpus v2.0 completo (18 categorías / 250 slots / 722 variantes) sigue validando con el campo nuevo presente en una sola variante — retrocompatibilidad demostrada sobre el fichero real, no sobre un fixture."
    # ── Edge probe · SCH-01 (4) ───────────────────────────────────────────────
    - "Dos variantes hermanas del mismo slot con `translationES.text` IDÉNTICO son ambas válidas: el schema no exige unicidad de texto. Test: slot con 2 variantes de texto idéntico valida sin error."
    - "Tres casos asserteados: `translationES` AUSENTE → valida (retrocompat, las 250 slots del corpus siguen verdes); `translationES` presente con `text` \"\" o solo espacios → RECHAZA; `translationES` presente con `text` conteniendo el hueco de tres guiones bajos → RECHAZA (D-46-03)."
    - "El schema NO normaliza ni recorta: `Nápoles` en NFC y en NFD son ambos string no vacío y ambos pasan. La corrección de acentos es criterio del QUÓRUM (S4/RAE), no del schema. Test: texto con tildes pasa y no se aplica NFKC ni trim destructivo."
    - "La validación no depende del orden del array `variants`: reordenar las variantes de un slot da veredicto idéntico y no mueve una traducción de una frase a otra — precisamente por lo que se descartó la validación a nivel de slot indexada por posición (D-46-02). Test: reordenar variantes y re-validar produce el mismo resultado."
    # ── Edge probe · SCH-02 (4) ───────────────────────────────────────────────
    - "Si el slot declara type `match` o `word-buttons`, NINGUNA de sus variantes admite `translationES`, sin importar cuántas variantes hermanas tenga. Test: slot `match` con 3 variantes, `translationES` en la segunda → RECHAZA."
    - "El rechazo es ESTRUCTURAL (por presencia de la clave), no por contenido: `translationES: {}` o con `text` \"\" en una variante `match` / `word-buttons` se rechaza igualmente. Test para las dos formas vacías."
    - "El rechazo no inspecciona el texto, así que ninguna forma de codificación, acento o escape puede esquivarlo. Test: `translationES` con texto acentuado en `match` → RECHAZA."
    - "El rechazo se evalúa por variante de forma independiente; ninguna posición del array puede colar una. Test: `translationES` en la primera y en la última variante de un `word-buttons` → RECHAZA en ambos casos, con el label de la variante correcta en el mensaje."
    # ── Edge probe · SCH-03 (2) ───────────────────────────────────────────────
    - "Arrancar la app dos veces con el contenido nuevo no migra ni resetea nada: NO existe migración 13→14. Test explícito: con un state pre-existente en localStorage, tras boot el progreso queda intacto y `schemaVersion` sigue en 13."
    - "Esta fase no escribe state en ningún momento — el campo es contenido en `content/`. Aserción: el diff de la fase no toca ninguna ruta de state ni de migración."
    # ── Edge probe · REND-01 (2) ──────────────────────────────────────────────
    - "Al fallar una variante SIN traducción no se pinta nodo y la caja de feedback conserva su anatomía previa exacta (REND-05 / D-121). Cubierto por E1-empty del UI-SPEC."
    - "`x-text` escapa: una traducción que contenga menor-que, mayor-que, ampersand o comillas se muestra literal y nunca se interpreta como marcado (T-02-01). Test: el recuento de la directiva de inyección de HTML crudo en `index.html` es idéntico al de `git show HEAD:index.html`, y el nodo de traducción usa `x-text`."
    # ── Edge probe · REND-02 (2) ──────────────────────────────────────────────
    - "Al ACERTAR una variante sin traducción tampoco hay hueco: el guard es el mismo nodo y la misma expresión en los dos desenlaces, no hay rama separada para acierto."
    - "Mismo nodo, mismo `x-text`, mismo escapado al acertar que al fallar — la traducción no se duplica en un segundo template."
    # ── Edge probe · REND-03 (2) ──────────────────────────────────────────────
    - "Pulsar el botón «¿Por qué?» o la tecla `e` repetidamente no duplica, oculta ni altera la traducción: la expresión `x-show` del nodo de traducción NO referencia el flag de explanation-revelada. Aserción de fuente sobre la expresión del guard."
    - "El toggle es síncrono y no hay estado async implicado; la fase no añade ningún temporizador. Aserción: `SESSION_AUTO_ADVANCE_MS` sin tocar y cero `setTimeout` nuevos en el diff."
    # ── Edge probe · REND-04 (4) ──────────────────────────────────────────────
    - "Dos fallos de DOS VARIANTES DEL MISMO SLOT muestran cada card SU propia traducción, no la de la hermana: `summaryVariantSurface` resuelve por `variantIndex`, no por slot. Test con un slot de 2 variantes falladas ambas."
    - "El fallo de una variante sin traducción produce una card sin el nodo, idéntica a la de hoy. Cubierto por E2-empty del UI-SPEC."
    - "`x-text` también en el resumen: la segunda superficie usa el mismo mecanismo de escapado, sin inyección de HTML crudo. Aserción sobre el nodo de la card."
    - "El orden de las cards de «Errores cometidos» sigue siendo el orden de fallos existente; añadir la traducción no lo altera, y dentro de la card el orden DOM es respuesta correcta → traducción → explanation (aserciones V6)."
    # ── Edge probe · REND-05 (4) ──────────────────────────────────────────────
    - "En un slot con una variante traducida y su hermana sin traducir, mostrar la sin-traducir NO hereda el nodo de la hermana: el guard se evalúa por variante mostrada (E2-partial del UI-SPEC)."
    - "Doble guard `x-show` (estado resuelto Y presencia del dato) en las dos superficies: sin traducción no hay nodo, hueco, etiqueta, placeholder ni guion. Test de fuente sobre las dos expresiones de guard."
    - "`x-text` EXCLUSIVO: el gate asserta que el recuento de la directiva de HTML crudo en `index.html` no crece respecto al de HEAD (invariante T-02-01 de todo el proyecto, no solo de estos dos nodos)."
    - "La ausencia del nodo no altera el orden de sus hermanos: respuesta correcta y explanation quedan contiguos exactamente como hoy, sin margen huérfano (el margin vive en el nodo de traducción, no en un contenedor)."
    # ── UI-SPEC ## UI Considerations · 12 explicit ────────────────────────────
    - "E1 · empty — Variante `multiple-choice` SIN `translationES`: el doble guard `x-show` no pinta el nodo, y la caja `.session-feedback` conserva su anatomía previa exacta — sin hueco, sin etiqueta, sin placeholder, sin guion (REND-05 / D-121)."
    - "E1 · loading — No existe estado de carga: el JSON de contenido ya está en memoria antes de que la caja de feedback pueda existir. El diff de la fase no introduce `fetch`, `await`, skeleton ni spinner en `index.html`."
    - "E1 · error — Cero superficie de error en pantalla. Un `translationES` malformado no llega a runtime: lo rechaza `src/data/schema-validator.js` con los tres mensajes de `## Copywriting Contract`, y hay un test por cada uno de los tres casos."
    - "E1 · populated — Traducción presente: párrafo `.session-translation` en `--ed-font-serif` 16px/400/1.5 `var(--ed-ink)` con `margin: 8px 0 0`, situado en el DOM ENTRE `.session-feedback-correct` y `.session-explanation` (aserciones V1 y V6)."
    - "E1 · overflow — Sin recorte posible: la regla CSS nueva no declara `height`, `max-height`, `overflow` ni `text-overflow`, y `.session-feedback` queda sin cambios, así que crece con su contenido."
    - "E2 · empty — Variante fallada SIN `translationES`: el guard con optional chaining no pinta el nodo y la card muestra prompt → tu respuesta → respuesta correcta → `explanation`, idéntica a hoy."
    - "E2 · loading — No aplica por la misma razón que en E1: el resumen se construye sobre resultados ya en memoria. La fase no añade ninguna frontera de carga a la card de error."
    - "E2 · error — Cero superficie de error: los mismos tres rechazos del schema-validator, en la consola del autor y nunca en pantalla. El optional chaining defensivo del guard impide un throw si `summaryVariantSurface` devolviera `undefined`."
    - "E2 · populated — `.summary-error-translation` comparte el bloque CSS con `.session-translation` (declarado UNA sola vez, aserción V2) y va ENTRE la respuesta correcta y `.summary-error-explanation`, cuyo muted+itálica produce el mismo contraste serif/comentario."
    - "E2 · partial — Slot con varias variantes donde solo algunas están traducidas: el campo vive POR VARIANTE (D-46-02) y `summaryVariantSurface` resuelve por `variantIndex`, así que la variante traducida pinta su traducción y su hermana sin traducir no pinta nada; ninguna hereda de la otra."
    - "E2 · overflow — El `li` de `.summary-errors` no tiene alto fijo ni scroll interno y crece con su contenido; la regla nueva no añade `height`, `max-height` ni `overflow`."
    - "E2 · zero-one-many — Con 0 fallos la sección `.summary-errors` entera no se renderiza, así que no hay estado vacío propio que diseñar. Con 1 o N fallos la traducción vive DENTRO del `li` que el `x-for` ya repite, y cada card lleva la de la variante EXACTA fallada. Ningún copy singular/plural nuevo."
  artifacts:
    - path: "src/data/schema-validator.js"
      provides: "SCH-01 (acepta translationES.text en variantes multiple-choice, guard de hueco) + SCH-02 (rechaza en match y word-buttons)"
      contains: "translationES"
    - path: "index.html"
      provides: "Los 2 nodos de traducción con doble guard, uno por superficie"
      contains: "session-translation"
    - path: "app.css"
      provides: "La regla única compartida .session-translation, .summary-error-translation"
      contains: "summary-error-translation"
    - path: "content/exercises/preposiciones.json"
      provides: "La traducción canónica de la fase en la variante 1 de preposiciones-di-origen"
      contains: "Paolo es de Nápoles de nacimiento."
    - path: "tests/schema-translation.test.js"
      provides: "Matriz SCH-01/02/03 completa (aceptación, rechazos, retrocompat sobre el corpus real, no-migración)"
    - path: "tests/screen-translation.test.js"
      provides: "V1-V9 del UI-SPEC, incluidos el no-leak V5 y el motor byte-intacto V8"
  key_links:
    - from: "content/exercises/preposiciones.json"
      to: "index.html"
      via: "el spread `payload: { ...surface, explanation: slot.explanation }` de sessionCurrentExercise y summaryVariantSurface hace fluir translationES sin una línea de JS nueva"
      pattern: "translationES\\?\\.text"
    - from: "index.html"
      to: "app.css"
      via: "los dos nodos declaran las clases que la regla única estiliza"
      pattern: "\\.session-translation,\\s*\\n\\.summary-error-translation"
    - from: "src/data/schema-validator.js"
      to: "content/exercises/preposiciones.json"
      via: "validateMultipleChoiceSurface valida el campo nuevo del contenido real"
      pattern: "translationES"
---

<objective>
Bajar el pipeline de traducción por su camino de producto, end-to-end, sobre UNA sola frase: la
frase canónica de la fase (`preposiciones-di-origen`, variante 1) atraviesa contenido → schema →
las DOS superficies de render, y se ve en pantalla acertando y fallando.

Purpose: probar la arquitectura completa del camino de producto con los tokens de mejor contexto
del ejecutor. Si `translationES` no fluyera solo por el spread del payload, o si la regla CSS no
ganara la cascada, se descubre tras UN commit y no tras diez. La expansión a las 95 variantes
restantes (plan 46-04) parte de un camino ya demostrado.

Output: el campo aceptado por el schema y rechazado donde no aporta, los 2 nodos de render, la
regla CSS única, la traducción canónica en disco, y las dos suites (`tests/schema-translation.test.js`,
`tests/screen-translation.test.js`) que congelan el contrato.
</objective>

<assumption_delta_decision>
**Detector:** `workflow.assumption_delta` — `detected: true`, UNA señal, **FALSO POSITIVO**.

- **Sustantivo primario:** sin cambios — **la VARIANTE sigue siendo la unidad de identidad**.
- **Decisión:** `no-change`.
- **Razón:** el detector casó la pista de pluralización `multiple` dentro del literal
  `multiple-choice` («722 variantes `multiple-choice`»), que es el NOMBRE DE UN TIPO de ejercicio,
  no una transición singular→plural. La fase añade un campo opcional nuevo sobre la variante, pero
  no le da un segundo caso a ningún sustantivo existente: el autor rechazó explícitamente la
  identidad posicional a nivel de slot en D-46-02 precisamente para conservar la variante como ancla
  («acopla el pase al ÍNDICE; reordenar variantes movería silenciosamente los pases a otra frase»).
- **Consecuencia:** NO se inventa ninguna migración del modelo de identidad. Este checkpoint es
  advisory y no bloquea.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-PATTERNS.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-UI-SPEC.md
</context>

## Artifacts this phase produces

Símbolos NUEVOS que esta fase crea (no existen hoy en el repo; el pase de source-grounding de
`plan-review-convergence` los excluye de la verificación de drift):

| Símbolo nuevo | Tipo | Plan que lo crea |
|---|---|---|
| `translationES` | clave JSON (sub-objeto en la variante) | 46-01 |
| `translationES.text` | clave JSON (string) | 46-01 |
| `translationES.validation` | clave JSON (sub-objeto `{ status, passes[] }`) | 46-01 / 46-02 |
| `.session-translation` | clase CSS | 46-01 |
| `.summary-error-translation` | clase CSS | 46-01 |
| `tests/schema-translation.test.js` | fichero de test nuevo | 46-01 |
| `tests/screen-translation.test.js` | fichero de test nuevo | 46-01 |
| `docs/TRANSLATION-VALIDATION-PROMPT.md` | doc nuevo | 46-02 |
| `scripts/validate-translation-pass.mjs` | script nuevo | 46-02 |
| `tests/fixtures/translation-pilot.json` | fixture nuevo | 46-02 |
| `tests/translation-validator.test.js` | fichero de test nuevo | 46-02 |
| `TRANSLATION_COVERAGE` | identificador (array de cobertura en el reporter) | 46-03 |
| `mcVariantCountOf` | identificador (helper de conteo derivado del disco) | 46-03 |
| `--slot` / `--variant` (o la forma compuesta `<slot-id>#<k>`) | flags/dirección CLI del script | 46-02 |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — la frase canónica atraviesa schema y las DOS superficies</name>
  <reversibility rating="costly">D-46-02 fija la forma `translationES: { text, validation }` en la VARIANTE; cambiarla después de esta fase obliga a reescribir las 96 traducciones, el schema-validator, los dos templates, el script de quórum y el gate del reporter — y desde la Phase 48, cientos de entradas.</reversibility>
  <read_first>
    - `src/data/schema-validator.js` — leer `:45` (`SURFACE_VALIDATORS`), `:210-231` (`validateVariants`, el punto de enganche real) y `:438-470` (`validateMultipleChoiceSurface`, con su firma de 5 parámetros y el uso de `label`). El molde del campo opcional retrocompatible está en `:419-423` (`payload.explanation`) — copiar el guard `!== undefined`, el `typeof !== 'string' || !x.trim()` y el `push(file, id, reason)` SIN early-return (D-08 acumula todos los errores). OJO: NO tocar los wrappers `validate*Payload` — `translationES` vive en la VARIANTE.
    - `index.html` — leer `:592-621` (la caja `.session-feedback` del sub-template `multiple-choice`, con la `explanation` de doble guard en `:613-615` y el cierre de `.session-feedback-correct` en `:606`) y `:1281-1298` (la card de «Errores cometidos», con el `<div>` de «Respuesta correcta:» en `:1284` y la `explanation` con optional chaining en `:1294-1296`).
    - `app.css` — leer `:930-980` (el banner «Caja de feedback», `.session-feedback` con su `padding: 14px 16px`, `.session-feedback-correct` con `margin: 8px 0 0`, y `.session-explanation` en `:969-977`) y `:1959-1985` (la sección de Resultados con `.summary-error-explanation`).
    - `styles.css` — leer `:445-460`, el precedente literal del selector doble compartido con su comentario de justificación.
    - `content/exercises/preposiciones.json` — leer los primeros ~45 líneas: el slot `preposiciones-di-origen`, su `explanation`, sus 2 variantes y su `validation` a nivel de SLOT (que NO se toca).
    - `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-UI-SPEC.md` — §CSS Contract y §DOM Contract llevan el bloque CSS y los dos fragmentos HTML LITERALES, comentarios incluidos. Copiarlos verbatim.
    - `tests/screen-session-editorial.test.js` — leer `:19-40`: el molde de test que lee `app.css` / `index.html` como TEXTO con `readFileSync(new URL('../app.css', import.meta.url), 'utf8')`.
    - `tests/exercise-types.test.js` — leer `:104-126`: el molde de `validateContent` con un documento inline mínimo.
  </read_first>
  <files>content/exercises/preposiciones.json, src/data/schema-validator.js, index.html, app.css, tests/screen-translation.test.js</files>
  <action>
Cablear UN solo camino de punta a punta, sin más call sites y sin lotes.

1. **Contenido (1 variante).** En `content/exercises/preposiciones.json`, slot `preposiciones-di-origen`, añadir a la variante cuyo `prompt` es `Paolo è ___ Napoli di nascita.` el sub-objeto `translationES` con `text` = `Paolo es de Nápoles de nacimiento.` y `validation` = `{ "status": "pending", "passes": [] }` (D-46-02). El `validation` a nivel de SLOT no se toca. Mantener indentación 2 y `options` expandidos uno por línea — el `writePass` del plan 46-02 depende de esa indentación estable. Acentos españoles obligatorios (PRES-05).

2. **Schema (camino de aceptación, SCH-01).** En `validateMultipleChoiceSurface(surface, exId, file, push, label)` de `src/data/schema-validator.js`, añadir el bloque del campo opcional: si `surface.translationES !== undefined`, exigir que `surface.translationES.text` sea string no vacío tras `trim()`, y además rechazar que ese texto contenga la secuencia de tres guiones bajos que marca el hueco (D-46-03: la traducción es de la frase YA RESUELTA; un hueco dentro solo puede ser el `prompt` copiado sin rellenar). Los dos mensajes están FIJADOS por el UI-SPEC §Copywriting Contract y se escriben literalmente, con la ruta de campo entre comillas dobles y prefijados por `label` como todas las reglas hermanas: `"translationES.text" debe ser string no vacío si translationES está presente` y `"translationES.text" no puede contener "___" — la traducción es de la frase YA RESUELTA`. Usar `push(...)` sin early-return. NO tocar `schemaVersion` (SCH-03) ni ningún `validate*Payload`.

3. **Render superficie 1 (REND-01/02/05, D-46-06/07/09).** Insertar en `index.html` entre `:606` (cierre del `</p>` de `.session-feedback-correct`) y `:607` (apertura del comentario de la explanation) el nodo LITERAL del UI-SPEC §DOM Contract, con su comentario: un `<p>` de clase `session-translation`, `x-show` con el doble guard `sessionFeedback !== null` Y la presencia de `sessionCurrentExercise.payload.translationES?.text`, y `x-text` sobre esa misma expresión. Directiva de texto exclusiva; jamás la directiva de inyección de HTML crudo (T-02-01). Sin etiqueta «Traducción:» y sin comillas latinas envolventes (D-46-07).

4. **Render superficie 2 (REND-04/05, D-46-08/09).** Insertar entre `:1284` y `:1285` el segundo nodo LITERAL del UI-SPEC: `<p>` de clase `summary-error-translation` con optional chaining defensivo espejo del de la explanation (`summaryVariantSurface(result)?.payload?.translationES?.text`) tanto en el `x-show` como en el `x-text`. En esta superficie el guard de «estado resuelto» es estructural: la card solo existe dentro del `x-for` de fallos.

5. **CSS (D-46-07/08).** Insertar en `app.css` inmediatamente DESPUÉS de `.session-explanation` (fin del banner «Caja de feedback… D-09») el bloque LITERAL del UI-SPEC §CSS Contract: selector doble `.session-translation, .summary-error-translation` declarado UNA sola vez, con `font-family: var(--ed-font-serif)`, `font-size: 16px`, `font-weight: 400`, `line-height: 1.5`, `color: var(--ed-ink)` y `margin: 8px 0 0`. Cero tokens `--ed-*` nuevos, cero hex literales, sin `@media`, sin `overflow-wrap`, sin `!important`, sin subir especificidad a `.summary-errors .summary-error-translation` (son nombres nuevos sin regla legacy que vencer). En la sección de Resultados (`app.css:1959-1985`) dejar SOLO un comentario de referencia cruzada, sin regla.

6. **Verificación end-to-end.** Crear `tests/screen-translation.test.js` siguiendo el molde de `tests/screen-session-editorial.test.js` (leer los ficheros como texto). Este task aporta V1, V2, V4 y V6 más la aserción de que el contenido real con el campo nuevo valida: importar `validateContent` y correrlo sobre `content/exercises/preposiciones.json` leído de disco, esperando `ok: true` y `errors: []`. Los recuentos de V2 se DERIVAN con `match(/…/g)?.length ?? 0`, nunca transcritos (D-31-06).
  </action>
  <verify>
    <automated>node --test tests/screen-translation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/*.test.js tests/fixtures/*.test.js` termina con exit 0 (suite entera verde, retrocompatibilidad del corpus demostrada).
    - `node -e "const {validateContent}=await import('./src/data/schema-validator.js')"` no es necesario; en su lugar el test nuevo asserta `validateContent(<preposiciones.json real>).ok === true` y `errors` vacío.
    - V1: `tests/screen-translation.test.js` asserta que `app.css` contiene el bloque con los seis declarantes exactos `var(--ed-font-serif)`, `16px`, `400`, `1.5`, `var(--ed-ink)` y `margin: 8px 0 0`.
    - V2 (derivado, no literal): `app.css.match(/\.session-translation\b/g).length === 1` — el criterio de estilo se declara UNA sola vez.
    - V4: el nodo de cada superficie enlaza por `x-text`, y el recuento de la directiva de HTML crudo en `index.html` es IGUAL al de `git show HEAD:index.html` (comparación derivada de dos fuentes distintas, no un `== 0` desnudo).
    - V6 (orden DOM, derivado por índices): en `index.html`, `indexOf('session-feedback-correct') < indexOf('session-translation') < indexOf('session-explanation')`; y `indexOf('summary-error-translation') < indexOf('summary-error-explanation')`.
    - `grep -c 'Paolo es de Nápoles de nacimiento.' content/exercises/preposiciones.json` devuelve 1.
    - `git diff --stat src/domain/ src/screens/app.js` está VACÍO (V8, D-46-01) — el render es HTML + CSS puro.
    - `git diff app.css styles.css` no elimina ni modifica ninguna línea preexistente: el diff de `app.css` es puramente aditivo (solo líneas `+`, más el comentario de referencia cruzada).
  </acceptance_criteria>
  <done>El camino de producto funciona de punta a punta para la frase canónica: el schema la acepta, las dos superficies la pintan con doble guard, la regla CSS única la estiliza, el motor está byte-intacto y la suite queda verde. Committeado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expansión — rechazos SCH-02, no-migración SCH-03 y el resto del contrato V3/V5/V7/V8/V9</name>
  <read_first>
    - `src/data/schema-validator.js` — `:497-534` (`validateWordButtonsSurface`) y `:563-600` (`validateMatchSurface`): las dos firmas donde va el rechazo, con su convención de `label`.
    - `tests/exercise-types.test.js` — `:104-126` (molde `validateContent` inline) y `:1328-1375` (`slotCountOf` + el caveat de tautología del `expected` derivado, que aquí se cita para NO repetirlo).
    - `tests/screen-translation.test.js` — el fichero creado en Task 1, al que este task añade V3, V5, V7, V8 y V9.
    - `src/data/schema-validator.js:210-231` — `validateVariants`, para confirmar que el `label` `variants[k]` prefija los mensajes de rechazo.
    - `src/screens/app.js:73` — la constante `SESSION_AUTO_ADVANCE_MS` con su valor actual, que V8 congela.
    - `index.html:617-620` (`.session-why`), `:629-632` (`.session-cta`) y `:525-540` (`.session-prompt`) — los literales que V9 congela.
    - `src/data/state-migrations.js` o el módulo de migración de state que exista (localizarlo por grep de `schemaVersion`) — solo para assertear que NO se añade una rama 13→14.
  </read_first>
  <files>src/data/schema-validator.js, tests/schema-translation.test.js, tests/screen-translation.test.js</files>
  <behavior>
    - Test: slot `word-buttons` con `translationES` en su primera variante → `validateContent` devuelve `ok: false` con el mensaje de rechazo y el label `variants[0]`.
    - Test: slot `word-buttons` con `translationES` en su ÚLTIMA variante → rechaza con el label de esa variante.
    - Test: slot `match` de 3 variantes con `translationES` en la segunda → rechaza con label `variants[1]`.
    - Test: `translationES: {}` en una variante `match` → rechaza (rechazo estructural, no por contenido).
    - Test: `translationES: { text: "" }` en una variante `word-buttons` → rechaza.
    - Test: `translationES: { text: "Él está en Nápoles" }` (acentuado) en `match` → rechaza (el rechazo no inspecciona el texto).
    - Test: variante `multiple-choice` con `translationES.text` = `""` → rechaza; con solo espacios → rechaza.
    - Test: variante `multiple-choice` con `translationES.text` que contiene el hueco de tres guiones bajos → rechaza con el segundo mensaje.
    - Test: dos variantes hermanas con `translationES.text` idéntico → válido (no hay unicidad).
    - Test: reordenar el array `variants` de un slot y re-validar → veredicto idéntico.
    - Test: `translationES` ausente en las 250 slots del corpus real → `ok: true` (retrocompat).
    - Test: `text` en NFC y en NFD → ambos válidos, sin normalización destructiva.
    - Test (SCH-03): el corpus nuevo no introduce ninguna rama de migración `13→14`; `schemaVersion` sigue en 13 y un state pre-existente sobrevive al boot con su progreso intacto.
  </behavior>
  <action>
1. **SCH-02 (D-46-04).** Añadir a `validateWordButtonsSurface` y a `validateMatchSurface` el rechazo estructural: si `surface.translationES !== undefined`, `push` el mensaje fijado por el UI-SPEC §Copywriting Contract — `"translationES" no está permitido en variantes de tipo "match" / "word-buttons"` — prefijado por `label` igual que sus reglas hermanas. El rechazo es por PRESENCIA de la clave, nunca por su contenido: no inspeccionar `text`. Sin early-return.

2. **Matriz de schema.** Crear `tests/schema-translation.test.js` con la matriz completa del bloque `<behavior>`, usando el molde `validateContent` con documentos inline mínimos (rama `variants[]` + `explanation` a nivel de slot) y, para la retrocompatibilidad, el `content/exercises/preposiciones.json` REAL leído de disco. Toda cifra congelada se DERIVA del disco (D-31-06): el número de slots del corpus se computa, no se transcribe.

3. **SCH-03 (D-46-05).** Añadir en el mismo fichero el test explícito de no-migración: localizar por grep el módulo que gobierna `schemaVersion`, assertear que su versión actual sigue siendo 13 y que no existe ninguna rama de migración cuyo destino sea 14, y que la fase no toca ninguna ruta de state. Derivar el 13 del propio módulo, no escribirlo dos veces.

4. **V3, V5, V7, V8, V9 en `tests/screen-translation.test.js`.**
   - **V3 (cero tokens nuevos, derivado):** contar las definiciones `--ed-*` dentro del `:root` de `app.css` en HEAD y en el working tree, y assertear igualdad. Dos magnitudes distintas, no una leída dos veces.
   - **V5 (no-leak, D-46-11 — la aserción cabeza de serie):** para CADA ocurrencia de `translationES` en `index.html`, exigir que la línea o el nodo que la contiene lleve un guard de estado resuelto (`sessionFeedback !== null`) o esté dentro del bloque del resumen. Escribirlo como un escáner sobre las líneas, con su propia CLÁUSULA DE NO-VACUIDAD PRIMERO: el número de ocurrencias encontradas debe ser exactamente el número de nodos que la fase declara (derivado contando las clases nuevas), porque un escáner que deja de casar devuelve lista vacía y pasaría en verde.
   - **V7 (SCH-02 en el render):** acotar en `index.html` los sub-templates `word-buttons` y `match` de las dos superficies por sus rangos de `<template x-if=…>`, y assertear cero ocurrencias de `translationES` DENTRO de esas regiones (gate con alcance de región, con la no-vacuidad de que las cuatro regiones se localizaron).
   - **V8 (motor byte-intacto, D-46-01):** assertear que `src/screens/app.js` sigue declarando `SESSION_AUTO_ADVANCE_MS` con el valor que tiene en HEAD (derivado de `git show HEAD:src/screens/app.js`, no transcrito) y que el diff de la fase en `src/domain/` y en `src/screens/app.js` está vacío.
   - **V9 (D-46-01/D-46-10, REND-03):** assertear que `.session-prompt`, `.session-why` y `.session-cta` y sus literales de copy siguen presentes sin cambios, y que la expresión `x-show` del nodo de traducción NO referencia el flag de explanation-revelada (`sessionExplanationRevealed`) — el botón «¿Por qué?» y la tecla `e` no tocan la traducción.
  </action>
  <verify>
    <automated>node --test tests/schema-translation.test.js tests/screen-translation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/*.test.js tests/fixtures/*.test.js` termina con exit 0.
    - Los 13 casos del bloque `<behavior>` existen como tests nombrados en `tests/schema-translation.test.js` y todos pasan.
    - Prueba por MUTACIÓN del rechazo SCH-02: añadir temporalmente `translationES` a una variante de `content/exercises/articoli.json` (que es `multiple-choice`) NO debe fallar; añadirlo a un slot `word-buttons` o `match` real SÍ debe fallar. Ejecutar la segunda mitad contra un documento inline y observar el `ok: false`, con el mensaje exacto y el label de variante correcto en `errors`.
    - V5 lleva su cláusula de no-vacuidad ANTES del assert principal, y su cifra de referencia se deriva del disco: `grep -c 'session-translation\|summary-error-translation' index.html` alimenta la comprobación en lugar de un literal.
    - V7 acota las cuatro regiones por índice y asserta que las cuatro se localizaron (no-vacuidad) antes de assertear la ausencia.
    - V8: `git diff --stat src/domain/ src/screens/app.js` vacío, y el valor de `SESSION_AUTO_ADVANCE_MS` en el working tree es idéntico al de `git show HEAD:src/screens/app.js`.
    - `grep -n 'schemaVersion' src/data/*.js | head` confirma que la versión actual sigue siendo 13 y `grep -rn '14' <el módulo de migración>` no revela ninguna rama nueva de destino 14.
  </acceptance_criteria>
  <done>El schema rechaza `translationES` en `match` y `word-buttons` con el label correcto, `schemaVersion` sigue en 13 sin migración verificado por test explícito, y las nueve aserciones V1-V9 del UI-SPEC existen y pasan — con V5 y V7 llevando su propia cláusula de no-vacuidad derivada del disco. Committeado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `content/exercises/*.json` → DOM | El texto de la traducción es dato de contenido autorado que cruza al DOM del navegador. Es la ÚNICA frontera de este plan. |
| `content/exercises/*.json` → `schema-validator.js` | Entrada de contenido hacia el validador; la única puerta que rechaza formas malas antes de runtime. |

## STRIDE Threat Register (ASVS L1 · block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Tampering (inyección de contenido en el DOM) | los 2 nodos nuevos de `index.html` | **high** | mitigate | `x-text` EXCLUSIVO en ambos nodos; jamás la directiva de inyección de HTML crudo (T-02-01). Verificado por V4, que compara el recuento de esa directiva en el working tree contra `git show HEAD:index.html` — un incremento pone la suite roja. |
| T-46-02 | Information Disclosure (leak de la respuesta antes de resolver) | el guard `x-show` de la superficie 1 | **high** | mitigate | Doble guard `sessionFeedback !== null` Y presencia del dato (D-46-09). El invariante lo congela V5, con cláusula de no-vacuidad para que un escáner que deje de casar no pase en verde. Nota honesta (D-46-11): el payload ya expone `correctIndex`, así que el guard defiende el FLUJO pedagógico, no el secreto criptográfico. |
| T-46-03 | Tampering (regresión silenciosa del motor) | `src/domain/`, `src/screens/app.js` | medium | mitigate | V8: `git diff` vacío en ambos y `SESSION_AUTO_ADVANCE_MS` derivado de HEAD. La fase es HTML + CSS puro. |
| T-46-04 | Denial of Service | n/a | low | accept | La app es estática, sin servidor, sin red y de un solo usuario en su propia máquina: no hay superficie de DoS. |
| T-46-05 | Spoofing / Repudiation / Elevation of Privilege | n/a | low | accept | No hay autenticación, sesiones, roles ni multi-usuario en esta app (CLAUDE.md: local, un solo autor, sin cuentas). Padear el registro con ítems ASVS inaplicables lo volvería ceremonial. |

**Sin instalaciones de paquetes en este plan** → no aplica el gate de legitimidad de paquetes (cero dependencias nuevas, UI-SPEC §Registry Safety).
</threat_model>

<verification>
1. `node --test tests/*.test.js tests/fixtures/*.test.js` → exit 0.
2. `git diff --stat src/domain/ src/screens/app.js` → vacío.
3. Inspección manual del autor NO requerida en este plan; las dos comprobaciones visuales `backstop`
   del UI-SPEC (long-text en cada superficie) viven en el plan 46-05, cuando exista la traducción
   más larga del piloto de la que derivarlas.
</verification>

<success_criteria>
- La frase canónica se ve en las dos superficies, acertando y fallando, con doble guard.
- `translationES` aceptado en `multiple-choice` y rechazado en `match` / `word-buttons`.
- `schemaVersion` en 13, sin migración, verificado por test.
- V1-V9 del UI-SPEC existen como aserciones que pasan; V5 y V7 con no-vacuidad derivada del disco.
- Suite verde, motor byte-intacto.
</success_criteria>

<output>
Create `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-01-SUMMARY.md` when done
</output>
