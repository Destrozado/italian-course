---
phase: 46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones
plan: 05
type: execute
wave: 5
depends_on: [46-04]
files_modified:
  - content/exercises/preposiciones.json
autonomous: false
requirements: [TVAL-04, GATE-01, GATE-02, TRAD-01]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    # ── Goal-backward ─────────────────────────────────────────────────────────
    - "Las dos mutaciones de contenido de D-46-18 se EJECUTARON y el rojo se OBSERVÓ con exit code apuntado: leer el código del gate no cuenta como verificación (lección de la Phase 45, cinco gates vacuos cazados los cinco corriendo la mutación)."
    - "Tras restaurar las dos mutaciones, el reporter vuelve a exit 0 y la suite a verde — la restauración se verifica, no se asume."
    # ── Edge probe · TVAL-04 (1) ──────────────────────────────────────────────
    - "UNA sola traducción no-`validated` entre las 96 pone el gate ROJO: no se promedia ni se tolera un umbral. MUTACIÓN 1 de la fase (dejar una traducción en `pending`) se EJECUTA y se observa el rojo."
    # ── Edge probe · GATE-01 (1) ──────────────────────────────────────────────
    - "95/96 → ROJO; 96/96 → VERDE. Los dos lados del umbral se ejecutan, no solo el verde."
    # ── UI-SPEC ## UI Considerations · 2 backstop ─────────────────────────────
    - statement: "E1 · long-text — la traducción de 2+ líneas dentro de `.session-feedback` fluye por espacios con `line-height: 1.5` y ancho heredado de la caja, sin `overflow-wrap`, sin `max-width` y sin truncado."
      verification: backstop
    - statement: "E2 · long-text — la misma envoltura multilínea dentro de la card de «Errores cometidos», comprobada también en esta segunda superficie."
      verification: backstop
  artifacts:
    - path: "content/exercises/preposiciones.json"
      provides: "Estado final restaurado: 96 translationES validated tras las dos mutaciones ejecutadas y revertidas"
      contains: "translationES"
  key_links:
    - from: "content/exercises/preposiciones.json"
      to: "scripts/run-validation-271.mjs"
      via: "la mutación cambia el status derivado de una variante y el sub-gate de cobertura lo convierte en exit 1"
      pattern: "TRANSLATION_COVERAGE"
    - from: "docs/TRANSLATION-VALIDATION-PROMPT.md"
      to: "content/exercises/preposiciones.json"
      via: "el criterio S4 del prompt es la autoridad sobre acentos; la mutación 2 lo prueba haciendo que el quórum devuelva incorrecta con el tag de acentos"
      pattern: "s4_acentos"
---

<objective>
Cerrar la fase probando que los gates MUERDEN: ejecutar las mutaciones 1 y 2 de D-46-18 sobre el
contenido, observar el rojo con exit code apuntado, restaurar, y verificar con el autor las dos
comprobaciones visuales `backstop` que ningún test puede confirmar.

Purpose: la lección de la Phase 45 es que cinco gates vacuos se cazaron los cinco corriendo la
mutación, y ninguno leyendo el código. Y la del CR-01 de la Phase 44 es que un registro que miente
certifica en verde. Un gate cuya mordida no se ha observado es una afirmación, no una garantía.

Output: los exit codes de las dos mutaciones registrados literalmente en el SUMMARY, el corpus
restaurado y verificado, y la aprobación del autor sobre las dos envolturas de texto largo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-UI-SPEC.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-03-SUMMARY.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-04-SUMMARY.md
</context>

## Artifacts this phase produces

Este plan no crea símbolos nuevos: ejecuta mutaciones transitorias sobre `translationES` (creado en
46-01) contra los gates de 46-03. El inventario completo de símbolos nuevos de la fase está en
`46-01-PLAN.md § Artifacts this phase produces`.

## Nota de método (no negociable)

Las tres mutaciones de D-46-18 son **corridas reales con exit code apuntado**, no lecturas. La
mutación 3 (desenganche del array de cobertura) ya se ejecutó en el plan 46-03 Task 3. Este plan
ejecuta las dos que necesitan las 96 traducciones ya `validated`. Cada una sigue el mismo ciclo:
**foto verde → mutar → correr → apuntar el rojo → restaurar → correr → confirmar el verde.**

Y la regla derivada: **si un fix propuesto por el code review de esta fase toca un gate, ese fix se
verifica con la MISMA mutación que el código que arregla.** 2 de 4 fixes de revisor en la Phase 44
eran incorrectos y uno era peor que el bug (blanqueaba el array `CATEGORIES` entero). Cerrar un gap
no es verificarlo cerrado.

<tasks>

<task type="auto">
  <name>Task 1: MUTACIÓN 1 — una traducción en `pending` y el gate de cobertura en ROJO</name>
  <read_first>
    - `scripts/run-validation-271.mjs` — la región `TRANSLATION_COVERAGE` y su sub-gate escritos en el plan 46-03: qué imprime en PASS y qué imprime en FAIL, y cómo su veredicto entra en `gatePass`.
    - `content/exercises/preposiciones.json` — un slot cualquiera con su `variants[k].translationES.validation.passes` ya poblado, para saber exactamente qué se vacía y qué se restaura.
    - `src/data/validation-state.js` — `deriveStatus`: con `passes: []` el status derivado es `pending`, jamás `validated`.
    - `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-03-SUMMARY.md` — los exit codes registrados de la mutación 3, como molde del registro que este task tiene que producir.
  </read_first>
  <files>content/exercises/preposiciones.json</files>
  <action>
Ejecutar la primera mutación de D-46-18 y REGISTRAR el resultado observado.

1. **Foto verde de partida.** `git status --porcelain content/exercises/preposiciones.json` limpio.
   Correr `node scripts/run-validation-271.mjs` y apuntar el exit code (debe ser 0) y la línea del
   sub-gate de cobertura de traducción en PASS con sus dos cifras.

2. **La mutación.** Elegir UNA variante (registrar cuál, por su dirección compuesta `<slot-id>#<k>`)
   y vaciar su `translationES.validation.passes` a `[]`, dejando `status` en `"pending"`. El texto de
   la traducción NO se toca: la mutación prueba el umbral de cobertura, no la calidad.

3. **La observación.** Correr `node scripts/run-validation-271.mjs`. DEBE salir con exit 1, y el
   sub-gate de cobertura de traducción DEBE imprimir FAIL con el contador `pending` a 1 y las dos
   cifras separadas por una unidad. Apuntar literalmente la línea impresa y el exit code. **Una sola
   traducción no-`validated` entre las 96 pone el gate rojo: es el lado fail-first del umbral
   (edge GATE-01/boundary), y no se promedia ni se tolera.**

4. **Comprobación del otro lado del umbral.** Restaurar la variante (`git checkout --
   content/exercises/preposiciones.json`), correr de nuevo y confirmar exit 0 con el sub-gate en PASS.
   Los DOS lados del umbral quedan ejecutados, no solo el verde.

5. **Registro.** Anotar en el SUMMARY: la dirección compuesta mutada, el exit code y la línea impresa
   del rojo, y el exit code y la línea del verde restaurado, con fecha. Sin ese registro la mutación
   no cuenta como ejecutada — y una cifra afirmada en prosa en lugar de derivada de la corrida es
   exactamente el CR-01 de la Phase 44.
  </action>
  <verify>
    <automated>node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Rojo OBSERVADO: exit code 1 registrado literalmente en el SUMMARY, junto con la línea impresa del sub-gate de cobertura de traducción en FAIL mostrando `pending=1`.
    - Las dos cifras del FAIL difieren en exactamente una unidad, y ambas están interpoladas por el reporter desde valores computados (no transcritas por el ejecutor).
    - Verde restaurado y VERIFICADO: `git status --porcelain content/exercises/preposiciones.json` vacío y `node scripts/run-validation-271.mjs; echo $?` devuelve 0, con la línea del sub-gate en PASS registrada.
    - `node --test tests/*.test.js tests/fixtures/*.test.js` termina con exit 0 tras la restauración.
    - El SUMMARY registra la dirección compuesta concreta que se mutó, no «una variante».
  </acceptance_criteria>
  <done>La mutación 1 se ejecutó: el gate de cobertura de traducción salió ROJO con exit 1 por una única traducción en `pending`, los dos lados del umbral quedaron ejecutados, el corpus se restauró y el verde se re-verificó. Exit codes y líneas impresas registrados en el SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: MUTACIÓN 2 — quitar los acentos a una traducción y ver al quórum cazarla</name>
  <read_first>
    - `docs/TRANSLATION-VALIDATION-PROMPT.md` — el criterio S4 (acentos RAE), el tag literal `[S4-acentos]` del contrato de output, y el few-shot FAIL cuyo motivo es una tilde ausente. Es el criterio que esta mutación pone a prueba.
    - `scripts/validate-translation-pass.mjs` — la CLI con la dirección compuesta y `--write`, para re-correr el quórum sobre una sola variante.
    - `src/data/validation-state.js` — `deriveStatus`: un `verdict: "incorrecta"` presente en `passes[]` fuerza `disputed` salvo override de autor con quórum, así que un solo `incorrecta` basta para poner el gate rojo.
    - `scripts/run-validation-271.mjs` — el sub-gate de cobertura de traducción y el sub-gate VAL-08 (cero `disputed`), los dos que esta mutación debe poner rojos.
    - `content/exercises/preposiciones.json` — elegir una variante cuya traducción lleve al menos dos tildes, para que la mutación sea inequívoca.
  </read_first>
  <files>content/exercises/preposiciones.json</files>
  <action>
Ejecutar la segunda mutación de D-46-18. **La autoridad sobre acentos es el QUÓRUM (criterio S4), NO
un escáner mecánico de tildes** — el escáner se descartó explícitamente porque produce falsos
positivos sobre nombres propios y monosílabos (TRAD-01/encoding, D-46-12). Por tanto la cadena que
esta mutación prueba es completa: **texto sin tildes → el quórum devuelve `incorrecta` con el tag
`[S4-acentos]` → `deriveStatus` da `disputed` → el gate sale ROJO.** No inventar un escáner de
acentos: eso sería re-litigar una decisión cerrada y añadir un gate que el autor rechazó.

1. **Foto verde de partida.** `git status --porcelain` limpio en el fichero. `node
   scripts/run-validation-271.mjs` → exit 0, sub-gate de traducción en PASS, VAL-08 en PASS. Apuntar.

2. **La mutación.** Elegir UNA variante cuya traducción lleve ≥2 tildes (registrar su dirección
   compuesta y el texto original literal). Sustituir el texto por el MISMO texto con todas las tildes
   quitadas, y vaciar su `translationES.validation.passes` a `[]` — porque los pases anteriores
   validaron el texto ANTERIOR y dejarlos sería un registro que miente sobre qué se validó.

3. **Re-correr el quórum sobre esa única variante.** Dos invocaciones de
   `scripts/validate-translation-pass.mjs` con `--write` (una por vendor, con `--avoid` para garantizar
   `by` distintos), exactamente como en el plan 46-04. Apuntar los dos veredictos y los `concerns`
   literales.

4. **La observación.** Al menos uno de los dos vendors DEBE devolver `verdict: "incorrecta"` con un
   `concern` que empiece por el tag `[S4-acentos]` (la memoria del proyecto registra que DeepSeek es el
   estricto en acentos, que es justo el criterio que más pesa aquí). Correr `node
   scripts/run-validation-271.mjs`: DEBE salir con exit 1, con el sub-gate de cobertura de traducción
   en FAIL y VAL-08 (cero `disputed`) también en FAIL, nombrando la dirección compuesta. Apuntar los
   dos exit codes y las dos líneas.

5. **Si los DOS vendors aprueban el texto sin tildes: es un HALLAZGO BLOQUEANTE, no una excusa.**
   Significa que el criterio S4 del prompt no muerde. En ese caso: NO restaurar y seguir adelante. Hay
   que (a) registrar los dos veredictos literales en el SUMMARY, (b) reforzar el criterio S4 y el
   few-shot FAIL de `docs/TRANSLATION-VALIDATION-PROMPT.md` — la excepción o el refuerzo se escribe
   EN EL DOC, nunca solo en el `notes` de un plan, porque el validador no ve el `notes` y el síntoma de
   haberlo hecho mal es que el modelo marca un patrón y aprueba otro idéntico — y (c) repetir el ciclo
   completo de mutación hasta observar el rojo. Y como el prompt cambió, re-validar lo que ya estaba
   `validated` bajo el prompt viejo (D-46-12 lo advierte: es su coste `costly`).

6. **Restauración y re-verificación.** `git checkout -- content/exercises/preposiciones.json`, correr
   el reporter y confirmar exit 0 con los dos sub-gates en PASS, y `node --test` en verde.

7. **Registro.** En el SUMMARY: la dirección compuesta, el texto original y el mutado, los dos
   veredictos con sus `concerns` literales y sus `by` reales, los exit codes del rojo y del verde
   restaurado, y la fecha de cada foto. Fechar las dos fotos es lo que impide que un número obsoleto
   quede certificado en verde (CR-01 de la Phase 44).
  </action>
  <verify>
    <automated>node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Rojo OBSERVADO: exit code 1 registrado, con la línea del sub-gate de cobertura de traducción en FAIL Y la línea de VAL-08 en FAIL nombrando la dirección compuesta mutada.
    - Al menos un `verdict: "incorrecta"` con un `concern` que empieza por el tag `[S4-acentos]`, transcrito LITERALMENTE en el SUMMARY junto con el `by` real del modelo que lo emitió (el que respondió, no el pinneado).
    - No se creó ningún escáner mecánico de acentos: `git diff --stat` de este task no toca `scripts/` ni `tests/`; el único fichero modificado (y luego restaurado) es `content/exercises/preposiciones.json`.
    - Verde restaurado y VERIFICADO: `git status --porcelain content/exercises/preposiciones.json` vacío, `node scripts/run-validation-271.mjs; echo $?` devuelve 0, y `node --test tests/*.test.js tests/fixtures/*.test.js` exit 0.
    - Las dos fotos (verde de partida y verde restaurado) llevan fecha en el SUMMARY, y las cifras de ambas se copian de la salida del reporter, no se afirman.
    - Si se activó el camino del punto 5: el refuerzo quedó escrito en `docs/TRANSLATION-VALIDATION-PROMPT.md`, el ciclo se repitió hasta observar el rojo, y el SUMMARY registra qué se re-validó bajo el prompt nuevo.
  </acceptance_criteria>
  <done>La mutación 2 se ejecutó de punta a punta: el texto sin tildes fue cazado por el quórum con el tag `[S4-acentos]`, `deriveStatus` lo llevó a `disputed`, el reporter salió en exit 1 con dos sub-gates en FAIL, el corpus se restauró y el verde se re-verificó. Sin inventar ningún escáner de acentos. Todo registrado con exit codes, veredictos literales y fechas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual del autor — las dos envolturas de texto largo (backstop del UI-SPEC)</name>
  <read_first>
    - `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-UI-SPEC.md` — §UI Considerations, los dos ítems `backstop`: E1 · long-text y E2 · long-text, con su `statement` y la forma de verificación (inspección visual del autor sobre la traducción MÁS LARGA del piloto, derivada del disco, no elegida a ojo).
    - `app.css` — la regla `.session-translation, .summary-error-translation` escrita en el plan 46-01 (sin `overflow-wrap`, sin `max-width`, sin `height`) y `.session-feedback` con su `padding: 14px 16px`.
    - `content/exercises/preposiciones.json` — el corpus final, del que se DERIVA la traducción más larga.
  </read_first>
  <what-built>
Las 96 traducciones de Preposiciones están autoradas, `validated` por el quórum cross-vendor y
enganchadas a los dos gates, que ya se han verificado por mutación (las tres mutaciones de D-46-18
ejecutadas con su rojo observado y apuntado). Lo que ningún test puede confirmar son los dos ítems
`backstop` del UI-SPEC: que la traducción MÁS LARGA del piloto envuelve bien dentro de la caja de
feedback y dentro de la card de «Errores cometidos», sin desbordar, sin truncarse y sin empujar el
CTA «Continuar →» fuera de vista. Y el `backstop` de TRAD-01/encoding: que las 96 se leen como español
correcto y con registro adecuado — cuya autoridad mecánica es el quórum (que ya pasó), y cuyo último
lector es el autor.
  </what-built>
  <how-to-verify>
1. **Derivar del disco** —no elegir a ojo— la traducción más larga del piloto y su dirección
   compuesta:
   `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8'));let best=null;for(const s of d.exercises)for(const [k,v] of (s.variants||[]).entries()){const t=v.translationES?.text;if(t&&(!best||t.length>best.t.length))best={id:s.id,k,t};}console.log(best.id+'#'+best.k, best.t.length, JSON.stringify(best.t))"`
   Apuntar la dirección compuesta y el `prompt` italiano de esa variante.

2. **Arrancar la app:** `npx serve` en la raíz del proyecto y abrir `http://localhost:3000`
   (localStorage no funciona bajo `file://` en Firefox, así que hay que servirlo).

3. **Superficie 1 — caja de feedback.** Entrar en la categoría Preposiciones y llegar a esa variante
   (repaso o test completo). Responderla **mal** a propósito. Comprobar en la caja de feedback:
   - la traducción aparece debajo de «Respuesta correcta: **…**» y ENCIMA de la explicación;
   - está en serif (misma familia que la frase italiana de arriba), no en la sans de la explicación;
   - no lleva etiqueta «Traducción:» ni comillas envolventes;
   - **envuelve en 2+ líneas dentro de la caja, sin desbordar por los lados, sin cortarse con puntos
     suspensivos, y sin empujar el botón «Continuar →» fuera de la vista.**
4. Volver a esa misma variante y responderla **bien**. Comprobar que la traducción aparece igual, que
   NO hay auto-avance (el avance es manual con «Continuar →») y que el botón «¿Por qué?» sigue
   revelando la explicación aparte, sin que la traducción le robe el sitio.
5. **Superficie 2 — «Errores cometidos».** Terminar la sesión con esa variante fallada y abrir el
   resumen. Comprobar en su card: prompt → «Tu respuesta» tachada → «Respuesta correcta» → traducción
   en serif → explicación en itálica muted; y que **la traducción larga envuelve dentro de la card sin
   desbordar ni recortarse.**
6. **Caso «sin traducción» (REND-05).** Ir a cualquier categoría que todavía NO esté traducida (p. ej.
   Articoli) y fallar un ejercicio: la caja de feedback debe verse **exactamente como antes** — sin
   hueco, sin etiqueta, sin placeholder, sin guion.
7. **Lectura de muestra (backstop de TRAD-01/encoding).** Leer las traducciones de 3 o 4 slots
   completos: ¿se leen como español natural y acentuado, con el registro adecuado? ¿Alguna se ha
   convertido en una explicación gramatical o en una repetición del `gloss` español del `prompt`?
   Si aparece cualquiera de las dos, es un hallazgo que se arregla en el texto y se re-valida por
   quórum: no se aprueba.
  </how-to-verify>
  <acceptance_criteria>
    - La dirección compuesta de la traducción más larga se DERIVÓ del disco con el comando indicado y quedó registrada en el SUMMARY junto con su longitud en caracteres — no se eligió a ojo.
    - El autor confirma explícitamente los cuatro puntos de la superficie 1 (posición, serif, sin etiqueta, envoltura sin desborde ni truncado ni CTA desplazado).
    - El autor confirma la envoltura sin desborde en la superficie 2.
    - El autor confirma que una categoría sin traducir no deja hueco, etiqueta ni placeholder (REND-05).
    - El autor confirma la lectura de muestra, o registra los hallazgos concretos (dirección compuesta + qué está mal) para arreglarlos en el texto y re-validarlos por quórum.
    - Los dos ítems `backstop` del UI-SPEC quedan cerrados con la evidencia de la inspección (qué se miró, en qué variante, en qué superficie) o abiertos con el hallazgo; **nunca cerrados en silencio.** Un backstop que no se puede confirmar con evidencia se abstiene y se marca como pendiente de revisión humana, no pasa.
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" si las dos envolturas y la lectura de muestra están bien, o describe los hallazgos con su dirección compuesta (`<slot-id>#<k>`) para que se arreglen en el texto y se re-validen por quórum.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus mutado → gates | Una mutación deliberada del contenido es entrada adversarial CONTRA los gates: el objetivo es comprobar que la rechazan. |
| corpus mutado → modelos externos (mutación 2) | 2 llamadas adicionales con el texto sin tildes, para que el quórum lo juzgue. |
| corpus mutado → repositorio | El riesgo real de este plan: que una mutación quede sin restaurar y se committee. |

## STRIDE Threat Register (ASVS L1 · block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-23 | Tampering (una mutación queda committeada) | `content/exercises/preposiciones.json` | **high** | mitigate | Cada mutación termina con `git checkout --` del fichero y una re-verificación EXPLÍCITA: `git status --porcelain` vacío + reporter exit 0 + suite exit 0, todo antes de cerrar el task. La restauración es un criterio de aceptación, no un paso implícito. |
| T-46-24 | Repudiation (afirmar que el gate muerde sin haberlo observado) | el SUMMARY del plan | **high** | mitigate | El criterio de aceptación es el EXIT CODE OBSERVADO y la línea impresa TRANSCRITA, con fecha en las dos fotos. Una cifra afirmada en prosa en lugar de copiada de la corrida es exactamente el CR-01 de la Phase 44 (suite firmando 247 con el reporter en 250). |
| T-46-25 | Tampering (inventar un gate que el autor rechazó) | la mutación 2 | medium | mitigate | El plan prohíbe explícitamente crear un escáner mecánico de acentos (descartado en D-46-12/TRAD-01-encoding por falsos positivos). Verificado por el criterio de que el diff de la mutación 2 no toca `scripts/` ni `tests/`. |
| T-46-26 | Information Disclosure (fuga de secretos en las 2 llamadas extra) | `callModel` | medium | mitigate | Mismas mitigaciones ya verificadas en el plan 46-02: clave en header, nunca en log; el pase escrito solo lleva `{ by, date, verdict, concerns }`. |
| T-46-27 | Spoofing / Denial of Service / Elevation of Privilege | n/a | low | accept | Corridas locales del autor sobre su propia máquina; sin servidor persistente, sin sesiones, sin roles, sin multi-usuario. El `npx serve` del checkpoint escucha solo en localhost y se cierra al terminar. |

## Gate de legitimidad de paquetes

`npx serve` aparece en el checkpoint, pero **no es una instalación de dependencia del proyecto**: es la
herramienta de desarrollo ya establecida en CLAUDE.md § Installation para servir la app estática, sin
`package.json` ni `node_modules` en el repo. No hay altas de paquetes en este plan, así que
`T-{phase}-SC` se declara no aplicable por ausencia de instalaciones, no por omisión.
</threat_model>

<verification>
1. Mutación 1: exit 1 observado y transcrito; restauración verificada con exit 0.
2. Mutación 2: exit 1 observado en dos sub-gates, veredicto `incorrecta` con tag `[S4-acentos]`
   transcrito; restauración verificada con exit 0.
3. `node scripts/run-validation-271.mjs` final → **exit 0**.
4. `node --test tests/*.test.js tests/fixtures/*.test.js` final → **exit 0**.
5. `git diff --stat src/domain/ src/screens/app.js` → vacío (V8, motor byte-intacto).
6. `git status --porcelain` → limpio; ninguna mutación residual.
7. Checkpoint del autor aprobado, con la dirección compuesta de la traducción más larga registrada.
</verification>

<success_criteria>
- Las TRES mutaciones de D-46-18 están ejecutadas (la 3 en el plan 46-03, la 1 y la 2 aquí) y sus
  rojos observados y registrados con exit code, línea impresa y fecha.
- El corpus está restaurado y verificado: reporter exit 0, suite exit 0, `git status` limpio.
- Los dos ítems `backstop` del UI-SPEC y el `backstop` de TRAD-01/encoding están cerrados con
  evidencia de inspección del autor, o abiertos con hallazgos concretos — nunca en silencio.
- El motor sigue byte-intacto.
</success_criteria>

<output>
Create `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-05-SUMMARY.md` when done
</output>
