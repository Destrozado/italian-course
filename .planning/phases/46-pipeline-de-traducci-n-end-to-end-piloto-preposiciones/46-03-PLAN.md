---
phase: 46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones
plan: 03
type: execute
wave: 3
depends_on: [46-01, 46-02]
files_modified:
  - scripts/run-validation-271.mjs
  - tests/count-arrays-lockstep.test.js
autonomous: true
requirements: [TVAL-04, GATE-01, GATE-02]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    # ── Goal-backward ─────────────────────────────────────────────────────────
    - "El reporter tiene un gate de traducción que sale ROJO si alguna traducción presente en una categoría DECLARADA CUBIERTA no está `validated`, y su veredicto entra en la conjunción `gatePass` que gobierna el exit code."
    - "El gate anti-ceguera pone ROJO cualquier categoría declarada cubierta de traducción que no esté enganchada al array de cobertura: el olvido de enganchar no puede emitir un PASS."
    - "El `expected` de cobertura de traducción se DERIVA del disco (número de variantes `multiple-choice` del fichero de la categoría) y no está transcrito como número mágico en ningún sitio."
    # ── Edge probe · TVAL-04 (2) ──────────────────────────────────────────────
    - "CLÁUSULA DE NO-VACUIDAD obligatoria: un gate cuyo extractor devuelve lista vacía sale VERDE con `deepEqual([],[])` — el modo de fallo real de este repo. El gate lleva su aserción de no-vacuidad con la cifra DERIVADA del disco, siguiendo el precedente de `tests/count-arrays-lockstep.test.js:747-759`."
    - "El veredicto no depende del orden de recorrido de categorías ni de variantes: el gate agrega y compara, no cortocircuita en la primera. Test: permutar el array de cobertura da el mismo veredicto."
    # ── Edge probe · GATE-01 (4) ──────────────────────────────────────────────
    - "El `expected` y el `actual` NO pueden salir de la misma lectura del mismo campo, o el assert es TAUTOLÓGICO (caveat documentado en `tests/exercise-types.test.js:1328-1334`). El gate compara DOS magnitudes distintas derivadas del disco: número de variantes `multiple-choice` del fichero de la categoría (expected) frente a número de `translationES` con status `validated` (actual). Aserción: ninguna de las dos cifras aparece transcrita como literal."
    - "No-vacuidad: un array de cobertura vacío, o una categoría cuyo fichero no se pudo leer, NO puede emitir PASS — sale ROJO por ausencia de datos, no verde por lista vacía."
    - "El gate es independiente del orden del array de cobertura; añadir una categoría al final o al principio no cambia el veredicto."
    - "El 100% se expresa como IGUALDAD DE ENTEROS (`validated === expected`), nunca como un float redondeado ni un porcentaje `>= 0.999` que dejaría pasar una variante suelta con formateo a 99%. Aserción de fuente: el gate no formatea porcentajes para decidir."
    # ── Edge probe · GATE-02 (3) ──────────────────────────────────────────────
    - "Una categoría declarada cubierta y enganchada con un slug que difiera en un solo byte → ROJO. La forma de la entrada del array es load-bearing (`slug: '<v>',` seguido de `file:` en la MISMA línea): si el extractor no forma el par, el gate se vuelve vacuo. Test con un slug alterado."
    - "Cláusula de no-vacuidad propia, por la misma razón que TVAL-04-empty: `deepEqual([], [])` pasa verde. MUTACIÓN 3 de la fase (declarar Preposiciones cubierta y NO engancharla al array) se EJECUTA y se observa el rojo."
    - "El lockstep compara CONJUNTOS de slugs, no secuencias: reordenar el array no puede poner rojo ni verde por sí solo."
  artifacts:
    - path: "scripts/run-validation-271.mjs"
      provides: "GATE-01 — array TRANSLATION_COVERAGE con expected derivado del disco a granularidad de VARIANTE, conteo por deriveStatus, no-vacuidad, igualdad de enteros, enganche a gatePass y línea de acción sugerida"
      contains: "TRANSLATION_COVERAGE"
    - path: "tests/count-arrays-lockstep.test.js"
      provides: "GATE-02 — el array de cobertura de traducción entra como fuente vigilada, con goldens fail-first propios y su cláusula de no-vacuidad derivada del disco"
      contains: "TRANSLATION_COVERAGE"
  key_links:
    - from: "scripts/run-validation-271.mjs"
      to: "src/data/validation-state.js"
      via: "effectiveStatus = deriveStatus — fuente única, sin relax local en el gate de traducción"
      pattern: "deriveStatus"
    - from: "tests/count-arrays-lockstep.test.js"
      to: "scripts/run-validation-271.mjs"
      via: "slugsCiegos + paresSlugFile leen el fuente del reporter como texto y exigen que el array de traducción enganche cada categoría declarada cubierta"
      pattern: "slugsCiegos|paresSlugFile"
    - from: "scripts/run-validation-271.mjs"
      to: "content/exercises/preposiciones.json"
      via: "mcVariantCountOf deriva el expected del disco a granularidad de variante"
      pattern: "mcVariantCountOf"
---

<objective>
Montar los dos gates que hacen que la cobertura de traducción no pueda mentir: el de cobertura
(GATE-01, en el reporter) y el anti-ceguera (GATE-02, en el meta-test de mutación), y EJECUTAR la
tercera mutación de la fase para observar el rojo.

Purpose: la lección de v2.0 es que un array de conteo añadido tarde emitió `225/225 PASS` con una
categoría desenganchada. Los gates van AQUÍ, antes de las 96 traducciones, no al final. Y la lección
de la Phase 45 es que cinco gates vacuos se cazaron los cinco corriendo la mutación — leer el código
del gate no cuenta como verificación (D-46-18).

Output: `TRANSLATION_COVERAGE` en el reporter con `expected` derivado del disco, la familia de
goldens fail-first del nuevo array en el lockstep, y el rojo de la mutación 3 observado y registrado.

**Estado intermedio ESPERADO y correcto al cerrar este plan:** con solo la traducción canónica
`validated` (plan 46-01) y las demás variantes de Preposiciones aún sin traducir, el reporter sale
en ROJO por GATE-01. Eso NO es un fallo a arreglar: es el lado fail-first del umbral (edge
GATE-01/boundary) y la prueba de que el gate muerde. **Prohibido ablandar el gate, bajar el umbral,
excluir Preposiciones del array o añadir una tolerancia para volverlo verde.** El plan 46-04 lo pone
verde autorando y validando las variantes que faltan; el 46-05 vuelve a verificarlo por mutación.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-PATTERNS.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-01-SUMMARY.md
@.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-02-SUMMARY.md
</context>

## Artifacts this phase produces

Símbolos NUEVOS creados por ESTE plan: `TRANSLATION_COVERAGE` (el array de cobertura de traducción en
el reporter), `mcVariantCountOf` (helper de conteo de variantes `multiple-choice` derivado del disco),
`TOTAL_TRANSLATION_EXPECTED`, y el sub-gate impreso `TRAD-COV`. El inventario completo de la fase está
en `46-01-PLAN.md § Artifacts this phase produces`.

<tasks>

<task type="auto">
  <name>Task 1: GATE-01 — cobertura de traducción en el reporter, con `expected` derivado del disco</name>
  <reversibility rating="reversible">D-46-16: el array vive en un solo fichero y su forma la impone el extractor de GATE-02; cambiarlo es un cambio local.</reversibility>
  <read_first>
    - `scripts/run-validation-271.mjs` — leer `:280-345` (`slotCountOf` en `:289-290`, el array `CATEGORIES` en `:292-316` con la forma de entrada exacta y las columnas alineadas, `TOTAL_EXPECTED` en `:320`, y el guard de coherencia dinámico en `:332-345`); `:373` (`effectiveStatus = (passes) => deriveStatus(passes)`, la fuente única sin relax local); `:399-490` (el bucle de conteo por categoría con su rama defensiva para `validation` ausente/malformado y la detección VAL-09 de status escrito vs derivado); `:570-630` (la forma de los sub-gates VAL-06/08/04/09 con la cifra INTERPOLADA desde el valor computado y el comentario que advierte de «plantar la siguiente CR-01»); `:628` (`gatePass`); `:630-677` (el exit gate con sus acciones sugeridas por sub-gate).
    - `tests/exercise-types.test.js:1328-1375` — `slotCountOf` y, sobre todo, el CAVEAT documentado: cuando `expected` y `actual` resuelven a la misma ruta leída en el mismo momento, el assert de conteo es TAUTOLÓGICO. Este task tiene que elegir conscientemente dónde muerde.
    - `content/exercises/preposiciones.json` — la estructura real: `exercises[]`, cada slot con `type` y `variants[]`, y (tras el plan 46-01) `variants[k].translationES.validation.passes`.
    - `src/data/validation-state.js` — `deriveStatus`, que es la única fuente de status.
  </read_first>
  <files>scripts/run-validation-271.mjs</files>
  <action>
Añadir al reporter un segundo array paramétrico, `TRANSLATION_COVERAGE`, hermano de `CATEGORIES` pero
a granularidad de VARIANTE, y su sub-gate.

1. **Helper de conteo derivado.** Junto a `slotCountOf` (`:289-290`) añadir `mcVariantCountOf(file)`:
   lee el JSON de `file` y devuelve la suma de `variants.length` de los slots cuyo `type` es
   `multiple-choice`. Es la unidad de conteo que este proyecto no tenía (los conteos existentes son
   de SLOTS); dejarlo dicho en un comentario junto al helper.

2. **El array.** Declarar `TRANSLATION_COVERAGE` con UNA entrada por categoría DECLARADA CUBIERTA de
   traducción. Hoy solo `preposiciones`. **La forma de la entrada es LOAD-BEARING** porque el
   extractor de GATE-02 la escanea por regex: `slug: '<valor>',` y a continuación `file:` **en la
   MISMA línea**, slug COMPLETO byte a byte (`fare-ind` es prefijo ambiguo y no se trunca jamás,
   D-40-03), columnas alineadas al estilo de `CATEGORIES`. Un `slug` detrás del `file` NO produce par
   y el gate anti-ceguera se vuelve vacuo. El `expected` de cada entrada es
   `mcVariantCountOf('<su fichero>')` — DERIVADO, nunca una cifra escrita (D-31-06; el precedente
   contrario ya costó un CR-01 en la Phase 44). Y `TOTAL_TRANSLATION_EXPECTED` = la reducción de esos
   `expected`.

3. **El conteo (actual).** Espejando el bucle de `:429-472` pero bajando a la variante: para cada
   entrada del array, recorrer los slots `multiple-choice` del fichero y, por cada variante,
   clasificar `variants[k].translationES`:
   - campo AUSENTE → `missing` (una variante sin traducir en una categoría declarada cubierta es un
     fallo de cobertura, no un caso benigno);
   - presente pero sin `validation` o con `passes` no-array → `missing`, con la misma rama defensiva
     que el bucle existente;
   - presente → pasar `passes` por `effectiveStatus` (que es `deriveStatus`, importado; **prohibido
     reimplementar ni relajar la derivación en local**) y acumular en `validated` / `disputed` /
     `pending` según el resultado, guardando las direcciones compuestas de los `disputed` para
     imprimirlas;
   - si el `status` escrito discrepa del derivado, acumularlo en la lista de incoherencias al estilo
     VAL-09, con la dirección compuesta en el mensaje.
   Un fichero que no se pueda leer o parsear marca el flag de error de carga existente.

4. **DOS MAGNITUDES DISTINTAS, no una tautología.** El `expected` sale de contar variantes
   `multiple-choice` en el fichero; el `actual` sale de contar `translationES` cuyo status derivado es
   `validated`. Son dos recorridos que cuentan cosas distintas: por eso el assert muerde, y por eso
   NO se puede sustituir uno por el otro «para simplificar». Dejarlo escrito en un comentario junto
   al sub-gate, citando el caveat de `tests/exercise-types.test.js:1328-1334`.

5. **No-vacuidad, y va PRIMERO.** Antes de decidir el veredicto: si `TRANSLATION_COVERAGE` está
   vacío, o si `TOTAL_TRANSLATION_EXPECTED` es cero, o si alguna categoría declarada no se pudo
   leer, el sub-gate sale ROJO por AUSENCIA DE DATOS. Nunca verde por lista vacía.

6. **Igualdad de enteros.** El veredicto es `totalTranslationValidated === TOTAL_TRANSLATION_EXPECTED`
   y `totalTranslationActual === TOTAL_TRANSLATION_EXPECTED`, más la ausencia de error de carga y la
   comprobación de no-vacuidad. **Prohibido** calcular un porcentaje, redondearlo o comparar contra
   un umbral flotante: un `>= 0.999` dejaría pasar una variante suelta formateada a 99%.

7. **Impresión y enganche.** Imprimir la línea del sub-gate con la MISMA forma que VAL-06
   (`:583-589`), con las cifras INTERPOLADAS desde los valores computados —escribirlas en la prosa es
   plantar la siguiente CR-01, como advierte el comentario del propio analog—, listando en el FAIL los
   contadores `pending` / `missing` / `disputed`. Añadir el nuevo veredicto a la conjunción `gatePass`
   de `:628` y añadir su propia línea de acción sugerida en el bloque de `:654-674`, nombrando el
   comando concreto del plan 46-02 con la dirección compuesta.
  </action>
  <verify>
    <automated>node scripts/run-validation-271.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/run-validation-271.mjs` imprime una línea del sub-gate de cobertura de traducción con la forma `PASS (n/N)` o `FAIL (n/N — pending=…, missing=…, disputed=…)`, y en este momento del plan sale en **FAIL** con `exit=1`, porque solo la traducción canónica está traducida. **Ese rojo es el criterio de aceptación, no un fallo a arreglar.**
    - `node -e "const s=require('fs').readFileSync('scripts/run-validation-271.mjs','utf8'); const i=s.indexOf('TRANSLATION_COVERAGE'); const j=s.indexOf('];', i); const region=s.slice(i,j); const code=region.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); console.log('literales-magicos:', (code.match(/expected:\s*\d+/g)||[]).length)"` imprime `literales-magicos: 0` — ningún `expected` transcrito; todos vía `mcVariantCountOf(...)`.
    - Comment-filtered: en la región del nuevo array y su bucle de conteo, `grep -vE '^\s*(//|\*|/\*)' | grep -cE '\b(96|50)\b'` devuelve 0 (las cifras del piloto no aparecen como literales en el código del gate).
    - El gate no decide por porcentaje: `grep -nE 'toFixed|Math\.round|/ *TOTAL_TRANSLATION_EXPECTED|0\.99' scripts/run-validation-271.mjs` no devuelve ninguna línea dentro de la región del nuevo sub-gate.
    - La forma de entrada casa el ancla de GATE-02: `node -e "…"` usando `paresSlugFile` importado de `tests/count-arrays-lockstep.test.js` sobre el fuente del reporter devuelve al menos una entrada nueva cuyo `slug` es `preposiciones` y cuyo `file` es `content/exercises/preposiciones.json`.
    - `grep -c 'deriveStatus' scripts/run-validation-271.mjs` es ≥ 2 y el nuevo bucle usa `effectiveStatus`, no una comparación local de `verdict`.
    - Permutación (orden): reordenar temporalmente las entradas de `TRANSLATION_COVERAGE` y re-correr el reporter da el MISMO exit code y los MISMOS contadores. Restaurar.
    - No-vacuidad: vaciar temporalmente `TRANSLATION_COVERAGE` a `[]` y re-correr → `exit=1` con un mensaje de ausencia de datos, NUNCA `exit=0`. Restaurar.
  </acceptance_criteria>
  <done>El reporter declara `TRANSLATION_COVERAGE` con `expected` derivado del disco a granularidad de variante, cuenta el `actual` por una magnitud DISTINTA vía `deriveStatus`, sale rojo por no-vacuidad y por lista vacía, decide por igualdad de enteros, y su veredicto entra en `gatePass` con su propia acción sugerida. Committeado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GATE-02 — el array de traducción entra como fuente vigilada, con goldens fail-first</name>
  <reversibility rating="reversible">D-46-17: el anti-ceguera vive en el meta-test que ya existe; añadir una fuente vigilada es aditivo.</reversibility>
  <read_first>
    - `tests/count-arrays-lockstep.test.js` — leer `:56-74` (`COUNT_ARRAY_SOURCES`, `readSrc`, y `SLUGS_REGISTRADOS` derivado de `content/categories.json` con su doctrina literal de que una lista escrita a mano aquí sería un gate vacuo); `:163-235` (`sinComentarios`, con la razón de por qué va PRIMERO); `:249-264` (`slugsCiegos` con su ancla de whitespace HORIZONTAL `[^\S\n]*` — `\s*` cruzaría el salto de línea y fue un bug real, WR-07 — y el slug completo por la colisión de prefijo); `:285-301` (`paresSlugFile` y `paresCruzados`, que comparten el MISMO `sinComentarios`); `:313-700` (la familia de goldens `SRC_VACIO` / `SRC_SOLO_HERMANO` / `SRC_COMENTADO` / `SRC_BLOQUE_UNA` / `SRC_SLUG_A_DOS_LINEAS` / `SRC_TRAMPA` / `SRC_COMPLETO`, todos sobre CADENAS LITERALES del propio fichero, nunca sobre disco — es lo que los hace deterministas); `:706-719` (el gate contra el disco por cada fuente declarada); `:735-800` (la constante `REPORTER` y, sobre todo, `:747-759`, la CLÁUSULA DE NO-VACUIDAD y su mensaje); `:824-870` (la mitad posicional del ancla compartida y el gate DEUDA-02 de integridad del escáner).
    - `scripts/run-validation-271.mjs` — el `TRANSLATION_COVERAGE` escrito en Task 1, para confirmar la forma real de sus entradas.
    - `content/categories.json` — la lista de categorías registradas de la que sale `SLUGS_REGISTRADOS`.
  </read_first>
  <files>tests/count-arrays-lockstep.test.js</files>
  <behavior>
    - Golden `SRC_TRAD_VACIO`: un array de traducción sin ninguna entrada → el extractor lo declara ciego a la categoría declarada cubierta.
    - Golden `SRC_TRAD_SOLO_HERMANO`: entrada de otra categoría presente, la declarada ausente → ciega solo la ausente.
    - Golden `SRC_TRAD_PREFIJO`: entrada con un slug que es PREFIJO del declarado (`fare-ind` frente al completo) → NO cuenta como enganche; el slug se exige completo byte a byte.
    - Golden `SRC_TRAD_UN_BYTE`: entrada cuyo slug difiere en UN solo byte → ciega.
    - Golden `SRC_TRAD_COMENTADO_LINEA`: entrada comentada con doble barra → ciega (`sinComentarios` va primero).
    - Golden `SRC_TRAD_COMENTADO_BLOQUE`: entrada envuelta en comentario de bloque → ciega.
    - Golden `SRC_TRAD_DOS_LINEAS`: `slug` y `file` en líneas distintas → NO forma par (`paresSlugFile` devuelve vacío para esa entrada).
    - Golden `SRC_TRAD_CRUZADO`: `slug` de una categoría con el `file` de otra → aparece en `paresCruzados`.
    - Golden `SRC_TRAD_COMPLETO`: control VERDE — todas las categorías declaradas enganchadas con su propio fichero → cero ciegas, cero cruzadas.
    - Gate contra el disco: el fuente real del reporter no deja CIEGA ninguna categoría declarada cubierta de traducción, con su cláusula de no-vacuidad ANTES del assert.
    - Ordering: permutar las entradas del array en un golden no cambia el conjunto de ciegas ni de cruzadas.
  </behavior>
  <action>
1. **Fuente de verdad de «declarada cubierta».** El anti-ceguera necesita una referencia DERIVADA,
   no una lista escrita a mano en el test (esa es la doctrina literal del fichero: una lista a mano
   «sería un gate vacuo: verde para siempre»). Derivar el conjunto de categorías declaradas cubiertas
   de traducción del DISCO: una categoría está declarada cubierta cuando su fichero de contenido tiene
   al menos una variante `multiple-choice` con la clave `translationES` presente. Ese conjunto,
   computado recorriendo `content/exercises/*.json`, es la referencia contra la que se mide la ceguera
   del array del reporter. Consecuencia deliberada y correcta: en cuanto una categoría recibe su
   primera traducción, el gate EXIGE que esté enganchada.

2. **Registrar la fuente vigilada.** Añadir el array de traducción del reporter al perímetro del
   anti-ceguera. `COUNT_ARRAY_SOURCES` ya incluye `scripts/run-validation-271.mjs`, así que el
   escáner ya lee ese fuente; lo que falta es una comprobación NUEVA que mida la ceguera del array de
   TRADUCCIÓN específicamente contra el conjunto derivado del punto 1, además de la comprobación
   existente que mide `CATEGORIES` contra `SLUGS_REGISTRADOS`. Reutilizar `slugsCiegos`,
   `paresSlugFile`, `paresCruzados` y su `sinComentarios` compartido — no escribir un segundo escáner
   (la razón por la que comparten el ancla está en el comentario de `:824-830`).

3. **La familia de goldens fail-first.** Escribir el bloque `describe` con los goldens del bloque
   `<behavior>`, sobre CADENAS LITERALES declaradas en el propio fichero de test, nunca leídas del
   disco — es lo que los hace deterministas y es el patrón exacto de `:313-319`. Cada golden imita la
   forma real de las entradas del array de traducción.

4. **Cláusula de no-vacuidad, PRIMERO.** El gate contra el disco lleva, ANTES del `deepEqual` de
   ciegas, la comprobación de que el extractor VE algo: el número de pares que `paresSlugFile` extrae
   de la región del array de traducción del reporter debe igualar el número de categorías declaradas
   cubiertas derivado del punto 1. Un extractor por regex que deja de casar devuelve `[]`, y
   `deepEqual([], [])` pasa en VERDE — es el modo de fallo real de este repo (`:747-759`). El mensaje
   de fallo debe nombrar las dos posibilidades: o el reporter dejó de declarar una entrada, o el
   extractor dejó de ver su array.

5. **Integridad del escáner.** Extender el gate DEUDA-02 (`:831-870`) para que las líneas de entrada
   del array de traducción también se comprueben supervivientes a `sinComentarios` sin alteración.
  </action>
  <verify>
    <automated>node --test tests/count-arrays-lockstep.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/*.test.js tests/fixtures/*.test.js` termina con exit 0.
    - Los 11 casos del bloque `<behavior>` existen como tests nombrados y pasan.
    - El conjunto de referencia se deriva del disco: `grep -n "readdirSync\|content/exercises" tests/count-arrays-lockstep.test.js` muestra el recorrido que computa las categorías declaradas cubiertas, y NO existe ninguna lista literal de slugs de traducción escrita a mano en el fichero.
    - La cláusula de no-vacuidad está ANTES del assert de ciegas: en el fuente del test, el offset de la aserción de igualdad de recuentos es MENOR que el del `deepEqual` de ciegas dentro del mismo bloque `test(...)`.
    - Los goldens operan sobre literales: cada `SRC_TRAD_*` es una plantilla declarada en el fichero (`grep -c "const SRC_TRAD_" tests/count-arrays-lockstep.test.js` es ≥ 9) y ninguno llama a `readSrc` ni a `readFileSync`.
    - Se reutilizan los helpers existentes, no se duplica el escáner: `grep -c "function slugsCiegos\|function paresSlugFile\|function sinComentarios" tests/count-arrays-lockstep.test.js` sigue devolviendo 3 (una definición de cada, no cuatro ni seis).
  </acceptance_criteria>
  <done>El array de cobertura de traducción del reporter es una fuente vigilada por el anti-ceguera, medida contra un conjunto de categorías-declaradas-cubiertas DERIVADO del disco, con nueve goldens fail-first sobre literales, su cláusula de no-vacuidad primero y el gate de integridad del escáner extendido. Suite verde. Committeado.</done>
</task>

<task type="auto">
  <name>Task 3: MUTACIÓN 3 de D-46-18 — declarar Preposiciones cubierta sin engancharla, y OBSERVAR el rojo</name>
  <read_first>
    - `tests/count-arrays-lockstep.test.js` — el gate contra el disco y su cláusula de no-vacuidad, escritos en Task 2: es el gate que esta mutación tiene que poner rojo.
    - `scripts/run-validation-271.mjs` — la región de `TRANSLATION_COVERAGE` escrita en Task 1: es lo que se muta.
    - `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-CONTEXT.md` — D-46-18: las tres mutaciones se EJECUTAN y se observa el rojo; leer el código del gate no cuenta como verificación.
  </read_first>
  <files>scripts/run-validation-271.mjs, tests/count-arrays-lockstep.test.js</files>
  <action>
Ejecutar la tercera mutación de D-46-18 y REGISTRAR el resultado observado. Esto no es una lectura de
código: es una corrida real con un exit code apuntado.

1. **Fotografía previa.** `git status --porcelain` limpio en los dos ficheros. Correr
   `node --test tests/count-arrays-lockstep.test.js` y apuntar el exit code de partida (0).

2. **La mutación.** Preposiciones YA está declarada cubierta por el disco (tiene la traducción
   canónica del plan 46-01, así que el conjunto derivado del Task 2 la incluye). Retirar su entrada
   de `TRANSLATION_COVERAGE` en `scripts/run-validation-271.mjs` — dejando el array declarado pero
   sin esa entrada. Ese es exactamente el escenario que costó un `225/225 PASS` ciego en v2.0.

3. **La observación.** Correr `node --test tests/count-arrays-lockstep.test.js` y apuntar el exit
   code y el nombre del test que falla. DEBE ser distinto de 0 y el mensaje debe nombrar
   `preposiciones` como categoría que quedaría CIEGA.

4. **Segunda mutación de control, la del extractor.** Devolver la entrada pero partirla en dos líneas
   (`slug` en una, `file` en la siguiente), que es la forma que rompe el ancla. Correr de nuevo:
   DEBE salir rojo por la CLÁUSULA DE NO-VACUIDAD (el extractor ve menos pares que categorías
   declaradas cubiertas), no en verde. Esto prueba que el gate no es vacuo por un extractor que dejó
   de casar — el fallo exacto que la Phase 44 pagó como CR-01.

5. **Restauración y re-verificación.** `git checkout -- scripts/run-validation-271.mjs`, correr la
   suite de nuevo y confirmar exit 0. Volver a correr el reporter y confirmar que sigue en el ROJO
   ESPERADO de GATE-01 (cobertura incompleta), no en un rojo nuevo.

6. **Registro.** Anotar en el SUMMARY los tres exit codes observados (verde de partida, rojo de la
   mutación de desenganche, rojo de la mutación del extractor) y el nombre del test que cazó cada uno.
   Sin ese registro la mutación no cuenta como ejecutada.

**Y la regla de la casa que aplica a partir de aquí (D-46-18):** si un fix propuesto por el code
review de esta fase toca cualquiera de estos dos gates, ese fix se verifica con la MISMA mutación que
el código que arregla — 2 de 4 fixes de revisor en la Phase 44 eran incorrectos y uno era peor que el
bug.
  </action>
  <verify>
    <automated>node --test tests/count-arrays-lockstep.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Mutación de desenganche EJECUTADA: exit code observado ≠ 0 en `node --test tests/count-arrays-lockstep.test.js`, con el nombre del test que falla y el mensaje que nombra `preposiciones` registrados literalmente en el SUMMARY.
    - Mutación del extractor (slug y file en líneas distintas) EJECUTADA: exit code observado ≠ 0, disparado por la cláusula de NO-VACUIDAD (no por el `deepEqual` de ciegas), registrado en el SUMMARY.
    - Restauración verificada: `git status --porcelain scripts/run-validation-271.mjs tests/count-arrays-lockstep.test.js` vacío y `node --test tests/*.test.js tests/fixtures/*.test.js` con exit 0.
    - El reporter sigue en el ROJO ESPERADO por cobertura incompleta (`exit=1` con el sub-gate de traducción en FAIL): ninguna de las dos mutaciones dejó residuo que lo ablande.
    - El SUMMARY registra los tres exit codes con su fecha, no una afirmación de que «el gate muerde».
  </acceptance_criteria>
  <done>Las dos mutaciones anti-ceguera se ejecutaron, se observó el rojo en ambas con el test y el mensaje concretos apuntados, el estado se restauró y la suite volvió a verde. La verificación por mutación de D-46-18 punto 3 está cerrada con evidencia de corrida, no de lectura.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `content/exercises/*.json` → reporter / meta-test | Contenido de disco entra en los gates; un JSON corrupto o un campo malformado no debe poder emitir PASS. |
| fuente del reporter → escáner por regex del meta-test | El TEXTO de un fichero de código es la entrada de un extractor; una forma de entrada alterada puede volver vacuo el gate. |

## STRIDE Threat Register (ASVS L1 · block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-13 | Repudiation (un gate que certifica en verde midiendo nada) | `TRANSLATION_COVERAGE` + el gate anti-ceguera | **high** | mitigate | Cláusula de no-vacuidad ANTES de cada assert, con la cifra derivada del disco; array vacío o fichero ilegible → ROJO por ausencia de datos; y las dos mutaciones del Task 3 EJECUTADAS con exit code registrado. Es el modo de fallo que este repo ya pagó (Phase 45: cinco gates vacuos; Phase 44 CR-01: suite firmando 247 con el reporter en 250). |
| T-46-14 | Tampering (umbral ablandado silenciosamente) | el veredicto del sub-gate | **high** | mitigate | Veredicto por IGUALDAD DE ENTEROS; prohibido el porcentaje, el redondeo y el umbral flotante, verificado por aserción de fuente sobre la región del sub-gate. El objetivo declara explícitamente que el ROJO intermedio no se arregla ablandando el gate. |
| T-46-15 | Information Disclosure | mensajes del reporter | low | accept | El reporter imprime ids de slot y direcciones compuestas de variante en la consola del autor, en su propia máquina. No hay dato sensible en el corpus (frases de ejercicio de italiano) ni destino remoto. |
| T-46-16 | Spoofing / Denial of Service / Elevation of Privilege | n/a | low | accept | Scripts locales sin red, sin servidor, sin autenticación, sin multi-usuario. Este plan no abre ninguna superficie nueva de esas categorías. |

## Gate de legitimidad de paquetes

**No aplica:** cero instalaciones de paquetes en este plan. `T-{phase}-SC` declarado no aplicable por
ausencia de instalaciones, no por omisión.
</threat_model>

<verification>
1. `node --test tests/*.test.js tests/fixtures/*.test.js` → exit 0.
2. `node scripts/run-validation-271.mjs` → exit 1 con el sub-gate de traducción en FAIL. **Rojo
   ESPERADO** en este punto de la fase (cobertura incompleta); lo cierra el plan 46-04.
3. Los tres exit codes de las mutaciones del Task 3 registrados en el SUMMARY.
4. `git diff --stat src/domain/ src/screens/app.js` → vacío.
</verification>

<success_criteria>
- `TRANSLATION_COVERAGE` existe con `expected` derivado del disco a granularidad de variante y su
  `actual` computado por una magnitud DISTINTA vía `deriveStatus`.
- El veredicto es igualdad de enteros, sale rojo por no-vacuidad y entra en `gatePass`.
- El array es fuente vigilada del anti-ceguera contra un conjunto derivado del disco, con nueve
  goldens fail-first y su cláusula de no-vacuidad primero.
- La mutación 3 de D-46-18 se EJECUTÓ y el rojo se observó y se registró, dos veces (desenganche y
  extractor roto).
</success_criteria>

<output>
Create `.planning/phases/46-pipeline-de-traducci-n-end-to-end-piloto-preposiciones/46-03-SUMMARY.md` when done
</output>
