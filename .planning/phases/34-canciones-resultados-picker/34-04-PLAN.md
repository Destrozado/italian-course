---
phase: 34-canciones-resultados-picker
plan: 04
type: execute
wave: 4
depends_on: ["34-03"]
files_modified:
  - index.html
  - app.css
  - tests/screen-canciones.test.js
autonomous: true
requirements: [SRP-02]

must_haves:
  truths:
    - "Song playback shows the Editoriale top bar: circular back ‹ + green progress bar + Space Grotesk Frase X/N counter, NO timer chip"
    - "The song phrase renders with the serif prompt treatment and word-buttons inherit the Phase 33 Editoriale styling"
    - "Auto-advance 600ms (sessionMode cancion) and songCheck/songAdvance stay untouched"
  artifacts:
    - path: "index.html"
      provides: "repainted cancion template top bar + prompt"
      contains: "session-topbar"
  key_links:
    - from: "cancion top bar back"
      to: "returnToSongList"
      via: ".session-back @click reused"
      pattern: "returnToSongList"
    - from: "cancion counter"
      to: "songProgressLabel"
      via: ".session-counter x-text"
      pattern: "songProgressLabel"
---

<objective>
Repaint the song playback screen (`cancion`, SRP-02) to the Editoriale language by inheriting Phase 33's top bar + word-buttons treatment: replace the bare `<header>` with the `.session-topbar` scaffold (circular `‹` back + green progress bar + "Frase X/N" Space Grotesk counter, NO timer chip), apply `.session-context` overline + `.session-prompt` to the lyric, and rely on the already-styled `wb-*` classes — all with the engine (songCheck/songAdvance/auto-advance) untouched.

Purpose: Deliver SRP-02 by extrapolating the Phase 33 EX-01 top bar + D-33-12 word-buttons treatment to the song screen (D-08), with no Contrarreloj chip (songs never use it).
Output: Repainted `cancion` `<template>` interior + a song-progress fill width in app.css (mirroring sessionProgressPercent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-canciones-resultados-picker/34-CONTEXT.md
@.planning/phases/34-canciones-resultados-picker/34-UI-SPEC.md
@.planning/phases/34-canciones-resultados-picker/34-PATTERNS.md
@.planning/phases/34-canciones-resultados-picker/34-03-SUMMARY.md

<interfaces>
<!-- Existing cancion markup — index.html L868-935. ALREADY uses word-buttons: -->
  outer guard: x-if="currentScreen === 'cancion' && songCurrentPhrase"
  L871 <h2 class="session-context" x-text="sessionContextLabel"> (already Editoriale)
  L873 <header x-text="songProgressLabel">  ← replace with .session-topbar scaffold
  L876 <p x-text="songCurrentPhrase.payload.prompt">  ← apply .session-prompt
  word-buttons: .wb-bank / bankWithKeys / wordButtonsAddWord / .wb-answer / wordButtonsRemoveWord
  check/advance: songCheck (@click, :disabled="!wordButtonsCanCheck"), songAdvance (@click)
  L933 <button @click="returnToSongList">← Volver a Canciones</button>  ← move into .session-back

<!-- Reuse VERBATIM (engine + getters, do NOT modify): -->
  songCheck, songAdvance, returnToSongList, handleSongKey, wordButtonsAddWord/RemoveWord,
  songProgressLabel ("Frase X/N"), songCurrentPhrase, sessionContextLabel, bankWithKeys,
  auto-advance 600ms (sessionMode === 'cancion') — UNTOUCHED.

<!-- Reusable CSS (app.css, Phase 33). -->
  .session-topbar (L704) / .session-back (L711, 36px circular ‹, aria-label="Volver") /
  .session-progress + .session-progress-fill (L735-742, 4px green bar) /
  .session-counter (L750, Space Grotesk) / .session-progress-sr (L761)
  .session-context (L811, overline) / .session-prompt (serif 30/500)
  .wb-bank / .wb-answer / .wb-placed / post-check tint (L1054-1168) — ALREADY applied to cancion
  .session-feedback.wrong / -title / -correct (L878-916) — optional post-check feedback
  DO NOT add .session-timer* (L778-808) — songs never use Contrarreloj (D-08).
  get sessionProgressPercent() (L2616) — mirror for the song-progress fill width.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the cancion header with the Editoriale top bar</name>
  <files>index.html, app.css, tests/screen-canciones.test.js</files>
  <read_first>
    - index.html L868-935 (cancion template; L871 context, L873 header, L876 prompt, L933 back button)
    - 34-PATTERNS.md §`cancion` template — "Top bar REPLACE the bare <header> with .session-topbar scaffold", move ← Volver into circular .session-back (‹ glyph, aria-label="Volver"), songProgressLabel feeds .session-counter, progress fill mirrors sessionProgressPercent, NO timer chip
    - 34-UI-SPEC.md §Established assets (.session-topbar/.session-back/.session-progress/.session-counter row, "No timer chip — D-08") + §Interaction Contract (cancion row)
    - 34-CONTEXT.md D-08; app.css L704-761 (.session-topbar family), L2616 (sessionProgressPercent)
  </read_first>
  <acceptance_criteria>
    - The bare `<header x-text="songProgressLabel">` (L873) is replaced by a `.session-topbar` scaffold containing: a `.session-back` circular `‹` button (`aria-label="Volver"`) whose `@click` reuses `returnToSongList` (the old "← Volver a Canciones" action, L933); a `.session-progress`/`.session-progress-fill` 4px green bar whose fill width derives from a song-progress percent (mirror `sessionProgressPercent`, NOT a new engine field); and a `.session-counter` (Space Grotesk) bound to `songProgressLabel` ("Frase X/N").
    - NO `.session-timer*` element is added (D-08 — songs never use Contrarreloj).
    - The duplicate bottom "← Volver a Canciones" button is either removed (now in the top bar) or left only if intentional; the back affordance reuses `returnToSongList` verbatim either way.
    - The `.session-context` overline (L871) is kept; `songCurrentPhrase.payload.prompt` (L876) gets the `.session-prompt` serif treatment.
    - All dynamic text via `x-text`; back/counter labels follow the hardcoded-glyph convention; no `x-html`.
    - The song-progress fill width rule lives in the cancion app.css block (`--ed-*` only).
  </acceptance_criteria>
  <action>
    In the `cancion` template (index.html L868-935): replace the bare `<header x-text="songProgressLabel">` (L873) with the `.session-topbar` scaffold (app.css L704-761): a `.session-back` circular `‹` button with `aria-label="Volver"` and `@click="returnToSongList"` (reuse the existing action from L933), a `.session-progress` track with a `.session-progress-fill` whose width binds to a song-progress percentage mirroring the `sessionProgressPercent` derive idiom (presentational, no engine field added — derive from `songCurrentPhrase` index / total via an inline expression or a thin getter), and a `.session-counter` with `x-text="songProgressLabel"`. Do NOT add any `.session-timer` element (D-08). Apply `.session-prompt` to the lyric `<p x-text="songCurrentPhrase.payload.prompt">` (L876). Keep the `.session-context` overline (L871). Remove the now-redundant bottom "← Volver a Canciones" button (L933) since the back affordance moved to `.session-back` (or keep one canonical back control — do not duplicate the action confusingly). Leave the entire word-buttons block (`.wb-bank`/`.wb-answer`/songCheck/songAdvance) and the `@keydown.window="handleSongKey"` untouched — the wb-* CSS already styles it. Add only the song-progress fill width rule to a new bottom-of-`app.css` `═══` block citing SRP-02/D-08. T-02-01: x-text only. Extend `tests/screen-canciones.test.js`: cancion template contains `session-topbar`/`session-back`/`session-counter`, `.session-back` reuses `returnToSongList`, `songProgressLabel` feeds the counter, the prompt uses `session-prompt`, NO `session-timer` token appears in the cancion block, and songCheck/songAdvance bindings are preserved.
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js</automated>
  </verify>
  <done>Cancion screen shows the Editoriale top bar (circular ‹ back via returnToSongList + green progress + Frase X/N counter) with NO timer chip; prompt is .session-prompt; word-buttons + songCheck/songAdvance + auto-advance untouched; asserts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Song JSON → DOM | Lyric prompt + progress label bound into the cancion screen. Single-user local tool; no network/backend/auth/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | cancion prompt + songProgressLabel + word-button text bindings | mitigate | All dynamic content via `x-text` exclusively; NEVER `x-html`. Back glyph `‹` and any labels hardcoded; word-button text is data via x-text only (inherited Phase 33 invariant). |

No other threats apply (presentation-only, localStorage, single-user local).
</threat_model>

<verification>
- `node --test tests/screen-canciones.test.js` passes.
- `node --test tests/*.test.js` stays green except the single preexisting unrelated failure.
- Grep guards: `grep -c 'x-html=' index.html` returns 0; the cancion block contains no `session-timer`.
- Visual: top bar + serif prompt + word-buttons render; auto-advance still fires (human-verifiable via `npx serve`).
</verification>

<success_criteria>
- Editoriale top bar (circular back + green progress + Frase X/N counter), NO timer chip (D-08).
- Lyric uses .session-prompt; word-buttons inherit Phase 33 treatment.
- returnToSongList/songCheck/songAdvance/handleSongKey/auto-advance preserved; no x-html.
</success_criteria>

<output>
Create `.planning/phases/34-canciones-resultados-picker/34-04-SUMMARY.md` when done.
</output>
