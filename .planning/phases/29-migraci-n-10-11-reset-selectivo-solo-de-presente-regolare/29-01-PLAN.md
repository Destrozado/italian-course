---
phase: 29-migraci-n-10-11-reset-selectivo-solo-de-presente-regolare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/storage.js
  - src/data/backup.js
  - tests/data-storage.test.js
  - tests/backup.test.js
autonomous: true
requirements: [MIG-05, MIG-06]

must_haves:
  truths:
    - "Tras migrar un state v10, el progreso bajo el prefijo `presente-regolare` queda reseteado (categoryProgress borrado, exerciseStats podado, inFlightTest invalidado si lo referencia)."
    - "Las 9 categorías de gramática existentes + songProgress quedan byte-intactas tras migrar (verificado por fixture deep-equal)."
    - "migrate10to11 es idempotente (re-ejecutar sobre un v11 ya migrado = misma shape) y puro (no muta el input)."
    - "backup.js exporta v11 reimportable round-trip; un backup v10 importado migra a v11 aplicando el reset; backups con schemaVersion > 11 se rechazan."
    - "La suite completa (`node --test tests/*.test.js`) sigue verde tras el bump (asserts de schemaVersion 10 actualizados a 11)."
  artifacts:
    - path: "src/data/storage.js"
      provides: "migrate10to11 + hydrateV11 + RESET_PREFIXES_V11 + CURRENT_SCHEMA_VERSION=11 + eslabón en el dispatcher"
      contains: "export function migrate10to11"
    - path: "src/data/backup.js"
      provides: "CURRENT_SCHEMA_VERSION=11 + import migrate10to11/hydrateV11 + eslabón en la cadena + reject >11 (genérico)"
      contains: "migrate10to11"
    - path: "tests/data-storage.test.js"
      provides: "bloque de tests de la cadena v11 (reset, byte-intacto, idempotencia, pureza, anti-prototype-pollution, cadena) + asserts schemaVersion 11"
      contains: "migrate10to11"
    - path: "tests/backup.test.js"
      provides: "tests round-trip v11 + import v10→v11 + reject >11 + asserts schemaVersion 11"
      contains: "schemaVersion === 11"
  key_links:
    - from: "src/data/storage.js migrate() dispatcher"
      to: "migrate10to11 + hydrateV11"
      via: "if (s.schemaVersion === 10) s = migrate10to11(s); if (s.schemaVersion === 11) return hydrateV11(s);"
      pattern: "migrate10to11"
    - from: "src/data/backup.js parseBackupFile cadena"
      to: "migrate10to11 + hydrateV11"
      via: "eslabón v10→v11 + hydrateV11 final"
      pattern: "migrate10to11"
---

<objective>
Añadir el eslabón `10→11` a la cadena de migración del state, con reset selectivo por prefijo SOLO de la categoría nueva `presente-regolare`, espejando el patrón probado `migrate8to9`/`hydrateV9` (Phase 21) con UNA desviación: un solo prefijo. Cubre MIG-05 (migración + hydrate + bump idempotentes con reset selectivo) y MIG-06 (backup round-trip v11 + import v10→v11 + rechazo `>11`).

Purpose: dejar el state listo para que la 10ª categoría (`presente-regolare`, Phase 30) nazca limpia y para que un backup futuro que ya contenga progreso de esa categoría se reseteé limpiamente al re-importar (forward-compat). El reset es HOY un no-op (ningún state actual tiene ese prefijo) — se hace por simetría con v1.5/v1.6 y defensa forward-compat (D-29-01).

Output: `migrate10to11` + `hydrateV11` + `RESET_PREFIXES_V11` + `CURRENT_SCHEMA_VERSION=11` en `storage.js`; bump espejo + cadena + import en `backup.js`; bloque de tests de la cadena v11 en ambos test files + actualización de los asserts existentes que esperaban `schemaVersion 10`.

Brownfield puro: el motor v1.4 (cascada D-54, sampler, slot-engine, render) NO se toca. Esta fase NO autora contenido (Phase 30) ni toca counts/smoke (Phase 31).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-migraci-n-10-11-reset-selectivo-solo-de-presente-regolare/29-CONTEXT.md
@.planning/phases/29-migraci-n-10-11-reset-selectivo-solo-de-presente-regolare/29-PATTERNS.md

# Plan análogo directo (plantilla de tareas/tests — adaptar 6 prefijos → 1)
@.planning/milestones/v1.6-phases/21-migraci-n-8-9-reset-selectivo-de-las-6-categor-as/21-01-PLAN.md

<interfaces>
<!-- Contratos que el executor consume. Extraídos del codebase 2026-06-16. NO explorar. -->

GATE DE VERSIÓN (VERIFICADO en código 2026-06-16):
  src/data/storage.js:35 → const CURRENT_SCHEMA_VERSION = 10;
  src/data/backup.js:49  → const CURRENT_SCHEMA_VERSION = 10;
  Target real de la migración: 10 → 11 (NO 9→10 como dicen los IDs MIG-05/06 en docs).
  Último eslabón existente: migrate9to10 (NOMINAL puro) / hydrateV10.

Shape root del state (idéntico v8..v11):
  { schemaVersion, exerciseStats, categoryProgress, dailyLog, songProgress, lastBackupAt, firstUsedAt, inFlightTest }

Analog funcional a copiar — migrate8to9 (storage.js:851-895), 3 pasos:
  (1) deep-clone categoryProgress + delete de la(s) clave(s) de la categoría reseteada
  (2) deep-clone exerciseStats + filtro por prefijo con RESET_PREFIXES.some(p => k.startsWith(p))
  (3) invalidar inFlightTest a undefined si inFlightTest.exerciseIds referencia un id con prefijo reseteado
  Cada sub-dict: (typeof X === 'object' && X !== null) ? JSON.parse(JSON.stringify(X)) : {}

Analog de hydrate a copiar — hydrateV10 (storage.js:994-1014), NO hydrateV9:
  hydrateV10 YA trae el guard root `const p = (parsed && typeof parsed === 'object') ? parsed : {};`
  hydrateV11 = copia literal cambiando 10→11; NO repite la poda de migrate10to11.

Analog de constante — RESET_PREFIXES_V9 (storage.js:811-823): const + docblock con el gate de colisión.

Dispatcher actual (storage.js:159-161):
  if (s.schemaVersion === 8) s = migrate8to9(s);
  if (s.schemaVersion === 9) s = migrate9to10(s);
  if (s.schemaVersion === 10) return hydrateV10(s);   ← ESTA línea cambia

backup.js cadena actual (backup.js:135-137):
  if (migrated.schemaVersion === 8) migrated = migrate8to9(migrated);
  if (migrated.schemaVersion === 9) migrated = migrate9to10(migrated);
  migrated = hydrateV10(migrated);                    ← ESTA línea cambia
backup.js import actual (backup.js:26): lista nombrada terminada en `migrate9to10, hydrateV10`
backup.js reject >N (backup.js:119-124): GENÉRICO contra CURRENT_SCHEMA_VERSION — NO requiere edición de lógica.

GATE DE COLISIÓN DE PREFIJO (VERIFICADO 29-CONTEXT.md:73): ninguno de los 9 slugs existentes
  (avere, essere, preposiciones, verbos-movimiento, sustantivos-irregulares, genero-numero,
   profesiones, articoli, partitivos) empieza por `presente-regolare` ni es prefijo suyo.
  El filtro startsWith no tiene colisiones y preserva las 9 byte a byte.

TRAMPA DE TESTS — asserts existentes que el bump rompe (localizados por grep 2026-06-16):
  tests/data-storage.test.js: líneas 39, 52-53, 639 (blankState/loadState → schemaVersion 10 pasa a 11).
    NOTA: el bloque `describe('… v10 — migrate9to10 nominal …')` (1387-1497) testea migrate9to10
    como NOMINAL: sus asserts internos `schemaVersion === 10` deben SEGUIR siendo 10 donde testean
    el OUTPUT de migrate9to10 aislado (migrate9to10 sigue devolviendo 10). Solo cambian a 11 los
    asserts que recorren la cadena COMPLETA hasta hydrate (p.ej. 1486 `hydrateV10(migrate9to10(...))`
    → pasa a `hydrateV11(migrate10to11(migrate9to10(...)))` y assert 11). Leer cada assert en contexto.
  tests/backup.test.js: líneas 90-93, 121, 155, 175, 198, 246, 294, 345, 386, 446, 488, 509, 524, 527
    (la cadena de backup ahora sale v11, así que TODOS estos `r.state.schemaVersion === 10` pasan a 11;
     buildBackupWrapper espeja state.schemaVersion=11). Verificar cada uno con grep antes de tocar.
  Comando de localización: `grep -n "schemaVersion, 10\|schemaVersion: 10\|schemaVersion === 10" tests/data-storage.test.js tests/backup.test.js`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migrate10to11 + hydrateV11 + RESET_PREFIXES_V11 + dispatcher en storage.js</name>
  <files>src/data/storage.js, tests/data-storage.test.js</files>
  <read_first>
    - src/data/storage.js §migrate8to9 (851-895), §hydrateV10 (994-1014), §RESET_PREFIXES_V9 (811-823), §migrate() dispatcher (149-167), §CURRENT_SCHEMA_VERSION (35), §blankState (68-79)
    - .planning/phases/29-.../29-PATTERNS.md §1 (migrate10to11), §2 (hydrateV11), §3 (RESET_PREFIXES_V11), §4 (dispatcher) — incluyen el código exacto a producir
    - .planning/phases/29-.../29-CONTEXT.md §code_context (gate de colisión, 73) + §decisions (D-29-02..05)
    - tests/data-storage.test.js bloque describe('… v9 — migrate8to9 …') (1110-1375) — plantilla exacta de tests a clonar; bloque describe('… v10 — migrate9to10 nominal …') (1387-1497) para blankState/cadena; import (28)
    - .planning/milestones/v1.6-phases/21-.../21-01-PLAN.md (Task 1 análogo)
  </read_first>
  <behavior>
    - migrate10to11(v10) borra categoryProgress['presente-regolare'] (bracket: lleva guion) tras deep-clone, deja las 9 categorías reales intactas.
    - Poda exerciseStats por prefijo vía `RESET_PREFIXES_V11.some(p => k.startsWith(p))`; preserva las 9 reales byte a byte.
    - Invalida inFlightTest a undefined SOLO si inFlightTest.exerciseIds referencia un id con prefijo `presente-regolare`; preserva inFlightTest ajeno; sin inFlightTest no crashea.
    - Idempotente: migrate10to11(migrate10to11(x)) deep-equals migrate10to11(x).
    - Puro: no muta el input (el v10 original conserva su presente-regolare ficticio).
    - Anti-prototype-pollution: un input con `"__proto__"` own-property (schemaVersion: 10) no contamina el global; sub-dict corrupto (no-objeto) cae a {}.
    - hydrateV11 espejo de hydrateV10 con versión 11: garantiza shape, NO repite la poda (preserva presente-regolare si llega ya v11-shaped); normaliza sub-dicts ausentes a {}.
    - blankState() devuelve schemaVersion 11 (vía CURRENT_SCHEMA_VERSION); cadena end-to-end (p.ej. v8 → v11) sale v11 con presente-regolare reseteada.
    - No-regresión byte-idéntica: fixture con las 9 reales + presente-regolare ficticio → snapshot pre-migración → deep-equal de las 9 post-migración + presente-regolare ausente.
  </behavior>
  <action>
    Espejar `migrate8to9` (storage.js:851-895) como `export function migrate10to11(v10)` con la ÚNICA desviación de un solo prefijo, INSERTADO en el archivo justo DESPUÉS de `migrate9to10` (tras storage.js:978) y ANTES de `hydrateV10`. OJO: `migrate9to10` (el eslabón inmediatamente anterior) es un bump NOMINAL puro — NO es el analog funcional; solo se imita su posición. El analog funcional es `migrate8to9`. Los 3 pasos: (1) deep-clone de categoryProgress + `delete categoryProgress['presente-regolare']`; (2) deep-clone de exerciseStats + filtro `if (!RESET_PREFIXES_V11.some(p => k.startsWith(p)))`; (3) invalidar inFlightTest condicionalmente sin mutar el input (estilo migrate3to4). Mantener la forma `.some(...)` aunque el array tenga 1 elemento (preserva la simetría del patrón de constante, D-29-04). Devolver el root literal `{ schemaVersion: 11, exerciseStats, categoryProgress, dailyLog (deep-clone), songProgress (deep-clone), lastBackupAt, firstUsedAt, inFlightTest }`.

    Definir `const RESET_PREFIXES_V11 = ['presente-regolare'];` justo antes de `migrate10to11`, espejando `RESET_PREFIXES_V9` (811-823) con el docblock del gate de colisión — copiar la lista de 9 slugs de 29-CONTEXT.md:73 y adaptar el wording ("UNA categoría nueva `presente-regolare` que nace en Phase 30").

    Espejar `hydrateV10` (994-1014) como `export function hydrateV11(parsed)` cambiando 10→11 (NO copiar hydrateV9 — hydrateV10 ya trae el guard root `const p = ...`). NO repetir la poda. Docblock: espejo del de hydrateV9 (898-915) adaptado a v11, recalcando "NO repite la poda de migrate10to11; solo garantiza shape".

    Bump `CURRENT_SCHEMA_VERSION = 11` en storage.js:35 (NO tocar el cuerpo de blankState — usa la constante; sube a 11 automáticamente, igual que migrate8to9 dejó blankState sin reset).

    En el dispatcher migrate() (storage.js:159-161): mantener `if (s.schemaVersion === 9) s = migrate9to10(s);`, cambiar la línea de hydrate a: `if (s.schemaVersion === 10) s = migrate10to11(s);` seguida de `if (s.schemaVersion === 11) return hydrateV11(s);`. El bloque "Versión desconocida → warn + blankState" (163-166) NO cambia.

    Docblocks: usar el de migrate8to9 (826-850) como plantilla, adaptando "SEIS prefijos"→"UN prefijo (`presente-regolare`)", "Phases 22-27"→"Phase 30", "D-21/MIG-03"→"D-29/MIG-05". Citar T-21-01 (deep-clone) y T-21-02 (idempotencia/pureza) como precedente.

    TESTS (clonar el bloque v9 → bloque v11, adaptando 6 categorías → 1): añadir `migrate10to11, hydrateV11` al import (línea 28). Crear un helper fixture `v10WithPresenteRegolare()` (espejo de `v8WithSixCategories` 1126-1156) con `schemaVersion: 10`, progreso ficticio bajo `presente-regolare` (la reseteada) + las 9 reales (las preservadas), shape por entrada `{ status, streakDays, clearedExerciseIds: ['<cat>-001'], lastSuccessDate }` en categoryProgress y `{ timesShown, timesCorrect, timesFailed }` en exerciseStats keyed `<cat>-001`. Clonar uno a uno los tests listados en 29-PATTERNS.md §6 (borra categoryProgress de presente-regolare / poda exerciseStats / invalida inFlightTest / preserva ajeno / sin inFlightTest no crashea / idempotente / puro / anti-prototype-pollution con schemaVersion:10 en el JSON malicioso / sub-dict corrupto / hydrateV11 SIN poda / hydrateV11 normaliza ausentes / cadena v10→v11 / no-regresión byte-idéntica). Añadir los del bump: `blankState() devuelve schemaVersion 11` y la cadena end-to-end v8→v11 (espejo de 1470-1492, ahora vía migrate10to11). ACTUALIZAR los asserts existentes que el bump rompe SOLO donde corresponda: blankState/loadState (líneas 39, 52-53, 639) pasan a 11; en el bloque `migrate9to10 nominal` mantener `=== 10` donde se testea el OUTPUT aislado de migrate9to10 (sigue devolviendo 10) y cambiar a 11 SOLO los asserts que recorren la cadena hasta hydrate. Leer cada assert en su contexto antes de tocar; NO hacer reemplazo ciego de "10"→"11".

    NO hay bloques de código completos aquí — el código exacto a producir está en 29-PATTERNS.md §§1-4.
  </action>
  <verify>
    <automated>node --test tests/data-storage.test.js 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function migrate10to11" src/data/storage.js` == 1
    - `grep -c "export function hydrateV11" src/data/storage.js` == 1
    - `grep -c "RESET_PREFIXES_V11" src/data/storage.js` >= 2 (definición + uso)
    - `grep -v '^//' src/data/storage.js | grep -c "CURRENT_SCHEMA_VERSION = 11"` == 1
    - `grep -c "if (s.schemaVersion === 10) s = migrate10to11(s)" src/data/storage.js` == 1
    - `grep -c "if (s.schemaVersion === 11) return hydrateV11(s)" src/data/storage.js` == 1
    - `node --test tests/data-storage.test.js` exit 0 (suite de storage verde, incluido el bloque v11 nuevo y los asserts actualizados)
  </acceptance_criteria>
  <done>migrate10to11 + hydrateV11 + RESET_PREFIXES_V11 existen y espejean migrate8to9/hydrateV10 con un solo prefijo; el dispatcher encadena 10→11→hydrateV11; CURRENT_SCHEMA_VERSION=11; el bloque de tests v11 (reset + byte-intacto + idempotencia + pureza + anti-prototype-pollution + cadena) pasa y los asserts existentes están corregidos. `node --test tests/data-storage.test.js` verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bump espejo + cadena + import en backup.js + tests round-trip v11</name>
  <files>src/data/backup.js, tests/backup.test.js</files>
  <read_first>
    - src/data/backup.js completo (import 26, docblock CURRENT_SCHEMA_VERSION 28-49, parseBackupFile cadena 126-137, reject >N 118-124, buildBackupWrapper 161-168)
    - src/data/storage.js §migrate10to11 + §hydrateV11 (recién creadas en Task 1)
    - .planning/phases/29-.../29-PATTERNS.md §5 (backup.js: import, bump, cadena, reject) + §7 (tests round-trip)
    - tests/backup.test.js bloque describe('… backup v9 — round-trip + import v8→v9 (Phase 21)') (417-533) + el reject-future test (578-586) + helper stateV9 (420-438) — plantilla exacta; import (26-27)
    - .planning/milestones/v1.6-phases/21-.../21-01-PLAN.md (Task de backup análogo)
  </read_first>
  <behavior>
    - parseBackupFile de un wrapper v11 round-trip: export → import sin error "versión más nueva"; r.state.schemaVersion === 11; preserva el progreso de las 9 categorías intacto.
    - parseBackupFile de un backup v10 (progreso ficticio bajo presente-regolare + una categoría real) → state v11 con presente-regolare podada y la real preservada byte a byte (el reset se aplica vía la cadena de storage.js).
    - parseBackupFile rechaza un wrapper con schemaVersion 12 (state + wrapper = 12) con el reason que menciona "versión más nueva" (reject genérico contra CURRENT_SCHEMA_VERSION=11).
    - buildBackupWrapper espeja state.schemaVersion (un state v11 produce wrapper.schemaVersion === 11).
    - Los asserts existentes que esperaban r.state.schemaVersion === 10 ahora esperan 11 (la cadena de backup sale v11).
  </behavior>
  <action>
    En backup.js: añadir `migrate10to11, hydrateV11` a la lista de import nombrada (línea 26). Revisar si `hydrateV10` sigue usándose tras cambiar el final de la cadena a `hydrateV11`; si ya no se usa en backup.js, quitarlo del import (verificar con grep `hydrateV10` en backup.js antes de quitar). Mantener `migrate9to10` en el import.

    Bump `const CURRENT_SCHEMA_VERSION = 11;` (línea 49), manteniendo la independencia documentada (espejo manual de storage.js, NO import de la constante — es deliberado para testear backup.js aislado, docblock 28-48). Añadir una frase al docblock siguiendo el precedente de Phase 21 (39-42): "Phase 29 (D-29 / MIG-05/06): bump 10 a 11 — `migrate10to11` resetea el progreso de la categoría nueva `presente-regolare` (no-op hoy; forward-compat de un backup futuro que ya la contenga); el set de sub-dicts sigue sin cambiar."

    En la cadena de migración de parseBackupFile (135-137): mantener `if (migrated.schemaVersion === 9) migrated = migrate9to10(migrated);`, añadir `if (migrated.schemaVersion === 10) migrated = migrate10to11(migrated);`, y cambiar el final a `migrated = hydrateV11(migrated);` (antes hydrateV10).

    El reject `>N` (118-124) NO requiere edición de lógica — usa CURRENT_SCHEMA_VERSION (ahora 11), así que rechaza schemaVersion > 11 automáticamente. buildBackupWrapper (161-168) NO cambia (espeja state.schemaVersion). Verificar (no editar) que el mensaje sigue mencionando "versión más nueva" y "${CURRENT_SCHEMA_VERSION}".

    TESTS (clonar el bloque v9 → v11 en tests/backup.test.js, espejo 29-PATTERNS.md §7): añadir `migrate10to11, hydrateV11` al import de storage si el bloque los necesita (el bloque v9 usa los helpers locales — revisar). Crear helper `stateV11()` (espejo de stateV9 420-438) con `schemaVersion: 11` y progreso en las 9 categorías PRESERVADAS. Clonar: `round-trip v11: export → import sin "versión más nueva"` (assert r.state.schemaVersion === 11); `round-trip v11 preserva el progreso de las 9 categorías intacto`; `import de backup v10 → state v11 con presente-regolare reseteada` (fixture v10 con progreso ficticio bajo presente-regolare + una real → assert presente-regolare podada, la real byte a byte); `rejects future schemaVersion > 11 (menciona "versión más nueva")` con schemaVersion 12 en wrapper Y state. ACTUALIZAR los asserts existentes que esperaban r.state.schemaVersion === 10 → 11 (líneas 90-93, 121, 155, 175, 198, 246, 294, 345, 386, 446, 488, 509, 524, 527; verificar cada una con grep en contexto antes de tocar — son la cadena de backup que ahora sale v11). NO reemplazo ciego.

    Código exacto del bump/cadena/import en 29-PATTERNS.md §5.
  </action>
  <verify>
    <automated>node --test tests/backup.test.js 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "migrate10to11" src/data/backup.js` >= 2 (import + uso en cadena)
    - `grep -c "hydrateV11" src/data/backup.js` >= 2 (import + final de cadena)
    - `grep -v '^ \*\|^//' src/data/backup.js | grep -c "CURRENT_SCHEMA_VERSION = 11"` == 1
    - `grep -c "if (migrated.schemaVersion === 10) migrated = migrate10to11(migrated)" src/data/backup.js` == 1
    - `node --test tests/backup.test.js` exit 0 (round-trip v11, import v10→v11 con reset, reject >11, asserts actualizados)
  </acceptance_criteria>
  <done>backup.js bumpea CURRENT_SCHEMA_VERSION a 11, encadena migrate10to11 + hydrateV11, importa ambos, rechaza >11 (genérico); los tests de round-trip v11 + import v10→v11 + reject futuro pasan y los asserts existentes están corregidos a 11. `node --test tests/backup.test.js` verde.</done>
</task>

<task type="auto">
  <name>Task 3: verificación de suite completa + no-regresión global</name>
  <files>tests/data-storage.test.js, tests/backup.test.js</files>
  <read_first>
    - Salida de `node --test tests/*.test.js` (suite completa)
    - .planning/STATE.md §Quick Tasks (nota: hay 1 fail preexistente AJENO — genero-numero 12→13 — documentado repetidamente; NO es regresión de esta fase)
    - 29-PATTERNS.md §7 punto 4 (asserts a cazar por grep)
  </read_first>
  <action>
    Correr la suite completa `node --test tests/*.test.js` y comprobar que NO hay regresiones nuevas atribuibles al bump 10→11. Cazar cualquier assert residual de `schemaVersion 10` que el bump haya roto en CUALQUIER test file (no solo los dos ya tocados): `grep -rn "schemaVersion, 10\|schemaVersion: 10\|schemaVersion === 10\|schemaVersion === 11" tests/`. Para cada match que falle por el bump, decidir en contexto: si testea el OUTPUT aislado de migrate9to10 (que sigue devolviendo 10) → dejar en 10; si recorre la cadena completa hasta el state final (ahora v11) → corregir a 11.

    NOTA CRÍTICA sobre el conteo: STATE.md documenta 1 fail PREEXISTENTE y AJENO a esta fase (`genero-numero` 12→13, presente desde antes de la fase). NO intentar "arreglarlo" — está fuera de scope (es contenido, no migración). El criterio de éxito de ESTA fase es: 0 fails NUEVOS introducidos por el bump 10→11. Documentar en el SUMMARY el delta de tests (baseline pre-fase vs post-fase) y confirmar que el único fail restante, si lo hay, es el preexistente ajeno.

    Si la suite completa pasa con 0 fails (o solo el 1 preexistente ajeno documentado), la fase está verificada. NO tocar contenido, motor, smoke ni counts (eso es Phase 31).
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "schemaVersion === 10" tests/` no devuelve asserts que recorran la cadena completa hasta el state final (solo, como máximo, los del OUTPUT aislado de migrate9to10).
    - `node --test tests/*.test.js` termina con `# fail 0` O `# fail 1` siendo ese 1 el preexistente ajeno documentado en STATE.md (genero-numero) — confirmado leyendo la línea de fallo.
    - El SUMMARY documenta el delta de tests (count baseline vs post-fase) y que no hay regresión nueva del bump.
  </acceptance_criteria>
  <done>La suite completa pasa sin regresiones nuevas del bump 10→11; el único fail tolerado es el preexistente ajeno (genero-numero) ya documentado. Ningún assert de cadena completa quedó en schemaVersion 10. La app arranca limpia sobre el state migrado (verificado vía blankState/loadState tests verdes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archivo de backup JSON → parseBackupFile | Único input no confiable: un `.json` importado por el autor (potencialmente editado a mano o de origen no verificado). Cruza a `state` que se persiste y se monta en la app. |
| localStorage raw → loadState/migrate | Blob persistido que el autor podría editar manualmente vía DevTools; cruza al live state al arrancar. |

App personal estática, sin backend, sin red, sin auth, sin multi-usuario. La migración NO añade superficie de ataque nueva — solo añade un eslabón a una cadena de transformación pura ya existente.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | migrate10to11 / hydrateV11 — sub-dicts derivados de un backup JSON importado (prototype pollution vía `"__proto__"` own-property o getters) | mitigate | Deep-clone defensivo `JSON.parse(JSON.stringify(...))` en CADA sub-dict (exerciseStats, categoryProgress, dailyLog, songProgress), idéntico al patrón canónico de migrate8to9 (T-21-01) — la serialización JSON descarta funciones, getters y cadenas de prototipo no-Object.prototype. Reconstrucción del root con literal `{ schemaVersion: 11, ... }` (prototipo Object.prototype limpio). Verificado por el test obligatorio `migrate10to11 anti-prototype-pollution: __proto__ own-property no contamina el global` (Task 1, con schemaVersion:10 en el JSON malicioso) y el equivalente de hydrateV11. |
| T-29-02 | Tampering | parseBackupFile — wrapper con schemaVersion futuro (>11) o inconsistente wrapper↔state | mitigate | El reject `>CURRENT_SCHEMA_VERSION` (genérico, backup.js:119) rechaza schemaVersion > 11 automáticamente tras el bump; la guard de coherencia wrapper↔state (backup.js:111) y el lower-bound/integer guard (backup.js:102) ya existen y NO se tocan. Verificado por el test `rejects future schemaVersion > 11` (Task 2). |
| T-29-03 | Tampering | migrate10to11 con sub-dict corrupto (no-objeto) en el input | mitigate | Type-guard `(typeof X === 'object' && X !== null) ? clone : {}` por sub-dict cae a `{}` ante corrupción; idéntico al analog. Verificado por el test `sub-dict corrupto cae a {}` (Task 1). |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (web estática zero-deps; tests con `node --test` nativo). No hay nuevas dependencias → no aplica gate de legitimidad de paquetes. |

Block-on: high. No hay amenazas de severidad alta NUEVAS — la migración es una transformación pura que reusa exactamente las mitigaciones ya probadas en la cadena v2→v10. El único vector relevante (prototype-pollution vía JSON de backup importado) está cubierto por el deep-clone obligatorio del patrón.
</threat_model>

<verification>
1. `node --test tests/data-storage.test.js` verde — bloque v11 (reset, byte-intacto de las 9 + songProgress, idempotencia, pureza, anti-prototype-pollution, cadena v10→v11, blankState 11) + asserts existentes corregidos.
2. `node --test tests/backup.test.js` verde — round-trip v11, import v10→v11 con reset de presente-regolare, reject >11, asserts existentes corregidos.
3. `node --test tests/*.test.js` sin regresiones nuevas del bump (único fail tolerado = el preexistente ajeno genero-numero documentado en STATE.md).
4. `grep` gates: migrate10to11 + hydrateV11 + RESET_PREFIXES_V11 presentes en storage.js; CURRENT_SCHEMA_VERSION=11 en storage.js Y backup.js; eslabones en ambos dispatchers.
5. Gate de colisión de prefijo respetado: ninguna de las 9 categorías reales reseteada (verificado por el test de no-regresión byte-idéntica).
</verification>

<success_criteria>
- `migrate10to11` + `hydrateV11` + `RESET_PREFIXES_V11` + `CURRENT_SCHEMA_VERSION=11` en `storage.js`; dispatcher encadena 10→11→hydrateV11.
- `backup.js` bumpea a 11 espejo, encadena migrate10to11 + hydrateV11, importa ambos, rechaza `>11`.
- Un state v10 migra dejando presente-regolare reseteada y las 9 categorías + songProgress byte-intactas.
- migrate10to11 idempotente + pura + anti-prototype-pollution (deep-clone por sub-dict).
- Backup round-trip v11 + import v10→v11 con reset + reject >11.
- Suite completa verde (sin regresiones nuevas del bump); asserts de schemaVersion 10 actualizados a 11 donde corresponde (sin romper los del output aislado de migrate9to10).
- MIG-05 (migración/hydrate/bump idempotentes con reset selectivo) y MIG-06 (backup round-trip + import + reject) cubiertos.
</success_criteria>

<output>
Create `.planning/phases/29-migraci-n-10-11-reset-selectivo-solo-de-presente-regolare/29-01-SUMMARY.md` when done
</output>
