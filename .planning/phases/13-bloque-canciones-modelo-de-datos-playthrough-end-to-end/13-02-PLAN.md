---
phase: 13-bloque-canciones-modelo-de-datos-playthrough-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/main.js
  - src/screens/app.js
  - index.html
  - tests/screen-canciones.test.js
autonomous: true
requirements: [SONG-01, SONG-02, SONG-03, SONG-04, PLAY-01, PLAY-02, PLAY-03, PLAY-04, PLAY-05, LINK-02, LINK-04]

must_haves:
  truths:
    - "El home tiene un botón protagonista 'Canciones' (no muted) en la fila prominente que abre currentScreen='canciones'"
    - "El listado muestra cada canción con su estado (no-hecha/pasada/fallada) y número de frases, persistido en localStorage"
    - "Con 1 clic se inicia una canción y se recorren sus N frases EN ORDEN hasta el final, sin reinicio a mitad (PLAY-01)"
    - "Cada frase muestra la línea italiana y se construye la traducción española con word-buttons (it->es), feedback verde/rojo, al fallar muestra la correcta"
    - "Fallar una frase con categoryIds dispara applyImmediateFailure inmediato (UN call-site); frases sin categoría no cascadean"
    - "Al terminar, el resumen lista frases falladas (tu respuesta vs correcta) + bloque de categorías que bajaron de estado"
    - "El estado pasada/fallada se escribe write-once-at-end y persiste; abandonar a mitad descarta el progreso no comprometido (los fallos cascadeados quedan), re-entrar empieza de cero"
    - "Las canciones NO entran en buildSession/buildFullTest ni en la tabla de categorías del home"
  artifacts:
    - path: "index.html"
      provides: "Botón protagonista Canciones + screen 'canciones' (listado x-for) + render del playthrough word-buttons + bloques de resumen"
      contains: "currentScreen === 'canciones'"
    - path: "src/screens/app.js"
      provides: "songsForDisplay getter, startSong, completion routine con songProgress write-once + category-impact block"
      contains: "startSong"
    - path: "src/main.js"
      provides: "boot: loadSongs tras loadContent, songsById en el handoff de content"
      contains: "loadSongs"
  key_links:
    - from: "index.html botón Canciones"
      to: "currentScreen = 'canciones'"
      via: "@click protagonista (no class secondary)"
      pattern: "currentScreen = 'canciones'"
    - from: "src/screens/app.js startSong"
      to: "song.phrases en orden (NO buildFullTest)"
      via: "playthrough secuencial sin shuffle de frases"
      pattern: "startSong\\("
    - from: "src/screens/app.js completion routine"
      to: "applyImmediateFailure (per frase fallada) + songProgress[songId].status"
      via: "cascada inmediata (un call-site) + write-once-at-end"
      pattern: "applyImmediateFailure"
---

<objective>
Construir el slice VERTICAL jugable del bloque Canciones reutilizando el engine: botón protagonista en el home, pantalla de listado con estado por canción, playthrough secuencial it->es con feedback y cascada D-54, y resumen post-canción (errores + impacto en categorías). Brownfield: cada pieza copia un analog de `app.js`/`index.html`; el único call-site de cascada es `applyImmediateFailure` (Pitfall #2).

Purpose: Cierra el loop end-to-end "jugar una canción mínima" sobre la mini-canción de Plan 13-01, verificable sin el contenido real de Phase 14. Standalone (LINK-04): las canciones nunca entran en el sampler ni en la tabla de categorías.
Output: home button + screen `'canciones'` + playthrough + summary en `index.html`/`app.js`, `loadSongs` cableado en boot (`main.js`), tests de presencia/smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-bloque-canciones-modelo-de-datos-playthrough-end-to-end/13-CONTEXT.md
@.planning/phases/13-bloque-canciones-modelo-de-datos-playthrough-end-to-end/13-PATTERNS.md
@.planning/phases/13-bloque-canciones-modelo-de-datos-playthrough-end-to-end/13-01-SUMMARY.md

<interfaces>
<!-- Contratos del engine existente que el ejecutor reutiliza tal cual. -->

src/screens/app.js (factory appShell):
- `currentScreen` (línea 93): 'home'|'picker'|'session'|'summary'|'backup'. AÑADIR 'canciones' (listado) + reuso/dedicado para playthrough y summary (Claude's Discretion: reusar 'session'/'summary' con sub-estado song* O añadir 'cancion'/'cancion-summary'; mantener templates x-if mutuamente excluyentes — D-24).
- `_launchExamen(catId)` (líneas 370-423): analog de launch — "build lista ordenada -> reset sub-estado -> initSubStateForExercise(first) -> currentScreen=...". startSong NO llama buildFullTest/buildSession; usa `song.phrases` EN ORDEN (PLAY-01) y NO escribe inFlightTest (PLAY-05).
- `initSubStateForExercise(ex)` rama word-buttons (líneas ~1603-1613): bank = `fisherYates([...answer, ...distractors])`, `wordButtonsAnswer = []`. Reutilizable para una frase (es word-buttons-typed).
- `wordButtonsCheck()` (líneas ~1344-1357): `registry[ex.type].grade(ex, { tokens: this.wordButtonsAnswer })` -> `applyResultToSession(ex, correct, [...this.wordButtonsAnswer])`.
- `applyResultToSession(ex, correct, userAnswer)` (líneas 1217-1247): verde -> setTimeout 600ms sessionAdvance (PLAY-03); rojo -> `applyImmediateFailure(this.state, ex, this.content, todayLocal())` + saveState SÍNCRONO + expone "Siguiente". La rama song OMITE la persistencia de inFlightTest.
- `applyImmediateFailure(state, exercise, content, today)` (domain/progress.js líneas 296-334): lee `exercise.categoryIds`; vacío = no-op (LINK-03). REUSAR AS-IS, NO añadir segundo call-site (D-54 / Pitfall #2).
- `sessionAdvance()` (líneas 1262-1285): incrementa cursor, resetea selección/feedback, init siguiente; al pasar el final -> rutina de completado.
- `completeSession()` (líneas 1811-1848): snapshot `before` (deep-clone categoryProgress) -> apply -> computeSummaryDelta -> snapshot `summarySessionResults` (CR-02, sobrevive unmount) -> currentScreen='summary'. La rutina de canción DIVERGE: NO llama applySessionResult (standalone); escribe `songProgress[songId].status = results.some(r=>!r.correct) ? 'fallada' : 'pasada'` (write-once D-02) + saveState; el `before` snapshot se captura al INICIO de la canción (la cascada es incremental, muta categoryProgress mid-playthrough).
- `computeSummaryDelta(before, after, sessionResult, content)` (líneas 2288-2372): concepto de set auxiliar + status antes->después para el bloque de impacto. Para canción: `failedCategoryIds = Set(answers.filter(!correct).flatMap(a => songPhraseById[a.exerciseId]?.categoryIds ?? []))`.
- `returnToHomeFromSummary()` (líneas 1856-1864): limpia summary* + resetSession; la versión song vuelve a currentScreen='canciones' (listado actualizado — SONG-02/04).
- `categoriesForDisplay` getter + tabla x-for (index.html líneas 164-186): analog de `songsForDisplay` (mapea songsById + state.songProgress a filas con badge + phraseCount + botón Jugar). Double-defense: `if (!this.content) return []`.
- `cancelAutoAdvance()` (líneas 1291-1296): cancela el handle de auto-advance 600ms (Pitfall #5) — reusar el mismo handle session*.
- `handleSessionKey($event)` bound en `@keydown.window` (index.html línea 256): atajos word-buttons (1-9 banco, Backspace, Enter=Comprobar/avanzar). Auto-unmount al cambiar currentScreen (D-72).

index.html:
- Fila prominente del home (líneas 144-148): `.button-row button-row-prominent` con Repaso/Test (protagonistas) + Backup (`class="secondary"`). El botón Canciones es PROTAGONISTA (NO secondary — D-01).
- Render word-buttons de session (líneas 330-385): `.wb-bank`/`.wb-answer`/"Respuesta correcta:"/Comprobar+Siguiente — renderiza `payload.prompt` (línea italiana) y `payload.answer.join(' ')` en fallo (PLAY-03). Reutilizable verbatim.
- summary-errors (líneas 590-633): sub-template word-buttons "Tu respuesta: ... / Respuesta correcta: ..." con `(result.userAnswer || []).join(' ')` — para canción el lookup es songPhraseById en vez de content.exerciseById.
- delta render factual (líneas 522-543): `categoría: antes -> después` con flecha `delta-regression` — SIN gamificación.

src/main.js boot (líneas 61-102): tras `loadContent` (línea 74) añadir `loadSongs(songIds, knownCategoryIds)` y adjuntar `songsById` al handoff de content. Errores de song fluyen al mismo catch -> renderValidationBanner (líneas 103-106). `applyNewExerciseRegression` (línea 85) NO corre sobre canciones (standalone).

src/exercise-types/index.js: `registry[ex.type].grade(...)` — `word-buttons` grade es agnóstico al idioma; una frase con `type` implícito word-buttons gradúa igual. Si la frase de canción no lleva `type`, normalizarla a un objeto graduable `{ type:'word-buttons', payload:{prompt, answer, distractors} }` en el path de carga/launch (decisión del builder, dentro de Claude's Discretion sobre tipo de ejercicio).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot wiring + botón Canciones + pantalla listado (SONG-01/02/03, LINK-04)</name>
  <files>src/main.js, src/screens/app.js, index.html, tests/screen-canciones.test.js</files>
  <read_first>
    - src/main.js (boot pipeline líneas 61-110 — dónde insertar loadSongs y el handoff de songsById)
    - src/screens/app.js (factory: `currentScreen` línea 93, getter `categoriesForDisplay` y su double-defense, `startExamen` líneas 325-340 como analog 1-clic)
    - index.html (fila prominente líneas 144-148; tabla x-for categorías líneas 164-186; patrón template x-if mutuamente excluyente)
    - .planning/phases/13-...-end-to-end/13-PATTERNS.md §surface 5 (home button D-01 + currentScreen switch D-24 + listing table SONG-01/02 + double-defense)
    - .planning/phases/13-...-end-to-end/13-01-SUMMARY.md (shape de songsById y de cada canción que entrega loadSongs)
    - tests/screen-examen.test.js (estilo presence-check / windowed slicing sobre source — analog para tests/screen-canciones.test.js)
  </read_first>
  <action>
    En `src/main.js`: tras `loadContent` (línea 74) derivar `songIds` del índice (`content/songs.json`) y llamar `loadSongs(songIds, knownCategoryIds)`; adjuntar el `songsById` resultante al objeto `content` del handoff (campo hermano de `exerciseById`); los errores de carga de canción ya fluyen al `catch` -> `renderValidationBanner` (no tocar). NO ejecutar `applyNewExerciseRegression` sobre canciones (LINK-04). En `src/screens/app.js`: añadir el valor `'canciones'` al campo `currentScreen` (doc del comentario) y un getter `songsForDisplay` espejo de `categoriesForDisplay` que mapea `this.content.songsById` + `this.state.songProgress` a filas `{ id, title, phraseCount, status }` (status por defecto `'no-hecha'` si no hay entrada en songProgress); double-defense `if (!this.content) return []`. En `index.html`: añadir un 4º botón PROTAGONISTA `<button type="button" @click="currentScreen = 'canciones'">Canciones</button>` en `.button-row button-row-prominent` (NO `class="secondary"` — D-01); añadir un nuevo `<template x-if="currentScreen === 'canciones'">` con un listado `x-for` (copia de la estructura de la tabla de categorías) mostrando por canción: badge de estado (no-hecha/pasada/fallada — D-02, con texto del badge LITERAL en el HTML, NO x-text para glyph fijo; el title de la canción via `x-text` — T-02-01), `phraseCount` (SONG-02), y un botón "Jugar" `@click="startSong(song.id)"` (SONG-03, 1 clic), más un botón "Volver al home". Crear `tests/screen-canciones.test.js` con presence-checks (botón Canciones protagonista presente y SIN class secondary; `currentScreen === 'canciones'` template presente; getter `songsForDisplay` definido; `startSong(` referenciado en el @click) en el estilo de screen-examen.test.js.
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js; node --test tests/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "currentScreen = 'canciones'" index.html` >= 1 y el botón Canciones NO lleva `class="secondary"` (verificar con un grep windowed sobre la fila prominente: el `<button ...>Canciones</button>` no contiene `class="secondary"`).
    - `grep -c "currentScreen === 'canciones'" index.html` >= 1 (template del listado).
    - `grep -c "songsForDisplay" src/screens/app.js` >= 1; el getter contiene la guarda `if (!this.content)`.
    - `grep -c "loadSongs" src/main.js` >= 1; `songsById` se adjunta a content y NO se mezcla en exerciseById.
    - `grep -c "startSong(song.id)" index.html` >= 1 (botón Jugar 1-clic).
    - `node --test tests/*.test.js` sale 0.
  </acceptance_criteria>
  <done>El home muestra Canciones como botón protagonista; el listado renderiza cada canción con estado y nº de frases desde songProgress; loadSongs entrega songsById separado del pool; los presence-checks pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Playthrough secuencial it->es + cascada D-54 (PLAY-01/02/03/05, LINK-02/04)</name>
  <files>src/screens/app.js, index.html, tests/screen-canciones.test.js</files>
  <read_first>
    - src/screens/app.js (`_launchExamen` 370-423; `initSubStateForExercise` rama word-buttons ~1603-1613; `wordButtonsCheck` ~1344-1357; `applyResultToSession` 1217-1247; `sessionAdvance` 1262-1285; `cancelAutoAdvance` 1291-1296)
    - src/domain/progress.js (`applyImmediateFailure` 296-334 — REUSAR AS-IS, no modificar)
    - src/exercise-types/word-buttons.js (`grade()` agnóstico al idioma — usado en dirección inversa)
    - index.html (render word-buttons de session líneas 330-385; `@keydown.window="handleSessionKey($event)"` línea 256)
    - .planning/phases/13-...-end-to-end/13-PATTERNS.md §surface 6 (playthrough — divergencia standalone PLAY-01/PLAY-05) y §surface 7 (cascada reuse AS-IS, single call-site)
  </read_first>
  <action>
    En `src/screens/app.js` añadir `startSong(songId)` copiando la FORMA de `_launchExamen` PERO: tomar `this.content.songsById[songId].phrases` EN ORDEN DECLARADA (PLAY-01 — NO `buildFullTest`/`buildSession`, NO `fisherYates` sobre el orden de frases); normalizar cada frase a un objeto graduable word-buttons si no lleva `type` (`{ id, type:'word-buttons', payload:{ prompt, answer, distractors }, categoryIds }`); guardar el `songId` activo y un mapa `songPhraseById` (mapa frase-id -> objeto frase graduable, DEDICADO para canción — NO reutilizar `sessionExerciseIds`/`sessionCurrentExercise`, que resuelven contra `content.exerciseById` donde las frases de canción NO existen por LINK-04; el camino `songPhraseById` evita tener que volver song-aware a `sessionCurrentExercise`); resetear sub-estado session* (cursor 0, results [], feedback null, wordButtonsBank/Answer []); capturar el snapshot `before` de `categoryProgress` (deep-clone) AL INICIO (la cascada es incremental); `initSubStateForExercise` de la primera frase (rama word-buttons: bank = fisherYates(answer ∪ distractors) — el shuffle del BANCO sí está permitido, D-05); transición a la pantalla de playthrough; NO llamar `persistInFlightTest` (PLAY-05). La frase se gradúa por el camino existente `wordButtonsCheck` -> `applyResultToSession`: verde = setTimeout 600ms `sessionAdvance` (PLAY-03); rojo = `applyImmediateFailure(this.state, frase, this.content, todayLocal())` + saveState SÍNCRONO (LINK-02 — cascada inmediata; frase con categoryIds vacío = no-op por LINK-03) + expone "Siguiente"; la rama song OMITE el branch de persistencia inFlightTest. Mantener `applyImmediateFailure` como ÚNICO call-site de cascada (Pitfall #2 — no añadir un segundo). `sessionAdvance` al pasar el final invoca la rutina de completado de canción (Task 3). En `index.html` añadir el render del playthrough: si se reusa `currentScreen='session'` envolver con sub-estado song; si se añade `'cancion'`, copiar verbatim el bloque word-buttons (líneas 330-385) que renderiza `payload.prompt` (línea italiana, via x-text — T-02-01) como texto a traducir y `payload.answer.join(' ')` en fallo; reusar `@keydown.window="handleSessionKey($event)"` (D-72, auto-unmount al cambiar pantalla). Double-defense en el guard del template (`currentScreen === 'cancion' && <frase actual>`). Añadir presence-checks a `tests/screen-canciones.test.js`: `startSong(` definido; NO contiene `buildFullTest`/`buildSession` dentro del cuerpo de startSong (windowed slice); `applyImmediateFailure` aparece exactamente en el mismo single call-site que el path de session (no duplicado en el cuerpo de startSong).
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js; node --test tests/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "startSong(songId)" src/screens/app.js` >= 1.
    - Windowed slice de ~3000 chars desde la definición de `startSong`: NO contiene `buildFullTest` ni `buildSession` (PLAY-01/LINK-04) y NO contiene `persistInFlightTest` (PLAY-05).
    - `grep -c "applyImmediateFailure" src/screens/app.js` no aumenta respecto al único call-site dentro de `applyResultToSession` (la cascada de canción REUSA ese call-site, no añade otro).
    - El render del playthrough usa `x-text` para el prompt italiano (no `x-html`) — `grep` confirma ausencia de x-html en el bloque nuevo.
    - `node --test tests/*.test.js` sale 0.
  </acceptance_criteria>
  <done>startSong recorre las frases en orden sin shuffle ni resume slot; cada frase se gradúa it->es con feedback verde/rojo; fallar una frase con categoría cascadea inmediato vía el único call-site; frases sin categoría no cascadean.</done>
</task>

<task type="auto">
  <name>Task 3: Resumen post-canción + songProgress write-once + retorno (PLAY-04, SONG-02/04, D-02/D-04)</name>
  <files>src/screens/app.js, index.html, tests/screen-canciones.test.js</files>
  <read_first>
    - src/screens/app.js (`completeSession` 1811-1848; `computeSummaryDelta` 2288-2372; `returnToHomeFromSummary` 1856-1864; props summary* 189-205)
    - index.html (summary-errors template 590-633; delta render factual 522-543)
    - .planning/phases/13-...-end-to-end/13-PATTERNS.md §surface 8 (songProgress write-once-at-end D-02, divergencia vs categoryProgress) y §surface 9 (post-song summary — block A errores + block B impacto, before-snapshot timing)
    - .planning/phases/13-...-end-to-end/13-01-SUMMARY.md (shape de songProgress[songId] = { status, lastPlayedAt? })
  </read_first>
  <action>
    En `src/screens/app.js` añadir la rutina de completado de canción (analog de `completeSession`, líneas 1811-1848) que: calcula `status = this.sessionResults.some(r => !r.correct) ? 'fallada' : 'pasada'` y escribe `this.state.songProgress[songId] = { status, lastPlayedAt: todayLocal() }` (write-once-at-end, D-02) + `saveState(newState)` + `this.state = newState`; NO llama `applySessionResult` (standalone — la cascada ya ocurrió per-frase via applyImmediateFailure); computa el bloque de impacto a partir del set `failedCategoryIds = new Set(this.sessionResults.filter(r => !r.correct).flatMap(r => songPhraseById[r.exerciseId]?.categoryIds ?? []))` y, usando el snapshot `before` capturado al INICIO de la canción (Task 2) vs `this.state.categoryProgress`, lista solo las categorías que regresaron (status antes hecha/dominada -> ahora no-hecha) — reusa el concepto de `computeSummaryDelta` (no la función entera ni applySessionResult); snapshot `summarySessionResults = [...this.sessionResults]` (CR-02, sobrevive unmount); `cancelAutoAdvance()`; transición a la pantalla de summary de canción. En `index.html` añadir los dos bloques del resumen: Block A = lista de frases falladas reutilizando el sub-template word-buttons de `summary-errors` (líneas 624-633) con "Tu respuesta: ... / Respuesta correcta: ..." pero resolviendo el lookup contra `songPhraseById` en vez de `content.exerciseById` (las frases sin categoría falladas TAMBIÉN aparecen aquí — D-04); Block B = "Categorías que bajaron de estado por esto" reutilizando el render factual `antes -> después` (líneas 522-543, flecha delta-regression, SIN gamificación) listando solo las categorías del set que regresaron. Añadir `returnToSongList()` (analog de `returnToHomeFromSummary` líneas 1856-1864): limpia summary*/song activo, `resetSession()`, `currentScreen = 'canciones'` (el listado refleja el nuevo estado — SONG-02/04). Todo el contenido de canción se renderiza con `x-text` (T-02-01). Añadir presence-checks a `tests/screen-canciones.test.js`: rutina de completado escribe `songProgress`; el cuerpo NO llama `applySessionResult`; `returnToSongList` setea `currentScreen = 'canciones'`; el resumen usa x-text (no x-html).
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js; node --test tests/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - El cuerpo de la rutina de completado escribe en `songProgress` (`grep -c "songProgress\\[" src/screens/app.js` >= 1) y NO contiene `applySessionResult` (windowed slice).
    - `grep -c "summary-errors" index.html` aumenta o el bloque song reusa la clase; el lookup de Block A usa `songPhraseById`.
    - `grep -c "returnToSongList" src/screens/app.js` >= 1 y su cuerpo setea `currentScreen = 'canciones'`.
    - El resumen de canción no usa `x-html` (grep confirma ausencia en el bloque nuevo).
    - `node --test tests/*.test.js` sale 0.
  </acceptance_criteria>
  <done>Al terminar, el resumen lista frases falladas (tu respuesta vs correcta) y las categorías que bajaron de estado; el estado pasada/fallada se persiste write-once y el listado lo refleja al volver; abandonar a mitad (cambio de pantalla) descarta el progreso no comprometido conservando los fallos ya cascadeados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenido JSON de canción -> render Alpine | prompt/answer/title de la canción se inyectan en el DOM; deben escaparse |
| playthrough -> localStorage (cascada + songProgress) | el fallo debe persistir inmediatamente (no perdible al abandonar) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering / XSS | render de prompt/answer/title de canción en index.html | mitigate | Todo el contenido de canción se renderiza con `x-text` (NUNCA `x-html`) — invariante T-02-01 heredado; verificado por grep en los acceptance criteria |
| T-13-05 | Repudiation | exploit "fallo + abandono" sobre frase enganchada | mitigate | La cascada `applyImmediateFailure` + `saveState` síncrono ocurre en el instante de fallar (D-54); abandonar después NO revierte el fallo persistido (PLAY-05 descarta solo lo no comprometido) |
| T-13-06 | Tampering | songProgress en localStorage editado a mano | accept | App local single-user; `hydrateV5` (Plan 13-01) ya hace deep-clone defensivo del sub-árbol al cargar |

Nota: app LOCAL single-user, sin backend/red/secretos. No se modelan amenazas de servidor/auth/transporte.
</threat_model>

<verification>
- `node --test tests/*.test.js` sale 0 (suite existente + screen-canciones presence/smoke).
- Manual (UAT post-merge, coherente con el patrón de Phase 3+ para lógica mid-sesión no instanciable bajo node sin Alpine): abrir Canciones desde el home, jugar la mini-canción completa, fallar una frase enganchada y comprobar que el resumen lista la categoría que bajó de estado y que al volver al home esa categoría aparece reseteada.
</verification>

<success_criteria>
- Botón Canciones protagonista + pantalla listado con estado y nº de frases (SONG-01, SONG-02), inicio 1-clic (SONG-03), estado persistido (SONG-04).
- Playthrough secuencial it->es sin reinicio a mitad (PLAY-01, PLAY-02), feedback verde/rojo con la correcta al fallar (PLAY-03), resumen de errores (PLAY-04), abandono descarta no comprometido y re-entra de cero (PLAY-05).
- Fallar frase enganchada cascadea inmediato (LINK-02, único call-site); canciones fuera del sampler y de la tabla de categorías (LINK-04).
- Slice end-to-end jugable sobre la mini-canción de Plan 13-01.
</success_criteria>

<output>
Create `.planning/phases/13-bloque-canciones-modelo-de-datos-playthrough-end-to-end/13-02-SUMMARY.md` when done
</output>
