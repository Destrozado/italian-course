---
phase: 04-backup-robusto-contenido-completo
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - content/exercises/avere.json
  - tests/domain.test.js
autonomous: false
requirements: [SEED-02, BACK-04, BACK-05, BACK-06, SEED-01]
tags: [content, italian, multi-category, smoke-test, uat, cascade]

must_haves:
  truths:
    - "content/exercises/avere.json contiene los 17 ejercicios originales INTACTOS (sin modificar ningún id, payload ni categoryIds) + ≥6 ejercicios multi-categoría nuevos en range avere-3XX que crucen Avere con las 5 categorías nuevas"
    - "Al fallar un ejercicio multi-cat (por ejemplo avere-301 que toca avere+profesiones), la cascada D-54 INMEDIATA resetea ambas categorías simultáneamente — verificable en UAT en el resumen de fin de sesión"
    - "El smoke test integrado de 30 días extendido (tests/domain.test.js) carga avere.json real y ejecuta un escenario que falla un ejercicio multi-cat real, asertando cascada sobre las 2+ categorías afectadas"
    - "Los 5 success criteria del ROADMAP §Phase 4 se cumplen end-to-end con UAT humano:
       1. botón Exportar progreso descarga JSON con fecha en el nombre;
       2. botón Importar reemplaza state idéntico tras confirmación;
       3. banner home aparece >7 días sin backup (validable simulando fecha o esperando);
       4. 6 PDFs transcritos a JSONs ≥10 ejercicios validados (cubierto por planes 04-02/04-03 + extensión aquí);
       5. ≥1-2 multi-cat por PDF dispara cascada multi-cat visible en resumen"
    - "Tras este plan, las 5 requisitos pendientes (BACK-04, BACK-05, BACK-06, SEED-01, SEED-02) están marcados Complete con evidencia en SUMMARY"
  artifacts:
    - path: "content/exercises/avere.json"
      provides: "17 ejercicios originales preservados (ids avere-100..116 si la convención original lo permite — verificar) + ≥6 ejercicios nuevos avere-300..3NN con categoryIds.length ≥ 2 cruzando Avere con las 5 categorías nuevas (al menos 1 cruce por nueva categoría)"
      contains: "avere-300"
    - path: "tests/domain.test.js"
      provides: "Smoke test extendido (o nuevo test separado) que carga avere.json via readFileSync, escoge un ejercicio multi-cat real, simula fallo, assertea cascada D-54 sobre las 2+ categorías afectadas (rachas a 0, state no-hecha, clearedExerciseIds vacío). Mantiene los tests sintéticos de 30 días existentes."
      contains: "multi-cat"
  key_links:
    - from: "avere.json ejercicios avere-3XX nuevos"
      to: "categoryIds[] con ≥2 slugs de categorías existentes"
      via: "schema validator existente acepta multi-cat sin cambios"
      pattern: "\"categoryIds\": \\[\"avere\",\\s*\"(preposiciones|verbos-movimiento|sustantivos-irregulares|genero-numero|profesiones)\""
    - from: "applyImmediateFailure (existente)"
      to: "cascada D-54 sobre categoryIds de cualquier ejercicio fallado"
      via: "código existente — solo se ejercita con contenido nuevo"
      pattern: "categoryIds.*forEach"
---

<objective>
Cerrar Phase 4 con el slice de **cruces multi-categoría reales** que ejercitan la cascada D-54 en uso diario, más el UAT humano integral que valida los 5 success criteria del ROADMAP §Phase 4. SEED-02 cierra aquí; los 3 requisitos de backup (BACK-04/05/06) y SEED-01 se validan en uso real con el contenido completo.

**Purpose:** Phase 4 termina cuando el autor puede usar la app como daily driver: 6 categorías reales, cascada multi-cat ejerciéndose con frecuencia (porque los cruces son reales), backup como red de seguridad antes de cualquier cambio futuro. Este plan transforma "código existe" en "valor real validado".

**Output:** ≥6 ejercicios multi-cat añadidos a `avere.json` (NO modificar los 17 existentes — D-88 invariante estricto), 1 test smoke nuevo que verifica cascada multi-cat real, UAT humano que cierra los 5 ROADMAP criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md
@.planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md
@.planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md

@content/exercises/avere.json
@content/categories.json
@src/data/schema-validator.js
@src/domain/progress.js
@tests/domain.test.js
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing 17 avere exercises | D-88 invariante: nunca modificar. exerciseStats del usuario los referencia. |
| Multi-cat exercises → cascada D-54 | Cada nuevo avere-3XX dispara cascada sobre N categorías al fallar — exploit funcional del core value Phase 2. |
| UAT environment | UAT humano en máquina del autor. Reproducible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.04-01 | Tampering | Modificación accidental de los 17 ejercicios existentes (D-88) | high | mitigate | Acción explícita: `git diff content/exercises/avere.json` debe mostrar SOLO líneas añadidas dentro del array exercises (sin modificaciones de ids/payload/categoryIds existentes). Verificable con git diff inspección pre-commit. |
| T-04.04-02 | Tampering | Cruce multi-cat con categoría inexistente (typo en slug) | medium | mitigate | Schema validator existente bloquea categoryIds desconocidos (CONT-04). Banner CONT-05 visible. Smoke node-e antes del commit. |
| T-04.04-03 | Information Disclosure | UAT humano expone state real del autor durante export demo | low | accept | App single-user, autor controla qué exporta. |
| T-04.04-SC | Tampering | npm/pip/cargo installs | n/a | mitigate | Plan 04-04 instala CERO paquetes. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir ejercicios multi-cat a avere.json + smoke test cascada multi-cat real</name>
  <files>content/exercises/avere.json, tests/domain.test.js</files>
  <read_first>
    - content/exercises/avere.json (full read — los 17 ejercicios actuales son INTOCABLES per D-88; identificar el último ID usado para continuar la secuencia)
    - content/categories.json (verificar las 6 categorías existen tras 04-02)
    - content/exercises/{preposiciones,verbos-movimiento,sustantivos-irregulares,genero-numero,profesiones}.json (creados en planes 04-02 y 04-03 — leer brevemente para identificar palabras italianas ya validadas que puedan reutilizarse en los cruces multi-cat, evitando reinventar vocabulario)
    - src/data/schema-validator.js (líneas 1-110 — verificar que `categoryIds` array de ≥2 elementos no requiere validación adicional; Phase 2 ya cubre)
    - src/domain/progress.js (full read — `applyImmediateFailure` y `applySessionResult` cascade logic; verificar firma para escribir el test)
    - tests/domain.test.js (full read — patrón readFileSync + describe/test + assertions sobre state result)
    - tests/exercise-types.test.js (lines 1-60 — patrón readFileSync fixture)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-87, D-88 — locks)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§C Specific Multi-Category Crosses líneas 1146-1158 — ejemplos validados con material real; §B orden 6 = avere multi-cat ya transcritos los 5 PDFs)
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (sección `content/exercises/avere.json` líneas 733-737 + multi-categoría ejemplo líneas 712-723)
  </read_first>
  <behavior>
    - Test 1 (smoke multi-cat cascada real): cargar avere.json via readFileSync, encontrar el ejercicio multi-cat avere-301 (o el primer ejercicio con `categoryIds.length >= 2`), simular sesión donde el usuario lo falla, ejecutar `applySessionResult` (o `applyImmediateFailure` según firma actual), assertar que las 2+ categorías referenciadas en sus `categoryIds` quedan en estado `no-hecha` con racha 0 y `clearedExerciseIds: []`.
    - Test 2 (smoke avere.json sigue cargable con schema validator post-extension): cargar avere.json completo + categories.json completo, llamar validateContent, assertar `result.ok === true` (ningún ejercicio multi-cat rompe el schema validator existente).
    - Cuenta total tests Phase 4: ≥ 21 backup + ≥ 2 multi-cat = ≥ 23 nuevos; total con baseline ≥ 128 verdes.
  </behavior>
  <action>
    Dos archivos editados:

    1. **`content/exercises/avere.json`** (EXTEND — APPEND-ONLY):
       a. Leer el archivo completo para identificar:
          - El último ID usado por los 17 ejercicios existentes (probablemente en rango `avere-100..299` según convenciones por tipo).
          - Que el shape `{"exercises": [...]}` se preserva.
       b. **APPEND ≥6 ejercicios nuevos** en el rango `avere-300..3NN` cada uno con `categoryIds.length ≥ 2`. **Al menos 1 cruce por cada nueva categoría** (preposiciones, verbos-movimiento, sustantivos-irregulares, genero-numero, profesiones). Distribución recomendada (basada en RESEARCH §C lista de cruces validados):

          - `avere-300` — **Avere + Profesiones**: `prompt: "Lui ___ un fratello medico."`, multi-choice options `["ho","hai","ha","abbiamo"]`, correct 2. `categoryIds: ["avere", "profesiones"]`. notes: `"Cruce Avere + Profesiones (D-87 / SEED-02). Fallarlo cascadeará D-54 sobre ambas categorías."`.
          - `avere-301` — **Avere + Profesiones (fem)**: `prompt: "Io ___ una sorella avvocata."`, options `["ho","hai","ha","abbiamo"]`, correct 0. `categoryIds: ["avere","profesiones"]`. notes: refuerza fem.
          - `avere-302` — **Avere + Sustantivos Irregulares (cuerpo)**: `prompt: "Lui ___ due braccia."`, options `["ho","hai","ha","abbiamo"]`, correct 2. `categoryIds: ["avere","sustantivos-irregulares"]`. notes: cuerpo + posesión.
          - `avere-303` — **Avere + Verbos Movimiento + Preposiciones (triple si cabe)**: si no se puede triple naturalmente, sustituir por **Avere + Preposiciones** directo. Ejemplo: `prompt: "Ho un'amica ___ Roma."`, options `["a","in","da","per"]`, correct 0. `categoryIds: ["avere","preposiciones"]`. (En este caso la frase tiene `ho` implícito como sujeto — verificar pedagógicamente que ejercita ambas reglas.)
          - `avere-304` — **Avere + Genero/Numero**: `prompt: "Lui ___ due figli e una figlia."`, options `["ho","hai","ha","abbiamo"]`, correct 2. `categoryIds: ["avere","genero-numero"]`. notes: plural + género.
          - `avere-305` — **Avere + Verbos Movimiento**: `prompt: "Io ___ una bicicletta per andare al lavoro."`, options `["ho","hai","ha","abbiamo"]`, correct 0. `categoryIds: ["avere","verbos-movimiento"]`. (Andare implícito.) Si pedagógicamente no es lo bastante claro, sustituir por otro cruce verbos-movimiento; D-87 acepta que algunos cruces puedan descartarse si no aportan.
          - **Opcional avere-306+:** cruces extra si surgen naturalmente.

          NOTA: estos son **propuestas iniciales basadas en RESEARCH §C**. El autor podría preferir variantes pedagógicamente mejores tras revisión — pero a diferencia de los planes 04-02/04-03 que tenían checkpoints humanos explícitos por categoría, este task es `auto` con verificación humana en Task 2 (UAT). La justificación: los cruces salen del material ya transcritos por el autor en planes anteriores, así que el autor ya conoce el vocabulario; los cruces son combinaciones gramaticales simples (sólo `avere` + un sustantivo/preposición de las categorías ya validadas).
       c. **APPEND only** — JAMÁS modificar los 17 ejercicios existentes:
          - No cambiar id, type, categoryIds, payload, notes de ninguno.
          - Solo añadir nuevas entradas al array `exercises` ANTES del `]` final.
          - Preservar formato (indent, comas) idéntico al resto del archivo.
       d. **Apostrofes**: si alguno de los cruces usa `un'amica` o similar (apóstrofe necesario para vocal inicial femenina), usar `'` U+0027 — verificable con grep U+2019.

    2. **`tests/domain.test.js`** (EXTEND): añadir un nuevo describe block AL FINAL del archivo, sin modificar tests existentes:

       ```javascript
       describe('Phase 4 — multi-categoría cascade with real content', () => {
         test('failing a real multi-cat exercise from avere.json cascades to all its categoryIds', () => {
           // Cargar avere.json y categories.json desde el filesystem
           const avere = JSON.parse(readFileSync(new URL('../content/exercises/avere.json', import.meta.url), 'utf8'));
           const categories = JSON.parse(readFileSync(new URL('../content/categories.json', import.meta.url), 'utf8'));

           // Encontrar el primer ejercicio multi-cat (categoryIds.length >= 2)
           const multiCat = avere.exercises.find(ex => Array.isArray(ex.categoryIds) && ex.categoryIds.length >= 2);
           assert.ok(multiCat, 'expected at least one multi-cat exercise in avere.json (SEED-02)');
           assert.ok(multiCat.categoryIds.length >= 2);

           // Construir un state v3 con esas categorías "hechas" con racha
           const targetCats = multiCat.categoryIds;
           const state = {
             schemaVersion: 3,
             exerciseStats: { [multiCat.id]: { timesShown: 1, timesCorrect: 1, timesFailed: 0 } },
             categoryProgress: {},
             dailyLog: {},
             lastBackupAt: null,
             firstUsedAt: null
           };
           targetCats.forEach(catId => {
             state.categoryProgress[catId] = {
               status: 'hecha',
               streakDays: 5,
               clearedExerciseIds: [multiCat.id],
               lastSuccessDate: '2026-05-23'
             };
           });

           // Simular fallo via applyImmediateFailure (o equivalente — verificar firma actual)
           // Reusar el patrón usado en Phase 2 smoke test 30 días.
           // [PSEUDO — el agente ejecutor adapta a la firma exacta importada]
           const today = '2026-05-24';
           // Importar applyImmediateFailure de '../src/domain/progress.js' al inicio del archivo
           // si no estaba ya importado.
           const result = applyImmediateFailure(state, multiCat, /* allExercises */ avere.exercises, today);

           // Assert cascada sobre TODAS las categorías de multiCat.categoryIds:
           targetCats.forEach(catId => {
             assert.equal(result.categoryProgress[catId].status, 'no-hecha', `${catId} debe pasar a no-hecha`);
             assert.equal(result.categoryProgress[catId].streakDays, 0, `${catId} racha a 0`);
             assert.deepEqual(result.categoryProgress[catId].clearedExerciseIds, [], `${catId} clearedExerciseIds vacío`);
           });
         });

         test('avere.json + categories.json continue passing schema validator after multi-cat extension', () => {
           // Carga combinada de los 6 JSONs + categories, llamar validateContent.
           // [PSEUDO — adapter a firma actual de validateContent]
           // Reusar el patrón ya en tests/exercise-types.test.js si existe; si no, ad-hoc:
           const avere = JSON.parse(readFileSync(new URL('../content/exercises/avere.json', import.meta.url), 'utf8'));
           const categories = JSON.parse(readFileSync(new URL('../content/categories.json', import.meta.url), 'utf8'));
           // Sólo avere — los otros 5 archivos pueden cargarse adicionalmente si la firma de validateContent lo requiere.
           // Verificar firma real de validateContent en src/data/schema-validator.js exports y adaptar.
           // Smoke aceptable: importar validateContent y assertear que avere multi-cat exercises devuelve ok.

           // Ejemplo del shape esperado:
           // const result = validateContent({ avere }, categories);
           // assert.equal(result.ok, true, JSON.stringify(result.errors));

           // Defensivo: si la firma exacta no se infiere desde Read, fallar el test explícitamente
           // hasta que se ajuste — NO commitear un test que no corre.
         });
       });
       ```

       Notas para el ejecutor del test:
       - Importar `readFileSync` en la cabecera si no estaba ya (`import { readFileSync } from 'node:fs';`).
       - Importar `applyImmediateFailure` desde `'../src/domain/progress.js'`.
       - Importar `validateContent` desde `'../src/data/schema-validator.js'`.
       - Si la firma de `applyImmediateFailure` o `validateContent` difiere de los pseudo-llamadas, **adaptar al actual** — verificable leyendo `src/domain/progress.js` y `src/data/schema-validator.js` antes de escribir el test. NO inventar una firma.
       - El test debe ser **deterministic** — el `today` se pasa explícito, no se confía en `Date.now()`.
       - El primer test usa el primer multi-cat exercise encontrado — esto asegura que el test se autoactualiza si el orden de los multi-cat cambia (no hardcode `avere-300`).
  </action>
  <verify>
    <automated>node --test tests/domain.test.js tests/*.test.js && grep -c '"id": "avere-3' content/exercises/avere.json && node -e "const c=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); const multi = c.exercises.filter(e => e.categoryIds && e.categoryIds.length >= 2); if (multi.length < 6) {console.error('expected >=6 multi-cat, got', multi.length); process.exit(1)}; console.log('OK', multi.length, 'multi-cat exercises')"</automated>
  </verify>
  <acceptance_criteria>
    - **Source: `git diff content/exercises/avere.json` (or equivalente)** muestra SOLO líneas añadidas — ninguna modificación a los 17 ejercicios existentes (D-88).
    - **Source: count nuevos avere-3XX — `grep -c '"id": "avere-3' content/exercises/avere.json`** ≥ 6.
    - **Source: cada nuevo ejercicio tiene `categoryIds.length >= 2`** (verificable con node -e en `<automated>`).
    - **Source: cobertura de cruces — al menos 1 cruce por categoría nueva:** `grep -c '"avere", "preposiciones"\\|"preposiciones", "avere"' content/exercises/avere.json` ≥ 1; mismo check para `verbos-movimiento`, `sustantivos-irregulares`, `genero-numero`, `profesiones`. **5 categorías nuevas × ≥1 cruce = ≥5 cruces obligatorios.** Si solo se entregan 5, el sexto del MUST puede ser un cruce extra de cualquier categoría.
    - **Source: `grep -P "[\\x{2019}]" content/exercises/avere.json`** exit 1 (sin U+2019).
    - **Source: schema validation passes** sobre avere.json post-extension (smoke node-e con validateContent).
    - **Behavior: `node --test tests/domain.test.js` exits 0** con el nuevo describe block "Phase 4 — multi-categoría cascade with real content" pasando.
    - **Behavior: `node --test tests/*.test.js` exits 0** — total ≥ 128 tests verdes.
    - **Source: tests/domain.test.js NO modifica el bloque smoke 30 días existente** (`grep -c "30 day\\|30-day\\|30 días" tests/domain.test.js` mantiene el conteo previo a este task; el nuevo describe es ADDITIVO).
    - **Source: `grep -c 'applyImmediateFailure' tests/domain.test.js`** ≥ 1 (importado + usado en el test multi-cat).
  </acceptance_criteria>
  <done>
    avere.json tiene ≥6 ejercicios multi-cat avere-3XX cubriendo al menos 1 cruce con cada nueva categoría; los 17 ejercicios originales intactos; smoke test multi-cat real verifica cascada D-54 sobre las 2+ categorías afectadas; ≥ 128 tests verdes; schema validation sobre avere extendido passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT humano integral — 5 ROADMAP success criteria + cascada multi-cat visible + flujo backup end-to-end</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md (§"Phase 4: Backup robusto + contenido completo" — los 5 success criteria literales)
    - .planning/REQUIREMENTS.md (BACK-04, BACK-05, BACK-06, SEED-01, SEED-02 — los 5 IDs Phase 4)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-73..D-88 todos los locks)
    - .planning/phases/04-backup-robusto-contenido-completo/04-01-SUMMARY.md (backup runtime entregado — qué probar)
    - .planning/phases/04-backup-robusto-contenido-completo/04-02-SUMMARY.md y 04-03-SUMMARY.md (contenido transcrito — qué categorías esperar en home)
    - content/exercises/avere.json (cruces avere-3XX recién añadidos en Task 1 — para identificar qué fallar en UAT 5)
  </read_first>
  <what-built>
    Claude prepara el environment para UAT (sin tocar código):
    1. Verifica que `node --test tests/*.test.js` exits 0 (≥ 128 tests verdes baseline).
    2. Verifica con grep que las 6 categorías + ≥10 ejercicios por categoría existen.
    3. Lista en mensaje claro al autor los 5 criterios ROADMAP a verificar + los pasos exactos para cada uno.

    El autor (humano) ejecuta UAT estructurado:

    **Setup:**
    - `cd /home/vcompanyb/italian-course`
    - `npx serve` (background o terminal aparte)
    - Abrir `http://localhost:3000` en el navegador
    - Abrir DevTools → Application → Local Storage para inspeccionar `italianCourse.v1` durante la sesión

    **Sesión UAT (orden recomendado):**

    **UAT-1 (Criterio ROADMAP 4 — SEED-01):**
    1. Verificar que la home muestra las 6 categorías en orden Avere → Preposiciones → Verbos de movimiento → Sustantivos irregulares → Género y número → Profesiones.
    2. Verificar que NO aparece el banner CONT-05 de error (ningún JSON malformado).
    3. Verificar que cada categoría muestra "X ejercicios" con X ≥ 10 (excepto Avere que tiene ≥17 + multi-cat).
    4. **Pass:** las 6 categorías visibles con ≥10 ejercicios cada una.

    **UAT-2 (Criterio ROADMAP 1 — BACK-04 export):**
    1. Click botón "Backup" en home (esquina derecha del button-row).
    2. Verificar pantalla Backup carga con título "Backup", línea de estado "Aún no has exportado tu progreso." o "Último backup: ...".
    3. Click "Exportar progreso".
    4. Verificar:
       - Navegador descarga un archivo `italian-course-backup-YYYY-MM-DD.json` (con la fecha de hoy).
       - Mensaje verde en pantalla "Progreso exportado. Guarda el archivo en lugar seguro."
       - DevTools → Local Storage → `italianCourse.v1` ahora tiene `lastBackupAt: "..."` ISO actual.
    5. Abrir el JSON descargado en un editor — verificar shape `{kind: "italian-course-backup", exportedAt: "...", schemaVersion: 3, state: {...}}`.
    6. **Pass:** archivo descargado con shape correcto + mensaje + state.lastBackupAt actualizado.

    **UAT-3 (Criterio ROADMAP 2 — BACK-05 import + confirmación):**
    1. En la pantalla Backup, click "Importar progreso".
    2. Seleccionar el archivo recién descargado.
    3. Verificar aparece el panel `.confirm-inline` arriba-derecha con:
       - "Vas a importar un backup del {fecha local}..."
       - "Categorías con progreso: N"
       - "Ejercicios con stats: M"
       - "Esto REEMPLAZARÁ tu progreso actual y no se puede deshacer."
       - Botones "Continuar" + "Cancelar (sec)".
    4. Click "Continuar".
    5. Verificar:
       - Mensaje verde "Progreso importado correctamente."
       - `currentScreen === 'home'` (volvemos a home automáticamente).
       - DevTools → Local Storage → estado IDÉNTICO al exportado.
    6. **Bonus exploit-proof:** seleccionar un archivo .txt renombrado a .json → verificar mensaje rojo "JSON inválido: ..." (no crash).
    7. **Pass:** idempotencia export→import + manejo de errores en español.

    **UAT-4 (Criterio ROADMAP 3 — BACK-06 banner 7 días):**
    Hay dos formas de validar:
    - **Opción A (esperar 8 días):** poco práctico para UAT.
    - **Opción B (manipulación de state):** DevTools → Application → Local Storage → editar `italianCourse.v1` manualmente para fijar `lastBackupAt` a una fecha hace 10 días (e.g., `"2026-05-14T10:00:00.000Z"` si hoy es 2026-05-24). Refrescar la página.
    1. Tras el refresh, verificar:
       - El banner ámbar `.backup-banner` aparece arriba de la button-row con texto "⚠ Han pasado 10 días desde tu último backup."
       - Botón "Ir a Backup" navega a la pantalla Backup.
    2. Click "Exportar progreso" en la pantalla Backup.
    3. Volver a home (botón "← Volver al home"). El banner ya **no aparece** (Alpine reactivity al cambiar `lastBackupAt`).
    4. **Bonus null path:** DevTools → editar `lastBackupAt` a `null` y `firstUsedAt` a hace 10 días. Refresh. El banner debe aparecer con texto "⚠ Aún no has exportado tu progreso." (variante D-78 fallback).
    5. **Bonus future-date defense:** DevTools → `lastBackupAt: "2027-01-01T00:00:00.000Z"` (futuro). Refresh. El banner NO aparece (Pitfall #5).
    6. **Pass:** banner aparece >7d, desaparece tras export, variantes texto correctas, futuro no rompe.

    **UAT-5 (Criterio ROADMAP 5 — SEED-02 cascada multi-cat real):**
    1. En la home, click "Repaso 20".
    2. En el picker, marcar las categorías que se cruzan en avere-300 (por ejemplo Avere + Profesiones).
    3. Lanzar el Repaso.
    4. **Antes de fallar:** anotar mentalmente el estado de Avere y Profesiones (estado + racha) que muestra la home.
    5. Durante la sesión, identificar el ejercicio multi-cat `avere-300` (el prompt incluye "fratello medico" — multi-cat por la nature del ejercicio). **Fallar deliberadamente** (clic en una opción incorrecta).
    6. Verificar **inmediatamente** (D-54 immediate cascade) — sin terminar la sesión, abrir DevTools → Local Storage → `italianCourse.v1`:
       - `categoryProgress.avere.status === 'no-hecha'`
       - `categoryProgress.avere.streakDays === 0`
       - `categoryProgress.avere.clearedExerciseIds === []`
       - `categoryProgress.profesiones.status === 'no-hecha'`
       - `categoryProgress.profesiones.streakDays === 0`
       - `categoryProgress.profesiones.clearedExerciseIds === []`
    7. Completar la sesión (los demás ejercicios pueden acertarse).
    8. En el resumen, verificar que **ambas categorías** (Avere + Profesiones) aparecen en la lista de "categorías tocadas" con la transición `hecha→no-hecha` (o `dominada→no-hecha` si ya estaban dominadas).
    9. **Bonus triple-cat (si avere-303 o similar cruza 3 categorías):** repetir el ejercicio con un cruce de 3 — verificar las 3 cascadean.
    10. **Pass:** cascada D-54 inmediata sobre las N categorías del ejercicio fallado, visible en resumen.

    **UAT-6 (Regression Phase 1-3 smoke):**
    1. Lanzar un Repaso de Avere SOLA → completar acertando todo → verificar resumen muestra promoción a `hecha` o mantenimiento si ya estaba.
    2. Lanzar Test completo de las 6 categorías → ver advertencia con total de ejercicios (≥50-60) → arrancar → cerrar la pestaña a medias.
    3. Reabrir la app → verificar banner `.inflight-banner` "Tienes un Test completo a medias" → "Reanudar" funciona.
    4. **Pass:** no regresiones en flujos Phase 1-3.

    **Veredicto final del autor:**
    - **PASS:** los 5 criterios ROADMAP + regression Phase 1-3 pasan limpios.
    - **NEEDS-PATCH:** uno o más criterios necesitan ajustes menores; documentar en SUMMARY y crear plan 04-05 (gap closure) si emerge.
    - **FAIL:** un criterio bloqueante falla; reabrir el plan correspondiente.
  </what-built>
  <how-to-verify>
    - **Pre-checkpoint (Claude):** ejecutar `node --test tests/*.test.js` y confirmar 0 fallos. Mensaje al autor con tests count + lista de UAT-1..UAT-6 pasos.
    - **Author:** seguir los UAT-1..UAT-6 marcando cada uno como PASS/FAIL en el mensaje de resume.
    - Capturas opcionales pero recomendadas: screenshots de la home con banners + screenshot del resumen tras cascada multi-cat.
  </how-to-verify>
  <acceptance_criteria>
    - **Behavior: `node --test tests/*.test.js` exits 0** (≥ 128 tests verdes — sin regresión).
    - **Source: las 6 categorías existen — `ls content/exercises/{avere,preposiciones,verbos-movimiento,sustantivos-irregulares,genero-numero,profesiones}.json | wc -l`** == 6.
    - **Source: counts mínimos por categoría:** cada archivo tiene ≥10 ejercicios (verificable: `for f in content/exercises/*.json; do n=$(node -e "console.log(JSON.parse(require('fs').readFileSync('$f','utf8')).exercises.length)"); if [ "$n" -lt 10 ]; then echo "FAIL $f $n"; exit 1; fi; done`).
    - **Source: avere multi-cat ≥6** (verificable con grep ya en Task 1).
    - **Source: cruces por categoría cubiertos** (5 cruces obligatorios con grep por slug).
    - **Human checkpoint:** el autor confirma PASS/NEEDS-PATCH/FAIL para cada UAT-1..UAT-6 explícitamente.
    - **Behavior funcional:** los 5 criterios ROADMAP (export descarga archivo, import idempotente, banner 7d, 6 PDFs ≥10 ejercicios, multi-cat cascada visible) están todos verificados.
  </acceptance_criteria>
  <resume-signal>
    Type:
    - `PASS` si los 6 UAT pasan limpios.
    - `NEEDS-PATCH: <descripción>` si algún detalle es ajustable in-line (e.g., copy text, banner color tint).
    - `FAIL: <descripción>` si bloqueante (necesita re-planificación).
  </resume-signal>
</task>

</tasks>

<verification>
- Task 1 autonomous; Task 2 human checkpoint.
- Tests: ≥ 128 verdes (≥ 126 tras 04-01 + ≥ 2 multi-cat en Task 1).
- UAT cierra los 5 ROADMAP criteria. NEEDS-PATCH dispara plan 04-05 (gap closure) si necesario; FAIL reabre plan(es).
- 5 requirements de Phase 4 (BACK-04, BACK-05, BACK-06, SEED-01, SEED-02) marcados Complete tras PASS.
</verification>

<success_criteria>
- avere.json tiene ≥6 multi-cat exercises cubriendo las 5 nuevas categorías (al menos 1 cruce por nueva categoría).
- Los 17 avere existentes INTACTOS.
- Smoke test multi-cat real ejecuta cascada D-54 sobre 2+ categorías y assertea.
- UAT humano: los 5 criterios ROADMAP pasan + regression Phase 1-3 sin sorpresas.
- BACK-04, BACK-05, BACK-06, SEED-01, SEED-02 marcables Complete en REQUIREMENTS.md con evidencia documentada en SUMMARY.
</success_criteria>

<output>
Crear `.planning/phases/04-backup-robusto-contenido-completo/04-04-SUMMARY.md` al finalizar:
- Multi-cat exercises añadidos (ids + categorías cruzadas).
- Smoke test multi-cat añadido (nombre describe block).
- UAT veredicto por criterio (PASS/NEEDS-PATCH/FAIL).
- Tests count final.
- Phase 4 marcado complete en STATE.md y ROADMAP.md (si PASS); si NEEDS-PATCH, listar los ajustes pendientes.
- Lecciones aprendidas Phase 4 (pedagógicas + técnicas).
- Recomendaciones para milestone close (`/gsd:complete-milestone v1.0`).
</output>
