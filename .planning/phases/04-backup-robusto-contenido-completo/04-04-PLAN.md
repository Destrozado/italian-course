---
phase: 04-backup-robusto-contenido-completo
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - content/exercises/avere.json
  - tests/domain.test.js
  - scripts/snapshot-avere-prefix.mjs
  - scripts/assert-avere-prefix-unchanged.mjs
  - scripts/assert-multi-cat-cross.mjs
  - .gitignore  # W-NEW-3 fix: añadir/crear entrada para scripts/.avere-prefix-snapshot.json
autonomous: false
requirements: [SEED-02, BACK-04, BACK-05, BACK-06, SEED-01]
tags: [content, italian, multi-category, smoke-test, uat, cascade]

must_haves:
  truths:
    - "content/exercises/avere.json contiene los 17 ejercicios originales INTACTOS — verificable estructuralmente por snapshot+assert (NO por git diff que requiere setup git): scripts/snapshot-avere-prefix.mjs captura los primeros 17 ejercicios ANTES de editar; scripts/assert-avere-prefix-unchanged.mjs compara DESPUÉS de editar (W-4 fix)"
    - "Tras la extensión, content/exercises/avere.json contiene ≥6 ejercicios multi-categoría nuevos en range avere-3XX que crucen Avere con las 5 categorías nuevas (al menos 1 cruce por nueva categoría)"
    - "Cada cruce obligatorio (5 categorías × 1 cruce mínimo) se verifica via scripts/assert-multi-cat-cross.mjs que parsea JSON y comprueba `categoryIds.includes('avere') && categoryIds.includes('<slug>')` — NO grep literal (W-7 fix)"
    - "El autor revisa las propuestas multi-cat de Claude ANTES de aceptar el commit (W-3 fix: checkpoint humano explícito antes del smoke test)"
    - "Al fallar un ejercicio multi-cat (por ejemplo avere-301 que toca avere+profesiones), la cascada D-54 INMEDIATA resetea ambas categorías simultáneamente — verificable en UAT en el resumen de fin de sesión"
    - "El smoke test integrado de 30 días extendido (tests/domain.test.js) carga avere.json real y ejecuta un escenario que falla un ejercicio multi-cat real, asertando cascada sobre las 2+ categorías afectadas"
    - "Todas las invocaciones de schema validation usan scripts/validate-content-fixture.mjs (creado en 04-02 Task 1, B-4 fix) y todos los smoke checks complejos usan helper scripts en scripts/ — NO `node -e` inline con apóstrofes anidados (B-2 fix)"
    - "Los 5 success criteria del ROADMAP §Phase 4 se cumplen end-to-end con UAT humano:
       1. botón Exportar progreso descarga JSON con fecha en el nombre;
       2. botón Importar reemplaza state idéntico tras confirmación;
       3. banner home aparece >7 días sin backup (validable simulando fecha o esperando);
       4. 6 PDFs transcritos a JSONs ≥10 ejercicios validados (cubierto por planes 04-02/04-03 + extensión aquí);
       5. ≥1-2 multi-cat por PDF dispara cascada multi-cat visible en resumen"
    - "Tras este plan, las 5 requisitos pendientes (BACK-04, BACK-05, BACK-06, SEED-01, SEED-02) están marcados Complete con evidencia en SUMMARY"
  artifacts:
    - path: "content/exercises/avere.json"
      provides: "17 ejercicios originales preservados (ids avere-001..012, avere-100..101, avere-200..202 verificados por snapshot+assert) + ≥6 ejercicios nuevos avere-300..3NN con categoryIds.length ≥ 2 cruzando Avere con las 5 categorías nuevas (al menos 1 cruce por nueva categoría)"
      contains: "avere-300"
    - path: "tests/domain.test.js"
      provides: "Smoke test extendido (nuevo describe block AL FINAL del archivo) que carga avere.json via readFileSync, escoge el primer ejercicio multi-cat, simula fallo, assertea cascada D-54 sobre las 2+ categorías afectadas (rachas a 0, state no-hecha, clearedExerciseIds vacío). Mantiene los tests sintéticos de 30 días existentes."
      contains: "multi-cat"
    - path: "scripts/snapshot-avere-prefix.mjs"
      provides: "Captura los primeros 17 ejercicios (ids+payload completos) de content/exercises/avere.json y los escribe a un archivo temporal scripts/.avere-prefix-snapshot.json (gitignored). Usado en Task 1a antes de editar avere.json (W-4 fix)."
      contains: "exercises.slice(0, 17)"
    - path: "scripts/assert-avere-prefix-unchanged.mjs"
      provides: "Lee scripts/.avere-prefix-snapshot.json y compara con los primeros 17 ejercicios del avere.json actual via deepEqual. Exit 0 si idénticos; exit 1 + diff verbose si difieren. Usado tras Task 1b para verificar D-88 sin depender de git (W-4 fix)."
      contains: "assert.deepStrictEqual"
    - path: "scripts/assert-multi-cat-cross.mjs"
      provides: "Args: <slug1> <slug2>. Parsea avere.json, busca al menos 1 ejercicio con `categoryIds.includes(slug1) && categoryIds.includes(slug2)`. Exit 0 si existe; exit 1 si no. Reemplaza grep literal por categoría que era fragile ante line-break formatting (W-7 fix)."
      contains: "categoryIds.includes"
  decisions_covered:
    - "D-85: Claude propone los ejercicios multi-cat avere-3XX y el autor los revisa vía checkpoint:human-verify (Task 1b) ANTES del commit; replica el patrón de 04-02/04-03 para contenido más subtle (W-3 fix)"
    - "D-86: tipo de ejercicio multi-cat es mayoría multi-choice (coherente con la asignación de Avere — `Lui ha un fratello medico` formato hueco); algún match si emerge naturalmente"
    - "D-87: cruces naturales semánticos definidos — al menos 1 cruce por categoría nueva (avere+preposiciones, avere+verbos-movimiento, avere+sustantivos-irregulares, avere+genero-numero, avere+profesiones), verificados por scripts/assert-multi-cat-cross.mjs en Task 1a"
    - "D-88: avere.json append-only — los 17 ejercicios originales (avere-001..012, avere-100..101, avere-200..202) preservados; verificado estructuralmente por snapshot pre-edit (sub-acción D) + assert-avere-prefix-unchanged post-edit (W-4 fix robusto vs git diff manual)"
  key_links:
    - from: "avere.json ejercicios avere-3XX nuevos"
      to: "categoryIds[] con ≥2 slugs de categorías existentes"
      via: "schema validator existente acepta multi-cat sin cambios"
      pattern: "\"categoryIds\":"
    - from: "applyImmediateFailure (existente)"
      to: "cascada D-54 sobre categoryIds de cualquier ejercicio fallado"
      via: "código existente — solo se ejercita con contenido nuevo"
      pattern: "categoryIds.*forEach"
    - from: "scripts/assert-avere-prefix-unchanged.mjs"
      to: "D-88 invariante (los 17 originales intocables)"
      via: "snapshot pre + assert post; reemplaza git diff"
      pattern: "snapshot.*deepEqual"
    - from: "scripts/assert-multi-cat-cross.mjs"
      to: "verificación de cruces obligatorios"
      via: "JSON.parse + categoryIds.includes — NO grep"
      pattern: "categoryIds.includes\\("
---

<objective>
Cerrar Phase 4 con el slice de **cruces multi-categoría reales** que ejercitan la cascada D-54 en uso diario, más el UAT humano integral que valida los 5 success criteria del ROADMAP §Phase 4. SEED-02 cierra aquí; los 3 requisitos de backup (BACK-04/05/06) y SEED-01 se validan en uso real con el contenido completo.

**Purpose:** Phase 4 termina cuando el autor puede usar la app como daily driver: 6 categorías reales, cascada multi-cat ejerciéndose con frecuencia (porque los cruces son reales), backup como red de seguridad antes de cualquier cambio futuro. Este plan transforma "código existe" en "valor real validado".

**Output:** 3 helper scripts (`snapshot-avere-prefix.mjs`, `assert-avere-prefix-unchanged.mjs`, `assert-multi-cat-cross.mjs`) que reemplazan checks fragiles (W-4 + W-7 + B-2 fixes), ≥6 ejercicios multi-cat añadidos a `avere.json` con checkpoint humano explícito (W-3 fix), 1 test smoke nuevo que verifica cascada multi-cat real (helper-driven, no apóstrofes anidados — B-2 fix), UAT humano que cierra los 5 ROADMAP criteria.

**Task structure:**
- Task 1a (auto): snapshot prefix + escribir helpers + proponer multi-cat exercises
- Task 1b (checkpoint:human-verify): el autor revisa pedagógicamente los multi-cat ANTES del commit (W-3 fix)
- Task 1c (auto): smoke test multi-cat + assertions post-edit
- Task 2 (checkpoint:human-verify): UAT integral de 5 ROADMAP criteria
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md
@.planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md
@.planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md

@content/exercises/avere.json
@content/categories.json
@src/data/schema-validator.js
@src/domain/progress.js
@tests/domain.test.js
@scripts/validate-content-fixture.mjs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing 17 avere exercises | D-88 invariante: nunca modificar. exerciseStats del usuario los referencia. Verificable structuralmente (snapshot+assert) sin depender de git. |
| Multi-cat exercises → cascada D-54 | Cada nuevo avere-3XX dispara cascada sobre N categorías al fallar — exploit funcional del core value Phase 2. |
| Multi-cat exercises → author review | Cruces son pedagógicamente subtler que mono-cat; el autor revisa ANTES del commit (W-3 checkpoint humano explícito). |
| UAT environment | UAT humano en máquina del autor. Reproducible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.04-01 | Tampering | Modificación accidental de los 17 ejercicios existentes (D-88) | high | mitigate | W-4 fix: snapshot+assert structural en lugar de `git diff` (que no es robusto en repos sin commit previo o con cambios sin stage). Helpers `scripts/snapshot-avere-prefix.mjs` (Task 1a) + `scripts/assert-avere-prefix-unchanged.mjs` (Task 1c) reemplazan la inspección manual de diff con `assert.deepStrictEqual`. |
| T-04.04-02 | Tampering | Cruce multi-cat con categoría inexistente (typo en slug) | medium | mitigate | Schema validator existente bloquea categoryIds desconocidos (CONT-04). Helper `scripts/validate-content-fixture.mjs` (B-4 fix) ejecuta validación con firma real. Banner CONT-05 visible en UAT. |
| T-04.04-03 | Tampering | Cruce con typo en slug NO bloqueado por validator estructural (false-positive coverage por categoría) | medium | mitigate | W-7 fix: `scripts/assert-multi-cat-cross.mjs <slug1> <slug2>` parsea JSON y verifica `categoryIds.includes()` programáticamente — reemplaza el grep literal que era fragile ante formateo line-break diferente. Se invoca una vez por par obligatorio (5 cruces). |
| T-04.04-04 | Tampering | Author review of multi-cat exercises insuficiente | medium | mitigate | W-3 fix: el flujo Task 1a (Claude propone) → Task 1b (humano revisa+aprueba) → Task 1c (smoke test) materializa el checkpoint humano EXPLÍCITAMENTE antes del smoke test, no como un side note del UAT-5 en Task 2. |
| T-04.04-05 | Tampering | UAT humano expone state real del autor durante export demo | low | accept | App single-user, autor controla qué exporta. |
| T-04.04-06 | Tampering | Heredoc-incompatible commands en planes (apóstrofes anidados) hacen que verificaciones fallen al pasarse a shells | medium | mitigate | B-2 fix: TODOS los smoke checks complejos del plan se materializan como helper scripts en `scripts/` (snapshot-avere-prefix, assert-avere-prefix-unchanged, assert-multi-cat-cross, validate-content-fixture). Las invocaciones desde `<verify>` y `<acceptance_criteria>` son `node scripts/<helper>.mjs <args>` — solo apóstrofes simples, sin anidación. |
| T-04.04-SC | Tampering | npm/pip/cargo installs | n/a | mitigate | Plan 04-04 instala CERO paquetes. Helpers son node-puros usando solo `node:fs`, `node:assert/strict`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1a: Snapshot avere prefix + escribir helper scripts + proponer ≥6 ejercicios multi-cat (sin commit aún)</name>
  <files>scripts/snapshot-avere-prefix.mjs, scripts/assert-avere-prefix-unchanged.mjs, scripts/assert-multi-cat-cross.mjs, content/exercises/avere.json</files>
  <read_first>
    - content/exercises/avere.json (full read — los 17 ejercicios actuales: avere-001..012 + avere-100..101 + avere-200..202; son INTOCABLES per D-88; identificar el último ID usado para continuar la secuencia con avere-300+)
    - content/categories.json (verificar las 6 categorías existen tras 04-02 Task 1)
    - content/exercises/{preposiciones,verbos-movimiento,sustantivos-irregulares,genero-numero,profesiones}.json (creados en planes 04-02 y 04-03 — leer brevemente para identificar palabras italianas ya validadas que puedan reutilizarse en los cruces multi-cat, evitando reinventar vocabulario)
    - src/data/schema-validator.js (líneas 1-110 — verificar que `categoryIds` array de ≥2 elementos no requiere validación adicional; Phase 2 ya cubre)
    - src/domain/progress.js (full read — `applyImmediateFailure` y `applySessionResult` cascade logic; verificar firma para escribir el test en Task 1c)
    - scripts/validate-content-fixture.mjs (helper de 04-02 — reusable como modelo de estilo node-puro ESM)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-87, D-88 — locks)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§C Specific Multi-Category Crosses líneas 1146-1158 — ejemplos validados con material real)
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (sección `content/exercises/avere.json` líneas 733-737 + multi-categoría ejemplo líneas 712-723)
  </read_first>
  <action>
    Tres sub-acciones en este task. Ninguna implica commit todavía — el commit ocurre tras Task 1b (checkpoint humano).

    **Sub-acción A — Crear `scripts/snapshot-avere-prefix.mjs` (NEW):**

    Node ESM pure, sin imports externos. Comportamiento:
    1. `readFileSync('content/exercises/avere.json', 'utf8')` → `JSON.parse`.
    2. `const prefix = c.exercises.slice(0, 17);` (los 17 originales documentados en read_first).
    3. `writeFileSync('scripts/.avere-prefix-snapshot.json', JSON.stringify(prefix, null, 2));`.
    4. `console.log('Snapshot escrito: 17 ejercicios →', 'scripts/.avere-prefix-snapshot.json');` exit 0.
    5. Si falla read/parse → exit 1 con mensaje en español.
    6. Header JSDoc: "Captura los primeros 17 ejercicios de avere.json antes de extender con multi-cat (D-88 invariante). El snapshot se compara post-edit via scripts/assert-avere-prefix-unchanged.mjs (W-4 fix: reemplaza git diff por structural deepEqual)."

    **Añadir el archivo snapshot al gitignore** si existe `.gitignore`; si no existe, crear `.gitignore` mínimo con `scripts/.avere-prefix-snapshot.json` (el snapshot es temporal del proceso, no debe commitearse).

    **Sub-acción B — Crear `scripts/assert-avere-prefix-unchanged.mjs` (NEW):**

    Node ESM pure. Comportamiento:
    1. Imports: `import { readFileSync } from 'node:fs'; import assert from 'node:assert/strict';`.
    2. Cargar `scripts/.avere-prefix-snapshot.json` → `before`. Si missing → exit 1 + "Snapshot no encontrado. Ejecuta primero scripts/snapshot-avere-prefix.mjs.".
    3. Cargar `content/exercises/avere.json` → `c`.
    4. `const after = c.exercises.slice(0, 17);`.
    5. `try { assert.deepStrictEqual(after, before); } catch (err) { console.error('Los primeros 17 ejercicios de avere.json HAN CAMBIADO. D-88 invariante violado.'); console.error(err.message); process.exit(1); }`.
    6. `console.log('OK: los 17 ejercicios originales de avere.json están intactos (D-88).'); exit 0.
    7. Header JSDoc: "Verifica que los primeros 17 ejercicios de avere.json no han cambiado vs snapshot pre-edit (D-88, W-4 fix). Exit 0 si idénticos, exit 1 + diff si difieren."

    **Sub-acción C — Crear `scripts/assert-multi-cat-cross.mjs` (NEW):**

    Node ESM pure. Comportamiento:
    1. Args: `node scripts/assert-multi-cat-cross.mjs <slug1> <slug2>`. Si missing → exit 1 + "Uso: node scripts/assert-multi-cat-cross.mjs <slug1> <slug2>".
    2. Cargar `content/exercises/avere.json`.
    3. `const crosses = c.exercises.filter(e => Array.isArray(e.categoryIds) && e.categoryIds.includes(slug1) && e.categoryIds.includes(slug2));`.
    4. Si `crosses.length === 0` → exit 1 + `No se encontró ningún ejercicio en avere.json con categoryIds que incluyan tanto "${slug1}" como "${slug2}".`.
    5. Si ≥ 1 → `console.log('OK:', crosses.length, 'cruce(s) avere ↔', slug2, ':', crosses.map(e=>e.id).join(', '));` exit 0.
    6. Header JSDoc: "Verifica que existe al menos un ejercicio multi-cat con los 2 slugs dados (W-7 fix: reemplaza grep literal por JSON parsing). Usado para los 5 cruces obligatorios (avere+preposiciones, avere+verbos-movimiento, avere+sustantivos-irregulares, avere+genero-numero, avere+profesiones)."

    **Sub-acción D — Ejecutar snapshot de los 17 originales:**

    Tras crear los 3 helpers, ejecutar: `node scripts/snapshot-avere-prefix.mjs` y confirmar que escribe `scripts/.avere-prefix-snapshot.json` con 17 entries.

    **Sub-acción E — Proponer ≥6 ejercicios multi-cat (APPEND-ONLY a avere.json):**

    Cargar avere.json, identificar el array `exercises`, añadir ≥6 nuevos al final ANTES del `]`. Cada uno con `categoryIds.length ≥ 2`. **Al menos 1 cruce por cada nueva categoría** (preposiciones, verbos-movimiento, sustantivos-irregulares, genero-numero, profesiones). Distribución recomendada (basada en RESEARCH §C, AJUSTABLE en Task 1b por el autor):

    - `avere-300` — **Avere + Profesiones**: `prompt: "Lui ___ un fratello medico."`, multi-choice options `["ho","hai","ha","abbiamo"]`, correct 2. `categoryIds: ["avere", "profesiones"]`. notes: `"Cruce Avere + Profesiones (D-87 / SEED-02). Fallarlo cascadeará D-54 sobre ambas categorías."`.
    - `avere-301` — **Avere + Profesiones (fem)**: `prompt: "Io ___ una sorella avvocata."`, options `["ho","hai","ha","abbiamo"]`, correct 0. `categoryIds: ["avere","profesiones"]`. notes: refuerza fem.
    - `avere-302` — **Avere + Sustantivos Irregulares (cuerpo)**: `prompt: "Lui ___ due braccia."`, options `["ho","hai","ha","abbiamo"]`, correct 2. `categoryIds: ["avere","sustantivos-irregulares"]`. notes: cuerpo + posesión.
    - `avere-303` — **Avere + Preposiciones**: `prompt: "Ho un amico ___ Roma."`, options `["a","in","da","per"]`, correct 0. `categoryIds: ["avere","preposiciones"]`. (Frase tiene `ho` explícito; verificar pedagógicamente en Task 1b.)
    - `avere-304` — **Avere + Genero/Numero**: `prompt: "Lui ___ due figli e una figlia."`, options `["ho","hai","ha","abbiamo"]`, correct 2. `categoryIds: ["avere","genero-numero"]`. notes: plural + género.
    - `avere-305` — **Avere + Verbos Movimiento**: `prompt: "Io ___ una bicicletta per andare al lavoro."`, options `["ho","hai","ha","abbiamo"]`, correct 0. `categoryIds: ["avere","verbos-movimiento"]`. notes: andare implícito.

    **Reglas estrictas:**
    - **APPEND only** — JAMÁS modificar los 17 ejercicios existentes. Snapshot+assert post-edit lo verifica.
    - Preservar formato (indent 2 espacios, comas) idéntico al resto del archivo.
    - Apostrofes `'` U+0027 si alguno necesita (e.g. `un'amica` → si se incluye, verificar U+0027).
    - Cada `categoryIds[0]` puede ser cualquier orden, lo importante es que ambos slugs estén presentes — el helper assert-multi-cat-cross.mjs no impone orden.

    **NO commit todavía** — el commit es responsabilidad de Task 1b tras human-verify.
  </action>
  <verify>
    <automated>node scripts/assert-multi-cat-cross.mjs avere preposiciones && node scripts/assert-multi-cat-cross.mjs avere verbos-movimiento && node scripts/assert-multi-cat-cross.mjs avere sustantivos-irregulares && node scripts/assert-multi-cat-cross.mjs avere genero-numero && node scripts/assert-multi-cat-cross.mjs avere profesiones && node scripts/assert-avere-prefix-unchanged.mjs && node --test tests/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - **Source: 3 helper scripts existen** — `ls scripts/snapshot-avere-prefix.mjs scripts/assert-avere-prefix-unchanged.mjs scripts/assert-multi-cat-cross.mjs` retorna 3 paths.
    - **Source: snapshot creado** — `ls scripts/.avere-prefix-snapshot.json` retorna 1 path; `node -e "const a=require('fs').readFileSync('scripts/.avere-prefix-snapshot.json','utf8'); const p=JSON.parse(a); if(p.length!==17) process.exit(1); console.log('snapshot tiene',p.length,'ejercicios')"` exit 0.
    - **Source: avere.json post-extend tiene ≥6 multi-cat avere-3XX** — `grep -c '"id": "avere-3' content/exercises/avere.json` ≥ 6.
    - **Behavior: los 5 cruces obligatorios pasan (W-7 fix)** — los 5 `node scripts/assert-multi-cat-cross.mjs avere <slug>` del `<verify>` block exit 0 cada uno.
    - **Behavior: los 17 originales intactos (W-4 fix)** — `node scripts/assert-avere-prefix-unchanged.mjs` exit 0.
    - **Behavior: schema validation passes para avere extendido (B-4 fix)** — `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0.
    - **Source: `grep -P "[\\x{2019}]" content/exercises/avere.json`** exit 1 (sin U+2019).
    - **Behavior: `node --test tests/*.test.js` exits 0** sin regresión (los tests existentes no rompen).
    - **Source: NO commit todavía** — git diff muestra cambios staged/unstaged en `content/exercises/avere.json` Y nuevos archivos en `scripts/` pero NO ha habido commit aún (verificable manualmente; el commit lo hace Task 1b).
  </acceptance_criteria>
  <done>
    3 helper scripts creados; snapshot pre-edit capturado; ≥6 multi-cat avere-3XX añadidos (proposición de Claude); los 5 cruces obligatorios verificados via helper scripts; los 17 originales intactos via assert; schema validation passes; tests verdes. **Pendiente:** revisión humana en Task 1b.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1b: Author review pedagógico de los multi-cat exercises propuestos + commit (W-3 fix)</name>
  <files>content/exercises/avere.json</files>
  <read_first>
    - content/exercises/avere.json (LECTURA OBLIGATORIA — los ≥6 ejercicios avere-3XX recién añadidos por Task 1a; el autor revisa cada uno pedagógicamente)
    - material-profesora/{preposizioni_italiano,Clase_Italiano_Verbos_Movimiento,Sustantivos Irregulares,Clase_Italiano_Genero_y_Numero,Professioni_Italiano_Facile}.pdf (cross-reference si surge duda sobre una construcción)
    - content/exercises/{preposiciones,verbos-movimiento,sustantivos-irregulares,genero-numero,profesiones}.json (analogs para verificar consistencia de vocabulario)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-85 — el principio de "autor revisa cada categoría ANTES del commit" aplica también a multi-cat)
  </read_first>
  <what-built>
    Claude:
    1. Confirma que Task 1a ha completado (los helpers existen, snapshot capturado, ≥6 multi-cat añadidos a avere.json, los 5 cruces obligatorios pasan).
    2. Resume al autor los ejercicios multi-cat propuestos (lista de ids + prompts + categoryIds + correctIndex) en mensaje claro.
    3. **PAUSA** para revisión humana.

    El autor (humano):
    1. Abre `content/exercises/avere.json` en su editor; navega hasta avere-300 (final del array).
    2. Revisa cada ejercicio nuevo (avere-300, 301, 302, 303, 304, 305, y cualquier extra) verificando:
       - [ ] La construcción italiana es correcta y A1-apropiada (no inventos, no calcos del español, conjugación essere/avere correcta).
       - [ ] La categoría cruzada es PEDAGÓGICAMENTE relevante (e.g. `avere-302 "Lui ha due braccia"` cruza con sustantivos-irregulares porque `braccia` es plural irregular de `braccio`; este cruce TIENE SENTIDO didáctico — el autor lo confirma).
       - [ ] El `correctIndex` es correcto.
       - [ ] Las distractoras son plausibles (no absurdas, no obvias).
       - [ ] Los apostrofes son U+0027 (ASCII) si los hay.
       - [ ] Ningún cruce es "forzado" o "no aporta" pedagógicamente — si alguno no convence, el autor lo pide reformular o eliminar (y Claude propone alternativo manteniendo el mínimo de 1 cruce por categoría).
    3. Si pide ediciones: Claude las aplica; tras edición, re-ejecutar TODOS los checks de Task 1a `<verify>` block antes de re-solicitar approval.
    4. Si aprueba todos los multi-cat:
       - Claude ejecuta `node scripts/assert-avere-prefix-unchanged.mjs` UNA ÚLTIMA VEZ (sanity por si hubo ediciones).
       - Claude hace commit: `content: add avere multi-cat exercises (Phase 4 SEED-02)`. Files staged: `content/exercises/avere.json`, `scripts/snapshot-avere-prefix.mjs`, `scripts/assert-avere-prefix-unchanged.mjs`, `scripts/assert-multi-cat-cross.mjs`, y `.gitignore` si se creó/modificó.
       - Claude NO commitea `scripts/.avere-prefix-snapshot.json` (gitignored).
  </what-built>
  <how-to-verify>
    1. **Pre-checkpoint (Claude):**
       - Confirma que `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (snapshot todavía válido tras Task 1a).
       - Confirma que los 5 cruces obligatorios pasan (5x `node scripts/assert-multi-cat-cross.mjs avere <slug>` exit 0).
       - Lista al autor los ids + prompts + categoryIds + correctIndex de cada multi-cat añadido.
    2. **Author review (~5-10 min):**
       - Lee cada ejercicio nuevo en avere.json.
       - Verifica el checklist arriba.
       - Decide: PASS (commit) / EDIT (Claude ajusta y re-solicita) / FAIL (rechaza un cruce; Claude propone alternativo).
  </how-to-verify>
  <acceptance_criteria>
    - **Behavior: 5 cruces obligatorios verificados (W-7 fix) tras ediciones (si las hubo):** los 5 `node scripts/assert-multi-cat-cross.mjs` exit 0 cada uno.
    - **Behavior: 17 originales intactos tras ediciones:** `node scripts/assert-avere-prefix-unchanged.mjs` exit 0.
    - **Behavior: schema validation passes tras ediciones:** `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0.
    - **Source: counts multi-cat ≥ 6** — `grep -c '"id": "avere-3' content/exercises/avere.json` ≥ 6.
    - **Human checkpoint signal:** el autor confirma "approved" tras revisión pedagógica de cada multi-cat (W-3 fix).
    - **Behavior: commit realizado** — `git log -1 --format=%s` matches `content: add avere multi-cat exercises (Phase 4 SEED-02)` (si la operación de git está disponible; si no, el ejecutor manualmente confirma que el commit se hizo).
  </acceptance_criteria>
  <resume-signal>
    Type:
    - `approved` para confirmar review pedagógico → Claude commitea.
    - `edit: <id> <descripción>` para pedir cambios en un ejercicio específico → Claude ajusta y re-solicita approval.
    - `replace: <id> with <descripción>` para sustituir un cruce por otro.
    - `manual` si ya editaste tú y quieres que Claude commitee el estado actual.
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1c: Smoke test cascada multi-cat real (tests/domain.test.js extend)</name>
  <files>tests/domain.test.js</files>
  <read_first>
    - tests/domain.test.js (full read — patrón readFileSync + describe/test + assertions sobre state result; identificar imports existentes para no duplicar)
    - tests/exercise-types.test.js (lines 1-60 — patrón readFileSync fixture)
    - src/domain/progress.js (full read — `applyImmediateFailure` signature; verificar firma exacta para escribir el test correctamente — NO inventar)
    - content/exercises/avere.json (post Task 1b — los multi-cat avere-3XX ya están commiteados)
    - content/categories.json
    - scripts/validate-content-fixture.mjs (analog de estilo para imports ESM)
  </read_first>
  <behavior>
    - Test 1 (smoke multi-cat cascada real): cargar avere.json via readFileSync, encontrar el PRIMER ejercicio con `categoryIds.length >= 2` (auto-actualizable: no hardcode `avere-300`), simular sesión donde el usuario lo falla, ejecutar `applyImmediateFailure` (firma exacta verificada en read_first), assertar que las 2+ categorías referenciadas en sus `categoryIds` quedan en estado `no-hecha` con racha 0 y `clearedExerciseIds: []`.
    - Test 2 (smoke validateContent post-extension): cargar avere.json + categories.json, llamar `validateContent({ categories, exercisesByFile: { 'content/exercises/avere.json': avere.exercises } })`, assertar `result.ok === true`.
    - Cuenta total tests Phase 4: ≥ 21 backup (04-01) + ≥ 2 multi-cat (este task) = ≥ 23 nuevos; total con baseline ≥ 128 verdes.
  </behavior>
  <action>
    Extender `tests/domain.test.js` AÑADIENDO un nuevo describe block AL FINAL del archivo, sin modificar tests existentes.

    1. **Verificar imports existentes** en `tests/domain.test.js` para no duplicar:
       - Si `readFileSync` no está importado, añadir `import { readFileSync } from 'node:fs';` al top.
       - Si `applyImmediateFailure` no está importado, añadir `import { applyImmediateFailure } from '../src/domain/progress.js';` (verificar firma real antes — NO inventar; si la firma actual de progress.js usa otro nombre o requiere otros args, adaptar).
       - Si `validateContent` no está importado, añadir `import { validateContent } from '../src/data/schema-validator.js';`.

    2. **Añadir describe block AL FINAL del archivo:**

       ```javascript
       describe('Phase 4 — multi-categoría cascade with real content', () => {
         test('failing a real multi-cat exercise from avere.json cascades to all its categoryIds', () => {
           const avere = JSON.parse(readFileSync(new URL('../content/exercises/avere.json', import.meta.url), 'utf8'));

           // Encontrar el primer ejercicio multi-cat (auto-actualizable si el orden cambia)
           const multiCat = avere.exercises.find(ex => Array.isArray(ex.categoryIds) && ex.categoryIds.length >= 2);
           assert.ok(multiCat, 'expected at least one multi-cat exercise in avere.json (SEED-02)');
           assert.ok(multiCat.categoryIds.length >= 2);

           // Construir un state v3 con esas categorías "hechas" con racha
           const targetCats = multiCat.categoryIds;
           const state = {
             schemaVersion: 3,
             exerciseStats: { [multiCat.id]: { timesShown: 1, timesCorrect: 1, timesFailed: 0 } },
             categoryProgress: {},
             dailyLog: {},
             lastBackupAt: null,
             firstUsedAt: null
           };
           targetCats.forEach(catId => {
             state.categoryProgress[catId] = {
               status: 'hecha',
               streakDays: 5,
               clearedExerciseIds: [multiCat.id],
               lastSuccessDate: '2026-05-23'
             };
           });

           const today = '2026-05-24';
           // W-NEW-2 fix: applyImmediateFailure documenta content como {exerciseById: Record<string, object>}
           // (ver src/domain/progress.js:292 JSDoc). El body actual no lee `content`, pero respetamos
           // el contrato de tipo para que el test no viole la firma documentada.
           const contentArg = { exerciseById: Object.fromEntries(avere.exercises.map(e => [e.id, e])) };
           const result = applyImmediateFailure(state, multiCat, contentArg, today);

           targetCats.forEach(catId => {
             assert.equal(result.categoryProgress[catId].status, 'no-hecha', `${catId} debe pasar a no-hecha`);
             assert.equal(result.categoryProgress[catId].streakDays, 0, `${catId} racha a 0`);
             assert.deepEqual(result.categoryProgress[catId].clearedExerciseIds, [], `${catId} clearedExerciseIds vacío`);
           });
         });

         test('avere.json + categories.json pass validateContent after multi-cat extension', () => {
           const avere = JSON.parse(readFileSync(new URL('../content/exercises/avere.json', import.meta.url), 'utf8'));
           const categoriesRaw = JSON.parse(readFileSync(new URL('../content/categories.json', import.meta.url), 'utf8'));

           const result = validateContent({
             categories: categoriesRaw.categories,
             exercisesByFile: { 'content/exercises/avere.json': avere.exercises }
           });

           assert.equal(result.ok, true, JSON.stringify(result.errors, null, 2));
         });
       });
       ```

    3. **Adaptaciones de firma:** la firma documentada en `src/domain/progress.js:292` JSDoc es `applyImmediateFailure(state, exercise, content, today)` donde `content = {exerciseById: Record<string, object>}` (NO `Array`). El test usa `contentArg = { exerciseById: Object.fromEntries(avere.exercises.map(e => [e.id, e])) }` para respetar el contrato (W-NEW-2 fix). Si la firma real en el codebase ha cambiado, leer `src/domain/progress.js` y adaptar. NO inventar args. Si el test no compila, fix antes de commitear (no commitear test rojo).

    4. **Commit:** `test: smoke cascada multi-cat con avere.json real (Phase 4 SEED-02)`.
  </action>
  <verify>
    <automated>node --test tests/domain.test.js && node --test tests/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - **Behavior: `node --test tests/domain.test.js` exits 0** con el nuevo describe block "Phase 4 — multi-categoría cascade with real content" pasando los 2 tests.
    - **Behavior: `node --test tests/*.test.js` exits 0** — total ≥ 128 tests verdes (105 baseline + 21 backup + ≥2 multi-cat).
    - **Source: tests/domain.test.js NO modifica el bloque smoke 30 días existente** (`grep -c "30 day\\|30-day\\|30 días" tests/domain.test.js` mantiene el conteo previo a este task; el nuevo describe es ADDITIVO).
    - **Source: `grep -c 'applyImmediateFailure' tests/domain.test.js`** ≥ 1 (importado + usado en el test multi-cat).
    - **Source: `grep -c 'validateContent' tests/domain.test.js`** ≥ 1 (importado + usado en el segundo test).
    - **Source: imports añadidos sin duplicar:** `grep -c "from 'node:fs'" tests/domain.test.js` ≤ 1 (un único import line; no duplicar si ya existía).
  </acceptance_criteria>
  <done>
    Smoke test multi-cat real ejecuta cascada D-54 sobre 2+ categorías y assertea; smoke validateContent passes sobre avere extendido; ≥ 128 tests verdes; commit `test:` realizado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT humano integral — 5 ROADMAP success criteria + cascada multi-cat visible + flujo backup end-to-end + regression Phase 1-3</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md (§"Phase 4: Backup robusto + contenido completo" — los 5 success criteria literales)
    - .planning/REQUIREMENTS.md (BACK-04, BACK-05, BACK-06, SEED-01, SEED-02 — los 5 IDs Phase 4)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-73..D-88 todos los locks)
    - .planning/phases/04-backup-robusto-contenido-completo/04-01-SUMMARY.md (backup runtime entregado + mini-UAT veredicto; qué retest aquí)
    - .planning/phases/04-backup-robusto-contenido-completo/04-02-SUMMARY.md y 04-03-SUMMARY.md (contenido transcrito — qué categorías esperar en home)
    - content/exercises/avere.json (cruces avere-3XX commiteados en Task 1b — para identificar qué fallar en UAT-5)
  </read_first>
  <what-built>
    Claude prepara el environment para UAT integral (sin tocar código):
    1. Verifica que `node --test tests/*.test.js` exits 0 (≥ 128 tests verdes baseline).
    2. Verifica con helper scripts el estado del contenido:
       - `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0.
       - Para cada categoría nueva: `node scripts/validate-content-fixture.mjs <slug> content/exercises/<slug>.json` exit 0.
       - `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (D-88 sanity).
       - Los 5 cruces obligatorios passing.
    3. Verifica que las 6 categorías + ≥10 ejercicios por categoría existen.
    4. Lista en mensaje claro al autor los 5 criterios ROADMAP a verificar + los pasos exactos para cada uno.

    El autor (humano) ejecuta UAT estructurado:

    **Setup:**
    - `cd /home/vcompanyb/italian-course`
    - `npx serve` (background o terminal aparte)
    - Abrir `http://localhost:3000` en el navegador
    - Abrir DevTools → Application → Local Storage para inspeccionar `italianCourse.v1` durante la sesión

    **Sesión UAT (orden recomendado):**

    **UAT-1 (Criterio ROADMAP 4 — SEED-01):**
    1. Verificar que la home muestra las 6 categorías en orden Avere → Preposiciones → Verbos de movimiento → Sustantivos irregulares → Género y número → Profesiones.
    2. Verificar que NO aparece el banner CONT-05 de error (ningún JSON malformado).
    3. Verificar que cada categoría muestra "X ejercicios" con X ≥ 10 (excepto Avere que tiene ≥17 + multi-cat).
    4. **Pass:** las 6 categorías visibles con ≥10 ejercicios cada una.

    **UAT-2 (Criterio ROADMAP 1 — BACK-04 export):**
    (Nota: en Plan 04-01 Task 3 mini-UAT ya se verificó el flujo básico de export — aquí se retest con contenido completo.)
    1. Click botón "Backup" en home (3er botón del button-row).
    2. Verificar pantalla Backup carga.
    3. Click "Exportar progreso".
    4. Verificar:
       - Navegador descarga un archivo `italian-course-backup-YYYY-MM-DD.json`.
       - Mensaje verde "Progreso exportado. Guarda el archivo en lugar seguro."
       - DevTools → Local Storage → `italianCourse.v1` con `lastBackupAt: "..."` ISO actual.
    5. Abrir el JSON descargado — verificar shape `{kind: "italian-course-backup", exportedAt: "...", schemaVersion: 3, state: {...}}`.
    6. **Pass:** archivo descargado con shape correcto.

    **UAT-3 (Criterio ROADMAP 2 — BACK-05 import + confirmación):**
    1. En la pantalla Backup, click "Importar progreso".
    2. Seleccionar el archivo recién descargado.
    3. Verificar panel `.confirm-inline` aparece con fecha + categorías + ejercicios + warning + botones.
    4. Click "Continuar".
    5. Verificar mensaje verde + retorno a home + state idéntico al exportado.
    6. **Bonus exploit-proof:** seleccionar un .txt renombrado a .json → mensaje rojo "JSON inválido: ...".
    7. **Pass:** idempotencia export→import + error handling en español.

    **UAT-4 (Criterio ROADMAP 3 — BACK-06 banner 7 días):**
    1. DevTools → Local Storage → editar `italianCourse.v1` para fijar `lastBackupAt` a hace 10 días.
    2. Refresh.
    3. Verificar banner ámbar "⚠ Han pasado 10 días desde tu último backup." + botón "Ir a Backup" funciona.
    4. Exportar → volver a home → banner desaparece.
    5. **Bonus null-path:** `lastBackupAt: null` + `firstUsedAt: hace 10 días` → banner "Aún no has exportado tu progreso."
    6. **Bonus future-date:** `lastBackupAt` en el futuro → banner NO aparece.
    7. **Pass:** banner aparece/desaparece reactivo, variantes correctas.

    **UAT-5 (Criterio ROADMAP 5 — SEED-02 cascada multi-cat real):**
    1. En la home, click "Repaso 20".
    2. En el picker, marcar las categorías que cruzan el primer multi-cat de avere.json (e.g. Avere + Profesiones si `avere-300` es el primero).
    3. Lanzar el Repaso.
    4. Anotar mentalmente el estado pre-fallo de las 2 categorías cruzadas.
    5. Durante la sesión, identificar el ejercicio multi-cat. **Fallar deliberadamente**.
    6. Verificar **inmediatamente** (D-54 immediate cascade) en DevTools → Local Storage → `italianCourse.v1`:
       - Ambas categorías cruzadas: `status === 'no-hecha'`, `streakDays === 0`, `clearedExerciseIds === []`.
    7. Completar la sesión.
    8. En el resumen, verificar que ambas categorías aparecen en "categorías tocadas" con transición `hecha→no-hecha`.
    9. **Pass:** cascada D-54 inmediata sobre las N categorías del ejercicio fallado, visible en resumen.

    **UAT-6 (Regression Phase 1-3 smoke):**
    1. Repaso de Avere SOLA → completar acertando todo → resumen muestra promoción o mantenimiento.
    2. Test completo de las 6 categorías → ver advertencia con total de ejercicios → arrancar → cerrar pestaña a medias.
    3. Reabrir la app → banner `.inflight-banner` "Tienes un Test completo a medias" → "Reanudar" funciona.
    4. **Pass:** sin regresiones en flujos Phase 1-3.

    **Veredicto final del autor:**
    - **PASS:** los 5 criterios ROADMAP + regression Phase 1-3 pasan limpios.
    - **NEEDS-PATCH:** uno o más criterios necesitan ajustes menores; documentar en SUMMARY y crear plan 04-05 (gap closure) si emerge.
    - **FAIL:** un criterio bloqueante falla; reabrir el plan correspondiente.
  </what-built>
  <how-to-verify>
    - **Pre-checkpoint (Claude):**
       - `node --test tests/*.test.js` exit 0 (≥ 128 tests verdes).
       - `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0.
       - `node scripts/assert-avere-prefix-unchanged.mjs` exit 0.
       - Los 5 cruces obligatorios passing (5x assert-multi-cat-cross).
       - Para cada slug en {preposiciones, verbos-movimiento, sustantivos-irregulares, genero-numero, profesiones}: `node scripts/validate-content-fixture.mjs <slug> content/exercises/<slug>.json` exit 0.
       - Mensaje al autor con tests count + lista de UAT-1..UAT-6 pasos.
    - **Author:** seguir los UAT-1..UAT-6 marcando cada uno como PASS/FAIL en el mensaje de resume.
    - Capturas opcionales pero recomendadas: screenshots de la home con banners + screenshot del resumen tras cascada multi-cat.
  </how-to-verify>
  <acceptance_criteria>
    - **Behavior: `node --test tests/*.test.js` exits 0** (≥ 128 tests verdes — sin regresión).
    - **Source: las 6 categorías existen** — `ls content/exercises/avere.json content/exercises/preposiciones.json content/exercises/verbos-movimiento.json content/exercises/sustantivos-irregulares.json content/exercises/genero-numero.json content/exercises/profesiones.json` retorna 6 paths.
    - **Behavior: counts mínimos por categoría — cada archivo tiene ≥10 ejercicios:** ejecutar para cada slug `node -e "const c=JSON.parse(require('fs').readFileSync('content/exercises/<slug>.json','utf8')); if(c.exercises.length<10){console.error('FAIL <slug>',c.exercises.length);process.exit(1)}; console.log('OK <slug>',c.exercises.length)"` y confirmar exit 0 para los 6.
    - **Behavior: avere multi-cat ≥6** — `node -e "const c=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); const m=c.exercises.filter(e=>e.categoryIds&&e.categoryIds.length>=2); if(m.length<6)process.exit(1); console.log('multi-cat:',m.length)"` exit 0.
    - **Behavior: los 5 cruces obligatorios cubiertos (W-7 fix):** 5x `node scripts/assert-multi-cat-cross.mjs avere <slug>` exit 0.
    - **Behavior: D-88 invariante mantenido (W-4 fix):** `node scripts/assert-avere-prefix-unchanged.mjs` exit 0.
    - **Behavior: schema validation passes para los 6 archivos (B-4 fix):** 6x `node scripts/validate-content-fixture.mjs <slug> content/exercises/<slug>.json` exit 0.
    - **Human checkpoint:** el autor confirma PASS/NEEDS-PATCH/FAIL para cada UAT-1..UAT-6 explícitamente.
    - **Behavior funcional:** los 5 criterios ROADMAP (export descarga archivo, import idempotente, banner 7d, 6 PDFs ≥10 ejercicios, multi-cat cascada visible) están todos verificados.
  </acceptance_criteria>
  <resume-signal>
    Type:
    - `PASS` si los 6 UATs pasan limpios.
    - `NEEDS-PATCH: <descripción>` si algún detalle es ajustable in-line.
    - `FAIL: <descripción>` si bloqueante.
  </resume-signal>
</task>

</tasks>

<verification>
- Task 1a autonomous (3 helpers + snapshot + propuesta multi-cat sin commit).
- Task 1b human checkpoint (autor revisa los multi-cat ANTES del commit, W-3 fix; commit ocurre tras approval).
- Task 1c autonomous (smoke test multi-cat + commit del test).
- Task 2 human checkpoint (UAT integral 6 puntos).
- Tests: ≥ 128 verdes (≥ 126 tras 04-01 + ≥ 2 multi-cat en Task 1c).
- UAT cierra los 5 ROADMAP criteria. NEEDS-PATCH dispara plan 04-05 (gap closure) si necesario; FAIL reabre plan(es).
- 5 requirements de Phase 4 (BACK-04, BACK-05, BACK-06, SEED-01, SEED-02) marcados Complete tras PASS.
- TODOS los smoke checks usan helper scripts en `scripts/` — cero `node -e` con apóstrofes anidados (B-2 fix anclado).
</verification>

<success_criteria>
- avere.json tiene ≥6 multi-cat exercises cubriendo las 5 nuevas categorías (al menos 1 cruce por nueva categoría, verificable por assert-multi-cat-cross — W-7 fix).
- Los 17 avere existentes INTACTOS, verificable por snapshot+assert structural (W-4 fix).
- Smoke test multi-cat real ejecuta cascada D-54 sobre 2+ categorías y assertea (B-2 fix: test usa imports node, no `node -e` inline con apóstrofes anidados).
- Author review pedagógico explícito de los multi-cat ANTES del smoke test (W-3 fix: Task 1b checkpoint humano).
- Schema validation de los 6 archivos via helper único (B-4 fix anclado).
- UAT humano: los 5 criterios ROADMAP pasan + regression Phase 1-3 sin sorpresas.
- BACK-04, BACK-05, BACK-06, SEED-01, SEED-02 marcables Complete en REQUIREMENTS.md con evidencia documentada en SUMMARY.
</success_criteria>

<output>
Crear `.planning/phases/04-backup-robusto-contenido-completo/04-04-SUMMARY.md` al finalizar:
- Multi-cat exercises añadidos (ids + categorías cruzadas).
- Smoke test multi-cat añadido (nombre describe block).
- Helper scripts creados (los 3 nuevos de Task 1a).
- UAT veredicto por criterio (PASS/NEEDS-PATCH/FAIL).
- Tests count final.
- Phase 4 marcado complete en STATE.md y ROADMAP.md (si PASS); si NEEDS-PATCH, listar los ajustes pendientes.
- Lecciones aprendidas Phase 4 (pedagógicas + técnicas).
- Recomendaciones para milestone close (`/gsd:complete-milestone v1.0`).
- Confirmación de fixes del checker aplicados:
  - B-1 (placeholders obligatorios) — anclado en 04-02 Task 1.
  - B-2 (no heredoc-incompatible commands) — anclado: TODOS los smoke checks usan helper scripts en scripts/.
  - B-3 (mini-UAT antes de contenido) — anclado en 04-01 Task 3.
  - B-4 (helper único validateContent firma real) — anclado en scripts/validate-content-fixture.mjs.
  - B-5 (frontmatter coherente con action) — anclado en 04-01.
  - W-1 (Task 2 04-01 grande) — aceptado (no se split; aceptable tras Task 3 mini-UAT como compensación).
  - W-2 (backupLastMessage clear en requestReturnToHome) — anclado en 04-01 Task 2 step j.
  - W-3 (multi-cat human-verify gate) — anclado en 04-04 Task 1b.
  - W-4 (snapshot+assert en lugar de git diff) — anclado en scripts/snapshot-avere-prefix.mjs + scripts/assert-avere-prefix-unchanged.mjs.
  - W-5 (orden Avere=1) — anclado en 04-02 Task 1 categories.json.
  - W-6 (node-based no-period check) — anclado en 04-02/04-03/04-04.
  - W-7 (node-based JSON parsing para cruces) — anclado en scripts/assert-multi-cat-cross.mjs.
</output>
</content>
</invoke>
