---
phase: 04-backup-robusto-contenido-completo
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - content/exercises/sustantivos-irregulares.json
  - content/exercises/genero-numero.json
  - content/exercises/profesiones.json
autonomous: false
requirements: [SEED-01]
tags: [content, italian, pdf-transcription, match, multiple-choice, word-buttons]

must_haves:
  truths:
    - "content/exercises/sustantivos-irregulares.json contiene ≥10 ejercicios validados (mayoría match singular↔plural irregular + algún multi-choice para casos límite)"
    - "content/exercises/genero-numero.json contiene ≥10 ejercicios validados (mayoría match sustantivo↔artículo + multi-choice para excepciones de plurales)"
    - "content/exercises/profesiones.json contiene ≥10 ejercicios validados (mezcla — multi-choice masculino/femenino + match masc↔fem + 1-2 word-buttons frases completas)"
    - "Las 6 categorías están en la home tras este plan; el autor puede lanzar Repaso de las 6 sin banner CONT-05"
    - "El autor ha revisado pedagógicamente cada una de las 3 categorías ANTES de su commit (D-85)"
    - "El tipo `match` ejercita la regla D-66 (duplicados textuales en columna derecha permitidos, grading por índice de pair) en al menos uno de los nuevos ejercicios de profesiones o género-número (varios sustantivos comparten artículo `la`/`il`)"
  artifacts:
    - path: "content/exercises/sustantivos-irregulares.json"
      provides: "≥10 ejercicios mayoría match singular↔plural irregular (`uomo↔uomini`, `braccio↔braccia`, `uovo↔uova`, `dito↔dita`, etc.), 1-2 multi-choice para casos límite. IDs sustantivos-irregulares-001..NNN. Cero multi-cat aquí."
      contains: "sustantivos-irregulares-200"
    - path: "content/exercises/genero-numero.json"
      provides: "≥10 ejercicios mayoría match (sustantivo↔artículo, singular↔plural regular) + 1-2 multi-choice para excepciones. IDs genero-numero-001..NNN. Cero multi-cat aquí."
      contains: "genero-numero-200"
    - path: "content/exercises/profesiones.json"
      provides: "≥10 ejercicios mezcla: multi-choice masc/fem (`Lui è ___ medico` con artículo), match masc↔fem (`avvocato↔avvocata`), 1-2 word-buttons frases completas (`Yo soy abogada → io sono avvocata`). IDs profesiones-001..NNN. Cero multi-cat aquí — cruces Avere+Profesiones llegan en 04-04."
      contains: "profesiones-001"
  key_links:
    - from: "ejercicios match"
      to: "schema-validator.js PAYLOAD_VALIDATORS.match"
      via: "pairs:[[izq,der],...] 2..10 pares, duplicados en der permitidos D-66"
      pattern: "\"type\": \"match\""
    - from: "tipos del PDF Profesiones"
      to: "schema dispatch table cerrada Phase 3 (multi-choice + word-buttons + match)"
      via: "cero expansion del validator necesaria"
      pattern: "\"type\": \"(match|multiple-choice|word-buttons)\""
---

<objective>
Segundo slice de contenido: transcribir las 3 categorías restantes con mayor complejidad pedagógica (Sustantivos Irregulares, Género y Número, Profesiones). El tipo `match` domina aquí (las tablas del PDF se mapean naturalmente a parejas). **Cada categoría es un commit independiente** tras revisión humana (D-85).

**Purpose:** Cierra SEED-01 (las 5 categorías nuevas listas, ≥10 ejercicios cada una). Tras este plan el autor tiene 6 categorías reales en daily use; falta solo el slice multi-cat (avere extensions en 04-04) para cerrar SEED-02 y el UAT integral en 04-05.

**Output:** 3 archivos nuevos `content/exercises/{sustantivos-irregulares,genero-numero,profesiones}.json`. Cero cambios en código. Cubre la mayor parte de SEED-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md
@.planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md
@.planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md

@content/categories.json
@content/exercises/avere.json
@src/data/schema-validator.js
@src/data/content-loader.js
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF source → Claude transcription | 3 PDFs medianos. Risk de transcripción incorrecta concentrado en este plan. Mitigado por D-85 revisión humana por categoría. |
| Multi-letter Italian words → match pairs | Sustantivos irregulares con plurales raros (`uomo↔uomini`) son alta densidad de typos. NFC normalize + revisión humana defienden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.03-01 | Tampering | Italian incorrectness en match pairs (singular↔plural irregular, masc↔fem) | medium | mitigate | D-85 author review per category. Schema validator bloquea malformación. Profesiones es PDF más complejo (5 tablas, ~50 profesiones) — revisión cuidadosa. |
| T-04.03-02 | Tampering | Duplicate-string trap en columna derecha de match (D-66) | low | mitigate | RESEARCH §G — D-66 permite duplicados con grading por índice de pair. Tests Phase 3 ya cubren. Aplicar conscientemente para "todas estas tienen `la`" en genero-numero o profesiones. |
| T-04.03-03 | Tampering | Apostrophe inconsistency (`'` vs `'`) — más probable en l'amico/l'avvocato | low | mitigate | RESEARCH §D apostrophe landmine. `grep -P "[\\x{2019}]"` pre-commit. |
| T-04.03-SC | Tampering | npm/pip/cargo installs | n/a | mitigate | Plan 04-03 instala CERO paquetes. Solo JSON. |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Transcribir Sustantivos Irregulares a JSON + checkpoint humano</name>
  <files>content/exercises/sustantivos-irregulares.json</files>
  <read_first>
    - material-profesora/Sustantivos Irregulares.pdf (Read tool — PDF largo ~220 KB, 2 páginas; cuatro tablas tematizadas: familia, cuerpo, casos especiales)
    - content/exercises/avere.json (líneas 158-170 match shape como modelo literal)
    - content/exercises/preposiciones.json + content/exercises/verbos-movimiento.json (CREADOS EN PLAN 04-02 — leer para mantener convenciones de IDs y notes)
    - src/data/schema-validator.js (lines 1-110 — match payload requiere `{prompt:string, pairs: [[izq, der]] array de 2..10 pares con strings}`)
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (sección `content/exercises/{5 new}.json` líneas 690-706 match shape + RESEARCH §G match strategy)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§G Match strategy líneas 1183-1190 — 2-10 pares sweet spot 4-7; §B Sustantivos es PDF 3, vocabulario cerrado)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-86 — Sustantivos mayoría match)
  </read_first>
  <what-built>
    Claude:
    1. Lee `material-profesora/Sustantivos Irregulares.pdf`.
    2. Extrae las 4 tablas: familia (`padre/madre`, `figlio/figlia`, `fratello/sorella`), cuerpo (`braccio/braccia`, `dito/dita`, `osso/ossa`), casos especiales (`uomo/uomini`, `uovo/uova`, `dio/dei`, `bue/buoi`), invariables (`città/città`, `caffè/caffè`).
    3. Construye ≥10 ejercicios (apuntar 12-15) distribuidos:
       - **Mayoría match** (ids `sustantivos-irregulares-200..299`): cada ejercicio agrupa 4-7 parejas singular↔plural según RESEARCH §G sweet spot. Ejemplo:
          ```json
          {
            "id": "sustantivos-irregulares-200",
            "type": "match",
            "categoryIds": ["sustantivos-irregulares"],
            "payload": {
              "prompt": "Empareja singular ↔ plural irregular (cuerpo).",
              "pairs": [
                ["braccio", "braccia"],
                ["dito", "dita"],
                ["osso", "ossa"],
                ["ginocchio", "ginocchia"]
              ]
            },
            "notes": "PDF sustantivos-irregulares §cuerpo."
          }
          ```
       - **1-2 multi-choice** (ids `sustantivos-irregulares-001..099`) para casos límite ("¿cuál es el plural de `uovo`?" con options `uovi/uova/uove/uovi`).
       - **Opcional 1 multi-choice "invariables"** ("¿cuál es el plural de `città`?" → correct `città`).
    4. Convenciones de RESEARCH §D + §G:
       - Apostrofes `'` U+0027 (en este PDF no abundan).
       - Capitalización: prompts mayúscula inicial; pairs[][] lowercase (no son nombres propios).
       - **Match pairs MIN 2, MAX 10** (schema enforced). Sweet spot 4-7.
       - NO punctuation en pairs[].
    5. Escribe `content/exercises/sustantivos-irregulares.json`.
    6. Pre-checkpoint verificaciones automáticas.

    Después PAUSA para checkpoint humano:
    El autor revisa:
    - Cada par singular/plural correcto según el PDF.
    - Casos especiales bien etiquetados (uomo→uomini con cambio interno, no "uomos").
    - Tablas agrupadas coherentemente (familia / cuerpo / casos especiales / invariables) — facilita memorización pedagógica.

    Si aprueba: commit `content: add sustantivos-irregulares exercises (Phase 4 SEED-01)`.
  </what-built>
  <how-to-verify>
    1. **Pre-checkpoint (Claude):**
       - `node -e "const c = JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); if (c.exercises.length < 10) process.exit(1); console.log('OK', c.exercises.length)"` exit 0.
       - `grep -P "[\\x{2019}]" content/exercises/sustantivos-irregulares.json` exit 1.
       - **Match pairs en rango 2..10:** `node -e "const c=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); c.exercises.filter(e=>e.type==='match').forEach(e=>{const n=e.payload.pairs.length; if(n<2||n>10){console.error('BAD pairs count',e.id,n);process.exit(1)}})"` exit 0.
       - Schema validation smoke.
       - `node --test tests/*.test.js` exits 0.
    2. **Author review:**
       - **Checklist:**
          - [ ] Plurales irregulares correctos: `uomo→uomini`, `braccio→braccia`, `uovo→uova`, `dito→dita`, `osso→ossa`, `dio→dei`.
          - [ ] Plurales invariables si los incluiste: `città→città`, `caffè→caffè`, `università→università`.
          - [ ] Acentos italianos (`è`, `à`) consistentes — NFC normalize hará su trabajo on load pero el JSON debe estar lo más limpio posible.
          - [ ] Match agrupados con sentido temático (no mezclar todo en una sola tabla de 10 — preferir 2-3 ejercicios de 4-6 pares).
       - Opcional: `npx serve` → Repaso → "Sustantivos irregulares" → completar.
  </how-to-verify>
  <acceptance_criteria>
    - **Source: file exists `content/exercises/sustantivos-irregulares.json`** y JSON.parse exit 0.
    - **Source: ≥10 ejercicios** total.
    - **Source: cada `id` matching `^sustantivos-irregulares-\\d{3}$`** (range 001-099 multi-choice, 200-299 match).
    - **Source: cada `categoryIds === ["sustantivos-irregulares"]`** (sin multi-cat aquí).
    - **Source: mayoría match (D-86) — `grep -c '"type": "match"' content/exercises/sustantivos-irregulares.json` ≥ count('multiple-choice')**.
    - **Source: pairs counts en rango 2..10** (verificable con node-e en `<how-to-verify>` paso 1).
    - **Source: `grep -P "[\\x{2019}]" content/exercises/sustantivos-irregulares.json`** exit 1.
    - **Source: schema validation passes** (smoke node-e exit 0).
    - **Behavior: `node --test tests/*.test.js` exits 0**.
    - **Human checkpoint signal:** "approved".
  </acceptance_criteria>
  <resume-signal>
    Type `approved` / `edit: <desc>` / `manual`.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Transcribir Género y Número a JSON + checkpoint humano</name>
  <files>content/exercises/genero-numero.json</files>
  <read_first>
    - material-profesora/Clase_Italiano_Genero_y_Numero.pdf (Read tool — PDF mediano ~22 KB, 2 páginas; reglas de género/número regular + tablas excepciones + uso de apóstrofes `l'amico`)
    - content/exercises/avere.json (multi-choice + match shapes)
    - content/exercises/sustantivos-irregulares.json (CREADO EN TASK 1 — match patterns reusables)
    - src/data/schema-validator.js (lines 1-110)
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (líneas 643-706)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§G Match strategy + §D apostrophes en `l'amico`/`l'avvocata`)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-86 — Género/Número mayoría match)
  </read_first>
  <what-built>
    Claude:
    1. Lee `material-profesora/Clase_Italiano_Genero_y_Numero.pdf`.
    2. Extrae:
       - **Reglas regular:** masc `-o`/`-e`/`-i`; fem `-a`/`-e`/`-i`; plural masc `-i`; plural fem `-e`.
       - **Artículos determinados:** `il` (masc cons), `lo` (masc s+cons/z), `l'` (vocal), `la` (fem cons), `i`/`gli`/`le` (plurales).
       - **Excepciones:** `-essa` para profesiones femeninas (`dottore→dottoressa`), invariables, palabras en `-ema/-ista` masculinas.
       - Apostrofes `l'amico`, `l'aula` — usar U+0027 (`l'amico`).
    3. Construye ≥10 ejercicios distribuidos:
       - **Mayoría match** (`genero-numero-200..299`): sustantivo↔artículo (`[["ragazzo","il"], ["ragazza","la"], ["amico","l'"], ["studente","lo"]]`) — **D-66 duplicados en derecha posibles** si varios sustantivos comparten artículo (`[["donna","la"], ["casa","la"], ["porta","la"]]` con 3 items `"la"` en derecha — grading consume por índice).
       - **Match singular↔plural regular:** `[["libro","libri"], ["casa","case"], ["amico","amici"]]`.
       - **1-2 multi-choice** (`genero-numero-001..099`) para excepciones: "¿cuál es el plural de `re`?" → invariable.
    4. Convenciones:
       - Apostrofes consistentes `'` U+0027. **Si el PDF usa `l'` con apóstrofe tipográfico, normalizar al ASCII.**
       - Capitalización lowercase salvo nombres propios.
       - Match pairs 2..10, sweet spot 4-6.
    5. Pre-checkpoint verificaciones.

    PAUSA para checkpoint humano:
    Author review:
    - Artículos correctos (`l'aula` no `la aula`; `lo zaino` no `il zaino`).
    - Excepciones bien identificadas (`-ema/-ista` masculinas, `mano` femenina).
    - Si hay D-66 duplicados intencionales en match, verificar visualmente que se grading correctamente (Phase 3 lo cubre).

    Si aprueba: commit `content: add genero-numero exercises (Phase 4 SEED-01)`.
  </what-built>
  <how-to-verify>
    1. **Pre-checkpoint (Claude):**
       - JSON.parse exit 0.
       - `grep -P "[\\x{2019}]" content/exercises/genero-numero.json` exit 1.
       - Pairs counts 2..10.
       - Schema smoke.
       - `node --test tests/*.test.js` exits 0.
       - **Apostrophe check:** `grep -c "l'" content/exercises/genero-numero.json` esperar ≥ 2 (probable que aparezcan al menos `l'amico`/`l'aula`) y ninguno usa U+2019.
    2. **Author review checklist:**
       - [ ] Artículos correctos por género + sonido inicial.
       - [ ] Apóstrofes ASCII (`'`).
       - [ ] Excepciones del PDF cubiertas (al menos 2-3).
       - [ ] Match pairs balanceados (4-6 sweet spot).
       - [ ] D-66 duplicados intencionales (si los hay) tienen sentido pedagógico.
       - Opcional: `npx serve` → Repaso "Género y número" → completar.
  </how-to-verify>
  <acceptance_criteria>
    - **Source: file exists `content/exercises/genero-numero.json`** y JSON.parse exit 0.
    - **Source: ≥10 ejercicios**.
    - **Source: cada `id` matching `^genero-numero-\\d{3}$`** (range 001-099 multi-choice, 200-299 match).
    - **Source: cada `categoryIds === ["genero-numero"]`**.
    - **Source: mayoría match** — count match ≥ count multi-choice.
    - **Source: pairs counts en 2..10**.
    - **Source: `grep -P "[\\x{2019}]" content/exercises/genero-numero.json`** exit 1.
    - **Source: schema validation passes**.
    - **Behavior: `node --test tests/*.test.js` exits 0**.
    - **Human checkpoint signal:** "approved".
  </acceptance_criteria>
  <resume-signal>
    Type `approved` / `edit: <desc>` / `manual`.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Transcribir Profesiones a JSON + checkpoint humano (PDF más complejo, mezcla 3 tipos)</name>
  <files>content/exercises/profesiones.json</files>
  <read_first>
    - material-profesora/Professioni_Italiano_Facile.pdf (Read tool — PDF mediano ~20 KB, 3 páginas; CINCO tablas amplias ~50 profesiones; mezcla más rica de tipos)
    - content/exercises/avere.json (los 3 shapes son necesarios aquí — múltiple-choice + word-buttons + match)
    - content/exercises/preposiciones.json + verbos-movimiento.json + sustantivos-irregulares.json + genero-numero.json (analogs creados en planes 04-02 y 04-03 Tasks 1+2)
    - src/data/schema-validator.js
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (todas las shapes)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§D apostrophes en `l'avvocato`, §E distractoras, §F word-buttons, §G match)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-86 — Profesiones es mezcla rica)
  </read_first>
  <what-built>
    Claude:
    1. Lee `material-profesora/Professioni_Italiano_Facile.pdf`.
    2. Extrae las 5 tablas de profesiones (~50 entries): masc/fem, regular endings (-tore/-trice, -o/-a, -e/-essa), invariables (-ista).
    3. Construye ≥10 ejercicios (apuntar 12-15) MEZCLA RICA per D-86 — este es el PDF más diverso del corpus:
       - **Multi-choice masc/fem + artículo** (`profesiones-001..099`): "`Lui è ___ medico`" → options `il/la/lo/l'`, correct depende del sonido inicial. O "¿cuál es el femenino de `dottore`?" → options `dottora/dottoressa/dotrice/dottore`, correct `dottoressa`.
       - **Match masc↔fem** (`profesiones-200..299`): `[["avvocato","avvocata"], ["maestro","maestra"], ["medico","medico"]]` (último invariable). O `[["dottore","dottoressa"], ["professore","professoressa"], ["studente","studentessa"]]` (-essa family).
       - **Word-buttons frases completas** (`profesiones-100..199`): 1-2 ejercicios. Ejemplo del PDF: `prompt: "Yo soy abogada"`, `answer: ["io", "sono", "avvocata"]`, `distractors: ["avvocato", "siamo"]`. Otro: `prompt: "Ella es médica"`, `answer: ["lei", "è", "medico"]` (medico invariable).
    4. **Apostrofes** `l'avvocato`/`l'avvocata`/`l'infermiere`: ASCII `'` U+0027.
    5. **Distractoras pedagógicas:**
       - Multi-choice masc/fem: distractoras del mismo grupo gramatical (otros artículos válidos pero incorrectos por género/sonido).
       - Word-buttons distractors: 1-2 tokens del mismo género gramatical que NO entran en la respuesta correcta.
    6. **NO multi-cat aquí** — los cruces Avere+Profesiones llegan en 04-04 (`avere-3XX`).
    7. Convenciones RESEARCH §D/E/F/G aplicadas.
    8. Pre-checkpoint verificaciones.

    PAUSA para checkpoint humano:
    Author review (este es el más cuidadoso porque hay mayor variedad):
    - Femeninos correctos según ending del masc: `-o→-a` (avvocato→avvocata), `-tore→-trice` (attore→attrice), `-e→-essa` (dottore→dottoressa), `-ista` invariable (artista→artista).
    - Artículos correctos por sonido inicial (`l'avvocato`, `lo zucchero`).
    - Word-buttons: tokens correctos sin punto, distractoras plausibles.
    - Match pairs coherentes y agrupados temáticamente (e.g., uno con familia `-essa`, otro con `-tore/-trice`).

    Si aprueba: commit `content: add profesiones exercises (Phase 4 SEED-01)`.
  </what-built>
  <how-to-verify>
    1. **Pre-checkpoint (Claude):**
       - JSON.parse exit 0.
       - `grep -P "[\\x{2019}]" content/exercises/profesiones.json` exit 1.
       - Pairs counts 2..10.
       - Schema smoke validation.
       - `node --test tests/*.test.js` exits 0.
       - **Type mix check:** `grep -c '"type": "multiple-choice"' content/exercises/profesiones.json` ≥ 2 y `grep -c '"type": "match"' content/exercises/profesiones.json` ≥ 2 y `grep -c '"type": "word-buttons"' content/exercises/profesiones.json` ≥ 1 — confirma mezcla rica D-86.
       - **Word-buttons sin punto final:** `grep -E '"answer": \\[[^]]*"\\.[^"]*"' content/exercises/profesiones.json` exit 1.
    2. **Author review checklist:**
       - [ ] Femeninos correctos por familia (-o/-a, -tore/-trice, -e/-essa, -ista invariable).
       - [ ] Artículos por sonido inicial.
       - [ ] Apóstrofes ASCII.
       - [ ] Mezcla de los 3 tipos (≥2 multi-choice + ≥2 match + ≥1 word-buttons).
       - [ ] Word-buttons answer[] sin puntos.
       - [ ] Distractoras pedagógicas válidas.
       - Opcional: `npx serve` → Repaso "Profesiones" → completar.
  </how-to-verify>
  <acceptance_criteria>
    - **Source: file exists `content/exercises/profesiones.json`** y JSON.parse exit 0.
    - **Source: ≥10 ejercicios**.
    - **Source: cada `id` matching `^profesiones-\\d{3}$`** (ranges 001-099 / 100-199 / 200-299).
    - **Source: cada `categoryIds === ["profesiones"]`** (sin multi-cat — llegará en 04-04).
    - **Source: mezcla 3 tipos** — count(multi-choice) ≥ 2, count(match) ≥ 2, count(word-buttons) ≥ 1.
    - **Source: word-buttons sin punto final en `answer[]`**.
    - **Source: `grep -P "[\\x{2019}]" content/exercises/profesiones.json`** exit 1.
    - **Source: schema validation passes**.
    - **Behavior: `node --test tests/*.test.js` exits 0**.
    - **Human checkpoint signal:** "approved".
  </acceptance_criteria>
  <resume-signal>
    Type `approved` / `edit: <desc>` / `manual`.
  </resume-signal>
</task>

</tasks>

<verification>
- 3 tasks secuenciales (orden: sustantivos → genero-numero → profesiones — más simple a más complejo).
- Cada task: 1 archivo nuevo + 1 commit tras human-verify.
- Total commits esperados: 3.
- Tests baseline 126 siguen verdes.
- App `npx serve` muestra las 6 categorías sin banner CONT-05 — verificable por autor antes del último checkpoint.
</verification>

<success_criteria>
- 3 archivos nuevos creados, cada uno ≥10 ejercicios validados por schema y autor-aprobados.
- SEED-01 cubierto 5/5 (las 5 categorías nuevas tienen ≥10 ejercicios cada una).
- Las 6 categorías visibles en home con ejercicios reales utilizables.
- Cero multi-cat aún — SEED-02 cubierto en 04-04.
</success_criteria>

<output>
Crear `.planning/phases/04-backup-robusto-contenido-completo/04-03-SUMMARY.md` al finalizar:
- Counts de ejercicios por tipo y categoría.
- Commits per categoría.
- Iteraciones del autor (cambios pedidos, si los hubo).
- Apuntes pedagógicos relevantes para 04-04 (cruces multi-cat candidatos identificados durante la transcripción).
- Open items para Plan 04-04: avere multi-cat extension + smoke test multi-cat real + UAT.
</output>
