---
phase: 42-fare-congiuntivo-4-slots-hom-grafas-disparador
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - content/exercises/fare-congiuntivo.json
  - tests/content-fare-congiuntivo.test.js
autonomous: true
requirements: [CONG-03, CONG-04]

estimate:
  tokens: 98000
  raw_tokens: 98000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- CONG-03 / D-42-02 : los dos compuestos, eje = las 6 personas del auxiliar ---
    - "El slot `fare-congiuntivo-passato` tiene EXACTAMENTE 6 variantes y sus 6 keys son, en orden de persona io/tu/lui-lei/noi/voi/loro, `abbia fatto`, `abbia fatto`, `abbia fatto`, `abbiamo fatto`, `abbiate fatto`, `abbiano fatto` — las 6 personas del auxiliar `avere` en congiuntivo presente, con `fatto` invariable y la key repetida tres veces a propósito (CONG-03, D-42-02, D-42-05, SC-3)."
    - "El slot `fare-congiuntivo-trapassato` tiene EXACTAMENTE 6 variantes y sus 6 keys son, en orden de persona, `avessi fatto`, `avessi fatto`, `avesse fatto`, `avessimo fatto`, `aveste fatto`, `avessero fatto` — las 6 personas del auxiliar en congiuntivo imperfetto, con la key repetida en io y tu (CONG-03, D-42-02, D-42-05, SC-3)."
    - "En los dos slots el marco de concordancia con la oración principal COVARÍA sobre el mismo eje único de persona: los 6 marcos de cada slot son distintos entre sí, así que el autor no puede aprender el slot por la forma de la principal en vez de por la concordancia (D-42-02)."
    # --- SC-3 : el criterio más restrictivo de la fase ---
    - "En las 12 variantes de `passato` y `trapassato` las 3 distractoras de cada variante son formas de SUBJUNTIVO de esa MISMA persona — el otro compuesto más los dos simples — y hay CERO formas de indicativo entre las distractoras: SC-3 dice literalmente «distractoras que son el OTRO tiempo del subjuntivo (no otro modo)» y la verificación lo lee al pie de la letra (D-42-09, SC-3)."
    - "El gate de cero-indicativo se comprueba sobre las **distractoras** (`options` menos `options[correctIndex]`) y en positivo: cada distractora pertenece al conjunto cerrado de 3 formas de congiuntivo de la persona de esa variante. La formulación es deliberada: la key `abbiamo fatto` de la variante de `noi` es a la vez congiuntivo passato y passato prossimo de indicativo — la misma cadena —, así que un gate de ausencia de indicativo sobre `options` completo sería insatisfacible (D-42-09; edge: homograph boundary)."
    - "El marco de concordancia de cada una de las 12 variantes excluye limpiamente los DOS simples: en `passato`, principal en presente más un marcador temporal de acción ya completada, que hace imposibles el congiuntivo presente y el imperfetto; en `trapassato`, principal en pasado más un marcador de anterioridad respecto a ese punto, que hace imposibles el congiuntivo presente y el passato y deja el imperfetto como simultaneidad y no anterioridad. Sin ese blindaje `faccia` y `facessi` serían defendibles y la variante quedaría injusta (D-42-09, CONG-03)."
    # --- CONG-04 / D-42-01 / D-42-12 : el 5º slot ---
    - "El slot `fare-congiuntivo-disparador` existe con id literal `fare-congiuntivo-disparador`, `type: \"multiple-choice\"`, `categoryIds: [\"fare-congiuntivo\"]` y EXACTAMENTE 6 variantes cuyo eje de variante es **el disparador**, no la persona — un único eje por slot igual que en los otros cuatro, así que `pickVariantIndex` (`src/domain/session.js:232`, que solo lee `slot.variants.length`) lo sirve sin una línea de motor nueva (CONG-04, D-42-01, SC-4)."
    - "Los 6 disparadores del slot son distintos entre sí, aparece exactamente uno por prompt, y cubren los cuatro que CONG-04 y SC-4 nombran — un verbo de opinión del tipo `penso che`, `benché`, `prima che` y el `se` hipotético — más un disparador de necesidad o voluntad y el de contraste (CONG-04, SC-4)."
    - "EXACTAMENTE UNA de las 6 variantes es la variante de contraste cuya respuesta correcta es el INDICATIVO, con un verbo de certeza del tipo `so che` como disparador: SC-4 exige «al menos una» y esta fase pone exactamente una, lo que mantiene la categoría siendo lo que dice ser; subir a dos es una edición local posterior (D-42-01, SC-4)."
    - "En las 6 variantes del slot las 4 opciones son las 4 casillas REALES de la persona del hueco — congiuntivo presente, indicativo presente, congiuntivo imperfetto e indicativo imperfetto de esa persona — y NINGUNA opción es forma inventada: el autor decide modo Y tiempo, y solo el disparador más el marco determinan la casilla (D-42-12, SC-4)."
    - "La variante del `se` hipotético lleva la oración principal en condizionale de un verbo que NO es `fare`, de modo que el periodo hipotético queda bien construido, el disparador se examina de verdad y ninguna casilla de Phase 43 entra en el fichero (D-42-16, SC-4)."
    # --- D-42-06 : el gate HARD, también en los 3 slots de este plan ---
    - "En las 18 variantes de este plan el sujeto de la oración principal NUNCA coincide con el sujeto del hueco: o el disparador es impersonal y no hay sujeto de principal que coincidir, o la principal lleva un sujeto explícito distinto del pronombre del hueco. Con las 12 de 42-01, el gate se cumple en las 30 (D-42-06)."
    - "Los 18 `prompt` contienen el hueco literal `___` y el sujeto del hueco como pronombre explícito; en los dos slots del paradigma ese pronombre es el de la persona de la variante, y en el slot del disparador es el de la persona cuyas 4 casillas forman las `options` (D-42-18, D-42-12)."
    # --- D-42-05 / D-42-07 : las otras 5 homógrafas ---
    - "Las 5 variantes homógrafas restantes de la fase viven en este plan — `abbia fatto` en io/tu/lui-lei y `avessi fatto` en io/tu — y se diferencian por disparador distinto y objeto literal distinto. En `passato` y `trapassato` los conjuntos de `options` de io y tu son necesariamente IDÉNTICOS, porque D-42-09 determina las 3 distractoras por completo a partir de la persona y las dos personas comparten las cuatro formas: el diferenciador es el disparador, el objeto y el pronombre explícito, no el conjunto de opciones (D-42-05, D-42-07, D-42-09)."
    # --- D-42-14 : la dosificación, incluida la ausencia deliberada ---
    - "La explanation de `fare-congiuntivo-disparador` DESARROLLA el error de interferencia con el par explícito (`penso che` más congiuntivo en italiano frente a «pienso que» más indicativo en español) y lista qué disparadores italianos exigen subjuntivo donde el español no lo exige; y las explanations de `passato` y `trapassato` NO lo repiten, porque su eje es la anterioridad y no el modo — es dosificación deliberada, no inconsistencia editorial (D-42-14)."
    # --- D-42-13 : 0-gloss del verbo, gloss léxico de la conjunción ---
    - "Ninguno de los 18 `prompt` lleva gloss sobre la forma verbal ni la subcadena `espa`; el único paréntesis admitido contiene una de las 4 glosas léxicas de conjunción del conjunto cerrado (`aunque`, `siempre que`, `antes de que`, `a pesar de que`), va inmediatamente detrás de la conjunción glosada y nunca junto a una forma de `fare` ni de `avere` (D-42-13)."
    # --- D-42-11 : blacklist, por campo y por coincidencia exacta ---
    - "Un escaneo por campo con coincidencia EXACTA sobre `variants[].options[]` de los 5 slots devuelve 0 coincidencias para las formas atestiguadas heredadas, para las trampas nuevas de esta categoría (`facci`, `facciam`, `facce`) y para las casillas de Phase 43 (la familia del condizionale y el imperativo apostrofado); y ninguna opción contiene un participio concordado (`fatta`, `fatti`, `fatte`), porque la concordancia es el magnet de Phase 43 (D-42-11, D-42-15)."
    # --- el fichero de test como invariante permanente ---
    - "`tests/content-fare-congiuntivo.test.js` existe, corre dentro del glob de la suite en verde, y convierte en aserciones permanentes los gates de la categoría en 13 bloques `describe`: estructura y conteos, paradigma completo de las 30 keys con homografía deliberada, sujeto explícito y no-correferencia, 0-gloss del verbo con la excepción de conjunción, SCOPE-GATE léxico, blacklist por campo, los tres patrones de distractoras en tres bloques, disparadores y marcos distintos por variante, canon editorial e higiene del JSON, audit trail de validación con la ronda EXTRA condicionada, y registro de la categoría (D-42-17, SC-5)."
    - "El fichero de test implementa las TRES desviaciones deliberadas respecto del análogo `tests/content-fare-indicativo.test.js`, cada una con su comentario y su decision-id: el assert de unicidad de keys dentro de un slot NO se clona y se sustituye por igualdad con la tabla `CANON` (D-42-05); el assert de «ningún prompt lleva paréntesis» se reescribe a un set cerrado de glosas de conjunción (D-42-13); y el assert de «exactamente un pronombre por prompt» se reescribe, porque la no-correferencia obliga a dos sujetos explícitos en varias variantes (D-42-06)."
    - "Los escaneos de ausencia del fichero de test van SIEMPRE por campo (`variants[].prompt` y `variants[].options[]`) y por coincidencia exacta, nunca sobre el fichero completo ni sobre `notes`, y la cabecera del test lo declara con su razón: el `notes` nombra a propósito cada forma de la blacklist, y la forma corta atestiguada de 3ª singular del passato remoto es prefijo de tres formas legítimas de esta categoría (D-42-11)."
    - "El gate de la ronda EXTRA está CONDICIONADO al estado: para los 4 slots del paradigma, si `validation.status` es `validated` entonces al menos un `by` empieza por `deepseek`; si es `pending` el bloque pasa. Es lo que permite que el test entre en verde antes de que corra el quórum top-level, e impide a la vez que un `validated` sin ronda extra pase por bueno (D-42-08, D-42-04)."
    - "El fichero de test importa `deriveStatus` de `../src/data/validation-state.js` y no reimplementa sus reglas, y exige `status === deriveStatus(passes)` en los 5 slots más las condiciones de `validated` (≥2 `correcta`, ≥2 `by` distintos, cero `incorrecta`) (T-42-03)."
    - statement: "El fichero de test tiene dientes: mutado en 5 sentidos sobre una copia temporal fuera del repo — un gloss del verbo en un prompt, una distractora de indicativo en el slot de passato, una forma de la blacklist como opción, dos sujetos coincidentes en una variante, y un `status: \"validated\"` con `passes: []` — se pone rojo en los 5 casos."
      verification: backstop
    # --- D-42-04 : hand-off ---
    - "Al cerrar este plan los 5 slots llevan `validation` con `status: \"pending\"` y `passes: []`, y el fichero de test está en verde con ese estado: el quórum base canónico Opus+Sonnet y la ronda EXTRA DeepSeek de D-42-08 corren en la pasada TOP-LEVEL posterior, un ejercicio por contexto y NUNCA batched (D-42-04, VAL-03)."
    - "`VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 5 slots `pending`, y ninguna otra categoría cambia de estado: ese rojo es el marcador honesto del trabajo pendiente de la fase, porque el reporter queda CIEGO al fichero nuevo y sigue reportando su total anterior en PASS hasta Phase 44 / INT-02 (D-42-04)."
    # --- conteos finales de la fase ---
    - "Al cerrar este plan el fichero tiene 5 slots y 30 variantes, los 5 ids son literalmente `fare-congiuntivo-presente`, `fare-congiuntivo-imperfetto`, `fare-congiuntivo-passato`, `fare-congiuntivo-trapassato` y `fare-congiuntivo-disparador`, los 5 son `multiple-choice` con `options` de 4 y `categoryIds` de longitud 1, y ningún id termina en sufijo numérico de 3 cifras: el espacio `fare-congiuntivo-300`+ queda libre para Phase 44 / INT-03 (D-42-01, D-42-03, D-42-18; edge: count boundary, id-space boundary)."
    # --- valla del motor ---
    - "`git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0 al cierre de la fase: el motor v1.4 queda byte-intacto, incluido el slot de eje nuevo, que no necesita una sola línea (Out of Scope literal del milestone, SC-5)."

  artifacts:
    - "`content/exercises/fare-congiuntivo.json` — completado a 5 slots y 30 variantes: `fare-congiuntivo-passato` (6), `fare-congiuntivo-trapassato` (6) y `fare-congiuntivo-disparador` (6) añadidos a los 2 de 42-01, los 5 en `validation.status: \"pending\"` con `passes: []`, más las correcciones de autoría que este plan descubra anotadas en `notes` con su fecha."
    - "`tests/content-fare-congiuntivo.test.js` — fichero NUEVO, cero dependencias, 13 bloques `describe` que congelan los gates de la categoría como invariantes permanentes; importa `deriveStatus` del código real; declara en la cabecera la advertencia de escaneo por campo y las tres desviaciones respecto del análogo."

  key_links:
    - "el marco de concordancia del `prompt` → la exclusión de los dos simples en `passato` y `trapassato`: es el único mecanismo que hace que `faccia` y `facessi` no sean defendibles. Si el marco no fija el punto temporal de la principal y la anterioridad del subordinado, la variante tiene dos respuestas y la cascada D-54 resetea la categoría entera."
    - "el disparador del `prompt` → la casilla correcta en el slot del disparador: las 4 opciones son las 4 casillas reales, así que el prompt es el ÚNICO desambiguador. Un disparador ambiguo convierte la variante en una moneda al aire con apariencia de ejercicio."
    - "`fare-congiuntivo-disparador` → `pickVariantIndex` (`src/domain/session.js:232`): la función solo lee `slot.variants.length` y es axis-agnostic, así que un slot cuyo eje es el disparador funciona igual que uno de eje-persona. Es lo que hace que el 5º slot no cueste una línea de motor (D-42-01)."
    - "`tests/content-fare-congiuntivo.test.js` → `deriveStatus` (`src/data/validation-state.js:32-43`): el test importa la función real en vez de reimplementar las reglas, de modo que si D-VAL-07 cambia el test lo sigue. `deriveStatus([])` da `pending`, que es lo que hace verde el bloque 12 antes del quórum."
    - "`EXTRA_ROUND_SLOTS` del test → los 4 slots del paradigma: el audit trail del quórum se agrega a nivel de SLOT, no de variante, así que la ronda EXTRA sobre las 10 variantes homógrafas se materializa como un `by` `deepseek-*` en esos 4 slots. El slot del disparador NO lleva ronda extra (D-42-08)."
    - "`CATEGORIES_WITH_EXPLANATIONS` (línea añadida en 42-01) → el gate VAL-07 de `tests/exercise-types.test.js`: la misma línea que activa los sub-tests editoriales activa el gate estricto, que es el marcador del hand-off. Este plan no la toca."
    - "el conjunto cerrado de 7 objetos literales → el SCOPE-GATE HARD heredado: mantiene fuera las perífrasis y, de paso, elimina el léxico verbal que habría que glosar, lo que refuerza el 0-gloss del verbo."

  prohibitions:
    - "MUST NOT dar de alta una variante en la que más de una opción sea una respuesta defendiblemente correcta para ese prompt: en `passato` y `trapassato` el riesgo es concreto y está declarado — si el marco no excluye los dos simples, `faccia` o `facessi` son correctas y el ejercicio le enseña al autor que su respuesta correcta es incorrecta, con reset de la categoría entera por la cascada D-54."
    - "MUST NOT examinar vocabulario que el autor no ha estudiado sin glosarlo: fallar el slot del disparador por no conocer `benché` o `purché` — no por no entender qué rige el subjuntivo — es exactamente la injusticia que el gloss léxico de D-42-13 existe para evitar."
    - "MUST NOT escribir `validation.status: \"validated\"` ni ninguna entrada de `passes[]` que no venga de un pase de modelo real, ni marcar como hecha la ronda EXTRA DeepSeek sin haberla corrido: el audit trail es la única evidencia que el autor tiene de que una variante fue revisada."
    - "MUST NOT presentar una forma regional, dialectal, arcaica o de registro literario como si fuera la norma del italiano estándar, ni en un sentido ni en el otro: el autor no puede distinguirlas y la interiorizaría como regla."
    - "MUST NOT escribir un test cuyas aserciones no tengan dientes — un gate que pasa igual con el contenido correcto y con el incorrecto da una falsa sensación de red y es peor que no tenerlo, porque el autor deja de mirar."
    - "MUST NOT reducir el alcance de una decisión de 42-CONTEXT.md a una versión «v1», «simplificada» o «pendiente de cablear»: en particular el 5º slot del disparador y su variante de contraste son entregables completos de esta fase, no un esbozo."
    - "MUST NOT renombrar el slug `fare-congiuntivo` ni ningún id de slot: los ids viven en el `exerciseStats` real del autor y en `RESET_PREFIXES_V13`, y un renombrado deja su progreso huérfano en silencio."
    - "MUST NOT introducir ninguna dependencia, `package.json`, paso de build ni proceso que haya que arrancar: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node (CLAUDE.md §Constraints)."
    - "MUST NOT tocar el motor v1.4 (`src/screens/app.js`, `src/domain/`, `src/data/`): el slot de eje-disparador cabe en `pickVariantIndex` tal cual, y si pareciera necesitar una línea nueva es el diseño del contenido lo que está mal (Out of Scope literal del milestone)."
    - "MUST NOT autorar cruces multi-categoría ni usar el espacio de ids `fare-congiuntivo-300`+: son de Phase 44 / INT-03, y la variante de contraste en indicativo NO es un cruce (D-42-03)."
    - "MUST NOT sincronizar `CATEGORIES` de `scripts/run-validation-271.mjs` ni `REAL_CATEGORIES` de la fixture de integración ni `TOTAL_EXPECTED` ni la fórmula del baseline-guard: son el entregable literal de Phase 44 / INT-02."
    - "MUST NOT introducir en `options` ninguna forma del condizionale ni del imperativo de `fare`, ni mencionar el imperativo en una explanation: son casillas declaradas de Phase 43 y adelantarlas colisionaría con su magnet (D-42-15, D-42-16)."
    - "MUST NOT arreglar de paso ninguna discrepancia de conteo preexistente de `genero-numero` ni de `preposiciones` (D-35-08 / D-40-12)."
---

<objective>
Completar `content/exercises/fare-congiuntivo.json` de 2 slots a **5**, y convertir los gates de la
categoría en invariantes permanentes de la suite.

Tres tareas: (1) los dos COMPUESTOS `fare-congiuntivo-passato` y `fare-congiuntivo-trapassato`, 12
variantes cuyo eje son las 6 personas del auxiliar con el marco de concordancia covariando — donde vive
el criterio más restrictivo de la fase, SC-3: **cero indicativo entre las distractoras**, justo donde el
compuesto de indicativo es la distractora más tentadora; (2) el 5º slot **`fare-congiuntivo-disparador`**,
6 variantes cuyo eje es el DISPARADOR y no la persona, con las 4 casillas reales modo x tiempo como
opciones y **exactamente una** variante de contraste cuya respuesta correcta es el indicativo; y (3)
`tests/content-fare-congiuntivo.test.js`, 13 bloques `describe`.

Cubre CONG-03 (passato y trapassato, con el tiempo elegido por concordancia con la principal) y CONG-04
(el disparador del subjuntivo, para que la casilla no se responda por reconocimiento de forma).

Purpose: aquí se paga el precio de las dos decisiones más caras de la fase. SC-3 obliga a que el marco
de cada prompt excluya limpiamente los dos simples — si no lo hace, `faccia` y `facessi` son defendibles
y la variante queda injusta —, y ese blindaje es el trabajo fino que D-42-09 deja abierto. Y el fichero
de test es lo que impide que una pasada futura «arregle» el gloss de conjunción, quite el sujeto de una
homógrafa o meta un indicativo en los compuestos sin que nadie lo vea.

Output: el fichero con 5 slots y 30 variantes en `validation.status: "pending"`, y
`tests/content-fare-congiuntivo.test.js` en verde dentro del glob de la suite. Ningún fichero de `src/`
cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-CONTEXT.md
@.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-PATTERNS.md
@.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-01-SUMMARY.md
</context>

## Frontera de este plan (y de la fase)

Lo que este plan NO hace, declarado para que la verificación no lo busque:

- **Cruces multi-categoría** (`fare-congiuntivo-300`+) → Phase 44, INT-03. La variante de contraste en
  indicativo **no es un cruce** (D-42-03): es un test de reconocimiento de disparador *dentro* del
  congiuntivo, donde el indicativo aparece como respuesta y no como tema examinado. La lista de cruces
  de INT-03 queda cerrada y el cruce congiuntivo ↔ indicativo no existe.
- **Sync de counts**: `CATEGORIES` del reporter, `REAL_CATEGORIES` de la fixture de integración,
  `TOTAL_EXPECTED` y la fórmula del baseline-guard → Phase 44, INT-02.
- **La línea de `CATEGORIES_WITH_EXPLANATIONS`** — la puso 42-01; este plan no toca
  `tests/exercise-types.test.js`.
- **El motor v1.4.** `git diff src/screens/app.js src/domain/ src/data/` queda vacío al cierre,
  **incluido el slot de eje nuevo**: `pickVariantIndex` es axis-agnostic (solo lee
  `slot.variants.length`), así que un eje «disparador» funciona igual que un eje «persona».
- **Un eje de variante nuevo en el motor** — Out of Scope literal del milestone. El slot del disparador
  tiene UN solo eje, como los otros cuatro; lo que cambia es su *significado*, no el primitivo.
- **El condizionale y el imperativo de `fare`** — casillas declaradas de Phase 43. El `se` hipotético se
  resuelve con la principal en condizionale de OTRO verbo (D-42-16), y ninguna explanation menciona el
  imperativo (D-42-15).
- **Perífrasis y modismos de `fare`** — SCOPE-GATE HARD heredado.
- **El quórum base canónico Opus+Sonnet y la ronda EXTRA DeepSeek** — no corren dentro del executor
  (D-42-04). Ver §Hand-off.

## Precisiones de plan-time (verificadas en el codebase el 2026-08-06)

Cuatro puntos donde la letra de 42-CONTEXT.md o del análogo necesita una precisión sobre hechos
comprobados. Ninguna cambia una decisión; todas evitan un gate insatisfacible.

**1. El gate de cero-indicativo de SC-3 se formula EN POSITIVO y sobre las distractoras, no como
ausencia sobre `options`.** `abbiamo fatto` es la misma cadena en congiuntivo passato de `noi` y en
passato prossimo de indicativo de `noi` — y es la KEY de la variante de `noi`. Un gate del tipo «ninguna
opción de estos dos slots es una forma de indicativo» sería por tanto insatisfacible. Formulación
correcta, y más fuerte: **cada una de las 3 distractoras pertenece al conjunto cerrado de 3 formas de
congiuntivo de la persona de esa variante** (el otro compuesto, el congiuntivo presente y el congiuntivo
imperfetto). Eso implica cero indicativo por construcción y además comprueba el patrón completo de
D-42-09 en una sola aserción.

**2. En `passato` y `trapassato`, io y tu tienen necesariamente el MISMO conjunto de `options`.**
D-42-09 determina las 3 distractoras por completo a partir de la persona, y io y tu comparten las cuatro
formas implicadas (`abbia fatto`, `avessi fatto`, `faccia`, `facessi`). Consecuencia: el gate de
«conjuntos de opciones distintos dos a dos entre variantes que comparten key» de D-42-07 se aplica a
`presente` e `imperfetto`, y **NO** a `passato` ni `trapassato`. Ahí los diferenciadores declarados son
el disparador, el objeto literal y el pronombre sujeto explícito. Anotarlo en `notes` como corrección de
autoría con su fecha, para que una pasada futura no lo lea como descuido.

**3. Dos asserts del análogo `tests/content-fare-indicativo.test.js` no se clonan y uno se refuerza.**
El de unicidad de keys dentro del slot (`new Set(keys).size === 6`) es FALSO por diseño aquí — hay 10
repeticiones deliberadas — y se sustituye por igualdad ordenada con la tabla `CANON`. El de «ningún
prompt lleva paréntesis» se reescribe al set cerrado de glosas de conjunción de D-42-13. El de
«exactamente un pronombre por prompt» se reescribe: la no-correferencia de D-42-06 obliga a dos sujetos
explícitos en varias variantes, así que el gate correcto es una **tabla declarativa por variante**
(disparador, sujeto de la principal o `null` si es impersonal, sujeto del hueco) con las aserciones de
que los dos sujetos difieren, de que el disparador declarado aparece en el prompt y de que el sujeto del
hueco es el de la persona esperada. Es el idioma de la tabla `FRAMES` del análogo, que ya tiene dientes.

**4. `faceste` y `abbiamo fatto` son homógrafas entre modos, y `facesti`/`facemmo` son formas reales del
passato remoto.** Verificado contra `content/exercises/fare-indicativo.json`. Consecuencia para la
autoría: ninguna distractora «malformada» puede coincidir con esas cadenas, y el conjunto de formas
reales de `fare` contra el que se comprueba la distractora inexistente tiene que incluir el paradigma de
indicativo completo, no solo el de subjuntivo.

## Artifacts this phase produces

Ver la tabla completa en `42-01-PLAN.md` §Artifacts this phase produces — cubre los 5 ids de slot, la
entrada de `categories.json` con su `name`, la entrada de `CATEGORIES_WITH_EXPLANATIONS`, el fichero de
test nuevo y sus 13 nombres de `describe`. Lo que **este** plan crea de esa tabla:

| Símbolo / identificador | Fichero | Tipo |
|---|---|---|
| `fare-congiuntivo-passato` | `content/exercises/fare-congiuntivo.json` | id de slot MC, 6 variantes (eje: persona del auxiliar) |
| `fare-congiuntivo-trapassato` | idem | id de slot MC, 6 variantes (eje: persona del auxiliar) |
| `fare-congiuntivo-disparador` | idem | id de slot MC, 6 variantes (eje: el DISPARADOR) |
| `tests/content-fare-congiuntivo.test.js` | (nuevo) | fichero de test de invariantes de la categoría |
| `CANON`, `PRONOUNS`, `TRIGGERS`, `SIMPLE_SLOTS`, `COMPOUND_SLOTS`, `BLACKLIST`, `PHASE43_FORMS`, `PERIPHRASIS`, `OBJECTS`, `GLOSSES`, `INDICATIVO_FORMS`, `EXTRA_ROUND_SLOTS` | idem | constantes de datos del test (la especificación ejecutable de la categoría) |
| los 13 `describe` (nombres literales en la tabla de 42-01) | idem | bloques de test |

Espacio de ids reservado y **no usado**: `fare-congiuntivo-300` y siguientes (Phase 44, INT-03).

## Multi-source coverage audit

La auditoría completa de las 4 fuentes (GOAL / REQ / RESEARCH / CONTEXT) vive en `42-01-PLAN.md`
§Multi-source coverage audit, con **0 items MISSING** y sin necesidad de split de fase. Los ítems cuya
cobertura corre en ESTE plan:

| Fuente | Item | Tarea | Estado |
|--------|------|-------|--------|
| GOAL | SC-2 (segunda mitad) — las 5 homógrafas de los compuestos con sujeto en la frase | T1 | COVERED |
| GOAL | SC-3 — passato y trapassato por concordancia, distractoras del OTRO tiempo del subjuntivo y no de otro modo | T1 | COVERED |
| GOAL | SC-4 — slot del DISPARADOR con al menos una variante de contraste en indicativo | T2 | COVERED |
| GOAL | SC-5 (parte) — canon editorial y ausencia de leak R1 congelados como invariantes | T3 | COVERED |
| REQ | CONG-03 congiuntivo passato y trapassato, tiempo por concordancia | T1 | COVERED |
| REQ | CONG-04 el disparador del subjuntivo | T2 | COVERED |
| CONTEXT | D-42-01 5º slot dedicado al disparador, 5x6=30 (costly) | T2 | COVERED |
| CONTEXT | D-42-02 eje = las 6 personas del auxiliar, marco de concordancia covariando | T1 | COVERED |
| CONTEXT | D-42-03 la variante de contraste no es un cruce; `categoryIds` de 1 | T2 + T3 | COVERED |
| CONTEXT | D-42-05 las otras 5 de las 10 homógrafas | T1 + T3 | COVERED |
| CONTEXT | D-42-06 gate HARD de no-correferencia | T1 + T2 + T3 | COVERED |
| CONTEXT | D-42-07 qué diferencia a las variantes que comparten key (con la precisión 2) | T1 + T3 | COVERED |
| CONTEXT | D-42-08 ronda EXTRA DeepSeek sobre las 10 homógrafas | §Hand-off + T3 (gate condicionado) | COVERED |
| CONTEXT | D-42-09 cero indicativo en los compuestos, las 3 distractoras son subjuntivo de esa persona | T1 + T3 | COVERED |
| CONTEXT | D-42-10 patrón de distractoras de los simples (autorado en 42-01) | T3 (gate) | COVERED |
| CONTEXT | D-42-11 blacklist con criterio de defendibilidad | T1 + T2 + T3 | COVERED |
| CONTEXT | D-42-12 las 4 casillas reales modo x tiempo, ninguna inventada | T2 + T3 | COVERED |
| CONTEXT | D-42-13 0-gloss del verbo + gloss léxico de conjunción (reversible) | T1 + T2 + T3 | COVERED |
| CONTEXT | D-42-14 interferencia: desarrollo en el disparador, ausencia deliberada en los compuestos | T1 + T2 | COVERED |
| CONTEXT | D-42-15 hand-off del imperativo a Phase 43, sin mencionarlo | prohibición + T3 (gate de ausencia) | COVERED |
| CONTEXT | D-42-16 `se` hipotético con la principal en condizionale de otro verbo | T2 + T3 | COVERED |
| CONTEXT | D-42-17 canon editorial locked | T1 + T2 + T3 | COVERED |
| CONTEXT | D-42-18 heredados (slug, MC-only, sujeto explícito, ids semánticos, SCOPE-GATE) | T1 + T2 + T3 | COVERED |

## Hand-off al pase TOP-LEVEL de quórum (D-42-04, D-42-08)

Idéntico al de `42-01-PLAN.md` §Hand-off, que es la referencia completa. Resumen operativo al cierre de
este plan, que es el cierre de la autoría de la fase:

| Momento | `validation.status` | `validation.passes[]` | Cómo se comprueba |
|---|---|---|---|
| Al cerrar 42-02 | `"pending"` en los 5 slots | `[]` | `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 5 slots `pending`; `tests/content-fare-congiuntivo.test.js` está en VERDE con ese estado |
| Tras el pase top-level | `"validated"` en los 5 | ≥2 pases `correcta` con `by` distintos en los 5; y al menos un `by` `deepseek-*` en los 4 slots del paradigma | `VAL_07_STRICT=1 node --test tests/*.test.js` en verde, y el bloque 12 del test de categoría exige la ronda extra |

**Es un entregable visible, no un supuesto.** El skill `gsd-validate-exercise` spawnea Task subagents y
un subagent `gsd-executor` no puede hacerlo (`[[executor_cannot_run_task_quorum]]`), así que ninguna
tarea de este plan ejecuta el quórum. Se corre en top-level tras `execute-phase`, **un ejercicio por
contexto, NUNCA batched (VAL-03)**. La ronda EXTRA de D-42-08 es un pase DeepSeek obligatorio vía
`scripts/validate-ai-pass.mjs` (claves en `.env`) sobre exactamente las **10 variantes homógrafas** —
`faccia` io/tu/lui-lei, `facessi` io/tu, `abbia fatto` io/tu/lui-lei, `avessi fatto` io/tu —, además del
quórum base; el slot del disparador NO la lleva. Falso positivo de política esperado: el flag C5-leak
sobre el gloss de conjunción NO se arregla (D-42-13); un flag C4-accent sobre español sin tildes SÍ, es
bug real.

## Assumptions (probe de edges sin SPEC — 4 filas `unclassified` surfaceadas, 0 descartes silenciosos)

La tabla completa vive en `42-01-PLAN.md` §Assumptions, con la igualdad de no-descarte-silencioso: **4
filas surfaceadas por el probe == 0 autoradas como truths desde el probe + 4 surfaceadas como supuestos
marcados**. Los dos supuestos que este plan asume, repetidos aquí porque es donde se materializan:

| Requisito | Qué NO derivó el probe | Supuesto marcado que asume este plan |
|---|---|---|
| CONG-03 | ninguna categoría de edge (boundary / adjacency / encoding / ordering / empty / precision): el clasificador funciona por palabras clave en inglés y el requisito está en español | el edge real es que el marco de concordancia excluya limpiamente los DOS simples — sin ese blindaje `faccia` y `facessi` son defendibles —, más la homografía `abbiamo fatto` entre congiuntivo passato y passato prossimo de indicativo, que obliga a formular el gate de cero-indicativo en positivo y sobre las distractoras (§Precisiones 1). Autorados a mano como `must_haves.truths` |
| CONG-04 | idem | el edge real es que el disparador determine modo Y tiempo sin ambigüedad, incluida la variante de contraste cuya respuesta correcta es el indicativo, y que el `se` hipotético lleve la principal en condizionale de un verbo que no es `fare` para no invadir Phase 43. Autorados a mano como truths |

Más **1 `backstop`** en este plan: que el fichero de test tenga dientes, marcador estructurado con
`verification: backstop`, porque la prueba de fail-first se corre sobre una copia temporal y su
resultado no queda registrado en ningún artefacto del repo. Si el verificador no puede confirmarlo con
evidencia explícita debe abstenerse a `human_needed`, nunca dar pase silencioso.

## Nota de escaneo (no negociable, heredada de 41-01)

Todas las comprobaciones de **ausencia** de este plan y del fichero de test que crea van SIEMPRE **por
campo** — `variants[].prompt` y `variants[].options[]`, sobre el JSON parseado — y **nunca** sobre el
fichero completo ni sobre `notes`, porque el `notes` nombra a propósito cada forma de la blacklist y cada
perífrasis excluida con su audit trail. Y siempre por coincidencia **exacta**, nunca por subcadena: la
forma corta atestiguada de 3ª singular del passato remoto es prefijo de tres formas legítimas de esta
categoría. Por la misma razón este plan no usa ni un solo grep negativo de fichero completo sobre
contenido: los conjuntos prohibidos viven como arrays en criterios `node -e`. Los `grep -c` que sí
aparecen son **positivos** sobre el fichero de test (contar `describe`, contar citas de decision-id).

<!-- planner-discipline-allow: facci -->
<!-- planner-discipline-allow: facciam -->
<!-- planner-discipline-allow: facce -->
<!-- planner-discipline-allow: farei -->
<!-- planner-discipline-allow: fo -->
<!-- planner-discipline-allow: fan -->
<!-- planner-discipline-allow: face -->
<!-- planner-discipline-allow: espa -->
<!-- planner-discipline-allow: fatta -->

<tasks>

<task type="auto">
  <name>Task 1: Los dos COMPUESTOS — `fare-congiuntivo-passato` y `fare-congiuntivo-trapassato`, con el blindaje de SC-3 (12 variantes, CONG-03)</name>

  <files>content/exercises/fare-congiuntivo.json</files>

  <read_first>
    - `content/exercises/fare-congiuntivo.json` completo tal como quedó tras 42-01 — el `notes` (con el
      gate de no-correferencia, la blacklist, el SCOPE-GATE y los tres patrones de distractoras ya
      declarados) y los 2 slots simples son el molde literal: mismo orden de campos, mismos 3 campos por
      variante, mismo `validation` en `pending`, mismo conjunto cerrado de objetos, mismas 4 glosas de
      conjunción admitidas. Las keys de esos 2 slots son además el conjunto de distractoras simples de
      estos dos slots, así que hay que leerlas verbatim.
    - `content/exercises/fare-indicativo.json`, slots de los compuestos con `avere` — el shape de una
      opción de dos palabras (`auxiliar` + `fatto`), y **las 6 formas del passato prossimo de indicativo
      verbatim, que en estos dos slots están PROHIBIDAS como distractoras** (SC-3): se leen para saber
      qué no escribir.
    - `content/exercises/fare-indicativo.json`, slot del passato remoto — `facesti`, `facemmo` y
      `faceste` son formas REALES de indicativo; ninguna distractora inventada puede coincidir con ellas.
    - `src/data/schema-validator.js` líneas 435-456 — la superficie MC que cada variante debe cumplir
      (`prompt` con `___`, `options` de 3 o 4 strings no vacíos, `correctIndex` entero en rango).
    - `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-CONTEXT.md` §decisions —
      D-42-02, D-42-05, D-42-06, D-42-07, D-42-09, D-42-11, D-42-13, D-42-14, D-42-17, D-42-18, y
      §Claude's Discretion, que deja a criterio propio los marcos temporales concretos y **cómo se
      blinda que el subjuntivo simple no sea también válido** — el trabajo fino de estos dos slots.
    - `.planning/REQUIREMENTS.md` línea 32 — el texto literal de CONG-03, incluida «la elección del
      tiempo según la concordancia con la principal».
    - `.planning/ROADMAP.md` §Phase 42 SC-3 — la formulación literal «distractoras que son el OTRO
      tiempo del subjuntivo (no otro modo)», que es el criterio más restrictivo de la fase.
  </read_first>

  <action>
    Añadir DOS slots al array `exercises`, después de `fare-congiuntivo-imperfetto`, con el molde exacto
    de los dos primeros. `validation` de los dos: `{ "status": "pending", "passes": [] }`.

    **Slot `fare-congiuntivo-passato` (CONG-03)**

    - `id`: `fare-congiuntivo-passato`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-congiuntivo"]`.
    - Las 6 keys, una por variante y en orden de persona io/tu/lui-lei/noi/voi/loro: `abbia fatto`,
      `abbia fatto`, `abbia fatto`, `abbiamo fatto`, `abbiate fatto`, `abbiano fatto`. El eje es la
      persona **del auxiliar** (D-42-02); `fatto` es invariable. La key se repite tres veces a propósito
      (D-42-05): son 3 de las 10 variantes homógrafas de la fase.
    - **El blindaje de la concordancia — el trabajo fino de este slot.** El marco de cada prompt tiene
      que hacer imposible el congiuntivo presente Y el congiuntivo imperfetto, o esas dos distractoras
      serían defendibles. Mecanismo: **oración principal en PRESENTE** (o en futuro) **más un marcador
      temporal en el subordinado que sitúe la acción como ya completada** — del tipo `ieri`,
      `la settimana scorsa`, `stamattina`, `già`, `l'anno scorso`, `finalmente`. Con la principal en
      presente, el congiuntivo imperfetto y el trapassato quedan fuera por concordancia; con el marcador
      de acción completada, el congiuntivo presente queda fuera por aspecto. Los 6 marcos de
      concordancia tienen que ser distintos entre sí (D-42-02): el autor no debe poder aprender el slot
      por la forma de la principal.
    - Disparadores: 6 distintos entre sí (D-42-07) y compatibles con una principal en presente — del
      tipo `Non credo che`, `Mi sembra che`, `Sono contento che`, `È strano che`, `Peccato che`,
      `Benché`, `Nonostante`, o con sujeto explícito distinto (`Il professore dubita che`,
      `Mia madre pensa che`). **Gate HARD de no-correferencia (D-42-06)**: el sujeto de la principal
      nunca coincide con el del hueco. Si se usa `benché` o `nonostante`, va con su gloss léxico del
      conjunto cerrado de 4 (D-42-13), inmediatamente detrás de la conjunción y nunca junto a una forma
      verbal.
    - El objeto literal sale del conjunto cerrado de 7 y es **distinto entre las tres variantes que
      comparten la key `abbia fatto`** (D-42-07).
    - Distractoras (D-42-09, **SC-3 al pie de la letra**): las 3 son formas de SUBJUNTIVO de ESA MISMA
      persona, y **ninguna es indicativo**. Concretamente, para cada persona: el otro compuesto
      (`avessi fatto` para io y tu, `avesse fatto` para lui/lei, `avessimo fatto` para noi,
      `aveste fatto` para voi, `avessero fatto` para loro), el congiuntivo presente de esa persona
      (`faccia`, `faccia`, `faccia`, `facciamo`, `facciate`, `facciano`) y el congiuntivo imperfetto de
      esa persona (`facessi`, `facessi`, `facesse`, `facessimo`, `faceste`, `facessero`).
      **Prohibición dura y específica de este slot:** ninguna opción puede ser una forma de passato
      prossimo de indicativo (`ho fatto`, `hai fatto`, `ha fatto`, `avete fatto`, `hanno fatto`) ni de
      trapassato prossimo de indicativo (`avevo fatto` y familia), ni siquiera como distractora
      «claramente peor» — SC-3 lo excluye literalmente y `ha fatto` es exactamente la distractora más
      tentadora aquí.
    - **Consecuencia declarada (§Precisiones 2), que hay que anotar en `notes` como corrección de
      autoría con su fecha:** como D-42-09 determina las 3 distractoras por completo a partir de la
      persona, las variantes de io y de tu tienen el MISMO conjunto de `options`. No es un descuido: los
      diferenciadores son el disparador, el objeto literal y el pronombre sujeto explícito.
    - `correctIndex`: entero en `[0,4)`, variado entre las 6 variantes.
    - `explanation`: párrafo largo en español acentuado, tono D-127. Debe citar las 4 cadenas distintas
      del paradigma (`abbia fatto`, `abbiamo fatto`, `abbiate fatto`, `abbiano fatto`) señalando que las
      tres primeras personas comparten forma y que por eso el sujeto tiene que estar en la frase;
      explicar que en el compuesto se conjuga el AUXILIAR y el participio no cambia; explicar la regla de
      concordancia que el slot examina — principal en presente más acción ya completada da congiuntivo
      passato — y por qué las distractoras son el otro tiempo del subjuntivo y no otro modo (el eje aquí
      es la ANTERIORIDAD, no el modo); y cerrar nombrando la trampa. **NO repite la línea del error de
      interferencia** (D-42-14: el eje de este slot es la anterioridad). **No menciona el imperativo**
      (D-42-15). Ninguna referencia a otro ejercicio por id (sub-test R2).

    **Slot `fare-congiuntivo-trapassato` (CONG-03)**

    - `id`: `fare-congiuntivo-trapassato`. Mismo `type` y `categoryIds`.
    - Las 6 keys en orden de persona: `avessi fatto`, `avessi fatto`, `avesse fatto`, `avessimo fatto`,
      `aveste fatto`, `avessero fatto`. La key se repite en io y tu (D-42-05): son las 2 homógrafas
      restantes de la fase.
    - **El blindaje de la concordancia.** Mecanismo simétrico al del passato: **oración principal en
      PASADO** (imperfetto, passato prossimo o passato remoto) **más un marcador de ANTERIORIDAD respecto
      a ese punto** — del tipo `il giorno prima`, `la settimana precedente`, `già`, `prima`,
      `l'anno prima`. Con la principal en pasado, el congiuntivo passato queda fuera por concordancia;
      con el marcador de anterioridad, el congiuntivo imperfetto queda fuera porque expresaría
      simultaneidad y no anterioridad; y el congiuntivo presente queda fuera dos veces. Los 6 marcos
      distintos entre sí (D-42-02).
    - Disparadores: 6 distintos entre sí y compatibles con una principal en pasado — del tipo
      `Non sapevo che`, `Credevo che`, `Mi dispiaceva che`, `Era strano che`, `Benché`, `Nonostante`, o
      con sujeto explícito distinto (`Il professore non sapeva che`). Gate HARD de no-correferencia
      (D-42-06). Gloss léxico solo del conjunto cerrado de 4 (D-42-13).
    - El objeto literal del conjunto cerrado, distinto entre las dos variantes que comparten
      `avessi fatto`.
    - Distractoras (D-42-09, cero indicativo): para cada persona, el otro compuesto (`abbia fatto` para
      io/tu/lui-lei, `abbiamo fatto` para noi, `abbiate fatto` para voi, `abbiano fatto` para loro), el
      congiuntivo presente de esa persona y el congiuntivo imperfetto de esa persona. Misma prohibición
      dura: ninguna forma de indicativo, y en particular ninguna del trapassato prossimo de indicativo,
      que aquí es la tentación equivalente.
    - `correctIndex` variado. `explanation`: mismo molde que el passato, con el paradigma de las 5
      cadenas distintas (`avessi fatto`, `avesse fatto`, `avessimo fatto`, `aveste fatto`,
      `avessero fatto`), la homografía de io y tu señalada, la regla de concordancia — principal en
      pasado más anterioridad da congiuntivo trapassato —, el contraste explícito con el passato para que
      el par quede aprendido como par, y el cierre nombrando la trampa. Tampoco repite la línea de
      interferencia ni menciona el imperativo.

    Reglas duras comunes a los dos slots: hueco literal `___`, pronombre sujeto explícito de la persona
    de la variante, cero español en el prompt fuera de las 4 glosas de conjunción admitidas, cero
    corchetes angulares, cero backticks, cero comillas tipográficas, apóstrofes ASCII, `options` de 4 sin
    duplicados internos, y **ningún participio concordado** (`fatta`, `fatti`, `fatte`) en ninguna
    opción: la concordancia del participio es el magnet declarado de Phase 43 y adelantarla colisionaría
    con él.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 4`; los ids son exactamente `fare-congiuntivo-presente`, `fare-congiuntivo-imperfetto`, `fare-congiuntivo-passato`, `fare-congiuntivo-trapassato` en ese orden; los 4 son `multiple-choice`, con `categoryIds` de longitud 1 y valor `fare-congiuntivo`, y `variants.length === 6`; total 24 variantes.
    - `node -e`: el array de keys de `fare-congiuntivo-passato` es exactamente `["abbia fatto","abbia fatto","abbia fatto","abbiamo fatto","abbiate fatto","abbiano fatto"]` en ese orden (CONG-03, D-42-02, D-42-05).
    - `node -e`: el array de keys de `fare-congiuntivo-trapassato` es exactamente `["avessi fatto","avessi fatto","avesse fatto","avessimo fatto","aveste fatto","avessero fatto"]` en ese orden.
    - **Gate de SC-3, en positivo y sobre las distractoras** (§Precisiones 1): `node -e` sobre las 12 variantes de los dos slots — para cada variante, el conjunto `options` menos la key tiene exactamente 3 elementos y los 3 pertenecen al conjunto cerrado de 3 formas de congiuntivo de la persona de esa variante (el otro compuesto, el congiuntivo presente de esa persona, el congiuntivo imperfetto de esa persona). Cero excepciones (D-42-09, SC-3).
    - `node -e` sobre `options[]` de los dos slots, coincidencia **EXACTA**: **0** coincidencias con el conjunto de passato prossimo de indicativo `["ho fatto","hai fatto","ha fatto","avete fatto","hanno fatto"]` ni con el de trapassato prossimo de indicativo `["avevo fatto","avevi fatto","aveva fatto","avevamo fatto","avevate fatto","avevano fatto"]` (SC-3). El único solapamiento de cadena con el indicativo es la key `abbiamo fatto` de la variante de `noi`, que es homografía declarada en `notes` y no una distractora.
    - `node -e` sobre `options[]` de los 4 slots: **0** opciones coinciden con el patrón de participio concordado `/\bfatt[aie]\b/` (D-42-15).
    - `node -e` sobre los 12 `prompt` de los dos slots: los 12 contienen `___`; los 12 contienen el pronombre sujeto de su persona como palabra suelta; los 6 disparadores de cada slot son distintos entre sí; los 6 marcos de concordancia de cada slot son strings distintos entre sí (D-42-02, D-42-06, D-42-07, D-42-18).
    - `node -e`: el objeto literal es distinto entre las tres variantes de `passato` cuya key es `abbia fatto`, y entre las dos de `trapassato` cuya key es `avessi fatto` (D-42-07).
    - `node -e` sobre los 12 `prompt`: **0** contienen `espa`; todo contenido entre paréntesis pertenece al conjunto cerrado `["aunque","siempre que","antes de que","a pesar de que"]`, va inmediatamente detrás de una de las conjunciones glosadas y en los 12 caracteres previos no aparece `facc`, `face`, `abbia` ni `aves` (D-42-13).
    - `node -e` sobre `prompt` y `options[]` de los 4 slots: cada prompt contiene al menos un objeto del conjunto cerrado de 7; **0** coincidencias con `["colazione","spesa","freddo","farcel","far fare"]` (D-42-18).
    - `node -e` sobre `options[]` de los 4 slots, coincidencia exacta: **0** coincidencias con el conjunto heredado de 18 formas atestiguadas, **0** con `["facci","facciam","facce"]`, **0** con `["farei","faresti","farebbe","faremmo","fareste","farebbero","fa'"]` (D-42-11).
    - `node -e` sobre las 2 explanations nuevas: cada una contiene al menos un carácter del conjunto `áéíóúñÁÉÍÓÚÑ`; la del passato contiene las 4 cadenas de su paradigma y la del trapassato las 5 del suyo; ambas contienen la subcadena `anterior` (la regla de concordancia que examinan); longitud `> 400` cada una; **0** contienen `imperativo`; **0** contienen `pienso que` — la línea de interferencia es ausencia deliberada en estos dos slots (D-42-14, D-42-15, D-42-17).
    - `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:`, comillas tipográficas y backticks; ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`.
    - `node -e` sobre `notes`: contiene la corrección de autoría de §Precisiones 2 con su fecha — la nota de que io y tu comparten conjunto de `options` en los dos compuestos por construcción de D-42-09, con su razón; y contiene la declaración del blindaje de concordancia de los dos slots.
    - `node -e`: los 4 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`; `correctIndex` no es el mismo valor en las 6 variantes de ningún slot.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`; `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 4 slots con `pending`.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ content/categories.json tests/` no produce ninguna línea de salida — esta tarea toca UN solo fichero.
  </acceptance_criteria>

  <done>
    El fichero tiene 4 slots y 24 variantes en `pending`; las keys de `passato` y `trapassato` son las 6
    personas del auxiliar de cada tiempo con `fatto` invariable, con `abbia fatto` repetida tres veces y
    `avessi fatto` dos, resueltas por sujeto explícito; las 3 distractoras de las 12 variantes son
    formas de subjuntivo de esa misma persona y **no hay un solo indicativo** (SC-3 al pie de la letra);
    el marco de concordancia de cada prompt excluye limpiamente los dos simples; y la suite completa
    sigue en `fail 0` con el motor sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: El 5º slot — `fare-congiuntivo-disparador`, eje = el DISPARADOR, con la variante de contraste en indicativo (6 variantes, CONG-04, SC-4)</name>

  <files>content/exercises/fare-congiuntivo.json</files>

  <read_first>
    - `content/exercises/fare-congiuntivo.json` completo tal como quedó tras la Task 1 — el `notes` (con
      la divergencia de volumen de D-42-01 y el patrón de distractoras de D-42-12 ya declarados) y los 4
      slots del paradigma: sus keys son el material del que salen las 4 opciones de este slot.
    - `content/exercises/fare-indicativo.json`, slots del presente y del imperfetto de indicativo — las
      6 formas de cada uno verbatim (`faccio/fai/fa/facciamo/fate/fanno` y
      `facevo/facevi/faceva/facevamo/facevate/facevano`). En este slot **sí** son opciones legítimas
      (D-42-12), a diferencia de en los dos compuestos; hay que copiarlas sin inventar ninguna.
    - `src/domain/session.js` línea 232 (`pickVariantIndex`) — la confirmación de que la función solo lee
      `slot.variants.length` y no sabe nada del *significado* del eje: es la razón por la que este slot
      no cuesta una línea de motor.
    - `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-CONTEXT.md` §decisions —
      D-42-01 (el 5º slot y su razón), D-42-03 (la variante de contraste NO es un cruce), D-42-06,
      D-42-12 (las 4 casillas reales), D-42-13, D-42-14 (aquí se DESARROLLA la interferencia), D-42-16
      (el `se` hipotético), y §Claude's Discretion, que deja a criterio propio **el reparto de los 6
      disparadores entre las 6 variantes y cuál es el de contraste**.
    - `.planning/REQUIREMENTS.md` línea 33 y §Mapping rationale línea 116 — el texto literal de CONG-04
      y por qué el disparador es un requisito propio y no una casilla del paradigma.
    - `.planning/ROADMAP.md` §Phase 42 SC-4 — «al menos un slot testea el DISPARADOR (`penso che`,
      `benché`, `prima che`, `se` hipotético) ... incluida al menos una variante de contraste donde lo
      correcto es el indicativo».
    - `.planning/ROADMAP.md` §Phase 43 — para respetar la frontera: el condizionale y el imperativo son
      de esa fase, así que la principal del `se` hipotético usa el condizionale de OTRO verbo.
  </read_first>

  <reversibility rating="costly">Este es el slot que D-42-01 califica de `costly`: quitarlo después exige borrar 6 variantes ya validadas por quórum, y si el autor ya ha fallado el slot sus ids viven en su `exerciseStats` real. Además cambia el total del milestone que Phase 44 / INT-02 va a derivar (21 slots y ≈107 variantes pasan a 22 y ≈113). No se inserta `checkpoint:decision`: la calificación es `costly`, no `one-way` — la decisión ya está tomada y razonada en D-42-01 contra la letra del propio roadmap, y el `notes` la deja documentada con su knock-on.</reversibility>

  <action>
    Añadir UN slot al array `exercises`, al final, con el molde de los cuatro anteriores. `validation`:
    `{ "status": "pending", "passes": [] }`.

    **Slot `fare-congiuntivo-disparador` (CONG-04, SC-4)**

    - `id`: `fare-congiuntivo-disparador`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-congiuntivo"]` — **exactamente un id**: la variante de contraste no es un cruce
      multi-categoría, es un test de reconocimiento de disparador dentro del congiuntivo donde el
      indicativo aparece como respuesta y no como tema examinado, así que la lista de cruces de INT-03
      queda cerrada y fallar el disparador no arrastra `fare-indicativo` en la cascada D-54 (D-42-03).
    - **El eje de variante es el DISPARADOR, no la persona** (D-42-01). Un solo eje, igual que en los
      otros cuatro slots, así que `pickVariantIndex` lo sirve sin código nuevo. Cada variante es un
      disparador distinto.
    - **Las 4 opciones son las 4 casillas REALES de la persona del hueco** (D-42-12): congiuntivo
      presente, indicativo presente, congiuntivo imperfetto e indicativo imperfetto de esa persona. Para
      3ª singular eso da `faccia`, `fa`, `facesse`, `faceva`; para 1ª singular `faccia`, `faccio`,
      `facessi`, `facevo`; para 2ª singular `faccia`, `fai`, `facessi`, `facevi`; para 2ª plural
      `facciate`, `fate`, `faceste`, `facevate`; para 3ª plural `facciano`, `fanno`, `facessero`,
      `facevano`. **Ninguna opción es forma inventada** — el autor decide modo Y tiempo, y solo el
      disparador más el marco determinan la casilla. Se rechazó el eje-modo-puro (con dos opciones reales
      y dos obviamente falsas sería una moneda al aire de facto) y el eje modo x persona (mezclaría dos
      ejes en el slot cuyo propósito declarado es aislar uno).
    - **Los 6 disparadores**, distintos entre sí y exactamente uno por prompt. Tienen que cubrir los
      cuatro que SC-4 nombra explícitamente porque la verificación los va a buscar: un verbo de opinión
      del tipo `penso che`, `benché`, `prima che` y el `se` hipotético; más un disparador de necesidad o
      voluntad (`è necessario che` o `voglio che`) y el de contraste. Reparto recomendado, que es
      discreción de la autoría siempre que se respeten las restricciones duras de abajo:
      1. verbo de opinión (`penso che`) con el hueco en 3ª singular → key: congiuntivo presente.
      2. `benché` con su gloss `(aunque)` y el hueco en 2ª singular → key: congiuntivo presente.
      3. `è necessario che` (impersonal) con el hueco en 2ª plural → key: congiuntivo presente.
      4. `prima che` con su gloss `(antes de que)` y el hueco en 3ª plural → key: congiuntivo presente.
      5. `se` hipotético con el hueco en 1ª singular → key: congiuntivo imperfetto, y la principal en
         **condizionale de un verbo que NO es `fare`** (D-42-16), del tipo `Se io ___ i compiti, mia
         madre sarebbe contenta`. El periodo hipotético queda bien construido, el disparador se examina
         de verdad, y ninguna casilla de Phase 43 entra en el fichero.
      6. **La variante de CONTRASTE**, exactamente una (SC-4 pide «al menos una»; una es suficiente y
         mantiene la categoría siendo lo que dice ser): un verbo de CERTEZA del tipo `so che` con el
         hueco en 3ª singular → **key: el INDICATIVO presente**. Usa el mismo patrón de 4 opciones sin
         excepción, así que forma par directo con la variante 1: mismo conjunto de opciones, disparador
         distinto, respuesta distinta. Esa colisión de conjuntos es deliberada y es el par más
         pedagógico de la fase.
    - **Restricciones duras del slot:** los 6 disparadores distintos; exactamente 1 disparador por
      prompt; **exactamente 1** variante con key de indicativo y las otras 5 con key de subjuntivo;
      **gate HARD de no-correferencia (D-42-06)** en las 6 — el sujeto de la principal nunca coincide
      con el del hueco, y en las variantes con verbo de opinión o de certeza eso significa dos sujetos
      explícitos distintos en el prompt, que es correcto y esperado; sujeto del hueco como pronombre
      explícito; hueco literal `___`; objeto literal del conjunto cerrado de 7; gloss léxico solo para
      `benché`, `purché`, `prima che` y `nonostante`, con las 4 glosas del conjunto cerrado,
      inmediatamente detrás de la conjunción y nunca junto a una forma verbal (D-42-13); `correctIndex`
      variado entre las 6; ninguna forma del condizionale ni del imperativo de `fare` en `options`
      (D-42-16); cero participio concordado.
    - `explanation`: párrafo largo en español acentuado, tono D-127, y **el más importante de la fase**
      porque aquí se DESARROLLA el error de interferencia (D-42-14). Debe: dar el par explícito — el
      italiano pide congiuntivo tras un verbo de opinión mientras el español pide indicativo tras su
      equivalente, con el ejemplo paralelo escrito; listar qué disparadores italianos exigen subjuntivo
      donde el español no lo exige, y decir que los verbos de CERTEZA piden indicativo en los dos
      idiomas, que es lo que la variante de contraste examina; explicar el periodo hipotético con `se`
      más congiuntivo imperfetto y principal en condicional; y cerrar diciendo que en este slot las
      cuatro opciones son formas reales y correctas del verbo, así que lo que se examina no es reconocer
      una forma sino leer el contexto — que es exactamente la razón de ser de CONG-04. **No menciona el
      imperativo** (D-42-15). Ninguna referencia a otro ejercicio por id (sub-test R2).
    - Anotar en `notes`, como corrección de autoría con su fecha, el reparto final de los 6 disparadores
      y cuál es la variante de contraste, para que el quórum y una pasada futura no tengan que
      reconstruirlo del contenido.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 5`; los ids son exactamente `fare-congiuntivo-presente`, `fare-congiuntivo-imperfetto`, `fare-congiuntivo-passato`, `fare-congiuntivo-trapassato`, `fare-congiuntivo-disparador` en ese orden; los 5 son `multiple-choice` con `categoryIds` de longitud 1 y `variants.length === 6`; total **30** variantes (D-42-01, edge: count boundary).
    - `node -e`: ningún id del fichero coincide con `/-\d{3}$/` — el espacio `fare-congiuntivo-300`+ queda libre para Phase 44 / INT-03 (D-42-18, edge: id-space boundary).
    - `node -e` sobre las 6 variantes del disparador: cada `options` tiene 4 entradas sin duplicados, y las 4 pertenecen a la unión de las formas REALES de `fare` en congiuntivo presente, indicativo presente, congiuntivo imperfetto e indicativo imperfetto — **0 formas inventadas** en el slot (D-42-12).
    - `node -e` sobre las 6 variantes del disparador: en cada una las 4 opciones son las 4 casillas de UNA MISMA persona (un cuarteto del tipo `faccia`/`fa`/`facesse`/`faceva`, `faccia`/`faccio`/`facessi`/`facevo`, `facciate`/`fate`/`faceste`/`facevate`, `facciano`/`fanno`/`facessero`/`facevano`), y ese cuarteto es el de la persona del pronombre sujeto del hueco (D-42-12, D-42-18).
    - `node -e`: **exactamente 1** de las 6 keys del slot pertenece al conjunto de formas de indicativo, y las otras 5 al de congiuntivo — la variante de contraste es exactamente una (SC-4, D-42-01).
    - `node -e` sobre los 6 `prompt` del slot: los 6 contienen `___`; los 6 disparadores son distintos entre sí y aparece exactamente 1 por prompt; entre los 6 aparecen un verbo de opinión, `benché`, `prima che`, el `se` hipotético, un disparador de necesidad o voluntad, y un verbo de certeza (SC-4, CONG-04).
    - `node -e` sobre la variante del `se` hipotético: su prompt contiene una forma de condizionale de un verbo que **no** es `fare`, y **0** opciones del slot coinciden con el conjunto `["farei","faresti","farebbe","faremmo","fareste","farebbero"]` ni con el imperativo apostrofado (D-42-16).
    - `node -e` sobre los 6 `prompt` del slot: en las variantes con verbo de opinión o de certeza hay dos sujetos explícitos distintos y el de la principal no coincide con el del hueco; en las de disparador impersonal no hay sujeto de principal que coincidir (D-42-06).
    - `node -e` sobre los 6 `prompt` del slot: **0** contienen `espa`; todo contenido entre paréntesis pertenece al conjunto cerrado de 4 glosas, va inmediatamente detrás de una de las conjunciones glosadas y en los 12 caracteres previos no aparece `facc`, `face`, `abbia` ni `aves` (D-42-13).
    - `node -e` sobre los 30 `prompt` del fichero: los 30 contienen `___` y al menos un objeto del conjunto cerrado de 7; **0** coincidencias con `["colazione","spesa","freddo","farcel","far fare"]`; **0** contienen `espa` (D-42-18).
    - `node -e` sobre `options[]` de los 5 slots, coincidencia **EXACTA**: **0** coincidencias con el conjunto heredado de 18 formas atestiguadas, **0** con `["facci","facciam","facce"]`, **0** con la familia del condizionale ni con el imperativo apostrofado; y **0** opciones coinciden con `/\bfatt[aie]\b/` (D-42-11, D-42-15).
    - `node -e` sobre la `explanation` del disparador: contiene al menos un carácter acentuado del español; contiene un ejemplo paralelo italiano-español del error de interferencia (la subcadena `pienso que` o su equivalente literal escrito en el par); contiene la subcadena `certeza`; contiene la subcadena `condicional`; longitud `> 600` caracteres (es la más desarrollada de la fase); **0** contiene `imperativo`; no coincide con el patrón de cross-ref R2 (D-42-14, D-42-15, D-42-16, D-42-17).
    - `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:`, comillas tipográficas y backticks; ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`; las 5 explanations son no vacías y todas contienen al menos un carácter acentuado del español.
    - `node -e` sobre `notes`: contiene la anotación del reparto de los 6 disparadores y de cuál es la variante de contraste, con su fecha.
    - `node -e`: los 5 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`; `correctIndex` no es el mismo valor en las 6 variantes de ningún slot.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`; `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 5 slots con `pending`.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0 — el slot de eje nuevo no ha costado una línea de motor (D-42-01, SC-5). `git status --porcelain src/ scripts/ content/categories.json tests/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    El fichero tiene 5 slots y 30 variantes en `pending`. El slot del disparador existe con el disparador
    como eje de variante, sus 6 disparadores cubren los cuatro que SC-4 nombra, sus 4 opciones son
    siempre las 4 casillas reales modo x tiempo de la persona del hueco sin ninguna forma inventada, y
    exactamente una de las 6 variantes tiene el indicativo como respuesta correcta con un verbo de
    certeza como disparador. El `se` hipotético lleva la principal en condizionale de otro verbo, así
    que ninguna casilla de Phase 43 ha entrado en el fichero. `pickVariantIndex` sirve el eje nuevo sin
    una línea de motor y la suite completa sigue en `fail 0`.
  </done>
</task>

<task type="auto">
  <name>Task 3: `tests/content-fare-congiuntivo.test.js` — convertir los gates de la categoría en invariantes permanentes, con las tres desviaciones del análogo</name>

  <files>tests/content-fare-congiuntivo.test.js</files>

  <read_first>
    - `content/exercises/fare-congiuntivo.json` completo tal como quedó tras la Task 2 — el test se
      escribe contra el fichero real; los conjuntos de formas canónicas del test tienen que coincidir
      byte a byte con las keys, con los disparadores y con los objetos literales del fichero.
    - `tests/content-fare-indicativo.test.js` (682 líneas, 12 bloques `describe`) — el molde directo. Hay
      que fijarse en cuatro cosas: la cabecera con la ADVERTENCIA DE ESCANEO y el setup (`readFileSync`
      con `new URL(..., import.meta.url)`, `assert` estricto, import de `deriveStatus` del código real,
      helpers `byId`, `keyOf`, `eachVariant`, `allVariants`); el **idioma del escaneo por campo**
      (construir `const campos = [v.prompt, ...v.options]` y filtrar, nunca mirar el fichero como texto);
      el patrón `assert.deepEqual(sucio, [], mensaje)` en vez de `assert.ok(length === 0)`, para que el
      diff del fallo LISTE las ofensoras; y el gate condicionado de ronda extra («exige solo si ya está
      `validated`»), que es lo que permite que el test entre en verde antes del quórum. Ojo: los
      comentarios de los tests van **sin acentos** (a diferencia del contenido, que va acentuado RAE).
    - `src/data/validation-state.js` líneas 22-43 — la firma y las reglas de `deriveStatus`, que el test
      importa: `incorrecta` es sticky y da `disputed`; ≥2 `correcta` con `by` distintos y cero
      `incorrecta` da `validated`; todo lo demás, incluido `passes: []`, da `pending`. Esa última rama es
      la que hace verde el bloque 12 antes de la pasada de quórum.
    - `tests/exercise-types.test.js` líneas 1265-1295 y 1336-1405 y el gate VAL-07 (~1429) — lo que el
      smoke paramétrico YA cubre sobre este fichero gracias a la línea que puso 42-01: cobertura de
      explanation, smart-quotes, markdown, leak R1 en prompts, cross-refs R2 en explanations, y VAL-07
      bajo `VAL_07_STRICT=1`. Este fichero **NO duplica** ninguno de esos.
    - `tests/domain.test.js` — lo que ya cubre el auto-descubrimiento y la validación de schema del
      bundle. Tampoco se duplica.
    - `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-PATTERNS.md` §`tests/content-fare-congiuntivo.test.js`
      — la tabla de los 12 `describe` del análogo con su traducción a Phase 42, los excerpts verbatim del
      idioma de escaneo y del gate condicionado, y la tabla de las **tres desviaciones deliberadas**.
    - `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-CONTEXT.md`
      §Claude's Discretion — deja a criterio propio los nombres y la estructura de los gates siempre que
      congelen la lista de invariantes que enumera.
  </read_first>

  <action>
    Crear `tests/content-fare-congiuntivo.test.js`, cero dependencias, ejecutable con
    `node --test tests/content-fare-congiuntivo.test.js` y recogido por el glob de la suite.

    **Cabecera.** Comentario en el estilo del repo, sin acentos: qué fase y qué requisitos cubre (v2.0
    Phase 42, CONG-01..CONG-04), con qué comando se corre, y la **ADVERTENCIA DE ESCANEO** literal — los
    escaneos de ausencia van SIEMPRE por campo (`variants[].prompt`, `variants[].options[]`), NUNCA sobre
    el fichero completo ni sobre `notes`, porque el `notes` nombra a propósito cada forma de la blacklist
    con su audit trail y un escaneo de fichero entero se auto-invalidaria; y SIEMPRE por coincidencia
    exacta, nunca por subcadena, porque la forma corta atestiguada de 3ª singular del passato remoto es
    prefijo de tres formas legitimas de esta categoria. Añadir a la cabecera un párrafo con **las tres
    desviaciones respecto de `tests/content-fare-indicativo.test.js`**, cada una con su decision-id, para
    que quien compare los dos ficheros no las lea como descuidos: el assert de unicidad de keys dentro
    del slot NO se clona porque aqui hay 10 repeticiones deliberadas (D-42-05); el assert de «ningun
    prompt lleva parentesis» se reescribe a un set cerrado de glosas de conjuncion (D-42-13); y el assert
    de «exactamente un pronombre por prompt» se reescribe porque la no-correferencia obliga a dos sujetos
    explicitos en varias variantes (D-42-06).

    **Setup.** Leer el JSON una vez en el top-level del módulo con `readFileSync` y
    `new URL('../content/exercises/fare-congiuntivo.json', import.meta.url)`, y también
    `content/categories.json`. Importar `deriveStatus` de `../src/data/validation-state.js` — la función
    real, nunca reimplementada. Helpers: `byId`, `keyOf(v) = v.options[v.correctIndex]`,
    `eachVariant(slotId, fn)`, `allVariants()`.

    **Constantes de datos**, que son la especificación ejecutable de la categoría:

    - `CANON`: mapa de los 5 ids de slot a su array de 6 keys **en orden y CON las repeticiones**
      (`faccia` x3, `facessi` x2, `abbia fatto` x3, `avessi fatto` x2). Para el slot del disparador, el
      array de sus 6 keys tal como quedaron autoradas.
    - `PRONOUNS`: los 6 pronombres sujeto, con las dos variantes de 3ª singular.
    - `VARIANT_TABLE`: la tabla declarativa por variante que reemplaza el conteo de pronombres — para
      cada slot y cada índice `k`, `{ trigger, mainSubject (o null si el disparador es impersonal),
      blankSubject }`. Es el equivalente de la tabla `FRAMES` del análogo y es lo que da dientes al gate
      de no-correferencia.
    - `SIMPLE_SLOTS` (`presente`, `imperfetto`), `COMPOUND_SLOTS` (`passato`, `trapassato`) y
      `TRIGGER_SLOT` (`disparador`): los tres grupos, porque el patrón de distractoras es distinto en
      cada uno.
    - `CONG_PRES`, `CONG_IMPF`, `CONG_PASS`, `CONG_TRAP`: los cuatro paradigmas de subjuntivo por
      persona. `IND_PRES`, `IND_IMPF`: los dos de indicativo por persona.
      `IND_COMPOUND`: las formas de passato prossimo y trapassato prossimo de indicativo, prohibidas como
      distractoras en `COMPOUND_SLOTS`. `REAL_FORMS`: la unión de TODAS las formas reales de `fare` en
      cualquier modo y tiempo, incluidos el passato remoto y el futuro, contra la que se comprueba que
      una distractora «inexistente» lo sea de verdad.
    - `BLACKLIST`: las formas atestiguadas heredadas más las trampas nuevas de esta categoría, exactamente
      las que el `notes` documenta entre apóstrofes ASCII.
    - `PHASE43_FORMS`: la familia del condizionale y el imperativo apostrofado, más el patrón de
      participio concordado.
    - `PERIPHRASIS` y `OBJECTS`: el SCOPE-GATE léxico heredado y el conjunto cerrado de 7 objetos.
    - `GLOSSES`: el conjunto cerrado de 4 glosas de conjunción y el de conjunciones que las admiten.
    - `EXTRA_ROUND_SLOTS`: los **4 slots del paradigma**, que son los que contienen las 10 variantes
      homógrafas y por tanto los que la ronda EXTRA de D-42-08 tiene que tocar. El slot del disparador NO
      está en esta lista.

    **Los 13 bloques `describe`**, con los nombres literales de la tabla §Artifacts, uno por decisión,
    para que un fallo diga qué decisión se rompió. Todo mensaje de aserción va en español, cita la
    decisión (`D-42-09: ...`) y la coordenada `${slot.id}#${k}`, para que un fallo se localice sin abrir
    el JSON.

    1. **Estructura y conteos.** 2 claves top-level; 5 slots; los 5 ids son los de `CANON` y en ese
       orden; los 5 son `multiple-choice` y no existe ningún slot de otro tipo; cada slot tiene
       exactamente 6 variantes y el total es 30; `categoryIds` de longitud 1 y valor `fare-congiuntivo`
       en los 5; ningún id lleva sufijo numérico de 3 cifras; cada variante tiene exactamente las 3
       claves `prompt`, `options`, `correctIndex`; `options` de longitud 4 sin duplicados internos;
       `correctIndex` entero en rango y no constante dentro de un slot.
    2. **Paradigma completo, 30 keys con homografía deliberada.** Por slot, el array de `keyOf` de sus 6
       variantes es `deepEqual` al array de `CANON` — **igualdad ordenada, no conjunto**, porque las
       repeticiones son parte de la especificación. Comentario explicando por qué el
       `new Set(keys).size === 6` del análogo NO se clona.
    3. **Sujeto explícito y no-correferencia.** Cada prompt contiene `___`. Recorriendo `VARIANT_TABLE`:
       el `trigger` declarado aparece en el prompt; el `blankSubject` declarado aparece en el prompt como
       palabra suelta y es el pronombre de la persona `k` en los 4 slots del paradigma; y
       `mainSubject === null || mainSubject !== blankSubject` en las 30 — el gate HARD de D-42-06.
       Comentario explicando por qué «exactamente un pronombre por prompt» es falso por diseño aquí.
    4. **0-gloss del verbo con gloss léxico de conjunción.** Ningún prompt contiene `espa` en ninguna
       capitalización. Para cada prompt, extraer todos los paréntesis: cada contenido pertenece a
       `GLOSSES`; el texto que precede al paréntesis termina en una de las conjunciones que lo admiten
       seguida de espacio; y en los 12 caracteres previos no aparece ninguna raíz verbal de `fare` ni de
       `avere`. Comentario explicando que el gloss del verbo filtraría modo Y tiempo, que el de la
       conjunción no filtra nada porque «aunque» rige los dos modos en español, y que un flag C5-leak del
       quórum sobre él es falso positivo de política — para que una pasada futura que quiera «arreglarlo»
       se encuentre el porqué en el propio test.
    5. **SCOPE-GATE léxico del objeto literal.** Ningún `prompt` ni `option` contiene un marcador de
       `PERIPHRASIS`; cada prompt contiene al menos un objeto de `OBJECTS`.
    6. **Blacklist de formas atestiguadas y defendibles.** Ninguna opción de ningún slot coincide
       EXACTAMENTE con una entrada de `BLACKLIST` ni de `PHASE43_FORMS`; ninguna opción coincide con el
       patrón de participio concordado. Y en positivo: el `notes` nombra cada forma de `BLACKLIST` con el
       formato `'forma'` (apóstrofes ASCII), y nombra las cuatro homografías declaradas.
    7. **Distractoras de `presente` e `imperfetto` (D-42-10).** Por variante de `SIMPLE_SLOTS`:
       exactamente 1 opción es la key; el conteo de opciones que son el indicativo de esa misma persona
       es 1, con la excepción declarada de la variante de `noi` del presente, donde es 0 y en su lugar
       hay 2 formas reales de otra persona; el conteo de opciones que son forma real de subjuntivo de
       otra persona del mismo tiempo es 1 (2 en la excepción); y las opciones restantes NO pertenecen a
       `REAL_FORMS`, es decir son inexistentes. Y: los conjuntos de `options` de las variantes que
       comparten key son distintos dos a dos **en estos dos slots** (D-42-07).
    8. **Distractoras de `passato` y `trapassato`, cero indicativo (D-42-09, SC-3).** Por variante de
       `COMPOUND_SLOTS`, el gate **en positivo**: el conjunto `options` menos la key tiene exactamente 3
       elementos y los 3 pertenecen al conjunto cerrado de 3 formas de congiuntivo de la persona de esa
       variante — el otro compuesto, el congiuntivo presente y el congiuntivo imperfetto. Y en negativo,
       como red: ninguna DISTRACTORA pertenece a `IND_COMPOUND`. Comentario explicando por qué el gate va
       en positivo y sobre las distractoras y no como ausencia sobre `options`: la key `abbiamo fatto` de
       la variante de `noi` es la misma cadena que el passato prossimo de indicativo, así que un gate de
       ausencia sería insatisfacible. Y comentario declarando que en estos dos slots los conjuntos de
       `options` de io y tu son idénticos por construcción de D-42-09, así que el gate de conjuntos
       distintos del bloque 7 **no** se aplica aquí.
    9. **Slot del disparador: 4 casillas modo x tiempo (D-42-12, D-42-16, SC-4).** Las 4 opciones de cada
       variante pertenecen a `REAL_FORMS` — cero formas inventadas —; las 4 son el cuarteto completo de
       UNA misma persona (congiuntivo presente, indicativo presente, congiuntivo imperfetto, indicativo
       imperfetto) y esa persona es la del `blankSubject` declarado; exactamente 1 de las 6 keys del slot
       es una forma de indicativo y las otras 5 de congiuntivo; entre los 6 disparadores aparecen un
       verbo de opinión, `benché`, `prima che`, el `se` hipotético, un disparador de necesidad o
       voluntad y un verbo de certeza; el prompt del `se` hipotético contiene una forma de condizionale
       de un verbo que no es `fare`; y ninguna opción del slot pertenece a `PHASE43_FORMS`.
    10. **Disparadores y marcos distintos por variante (D-42-02, D-42-07).** Los 6 `trigger` de cada slot
        son distintos entre sí, en los 5 slots. En `COMPOUND_SLOTS`, los 6 marcos de concordancia
        declarados en `VARIANT_TABLE` son distintos entre sí. El objeto literal es distinto entre las
        variantes que comparten key, en los 4 slots del paradigma.
    11. **Canon editorial e higiene del JSON (D-42-17, T-42-01, T-42-02).** Walk recursivo de todos los
        strings y todas las claves del objeto parseado: ninguno contiene los caracteres de corchete
        angular, `&#`, el esquema `javascript:`, comillas tipográficas ni backticks; las 5 explanations
        son no vacías y todas contienen al menos un carácter del conjunto `áéíóúñÁÉÍÓÚÑ`; ninguna
        explanation contiene `imperativo` (D-42-15); las explanations de `COMPOUND_SLOTS` **no** contienen
        la línea de interferencia y la del disparador **sí** (D-42-14); y ninguna clave propia se llama
        `__proto__`, `constructor` ni `prototype`.
    12. **Audit trail de validación y ronda EXTRA (D-42-08, T-42-03).** Para los 5 slots: `validation`
        existe con las claves `status` y `passes`, `passes` es array, y `status === deriveStatus(passes)`.
        Condicionalmente: si `status` es `validated`, `passes` tiene ≥2 entradas con
        `verdict: "correcta"`, ≥2 valores distintos de `by`, y cero `incorrecta`. Y para los 4 ids de
        `EXTRA_ROUND_SLOTS`: si `status` es `validated`, al menos un `by` empieza por `deepseek`; si es
        `pending`, el bloque pasa. Comentario explicando que estas aserciones son verdes con `passes: []`
        porque `deriveStatus([])` da `pending`, que su función es impedir que un `validated` escrito a
        mano pase por bueno, y que exigir que la categoría esté validada es trabajo de `VAL_07_STRICT`, no
        de este fichero.
    13. **Registro de la categoría (D-42-18, SC-5).** `content/categories.json` contiene una entrada
        `fare-congiuntivo` con exactamente las 4 claves `id`, `name`, `order`, `origen`, con `order: 16` y
        `origen: "ia-quorum"`; es la última del array; y al menos un slot del fichero la referencia en
        `categoryIds`, de modo que la derivación de `categoriesForDisplay` da `totalCount > 0` y por tanto
        `examenEnabled` verdadero. Comentario: el assert de «es la última del array» es correcto hoy y se
        pondrá rojo cuando Phase 43 apende la 17ª — es el patrón heredado del análogo y se mantiene a
        propósito, porque es la red de regresión del registro.

    No se duplica ninguna aserción que ya haga `tests/domain.test.js` (schema del bundle) ni el smoke
    paramétrico de `tests/exercise-types.test.js` (cobertura de explanation, smart-quotes, markdown, leak
    R1, cross-refs R2, VAL-07 estricto).
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/content-fare-congiuntivo.test.js && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-congiuntivo.test.js` sale con código 0 y reporta `fail 0` con al menos 25 tests pasados.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con `pass` mayor que el valor tras la Task 2.
    - `grep -c 'describe(' tests/content-fare-congiuntivo.test.js` es `>= 13`.
    - `grep -c 'D-42-' tests/content-fare-congiuntivo.test.js` es `>= 13` — cada bloque cita la decisión que verifica.
    - `grep -c 'validation-state.js' tests/content-fare-congiuntivo.test.js` devuelve `1`, y `grep -c 'deriveStatus' tests/content-fare-congiuntivo.test.js` es `>= 3` — el test importa la función real y la usa, no reimplementa las reglas.
    - `grep -c 'deepseek' tests/content-fare-congiuntivo.test.js` es `>= 1` (D-42-08), y `grep -c 'categories.json' tests/content-fare-congiuntivo.test.js` es `>= 1` (D-42-18 / SC-5).
    - `grep -c 'deepEqual' tests/content-fare-congiuntivo.test.js` es `>= 8` — el patrón de aserción que lista las ofensoras en el diff, no `assert.ok` de longitud.
    - `grep -c 'D-42-05' tests/content-fare-congiuntivo.test.js`, `grep -c 'D-42-13'` y `grep -c 'D-42-06'` son cada uno `>= 1` en el bloque de cabecera **y** en su bloque `describe` correspondiente: las tres desviaciones respecto del análogo están declaradas donde se aplican.
    - `node -e` sobre el propio fichero de test leído como texto: contiene la subcadena `ADVERTENCIA DE ESCANEO`, y contiene `import.meta.url` (rutas relativas al módulo, no rutas absolutas).
    - **Prueba de fail-first, sobre una copia temporal fuera del repo** (nunca sobre el fichero real ni sobre `content/`): mutar la copia del JSON en 5 sentidos y comprobar que el test se pone rojo en cada uno — (a) añadir un gloss de traducción del VERBO entre paréntesis a un prompt, (b) sustituir una distractora de `fare-congiuntivo-passato` por una forma de passato prossimo de indicativo, (c) sustituir una opción por una forma de la blacklist, (d) hacer coincidir el sujeto de la principal con el del hueco en una variante, (e) escribir `status: "validated"` dejando `passes: []`. Los 5 casos tienen que fallar; si alguno pasa, la aserción correspondiente no tiene dientes y hay que arreglarla antes de cerrar la tarea. Registrar el resultado de los 5 en el SUMMARY.
    - `node -e` sobre el fichero de contenido: sigue con 5 slots, 30 variantes, `validation.status === 'pending'` en los 5 y `passes.length === 0` — **esta tarea NO toca el contenido**.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` sigue fallando solo por los 5 slots `pending`, y `tests/content-fare-congiuntivo.test.js` sigue en verde con ese estado (el gate de ronda extra está condicionado).
    - `git status --porcelain` lista únicamente `tests/content-fare-congiuntivo.test.js`; `git diff --quiet src/ scripts/ content/` sale con código 0.
  </acceptance_criteria>

  <done>
    `tests/content-fare-congiuntivo.test.js` existe, corre dentro del glob de la suite en verde con los 5
    slots en `pending`, y convierte en aserciones permanentes los gates de la categoría en 13 bloques:
    conteos y paradigma completo con las repeticiones como especificación, sujeto explícito y
    no-correferencia por tabla declarativa, 0-gloss del verbo con la excepción de conjunción,
    SCOPE-GATE léxico, blacklist por campo y por coincidencia exacta, los tres patrones de distractoras
    en tres bloques (incluido el gate de SC-3 formulado en positivo sobre las distractoras), disparadores
    y marcos distintos por variante, canon editorial e higiene del JSON, coherencia de `validation` con
    `deriveStatus` y la ronda EXTRA condicionada, y el registro de la categoría. Las tres desviaciones
    respecto del análogo están declaradas en la cabecera y en sus bloques con su decision-id. La prueba
    de fail-first demuestra en 5 mutaciones que las aserciones más importantes tienen dientes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Idénticas a las de `42-01-PLAN.md` §threat_model, que es la referencia completa. App web estática de un
solo usuario, sin servidor, sin autenticación, sin red en runtime y sin sincronización (CLAUDE.md
§Constraints). La superficie real de una fase de **autoría de contenido** aquí es la **integridad de
datos**, no el ataque.

| Boundary | Description |
|----------|-------------|
| `content/exercises/fare-congiuntivo.json` en disco → `fetch()` en boot → `JSON.parse` en `src/data/content-loader.js` | Un JSON malformado rompe `loadContent()` en boot y la app no arranca; un `categoryIds` inválido rompe la validación del bundle. |
| strings de contenido (`prompt`, `options[]`, `explanation`, `notes`) → render en el DOM por Alpine | La invariante anti-XSS del proyecto (T-02-01): el contenido se renderiza SIEMPRE como texto, nunca como HTML. |
| `validation.passes[]` → `deriveStatus` → el estado `validated` que el autor lee como «revisado» | Integridad del audit trail: es la única evidencia de revisión que el autor tiene. |
| `tests/content-fare-congiuntivo.test.js` → la suite → la confianza del autor en que los gates existen | Un gate sin dientes es peor que no tenerlo: da una falsa sensación de red y el autor deja de mirar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | strings de `content/exercises/fare-congiuntivo.json` renderizados en el DOM | medium | mitigate | Se preserva la invariante de render como texto (T-02-01): esta fase NO añade ni cambia una línea de render, y el contenido queda libre de marcado. Verificación doble: el sub-test `plain text (no markdown)` del smoke, activado por la línea que puso 42-01; el escaneo `node -e` de las tareas 1 y 2 que exige 0 apariciones de corchetes angulares, `&#`, el esquema `javascript:`, comillas tipográficas y backticks en TODOS los strings; y el bloque 11 del fichero de test, que lo congela como invariante permanente con un walk recursivo. |
| T-42-02 | Tampering | `JSON.parse` del fichero de contenido en `content-loader.js` | medium | mitigate | Prototype-pollution por contenido: `JSON.parse` crea `__proto__` como own-property si el literal la declara. Mitigación: el fichero declara ÚNICAMENTE el key set del schema (bloque 1 del test lo congela), y el bloque 11 recorre recursivamente el objeto parseado exigiendo que ninguna clave propia se llame `__proto__`, `constructor` ni `prototype`, más `({}).polluted === undefined` tras el parse. El deep-clone defensivo del state vive en la cadena de migración y no se toca. |
| T-42-03 | Repudiation | `validation.passes[]` como audit trail del quórum, y la ronda EXTRA de D-42-08 | medium | mitigate | Un `status: "validated"` escrito a mano, un pase con `by`/`date` inventados, o una ronda EXTRA marcada como hecha sin haberla corrido, forjan la única evidencia de revisión que el autor tiene, y la falsificación es indetectable a ojo. Mitigación: el executor deja los 5 slots en `pending` con `passes: []` y NO fabrica pases; el bloque 12 del fichero de test exige `status === deriveStatus(passes)` importando la función real, más las condiciones de `validated` (≥2 `correcta`, ≥2 `by` distintos, cero `incorrecta`) y el gate condicionado de `deepseek` en los 4 slots del paradigma; y el quórum real corre en la pasada top-level de §Hand-off, un ejercicio por contexto (VAL-03). |
| T-42-04 | Tampering | el fichero de test como red de regresión | medium | mitigate | Un gate sin dientes es una regresión silenciosa: la suite queda verde y el autor deja de revisar el contenido a mano. Mitigación: la prueba de fail-first obligatoria de la Task 3 muta una copia temporal en 5 sentidos (gloss del verbo, indicativo en los compuestos, forma de la blacklist, sujetos coincidentes, `validated` sin pases) y exige rojo en los 5, con el resultado registrado en el SUMMARY; y el patrón de aserción es `assert.deepEqual(sucio, [], mensaje)`, que hace que el diff del fallo liste las ofensoras en vez de decir solo «longitud distinta de 0». |
| T-42-05 | Tampering | coincidencia del slug con `RESET_PREFIXES_V13` y con el `id` de `categories.json` | medium | mitigate | Si el slug o el prefijo de los ids no son byte a byte `fare-congiuntivo`, el reset selectivo de v13 no alcanza la categoría y el progreso real del autor queda huérfano sin error visible. Mitigación: `src/data/storage.js:1345` ya contiene el string exacto (Phase 40) y esta fase no lo edita; los bloques 1 y 13 del fichero de test congelan los 5 ids literales, el `categoryIds` de longitud 1 y la entrada de `categories.json`; y la prohibición de renombrar ids después del alta está en `must_haves.prohibitions`. |
| T-42-06 | Denial of Service | tamaño del bundle de contenido cargado en boot | low | accept | 30 variantes son del orden de 400 líneas de JSON; el loader ya trae 15 ficheros con 233 slots. El coste de `JSON.parse` a esta escala es despreciable y la app no tiene atacante remoto: sin servidor, sin red, sin multiusuario. |
| T-42-07 | Information Disclosure | el contenido del fichero y del test | low | accept | Son conjugaciones del subjuntivo de un verbo italiano, explicaciones gramaticales y aserciones sobre ellas. Cero PII, cero credenciales, cero secretos. Nada sale de la máquina del autor. |
| T-42-08 | Spoofing | — | n/a | n/a — no aplica | No existen identidades, sesiones, tokens ni cuentas en esta app: es local, monousuario y sin autenticación. No hay superficie de suplantación que evaluar en L1. |
| T-42-09 | Elevation of Privilege | — | n/a | n/a — no aplica | No hay roles, permisos ni frontera de privilegio: un único usuario con acceso total a su propio navegador y su propio disco. |
| T-42-SC | Tampering | instalaciones de npm / pip / cargo | n/a | n/a — gate no disparado | Esta fase no ejecuta ninguna instalación de gestor de paquetes ni añade dependencias: el proyecto es web estática de dependencias cero y el fichero de test nuevo corre con el runner nativo de Node. El Package Legitimacy Gate no aplica y no se requiere tabla de auditoría. |

**ASVS L1, block on high.** No hay ninguna amenaza `high` en este plan, y no se inventa ninguna para
rellenar la tabla. La superficie real de este plan son la invariante de render como texto, la higiene del
key set del JSON parseado, la integridad del audit trail de validación incluida la ronda EXTRA, los
dientes del propio fichero de test, y la coincidencia byte a byte del slug con el contrato de reset. Las
cinco están en `mitigate` con verificación automatizada. Las dos `accept` son `low` y su razón es
estructural: no hay atacante remoto y no hay datos sensibles.

### Candidatos de prohibición canon descartados (breadcrumbs)

- **prototype-pollution** — canon de seguridad del proyecto; vive en T-42-02 y en `/gsd-secure-phase`,
  no se acuña como prohibición.
- **XSS / inyección de HTML** — canon; vive en T-42-01 (invariante de render como texto, T-02-01).
- **path-traversal** — sin acceso a sistema de ficheros en runtime: el contenido se sirve por HTTP local
  y el runtime es el navegador. Descartado.
- **GDPR / privacidad de datos personales** — el contenido son conjugaciones y el state es el progreso de
  estudio del propio autor en su máquina. Descartado.
- **fairness / sesgo genérico** — sin decisión automatizada sobre personas. Lo que SÍ se acuña son las
  variantes bespoke de esta fase, que ninguna herramienta canon detectaría: el ejercicio con dos
  respuestas defendibles (aquí con el riesgo concreto y nombrado de que el marco de los compuestos no
  excluya los simples), el examen de vocabulario no enseñado, la forma regional presentada como norma, y
  el gate de test sin dientes.
</threat_model>

<verification>
Comprobaciones al terminar las 3 tareas de este plan, que son el cierre de la autoría de la fase:

1. `node --test tests/*.test.js` → código de salida 0, `fail 0`, `pass` mayor que el valor al cerrar
   42-01.
2. `node --test tests/content-fare-congiuntivo.test.js` → código de salida 0, `fail 0`, al menos 25
   tests, con los 5 slots en `pending`.
3. `node -e` sobre `content/exercises/fare-congiuntivo.json`: **5 slots, 30 variantes**, ids exactamente
   los 5 de la tabla §Artifacts y en ese orden, los 5 en `type: "multiple-choice"` con `options` de 4 y
   `categoryIds` de longitud 1, ningún id con sufijo numérico de 3 cifras.
4. El gate de SC-3 en positivo: las 12 distractoras-por-3 de `passato` y `trapassato` pertenecen todas al
   conjunto cerrado de congiuntivo de su persona, y ninguna a `IND_COMPOUND`.
5. El gate de SC-4: el slot del disparador tiene 6 disparadores distintos, 4 opciones reales por variante
   formando el cuarteto de una misma persona, y exactamente 1 key de indicativo.
6. Los escaneos de ausencia, **todos por campo y por coincidencia exacta**, nunca sobre el fichero
   completo: blacklist heredada, trampas nuevas, casillas de Phase 43, participio concordado, compuestos
   de indicativo en los dos slots de compuestos, perífrasis en `prompt` y `options`, español y paréntesis
   no autorizados en `prompt`.
7. La prueba de fail-first del fichero de test: 5 mutaciones sobre una copia temporal, 5 rojos,
   registrados en el SUMMARY.
8. `VAL_07_STRICT=1 node --test tests/*.test.js` → falla nombrando los 5 slots `pending`. Ese rojo es el
   estado CORRECTO al cerrar la autoría; lo cierra el pase top-level de quórum de §Hand-off, con la ronda
   EXTRA DeepSeek sobre las 10 variantes homógrafas.
9. `node scripts/run-validation-271.mjs` → sigue reportando su total anterior en PASS. Es la ceguera
   documentada, no un fallo: el reporter no verá la categoría hasta Phase 44 / INT-02.
10. `git diff --quiet src/screens/app.js src/domain/ src/data/` → código de salida 0. El motor v1.4 queda
    byte-intacto **incluido el slot de eje nuevo** (SC-5, Out of Scope del milestone).
11. `git status --porcelain` → exactamente los 2 ficheros de `files_modified`.
</verification>

<success_criteria>
- CONG-03: el autor es examinado sobre `abbia fatto` y `avessi fatto` en las 6 personas del auxiliar
  cada uno, eligiendo el tiempo por concordancia con la principal, y con las 3 distractoras de cada
  variante siendo el OTRO tiempo del subjuntivo de esa misma persona — cero indicativo (SC-3 al pie de
  la letra).
- CONG-04: el autor es examinado sobre el disparador del subjuntivo en un slot dedicado cuyo eje de
  variante es el disparador, con las 4 casillas reales modo x tiempo como opciones, de modo que la
  casilla no se puede responder por reconocimiento de forma sin entender el contexto.
- SC-2 (segunda mitad): las 5 variantes homógrafas restantes (`abbia fatto` x3, `avessi fatto` x2)
  quedan resueltas por sujeto explícito, con el gate de no-correferencia cumplido en las 30 y la ronda
  EXTRA obligatoria declarada como entregable del pase top-level.
- SC-3: passato y trapassato por concordancia, con el marco de cada prompt excluyendo limpiamente los
  dos simples y con cero formas de otro modo entre las distractoras.
- SC-4: un slot dedicado al disparador que cubre `penso che`, `benché`, `prima che` y el `se` hipotético,
  con exactamente una variante de contraste cuya respuesta correcta es el indicativo, y con el `se`
  hipotético construido sobre el condizionale de un verbo que no es `fare`.
- SC-5 (parte de este plan): los gates de la categoría quedan congelados como invariantes permanentes en
  13 bloques `describe` con dientes demostrados, y la categoría sigue cargando en boot como unidad de
  reset independiente sin una línea de motor nueva.
- Estado de hand-off correcto: los 5 slots en `pending` con `passes: []`, el fichero de test en verde con
  ese estado, y el rojo de `VAL_07_STRICT=1` como marcador visible de que el quórum base y la ronda
  EXTRA todavía no han corrido.
</success_criteria>

<output>
Crear `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-02-SUMMARY.md` al terminar.
</output>
