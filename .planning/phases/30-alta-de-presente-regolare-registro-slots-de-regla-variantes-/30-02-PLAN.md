---
phase: 30-alta-de-presente-regolare-registro-slots-de-regla-variantes-
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - content/exercises/presente-regolare.json
autonomous: true
requirements: [PRES-04]
must_haves:
  truths:
    - "Cada slot de presente-regolare.json fue validado por quórum 1-por-1 (NUNCA batched)"
    - "Toda variante nueva queda con validation.status: validated (>=2 modelos distintos correcta, 0 incorrecta sin resolver)"
    - "Los slots disputed se resuelven por el autor-oráculo con audit trail (pass autor con concern [override])"
  artifacts:
    - path: "content/exercises/presente-regolare.json"
      provides: "Los 6 slots con validation.passes[] populado por el quórum cross-vendor"
      contains: "validated"
  key_links:
    - from: ".claude/skills/gsd-validate-exercise/SKILL.md"
      to: "content/exercises/presente-regolare.json"
      via: "1 subagent fresh-context por ejercicio actualiza validation.passes[]"
      pattern: "passes"
---

<objective>
Validar por quórum cross-vendor R1-R7 TODOS los slots autorados en el plan 30-01, uno por uno (NUNCA batched, VAL-03), hasta dejar cada slot con `validation.status: "validated"`. Los `disputed` se resuelven por el autor-oráculo con audit trail (calidad > tokens, NO override-atajo).

Purpose: Cierra PRES-04 — el contenido gramatical real pasa el quórum estricto (no la validación ligera autor-oráculo de canciones). Garantía editorial: cada variante revisada por ≥2 modelos distintos con consenso `correcta`.

Output: `content/exercises/presente-regolare.json` con `validation.passes[]` populado en los 6 slots, `status: validated` en todos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-CONTEXT.md

<interfaces>
<!-- Shape del bloque validation que el quórum rellena. De essere.json. -->
validation = {
  "status": "validated" | "disputed" | "pending",
  "passes": [
    { "by": "claude-opus-4-8", "date": "YYYY-MM-DD", "verdict": "correcta", "concerns": [] },
    { "by": "claude-sonnet-4-6", "date": "YYYY-MM-DD", "verdict": "correcta", "concerns": [] }
  ]
}
<!-- status validated = >=2 passes con `by` distintos y verdict correcta; disputed = cualquier incorrecta (sticky); pending = resto. -->
<!-- Override autor-oráculo (patrón avere-passato-prossimo): un pass `{ "by": "autor", ..., "verdict": "correcta", "concerns": ["[override] <razón>"] }`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validar por quórum 1-por-1 los 6 slots de presente-regolare (NUNCA batched)</name>
  <files>content/exercises/presente-regolare.json</files>
  <read_first>
    - .claude/skills/gsd-validate-exercise/SKILL.md (el workflow 1-por-1 con quórum Opus+Sonnet que operacionaliza R1-R7 en C1-C5; un subagent fresh-context por ejercicio, NUNCA batched)
    - content/exercises/presente-regolare.json (los 6 slots autorados en 30-01, status pending — la entrada de la validación)
    - .planning/phases/30-01-SUMMARY.md (la lista de ids de slot del plan anterior)
    - scripts/validate-ai-pass.mjs (validador cross-vendor Gemini/DeepSeek complementario; claves en .env; pool elegible por verificación)
    - Memoria del agente `feedback-disputed-resolution` (al resolver disputed: calidad > tokens, NO override-atajo; incluir traducción ES en prompt si hay doble-validez)
    - Memoria del agente `gloss-es-desambiguacion-canon` (el C5-leak que Gemini/DeepSeek marcan sobre el gloss "(en español: …)" es falso-positivo de política; base de aprobación = Claude Opus+Sonnet)
    - Memoria del agente `feedback-cross-vendor-catches-bugs` (DeepSeek estricto en acentos, Opus indulgente → complementar con scan de acentos del canon español)
  </read_first>
  <action>
    Para CADA uno de los 6 slots del JSON (ids resueltos del SUMMARY de 30-01: `presente-regolare-are`, `-ere`, `-ire`, `-isc`, `-velar`, `-palatal` o los ids reales), ejecutar el skill `gsd-validate-exercise <slot-id>` UNA VEZ POR SLOT — 6 invocaciones discretas, JAMÁS un único spawn con los 6 slots en el prompt (VAL-03: un subagent fresh-context por ejercicio es la garantía arquitectónica que cierra el bug-class de batched-curation). Cada invocación spawnea el quórum (Opus + Sonnet secuencial), parsea verdicts, construye `passes[]`, deriva `status` y comitea atómico (1 commit por slot).

    Complementar con el validador cross-vendor `scripts/validate-ai-pass.mjs` (Gemini/DeepSeek) sobre los mismos slots para cazar bugs que el human-verify aprueba (memoria feedback-cross-vendor) — especialmente un scan de acentos del canon español de las explanations (DeepSeek estricto). El gloss "(en español: …)" marcado como C5-leak por Gemini/DeepSeek es FALSO-POSITIVO de política (canon R7): NO lo trates como bloqueante; la base de aprobación es el quórum Claude Opus+Sonnet.

    Si un slot queda `disputed`: resolver por autor-oráculo con audit trail — añadir un pass `{ "by": "autor", "verdict": "correcta"|"incorrecta", "concerns": ["[override] <razón explícita>"] }` SOLO tras analizar el concern real (calidad > tokens, NO override-atajo; memoria feedback-disputed-resolution). Si el concern es un bug real de gramática/semántica/leak, CORREGIR la variante en el JSON y re-validar, NO override. Si es doble-validez (ambigüedad legítima), desambiguar el prompt con contexto/gloss ES y re-validar. El objetivo es `status: validated` en los 6 slots con consenso genuino, no forzado.

    NO modificar el motor, el sampler ni ningún `.js` de runtime (brownfield) — este plan solo muta el bloque `validation` del JSON de contenido.
  </action>
  <verify>
    <automated>node -e "const d=require('./content/exercises/presente-regolare.json');const bad=d.exercises.filter(e=>e.validation.status!=='validated');if(bad.length)throw new Error('no validated: '+bad.map(e=>e.id+'='+e.validation.status).join(', '));for(const e of d.exercises){const p=e.validation.passes||[];const by=new Set(p.filter(x=>x.verdict==='correcta').map(x=>x.by));if(by.size<2)throw new Error('<2 modelos correcta distintos en '+e.id);}console.log('OK 6/6 validated por quorum')"</automated>
  </verify>
  <acceptance_criteria>
    - Los 6 slots de `presente-regolare.json` tienen `validation.status === "validated"`.
    - Cada slot tiene `passes[]` con ≥2 entradas `verdict: correcta` de `by` distintos (modelos distintos, o autor-override documentado).
    - Ningún slot queda `disputed` o `pending` sin resolver.
    - Cualquier `disputed` resuelto deja audit trail: un pass `autor` con concern `[override] ...`, O la variante fue corregida y re-validada (sin override).
    - La validación se ejecutó 1-por-1 (commits atómicos separados por slot, verificable en `git log`), NUNCA batched.
  </acceptance_criteria>
  <done>Los 6 slots pasan el quórum cross-vendor con status validated; disputed resueltos con audit trail; PRES-04 satisfecho.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Output del subagent de validación → bloque `validation` del JSON | El quórum escribe `passes[]`/`status` en el JSON; el subagent arranca con contexto vacío (no hereda CLAUDE.md ni .planning) y solo ve 1 ejercicio + el prompt R1-R7 self-contained. No hay input de usuario, red de terceros (más allá de las APIs de validación con claves locales en .env) ni auth de la app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | bloque validation escrito por el skill (status/verdict incoherentes) | mitigate | `deriveStatus()` deriva el status del array de passes (no se escribe a mano); el verify del plan exige ≥2 modelos distintos `correcta` para `validated`; la verificación es por `node -e` sobre el JSON resultante. |
| T-30-05 | Spoofing | `passes[].by` (model id falseado / alias remapeable) | accept | Single-user local; los model ids son literales explícitos (no aliases) por convención del skill; el riesgo de spoofing del audit trail por un atacante externo es nulo (sin red expuesta, sin multi-usuario). |
| T-30-06 | Information Disclosure | claves de API (Gemini/DeepSeek) en .env usadas por validate-ai-pass.mjs | accept | `.env` es local y gitignored; las claves no entran en el JSON de contenido ni en los commits; fuera del scope de esta fase de contenido cambiar la gestión de secretos. |

**No high-severity threats apply.** El único code path que se ejecuta es el skill de validación editorial sobre JSON estático; no se toca el motor ni se añade superficie de runtime. El riesgo es validación incoherente (mitigado por `deriveStatus` + el verify del plan) y exposición de claves locales (aceptado: single-user, .env gitignored).
</threat_model>

<verification>
- `node -e` sobre presente-regolare.json: 6/6 slots `status: validated`, cada uno ≥2 modelos distintos `correcta`.
- `git log` muestra commits atómicos 1-por-slot (no un único commit con los 6).
- Disputed resueltos: pass `autor` con `[override]` o variante corregida + re-validada.
</verification>

<success_criteria>
- Los 6 slots de presente-regolare validados por quórum cross-vendor R1-R7 (`status: validated`).
- 1-por-1, nunca batched (commits atómicos separados).
- Disputed resueltos por autor-oráculo con audit trail.
- PRES-04 satisfecho.
</success_criteria>

<output>
Create `.planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-02-SUMMARY.md` when done. Incluir: tabla por slot con status final, modelos que validaron, y cualquier disputed con su resolución (override autor o corrección + re-validación).
</output>
