---
phase: 30-alta-de-presente-regolare-registro-slots-de-regla-variantes-
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified: []
autonomous: true
requirements: [PRES-01]
must_haves:
  truths:
    - "La categoría presente-regolare carga en boot vía content-loader sin error de shape"
    - "El schema-validator acepta presente-regolare.json (validateContent pasa)"
    - "La suite baseline sigue verde (sin regresiones nuevas introducidas por el alta)"
    - "La categoría es usable end-to-end (home/picker/Repaso 20/Examen) sin tocar el motor"
    - "El conteo definitivo de slots (N) queda registrado para que Phase 31 lo consuma"
  artifacts: []
  key_links:
    - from: "src/data/content-loader.js"
      to: "content/exercises/presente-regolare.json"
      via: "carga + validateContent en boot"
      pattern: "presente-regolare"
---

<objective>
Verificar que la categoría `presente-regolare` recién dada de alta carga en boot, pasa el schema-validator, y la suite baseline sigue verde — sin tocar el motor ni los hardcodes de count (eso es Phase 31). Registrar el conteo de slots DEFINITIVO (N) para que Phase 31 sincronice `TOTAL_EXPECTED` (`183 → 183 + N`).

Purpose: Cierra PRES-01 (usable en home/picker/Repaso 20/Examen como las otras 9) y deja el JSON consistente y consumible por Phase 31. La usabilidad end-to-end es automática una vez registrada + válida (no hay cambio de motor); la verificación es: boot carga, schema-validator pasa, baseline verde.

Output: confirmación de boot/load + suite verde; el SUMMARY registra el conteo N de slots y la lista de ids para Phase 31.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar boot/load + schema-validator + suite baseline verde</name>
  <files>(verificación read-only — no se modifican archivos)</files>
  <read_first>
    - src/data/content-loader.js (cómo se cargan y validan los content files en boot)
    - src/data/schema-validator.js (validateContent — el contrato que presente-regolare.json debe satisfacer)
    - content/exercises/presente-regolare.json (el archivo dado de alta y validado en 30-01/30-02)
    - content/categories.json (la entrada presente-regolare order 10)
    - tests/exercise-types.test.js (la suite de contenido; NOTA: NO añadir presente-regolare a CATEGORIES_WITH_EXPLANATIONS aquí — eso es Phase 31/INT-02)
  </read_first>
  <action>
    Verificar (sin modificar código de runtime ni tests — brownfield, lockstep es Phase 31):

    1. BOOT/LOAD: ejecutar la carga de contenido vía content-loader sobre presente-regolare.json y comprobar que `validateContent` (schema-validator) la acepta sin lanzar — `categoryIds` referencia la categoría registrada, los 6 slots cumplen el shape de variantes, multiple-choice/word-buttons válidos. Hacerlo con un `node -e` que importe el content-loader / schema-validator y valide el archivo (o el script de fixture existente si lo hay).

    2. SUITE BASELINE: ejecutar `node --test tests/*.test.js` (path con glob — el path desnudo falla en Node 22.20, memoria test_command_node_glob). Confirmar que NO hay regresiones NUEVAS atribuibles al alta. El fail preexistente AJENO de `genero-numero` (content count 12→13, documentado en STATE.md) NO cuenta como regresión. Los hardcodes de count (`TOTAL_EXPECTED=183`, `CATEGORIES_WITH_EXPLANATIONS`) NO se tocan en esta fase: la suite baseline sigue verde porque presente-regolare aún no está en esos arrays (Phase 31 los sincroniza). Si algún test se vuelve rojo por la presencia del nuevo archivo (p.ej. un test que itera content/exercises/*.json y exige una entrada en una tabla), DOCUMENTARLO como hallazgo para Phase 31 — NO modificar la tabla aquí.

    3. CONTEO DEFINITIVO: leer el nº real de slots de presente-regolare.json (`exercises.length`, debe ser 6 = N) y registrarlo explícitamente en el SUMMARY junto con los ids de slot, para que Phase 31 sincronice `TOTAL_EXPECTED` (`183 → 183 + N`) y el smoke contra el número REAL del JSON (no estimación).

    4. USABILIDAD: confirmar que con la categoría registrada + válida, el flujo home/picker/Repaso 20/Examen la incluye automáticamente (no requiere cambio de motor); basta verificar que la categoría aparece en el listado derivado de categories.json + tiene ejercicios cargables. Documentar la verificación.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `validateContent` acepta `content/exercises/presente-regolare.json` (boot/load sin error de shape) — verificado por `node -e` importando schema-validator/content-loader.
    - `node --test tests/*.test.js` no introduce regresiones NUEVAS (el único fail tolerado es el preexistente ajeno de genero-numero, documentado en STATE.md).
    - El conteo real de slots (N = `exercises.length` = 6) queda registrado en el SUMMARY con la lista de ids, listo para Phase 31.
    - La categoría presente-regolare aparece en el listado derivado de categories.json (usable en home/picker/Repaso/Examen) sin cambios de motor.
    - 0 archivos `.js` de runtime o tests modificados (lockstep es Phase 31).
  </acceptance_criteria>
  <done>La categoría carga en boot, pasa el schema-validator, la suite baseline sigue verde, y el conteo N=6 queda registrado para Phase 31. PRES-01 satisfecho end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/exercises/presente-regolare.json → content-loader/schema-validator en boot | Misma frontera que el plan 30-01: el JSON estático cruza al validador al arrancar. Este plan solo LEE y verifica; no escribe contenido. Sin input de usuario, sin red, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Denial of Service | boot falla si presente-regolare.json no satisface el shape contract | mitigate | Este plan ES la mitigación: ejecuta el boot/load real + `validateContent` + suite completa; si el JSON no carga, la fase no cierra hasta corregirlo (loop a 30-01). |
| T-30-08 | Tampering | un test que itera content/exercises/*.json se vuelve rojo por el archivo nuevo | mitigate | El plan distingue regresión NUEVA (bloquea) del fail preexistente ajeno (tolerado, documentado en STATE.md); cualquier rojo nuevo por count se documenta como hallazgo para Phase 31, NO se parchea aquí (preserva el lockstep). |

**No high-severity threats apply.** Plan de verificación read-only: no escribe contenido ni código. La única superficie es que el JSON dado de alta no cargue (mitigado por este mismo plan ejecutando boot + suite antes de cerrar la fase).
</threat_model>

<verification>
- `node -e` (schema-validator/content-loader): presente-regolare.json acepta sin error.
- `node --test tests/*.test.js`: sin regresiones nuevas (solo el fail preexistente ajeno de genero-numero tolerado).
- Conteo N=6 registrado en el SUMMARY con ids de slot (input de Phase 31).
- Categoría presente en el listado derivado de categories.json (usable end-to-end).
</verification>

<success_criteria>
- presente-regolare carga en boot y pasa el schema-validator.
- Suite baseline verde (sin regresiones nuevas).
- Usable en home/picker/Repaso 20/Examen como las otras 9.
- Conteo definitivo N=6 + ids registrados para Phase 31.
- 0 archivos de runtime/tests modificados (lockstep diferido a Phase 31).
</success_criteria>

<output>
Create `.planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-03-SUMMARY.md` when done. Incluir: resultado del boot/load + schema-validator, conteo de tests (pass/fail con el fail ajeno identificado), N=6 + lista de ids de slot para Phase 31, y la confirmación de usabilidad end-to-end.
</output>
