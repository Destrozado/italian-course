---
phase: 30-alta-de-presente-regolare-registro-slots-de-regla-variantes-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/categories.json
  - content/exercises/presente-regolare.json
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03, PRES-05, PRES-06]
must_haves:
  truths:
    - "La categoría presente-regolare aparece en content/categories.json con order 10"
    - "Existe content/exercises/presente-regolare.json con exactamente 6 slots de regla"
    - "Cada slot tiene >=2 variantes intercambiables (multiple-choice y/o word-buttons), el slot -isc- >=3 y con word-buttons"
    - "Cada slot tiene explanation curada a nivel de slot en canon español acentuado plain-text"
    - "Las 6 personas io/tu/lui/noi/voi/loro aparecen al menos una vez a lo largo de la categoría"
    - "0 ejercicios match, con la razón documentada en un campo notes autor-internal"
  artifacts:
    - path: "content/categories.json"
      provides: "Registro de la categoría presente-regolare (order 10)"
      contains: "presente-regolare"
    - path: "content/exercises/presente-regolare.json"
      provides: "6 slots de regla del presente indicativo regular, nacidos en slot+variantes"
      contains: "presente-regolare-"
  key_links:
    - from: "content/exercises/presente-regolare.json"
      to: "content/categories.json"
      via: "categoryIds: [presente-regolare] referencia el id registrado"
      pattern: "presente-regolare"
    - from: "content/exercises/presente-regolare.json"
      to: "src/data/schema-validator.js"
      via: "shape contract (validateContent) en boot"
      pattern: "variants"
---

<objective>
Dar de alta la 10ª categoría `presente-regolare` (presente indicativo de verbos regulares) nacida directamente en formato slot+variantes: registrar la categoría en `content/categories.json` (order 10) y materializar `content/exercises/presente-regolare.json` con los 6 slots de regla, cada uno con ≥2 variantes intercambiables y `explanation` curada a nivel de slot. TODAS las variantes nacen con `validation.status: "pending"` (el quórum las valida en el plan 30-02).

Purpose: Es el cimiento verbal A1 que faltaba — el corazón del milestone v1.7. La categoría debe quedar con el conteo de slots DEFINITIVO (N=6) para que Phase 31 sincronice los counts/TOTAL_EXPECTED a él.

Output: `content/categories.json` con una entrada nueva; `content/exercises/presente-regolare.json` con 6 slots + un campo `notes` autor-internal documentando el 0-match (D-30-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-CONTEXT.md
@.planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-PATTERNS.md

<interfaces>
<!-- Shapes exactos que el schema-validator enforca al boot. Extraídos de essere.json/avere.json. NO explorar el codebase. -->

content/categories.json — array `categories[]`; cada entrada `{ "id": slug-ascii, "name": string-italiano, "order": int }`. Última entrada actual: `{ "id": "partitivos", "name": "Partitivi", "order": 9 }`.

content/exercises/*.json — objeto único `{ "exercises": [ slot, ... ] }`. Cada slot:
- `id` — slug kebab único global, prefijo `presente-regolare-…`
- `type` — uno de `"multiple-choice"` | `"word-buttons"` (este plan NO usa `"match"`)
- `categoryIds` — array no vacío de ids de categoría conocidos → `["presente-regolare"]`
- `explanation` — string no vacío a nivel de slot (REQUERIDO con `variants[]`); plain-text español acentuado
- `variants` — array no vacío; cada entry es una superficie FLAT del `type` del slot
- `validation` — `{ "status": "pending", "passes": [] }` en este plan (lo rellena el quórum en 30-02)

Superficie `multiple-choice` (de essere.json `essere-sono`): `{ "prompt": "Io ___ italiano.", "options": [3 o 4 strings], "correctIndex": int }`. `prompt` DEBE contener el hueco `___`. `options` exactamente 3 o 4 strings no vacíos. `correctIndex` en `[0, options.length)`.

Superficie `word-buttons` (de avere.json `avere-wb-posesion`): `{ "prompt": "Yo tengo un coche.", "answer": ["io","ho","una","macchina"], "distractors": ["hai","sono"] }`. CONVENCIÓN load-bearing: `prompt` = frase ESPAÑOLA a traducir; `answer[]` = tokens ITALIANOS ordenados de la frase completa; `distractors[]` = formas verbales ERRÓNEAS (no sílabas, no letras). Construye la FRASE completa (D-30-07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar la categoría presente-regolare en categories.json (order 10)</name>
  <files>content/categories.json</files>
  <read_first>
    - content/categories.json (el archivo a modificar, las 9 entradas existentes)
    - .planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-PATTERNS.md (sección `content/categories.json` — shape exacto)
    - src/data/schema-validator.js (líneas ~77-90, contrato de la entrada de categoría: id slug `^[a-z0-9][a-z0-9-]*$` único, name no vacío)
  </read_first>
  <action>
    Añadir UNA entrada nueva al final del array `categories` en content/categories.json (per PRES-01, D-30): id `presente-regolare`, name `Presente indicativo (verbi regolari)` (italiano, espejo del estilo de las otras 9), order `10`. Mantener el formato de columnas hand-aligned del archivo (cosmético, coincide con el estilo hand-edited). NO tocar las 9 entradas existentes. El id debe ser único (no duplicado) y coincidir EXACTAMENTE con el `categoryIds` que usará el JSON de ejercicios de la Task 2.
  </action>
  <verify>
    <automated>node -e "const c=require('./content/categories.json');const e=c.categories.find(x=>x.id==='presente-regolare');if(!e)throw new Error('falta entrada');if(e.order!==10)throw new Error('order != 10');if(c.categories.filter(x=>x.id==='presente-regolare').length!==1)throw new Error('duplicado');console.log('OK',e.name)"</automated>
  </verify>
  <acceptance_criteria>
    - `content/categories.json` parsea como JSON válido.
    - Existe exactamente UNA entrada con `id === "presente-regolare"` y `order === 10`.
    - `name` es un string italiano no vacío.
    - Las 9 entradas existentes quedan intactas (orders 1-9 sin cambios).
  </acceptance_criteria>
  <done>La categoría presente-regolare está registrada con order 10 y el id coincide con el categoryIds del JSON de ejercicios.</done>
</task>

<task type="auto">
  <name>Task 2: Autorar content/exercises/presente-regolare.json — 6 slots de regla + variantes + explanations + notes</name>
  <files>content/exercises/presente-regolare.json</files>
  <read_first>
    - content/exercises/essere.json (esquema slot+variantes de referencia: multiple-choice `essere-sono`, word-buttons `essere-wb-identidad`, bloque validation, explanation a nivel de slot)
    - content/exercises/avere.json (word-buttons `avere-wb-posesion` answer[]+distractors[] frase completa — patrón a copiar para D-30-07)
    - .planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-PATTERNS.md (todas las superficies + el ALERTA del campo `notes` sin precedente)
    - .planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-CONTEXT.md (D-30-01..07: eje de variación verbo+persona, N=6, 6 personas, refuerzo -isc-, 0 match, mix de tipos, framing word-buttons)
    - src/data/schema-validator.js (contrato de shape que el archivo debe satisfacer: validateContent, validateVariants, validateMultipleChoiceSurface, validateWordButtonsSurface)
    - Memoria del agente `exercise-authoring-rules` (R1-R7, OBLIGATORIA — no es fichero del repo, vive en memoria; aplicarla íntegra)
    - Memoria del agente `gloss-es-desambiguacion-canon` (el gloss "(en español: …)" es canon R7, NO leak)
  </read_first>
  <action>
    Crear content/exercises/presente-regolare.json como objeto `{ "notes": "...", "exercises": [ ...6 slots... ] }`.

    Materializar EXACTAMENTE 6 slots de regla (D-30-02), `categoryIds: ["presente-regolare"]` en todos, ids kebab `presente-regolare-<slug>`:
    1. slot `-are` (id p.ej. `presente-regolare-are`): verbos A1 regulares en -are (parlare, lavorare, studiare, abitare…). ≥2 variantes.
    2. slot `-ere` (id `presente-regolare-ere`): temere, prendere, scrivere, vedere… ≥2 variantes.
    3. slot `-ire` simple (id `presente-regolare-ire`): dormire, partire, aprire (verbos nombrados en ROADMAP SC-2). ≥2 variantes.
    4. slot `-ire` con `-isc-` (id `presente-regolare-isc`): finire, capire, preferire, pulire (verbos nombrados). REFUERZO (D-30-04): ≥3 variantes Y al menos una variante word-buttons OBLIGATORIA en este slot.
    5. slot ortográficos velares `-care/-gare` (id `presente-regolare-velar`): UNA sola regla (añadir `h` ante `-i` para conservar el sonido velar: giocare→giochi, pagare→paghi). ≥2 variantes. NO separar -care de -gare.
    6. slot ortográficos palatales `-ciare/-giare` (id `presente-regolare-palatal`): UNA sola regla (perder la `i` átona ante `-i`: cominciare→cominci, mangiare→mangi). ≥2 variantes. NO separar -ciare de -giare.

    EJE DE VARIACIÓN (D-30-01): dentro de cada slot las variantes varían VERBO + PERSONA simultáneamente (var1 io+verboA, var2 tu+verboB, etc.) → re-hacer el slot presenta léxico Y persona nuevos del mismo slot, obligando a aplicar la regla a un lexema fresco.

    COBERTURA DE PERSONAS (D-30-03): a lo largo del conjunto de variantes de los 6 slots, las 6 personas io/tu/lui/noi/voi/loro deben aparecer ≥1 vez. Ningún slot necesita las 6; la cobertura es de categoría. Vigilar voi/loro (las menos frecuentes).

    MIX DE TIPOS (D-30-06): multiple-choice en TODOS los slots (núcleo de re-verificación rápida). word-buttons selectivo: OBLIGATORIO en el slot -isc-; en los otros slots, añadir word-buttons donde aporte (balance estilo avere/essere) a discreción. NO solo-multi-choice; NO forzar ambos tipos en cada slot.

    SUPERFICIES:
    - multiple-choice: prompt italiano con hueco `___` (p.ej. `"Io ___ italiano."`), 3 o 4 options con DISTRACTORES que sean conjugaciones ERRÓNEAS del mismo/cercano verbo (para `parlo`: `parli`/`parla`/`parlate`), correctIndex apunta a la forma correcta.
    - word-buttons (D-30-07): prompt = frase ESPAÑOLA; answer[] = tokens italianos de la FRASE completa (`["io","finisco","il","lavoro"]`); distractors[] = formas verbales erróneas (`"fino"`,`"finisci"`). NUNCA sílabas/letras (rompería render — prohibido en brownfield).

    EXPLANATION (PRES-05, a nivel de slot): cada slot lleva una `explanation` curada en canon ortográfico ESPAÑOL acentuado, plain-text, apóstrofes ASCII, regla + ejemplo paralelo italiano-español. Coherente con la cobertura editorial 100% de las 9 categorías (espejo del estilo de essere.json). RESPETAR R1-R7: la explanation enseña al ALUMNO (R4, sin meta "cierra la serie"), nunca referencia otros ejercicios por #NNN (R2), una modificación pedagógica por ejercicio (R6). El gloss "(en español: …)" en prompts es canon R7 admitido (NO es leak).

    R7 una sola opción válida: el prompt admite UNA forma correcta; si hay ambigüedad de persona (p.ej. `sono` io/loro) añadir contexto o gloss ES para desambiguar.

    0 MATCH (D-30-05/PRES-06): NO incluir ningún slot `type: "match"`. La conjugación io→parlo es derivable por raíz (raíz + desinencia) → un match sería "arrastrar sin pensar" (viola D-04/R3). Documentar el porqué en un campo `notes` autor-internal: usar un string top-level `"notes"` HERMANO de `"exercises"` (NO dentro de un ejercicio) — el validator ignora claves desconocidas, así que no rompe el boot; es file-scoped y no colisiona con ningún campo que el validator inspeccione. El texto del notes explica que 0-match es decisión D-04 (derivable por raíz), no omisión, y contrasta con avere/essere (ho/hai/ha NO derivables por raíz → aquéllas SÍ tienen match).

    VALIDATION: cada slot nace con `"validation": { "status": "pending", "passes": [] }`. NO rellenar passes a mano — lo hace el quórum en el plan 30-02.

    Verbos concretos más allá de los nombrados: elegir A1 de alta frecuencia, evitando irregulares, manteniendo R6.
  </action>
  <verify>
    <automated>node -e "const d=require('./content/exercises/presente-regolare.json');const ex=d.exercises;if(ex.length!==6)throw new Error('slots != 6: '+ex.length);if(typeof d.notes!=='string'||!d.notes.length)throw new Error('falta notes top-level');for(const e of ex){if(!Array.isArray(e.categoryIds)||e.categoryIds[0]!=='presente-regolare')throw new Error('categoryIds malo en '+e.id);if(e.type==='match')throw new Error('match prohibido en '+e.id);if(!e.explanation||!e.explanation.length)throw new Error('falta explanation en '+e.id);if(!Array.isArray(e.variants)||e.variants.length<2)throw new Error('<2 variantes en '+e.id);if(!e.validation||e.validation.status!=='pending')throw new Error('validation no pending en '+e.id);}const isc=ex.find(e=>e.id.includes('isc'));if(!isc||isc.variants.length<3)throw new Error('slot -isc- necesita >=3 variantes');console.log('OK 6 slots, notes presente, -isc- reforzado')"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/presente-regolare.json` parsea como JSON válido.
    - `exercises[]` tiene EXACTAMENTE 6 entradas (slots), todas con `categoryIds: ["presente-regolare"]`.
    - Cada slot tiene `explanation` no vacío, `variants[]` con ≥2 entradas, y `validation.status === "pending"` con `passes: []`.
    - El slot `-isc-` tiene ≥3 variantes e incluye al menos una variante de tipo word-buttons (su `type` es `word-buttons`, o el slot está duplicado en multi-choice + un slot word-buttons — ver nota abajo).
    - Existe un campo top-level `notes` (string no vacío) hermano de `exercises`, documentando el 0-match (D-30-05).
    - 0 slots de `type: "match"`.
    - Las 6 personas (io/tu/lui/noi/voi/loro) aparecen ≥1 vez en el conjunto de prompts/answer de las variantes (verificable por revisión: el executor lista la cobertura en el SUMMARY).
    - Cada variante multiple-choice: prompt con hueco `___`, 3 o 4 options, correctIndex válido. Cada variante word-buttons: answer[] frase completa italiana, distractors[] formas erróneas.
  </acceptance_criteria>
  <done>El JSON tiene los 6 slots definitivos con sus variantes (pending), explanations curadas, notes del 0-match, y cobertura de las 6 personas. El conteo N=6 queda fijado para Phase 31.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido hand-authored → content-loader/schema-validator en boot | El único input que cruza una frontera de confianza es el archivo JSON estático autorado a mano; lo consume `src/data/schema-validator.js` al arrancar. No hay input de usuario, ni red, ni auth en esta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | content/exercises/presente-regolare.json (JSON malformado o shape inválido) | mitigate | El shape contract de `src/data/schema-validator.js` (validateContent/validateVariants/validateMultipleChoiceSurface/validateWordButtonsSurface) rechaza al boot cualquier slot inválido; el plan 30-03 verifica boot + suite verde antes de cerrar la fase. |
| T-30-02 | Tampering | claves prototype-pollution-style (`__proto__`/`constructor`) en JSON hand-authored | accept | Contenido autorado a mano por el único autor (single-user, local, sin red); el riesgo de inyección de claves `__proto__` es nulo en la práctica. `JSON.parse` nativo no asigna `__proto__` como propiedad enumerable por defecto. Sin superficie de explotación. |
| T-30-03 | Denial of Service | content load falla en boot por JSON inválido (la app no arranca) | mitigate | El plan 30-03 ejecuta un boot/load real + `node --test tests/*.test.js` para garantizar que la categoría carga y la suite baseline sigue verde antes de cerrar. |

**No high-severity threats apply.** Esta es una fase de CONTENT-AUTHORING pura: añade JSON estático + 1 entrada de registro. NO toca el motor, NO añade manejo de input de usuario, NO añade llamadas de red ni auth, NO cambia ningún code path (brownfield). La superficie realista es JSON malformado/shape-inválido (mitigado por el schema-validator existente + la verificación de boot del plan 30-03).
</threat_model>

<verification>
- `node -e` sobre categories.json: entrada presente-regolare order 10, única.
- `node -e` sobre presente-regolare.json: 6 slots, notes top-level, -isc- ≥3 variantes + word-buttons, 0 match, todas pending.
- Revisión de cobertura: las 6 personas io/tu/lui/noi/voi/loro presentes en el conjunto de variantes (el executor lista la distribución en el SUMMARY).
- El JSON satisface el shape contract de schema-validator (verificado en boot por el plan 30-03; aquí basta con que parsee y cumpla las invariantes de slot).
</verification>

<success_criteria>
- Categoría `presente-regolare` registrada en categories.json con order 10.
- `content/exercises/presente-regolare.json` con 6 slots de regla, cada uno ≥2 variantes (-isc- ≥3 + word-buttons), explanation curada a nivel de slot, validation pending.
- Campo `notes` autor-internal documenta el 0-match (D-30-05).
- Las 6 personas cubiertas a nivel de categoría.
- 0 ejercicios match.
</success_criteria>

<output>
Create `.planning/phases/30-alta-de-presente-regolare-registro-slots-de-regla-variantes-/30-01-SUMMARY.md` when done. Incluir en el SUMMARY: la lista de los 6 slots con sus ids, el nº de variantes por slot, qué slots llevan word-buttons, y la tabla de cobertura de las 6 personas (qué persona aparece en qué slot/variante).
</output>
