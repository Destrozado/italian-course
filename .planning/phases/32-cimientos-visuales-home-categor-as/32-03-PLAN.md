---
phase: 32-cimientos-visuales-home-categor-as
plan: 03
type: execute
wave: 1
depends_on: [32-01, 32-02]
files_modified: [app.css, styles.css]
autonomous: false
gap_closure: true
requirements: [FND-03]

must_haves:
  truths:
    - "El banner ⚠ de export ('Aún no has exportado tu progreso') es legible: texto tinta sobre papel, contraste AA ≥ 4.5:1"
    - "Los nombres de categoría (Avere, Essere, …) y el tema en cursiva son legibles: tinta sobre papel"
    - "Ninguna superficie pintada por Pico (article/header/footer/form/table) muestra el color stock de Pico encima del papel — todas heredan papel/tinta (salvo --ed-paper-elevated intencional)"
    - "Pico sigue presente como reset/base (no se elimina)"
  artifacts:
    - path: "app.css"
      provides: "Bloque :root que remapea los --pico-* de color a los tokens --ed-*"
      contains: "--pico-background-color"
    - path: "styles.css"
      provides: ".backup-banner y reglas legacy de color auditadas para contrastar con papel"
      contains: ".backup-banner"
  key_links:
    - from: "app.css :root remap"
      to: "Pico component surfaces (article/header/footer)"
      via: "custom property override en :root"
      pattern: "--pico-card-background-color:\\s*var\\(--ed-"
    - from: "styles.css .backup-banner / .inflight-banner"
      to: "--ed-* tokens"
      via: "color/background tintado legible sobre papel"
      pattern: "backup-banner"
---

<objective>
Cerrar **GAP-01** (FND-03 — "modo papel siempre"): el fondo `body` ya es papel,
pero Pico sigue pintando sus superficies de componente (`<article>`, `<header>`,
`<footer>`, formularios, tablas) con sus tokens stock de tema claro, creando una
"card" del color viejo encima del papel. Además los banners de aviso (`.backup-banner`,
`.inflight-banner`) y el texto de los nombres de categoría quedan sin contraste
(texto ≈ fondo) porque heredan colores que ya no contrastan con la paleta papel.

Purpose: dejar la Home/Categorías legible y coherente con la paleta Editoriale —
el banner ⚠ de export y los nombres/temas de categoría se leen, sin parches de
fondo blanco de Pico. Es el último gap de la fase 32.

Output: bloque de remapeo `--pico-*` → `--ed-*` en `app.css` + auditoría de
`.backup-banner`/banners legacy en `styles.css`. Cambio CSS-only — sin tocar
markup funcional ni motor.

**Arreglo APROBADO y NO negociable:** mantener Pico como base y REMAPEAR los
`--pico-*` de color a la paleta Editoriale. NO eliminar Pico (contradice el UI-SPEC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-VERIFICATION.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-HUMAN-UAT.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-CONTEXT.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-01-SUMMARY.md

<source_of_truth>
<!-- Tokens Editoriale --ed-* — ya definidos en :root de app.css (Plan 01). Úsalos
     directamente como valor de los --pico-* de color; NO inventes hex nuevos. -->

Paleta papel (32-UI-SPEC §Color / 32-CONTEXT D-03 / app.css :root):
  --ed-paper           #f4f0e8   (fondo app — dominante)
  --ed-paper-elevated  #fbf8f1   (tarjetas destacadas — único color "card" intencional)
  --ed-surface         #fbfaf6   (superficies claras / segmento crema tricolore)
  --ed-ink             #2b2722   (texto principal, títulos, nombres de categoría)
  --ed-muted           #8c8576   (texto secundario / labels)
  --ed-faint           #a39a88   (subtítulos cursiva, meta)
  --ed-faint-2         #a89f8c   (overlines)
  --ed-hairline        rgba(43,39,34,0.09)   (separadores)
  --ed-border-soft     rgba(43,39,34,0.16)   (bordes ghost)
  --ed-green           #2f7d56   (acento primario)
  --ed-red             #b5412e   (error / tricolore derecho)
  --ed-amber           #b9852f   (dot hecha)

Decisión bloqueada (32-CONTEXT D-03 / 32-UI-SPEC §Design System "Dark mode DISABLED"):
  Pico = reset/base; capa app.css ENCIMA (linkeada después de styles.css). NO quitar Pico.
</source_of_truth>

<interfaces>
<!-- Custom properties stock de color de Pico 2.1.1 que pintan las superficies del
     componente. Son los que hay que remapear a --ed-* en :root. Lista derivada del
     GAP-01 (32-VERIFICATION líneas 209-211) + auditoría de styles.css. -->

Superficies y texto (causa directa de la "card" sobre el papel):
  --pico-background-color                      (fondo de body/contenedores)
  --pico-color                                 (texto base)
  --pico-card-background-color                 (<article> fondo)
  --pico-card-sectioning-background-color      (<header>/<footer> de <article> — el banner ⚠ vive aquí)
  --pico-muted-color                           (.picker-warning, .delta-*, .backup-banner hereda)
  --pico-muted-border-color                    (separadores legacy)
  --pico-h1-color … --pico-h6-color            (color de headings)
  --pico-primary                               (acento azul Pico — switch checked, focus)
  --pico-secondary / --pico-secondary-background (botones .secondary — "Ir a Backup")
  --pico-border-color                          (bordes de form/table)

Verde/rojo semánticos que styles.css ya consume (mantener legibles, no son el gap pero conviven):
  --pico-color-green-500 / --pico-color-red-500 / --pico-color-amber-500

Reglas legacy en styles.css que asumen el tema viejo (auditar contraste sobre papel):
  .backup-banner   (styles.css ~línea 335 — border amber + background rgba amber 8%)
  .inflight-banner (styles.css ~línea 136 — mismo patrón)
  .picker-warning  (styles.css ~línea 105 — color --pico-muted-color)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Remapear los --pico-* de color a la paleta Editoriale en app.css</name>
  <files>app.css</files>
  <read_first>
    - app.css (bloque :root con tokens --ed-* ya definido en Plan 01, líneas ~132-171; aquí se AÑADE el remapeo de --pico-* DESPUÉS de los --ed-*, dentro del mismo :root o un :root contiguo)
    - styles.css (qué --pico-* consumen las reglas legacy — banners, badges, picker-warning)
    - 32-UI-SPEC.md §Color (mapeo de roles → tokens) y §Design System (Pico = base, dark mode off)
    - 32-CONTEXT.md D-03 (paleta papel siempre)
  </read_first>
  <action>
    Añadir en `app.css` un bloque `:root` (después de la declaración de tokens `--ed-*`
    existente, para ganar la cascada) que remapee los custom properties de color de Pico
    a la paleta Editoriale, de modo que TODA superficie pintada por Pico (article/header/
    footer/form/table) herede papel/tinta. Mapeo concreto (usa `var(--ed-*)`, no hex):
      - `--pico-background-color: var(--ed-paper)`
      - `--pico-color: var(--ed-ink)`
      - `--pico-card-background-color: var(--ed-paper)` (el `<article class="home-editorial">` deja de pintar card de color)
      - `--pico-card-sectioning-background-color: var(--ed-paper)` (CLAVE — `<header>/<footer>` donde viven los banners ⚠ dejan de tapar el texto)
      - `--pico-muted-color: var(--ed-muted)`
      - `--pico-muted-border-color: var(--ed-hairline)`
      - `--pico-h1-color`..`--pico-h6-color: var(--ed-ink)`
      - `--pico-primary: var(--ed-green)` y `--pico-primary-background: var(--ed-green)` (acento Editoriale en vez de azul Pico)
      - `--pico-secondary` / `--pico-secondary-background` → un neutro legible sobre papel (`var(--ed-muted)` para texto/borde; el botón "Ir a Backup" debe leerse)
      - `--pico-border-color: var(--ed-border-soft)`
    NO uses `--ed-paper-elevated` como fondo global — es el ÚNICO color "card" intencional
    y se reserva para tarjetas destacadas de P33-34, no para las superficies de Home.
    Comenta el bloque con el banner de sección al estilo de app.css (`/* ─── Remap Pico tokens (FND-03 / GAP-01) ─── */`) explicando que cierra el GAP-01 de contraste.
    Mantén Pico cargado y como base — este bloque solo redefine sus variables, NO lo elimina.
  </action>
  <acceptance_criteria>
    <source>`grep -E -- '--pico-background-color:\s*var\(--ed-paper\)' app.css` → coincide</source>
    <source>`grep -E -- '--pico-card-background-color:\s*var\(--ed-paper\)' app.css` → coincide</source>
    <source>`grep -E -- '--pico-card-sectioning-background-color:\s*var\(--ed-' app.css` → coincide</source>
    <source>`grep -E -- '--pico-color:\s*var\(--ed-ink\)' app.css` → coincide</source>
    <source>`grep -E -- '--pico-primary(-background)?:\s*var\(--ed-green\)' app.css` → coincide (acento Editoriale)</source>
    <source>`grep -v '^[[:space:]]*[/*]' app.css | grep -Ec -- '--pico-(background-color|color|card-background-color|card-sectioning-background-color|muted-color|primary)' app.css` ≥ 6 (excluye líneas de comentario)</source>
    <source>Pico sigue enlazado: `grep -ci 'pico' index.html` ≥ 1 y `app.css` NO contiene `@import` que elimine Pico — el remapeo solo redefine variables.</source>
    <behavior>Tras el remapeo, ningún `--pico-*-background-color` referenciado por las superficies de Home resuelve a un color distinto del papel (salvo el --ed-paper-elevated intencional, que NO se usa como fondo global).</behavior>
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vcompanyb/italian-course && grep -E -- '--pico-card-sectioning-background-color:\s*var\(--ed-' app.css && grep -E -- '--pico-background-color:\s*var\(--ed-paper\)' app.css && node --test tests/*.test.js 2>&1 | tail -3</automated>
  </verify>
  <done>
    `app.css` contiene un bloque `:root` que remapea al menos background, color,
    card-background, card-sectioning-background, muted-color, headings, primary/secondary
    y border-color a tokens `--ed-*`. La suite de tests sigue en 494 pass / 1 fail
    (el único fail es el preexistente AJENO `genero-numero`; cero fails nuevos — este
    cambio es CSS-only). Pico sigue cargado como base.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: Auditar .backup-banner y reglas legacy de color en styles.css para contraste sobre papel</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (`.backup-banner` ~línea 335; `.inflight-banner` ~línea 136; `.picker-warning` ~línea 105; `.backup-message-*` ~línea 356; banderas de color que asumen tema viejo)
    - app.css (el remap de la Tarea 1 — qué heredan ahora las reglas legacy)
    - 32-UI-SPEC.md §Color (red --ed-red para error/aviso; ink/muted para texto)
  </read_first>
  <action>
    Auditar las reglas de `styles.css` que asumen el tema claro stock y asegurar que
    contrastan con el papel ahora que Pico hereda papel/tinta:
      - `.backup-banner` (border amber + background rgba amber 8%): verificar que el
        TEXTO del banner ("Aún no has exportado tu progreso") usa tinta legible
        (hereda `color: var(--ed-ink)` o decláralo explícito) — NO debe quedar texto
        del color del fondo. El glyph ⚠ y el botón "Ir a Backup" deben leerse.
        Mantener el tinte amber del border/background (aviso) pero garantizar contraste
        AA ≥ 4.5:1 del texto contra ese fondo tintado.
      - `.inflight-banner`: mismo patrón, misma garantía de contraste del texto.
      - `.picker-warning` y `.backup-message-*`: confirmar que el color (`--pico-muted-color`
        ya remapeado a `--ed-muted`, verde/rojo semánticos) es legible sobre papel.
      - Si alguna regla fijaba un `background` claro stock que ahora compite con el papel,
        ajustarla a `transparent` o a `var(--ed-*)`.
    Solo CSS de color/contraste; NO cambiar el markup de los banners, ni sus bindings
    Alpine (`x-text="backupBannerText"`, `@click="currentScreen='backup'"`), ni el motor.
    Donde toques color, prefiere los tokens `--ed-*` (con fallback hex si el patrón
    legacy lo usaba) para coherencia con la capa Editoriale.
  </action>
  <acceptance_criteria>
    <source>`grep -n 'backup-banner' styles.css` → la regla sigue presente (no se borra el banner).</source>
    <source>El texto del banner tiene color tinta legible: `grep -E -- '\.backup-banner[^{]*\{[^}]*color:\s*var\(--ed-ink' styles.css` O bien hereda `color` del documento que ya es `--ed-ink` (documentar en el SUMMARY cuál de las dos vías se usó).</source>
    <source>`grep -c 'x-html' styles.css` → 0 (sin sumideros DOM nuevos; cambio CSS-only).</source>
    <behavior>El contraste del texto del banner ⚠ contra su fondo tintado (rgba amber 8% sobre papel) es AA ≥ 4.5:1 — `--ed-ink` (#2b2722) sobre papel/amber-8% cumple holgadamente; verificar que NINGUNA regla deja el texto en un color ≈ fondo.</behavior>
    <behavior>No queda ninguna superficie de Home con fondo blanco/azul stock de Pico salvo `--ed-paper-elevated` intencional.</behavior>
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vcompanyb/italian-course && grep -n 'backup-banner' styles.css && [ "$(grep -c 'x-html' styles.css)" -eq 0 ] && node --test tests/*.test.js 2>&1 | tail -3</automated>
  </verify>
  <done>
    `.backup-banner`, `.inflight-banner`, `.picker-warning` y `.backup-message-*`
    auditados: el texto de los banners se lee con contraste AA sobre el papel/tinte,
    sin fondos stock de Pico compitiendo con el papel. Markup y bindings intactos.
    Suite sigue 494 pass / 1 fail (mismo fail preexistente ajeno).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 3 (checkpoint): Re-test humano de contraste/legibilidad en navegador (UAT items 1, 2, 5)</name>
  <action>Tras las Tareas 1 y 2 (CSS-only, ya verificadas por grep + suite), PAUSAR para confirmacion humana en navegador del renderizado real (no automatizable en este entorno). El autor abre la app con npx serve y re-testea los items 1, 2 y 5 de 32-HUMAN-UAT segun los pasos de how-to-verify. Sin accion de implementacion adicional: este checkpoint solo valida que el GAP-01 esta cerrado visualmente.</action>
  <what-built>
    Remapeo de los `--pico-*` de color a la paleta Editoriale (Tarea 1) + auditoría
    de contraste de los banners legacy (Tarea 2). Cambio CSS-only que hace que las
    superficies de Pico hereden papel/tinta y que el banner ⚠ de export y los nombres
    de categoría se lean.
  </what-built>
  <how-to-verify>
    Re-test HUMANO en navegador de los items 1, 2 y 5 de 32-HUMAN-UAT.md (los que el
    GAP-01 afectaba):
    1. Arranca el servidor: `npx serve` en `/home/vcompanyb/italian-course` y abre
       `http://localhost:3000` (NO `file://`).
    2. **(Item 1 — fondo papel sin card vieja):** Confirma que el fondo de la Home es
       papel cálido (#f4f0e8) y que el `<article>`/`<header>`/`<footer>` NO muestran una
       "card" de color distinto al papel encima. El banner ⚠ "Aún no has exportado tu
       progreso" (aparece si han pasado >7 días desde el último backup; si no aparece,
       fuerza el estado borrando `lastBackupAt` en DevTools → Application → Local Storage
       o avanza la fecha) DEBE leerse con claridad: texto tinta oscuro sobre el banner.
    3. **(nombres de categoría):** En la lista/tabla de categorías, los nombres
       (Avere, Essere, …) y el tema en cursiva deben leerse con contraste claro sobre
       el papel — NO texto ≈ fondo.
    4. **(Item 2 — tricolore):** La barra tricolore verde/crema/rojo sigue visible en
       la cabecera (no debe haberse roto con el remapeo).
    5. **(Item 5 — dots de estado):** Con categorías en distintos estados, los dots
       siguen siendo verde (dominada) / ámbar (hecha) / gris (no-hecha) y la barra de
       racha se rellena del color correcto.
    Resultado esperado: todo legible, paleta papel coherente, sin azul/blanco stock de
    Pico ni "card" del color viejo; tricolore y dots intactos.
  </how-to-verify>
  <resume-signal>Escribe "approved" si el banner ⚠ y los nombres de categoría se leen y no hay card de color viejo; o describe qué superficie/texto sigue sin contraste.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | App estática 100% local, sin servidor, sin red, sin cuentas, un solo usuario. No hay frontera de confianza nueva en este cambio CSS-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-XSS | Tampering/Info-disclosure | Render de contenido en Home (banners, nombres de categoría) | accept | El único vector relevante (XSS) ya está mitigado y se PRESERVA: la app usa `x-text`, NUNCA `x-html`. Este plan es CSS-only (no añade markup ni sumideros DOM). La acceptance de la Tarea 2 verifica `grep -c 'x-html' styles.css` → 0. Sin nuevas entradas de datos. |
| T-32-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — cero instalaciones de paquetes; cambio CSS puro sobre ficheros existentes. |

Sin amenazas de severidad alta/media: app offline, local, single-user; este cambio
no introduce red, persistencia nueva, ni sumideros DOM.
</threat_model>

<verification>
- `grep -E -- '--pico-card-sectioning-background-color:\s*var\(--ed-' app.css` → presente (la "card" del header deja de tapar el banner ⚠).
- `grep -E -- '--pico-background-color:\s*var\(--ed-paper\)' app.css` → presente.
- `grep -n 'backup-banner' styles.css` → regla presente (banner no eliminado), texto legible.
- `grep -ci 'pico' index.html` ≥ 1 → Pico sigue como base (no se elimina).
- `node --test tests/*.test.js` → 494 pass / 1 fail (el fail es el preexistente ajeno `genero-numero`; cero fails nuevos).
- Re-test humano (checkpoint): items 1, 2 y 5 de 32-HUMAN-UAT — banner ⚠ y nombres de categoría legibles, sin card de color viejo, tricolore y dots intactos.
</verification>

<success_criteria>
- GAP-01 cerrado: banner ⚠ de export legible (contraste AA ≥ 4.5:1), nombres/tema de categoría legibles.
- Ninguna superficie de Pico (article/header/footer/form/table) pinta color stock encima del papel; todas heredan papel/tinta (salvo `--ed-paper-elevated` intencional).
- Pico mantenido como base/reset (NO eliminado — decisión bloqueada del UI-SPEC).
- Cambio CSS-only: motor, markup funcional y bindings Alpine intactos; suite sin fails nuevos.
- FND-03 ("modo papel siempre") satisfecho en renderizado real, confirmado por re-test humano.
</success_criteria>

<output>
Create `.planning/phases/32-cimientos-visuales-home-categor-as/32-03-SUMMARY.md` when done
</output>
