---
phase: 32-cimientos-visuales-home-categor-as
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/fonts/
  - app.css
  - styles.css
  - index.html
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04]
user_setup: []

must_haves:
  truths:
    - "Al abrir la app con npx serve, el fondo es papel cálido #f4f0e8 y los títulos usan serif Spectral (la estética azul/blanca de Pico ha desaparecido)"
    - "Las 3 familias (Spectral, Hanken Grotesk, Space Grotesk) cargan desde vendor/fonts/ vía @font-face con CERO peticiones a fonts.googleapis.com (funciona offline)"
    - "El modo oscuro automático de Pico está desactivado: la app siempre muestra la paleta papel, ignore prefers-color-scheme"
    - "Existe un motivo tricolore reutilizable (3 segmentos verde/crema/rojo, 4px, radio 999) disponible como clase para esta y futuras fases"
    - "El estado dominada se pinta verde y hecha ámbar (corrige la inversión legacy de .badge-*)"
  artifacts:
    - path: "app.css"
      provides: "Capa Editoriale sobre Pico: tokens :root --ed-*, @font-face de las 3 familias, fondo papel, tipografía serif, tricolore, override de .badge-* por D-01"
      contains: "--ed-paper"
    - path: "vendor/fonts/"
      provides: "Ficheros .woff2 auto-hospedados de las 3 familias en sus pesos subset"
    - path: "styles.css"
      provides: "color-scheme: light (modo oscuro desactivado, D-03)"
      contains: "color-scheme: light"
  key_links:
    - from: "index.html <head>"
      to: "app.css"
      via: "<link rel=stylesheet> DESPUÉS de styles.css para ganar la cascada"
      pattern: "app\\.css"
    - from: "app.css @font-face"
      to: "vendor/fonts/*.woff2"
      via: "src: url('./vendor/fonts/...') format('woff2'), rutas relativas, sin fonts.googleapis.com"
      pattern: "vendor/fonts"
---

<objective>
Sienta la capa de cimientos visuales Editoriale de la que dependen TODAS las fases de v1.8: tokens CSS (`--ed-*`), las 3 fuentes auto-hospedadas en `vendor/fonts/` vía `@font-face` (sin red), una capa `app.css` aplicada sobre Pico (Pico = reset/base), el motivo tricolore reutilizable, y la desactivación del modo oscuro de Pico. También reconcilia los colores de `.badge-*` a D-01 (dominada=verde, hecha=ámbar) porque la Home (Plan 02) y futuras fases dependen de que el color renderizado sea correcto.

Purpose: Phase 33 y 34 no pueden empezar sin tokens/fuentes/`app.css`. Esta es la PRIMERA fase de v1.8 y su salida es consumida por todo lo demás.
Output: `vendor/fonts/*.woff2`, `app.css` (tokens + @font-face + base Editoriale + tricolore + badge override), `styles.css` editado (dark-mode off + badge), `index.html` `<head>` editado (link app.css, quitar meta color-scheme).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-CONTEXT.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md
@design_handoff_italiano_redesign/README.md

<read_first>
Lectura obligatoria antes de implementar (no re-derivar valores — están fijados):
- `design_handoff_italiano_redesign/README.md` §Design Tokens (colores hex EXACTOS, escala tipográfica, radios, sombras, tricolore). Fidelidad alta: "Mantén las medidas exactas".
- `.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md` §Spacing Scale, §Typography, §Color, §Radii & Shadows, §Self-Hosted Fonts (es la fuente canónica de los tokens y de los pesos subset).
- `styles.css` líneas 1-13 (bloque `:root { color-scheme: light dark }` a modificar) y líneas 35-59 (idioma `var(--pico-*, #hex)` + las 3 reglas `.badge-*` invertidas a corregir).
- `index.html` líneas 5-15 (meta color-scheme + orden de `<link>` en `<head>`).
- `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`app.css`, §`vendor/fonts/`, §`styles.css`, §Shared Patterns (idioma override-on-Pico + secciones comentadas).
</read_first>

<interfaces>
<!-- Tokens Editoriale a declarar en :root (valores EXACTOS de UI-SPEC §Color/Typography/Radii + README). El executor los usa directos. -->

Colores (UI-SPEC §Color + README §Colores):
  --ed-paper: #f4f0e8           (fondo app)
  --ed-paper-elevated: #fbf8f1  (tarjetas — P33-34)
  --ed-surface: #fbfaf6         (superficies claras + segmento crema del tricolore)
  --ed-ink: #2b2722             (texto principal, borde+texto píldora Examen)
  --ed-muted: #8c8576           (texto secundario/labels)
  --ed-faint: #a39a88           (subtítulos cursiva, meta, streak label)
  --ed-faint-2: #a89f8c         (overlines)
  --ed-hairline: rgba(43,39,34,0.09)     (separadores de fila)
  --ed-border-soft: rgba(43,39,34,0.16)  (bordes ghost)
  --ed-streak-track: rgba(43,39,34,0.08) (pista vacía de la barra de racha)
  --ed-green: #2f7d56           (CTA, dominada, éxito, tricolore izq)
  --ed-red: #b5412e             (error, tricolore der)
  --ed-amber: #b9852f           (dot hecha "en progreso")
  --ed-neutral-dot: #c4bcab     (dot no-hecha "sin empezar")

Tipografía (UI-SPEC §Typography):
  --ed-font-serif: 'Spectral', Georgia, serif
  --ed-font-sans: 'Hanken Grotesk', system-ui, sans-serif
  --ed-font-mono: 'Space Grotesk', ui-monospace, monospace

Radios (UI-SPEC §Radii & Shadows):
  --ed-radius-card: 18px · --ed-radius-cta: 16px · --ed-radius-feedback: 14px
  --ed-radius-ghost: 12px · --ed-radius-pill: 999px

Sombras (usar el valor del README, .26 NO .28):
  --ed-shadow-cta: 0 10px 22px rgba(47,125,86,.26)
  --ed-shadow-card: 0 8px 22px rgba(40,34,24,.07)

Padding horizontal de pantalla (KEY, off-grid, verbatim del handoff):
  --ed-space-screen: 22px

Pesos a auto-hospedar (UI-SPEC §Self-Hosted Fonts — subset SOLO estos):
  Spectral: 400, 500, 600, 700 + italic 400, 500
  Hanken Grotesk: 400, 500, 600, 700, 800
  Space Grotesk: 500, 700
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-hospedar las 3 familias en vendor/fonts/</name>
  <files>vendor/fonts/, vendor/fonts/fonts.css (o @font-face dentro de app.css — discreción del executor)</files>
  <read_first>
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md` §Self-Hosted Fonts (pesos exactos, font-display swap, rutas relativas).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`vendor/fonts/` (idioma de link relativo `./` como `./styles.css`; CERO `fonts.googleapis.com` en runtime).
    - `design_handoff_italiano_redesign/README.md` línea 72 (la línea import de Google Fonts es la LISTA DE ORIGEN solamente — NO pegar ese `<link>` en producción).
  </read_first>
  <action>
    Crear el directorio `vendor/fonts/` (no existe). Descargar los ficheros .woff2 subset de las 3 familias en SOLO los pesos del contrato (Spectral 400/500/600/700 + italic 400/500; Hanken Grotesk 400/500/600/700/800; Space Grotesk 500/700) desde el CDN de Google Fonts (google-webfonts-helper o `curl` sobre las URLs woff2 que devuelve `fonts.googleapis.com/css2` con un User-Agent moderno). Guardar los .woff2 en `vendor/fonts/` con nombres claros (p.ej. `spectral-600.woff2`, `spectral-italic-400.woff2`, `hanken-grotesk-700.woff2`, `space-grotesk-500.woff2`).
    Declarar las reglas `@font-face` correspondientes (una por peso/estilo) con `font-family` exacto ('Spectral' / 'Hanken Grotesk' / 'Space Grotesk'), `font-weight`/`font-style` correctos, `font-display: swap` y `src: url('./vendor/fonts/<archivo>.woff2') format('woff2')` con RUTA RELATIVA. Pueden vivir en `vendor/fonts/fonts.css` (linkeado en `<head>`) o al inicio de `app.css` — discreción del executor; mantener una sola decisión.
    PROHIBIDO: cualquier referencia a `fonts.googleapis.com`, `fonts.gstatic.com` o `<link href="https://fonts...">` que se cargue en runtime. Las fuentes deben funcionar offline (constraint del proyecto: la app es local).
    Si la descarga del CDN no es posible en el entorno de ejecución, crear un checkpoint:human-action pidiendo al autor colocar los .woff2 en `vendor/fonts/` (es lo único que Claude no puede hacer sin red); las reglas @font-face se escriben igual.
  </action>
  <verify>
    <automated>test -d vendor/fonts && ls vendor/fonts/*.woff2 | wc -l | awk '{ if ($1 >= 11) print "OK fonts:", $1; else { print "FAIL: faltan woff2, hay", $1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/fonts/` existe y contiene >= 11 ficheros .woff2 (5 Spectral + 5 Hanken + 2 Space... el conteo mínimo es 11 contando los italic de Spectral por separado; ajustar si se combinan pesos en un fichero variable).
    - Las reglas `@font-face` usan `src: url('./vendor/fonts/...')` relativas y `format('woff2')`.
    - CERO ocurrencias de `fonts.googleapis.com` o `fonts.gstatic.com` en cualquier fichero que se cargue en runtime (`grep -rl "fonts.googleapis.com" index.html app.css vendor/fonts/ 2>/dev/null` vacío).
  </acceptance_criteria>
  <done>vendor/fonts/ poblado con los .woff2 subset; @font-face declarados con rutas relativas y font-display:swap; cero peticiones a Google Fonts en runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Crear app.css (tokens + base Editoriale + tricolore) y enlazarlo en index.html</name>
  <files>app.css, index.html</files>
  <read_first>
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md` §Color, §Typography, §Radii & Shadows, §Components (tricolore motif), §Self-Hosted Fonts (orden de link).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`app.css` (idioma `var(--ed-*)`, secciones comentadas `/* ─── ... ─── */`, link DESPUÉS de styles.css).
    - `styles.css` líneas 35-97 (el idioma `var(--pico-*, #hex)` y `.button-row` flex a imitar en estructura).
    - `index.html` líneas 9-31 (orden de `<link>`/`<script>` en `<head>`).
  </read_first>
  <action>
    Crear `app.css` con (en este orden, con banners comentados al estilo `styles.css`):
    (1) Bloque de tokens: declarar TODOS los `--ed-*` del `<interfaces>` en `:root` (valores exactos — son fidelidad alta, no redondear el off-grid 22px ni la sombra .26).
    (2) Base Editoriale sobre Pico: `body { background: var(--ed-paper); color: var(--ed-ink); font-family: var(--ed-font-sans); }`; títulos/headings `font-family: var(--ed-font-serif)`. Aplicar el padding horizontal de pantalla `--ed-space-screen` (22px) al contenedor principal de pantalla (`main` o el `article` de Home — el executor elige el ancla coherente con Pico sin romper el layout existente). El objetivo: al cargar, el fondo es papel cálido y la tipografía base es serif/sans Editoriale, la estética azul/blanca de Pico desaparece. NO romper las pantallas existentes (picker/session/summary/canciones) — solo aplican la capa global, su markup se rediseña en Phases 33-34.
    (3) Motivo tricolore (FND-04): una clase reutilizable (p.ej. `.ed-tricolore`) = 3 segmentos iguales `height:4px`, `border-radius:999px`, `overflow:hidden`, `width:~56px`, colores izq→der `var(--ed-green)` / `var(--ed-surface)` (crema) / `var(--ed-red)`. Documentar con comentario que es reutilizable por Phases 33-34.
    Enlazar `app.css` en `index.html <head>` con `<link rel="stylesheet" href="./app.css">` colocado DESPUÉS de `./styles.css` (línea 15) para ganar la cascada. Si las @font-face viven en `vendor/fonts/fonts.css`, enlazarlo también (antes o dentro de app.css).
    PROHIBIDO: tocar lógica JS, bindings Alpine, motor, o reestructurar el DOM existente. Solo CSS nuevo + un `<link>`.
  </action>
  <verify>
    <automated>grep -q -- '--ed-paper: *#f4f0e8' app.css && grep -q -- '--ed-green: *#2f7d56' app.css && grep -q '22px' app.css && grep -Eq 'rgba\(47, *125, *86, *\.26\)' app.css && grep -q 'tricolore\|ed-tricolore' app.css && grep -A40 '<link rel="stylesheet" href="./styles.css">' index.html | grep -q 'app.css' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app.css` define `--ed-paper: #f4f0e8`, `--ed-green: #2f7d56`, el padding `22px` (--ed-space-screen) y la sombra CTA con `.26` (no `.28`).
    - `app.css` contiene una clase de tricolore reutilizable con los 3 segmentos verde/crema/rojo.
    - `body` (o equivalente) consume `var(--ed-paper)` para fondo y la familia serif para títulos.
    - `index.html` enlaza `./app.css` DESPUÉS de `./styles.css`.
    - Suite intacta: `node --test tests/*.test.js` sigue en 473 pass / 1 fail preexistente (genero-numero) — esta tarea no añade ni rompe tests de lógica.
  </acceptance_criteria>
  <done>app.css existe con tokens + base papel/serif + tricolore reutilizable; enlazado tras styles.css; la Home se ve sobre papel cálido al abrir con npx serve.</done>
</task>

<task type="auto">
  <name>Task 3: Desactivar modo oscuro (D-03) y reconciliar .badge-* a D-01</name>
  <files>styles.css, index.html, app.css</files>
  <read_first>
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-CONTEXT.md` D-01 (dominada=verde #2f7d56, hecha=ámbar #b9852f, no-hecha=neutro #c4bcab) y D-03 (forzar paleta papel, desactivar dark mode).
    - `styles.css` líneas 11-13 (`:root { color-scheme: light dark }`) y líneas 57-59 (las 3 `.badge-*` INVERTIDAS: hoy hecha=verde, dominada=ámbar).
    - `index.html` línea 6 (`<meta name="color-scheme" content="light dark">`).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`styles.css` (LOAD-BEARING BUG D-01: decidir UN solo hogar para el color del badge).
  </read_first>
  <action>
    D-03 (FND-03): en `styles.css` cambiar `:root { color-scheme: light dark; }` (líneas 11-13) a `:root { color-scheme: light; }` y en `index.html` línea 6 quitar (o cambiar a `content="light"`) el `<meta name="color-scheme" content="light dark">`. Esto fuerza la paleta papel siempre e ignora `prefers-color-scheme`. NO inventar variante oscura.
    D-01 (HOME-04): el resultado renderizado de los 3 estados DEBE ser dominada→verde `#2f7d56`, hecha→ámbar `#b9852f`, no-hecha→neutro `#c4bcab`. Hoy `styles.css` líneas 57-59 los invierte (hecha=verde, dominada=ámbar). Corregir en UN solo hogar (discreción del executor): bien editar las 3 reglas en `styles.css` a los tokens Editoriale, bien overridearlas en `app.css` (que carga después). Las dos hojas NO deben pelearse: el binding existente `:class="\`badge-${cat.status}\`"` se conserva, solo cambia el color que esas clases pintan. Reutilizar las custom properties `--ed-green`/`--ed-amber`/`--ed-neutral-dot`.
    PROHIBIDO: cambiar el binding `:class`, la lógica de `status`, o `categoriesForDisplay`. Solo CSS + el meta.
  </action>
  <verify>
    <automated>grep -q 'color-scheme: light;' styles.css && ! grep -q 'color-scheme: light dark' styles.css && ! grep -q 'content="light dark"' index.html && node -e "const fs=require('fs');const css=fs.readFileSync('styles.css','utf8')+fs.readFileSync('app.css','utf8');const dom=css.split('.badge-dominada')[1]||'';const hec=css.split('.badge-hecha')[1]||'';if(/2f7d56/i.test(dom.slice(0,160))&&/b9852f/i.test(hec.slice(0,160))){console.log('OK D-01: dominada=verde, hecha=ambar')}else{console.error('FAIL D-01: revisar colores badge');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `styles.css` contiene `color-scheme: light;` y NO `color-scheme: light dark`.
    - `index.html` NO contiene `content="light dark"`.
    - El color efectivo de `.badge-dominada` es verde `#2f7d56` y el de `.badge-hecha` es ámbar `#b9852f` (en `styles.css` o `app.css`, sin conflicto entre ambas).
    - `.badge-no-hecha` es neutro `#c4bcab`.
    - Suite intacta: `node --test tests/*.test.js` → 473 pass / 1 fail preexistente.
  </acceptance_criteria>
  <done>Modo oscuro desactivado (paleta papel siempre); .badge-* renderiza dominada=verde, hecha=ámbar, no-hecha=neutro per D-01; binding y lógica intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disco → navegador (assets de fuente) | Los .woff2 auto-hospedados en `vendor/fonts/` se sirven localmente; integridad de los ficheros vendoreados. |
| JSON de contenido → DOM (Home) | Nombres de categoría provenientes de `categories.json` se renderizan; el anti-XSS por `x-text` debe preservarse (relevante en Plan 02, no aquí). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | `vendor/fonts/*.woff2` (assets vendoreados) | accept | Superficie mínima: fuentes estáticas servidas localmente, sin red en runtime, sin ejecución de código. Riesgo bajo (app single-user, local). Se documenta sin SRI porque son ficheros locales del propio autor. |
| T-32-02 | Information Disclosure | @font-face / `<link>` | mitigate | CERO peticiones a `fonts.googleapis.com`/`gstatic.com` en runtime (FND-02 verificable en Network/offline) → no hay fuga de origen ni huella a Google. Verificado en acceptance criteria de Task 1. |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | NO hay instalación de paquetes en esta fase (stack vanilla, fuentes descargadas como assets estáticos, no como dependencias npm). N/A — no se ejecuta gestor de paquetes. |

Severidad máxima: baja. Sin entradas nuevas de usuario, sin red en runtime, sin auth. No hay umbral `high` que bloquee.
</threat_model>

<verification>
- `node --test tests/*.test.js` → 473 pass / 1 fail (el único fail es el preexistente AJENO genero-numero; CERO fails nuevos — esta fase no toca lógica).
- `grep -rl "fonts.googleapis.com" index.html app.css vendor/ 2>/dev/null` → vacío (FND-02 offline).
- `grep -q -- '--ed-paper: #f4f0e8' app.css` → tokens presentes (FND-01).
- `grep -q 'color-scheme: light;' styles.css && ! grep -q 'color-scheme: light dark' styles.css` → dark mode off (FND-03/D-03).
- Inspección visual con `npx serve`: fondo papel cálido, títulos serif, tricolore visible (FND-03/FND-04).
</verification>

<success_criteria>
- FND-01: tokens `--ed-*` en `:root` (app.css) consumidos vía `var(...)`.
- FND-02: 3 familias auto-hospedadas en `vendor/fonts/` vía @font-face, cero peticiones a Google Fonts (offline).
- FND-03: `app.css` sobre Pico (papel cálido + serif + 22px de aire) sin romper la app; modo oscuro de Pico desactivado (D-03).
- FND-04: motivo tricolore reutilizable disponible como clase.
- D-01 reconciliado: dominada=verde, hecha=ámbar, no-hecha=neutro (cimiento para Plan 02).
- Suite verde (473 pass / 1 fail preexistente).
</success_criteria>

<output>
Create `.planning/phases/32-cimientos-visuales-home-categor-as/32-01-SUMMARY.md` when done
</output>
