---
phase: 32-cimientos-visuales-home-categor-as
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/screens/app.js
  - index.html
  - app.css
  - tests/screen-home-editorial.test.js
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06]
user_setup: []

must_haves:
  truths:
    - "La Home muestra cabecera editorial: overline ITALIANO · A1 / A2 + título serif Categorías (Spectral 38)"
    - "El CTA verde Repaso 20 (ancho completo, título + subtítulo + flecha, sombra verde) abre el picker con openPicker('repaso') — mismo comportamiento que hoy"
    - "La fila de 3 botones ghost (Test completo · Canciones · Backup) lanza/conmuta exactamente lo mismo que hoy"
    - "El switch Contrarreloj conserva x-model=homeExamTimed y conmuta igual que hoy"
    - "Cada categoría se muestra con punto de estado (verde dominado/ámbar progreso/neutro sin empezar), nombre serif + tema en cursiva, barra streak/21 y píldora Examen, con datos reales del state"
    - "En desktop la Home se presenta como tabla editorial (Estado · Categoría · Racha · Ejercicios · Examen) con papel/serif/hairlines, conservando lanzar práctica y Examen 1-clic"
  artifacts:
    - path: "src/screens/app.js"
      provides: "categoriesForDisplay devuelve además streakDays crudo (1 campo presentacional para la barra streak/21)"
      contains: "streakDays: streak"
    - path: "index.html"
      provides: "Bloque Home rediseñado dentro de <template x-if=currentScreen==='home'> con todos los bindings Alpine preservados"
      contains: "ITALIANO · A1 / A2"
    - path: "app.css"
      provides: "Estilos Editoriale de Home: header, CTA, ghost row, switch, category row (dot/streak bar) y tabla editorial desktop"
    - path: "tests/screen-home-editorial.test.js"
      provides: "Source-asserts del contrato Home (bindings preservados, copy, getter streakDays)"
  key_links:
    - from: "index.html CTA Repaso 20"
      to: "openPicker('repaso')"
      via: "@click verbatim (D-05, comportamiento intacto)"
      pattern: "openPicker\\('repaso'\\)"
    - from: "index.html barra de racha"
      to: "categoriesForDisplay.streakDays"
      via: "ancho de relleno = cat.streakDays / 21 * 100%"
      pattern: "streakDays"
    - from: "index.html switch Contrarreloj"
      to: "homeExamTimed"
      via: "x-model verbatim (HOME-05)"
      pattern: "x-model=\"homeExamTimed\""
---

<objective>
Rediseña la pantalla Home/Categorías al lenguaje Editoriale: cabecera serif (overline + título Categorías), CTA verde "Repaso 20" de ancho completo, fila ghost de 3 (Test completo · Canciones · Backup), switch Contrarreloj, filas de categoría con punto de estado + nombre/tema + barra streak/21 + píldora Examen, y tabla editorial en desktop. Añade el ÚNICO cambio JS permitido: exponer `streakDays` crudo en `categoriesForDisplay`. Consume la capa de cimientos del Plan 01 (tokens, fuentes, app.css, dark-mode-off, badge D-01).

Purpose: La Home es la pantalla principal y la prueba viva de que la dirección Editoriale funciona end-to-end con datos reales del state, sin tocar el motor.
Output: `app.js` (1 campo), `index.html` (bloque Home reescrito reusando bindings), `app.css` (componentes Home + tabla desktop), `tests/screen-home-editorial.test.js` (source-asserts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-CONTEXT.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md
@.planning/phases/32-cimientos-visuales-home-categor-as/32-01-SUMMARY.md

<read_first>
- `.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md` §Components (Home header, CTA, ghost row, switch, category row, desktop table), §Copywriting Contract, §Data Contract, §Spacing Scale (excepciones off-grid verbatim).
- `.planning/phases/32-cimientos-visuales-home-categor-as/32-CONTEXT.md` D-01..D-05 (LOCKED).
- `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`index.html`, §`src/screens/app.js`, §Shared Patterns (restyle-the-table, anti-XSS, double-defense).
- `index.html` líneas 65-210 (bloque Home actual a reemplazar EN SITIO; los `@click`/`x-model`/`x-for`/`x-text`/`:disabled`/`:title` a preservar verbatim).
- `src/screens/app.js` líneas 2966-3003 (getter `categoriesForDisplay`; `const streak` ya computado en 2979 pero no devuelto — añadir 1 campo).
- `styles.css` líneas 532-601 (capa responsive `@media (max-width:640px)` tabla→tarjetas vía `data-label` + tap-targets 44px — NO regresar; la tabla desktop es el hermano inverso de este patrón).
- `content/categories.json` (10 categorías; 4 con paréntesis en `name`: avere, essere, articoli, presente-regolare → derivan título+tema D-02; las otras 6 sin paréntesis → solo nombre).
- `tests/screen-responsive.test.js` (idioma source-assert: leer index.html/styles.css/app.css como strings y afirmar substrings — replicar este estilo de test).
</read_first>

<interfaces>
<!-- Forma de cada fila devuelta por categoriesForDisplay (app.js ~2985), TRAS añadir streakDays. -->
{
  id, name, status,          // status ∈ 'no-hecha'|'hecha'|'dominada'
  badgeGlyph, statusLabel,
  streakLabel,               // texto "N / 21 d" o "Sin empezar" (reutilizar)
  totalCount,                // nº ejercicios (columna Ejercicios + meta "M ejercicios")
  lastPracticedLabel,        // "Última vez" (se OCULTA en la tabla editorial desktop)
  examenEnabled, examenTooltip,
  vecesFallada,              // indicador secundario en columna Racha (D-01: NO cambia el dot)
  streakDays                 // NUEVO (este plan): valor crudo 0..21+ para ancho de barra streak/21
}

<!-- Bindings a preservar VERBATIM (index.html, D-05/HOME-05/anti-XSS): -->
@click="openPicker('repaso')"          // CTA Repaso 20 (D-05: abre picker, NO lanzamiento directo)
@click="openPicker('test-completo')"   // ghost Test completo
@click="currentScreen = 'canciones'"   // ghost Canciones (baja a ghost, D-04)
@click="currentScreen = 'backup'"      // ghost Backup
x-model="homeExamTimed"                 // switch Contrarreloj
x-for="cat in categoriesForDisplay" :key="cat.id"
:class="`badge-${cat.status}`"          // dot color (clases reconciliadas a D-01 en Plan 01)
:disabled="!cat.examenEnabled" :title="cat.examenTooltip" @click="startExamen(cat.id)"  // píldora Examen
data-label="..."                         // mantener en celdas (capa móvil Phase-28 depende de ellos)
<!-- Anti-XSS: TODO texto de contenido vía x-text; NUNCA x-html. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exponer streakDays crudo en categoriesForDisplay</name>
  <files>src/screens/app.js, tests/screen-home-editorial.test.js</files>
  <read_first>
    - `src/screens/app.js` líneas 2966-3003 (getter completo; `const streak = progress?.streakDays ?? 0` en línea 2979; objeto devuelto 2985-3001).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`src/screens/app.js` (el ÚNICO cambio permitido: añadir 1 campo `streakDays: streak`).
    - `tests/screen-context-label.test.js` (idioma de test source-assert sobre app.js si aplica) o `tests/screen-responsive.test.js`.
  </read_first>
  <behavior>
    - El objeto devuelto por `categoriesForDisplay` incluye una propiedad `streakDays` con el valor crudo de la racha (el mismo que ya se lee en `const streak`).
    - El resto de campos (status, streakLabel, totalCount, examenEnabled, vecesFallada, ...) NO cambian.
    - La guarda `if (!this.content || !this.state) return []` se conserva.
  </behavior>
  <action>
    En `src/screens/app.js`, dentro del objeto devuelto por el getter `categoriesForDisplay` (líneas 2985-3001), añadir UNA propiedad: `streakDays: streak,` (el `const streak` ya existe en línea 2979; solo se incluye en el return). Comentario corto: valor crudo para la barra streak/21 (presentacional, Phase 32).
    PROHIBIDO tocar: la derivación de `status`, `formatStreak`, la cascada D-54 (`applyImmediateFailure`), sampler, slot-engine, localStorage, schema, migraciones, ni el getter hermano `songsForDisplay`. Es UN campo.
    Crear `tests/screen-home-editorial.test.js` con un source-assert que verifique que `app.js` contiene `streakDays: streak` (o `streakDays:streak`) dentro del getter — coherente con el idioma source-assert de `tests/screen-responsive.test.js`.
  </action>
  <verify>
    <automated>grep -q 'streakDays: *streak' src/screens/app.js && grep -c 'applyImmediateFailure(this.state' src/screens/app.js | grep -q '^2$' && node --test tests/screen-home-editorial.test.js 2>&1 | grep -q 'pass'</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/app.js` contiene `streakDays: streak` dentro del return de `categoriesForDisplay`.
    - Cascada D-54 intacta: `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2 (motor no tocado).
    - `tests/screen-home-editorial.test.js` existe y su(s) test(s) pasan.
    - Suite global: `node --test tests/*.test.js` → 473 pass / 1 fail preexistente (genero-numero); CERO fails nuevos.
  </acceptance_criteria>
  <done>categoriesForDisplay expone streakDays crudo; única línea JS añadida; motor verificablemente intacto; test source-assert verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reescribir el bloque Home en index.html (header, CTA, ghost row, switch, filas, tabla) reusando bindings</name>
  <files>index.html, tests/screen-home-editorial.test.js</files>
  <read_first>
    - `index.html` líneas 65-210 (bloque Home actual; banners in-flight/backup en 93-128 SE CONSERVAN tal cual — out of scope visual aquí salvo herencia global).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md` §Components + §Copywriting Contract (copy exacto: overline `ITALIANO · A1 / A2`, título `Categorías`, CTA `Repaso 20`, ghost `Test completo`/`Canciones`/`Backup`, overline `CATEGORÍAS`, label `Contrarreloj`, píldora `Examen`).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-CONTEXT.md` D-02 (split del paréntesis del name), D-04 (Canciones baja a ghost), D-05 (CTA conserva openPicker).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`index.html` (qué bindings preservar verbatim; anti-XSS).
  </read_first>
  <action>
    Reemplazar EN SITIO el contenido del `<template x-if="currentScreen === 'home'">` (mantener este gate exterior y los DOS banners `inFlightTestActive`/`shouldShowBackupBanner` intactos) por el layout Editoriale:
    (1) HOME-01 cabecera: motivo tricolore (clase de Plan 01) + overline `ITALIANO · A1 / A2` (texto literal, NO de JSON) + título serif `Categorías`.
    (2) HOME-02 CTA primario: un `<button>` de ancho completo con `@click="openPicker('repaso')"` VERBATIM (D-05, NO lanzamiento directo) conteniendo título "Repaso 20" + subtítulo + flecha `→`. Copy del subtítulo: usar `Repaso rápido` (neutro) si "20 ejercicios al azar · 5 min" choca con el paso del picker — el comportamiento NO cambia (D-05).
    (3) HOME-03 ghost row: fila flex de 3 botones — `Test completo` (`@click="openPicker('test-completo')"`) · `Canciones` (`@click="currentScreen = 'canciones'"`) · `Backup` (`@click="currentScreen = 'backup'"`). Todos los `@click` VERBATIM. Canciones baja de protagonista a ghost (D-04).
    (4) HOME-05 fila de sección + switch: overline `CATEGORÍAS` a la izquierda; a la derecha label `Contrarreloj` + `<input type="checkbox" role="switch" x-model="homeExamTimed">` (binding VERBATIM).
    (5) HOME-04/06 tabla de categorías: CONSERVAR `<figure><table>` con `x-for="cat in categoriesForDisplay" :key="cat.id"` y los atributos `data-label` (la capa móvil Phase-28 depende de ellos). En cada fila:
        - Estado: el dot (reusar `:class="\`badge-${cat.status}\`"`; el color lo da Plan 01 D-01). Mantener `:aria-label="cat.statusLabel"`.
        - Categoría: título = parte del `name` antes de `(` y subtítulo en cursiva = texto dentro del paréntesis (D-02, en expresión Alpine `x-text` o getter presentacional — discreción; las 6 sin paréntesis muestran solo el nombre, sin fila de subtítulo). TODO vía `x-text` (anti-XSS, NUNCA x-html).
        - Racha: barra streak/21 (ancho de relleno = `cat.streakDays / 21 * 100%`, verde si dominada / ámbar si hecha) + `cat.streakLabel` + el `<small x-show="cat.vecesFallada > 0">` existente.
        - Ejercicios: `cat.totalCount`.
        - Examen: el `<button>` con `:disabled="!cat.examenEnabled"`, `:title="cat.examenTooltip"`, `@click="startExamen(cat.id)"` VERBATIM, restilable a píldora.
        - La columna "Última vez" (`data-label="Última vez"`): puede dejarse en el DOM y ocultarse en desktop por CSS (Plan task 3) o quitarse para llegar al contrato 5-columnas — discreción; presentacional.
    PROHIBIDO: introducir `x-html`, cambiar cualquier `@click`/`x-model`/`x-for`/`:disabled`/`:title`, tocar lógica JS o el motor.
    Ampliar `tests/screen-home-editorial.test.js` con source-asserts: presencia del copy (`ITALIANO · A1 / A2`, `Categorías`, `Repaso 20`), de los bindings preservados (`openPicker('repaso')`, `currentScreen = 'canciones'`, `x-model="homeExamTimed"`, `startExamen(cat.id)`), uso de `streakDays`, y ausencia de `x-html` en el bloque Home.
  </action>
  <verify>
    <automated>grep -q 'ITALIANO · A1 / A2' index.html && grep -q "openPicker('repaso')" index.html && grep -q "currentScreen = 'canciones'" index.html && grep -q 'x-model="homeExamTimed"' index.html && grep -q 'startExamen(cat.id)' index.html && grep -q 'streakDays' index.html && ! grep -q 'x-html' index.html && node --test tests/screen-home-editorial.test.js 2>&1 | grep -q 'pass'</automated>
  </verify>
  <acceptance_criteria>
    - Copy presente: `ITALIANO · A1 / A2`, `Categorías`, `Repaso 20`, ghost `Test completo`/`Canciones`/`Backup`, `CATEGORÍAS`, `Contrarreloj`, `Examen`.
    - Bindings preservados verbatim: `openPicker('repaso')`, `openPicker('test-completo')`, `currentScreen = 'canciones'`, `currentScreen = 'backup'`, `x-model="homeExamTimed"`, `x-for="cat in categoriesForDisplay"`, `startExamen(cat.id)`, `:disabled`/`:title` en Examen.
    - La barra de racha usa `cat.streakDays`.
    - CERO `x-html` en index.html (anti-XSS preservado).
    - Los `data-label` se conservan en las celdas de la tabla.
    - Suite: `node --test tests/*.test.js` → 473 pass / 1 fail preexistente; nuevos source-asserts verdes.
  </acceptance_criteria>
  <done>Bloque Home reescrito al lenguaje Editoriale con header/CTA/ghost/switch/filas/tabla; todos los bindings Alpine intactos; subtítulo de categoría derivado del name (D-02); anti-XSS preservado.</done>
</task>

<task type="auto">
  <name>Task 3: Estilos Editoriale de Home en app.css (header, CTA, ghost, switch, category row, tabla desktop)</name>
  <files>app.css, tests/screen-home-editorial.test.js</files>
  <read_first>
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-UI-SPEC.md` §Components (medidas exactas: CTA `padding:16px 18px` radio 16 sombra `--ed-shadow-cta`; ghost `gap:8px` `margin-top:9px` cada botón `flex:1 padding:11px 4px` borde `--ed-border-soft` radio 12; category row `gap:13px padding:15px 0 border-bottom hairline`; dot 9px; barra alto 4 max-width 110 radio 999; píldora Examen `padding:7px 15px` borde ink radio 999; header `margin-bottom:18px`, título Spectral 38/600 letter-spacing −0.6px; overline Hanken 11/700 letter-spacing 2-2.5px MAYÚSCULAS).
    - `.planning/phases/32-cimientos-visuales-home-categor-as/32-PATTERNS.md` §`app.css` punto (3) ghost row y (4) tabla desktop (hermano inverso del `@media (max-width:640px)`), §Shared Patterns (restyle-the-table, NO regresar tap-targets 44px).
    - `styles.css` líneas 532-601 (capa móvil a NO romper; reglas tap-target 591-601).
    - `app.css` del Plan 01 (tokens `--ed-*` a consumir vía `var(...)`).
  </read_first>
  <action>
    Añadir a `app.css` (con banners comentados estilo `styles.css`) las reglas de los componentes Home, todas consumiendo `var(--ed-*)`:
    (1) Cabecera: título serif 38/600 letter-spacing −0.6px color ink; overline Hanken 11/700 letter-spacing ~2.5px MAYÚSCULAS color faint-2; `margin-bottom:18px`.
    (2) CTA Repaso 20: ancho completo, `padding:16px 18px`, radio `var(--ed-radius-cta)` (16), fondo `var(--ed-green)`, texto surface, `box-shadow: var(--ed-shadow-cta)`, flex space-between (título Hanken 16/700 + subtítulo 12/400 opacity .85 a la izquierda, flecha → a la derecha), `text-align:left`.
    (3) Ghost row: flex `gap:8px`, `margin-top:9px`; cada botón `flex:1`, `padding:11px 4px`, borde `1px solid var(--ed-border-soft)`, fondo transparente, radio `var(--ed-radius-ghost)` (12), texto Hanken 12.5/600.
    (4) Fila de sección + switch: flex space-between `margin:24px 0 4px`; overline `CATEGORÍAS`; el `role="switch"` de Pico restilado a track verde cuando on (NO cambiar el modelo Alpine). `gap:9px` label↔switch.
    (5) Category row + tabla desktop (HOME-04/06): bajo un `@media (min-width: 641px)` (o reglas base que la capa móvil 640px sobreescribe), estilar `figure table` editorial: fondo papel, celdas serif Spectral, `border-bottom:1px solid var(--ed-hairline)`, sin bordes Pico azules. Dot 9px redondo (el span del badge) `flex-shrink:0`. Nombre Spectral 18/600; subtítulo Spectral italic 12.5 color faint `margin-top:1px`. Barra de racha: pista `var(--ed-streak-track)` alto 4 radio 999 max-width 110px, relleno verde (dominada) / ámbar (hecha) con ancho por estilo inline o variable desde el template; label Hanken 11 color faint nowrap. Píldora Examen: `padding:7px 15px` borde `1px solid var(--ed-ink)` fondo transparente radio 999 Hanken 12.5/700 color ink. Ocultar la columna "Última vez" en desktop (contrato 5 columnas Estado · Categoría · Racha · Ejercicios · Examen).
    CRÍTICO: NO tocar ni regresar el bloque `@media (max-width: 640px)` de `styles.css` (capa móvil Phase-28, tap-targets 44px). La tabla editorial es el hermano del breakpoint superior; la capa móvil sigue dueña de `<=640px`.
    Añadir a `tests/screen-home-editorial.test.js` source-asserts: `app.css` consume `var(--ed-green)` para el CTA, define la fila de categoría/píldora, y existe una regla de tabla desktop (p.ej. `@media (min-width: 641px)` o selector `figure table` editorial); y que `styles.css` conserva `@media (max-width: 640px)` con `min-height: 44px` (no regresión).
  </action>
  <verify>
    <automated>grep -q 'var(--ed-green)' app.css && grep -q 'var(--ed-shadow-cta)' app.css && grep -Eq 'min-width: *641px|figure table' app.css && grep -q '@media (max-width: 640px)' styles.css && grep -q 'min-height: 44px' styles.css && node --test tests/screen-home-editorial.test.js 2>&1 | grep -q 'pass'</automated>
  </verify>
  <acceptance_criteria>
    - `app.css` estila CTA (fondo `var(--ed-green)`, `var(--ed-shadow-cta)`), ghost row (flex gap 8 / borde border-soft), category row (dot 9px, barra streak, píldora Examen borde ink) y tabla editorial desktop.
    - El bloque `@media (max-width: 640px)` de `styles.css` y sus `min-height: 44px` siguen presentes (capa móvil Phase-28 sin regresión).
    - Todas las reglas nuevas consumen tokens `var(--ed-*)` (no hex sueltos repetidos).
    - Suite: `node --test tests/*.test.js` → 473 pass / 1 fail preexistente; source-asserts verdes.
  </acceptance_criteria>
  <done>Home renderiza en estilo Editoriale (papel/serif/tricolore, CTA verde, ghost, switch verde, filas con dot+streak+píldora) en móvil y como tabla editorial en desktop; capa responsive Phase-28 intacta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON contenido (`categories.json`) → DOM Home | Los `name` de categoría se renderizan (incluida la derivación título/tema D-02). Único punto donde contenido cruza al DOM en esta fase. |
| Estado runtime (`homeExamTimed`, `categoriesForDisplay`) → DOM | Solo lectura/binding; sin escritura de contenido no confiable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Tampering/XSS | Markup Home (`cat.name`, tema derivado) | mitigate | TODO el contenido se renderiza vía `x-text` (textContent); PROHIBIDO `x-html`. La derivación D-02 (split del paréntesis) se hace en expresión Alpine sobre `x-text`, nunca interpolando markup. Acceptance criteria verifica ausencia de `x-html`. |
| T-32-04 | Tampering | Lógica de motor (cascada D-54) | mitigate | El único cambio JS es 1 campo presentacional en un getter. Verificado por `grep -c "applyImmediateFailure(this.state"` == 2 (sin regresión del motor). |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No hay instalación de paquetes en esta fase (stack vanilla, solo markup/CSS/1 getter). N/A. |

Severidad máxima: baja. Sin entradas nuevas de usuario, sin red, sin auth. El invariante anti-XSS existente se preserva explícitamente. No hay umbral `high` que bloquee.
</threat_model>

<verification>
- `node --test tests/*.test.js` → 473 pass / 1 fail preexistente (genero-numero); CERO fails nuevos.
- `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2 (motor intacto).
- `! grep -q 'x-html' index.html` (anti-XSS preservado).
- Bindings preservados (greps de Task 2).
- `@media (max-width: 640px)` + `min-height: 44px` presentes en styles.css (capa móvil Phase-28 sin regresión).
- Inspección visual con `npx serve` (móvil columna + desktop tabla editorial): cabecera serif, CTA verde con sombra, ghost row, switch verde, filas con dot/streak/píldora, datos reales del state.
</verification>

<success_criteria>
- HOME-01: cabecera overline `ITALIANO · A1 / A2` + título serif `Categorías` (Spectral 38).
- HOME-02: CTA verde Repaso 20 ancho completo con título+subtítulo+flecha+sombra, abre el picker (openPicker('repaso'), D-05).
- HOME-03: fila ghost de 3 (Test completo · Canciones · Backup), borde border-soft, equiespaciados.
- HOME-04: fila de categoría con dot (D-01), nombre serif + tema cursiva (D-02), barra streak/21 + meta, píldora Examen — datos reales.
- HOME-05: switch Contrarreloj Editoriale, `x-model="homeExamTimed"` intacto.
- HOME-06: tabla editorial desktop (Estado · Categoría · Racha · Ejercicios · Examen) papel/serif/hairlines, funcionalidad intacta.
- Motor/lógica/persistencia intactos (único cambio JS: streakDays).
</success_criteria>

<output>
Create `.planning/phases/32-cimientos-visuales-home-categor-as/32-02-SUMMARY.md` when done
</output>
