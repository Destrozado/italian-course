---
phase: 31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/presente-regolare.json
autonomous: true
requirements: [PRES-07]
must_haves:
  truths:
    - "presente-regolare.json contiene ~4 objetos de cruce nuevos (M≈4) con categoryIds de 2 categorías (hub presente-regolare primero)"
    - "La matriz {compuesto·avere, compuesto·essere, presente-con-contraste·avere, presente-con-contraste·essere} está cubierta"
    - "Cada cruce es slot+variantes con ≥2 variantes intercambiables (NO single-variant)"
    - "Todos los participios usados son REGULARES (-ato/-uto/-ito); 0 irregulares; 0 auxiliares irregulares"
    - "Los 8 slots base de Phase 30 quedan byte-intactos (NO se tocan)"
    - "Cada cruce nuevo tiene validation.status=validated con ≥2 passes distintos (opus + sonnet) verdict correcta"
  artifacts:
    - path: "content/exercises/presente-regolare.json"
      provides: "8 slots base intactos + ~4 objetos de cruce presente-regolare-300.. validados"
      contains: "presente-regolare-300"
  key_links:
    - from: "content/exercises/presente-regolare.json cruces"
      to: "content/categories.json (avere, essere, presente-regolare)"
      via: "categoryIds[] resuelto por schema-validator"
      pattern: "categoryIds.*presente-regolare.*(avere|essere)"
    - from: "cruces essere"
      to: "concordancia participio↔sujeto"
      via: "è partito/partita/sono partiti/partite verificado por quórum D-31-08"
      pattern: "(partito|partita|partiti|partite|arrivat|tornat)"
---

<objective>
Autorar y validar por quórum los ~4 objetos de cruce multi-cat `presente-regolare`↔avere/essere (contraste presente vs passato prossimo) que cierran PRES-07. Los cruces viven en el hub `content/exercises/presente-regolare.json` (D-31-04), son slot+variantes con ≥2 variantes cada uno (D-31-05), usan SOLO participios regulares (D-31-02) y cubren las dos direcciones del contraste temporal (D-31-01). Cada cruce pasa el quórum completo R1-R7 idéntico a los slots base (D-31-07), con chequeo explícito de concordancia participio↔sujeto con essere y de elección de auxiliar (D-31-08).

Purpose: enganchar la 10ª categoría al motor de re-verificación vía cruces multi-cat con cascada D-54. Este plan produce el `exercises.length` FINAL (8 -> 8+M) que el Plan 02 lee para sincronizar los counts.
Output: `content/exercises/presente-regolare.json` con 8 slots base intactos + ~4 cruces nuevos, todos `validated`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-CONTEXT.md
@.planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-PATTERNS.md
@.claude/skills/gsd-validate-exercise/SKILL.md

<interfaces>
<!-- Shape EXACTO del cruce (espejo de avere-300, avere.json:702-738) — multiple-choice -->
<!-- Deviations per D-31-04/D-31-05: categoryIds = [hub primero, auxiliar], variants[] con ≥2 entries -->

Cruce multiple-choice (shape a replicar):
- "id": "presente-regolare-300" (bloque numérico 300.., espejo avere-300)
- "type": "multiple-choice"
- "categoryIds": ["presente-regolare", "avere"]  ó  ["presente-regolare", "essere"]  (hub PRIMERO)
- "explanation": canon español acentuado, gloss "(en español: ...)" permitido (R7)
- "variants": [ { "prompt", "options": [4 strings], "correctIndex" }, ... ≥2 entries ]
- "validation": { "status": "validated", "passes": [ {by:"claude-opus-4-8",date,verdict:"correcta",concerns:[]}, {by:"claude-sonnet-4-6",...} ] }

Cruce word-buttons (solo si aporta, D-31 discretion; shape espejo presente-regolare-isc-wb, líneas 307-340):
- "variants": [ { "prompt": "<español>", "answer": [tokens], "distractors": [tokens] }, ... ≥2 ]

Forma base slot+variantes de referencia (presente-regolare.json:4-59, presente-regolare-are): ≥2 variants, explanation top-level, validation con opus-4-8 + sonnet-4-6.

Formas de essere como auxiliar (essere.json present forms): sono / sei / è / siamo / siete / sono. En passato prossimo essere: è partito/partita, sono partiti/partite (concordancia participio↔sujeto — riesgo NUEVO D-31-08).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autorar los ~4 cruces presente-regolare-300.. (slot+variantes, participios regulares, ambas direcciones)</name>
  <files>content/exercises/presente-regolare.json</files>
  <read_first>
    - content/exercises/presente-regolare.json (los 8 slots base DEFINITIVOS — NO tocarlos; shape slot+variantes a replicar líneas 4-59)
    - content/exercises/avere.json líneas 702-738 (avere-300: shape EXACTO del cruce multi-cat, categoryIds de 2, bloque numérico 300..)
    - content/exercises/essere.json (formas presente de essere que pasan a auxiliar: sono/sei/è/siamo)
    - content/categories.json (verificar que ids `presente-regolare`, `avere`, `essere` existen para que categoryIds resuelvan)
    - src/data/schema-validator.js (regla payload XOR variants[], validateVariants, categoryIds no-vacío de ids conocidos)
    - .planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-CONTEXT.md (D-31-01..06)
    - Memoria del agente `exercise-authoring-rules` (R1-R7) — aplicar íntegra; R7 gloss ES "(en español: ...)" es canon
    - Memoria `explanations-must-be-accented` (español acentuado obligatorio en explanations/glosses)
  </read_first>
  <action>
    Añadir al array `exercises` de `content/exercises/presente-regolare.json` exactamente 4 objetos de cruce nuevos (M=4), DESPUÉS de los 8 slots base (que quedan byte-intactos). Ids `presente-regolare-300`, `-301`, `-302`, `-303` (bloque numérico espejo de avere-300..305). Cubrir la matriz D-31-05:
      1. `presente-regolare-300` — compuesto·avere: pide la forma compuesta de passato prossimo con auxiliar avere + participio REGULAR (-ato/-uto/-ito). categoryIds ["presente-regolare","avere"]. Ej. "Ieri io ___ con Maria" -> "ho parlato". Opciones-distractoras: otros auxiliares (sono/è) o participio mal formado — solo 1 correcta.
      2. `presente-regolare-301` — compuesto·essere: pide la forma compuesta con auxiliar essere + participio regular intransitivo/movimiento. categoryIds ["presente-regolare","essere"]. Ej. "Ieri Marco ___ presto" -> "è partito". CRÍTICO: las variantes deben ejercitar concordancia participio↔sujeto (è partito / è partita / sono partiti / sono partite) — varía el sujeto entre variantes para cubrir género/número (D-31-08).
      3. `presente-regolare-302` — presente-con-contraste·avere: pide el PRESENTE regular, con el passato prossimo (avere) YA ESCRITO en el prompt como contraste temporal. categoryIds ["presente-regolare","avere"]. Ej. "Ieri ho parlato spagnolo, ma di solito io ___ italiano" -> "parlo". El passato solo se EXPONE, no se produce.
      4. `presente-regolare-303` — presente-con-contraste·essere: pide el presente regular, con el passato prossimo (essere) escrito como contraste. categoryIds ["presente-regolare","essere"]. Ej. "Ieri sono partito tardi, ma di solito io ___ presto" -> "parto".
    Cada cruce: slot+variantes con ≥2 variants intercambiables (D-31-05/D-30-01 anti-memorización), verbo FRESCO por variante. `type` por defecto multiple-choice (espejo avere-300); usar word-buttons solo si aporta y no rompe el patrón (D-31 discretion). PROHIBIDO (D-31-02): participios irregulares (preso/fatto/detto/messo), auxiliares irregulares (andare/venire). Verbos de entre A1 alta frecuencia con participio regular (avere: parlato/temuto/dormito/lavorato; essere: partito/arrivato/tornato/entrato). Respetar R6 (una sola dificultad por ejercicio). Cada cruce con `explanation` top-level en canon español acentuado (regla + ejemplo paralelo it-es); para los cruces essere la explanation explica explícitamente la concordancia del participio. Gloss "(en español: ...)" inline en prompt cuando desambigüe la dirección del contraste (pidió compuesto vs pidió presente). Nacen con `validation.status: "pending"` y `passes: []` (los valida la Task 2). NO tocar el campo `notes` top-level ni los 8 slots base. Mantener JSON válido (apóstrofes ASCII en explanations plain-text per R-canon).
  </action>
  <acceptance_criteria>
    - `node -e "const d=require('./content/exercises/presente-regolare.json'); console.log(d.exercises.length)"` imprime 12 (8 base + 4 cruces).
    - `node -e "const d=require('./content/exercises/presente-regolare.json'); const x=d.exercises.filter(e=>/^presente-regolare-30[0-3]$/.test(e.id)); console.log(x.length, x.every(e=>e.categoryIds.length===2 && e.categoryIds[0]==='presente-regolare' && e.variants.length>=2))"` imprime `4 true`.
    - Cada cruce tiene categoryIds[1] en {avere, essere}; los 4 cubren la matriz {300:avere-compuesto, 301:essere-compuesto, 302:avere-presente, 303:essere-presente}.
    - grep de participios irregulares prohibidos en los cruces nuevos = 0: `node -e "const d=require('./content/exercises/presente-regolare.json'); const s=JSON.stringify(d.exercises.filter(e=>/-30[0-3]$/.test(e.id))); console.log(/\b(preso|fatto|detto|messo|andato|venuto)\b/.test(s))"` imprime `false`.
    - El cruce essere-compuesto (301) contiene al menos 2 formas de concordancia distintas (regex `partito|partita|partiti|partite` o equivalente con otro verbo essere — ≥2 sufijos -o/-a/-i/-e presentes entre variantes).
    - schema-validator acepta el archivo: reproducir el path de boot `validateContent({ categories, exercisesByFile })` (firma ES module per memoria del plan 30-03) -> 0 errores de shape sobre presente-regolare.json.
    - Los 8 ids base (presente-regolare-are/ere/ire/isc/velar/palatal/isc-wb/are-wb) siguen presentes y sin cambios (diff solo añade los 300..303).
    - explanations de los cruces NO contienen markdown ni #NNN refs; español acentuado (al menos una tilde presente en cada explanation).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const d=require('./content/exercises/presente-regolare.json'); const c=d.exercises.filter(e=>/-30[0-3]$/.test(e.id)); if(d.exercises.length!==12) throw new Error('length '+d.exercises.length); if(c.length!==4) throw new Error('crosses '+c.length); if(!c.every(e=>e.categoryIds[0]==='presente-regolare'&&e.categoryIds.length===2&&e.variants.length>=2)) throw new Error('shape'); console.log('OK')"</automated>
  </verify>
  <done>4 cruces presente-regolare-300..303 añadidos (matriz completa, slot+variantes ≥2, solo participios regulares, ambas direcciones), 8 slots base intactos, exercises.length=12, schema-validator 0 errores, validation.status=pending en los 4.</done>
</task>

<task type="auto">
  <name>Task 2: Validar por quórum cross-vendor R1-R7 los 4 cruces (1-por-1, status validated, D-31-08 concordancia+auxiliar)</name>
  <files>content/exercises/presente-regolare.json</files>
  <read_first>
    - content/exercises/presente-regolare.json (los 4 cruces pending de la Task 1 + el shape passes[] de los slots base ya validated, líneas 43-58)
    - .claude/skills/gsd-validate-exercise/SKILL.md (workflow 1-por-1, NUNCA batched VAL-03, base Opus+Sonnet, Model IDs literales)
    - scripts/validate-ai-pass.mjs (validador cross-vendor Gemini/DeepSeek, fallback 429, --write; pool elegible si Task no disponible en el executor secuencial — patrón del plan 30-02)
    - .planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-CONTEXT.md (D-31-07/D-31-08)
    - Memoria `feedback-disputed-resolution` (disputed -> autor-oráculo, calidad > tokens, SIN override-atajo, traducción ES en prompt para desambiguar)
    - Memoria `feedback-cross-vendor-catches-bugs` (DeepSeek estricto en acentos; complementar con scan de acentos)
    - Memoria `gloss-es-desambiguacion-canon` (C5-leak sobre el gloss "(en español: ...)" es falso-positivo de política — NO arreglar, base de aprobación = Claude Opus+Sonnet)
    - Memoria `explanations-must-be-accented` (C4-accent sobre español sin tildes es bug REAL — arreglar acentos, NO override)
  </read_first>
  <action>
    Pasar los 4 cruces (presente-regolare-300..303) por el quórum completo R1-R7 idéntico a los slots base (D-31-07): UN ejercicio por pase, NUNCA batched (VAL-03). Base de aprobación = `claude-opus-4-8` + `claude-sonnet-4-6` (dos `by` distintos, verdict `correcta`), mismo shape de passes[] que los slots base ya validados. Si la herramienta `Task` no está disponible en el executor secuencial (precedente plan 30-02), usar el pool elegible alternativo `scripts/validate-ai-pass.mjs` (Gemini/DeepSeek, claves `.env`) también 1-por-1, aportando el segundo `correcta` distinto vía author-oracle Claude. Los criterios de validación DEBEN verificar EXPLÍCITAMENTE (D-31-08): (a) concordancia participio↔sujeto con essere en género/número (è partito/partita/sono partiti/partite correctas según el sujeto de cada variante); (b) elección correcta de auxiliar (avere vs essere) para cada verbo. Aplicar también el scan de acentos del canon español sobre explanations/glosses. Resolución de disputed (memoria `feedback-disputed-resolution`): si un vendor marca C4-accent sobre español SIN tildes -> es bug REAL, arreglar los acentos y re-verificar (NO override). Si marca C5-leak/gloss sobre el "(en español: ...)" -> falso-positivo de política (canon R7), resolver por autor-oráculo con audit trail (`{by:"autor",verdict:"correcta",concerns:["[override] ..."]}` + conservar el verdict cross-vendor real como audit trail) + status validated manual (precedente avere-passato-prossimo / plan 30-02). Si el quórum caza un bug ORTOGRÁFICO italiano real (precedente: cross-vendor cazó 3 en plan 30-02) -> corregir por Rule 1 (NO override) y re-verificar que ambos vendors confirman tras el fix. Al cerrar: los 4 cruces con `validation.status: "validated"` y `passes[]` populado (≥2 distinct `by` correcta). 1 commit atómico por ejercicio (granularidad 1 ejercicio = 1 commit, mensaje `validate(presente-regolare): cross 30X`). NO tocar los 8 slots base ni el campo notes.
  </action>
  <acceptance_criteria>
    - Los 4 cruces tienen `validation.status === "validated"`: `node -e "const d=require('./content/exercises/presente-regolare.json'); const c=d.exercises.filter(e=>/-30[0-3]$/.test(e.id)); console.log(c.length, c.every(e=>e.validation.status==='validated'))"` imprime `4 true`.
    - Cada cruce tiene ≥2 passes con verdict `correcta` y `by` distintos (o author-override con audit trail): `node -e "const d=require('./content/exercises/presente-regolare.json'); const c=d.exercises.filter(e=>/-30[0-3]$/.test(e.id)); console.log(c.every(e=>{const ok=e.validation.passes.filter(p=>p.verdict==='correcta');const by=new Set(ok.map(p=>p.by));return by.size>=2;}))"` imprime `true`.
    - 0 cruces en estado `disputed` o `pending` sin resolver.
    - El cruce essere-compuesto (301) validado verifica concordancia (las variantes con sujeto plural usan `sono ...i/...e`, las singular `è ...o/...a`) — confirmado en passes[].concerns sin C-flags abiertos de concordancia.
    - Si hubo correcciones ortográficas reales (Rule 1), ambos pases confirman `correcta` tras el fix (NO author-override sobre un bug real).
    - schema-validator sigue verde sobre el archivo (0 errores de shape tras añadir passes[]).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const d=require('./content/exercises/presente-regolare.json'); const c=d.exercises.filter(e=>/-30[0-3]$/.test(e.id)); if(c.length!==4) throw new Error('crosses'); for(const e of c){ if(e.validation.status!=='validated') throw new Error(e.id+' '+e.validation.status); const ok=e.validation.passes.filter(p=>p.verdict==='correcta'); if(new Set(ok.map(p=>p.by)).size<2) throw new Error(e.id+' <2 correcta by'); } console.log('OK 4/4 validated')"</automated>
  </verify>
  <done>Los 4 cruces presente-regolare-300..303 en status validated, ≥2 passes correcta distintos cada uno (D-31-08 concordancia+auxiliar verificadas explícitamente), 0 disputed/pending, scan de acentos aplicado, disputed resueltos por la política correcta (Rule 1 para bugs reales, autor-oráculo solo para falsos-positivos de política). exercises.length FINAL = 12 registrado para Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Arquitectura: app web estática single-user, offline, sin servidor, sin red, sin auth. La ÚNICA frontera de confianza real en esta fase es **contenido JSON hand-authored -> motor de ejercicios** (la app parsea y renderiza los cruces nuevos en boot vía `loadContent`/`validateContent`).

| Boundary | Description |
|----------|-------------|
| content JSON -> schema-validator -> engine | Los objetos de cruce nuevos cruzan al motor en boot; shape malformado o `categoryIds` con ids inexistentes rompería el load |
| content JSON -> backup export/import | El JSON forma parte del state exportable; un cruce con shape inválido contamina el round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | objetos de cruce en presente-regolare.json | mitigate | schema-validator (`validateContent`) valida shape (payload XOR variants[], categoryIds no-vacío de ids conocidos) en boot — acceptance_criteria de ambas tasks reproduce el path real; el deep-clone anti-prototype-pollution ya está en migrate10to11 (Phase 29) |
| T-31-02 | Tampering | categoryIds referencia categoría inexistente | mitigate | acceptance_criteria verifica que categoryIds resuelve contra content/categories.json (presente-regolare/avere/essere existen); schema-validator rechaza ids desconocidos |
| T-31-03 | Information Disclosure | leak de la respuesta en el prompt (R1) | mitigate | quórum R1-R7 (Task 2) incluye R1 no-leak; el gloss "(en español: ...)" es canon R7 (falso-positivo de política, memoria gloss-es) |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | N/A — esta fase NO instala paquetes (zero-deps invariant CLAUDE.md; los validadores cross-vendor son scripts existentes zero-deps con claves en .env). Sin superficie de supply-chain nueva |

**ASVS L1 categorías N/A (documentadas para evitar inventar amenazas inexistentes):** Authentication, Session Management, Access Control, Communication/TLS, API/Web Service — todas N/A: no hay servidor, red, auth, sesiones ni endpoints en esta arquitectura. La única clase genuina es integridad/shape del contenido (T-31-01/02), cubierta por el schema-validator existente.
</threat_model>

<verification>
- `node -e "require('./content/exercises/presente-regolare.json')"` parsea sin error; exercises.length === 12.
- 4 cruces presente-regolare-300..303 con categoryIds de 2 (hub primero), ≥2 variants, status validated.
- 8 slots base byte-intactos (diff solo añade los cruces).
- schema-validator (`validateContent`) -> 0 errores de shape sobre el archivo final.
- grep de participios/auxiliares irregulares en los cruces = 0.
- Cascada D-54 NO tocada: `grep -c "applyImmediateFailure(this\.state" src/screens/app.js` === 2 (este plan no toca src/, debe seguir en 2).
</verification>

<success_criteria>
PRES-07 (contenido) cerrado: existen los cruces multi-cat presente-regolare↔avere/essere (contraste presente vs passato prossimo, ambas direcciones, solo participios regulares, slot+variantes ≥2), todos validados por quórum R1-R7 con chequeo explícito de concordancia participio↔sujeto (essere) y elección de auxiliar (D-31-08). exercises.length FINAL = 12 disponible para que el Plan 02 sincronice los counts. Motor v1.4 NO tocado.
</success_criteria>

<output>
Create `.planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-01-SUMMARY.md` when done. Registrar el `exercises.length` FINAL (12) leído del JSON para que el Plan 02 lo use en los counts.
</output>
