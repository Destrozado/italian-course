---
phase: 31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [INT-01, INT-02]
must_haves:
  truths:
    - "presente-regolare aparece como NUEVA entrada en los 3 count arrays (antes AUSENTE de los tres)"
    - "El expected de presente-regolare se deriva del nº REAL de slots del JSON (12), NO un número mágico"
    - "TOTAL_EXPECTED sube de 183 a 183+12=195 en run-validation-271.mjs"
    - "El smoke paramétrico (CATEGORIES_WITH_EXPLANATIONS) cubre presente-regolare: count + explanation + ASCII + no-markdown + R1-no-leak"
    - "La suite completa pasa verde incluido VAL_07_STRICT=1"
    - "El reporter VAL-06 cuenta la categoría nueva (todas las variantes validated, 0 disputed sin resolver)"
    - "La cascada D-54 sigue con EXACTAMENTE 2 call-sites de applyImmediateFailure (motor NO tocado)"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "10ª entrada en CATEGORIES_WITH_EXPLANATIONS (presente-regolare, expected dinámico)"
      contains: "presente-regolare.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "10ª entrada en REAL_CATEGORIES + comentarios 9->10"
      contains: "presente-regolare"
    - path: "scripts/run-validation-271.mjs"
      provides: "10ª entrada en CATEGORIES + TOTAL_EXPECTED 183->195 + history line"
      contains: "presente-regolare"
  key_links:
    - from: "los 3 count arrays + TOTAL_EXPECTED"
      to: "content/exercises/presente-regolare.json exercises.length"
      via: "lectura dinámica del JSON real (require/readJson), no estimación"
      pattern: "presente-regolare.json"
    - from: "smoke paramétrico"
      to: "invariantes editoriales (ASCII, no-markdown, R1-no-leak, status validated)"
      via: "loop sobre CATEGORIES_WITH_EXPLANATIONS"
      pattern: "for.*CATEGORIES_WITH_EXPLANATIONS"
---

<objective>
Cerrar la integración lockstep de `presente-regolare` (INT-01 + INT-02). La categoría está actualmente AUSENTE de los TRES count arrays Y de `TOTAL_EXPECTED` (descubrimiento clave de PATTERNS.md): los 8 slots base de Phase 30 + los 4 cruces de Plan 01 no están contados por el harness. Este plan AÑADE una entrada nueva a cada uno de los tres arrays (leyendo el nº REAL de slots del JSON final = 12, D-31-06 dynamic-count, NUNCA un número mágico), sube `TOTAL_EXPECTED` 183->195, deja la entrada del smoke paramétrico cubriendo count + explanation + ASCII + no-markdown + R1-no-leak + status validated, y deja la suite COMPLETA verde incluido `VAL_07_STRICT=1` con el reporter VAL-06 contando la categoría.

Purpose: dejar la 10ª categoría usable end-to-end y bajo el gate de regresión, cerrando v1.7.
Output: 3 archivos de test/script sincronizados; suite verde estricta; reporter VAL-06 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-CONTEXT.md
@.planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-PATTERNS.md
@.planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-01-SUMMARY.md

<interfaces>
<!-- Los 3 hardcodes a sincronizar (líneas verificadas en plan-time) -->

tests/exercise-types.test.js — CATEGORIES_WITH_EXPLANATIONS (línea 1265-1276), HOY 9 entradas, presente-regolare AUSENTE:
  shape de entrada: { file: 'content/exercises/<slug>.json', expected: <slotCount> }
  consumo (loop ~1293-1305): asserts data.exercises.length === expected (SLOTS, no variants) + explanation presente + ASCII apóstrofes (~1319) + no-markdown (~1332) + R1 no-leak (~1350) + R2 no-cross-refs (~1371)
  reuso (línea 1410-1418, D-VAL-18): el MISMO array gatea status="validated" de cada ejercicio
  NOTA: la entrada genero-numero (línea 1267) tiene expected:12 pero el archivo real tiene 13 (fail preexistente AJENO quick task 260614-hxn) — NO TOCAR, fuera de scope Phase 31.

tests/fixtures/slot-variants-integration.test.js — REAL_CATEGORIES (línea 166-176), HOY 9 entradas:
  shape: { slug: '<slug>', expected: <slotCount> }
  consumo (~178-201): file=content/exercises/${slug}.json, validateContent + assert exercises.length===expected
  comentarios a actualizar 9->10: líneas 13, 160, 163; título del bundle test "9 categorías reales" línea 204
  readJson(file).exercises ya se usa (líneas 182,195) -> derivar expected del real

scripts/run-validation-271.mjs — CATEGORIES (línea 158-168, 9 entradas) + TOTAL_EXPECTED (línea 170 = 183):
  shape CATEGORIES: { slug, file: 'content/exercises/<slug>.json', expected: <slotCount> }
  consumo VAL-06 (~386-393): totalValidated===TOTAL_EXPECTED && totalActual===TOTAL_EXPECTED
  history-comment block líneas 64-153 (línea 64: "La suma de expected es 183") — añadir línea de historial estilo existente
  loadCategory ya parsea el JSON (~200-211) -> derivar el count del real

Cascada D-54 (verificar, NO tocar): tests/exercise-types.test.js:785-792 afirma EXACTAMENTE 2 matches de /applyImmediateFailure\(this\.state/g en src/screens/app.js.

Test runner: `node --test tests/*.test.js` (path desnudo falla en Node 22.20 — memoria test-command-node-glob).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar los 3 count arrays + TOTAL_EXPECTED leyendo el nº REAL de slots del JSON (INT-01)</name>
  <files>tests/exercise-types.test.js, tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - content/exercises/presente-regolare.json (el JSON FINAL de Plan 01: exercises.length === 12 — es la fuente de verdad del count)
    - .planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-01-SUMMARY.md (confirma el exercises.length FINAL registrado)
    - tests/exercise-types.test.js líneas 1265-1305 + 1410-1418 (CATEGORIES_WITH_EXPLANATIONS + ambos consumos)
    - tests/fixtures/slot-variants-integration.test.js líneas 13, 160-206 (REAL_CATEGORIES + comentarios + bundle test)
    - scripts/run-validation-271.mjs líneas 64-170 + 386-393 (history block + CATEGORIES + TOTAL_EXPECTED + VAL-06)
    - .planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-CONTEXT.md (D-31-06 contrato de conteo dinámico)
    - Memoria `test-command-node-glob` (runner `node --test tests/*.test.js`)
  </read_first>
  <action>
    Verificar PRIMERO el nº real: `node -e "console.log(require('./content/exercises/presente-regolare.json').exercises.length)"` -> debe ser 12 (8 base + 4 cruces). Usar ESE número real (NO hardcodear 8 ni una estimación de M). Añadir presente-regolare como entrada NUEVA (la categoría está AUSENTE de los tres arrays — no es bumpear, es AÑADIR) en:
      1. `tests/exercise-types.test.js` — array `CATEGORIES_WITH_EXPLANATIONS` (~línea 1274, tras la entrada partitivos): añadir la entrada con file 'content/exercises/presente-regolare.json' y expected 12. Per D-31-06 (Claude's discretion línea 57), preferir derivar el expected del JSON real en tiempo de construcción del array (p.ej. un helper que lea exercises.length) en vez del literal 12, para que el número siga el JSON final. NO tocar la entrada genero-numero (expected 12, fail preexistente AJENO — fuera de scope).
      2. `tests/fixtures/slot-variants-integration.test.js` — array `REAL_CATEGORIES` (~línea 175, tras partitivos): añadir la entrada con slug 'presente-regolare' y expected 12 (derivado del real, mismo criterio). Actualizar los comentarios "9 categorías" -> "10 categorías" en líneas 13, 160, 163 y el título del bundle test en línea 204 ("las 9 categorías reales" -> "las 10 categorías reales").
      3. `scripts/run-validation-271.mjs` — array `CATEGORIES` (~línea 167, tras verbos-movimiento): añadir la entrada con slug 'presente-regolare', file 'content/exercises/presente-regolare.json', expected 12. Subir `TOTAL_EXPECTED` de 183 a 195 (=183+12); si se mantiene el literal expected por entrada, TOTAL_EXPECTED DEBE igualar la suma de los literales — preferir computar el count de presente-regolare desde disco (loadCategory ya parsea, ~líneas 200-211) per D-31-06. Actualizar la línea 64 ("La suma de expected es 183" -> "...es 195") y añadir una línea al history block (líneas 64-153) en el estilo existente, p.ej.: "-> 183 + 12 (v1.7 Phase 31, PRES-07/INT-01): presente-regolare engancha al motor (8 slots base + 4 cruces avere/essere)".
    NO tocar el motor (src/), ni los 8 slots base, ni la entrada genero-numero. NO inventar un M: el número sale del JSON real de Plan 01.
  </action>
  <acceptance_criteria>
    - `node -e "console.log(require('./content/exercises/presente-regolare.json').exercises.length)"` imprime 12 (precondición; si difiere, usar el valor real impreso en los tres sitios).
    - `grep -c "presente-regolare" tests/exercise-types.test.js` >= 1 y la entrada está dentro de CATEGORIES_WITH_EXPLANATIONS.
    - `grep -c "presente-regolare" tests/fixtures/slot-variants-integration.test.js` >= 1 (entrada en REAL_CATEGORIES).
    - `grep -c "presente-regolare" scripts/run-validation-271.mjs` >= 1 (entrada en CATEGORIES).
    - `grep -c "TOTAL_EXPECTED = 195" scripts/run-validation-271.mjs` === 1 (o el valor real 183+length si length distinto de 12).
    - Los comentarios "9 categorías reales" de slot-variants-integration.test.js actualizados a "10": `grep -c "9 categorías reales" tests/fixtures/slot-variants-integration.test.js` === 0.
    - El history block de run-validation-271.mjs tiene una línea nueva mencionando Phase 31: `grep -c "Phase 31" scripts/run-validation-271.mjs` >= 1.
    - El expected de presente-regolare en los tres sitios es 12 (o derivado del real), coherente entre los tres y con TOTAL_EXPECTED = 183 + ese valor.
    - Cascada NO tocada: `grep -c "applyImmediateFailure(this\.state" src/screens/app.js` === 2.
  </acceptance_criteria>
  <verify>
    <automated>L=$(node -e "console.log(require('./content/exercises/presente-regolare.json').exercises.length)"); test "$L" = "12" && grep -q "presente-regolare" tests/exercise-types.test.js && grep -q "presente-regolare" tests/fixtures/slot-variants-integration.test.js && grep -q "presente-regolare" scripts/run-validation-271.mjs && test "$(grep -c 'applyImmediateFailure(this\.state' src/screens/app.js)" = "2" && echo OK</automated>
  </verify>
  <done>Los 3 count arrays incluyen presente-regolare con expected derivado del JSON real (12); TOTAL_EXPECTED = 195; comentarios 9->10 y history line añadidos; entrada genero-numero y motor intactos; cascada sigue en 2 call-sites.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar smoke + reporter VAL-06 + suite verde estricta (INT-02)</name>
  <files>tests/exercise-types.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/exercise-types.test.js líneas 1293-1418 (el loop del smoke + el gate de status validated que ahora corre sobre presente-regolare)
    - scripts/run-validation-271.mjs líneas 375-393 (reporter VAL-06: totalValidated/totalActual === TOTAL_EXPECTED)
    - content/exercises/presente-regolare.json (los 12 ejercicios, todos validated tras Plan 01)
    - tests/fixtures/slot-variants-integration.test.js líneas 178-206 (validateContent + bundle de ids únicos globales)
    - Memoria `test-command-node-glob` (runner `node --test tests/*.test.js`; path desnudo falla en Node 22.20)
  </read_first>
  <action>
    Correr la suite COMPLETA y el reporter, y confirmar verde. Comandos:
      1. `node --test tests/*.test.js` (NO `node --test tests/exercise-types.test.js` desnudo — el path desnudo falla en Node 22.20). El smoke paramétrico (CATEGORIES_WITH_EXPLANATIONS, ya con la 10ª entrada de Task 1) corre automáticamente sobre presente-regolare: count === 12, explanation presente en los 12, ASCII apóstrofes, no-markdown, R1 no-leak, R2 no-cross-refs, y el gate D-VAL-18 exige status="validated" en los 12. El test de cascada (líneas 785-792) debe seguir verde (2 call-sites).
      2. `VAL_07_STRICT=1 node --test tests/*.test.js` — la suite estricta también verde.
      3. `node scripts/run-validation-271.mjs` — reporter VAL-06 PASS: totalValidated === 195 && totalActual === 195, 0 pending/missing/disputed (presente-regolare aporta sus 12 todos validated).
    Resultado esperado de regresión: el ÚNICO fail tolerado es el preexistente AJENO genero-numero ("Esperaba 12, encontré 13", quick task 260614-hxn) que NO es de scope Phase 31 y NO debe agravarse; CERO fails NUEVOS introducidos por presente-regolare. Documentar en el SUMMARY el conteo exacto (p.ej. "481/482, único fail preexistente AJENO genero-numero"). Si aparece cualquier fail NUEVO ligado a presente-regolare (count mismatch, explanation faltante, status no-validated, R1-leak, cascada != 2), volver a Plan 01 o Task 1 según corresponda y corregir — NO silenciar el assert ni tocar el motor. Este task NO modifica archivos salvo que un fail revele un desajuste de count introducido en Task 1 (en cuyo caso se corrige el array, no el test ni el motor).
  </action>
  <acceptance_criteria>
    - `node --test tests/*.test.js` termina sin fails NUEVOS; el único fail es el preexistente AJENO genero-numero 12->13 (verificable: el mensaje de fallo menciona genero-numero, NO presente-regolare).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` igualmente sin fails NUEVOS ligados a presente-regolare.
    - `node scripts/run-validation-271.mjs` imprime `VAL-06 (195/195 validated): ... PASS` (o el total real 183+length); 0 disputed sin resolver para presente-regolare.
    - El smoke corre sobre presente-regolare (el output del runner lista los asserts de content/exercises/presente-regolare.json: count 12, explanation, ASCII, no-markdown).
    - El gate D-VAL-18 (status validated) pasa para los 12 ejercicios de presente-regolare.
    - Cascada D-54: el test "EXACTAMENTE 2 call-sites" sigue verde; `grep -c "applyImmediateFailure(this\.state" src/screens/app.js` === 2.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -5; node scripts/run-validation-271.mjs 2>&1 | grep "VAL-06"; test "$(grep -c 'applyImmediateFailure(this\.state' src/screens/app.js)" = "2" && echo CASCADE-OK</automated>
  </verify>
  <done>Suite completa verde (sin fails nuevos; único fail tolerado = preexistente AJENO genero-numero), VAL_07_STRICT=1 verde, reporter VAL-06 PASS a 195/195 contando presente-regolare, smoke + gate validated corriendo sobre la 10ª categoría, cascada D-54 en 2 call-sites. v1.7 cerrado end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Arquitectura: app web estática single-user, offline, sin servidor, sin red, sin auth. Este plan solo edita archivos de test/script (harness de regresión) — no toca contenido ni motor. La única frontera relevante sigue siendo **contenido JSON -> schema-validator -> engine**, que aquí se EJERCITA (no se modifica) al correr `validateContent` sobre presente-regolare dentro de los tests.

| Boundary | Description |
|----------|-------------|
| count arrays -> JSON real | Los arrays leen exercises.length del JSON; un número mágico desincronizado dejaría pasar contenido no contado (fallo de cobertura del gate) |
| smoke/reporter -> invariantes editoriales | El loop aplica R1-no-leak, no-markdown, ASCII, status validated sobre la categoría nueva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | count array desincronizado del JSON real | mitigate | D-31-06 dynamic-count: expected derivado del JSON real, no número mágico; acceptance_criteria verifica los tres arrays + TOTAL_EXPECTED == 183+length; el smoke falla si el count no cuadra |
| T-31-05 | Repudiation | cobertura del gate validated sobre la categoría nueva | mitigate | el gate D-VAL-18 (mismo array) exige status="validated" en los 12; el reporter VAL-06 cuenta 0 disputed/pending — auditable por el output del script |
| T-31-06 | Information Disclosure | R1 leak en contenido nuevo | mitigate | el smoke (R1 no-leak regex, ~línea 1350) corre automáticamente sobre presente-regolare al añadir la entrada; el gloss "(en español: ...)" es canon R7 y el regex no lo marca |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | N/A — esta fase NO instala paquetes (zero-deps invariant CLAUDE.md). Sin superficie de supply-chain nueva |

**ASVS L1 categorías N/A:** Authentication, Session Management, Access Control, Communication/TLS, API/Web Service — todas N/A en esta arquitectura (sin servidor/red/auth/sesiones/endpoints). La única clase genuina es integridad/cobertura del harness sobre el contenido (T-31-04/05/06).
</threat_model>

<verification>
- `node --test tests/*.test.js` sin fails NUEVOS (único fail tolerado: preexistente AJENO genero-numero 12->13).
- `VAL_07_STRICT=1 node --test tests/*.test.js` verde (sin fails nuevos ligados a presente-regolare).
- `node scripts/run-validation-271.mjs` -> VAL-06 PASS (195/195, 0 disputed).
- Los 3 count arrays + TOTAL_EXPECTED sincronizados al nº real del JSON (12).
- Cascada D-54: `grep -c "applyImmediateFailure(this\.state" src/screens/app.js` === 2 (motor NO tocado).
- Entrada genero-numero NO modificada (fail preexistente AJENO no agravado ni "arreglado" fuera de scope).
</verification>

<success_criteria>
INT-01 + INT-02 cerrados: presente-regolare integrada en lockstep — los 3 count hardcodes + TOTAL_EXPECTED (183->195) sincronizados al nº REAL de slots del JSON (D-31-06, no estimación); +1 entrada en el smoke paramétrico cubriendo la categoría (count/explanation/ASCII/no-markdown/R1-no-leak/status validated); suite completa verde incluido VAL_07_STRICT=1; reporter VAL-06 cuenta la categoría nueva (12 todos validated, 0 disputed sin resolver). Cascada D-54 en 2 call-sites (motor intacto). v1.7 cerrado, 10ª categoría usable end-to-end.
</success_criteria>

<output>
Create `.planning/phases/31-cruces-multi-cat-integraci-n-lockstep-cierre-v1-7/31-02-SUMMARY.md` when done. Documentar el conteo exacto de la suite (pass/fail) y confirmar que el único fail es el preexistente AJENO genero-numero.
</output>
