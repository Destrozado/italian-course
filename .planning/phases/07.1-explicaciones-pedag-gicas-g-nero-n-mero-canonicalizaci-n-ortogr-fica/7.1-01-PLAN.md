---
phase: 07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/preposiciones.json
  - tests/exercise-types.test.js
requirements: [EXPL-06, EXPL-08]
autonomous: false

must_haves:
  truths:
    - "Las 50 explanations existentes de `content/exercises/preposiciones.json` están re-acentuadas al canon nuevo (español correctamente escrito: acentos á/é/í/ó/ú + ñ donde la RAE lo exige) preservando integralmente el contenido pedagógico (D-135)."
    - "Los italianismos citados dentro de las explanations (`città`, `caffè`, `è`, `perché`, `da`, `dalla`, etc.) mantienen su ortografía italiana original — NO se espanholizan (D-137)."
    - "Los apóstrofes ASCII U+0027 siguen siendo ASCII tras el re-accent — cero smart quotes introducidas (D-136 / D-154)."
    - "El bloque `describe('content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)')` hardcoded de `tests/exercise-types.test.js` se reemplaza por un loop paramétrico sobre `CATEGORIES_WITH_EXPLANATIONS = [{file, expected}]` (D-144)."
    - "Tras Plan 7.1-01 el array paramétrico contiene SOLO la entry de preposiciones (`{file: 'content/exercises/preposiciones.json', expected: 50}`); la entry de génnum la añade Plan 7.1-02 cuando ingresa el draft (esto garantiza tests verdes en cada commit de este plan)."
    - "`node --test tests/*.test.js` exit 0 con 181/181 tests verdes (sin cambio neto de count en este plan — 3 tests hardcoded sustituidos por 3 paramétricos sobre 1 sola categoría)."
    - "El motor de re-verificación (cascada D-54, sampler, exerciseStats, racha) no se modifica (D-150..D-157 preservados)."
    - "El schema validator no se modifica (D-150)."
    - "El render UI no se modifica (D-151)."
  artifacts:
    - path: content/exercises/preposiciones.json
      provides: "50 explanations re-acentuadas (canon español con tildes + ñ) preservando los 50 prompts/options/correctIndex/notes/categoryIds/id/type sin alteración"
      contains: "\"explanation\":"
    - path: tests/exercise-types.test.js
      provides: "Bloque paramétrico nuevo `describe('Categorías con explanation coverage (Phase 7.1+)', ...)` iterando sobre array CATEGORIES_WITH_EXPLANATIONS con 1 sola entry (preposiciones); el bloque hardcoded Phase 7 EXPL-04 ELIMINADO"
      contains: "CATEGORIES_WITH_EXPLANATIONS"
  key_links:
    - from: "content/exercises/preposiciones.json"
      to: "src/data/schema-validator.js validateMultipleChoicePayload"
      via: "boot loadContent → validateContent"
      pattern: "explanation"
    - from: "tests/exercise-types.test.js bloque paramétrico"
      to: "readFileSync content/exercises/preposiciones.json"
      via: "loop sobre CATEGORIES_WITH_EXPLANATIONS"
      pattern: "CATEGORIES_WITH_EXPLANATIONS"
---

<objective>
Establecer el canon ortográfico "español correctamente escrito" (D-135) en TODAS las explanations existentes del proyecto + refactorizar el bloque smoke hardcoded Phase 7 EXPL-04 a un loop paramétrico extensible (D-144), preparando la infraestructura para que Plan 7.1-02 ingrese las 40 explanations de Género-Número con solo añadir 1 entry al array.

Purpose: tras Phase 7 cerrada con 50/50 explanations en Preposiciones, el autor detectó que el español está sistemáticamente sin acentuar (canon incidental del executor inicial, no decisión consciente). Phase 7.1 reabre esto y aplica el canon correcto retroactivamente. En paralelo, el bloque smoke test hardcoded (1 archivo) se generaliza a un array (N archivos) para que futuras fases incrementales (7.2..7.6) solo añadan 1 línea por categoría.

Output:
- 50 explanations re-acentuadas en `content/exercises/preposiciones.json` (contenido pedagógico INTACTO, solo se añaden tildes y ñ donde el español lo exige; italianismos preservan acento italiano).
- 1 commit del re-accent tras diff review aprobado por el autor.
- Bloque smoke test paramétrico que reemplaza al hardcoded; conserva las 3 invariantes (coverage, ASCII apóstrofes, no markdown) iteradas sobre cada entry del array.
- 1 commit del refactor paramétrico.
- 1 commit final del plan summary `7.1-01-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-CONTEXT.md
@.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-02-SUMMARY.md
@.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-CONTEXT.md

@content/exercises/preposiciones.json
@tests/exercise-types.test.js

<interfaces>
<!-- Identifiers que el executor necesita sin explorar. -->

From content/exercises/preposiciones.json (estado post-Phase 7 — 50 ejercicios, 50/50 explanations curadas SIN acentos españoles):
- Shape uniforme: `{id, type: "multiple-choice", categoryIds: ["preposiciones"], payload: {prompt, options, correctIndex, explanation, notes?}}`.
- `explanation` actuales tienen patrón típico: `"La preposicion X significa Y. ..."` (sin tildes en `preposición`, `artículo`, `también`, `está`, `están`, `más`, `día`, `físico`, `práctica`, `país`, `región`, `excepción`, `después`, `según`, etc.).
- Italianismos que aparecen LITERALMENTE dentro de las explanations: `'Sono di Roma'`, `'Vado a casa'`, `'città'`, `'caffè'`, `'è'`, `'perché'`, `'dalla'`, `'sulle'`, etc. → estos mantienen tildes italianas EXACTAS (à, è, í, ò, ù).

From tests/exercise-types.test.js líneas 953-1004 (bloque hardcoded a reemplazar):
- `describe('content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)', ...)` con 3 tests sub-cases:
  1. `'all 50 exercises have payload.explanation: string no vacío'` — assert `length === 50` + `missing.length === 0`.
  2. `'explanations use ASCII apostrophes only (CONT-06 / D-129)'` — regex `/[‘’“”]/`.
  3. `'explanations are plain text (no markdown markers — D-126)'` — regex `/(\*\*|__|##|`)/`.
- Imports `readFileSync`, `fileURLToPath`, `dirname`, `resolve` ya están al top del archivo (líneas 17-19) — no re-importar.

Post-refactor target shape:
```js
const CATEGORIES_WITH_EXPLANATIONS = [
  { file: 'content/exercises/preposiciones.json', expected: 50 },
  // Plan 7.1-02 añadirá: { file: 'content/exercises/genero-numero.json', expected: 40 }
];

describe('Categorías con explanation coverage (Phase 7.1+)', () => {
  for (const { file, expected } of CATEGORIES_WITH_EXPLANATIONS) {
    describe(file, () => {
      // load data once
      // 3 sub-tests (coverage, ASCII apóstrofes, no markdown markers)
    });
  }
});
```

From .planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-02-SUMMARY.md:
- Patrón D-85 batch ingest (3 commits: A/B/C × 16 explanations c/u, autor revisa frase por frase).
- Patrón string-insertion targeted preservando formato compacto JSON (NO `json.dump` que destruiría las notes multi-línea).
- Audit trail: PROJECT.md Validated + Key Decisions + REQUIREMENTS.md + ROADMAP.md actualizados al final del Plan último.

From src/data/schema-validator.js (post-Phase 7, NO MODIFICAR en Phase 7.1):
- `validateMultipleChoicePayload`, `validateWordButtonsPayload`, `validateMatchPayload` tienen la regla `if (ex.payload.explanation !== undefined) check string && !!ex.payload.explanation.trim()`.
- El re-accent no introduce strings vacíos (solo se añaden caracteres) → validator sigue pasando.
</interfaces>

<canonical_reaccent_examples>
<!-- Pares (sin acento → con acento) extraídos de las 50 explanations actuales — referencia léxica para el executor. NO modificar italianismos citados literalmente. -->

Cambios típicos esperados (palabra → palabra acentuada):
- preposicion → preposición (también plural preposiciones → preposiciones — ya correcto sin acento)
- articulo → artículo (también articulos → artículos)
- espanol → español (también espanola/espanoles → española/españoles)
- italiano → italiano (ya correcto sin acento)
- ingles → inglés
- frances → francés
- aleman → alemán
- pais → país (también paises → países)
- region → región (regiones → regiones — ya correcto sin acento)
- ciudad → ciudad (ya correcto sin acento)
- traduccion → traducción
- accion → acción (acciones → acciones — ya correcto)
- excepcion → excepción (excepciones → excepciones — ya correcto)
- expresion → expresión
- direccion → dirección
- formacion → formación
- conjuncion → conjunción
- locucion → locución
- idiomatica → idiomática
- semantica → semántica
- gramatical → gramatical (ya correcto)
- categoria → categoría
- atencion → atención
- distincion → distinción
- combinacion → combinación
- pronunciacion → pronunciación
- contraccion → contracción
- regla → regla (ya correcto)
- adjetivo → adjetivo (ya correcto)
- mas (adverbio) → más  ← OJO: solo cuando es adverbio comparativo; "mas" conjunción adversativa NO se acentúa pero es raro en este corpus
- esta (verbo) → está  ← OJO: solo cuando es 3a persona verbo `estar`; "esta" demostrativo NO se acentúa
- estan → están
- estamos → estamos (ya correcto)
- estaba/estaban/estuvo → ya correctos
- tambien → también
- aqui → aquí
- ahi → ahí
- alli → allí
- alla → allá
- aca → acá
- asi → así
- segun → según
- despues → después
- ademas → además
- dia → día (también dias → días)
- mio/mia/mios/mias → mío/mía/míos/mías
- tio/tia → tío/tía
- frio → frío
- guia → guía
- vacio → vacío
- numero → número
- sintoma → síntoma
- termino → término
- ultimo → último
- proxima → próxima
- maximo → máximo
- minimo → mínimo
- practico/practica (adj) → práctico/práctica
- fisico → físico
- tecnico → técnico
- gramatical → gramatical (ya correcto, esdrújula falsa)
- formula → fórmula
- musica → música
- politica → política
- publica → pública
- logica → lógica
- categoria → categoría
- biologia → biología
- analogia → analogía
- canto → canto (ya correcto)
- canon → canon (ya correcto)
- pedagogica → pedagógica
- compania → compañía  ← OJO: tilde + ñ
- ano → año (también anos → años)
- ninos → niños
- senor → señor (señora → señora ya correcta)
- ensenanza → enseñanza
- companeros → compañeros
- montana → montaña
- pequeno → pequeño
- mananas → mañanas
- diseno → diseño
- nigun/ningun → ningún (también ninguna/ninguno ya correctos)
- algun → algún (alguna/alguno ya correctos)
- esta/estas/este/estos (demostrativos) → NO cambian (sin tilde según RAE 2010)
- solo (adverbio o adjetivo) → solo (RAE 2010 ya no tilda; mantener como está)

Italianismos NUNCA tocar:
- `città`, `caffè`, `è` (verbo essere), `perché`, `può`, `più`, `così`, `lì`, `là`, `vendrò`, `andò`
- todas las preposiciones italianas: `di`, `a`, `da`, `in`, `con`, `su`, `per`, `fra`, `tra`
- artículos italianos: `il`, `lo`, `la`, `l'`, `i`, `gli`, `le`
- formas articuladas: `dello`, `della`, `dell'`, `dei`, `degli`, `delle`, `al`, `allo`, `alla`, `all'`, `ai`, `agli`, `alle`, `dal`, `dallo`, `dalla`, `dall'`, `dai`, `dagli`, `dalle`, `nel`, `nello`, `nella`, `nell'`, `nei`, `negli`, `nelle`, `sul`, `sullo`, `sulla`, `sull'`, `sui`, `sugli`, `sulle`
- ejemplos italianos completos citados entre comillas o paréntesis: `Sono di Roma`, `Vado a casa`, `Vengo dalle mie zie`, `Il libro è scritto da lui`, `Il film di Fellini`, etc.

**Heurística operativa:** si la palabra aparece dentro de una cita de frase italiana (e.g., `Vado a casa`, `Sono di Roma`), respetar su grafía italiana. Si la palabra forma parte del texto explicativo en español, aplicar el canon de la RAE.

**Caso de borde explícito:** la palabra `da` puede aparecer en 2 contextos:
- Como preposición italiana citada (e.g., `la preposición Da indica...`) → escribir con mayúscula y SIN tilde (es la pieza léxica italiana).
- Como verbo español `dar` 3a persona presente (e.g., `da lugar a`) → escribir `da` sin tilde (la RAE solo tilda el imperativo `dé`).
- En la práctica, en este corpus `da` aparece SIEMPRE como preposición italiana → no se modifica.
</canonical_reaccent_examples>

<parametric_test_skeleton>
<!-- Forma exacta del bloque post-refactor — referencia normativa para Task 2. -->

```js
// ────────────────────────────────────────────────────────────────────────────
// Phase 7.1 Plan 7.1-01 — Smoke test paramétrico explanation coverage (EXPL-08)
// ────────────────────────────────────────────────────────────────────────────
// Reemplaza el bloque hardcoded Phase 7 EXPL-04 (1 sola categoría) por un loop
// sobre CATEGORIES_WITH_EXPLANATIONS que itera sobre N archivos con N×3 sub-tests
// (coverage, ASCII apóstrofes, plain text). Añadir una categoría futura (7.2+)
// es 1 línea en el array — sin tocar el código del describe.

const CATEGORIES_WITH_EXPLANATIONS = [
  { file: 'content/exercises/preposiciones.json', expected: 50 },
  // Plan 7.1-02 añadirá la entry de genero-numero (40 ejercicios).
  // Fases 7.2+ añadirán avere, essere, verbos-movimiento, profesiones, sustantivos-irregulares.
];

describe('Categorías con explanation coverage (Phase 7.1+)', () => {
  for (const { file, expected } of CATEGORIES_WITH_EXPLANATIONS) {
    describe(file, () => {
      const __filename = fileURLToPath(import.meta.url);
      const __dirname = dirname(__filename);
      const path = resolve(__dirname, '..', file);
      const data = JSON.parse(readFileSync(path, 'utf-8'));

      test(`${expected}/${expected} ejercicios con payload.explanation: string no vacío`, () => {
        assert.equal(data.exercises.length, expected,
          `Esperaba ${expected} ejercicios en ${file}, encontré ${data.exercises.length}`);

        const missing = data.exercises.filter(ex =>
          typeof ex.payload?.explanation !== 'string' || !ex.payload.explanation.trim()
        );

        assert.equal(missing.length, 0,
          `Ejercicios sin explanation válida en ${file}: ${missing.map(ex => ex.id).join(', ')}`);
      });

      test('explanations use ASCII apostrophes only (CONT-06 / D-129 / D-154)', () => {
        const smartQuotePattern = /[‘’“”]/;
        const violations = data.exercises
          .filter(ex => smartQuotePattern.test(ex.payload?.explanation || ''))
          .map(ex => ex.id);

        assert.equal(violations.length, 0,
          `Smart quotes encontradas en ${file}: ${violations.join(', ')}`);
      });

      test('explanations are plain text (no markdown markers — D-126 / D-153)', () => {
        const mdPattern = /(\*\*|__|##|`)/;
        const violations = data.exercises
          .filter(ex => mdPattern.test(ex.payload?.explanation || ''))
          .map(ex => ex.id);

        assert.equal(violations.length, 0,
          `Markdown markers encontradas en ${file}: ${violations.join(', ')}`);
      });
    });
  }
});
```

**Importante:** el bloque `describe('content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)')` original (líneas 964-1004) se ELIMINA por completo y se reemplaza con el bloque paramétrico arriba. Esto mantiene 3 sub-tests sobre Preposiciones (idéntica cobertura), solo cambia la forma de iteración (loop vs hardcoded).

**Tests count tras Plan 7.1-01:**
- Bloque hardcoded eliminado: 3 tests.
- Bloque paramétrico con 1 entry: 1 × 3 = 3 tests.
- **Delta neto: 0.** Total tras Plan 7.1-01: 181/181 verdes.
- Plan 7.1-02 al añadir genero-numero al array sumará +3 tests → 184/184 (D-145).
</parametric_test_skeleton>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-acentuación canónica de las 50 explanations Preposiciones — Claude genera diff propuesto + autor aprueba + aplicar con string-insertion preservando formato compacto</name>
  <files>content/exercises/preposiciones.json</files>
  <read_first>
    - `content/exercises/preposiciones.json` ENTERO (50 ejercicios, 604 líneas). Leer una sola vez para tener TODAS las explanations en contexto. NO re-leer durante la generación del diff (extraer todo de la primera lectura).
    - `.planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-CONTEXT.md` sección `### Canon ortográfico` (D-135..D-138) y `### Estrategia de re-acentuación` (D-139..D-140) para confirmar la operación: SOLO se añaden tildes/ñ donde el español lo exige; los italianismos citados (`città`, `caffè`, `dalla`, `Vado a casa`, etc.) NO se tocan; cero modificación al contenido pedagógico.
    - El bloque `<canonical_reaccent_examples>` arriba en este PLAN.md — léxico de referencia (palabra → palabra acentuada) y heurística operativa (palabra en cita italiana vs palabra en texto español).
  </read_first>
  <action>
    Per D-135 (canon nuevo: español correctamente escrito) + D-137 (italianismos preservan ortografía italiana) + D-139 (1 pasada Claude + diff review autor + 1 commit) + D-140 (cambios típicos: añadir tildes/ñ donde RAE lo exige):

    **Sub-step 1a — Diff propuesto (sin escribir):**

    El executor lee las 50 explanations actuales (todas SIN acentos españoles) y genera, en un mensaje al autor, el diff completo en formato `id → before / after` (50 entries). Solo entries donde HAY cambio se muestran (si una explanation ya está perfectamente acentuada — improbable pero posible — se omite del diff con nota "sin cambios").

    Formato del mensaje (ejemplo):
    ```
    Diff propuesto re-acentuación 50 Preposiciones (Plan 7.1-01 Task 1):

    preposiciones-001:
      BEFORE: "La preposicion Di indica origen o procedencia: de donde viene alguien o algo de forma estable. Con nombres de ciudad va sin articulo: Sono di Roma significa 'soy de Roma'. Distinta de Da, que marca el punto de partida de un movimiento (Vengo da Roma = vengo desde Roma)."
      AFTER:  "La preposición Di indica origen o procedencia: de dónde viene alguien o algo de forma estable. Con nombres de ciudad va sin artículo: Sono di Roma significa 'soy de Roma'. Distinta de Da, que marca el punto de partida de un movimiento (Vengo da Roma = vengo desde Roma)."

    preposiciones-002:
      BEFORE: "Con la palabra casa el italiano usa la preposicion A como excepcion idiomatica..."
      AFTER:  "Con la palabra casa el italiano usa la preposición A como excepción idiomática..."

    [... 48 más entries hasta preposiciones-050 ...]

    Resumen: 50/50 explanations modificadas. Italianismos preservados (verificable: `città`, `caffè`, `dalla`, `Sono di Roma`, `Vado a casa`, etc. NO aparecen en el diff). Apóstrofes ASCII preservados. Contenido pedagógico INTACTO (solo se añaden caracteres acentuados; cero re-escritura).

    Esperando review del autor antes de aplicar al archivo.
    ```

    Constraints DUROS al generar el diff (per D-135..D-140):
    - SOLO añadir tildes/ñ donde la RAE lo exige. Cero re-escritura de frases. Cero cambio léxico (e.g., NO cambiar `usa` por `emplea`).
    - Italianismos citados literalmente (`città`, `caffè`, `è`, `perché`, `dalla`, `sulle`, `Sono di Roma`, `Vado a casa`, `Vengo dalle mie zie`, `Il libro è scritto da lui`, etc.) NO se modifican.
    - Las preposiciones italianas escritas con mayúscula como pieza léxica (`la preposición Di`, `la preposición Da`, `la preposición A`, etc.) NO se acentúan — son ítems léxicos italianos.
    - Apóstrofes ASCII U+0027 estrictos. Cero smart quotes `'` `'` `"` `"`. El executor verifica en el diff que ninguna comilla automática se ha colado (D-136).
    - Demostrativos `este/esta/estos/estas` SIN tilde (RAE 2010). `solo` (adv. o adj.) SIN tilde (RAE 2010).
    - Distinguir `esta` demostrativo (sin tilde) vs `está` verbo (con tilde). Distinguir `mas` conjunción (sin tilde, raro) vs `más` adverbio (con tilde, frecuente).
    - Distinguir `da` preposición italiana citada (sin tilde, jerga léxica) vs cualquier otro contexto (en este corpus no hay otro contexto).
    - Cero cambio a `prompt`, `options`, `correctIndex`, `categoryIds`, `notes`, `id`, `type`, ni a ningún otro campo. SOLO se modifica `payload.explanation`.

    **Sub-step 1b — Review del autor:**

    El autor revisa el diff completo. Posibles respuestas:
    - `approved re-accent` → seguir a sub-step 1c.
    - `refine: preposiciones-NNN — feedback breve` (e.g., "preposiciones-007 — `tambien` debe seguir sin tilde porque…") → el executor itera el diff de ESE entry y vuelve a presentar el diff parcial actualizado. Repetir hasta APPROVED.

    **Sub-step 1c — Aplicar el diff al archivo:**

    Tras `approved re-accent`, el executor MODIFICA `content/exercises/preposiciones.json` aplicando los cambios aprobados. Método: string-insertion targeted (e.g., Edit tool con `old_string`/`new_string` exactos por cada explanation, o regenerar el archivo preservando formato compacto). NO usar `json.dump` de Python ni `JSON.stringify` con indent — eso reformatearía todo el archivo destrozando el formato compacto actual de `options` y `notes`.

    Verificaciones post-aplicación (en este orden):
    1. JSON parseable: `python3 -c "import json; json.load(open('content/exercises/preposiciones.json'))"` exit 0.
    2. Coverage preservada 50/50: `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print(sum(1 for x in d['exercises'] if x['payload'].get('explanation') and x['payload']['explanation'].strip()))"` → `50`.
    3. Cero smart quotes introducidas: `python3 -c "import json,sys; d=json.load(open('content/exercises/preposiciones.json')); errs=[x['id'] for x in d['exercises'] if any(c in (x['payload'].get('explanation') or '') for c in ['‘','’','“','”'])]; sys.exit(0 if not errs else (print('SMART QUOTES IN:', errs) or 1))"` exit 0.
    4. Cero markdown introducido: `python3 -c "import json,sys,re; d=json.load(open('content/exercises/preposiciones.json')); errs=[x['id'] for x in d['exercises'] if re.search(r'(\*\*|__|##|\`)', x['payload'].get('explanation') or '')]; sys.exit(0 if not errs else (print('MARKDOWN IN:', errs) or 1))"` exit 0.
    5. Italianismos preservados: `grep -c "Sono di Roma\|Vado a casa\|città\|caffè\|dalla\|sulle" content/exercises/preposiciones.json` ≥ 6 (presencia mínima de los italianismos canónicos esperados — sanity check).
    6. Acentos españoles añadidos (sanity check positiva): `grep -c "preposición\|artículo\|excepción\|también\|está\|están\|región\|país\|día\|más\|según\|también\|español" content/exercises/preposiciones.json` ≥ 30 (varias docenas de acentos esperados en 50 explanations).
    7. Schema validator sigue verde: `node --test tests/exercise-types.test.js 2>&1 | tail -5` muestra 181 tests pass / 0 fail.

    Si CUALQUIER verificación falla, el executor reporta al autor antes de commitear.

    Commit (tras 7/7 verificaciones OK):
    `refactor(7.1-01): re-acentuación canónica 50 explanations Preposiciones (EXPL-06)` — per D-139.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('content/exercises/preposiciones.json'))"` exit 0 (parseable).
    - `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); assert sum(1 for x in d['exercises'] if x['payload'].get('explanation') and x['payload']['explanation'].strip()) == 50"` exit 0 (50/50 coverage preservada).
    - `python3 -c "import json,sys; d=json.load(open('content/exercises/preposiciones.json')); errs=[x['id'] for x in d['exercises'] if any(c in (x['payload'].get('explanation') or '') for c in ['‘','’','“','”'])]; sys.exit(0 if not errs else 1)"` exit 0 (cero smart quotes).
    - `python3 -c "import json,sys,re; d=json.load(open('content/exercises/preposiciones.json')); errs=[x['id'] for x in d['exercises'] if re.search(r'(\*\*|__|##|\`)', x['payload'].get('explanation') or '')]; sys.exit(0 if not errs else 1)"` exit 0 (cero markdown).
    - `grep -v '^\s*//' content/exercises/preposiciones.json | grep -c "preposición\|artículo\|excepción"` ≥ 10 (acentos añadidos verificables — al menos 10 ocurrencias combinadas de palabras canónicamente acentuadas).
    - `grep -c "Sono di Roma\|Vado a casa" content/exercises/preposiciones.json` ≥ 1 (italianismo canónico preservado como sanity check).
    - `node --test tests/exercise-types.test.js 2>&1 | grep -E "^# (tests|pass|fail)"` muestra `# tests 181 / # pass 181 / # fail 0`.
    - El autor confirmó APPROVED el diff completo (resume signal `approved re-accent`).
    - Commit `refactor(7.1-01): re-acentuación canónica 50 explanations Preposiciones (EXPL-06)` hecho.
    - `git diff HEAD~1 -- content/exercises/preposiciones.json | grep -c "^-"` ≥ 50 y `git diff HEAD~1 -- content/exercises/preposiciones.json | grep -c "^+"` ≥ 50 (cada explanation modificada genera 1 línea `-` y 1 línea `+` aprox; suma ≥ 100 en total — sanity check del scope del diff).
  </acceptance_criteria>
  <how-to-verify>
    1. Tras la propuesta del executor (diff de 50 entries en chat), el autor lee el diff completo (~50-100 líneas modificadas).
    2. Para cada entry el autor puede:
       - Aceptar tácitamente (silencio = aceptación al hacer el approval global).
       - Pedir refine: `refine: preposiciones-NNN — feedback`. El executor itera ese entry y vuelve a presentar el diff parcial.
    3. Cuando el autor está satisfecho con las 50 entries, responde `approved re-accent`.
    4. El executor ESCRIBE el archivo + ejecuta las 7 verificaciones + COMMITEA.
    5. Muestra al autor el output de las verificaciones (especialmente coverage 50/50, tests 181/181, italianismos preservados) para que pueda confirmar visualmente que todo PASS.
  </how-to-verify>
  <resume-signal>Type "approved re-accent" cuando el diff completo de las 50 explanations esté aprobado y el commit hecho con las 7 verificaciones PASS.</resume-signal>
  <verify><human-check>Diff propuesto visible al autor; refine iterativo hasta APPROVED; commit tras "approved re-accent" con las 7 verificaciones automáticas PASS.</human-check></verify>
  <done>Las 50 explanations de Preposiciones re-acentuadas al canon español + italianismos preservados + cero smart quotes/markdown introducidos + tests 181/181 verdes + commit hecho.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor del smoke test EXPL-04 hardcoded a loop paramétrico CATEGORIES_WITH_EXPLANATIONS (1 entry Preposiciones — Plan 7.1-02 añadirá génnum)</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - `tests/exercise-types.test.js` líneas 953-1004 (bloque `describe('content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)')` actual a eliminar).
    - `tests/exercise-types.test.js` líneas 1-25 (imports al top — confirmar que `readFileSync`, `fileURLToPath`, `dirname`, `resolve` ya están importados; NO re-importar).
    - El bloque `<parametric_test_skeleton>` arriba en este PLAN.md — forma exacta del bloque post-refactor.
  </read_first>
  <action>
    Per D-144 (array paramétrico reemplaza describe hardcoded):

    **Sub-step 2a — Eliminar el bloque hardcoded:**

    En `tests/exercise-types.test.js`, ELIMINAR las líneas 953-1004 enteras (desde el comentario `// ────...` antes del describe Phase 7 EXPL-04 hasta el `});` de cierre del describe). Esto incluye:
    - El comentario header del bloque (líneas 953-963).
    - El `describe('content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)', () => { ... })` completo (líneas 964-1004).

    **Sub-step 2b — Insertar el bloque paramétrico al FINAL del archivo:**

    Pegar EXACTAMENTE el contenido del bloque `<parametric_test_skeleton>` (sección de este PLAN.md) al final del archivo. El array CATEGORIES_WITH_EXPLANATIONS contiene SOLO una entry en este plan:

    ```js
    const CATEGORIES_WITH_EXPLANATIONS = [
      { file: 'content/exercises/preposiciones.json', expected: 50 },
      // Plan 7.1-02 añadirá: { file: 'content/exercises/genero-numero.json', expected: 40 }
    ];
    ```

    El comentario inline `// Plan 7.1-02 añadirá: ...` documenta el patrón para futuras fases (7.2+). Per `<parametric_test_skeleton>` el comentario header del describe lleva la attribution a `Phase 7.1 Plan 7.1-01 — Smoke test paramétrico explanation coverage (EXPL-08)`.

    **Sub-step 2c — Verificaciones:**

    1. Tests verdes: `node --test tests/*.test.js 2>&1 | grep -E "^# (tests|pass|fail)"` → `# tests 181 / # pass 181 / # fail 0`.
    2. El nuevo bloque paramétrico está presente: `grep -c "CATEGORIES_WITH_EXPLANATIONS" tests/exercise-types.test.js` ≥ 2 (declaración del array + uso en el loop).
    3. El bloque hardcoded ya NO está: `grep -c "content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)" tests/exercise-types.test.js` == 0.
    4. El nuevo describe está presente: `grep -c "Categorías con explanation coverage (Phase 7.1+)" tests/exercise-types.test.js` ≥ 1.
    5. Los 3 sub-tests siguen ejerciéndose (coverage, ASCII apóstrofes, no markdown markers) — el output de `node --test` debe mostrar los 3 sub-tests bajo el describe nuevo. Comprobar con `node --test tests/exercise-types.test.js 2>&1 | grep -E "ejercicios con payload.explanation|ASCII apostrophes|plain text"` → 3 líneas matched.

    Commit:
    `refactor(7.1-01): smoke test paramétrico CATEGORIES_WITH_EXPLANATIONS — sustituye bloque EXPL-04 hardcoded (EXPL-08)` — per D-144.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | grep -E "^# (tests|pass|fail)"</automated>
    <automated>grep -c "CATEGORIES_WITH_EXPLANATIONS" tests/exercise-types.test.js</automated>
    <automated>grep -c "content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)" tests/exercise-types.test.js</automated>
    <automated>grep -c "Categorías con explanation coverage (Phase 7.1+)" tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/*.test.js 2>&1 | grep "^# tests"` muestra `# tests 181` (sin cambio neto de count: 3 hardcoded eliminados, 3 paramétricos añadidos sobre 1 entry).
    - `node --test tests/*.test.js 2>&1 | grep "^# fail"` muestra `# fail 0`.
    - `grep -c "CATEGORIES_WITH_EXPLANATIONS" tests/exercise-types.test.js` ≥ 2.
    - `grep -c "content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)" tests/exercise-types.test.js` == 0 (bloque hardcoded eliminado).
    - `grep -c "Categorías con explanation coverage (Phase 7.1+)" tests/exercise-types.test.js` ≥ 1 (nuevo describe parent presente).
    - Sub-tests verificables individualmente por output de `node --test`: 1 sub-test "X/X ejercicios con payload.explanation", 1 sub-test "ASCII apostrophes", 1 sub-test "plain text" — total 3 sub-tests bajo el nuevo describe iterando 1 archivo (preposiciones).
    - El comentario inline `// Plan 7.1-02 añadirá` está presente — documenta el patrón para extensión: `grep -c "Plan 7.1-02 añadirá" tests/exercise-types.test.js` ≥ 1.
    - Commit `refactor(7.1-01): smoke test paramétrico CATEGORIES_WITH_EXPLANATIONS` hecho.
  </acceptance_criteria>
  <done>Bloque hardcoded EXPL-04 eliminado + bloque paramétrico CATEGORIES_WITH_EXPLANATIONS inserto con 1 entry + comentario placeholder para Plan 7.1-02 + tests 181/181 verdes + commit hecho.</done>
</task>

<task type="auto">
  <name>Task 3: Plan summary 7.1-01-SUMMARY.md + ROADMAP plan progress check</name>
  <files>.planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-01-SUMMARY.md, .planning/ROADMAP.md</files>
  <read_first>
    - `$HOME/.claude/get-shit-done/templates/summary.md` (template oficial — usar como base estructural).
    - `.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-02-SUMMARY.md` (precedente directo — para tono y nivel de detalle).
    - `.planning/ROADMAP.md` líneas 135-160 (Phase 7.1 entry con Plans 0/0 — actualizar la lista de plans a `[x] 7.1-01-PLAN.md — ...`).
  </read_first>
  <action>
    Per templates/summary.md:

    **Sub-step 3a — Crear `7.1-01-SUMMARY.md`:**

    Crear archivo `.planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-01-SUMMARY.md` documentando:

    - **Goal alcanzado:** canon ortográfico canónico aplicado a 50/50 Preposiciones + infraestructura paramétrica del smoke test instalada para futuras categorías.
    - **Tasks completed:**
      - Task 1: Re-acentuación canónica 50 explanations (commit hash).
      - Task 2: Refactor smoke test a CATEGORIES_WITH_EXPLANATIONS (commit hash).
      - Task 3: Plan summary (este commit).
    - **Commits:** lista de 3 commits con hash + mensaje breve.
    - **Tests:** baseline 181 → 181 verdes (cero delta — 3 hardcoded sustituidos por 3 paramétricos sobre 1 entry).
    - **Decisions materialized:** D-135 (canon ortográfico), D-136 (apóstrofes ASCII preservados), D-137 (italianismos preservados), D-139 (1 pasada + diff review autor), D-140 (cambios típicos), D-144 (array paramétrico), D-145 (tests count esperado — anclaje para Plan 7.1-02 que sumará +3).
    - **Decisions preserved (no-op verifications):** D-150 (schema validator no tocado), D-151 (render UI no tocado), D-152 (graceful degradation preservado), D-153 (no markdown markers preserved), D-154 (apóstrofes ASCII preserved), D-155 (zero schemaVersion migration), D-156 (avere APPEND-ONLY preserved — Phase 7.1 no toca avere.json), D-157 (T-02-01 anti-XSS preserved).
    - **Sample diff (representativo):** mostrar 3-5 explanations BEFORE/AFTER del re-accent como muestra de la operación (sin reproducir las 50).
    - **Italianismos preservados (sanity check):** lista de italianismos verificados presentes post-re-accent (e.g., `Sono di Roma`, `Vado a casa`, `Vengo dalle mie zie`, `città`, `caffè`, `è scritto`, etc.).
    - **Next steps:** referencia a Plan 7.1-02 (ingesta génnum + audit trail) que añadirá 1 entry a CATEGORIES_WITH_EXPLANATIONS y subirá tests count a 184/184.
    - **Lessons learned:** observaciones del autor durante diff review (e.g., entries que requirieron refine; matices de la heurística italianismo vs español).

    **Sub-step 3b — Actualizar ROADMAP.md:**

    EDITAR `.planning/ROADMAP.md`:
    - En la sección Phase 7.1 (líneas ~135-145), ACTUALIZAR:
      - `**Plans:** 0 plans` → `**Plans:** 2 plans` (este plan establece el count total esperado tras la planificación).
      - La lista de Plans:
        ```
        Plans:
        - [x] 7.1-01-PLAN.md — Canon ortográfico canónico aplicado a 50 Preposiciones + refactor smoke test a paramétrico (EXPL-06, EXPL-08)
        - [ ] 7.1-02-PLAN.md — Ingesta 40 explanations Género-Número del draft del autor + audit trail PROJECT/REQ/ROADMAP (EXPL-06, EXPL-07)
        ```
    - En la tabla `## Progress` (líneas ~145-160), ACTUALIZAR la fila Phase 7.1:
      - `| 7.1. Explicaciones Género-Número + canonicalización ortográfica | 1/2 | In Progress |   |`

    Commit:
    `docs(7.1-01): plan summary + ROADMAP plan progress (Phase 7.1 1/2 plans complete)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-01-SUMMARY.md && echo OK</automated>
    <automated>grep -c "7.1-01-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -c "EXPL-06\|EXPL-08" .planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-01-SUMMARY.md` existe.
    - SUMMARY referencia ≥ 3 commits (Task 1 re-accent, Task 2 refactor, Task 3 docs).
    - SUMMARY menciona D-135, D-137, D-139, D-144 (al menos esos 4 IDs).
    - SUMMARY incluye ≥ 3 BEFORE/AFTER samples del re-accent.
    - SUMMARY menciona "tests 181/181" o "delta 0" explícitamente.
    - `.planning/ROADMAP.md` actualizado:
      - Phase 7.1 lista de Plans tiene 2 entradas (`7.1-01-PLAN.md` con `[x]`, `7.1-02-PLAN.md` con `[ ]`).
      - Tabla `## Progress` fila 7.1 muestra `1/2`.
    - `grep -c "Phase 7.1" .planning/ROADMAP.md` ≥ 2 (entry + Progress).
    - Commit `docs(7.1-01): plan summary` hecho.
  </acceptance_criteria>
  <done>7.1-01-SUMMARY.md creado documentando los 2 task commits + decisiones materializadas + sample diffs + next steps. ROADMAP.md Phase 7.1 actualizado con Plans 1/2 In Progress. Plan 7.1-01 cierra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author-curated content (50 existing Preposiciones explanations) → re-accent diff → JSON file | Single-user manual editing; threat surface idéntico a Phase 7 SEED-01 (autor revisa diff frase por frase, oracle pedagógico). |
| Re-accent transformación (Claude regenera con tildes) → string-insertion → JSON file | El riesgo es destrucción pedagógica si Claude reescribe en vez de añadir tildes. Mitigado por D-139 (diff review humano explícito) + acceptance criterion 7 (italianismos preservados verificables por grep). |
| Smoke test refactor (hardcoded → paramétrico) | Riesgo: el refactor cambia el conjunto efectivo de tests sin que el autor lo note → regresión de cobertura. Mitigado por acceptance criteria de Task 2 que verifica 3 sub-tests presentes Y tests verdes globales 181/181. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-01 | Tampering (smart quote contamination durante re-accent) | content/exercises/preposiciones.json post-re-accent | mitigate | Acceptance criterion 3 de Task 1 (`smart quote pattern /[‘’“”]/` exit 0 sobre 50 explanations). El smoke test paramétrico de Task 2 también ejerce esta defensa cada vez que se corre `node --test`. |
| T-7.1-02 | Tampering (markdown markers contamination durante re-accent) | content/exercises/preposiciones.json post-re-accent | mitigate | Acceptance criterion 4 de Task 1 (`mdPattern: /(\*\*|__|##|\`)/` exit 0). Smoke test paramétrico verifica también. |
| T-7.1-03 | Information Integrity (re-acentuación destructiva — Claude reescribe en vez de añadir tildes) | 50 explanations Preposiciones | mitigate | D-139: diff review humano explícito en Task 1; el autor APRUEBA frase por frase antes del commit. Acceptance criterion 5 (`grep` italianismos canónicos `Sono di Roma`, `Vado a casa` ≥ 1) verifica preservación de citas italianas. Acceptance criterion 8 (`git diff` ratio `-/+`) confirma scope diff (50 entries × ~2 líneas modificadas — coherente con el escenario "solo se añaden acentos"). |
| T-7.1-04 | Information Integrity (italianismo accidentalmente espanholizado) | citas italianas dentro de explanations | mitigate | D-137 explícito + heurística operativa documentada en `<canonical_reaccent_examples>` (palabra en cita italiana vs palabra en texto español). Verificación por `grep -c "Sono di Roma\|Vado a casa\|città\|caffè"` durante review humano del diff. |
| T-7.1-05 | Repudiation (refactor smoke test pierde cobertura sin que se note) | tests/exercise-types.test.js | mitigate | Acceptance criterion 5 de Task 2 verifica que los 3 sub-tests aparecen en el output de `node --test` (coverage, ASCII apóstrofes, no markdown). Acceptance criterion 6 verifica que el comentario `// Plan 7.1-02 añadirá` está presente — pista visible para el siguiente plan. |
| T-7.1-06 | Tampering (schema validator modificado sin querer) | src/data/schema-validator.js | accept | `src/data/schema-validator.js` NO está en `files_modified` del plan. D-150 explícito. Si por error se modificara, los 12 tests Phase 7 D-116 lo detectarían inmediatamente. |
| T-7.1-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas en Phase 7.1. Plan 7.1-01 no introduce installs. |

</threat_model>

<verification>
## Plan-level verification

- [ ] `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); assert sum(1 for x in d['exercises'] if x['payload'].get('explanation') and x['payload']['explanation'].strip()) == 50"` exit 0 (Task 1 done).
- [ ] Cero smart quotes en explanations post-re-accent (Task 1 acceptance criterion 3 + smoke test paramétrico Task 2).
- [ ] Cero markdown markers en explanations post-re-accent (Task 1 acceptance criterion 4 + smoke test paramétrico Task 2).
- [ ] Italianismos preservados — `grep -c "Sono di Roma\|Vado a casa\|città\|caffè\|dalla\|sulle" content/exercises/preposiciones.json` ≥ 5.
- [ ] Acentos españoles añadidos — `grep -c "preposición\|artículo\|excepción"` ≥ 10.
- [ ] `tests/exercise-types.test.js` contiene `CATEGORIES_WITH_EXPLANATIONS` (declaración + uso).
- [ ] `tests/exercise-types.test.js` NO contiene `content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)` (bloque hardcoded eliminado).
- [ ] `node --test tests/*.test.js` exit 0 con 181/181 verdes (cero delta).
- [ ] El autor aprobó el diff re-accent (resume signal `approved re-accent`).
- [ ] `7.1-01-SUMMARY.md` creado documentando 3 task commits + 3 BEFORE/AFTER samples + decisiones materializadas + tests 181/181.
- [ ] ROADMAP.md Phase 7.1 actualizado con Plans 1/2 In Progress.
- [ ] Schema validator (`src/data/schema-validator.js`) NO modificado (D-150).
- [ ] Render UI (`index.html`, `styles.css`) NO modificado (D-151).
- [ ] schemaVersion NO incrementado (D-155 — sigue 4).
- [ ] `content/exercises/avere.json` NO modificado (D-156 APPEND-ONLY preservado).
</verification>

<success_criteria>
- 50/50 explanations de Preposiciones re-acentuadas al canon español (D-135).
- Italianismos citados preservados al 100% (D-137) — verificable por grep.
- Cero smart quotes / cero markdown introducidos durante el re-accent (D-136 / D-154 / D-153).
- Apóstrofes ASCII U+0027 preservados (D-129 / D-136).
- Bloque smoke test paramétrico `CATEGORIES_WITH_EXPLANATIONS` operativo con 1 entry (Preposiciones) y comentario placeholder para Plan 7.1-02 (D-144).
- Cero delta de tests count (181 → 181) — 3 hardcoded sustituidos por 3 paramétricos sobre 1 archivo (D-145 anclaje).
- Schema validator NO modificado (D-150).
- Render UI NO modificado (D-151).
- schemaVersion NO incrementado — sigue 4 (D-155).
- avere.json NO modificado (D-156 APPEND-ONLY preservado).
- 7.1-01-SUMMARY.md creado con 3 task commits documentados.
- ROADMAP.md Phase 7.1 actualizado: Plans 1/2 In Progress.
- 3 commits hechos: re-accent, refactor paramétrico, plan summary.
</success_criteria>

<output>
Create `.planning/phases/07.1-explicaciones-pedag-gicas-g-nero-n-mero-canonicalizaci-n-ortogr-fica/7.1-01-SUMMARY.md` documentando:
- Las 50 explanations re-acentuadas (referencia al commit + 3-5 BEFORE/AFTER samples).
- El bloque paramétrico instalado (referencia al commit + extracto del array CATEGORIES_WITH_EXPLANATIONS).
- Decisiones materializadas: D-135, D-137, D-139, D-140, D-144.
- Decisiones preserved (no-op verifications): D-150, D-151, D-155, D-156, D-157.
- Tests 181/181 verdes (delta 0).
- Italianismos preservados (sanity check con grep counts).
- Next step: Plan 7.1-02 añadirá la 2ª entry a CATEGORIES_WITH_EXPLANATIONS y subirá tests count a 184/184.
- Phase 7.1 Plans 1/2 In Progress.
</output>
