---
phase: 41-fare-indicativo-8-slots-el-bloque-grande
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/fare-indicativo.json
  - content/categories.json
  - tests/exercise-types.test.js
autonomous: false
requirements: [IND-01, IND-02, IND-03, IND-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # --- SC-5 / D-41-16 : el registro que hace existir la categoría ---
    - "`content/categories.json` contiene exactamente UNA entrada nueva con las 4 claves `id`, `name`, `order`, `origen`, valores `id: \"fare-indicativo\"`, `order: 15`, `origen: \"ia-quorum\"`, y las 14 entradas previas quedan byte-intactas (D-41-16, SC-5)."
    - "`categoriesForDisplay` (`src/screens/app.js:3238`) mapea `content.categories` 1:1 y deriva `totalCount` de `categoryIds`, así que con la entrada registrada y ≥1 slot que la referencia la fila aparece en home y `examenEnabled` es `true` — sin una línea de motor nueva: `git diff --stat src/screens/app.js src/domain/ src/data/` no produce salida (D-41-16, SC-5)."
    # --- edge: schema boundary (el orden de las dos ediciones) ---
    - "El fichero `content/exercises/fare-indicativo.json` nunca queda en disco sin su entrada en `categories.json`: `schema-validator.js:133-141` rechaza un `categoryIds` que referencia una categoría desconocida y `tests/domain.test.js:322-341` exige `result.errors` vacío sobre el bundle auto-descubierto, así que `node --test tests/*.test.js` termina con `fail 0` en cada commit de este plan (edge: schema boundary)."
    # --- D-41-01 / D-41-02 : el eje de variante ---
    - "Los 4 slots simples de este plan tienen EXACTAMENTE 6 variantes cada uno — ni 5 ni 7 — y las 6 corresponden a las 6 personas `io`, `tu`, `lui/lei`, `noi`, `voi`, `loro` sin repetir ninguna dentro del slot; el eje de variante es la persona y lo sirve `pickVariantIndex` (`src/domain/session.js:232`) sin código nuevo (D-41-01, D-41-02, SC-1, SC-5; edge: count boundary)."
    - "Cada una de las 24 variantes lleva su propio marco temporal, y los 6 marcos de un mismo slot son distintos entre sí — persona y marco covarían sobre un único eje de variante (D-41-02)."
    # --- D-41-07 : pronombre sujeto explícito ---
    - "Los 24 `prompt` contienen el hueco literal `___` y empiezan por, o contienen, uno de los 6 pronombres sujeto explícitos (`Io`/`io`, `Tu`/`tu`, `Lui`/`lui`, `Lei`/`lei`, `Noi`/`noi`, `Voi`/`voi`, `Loro`/`loro`), y el pronombre presente coincide con la persona de la forma correcta (D-41-07)."
    # --- D-41-05 : 0-gloss ---
    - "Ninguno de los 24 `prompt` contiene paréntesis `(` ni `)`, ni la subcadena `espa` en ninguna capitalización: cero gloss de traducción al castellano en el prompt, que es lo que en esta categoría regalaría el tiempo (D-41-05, R1)."
    - "El `notes` del fichero declara el 0-gloss como decisión razonada y NO como omisión, dando el motivo (7 de las 8 casillas mapean casi 1:1 al castellano, así que el gloss sobre el verbo filtra el tiempo en vez de desambiguar la persona), con el mismo esqueleto de 5 movimientos que `content/exercises/riflessivi.json:2` usa para su 0-match (D-41-05)."
    # --- D-41-06 : SCOPE-GATE léxico ---
    - "El objeto de `fare` en las 24 variantes sale SOLO del conjunto literal `i compiti`, `un errore`, `il lavoro`, `una torta`, `il letto`, `tutto`, `una foto`; un escaneo sobre `variants[].prompt` y `variants[].options[]` de los 4 slots devuelve 0 coincidencias para `colazione`, `spesa`, `freddo`, `farcela`, `farcel`, `far fare` y `fare + infinitivo` (D-41-06)."
    - "El `notes` declara el SCOPE-GATE de perífrasis etiquetado HARD, nombrando las construcciones excluidas y la frontera con lo que SÍ es in-scope (el objeto literal y transparente), mirror del SCOPE-GATE de recíprocos de `riflessivi.json:2` (D-41-06)."
    # --- D-41-08 : blacklist de formas atestiguadas ---
    - "Un escaneo sobre `variants[].options[]` de los 4 slots devuelve 0 coincidencias exactas para las 5 formas atestiguadas de la blacklist (`fo`, `fé`, `fenno`, `facea`, `fan`), ni como key ni como distractora; el `notes` las declara con audit trail — qué forma es, por qué está atestiguada y por qué no se usa — y la explanation de `fare-indicativo-presente` y la de `fare-indicativo-passato-remoto` las mencionan como formas de lectura, no de producción (D-41-08; edge: blacklist absence)."
    - "Ninguna opción de los 4 slots es una forma atestiguada de otro modo de `fare` — nada de `faccia`, `facciate`, `facciano`, `facessi`, `facesse`, `fa'`, `farei`, `farebbe` (casillas de las Phases 42 y 43) — ni una palabra italiana homógrafa (`facci`, `facce`, `fava`, `faro`): las distractoras malformadas son inexistentes en italiano (D-41-08, D-41-09)."
    # --- D-41-09 : patrón fijo de distractoras de los simples ---
    - "Los 4 slots simples usan el MISMO molde de 4 opciones: la key, 2 distractoras de raíz equivocada aplicada a la persona correcta del prompt, y 1 distractora que es una forma REAL de otra persona del mismo paradigma; el `futuro semplice` incluye `facerò` (o su equivalente de persona) entre las de raíz equivocada, exigencia literal de SC-1 (D-41-09, SC-1)."
    - "En cada variante hay EXACTAMENTE una opción gramatical para ese prompt: la distractora de forma real de otra persona queda excluida por el pronombre sujeto explícito, y las 2 de raíz equivocada no existen en italiano (D-41-07, D-41-09; edge: homograph/ambiguity boundary)."
    - statement: "Ninguna de las 24 variantes admite una segunda lectura del marco temporal que hiciera correcta una forma de otro tiempo del paradigma de `fare`."
      verification: backstop
    # --- D-41-11 : marcos disjuntos (mitad remota) ---
    - "El slot `fare-indicativo-passato-remoto` usa SOLO marcos narrativos remotos de las 3 familias declaradas (`Nel <año>`, `Quell'<estación>`, `<cuantificador> anni fa`) y un escaneo sobre sus 6 `prompt` devuelve 0 coincidencias para `stamattina`, `ieri`, `questa settimana`, `oggi`, `poco fa` y `questo mese`; ninguna de sus `options` es una forma de passato prossimo (`ho fatto`, `hai fatto`, `ha fatto`, `abbiamo fatto`, `avete fatto`, `hanno fatto`) (D-41-11; edge: disjoint-frame boundary)."
    # --- D-41-13 : MC-only ---
    - "Los 4 slots tienen `type: \"multiple-choice\"` y `options` de longitud 4; no existe ningún slot `match` ni `word-buttons` en el fichero, y el `notes` declara el 0-match y el 0-word-buttons como decisiones razonadas con su motivo (derivabilidad por raíz para el match; lo examinado es elegir forma o tiempo, no orden de palabras, para los word-buttons) (D-41-13)."
    # --- D-41-14 : ids semánticos y espacio -300 libre ---
    - "Los ids de los 4 slots son literalmente `fare-indicativo-presente`, `fare-indicativo-imperfetto`, `fare-indicativo-passato-remoto` y `fare-indicativo-futuro-semplice`, y ningún id del fichero termina en un sufijo numérico de 3 cifras — el espacio `fare-indicativo-300`+ queda libre para los cruces de Phase 44 (D-41-14; edge: id-space boundary)."
    # --- D-41-15 / SC-4 : el estado del hand-off ---
    - "Al cerrar este plan los 4 slots llevan `validation` con `status: \"pending\"` y `passes: []`: el executor NO fabrica pases ni estampa `validated`, porque el quórum base canónico Opus+Sonnet vive en una pasada TOP-LEVEL posterior (D-41-15, VAL-03)."
    - "`VAL_07_STRICT=1 node --test tests/*.test.js` falla EXACTAMENTE en el test de `content/exercises/fare-indicativo.json` reportando los slots `pending`, y ninguna otra categoría cambia de estado: ese rojo es el marcador visible del hand-off al pase top-level (D-41-15, SC-4)."
    # --- D-41-17 : canon editorial, ya automatizado ---
    - "La línea nueva de `CATEGORIES_WITH_EXPLANATIONS` (`tests/exercise-types.test.js:1273-1293`) con `expected: slotCountOf('content/exercises/fare-indicativo.json')` activa los sub-tests editoriales sobre el fichero nuevo, y los 5 pasan: explanation no vacía en los 4 slots, cero smart-quotes, cero markdown, cero leak R1 en los 24 prompts, cero cross-refs R2 en las explanations (D-41-17, SC-4)."
    - "Las 4 explanations están en español con acentos RAE, en plain text de un solo párrafo, con apóstrofes ASCII U+0027 al citar italiano, y cada una cierra nombrando la trampa de sus propias distractoras — patrón de `riflessivi.json:192` (D-41-17)."
    # --- SC-1 : el contenido pedido ---
    - "El autor es examinado sobre las 6 formas exactas de cada tiempo simple: `faccio/fai/fa/facciamo/fate/fanno` (IND-01), `facevo/facevi/faceva/facevamo/facevate/facevano` (IND-02), `feci/facesti/fece/facemmo/faceste/fecero` (IND-03) y `farò/farai/farà/faremo/farete/faranno` (IND-04), una forma por variante y sin repetir key dentro del slot (SC-1)."
    # --- valla del motor ---
    - "`git status --porcelain` no lista ningún fichero fuera de los 3 de `files_modified`; en particular `src/`, `scripts/` y `tests/fixtures/` quedan sin tocar (Out of Scope del milestone)."

  artifacts:
    - "`content/exercises/fare-indicativo.json` — fichero NUEVO con exactamente 2 claves top-level (`notes`, `exercises`), el `notes` con las 6 declaraciones (0-gloss, 0-match, 0-word-buttons, blacklist con audit trail, SCOPE-GATE de perífrasis, marcos disjuntos) más la nota de count-sync apuntando a Phase 44 / INT-02, y los 4 slots simples con 6 variantes cada uno."
    - "`content/categories.json` — 15 entradas; la nueva es `fare-indicativo`, order 15, `origen: \"ia-quorum\"`."
    - "`tests/exercise-types.test.js` — 1 línea nueva en `CATEGORIES_WITH_EXPLANATIONS` con `expected` dinámico vía `slotCountOf`."

  key_links:
    - "`content/categories.json` → `schema-validator.js:133-141` (`knownCategoryIds`): sin la entrada `fare-indicativo`, cada uno de los 4 slots emite `referencia a categoría desconocida` y `tests/domain.test.js:322-341` se pone rojo. Es la razón de que la entrada viva en la tarea tracer y no al final."
    - "`content/categories.json` → `categoriesForDisplay` (`src/screens/app.js:3248`): el array de `content.categories` se mapea 1:1, así que la entrada es lo ÚNICO que hace aparecer la fila en home, en el picker, en Repaso y en Examen. `order` es documental; el orden del array define el display."
    - "`variants[]` → `pickVariantIndex` (`src/domain/session.js:232`): la longitud 6 del array es lo que produce 'una persona distinta cada pasada'. Un slot con 5 variantes deja una casilla del paradigma sin examinar nunca."
    - "`slotCountOf` (`tests/exercise-types.test.js:1268-1271`) → el `expected` de la línea nueva: al ser dinámico, la línea es auto-consistente y no puede ponerse roja por conteo; es lo que permite añadirla ya sin invadir INT-02."
    - "`validation.passes[]` → `deriveStatus` (`src/data/validation-state.js:34-39`): `validated` exige ≥2 `correcta` con `by` distintos y cero `incorrecta`. Escribir `status: \"validated\"` con `passes: []` produce un estado incoherente que el gate de 41-02 detecta."

  prohibitions:
    - "MUST NOT dar de alta una variante en la que más de una opción sea una respuesta defendiblemente correcta para ese prompt: un ejercicio injusto le enseña al autor que su respuesta correcta es incorrecta, que es el daño exacto que esta herramienta existe para evitar."
    - "MUST NOT escribir `validation.status: \"validated\"` ni ninguna entrada de `passes[]` que no venga de un pase de modelo real: el audit trail es la única evidencia que el autor tiene de que una variante fue revisada, y un pase inventado la destruye sin dejar rastro."
    - "MUST NOT presentar una forma regional, dialectal o de registro literario como si fuera la norma del italiano estándar, ni en un sentido ni en el otro: el autor no puede distinguirlas y la interiorizaría como regla."
    - "MUST NOT introducir ninguna dependencia, `package.json`, paso de build ni proceso que haya que arrancar: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node (CLAUDE.md §Constraints)."
    - "MUST NOT tocar el motor v1.4 (`src/screens/app.js`, `src/domain/`, `src/data/`): si el contenido exigiera cambiar `session.js`, `applyResultToSession` o la cascada, el diseño del contenido está mal (Out of Scope literal del milestone)."
    - "MUST NOT autorar cruces multi-categoría ni usar el espacio de ids `fare-indicativo-300`+: son de Phase 44 / INT-03."
    - "MUST NOT sincronizar `CATEGORIES` de `scripts/run-validation-271.mjs` ni `REAL_CATEGORIES` de `tests/fixtures/slot-variants-integration.test.js`: son el entregable literal de Phase 44 / INT-02."
    - "MUST NOT arreglar de paso ninguna discrepancia de conteo preexistente de `genero-numero` ni de `preposiciones` (D-35-08 / D-40-12)."
    - "MUST NOT presentar `fatto` concordado (`fatta`, `fatti`, `fatte`) ni como key ni como distractora: la concordancia es el MAGNET de Phase 43 (INDEF-04) y adelantarla aquí colisionaría con él."
---

<objective>
Dar de alta la categoría `fare-indicativo` en el disco y en el registro, y autorar sus **4 tiempos
simples** — presente, imperfetto, passato remoto y futuro semplice — como 4 slots
`multiple-choice` de **6 variantes cada uno** (24 variantes = 24 casillas persona×tiempo),
naciendo directamente en slot+variantes y NUNCA en legacy payload.

La primera tarea es un **tracer**: un solo slot (`fare-indicativo-presente`) + su `notes` completo +
la entrada de `categories.json` + la línea del smoke paramétrico, verificado de punta a punta antes
de que exista una segunda variante. Si la forma está mal, está mal una vez y no ocho.

Cubre IND-01 (presente, trampa de la raíz doble `facc-`/`fa-`), IND-02 (imperfetto, raíz latina
`fac-`), IND-03 (passato remoto, alternancia `fec-`/`fac-`) e IND-04 (futuro semplice, raíz contracta
`far-`, nunca `facerò`).

Purpose: es la mitad del bloque más pesado del milestone y el que fija la forma que 41-02 clona. El
`notes` que se escribe aquí es el mecanismo anti-inercia de toda la fase: las 6 declaraciones
(0-gloss, 0-match, 0-word-buttons, blacklist de arcaísmos, SCOPE-GATE de perífrasis, marcos
disjuntos) tienen que existir ANTES de que se escriba la primera distractora, porque el fallo
esperable no es que el autor escriba `fo` — es que la autoría genere `fo` creyéndola obviamente mala.

Output: `content/exercises/fare-indicativo.json` con `notes` + 4 slots + 24 variantes en
`validation.status: "pending"`, `content/categories.json` con 15 entradas, y 1 línea nueva en
`CATEGORIES_WITH_EXPLANATIONS`. Ningún fichero de `src/` cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md
@.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-PATTERNS.md
</context>

## Frontera de este plan (y de la fase)

Lo que este plan NO hace, declarado para que la verificación no lo busque:

- **Cruces multi-categoría** (`fare-indicativo-300`+, hacia `avere` / `modali` /
  `presente-regolare`) → Phase 44, INT-03. El espacio de ids `-300`+ se deja libre; no se
  pre-crea, no se reserva con placeholder, simplemente no se usa.
- **Sync de counts**: los 2 arrays restantes (`CATEGORIES` de `scripts/run-validation-271.mjs:174-189`
  y `REAL_CATEGORIES` de `tests/fixtures/slot-variants-integration.test.js:167-186`) más la fórmula
  del baseline-guard → Phase 44, INT-02.
- **El motor v1.4.** `git diff src/screens/app.js src/domain/ src/data/` queda vacío al cierre. La
  categoría carga genéricamente; si algo exigiera una línea de motor, el diseño del contenido está mal.
- **Un eje de variante nuevo** (variar por tiempo dentro de un slot) — Out of Scope literal del
  milestone. El eje es uno solo: el índice de variante, que aquí significa la persona.
- **Perífrasis y modismos de `fare`** (`fare colazione`, `fare la spesa`, `fa freddo`, `farcela`,
  causativo) — Out of Scope del milestone; D-41-06 es el gate de autoría.
- **Las discrepancias de conteo VAL-06 preexistentes** de `genero-numero` y `preposiciones` — fuera de
  scope desde v1.9 (D-35-08 / D-40-12). No se arreglan de paso.
- **Los 4 tiempos compuestos** — son 41-02.
- **El quórum base canónico Opus+Sonnet** — no corre dentro del executor (D-41-15). Ver §Hand-off.

## Correcciones de plan-time (verificadas en el codebase el 2026-08-03)

Tres desviaciones respecto a la letra de 41-CONTEXT.md, cada una por un hecho comprobado. D-41-15
lockea el reparto simples/compuestos, no qué plan lleva el registro ni los tests.

**1. La entrada de `content/categories.json` se mueve de 41-02 a 41-01, dentro de la tarea tracer.**
D-41-15 la puso entre paréntesis en 41-02. Es incorrecto sobre los hechos:
`src/data/schema-validator.js:133-141` exige que todo valor de `categoryIds` referencie una categoría
CONOCIDA, y `tests/domain.test.js` auto-descubre `content/exercises/*.json` con `readdirSync`
(línea 225) y valida el bundle completo exigiendo `result.errors` vacío (líneas 322-341). En el
instante en que `content/exercises/fare-indicativo.json` existe en disco sin su entrada,
`domain.test.js` se pone rojo — independientemente de cualquier sync de conteo. La entrada no es
cosmética: es prerequisito de schema.

**2. La línea de `CATEGORIES_WITH_EXPLANATIONS` también se mueve al tracer.** Es 1 línea con
`expected` DINÁMICO (`slotCountOf`, `tests/exercise-types.test.js:1268-1271`), así que es
auto-consistente y no puede ponerse roja por conteo. Sin ella, el fichero nuevo no recibe NINGÚN
sub-test editorial y D-41-17 quedaría sin verificación automatizada durante los 24 primeros variantes.
Con ella, los 5 sub-tests (explanation no vacía, apóstrofes ASCII, plain text sin markdown, leak R1 en
prompts, cross-refs R2 en explanations) corren desde el primer commit. Los OTROS 2 arrays de conteo
siguen diferidos a Phase 44.

**3. El "rojo esperado" de los counts NO es el que dice 41-CONTEXT.md.** Verificado ejecutando
`node scripts/run-validation-271.mjs`: hoy el reporter da **VAL-06 PASS (225/225)**, VAL-08 PASS,
VAL-04 PASS, gate global PASS — no quedan discrepancias preexistentes de `genero-numero` ni de
`preposiciones`. Y como AMBOS lados del baseline-guard (`TOTAL_EXPECTED` en la línea 192 y
`Σ slotCountOf` en la 205) iteran el array `CATEGORIES`, omitir `fare-indicativo` de ese array no
hace `process.exit(1)` (línea 206) **y tampoco pone rojo VAL-06** (líneas 429-437): el reporter queda
simplemente **CIEGO** al fichero nuevo y sigue diciendo 225/225 PASS. Eso es peor que un rojo, porque
parece verde.

→ Consecuencia operativa: el marcador honesto del trabajo pendiente de esta fase **no** es el
reporter, es **`VAL_07_STRICT=1 node --test tests/*.test.js`**
(`tests/exercise-types.test.js:1432-1448`), que sí ve el fichero nuevo gracias a la corrección 2 y
falla mientras los slots estén `pending`. La verificación de la fase debe mirar ahí y NO perseguir un
rojo del reporter que no va a existir.

**Decisión sobre la discreción de count-sync (41-CONTEXT.md §Claude's Discretion):** NO se sincroniza
`fare-indicativo` en `scripts/run-validation-271.mjs` ni en
`tests/fixtures/slot-variants-integration.test.js` en Phase 41. Motivo: el ROADMAP §Phase 44 SC-2
compromete literalmente "+4 entradas" en los 3 arrays como entregable de INT-02; meter 1 de 4 ahora
crea un estado parcial que Phase 44 tendría que detectar antes de completarlo. El coste de no hacerlo
es la ceguera del reporter descrita arriba, que queda documentada en el `notes` del fichero y en la
§Hand-off.

**4. D-41-04 dice "las 6 explanations"; el schema tiene UNA explanation por slot.**
`schema-validator.js:211-214` la exige a nivel de slot y las variantes no llevan explanation propia
(comentario 190-193). Afecta a 41-02 (trapassato remoto), pero se anota aquí porque el mismo hecho
aplica a los 4 slots de este plan: cada slot tiene una sola explanation que cubre sus 6 variantes.

## Artifacts this phase produces

Símbolos e identificadores **creados** por esta fase; un pase de source-grounding debe excluirlos de
la verificación de drift porque no existen hoy en el codebase.

| Símbolo / identificador | Fichero | Tipo | Plan |
|---|---|---|---|
| `content/exercises/fare-indicativo.json` | (nuevo) | fichero de contenido, 2 claves top-level | 41-01 |
| `fare-indicativo` | `content/categories.json` | `id` de categoría (order 15, `origen: "ia-quorum"`) | 41-01 |
| `fare-indicativo-presente` | `content/exercises/fare-indicativo.json` | id de slot MC, 6 variantes | 41-01 |
| `fare-indicativo-imperfetto` | idem | id de slot MC, 6 variantes | 41-01 |
| `fare-indicativo-passato-remoto` | idem | id de slot MC, 6 variantes | 41-01 |
| `fare-indicativo-futuro-semplice` | idem | id de slot MC, 6 variantes | 41-01 |
| `fare-indicativo-passato-prossimo` | idem | id de slot MC, 6 variantes | 41-02 |
| `fare-indicativo-trapassato-prossimo` | idem | id de slot MC, 6 variantes | 41-02 |
| `fare-indicativo-trapassato-remoto` | idem | id de slot MC, 6 variantes | 41-02 |
| `fare-indicativo-futuro-anteriore` | idem | id de slot MC, 6 variantes | 41-02 |
| entrada `fare-indicativo` en `CATEGORIES_WITH_EXPLANATIONS` | `tests/exercise-types.test.js` | elemento de array de test | 41-01 |
| `tests/content-fare-indicativo.test.js` | (nuevo) | fichero de test de invariantes de la categoría | 41-02 |

Espacio de ids reservado y **no usado** por esta fase: `fare-indicativo-300` y siguientes (Phase 44,
INT-03).

## Multi-source coverage audit

| Fuente | Item | Plan | Estado |
|--------|------|------|--------|
| GOAL | ROADMAP §Phase 41 Goal — indicativo completo como UNA categoría en slot+variantes, persona como eje | 41-01 (simples) + 41-02 (compuestos) | COVERED |
| GOAL | SC-1 — 4 tiempos simples, persona distinta cada pasada, distractoras del error real | 41-01 T1, T3, T4 | COVERED |
| GOAL | SC-2 — 3 compuestos con `avere`, tiempo del auxiliar por el marco, sin doble lectura | 41-02 T1 | COVERED |
| GOAL | SC-3 — trapassato remoto siempre en su marco, explanation que dice que fuera no se usa | 41-02 T2 | COVERED |
| GOAL | SC-4 — todas `validated` por quórum + canon editorial + sin leak R1 | 41-01 T1 (sub-tests activados) + 41-02 T3 + pase top-level | COVERED |
| GOAL | SC-5 — carga en boot y aparece en home/picker/Repaso/Examen sin línea de motor | 41-01 T1, T2 | COVERED |
| REQ | IND-01 presente (`facc-`/`fa-`) | 41-01 T1 | COVERED |
| REQ | IND-02 imperfetto (`fac-`) | 41-01 T3 | COVERED |
| REQ | IND-03 passato remoto (`fec-`/`fac-`) | 41-01 T4 | COVERED |
| REQ | IND-04 futuro semplice (`far-`) | 41-01 T3 | COVERED |
| REQ | IND-05 los 3 compuestos con `avere` | 41-02 T1 | COVERED |
| REQ | IND-06 trapassato remoto en su marco | 41-02 T2 | COVERED |
| RESEARCH | — v2.0 sin RESEARCH.md (`--skip-research` deliberado: el diseño FARE-X1 estaba cerrado). Los análogos los aportan 41-CONTEXT.md §canonical_refs y 41-PATTERNS.md | n/a | N/A por diseño |
| CONTEXT | D-41-01 48 variantes = 8×6, eje persona (costly) | 41-01 T1/T3/T4 + 41-02 T1/T2 | COVERED |
| CONTEXT | D-41-02 marco temporal propio en las 8 casillas | 41-01 T1/T3/T4 + 41-02 T1/T2 | COVERED |
| CONTEXT | D-41-03 trapassato remoto: subordinada + 3 conectores 2+2+2 + check de `quando` | 41-02 T2 | COVERED |
| CONTEXT | D-41-04 la explanation dice que fuera del marco no se usa | 41-02 T2 | COVERED |
| CONTEXT | D-41-05 0-gloss declarado (reversible) | 41-01 T1 (`notes` + prompts) + 41-02 T3 (gate) | COVERED |
| CONTEXT | D-41-06 SCOPE-GATE HARD de perífrasis | 41-01 T1 (`notes`) + todas las tareas de contenido + 41-02 T3 | COVERED |
| CONTEXT | D-41-07 pronombre sujeto explícito en las 48 | 41-01 T1/T3/T4 + 41-02 T1/T2 + 41-02 T3 | COVERED |
| CONTEXT | D-41-08 blacklist con audit trail + mención en 2 explanations | 41-01 T1 (`notes` + presente), T4 (passato remoto) + 41-02 T3 | COVERED |
| CONTEXT | D-41-09 patrón fijo de distractoras de los simples | 41-01 T1/T3/T4 | COVERED |
| CONTEXT | D-41-10 patrón fijo de distractoras de los compuestos | 41-02 T1/T2 | COVERED |
| CONTEXT | D-41-11 marcos disjuntos pr↔pp, sin contraste (reversible) | 41-01 T4 + 41-02 T1 + 41-02 T3 | COVERED |
| CONTEXT | D-41-12 ronda EXTRA DeepSeek en los 12 variantes de pr + tr | §Hand-off de 41-01 y 41-02 + 41-02 T3 (coherencia de `passes`) | COVERED |
| CONTEXT | D-41-13 MC-only con 0-match Y 0-wb declarados | 41-01 T1 (`notes` + `type`) | COVERED |
| CONTEXT | D-41-14 los 8 ids verbatim, `-300`+ libre (costly) | 41-01 T1/T3/T4 + 41-02 T1/T2 + 41-02 T3 | COVERED |
| CONTEXT | D-41-15 2 plans + validación top-level, VAL-03 inviolable | la estructura de los 2 plans + §Hand-off | COVERED |
| CONTEXT | D-41-16 entrada en `categories.json` (order 15, `origen`) | 41-01 T1 (movida aquí, ver §Correcciones) | COVERED |
| CONTEXT | D-41-17 canon editorial heredado | 41-01 T1 (línea del smoke) + todas las tareas de contenido | COVERED |

**0 items MISSING.** No se requiere split de fase.

## Hand-off al pase TOP-LEVEL de quórum (D-41-15)

Explícito porque es el punto donde algo puede parecer hecho sin estarlo.

| Momento | `validation.status` | `validation.passes[]` | Cómo se comprueba |
|---|---|---|---|
| Al cerrar 41-01 | `"pending"` en los 4 slots simples | `[]` | `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los slots `pending` |
| Al cerrar 41-02 | `"pending"` en los 8 slots | `[]` | idem, con 8 slots |
| Tras el pase top-level | `"validated"` en los 8 | ≥2 pases `correcta` con `by` distintos; ≥4 pases con al menos uno `deepseek-*` en `fare-indicativo-passato-remoto` y `fare-indicativo-trapassato-remoto` | `VAL_07_STRICT=1 node --test tests/*.test.js` en verde |

El quórum base canónico (Opus + Sonnet vía el skill `gsd-validate-exercise`, que spawnea Task
subagents) **no está disponible dentro de un subagent `gsd-executor`**, así que ninguna tarea de estos
plans lo ejecuta. Se corre en top-level tras `execute-phase`, **un ejercicio por contexto, NUNCA
batched (VAL-03)**: 8 invocaciones, una por id de slot. La ronda EXTRA DeepSeek de D-41-12 entra en
esa misma pasada sobre `fare-indicativo-passato-remoto` y `fare-indicativo-trapassato-remoto`
(`scripts/validate-ai-pass.mjs`, claves en `.env`). Además, en esa pasada el pase Opus de cada slot
debe registrar en sus `concerns` la re-declaración local del 0-gloss, mirror literal de
`content/exercises/riflessivi.json:245`.

## Assumptions (probe de edges sin SPEC — 6 filas `unclassified` surfaceadas, 0 descartes silenciosos)

No existe SPEC.md para esta fase, así que el probe determinista de edges corrió sobre IND-01..IND-06 y
devolvió las 6 filas como `unclassified`: su clasificador funciona por palabras clave en inglés y el
texto de los requisitos está en español. Por protocolo una fila `unclassified` queda `unresolved` y
**nunca** se auto-`backstop`ea ni se descarta — se surfacea aquí como supuesto explícito.

| Requisito | Qué NO derivó el probe | Supuesto que asume este plan |
|---|---|---|
| IND-01 | ninguna categoría de edge (boundary / adjacency / encoding / ordering / empty / precision) | los edges reales del presente son la frontera de homografía (una sola opción gramatical por prompt) y la ausencia de la blacklist en `options`; autorados como truths |
| IND-02 | idem | el edge real del imperfetto es que ninguna distractora de raíz sea una forma atestiguada de otro modo; autorado como truth |
| IND-03 | idem | los edges reales del passato remoto son la frontera de marcos disjuntos (D-41-11, en las dos direcciones) y el reparto `fec-`/`fac-` por persona; autorados como truths |
| IND-04 | idem | el edge real del futuro es que `facerò` sea distractora y NUNCA key; autorado como truth |
| IND-05 | idem | tratado en 41-02 (frontera de tiempo del auxiliar y de participio invariable) |
| IND-06 | idem | tratado en 41-02 (frontera sintáctica del marco y unicidad de lectura con `quando`) |

Igualdad de no-descarte-silencioso: **6 filas surfaceadas por el probe == 6 supuestos declarados
aquí**, más los edges derivados a mano y escritos como `must_haves.truths` (count boundary, id-space
boundary, schema boundary, homograph boundary, blacklist absence, disjoint-frame boundary) y **1
`backstop`** (la unicidad de lectura del marco, marcador estructurado con `verification: backstop`,
porque es un juicio lingüístico que ninguna aserción mecánica puede cerrar; si el verificador no puede
confirmarlo con evidencia explícita debe abstenerse a `human_needed`, nunca dar pase silencioso).

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — `fare-indicativo-presente` de punta a punta: fichero + `notes` + entrada de `categories.json` + línea del smoke</name>

  <files>content/exercises/fare-indicativo.json, content/categories.json, tests/exercise-types.test.js</files>

  <read_first>
    - `content/exercises/riflessivi.json` líneas 1-68 — el molde exacto: las 2 claves top-level, el
      esqueleto de 5 movimientos del `notes` (línea 2), el orden literal de campos de un slot MC
      (`id`, `type`, `categoryIds`, `explanation`, `variants`, `validation`), los 3 campos de una
      variante (`prompt`, `options`, `correctIndex`, nada más) y el shape de `validation.passes[]`.
    - `content/exercises/riflessivi.json` líneas 186-268 — el slot `riflessivi-pp-concordanza`: el
      precedente de slot SIN gloss, el molde de 4 opciones repetido variante a variante, la
      explanation que cierra nombrando sus dos trampas (línea 192), y el shape de 4 pases de la ronda
      extra (líneas 237-266).
    - `content/categories.json` líneas 1-18 — las 14 entradas actuales, las 4 claves exactas de una
      entrada y la alineación por columnas hecha a mano.
    - `src/data/schema-validator.js` líneas 133-146 (`categoryIds` con referencias conocidas),
      148-174 (`payload` XOR `variants`), 210-231 (`explanation` obligatoria a nivel de slot,
      `variants` no vacío) y 437-456 (`validateMultipleChoiceSurface`: `prompt` con `___`, `options`
      de 3 o 4 strings no vacíos, `correctIndex` entero en rango).
    - `tests/exercise-types.test.js` líneas 1265-1293 (`slotCountOf` + `CATEGORIES_WITH_EXPLANATIONS`,
      la entrada de `riflessivi` en la 1291 como análogo literal) y 1336-1403 (los sub-tests de
      apóstrofes, markdown, leak R1 y cross-refs R2 que la línea nueva activa; ojo al regex de leak de
      la 1373).
    - `tests/domain.test.js` líneas 220-230 (auto-descubrimiento con `readdirSync`) y 322-341 (el test
      de bundle completo que exige `result.errors` vacío).
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-PATTERNS.md` — extractos
      verbatim del molde, del shape de `validation` y de la entrada de `categories.json`.
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md` §decisions —
      D-41-01, D-41-02, D-41-05, D-41-06, D-41-07, D-41-08, D-41-09, D-41-13, D-41-14, D-41-16, D-41-17.
  </read_first>

  <precondition>`node --test tests/*.test.js` reporta 697 pass / 0 fail en el árbol limpio ANTES de cualquier edición — baseline medido el 2026-08-03. Re-medirlo, no confiar en el número: si no da `fail 0`, parar y reportar en vez de seguir, porque el resto del plan mide "sin fails nuevos" contra él.</precondition>

  <reversibility rating="costly">Escribe los 8 ids semánticos de D-41-14 y la entrada `fare-indicativo` de `categories.json`. Renombrar un id después de que el autor haya fallado ese slot deja estado huérfano en su `exerciseStats` real, y el slug ya es elemento de `RESET_PREFIXES_V13` (Phase 40 shippeada), nombre del fichero y prefijo de los ids de cruce de Phase 44. No se inserta checkpoint: D-41-14 lo excluye explícitamente porque las 8 casillas del paradigma no tienen grados de libertad que aprobar.</reversibility>

  <action>
    Tres ediciones que van juntas porque separarlas deja la suite en rojo entre commits: el fichero de
    contenido no valida sin la entrada de `categories.json`, y sin la línea del smoke el fichero no
    recibe ningún sub-test editorial.

    **A. `content/categories.json` — append de 1 entrada (D-41-16)**

    Añadir coma al final de la línea 16 (`riflessivi`, order 14) y una línea 17 con las 4 claves
    exactas y en este orden: `id` = `fare-indicativo`, `name` = `Fare — indicativo (faccio/feci/ho fatto)`,
    `order` = 15, `origen` = `ia-quorum`. Respetar la alineación por columnas de las 14 filas
    existentes (no se pasa formateador sobre este fichero). `origen` solo acepta `ia-quorum` o
    `apuntes-profesora` (enum PROV-01, `tests/schema-validator-origen.test.js:25-52`): un typo da
    `ok:false`. `order` es documental; el orden del array define el display.

    **B. `content/exercises/fare-indicativo.json` — fichero nuevo con `notes` + 1 slot**

    Exactamente 2 claves top-level, `notes` y `exercises`. Sin `version`, sin `category`.

    `notes` es UN único string en prosa, plain text, sin markdown, sin saltos de línea, en español con
    acentos RAE y apóstrofes ASCII U+0027. **Sin corchetes angulares en ningún punto del fichero**
    (usar "frente a" o " a " donde `riflessivi.json:2` usa una flecha ASCII): eso deja los escaneos de
    seguridad de la tarea limpios y sin excepciones. Instancia el esqueleto de 5 movimientos de
    `riflessivi.json:2` (identidad y linaje · la omisión en MAYÚSCULAS con su decision-id y su
    razonamiento · el cierre anti-inercia "es decisión, no omisión" · el gate etiquetado HARD con su
    frontera · la nota de count-sync) SEIS veces, sobre:

    1. **Identidad y linaje.** Categoría `fare-indicativo` (order 15) nacida DIRECTAMENTE en
       slot+variantes, clon del patrón v1.7/v1.9 (`presente-regolare`, `modali`, `possessivi`,
       `riflessivi`), NUNCA legacy payload. 8 slots = las 8 casillas del indicativo de `fare`, 6
       variantes cada uno = las 6 personas; el eje de variante es la persona y lo sirve
       `pickVariantIndex` sin código nuevo.
    2. **0-GLOSS (D-41-05).** Cero gloss de traducción al castellano entre paréntesis en los prompts,
       el desambiguador R7 que el resto de las categorías sí usa. Razón: el castellano mapea casi 1:1
       en 7 de las 8 casillas (`hacía`, `hice`, `haré`, `he hecho`, `había hecho`, `habré hecho`), así
       que un gloss sobre el verbo REGALA el tiempo — leak R1 directo en una categoría cuyo ejercicio
       es justamente elegir el tiempo. El único desambiguador es el marco temporal del prompt. Esto NO
       contradice el canon R7 del proyecto: el gloss es legítimo DONDE desambigua; aquí no desambigua,
       filtra. Cerrar con: el 0-gloss aquí es decisión, no omisión.
    3. **0-MATCH (D-41-13 / DESIGN RULE D-04).** Ningún slot `match`: el pareo persona a forma es una
       asociación MECÁNICA derivable en cuanto se conoce la raíz, es decir "arrastrar sin pensar", lo
       que D-04/R3 prohíben. Cerrar con: es decisión, no omisión.
    4. **0-WORD-BUTTONS (D-41-13).** Ningún slot `word-buttons`: lo que se examina siempre es elegir
       una forma o un tiempo, no construir orden de palabras. Nombrar los dos candidatos considerados y
       rechazados — construir el marco del trapassato remoto (banco demasiado largo, y el MC ya obliga
       a leer el marco) y la colocación adverbial del compuesto (es sintaxis del compuesto, no una
       casilla del paradigma). Cerrar con: es decisión, no omisión.
    5. **BLACKLIST DE FORMAS ATESTIGUADAS (D-41-08), con audit trail.** Las cinco formas, cada una con
       qué es, por qué está atestiguada y por qué no se usa: `fo` (toscano-arcaico por `faccio`), `fé`
       (poético por `fece`), `fenno` (poético por `fecero`), `facea` (poético por `faceva`) y `fan`
       (truncamiento poético de `fanno`). Ninguna aparece como key ni como distractora en las 48
       variantes. El riesgo real no es que el autor las escriba: es que la autoría las genere como
       distractora "obviamente mala" siendo formas válidas, lo que produciría un ejercicio injusto.
       Extender la misma lógica a las formas de otros modos que sí existen (congiuntivo `faccia` y
       familia, imperativo con apóstrofe, condizionale `farei` y familia): son casillas de las Phases
       42 y 43 y tampoco se usan como distractora aquí.
    6. **SCOPE-GATE (HARD, D-41-06) de perífrasis y modismos.** El objeto de `fare` es SIEMPRE literal
       y transparente, del conjunto cerrado `i compiti`, `un errore`, `il lavoro`, `una torta`, `il
       letto`, `tutto`, `una foto`. Quedan FUERA las perífrasis y modismos (desayunar, la compra, el
       tiempo meteorológico, `farcela`, el causativo con infinitivo): son vocabulario y sintaxis, no
       paradigma, y tienen categoría propia si el autor los echa en falta. NINGUNA de las 48 variantes
       los cruza. Declarar la frontera: lo que SÍ es in-scope es el objeto literal, y el efecto
       colateral buscado es que sin perífrasis no queda léxico que glosar, lo que refuerza el 0-gloss.
    7. **MARCOS DISJUNTOS (D-41-11).** El slot de passato remoto usa SOLO marcos narrativos remotos; el
       de passato prossimo SOLO marcos recientes o conectados con el presente. Nunca se ofrecen
       mutuamente como distractora, ni en un sentido ni en el otro. Razón: en el italiano real del
       norte el passato prossimo cubre lo que el sur expresa con passato remoto, así que una distractora
       cruzada sería válida para media Italia. El contraste de uso es registro y variación regional, no
       una casilla del paradigma: fuera de esta fase por diseño, no por olvido.
    8. **Nota de count-sync.** La sincronización de `CATEGORIES` de `scripts/run-validation-271.mjs` y
       de `REAL_CATEGORIES` de `tests/fixtures/slot-variants-integration.test.js`, más la fórmula del
       baseline-guard, son de Phase 44 / INT-02. Consecuencia verificada el 2026-08-03: mientras
       `fare-indicativo` falte de esos arrays el reporter no se pone rojo, queda CIEGO al fichero y
       sigue reportando su total anterior en PASS. El marcador honesto del trabajo pendiente es
       `VAL_07_STRICT=1 node --test tests/*.test.js`, que sí ve el fichero por la entrada del smoke
       paramétrico. El campo `origen` se estampa ya (PROV-01, existe en schema desde v1.9).

    Después, `exercises` con UN solo elemento:

    - `id`: `fare-indicativo-presente`. `type`: `multiple-choice`. `categoryIds`: `["fare-indicativo"]`.
    - `explanation`: un párrafo largo en español acentuado, tono D-127 (3ª persona impersonal, regla,
      paradigma completo, paralelo italiano-castellano, y la trampa del hispanohablante al final). Debe
      citar las 6 formas `faccio`, `fai`, `fa`, `facciamo`, `fate`, `fanno`; explicar la raíz doble
      (`facc-` en `io`, `noi` y `loro`; `fa-` en `tu`, `lui/lei` y `voi`); mencionar `fo` y las otras
      cuatro formas de la blacklist como formas que el autor puede ENCONTRAR leyendo pero no debe
      PRODUCIR (D-41-08); y cerrar nombrando las dos trampas de sus distractoras (la raíz regularizada
      y la forma real de otra persona). Cero corchetes angulares, cero markdown, cero smart-quotes,
      apóstrofes ASCII, y ninguna referencia a otro ejercicio por id (el sub-test R2 lo escanea).
    - `variants`: 6 objetos con SOLO `prompt`, `options`, `correctIndex`. Una persona por variante en
      el orden `io`, `tu`, `lui/lei`, `noi`, `voi`, `loro`. Cada `prompt`: pronombre sujeto explícito
      (D-41-07) + el hueco literal `___` + un objeto literal del conjunto cerrado + un marco temporal
      de presente habitual o actual, distinto en cada una de las 6 (por ejemplo del repertorio `ogni
      giorno`, `di solito`, `adesso`, `sempre`, `ogni mattina`, `in questo momento`). Sin paréntesis y
      sin una sola palabra en castellano dentro del prompt.
    - `options`: 4 strings. La key es la forma correcta de la persona del prompt. Las otras 3 siguen el
      molde fijo de D-41-09: DOS de raíz equivocada aplicada a la persona correcta, construidas
      regularizando desde la raíz latina que el autor ve en el imperfetto y en el gerundio (por
      ejemplo `faco` y `facio` para `io`, `faci` para `tu`, `face` para `lui/lei`, `faciamo` con una
      sola c para `noi`, `facete` para `voi`, `facono` para `loro`); y UNA que es una forma REAL de otra
      persona del mismo paradigma, segura porque el pronombre explícito la excluye. Regla dura: una
      distractora malformada tiene que ser INEXISTENTE en italiano — antes de fijarla, comprobar que no
      es forma de otro modo de `fare` ni palabra italiana homógrafa; en concreto están prohibidas las 5
      de la blacklist, las del congiuntivo y del imperativo, y las que colisionan con palabras reales.
    - `correctIndex`: entero en `[0,4)`, variado entre las 6 variantes (no las 6 en la misma posición).
    - `validation`: `{ "status": "pending", "passes": [] }`. No se fabrica ningún pase: el quórum base
      es la pasada top-level de D-41-15.

    **C. `tests/exercise-types.test.js` — 1 línea en `CATEGORIES_WITH_EXPLANATIONS`**

    Tras la entrada de `riflessivi` (línea 1291), un comentario de una línea que nombre la fase y los
    requisitos (v2.0 Phase 41, IND-01..IND-06, 8 slots por 6 personas) y la entrada con `file:
    'content/exercises/fare-indicativo.json'` y `expected: slotCountOf('content/exercises/fare-indicativo.json')`
    — dinámico, NUNCA número mágico (D-31-06). No tocar ningún otro array de conteo.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js && node -e "const fs=require('fs');const cats=JSON.parse(fs.readFileSync('content/categories.json','utf8')).categories;const e=cats.find(c=>c.id==='fare-indicativo');if(!e)throw new Error('falta la entrada de categories.json');if(e.order!==15||e.origen!=='ia-quorum')throw new Error('entrada mal formada: '+JSON.stringify(e));if(Object.keys(e).join(',')!=='id,name,order,origen')throw new Error('claves inesperadas: '+Object.keys(e));if(cats.length!==15)throw new Error('esperaba 15 categorias, hay '+cats.length);const d=JSON.parse(fs.readFileSync('content/exercises/fare-indicativo.json','utf8'));if(Object.keys(d).join(',')!=='notes,exercises')throw new Error('claves top-level inesperadas: '+Object.keys(d));const n=d.exercises.filter(x=>(x.categoryIds||[]).includes('fare-indicativo')).length;if(n===0)throw new Error('totalCount 0 -> examenEnabled false -> la fila no es jugable');console.log('SC-5 OK: fila order '+e.order+', totalCount '+n)"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con `pass` de al menos 703 (baseline re-medido + los 6 sub-tests que activa la línea nueva).
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; if(c.length!==15) process.exit(1); const e=c[14]; process.exit(e.id==='fare-indicativo'&&e.order===15&&e.origen==='ia-quorum'?0:1)"` sale con código 0 — la entrada es la 15ª y última.
    - `git diff content/categories.json | grep -c '^-' ` devuelve `1` — la única línea eliminada es la 16 original (reescrita con coma); ninguna de las 14 entradas se altera de otro modo.
    - `grep -c "fare-indicativo" tests/exercise-types.test.js` es `>= 2` (el `file` y el argumento de `slotCountOf`), y `grep -c "slotCountOf('content/exercises/fare-indicativo.json')" tests/exercise-types.test.js` devuelve `1`.
    - `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/fare-indicativo.json','utf8')); process.exit(d.exercises.length===1&&d.exercises[0].id==='fare-indicativo-presente'&&d.exercises[0].type==='multiple-choice'&&d.exercises[0].variants.length===6?0:1)"` sale con código 0 — 1 slot, id exacto, MC, 6 variantes.
    - Escaneo `node -e` sobre `exercises[0].variants[].prompt`: los 6 contienen `___`; los 6 contienen uno de `Io|Tu|Lui|Lei|Noi|Voi|Loro` en cualquier capitalización; **0** contienen `(`; **0** contienen `)`; **0** contienen `espa` en cualquier capitalización (D-41-05, D-41-07).
    - Escaneo `node -e` sobre `exercises[0].variants[].options[]`: las 6 tienen longitud 4; **0** coincidencias exactas con el conjunto `["fo","fé","fenno","facea","fan"]`; **0** coincidencias exactas con `["faccia","facciate","facciano","facessi","facesse","farei","farebbe","facci","facce","fava","faro"]`; **0** contienen `fatt` (D-41-08, prohibición de concordancia de Phase 43).
    - Escaneo `node -e` sobre `variants[].prompt` y `variants[].options[]`: **0** coincidencias con `colazione`, `spesa`, `freddo`, `farcel` (D-41-06).
    - Escaneo `node -e` sobre TODOS los strings del fichero (`notes`, `explanation`, `prompt`, `options`): **0** apariciones de los caracteres `<` y `>`, **0** de `&#`, **0** de `javascript:`, y **0** de las comillas tipográficas de apertura y cierre (T-41-01).
    - Escaneo `node -e` recursivo sobre el objeto parseado: ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`, y `({}).polluted === undefined` tras el parse (T-41-02).
    - `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/fare-indicativo.json','utf8')); const v=d.exercises[0].validation; process.exit(v.status==='pending'&&Array.isArray(v.passes)&&v.passes.length===0?0:1)"` sale con código 0 (D-41-15).
    - `node -e` sobre `notes`: contiene las 5 formas de la blacklist como subcadenas, y contiene los 6 marcadores de decisión `D-41-05`, `D-41-06`, `D-41-08`, `D-41-11`, `D-41-13`, `D-41-16` o su enunciado equivalente en prosa; longitud del string `> 1500` caracteres (las 8 declaraciones no caben en menos).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla, y su salida nombra `content/exercises/fare-indicativo.json` con `fare-indicativo-presente(pending)` — el marcador del hand-off existe y es legible.
    - `git status --porcelain src/ scripts/ tests/fixtures/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    `content/exercises/fare-indicativo.json` existe con su `notes` completo y un solo slot
    `fare-indicativo-presente` de 6 variantes en `pending`; `content/categories.json` tiene 15 entradas;
    la línea del smoke paramétrico está puesta y sus sub-tests editoriales pasan sobre el fichero nuevo;
    `node --test tests/*.test.js` sigue en `fail 0`; y la derivación de `categoriesForDisplay`
    (entrada presente + `totalCount > 0`) está comprobada por dato sin que `src/` haya cambiado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gate del tracer — la categoría se ve y se juega en la app antes de autorar 18 variantes más (SC-5)</name>
  <what-built>
    `content/exercises/fare-indicativo.json` con el slot `fare-indicativo-presente` (6 variantes, una
    por persona), la entrada `fare-indicativo` en `content/categories.json` (order 15,
    `origen: "ia-quorum"`) y la línea nueva del smoke paramétrico editorial. La suite completa está en
    `fail 0` y `src/` no se ha tocado: la categoría carga y se muestra por el camino genérico
    (`categoriesForDisplay` itera `content.categories`; `pickVariantIndex` rota la persona).

    Este checkpoint es el gate del tracer: si la forma del contenido está mal, se corrige aquí una
    vez, no ocho veces.
  </what-built>
  <how-to-verify>
    1. Arrancar el servidor local desde la raíz del repo: `npx serve` (o Live Server) y abrir la URL
       que imprime, normalmente `http://localhost:3000`.
    2. **Home:** en la tabla de categorías debe aparecer una fila nueva al final, con el nombre
       `Fare — indicativo (faccio/feci/ho fatto)`, estado `no-hecha`, racha `0 / 21 d`, y el botón
       `Examen` de esa fila HABILITADO (no gris, sin tooltip de "No hay ejercicios en esta categoría").
       Deben seguir apareciendo las 14 filas anteriores, sin cambios.
    3. **Examen de la categoría:** pulsar `Examen` en esa fila. Debe presentar el ejercicio de
       `fare` como multiple-choice, con 4 opciones, con pronombre sujeto visible en el enunciado y
       **sin ninguna traducción al castellano entre paréntesis**.
    4. **Rotación de persona (el eje de variante):** salir y volver a entrar en el Examen de la
       categoría 3 o 4 veces. El pronombre sujeto del enunciado debe cambiar entre pasadas
       (`io` / `tu` / `lui` / `noi` / `voi` / `loro`) — eso es `pickVariantIndex` funcionando sin
       código nuevo.
    5. **Repaso:** volver a home, entrar en `Repaso` y comprobar que la categoría `fare-indicativo`
       aparece como opción seleccionable en el picker.
    6. **Explicación:** dentro del ejercicio, contestar y pulsar `¿Por qué?` (o la tecla `e`). La
       explicación debe leerse en español con acentos, en un solo párrafo, sin markdown ni asteriscos,
       citando las 6 formas del presente y mencionando `fo` como forma que se lee pero no se produce.
    7. Confirmar de paso que la respuesta correcta es una sola: ninguna de las otras 3 opciones debe
       ser una forma italiana válida para ese sujeto.
  </how-to-verify>
  <resume-signal>Escribe `aprobado` para desbloquear la autoría de los 3 slots simples restantes, o describe qué falla (nombre de la fila, opción sospechosa, prompt concreto) para corregir la forma ANTES de replicarla 18 veces.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Expansión — `fare-indicativo-imperfetto` y `fare-indicativo-futuro-semplice` (12 variantes)</name>

  <files>content/exercises/fare-indicativo.json</files>

  <read_first>
    - `content/exercises/fare-indicativo.json` tal como quedó tras Task 1 — el `notes` y el slot
      `fare-indicativo-presente` son el molde literal que estos dos slots replican (mismo orden de
      campos, mismos 3 campos por variante, mismo `validation` en `pending`).
    - `content/exercises/riflessivi.json` línea 192 — el mejor ejemplar de explanation del repo: cita
      el paradigma completo con un ejemplo por forma y cierra nombrando las dos trampas.
    - `src/data/schema-validator.js` líneas 437-456 — la superficie MC que cada variante debe cumplir.
    - `tests/exercise-types.test.js` línea 1373 — el regex de leak R1 que escanea TODOS los prompts:
      prohíbe referencias a secciones, paréntesis que abran con palabras de metalenguaje, y guion largo
      seguido de palabras como regla, concordancia o excepción. Los prompts de estos slots no llevan
      paréntesis ni guion largo, así que basta no introducirlos.
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md` §decisions —
      D-41-02, D-41-07, D-41-09, D-41-14, D-41-17.
    - `.planning/REQUIREMENTS.md` líneas 22 y 24 — el texto literal de IND-02 e IND-04.
  </read_first>

  <action>
    Añadir DOS slots al array `exercises`, después de `fare-indicativo-presente`, con el molde exacto
    del tracer. `validation` de los dos: `{ "status": "pending", "passes": [] }`.

    **Slot `fare-indicativo-imperfetto` (IND-02)**

    - Las 6 keys, una por variante y en orden de persona: `facevo`, `facevi`, `faceva`, `facevamo`,
      `facevate`, `facevano`.
    - Familia de marco: hábito o trasfondo en el pasado, un marco distinto en cada una de las 6, del
      tipo `da bambino`, `ogni estate`, `a quei tempi`, `quando abitavamo a Roma`, `l'anno scorso`,
      `da piccoli`. El marco tiene que forzar el imperfetto por sí solo (D-41-02): hábito o duración,
      nunca un evento único y acotado, que abriría la lectura de otro tiempo.
    - Distractoras (D-41-09), molde fijo de 4 opciones: DOS de raíz equivocada aplicada a la persona
      correcta, de dos familias distintas — (a) la raíz contracta del infinitivo y del futuro con las
      desinencias del imperfetto (`farevo`, `farevi`, `fareva`, `farevamo`, `farevate`, `farevano`) y
      (b) `fac-` con las desinencias de imperfetto de la primera conjugación, el calco de tratar `fare`
      como verbo en `-are` (`facavo`, `facavi`, `facava`, `facavamo`, `facavate`, `facavano`) — más UNA
      que es forma REAL de otra persona del mismo paradigma del imperfetto.
    - Explanation: la raíz latina `fac-` que el infinitivo esconde y que reaparece completa en el
      imperfetto; las 6 formas citadas; el paralelo con el castellano `hacía`, que también conserva la
      `c`; y el cierre nombrando las dos trampas (la raíz contracta importada del futuro y el patrón
      de primera conjugación).

    **Slot `fare-indicativo-futuro-semplice` (IND-04)**

    - Las 6 keys: `farò`, `farai`, `farà`, `faremo`, `farete`, `faranno`. Cuidado con las tildes
      italianas: `farò` y `farà` llevan acento grave, `farai`/`faremo`/`farete`/`faranno` no.
    - Familia de marco: futuro, uno distinto por variante, del tipo `domani`, `stasera`,
      `la settimana prossima`, `il mese prossimo`, `l'anno prossimo`, `dopodomani`. `l'anno prossimo`
      lleva apóstrofe ASCII U+0027.
    - Distractoras (D-41-09): DOS de raíz equivocada — (a) `facer-` con las desinencias de futuro
      italianas, el calco directo del castellano `hacer` (`facerò`, `facerai`, `facerà`, `faceremo`,
      `facerete`, `faceranno`); esta familia es exigencia LITERAL de SC-1 y tiene que estar presente en
      las 6 variantes; y (b) `fac-` con la síncopa aplicada a la raíz equivocada (`facrò`, `facrai`,
      `facrà`, `facremo`, `facrete`, `facranno`) — más UNA que es forma REAL de otra persona del futuro.
    - Explanation: la raíz contracta `far-`, común con el condizionale, y que el futuro de `fare` NO
      pasa por la raíz latina; las 6 formas citadas; el paralelo con `haré`, que en castellano también
      contrae; y el cierre nombrando el calco de `hacer` como la trampa principal y la síncopa mal
      aplicada como la segunda.

    Para los dos slots, las reglas duras comunes: pronombre sujeto explícito en los 6 prompts
    (D-41-07), el hueco literal `___`, objeto del conjunto cerrado de D-41-06, cero paréntesis y cero
    castellano en el prompt (D-41-05), 4 opciones, `correctIndex` variado entre las 6, cero corchetes
    angulares en cualquier string, y ninguna distractora que sea forma atestiguada de `fare` en otro
    modo ni palabra italiana homógrafa. Cada distractora se comprueba una a una antes de fijarla.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 3`; los ids son exactamente `fare-indicativo-presente`, `fare-indicativo-imperfetto`, `fare-indicativo-futuro-semplice`; los 3 tienen `type === 'multiple-choice'` y `variants.length === 6`; total de variantes 18.
    - `node -e`: el conjunto de keys (`options[correctIndex]`) de `fare-indicativo-imperfetto` es exactamente `{facevo, facevi, faceva, facevamo, facevate, facevano}`, sin repetidos.
    - `node -e`: el conjunto de keys de `fare-indicativo-futuro-semplice` es exactamente `{farò, farai, farà, faremo, farete, faranno}`, sin repetidos.
    - `node -e`: en `fare-indicativo-futuro-semplice`, las 6 variantes tienen al menos una opción de la familia del calco de `hacer` (una opción que empieza por `facer`), y ninguna de esas opciones es la key (SC-1).
    - Escaneo `node -e` sobre los 18 `prompt`: los 18 contienen `___`; los 18 contienen un pronombre sujeto de los 6; **0** contienen `(`, `)` ni `espa`; los 6 marcos de cada slot son strings distintos entre sí.
    - Escaneo `node -e` sobre los `options[]` de los 3 slots: **0** coincidencias exactas con `["fo","fé","fenno","facea","fan"]`; **0** con `["faccia","facciate","facciano","facessi","facesse","farei","farebbe","facci","facce","fava","faro"]`; **0** contienen `fatt`; todas las listas tienen longitud 4 y sin duplicados dentro de la misma variante.
    - Escaneo `node -e` sobre `prompt` y `options[]` de los 3 slots: **0** coincidencias con `colazione`, `spesa`, `freddo`, `farcel`.
    - Escaneo `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:` y de las comillas tipográficas.
    - `node -e`: los 3 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`.
    - `node -e`: `correctIndex` no es el mismo valor en las 6 variantes de ningún slot.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`; el `expected` del smoke es dinámico, así que el conteo sigue solo.
    - `git status --porcelain src/ scripts/ content/categories.json tests/` no produce ninguna línea de salida — esta tarea toca UN solo fichero.
  </acceptance_criteria>

  <done>
    El fichero tiene 3 slots y 18 variantes en `pending`; las keys de imperfetto y de futuro semplice
    son las 6 formas correctas de cada tiempo, una por persona; el futuro ofrece el calco de `hacer`
    como distractora en las 6 variantes y nunca como key; y la suite completa sigue en `fail 0`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Expansión — `fare-indicativo-passato-remoto`, con la alternancia `fec-`/`fac-` y los marcos disjuntos (6 variantes)</name>

  <files>content/exercises/fare-indicativo.json</files>

  <read_first>
    - `content/exercises/fare-indicativo.json` tal como quedó tras Task 3 — molde y `notes` (la
      declaración de marcos disjuntos y la de la blacklist ya están escritas ahí; este slot es donde se
      aplican).
    - `content/exercises/riflessivi.json` líneas 186-268 — el slot MAGNET: el molde de 4 opciones
      repetido, la explanation que nombra las trampas, y el shape de 4 pases que la ronda extra de
      D-41-12 dejará aquí en la pasada top-level.
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md` §decisions —
      D-41-08 (la blacklist se menciona también en la explanation de este slot), D-41-09, D-41-11
      (marcos disjuntos, en las dos direcciones), D-41-12 (ronda extra).
    - `.planning/REQUIREMENTS.md` línea 23 — el texto literal de IND-03.
    - `.planning/ROADMAP.md` §Phase 41 SC-1 — la exigencia de distractoras que materialicen el error
      real y no ruido.
  </read_first>

  <action>
    Añadir UN slot al array `exercises`, con el molde del tracer y `validation` en
    `{ "status": "pending", "passes": [] }`.

    **Slot `fare-indicativo-passato-remoto` (IND-03)**

    - Las 6 keys, una por variante en orden de persona: `feci`, `facesti`, `fece`, `facemmo`,
      `faceste`, `fecero`. La alternancia radical reparte `fec-` en 1ª singular, 3ª singular y 3ª
      plural, y `fac-` en 2ª singular, 1ª plural y 2ª plural. Es el hecho que el slot examina.
    - Familia de marco (D-41-11, SOLO remoto): narrativa remota, de las 3 familias declaradas y con 2
      instancias de cada una para cubrir las 6 variantes — un año concreto (`Nel 1990`, `Nel 1985`),
      un demostrativo remoto de periodo (`Quell'estate`, `Quell'inverno`, con apóstrofe ASCII), y un
      cuantificador de lejanía (`Molti anni fa`, `Tanti anni fa`). Ningún marco reciente ni conectado
      con el presente: nada de `stamattina`, `ieri`, `questa settimana`, `oggi`, `poco fa`, `questo
      mese` — esos son la familia exclusiva del slot de passato prossimo de 41-02.
    - Distractoras (D-41-09), molde fijo de 4 opciones: DOS de raíz equivocada aplicada a la persona
      correcta, de dos familias distintas — (a) la alternancia cruzada, que es el error real de esta
      casilla: poner `fac-` donde el paradigma pide `fec-` y al revés (`faci` frente a `feci`, `face`
      frente a `fece`, `facero` frente a `fecero`, `fecesti` frente a `facesti`, `fecemmo` frente a
      `facemmo`, `feceste` frente a `faceste`); y (b) el passato remoto regular de primera conjugación
      construido sobre `fac-`, el calco de conjugar `fare` como un `-are` normal (`facai`, `facasti`,
      `facò`, `facammo`, `facaste`, `facarono`) — más UNA que es forma REAL de otra persona del mismo
      paradigma del passato remoto.
    - **Prohibición dura y específica de este slot (D-41-11):** ninguna opción puede ser una forma de
      passato prossimo de `fare` (`ho fatto`, `hai fatto`, `ha fatto`, `abbiamo fatto`, `avete fatto`,
      `hanno fatto`), en ninguna de las 6 variantes, ni siquiera como distractora "claramente peor".
      La razón está en el `notes`: en el italiano del norte el compuesto cubre lo que aquí se pide con
      el remoto, así que una distractora cruzada sería válida para media Italia. Tampoco `fé` ni
      `fenno` (blacklist D-41-08), que son exactamente las dos formas atestiguadas que la autoría
      tendería a generar aquí creyéndolas obviamente malas.
    - Explanation: las 6 formas citadas con el reparto explícito de `fec-` y `fac-` por persona; la
      naturaleza del tiempo (narración de hechos acotados y lejanos, registro sobre todo escrito y
      literario) SIN presentar ningún otro tiempo como alternativa para los mismos marcos, para no
      abrir por sugerencia la doble lectura que D-41-11 cierra por diseño; mención de `fé` y `fenno`
      como formas poéticas que el autor puede ENCONTRAR leyendo pero no debe PRODUCIR (D-41-08); el
      paralelo con el castellano `hice`, que también tiene raíz propia frente a `hacía`; y el cierre
      nombrando las dos trampas (la alternancia cruzada y el patrón de primera conjugación).

    Reglas duras comunes: pronombre sujeto explícito en los 6 prompts, hueco `___`, objeto del conjunto
    cerrado, cero paréntesis y cero castellano en el prompt, 4 opciones sin duplicados, `correctIndex`
    variado, cero corchetes angulares. Cada distractora malformada se comprueba una a una: tiene que
    ser inexistente en italiano y no colisionar con ninguna palabra real.

    **Nota para el pase top-level (no es trabajo de esta tarea):** este slot es uno de los dos que
    llevan ronda EXTRA DeepSeek obligatoria (D-41-12), por ser la casilla donde una distractora mal
    escogida puede resultar ser una forma válida. Dejarlo anotado en el SUMMARY del plan.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 4`; los ids son exactamente los 4 de D-41-14 para los tiempos simples; los 4 tienen `type === 'multiple-choice'` y `variants.length === 6`; total 24 variantes.
    - `node -e`: el conjunto de keys de `fare-indicativo-passato-remoto` es exactamente `{feci, facesti, fece, facemmo, faceste, fecero}`, sin repetidos, y cada key corresponde al pronombre sujeto de su propio prompt.
    - Escaneo `node -e` sobre los 6 `prompt` de este slot: **0** coincidencias con `stamattina`, `ieri`, `questa settimana`, `oggi`, `poco fa`, `questo mese`; los 6 marcos son distintos y cada uno pertenece a una de las 3 familias remotas (año concreto, demostrativo de periodo, cuantificador de lejanía), con 2 variantes por familia (D-41-11).
    - Escaneo `node -e` sobre los `options[]` de este slot: **0** opciones contienen la subcadena `fatt` — ninguna forma de passato prossimo ni de ningún compuesto aparece como distractora (D-41-11).
    - Escaneo `node -e` sobre los `options[]` de los 4 slots: **0** coincidencias exactas con `["fo","fé","fenno","facea","fan"]`; **0** con `["faccia","facciate","facciano","facessi","facesse","farei","farebbe","facci","facce","fava","faro"]`.
    - `node -e`: la `explanation` de `fare-indicativo-passato-remoto` contiene las 6 formas correctas como subcadenas, contiene `fé` y `fenno`, y contiene las subcadenas `fec` y `fac`; su longitud es `> 400` caracteres.
    - `node -e`: la `explanation` de `fare-indicativo-presente` contiene `fo` como palabra suelta y las otras 4 formas de la blacklist (D-41-08).
    - Escaneo `node -e` sobre los 24 `prompt`: los 24 contienen `___`, los 24 contienen un pronombre sujeto de los 6, **0** contienen `(`, `)` ni `espa`.
    - Escaneo `node -e` sobre `prompt` y `options[]` de los 4 slots: **0** coincidencias con `colazione`, `spesa`, `freddo`, `farcel`.
    - Escaneo `node -e` sobre todos los strings: **0** apariciones de `<`, `>`, `&#`, `javascript:` y de las comillas tipográficas; ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`.
    - `node -e`: los 4 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`.
    - `node -e`: ningún id del fichero coincide con el patrón de sufijo numérico de 3 cifras — `fare-indicativo-300` y siguientes están libres (D-41-14).
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 4 slots con `(pending)` — el marcador del hand-off refleja el estado real.
    - `git diff --stat src/ scripts/` no produce ninguna línea de salida, y `git status --porcelain` lista únicamente `content/exercises/fare-indicativo.json` para esta tarea.
  </acceptance_criteria>

  <done>
    Los 4 tiempos simples están autorados con 24 variantes en `pending`; el passato remoto usa
    exclusivamente marcos narrativos remotos y ninguna de sus opciones es un tiempo compuesto; las
    keys de los 4 slots son las 24 formas correctas del paradigma, una por casilla persona×tiempo; y la
    suite completa sigue en `fail 0` con el motor sin tocar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Honestamente acotadas: esto es una app web estática de un solo usuario, sin servidor, sin autenticación,
sin red y sin sincronización (CLAUDE.md §Constraints). El único dato nuevo que entra es un fichero JSON
escrito a mano por el propio autor.

| Boundary | Description |
|----------|-------------|
| `content/exercises/*.json` en disco → `fetch()` en boot → `JSON.parse` en `content-loader.js` | Contenido semi-confiable: lo escribe el autor (o una autoría asistida), no un tercero, pero pasa por `JSON.parse` y de ahí al estado de la app sin ninguna otra puerta. |
| strings de contenido (`prompt`, `options[]`, `explanation`, `notes`) → render en el DOM por Alpine | La invariante anti-XSS del proyecto (T-02-01): el contenido se renderiza SIEMPRE como texto (`x-text`), nunca como HTML. Si un string llevara marcado, o el render cambiara a HTML, el contenido pasaría a ser código. |
| `content/categories.json` → `schema-validator.js` (`knownCategoryIds`) → `categoriesForDisplay` | Registro: una entrada mal formada rompe la validación del bundle completo o deja la categoría invisible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | strings de `content/exercises/fare-indicativo.json` renderizados en el DOM | medium | mitigate | Se preserva la invariante `x-text`-only (T-02-01): esta fase NO añade ni cambia una sola línea de render, y el contenido queda libre de marcado. Doble verificación automatizada: el sub-test `plain text (no markdown — D-126)` (`tests/exercise-types.test.js:1349-1363`), activado sobre el fichero nuevo por la línea de `CATEGORIES_WITH_EXPLANATIONS` de la Task 1; más un escaneo `node -e` en cada tarea que exige **0** apariciones de `<`, `>`, `&#` y `javascript:` en TODOS los strings del fichero, incluido `notes`. El `notes` se redacta sin corchetes angulares a propósito, para que el escaneo no necesite excepciones. |
| T-41-02 | Tampering | `JSON.parse` del fichero de contenido en `content-loader.js` | medium | mitigate | Prototype-pollution por contenido: `JSON.parse` crea `__proto__` como own-property si el literal la declara. Mitigación de esta fase: el fichero declara ÚNICAMENTE el key set del schema (top-level `notes` y `exercises`; por slot `id`, `type`, `categoryIds`, `explanation`, `variants`, `validation`; por variante `prompt`, `options`, `correctIndex`), y cada tarea corre un escaneo recursivo `node -e` que exige que ninguna clave propia del objeto parseado se llame `__proto__`, `constructor` ni `prototype`, más `({}).polluted === undefined` tras el parse. El deep-clone defensivo del state ya vive en la cadena de migración (T-40-01) y no se toca. |
| T-41-03 | Repudiation | `validation.passes[]` como audit trail del quórum | medium | mitigate | Un `status: "validated"` escrito a mano, o un pase con `by`/`date` inventados, forja la única evidencia que el autor tiene de que una variante fue revisada, y la falsificación es indetectable a ojo. Mitigación: el executor deja los 4 slots en `status: "pending"` con `passes: []` y NO fabrica pases (criterio de aceptación en las 3 tareas de contenido); el quórum real corre en la pasada top-level de D-41-15, un ejercicio por contexto; y el gate permanente de 41-02 exige coherencia con `deriveStatus` (`src/data/validation-state.js:34-39`) — un `validated` con menos de 2 pases `correcta` de `by` distintos, o con algún `incorrecta`, pone la suite roja. |
| T-41-04 | Tampering | entrada de `content/categories.json` | low | mitigate | Un `origen` con typo o una clave extra hacen `ok:false` en el validador de procedencia (`tests/schema-validator-origen.test.js:25-52`), y un `id` que no case byte a byte con el nombre del fichero y con `RESET_PREFIXES_V13` rompería el contrato de slugs de Phase 40. Verificado por criterio de aceptación: la entrada tiene exactamente las 4 claves `id`, `name`, `order`, `origen`, es la 15ª, y `node --test tests/*.test.js` queda en `fail 0`. |
| T-41-05 | Denial of Service | tamaño del bundle de contenido cargado en boot | low | accept | 48 variantes son del orden de 600 líneas de JSON; el loader ya trae 14 ficheros con 225 slots. El coste de `JSON.parse` a esta escala es despreciable y la app no tiene atacante remoto: sin servidor, sin red, sin multiusuario. |
| T-41-06 | Information Disclosure | el contenido del fichero | low | accept | Son conjugaciones de un verbo italiano y explicaciones gramaticales. Cero PII, cero credenciales, cero secretos. El fichero no sale de la máquina del autor. |
| T-41-07 | Spoofing | — | n/a | n/a — no aplica | No existen identidades, sesiones, tokens ni cuentas en esta app: es local, monousuario y sin autenticación. No hay superficie de suplantación que evaluar en L1. |
| T-41-08 | Elevation of Privilege | — | n/a | n/a — no aplica | No hay roles, permisos ni frontera de privilegio: un único usuario con acceso total a su propio navegador y su propio disco. |
| T-41-SC | Tampering | instalaciones de npm / pip / cargo | n/a | n/a — gate no disparado | Esta fase no ejecuta ninguna instalación de gestor de paquetes ni añade dependencias: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node. El Package Legitimacy Gate no aplica y no se requiere tabla de auditoría en RESEARCH.md. |

**ASVS L1, block on high.** No hay ninguna amenaza `high` en este plan, y no se inventa ninguna para
rellenar la tabla: la superficie real de una fase de contenido en una app local sin servidor son la
invariante de render como texto, la higiene del key set del JSON parseado y la integridad del audit
trail de validación. Las tres están en `mitigate` con verificación automatizada. Las tres `accept` son
`low` y su razón es estructural (no hay atacante remoto, no hay datos sensibles).

### Candidatos de prohibición canon descartados (breadcrumbs)

- **prototype-pollution** — canon de seguridad del proyecto; vive en `<threat_model>` T-41-02 y en
  `/gsd-secure-phase`, no se acuña como prohibición.
- **XSS / inyección de HTML** — canon; vive en T-41-01 (invariante `x-text`-only T-02-01), no se acuña.
- **path-traversal** — sin acceso a sistema de ficheros en runtime: el contenido se sirve por HTTP
  local y el runtime es el navegador. Descartado.
- **GDPR / privacidad de datos personales** — el contenido son conjugaciones y el state es el progreso
  de estudio del propio autor en su máquina. Descartado.
- **fairness / sesgo genérico** — sin decisión automatizada sobre personas. Lo que SÍ se acuña es la
  variante bespoke y específica de esta fase: no presentar una variedad regional como norma
  (prohibición 3), que ninguna herramienta canon detectaría.
</threat_model>

<verification>
Comprobaciones al terminar las 4 tareas de este plan:

1. `node --test tests/*.test.js` → código de salida 0, `fail 0`, `pass` mayor que el baseline
   re-medido en la precondición de Task 1.
2. `node -e` sobre `content/exercises/fare-indicativo.json`: 4 slots, ids exactamente los 4 simples de
   D-41-14, 6 variantes cada uno, 24 en total, los 4 en `type: "multiple-choice"` con `options` de 4.
3. `node -e` sobre `content/categories.json`: 15 entradas, la 15ª es `fare-indicativo` con `order: 15`
   y `origen: "ia-quorum"`, y exactamente las 4 claves del schema.
4. Los 6 escaneos de ausencia, todos por campo y no por fichero completo (el `notes` nombra a propósito
   las formas y las perífrasis excluidas, así que un `grep` de fichero completo se auto-invalidaría):
   blacklist en `options`, formas de otros modos en `options`, perífrasis en `prompt` y `options`,
   paréntesis y castellano en `prompt`, marcos recientes en el `prompt` del passato remoto, y
   compuestos en las `options` del passato remoto.
5. `VAL_07_STRICT=1 node --test tests/*.test.js` → falla nombrando los 4 slots `pending`. Ese rojo es
   el estado CORRECTO al cerrar este plan; el pase top-level de quórum es lo que lo cierra.
6. `node scripts/run-validation-271.mjs` → sigue reportando su total anterior en PASS. Es la ceguera
   documentada en §Correcciones, no un fallo: el reporter no verá la categoría hasta Phase 44 / INT-02.
7. `git diff --stat src/screens/app.js src/domain/ src/data/` → salida vacía (SC-5, Out of Scope del
   milestone).
8. `git status --porcelain` → exactamente los 3 ficheros de `files_modified`.
</verification>

<success_criteria>
- IND-01: el autor es examinado sobre `faccio / fai / fa / facciamo / fate / fanno`, con la trampa de
  la raíz doble `facc-` frente a `fa-` materializada en las distractoras.
- IND-02: el autor es examinado sobre `facevo / facevi / faceva / facevamo / facevate / facevano`, con
  la raíz latina `fac-` que el infinitivo esconde.
- IND-03: el autor es examinado sobre `feci / facesti / fece / facemmo / faceste / fecero`, con la
  alternancia `fec-`/`fac-` repartida por persona y marcos exclusivamente remotos.
- IND-04: el autor es examinado sobre `farò / farai / farà / faremo / farete / faranno`, con la raíz
  contracta `far-` y el calco de `hacer` como distractora presente en las 6 variantes.
- SC-1: 4 tiempos simples, una persona distinta por pasada vía `pickVariantIndex`, y distractoras que
  materializan el error real del hispanohablante, no ruido.
- SC-5: la categoría carga en boot y aparece en home, picker, Repaso y Examen sin una línea de motor
  nueva — verificado por dato en Task 1 y a ojo en el checkpoint de Task 2.
- Estado de hand-off correcto: los 4 slots en `pending` con `passes: []`, y el rojo de
  `VAL_07_STRICT=1` como marcador visible de que el quórum base todavía no ha corrido.
</success_criteria>

<output>
Crear `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-01-SUMMARY.md` al terminar.
</output>
