---
phase: quick-260617-r10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/songs/bella-davvero.json
  - content/songs.json
autonomous: true
requirements: [QUICK-260617-R10]
must_haves:
  truths:
    - "La cancion 'Bella davvero — Ultimo' aparece en el bloque Canciones (es la 4a real) y es jugable end-to-end"
    - "Cada verso italiano (salvo exclusiones) es una phrase con prompt italiano + answer espanol troceado por palabras SIN puntuacion"
    - "Las onomatopeyas 'Ta-ra-ra...' y el 'mmh' suelto NO generan phrase de ejercicio"
    - "Las traducciones pasan el quorum S1-S5 (cada phrase validated o disputed marcada para el autor)"
    - "La suite queda verde (igual fail-baseline preexistente ajeno) y el schema-validator de canciones carga el nuevo doc sin errores"
  artifacts:
    - path: "content/songs/bella-davvero.json"
      provides: "Documento de cancion {id:'bella-davvero', title, phrases[]} espejo de solo.json"
      contains: "\"id\": \"bella-davvero\""
    - path: "content/songs.json"
      provides: "Entrada de indice {id, title, phraseCount} para bella-davvero"
      contains: "bella-davvero"
  key_links:
    - from: "content/songs.json"
      to: "content/songs/bella-davvero.json"
      via: "loadSongs deriva ids del indice y hace fetch de cada content/songs/<id>.json"
      pattern: "bella-davvero"
    - from: "content/songs/bella-davvero.json"
      to: "validateSongs (schema-validator)"
      via: "loadSongs boot path"
      pattern: "phrases"
---

<objective>
Anadir la cancion "Bella davvero — Ultimo" (id `bella-davvero`) al bloque Canciones, replicando el patron consolidado MUSIC-X1 / v1.3 (espejo exacto de `content/songs/solo.json`): top-level `{id, title, phrases[]}`, cada phrase `{id, prompt:<italiano>, answer:[tokens espanol SIN puntuacion], categoryIds:[], validation:{status, passes[]}}`. Es la 4a cancion real (tras equilibrio-mentale, solo, ti-dedico-il-silenzio).

Purpose: Ampliar el corpus de canciones del autor con su letra exacta, traducida al espanol tokenizada para el ejercicio word-buttons inverso (italiano->espanol), y dejarla validada por quorum.

Output: `content/songs/bella-davvero.json` (todas las phrases de los ~40 versos, menos exclusiones) + entrada en el indice `content/songs.json`, con cada phrase validada por el quorum S1-S5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<!-- ANALOGO EXACTO del shape a replicar (top-level + phrase + validation) -->
@content/songs/solo.json

<!-- Indice de canciones — anadir entrada {id, title, phraseCount} -->
@content/songs.json

<!-- Criterios del quorum de canciones (S1-S5), NO R1-R7 -->
@docs/SONG-VALIDATION-PROMPT.md

<!-- Validador cross-vendor de traduccion de canciones (writePass INSERTA validation) -->
@scripts/validate-song-pass.mjs

<interfaces>
<!-- Contratos que el ejecutor necesita; extraidos del codebase. Usar directamente. -->

Shape de cada phrase (de content/songs/solo.json):
- id: string slug ASCII unico dentro de la cancion (p.ej. "bella-davvero-001")
- prompt: string — el verso ORIGINAL en italiano (limpio, NFC, sin caracteres espurios)
- answer: string[] — la traduccion ESPANOLA troceada, UNA palabra por token, SIN puntuacion
  (sin comas/puntos pegados; espanol acentuado canon RAE: tildes y enie donde corresponda)
- categoryIds: [] — vacio (las canciones no enganchan categorias de gramatica salvo decision explicita)
- validation: { status: "pending"|"validated"|"disputed", passes: [...] } — nace "pending"; el quorum la rellena

Shape de validateSongs (src/data/schema-validator.js:255) — que exige el boot/load:
- songs debe ser array; cada song: id slug ASCII unico, title no vacio, phrases array NO vacio (WR-01)
- cada phrase: id presente y unico dentro de la cancion; prompt string no vacio; answer array no vacio de tokens string no vacios
- categoryIds OPCIONAL (LINK-03): ausente o [] es valido
- validation OPCIONAL por frase (back-compat: ausencia = OK)
- NO asserta phraseCount del indice == phrases.length (el indice es informativo; mantenerlo consistente igualmente)

Entrada de indice (content/songs.json):
  { "id": "<slug>", "title": "<titulo>", "phraseCount": <n> }

Discovery del boot (src/data/content-loader.js:186 loadSongs):
- loadSongs deriva los ids del indice songs.json y hace fetch de content/songs/<id>.json por cada uno
- => bella-davvero DEBE estar en el indice o el doc nunca se carga ni valida
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Autorar content/songs/bella-davvero.json + entrada en el indice</name>
  <files>content/songs/bella-davvero.json, content/songs.json</files>
  <action>
Crear `content/songs/bella-davvero.json` con shape espejo EXACTO de `content/songs/solo.json`: top-level `{ "id": "bella-davvero", "title": "Bella davvero — Ultimo", "phrases": [...] }`.

Procesar la letra italiana (los ~40 versos que dio el autor — leerla del input del autor / sesion) verso a verso, EN ORDEN. Para CADA verso elegible:
- id: `bella-davvero-NNN` (zero-padded, secuencial desde 001).
- prompt: el verso italiano LIMPIO. Normalizar NFC; quitar dobles espacios; verificar caracter a caracter que NO hay caracteres no-latinos espurios (p.ej. una `e` cirilica U+0435 en lugar de la `e` latina U+0065) — bug recurrente; los apostrofos siempre ASCII U+0027.
- answer: la traduccion ESPANOLA troceada en tokens, UNA palabra por elemento, SIN puntuacion (sin comas/puntos pegados a tokens). Espanol acentuado canon RAE (tildes y enie donde toque) — el autor exige acentos correctos (memoria explanations_must_be_accented / S4). Recordar S1: el answer NO lleva puntuacion POR DISENO; juzgar la traduccion reconstruida mentalmente con la puntuacion implicita, no penalizar run-on.
- categoryIds: `[]`.
- validation: `{ "status": "pending", "passes": [] }` (la valida el quorum en Task 2).

EXCLUSIONES (NO crear phrase de ejercicio):
- Onomatopeyas: los 2 versos "Ta-ra-ra, ra-ra-ra, ..." — omitir por completo.
- El "mmh" suelto al final de un verso — limpiar del prompt (no traducir como token); si el verso conserva contenido real tras quitar el "mmh", conservar el resto; si el verso ES solo "mmh", omitirlo.

Versos REPETIDOS (p.ej. "Mi ricordo di te", "Eri bella, eri bella...") SI se incluyen cada vez que aparecen (son frases reales del playthrough), con su propio id secuencial — salvo decision contraria del autor.

Luego anadir la entrada al indice `content/songs.json` dentro del array `songs`, DESPUES de las 4 existentes (mini-prueba, equilibrio-mentale, solo, ti-dedico-il-silenzio), preservandolas byte-intactas:
  `{ "id": "bella-davvero", "title": "Bella davvero — Ultimo", "phraseCount": <phrases.length real> }`
El phraseCount DEBE igualar el numero real de phrases del JSON (contar, no estimar).

Brownfield: NO tocar ningun `.js` (motor v1.4 / engine de canciones intacto), NO tocar `scripts/run-validation-271.mjs` ni los count arrays de gramatica (las canciones son un bloque aparte — verificado: `grep -c song scripts/run-validation-271.mjs` = 0). Solo contenido + indice.
  </action>
  <verify>
    <automated>node -e "const f=require('./content/songs/bella-davvero.json'); const idx=require('./content/songs.json'); const s=idx.songs.find(x=>x.id==='bella-davvero'); if(f.id!=='bella-davvero') throw new Error('id'); if(!Array.isArray(f.phrases)||f.phrases.length===0) throw new Error('phrases'); const ids=new Set(); for(const p of f.phrases){ if(!/^bella-davvero-\d{3}$/.test(p.id)) throw new Error('phrase id '+p.id); if(ids.has(p.id)) throw new Error('dup '+p.id); ids.add(p.id); if(typeof p.prompt!=='string'||!p.prompt.trim()) throw new Error('prompt '+p.id); if(!Array.isArray(p.answer)||p.answer.length===0||p.answer.some(t=>typeof t!=='string'||!t.trim())) throw new Error('answer '+p.id); if(p.answer.some(t=>/[,.;:!?]/.test(t))) throw new Error('puntuacion en token '+p.id); } const cyr=f.phrases.filter(p=>/[Ѐ-ӿ]/.test(p.prompt)); if(cyr.length) throw new Error('cirilico '+cyr[0].id); if(!s) throw new Error('falta en indice'); if(s.phraseCount!==f.phrases.length) throw new Error('phraseCount '+s.phraseCount+' != '+f.phrases.length); console.log('OK phrases='+f.phrases.length+' indice.phraseCount='+s.phraseCount);"</automated>
  </verify>
  <done>`content/songs/bella-davvero.json` existe con shape espejo de solo.json: id `bella-davvero`, title correcto, phrases[] con ids secuenciales unicos `bella-davvero-NNN`, cada phrase con prompt italiano limpio (0 cirilico), answer espanol tokenizado SIN puntuacion, categoryIds [], validation pending. Las onomatopeyas "Ta-ra-ra..." y el "mmh" suelto NO tienen phrase. Entrada en `content/songs.json` tras las 4 existentes con phraseCount == phrases.length real. 0 `.js` tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Validar por quorum S1-S5 (1-por-1, --write) + boot/load + suite verde</name>
  <files>content/songs/bella-davvero.json</files>
  <action>
Validar CADA phrase de `bella-davvero` por quorum cross-vendor S1-S5 (NO R1-R7), 1-por-1 (NUNCA batched — cada invocacion ve UNA sola frase con context fresco), usando `scripts/validate-song-pass.mjs`. Su `writePass` INSERTA el bloque `validation` si falta y re-deriva el status.

Procedimiento por cada `bella-davvero-NNN`:
- Pase 1: `node scripts/validate-song-pass.mjs bella-davvero-NNN --model=deepseek-chat --fallback=gemini-2.5-flash --write`
- Pase 2 (segundo vendor distinto, --avoid el `by` del pase 1): `node scripts/validate-song-pass.mjs bella-davvero-NNN --model=gemini-2.5-flash --fallback=deepseek-chat --avoid=<by-del-pase-1> --write`
- El quorum requiere >=2 passes `correcta` de `by` distintos para `status: validated`.
- Las claves GEMINI_API_KEY / DEEPSEEK_API_KEY se leen de `.env` (memoria multi_vendor_quorum_validator).

Resolucion de fallos del quorum (memorias del autor — song_quorum_validator + feedback_disputed_resolution + explanations_must_be_accented):
- El quorum LLM es RUIDOSO en lo sutil => calidad > tokens, NO override-atajo.
- Flag S4-acentos sobre espanol que SI deberia llevar tilde y no la lleva = bug REAL => ARREGLAR el acento en el answer, NO override.
- Flag S1-natural / "run-on" por falta de comas = falso-positivo de politica (S1 ignora puntuacion) => NO modificar; si el modelo insiste, dejar la phrase `disputed` y MARCARLA para el autor (el humano resuelve los disputed).
- Flag S5-italiano por caracter espurio / errata = bug REAL => limpiar el prompt.
- Si tras el quorum una phrase queda `disputed`, NO forzar `validated`: conservar el verdict real como audit trail y listar el id en el SUMMARY para que el autor decida.

Tras la pasada, verificar el boot/load real (path `loadSongs` -> `validateSongs`, firma ES module — NO `require()` posicional): que `bella-davvero.json` carga sin errores de shape junto con las otras canciones; luego correr la suite. El unico fail aceptable es el preexistente AJENO (genero-numero "Esperaba 12, encontre 13", quick `260614-hxn`) — 0 fails NUEVOS ligados a bella-davvero. Los tests de `validateSongs` (song-validator.test.js) son unitarios sinteticos => su conteo no cambia por anadir el archivo; deben seguir verdes.
  </action>
  <verify>
    <automated>node -e "const f=require('./content/songs/bella-davvero.json'); let bad=[],disputed=[]; for(const p of f.phrases){ const v=p.validation||{}; if(v.status==='disputed') disputed.push(p.id); else if(v.status!=='validated') bad.push(p.id+':'+(v.status||'none')); } if(bad.length) throw new Error('phrases sin resolver (ni validated ni disputed): '+bad.join(',')); console.log('OK. disputed-para-autor='+(disputed.join(',')||'ninguna'));" && node --test tests/song-validator.test.js 2>&1 | tail -3 && node --test tests/*.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>Cada phrase de bella-davvero tiene `validation.status` en `validated` (>=2 passes correcta, `by` distintos) o `disputed` (verdict real conservado como audit trail + id listado en el SUMMARY para el autor); ninguna queda `pending`. Bugs reales S4-acentos / S5-italiano arreglados en el contenido (no override). `validateSongs` carga bella-davvero.json sin errores de shape en el boot path. `tests/song-validator.test.js` verde; suite completa con 0 fails NUEVOS (solo el preexistente ajeno genero-numero permitido).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor -> JSON de contenido | El autor edita a mano `content/songs/*.json`; texto no estructurado (letra) entra al corpus |
| JSON de contenido -> motor de canciones (boot) | El doc cargado por `loadSongs` debe cumplir el shape de `validateSongs` o el banner DATA-02 / WR-01 rompe la pantalla |
| frase JSON -> modelo evaluador (quorum) | El payload de la frase se pasa a un LLM externo (DeepSeek/Gemini) como DATA, no como instruccion |

Single-user, offline, static web: sin red de runtime (los modelos solo se llaman en validacion offline por el autor), sin auth, sin servidor. Superficie = integridad / shape del JSON de contenido.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-r10-01 | Tampering | content/songs/bella-davvero.json (shape) | mitigate | `validateSongs` en el boot path + verify automatico Task 1 (id slug, ids unicos, prompt/answer no vacios, tokens sin puntuacion) |
| T-r10-02 | Information Disclosure | prompt italiano (caracteres espurios) | mitigate | Scan de cirilico U+0400-04FF en verify Task 1 + NFC normalize en el borde (loadSongs D-09); apostrofos ASCII |
| T-r10-03 | Denial of Service | phrases vacio / cancion callejon sin salida | mitigate | `validateSongs` WR-01 rechaza phrases [] ; verify Task 1 exige phrases.length > 0 |
| T-r10-04 | Tampering | prompt-injection en el payload de la frase hacia el evaluador | accept | El SONG-VALIDATION-PROMPT seccion 6 ya guarda contra inyeccion (DATA != instruccion); contenido del autor, no hostil; el humano resuelve disputed |
| T-r10-05 | Tampering | edicion accidental del motor v1.4 / count arrays de gramatica | mitigate | Brownfield: solo contenido + indice; verificado `grep -c song run-validation-271.mjs` = 0; suite con 0 fails NUEVOS |
| T-r10-SC | Tampering | npm/pip/cargo installs | accept | Sin instalaciones: `validate-song-pass.mjs` es zero-deps (https/fs/path nativos); 0 paquetes nuevos |
</threat_model>

<verification>
- `node -e` shape-check de bella-davvero.json: id, ids unicos `bella-davvero-NNN`, prompt no vacio, answer tokenizado sin puntuacion, 0 cirilico, entrada en indice con phraseCount == phrases.length.
- Cada phrase con `validation.status` resuelto (validated o disputed), ninguna pending.
- Boot/load: `validateSongs` carga el nuevo doc sin errores de shape (firma ES module).
- `node --test tests/song-validator.test.js` verde; `node --test tests/*.test.js` con 0 fails NUEVOS (solo el preexistente ajeno genero-numero permitido).
- Brownfield: 0 `.js` modificado; `run-validation-271.mjs` y count arrays de gramatica intactos.
</verification>

<success_criteria>
- "Bella davvero — Ultimo" (id `bella-davvero`) jugable en el bloque Canciones como 4a cancion real, con todos los versos elegibles como phrases (prompt italiano + answer espanol tokenizado).
- Onomatopeyas "Ta-ra-ra..." y "mmh" suelto EXCLUIDAS; versos repetidos incluidos cada vez.
- Cada phrase validated por quorum S1-S5 o disputed marcada para el autor en el SUMMARY.
- Indice `content/songs.json` con la entrada nueva (phraseCount real) y las 4 previas intactas.
- Suite verde (mismo fail-baseline preexistente ajeno) y schema-validator de canciones carga el doc sin errores.
</success_criteria>

<output>
Crear `.planning/quick/260617-r10-a-adir-canci-n-bella-davvero-ultimo-al-b/260617-r10-SUMMARY.md` al terminar (incluir lista de ids `disputed` pendientes de resolucion del autor, si los hay).
</output>
