---
phase: quick-260615-hr0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/app.js
  - index.html
  - styles.css
  - tests/exercise-types.test.js
autonomous: true
requirements: [TODO-3]
must_haves:
  truths:
    - "Al acertar un ejercicio con explanation, aparece un affordance '¿Por qué?' (botón + atajo de teclado)"
    - "Pulsar '¿Por qué?' (clic o tecla) cancela el auto-avance y revela la explicación + botón 'Siguiente'"
    - "El auto-avance en acierto dura más tiempo (constante nombrada) cuando hay explanation; sigue 600ms cuando no la hay"
    - "El revelado NO se arrastra entre ejercicios (se resetea al avanzar)"
    - "El flujo de FALLO no cambia; las canciones no cambian"
    - "node --test tests/*.test.js no introduce fallos NUEVOS (el fallo preexistente genero-numero 12→13 se mantiene, no se toca)"
  artifacts:
    - path: "src/screens/app.js"
      provides: "Flag sessionExplanationRevealed + acción reveal + auto-avance condicional + reset + atajo de teclado"
    - path: "index.html"
      provides: "Botón '¿Por qué?' y condiciones de explanation/Siguiente ampliadas en los 3 tipos"
    - path: "styles.css"
      provides: "Estilo discreto del botón '¿Por qué?'"
  key_links:
    - from: "botón/atajo '¿Por qué?'"
      to: "cancelAutoAdvance() + sessionExplanationRevealed = true"
      via: "acción reveal"
      pattern: "sessionExplanationRevealed"
    - from: "applyResultToSession (acierto)"
      to: "setTimeout con constante de tiempo condicional a explanation"
      via: "auto-avance"
      pattern: "setTimeout"
---

<objective>
Permitir consultar la `explanation` de un ejercicio TAMBIÉN al acertar (hoy solo se ve al fallar). Feature todo #3.

Al acertar se mantiene el auto-avance rápido, pero aparece un affordance "¿Por qué?" (botón + atajo de teclado). Interactuar con él cancela el auto-avance y revela la explicación + un botón "Siguiente" para continuar manualmente. Para dar tiempo a reaccionar, el auto-avance en acierto se alarga (constante nombrada) SOLO cuando el ejercicio tiene explanation.

Purpose: cubrir el caso "aciertas por casualidad o dudabas y quieres confirmar el porqué" sin estorbar cuando aciertas con seguridad.
Output: cambios puros de UI/interacción derivada en `app.js`, `index.html`, `styles.css` + un test del nuevo comportamiento.

ALCANCE ESTRICTO (CONTEXT.md, decisiones bloqueadas — NO revisar):
- Aplica a los 3 tipos de ejercicio (multiple-choice, word-buttons, match) en la pantalla `session`.
- Canciones: SIN cambios (sus frases no tienen explanation; el path `handleSongKey`/`songAdvance` NO se toca).
- Flujo de FALLO: SIN cambios.
- NO tocar estado persistido (localStorage), `storage.js`, scoring ni racha. Es UI/interacción derivada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260615-hr0-ver-la-explicaci-n-del-ejercicio-aunque-/260615-hr0-CONTEXT.md
@CLAUDE.md

# ANTES DE ESCRIBIR: LEER EL CÓDIGO REAL (no inventar APIs/nombres)
El executor DEBE leer y confirmar contra el código actual:
- src/screens/app.js:
  - Campos de estado de sesión declarados en ~L115-122 (`sessionFeedback: null` con valores null|'correct'|'incorrect'; `sessionAutoAdvanceHandle: null`). Aquí se añade el nuevo flag.
  - `applyResultToSession(ex, correct, userAnswer)` ~L1499: en acierto programa `this.sessionAutoAdvanceHandle = setTimeout(advance, 600)` (~L1521); `advance` dispatcha por `sessionMode === 'cancion'` → `songAdvance()` vs `sessionAdvance()`. Aquí va el tiempo condicional.
  - `sessionAdvance()` ~L1559: llama `cancelAutoAdvance()` y resetea `sessionSelectedIndex`/`sessionFeedback = null`. Aquí se resetea el nuevo flag.
  - `cancelAutoAdvance()` ~L1589: idempotente, limpia el handle.
  - `handleSessionKey(event)` ~L1996: bindings existentes — Enter/Space (~L2007), Backspace (~L2025), dígitos 1-9 (~L2034), **letras a..i para match (~L2073)**. OJO: la tecla sugerida `e` cae dentro del rango a..i; ese bloque hace `if (this.sessionFeedback !== null) return` (~L2074) y `if (ex.type !== 'match') return` (~L2075), por lo que en acierto ('correct') retorna antes — el nuevo handler de `e` debe ir colocado de forma que dispare en acierto. VERIFICAR esto antes de escribir.
  - Getter `sessionCurrentExercise` ~L2282 devuelve objeto con `.payload.explanation` (slot-aware). La explanation puede ser undefined/vacía.
- index.html:
  - `<article @keydown.window="handleSessionKey($event)">` ~L317 (pantalla session). El de canción es `handleSongKey` (~L589) — NO tocar.
  - Bloque MC ~L356-376: explanation en ~L373-375 con `x-show="sessionFeedback === 'incorrect' && sessionCurrentExercise.payload.explanation"` class `session-explanation`; botón "Siguiente" ~L376 con `x-show="sessionFeedback === 'incorrect'"` @click="sessionAdvance".
  - Bloque word-buttons ~L423-446: explanation ~L434-436 (misma condición/clase); botón Siguiente ~L445-446 @click="sessionAdvance".
  - Bloque match ~L523-528: explanation ~L523-525 (misma condición/clase); botón Siguiente ~L527-528 @click="sessionAdvance".
  - El bloque de canción ~L627-640 (`wb-correct-answer`, songAdvance) — NO tocar.
- styles.css: `.session-explanation` (D-120 muted/italic), `.button-row`/`button.correcta`/`.wb-correct-answer` ~L35-94, ~L223. Pico CSS clases mínimas.
- tests/exercise-types.test.js: el patrón para verificar comportamiento de `app.js` es ASSERT SOBRE EL TEXTO FUENTE (`readFileSync('../src/screens/app.js')` + includes/regex), NO instanciar Alpine (ver ~L759, ~L789, ~L816). El nuevo test sigue ese patrón.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lógica en app.js — flag, acción reveal, auto-avance condicional, reset, atajo</name>
  <files>src/screens/app.js</files>
  <action>
PRIMERO leer las regiones citadas en context para confirmar nombres/líneas reales.

(a) Añadir el campo de estado `sessionExplanationRevealed: false` junto a `sessionFeedback`/`sessionAutoAdvanceHandle` (~L115-122). Comentar que es UI derivada por-ejercicio (no persistida).

(b) Añadir una constante nombrada de módulo (top del archivo, junto a otras constantes si existen, o cerca de `applyResultToSession`) p.ej. `const SESSION_AUTO_ADVANCE_MS = 600;` y `const SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS = 1500;`. No usar números mágicos dispersos: reemplazar el literal `600` en `applyResultToSession` por la constante.

(c) En `applyResultToSession` rama `if (correct)` (~L1512-1521): elegir el tiempo de auto-avance de forma condicional. Cuando NO es modo canción Y `ex.payload.explanation` es truthy → usar `SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS`; en cualquier otro caso (sin explanation, o modo canción) → `SESSION_AUTO_ADVANCE_MS` (600). El dispatch existente de `advance` (`songAdvance` vs `sessionAdvance`) NO cambia. NO alterar la rama de canción más allá de mantener su 600ms. Comentar el porqué (dar tiempo a reaccionar al affordance "¿Por qué?"; quick-260615-hr0 feature todo #3).

(d) Añadir un método/acción `revealSessionExplanation()` (nombre a discreción) que: hace `this.cancelAutoAdvance()` y pone `this.sessionExplanationRevealed = true`. Guard: solo actuar si `this.sessionFeedback === 'correct'` y `this.sessionCurrentExercise?.payload?.explanation` es truthy (defensivo; evita revelar sin sentido). Será invocado por el botón y por el atajo.

(e) En `sessionAdvance()` (~L1559-1564): resetear `this.sessionExplanationRevealed = false` junto a `sessionFeedback = null` (no arrastrar el revelado entre ejercicios). VERIFICAR si hay otros puntos donde la sesión arranca/reinicia feedback (hay varios resets `sessionFeedback = null` por todo el archivo — ~L447, L554, L722, L843, L983, L1134). Resetear el flag al menos en `sessionAdvance` (avance normal) y en el/los punto(s) de INICIO de sesión donde se prepara el primer ejercicio, para no arrastrar revelado de una sesión previa. Usar criterio: co-localizar el reset con cada `sessionFeedback = null` que marque transición a un ejercicio nuevo o inicio de sesión. No es necesario tocar resets que pertenezcan exclusivamente al path de canción si no afectan.

(f) En `handleSessionKey(event)` (~L1996): añadir el atajo. La tecla sugerida es `e` (a discreción si colisiona). COLOCAR el nuevo handler ANTES del bloque de letras a..i (~L2073) o con guard explícito, porque ese bloque retorna temprano cuando `sessionFeedback !== null`. Condición del nuevo handler: disparar SOLO cuando `this.sessionFeedback === 'correct'` Y `this.sessionCurrentExercise?.payload?.explanation` es truthy Y aún no revelado. Acción: `event.preventDefault()` y llamar `this.revealSessionExplanation()`; luego `return`. Verificar que no colisiona con bindings activos en estado 'correct' (1-9 y a..i ya retornan en `sessionFeedback !== null`; Enter/Space en 'correct' es noop) — `e` en 'correct' está libre. NO tocar `handleSongKey`.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | grep -E "# (pass|fail)" </automated>
  </verify>
  <done>app.js define `sessionExplanationRevealed`, las dos constantes de tiempo, `revealSessionExplanation()`, auto-avance condicional a explanation en acierto, reset del flag en sessionAdvance (+ inicio de sesión), y atajo de teclado que cancela auto-avance y revela. node --test NO añade fallos nuevos respecto al baseline (1 fallo preexistente ajeno: genero-numero 12→13).</done>
</task>

<task type="auto">
  <name>Task 2: Markup en index.html (3 tipos) + estilo del botón en styles.css</name>
  <files>index.html, styles.css</files>
  <action>
PRIMERO leer los 3 bloques de session en index.html y confirmar condiciones/líneas reales.

En CADA uno de los 3 bloques de ejercicio de la pantalla session (MC ~L356-376, word-buttons ~L423-446, match ~L523-528) — NO en el bloque de canción:

(1) Botón "¿Por qué?": añadir un `<button>` con `x-show="sessionFeedback === 'correct' && sessionCurrentExercise.payload.explanation && !sessionExplanationRevealed"` y `@click="revealSessionExplanation()"`. Texto: "¿Por qué?". Clase discreta (p.ej. `class="session-why"`) para estilo en styles.css. Colocarlo en posición coherente con el botón "Siguiente" de cada bloque (cerca del final del bloque, en/junto a la fila de botones existente cuando la haya, p.ej. el `.button-row` de word-buttons).

(2) Explanation: AMPLIAR la condición del `<p class="session-explanation" ...>` existente para que muestre la explicación cuando (fallo) O (acierto revelado). Es decir, cambiar `x-show="sessionFeedback === 'incorrect' && sessionCurrentExercise.payload.explanation"` a algo equivalente a `x-show="(sessionFeedback === 'incorrect' || (sessionFeedback === 'correct' && sessionExplanationRevealed)) && sessionCurrentExercise.payload.explanation"`. Reutilizar el mismo `<p>` (condición ampliada), no duplicar el markup. Repetir en los 3 bloques.

(3) Botón "Siguiente": AMPLIAR su `x-show` para que aparezca también en acierto revelado. Cambiar `x-show="sessionFeedback === 'incorrect'"` a `x-show="sessionFeedback === 'incorrect' || (sessionFeedback === 'correct' && sessionExplanationRevealed)"`. El `@click="sessionAdvance"` NO cambia. Repetir en los 3 bloques (MC ~L376, word-buttons ~L445, match ~L527). NO tocar el botón Siguiente de canción (@click="songAdvance", ~L639).

En styles.css: añadir estilo discreto para `.session-why` (Pico CSS por defecto da botón sólido; hacerlo discreto — p.ej. variante `.secondary`/`.outline` de Pico, o tamaño/peso reducido coherente con `.session-explanation` muted). Mantener consistencia visual con los botones existentes; no introducir clases utilitarias masivas.

VERIFICAR tras editar: las condiciones de los 3 bloques quedan simétricas; el flujo de fallo sigue mostrando explanation+Siguiente exactamente como antes (la rama `incorrect` sigue presente en cada condición ampliada).
  </action>
  <verify>
    <automated>grep -c "revealSessionExplanation\|sessionExplanationRevealed" index.html</automated>
  </verify>
  <done>Los 3 bloques de session muestran el botón "¿Por qué?" en acierto-con-explanation-no-revelado; la explanation aparece en fallo O acierto-revelado; "Siguiente" aparece en fallo O acierto-revelado; el bloque de canción intacto. `.session-why` estilizado discretamente en styles.css. grep encuentra ≥6 referencias (botón + 2 condiciones ampliadas por bloque × 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test del nuevo comportamiento (patrón source-assert)</name>
  <files>tests/exercise-types.test.js</files>
  <behavior>
    - Test 1: app.js fuente contiene la acción reveal que llama cancelAutoAdvance() y pone sessionExplanationRevealed = true (revelar cancela el auto-avance).
    - Test 2: sessionAdvance resetea sessionExplanationRevealed a false (flag no se arrastra entre ejercicios).
    - Test 3: applyResultToSession usa una constante nombrada de tiempo condicional a explanation en acierto (no el literal 600 hardcodeado para el caso con-explanation); el handler de teclado/acción reveal está gated a sessionFeedback === 'correct' + explanation.
  </behavior>
  <action>
Seguir el patrón EXISTENTE del archivo (ver ~L759, L789, L816, L832): cargar `const src = readFileSync(new URL('../src/screens/app.js', import.meta.url), 'utf8')` y assert sobre el texto fuente con `assert.match`/`includes`. Añadir un `describe('quick-260615-hr0: ver explicación al acertar', ...)` con los 3 tests del bloque behavior. Mantener las aserciones robustas a nombres exactos a discreción (p.ej. buscar `sessionExplanationRevealed` y `cancelAutoAdvance` en proximidad, la constante de tiempo nombrada, y el gate `=== 'correct'`). NO instanciar Alpine. NO tocar el fixture/test del fallo preexistente genero-numero.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>Los 3 tests nuevos pasan. `node --test tests/*.test.js` no muestra fallos NUEVOS respecto al baseline (sigue 1 fallo preexistente ajeno: genero-numero 12→13).</done>
</task>

</tasks>

<verification>
- Baseline previo a editar (registrar): `node --test tests/*.test.js 2>&1 | grep -E "# (pass|fail)"` → confirmar el único fallo preexistente es genero-numero (12→13, tarea 260614-hxn). NO arreglarlo.
- Tras los cambios: `node --test tests/*.test.js` no introduce fallos NUEVOS; los 3 tests nuevos pasan.
- Sanidad manual del autor (no bloqueante, fuera del plan): en una sesión, acertar un ejercicio con explanation → aparece "¿Por qué?"; clic o `e` → se detiene el avance y aparece explanation + Siguiente; avanzar → el siguiente ejercicio NO muestra revelado arrastrado; acertar sin explanation → avanza rápido (600ms) sin botón; fallar → comportamiento idéntico al actual; canciones sin cambios.
</verification>

<success_criteria>
- Al acertar con explanation aparece el affordance "¿Por qué?" (botón + tecla) en los 3 tipos; interactuar cancela el auto-avance y revela explanation + Siguiente.
- Auto-avance en acierto: tiempo largo (constante nombrada ~1500ms) solo con explanation; 600ms sin explanation y en canción.
- `sessionExplanationRevealed` se resetea al avanzar e iniciar sesión.
- Flujo de fallo y canciones SIN cambios; estado persistido/scoring/racha SIN tocar.
- `node --test tests/*.test.js` sin fallos nuevos (1 fallo preexistente ajeno se mantiene).
</success_criteria>

<output>
Create `.planning/quick/260615-hr0-ver-la-explicaci-n-del-ejercicio-aunque-/260615-hr0-SUMMARY.md` when done.
</output>
