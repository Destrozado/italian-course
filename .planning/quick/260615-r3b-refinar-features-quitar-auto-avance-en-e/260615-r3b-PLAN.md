---
phase: quick-260615-r3b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/progress.js
  - src/screens/app.js
  - index.html
  - tests/domain-progress.test.js
  - tests/exercise-types.test.js
autonomous: true
requirements:
  - R3B-1
  - R3B-2
  - R3B-3
must_haves:
  truths:
    - "Al acertar un EJERCICIO (repaso/test-completo/examen) NO hay auto-avance: el usuario avanza manualmente con 'Siguiente'."
    - "Al acertar en una CANCIÓN el auto-avance (songAdvance, 600ms) se conserva intacto."
    - "El botón 'Siguiente' es visible en los 3 tipos de ejercicio siempre que sessionFeedback !== null (acierto o fallo)."
    - "El botón '¿Por qué?' sigue apareciendo al acertar con explanation (sin carrera porque ya no hay auto-avance)."
    - "Existe un toggle 'Contrarreloj' (homeExamTimed, default false, NO persistido) encima de la tabla de categorías de home."
    - "El examen de 1 clic desde la tabla (startExamen → _launchExamen) arranca cronometrado si y solo si el toggle está activo."
    - "El contador vecesFallada de categoría sube +1 una vez por sesión/examen fallado por categoría (al cerrar la sesión en applySessionResult), no por ejercicio."
    - "Fallar 2 ejercicios de la misma categoría en una sola sesión incrementa vecesFallada en +1, no +2."
    - "applyImmediateFailure ya NO incrementa vecesFallada; el helper hadProgress se elimina al quedar sin uso."
    - "El contador vecesFallada por canción (completeSong) NO cambia."
    - "node --test tests/*.test.js pasa con el único fallo preexistente ajeno (genero-numero 12→13); 0 fallos nuevos."
  artifacts:
    - path: "src/domain/progress.js"
      provides: "applySessionResult cuenta vecesFallada en FAIL-WINS; applyImmediateFailure sin incremento; hadProgress eliminado"
      contains: "cat.vecesFallada"
    - path: "src/screens/app.js"
      provides: "auto-avance solo en canción; flag homeExamTimed; _launchExamen respeta el toggle"
      contains: "homeExamTimed"
    - path: "index.html"
      provides: "toggle Contrarreloj encima de la tabla; Siguiente con sessionFeedback !== null en los 3 tipos"
      contains: "homeExamTimed"
    - path: "tests/domain-progress.test.js"
      provides: "suite del contador re-escrita a semántica por-sesión"
      contains: "applySessionResult"
    - path: "tests/exercise-types.test.js"
      provides: "tests de auto-avance actualizados (sin auto-avance en acierto de ejercicio)"
      contains: "sessionMode === 'cancion'"
  key_links:
    - from: "src/screens/app.js applyResultToSession (rama acierto)"
      to: "songAdvance (solo cancion)"
      via: "guard sessionMode === 'cancion'"
      pattern: "sessionMode === 'cancion'"
    - from: "src/screens/app.js _launchExamen"
      to: "this.sessionTimed"
      via: "asignación desde homeExamTimed"
      pattern: "sessionTimed = this\\.homeExamTimed"
    - from: "src/domain/progress.js applySessionResult FAIL-WINS"
      to: "cat.vecesFallada"
      via: "incremento por categoría fallada (una vez por sesión)"
      pattern: "vecesFallada = \\(cat\\.vecesFallada"
    - from: "index.html session (3 tipos)"
      to: "sessionAdvance"
      via: "botón Siguiente x-show sessionFeedback !== null"
      pattern: "x-show=\"sessionFeedback !== null\""
---

<objective>
Refinar tres features ya entregadas esta semana, según el UAT del autor (decisiones BLOQUEADAS en CONTEXT.md, canon, no revisar):

1. CAMBIO 1 — Quitar el auto-avance al ACERTAR en ejercicios (repaso/test-completo/examen). Las canciones lo conservan. "Siguiente" pasa a verse siempre que haya feedback.
2. CAMBIO 2 — Toggle "Contrarreloj" (flag runtime homeExamTimed, no persistido) encima de la tabla de categorías, para que el botón "Examen" de 1 clic arranque cronometrado.
3. CAMBIO 3 — Cambiar la semántica del contador vecesFallada de categoría: contar CADA examen/sesión fallada (una vez por categoría por sesión) en applySessionResult, en vez de "al perder progreso real" en applyImmediateFailure.

Purpose: alinear el comportamiento de las 3 features con lo que el autor espera tras probarlas en local. El loop de re-verificación (Core Value) no cambia: fallar sigue reseteando inmediatamente la categoría (cascada D-54 inmediata intacta); lo que cambia es DÓNDE se materializa el +1 del contador y cuándo avanza la UI.

Output: src/domain/progress.js, src/screens/app.js, index.html actualizados + tests de 260615-nzi (contador) y 260615-hr0 (auto-avance) re-escritos a la nueva semántica, con tests nuevos para el +1 por-sesión y la conservación del auto-avance en canción.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260615-r3b-refinar-features-quitar-auto-avance-en-e/260615-r3b-CONTEXT.md

# Estado real verificado del código (no re-explorar — usar tal cual):

## src/domain/progress.js
- `applySessionResult` (L69): bucle por `practicedCategoryIds` (L107). Rama FAIL-WINS en L113-121 (`if (failedCategoryIds.has(catId))` → `cat.status='no-hecha'; cat.clearedExerciseIds=[]; cat.streakDays=0; ...`). `failedCategoryIds` es un `Set` (L87-89) ya disponible en scope. `cat` es el clon por-categoría (L110-111). El bucle corre UNA vez por categoría tocada → incrementar aquí cuenta una vez por categoría por sesión.
- `applyImmediateFailure` (L306): hoy incrementa `vecesFallada` bajo el guard `hadProgress` (L326-341, ver `nextVecesFallada` L329 + `vecesFallada: nextVecesFallada` L338). El reset cascada (status/clearedExerciseIds/streakDays/becameHechaAt/becameDominadaAt/lastPracticedDate) debe CONSERVARSE; solo se retira la pieza del contador.
- `hadProgress` (L369): helper privado. Tras retirar el incremento queda SIN uso en código de producción (grep confirma: solo progress.js L328 + comentarios + tests). Eliminarlo (función + comentarios que lo referencian).

## src/screens/app.js
- Constantes auto-avance (L72-73): `SESSION_AUTO_ADVANCE_MS = 600` (conservar, canción), `SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS = 1500` (eliminar si queda sin uso tras el cambio).
- Estado (L180-181): `pickerTimed: false`, `sessionTimed: false`. Añadir `homeExamTimed: false` junto a ellos (runtime-only, no persistido).
- `applyResultToSession` (L1598): rama acierto en L1614-1631. Hoy: calcula `advance` (songAdvance si cancion, si no sessionAdvance), `hasExplanation`, `autoAdvanceMs` (1500 si explanation, 600 si no) y programa `setTimeout(advance, autoAdvanceMs)` en `sessionAutoAdvanceHandle`. Nueva semántica: SOLO programar auto-avance cuando `sessionMode === 'cancion'` (con `SESSION_AUTO_ADVANCE_MS`, 600ms vía `songAdvance`); en ejercicios NO programar nada (el HTML expone "Siguiente"). Rama de fallo (L1632-1643) intacta.
- `_launchExamen` (L490): L500 `this.sessionTimed = false;` → cambiar a `this.sessionTimed = this.homeExamTimed;`. (startExamen L445 delega en _launchExamen; no tocar startExamen.)
- `revealSessionExplanation` (L1797): conserva su `cancelAutoAdvance()` (no-op inofensivo en ejercicios) — dejar tal cual (discrecional, opción conservadora).
- Canción: `completeSong` (L2414) incrementa su propio `songProgress[songId].vecesFallada` — NO tocar.

## index.html
- Home: button-row cierra en L154; `<figure>` con la tabla abre en L157. El toggle "Contrarreloj" va ENTRE ambos (entre L154 y L157).
- Indicador de categoría existente: L179-180 (`x-show="cat.vecesFallada > 0"`). No tocar.
- Botón "Siguiente" de ejercicios (3 tipos), condición actual `sessionFeedback === 'incorrect' || (sessionFeedback === 'correct' && sessionExplanationRevealed)`:
  - multi-choice: L410
  - word-buttons: L484-485
  - match: L571-572
  Cambiar las 3 a `x-show="sessionFeedback !== null"`.
- Botón "¿Por qué?" (L408-409, L481-482, L568-569): NO tocar (sigue `sessionFeedback === 'correct' && ...explanation && !sessionExplanationRevealed`).
- Canción: botón Siguiente L683-684 usa `sessionFeedback === 'incorrect'` + `songAdvance` — NO tocar.

## Tests (estado verificado)
- tests/domain-progress.test.js:
  - Suite `quick-260615-nzi` (L871-983): TODA basada en `applyImmediateFailure` contando con guard `hadProgress`. Debe re-escribirse a la nueva semántica (contar en `applySessionResult`).
  - Test `idempotencia integral` (L697-757): la "EXCEPCIÓN" de divergencia de vecesFallada (L704-708, L752-756) se invierte — ahora Path A y Path B convergen en vecesFallada=1 (ambos terminan en applySessionResult sobre avere fallada).
  - Imports ya disponibles: `applySessionResult`, `applyImmediateFailure`, `setupHechaState`, `makeContent`, `blankStateV2`.
- tests/exercise-types.test.js suite `quick-260615-hr0` (L1443-1502): el test L1469-1501 asserta `SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS` y que `applyResultToSession` lo usa condicionalmente. Debe actualizarse a la nueva semántica (sin auto-avance en acierto de ejercicio; auto-avance gated a canción).
- tests/screen-canciones.test.js (contador canción), tests/backup.test.js, tests/data-storage.test.js (roundtrip/migración v10): NO tocar — no dependen de la semántica immediate-only del contador de categoría.
- Baseline confirmado: `node --test tests/*.test.js` → 430 pass / 1 fail PREEXISTENTE ajeno (genero-numero 12→13). NO tocar ese fallo; no introducir nuevos.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Contador vecesFallada por-sesión en applySessionResult (CAMBIO 3) + tests de dominio</name>
  <files>src/domain/progress.js, tests/domain-progress.test.js</files>
  <behavior>
    - Fallar una categoría con progreso real en una sesión → applySessionResult sube vecesFallada +1.
    - Fallar 2 ejercicios de la MISMA categoría en una sesión → vecesFallada +1 (no +2): el bucle por practicedCategoryIds corre una vez por categoría y failedCategoryIds es por-sesión.
    - vecesFallada previo se acumula (cat con vecesFallada=2 → sesión fallada → 3).
    - applyImmediateFailure ya NO toca vecesFallada (preserva el valor previo tal cual, sin +1) y conserva el reset cascada completo.
    - hadProgress eliminado; ninguna referencia en código de producción.
    - idempotencia integral (L697-757): applyImmediateFailure+applySessionResult con el mismo fail converge con solo-applySessionResult, incluido vecesFallada=1 (deja de ser "EXCEPCIÓN").
  </behavior>
  <action>
    En src/domain/progress.js: (per CONTEXT CAMBIO 3) en `applySessionResult`, dentro del bucle por categorías (~L107), en la rama FAIL-WINS (~L113-121, `if (failedCategoryIds.has(catId))`), añadir el incremento `cat.vecesFallada = (cat.vecesFallada ?? 0) + 1;` ANTES del reset de campos (el orden no importa porque `cat` es el clon que se persiste; el incremento sobre el valor previo del clon). Cuenta una vez por categoría fallada por sesión.
    Retirar de `applyImmediateFailure` (~L326-341) la pieza del contador: eliminar `const wasProgress = hadProgress(prev);`, eliminar `const nextVecesFallada = ...`, y quitar la línea `vecesFallada: nextVecesFallada` del objeto reset (conservar TODO lo demás del reset cascada: status, clearedExerciseIds, streakDays, becameHechaAt, becameDominadaAt, lastPracticedDate). Actualizar el comentario JSDoc del bloque "Contador vecesFallada" (~L279-296, L317-325) para reflejar que el conteo vive ahora en applySessionResult (FAIL-WINS, una vez por sesión).
    Eliminar el helper privado `hadProgress` (~L360-376) entero (función + bloque JSDoc), tras confirmar 0 usos restantes (grep). NO tocar `completeSong` ni el contador por canción.
    En tests/domain-progress.test.js: re-escribir la suite `quick-260615-nzi` (~L871-983) a la nueva semántica usando `applySessionResult`:
      - "sesión con un fallo en categoría con progreso → vecesFallada sube a 1 y aplica el reset".
      - "fallar 2 ejercicios de la misma categoría en una sesión → vecesFallada +1 (no +2)" (answers con dos ejercicios de avere, ambos correct:false).
      - "categoría sin fallo en la sesión (rama PROMOTION) → vecesFallada no cambia".
      - "vecesFallada previo se acumula: cat con vecesFallada=2 → sesión fallada → 3".
      - "applyImmediateFailure NO toca vecesFallada (preserva el previo)".
      - Reescribir/eliminar los tests que dependían de `hadProgress` (cada-disyunto, idempotencia immediate-only) reinterpretándolos a la nueva semántica (p.ej. idempotencia: dos applySessionResult con el mismo fail → +1 cada uno es semántica por-sesión esperada; documentar que dos sesiones falladas = +2).
      - Actualizar el test `idempotencia integral` (~L697-757): retirar la lógica `stripVF`/divergencia (L704-708, L741-756) y asertar que `finalA` y `finalB` son deepEqual íntegros incluyendo vecesFallada=1 en avere. El reset cascada y exerciseStats/dailyLog siguen idénticos.
    No usar lenguaje de reducción de scope; honrar CAMBIO 3 exactamente.
  </action>
  <verify>
    <automated>node --test tests/domain-progress.test.js 2>&1 | tail -6</automated>
  </verify>
  <done>node --test tests/domain-progress.test.js pasa al 100%; grep -n "hadProgress" src/domain/progress.js devuelve 0 líneas; grep -n "cat.vecesFallada" src/domain/progress.js muestra el incremento en applySessionResult.</done>
</task>

<task type="auto">
  <name>Tarea 2: Quitar auto-avance en ejercicios + flag homeExamTimed (CAMBIOS 1 y 2) en app.js</name>
  <files>src/screens/app.js</files>
  <action>
    (per CONTEXT CAMBIO 1) En `applyResultToSession` (~L1598), rama de acierto (~L1614-1631): sustituir la lógica de auto-avance condicional por un guard de modo. SOLO cuando `this.sessionMode === 'cancion'` programar el auto-avance vía `this.sessionAutoAdvanceHandle = setTimeout(() => this.songAdvance(), SESSION_AUTO_ADVANCE_MS);` (600ms, songAdvance — LINK-04). Cuando NO es canción: no programar ningún setTimeout (el HTML expone "Siguiente" que llamará sessionAdvance manualmente; ya hay un comentario equivalente en la rama de fallo ~L1641-1642 que sirve de espejo). Eliminar las variables `hasExplanation` y `autoAdvanceMs` y el uso de `SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS`. La rama de fallo (~L1632-1643), el push a sessionResults (~L1607-1612), `cancelSessionTimer()` (~L1601) y la persistencia de test-completo (~L1651-1653) quedan INTACTOS.
    Eliminar la constante `SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS` (~L73) ahora sin uso; conservar `SESSION_AUTO_ADVANCE_MS = 600` (~L72) para canción. Actualizar el comentario del bloque de constantes (~L68-71) para reflejar que el auto-avance solo aplica a canciones.
    (per CONTEXT CAMBIO 2) Añadir al estado el flag `homeExamTimed: false` junto a `pickerTimed`/`sessionTimed` (~L180-181), con un breve comentario (runtime-only, NO persistido, espejo de pickerTimed). En `_launchExamen` (~L490), cambiar la línea `this.sessionTimed = false;` (~L500) por `this.sessionTimed = this.homeExamTimed;` y actualizar el comentario adyacente (~L498-499) para reflejar que el examen de 1 clic respeta el toggle. NO tocar `startExamen` (~L445), `startSession` (~L932 `sessionTimed = pickerTimed`), ni `resetSession`.
    `revealSessionExplanation` (~L1797): dejar tal cual (su `cancelAutoAdvance()` es no-op inofensivo en ejercicios — opción conservadora discrecional del CONTEXT).
    NO tocar `completeSong` ni `songAdvance`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/screens/app.js','utf8'); const i=s.indexOf('applyResultToSession(ex, correct, userAnswer) {'); const b=s.slice(i,i+2000); if(/SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS/.test(s)) throw new Error('constante 1500 sigue presente'); if(!/sessionMode === .cancion./.test(b)) throw new Error('falta guard de cancion en auto-avance'); if(!/sessionTimed = this\.homeExamTimed/.test(s)) throw new Error('_launchExamen no respeta homeExamTimed'); if(!/homeExamTimed: false/.test(s)) throw new Error('falta flag homeExamTimed en estado'); console.log('OK app.js');"</automated>
  </verify>
  <done>El bloque de acierto de applyResultToSession solo programa setTimeout dentro del guard sessionMode==='cancion'; SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS eliminado; homeExamTimed:false en el estado; _launchExamen usa sessionTimed = this.homeExamTimed.</done>
</task>

<task type="auto">
  <name>Tarea 3: UI — toggle Contrarreloj en home + Siguiente siempre con feedback + tests de auto-avance</name>
  <files>index.html, tests/exercise-types.test.js</files>
  <action>
    (per CONTEXT CAMBIO 2) En index.html, insertar el toggle "Contrarreloj" ENTRE la button-row (cierra ~L154) y el `<figure>` de la tabla (abre ~L157). Usar un `<label>` Pico CSS discreto con `<input type="checkbox" x-model="homeExamTimed">` y texto "Contrarreloj ⏱" (o "Examen contrarreloj"). Debe quedar visualmente alineado con la estética de la tabla, discreto. x-model enlaza al flag runtime `homeExamTimed`.
    (per CONTEXT CAMBIO 1) Cambiar la condición x-show del botón "Siguiente" en los 3 tipos de ejercicio a `x-show="sessionFeedback !== null"`:
      - multi-choice (~L410)
      - word-buttons (~L484-485)
      - match (~L571-572)
    NO tocar el botón "¿Por qué?" (~L408-409, L481-482, L568-569) ni la pantalla cancion (botón Siguiente ~L683-684 con sessionFeedback === 'incorrect' + songAdvance).
    En tests/exercise-types.test.js, actualizar la suite `quick-260615-hr0` (~L1443-1502) a la nueva semántica:
      - El test L1469-1501 ya no puede asertar `SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS` ni el auto-avance condicional. Reescribirlo para verificar (source-assert sobre app.js) que en la rama de acierto de applyResultToSession el `setTimeout`/`songAdvance` está gated a `sessionMode === 'cancion'` (canción conserva auto-avance; ejercicios no programan auto-avance). Mantener las aserciones del atajo de tecla `e` y del guard de revealSessionExplanation (siguen vigentes).
      - Conservar los tests de `revealSessionExplanation` (L1446-1459) y `sessionAdvance` reset (L1461-1467) — siguen válidos.
      - Añadir un test source-assert sobre index.html: el botón Siguiente de los 3 tipos usa `x-show="sessionFeedback !== null"`, y la pantalla cancion conserva `sessionFeedback === 'incorrect'` + `songAdvance`.
      - Añadir un test source-assert: el toggle Contrarreloj con `x-model="homeExamTimed"` existe en index.html.
    No usar lenguaje de reducción de scope.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -6</automated>
  </verify>
  <done>node --test tests/*.test.js → solo el fallo preexistente ajeno (genero-numero 12→13), 0 fallos nuevos; grep cuenta 3 ocurrencias de `x-show="sessionFeedback !== null"` para Siguiente y 1 toggle con `x-model="homeExamTimed"` en index.html.</done>
</task>

</tasks>

<verification>
- `node --test tests/*.test.js` (glob obligatorio Node 22.20) pasa con un único fallo PREEXISTENTE ajeno (genero-numero 12→13). 0 fallos nuevos.
- grep -n "hadProgress" src/domain/progress.js → 0 líneas (helper eliminado).
- grep -n "SESSION_AUTO_ADVANCE_WITH_EXPLANATION_MS" src/screens/app.js → 0 líneas (constante eliminada).
- grep -c 'x-show="sessionFeedback !== null"' index.html → ≥3 (los 3 botones Siguiente de ejercicio).
- grep -n 'x-model="homeExamTimed"' index.html → 1 (toggle Contrarreloj).
- El contador de canción (completeSong) y la persistencia/migración v10 quedan sin cambios (tests screen-canciones/backup/data-storage verdes sin tocarlos).
</verification>

<success_criteria>
- CAMBIO 1: al acertar un ejercicio no hay auto-avance; "Siguiente" visible siempre con feedback en los 3 tipos; canción conserva songAdvance (600ms).
- CAMBIO 2: toggle Contrarreloj encima de la tabla; examen 1-clic respeta homeExamTimed (runtime-only).
- CAMBIO 3: vecesFallada de categoría sube +1 una vez por sesión fallada (applySessionResult FAIL-WINS); fallar 2 ejercicios de la misma cat en 1 sesión → +1; applyImmediateFailure sin incremento; hadProgress eliminado; contador de canción intacto.
- Tests actualizados (nzi + hr0) + nuevos (+1 por-sesión, no-doble-en-sesión, auto-avance solo en canción, toggle presente). Suite verde salvo el fallo preexistente ajeno.
</success_criteria>

<output>
Create `.planning/quick/260615-r3b-refinar-features-quitar-auto-avance-en-e/260615-r3b-SUMMARY.md` when done
</output>
