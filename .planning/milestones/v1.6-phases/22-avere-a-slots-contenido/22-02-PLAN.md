---
phase: 22-avere-a-slots-contenido
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - content/exercises/avere.json
  - scripts/.avere-prefix-snapshot.json
  - .planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md
autonomous: false
requirements: [AVE-02]
must_haves:
  truths:
    - "Cada variante NUEVA y cada slot NUEVO autorado pasa el quorum cross-vendor R1-R7 (Gemini + DeepSeek via validate-ai-pass + Claude Opus+Sonnet via skill gsd-validate-exercise) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot"
    - "Las celdas pobres de presente indicativo (ho/hai/ha/abbiamo/avete/hanno con 1 variante tras 22-01) quedan engordadas a >=2 variantes donde la regla admite reformulacion natural (D-19-05), con misma persona/forma y otro sujeto/contexto A1"
    - "Los huecos de regla detectados (mas idiomatismos de sensacion: avere sete/freddo/sonno/ragione/anni; mas verbos de passato prossimo con avere) se anaden como variantes/slots nuevos SOLO donde existe una construccion A1 natural verificada"
    - "Las variantes nuevas se autoran SOLO donde la regla admite reformulacion natural; ninguna lleva explanation propia (comparten la del slot); cada slot nuevo lleva validation.status top-level (D-19-09); el blindaje APPEND-ONLY D-88 se re-basa si la integracion mueve los primeros 17 slots"
  artifacts:
    - path: "content/exercises/avere.json"
      provides: "slots/variantes nuevas validated integradas en variants[]"
      contains: "\"variants\""
    - path: ".planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md"
      provides: "set propuesto de superficies nuevas con id temporal + slot destino + superficie + justificacion R1-R7"
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quorum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS donde la regla admite reformulacion natural (AVE-02, D-19-05 arrastrado) priorizando engordar las celdas pobres de presente indicativo que quedaron con 1 variante tras 22-01 (ho/hai/ha/abbiamo/avete/hanno) a >=2 variantes, y anadir variantes/slots NUEVOS para los huecos de regla detectados: mas idiomatismos de sensacion fisica con avere (avere sete, avere freddo, avere sonno, avere ragione, avere anni — la familia "avere + sustantivo de sensacion/estado" que el hispanohablante calca con essere) y mas verbos en el passato prossimo con auxiliar avere (otros transitivos/comunicacion/cognitivos A1). Cada superficie NUEVA pasa el quorum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched, con revision del autor por variante (D-85). Las superficies movidas intactas en 22-01 NO se re-validan.

Purpose: las variantes nuevas materializan el dolor anti-memorizacion (re-hacer un slot puede tocar una superficie distinta). Los idiomatismos de sensacion cierran la familia "avere + estado" del A1 (avere fame/sete/caldo/freddo/sonno/ragione/anni), el error A1 mas frecuente del hispanohablante (calco "sono affamato"/"sono freddo"/"sono 22 anni"). El quorum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazo 8 bugs en Articoli que el human-verify aprobo). R1/R5 criticos aqui: el prompt no debe leak de la respuesta (la forma de avere); las options deben tener 3+ valores distintos. Pitfall de Avere a vigilar: la h muda (ho/hai/ha/hanno con h; avete/abbiamo sin h; hanno != anno); la edad y las sensaciones se TIENEN con avere, no se SON con essere.

Si la integracion de variantes/slots nuevos altera la composicion de los primeros 17 slots del array (insercion/reordenacion), re-basar el blindaje APPEND-ONLY D-88 (`node scripts/snapshot-avere-prefix.mjs` + verificar `assert-avere-prefix-unchanged.mjs` exit 0) como en 22-01 (relax D-178 opcion A). Si los slots nuevos se anaden al final sin mover los primeros 17, el snapshot re-basado en 22-01 sigue valido y no requiere re-snapshot — verificar con el assert de todos modos.

Output: `avere.json` con las variantes nuevas + slots nuevos validated integrados; cada superficie nueva con su quorum verde en `validation.passes[]`; cada slot nuevo con validation.status top-level (D-19-09); blindaje D-88 consistente (assert verde).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/avere.json

<interfaces>
<!-- Pipeline de quorum cross-vendor. Verificado en validate-ai-pass.mjs + skill. NO modificar estos scripts/skill. Mismo flujo ejecutado con exito en Phase 19 (8 superficies) y Phase 20 (6 superficies), 0 excluidas. -->
Quorum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Mitad 1 — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondio; >=2 by distintos para el quorum; status re-derivado por deriveStatus, NO hand-editar status)

Mitad 2 — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus->Sonnet):
  /gsd-validate-exercise <id>
  (claude-opus-4-7 + claude-sonnet-4-6 literales; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio)
  Fallback cuando Task no esta disponible al executor (precedente Phase 19/20, D-19-08): la mitad Claude se corre con `claude -p --model claude-opus-4-7` y `--model claude-sonnet-4-6` headless, 1 proceso por modelo por superficie, con el 09-VALIDATION-PROMPT verbatim y el ejercicio SIN su campo validation (no sesgar). Equivalente funcional 1-por-1 NUNCA batched.

Gate D-17-07: el passes[] final de cada superficie nueva = >=4x "correcta" (gemini + deepseek + opus + sonnet, by distintos), CERO "incorrecta". deriveStatus -> "validated"; cualquier "incorrecta" -> "disputed" (sticky, bloquea, NO entra al slot).

Pitfall: el VALIDATION-PROMPT es legacy-payload-centrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/avere.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quorum, mover la superficie al variants[] del slot (sin re-validacion) y eliminar el id temporal legacy.

Alcance D-19-05 (priorizar celdas pobres, sin cuota rigida):
- Celdas pobres a engordar a >=2 variantes (reformular la MISMA persona/forma con otro sujeto/contexto A1): ho (io + otro objeto poseido), hai (tu + otra pregunta), ha (lui/lei + otro contexto; cuidado de no repetir el matiz edad), abbiamo (noi + otro complemento), avete (voi + otra pregunta), hanno (loro + otro complemento). Verificar que la forma sigue siendo inequivoca para esa persona.
- NO inflar slots cuya regla no admite reformulacion natural. Pragmatico como Articoli/Partitivi.

Huecos de regla (D-19-06, R1/R5 criticos):
- Idiomatismos de sensacion/estado con avere: avere sete, avere freddo, avere sonno, avere ragione, avere anni (edad). -> variantes del slot de sensaciones de 22-01, o slot(s) nuevo(s) si el autor prefiere separar por familia. categoryIds=["avere"]; misma explanation de slot (rule-first: las sensaciones/estados se expresan con avere, no essere). VERIFICAR (R5) que las options tienen 3+ valores distintos y (R1) que el prompt no leak la forma correcta de avere.
- Passato prossimo con avere: otros verbos A1 transitivos/comunicacion/cognitivos (comprare, vedere, leggere, scrivere, sentire) -> variantes del slot de passato de 22-01. VERIFICAR que el verbo pide avere (no essere) en el passato prossimo.
- El planner/autor decide en plan-time/checkpoint que huecos son naturales. Si un hueco no tiene construccion A1 natural, NO se materializa (alcance conservador).

Validation top-level para la gate (D-19-09): si se crea algun slot NUEVO (vs solo engordar slots existentes), ese slot debe portar el bloque `validation` top-level ({status:"validated", passes[]}) ademas del por-variante implicito — el gate VAL_07_STRICT lee `ex.validation?.status` a nivel de slot. Reflejar el quorum cross-vendor de sus variantes en el bloque top-level (NO inventar validacion; elevar la del nivel variante al nivel slot). Esta fue la correccion Rule-2 de 19-03.

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quorum, un scan ASCII/tildes (RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial.
Gloss ES (R7 canon): mantener "(en espanol: ...)" donde desambigue doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de politica (base de aprobacion = Claude Opus+Sonnet).

Blindaje APPEND-ONLY D-88: si la integracion mueve los primeros 17 slots del array (insercion/reordenacion al engordar slots tempranos), re-basar el snapshot (`node scripts/snapshot-avere-prefix.mjs`) y verificar `assert-avere-prefix-unchanged.mjs` exit 0 (relax D-178, igual que 22-01). Anadir las variantes nuevas a slots existentes NO cambia el id/type/categoryIds de los primeros 17 slots, pero SI puede cambiar su variants[] (que el snapshot captura como campo core) -> re-basar es lo seguro. Verificar con el assert en cualquier caso.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (celdas pobres de presente) + huecos de idiomatismos y passato prossimo</name>
  <files>.planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/avere.json (estado tras 22-01 — los slots ya reagrupados, para saber que slots existen, cuales son celdas pobres y que admite variante nueva)
    - .planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md (las celdas pobres marcadas como candidatas + los huecos de regla anotados en 22-01)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints del quorum: 1-por-1, fresh context, NUNCA batched)
    - .planning/milestones/v1.5-phases/19-articoli-a-slots-contenido/19-02-PLAN.md (Task 1 analogo de Articoli) y .planning/milestones/v1.5-phases/20-partitivi-a-slots-contenido/20-02-PLAN.md (Task 1 analogo de Partitivi — formato a espejar)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6 si accesible) o la seccion R1-R7 del 09-VALIDATION-PROMPT
  </read_first>
  <action>
    Proponer un set CONSERVADOR de superficies nuevas (D-19-05). (1) Para cada celda pobre de presente indicativo marcada en 22-01 (ho/hai/ha/abbiamo/avete/hanno): redactar 1 variante nueva (superficie `{prompt con ___, options 3-4, correctIndex}`) que reformule la MISMA persona/forma con otro sujeto/contexto A1 natural y respuesta inequivoca (cuidado de no repetir el matiz edad del slot `ha`). (2) Redactar las variantes/slots NUEVOS de huecos: idiomatismos de sensacion/estado con avere (avere sete, avere freddo, avere sonno, avere ragione, avere anni) — como variantes del slot de sensaciones de 22-01 o slots nuevos si el autor separa por familia; y passato prossimo con avere de otros verbos A1 (comprare, vedere, leggere, scrivere, sentire) — como variantes del slot de passato de 22-01. SOLO donde exista una construccion A1 italiana natural. VERIFICAR explicitamente: (R1) el prompt no leak la forma correcta de avere; (R5) las options tienen 3+ valores distintos; que la edad/sensacion/estado se expresa con avere (no essere) y que el verbo de passato pide avere (no essere). Cumplir R1-R7 (sin leak en prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigue doble-validez). Pasar un scan de acentos/ASCII (tildes RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. Producir `.planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md`: lista de cada superficie nueva con su id temporal (para el quorum aislado), slot destino (existente o nuevo), prompt/options/correctIndex/explanation, y la justificacion de por que la regla admite reformulacion / por que la construccion italiana es correcta. Reportar el conteo de superficies nuevas propuestas (driver del coste de quorum y del count final de 22-03).
  </action>
  <verify>
    <automated>test -f .planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md && grep -ci "avere\|sensaci\|passato\|sete\|freddo\|sonno" .planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 22-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + superficie + explanation
    - Las celdas pobres de presente indicativo (ho/hai/ha/abbiamo/avete/hanno) tienen al menos una variante nueva propuesta cada una (engorde a >=2, D-19-05), con la misma persona/forma y respuesta inequivoca
    - Los huecos de idiomatismos de sensacion/estado (avere sete/freddo/sonno/ragione/anni) y de passato prossimo (otros verbos A1) estan redactados SOLO donde existe construccion A1 natural, con verificacion explicita de que la construccion usa avere (no essere)
    - Ninguna superficie nueva se propone en una regla que no admite reformulacion natural (alcance conservador D-19-05); ningun hueco se materializa sin construccion A1 natural
    - Cada superficie nueva cumple R1 (sin leak de la forma de avere en el prompt) y R5 (options con 3+ valores distintos); cada explanation/prompt pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers
    - El conteo de superficies nuevas propuestas esta reportado
  </acceptance_criteria>
  <done>
    22-VARIANTES-NUEVAS.md lista el set conservador de superficies nuevas (celdas pobres de presente engordadas + idiomatismos de sensacion/estado + passato prossimo de otros verbos A1) con superficies R1-R7-conformes y scan de acentos pasado; conteo reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (22-VARIANTES-NUEVAS.md) — variantes para engordar las celdas pobres de presente indicativo + variantes/slots nuevos de idiomatismos de sensacion/estado (avere sete/freddo/sonno/ragione/anni) + passato prossimo de otros verbos A1, cumpliendo R1-R7 y con scan de acentos. AUN NO validadas por quorum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md`.
    2. Revisa cada variante nueva de celda pobre de presente: la forma de avere es inequivoca para esa persona (ho=io, hai=tu, ha=lui/lei, abbiamo=noi, avete=voi, hanno=loro)? El prompt NO contiene la respuesta (R1)? Las options tienen 3+ valores distintos (R5)?
    3. Revisa los idiomatismos de sensacion/estado: `ho sete`/`ho freddo`/`ho sonno`/`ho ragione`/`ho ... anni` son la construccion italiana A1 correcta con avere (no essere)? La distractora essere (`sono`) refuerza el error tipico del hispanohablante sin volver ambigua la respuesta?
    4. Revisa el passato prossimo: los verbos nuevos (comprare/vedere/leggere/scrivere/sentire) PIDEN avere (no essere) en el passato prossimo? El participio es correcto?
    5. Confirma el alcance: es conservador (no infla con variantes artificiales en reglas que no se reformulan; no materializa huecos sin construccion natural)?
    6. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quorum.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las superficies aprobadas al quorum cross-vendor, o lista las que descartar/reformular.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quorum cross-vendor (1-por-1) e integrarla al slot</name>
  <files>content/exercises/avere.json, scripts/.avere-prefix-snapshot.json</files>
  <read_first>
    - .planning/phases/22-avere-a-slots-contenido/22-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales claude-opus-4-7/claude-sonnet-4-6, 1 commit por ejercicio; fallback `claude -p` headless si no hay Task, D-19-08)
    - content/exercises/avere.json (slots destino donde integrar las superficies tras pasar el quorum)
    - .planning/milestones/v1.5-phases/19-articoli-a-slots-contenido/19-02-PLAN.md (Task 2 analogo de Articoli — el flujo real: 8 superficies, disputed resuelto, validation top-level) y .planning/milestones/v1.5-phases/20-partitivi-a-slots-contenido/20-02-PLAN.md (Task 2 analogo de Partitivi)
    - scripts/snapshot-avere-prefix.mjs y scripts/assert-avere-prefix-unchanged.mjs (re-base del blindaje D-88 si la integracion mueve los primeros 17 slots)
  </read_first>
  <action>
    Para CADA superficie nueva aprobada, ejecutar el ciclo completo de quorum 1-por-1 (NUNCA batched): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/avere.json como entry temporal o tests/fixtures/*.json). (2) Mitad 1 externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write`. (3) Mitad 2 Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus->Sonnet secuencial, 1 spawn); fallback `claude -p --model claude-opus-4-7` / `--model claude-sonnet-4-6` headless con el ejercicio SIN su campo validation si no hay Task (D-19-08). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4x "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` -> la superficie queda disputed y se resuelve (calidad > tokens; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de politica — NO override-atajo; si un externo alucina un acento sobre texto correcto, re-run sobre el MISMO texto, precedente Phase 19) o se descarta — NO entra al slot disputed. (5) Tras pasar el quorum, mover la superficie validada al variants[] del slot destino (existente, para celdas pobres y huecos absorbidos) o crear el slot nuevo `{id, type:"multiple-choice", categoryIds:["avere"], explanation, variants:[...], validation}` (si el autor separa idiomatismos por familia); eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Para cada slot NUEVO, anadir el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum cross-vendor aprobado de sus variantes (D-19-09 — el gate VAL_07_STRICT lo lee a nivel slot; NO inventar validacion, elevar la aprobada). Hacer 1 commit por superficie validada. NUNCA validar mas de una superficie en un mismo contexto/spawn. (6) RE-BASAR el blindaje APPEND-ONLY D-88: tras integrar, ejecutar `node scripts/snapshot-avere-prefix.mjs` y verificar `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (relax D-178 — engordar slots tempranos cambia su variants[], que el snapshot captura; re-basar es lo seguro). Documentar el re-base en el SUMMARY.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs avere content/exercises/avere.json && node scripts/snapshot-avere-prefix.mjs && node scripts/assert-avere-prefix-unchanged.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exits 0
    - Las celdas pobres de presente engordadas tienen >=2 variantes (verificable contando variants[] de los slots ho/hai/ha/abbiamo/avete/hanno en el JSON)
    - Los slots nuevos creados (si los hay) tienen variants[] no vacio y explanation top-level + validation.status=="validated" top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); console.log('slots variants sin validation top-level:', d.exercises.filter(e=>Array.isArray(e.variants)&&e.validation?.status!=='validated').length)"` imprime `0`
    - Cada superficie NUEVA paso el quorum con >=4x "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se valido como legacy)
    - Ningun id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 22-01 NO se re-validaron (# invocaciones de quorum == # superficies nuevas)
    - El blindaje D-88 re-basado pasa: `node scripts/assert-avere-prefix-unchanged.mjs` exits 0
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/avere.json` da 0 matches
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas pasaron el quorum cross-vendor (>=4x correcta) e integradas a sus slots; celdas pobres de presente engordadas a >=2 variantes; idiomatismos de sensacion/estado + passato prossimo de otros verbos integrados; slots nuevos (si los hay) con validation top-level (D-19-09); validateContent verde; sin ids temporales residuales; 1 commit por superficie; blindaje APPEND-ONLY D-88 re-basado (assert verde).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio -> evaluador del quorum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podria intentar dirigir al evaluador |
| JSON de contenido editado a mano -> loader/validator | superficies nuevas redactadas a mano cruzan a validateContent y al render |
| Estado de slots -> blindaje APPEND-ONLY D-88 | engordar slots tempranos cambia su variants[]; el re-base debe ser deliberado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | superficie nueva con respuesta ambigua (doble-validez R7), construccion avere/essere incorrecta o acento incorrecto entra al slot | mitigate | Quorum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); verificacion avere-no-essere en Task 1 + checkpoint para idiomatismos y passato; pre-scan de acentos; gate de >=4x "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-22-06 | Tampering | prompt-injection en el quorum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instruccion; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-22-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes; leak de la forma de avere en el prompt (R1) | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown); verificacion R1 (sin leak) en Task 1 + checkpoint; el smoke de 22-03 re-asserta sobre variants[].prompt + explanation. |
| T-22-08 | Tampering | slot nuevo sin validation.status top-level cuela como `status: absent` en VAL_07_STRICT (regresion de 19-03 Rule-2) | mitigate | Cada slot NUEVO porta el bloque `validation` top-level (status validated + passes[]) reflejando el quorum aprobado de sus variantes (D-19-09); acceptance asserta `slots variants sin validation top-level == 0`. NO se inventa validacion: se eleva la del nivel variante. |
| T-22-09 | Tampering / governance | el blindaje APPEND-ONLY D-88 queda inconsistente tras engordar slots tempranos (variants[] cambiado sin re-base) | mitigate | Re-base deliberado del snapshot (`snapshot-avere-prefix.mjs`) tras integrar; `assert-avere-prefix-unchanged.mjs` exit 0 verificado en el acceptance; documentado en el SUMMARY (relax D-178 opcion A). |
| T-22-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exits 0
- Cada superficie nueva: passes[] con >=4x "correcta", cero "incorrecta" (registrado en SUMMARY)
- Celdas pobres de presente engordadas a >=2 variantes; slots nuevos (si los hay) con validation top-level (D-19-09)
- `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (blindaje D-88 re-basado, consistente)
- `node --test tests/*.test.js` — el count de Avere sigue rojo a proposito hasta 22-03 (el conteo final ya es determinable tras este plan)
</verification>

<success_criteria>
- Variantes nuevas autoradas solo donde la regla admite reformulacion (D-19-05, conservador, priorizando celdas pobres de presente)
- Idiomatismos de sensacion/estado (avere sete/freddo/sonno/ragione/anni) + passato prossimo de otros verbos A1 anadidos SOLO donde existe construccion A1 natural verificada (avere no essere) + validation top-level (D-19-09)
- Cada superficie nueva paso el quorum cross-vendor R1-R7 (>=4x correcta) antes de entrar
- Superficies existentes movidas NO re-validadas
- validateContent verde; blindaje APPEND-ONLY D-88 consistente (assert verde); conteo final de slots determinable para 22-03
</success_criteria>

<output>
Create `.planning/phases/22-avere-a-slots-contenido/22-02-SUMMARY.md` when done
</output>
