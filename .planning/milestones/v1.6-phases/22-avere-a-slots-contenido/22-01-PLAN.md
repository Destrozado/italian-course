---
phase: 22-avere-a-slots-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/avere.json
  - scripts/.avere-prefix-snapshot.json
  - .planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md
autonomous: false
requirements: [AVE-01]
must_haves:
  truths:
    - "Los 23 ejercicios de Avere quedan reagrupados en slots por regla: presente indicativo por persona (ho/hai/ha/abbiamo/avete/hanno como slots por forma, D-19-01), usos idiomaticos de sensacion fisica (avere fame/caldo) como slot de regla, passato prossimo con auxiliar avere (transitivo/comunicacion/actividad-fisica/cognitivo) como UN slot de regla con variantes; explanation a nivel de slot"
    - "Los 2 word-buttons (100/101) quedan como slots-de-1 type:word-buttons y los 3 match (200/201/202) como slots-de-1 type:match sin variantes forzadas (D-19-03), preservando los duplicados textuales de 202 (D-66)"
    - "Los 6 cruces multi-cat (avere-300..305) quedan slots-de-1 con id ESTABLE intacto y sus categoryIds de 2 ids preservados (D-19-04 / D-87); la cascada D-54 sigue con 2 call-sites intactos"
    - "Cada slot tiene una explanation top-level no vacia (merge elegir-la-mas-completa + injertar matices, D-17-05); ninguna variante lleva explanation propia; payload eliminado de todos los ejercicios; ids articoli-only renumerados libremente RESERVANDO el rango avere-300..305"
    - "El blindaje APPEND-ONLY D-88 de avere se re-basa: el snapshot scripts/.avere-prefix-snapshot.json se regenera tras la conversion (la reagrupacion a slots supersede el invariante legacy de ids; relax D-178 documentado en el checkpoint)"
    - "Las superficies existentes movidas intactas a variants[] NO se re-validan; el JSON sigue pasando validateContent"
  artifacts:
    - path: "content/exercises/avere.json"
      provides: "Avere reagrupado a shape slot+variantes (payload eliminado, variants[]+explanation top-level por slot)"
      contains: "\"variants\""
    - path: ".planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md"
      provides: "Mapa id-fuente -> slot revisado por el autor (artefacto de auditoria); cubre los 23 ids 1:1; reserva el rango 300..305"
    - path: "scripts/.avere-prefix-snapshot.json"
      provides: "Snapshot APPEND-ONLY re-basado al estado slot+variantes (D-88/D-178)"
  key_links:
    - from: "content/exercises/avere.json"
      to: "validateContent / normalizeExerciseToSlot"
      via: "shape slot+variantes (payload XOR variants[])"
      pattern: "\"variants\"\\s*:\\s*\\["
    - from: "los 6 cruces avere-300..305"
      to: "cobertura de profesiones / sustantivos-irregulares / preposiciones / genero-numero / verbos-movimiento (clearedExerciseIds + cascada D-54)"
      via: "id estable + categoryIds de 2 ids preservados"
      pattern: "\"avere-30[0-5]\""
---

<objective>
Reagrupar los 23 ejercicios reales de Avere en slots por regla replicando el patron del plan 19-01 (Articoli) y 20-01 (Partitivi). Reglas propias de Avere: presente indicativo por persona (ho 001, hai 002, ha 003, abbiamo 004, avete 005, hanno 006 — un slot por forma siguiendo la granularidad por-forma D-19-01: cada persona es una trampa distinta de conjugacion para el hispanohablante); usos idiomaticos de sensacion fisica (avere fame 007, avere caldo 008 — misma regla "las sensaciones fisicas se expresan con avere, no essere" reformulada -> candidatos a UN slot con variantes); passato prossimo con auxiliar avere (transitivo mangiare 009, comunicacion parlare 010, actividad fisica dormire 011, cognitivo capire 012 — misma regla "los verbos que piden avere en el passato prossimo" reformulada -> UN slot con variantes). Los 2 word-buttons (100 posesion, 101 avere fame) -> slots-de-1 type:word-buttons (D-19-03: no se fuerzan variantes). Los 3 match (200, 201, 202) -> slots-de-1 type:match (D-19-03), preservando los duplicados textuales de 202 (dos `ha`, D-66). Los 6 cruces (avere-300..305) -> slots-de-1 con id ESTABLE + categoryIds de 2 ids (D-19-04 / D-87). Producir la explanation de cada slot via elegir-la-mas-completa + injertar matices (D-17-05). Las superficies existentes se MUEVEN intactas a `variants[]` (cambio de contenedor -> NO re-validacion). Renumeracion de ids articoli-only LIBRE RESERVANDO el rango 300..305 (D-19-04 + esquema semantico D-15-09: `avere-ho`, `avere-hai`, `avere-passato-prossimo`, etc.).

Punto critico de esta fase (no presente en 19/20): el blindaje APPEND-ONLY D-88 (`scripts/.avere-prefix-snapshot.json` + `scripts/assert-avere-prefix-unchanged.mjs`) asserta que los primeros 17 ejercicios (avere-001..012, 100..101, 200..202) conservan sus campos CORE (id, type, categoryIds, prompt, options, correctIndex, pairs, answer, distractors) byte-identicos en sus posiciones 0..16. La reagrupacion a slots renumera ids y reordena -> rompe ese invariante por diseno. La resolucion (aprobada en el checkpoint, relax D-178): el snapshot se RE-BASA al estado slot+variantes tras la conversion (la conversion a slots supersede el invariante legacy de ids; Phase 21 ya reseteo el progreso de avere -> renumeracion libre). El assert NO corre en la suite (`node --test tests/*.test.js`) ni en `run-validation-271.mjs` — es un script standalone — pero el invariante se preserva re-basandolo, no ignorandolo.

Este plan NO autora variantes nuevas (eso es 22-02) ni anade slots de huecos de regla (eso es 22-02). NO toca los counts de tests/scripts (eso es 22-03, que sincroniza contra el conteo REAL final tras 22-02).

Purpose: es la columna vertebral de la fase — convierte el contenido real de Avere al modelo slot+variantes que el motor de Phase 16 ya consume, replicando el patron de Articoli (19-01) y Partitivi (20-01). Primera de las 3 categorias de verbos de v1.6.

Output: `avere.json` reescrito a slot+variantes (sin payload, con variants[]+explanation top-level por slot), validando verde; mapa de reagrupacion auditado por el autor; snapshot APPEND-ONLY re-basado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/avere.json
@content/exercises/articoli.json

<interfaces>
<!-- Shape de slot con variantes que enforza el validator. NO modificar loader ni validator: ya aceptan variants[] desde Phase 15. articoli.json (34 slots, todos validated) y partitivos.json (19 slots) son las referencias AFTER exactas. -->
Shape autorado de un slot con variantes (verificado contra articoli/partitivos):
- `id`: string id de slot (= exercise id). Esquema = Claude's Discretion (D-15-09): renumeracion semantica limpia para slots avere-only (p.ej. `avere-ho`, `avere-hai`, `avere-ha`, `avere-abbiamo`, `avere-avete`, `avere-hanno`, `avere-sensazioni`, `avere-passato-prossimo`, `avere-match-persone`, `avere-wb-posesion`); RESERVAR/excluir el rango `avere-300..305` para no colisionar con los cruces (D-19-04). Los 6 cruces CONSERVAN su id legacy intacto (avere-300..305). Unicidad obligatoria.
- `type`: "multiple-choice" | "match" | "word-buttons" a NIVEL de slot (los slots avere-only MC; 200/201/202 match; 100/101 word-buttons; cruces MC).
- `categoryIds`: array no vacio a nivel de slot. `["avere"]` para todos los slots avere-only. Los 6 cruces conservan SUS DOS ids: `["avere","profesiones"]` (300/301), `["avere","sustantivos-irregulares"]` (302), `["avere","preposiciones"]` (303), `["avere","genero-numero"]` (304), `["avere","verbos-movimiento"]` (305).
- `explanation`: string no vacio OBLIGATORIO a nivel de slot (D-15-02). Merge D-17-05: base = la explanation fuente mas completa/general del grupo + injertar los matices unicos de las descartadas (pitfalls del hispanohablante: la edad se TIENE no se ES con avere [003]; `abbiamo` rompe la raiz av- [004]; la h muda inicial reaparece en hanno y desaparece en avete/abbiamo [005/006]; no confundir hanno con anno [006]; "si lleva qualcosa detras, auxiliar avere" [009]) — NO reescritura total a regla generalizada. Explanations RULE-FIRST (lideran con la regla/disparador, no con un sustantivo concreto), D-19-07.
- `variants`: array NO vacio. Cada variante MC = objeto plano `{prompt (con ___), options (3-4 strings), correctIndex (en rango)}`. Cada variante match = `{prompt, pairs}` (array de 2-tuplas, 3+ valores distintos en columna derecha R3, sin correctIndex). Cada variante word-buttons = `{prompt, answer, distractors}`. La variante NO lleva `explanation` ni `validation`.
- `validation`: top-level `{status, passes[]}` — se preserva del/los ejercicio(s) fuente.

Reglas hard del validator (verificadas):
- `payload` XOR `variants[]` — nunca ambos, nunca ninguno. Al convertir se ELIMINA `payload`.
- `explanation` top-level string no vacio cuando hay `variants[]`.
- `variants[]` no vacio; superficie validada por el SURFACE_VALIDATOR del `type` (MC: prompt con `___`, 3-4 options, correctIndex en rango; match: pairs con 3+ valores distintos en derecha; word-buttons: answer no vacio).
- Apostrofes ASCII U+0027 si aparecen; sin smart-quotes (el smoke FALLA con `'`).

Blindaje APPEND-ONLY D-88 (CRITICO para Avere — no aplicaba a Articoli/Partitivi):
- `scripts/assert-avere-prefix-unchanged.mjs` compara los primeros 17 ejercicios de avere.json contra `scripts/.avere-prefix-snapshot.json` (campos CORE, excluyendo payload.explanation/notes/validation que son aditivos D-178/D-VAL-08). La conversion a slots renumera + reordena -> FALLARIA.
- El assert NO esta en la suite (`grep` confirma: solo un comentario en slot-variants-integration.test.js; no hay execSync/child_process que lo invoque) ni en run-validation-271.mjs.
- Resolucion (relax D-178, aprobada en el checkpoint): RE-BASAR el snapshot al estado slot+variantes ejecutando `node scripts/snapshot-avere-prefix.mjs` DESPUES de reescribir el JSON, de modo que `assert-avere-prefix-unchanged.mjs` vuelva a pasar (exit 0) contra el nuevo ground truth. El snapshot captura los primeros 17 elementos del array nuevo (que tras la conversion son slots, no ejercicios legacy); su valor protector pasa a ser "estos 17 primeros slots no cambian silenciosamente en el futuro". NO se ignora el invariante — se re-basa una sola vez como parte de la conversion documentada.

Mapa de reagrupacion PROPUESTO (verificado contra los `notes`/options/correctIndex de los 23 ids; el planner lo deriva, el autor lo refina en el checkpoint):

PRESENTE INDICATIVO POR PERSONA (D-19-01, 1 slot por forma):
- `ho` (io, 1a sing): 001 -> slot-de-1 (celda pobre -> candidata a variante en 22-02)
- `hai` (tu, 2a sing): 002 -> slot-de-1 (celda pobre)
- `ha` (lui/lei, 3a sing): 003 -> slot-de-1 (celda pobre; matiz edad-con-avere)
- `abbiamo` (noi, 1a pl): 004 -> slot-de-1 (celda pobre; matiz raiz av- rota)
- `avete` (voi, 2a pl): 005 -> slot-de-1 (celda pobre; matiz h muda)
- `hanno` (loro, 3a pl): 006 -> slot-de-1 (celda pobre; matiz hanno vs anno)

USOS IDIOMATICOS DE SENSACION FISICA (1 slot de regla):
- `avere fame` 007 + `avere caldo` 008 -> 1 slot con 2 variantes (misma regla: sensaciones fisicas con avere no essere; el autor confirma en el checkpoint si van juntos o separados)

PASSATO PROSSIMO CON AUXILIAR AVERE (1 slot de regla, D-17-01 estilo invariable):
- transitivo (mangiare 009) + comunicacion (parlare 010) + actividad fisica (dormire 011) + cognitivo (capire 012) -> 1 slot con 4 variantes (misma regla: estos verbos piden avere en el passato prossimo). NOTA: 009 tiene un pase `disputed->override autor` (C5-leak falso-positivo); su validation top-level se preserva tal cual (no re-validar; documentar el criterio de merge de validation en el SUMMARY).

WORD-BUTTONS (D-19-03, 2 slots-de-1 type:word-buttons):
- 100 (posesion simple) -> slot-de-1 type:word-buttons
- 101 (avere fame idiomatico) -> slot-de-1 type:word-buttons

MATCH (D-19-03, 3 slots-de-1 type:match):
- 200 (io/tu/lui/noi <-> formas) -> slot-de-1 type:match
- 201 (voi/loro/io/lei <-> formas) -> slot-de-1 type:match
- 202 (lui/lei/noi/voi <-> formas, DUPLICADOS textuales 'ha' D-66) -> slot-de-1 type:match (preservar los dos `ha` verbatim)

CRUCES (D-19-04 / D-87, id ESTABLE):
- 300 (avere+profesiones, medico) / 301 (avere+profesiones, avvocata) / 302 (avere+sustantivos-irregulares, braccia) / 303 (avere+preposiciones, a Milano) / 304 (avere+genero-numero, figli/figlia) / 305 (avere+verbos-movimiento, e andata/ha visto) -> 6 slots-de-1, ids intactos, categoryIds de 2 ids intactos.

Este mapa propuesto da ~19 slots (6 presente + 1 sensaciones + 1 passato + 2 wb + 3 match + 6 cruces = 19), sin contar variantes nuevas ni huecos de 22-02. El conteo final lo determina el autor en el checkpoint + 22-02. El conteo se REPORTA, NO se hardcodea en tests aqui (eso es 22-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Producir el mapa de reagrupacion auditable (id-fuente -> slot)</name>
  <files>.planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md</files>
  <read_first>
    - content/exercises/avere.json (integro — los 23 ejercicios con su campo `notes` que declara la regla/persona/uso de cada uno, su `payload.explanation`, su `payload.correctIndex`, sus `payload.pairs` (para 200/201/202, incluido el duplicado 'ha' de 202 D-66), sus `payload.answer`/`distractors` (para 100/101), sus `categoryIds` de 2 ids (300..305) y su `validation.passes[]` (incluido el disputed->override de 009))
    - .planning/milestones/v1.5-phases/19-articoli-a-slots-contenido/19-01-PLAN.md (el plan analogo de Articoli que esta fase replica — Task 1) y .planning/milestones/v1.5-phases/20-partitivi-a-slots-contenido/20-01-PLAN.md (el segundo analogo — Partitivi, 0 cruces vs aqui 6 cruces; aqui los cruces SI existen como en Articoli)
    - scripts/assert-avere-prefix-unchanged.mjs (el blindaje D-88/D-178 — entender que campos core protege y por que la conversion a slots lo rompe; la cabecera documenta el relax D-178 opcion A)
    - scripts/.avere-prefix-snapshot.json (el ground truth actual — 17 ejercicios legacy; se re-basa en Task 2)
  </read_first>
  <action>
    Crear `.planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md` como artefacto de auditoria: una tabla por cada slot resultante con columnas (slot-id propuesto, regla/persona/uso, ids-fuente que entran como variantes, type [MC|match|word-buttons], categoryIds, explanation-base elegida + matices a injertar D-17-05, celda pobre candidata a variante nueva en 22-02?). Aplicar el mapa propuesto en <interfaces>: presente indicativo por persona (001..006 como 6 slots-de-1 por forma, D-19-01); usos idiomaticos de sensacion fisica (007 fame + 008 caldo, proponer como 1 slot de regla con 2 variantes — anotar para confirmacion del autor si van juntos o separados); passato prossimo con auxiliar avere (009..012 como 1 slot con 4 variantes, D-17-01; preservar el validation top-level de 009 con su disputed->override tal cual); 100/101 word-buttons slots-de-1 (D-19-03); 200/201/202 match slots-de-1 (D-19-03, preservar duplicados 'ha' de 202 D-66); cruces 300..305 slots-de-1 con id ESTABLE + categoryIds de 2 ids (D-19-04 / D-87). Para cada slot avere-only proponer un id semantico limpio RESERVANDO el rango 300..305 (D-15-09). Para cada slot con varias variantes (sensaciones, passato), comparar las explanations fuente y registrar cual es la base (la mas completa/general) y que matices unicos de las descartadas se injertan (incluir las pitfalls del hispanohablante listadas en <interfaces>). Marcar explicitamente las celdas pobres (los 6 slots de presente indicativo con 1 variante) como candidatas a engordar en 22-02 (D-19-05) y anotar los huecos de regla a evaluar en 22-02 (mas variantes de passato prossimo con otros verbos; mas idiomatismos de sensacion: avere sete/freddo/sonno/ragione/anni — D-19-06). Documentar EXPLICITAMENTE en una seccion del mapa el plan de re-base del blindaje APPEND-ONLY D-88: la conversion a slots rompe el snapshot legacy; el snapshot se regenera en Task 2 con `node scripts/snapshot-avere-prefix.mjs` (relax D-178 opcion A — la conversion a slots supersede el invariante legacy de ids; Phase 21 ya reseteo el progreso). NO autorar variantes nuevas aqui. NO incluir los slots de huecos nuevos (van a 22-02). Reportar el conteo de slots resultante de SOLO esta reagrupacion (sin slots nuevos ni variantes nuevas).
  </action>
  <verify>
    <automated>test -f .planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md && grep -c "avere-" .planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo 22-REAGRUPACION-MAP.md existe con una tabla que cubre los 23 ids fuente (cada id 001-012, 100-101, 200-202, 300-305 aparece exactamente una vez en alguna columna de ids-fuente)
    - El presente indicativo esta por persona (D-19-01): ho (001), hai (002), ha (003), abbiamo (004), avete (005), hanno (006) son 6 slots distintos
    - Los usos idiomaticos de sensacion fisica (007 fame, 008 caldo) estan propuestos como 1 slot de regla con 2 variantes (anotado para confirmacion del autor)
    - El passato prossimo con auxiliar avere (009..012) es UN slot con 4 variantes (D-17-01); el validation top-level de 009 (disputed->override autor) se preserva tal cual, anotado
    - 100/101 marcados como slots-de-1 type:word-buttons; 200/201/202 marcados como slots-de-1 type:match (con los duplicados 'ha' de 202 preservados, D-66)
    - 300..305 marcados como slots-de-1 con id ESTABLE + categoryIds de 2 ids (D-19-04); el rango 300..305 esta RESERVADO/excluido de la renumeracion de slots avere-only
    - Todos los slots avere-only tienen categoryIds=["avere"]; los ids propuestos son unicos y semanticos
    - Cada slot multi-variante (sensaciones, passato) registra explanation-base + matices a injertar (D-17-05, incluidas las pitfalls del hispanohablante); las celdas pobres (6 slots de presente) estan marcadas como candidatas a 22-02 y los huecos de regla anotados
    - Hay una seccion que documenta el plan de re-base del blindaje APPEND-ONLY D-88 (regenerar el snapshot en Task 2 via snapshot-avere-prefix.mjs; relax D-178 opcion A justificado)
    - El conteo de slots de esta reagrupacion esta reportado (sin slots nuevos ni variantes nuevas)
  </acceptance_criteria>
  <done>
    22-REAGRUPACION-MAP.md cubre los 23 ids->slots con presente indicativo por persona, sensaciones fisicas como slot de regla, passato prossimo como 1 slot de 4 variantes, 100/101 word-buttons + 200/201/202 match slots-de-1 (duplicados 'ha' preservados), cruces 300..305 con id estable + 2 cats; ids semanticos libres reservando 300..305; plan de explanation por slot multi-variante con pitfalls; plan de re-base del blindaje D-88 documentado; conteo de slots reportado.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Aprobar el mapa de reagrupacion de Avere (granularidad de slots, agrupacion de sensaciones fisicas y passato prossimo, merges de explanation) Y aprobar el RE-BASE del blindaje APPEND-ONLY D-88, antes de reescribir el JSON</decision>
  <context>
    El mapa (22-REAGRUPACION-MAP.md) determina la estructura final de slots y por tanto el conteo que los tests de 22-03 sincronizan. Decisiones cargadas de esta fase: (1) presente indicativo por persona (D-19-01) — cada forma (ho/hai/ha/abbiamo/avete/hanno) es una trampa distinta de conjugacion para el hispanohablante -> el motor re-verifica cada celda, a costa de 6 celdas pobres con 1 variante hasta engordarlas en 22-02; (2) la agrupacion de los usos idiomaticos de sensacion fisica (007 fame + 008 caldo) en UN slot de regla con 2 variantes vs slots separados — confirmar; (3) el passato prossimo (009..012) como UN slot de regla con 4 variantes — los 4 entrenan "estos verbos piden avere en passato prossimo" reformulado, son intercambiables; (4) RE-BASE del blindaje APPEND-ONLY D-88: la conversion a slots renumera + reordena los primeros 17 ejercicios -> rompe `assert-avere-prefix-unchanged.mjs` contra el snapshot legacy. El relax D-178 opcion A aplicado aqui es regenerar el snapshot al estado slot+variantes (la conversion a slots supersede el invariante legacy de ids; Phase 21 ya reseteo el progreso de avere -> renumeracion libre). El assert NO corre en la suite ni en run-validation-271 — es standalone — pero NO se ignora: se re-basa una sola vez como parte de esta conversion documentada, manteniendo su valor protector futuro sobre los nuevos 17 primeros slots. Ademas, las explanations mergeadas (D-17-05) requieren revision del autor (D-85) para no perder matices (edad con avere, raiz av- rota en abbiamo, h muda, hanno vs anno, "qualcosa detras = avere").
  </context>
  <options>
    <option id="por-persona-plus-rebase">
      <name>Presente por persona + passato 1 slot + RE-BASE del snapshot D-88 (recomendado/locked)</name>
      <pros>Coherente con D-19-01 (cada persona es una trampa distinta -> el motor re-verifica cada forma) y D-17-01 (passato: misma regla reformulada = variantes del mismo slot). El re-base del blindaje preserva el invariante D-88 sobre el nuevo ground truth en vez de ignorarlo; es la unica forma de reconciliar APPEND-ONLY con la conversion a slots (D-178 opcion A). Espeja la linea purista-por-forma de Articoli/Partitivi.</pros>
      <cons>6 celdas pobres (presente por persona con 1 variante) hasta engordarlas en 22-02. El snapshot legacy se descarta como ground truth (intencional).</cons>
    </option>
    <option id="presente-agrupado">
      <name>Un slot unico "presente indicativo" para las 6 personas (sin re-base, o mantener el snapshot legacy)</name>
      <pros>Menos slots.</pros>
      <cons>Contradice D-19-01: mezcla 6 trampas de conjugacion distintas en un slot -> el motor no re-verifica cada forma. Mantener el snapshot legacy es imposible (la conversion lo rompe estructuralmente); ignorarlo silenciosamente viola D-88. Descartado salvo override explicito del autor.</cons>
    </option>
  </options>
  <resume-signal>Confirma la granularidad (presente por persona + passato 1 slot + sensaciones agrupadas o separadas), aprueba el RE-BASE del snapshot D-88 (relax D-178 opcion A), ajusta los slot-ids/merges de explanation y marca las celdas pobres a engordar + huecos de regla a evaluar en 22-02. Escribe "aprobado" cuando el mapa este listo para reescribir el JSON.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reescribir avere.json a slot+variantes segun el mapa aprobado y re-basar el snapshot D-88</name>
  <files>content/exercises/avere.json, scripts/.avere-prefix-snapshot.json</files>
  <read_first>
    - .planning/phases/22-avere-a-slots-contenido/22-REAGRUPACION-MAP.md (el mapa aprobado por el autor — fuente de verdad de la estructura + el plan de re-base del snapshot)
    - content/exercises/avere.json (integro — superficies, explanations, correctIndex, pairs, answer/distractors y validation.passes[] fuente a mover/mergear, incluido el disputed->override de 009)
    - content/exercises/articoli.json (el output canonico de Articoli — shape multi-variante, slot-de-1 type:match real, 6 cruces con id estable + 2 cats; la referencia AFTER exacta a espejar)
    - .planning/milestones/v1.5-phases/19-articoli-a-slots-contenido/19-01-PLAN.md (Task 2 analogo — incluido el manejo de los cruces 300..305 con id+2 cats)
    - scripts/snapshot-avere-prefix.mjs (el regenerador del snapshot — leer su comportamiento: captura los primeros 17 elementos del array) y scripts/assert-avere-prefix-unchanged.mjs (el assert que debe volver a pasar tras el re-base)
  </read_first>
  <action>
    (A) Reescribir `content/exercises/avere.json` segun el mapa aprobado. Por cada slot, emitir `{id, type, categoryIds, explanation, variants:[...], validation}` ELIMINANDO `payload` de cada ejercicio fuente y MOVIENDO su superficie intacta a `variants[]`: para MC `{prompt, options, correctIndex}`; para match (200/201/202) `{prompt, pairs}` (sin correctIndex; preservar los dos `ha` de 202 verbatim, D-66); para word-buttons (100/101) `{prompt, answer, distractors}`. La `explanation` de cada slot multi-variante (sensaciones, passato) = la base elegida en el mapa + los matices injertados (D-17-05); para slots-de-1 la explanation existente sube a top-level tal cual; rule-first (D-19-07). Mover el texto intacto NO requiere re-validacion (cambio de contenedor); si al mergear/reformular cambia la SUPERFICIE de algun prompt/options existente, eso cuenta como variante nueva -> quorum en 22-02 (NO re-validar aqui; documentar en el mapa/SUMMARY cualquier superficie tocada). Para los 6 cruces (300..305): conservar id, type, categoryIds de 2 ids intactos; convertir su payload a un unico variant + explanation top-level (slot-de-1). Para los slots avere-only usar el id semantico del mapa RESERVANDO 300..305. Todos los slots avere-only con categoryIds=["avere"]. Preservar el campo `validation` top-level (de la fusion: combinar los passes[] o conservar el del ejercicio cuya superficie/explanation domina — para el slot de passato, preservar el validation de 009 con su disputed->override autor tal cual; registrar el criterio en el SUMMARY). Aplicar un scan de acentos sobre las superficies/explanations MOVIDAS (leccion Phase 19 WR-01/WR-02: el legacy puede arrastrar tildes ausentes; corregir defectos legacy de acento — es correccion ortografica, no cambio de superficie). Apostrofes ASCII U+0027 si aparecen; sin smart-quotes. NO autorar variantes nuevas. NO anadir slots de huecos. NO tocar counts de tests/scripts.
    (B) RE-BASAR el blindaje APPEND-ONLY D-88: tras reescribir el JSON, ejecutar `node scripts/snapshot-avere-prefix.mjs` para regenerar `scripts/.avere-prefix-snapshot.json` capturando los primeros 17 slots del array nuevo (relax D-178 opcion A — la conversion a slots supersede el invariante legacy de ids). Verificar que `node scripts/assert-avere-prefix-unchanged.mjs` vuelve a exit 0 contra el nuevo ground truth. Documentar en el SUMMARY que el snapshot se re-baso (con el commit) y por que (la conversion a slots rompe el invariante legacy de ids; el invariante se preserva sobre el nuevo estado, no se ignora).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs avere content/exercises/avere.json && node scripts/snapshot-avere-prefix.mjs && node scripts/assert-avere-prefix-unchanged.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exits 0
    - Ningun ejercicio conserva payload: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Todo slot con variants[] tiene explanation top-level no vacia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); console.log('slots sin explanation:', d.exercises.filter(e=>Array.isArray(e.variants)&&(!e.explanation||!e.explanation.trim())).length)"` imprime `0`
    - Ninguna variante lleva explanation propia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); console.log('variantes con explanation:', d.exercises.flatMap(e=>e.variants||[]).filter(v=>v.explanation!==undefined).length)"` imprime `0`
    - Los 6 cruces conservan id + 2 categoryIds: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); const c=d.exercises.filter(e=>/^avere-30[0-5]$/.test(e.id)); console.log('cruces:', c.length, 'con 2 cats:', c.filter(e=>e.categoryIds.length===2).length)"` imprime `cruces: 6 con 2 cats: 6`
    - Los 3 slots match y los 2 word-buttons existen: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); console.log('match:', d.exercises.filter(e=>e.type==='match').length, 'wb:', d.exercises.filter(e=>e.type==='word-buttons').length)"` imprime `match: 3 wb: 2`
    - Los slots avere-only tienen categoryIds=["avere"]: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); console.log('avere-only mal-cat:', d.exercises.filter(e=>!/^avere-30[0-5]$/.test(e.id)&&!(e.categoryIds.length===1&&e.categoryIds[0]==='avere')).length)"` imprime `0`
    - Ningun id de slot avere-only colisiona con 300..305 (rango reservado)
    - El blindaje D-88 re-basado pasa: `node scripts/assert-avere-prefix-unchanged.mjs` exits 0 contra el snapshot regenerado
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/avere.json` da 0 matches
  </acceptance_criteria>
  <done>
    avere.json reescrito a slot+variantes (sin payload, variants[]+explanation top-level por slot, variantes sin explanation propia; presente por persona; sensaciones y passato como slots de regla con variantes; 100/101 word-buttons + 200/201/202 match slots-de-1 con duplicados 'ha' preservados; cruces 300..305 con id+2 cats intactos; ids semanticos libres con categoryIds=["avere"]), validateContent verde, scan de acentos aplicado a superficies movidas, sin smart-quotes; blindaje APPEND-ONLY D-88 re-basado (snapshot regenerado, assert verde).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano -> loader/validator al boot | El `avere.json` reescrito a mano cruza a validateContent y al render; explanation/prompt se renderizan como texto |
| Estado de slots -> blindaje APPEND-ONLY D-88 | El snapshot de los 17 primeros protege contra cambios silenciosos; el re-base debe ser deliberado y documentado, no un by-pass accidental |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering / data-integrity | reagrupacion que pierde/duplica una superficie fuente, o renumera un cruce rompiendo su cobertura multi-cat | mitigate | El mapa auditado (Task 1 + checkpoint) garantiza los 23 ids fuente mapeados 1:1; el acceptance asserta cruces=6 con id estable + 2 categoryIds (renumerarlos haria stale clearedExerciseIds de profesiones/sustantivos-irregulares/preposiciones/genero-numero/verbos-movimiento + romperia la cascada D-54), match=3, wb=2, payload==0. Rango 300..305 reservado evita colision. |
| T-22-02 | Tampering | reagrupacion pierde un matiz de explanation al mergear (D-17-05) | mitigate | Merge elegir-la-mas-completa + injertar matices (no reescritura), incluidas las pitfalls del hispanohablante (edad con avere, raiz av- rota, h muda, hanno vs anno); revision del autor en el checkpoint (D-85); acceptance asserta cobertura de explanation por slot (slots sin explanation == 0). |
| T-22-03 | Tampering | explanation/prompt con HTML/markdown/smart-quotes; defecto de acento legacy arrastrado (Phase 19 WR-01/02) | mitigate | Acceptance asserta 0 smart-quotes; scan de acentos sobre superficies MOVIDAS tambien (no solo nuevas); el smoke (22-03) re-asserta markdown/HTML sobre variants[].prompt + explanation. |
| T-22-04 | Tampering | malformed JSON crashea el loader | mitigate | `validateContent` (existente, no modificado) rechaza con banner visible slot sin variantes / variante sin superficie valida / explanation ausente. El acceptance corre el validador. |
| T-22-09 | Tampering / governance | el blindaje APPEND-ONLY D-88 se rompe silenciosamente o se by-pasea sin re-base, perdiendo su valor protector futuro | mitigate | El re-base es DELIBERADO y aprobado en el checkpoint (relax D-178 opcion A documentado); se ejecuta `snapshot-avere-prefix.mjs` para regenerar el ground truth y `assert-avere-prefix-unchanged.mjs` vuelve a exit 0; el SUMMARY registra el re-base con el commit. El invariante se preserva sobre el nuevo estado (los 17 primeros slots), no se ignora. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que instalar/auditar (proyecto estatico offline). |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exits 0
- `node scripts/snapshot-avere-prefix.mjs` regenera el snapshot; `node scripts/assert-avere-prefix-unchanged.mjs` exits 0 (blindaje re-basado)
- `node --test tests/*.test.js` — nota: el count de Avere (23 hardcoded en 3 sitios) FALLARA tras esta reescritura porque ahora hay menos slots; esto es ESPERADO y se arregla en 22-03. Documentar el rojo esperado en el SUMMARY; NO arreglar los counts aqui.
- El mapa 22-REAGRUPACION-MAP.md aprobado por el autor (checkpoint), incluido el plan de re-base del blindaje
</verification>

<success_criteria>
- 23 ejercicios fuente reagrupados a slots por regla: presente por persona (D-19-01); sensaciones fisicas como slot de regla; passato prossimo como 1 slot de 4 variantes (D-17-01); 100/101 word-buttons + 200/201/202 match slots-de-1 (D-19-03, duplicados 'ha' preservados); cruces 300..305 id estable + 2 cats (D-19-04 / D-87); ids semanticos libres reservando 300..305 (D-15-09), categoryIds=["avere"] para avere-only
- Cada slot: explanation top-level no vacia (merge D-17-05, pitfalls injertadas), variantes sin explanation propia, payload eliminado
- validateContent verde; sin smart-quotes; scan de acentos aplicado a superficies movidas
- Blindaje APPEND-ONLY D-88 re-basado deliberadamente (snapshot regenerado, assert verde, documentado en el SUMMARY)
- (Los counts de tests quedan rojos a proposito hasta 22-03)
</success_criteria>

<output>
Create `.planning/phases/22-avere-a-slots-contenido/22-01-SUMMARY.md` when done
</output>
