---
phase: 24-verbi-di-movimento-a-slots-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/verbos-movimiento.json
  - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md
autonomous: false
requirements: [MOV-01]
must_haves:
  truths:
    - "Los 37 ejercicios de Verbi di movimento quedan reagrupados en slots por la REGLA DE AUXILIAR (D-24-01, NO por persona ni por verbo): un slot movimiento->essere que agrupa las superficies de seleccion de auxiliar essere de los verbos de movimiento (andare/venire/partire/tornare/uscire/entrare en distintas personas) como variantes intercambiables (D-24-02, pocos slots por sub-regla)"
    - "La concordancia del participio (andato/andata/andati/andate y otros) queda en UN solo slot con sus formas como variantes (D-24-03, DIVERGENCIA DELIBERADA vs Essere D-23-03: aqui NO se separa en 4 slots — la concordancia ya se drillea forma-por-forma en Essere; el drilling fuerte de esta categoria se reserva al auxiliar); la explanation del slot cubre la matriz genero x numero con varios verbos"
    - "Las excepciones que piden avere (viaggiare/nuotare/camminare/ballare — movimiento SIN destino concreto -> avere, participio invariable) quedan en UN slot dedicado excepciones->avere con esas superficies como variantes (D-24-04)"
    - "correre queda en un slot PROPIO separado del de excepciones puras (D-24-04): contrasta correre con destino (Marco e corso a casa -> essere) vs sin destino (ho corso al parco -> avere); la explanation pregunta hay un destino? con destino essere, sin destino avere"
    - "Los 3 word-buttons (verbos-movimiento-100/101/102) quedan como slots-de-1 type:word-buttons sin variantes forzadas (D-24-12); Verbi di movimento NO tiene match en el set legacy y no se inventa"
    - "Cada slot tiene una explanation top-level no vacia (merge elegir-la-mas-completa + injertar matices, D-24-10), rule-first, sin referenciar la categoria Essere por id ni por prosa (D-24-07/D-159 — usar essere como NOMBRE del verbo auxiliar es valido e inevitable); ninguna variante lleva explanation propia; payload eliminado de todos los ejercicios; ids semanticos (verbos-movimiento-essere, verbos-movimiento-excepcioni-avere, verbos-movimiento-correre, verbos-movimiento-concordanza...) — TODOS semanticos, sin cruces cross-cat"
    - "Las superficies existentes movidas intactas a variants[] NO se re-validan (D-24-11, cosmetico); los validation.passes[] se mueven verbatim (incluidos los disputed->override del autor de 011/024/026); el JSON sigue pasando validateContent"
    - "NO existe ni se necesita snapshot/assert APPEND-ONLY para Verbi di movimento (los scripts snapshot-/assert-avere-prefix son avere-only, hardcoded a avere.json); NO se replica la tarea de re-base D-88 de 22-01 (D-24-14)"
  artifacts:
    - path: "content/exercises/verbos-movimiento.json"
      provides: "Verbi di movimento reagrupado a shape slot+variantes (payload eliminado, variants[]+explanation top-level por slot)"
      contains: "\"variants\""
    - path: ".planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md"
      provides: "Mapa id-fuente -> slot revisado por el autor (artefacto de auditoria); cubre los 37 ids 1:1; reparte los pares aux-vs-participio (011/012, 013/014, 015/016) a sus slots; reporta el conteo de slots que fija el sync de 24-03"
  key_links:
    - from: "content/exercises/verbos-movimiento.json"
      to: "validateContent / normalizeExerciseToSlot"
      via: "shape slot+variantes (payload XOR variants[])"
      pattern: "\"variants\"\\s*:\\s*\\["
    - from: "el slot concordanza (UN solo slot, D-24-03)"
      to: "el slot movimiento->essere y el slot excepcioni->avere (slots distintos por regla)"
      via: "reparto de las superficies de los pares aux-vs-participio: la superficie elige-auxiliar va a movimiento->essere, la superficie elige-participio va a concordanza"
      pattern: "verbos-movimiento-concordanza"
---

<objective>
Reagrupar los 37 ejercicios reales de Verbi di movimento en slots POR LA REGLA DE AUXILIAR (D-24-01) replicando el patron del plan 23-01 (Essere) / 22-01 (Avere), pero con el eje organizador propio de esta categoria. La trampa A1 que entrena Verbi di movimento es la DECISION essere-vs-avere en passato prossimo (el calco "he ido -> ho andato"), NO la conjugacion de persona (aqui es passato prossimo, no presente indicativo -> NO aplica el precedente "1 slot por persona" de Avere/Essere). Reglas propias:

1. SLOT `verbos-movimiento-essere` (sub-regla mayoritaria, D-24-02): la seleccion del auxiliar essere para los verbos de movimiento (andare/venire/partire/tornare/uscire/entrare en distintas personas). Las distintas personas/verbos son VARIANTES INTERCAMBIABLES del mismo slot (la decision ho/sono es la misma regla; el examen elige 1 al azar -> mata la memorizacion por palabra). Superficies fuente candidatas (las que piden ELEGIR EL AUXILIAR): 001 (io sono andato), 002 (tu sei venuto), 003 (lui e partito), 004 (noi siamo tornati), 005 (voi siete entrati), 006 (loro sono usciti), 011 (Maria e andata — elige aux), 015 (Io donna sono tornata — elige aux), 024 (Anna e venuta — elige aux), 027 (io sono venuto), 033 (loro sono usciti). El autor afina el reparto en el checkpoint.

2. SLOT `verbos-movimiento-concordanza` (D-24-03, UN solo slot con las 4 formas como variantes): la concordancia del participio con essere (andato/andata/andati/andate, y otros participios uscito/a/i/e, tornato/a/i/e, entrato/a/i/e, venuto/a/i/e). DIVERGENCIA DELIBERADA vs Essere D-23-03 (donde stato/stata/stati/state SI se separaron en 4 slots): aqui la concordancia se AGRUPA en un solo slot, porque la concordancia del participio con essere ya se drillea forma-por-forma en la categoria Essere; duplicar ese drilling aqui restaria foco a la regla propia (la seleccion del auxiliar). La explanation del slot cubre la matriz genero x numero del participio con essere, con varios verbos (no solo andare) para completar la matriz. Superficies fuente candidatas (las que piden ELEGIR EL PARTICIPIO): 007 (andata fem sing), 008 (andato masc sing), 009 (andati masc pl), 010 (andate fem pl), 012 (andata — elige participio, par de 011), 014 (usciti — elige participio, par de 013), 016 (tornata — elige participio, par de 015), 023 (andate fem pl), 025 (entrati), 028 (uscito masc sing), 029 (tornati masc pl), 034 (entrati sujeto compuesto). El autor afina el reparto en el checkpoint.

3. SLOT `verbos-movimiento-excepcioni-avere` (D-24-04): las excepciones que SIEMPRE piden avere (movimiento sin destino concreto: viaggiare/nuotare/camminare/ballare -> avere + participio invariable). Superficies fuente: 017 (viaggiare abbiamo), 018 (nuotare hai), 019 (camminato ho — elige participio invariable), 020 (ballato hanno), 030 (nuotato avete), 031 (camminato ha). Todas la misma regla.

4. SLOT `verbos-movimiento-correre` (D-24-04, PROPIO, separado de las excepciones puras): correre ALTERNA segun destino. Aislarlo porque NO es una excepcion fija: con destino -> essere; sin destino -> avere. Mezclarlo con las excepciones puras (que SIEMPRE piden avere) difuminaria dos reglas distintas. Superficies fuente: 021 (ho corso al parco — sin destino -> avere), 022 (Marco e corso a casa — con destino -> essere). La explanation pregunta: "hay un destino? Con destino, essere; sin destino, avere."

5. DISCRIMINACION VERBO+AUXILIAR (032: "Lui e ___ a casa di Maria" -> andato, descarta camminato/ballato/nuotato porque con essere el verbo debe ser de movimiento con destino): el autor decide en el checkpoint si va al slot movimiento->essere (refuerza el reflejo essere-con-destino) o a un slot propio de discriminacion. Recomendacion del planner: a movimiento->essere (es una superficie de seleccion guiada por el auxiliar essere ya dado).

6. WORD-BUTTONS (D-24-12, 3 slots-de-1 type:word-buttons): 100 (Lei e andata al cinema), 101 (Noi abbiamo viaggiato — excepcion avere), 102 (Loro sono usciti). Slots-de-1; NO se fuerzan variantes. Verbi di movimento NO tiene match.

Pares aux-vs-participio (011/012, 013/014, 015/016 — y los implicitos por concordancia): son ejercicios gemelos sobre la MISMA frase que aislan las dos decisiones (auxiliar / participio). Reparto natural (Claude's Discretion, lo concreta el mapa): la superficie "elige auxiliar" va al slot `verbos-movimiento-essere`; la superficie "elige participio" va al slot `verbos-movimiento-concordanza`.

Producir la explanation de cada slot via elegir-la-mas-completa + injertar matices (D-24-10), rule-first (lidera con el disparador/regla, no con un sustantivo). RESTRICCION LOCKED D-24-07/D-159: las explanation NO referencian la categoria Essere por id ni por prosa (cero "ver Essere", cero "essere-0XX"); usar "essere" como NOMBRE del verbo auxiliar SI es valido e inevitable (es LA regla: el auxiliar de los verbos de movimiento es essere). El anti-calco "io ho andato" es el hilo conductor de la explanation del auxiliar. Las superficies existentes se MUEVEN intactas a `variants[]` (cambio de contenedor -> NO re-validacion, D-24-11; los validation.passes[] se mueven verbatim, incluidos los disputed->override del autor de 011/024/026). Ids TODOS semanticos (D-24-09 — NO hay cruces cross-cat en esta categoria, no existen verbos-movimiento-300..305).

DIVERGENCIA CRITICA #1 vs Essere: la concordancia es UN solo slot (D-24-03), NO 4 slots separados. Un mapa que separa andato/andata/andati/andate en 4 slots es un ERROR de replica ciega de Essere; D-24-03 lo prohibe explicitamente.

DIVERGENCIA CRITICA #2 vs Avere: Verbi di movimento NO tiene snapshot/assert APPEND-ONLY (D-24-14). Los scripts `snapshot-avere-prefix.mjs` / `assert-avere-prefix-unchanged.mjs` / `.avere-prefix-snapshot.json` estan hardcodeados a `content/exercises/avere.json` y NO leen verbos-movimiento.json (verificado: 0 refs). NO existe `.verbos-movimiento-prefix-snapshot.json`. Este plan OMITE toda la columna de snapshot/re-base D-88 (igual que Essere). NO ejecutar ningun script de snapshot ni assert de prefijo.

SIMPLIFICACION vs Avere/Essere: Verbi di movimento NO tiene cruces multi-cat (no hay verbos-movimiento-300..305 — el set legacy no los tiene; crearlos seria capacidad nueva fuera de scope, D-24-12/14). TODOS los ids son semanticos y categoryIds=["verbos-movimiento"] en todos los slots. No hay rango reservado.

Este plan NO autora variantes nuevas (eso es 24-02) ni anade slots de huecos de regla (eso es 24-02). NO toca los counts de tests/scripts (eso es 24-03, que sincroniza contra el conteo REAL final tras 24-02).

Purpose: es la columna vertebral de la fase — convierte el contenido real de Verbi di movimento al modelo slot+variantes que el motor de Phase 16 ya consume. Tercera y ULTIMA de las 3 categorias de verbos de v1.6 (Avere Phase 22 + Essere Phase 23 completadas).

Output: `verbos-movimiento.json` reescrito a slot+variantes (sin payload, con variants[]+explanation top-level por slot), validando verde; mapa de reagrupacion auditado por el autor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/verbos-movimiento.json
@content/exercises/essere.json

<interfaces>
<!-- Shape de slot con variantes que enforza el validator. NO modificar loader ni validator: ya aceptan variants[] desde Phase 15. essere.json (26 slots, todos validated, categoria hermana ya convertida en Phase 23) y avere.json (20 slots) son las referencias AFTER exactas. OJO: aqui NO hay cruces 300..305 — NO copiar esa columna de Essere/Avere. -->
Shape autorado de un slot con variantes (verificado contra essere.json/avere.json):
- `id`: string id de slot (= exercise id). Esquema = renumeracion semantica limpia TODOS los slots (D-24-09: NO hay cruces cross-cat -> NO hay rango reservado): p.ej. `verbos-movimiento-essere`, `verbos-movimiento-concordanza`, `verbos-movimiento-excepcioni-avere`, `verbos-movimiento-correre`, `verbos-movimiento-wb-andare`, `verbos-movimiento-wb-viaggiare`, `verbos-movimiento-wb-uscire`. Unicidad obligatoria. Reusar el patron `{categoria}-{regla}` (D-15-09).
- `type`: "multiple-choice" | "word-buttons" a NIVEL de slot (los slots MC; 100/101/102 word-buttons). Verbi di movimento NO tiene match (no se inventa).
- `categoryIds`: array no vacio a nivel de slot. `["verbos-movimiento"]` para TODOS los slots (no hay cruces). [VERIFIED: los 37 ejercicios fuente son categoryId unico verbos-movimiento]
- `explanation`: string no vacio OBLIGATORIO a nivel de slot. Merge D-24-10: base = la explanation fuente mas completa/general del grupo + injertar los matices unicos de las descartadas. RESTRICCION D-24-07/D-159: cero refs a la CATEGORIA Essere (ni "ver Essere" ni "essere-0XX"); "essere" como NOMBRE del verbo auxiliar SI (es la regla). Para el slot del auxiliar -> anti-calco "io ho andato" como hilo. Para el slot de concordancia -> matriz genero x numero del participio con essere, varios verbos. Para excepcioni->avere -> "movimiento sin destino concreto -> avere + participio invariable". Para correre -> "hay un destino? con destino essere, sin destino avere". Explanations RULE-FIRST. El campo `notes` legacy es autor-internal (NO se mueve a variants[]); su matiz pedagogico relevante se injerta en la explanation del slot si aporta (D-24-10).
- `variants`: array NO vacio. Cada variante MC = objeto plano `{prompt (con ___), options (3-4 strings), correctIndex (en rango)}`. Cada variante word-buttons = `{prompt, answer, distractors}`. La variante NO lleva `explanation` ni `validation`.
- `validation`: top-level `{status, passes[]}` — se preserva del/los ejercicio(s) fuente verbatim (D-24-11); incluye los disputed->override del autor de 011/024/026 (C5-leak participio-insinua-essere, override autor mantiene). Para slots multi-variante combinar segun el criterio del mapa (la del ejercicio cuya superficie/explanation domina o la fusion de passes[]); registrar el criterio en el SUMMARY.

Reglas hard del validator (verificadas contra scripts/validate-content-fixture.mjs):
- `payload` XOR `variants[]` — nunca ambos, nunca ninguno. Al convertir se ELIMINA `payload`.
- `explanation` top-level string no vacio cuando hay `variants[]`.
- `variants[]` no vacio; superficie validada por el SURFACE_VALIDATOR del `type` (MC: prompt con `___`, 3-4 options, correctIndex en rango; word-buttons: answer no vacio).
- Apostrofes ASCII U+0027 si aparecen; sin smart-quotes (el smoke FALLA con smart-quotes). El legacy de Verbi di movimento ya usa apostrofes ASCII (l'anno, quest'anno, un'ora) — preservar verbatim.

Mapa de reagrupacion PROPUESTO (verificado contra los `notes`/options/correctIndex de los 37 ids; el planner lo deriva, el autor lo refina en el checkpoint). Prediccion rough (Avere 23->20, Essere 39->26): Verbi di movimento ~7-10 slots de reagrupacion (antes de slots nuevos de 24-02). Eje = REGLA DE AUXILIAR (D-24-01):

SLOT `verbos-movimiento-essere` (seleccion del auxiliar essere, D-24-02 — variantes intercambiables):
- Superficies que piden ELEGIR EL AUXILIAR essere (options son formas de essere/avere; correctIndex apunta a la forma de essere): 001 (io sono), 002 (tu sei), 003 (lui e), 004 (noi siamo), 005 (voi siete), 006 (loro sono), 011 (Maria e — par aux), 015 (Io donna sono — par aux), 024 (Anna e — par aux), 027 (io sono venuto), 033 (loro sono usciti). + posible 032 (discriminacion verbo, e+destino->andato; el autor decide). Distractoras avere (ho/hai/ha/abbiamo/avete/hanno) preservadas — son el contraste que entrena la regla.

SLOT `verbos-movimiento-concordanza` (UN slot, D-24-03 — NO 4 slots):
- Superficies que piden ELEGIR EL PARTICIPIO concordado con essere (options son terminaciones -o/-a/-i/-e del participio): 007 (andata), 008 (andato), 009 (andati), 010 (andate), 012 (andata — par participio de 011), 014 (usciti — par de 013), 016 (tornata — par de 015), 023 (andate), 025 (entrati), 028 (uscito), 029 (tornati), 034 (entrati). La explanation cubre la matriz genero x numero con varios verbos.

SLOT `verbos-movimiento-excepcioni-avere` (D-24-04, excepciones que SIEMPRE piden avere):
- 017 (viaggiare abbiamo), 018 (nuotare hai), 019 (camminato — participio invariable con avere), 020 (ballato hanno), 030 (nuotato avete), 031 (camminato ha). Movimiento sin destino -> avere + participio invariable.

SLOT `verbos-movimiento-correre` (D-24-04, PROPIO):
- 021 (ho corso al parco — sin destino -> avere) + 022 (Marco e corso a casa — con destino -> essere). Alterna segun destino.

WORD-BUTTONS (D-24-12, 3 slots-de-1 type:word-buttons):
- 100 (Lei e andata al cinema) -> `verbos-movimiento-wb-andare`
- 101 (Noi abbiamo viaggiato — excepcion avere) -> `verbos-movimiento-wb-viaggiare`
- 102 (Loro sono usciti) -> `verbos-movimiento-wb-uscire`

Este mapa propuesto da ~9 slots (essere + concordanza + excepcioni-avere + correre + 3 wb = 7; +2 si el autor separa la discriminacion 032 o reparte distinto). El conteo final lo determina el autor en el checkpoint + 24-02 (huecos D-24-06 pueden anadir slots; las variantes no suben el count). El conteo se REPORTA, NO se hardcodea en tests aqui (eso es 24-03, que lee data.exercises.length REAL).

NO cruces: NO hay verbos-movimiento-300..305 en el set legacy (verificado). NO crearlos (capacidad nueva, fuera de scope D-24-12/14). TODOS los ids semanticos, categoryIds=["verbos-movimiento"].
NO snapshot: Verbi di movimento no tiene snapshot/assert APPEND-ONLY (avere-only, verificado: 0 refs). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs. NO buscar ni crear .verbos-movimiento-prefix-snapshot.json (no existe).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Producir el mapa de reagrupacion auditable (id-fuente -> slot) por regla de auxiliar</name>
  <files>.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md</files>
  <read_first>
    - content/exercises/verbos-movimiento.json (integro — los 37 ejercicios con su campo `notes` que declara la regla/persona/uso de cada uno, su `payload.explanation`, su `payload.prompt`/`options`/`correctIndex` (MC), su `payload.answer`/`distractors` (word-buttons 100/101/102) y su `validation.passes[]` incluidos los disputed->override del autor de 011/024/026)
    - .planning/phases/23-essere-a-slots-contenido/23-REAGRUPACION-MAP.md (la plantilla del formato del mapa old-id->slot a espejar cambiando categoria y eje organizador)
    - .planning/phases/23-essere-a-slots-contenido/23-01-PLAN.md (el plan analogo de Essere — Task 1: metodologia de reagrupacion por regla, merge D-23-10, las 2 divergencias; AQUI la concordancia DIVERGE en sentido opuesto: Essere separo el passato en 4 slots, aqui se AGRUPA en 1, D-24-03)
    - content/exercises/essere.json y content/exercises/avere.json (outputs canonicos ya convertidos — shape de slot, variants[], explanation/validation top-level, ids semanticos; OJO: ambos tienen cruces 300..305, AQUI no hay)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-CONTEXT.md (decisiones locked D-24-01..15 + Claude's Discretion sobre el reparto de los pares aux-vs-participio)
  </read_first>
  <action>
    Crear `.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md` como artefacto de auditoria (espejar el formato de 23-REAGRUPACION-MAP.md): una tabla por cada slot resultante con columnas (slot-id propuesto, regla/uso, ids-fuente que entran como variantes, type [MC|word-buttons], categoryIds, explanation-base elegida + matices a injertar D-24-10, celda pobre candidata a variante nueva en 24-02?). Aplicar el mapa propuesto en <interfaces> con el eje REGLA DE AUXILIAR (D-24-01, NO por persona ni por verbo): (1) slot `verbos-movimiento-essere` con las superficies de SELECCION DE AUXILIAR essere de los verbos de movimiento como variantes intercambiables (D-24-02): 001-006, 011 (aux), 015 (aux), 024 (aux), 027, 033, y decidir 032 (discriminacion verbo) — recomendado a este slot; anotar para confirmacion del autor; (2) slot `verbos-movimiento-concordanza` UN solo slot (D-24-03, DIVERGENCIA DELIBERADA vs Essere: NO separar en 4 slots) con las superficies de ELEGIR PARTICIPIO concordado: 007-010, 012 (participio, par de 011), 014 (par de 013), 016 (par de 015), 023, 025, 028, 029, 034; la explanation cubre la matriz genero x numero con varios verbos; (3) slot `verbos-movimiento-excepcioni-avere` (D-24-04): 017/018/019/020/030/031 (viaggiare/nuotare/camminare/ballare -> avere, participio invariable); (4) slot `verbos-movimiento-correre` PROPIO (D-24-04, separado de las excepciones puras): 021 (sin destino->avere) + 022 (con destino->essere), la explanation pregunta "hay un destino?"; (5) 100/101/102 word-buttons slots-de-1 (D-24-12). Verbi di movimento NO tiene match — no inventar. Repartir EXPLICITAMENTE los pares aux-vs-participio (011/012, 013/014, 015/016) segun Claude's Discretion: la superficie "elige auxiliar" (011, 013-aux, 015) al slot essere; la superficie "elige participio" (012, 014, 016) al slot concordanza — verificar contra las options/correctIndex reales de cada id (si las options son formas de essere/avere -> es elige-auxiliar; si son terminaciones de participio -> es elige-participio). Para cada slot proponer un id semantico limpio (D-24-09, TODOS semanticos, sin rango reservado porque no hay cruces). Para cada slot multi-variante comparar las explanations fuente y registrar cual es la base (la mas completa/general) y que matices unicos de las descartadas se injertan (D-24-10), incluido el anti-calco "io ho andato" en el slot del auxiliar y el "hay destino?" en correre; VERIFICAR que ninguna explanation propuesta referencia la CATEGORIA Essere por id ni por prosa (D-24-07/D-159; "essere" como nombre del auxiliar es valido). Marcar las celdas pobres como candidatas a engordar en 24-02 y anotar los 4 ejes de huecos PRIORIZADOS (D-24-06): (a) mas verbos essere — scendere/salire/cadere/rimanere-restare/diventare/nascere/morire — al slot movimiento->essere; (b) mas excepciones avere — passeggiare/sciare/saltare/viaggiare en mas personas — al slot excepcioni->avere; (c) mas test-de-destino — correre/volare/saltare/salire/scendere con y sin destino — al slot correre; (d) matriz de concordancia completa — las 4 formas con varios verbos — al slot concordanza. Anotar que cada verbo asignado a essere o avere debe VERIFICARSE que toma realmente ese auxiliar (no inventar excepciones) — eso lo cierra el quorum de 24-02. Incluir una SECCION EXPLICITA "Snapshot/re-base D-88: NO APLICA a Verbi di movimento" (los scripts snapshot-/assert-avere-prefix son avere-only, hardcoded a avere.json, 0 refs a verbos-movimiento; no existe ni se necesita snapshot — D-24-14) y una SECCION EXPLICITA "Sin cruces multi-cat: no existen verbos-movimiento-300..305; NO crear; todos los ids semanticos, categoryIds=['verbos-movimiento']" para que el ejecutor de Task 2 NO copie esas columnas de Essere/Avere. NO autorar variantes nuevas aqui. NO incluir los slots de huecos nuevos (van a 24-02). Reportar el conteo de slots resultante de SOLO esta reagrupacion (sin slots nuevos ni variantes nuevas).
  </action>
  <verify>
    <automated>test -f .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md && grep -c "verbos-movimiento-" .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo 24-REAGRUPACION-MAP.md existe con una tabla que cubre los 37 ids fuente (cada id 001-034, 100-102 aparece exactamente una vez en alguna columna de ids-fuente)
    - La reagrupacion es POR REGLA DE AUXILIAR (D-24-01): existe un slot `verbos-movimiento-essere` (seleccion de auxiliar essere, variantes intercambiables D-24-02), un slot `verbos-movimiento-excepcioni-avere` (D-24-04), un slot `verbos-movimiento-correre` PROPIO separado del de excepciones (D-24-04) — NO un eje por persona ni por verbo
    - La concordancia esta en UN SOLO slot `verbos-movimiento-concordanza` con las formas como variantes (D-24-03), NO separada en 4 slots — DIVERGENCIA DELIBERADA vs Essere documentada en el mapa
    - El slot correre contiene 021 (sin destino->avere) y 022 (con destino->essere) y su explanation pregunta "hay un destino?" (D-24-04)
    - El slot excepcioni-avere contiene viaggiare/nuotare/camminare/ballare (017/018/019/020/030/031), todas misma regla (movimiento sin destino -> avere, participio invariable)
    - Los pares aux-vs-participio (011/012, 013/014, 015/016) estan repartidos: la superficie elige-auxiliar al slot essere, la superficie elige-participio al slot concordanza (verificado contra las options reales de cada id)
    - 100/101/102 marcados como slots-de-1 type:word-buttons; NO hay ningun slot type:match (Verbi di movimento no tiene match)
    - TODOS los slots tienen categoryIds=["verbos-movimiento"]; los ids propuestos son unicos y semanticos; hay una seccion explicita "sin cruces 300..305 — todos semanticos"
    - Cada slot multi-variante registra explanation-base + matices a injertar (D-24-10); ninguna explanation propuesta referencia la CATEGORIA Essere por id ni por prosa (D-24-07/D-159) — verificado en el mapa; el anti-calco "io ho andato" esta en el slot del auxiliar
    - Las celdas pobres estan marcadas como candidatas a 24-02 y los 4 ejes de huecos PRIORIZADOS (D-24-06: mas verbos essere; mas excepciones avere; mas test-de-destino; matriz concordancia) estan anotados con la nota "verificar el auxiliar real de cada verbo"
    - Hay una seccion EXPLICITA "Snapshot/re-base D-88: NO APLICA a Verbi di movimento" (los scripts son avere-only; no replicar la tarea de re-base de 22-01)
    - El conteo de slots de esta reagrupacion esta reportado (sin slots nuevos ni variantes nuevas)
  </acceptance_criteria>
  <done>
    24-REAGRUPACION-MAP.md cubre los 37 ids->slots por REGLA DE AUXILIAR (D-24-01): slot essere (seleccion de auxiliar, variantes intercambiables), slot concordanza UN solo slot (D-24-03, divergencia vs Essere), slot excepcioni-avere, slot correre PROPIO (D-24-04), 100/101/102 word-buttons slots-de-1 (sin match); pares aux-vs-participio repartidos; ids TODOS semanticos categoryIds=["verbos-movimiento"]; plan de explanation por slot sin refs a la categoria Essere (D-24-07); secciones explicitas "sin cruces" y "snapshot no aplica"; 4 ejes de huecos D-24-06 anotados; conteo de slots reportado.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Aprobar el mapa de reagrupacion de Verbi di movimento (eje por REGLA DE AUXILIAR, reparto de los pares aux-vs-participio, ubicacion de 032, merges de explanation) antes de reescribir el JSON. Confirmar que la concordancia es UN solo slot (D-24-03, NO 4 como Essere) y que NO aplica snapshot ni cruces.</decision>
  <context>
    El mapa (24-REAGRUPACION-MAP.md) determina la estructura final de slots y por tanto el conteo que los tests de 24-03 sincronizan. Decisiones cargadas de esta fase (LOCKED en CONTEXT.md, no se re-litigan, solo se afinan los detalles abiertos): (1) eje organizador = REGLA DE AUXILIAR essere-vs-avere (D-24-01), NO por persona ni por verbo — la trampa A1 es la DECISION del auxiliar, no la conjugacion (aqui es passato prossimo, no presente); las distintas personas/verbos de movimiento->essere son VARIANTES INTERCAMBIABLES del mismo slot (D-24-02); (2) concordancia (andato/a/i/e) = UN SOLO slot con 4 formas como variantes (D-24-03), DIVERGENCIA DELIBERADA vs Essere (que separo stato/stata/stati/state en 4 slots): aqui la concordancia ya se drillea forma-por-forma en Essere -> el drilling fuerte se reserva al auxiliar; (3) correre = slot PROPIO (D-24-04) separado de las excepciones puras (correre alterna por destino; las excepciones SIEMPRE piden avere); (4) Verbi di movimento NO tiene cruces multi-cat (no existen 300..305) ni snapshot APPEND-ONLY (avere-only) -> NO se replican esas columnas; (5) RESTRICCION D-24-07/D-159: las explanation NO referencian la categoria Essere por id ni por prosa (usar "essere" como nombre del auxiliar es valido). Claude's Discretion a afinar: el reparto exacto de los pares aux-vs-participio (011/012, 013/014, 015/016), la ubicacion de 032 (discriminacion verbo+aux), y si la concordancia y el auxiliar se subdividen mas o menos. Las explanations mergeadas (D-24-10) requieren revision del autor (D-85) para no perder matices (anti-calco io ho andato, "hay destino?" de correre, participio invariable con avere).
  </context>
  <options>
    <option id="por-regla-auxiliar-concordanza-1-slot">
      <name>Por regla de auxiliar + concordancia en 1 slot + correre propio + sin snapshot/cruces (recomendado/locked)</name>
      <pros>Coherente con D-24-01 (eje = decision del auxiliar, la trampa A1 propia de la categoria), D-24-02 (personas/verbos como variantes intercambiables del slot essere -> el examen elige 1 al azar, mata la memorizacion), D-24-03 (concordancia agrupada — ya se drillea en Essere, no se duplica) y D-24-04 (correre aislado por alternar segun destino). No replicar snapshot ni cruces evita tareas inexistentes. Espeja el patron de Avere/Essere salvo en las divergencias deliberadas de esta categoria.</pros>
      <cons>El slot del auxiliar y el de concordancia quedan densos (muchas variantes) hasta que 24-02 verifique cuales son superficies intactas vs candidatas. Menos slots totales que Essere/Avere (categoria mas simple, sin cruces).</cons>
    </option>
    <option id="concordancia-4-slots">
      <name>Concordancia en 4 slots separados (replica ciega de Essere D-23-03)</name>
      <pros>Mas drilling de la concordancia.</pros>
      <cons>Contradice D-24-03 EXPLICITAMENTE: la concordancia del participio con essere ya se drillea forma-por-forma en la categoria Essere; duplicarla aqui resta foco a la regla propia de Verbi di movimento (la seleccion del auxiliar). Descartado salvo override explicito del autor.</cons>
    </option>
  </options>
  <resume-signal>Confirma el eje por regla de auxiliar, el reparto de los pares aux-vs-participio (011/012, 013/014, 015/016), la ubicacion de 032, que la concordancia es UN solo slot (D-24-03) y que NO aplican snapshot ni cruces, ajusta los slot-ids/merges de explanation y marca las celdas pobres + los 4 ejes de huecos a evaluar en 24-02. Escribe "aprobado" cuando el mapa este listo para reescribir el JSON.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reescribir verbos-movimiento.json a slot+variantes segun el mapa aprobado</name>
  <files>content/exercises/verbos-movimiento.json</files>
  <read_first>
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md (el mapa aprobado por el autor — fuente de verdad de la estructura)
    - content/exercises/verbos-movimiento.json (integro — superficies, explanations, prompt/options/correctIndex (MC), answer/distractors (100/101/102) y validation.passes[] fuente a mover/mergear, incluidos los disputed->override del autor de 011/024/026)
    - content/exercises/essere.json (el output canonico de Essere — categoria hermana de verbos ya convertida en Phase 23: shape de slot, variants[], explanation/validation top-level, slots-de-1 word-buttons real, agrupacion por regla; OJO: essere TIENE cruces 300..305, AQUI NO — no copiar esa columna)
    - .planning/phases/23-essere-a-slots-contenido/23-01-PLAN.md (Task 2 analogo de Essere — el move de superficies intactas a variants[], el merge de explanations y validation, la preservacion de los disputed->override)
  </read_first>
  <action>
    Reescribir `content/exercises/verbos-movimiento.json` segun el mapa aprobado. Por cada slot, emitir `{id, type, categoryIds, explanation, variants:[...], validation}` ELIMINANDO `payload` de cada ejercicio fuente y MOVIENDO su superficie intacta a `variants[]`: para MC `{prompt, options, correctIndex}`; para word-buttons (100/101/102) `{prompt, answer, distractors}`. La `explanation` de cada slot multi-variante (essere, concordanza, excepcioni-avere, correre) = la base elegida en el mapa + los matices injertados (D-24-10); para slots-de-1 (word-buttons) la explanation existente sube a top-level tal cual; rule-first. Para el slot `verbos-movimiento-essere` la explanation lidera con la regla del auxiliar essere de los verbos de movimiento + el anti-calco "io ho andato" (error A1 mas persistente). Para `verbos-movimiento-concordanza` la explanation cubre la matriz genero x numero del participio con essere (-o masc sing, -a fem sing, -i masc pl, -e fem pl) con varios verbos (D-24-03). Para `verbos-movimiento-excepcioni-avere` la explanation lidera con "movimiento sin destino concreto -> avere + participio invariable" (viaggiare/nuotare/camminare/ballare). Para `verbos-movimiento-correre` la explanation pregunta "hay un destino? con destino essere, sin destino avere" (D-24-04). RESTRICCION D-24-07/D-159: NINGUNA explanation referencia la CATEGORIA Essere por id ni por prosa (cero "ver Essere", cero "essere-0XX"); "essere" como NOMBRE del verbo auxiliar SI (es la regla) — las explanations legacy ya cumplen esto, preservarlo al mergear. Preservar el patron distractoras essere/avere donde aplica — NO eliminar las distractoras de avere/essere existentes en las options (son el contraste que entrena la regla). El campo `notes` legacy es autor-internal: NO se mueve a variants[]; su matiz pedagogico relevante se injerta en la explanation del slot si aporta (D-24-10). Mover el texto intacto NO requiere re-validacion (cambio de contenedor, D-24-11); si al mergear cambia la SUPERFICIE de algun prompt/options existente, eso cuenta como variante nueva -> quorum en 24-02 (NO re-validar aqui; documentar en el mapa/SUMMARY cualquier superficie tocada). Para los slots usar el id semantico del mapa (D-24-09, TODOS semanticos; NO hay rango reservado porque no hay cruces). TODOS los slots con categoryIds=["verbos-movimiento"]. Preservar el campo `validation` top-level moviendo los passes[] verbatim (D-24-11); para slots multi-variante combinar segun el criterio del mapa (la del ejercicio cuya superficie/explanation domina o la fusion de passes[]); preservar los disputed->override del autor de 011/024/026 tal cual (no re-validar); registrar el criterio en el SUMMARY. Aplicar un scan de acentos sobre las superficies/explanations MOVIDAS (leccion Phase 19 WR-01/WR-02: el legacy puede arrastrar tildes ausentes; DeepSeek es estricto en acentos; corregir defectos legacy de acento es correccion ortografica, no cambio de superficie). Apostrofes ASCII U+0027 (el legacy ya usa l'anno/quest'anno/un'ora — preservar); sin smart-quotes. NO autorar variantes nuevas. NO anadir slots de huecos (van a 24-02). NO tocar counts de tests/scripts (van a 24-03). NO crear ningun cruce 300..305 (no existen; fuera de scope). NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Verbi di movimento; avere-only). Documentar en el SUMMARY que "Verbi di movimento no tiene snapshot APPEND-ONLY ni cruces multi-cat — no aplica re-base D-88".
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json` exits 0
    - Ningun ejercicio conserva payload: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Todo slot con variants[] tiene explanation top-level no vacia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('slots sin explanation:', d.exercises.filter(e=>Array.isArray(e.variants)&&(!e.explanation||!e.explanation.trim())).length)"` imprime `0`
    - Ninguna variante lleva explanation propia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('variantes con explanation:', d.exercises.flatMap(e=>e.variants||[]).filter(v=>v.explanation!==undefined).length)"` imprime `0`
    - Existe exactamente UN slot de concordancia (D-24-03, NO 4): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('concordanza slots:', d.exercises.filter(e=>/concordanza|concordancia/.test(e.id)).length)"` imprime `concordanza slots: 1`
    - Existe el slot correre PROPIO con la superficie con-destino (essere) y sin-destino (avere): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); const c=d.exercises.find(e=>/correre/.test(e.id)); console.log('correre ok:', !!c && Array.isArray(c.variants) && c.variants.length>=2)"` imprime `correre ok: true`
    - Existe el slot excepcioni-avere: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('excepcioni-avere:', d.exercises.filter(e=>/excep/.test(e.id)).length)"` imprime al menos `1`
    - Los word-buttons existen y NO hay match: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('wb:', d.exercises.filter(e=>e.type==='word-buttons').length, 'match:', d.exercises.filter(e=>e.type==='match').length)"` imprime `wb: 3 match: 0`
    - TODOS los slots tienen categoryIds=["verbos-movimiento"] (no hay cruces): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('mal-cat:', d.exercises.filter(e=>!(e.categoryIds.length===1&&e.categoryIds[0]==='verbos-movimiento')).length)"` imprime `0`
    - NO existe ningun id verbos-movimiento-30[0-5] (no se crearon cruces): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('cruces:', d.exercises.filter(e=>/-30[0-5]$/.test(e.id)).length)"` imprime `0`
    - Ninguna explanation referencia la categoria Essere por id o prosa: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('refs Essere-cat:', d.exercises.filter(e=>/essere-0\d\d|ver Essere|categor.a Essere/i.test(e.explanation||'')).length)"` imprime `0` (D-24-07/D-159)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/verbos-movimiento.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Verbi di movimento (no existe `.verbos-movimiento-prefix-snapshot.json`)
  </acceptance_criteria>
  <done>
    verbos-movimiento.json reescrito a slot+variantes (sin payload, variants[]+explanation top-level por slot, variantes sin explanation propia; slot essere por regla de auxiliar con anti-calco; concordancia en UN solo slot D-24-03; excepcioni-avere; correre PROPIO con "hay destino?" D-24-04; 100/101/102 word-buttons slots-de-1 sin match; ids TODOS semanticos categoryIds=["verbos-movimiento"], sin cruces; explanations sin refs a la categoria Essere D-24-07; patron distractoras essere/avere preservado; disputed->override 011/024/026 verbatim), validateContent verde, scan de acentos aplicado a superficies movidas, sin smart-quotes; documentado que Verbi di movimento no tiene snapshot ni cruces (no aplica re-base D-88).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano -> loader/validator al boot | El `verbos-movimiento.json` reescrito a mano cruza a validateContent y al render; explanation/prompt se renderizan como texto plano |
| Estado de slots -> motor slot-aware / cascada D-54 | Los ids de slot alimentan clearedExerciseIds + la cascada; renumerar a ids semanticos es seguro porque Phase 21 ya reseteo el progreso de verbos-movimiento y NO hay cruces que desincronicen otras categorias |

> Nota: esta fase es una conversion de contenido JSON hand-authored (sin cruces multi-cat, sin nuevo runtime, motor/sampler/loader intactos) en una app local single-user sin backend; no introduce nueva superficie de ataque (sin red entrante, auth, input de usuario, ni deserializacion de datos no confiables). No se esperan amenazas HIGH. El threat surface realista = JSON malformado/invalido que rompa el loader al boot y reagrupacion que pierda una superficie fuente.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering / data-integrity | reagrupacion que pierde/duplica una superficie fuente, o asigna una superficie elige-participio al slot de auxiliar (o viceversa) rompiendo el eje por regla | mitigate | El mapa auditado (Task 1 + checkpoint) garantiza los 37 ids fuente mapeados 1:1; el reparto de los pares aux-vs-participio se verifica contra las options reales de cada id (formas de essere/avere -> auxiliar; terminaciones de participio -> concordancia); el acceptance asserta wb=3, match=0, payload==0, 1 solo slot de concordancia (D-24-03), correre con >=2 variantes (con/sin destino), categoryIds=["verbos-movimiento"] en todos. |
| T-24-02 | Tampering | reagrupacion pierde un matiz de explanation al mergear (D-24-10), o agrupa la concordancia en >1 slot rompiendo D-24-03 | mitigate | Merge elegir-la-mas-completa + injertar matices (no reescritura); UN solo slot de concordancia asegurado por acceptance (count==1); revision del autor en el checkpoint (D-85); acceptance asserta cobertura de explanation por slot (slots sin explanation == 0). |
| T-24-03 | Tampering | explanation/prompt con HTML/markdown/smart-quotes; defecto de acento legacy arrastrado (Phase 19 WR-01/02) | mitigate | Acceptance asserta 0 smart-quotes; scan de acentos sobre superficies MOVIDAS (DeepSeek estricto en acentos); el smoke (24-03) re-asserta markdown/HTML sobre variants[].prompt + explanation. Apostrofes ASCII U+0027 preservados del legacy. |
| T-24-04 | Tampering | malformed JSON crashea el loader | mitigate | `validateContent` (existente, no modificado) rechaza con banner visible slot sin variantes / variante sin superficie valida / explanation ausente. El acceptance corre el validador. |
| T-24-07 | Information disclosure / governance | una explanation referencia la CATEGORIA Essere por id o prosa, violando D-24-07/D-159 | mitigate | Acceptance asserta 0 refs a "essere-0XX"/"ver Essere"/"categoria Essere" en explanations; el merge preserva las explanations legacy que ya cumplen; "essere" como nombre del auxiliar es valido e inevitable (no se penaliza). |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que instalar/auditar (proyecto estatico offline single-user). |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json` exits 0
- `node --test tests/*.test.js` — nota: el count de Verbi di movimento (37 hardcoded en 3 sitios) FALLARA tras esta reescritura porque ahora hay otro nº de slots; esto es ESPERADO y se arregla en 24-03 (path desnudo falla en Node 22.20 — usar el glob `tests/*.test.js`). Documentar el rojo esperado en el SUMMARY; NO arreglar los counts aqui.
- El mapa 24-REAGRUPACION-MAP.md aprobado por el autor (checkpoint), incluida la confirmacion de la concordancia en 1 slot y que no aplica snapshot ni cruces
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Verbi di movimento)
</verification>

<success_criteria>
- 37 ejercicios fuente reagrupados a slots por REGLA DE AUXILIAR (D-24-01): slot essere (seleccion de auxiliar, variantes intercambiables D-24-02); concordancia en UN solo slot (D-24-03, divergencia deliberada vs Essere); excepcioni->avere (D-24-04); correre PROPIO con "hay destino?" (D-24-04); 100/101/102 word-buttons slots-de-1 sin match (D-24-12); ids TODOS semanticos (D-24-09), categoryIds=["verbos-movimiento"], sin cruces
- Cada slot: explanation top-level no vacia (merge D-24-10, anti-calco en el auxiliar, matriz en concordancia, "hay destino?" en correre), variantes sin explanation propia, payload eliminado; ninguna explanation referencia la categoria Essere por id ni prosa (D-24-07/D-159); patron distractoras essere/avere preservado; disputed->override 011/024/026 verbatim
- validateContent verde; sin smart-quotes; scan de acentos aplicado a superficies movidas
- Verbi di movimento no tiene snapshot APPEND-ONLY ni cruces multi-cat (avere-only) -> NO se replica la re-base D-88 ni se crean cruces; documentado en el SUMMARY
- (Los counts de tests quedan rojos a proposito hasta 24-03)
</success_criteria>

<output>
Create `.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-01-SUMMARY.md` when done
</output>
