---
phase: 24-verbi-di-movimento-a-slots-contenido
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - content/exercises/verbos-movimiento.json
  - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md
autonomous: false
requirements: [MOV-02]
must_haves:
  truths:
    - "Cada variante NUEVA y cada slot NUEVO autorado pasa el quorum cross-vendor R1-R7 (base canonica Claude Opus + Sonnet via skill gsd-validate-exercise; refuerzo Gemini + DeepSeek via validate-ai-pass) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot; 1-por-1, fresh context, NUNCA batched"
    - "Los 4 ejes de huecos PRIORIZADOS (D-24-06) se materializan con ambicion generosa sin cuota fija (D-24-05): (1) mas verbos essere (scendere/salire/cadere/rimanere-restare/diventare/nascere/morire) al slot movimiento->essere; (2) mas excepciones avere (passeggiare/sciare/saltare/viaggiare en mas personas) al slot excepcioni->avere; (3) mas test-de-destino (correre/volare/saltare/salire/scendere con y sin destino) al slot correre; (4) matriz de concordancia completa (las 4 formas con varios verbos) al slot concordanza"
    - "Cada verbo asignado a essere o a avere se VERIFICO que toma realmente ese auxiliar en italiano (D-24-06): el quorum cross-vendor caza cualquier excepcion inventada o asignacion erronea de auxiliar antes de entrar al slot"
    - "Las variantes nuevas se autoran SOLO donde la regla admite reformulacion natural (sin cuota fija D-24-05); ninguna lleva explanation propia (comparten la del slot); cualquier slot NUEVO lleva validation.status top-level (D-19-09)"
    - "Las superficies existentes movidas intactas en 24-01 NO se re-validan (D-24-11); el contraste essere/avere se refuerza en distractoras/explanations donde aplique; ninguna explanation referencia la categoria Essere por id ni prosa (D-24-07/D-159)"
  artifacts:
    - path: "content/exercises/verbos-movimiento.json"
      provides: "variantes nuevas validated integradas en variants[] de los slots por regla, + cualquier slot nuevo con validation top-level"
      contains: "\"variants\""
    - path: ".planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md"
      provides: "set propuesto de superficies nuevas con id temporal + slot destino + superficie + justificacion R1-R7 + auxiliar verificado"
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quorum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS con ambicion GENEROSA (MOV-02, D-24-05) donde la regla admite reformulacion natural, materializando los 4 EJES DE HUECOS PRIORIZADOS de Verbi di movimento (D-24-06, el autor marco los 4):

1. MAS VERBOS ESSERE (engorde del slot `verbos-movimiento-essere`): scendere, salire, cadere, rimanere/restare, diventare, nascere, morire — verbos de movimiento/estado/cambio que TAMBIEN piden essere en passato prossimo. Variantes con distintas personas y participios concordados (sceso/a/i/e, salito, caduto, rimasto, diventato, nato, morto). VERIFICAR (D-24-06) que cada verbo toma realmente essere (scendere/salire alternan en algunos contextos transitivos — el quorum lo confirma).

2. MAS EXCEPCIONES AVERE (engorde del slot `verbos-movimiento-excepcioni-avere`): passeggiare, sciare, saltare, viaggiare en mas personas. Actividades sin destino concreto -> avere + participio invariable. VERIFICAR que cada uno toma avere (no essere).

3. MAS TEST-DE-DESTINO (engorde del slot `verbos-movimiento-correre`): correre/volare/saltare/salire/scendere con y sin destino, drilleando la regla "hay un destino? con destino essere, sin destino avere". Pares contrastivos (mismo verbo, con destino -> essere; sin destino -> avere). VERIFICAR que el contraste destino/no-destino es real para cada verbo (correre y volare son los casos canonicos; saltare/salire/scendere el quorum confirma su comportamiento).

4. MATRIZ DE CONCORDANCIA COMPLETA (engorde del slot `verbos-movimiento-concordanza`): las 4 formas -o/-a/-i/-e con VARIOS verbos (no solo andare), para completar la matriz genero x numero del participio con essere.

Cada superficie NUEVA pasa el quorum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched, con revision del autor por variante (D-85). Las superficies movidas intactas en 24-01 NO se re-validan (D-24-11). Sin cuota fija de densidad (D-24-05): proponer variantes donde la regla lo pida, no por cumplir un minimo (evita variantes de relleno). RESTRICCION D-24-07/D-159: ninguna explanation nueva/tocada referencia la categoria Essere por id ni prosa (usar "essere" como nombre del auxiliar es valido).

El planner/autor decide en plan-time/checkpoint si los huecos materializan VARIANTES de slots existentes (lo natural — los 4 ejes engordan los 4 slots de 24-01) o algun SLOT NUEVO. Recomendacion del planner: NO se necesitan slots nuevos — los 4 ejes engordan los 4 slots existentes (essere, excepcioni-avere, correre, concordanza). Si el autor crea algun slot nuevo, este requiere validation.status top-level (D-19-09).

Purpose: las variantes nuevas materializan el dolor anti-memorizacion (re-hacer un slot puede tocar una superficie distinta) y cierran los huecos pedagogicos de Verbi di movimento, todos centrados en la regla distintiva de la categoria: la SELECCION DEL AUXILIAR essere-vs-avere. Mas verbos essere + mas excepciones avere + mas test-de-destino entrenan exactamente la decision que el hispanohablante calca mal ("io ho andato"). La matriz de concordancia completa cierra la otra cara (el participio con essere concuerda). El quorum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazo 8 bugs en Articoli, 3 en Avere y 2 en Essere que el human-verify aprobo) y la unica garantia de que cada verbo asignado a essere/avere toma realmente ese auxiliar (D-24-06). R1/R5 criticos: el prompt no debe leak la forma del auxiliar; las options deben tener 3+ valores distintos.

NO existe snapshot/assert APPEND-ONLY para Verbi di movimento (avere-only, verificado: 0 refs). NO ejecutar ningun script de snapshot ni assert de prefijo. NO existen cruces multi-cat (no hay verbos-movimiento-300..305); NO crearlos. A diferencia de 22-02 (que re-basaba el snapshot de Avere), aqui NO hay paso de re-base — igual que 23-02.

Output: `verbos-movimiento.json` con las variantes nuevas de los 4 ejes integradas; cada superficie nueva con su quorum verde en `validation.passes[]`; cualquier slot nuevo con validation.status top-level (D-19-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/verbos-movimiento.json

<interfaces>
<!-- Pipeline de quorum cross-vendor. Verificado en validate-ai-pass.mjs + skill. NO modificar estos scripts/skill. Mismo flujo ejecutado con exito en Phase 19 (8 superficies), Phase 20 (6), Phase 22 (14) y Phase 23 (14), 0 excluidas. -->
Quorum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Base canonica (D-24-13) — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus->Sonnet):
  /gsd-validate-exercise <id>
  (model IDs literales que emite el skill: claude-opus-4-7 + claude-sonnet-4-6; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio. Mantener los IDs literales del skill por consistencia del audit trail con el contenido existente — confirmado en Phase 23 Open Q #1.)
  Fallback cuando Task no esta disponible al executor (precedente Phase 19/20/22/23, D-19-08): la mitad Claude se corre con `claude -p --model claude-opus-4-7` y `--model claude-sonnet-4-6` headless, 1 proceso por modelo por superficie, con el 09-VALIDATION-PROMPT verbatim y el ejercicio SIN su campo validation (no sesgar). Equivalente funcional 1-por-1 NUNCA batched.

Refuerzo — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondio; >=2 by distintos para el quorum; status re-derivado por deriveStatus, NO hand-editar status. Si Gemini agota cuota 429/503, DeepSeek cubre los 2 externos con by distintos chat + reasoner — precedente 22-VERIFICATION / 23-02.)

Gate D-17-07: el passes[] final de cada superficie nueva = >=4x "correcta" (opus + sonnet + 2 externos by distintos), CERO "incorrecta". deriveStatus -> "validated"; cualquier "incorrecta" -> "disputed" (sticky, bloquea, NO entra al slot).

Pitfall: el VALIDATION-PROMPT es legacy-payload-centrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/verbos-movimiento.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quorum, mover la superficie al variants[] del slot (sin re-validacion) y eliminar el id temporal legacy.

Alcance D-24-05 (ambicion generosa) + D-24-06 (los 4 ejes priorizados, sin cuota fija):
- Engordar las celdas pobres marcadas en 24-01 + materializar los 4 ejes de huecos. NO inflar slots cuya regla no admite reformulacion natural; sin minimo por slot (D-24-05).

Los 4 ejes de huecos PRIORIZADOS (D-24-06, R1/R5 criticos, VERIFICAR el auxiliar real de cada verbo):
1. MAS VERBOS ESSERE (slot `verbos-movimiento-essere`): scendere, salire, cadere, rimanere/restare, diventare, nascere, morire en distintas personas, con su participio concordado. El prompt fija sujeto+verbo; la respuesta es la forma de essere (o el participio en variantes de concordancia). VERIFICAR (R1) que el prompt no leak la forma del auxiliar; (R5) options con 3+ valores distintos (distractoras avere). VERIFICAR que el verbo toma essere (cuidado: scendere/salire pueden tomar avere en uso transitivo "ha sceso le scale" — el quorum lo confirma; usar contextos intransitivos de movimiento).
2. MAS EXCEPCIONES AVERE (slot `verbos-movimiento-excepcioni-avere`): passeggiare, sciare, saltare, viaggiare en mas personas. Actividad sin destino -> avere + participio invariable. VERIFICAR que toman avere (no essere).
3. MAS TEST-DE-DESTINO (slot `verbos-movimiento-correre`): correre/volare/saltare/salire/scendere con y sin destino, pares contrastivos. VERIFICAR que el contraste destino/no-destino aplica a cada verbo (correre y volare canonicos; el quorum confirma saltare/salire/scendere).
4. MATRIZ DE CONCORDANCIA COMPLETA (slot `verbos-movimiento-concordanza`): las 4 formas -o/-a/-i/-e con varios verbos (sceso/a/i/e, salito, venuto, partito, tornato, uscito, entrato), para completar la matriz genero x numero. El prompt fija sujeto+genero+numero+auxiliar dado; la respuesta es el participio concordado. Distractoras = concordancias incorrectas.
- El planner/autor decide en plan-time/checkpoint que huecos son naturales. Si un hueco no tiene construccion A1 natural o el verbo no toma el auxiliar reclamado, NO se materializa.

Validation top-level para la gate (D-19-09): si el autor crea algun slot NUEVO, debe portar el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum cross-vendor de sus variantes (el gate VAL_07_STRICT lee `ex.validation?.status` a nivel de slot). Si todos los huecos engordan slots existentes (recomendado), esos slots ya tienen su validation top-level de 24-01 (de las superficies fuente movidas) — NO degradarla; el passes[] de las variantes nuevas se anade donde el flujo lo registre, pero la gate de slot ya esta cubierta. NO inventar validacion: elevar la aprobada.

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quorum, un scan ASCII/tildes (RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial.
Gloss ES (R7 canon): mantener "(en espanol: ...)" donde desambigue doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de politica (base de aprobacion = Claude Opus+Sonnet, D-24-13).
D-24-07/D-159: ninguna explanation nueva/tocada referencia la categoria Essere por id ni prosa; "essere" como nombre del auxiliar es valido.

NO snapshot: Verbi di movimento no tiene snapshot/assert APPEND-ONLY (avere-only, verificado). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs ni buscar .verbos-movimiento-prefix-snapshot.json. NO crear cruces 300..305 (no existen; fuera de scope). A diferencia de 22-02 NO hay re-base de snapshot aqui.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (los 4 ejes de huecos D-24-06) con auxiliar verificado</name>
  <files>.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/verbos-movimiento.json (estado tras 24-01 — los slots ya reagrupados, para saber que slots existen, cuales son celdas pobres y que admite variante nueva)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-REAGRUPACION-MAP.md (las celdas pobres marcadas como candidatas + los 4 ejes de huecos anotados en 24-01)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints del quorum: 1-por-1, fresh context, NUNCA batched; model IDs literales)
    - .planning/phases/23-essere-a-slots-contenido/23-02-PLAN.md (Task 1 analogo de Essere — formato de 23-VARIANTES-NUEVAS.md a espejar) y .planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md (la plantilla concreta)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6/R7 si accesible) y gloss_es_desambiguacion_canon.md (gloss ES canon)
  </read_first>
  <action>
    Proponer un set de superficies nuevas con ambicion GENEROSA (D-24-05) pero sin cuota fija, materializando los 4 EJES DE HUECOS PRIORIZADOS (D-24-06): (1) MAS VERBOS ESSERE para el slot `verbos-movimiento-essere`: variantes con scendere/salire/cadere/rimanere-restare/diventare/nascere/morire en distintas personas (superficie `{prompt con ___, options 3-4 con distractoras avere, correctIndex}`); VERIFICAR explicitamente por superficie que el verbo toma essere en passato prossimo (cuidado scendere/salire transitivos -> usar contexto intransitivo de movimiento) y anotar la verificacion. (2) MAS EXCEPCIONES AVERE para el slot `verbos-movimiento-excepcioni-avere`: passeggiare/sciare/saltare/viaggiare en mas personas (actividad sin destino -> avere + participio invariable); VERIFICAR que toman avere. (3) MAS TEST-DE-DESTINO para el slot `verbos-movimiento-correre`: pares contrastivos correre/volare/saltare/salire/scendere con destino (->essere) vs sin destino (->avere); VERIFICAR que el contraste aplica a cada verbo. (4) MATRIZ DE CONCORDANCIA COMPLETA para el slot `verbos-movimiento-concordanza`: las 4 formas -o/-a/-i/-e con varios verbos (sceso/a/i/e, salito, venuto, partito, tornato, uscito, entrato), prompt fija sujeto+genero+numero+auxiliar, respuesta = participio concordado, distractoras = concordancias incorrectas. VERIFICAR explicitamente por superficie: (R1) el prompt no leak la forma correcta del auxiliar/participio; (R5) las options tienen 3+ valores distintos; que el verbo toma realmente el auxiliar reclamado (essere o avere) en italiano A1 (D-24-06 — no inventar excepciones). Cumplir R1-R7 (sin leak en prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigue doble-validez). RESTRICCION D-24-07/D-159: ninguna explanation referencia la categoria Essere por id ni prosa; "essere" como nombre del auxiliar es valido. Pasar un scan de acentos/ASCII (tildes RAE en explanations espanolas, apostrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. Producir `.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md` (espejar el formato de 23-VARIANTES-NUEVAS.md): lista de cada superficie nueva con su id temporal (para el quorum aislado), slot destino (existente — recomendado que los 4 ejes engorden los 4 slots de 24-01; marcar cualquier slot NUEVO si el autor lo pide), eje al que pertenece (1-4), prompt/options/correctIndex/explanation, la justificacion de por que la regla admite reformulacion, y la VERIFICACION del auxiliar real del verbo. Recomendar en una seccion que NO se necesitan slots nuevos (los 4 ejes engordan los slots existentes) — surfacear al autor. Reportar el conteo de superficies nuevas propuestas por eje Y cuantas (si alguna) son SLOTS NUEVOS (los slots nuevos suben el count de 24-03; las variantes no).
  </action>
  <verify>
    <automated>test -f .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md && grep -ci "scendere\|salire\|cadere\|correre\|volare\|concordan\|essere\|avere" .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 24-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + eje (1-4) + superficie + explanation
    - EJE 1 (mas verbos essere) cubierto: variantes con scendere/salire/cadere/rimanere-restare/diventare/nascere/morire para el slot movimiento->essere, cada una con la verificacion de que el verbo toma essere (contexto intransitivo donde aplique)
    - EJE 2 (mas excepciones avere) cubierto: variantes con passeggiare/sciare/saltare/viaggiare para el slot excepcioni->avere, con la verificacion de que toman avere
    - EJE 3 (mas test-de-destino) cubierto: pares contrastivos con destino (essere) vs sin destino (avere) para el slot correre, con la verificacion del contraste por verbo
    - EJE 4 (matriz de concordancia) cubierto: las 4 formas -o/-a/-i/-e con varios verbos para el slot concordanza, distractoras = concordancias incorrectas
    - Cada superficie nueva cumple R1 (sin leak de la forma del auxiliar/participio en el prompt) y R5 (options con 3+ valores distintos); cada explanation/prompt pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers; ninguna referencia la categoria Essere por id/prosa (D-24-07)
    - Cada verbo asignado a essere o avere lleva su VERIFICACION de auxiliar real (D-24-06) — no se inventan excepciones
    - Hay una seccion que recomienda NO crear slots nuevos (los 4 ejes engordan los slots existentes), surfaceada al autor
    - El conteo de superficies nuevas propuestas por eje Y de slots nuevos (si alguno) esta reportado
  </acceptance_criteria>
  <done>
    24-VARIANTES-NUEVAS.md lista el set generoso de superficies nuevas de los 4 ejes D-24-06 (mas verbos essere + mas excepciones avere + mas test-de-destino + matriz de concordancia) con superficies R1-R7-conformes, scan de acentos pasado, sin refs a la categoria Essere, y la verificacion del auxiliar real de cada verbo; recomendacion de no crear slots nuevos surfaceada; conteo de superficies por eje y de slots nuevos reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (24-VARIANTES-NUEVAS.md) — los 4 ejes de huecos D-24-06: mas verbos essere (scendere/salire/cadere/rimanere/diventare/nascere/morire), mas excepciones avere (passeggiare/sciare/saltare/viaggiare), mas test-de-destino (correre/volare/saltare/salire/scendere con/sin destino), matriz de concordancia completa — cumpliendo R1-R7, con scan de acentos y la verificacion del auxiliar real de cada verbo. AUN NO validadas por quorum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md`.
    2. EJE 1 (mas verbos essere): scendere/salire/cadere/rimanere/diventare/nascere/morire toman essere en passato prossimo en el contexto propuesto? (Cuidado: scendere/salire transitivos "ha sceso le scale" toman avere — confirma que las variantes usan contexto intransitivo de movimiento.) El prompt NO contiene la respuesta (R1)? Las options tienen 3+ valores distintos con distractoras avere (R5)?
    3. EJE 2 (mas excepciones avere): passeggiare/sciare/saltare/viaggiare toman avere (actividad sin destino, participio invariable)? Las distractoras essere refuerzan la trampa sin volver ambigua la respuesta?
    4. EJE 3 (mas test-de-destino): los pares contrastivos (mismo verbo con destino -> essere, sin destino -> avere) son A1 correctos para cada verbo? correre/volare son canonicos; saltare/salire/scendere — confirma que el contraste destino/no-destino aplica realmente.
    5. EJE 4 (matriz de concordancia): las 4 formas -o/-a/-i/-e concuerdan correctamente con el sujeto+auxiliar del prompt, con varios verbos? Las distractoras son concordancias incorrectas (no formas de otro verbo)?
    6. Confirma que NINGUNA explanation referencia la categoria Essere por id ni prosa (D-24-07/D-159) — "essere" como nombre del auxiliar SI es valido.
    7. Confirma el alcance: generoso pero sin variantes artificiales en reglas que no se reformulan (D-24-05); decide si algun eje crea un slot nuevo (recomendado: NO, todos engordan slots existentes).
    8. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quorum (especialmente cualquier verbo cuyo auxiliar dudes).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las superficies aprobadas al quorum cross-vendor, o lista las que descartar/reformular (y cualquier verbo cuyo auxiliar quieras que el quorum confirme con prioridad).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quorum cross-vendor (1-por-1) e integrarla al slot</name>
  <files>content/exercises/verbos-movimiento.json</files>
  <read_first>
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales claude-opus-4-7/claude-sonnet-4-6, 1 commit por ejercicio; fallback `claude -p` headless si no hay Task, D-19-08)
    - content/exercises/verbos-movimiento.json (slots destino donde integrar las superficies tras pasar el quorum)
    - .planning/phases/23-essere-a-slots-contenido/23-02-PLAN.md (Task 2 analogo de Essere — el flujo real: 14 superficies, 2 disputes resueltas por reformulacion, slot nuevo con validation top-level, fallback DeepSeek reasoner cuando Gemini 429) y .planning/phases/23-essere-a-slots-contenido/23-VARIANTES-NUEVAS.md
  </read_first>
  <action>
    Para CADA superficie nueva aprobada, ejecutar el ciclo completo de quorum 1-por-1 (NUNCA batched): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/verbos-movimiento.json como entry temporal o tests/fixtures/*.json). (2) Base canonica Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus->Sonnet secuencial, 1 spawn; model IDs literales del skill claude-opus-4-7/claude-sonnet-4-6); fallback `claude -p --model claude-opus-4-7` / `--model claude-sonnet-4-6` headless con el ejercicio SIN su campo validation si no hay Task (D-19-08). (3) Refuerzo externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write` (si Gemini agota cuota 429/503, usar deepseek-chat + deepseek-reasoner como 2 by externos distintos, precedente 22-VERIFICATION / 23-02). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4x "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` -> la superficie queda disputed y se resuelve por REFORMULACION (calidad > tokens, NO override-atajo; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de politica; si un externo alucina un acento sobre texto correcto, re-run sobre el MISMO texto, precedente Phase 19/22/23) o se descarta — NO entra al slot disputed. PRESTAR ESPECIAL ATENCION (D-24-06) a que el quorum confirme el auxiliar correcto de cada verbo: si una superficie de scendere/salire/saltare resulta `incorrecta` por usar el auxiliar equivocado para el contexto, reformular el contexto o descartarla. (5) Tras pasar el quorum, mover la superficie validada al variants[] del slot destino (existente, para los 4 ejes: essere, excepcioni-avere, correre, concordanza) o crear el slot NUEVO `{id, type:"multiple-choice", categoryIds:["verbos-movimiento"], explanation, variants:[...], validation}` SOLO si el autor lo aprobo; eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Para cualquier slot NUEVO, anadir el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum aprobado de sus variantes (D-19-09). Para los slots EXISTENTES engordados, NO degradar su validation top-level de 24-01 (ya cubre la gate de slot). Reforzar el contraste essere/avere en distractoras/explanations donde aplique, sin re-validar las superficies movidas intactas (D-24-11). RESTRICCION D-24-07/D-159: ninguna explanation tocada referencia la categoria Essere por id ni prosa. Hacer 1 commit por superficie validada. NUNCA validar mas de una superficie en un mismo contexto/spawn. NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Verbi di movimento; avere-only). NO crear cruces 300..305.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json` exits 0
    - Los 4 ejes engordaron sus slots: el slot essere, el slot excepcioni-avere, el slot correre y el slot concordanza tienen mas variantes que tras 24-01 (verificable contando variants[] de cada slot)
    - Cada superficie NUEVA paso el quorum con >=4x "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se valido como legacy)
    - Si se creo algun slot NUEVO, lleva validation.status=="validated" top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('slots variants sin validation top-level:', d.exercises.filter(e=>Array.isArray(e.variants)&&e.validation?.status!=='validated').length)"` imprime `0`
    - Ningun id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 24-01 NO se re-validaron (# invocaciones de quorum == # superficies nuevas)
    - El slot de concordancia SIGUE siendo UN solo slot (D-24-03, las variantes nuevas de la matriz NO lo dividen): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('concordanza slots:', d.exercises.filter(e=>/concordanza|concordancia/.test(e.id)).length)"` imprime `concordanza slots: 1`
    - NO se crearon cruces: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('cruces:', d.exercises.filter(e=>/-30[0-5]$/.test(e.id)).length)"` imprime `0`; todos los slots categoryIds=["verbos-movimiento"]
    - Ninguna explanation referencia la categoria Essere por id/prosa: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); console.log('refs Essere-cat:', d.exercises.filter(e=>/essere-0\d\d|ver Essere|categor.a Essere/i.test(e.explanation||'')).length)"` imprime `0` (D-24-07)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/verbos-movimiento.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Verbi di movimento
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas de los 4 ejes D-24-06 pasaron el quorum cross-vendor (>=4x correcta) e integradas a sus slots; mas verbos essere + mas excepciones avere + mas test-de-destino + matriz de concordancia integrados; auxiliar de cada verbo confirmado por el quorum (D-24-06); concordancia sigue siendo 1 slot (D-24-03); validateContent verde; sin ids temporales residuales; sin cruces; sin refs a la categoria Essere; 1 commit por superficie; sin snapshot (no aplica a Verbi di movimento).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio -> evaluador del quorum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podria intentar dirigir al evaluador |
| JSON de contenido editado a mano -> loader/validator | superficies nuevas redactadas a mano cruzan a validateContent y al render |

> Nota: esta fase autora contenido JSON en una app local single-user sin backend; el unico acceso externo son las API keys de `.env` (leidas, no instaladas) para el quorum. Sin red entrante, auth, ni input de terceros. No se esperan amenazas HIGH.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | superficie nueva con auxiliar incorrecto (verbo asignado a essere que en realidad toma avere o viceversa — p.ej. scendere/salire transitivos), concordancia incorrecta, respuesta ambigua (doble-validez R7) o acento incorrecto entra al slot | mitigate | Quorum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); verificacion del auxiliar real de cada verbo en Task 1 + checkpoint (D-24-06, no inventar excepciones); pre-scan de acentos; gate de >=4x "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-24-06 | Tampering | prompt-injection en el quorum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instruccion; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-24-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes; leak de la forma del auxiliar en el prompt (R1) | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown); verificacion R1 (sin leak) en Task 1 + checkpoint; el smoke de 24-03 re-asserta sobre variants[].prompt + explanation. |
| T-24-07 | Information disclosure / governance | una explanation nueva referencia la CATEGORIA Essere por id o prosa, violando D-24-07/D-159 | mitigate | Verificacion en Task 1 + checkpoint; acceptance de Task 2 asserta 0 refs a "essere-0XX"/"ver Essere"/"categoria Essere"; "essere" como nombre del auxiliar es valido. |
| T-24-08 | Tampering | slot NUEVO (si lo hubiera) sin validation.status top-level cuela como `status: absent` en VAL_07_STRICT | mitigate | Cualquier slot NUEVO porta el bloque `validation` top-level reflejando el quorum aprobado (D-19-09); acceptance asserta `slots variants sin validation top-level == 0`. Si todos los ejes engordan slots existentes, su validation de 24-01 ya cubre la gate. |
| T-24-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json` exits 0
- Cada superficie nueva: passes[] con >=4x "correcta", cero "incorrecta" (registrado en SUMMARY); auxiliar de cada verbo confirmado por el quorum (D-24-06)
- Los 4 ejes de huecos D-24-06 materializados; concordancia sigue siendo 1 slot (D-24-03); sin cruces; sin refs a la categoria Essere (D-24-07)
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Verbi di movimento)
- `node --test tests/*.test.js` — el count de Verbi di movimento sigue rojo a proposito hasta 24-03 (el conteo final ya es determinable tras este plan; usar el glob `tests/*.test.js`, path desnudo falla en Node 22.20)
</verification>

<success_criteria>
- Variantes nuevas autoradas con ambicion generosa donde la regla admite reformulacion (D-24-05, sin cuota fija), materializando los 4 ejes priorizados (D-24-06): mas verbos essere + mas excepciones avere + mas test-de-destino + matriz de concordancia
- Cada verbo asignado a essere/avere verificado por el quorum como auxiliar real (D-24-06, no se inventan excepciones)
- Cada superficie nueva paso el quorum cross-vendor R1-R7 (>=4x correcta) antes de entrar (base canonica Opus+Sonnet, D-24-13)
- Superficies existentes movidas NO re-validadas; concordancia sigue siendo 1 slot (D-24-03); sin cruces; ninguna explanation referencia la categoria Essere (D-24-07)
- validateContent verde; conteo final de slots determinable para 24-03; sin snapshot (no aplica a Verbi di movimento)
</success_criteria>

<output>
Create `.planning/phases/24-verbi-di-movimento-a-slots-contenido/24-02-SUMMARY.md` when done
</output>
