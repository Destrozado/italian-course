---
phase: 26-professioni-a-slots-contenido-l-xica
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [PROF-01]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Professioni (expected: 51 en exercise-types.test.js:1272, slot-variants-integration.test.js:173 y run-validation-271.mjs:140) estan sincronizados contra el conteo REAL final de slots leido del JSON (data.exercises.length), no una estimacion"
    - "TOTAL_EXPECTED en run-validation-271.mjs:145 esta recalculado = 249 - 51 + n_slots_final_profesiones"
    - "El smoke parametrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero; Professioni convertido a slots pasa coverage/R1/R2/smart-quotes/markdown sin tocar la logica del test, solo el count; los 3 match preservados (type:match) pasan por la rama match del smoke y los 5 word-buttons (type:word-buttons) por la rama word-buttons"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Professioni en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots"
      contains: "profesiones.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de profesiones sincronizado al n de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected profesiones + TOTAL_EXPECTED recalculado + comentario del historial actualizado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de profesiones.json"
      via: "sincronizacion contra el conteo REAL del JSON, no estimacion"
      pattern: "profesiones"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Professioni (`expected: 51`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `profesiones.json` leido del JSON tras 26-01/26-02 (NO una estimacion), cerrando PROF-01 (cierre del count). El smoke parametrico YA es shape-agnostic (la bifurcacion `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17, se reuso en Articoli Phase 19, Partitivos Phase 20, Avere Phase 22, Essere Phase 23, Verbi di movimento Phase 24 y Genere e numero Phase 25, y esta en el codebase — verificado en exercise-types.test.js getExplanation/getPrompts; la rama match ya existe para los slots type:match de articoli/partitivos/genero-numero, y la rama word-buttons para los slots type:word-buttons de essere/verbos-movimiento), asi que Professioni convertido a slots (incluidos los 3 match y los 5 word-buttons preservados) pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la logica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]). Professioni NO tiene snapshot/assert APPEND-ONLY (avere-only) ni cruces multi-cat -> este plan no corre ningun script de snapshot.

Purpose: convertir Professioni rompe los 3 counts hoy verdes (51 replicado en 3 archivos + TOTAL_EXPECTED=249 que incluye preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, sustantivos-irregulares=31, verbos-movimiento=7). PROF-01 (cierre) ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase (segunda de las 2 conversiones lexicas de v1.6).

Output: los 3 test/script files con el count de Professioni + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validacion PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/profesiones.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-08). El smoke YA bifurca por shape (no requiere extension de logica, solo count). preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, sustantivos-irregulares=31, verbos-movimiento=7 — NO tocarlos. -->

1. tests/exercise-types.test.js:
   - linea 1272: `{ file: 'content/exercises/profesiones.json', expected: 51 }` en CATEGORIES_WITH_EXPLANATIONS <- cambiar 51 al n real de slots
   - las otras 8 categorias (preposiciones 49, articoli 34, avere 20, essere 26, genero-numero 12, partitivos 19, sustantivos-irregulares 31, verbos-movimiento 7) — YA sincronizadas, NO tocar
   - getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation — YA bifurcado por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero, NO tocar)
   - getPrompts YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar); la rama match (slots type:match) ya existe para articoli/partitivos/genero-numero -> los 3 match de profesiones pasan sin tocar logica; la rama word-buttons (slots type:word-buttons) ya existe para essere/verbos-movimiento -> los 5 WB de profesiones pasan sin tocar logica
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido -> se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:173: `{ slug: 'profesiones', expected: 51 }` en REAL_CATEGORIES <- cambiar 51 al n real de slots (las otras 8 en sus lineas, NO tocar)

3. scripts/run-validation-271.mjs:
   - linea 140: `{ slug: 'profesiones', file: 'content/exercises/profesiones.json', expected: 51 }` <- cambiar 51 al n real
   - linea 145: `const TOTAL_EXPECTED = 249;` <- recalcular = 249 - 51 + n_slots_final_profesiones
   - lineas del comentario explicativo del historial del total (alrededor de 64-144) <- anadir la conversion de Professioni a slots. El comentario narra cada cambio que movio el total; la ultima linea documenta Genere e numero (Phase 25, GEN-01). Anadir una linea analoga DESPUES: "-> <nuevo> (v1.6 Phase 26, PROF-01): Professioni se convirtio al shape slot+variantes HIBRIDO. La reagrupacion fundio 51 ejercicios payload a <count> slots reales: bloque REGLA por sub-regla de feminizacion (D-26-03: -o/-a, -iere/-iera, -tore/-trice, -e/-essa, invariabili) + bloque LEXICO PURO sin variantes (comprension + 3 match preservados D-26-02/D-04) + articolo-suono + word-buttons preservados (D-26-06). Las 51 superficies fuente se preservaron como variantes (+N superficies nuevas SOLO de feminizacion D-26-04: contraste -trice/-essa + invariables -ista/-ante + -o/-a y -iere/-iera — variantes, NO slots, asi que NO suben el count; el bloque lexico no recibio variantes, PROF-01). TOTAL = 249 - 51 + <count> = <nuevo>". OJO: el net BAJA (51 payload -> menos slots por la fusion a slot+variantes); pero LEER el count real, no asumir (las VARIANTES nuevas de 26-02 NO suben el count; solo SLOTS nuevos lo suben, y 26-02 recomienda NO crear slots nuevos -> el net = slots de reagrupacion de 26-01).

El count `expected` de profesiones = n de SLOTS final (data.exercises.length tras 26-01+26-02) — sincronizar contra el JSON REAL, NO contra una estimacion.
Comando: `node -e "console.log(require('./content/exercises/profesiones.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
Leccion 19-03/20-03/22-03/23-03/24-03/25-03: leer SIEMPRE del JSON, nunca estimar. Las VARIANTES nuevas (los ejes de feminizacion de 26-02) NO suben el count de slots; solo los SLOTS nuevos lo suben (26-02 recomienda NO crear slots nuevos -> el net = slots de reagrupacion de 26-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Professioni en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (las lineas alrededor de 1260-1300 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + la rama match + la rama word-buttons + el assert de conteo; y el bloque VAL_07_STRICT)
    - content/exercises/profesiones.json (estado final tras 26-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-03-PLAN.md (Task 1 analogo de Genere e numero — el mismo patron de sync de count contra el JSON real)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de profesiones.json (`data.exercises.length` tras 26-02, via `node -e "console.log(require('./content/exercises/profesiones.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry profesiones en CATEGORIES_WITH_EXPLANATIONS (linea 1272) a ese numero. NO tocar las entries de preposiciones (49), articoli (34), avere (20), essere (26), genero-numero (12), partitivos (19), sustantivos-irregulares (31) ni verbos-movimiento (7). NO tocar la logica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero) y las ramas match y word-buttons ya existen -> Professioni slot (MC + los 3 match + los 5 word-buttons preservados) pasa coverage/R1/R2/smart-quotes/markdown automaticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level -> no cambia salvo heredar el count sincronizado (todo slot/variante nuevo de 26-02 ya esta validated por el quorum; los slots movidos conservan su validation fuente incluido el disputed->override de collega). NO tocar los counts de las otras 8 categorias.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de profesiones en CATEGORIES_WITH_EXPLANATIONS == n REAL de slots: `node -e "const d=require('./content/exercises/profesiones.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en linea 1272
    - Las otras 8 categorias siguen verdes con sus counts originales (preposiciones 49, articoli 34, avere 20, essere 26, genero-numero 12, partitivos 19, sustantivos-irregulares 31, verbos-movimiento 7) — no se tocaron
    - La logica del test (getExplanation/getPrompts/bifurcacion/rama match/rama word-buttons) NO se modifico: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Professioni en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-profesiones intactas; logica del smoke (ya shape-agnostic, con rama match y word-buttons) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED)</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (las lineas alrededor de 165-200 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (las lineas alrededor de 64-150 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/profesiones.json (conteo REAL final de slots)
    - .planning/phases/25-genere-e-numero-a-slots-contenido/25-03-PLAN.md (Task 2 analogo de Genere e numero — mismo patron de 3 hardcodes + TOTAL_EXPECTED + comentario del historial)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'profesiones', expected: 51 }` (linea 173) a `expected` = n REAL de slots. NO tocar las entries de preposiciones (49), articoli (34), avere (20), essere (26), genero-numero (12), partitivos (19), sustantivos-irregulares (31) ni verbos-movimiento (7). El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 51` de la entry profesiones (linea 140) a n REAL de slots; recalcular `TOTAL_EXPECTED` (linea 145) = `249 - 51 + n_slots_final_profesiones`; actualizar el comentario explicativo del historial del total (alrededor de lineas 64-144) anadiendo una linea DESPUES de la de Genere e numero (Phase 25) que registre la conversion de Professioni a slots: "-> <nuevo> (v1.6 Phase 26, PROF-01): Professioni se convirtio al shape slot+variantes HIBRIDO. La reagrupacion fundio 51 ejercicios payload a <count> slots reales: bloque REGLA por sub-regla de feminizacion (D-26-03: -o/-a, -iere/-iera, -tore/-trice, -e/-essa, invariabili) + bloque LEXICO PURO sin variantes (comprension + 3 match preservados D-26-02/D-04) + articolo-suono + word-buttons preservados (D-26-06). Las 51 superficies fuente se preservaron como variantes (+N superficies nuevas SOLO de feminizacion D-26-04: contraste -trice/-essa + invariables -ista/-ante + -o/-a y -iere/-iera — variantes, NO slots; el bloque lexico no recibio variantes, PROF-01). TOTAL = 249 - 51 + <count> = <nuevo>". Leer el conteo real del JSON, NO usar una estimacion (el net BAJA por la fusion a slot+variantes; pero leer, no asumir). Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO numero.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de profesiones en ambos archivos == n REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/profesiones.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 249 - 51 + n_slots_final_profesiones (verificable aritmeticamente contra el conteo real; preposiciones sigue contribuyendo 49, articoli 34, avere 20, essere 26, genero-numero 12, partitivos 19, sustantivos-irregulares 31, verbos-movimiento 7)
    - El reporter de run-validation-271 no emite warning `total != esperado` para profesiones
    - El comentario del historial del total en run-validation-271.mjs menciona la conversion de Professioni a slots (incluida la naturaleza HIBRIDA D-26-01, los 3 match preservados D-26-02/D-04 y los word-buttons D-26-06)
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Professioni (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el n real de slots; TOTAL_EXPECTED recalculado (preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, sustantivos-irregulares=31, verbos-movimiento=7 intactos); reporter PASS sin warnings de count; comentario del historial actualizado con la conversion HIBRIDA de Professioni (3 match + word-buttons preservados).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido -> smoke parametrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

> Nota: esta fase edita hardcodes de count en tests/scripts de tooling de una app local single-user, offline, sin backend; no introduce nueva superficie de ataque. No se esperan amenazas HIGH/MEDIUM.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-11 | Tampering | count desincronizado entre los 3 hardcodes -> gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leido del JSON (`data.exercises.length`), no estimaciones (leccion 19-03/20-03/22-03/23-03/24-03/25-03). El acceptance asserta que coinciden y que run-validation-271 PASS. El net baja por la fusion a slot+variantes -> leer, no asumir. |
| T-26-03 | Tampering | explanation/prompt/pairs/answer de slot con HTML/markdown/smart-quotes pasa sin escanear (rompe T-02-01 x-text) | mitigate | El smoke YA bifurca por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento/Genere-numero): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots (la rama match cubre los 3 match, la rama word-buttons cubre los 5 WB) — la cobertura de seguridad de contenido NO se pierde al convertir Professioni. |
| T-26-10 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 26-02 paso el quorum (status validated) y cualquier slot nuevo lleva validation top-level (D-19-09) antes de integrarse -> la gate pasa solo si 26-02 cumplio. Si algo quedo disputed o sin validation top-level, la gate FALLA visible. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. No hay tabla de Package Legitimacy porque no hay installs. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v9 de Phase 21, integration, exercise-types, articoli/partitivos/avere/essere/verbos-movimiento/genero-numero de Phase 19/20/22/23/24/25). Usar el glob `tests/*.test.js`; el path desnudo falla en Node 22.20 (MEMORY test_command_node_glob).
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs profesiones content/exercises/profesiones.json` exits 0
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de profesiones.json
- (NO se corre ningun assert de snapshot — avere-only, no existe para Professioni)
</verification>

<success_criteria>
- Los 3 hardcodes de count de Professioni + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacia — verificado por el smoke shape-agnostic; los 3 match pasan por la rama match, los 5 WB por la rama word-buttons)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); logica del smoke NO tocada (ya shape-agnostic); preposiciones=49, articoli=34, avere=20, essere=26, genero-numero=12, partitivos=19, sustantivos-irregulares=31 y verbos-movimiento=7 intactos
</success_criteria>

<output>
Create `.planning/phases/26-professioni-a-slots-contenido-l-xica/26-03-SUMMARY.md` when done
</output>
</content>
