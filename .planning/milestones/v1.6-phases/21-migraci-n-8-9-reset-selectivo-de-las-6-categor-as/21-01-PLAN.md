---
phase: 21-migraci-n-8-9-reset-selectivo-de-las-6-categor-as
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/data/storage.js
  - src/data/backup.js
  - tests/data-storage.test.js
  - tests/backup.test.js
autonomous: true
requirements: [MIG-03, MIG-04]
tags: [migration, schemaVersion, reset-selectivo, backup, tdd]

must_haves:
  truths:
    - "MIG-03: Tras migrate8to9 el progreso de las 6 categorías a convertir (avere, essere, verbos-movimiento, genero-numero, profesiones, sustantivos-irregulares) queda reseteado: categoryProgress borrado para las 6, exerciseStats podado por los 6 prefijos, racha 0"
    - "MIG-03: Las 3 categorías ya convertidas (preposiciones, articoli, partitivos) conservan categoryProgress y exerciseStats byte-idénticos pre/post migración (verificado por fixture de las 9 categorías)"
    - "MIG-03: migrate8to9 invalida inFlightTest si algún exerciseId empieza por cualquiera de los 6 prefijos; lo preserva si todos los ids pertenecen a las 3 preservadas"
    - "MIG-03: migrate8to9 es idempotente y puro (no muta el input) y no contamina el prototipo global (deep-clone defensivo)"
    - "MIG-04: backup.js exporta v9 y un export v9 se reimporta sin error de versión más nueva (round-trip)"
    - "MIG-04: Un backup v8 importado migra a v9 reseteando las 6 categorías (progreso de backup viejo de las 6 se pierde por diseño); un backup con schemaVersion > 9 se rechaza"
    - "La suite completa (358 baseline + nuevos tests v9) pasa con node --test tests/*.test.js"
  artifacts:
    - path: "src/data/storage.js"
      provides: "migrate8to9 (reset selectivo de 6 prefijos) + hydrateV9 (espejo sin poda) + CURRENT_SCHEMA_VERSION = 9 + eslabón v8→v9 en migrate()"
      contains: "export function migrate8to9"
    - path: "src/data/backup.js"
      provides: "CURRENT_SCHEMA_VERSION espejo = 9 + eslabón migrate8to9/hydrateV9 en parseBackupFile"
      contains: "migrate8to9"
    - path: "tests/data-storage.test.js"
      provides: "bloque describe v9 (reset selectivo de 6, idempotencia, pureza, anti-prototype-pollution, inFlightTest, hydrateV9, cadena, no-regresión 9 categorías)"
      contains: "migrate8to9"
    - path: "tests/backup.test.js"
      provides: "bloque round-trip v9 + import v8→v9 con reset + sync de asserts de versión a 9 + forward-compat fixture 9→10"
      contains: "schemaVersion === 9"
  key_links:
    - from: "src/data/storage.js migrate()"
      to: "migrate8to9 + hydrateV9"
      via: "eslabón fall-through sobre s.schemaVersion === 8 / === 9"
      pattern: "schemaVersion === 8\\) s = migrate8to9"
    - from: "src/data/backup.js parseBackupFile"
      to: "migrate8to9 + hydrateV9 (importados de storage.js)"
      via: "cadena de parse antes del hydrate final"
      pattern: "migrate8to9\\(migrated\\)"
---

<objective>
Migrar el state de `schemaVersion 8` a `9` clonando LITERALMENTE el patrón validado `migrate7to8`/`hydrateV8` (plan 18-01, líneas 718-798 de storage.js), con UNA sola desviación funcional: el reset selectivo opera sobre SEIS prefijos de id de categoría (`avere`, `essere`, `verbos-movimiento`, `genero-numero`, `profesiones`, `sustantivos-irregulares`) en lugar de dos (`articoli` + `partitivos`). Extender `backup.js` al round-trip v9 (espejo de la constante + eslabón en la cadena de parse).

Purpose: Dejar libre la renumeración de ids que harán las 6 fases de contenido (22-27) — no se puede renumerar con progreso vivo. El progreso de las 6 categorías a convertir se resetea por diseño (Core Value: re-verificación constante por categoría), las 3 ya convertidas (`preposiciones`, `articoli`, `partitivos`) quedan byte-intactas.

Output: `migrate8to9`/`hydrateV9` + `CURRENT_SCHEMA_VERSION = 9` en `storage.js`; `backup.js` round-trip v9; bloques de test v9 en ambos test files. Cubre MIG-03 (storage) y MIG-04 (backup) en una sola unidad atómica (ambas comparten el bump de versión), igual que hicieron los planes canónicos 17-01 y 18-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.5-phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-01-PLAN.md
@.planning/milestones/v1.5-phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-01-SUMMARY.md

<interfaces>
<!-- Contratos exactos del analog a clonar. Líneas VERIFICADAS contra el código vivo (2026-06-05). El executor NO debe explorar el codebase: estos son los identificadores y posiciones. -->

LOS 9 SLUGS EXACTOS (verificados en content/categories.json — usar EXACTAMENTE estos strings):
- RESET (6): avere, essere, verbos-movimiento, genero-numero, profesiones, sustantivos-irregulares
- PRESERVAR (3, byte-intactas): preposiciones, articoli, partitivos

GATE DE COLISIÓN DE PREFIJO (crítico — verificar antes de implementar): el reset es por `startsWith`. Ninguno de los 3 slugs preservados (preposiciones, articoli, partitivos) empieza por ninguno de los 6 reset, y ningún slug reset es prefijo de otro slug. Verificado manualmente: los 6 reset y los 3 preservados son mutuamente exclusivos bajo `startsWith`. NO hay colisión. (Igual que D-03 en Phase 18, ahora con 6 prefijos.)

storage.js (líneas verificadas en el código vivo):
- CURRENT_SCHEMA_VERSION (línea 35) = 8 → bump a 9.
- blankState() (líneas 59-69): usa CURRENT_SCHEMA_VERSION; NO edita código, solo JSDoc (líneas 37-58, citar Phase 21 / D-21 además de Phase 18).
- migrate() chain (líneas 140-150): fall-through sobre s.schemaVersion; el eslabón terminal actual es `if (s.schemaVersion === 8) return hydrateV8(s);` (línea 150). El penúltimo es `if (s.schemaVersion === 7) s = migrate7to8(s);` (línea 149).
- migrate7to8(v7) (líneas 718-758): ANALOG a clonar → migrate8to9(v8). Devuelve objeto literal { schemaVersion: 8, ... } con deep-clone defensivo JSON.parse(JSON.stringify(...)) por sub-dict; DOS deletes (delete categoryProgress.articoli; delete categoryProgress.partitivos;); filtro exerciseStats con guard `if (!k.startsWith('articoli') && !k.startsWith('partitivos'))`; invalidación condicional de inFlightTest con `id.startsWith('articoli') || id.startsWith('partitivos')`.
- hydrateV8(parsed) (líneas 779-798): ANALOG → hydrateV9(parsed). Espejo SIN poda, solo type-guards de shape, schemaVersion: 9.

backup.js (líneas verificadas en el código vivo):
- import (línea 26): `{ ..., migrate6to7, hydrateV7, migrate7to8, hydrateV8 }` → añadir migrate8to9, hydrateV9.
- CURRENT_SCHEMA_VERSION (línea 42) = 8 → 9 (espejo inline, NO importa de storage).
- forward-compat reject (línea 112): `state.schemaVersion > CURRENT_SCHEMA_VERSION` → ya cubre > 9 al bumpear; SIN cambio de código.
- parseBackupFile cadena (líneas 121-128): la cadena termina en `if (migrated.schemaVersion === 7) migrated = migrate7to8(migrated);` (línea 127) y `migrated = hydrateV8(migrated);` (línea 128) → insertar eslabón v8 (`if (migrated.schemaVersion === 8) migrated = migrate8to9(migrated);`) ANTES del hydrate y cambiar el hydrate final a hydrateV9.

tests/data-storage.test.js:
- import (línea 28): añadir migrate8to9, hydrateV9.
- bloque analog a clonar: describe('data/storage v8 — migrate7to8 reset selectivo de articoli + partitivos (Phase 18)') líneas 862-991+ (helper v7WithArticoliPartitivos líneas 866-888; 13 tests incluido el reforzado D-04 con fixture de 9 categorías ~líneas 1058-1088).
- blankState v8: describe líneas 36-55 asserta `s.schemaVersion === 8` (líneas 39, 53) → sincronizar a 9. Test línea 637-639 ('blankState() ahora schemaVersion 8 ...') asserta 8 → sincronizar a 9.

tests/backup.test.js:
- import (línea 27): blankState, migrate2to3, hydrateV3 — NO necesita migrate8to9 directo (la cadena vive en backup.js); pero el helper round-trip nuevo y los asserts sí.
- bloque analog a clonar: describe('data/backup v8 — round-trip + import v7→v8 (Phase 18)') líneas 303-382 (helper stateV8 líneas 305-321; round-trip + import v7→v8 + preservación avere/essere).
- asserts de versión de FIN DE CADENA de current-version a sincronizar de 8 a 9: líneas 121, 155, 175, 196, 242, 329 (cubren round-trip / fin de cadena migración v1..v8 → ahora termina en v9).
- CRÍTICO — blankState(): el test de líneas 91-104 asserta `s.schemaVersion === 8` (línea 93). Tras el bump de Task 1, blankState() devuelve 9 → este assert FALLA si no se sincroniza a 9.
- CRÍTICO — forward-compat reject: el test 'rejects future schemaVersion > 8' (líneas 427-435) usa fixture `schemaVersion: 9` en AMBAS keys (wrapper línea 430 y state línea 431) y asserta `r.ok === false`. Tras bumpear backup.js a CURRENT=9, un fixture de 9 ya NO es `> 9` → la guard no dispara → el test flipea a ok:true y FALLA. Avanzar AMBAS keys del fixture de `9` a `10` y renombrar el test de `> 8` a `> 9`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: migrate8to9 + hydrateV9 + bump CURRENT_SCHEMA_VERSION (storage.js) — MIG-03</name>
  <files>src/data/storage.js, tests/data-storage.test.js</files>
  <read_first>
    - src/data/storage.js líneas 35, 37-69 (CURRENT_SCHEMA_VERSION + blankState + JSDoc), 140-150 (cadena migrate()), 695-758 (migrate7to8 — analog completo, incl. JSDoc), 760-798 (hydrateV8 — analog)
    - tests/data-storage.test.js líneas 25-28 (imports), 36-55 (blankState v8 a sincronizar), 637-639 (assert blankState a sincronizar), 852-991+ (bloque describe v8 completo + helper v7WithArticoliPartitivos + test reforzado D-04 ~1058-1088)
    - .planning/milestones/v1.5-phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-01-PLAN.md (Task 1 — patrón de clonado, behavior, action)
    - .planning/milestones/v1.5-phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-01-SUMMARY.md (desviaciones esperadas del bump: sync de asserts blankState)
  </read_first>
  <behavior>
    Tests a clonar del bloque v8 (renombrados 7to8→8to9, v7→v8, v8→v9, prefijos articoli/partitivos → los 6 reset), en un nuevo describe('data/storage v9 — migrate8to9 reset selectivo de las 6 categorías (Phase 21)'):
    - Test: migrate8to9 borra categoryProgress de las 6 (avere, essere, verbos-movimiento, genero-numero, profesiones, sustantivos-irregulares); deja preposiciones/articoli/partitivos intactas (deepEqual byte a byte).
    - Test: migrate8to9 poda exerciseStats con cualquiera de los 6 prefijos; preserva claves de preposiciones/articoli/partitivos (NO usar avere/essere como "preservado" — ahora se resetean).
    - Test: migrate8to9 invalida inFlightTest cuyos exerciseIds contienen algún id de cualquiera de los 6 prefijos (ej. un Test que toca avere; otro que toca profesiones).
    - Test: migrate8to9 preserva inFlightTest cuyos ids son SOLO de preposiciones/articoli/partitivos (ajenos a los 6 prefijos).
    - Test: migrate8to9 sin inFlightTest no crashea y preserva undefined.
    - Test: migrate8to9 es idempotente (migrate8to9(migrate8to9(x)) deep-equals migrate8to9(x)).
    - Test: migrate8to9 es puro (input v8 conserva avere/essere/etc tras la llamada).
    - Test: anti-prototype-pollution: fixture con __proto__ own-property (schemaVersion:8) no contamina el global (({}).polluted === undefined); schemaVersion de salida 9.
    - Test: con sub-dict no-objeto (corrupto) cae a {} (type-guards).
    - Test: hydrateV9 es espejo SIN poda — preserva avere/essere si llegan presentes; schemaVersion 9.
    - Test: hydrateV9 sobre v9 con sub-dicts ausentes los normaliza a {}.
    - Test: cadena v8→v9: hydrateV9(migrate8to9(...)) sale v9 con las 6 reseteadas y las 3 preservadas; schemaVersion 9.
    - Test REFORZADO (no-regresión, espejo del D-04 de Phase 18): fixture con progreso en las 9 categorías (las 6 reset + preposiciones + articoli + partitivos) en categoryProgress Y exerciseStats → migrate8to9 → asertar (a) las 6 ausentes de ambos dicts; (b) preposiciones, articoli y partitivos deep-equal byte a byte pre/post (assert.deepEqual por categoría preservada).
    Helper nuevo v8WithSixCategories(): schemaVersion 8, exerciseStats con avere-*, essere-*, verbos-movimiento-*, genero-numero-*, profesiones-*, sustantivos-irregulares-* (las 6) + preposiciones-*, articoli-*, partitivos-* (las 3 preservadas); categoryProgress con las 9 categorías (status hecha/dominada).
  </behavior>
  <action>
    RED: añadir el import de migrate8to9, hydrateV9 en tests/data-storage.test.js (línea 28). Escribir el bloque describe v9 con los 13 tests del bloque behavior + el helper v8WithSixCategories. Sincronizar a 9 los asserts blankState pre-existentes (líneas 39, 53, 637-639 — consecuencia del bump). Ejecutar `node --test tests/data-storage.test.js` — DEBE fallar (migrate8to9/hydrateV9 no existen aún). Commit test.
    GREEN: en src/data/storage.js: (1) bump CURRENT_SCHEMA_VERSION (línea 35) de 8 a 9; (2) clonar migrate7to8 → export function migrate8to9(v8) — firma (v7)→(v8), todas las refs v7.X→v8.X, schemaVersion de retorno 8→9, y las 3 desviaciones de SEIS prefijos: reemplazar los dos deletes (delete categoryProgress.articoli; delete categoryProgress.partitivos;) por SEIS deletes (delete categoryProgress.avere; delete categoryProgress.essere; delete categoryProgress['verbos-movimiento']; delete categoryProgress['genero-numero']; delete categoryProgress.profesiones; delete categoryProgress['sustantivos-irregulares'];); reemplazar el guard del filtro `if (!k.startsWith('articoli') && !k.startsWith('partitivos'))` por un guard que excluye los 6 prefijos (recomendado: definir const RESET_PREFIXES = ['avere','essere','verbos-movimiento','genero-numero','profesiones','sustantivos-irregulares'] y usar `if (!RESET_PREFIXES.some(p => k.startsWith(p)))` para no encadenar 6 condiciones — equivalente funcional, más legible); reemplazar el predicado de inFlightTest `(id.startsWith('articoli') || id.startsWith('partitivos'))` por `RESET_PREFIXES.some(p => id.startsWith(p))`. Mantener el deep-clone defensivo JSON.parse(JSON.stringify(...)) por sub-dict idéntico, y timestamps/songProgress/dailyLog idénticos byte a byte; (3) clonar hydrateV8 → export function hydrateV9(parsed) con schemaVersion: 9, SIN poda (solo type-guards de shape, idéntico al espejo); (4) en la cadena migrate() (línea 149-150) insertar `if (s.schemaVersion === 8) s = migrate8to9(s);` y `if (s.schemaVersion === 9) return hydrateV9(s);` ANTES del eslabón terminal actual, dejando que `if (s.schemaVersion === 8) return hydrateV8(s);` deje de ser terminal (pasa a migrate8to9); (5) actualizar el JSDoc de blankState (líneas 37-58) para citar Phase 21 / D-21 (las 6 categorías reseteadas) además de Phase 18 — blankState NO cambia de código (el bump es nominal, la versión sale de la constante). Mantener migrate7to8/hydrateV8 exportados por backward-compat (igual que migrate6to7/hydrateV7 se conservaron). Actualizar el comentario JSDoc de migrate8to9 enumerando que las 3 categorías preservadas (preposiciones, articoli, partitivos) NO empiezan por ninguno de los 6 prefijos (gate de colisión verificado). Ejecutar `node --test tests/data-storage.test.js` — DEBE pasar. Commit feat.
    Si algún test existente de blankState asume schemaVersion 8 y no se sincronizó en RED, sincronizar a 9 (consecuencia directa del bump, precedente 18-01).
  </action>
  <verify>
    <automated>node --test tests/data-storage.test.js 2>&1 | grep -E "^# (pass|fail)" | grep -q "fail 0" && grep -q "export function migrate8to9" src/data/storage.js && grep -q "export function hydrateV9" src/data/storage.js && grep -v '^[[:space:]]*//' src/data/storage.js | grep -q "CURRENT_SCHEMA_VERSION = 9" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/data/storage.js contiene `export function migrate8to9(` y `export function hydrateV9(`
    - CURRENT_SCHEMA_VERSION === 9 en src/data/storage.js (verificado ignorando comentarios)
    - migrate8to9 borra las 6 categorías de categoryProgress (avere, essere, verbos-movimiento, genero-numero, profesiones, sustantivos-irregulares) y filtra exerciseStats por los 6 prefijos; verificable por el test "borra categoryProgress de las 6" y "poda exerciseStats con los 6 prefijos"
    - El test de no-regresión (fixture 9 categorías) pasa: las 6 ausentes, las 3 preservadas (preposiciones, articoli, partitivos) deep-equal byte a byte
    - La cadena migrate() contiene el eslabón `s.schemaVersion === 8) s = migrate8to9` y `s.schemaVersion === 9) return hydrateV9`
    - `node --test tests/data-storage.test.js` exits 0 (fail 0)
    - El commit de test precede al commit de feat (TDD gate)
  </acceptance_criteria>
  <done>migrate8to9 + hydrateV9 implementadas y testadas; CURRENT_SCHEMA_VERSION = 9; cadena migrate() con eslabón v8→v9→hydrate; reset de las 6 categorías + preservación byte-idéntica de preposiciones/articoli/partitivos; node --test tests/data-storage.test.js verde.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: backup.js round-trip v9 + import v8→v9 con reset (MIG-04)</name>
  <files>src/data/backup.js, tests/backup.test.js</files>
  <read_first>
    - src/data/backup.js líneas 26 (import), 28-56 (JSDoc + CURRENT_SCHEMA_VERSION línea 42), 108-116 (forward-compat reject línea 112), 119-128 (cadena de migración en parseBackupFile), 148+ (buildBackupWrapper)
    - tests/backup.test.js líneas 23-29 (imports), 88-104 (test blankState v8 — assert línea 93), 121, 155, 175, 196 (asserts de versión de cadena), 242, 303-382 (bloque round-trip v8 + helper stateV8 + import v7→v8), 425-435 (forward-compat reject — fixture líneas 430-431, name línea 427)
    - src/data/storage.js (resultado de Task 1: migrate8to9, hydrateV9 exportadas)
    - .planning/milestones/v1.5-phases/18-migraci-n-7-8-reset-selectivo-articoli-partitivos/18-01-PLAN.md (Task 2 — patrón de clonado backup)
  </read_first>
  <behavior>
    Tests a clonar del bloque round-trip v8 (renombrados a v9), en un nuevo describe('data/backup v9 — round-trip + import v8→v9 (Phase 21)'):
    - Test: round-trip v9 — buildBackupWrapper → parseBackupFile sin error "versión más nueva"; wrapper.schemaVersion === 9 y r.state.schemaVersion === 9.
    - Test: round-trip v9 preserva preposiciones/articoli/partitivos progreso intacto (las 3 preservadas).
    - Test: import de backup v8 → state v9 con las 6 categorías reseteadas (clon de "import v7→v8"): fixture v8 con progreso en avere + profesiones (dos de las 6) + articoli (preservada); tras parseBackupFile, avere/profesiones podadas de categoryProgress y exerciseStats, articoli preservada (cubre MIG-04: el progreso de las 6 en backup viejo se pierde por diseño).
    - Verificación forward-compat (test existente actualizado): un backup con schemaVersion 10 (> 9) se rechaza con motivo "versión más nueva". El fixture pre-existente usaba 9 (> 8); tras el bump a CURRENT=9, debe avanzar a 10.
    Helper nuevo stateV9(): schemaVersion 9 con progreso en preposiciones/articoli/partitivos (preservados) — clon de stateV8.
    Asserts de versión existentes que cubren el fin de cadena de current-version (líneas 121, 155, 175, 196, 242) deben actualizarse de 8 a 9 porque la cadena ahora termina en v9. ADEMÁS: el assert de blankState() (línea 93) y el fixture forward-compat (líneas 430-431) también dependen del valor de current y deben sincronizarse (ver action).
  </behavior>
  <action>
    RED: en tests/backup.test.js: (a) escribir el bloque describe v9 con los tests del bloque behavior + helper stateV9; (b) actualizar a 9 los asserts de versión de fin-de-cadena de current-version en las líneas 121, 155, 175, 196, 242; (c) CRÍTICO — sincronizar línea 93: el test blankState() (líneas 91-104) asserta `s.schemaVersion === 8` → cambiar a `=== 9` (tras el bump de Task 1, blankState() devuelve 9; sin este cambio el verify falla). Opcionalmente refrescar el nombre del test "shape v8" → "shape v9" y su comentario para citar Phase 21; (d) CRÍTICO — actualizar el test forward-compat de líneas 427-435: el fixture usa `schemaVersion: 9` en las DOS keys (wrapper.schemaVersion línea 430 y state.schemaVersion línea 431) y asserta `r.ok === false`; tras bumpear backup.js a CURRENT=9, un fixture de 9 ya NO es `> 9` → la guard no dispara y el test flipea a ok:true. Avanzar AMBAS keys del fixture de `9` a `10`, y renombrar el test de `'rejects future schemaVersion > 8'` a `'rejects future schemaVersion > 9'` (y actualizar el comentario contiguo a CURRENT=9 / fixture 10). Ejecutar `node --test tests/backup.test.js` — DEBE fallar (cadena aún termina en v8). Commit test.
    GREEN: en src/data/backup.js: (1) añadir migrate8to9, hydrateV9 al import de línea 26 (mantener migrate7to8/hydrateV8 importados por backward-compat de la cadena); (2) bump CURRENT_SCHEMA_VERSION (línea 42) de 8 a 9 (espejo inline de storage.js, NO importar); (3) en parseBackupFile (líneas 127-128) insertar `if (migrated.schemaVersion === 8) migrated = migrate8to9(migrated);` ANTES del hydrate final, y cambiar `migrated = hydrateV8(migrated);` por `migrated = hydrateV9(migrated);`; (4) actualizar el JSDoc de la constante (líneas 28-41) y el comentario de la cadena (líneas 52-56) añadiendo el eslabón Phase 21 (migrate8to9 → hydrateV9). El reject forward-compat (línea 112) y buildBackupWrapper NO cambian de código — cubren > 9 y espejan dinámicamente al bumpear la constante. Ejecutar `node --test tests/backup.test.js` — DEBE pasar. Commit feat.
    Verificación final de toda la suite: `node --test tests/*.test.js` debe seguir verde (358 baseline + nuevos v9).
  </action>
  <verify>
    <automated>node --test tests/backup.test.js 2>&1 | grep -E "^# (pass|fail)" | grep -q "fail 0" && grep -q "migrate8to9" src/data/backup.js && grep -v '^[[:space:]]*\*' src/data/backup.js | grep -v '^[[:space:]]*//' | grep -q "CURRENT_SCHEMA_VERSION = 9" && node --test tests/*.test.js 2>&1 | grep -E "^# fail" | grep -q "fail 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/data/backup.js importa migrate8to9 y hydrateV9 de ./storage.js
    - CURRENT_SCHEMA_VERSION === 9 en src/data/backup.js (espejo, verificado ignorando comentarios)
    - parseBackupFile contiene el eslabón `migrated.schemaVersion === 8) migrated = migrate8to9(migrated)` y el hydrate final es hydrateV9
    - Test round-trip v9: wrapper.schemaVersion === 9 y r.state.schemaVersion === 9
    - Test import v8→v9: las 6 categorías reseteadas (verificable con avere + profesiones), las 3 preservadas (verificable con articoli) intactas tras parseBackupFile
    - El test blankState() (línea ~93) asserta schemaVersion === 9 (sincronizado tras el bump)
    - El test forward-compat usa fixture schemaVersion 10 (> 9) y se rechaza con "versión más nueva"
    - `node --test tests/backup.test.js` exits 0 (fail 0) Y `node --test tests/*.test.js` exits 0 (suite completa verde)
    - El commit de test precede al commit de feat (TDD gate)
  </acceptance_criteria>
  <done>backup.js extendido a round-trip v9 (constante espejo, eslabón en la cadena, import); import de backup v8 migra a v9 reseteando las 6 categorías; backups > 9 rechazados; asserts de versión y blankState sincronizados a 9; suite completa verde con node --test tests/*.test.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archivo de backup JSON → parseBackupFile | El backup importado es entrada no confiable (puede venir manipulado o corrupto); cruza a la lógica de migración como objeto parseado |
| localStorage state → migrate() | El state persistido puede haber sido editado a mano en DevTools; cruza a la cadena de migración al arrancar |

Nota de proporcionalidad: herramienta single-user local-only, sin red, sin auth, sin servidor. La única superficie real es el parseo de JSON arbitrario (state propio o backup importado).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | migrate8to9 / hydrateV9 (JSON con `__proto__` own-property crafted) | mitigate | Preservar el deep-clone defensivo `JSON.parse(JSON.stringify(...))` por sub-dict del analog migrate7to8 (neutraliza `__proto__` como own-property y getters); reconstruir el root como objeto literal fresco. Cubierto por el test anti-prototype-pollution clonado (({}).polluted === undefined) |
| T-21-02 | Tampering | poda sobre-amplia, colisión de prefijo, o que muta el input | mitigate | La poda (6 deletes + filtro por 6 prefijos) opera SOLO sobre el clon, nunca sobre el input → garantiza pureza. Gate de colisión verificado: los 6 reset y los 3 preservados son mutuamente exclusivos bajo startsWith (preposiciones/articoli/partitivos NO empiezan por ningún reset). Cubierto por test de pureza + test de no-regresión (las 3 preservadas deep-equal byte a byte) + test de idempotencia |
| T-21-03 | Denial of Service | inFlightTest stale tras reset (crash al reanudar Test con ids de las 6 categorías ya inexistentes) | mitigate | migrate8to9 invalida inFlightTest si algún exerciseId empieza por cualquiera de los 6 prefijos (no se confía del guard de UI). Cubierto por test "invalida inFlightTest que toca las 6" + "preserva el que solo toca las 3 preservadas" |
| T-21-04 | Elevation of Privilege | import de backup con schemaVersion > 9 (versión futura no soportada) | mitigate | El reject `state.schemaVersion > CURRENT_SCHEMA_VERSION` ya cubre > 9 automáticamente al bumpear la constante a 9 (forward-compat). Cubierto por test de rechazo de backup futuro (fixture schemaVersion 10) |
| T-21-05 | Tampering | sub-dict corrupto (no-objeto) en state/backup | mitigate | Type-guards `typeof x === 'object' && x !== null ? clone : {}` por sub-dict (heredados del analog). Cubierto por test "sub-dict no-objeto cae a {}" |
| T-21-SC | Tampering | instalaciones npm/pip/cargo | accept | N/A — zero-deps, no hay instalaciones de paquetes en esta fase (proyecto static web sin build) |
</threat_model>

<verification>
Verificación de toda la fase:
- `node --test tests/*.test.js` verde (baseline 358 + los nuevos tests v9 de ambos archivos).
- `grep -q "export function migrate8to9" src/data/storage.js` y `grep -q "export function hydrateV9" src/data/storage.js`.
- `CURRENT_SCHEMA_VERSION === 9` en storage.js Y backup.js (espejo coherente).
- Reset selectivo verificado: las 6 categorías reseteadas, las 3 preservadas (preposiciones, articoli, partitivos) byte-idénticas (test de no-regresión).
- Round-trip backup v9 + import v8→v9 con reset + rechazo > 9.
</verification>

<success_criteria>
- MIG-03 cubierto: migrate8to9 + hydrateV9 idempotentes, deep-clone defensivo, reset SOLO de las 6 categorías (categoryProgress + exerciseStats por prefijo + inFlightTest), racha 0; las 3 ya convertidas (preposiciones, articoli, partitivos) byte-intactas.
- MIG-04 cubierto: backup.js round-trip v9 (export v9 reimportable); import de backup v8 migra a v9 reseteando las 6 categorías; backups > 9 rechazados.
- La app arranca limpia sobre el state migrado y la suite completa sigue verde.
</success_criteria>

<output>
Create `.planning/phases/21-migraci-n-8-9-reset-selectivo-de-las-6-categor-as/21-01-SUMMARY.md` when done
</output>
