---
phase: 27-sostantivi-irregolari-a-slots-contenido-l-xica
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - content/exercises/sustantivos-irregulares.json
  - .planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md
autonomous: false
requirements: [SOST-02]
must_haves:
  truths:
    - "Cada variante NUEVA autorada pasa el quorum cross-vendor R1-R7 (base canonica Claude Opus 4.8 + Sonnet 4.6 via skill gsd-validate-exercise; refuerzo Gemini + DeepSeek via validate-ai-pass) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot; 1-por-1, fresh context, NUNCA batched (VAL-03)"
    - "Las variantes nuevas se autoran SOLO en los slots del BLOQUE REGLA (D-27-03): (a) sovrabbondanti -o->-a (mas partes del cuerpo / sobreabundantes A1-A2: ciglio->ciglia, sopracciglio->sopracciglia, paio->paia... — VERIFICAR la forma plural real); (b) invariabili-accentate (mas acentuadas/truncas: virtu, tribu, novita...); (c) invariabili-straniere (mas extranjeras/consonante: bar, computer, autobus...). El BLOQUE LEXICO PURO (cambio-radice: uomo/dio/bue/tempio) y el BLOQUE CONTRASTE (plurali-regolari) NO reciben NINGUNA variante nueva — documentado como cumplimiento explicito de SOST-01/D-27-03/D-27-05"
    - "Cada palabra asignada a una sub-regla del bloque regla se VERIFICO que toma realmente esa forma plural en italiano (D-27-04): el quorum cross-vendor caza cualquier plural inventado o invariabilidad erronea antes de entrar (NO inventar -a donde el plural es regular; NO declarar invariable una palabra que si flexiona; NO asignar al slot acentuadas una extranjera ni viceversa)"
    - "Las variantes nuevas se autoran con ambicion GENEROSA sin cuota fija (D-27-03, hereda D-26-04/D-25-04); ninguna lleva explanation propia (comparten la del slot); cualquier slot NUEVO (recomendado: ninguno) lleva validation.status top-level (D-19-09)"
    - "Las superficies existentes movidas intactas en 27-01 NO se re-validan (D-27-10); ninguna explanation nueva/tocada referencia las categorias Genere e numero / Articoli por id ni prosa (D-27-06); el set sigue 100% MC (sin match ni word-buttons introducidos); el bloque lexico (cambio-radice) y el de contraste (plurali-regolari) quedan intactos"
  artifacts:
    - path: "content/exercises/sustantivos-irregulares.json"
      provides: "variantes nuevas validated integradas en variants[] de los slots del bloque regla (sovrabbondanti, invariabili-accentate, invariabili-straniere), + cualquier slot nuevo con validation top-level"
      contains: "\"variants\""
    - path: ".planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md"
      provides: "set propuesto de superficies nuevas (SOLO bloque regla) con id temporal + slot destino + superficie + justificacion R1-R7 + forma plural verificada; seccion explicita de que el bloque lexico y el de contraste no reciben variantes (SOST-01/D-27-05)"
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quorum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS con ambicion GENEROSA (SOST-02, D-27-03) donde la regla admite reformulacion natural, materializando los EJES DE HUECOS PRIORIZADOS de Sostantivi irregolari — SOLO en los slots del BLOQUE REGLA (D-27-01/D-27-03). El BLOQUE LEXICO PURO (cambio de raiz: uomo/dio/bue/tempio) y el BLOQUE CONTRASTE (parentesco regular: donna/padre/madre/fratello/sorella/marito) NO reciben NINGUNA variante nueva: SOST-01 prohibe forzar variantes artificiales y D-27-03/D-27-05 lo documentan como cumplimiento explicito (los lemas de cambio-de-raiz son memorizables, raiz impredecible, no admiten variantes intercambiables; el contraste son foils que no se engordan, no es el proposito de la categoria).

Ejes de huecos PRIORIZADOS (D-27-03, el autor los marca/afina en el checkpoint), SOLO en el bloque regla:

1. SOVRABBONDANTI -o sing -> -a plur fem (engorde del slot `sustantivos-irregulares-sovrabbondanti`): mas partes del cuerpo / sustantivos sobreabundantes A1-A2 plausibles que cambian de genero al pluralizar — ciglio->ciglia, sopracciglio->sopracciglia, paio->paia, lenzuolo->lenzuola... (uovo/braccio/dito/ginocchio/labbro/osso ya en legacy — no duplicar). La trampa A1 estrella: masc -o singular -> fem -a plural; el hispanohablante regulariza (*cigli, *paii). VERIFICAR que cada palabra toma realmente el plural sobreabundante en -a (no inventar; descartar las dudosas o que no sean A1-A2 naturales).

2. INVARIABILI-ACCENTATE (vocal tonica final) (engorde del slot `sustantivos-irregulares-invariabili-accentate`): mas acentuadas/truncas — virtu, tribu, novita, citta? (ya en legacy — no duplicar), universita? (ya en legacy). Prompt fija el singular; respuesta = la MISMA forma; distractoras = el calco flexionado (*virtudi/*tribi). VERIFICAR que cada palabra acaba en vocal tonica y es realmente invariable.

3. INVARIABILI-STRANIERE (extranjerismos/consonante final) (engorde del slot `sustantivos-irregulares-invariabili-straniere`): mas extranjeras/consonante — bar, computer, autobus, sport? (ya en legacy — no duplicar), film? (ya en legacy). Respuesta = la MISMA forma; distractoras = el calco -s ingles (*films/*computers) o flexion italiana inventada (*bari). VERIFICAR que la palabra es realmente invariable en italiano.

Cada superficie NUEVA pasa el quorum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched (VAL-03), con revision del autor por variante (D-85). Las superficies movidas intactas en 27-01 NO se re-validan (D-27-10). Sin cuota fija de densidad (D-27-03 hereda D-26-04/D-25-04): proponer variantes donde la regla lo pida, no por cumplir un minimo (evita variantes de relleno). RESTRICCION D-27-06: ninguna explanation nueva/tocada referencia las categorias Genere e numero / Articoli por id ni prosa (el genero/numero como contenido del ejercicio es valido). NO autorar variantes en el bloque lexico (cambio-radice) ni en el de contraste (plurali-regolari) (SOST-01: esos slots quedan como en 27-01). El set sigue 100% MC: NO introducir match ni word-buttons.

El planner/autor decide en plan-time/checkpoint si los huecos materializan VARIANTES de slots existentes (lo natural — los ejes engordan los slots del bloque regla de 27-01) o algun SLOT NUEVO. Recomendacion del planner: NO se necesitan slots nuevos — los ejes engordan los slots existentes (sovrabbondanti, invariabili-accentate, invariabili-straniere). Si el autor crea algun slot nuevo, este requiere validation.status top-level (D-19-09).

Purpose: las variantes nuevas materializan el dolor anti-memorizacion (re-hacer un slot puede tocar una palabra distinta) y cierran los huecos pedagogicos del bloque regla de Sostantivi irregolari, todos centrados en las trampas A1 estrella del hispanohablante: el cambio de genero masc->fem de los sovrabbondanti (il braccio -> le braccia), el calco flexionado en las acentuadas (*cittadi) y el calco -s ingles en las extranjeras (*films). El quorum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazo 8 bugs en Articoli, 3 en Avere, 2 en Essere, 1 en Professioni que el human-verify aprobo) y la unica garantia de que cada palabra toma realmente la forma plural reclamada (D-27-04 — saber si un sustantivo es sobreabundante, invariable acentuado o invariable extranjero es la trampa central). R1/R5 criticos: el prompt no debe leak la forma correcta; las options deben tener 3+ valores distintos (distractoras = pluralizacion erronea plausible, p.ej. el calco regular *cigli o el calco -s *films).

SOST-02 SE CUMPLE ASI: hay autoria de variantes donde hay regla (sovrabbondanti + invariabili); se documenta explicitamente en 27-VARIANTES-NUEVAS.md que el bloque lexico (cambio-radice) y el de contraste (plurali-regolari) NO admiten autoria de variantes (cumplimiento de SOST-01).

NO existe snapshot/assert APPEND-ONLY para Sostantivi irregolari (avere-only, verificado: el unico es scripts/.avere-prefix-snapshot.json hardcoded a avere; 0 refs a sustantivos). NO ejecutar ningun script de snapshot ni assert de prefijo. NO existen cruces multi-cat (no hay sustantivos-irregulares-300..305); NO crearlos. A diferencia de 22-02 (que re-basaba el snapshot de Avere), aqui NO hay paso de re-base — igual que 23-02/24-02/25-02/26-02.

Output: `sustantivos-irregulares.json` con las variantes nuevas de los ejes del bloque regla integradas; cada superficie nueva con su quorum verde en `validation.passes[]`; cualquier slot nuevo con validation.status top-level (D-19-09); el bloque lexico (cambio-radice) y el de contraste (plurali-regolari) intactos (SOST-01/D-27-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/sustantivos-irregulares.json

<interfaces>
<!-- Pipeline de quorum cross-vendor. Verificado en validate-ai-pass.mjs + skill. NO modificar estos scripts/skill. Mismo flujo ejecutado con exito en Phase 19 (8 superficies), Phase 20 (6), Phase 22 (14), Phase 23 (14), Phase 24, Phase 25 (20), Phase 26 (12), 0 excluidas. -->
Quorum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Base canonica (D-27-11) — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus->Sonnet):
  /gsd-validate-exercise <id>
  (model IDs literales que emite el skill; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio. Mantener los IDs literales del skill por consistencia del audit trail con el contenido existente — confirmado en Phase 23 Open Q #1.)
  Fallback cuando Task no esta disponible al executor (precedente Phase 19/20/22/23/24/25/26, D-19-08): la mitad Claude se corre con `claude -p --model <opus>` y `--model <sonnet>` headless, 1 proceso por modelo por superficie, con el 09-VALIDATION-PROMPT verbatim y el ejercicio SIN su campo validation (no sesgar). Equivalente funcional 1-por-1 NUNCA batched. NOTA: la base canonica de aprobacion es Claude Opus 4.8 + Sonnet 4.6 (D-27-11); usar los model IDs literales que emite el skill para el audit trail.

Refuerzo — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondio; >=2 by distintos para el quorum; status re-derivado por deriveStatus, NO hand-editar status. Si Gemini agota cuota 429/503, DeepSeek cubre los 2 externos con by distintos chat + reasoner — precedente 22-VERIFICATION / 23-02 / 24-02 / 25-02 / 26-02. PITFALL CORREGIDO: forzar deepseek-reasoner como 2o externo o ambos colapsan al mismo `by`.)

Gate D-17-07: el passes[] final de cada superficie nueva = >=4x "correcta" (opus + sonnet + 2 externos by distintos), CERO "incorrecta". deriveStatus -> "validated"; cualquier "incorrecta" -> "disputed" (sticky, bloquea, NO entra al slot).

Pitfall: el VALIDATION-PROMPT es legacy-payload-centrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/sustantivos-irregulares.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quorum, mover la superficie al variants[] del slot (sin re-validacion) y eliminar el id temporal legacy.

Alcance D-27-03 (ambicion generosa, hereda D-26-04/D-25-04) — SOLO bloque regla, sin cuota fija:
- Engordar las celdas del bloque regla marcadas en 27-01 + materializar los ejes de huecos. NO inflar slots cuya regla no admite reformulacion natural; sin minimo por slot. NO autorar NADA en el bloque lexico (cambio-radice) ni en el de contraste (plurali-regolari) (SOST-01/D-27-05: esos slots quedan como en 27-01; solo el bloque regla recibe variantes nuevas).

Los ejes de huecos PRIORIZADOS (D-27-03, R1/R5 criticos, VERIFICAR la forma plural real de cada palabra):
1. SOVRABBONDANTI -o->-a (slot sovrabbondanti): mas partes del cuerpo / sobreabundantes (ciglio->ciglia, sopracciglio->sopracciglia, paio->paia, lenzuolo->lenzuola). El prompt fija el singular masculino; respuesta = el plural fem en -a; distractoras = el plural regular masc (*cigli) + el calco -s + flexion inventada. VERIFICAR que cada palabra toma realmente el plural sobreabundante en -a (NO *cigli como respuesta; NO inventar). NO duplicar las del legacy (uovo/braccio/dito/ginocchio/labbro/osso; orecchio si entro en 27-01).
2. INVARIABILI-ACCENTATE (slot invariabili-accentate): virtu, tribu, novita, eta. Prompt fija el singular; respuesta = la MISMA forma; distractoras = el calco flexionado (*virtudi, *tribi) + plural inventado. VERIFICAR (R1) que el prompt no leak la respuesta; (R5) options con 3+ valores distintos; que la palabra acaba en vocal tonica y es realmente invariable. NO duplicar las del legacy (citta/caffe/universita).
3. INVARIABILI-STRANIERE (slot invariabili-straniere): bar, computer, autobus, gas. Respuesta = la MISMA forma; distractoras = el calco -s ingles (*computers, *bars) + flexion italiana inventada. VERIFICAR que la palabra es realmente invariable. NO duplicar las del legacy (film/sport).
- El planner/autor decide en plan-time/checkpoint que huecos son naturales. Si un hueco no tiene plural A1-A2 natural (dudoso/literario) o la palabra es ambigua, NO se materializa.

Validation top-level para la gate (D-19-09): si el autor crea algun slot NUEVO, debe portar el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum cross-vendor de sus variantes (el gate VAL_07_STRICT lee `ex.validation?.status` a nivel de slot). Si todos los huecos engordan slots existentes (recomendado), esos slots ya tienen su validation top-level de 27-01 (de las superficies fuente movidas) — NO degradarla; el passes[] de las variantes nuevas se anade donde el flujo lo registre, pero la gate de slot ya esta cubierta. NO inventar validacion: elevar la aprobada.

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quorum, un scan ASCII/tildes (italiano: acentos donde aplique — citta/caffe/virtu/tribu llevan tilde; espanol RAE en explanations; apostrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial. OJO: las acentuadas (virtu/tribu/novita/eta) DEBEN llevar su tilde italiana en la vocal tonica final.
Gloss ES (R7 canon): mantener "(en espanol: ...)" donde desambigue doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de politica (base de aprobacion = Claude Opus+Sonnet, D-27-11).
D-27-06: ninguna explanation nueva/tocada referencia las categorias Genere e numero / Articoli por id ni prosa; el genero/numero como contenido del ejercicio es valido.

BLOQUE LEXICO Y CONTRASTE INTACTOS (SOST-01/D-27-03/D-27-05): el cambio-radice (uomo/dio/bue/tempio) y el plurali-regolari (parentesco) NO reciben variantes nuevas; documentar en 27-VARIANTES-NUEVAS.md una seccion explicita "El bloque lexico (cambio-radice) y el de contraste (plurali-regolari) no admiten autoria de variantes (SOST-01/D-27-05) — solo se autoran variantes en el bloque regla (sovrabbondanti + invariabili)".

NO snapshot: Sostantivi irregolari no tiene snapshot/assert APPEND-ONLY (avere-only, verificado). NO ejecutar snapshot-avere-prefix.mjs ni assert-avere-prefix-unchanged.mjs ni buscar .sustantivos-irregulares-prefix-snapshot.json. NO crear cruces 300..305 (no existen; fuera de scope). NO introducir match ni word-buttons (set 100% MC). A diferencia de 22-02 NO hay re-base de snapshot aqui.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (SOLO bloque regla, los ejes de huecos D-27-03) con forma plural verificada</name>
  <files>.planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/sustantivos-irregulares.json (estado tras 27-01 — los slots ya reagrupados HIBRIDO, para saber que slots del bloque regla existen, cuales son celdas pobres y que admite variante nueva; y para NO duplicar palabras del legacy)
    - .planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-REAGRUPACION-MAP.md (las celdas del bloque regla marcadas como candidatas + los ejes de huecos anotados en 27-01 + la seccion de la decision hibrida)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints del quorum: 1-por-1, fresh context, NUNCA batched; model IDs literales)
    - .planning/phases/26-professioni-a-slots-contenido-l-xica/26-02-PLAN.md (Task 1 analogo — formato de 26-VARIANTES-NUEVAS.md a espejar, la otra lexica hibrida; la seccion explicita "bloque lexico sin variantes" -> aqui "bloque lexico cambio-radice + contraste sin variantes") y .planning/phases/26-professioni-a-slots-contenido-l-xica/26-VARIANTES-NUEVAS.md (la plantilla concreta, si existe; si no, 25-VARIANTES-NUEVAS.md)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6/R7 si accesible) y gloss_es_desambiguacion_canon.md (gloss ES canon)
  </read_first>
  <action>
    Proponer un set de superficies nuevas con ambicion GENEROSA (D-27-03) pero sin cuota fija, materializando los EJES DE HUECOS PRIORIZADOS SOLO del bloque regla: (1) SOVRABBONDANTI -o->-a para el slot sovrabbondanti: ciglio->ciglia, sopracciglio->sopracciglia, paio->paia, lenzuolo->lenzuola; prompt fija el singular masculino, respuesta = el plural fem en -a, distractoras = el plural regular masc (*cigli) + el calco -s + flexion inventada; VERIFICAR que cada palabra toma realmente el plural sobreabundante en -a (no *cigli como respuesta). (2) INVARIABILI-ACCENTATE para el slot invariabili-accentate: virtu, tribu, novita, eta (con su tilde italiana en la vocal tonica final); prompt fija el singular, respuesta = la MISMA forma, distractoras = el calco flexionado (*virtudi, *tribi); VERIFICAR que la palabra es realmente invariable y acentuada. (3) INVARIABILI-STRANIERE para el slot invariabili-straniere: bar, computer, autobus, gas; respuesta = la MISMA forma, distractoras = el calco -s ingles (*computers) + flexion italiana inventada; VERIFICAR que la palabra es realmente invariable. VERIFICAR explicitamente por superficie: (R1) el prompt no leak la forma correcta; (R5) las options tienen 3+ valores distintos; que la palabra toma realmente el plural reclamado en italiano A1-A2 (D-27-04 — no inventar plurales; descartar las dudosas/literarias). Cumplir R1-R7 (sin leak en prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigue doble-validez). RESTRICCION D-27-06: ninguna explanation referencia las categorias Genere e numero / Articoli por id ni prosa; el genero/numero como contenido del ejercicio es valido. NO PROPONER NINGUNA variante para el bloque lexico (cambio-radice: uomo/dio/bue/tempio) ni para el de contraste (plurali-regolari: parentesco) — esos slots quedan como en 27-01 (SOST-01/D-27-05). Pasar un scan de acentos/ASCII (tildes italianas en las acentuadas virtu/tribu/novita/eta; espanolas RAE en explanations; apostrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. NO duplicar palabras ya presentes en el legacy (verificar contra el JSON tras 27-01: uovo/braccio/dito/ginocchio/labbro/osso/orecchio en sovrabbondanti; citta/caffe/universita en acentuadas; film/sport en extranjeras). Producir `.planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md` (espejar el formato de 26-VARIANTES-NUEVAS.md): lista de cada superficie nueva con su id temporal (para el quorum aislado), slot destino (existente — recomendado que los ejes engorden los slots del bloque regla de 27-01; marcar cualquier slot NUEVO si el autor lo pide), eje al que pertenece (1-3), prompt/options/correctIndex/explanation, la justificacion de por que la regla admite reformulacion, y la VERIFICACION de la forma plural real de la palabra. Incluir una SECCION EXPLICITA "Bloque lexico (cambio-radice) y contraste (plurali-regolari): SIN autoria de variantes (SOST-01/D-27-05)" que documente que esos slots no reciben variantes nuevas (cumplimiento explicito de SOST-01). Recomendar en una seccion que NO se necesitan slots nuevos (los ejes engordan los slots existentes) — surfacear al autor. Reportar el conteo de superficies nuevas propuestas por eje Y cuantas (si alguna) son SLOTS NUEVOS (los slots nuevos suben el count de 27-03; las variantes no).
  </action>
  <verify>
    <automated>test -f .planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md && grep -ci "sovrabbond\|invariab\|accentat\|stranier\|SOST-01\|cambio-radic\|plurali-regolar" .planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 27-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + eje (1-3) + superficie + explanation
    - EJE 1 (sovrabbondanti -o->-a) cubierto: variantes de partes del cuerpo / sobreabundantes (ciglio->ciglia, sopracciglio->sopracciglia, paio->paia...) para el slot sovrabbondanti, con la verificacion del plural sobreabundante real (no *cigli como respuesta)
    - EJE 2 (invariabili-accentate) cubierto: variantes acentuadas/truncas (virtu/tribu/novita/eta con su tilde) para el slot invariabili-accentate, cada una con la verificacion de invariabilidad y con la distractora del calco flexionado (*virtudi/*tribi)
    - EJE 3 (invariabili-straniere) cubierto: variantes extranjeras/consonante (bar/computer/autobus...) para el slot invariabili-straniere con la verificacion de invariabilidad y la distractora del calco -s ingles
    - Hay una SECCION EXPLICITA "Bloque lexico (cambio-radice) y contraste (plurali-regolari): SIN autoria de variantes (SOST-01/D-27-05)" que documenta que esos slots no reciben variantes nuevas
    - Cada superficie nueva cumple R1 (sin leak de la forma correcta en el prompt) y R5 (options con 3+ valores distintos); cada explanation/prompt pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers; ninguna referencia las categorias Genere e numero / Articoli por id/prosa (D-27-06); las acentuadas llevan su tilde italiana en la vocal tonica final
    - Cada palabra lleva su VERIFICACION de forma plural real (D-27-04) — no se inventan plurales; ninguna palabra duplica una ya presente en el legacy tras 27-01
    - Hay una seccion que recomienda NO crear slots nuevos (los ejes engordan los slots existentes), surfaceada al autor
    - El conteo de superficies nuevas propuestas por eje Y de slots nuevos (si alguno) esta reportado
  </acceptance_criteria>
  <done>
    27-VARIANTES-NUEVAS.md lista el set generoso de superficies nuevas de los 3 ejes del bloque regla D-27-03 (sovrabbondanti -o->-a + invariabili-accentate + invariabili-straniere) con superficies R1-R7-conformes, scan de acentos pasado (tildes italianas en las acentuadas), sin refs a Genere e numero/Articoli, sin duplicar palabras del legacy, y la verificacion de la forma plural real de cada palabra; seccion explicita de que el bloque lexico (cambio-radice) y el de contraste (plurali-regolari) NO reciben variantes (SOST-01/D-27-05); recomendacion de no crear slots nuevos surfaceada; conteo de superficies por eje y de slots nuevos reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (27-VARIANTES-NUEVAS.md) — SOLO los ejes del bloque regla D-27-03: sovrabbondanti -o->-a (ciglio->ciglia, sopracciglio->sopracciglia, paio->paia), invariabili-accentate (virtu/tribu/novita/eta), invariabili-straniere (bar/computer/autobus) — cumpliendo R1-R7, con scan de acentos y la verificacion de la forma plural real de cada palabra. El bloque lexico (cambio-radice: uomo/dio/bue/tempio) y el de contraste (plurali-regolari) NO reciben variantes (SOST-01/D-27-05). AUN NO validadas por quorum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md`.
    2. EJE 1 (sovrabbondanti): ciglio->ciglia, sopracciglio->sopracciglia, paio->paia, lenzuolo->lenzuola. Confirma que cada palabra toma realmente el plural sobreabundante en -a (fem plural), NO el regular masc en -i (*cigli, *paii). Las distractoras incluyen el plural regular masc como trampa? El prompt NO contiene la respuesta (R1)?
    3. EJE 2 (invariabili-accentate): virtu, tribu, novita, eta. Son realmente invariables (vocal tonica final)? La respuesta = la misma forma? Llevan su tilde italiana? Las options incluyen el calco flexionado (*virtudi/*tribi) como distractora (R5)?
    4. EJE 3 (invariabili-straniere): bar, computer, autobus, gas. Son realmente invariables en italiano? La respuesta = la misma forma? Las options incluyen el calco -s ingles (*computers) como distractora?
    5. Confirma que NINGUNA palabra duplica una ya presente en el legacy (uovo/braccio/dito/ginocchio/labbro/osso/orecchio; citta/caffe/universita; film/sport ya estan).
    6. Confirma que NINGUNA explanation referencia las categorias Genere e numero / Articoli por id ni prosa (D-27-06) — el genero/numero como contenido del ejercicio SI es valido.
    7. Confirma que el bloque lexico (cambio-radice: uomo/dio/bue/tempio) y el de contraste (plurali-regolari: parentesco) NO reciben variantes nuevas (SOST-01/D-27-05) — la seccion explicita esta presente.
    8. Confirma el alcance: generoso pero sin variantes artificiales en reglas que no se reformulan (D-27-03); decide si algun eje crea un slot nuevo (recomendado: NO, todos engordan slots existentes).
    9. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quorum (especialmente cualquier sobreabundante cuyo plural en -a dudes — algunos sustantivos sobreabundantes tienen doble plural con matiz semantico; y cualquier invariable de la que no estes seguro).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las superficies aprobadas al quorum cross-vendor, o lista las que descartar/reformular (y cualquier palabra cuyo plural/invariabilidad quieras que el quorum confirme con prioridad — especialmente los sobreabundantes en -a del eje 1 y las invariables de los ejes 2/3).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quorum cross-vendor (1-por-1) e integrarla al slot del bloque regla</name>
  <files>content/exercises/sustantivos-irregulares.json</files>
  <read_first>
    - .planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales del skill, 1 commit por ejercicio; fallback `claude -p` headless si no hay Task, D-19-08)
    - content/exercises/sustantivos-irregulares.json (slots del bloque regla destino donde integrar las superficies tras pasar el quorum; NO tocar el slot cambio-radice ni el plurali-regolari)
    - .planning/phases/26-professioni-a-slots-contenido-l-xica/26-02-PLAN.md (Task 2 analogo — el flujo real: superficies por eje, disputes resueltas por reformulacion, fallback DeepSeek reasoner cuando Gemini 429) y .planning/phases/25-genere-e-numero-a-slots-contenido/25-02-PLAN.md (precedente con engorde de slots de plural/genero)
  </read_first>
  <action>
    Para CADA superficie nueva aprobada (SOLO bloque regla), ejecutar el ciclo completo de quorum 1-por-1 (NUNCA batched, VAL-03): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/sustantivos-irregulares.json como entry temporal o tests/fixtures/*.json). (2) Base canonica Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus->Sonnet secuencial, 1 spawn; model IDs literales del skill; base canonica de aprobacion = Opus 4.8 + Sonnet 4.6, D-27-11); fallback `claude -p --model <opus>` / `--model <sonnet>` headless con el ejercicio SIN su campo validation si no hay Task (D-19-08). (3) Refuerzo externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write` (si Gemini agota cuota 429/503, usar deepseek-chat + deepseek-reasoner como 2 by externos distintos, precedente 22-VERIFICATION / 23-02 / 24-02 / 25-02 / 26-02). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4x "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` -> la superficie queda disputed y se resuelve por REFORMULACION (calidad > tokens, NO override-atajo; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de politica; si un externo alucina un acento sobre texto correcto, re-run sobre el MISMO texto, precedente Phase 19/22/23/25/26) o se descarta — NO entra al slot disputed. PRESTAR ESPECIAL ATENCION (D-27-04) a que el quorum confirme la FORMA PLURAL REAL: si una superficie de sovrabbondanti resulta `incorrecta` por usar el plural regular masc (cigli en vez de ciglia) como respuesta, reformular o descartar; idem si una invariable acentuada/extranjera usa una flexion inventada (*virtudi/*computers) como respuesta correcta por error, o si una palabra del eje 1 NO es realmente sobreabundante. (5) Tras pasar el quorum, mover la superficie validada al variants[] del slot destino (existente, para los ejes: sovrabbondanti, invariabili-accentate, invariabili-straniere) o crear el slot NUEVO `{id, type:"multiple-choice", categoryIds:["sustantivos-irregulares"], explanation, variants:[...], validation}` SOLO si el autor lo aprobo; eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Para cualquier slot NUEVO, anadir el bloque `validation` top-level ({status:"validated", passes[]}) reflejando el quorum aprobado de sus variantes (D-19-09). Para los slots EXISTENTES engordados, NO degradar su validation top-level de 27-01 (ya cubre la gate de slot). NO re-validar las superficies movidas intactas en 27-01 (D-27-10). RESTRICCION D-27-06: ninguna explanation tocada referencia las categorias Genere e numero / Articoli por id ni prosa. NO autorar NI tocar NADA en el bloque lexico (cambio-radice: uomo/dio/bue/tempio) ni en el de contraste (plurali-regolari: parentesco) (SOST-01/D-27-05: esos slots quedan exactamente como en 27-01). NO introducir match ni word-buttons (set 100% MC). Hacer 1 commit por superficie validada. NUNCA validar mas de una superficie en un mismo contexto/spawn (VAL-03). NO ejecutar ningun script de snapshot ni assert de prefijo (no existen para Sostantivi irregolari; avere-only). NO crear cruces 300..305.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs sustantivos-irregulares content/exercises/sustantivos-irregulares.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs sustantivos-irregulares content/exercises/sustantivos-irregulares.json` exits 0
    - Los slots del bloque regla engordaron: sovrabbondanti, invariabili-accentate y/o invariabili-straniere tienen mas variantes que tras 27-01 (verificable contando variants[] de cada slot)
    - El eje 1 incluye al menos un sobreabundante nuevo en -a (p.ej. ciglia) en el slot sovrabbondanti: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); const s=d.exercises.find(e=>/sovrabbond/.test(e.id)); console.log('sovrabbondanti variants:', s?s.variants.length:0)"` > el conteo de 27-01
    - Cada superficie NUEVA paso el quorum con >=4x "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se valido como legacy)
    - Todo slot con variants[] sigue con validation.status=="validated" top-level: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); console.log('slots variants sin validation top-level:', d.exercises.filter(e=>Array.isArray(e.variants)&&e.validation?.status!=='validated').length)"` imprime `0`
    - Ningun id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 27-01 NO se re-validaron (# invocaciones de quorum == # superficies nuevas)
    - El bloque lexico (cambio-radice) y el de contraste (plurali-regolari) estan INTACTOS (SOST-01/D-27-05): el slot cambio-radice y el plurali-regolari tienen el MISMO numero de variantes que tras 27-01 (no engordaron): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); const c=d.exercises.find(e=>/radic/.test(e.id)); const p=d.exercises.find(e=>/plurali.*regolar|regolar/.test(e.id)); console.log('cambio-radice variants:', c?c.variants.length:0, 'plurali-regolari variants:', p?p.variants.length:0)"` (comparar contra el SUMMARY de 27-01: ambos sin cambios)
    - El set sigue 100% MC (sin match/WB introducidos): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); console.log('no-MC:', d.exercises.filter(e=>e.type!=='multiple-choice').length)"` imprime `0`
    - NO se crearon cruces: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); console.log('cruces:', d.exercises.filter(e=>/-30[0-5]$/.test(e.id)).length)"` imprime `cruces: 0`; todos los slots categoryIds=["sustantivos-irregulares"]
    - Ninguna explanation referencia las categorias Genere e numero / Articoli por id/prosa: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/sustantivos-irregulares.json','utf8')); console.log('refs cross-cat:', d.exercises.filter(e=>/genero-numero-\d|articoli-\d|ver Genere|ver G[eé]nero|ver Articoli/i.test(e.explanation||'')).length)"` imprime `0` (D-27-06)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/sustantivos-irregulares.json` da 0 matches
    - NO se ejecuto ni creo ningun script/archivo de snapshot para Sostantivi irregolari
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas de los 3 ejes del bloque regla D-27-03 pasaron el quorum cross-vendor (>=4x correcta) e integradas a sus slots (sovrabbondanti -o->-a + invariabili-accentate + invariabili-straniere); forma plural de cada palabra confirmada por el quorum (D-27-04); el bloque lexico (cambio-radice) y el de contraste (plurali-regolari) INTACTOS sin variantes nuevas (SOST-01/D-27-05); set 100% MC; validateContent verde; sin ids temporales residuales; sin cruces; sin refs a Genere e numero/Articoli; 1 commit por superficie; sin snapshot (no aplica a Sostantivi irregolari).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio -> evaluador del quorum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podria intentar dirigir al evaluador |
| JSON de contenido editado a mano -> loader/validator (x-text, nunca x-html, T-02-01) | superficies nuevas redactadas a mano cruzan a validateContent y al render como texto plano |

> Nota: esta fase autora contenido JSON en una app local single-user, offline, sin backend; el unico acceso externo son las API keys de `.env` (leidas, no instaladas) para el quorum. Sin red entrante, auth, ni input de terceros. No se esperan amenazas HIGH/MEDIUM.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Tampering | superficie nueva con forma plural incorrecta (plural regular masc *cigli como respuesta en un sobreabundante, flexion inventada *virtudi/*computers en una invariable, palabra del eje 1 que no es realmente sobreabundante), respuesta ambigua (doble-validez R7) o acento incorrecto entra al slot | mitigate | Quorum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); verificacion de la forma plural real en Task 1 + checkpoint (D-27-04, no inventar); atencion especial al plural sobreabundante en -a (la trampa central del eje 1) y a la invariabilidad real (ejes 2/3); pre-scan de acentos (las acentuadas llevan tilde); gate de >=4x "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-27-07 | Tampering / governance | se autoran variantes artificiales en el bloque lexico (cambio-radice) o en el de contraste (plurali-regolari), violando SOST-01/D-27-03/D-27-05 | mitigate | Task 1 + checkpoint solo proponen variantes en el bloque regla; seccion explicita "bloque lexico + contraste sin variantes"; acceptance de Task 2 asserta que el slot cambio-radice y el plurali-regolari tienen el mismo conteo de variantes que tras 27-01 (no engordaron). |
| T-27-08 | Tampering | prompt-injection en el quorum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instruccion; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-27-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes (rompe T-02-01 x-text); leak de la forma correcta en el prompt (R1); tilde ausente en una acentuada (virtu sin tilde) | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown, tildes italianas en las acentuadas); verificacion R1 (sin leak) en Task 1 + checkpoint; el smoke de 27-03 re-asserta sobre variants[].prompt + explanation. Todo string permanece plain text -> x-text seguro. |
| T-27-05 | Information disclosure / governance | una explanation nueva referencia las CATEGORIAS Genere e numero / Articoli por id o prosa, violando D-27-06 | mitigate | Verificacion en Task 1 + checkpoint; acceptance de Task 2 asserta 0 refs a `genero-numero-`/`articoli-` ni "ver X"; el genero/numero como contenido del ejercicio es valido. |
| T-27-09 | Tampering | slot NUEVO (si lo hubiera) sin validation.status top-level cuela como `status: absent` en VAL_07_STRICT | mitigate | Cualquier slot NUEVO porta el bloque `validation` top-level reflejando el quorum aprobado (D-19-09); acceptance asserta `slots variants sin validation top-level == 0`. Si todos los ejes engordan slots existentes, su validation de 27-01 ya cubre la gate. |
| T-27-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. No hay tabla de Package Legitimacy porque no hay installs. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs sustantivos-irregulares content/exercises/sustantivos-irregulares.json` exits 0
- Cada superficie nueva: passes[] con >=4x "correcta", cero "incorrecta" (registrado en SUMMARY); forma plural de cada palabra confirmada por el quorum (D-27-04)
- Los ejes del bloque regla D-27-03 materializados (sovrabbondanti -o->-a, invariabili-accentate, invariabili-straniere); bloque lexico (cambio-radice) y contraste (plurali-regolari) INTACTOS (SOST-01/D-27-05); set 100% MC; sin cruces; sin refs a Genere e numero/Articoli (D-27-06)
- NO se ejecuta ningun script de snapshot/assert (avere-only; no existe para Sostantivi irregolari)
- `node --test tests/*.test.js` — el count de Sostantivi irregolari sigue rojo a proposito hasta 27-03 (el conteo final ya es determinable tras este plan; usar el glob `tests/*.test.js`, path desnudo falla en Node 22.20)
</verification>

<success_criteria>
- Variantes nuevas autoradas con ambicion generosa donde la regla admite reformulacion (D-27-03, sin cuota fija), SOLO en el bloque regla: sovrabbondanti -o->-a + invariabili-accentate + invariabili-straniere
- Cada palabra verificada por el quorum como forma plural real (D-27-04, no se inventan plurales ni invariabilidades)
- Cada superficie nueva paso el quorum cross-vendor R1-R7 (>=4x correcta) antes de entrar (base canonica Opus 4.8 + Sonnet 4.6, D-27-11)
- Bloque lexico (cambio-radice) y contraste (plurali-regolari) SIN variantes nuevas (SOST-01/D-27-05) — SOST-02 cumplido: autoria donde hay regla, documentado que el lexico/cambio-de-raiz y el contraste no la admiten
- Superficies existentes movidas NO re-validadas; set 100% MC (sin match/WB introducidos); ninguna explanation referencia Genere e numero / Articoli (D-27-06)
- validateContent verde; conteo final de slots determinable para 27-03; sin snapshot (no aplica a Sostantivi irregolari)
</success_criteria>

<output>
Create `.planning/phases/27-sostantivi-irregolari-a-slots-contenido-l-xica/27-02-SUMMARY.md` when done
</output>
</content>
