---
phase: 25-genere-e-numero-a-slots-contenido
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [GEN-01]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Genere e numero (expected: 40 en exercise-types.test.js:1267, slot-variants-integration.test.js:172 y run-validation-271.mjs:125) estan sincronizados contra el conteo REAL final de slots leido del JSON (data.exercises.length), no una estimacion"
    - "TOTAL_EXPECTED en run-validation-271.mjs:132 esta recalculado = 277 - 40 + n_slots_final_genero_numero"
    - "El smoke parametrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento; Genere e numero convertido a slots pasa coverage/R1/R2/smart-quotes/markdown sin tocar la logica del test, solo el count; los 3 match preservados (type:match) pasan por la rama match del smoke"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Genere e numero en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots"
      contains: "genero-numero.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de genero-numero sincronizado al n de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected genero-numero + TOTAL_EXPECTED recalculado + comentario del historial actualizado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de genero-numero.json"
      via: "sincronizacion contra el conteo REAL del JSON, no estimacion"
      pattern: "genero-numero"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Genere e numero (`expected: 40`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `genero-numero.json` leido del JSON tras 25-01/25-02 (NO una estimacion), cerrando GEN-01 (cierre del count). El smoke parametrico YA es shape-agnostic (la bifurcacion `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17, se reuso en Articoli Phase 19, Partitivos Phase 20, Avere Phase 22, Essere Phase 23 y Verbi di movimento Phase 24, y esta en el codebase — verificado en exercise-types.test.js getExplanation/getPrompts; la rama match ya existe para los slots type:match de articoli/partitivos), asi que Genere e numero convertido a slots (incluidos los 3 match preservados) pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la logica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]). Genere e numero NO tiene snapshot/assert APPEND-ONLY (avere-only) ni cruces multi-cat ni word-buttons -> este plan no corre ningun script de snapshot.

Purpose: convertir Genere e numero rompe los 3 counts hoy verdes (40 replicado en 3 archivos + TOTAL_EXPECTED=277 que incluye avere=20, articoli=34, partitivos=19, essere=26 y verbos-movimiento=el de Phase 24). GEN-01 (cierre) ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase (primera de las 3 conversiones de morfologia/lexico de v1.6).

Output: los 3 test/script files con el count de Genere e numero + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validacion PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/genero-numero.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-08). El smoke YA bifurca por shape (no requiere extension de logica, solo count). avere ya esta en expected:20, articoli en 34, partitivos en 19, essere en 26 y verbos-movimiento en su count de Phase 24 — NO tocarlos. -->

1. tests/exercise-types.test.js:
   - linea 1267: `{ file: 'content/exercises/genero-numero.json', expected: 40 }` en CATEGORIES_WITH_EXPLANATIONS <- cambiar 40 al n real de slots
   - avere expected:20 / articoli expected:34 / partitivos expected:19 / essere expected:26 / verbos-movimiento (count Phase 24) — YA sincronizados, NO tocar
   - getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation — YA bifurcado por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento, NO tocar)
   - getPrompts YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar); la rama match (slots type:match) ya existe para articoli/partitivos -> los 3 match de genero-numero pasan sin tocar logica
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido -> se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:172: `{ slug: 'genero-numero', expected: 40 }` en REAL_CATEGORIES <- cambiar 40 al n real de slots (avere/articoli/partitivos/essere/verbos-movimiento en sus lineas, NO tocar)

3. scripts/run-validation-271.mjs:
   - linea 125: `{ slug: 'genero-numero', file: 'content/exercises/genero-numero.json', expected: 40 }` <- cambiar 40 al n real
   - linea 132: `const TOTAL_EXPECTED = 277;` <- recalcular = 277 - 40 + n_slots_final_genero_numero
   - lineas del comentario explicativo del historial del total (alrededor de 64-124) <- anadir la conversion de Genere e numero a slots. El comentario narra cada cambio que movio el total; la ultima linea documenta Verbi di movimento (Phase 24, MOV-01). Anadir una linea analoga DESPUES: "-> <nuevo> (v1.6 Phase 25, GEN-01): Genere e numero se convirtio al shape slot+variantes. La reagrupacion fundio 40 ejercicios payload a 1 SLOT POR MICRO-REGLA (D-25-01, granularidad fina): plural -o/-i, -a/-e, -e/-i, sonido duro -co/-go, invariables, femenino -o/-a, -tore/-trice, -e/-essa, articulo por sonido + los 3 match preservados (D-25-03/D-04) = <count> slots reales (6 pares de duplicados literales colapsados a variantes). Las 40 superficies fuente se preservaron como variantes (+N superficies nuevas de los 4 ejes D-25-04: invariables + sonido duro con excepcion amico->amici + genero -trice/-essa + plural base). TOTAL = 277 - 40 + <count> = <nuevo>". OJO: a diferencia de Verbi di movimento, aqui la granularidad es FINA (mas slots por micro-regla) pero hay 6 duplicados que se colapsan -> el net puede subir o bajar; pero LEER el count real, no asumir (las VARIANTES nuevas de 25-02 NO suben el count; solo SLOTS nuevos lo suben, y 25-02 recomienda NO crear slots nuevos -> el net = slots de reagrupacion de 25-01).

El count `expected` de genero-numero = n de SLOTS final (data.exercises.length tras 25-01+25-02) — sincronizar contra el JSON REAL, NO contra una estimacion.
Comando: `node -e "console.log(require('./content/exercises/genero-numero.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
Leccion 19-03/20-03/22-03/23-03/24-03: leer SIEMPRE del JSON, nunca estimar. Las VARIANTES nuevas (los 4 ejes de 25-02) NO suben el count de slots; solo los SLOTS nuevos lo suben (25-02 recomienda NO crear slots nuevos -> el net = slots de reagrupacion de 25-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Genere e numero en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (las lineas alrededor de 1255-1300 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + la rama match + el assert de conteo; y el bloque VAL_07_STRICT)
    - content/exercises/genero-numero.json (estado final tras 25-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-03-PLAN.md (Task 1 analogo de Verbi di movimento — el mismo patron de sync de count contra el JSON real)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de genero-numero.json (`data.exercises.length` tras 25-02, via `node -e "console.log(require('./content/exercises/genero-numero.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry genero-numero en CATEGORIES_WITH_EXPLANATIONS (linea 1267) a ese numero. NO tocar las entries de avere (en 20), articoli (en 34), partitivos (en 19), essere (en 26) ni verbos-movimiento (en su count de Phase 24). NO tocar la logica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento) y la rama match ya existe -> Genere e numero slot (MC + los 3 match preservados) pasa coverage/R1/R2/smart-quotes/markdown automaticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level -> no cambia salvo heredar el count sincronizado (todo slot/variante nuevo de 25-02 ya esta validated por el quorum; los slots movidos conservan su validation fuente). NO tocar los counts de las otras 8 categorias.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de genero-numero en CATEGORIES_WITH_EXPLANATIONS == n REAL de slots: `node -e "const d=require('./content/exercises/genero-numero.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en linea 1267
    - Las otras 8 categorias siguen verdes con sus counts originales (avere 20, articoli 34, partitivos 19, essere 26, verbos-movimiento count-Phase-24, preposiciones 49, sustantivos-irregulares 31, profesiones 51) — no se tocaron
    - La logica del test (getExplanation/getPrompts/bifurcacion/rama match) NO se modifico: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Genere e numero en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-genero-numero intactas; logica del smoke (ya shape-agnostic, con rama match) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED)</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (las lineas alrededor de 160-200 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (las lineas alrededor de 63-135 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/genero-numero.json (conteo REAL final de slots)
    - .planning/phases/24-verbi-di-movimento-a-slots-contenido/24-03-PLAN.md (Task 2 analogo de Verbi di movimento — mismo patron de 3 hardcodes + TOTAL_EXPECTED + comentario del historial)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'genero-numero', expected: 40 }` (linea 172) a `expected` = n REAL de slots. NO tocar las entries de avere (en 20), articoli (en 34), partitivos (en 19), essere (en 26) ni verbos-movimiento (count Phase 24). El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 40` de la entry genero-numero (linea 125) a n REAL de slots; recalcular `TOTAL_EXPECTED` (linea 132) = `277 - 40 + n_slots_final_genero_numero`; actualizar el comentario explicativo del historial del total (alrededor de lineas 64-124) anadiendo una linea DESPUES de la de Verbi di movimento (Phase 24) que registre la conversion de Genere e numero a slots: "-> <nuevo> (v1.6 Phase 25, GEN-01): Genere e numero se convirtio al shape slot+variantes. La reagrupacion fundio 40 ejercicios payload a 1 SLOT POR MICRO-REGLA (D-25-01, granularidad fina): plural -o/-i, -a/-e, -e/-i, sonido duro -co/-go, invariables, femenino -o/-a, -tore/-trice, -e/-essa, articulo por sonido + los 3 match preservados (D-25-03/D-04) = <count> slots reales (6 pares de duplicados literales colapsados a variantes). Las 40 superficies fuente se preservaron como variantes (+N superficies nuevas de los 4 ejes D-25-04: invariables + sonido duro con excepcion amico->amici + genero -trice/-essa + plural base). TOTAL = 277 - 40 + <count> = <nuevo>". Leer el conteo real del JSON, NO usar una estimacion (granularidad fina pero con 6 duplicados colapsados -> el net puede subir o bajar; pero leer, no asumir). Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO numero.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de genero-numero en ambos archivos == n REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/genero-numero.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 277 - 40 + n_slots_final_genero_numero (verificable aritmeticamente contra el conteo real; avere sigue contribuyendo 20, articoli 34, partitivos 19, essere 26, verbos-movimiento su count de Phase 24)
    - El reporter de run-validation-271 no emite warning `total != esperado` para genero-numero
    - El comentario del historial del total en run-validation-271.mjs menciona la conversion de Genere e numero a slots (incluida la granularidad fina D-25-01 y los 3 match preservados D-25-03/D-04)
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Genere e numero (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el n real de slots; TOTAL_EXPECTED recalculado (avere=20, articoli=34, partitivos=19, essere=26, verbos-movimiento count-Phase-24 intactos); reporter PASS sin warnings de count; comentario del historial actualizado con la conversion de Genere e numero (granularidad fina + 3 match preservados).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido -> smoke parametrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

> Nota: esta fase edita hardcodes de count en tests/scripts de tooling de una app local single-user, offline, sin backend; no introduce nueva superficie de ataque. No se esperan amenazas HIGH/MEDIUM.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Tampering | count desincronizado entre los 3 hardcodes -> gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leido del JSON (`data.exercises.length`), no estimaciones (leccion 19-03/20-03/22-03/23-03/24-03). El acceptance asserta que coinciden y que run-validation-271 PASS. La granularidad fina + 6 duplicados colapsados hacen el net no-obvio -> leer, no asumir. |
| T-25-03 | Tampering | explanation/prompt/pairs de slot con HTML/markdown/smart-quotes pasa sin escanear (rompe T-02-01 x-text) | mitigate | El smoke YA bifurca por shape (heredado del piloto/Articoli/Partitivi/Avere/Essere/Verbi-movimiento): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots (y la rama match cubre los 3 match) — la cobertura de seguridad de contenido NO se pierde al convertir Genere e numero. |
| T-25-08 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 25-02 paso el quorum (status validated) y cualquier slot nuevo lleva validation top-level (D-19-09) antes de integrarse -> la gate pasa solo si 25-02 cumplio. Si algo quedo disputed o sin validation top-level, la gate FALLA visible. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. No hay tabla de Package Legitimacy porque no hay installs. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v9 de Phase 21, integration, exercise-types, articoli/partitivos/avere/essere/verbos-movimiento de Phase 19/20/22/23/24). Usar el glob `tests/*.test.js`; el path desnudo falla en Node 22.20 (MEMORY test_command_node_glob).
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs genero-numero content/exercises/genero-numero.json` exits 0
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de genero-numero.json
- (NO se corre ningun assert de snapshot — avere-only, no existe para Genere e numero)
</verification>

<success_criteria>
- Los 3 hardcodes de count de Genere e numero + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacia — verificado por el smoke shape-agnostic; los 3 match pasan por la rama match)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); logica del smoke NO tocada (ya shape-agnostic); avere=20, articoli=34, partitivos=19, essere=26 y verbos-movimiento (Phase 24) intactos
</success_criteria>

<output>
Create `.planning/phases/25-genere-e-numero-a-slots-contenido/25-03-SUMMARY.md` when done
</output>
