---
phase: 23-essere-a-slots-contenido
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [ESS-01]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Essere (expected: 39 en exercise-types.test.js:1271, slot-variants-integration.test.js:168 y run-validation-271.mjs:103) estan sincronizados contra el conteo REAL final de slots leido del JSON (no una estimacion)"
    - "TOTAL_EXPECTED en run-validation-271.mjs:111 esta recalculado = 320 - 39 + n_slots_final_essere"
    - "El smoke parametrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto/Articoli/Partitivi/Avere; Essere convertido a slots pasa coverage/R1/R2/smart-quotes/markdown sin tocar la logica del test, solo el count"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Essere en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots"
      contains: "essere.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de essere sincronizado al n de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected essere + TOTAL_EXPECTED recalculado + comentario del historial actualizado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de essere.json"
      via: "sincronizacion contra el conteo REAL del JSON, no estimacion"
      pattern: "essere"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Essere (`expected: 39`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `essere.json` leido del JSON tras 23-01/23-02 (NO una estimacion), cerrando ESS-01 (cierre del count). El smoke parametrico YA es shape-agnostic (la bifurcacion `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17, se reuso en Articoli Phase 19, Partitivos Phase 20 y Avere Phase 22, y esta en el codebase — verificado en exercise-types.test.js getExplanation/getPrompts), asi que Essere convertido a slots pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la logica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]). Essere NO tiene snapshot/assert APPEND-ONLY (avere-only) -> este plan no corre ningun script de snapshot.

Purpose: convertir Essere rompe los 3 counts hoy verdes (39 replicado en 3 archivos + TOTAL_EXPECTED=320 que incluye avere=20, articoli=34 y partitivos=19). ESS-01 (cierre) ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase (segunda de las 3 conversiones de verbos de v1.6).

Output: los 3 test/script files con el count de Essere + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validacion PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/exercises/essere.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-06). El smoke YA bifurca por shape (no requiere extension de logica, solo count). avere ya esta en expected:20, articoli en 34 y partitivos en 19 — NO tocarlos. -->

1. tests/exercise-types.test.js:
   - linea 1271: `{ file: 'content/exercises/essere.json', expected: 39 }` en CATEGORIES_WITH_EXPLANATIONS <- cambiar 39 al n real de slots
   - avere expected:20 / articoli expected:34 / partitivos expected:19 — YA sincronizados (Phase 19/20/22), NO tocar
   - getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation — YA bifurcado por shape (heredado del piloto/Articoli/Partitivi/Avere, NO tocar)
   - getPrompts YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar)
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido -> se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:168: `{ slug: 'essere', expected: 39 }` en REAL_CATEGORIES <- cambiar 39 al n real de slots (avere/articoli/partitivos en sus lineas, NO tocar)

3. scripts/run-validation-271.mjs:
   - linea 103: `{ slug: 'essere', file: 'content/exercises/essere.json', expected: 39 }` <- cambiar 39 al n real
   - linea 111: `const TOTAL_EXPECTED = 320;` <- recalcular = 320 - 39 + n_slots_final_essere
   - lineas 64-94: comentario explicativo del historial del total <- anadir la conversion de Essere a slots. El comentario narra cada cambio que movio el total; la ultima linea (lineas 88-94) documenta Avere ("-> 320 (v1.6 Phase 22, AVE-01): ... TOTAL = 323 - 23 + 20 = 320"). Anadir una linea analoga DESPUES: "-> <nuevo> (v1.6 Phase 23, ESS-01): Essere se convirtio al shape slot+variantes. ... TOTAL = 320 - 39 + <count> = <nuevo>". OJO: el passato de Essere se partio en 4 slots (D-23-03) y se anadio el slot nuevo ser/estar (D-23-07) + posibles slots de localizacion -> el net puede SUBIR o bajar respecto a 39 segun cuantos slots resulten; LEER el count real, no asumir que baja como en Avere.

El count `expected` de essere = n de SLOTS final (data.exercises.length tras 23-01+23-02) — sincronizar contra el JSON REAL, NO contra una estimacion. La fusion por regla reduce el count, pero los 4 slots del passato (D-23-03) y los slots nuevos de 23-02 (ser/estar D-23-07, posible localizacion) lo suben. Net = leer del JSON.
Comando: `node -e "console.log(require('./content/exercises/essere.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
Leccion 19-03/20-03/22-03: la estimacion fue ~25 (Articoli) y el real fue 34; leer SIEMPRE del JSON, nunca estimar. Las VARIANTES nuevas NO suben el count de slots; solo los SLOTS nuevos lo suben.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Essere en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (las lineas alrededor de 1265-1310 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + el assert de conteo; y el bloque VAL_07_STRICT)
    - content/exercises/essere.json (estado final tras 23-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/phases/22-avere-a-slots-contenido/22-03-PLAN.md (Task 1 analogo de Avere — el mismo patron de sync de count contra el JSON real)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de essere.json (`data.exercises.length` tras 23-02, via `node -e "console.log(require('./content/exercises/essere.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry essere en CATEGORIES_WITH_EXPLANATIONS (linea 1271) a ese numero. NO tocar las entries de avere (en 20), articoli (en 34) ni partitivos (en 19). NO tocar la logica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto/Articoli/Partitivi/Avere) -> Essere slot pasa coverage/R1/R2/smart-quotes/markdown automaticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level -> no cambia salvo heredar el count sincronizado (todo slot/variante nuevo de 23-02 ya esta validated por el quorum con validation top-level D-19-09; los slots de cruces 300..305 y los slots movidos conservan su validation fuente). NO tocar los counts de las otras 8 categorias.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de essere en CATEGORIES_WITH_EXPLANATIONS == n REAL de slots: `node -e "const d=require('./content/exercises/essere.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en linea 1271
    - Las otras 8 categorias siguen verdes con sus counts originales (avere 20, articoli 34, partitivos 19, preposiciones 49, genero-numero 40, sustantivos-irregulares 31, verbos-movimiento 37, profesiones 51) — no se tocaron
    - La logica del test (getExplanation/getPrompts/bifurcacion) NO se modifico: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Essere en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-essere intactas (avere sigue en 20, articoli en 34, partitivos en 19); logica del smoke (ya shape-agnostic) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED)</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (las lineas alrededor de 160-200 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (las lineas alrededor de 63-111 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/essere.json (conteo REAL final de slots)
    - .planning/phases/22-avere-a-slots-contenido/22-03-PLAN.md (Task 2 analogo de Avere — mismo patron de 3 hardcodes + TOTAL_EXPECTED + comentario del historial)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'essere', expected: 39 }` (linea 168) a `expected` = n REAL de slots. NO tocar las entries de avere (en 20), articoli (en 34) ni partitivos (en 19). El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 39` de la entry essere (linea 103) a n REAL de slots; recalcular `TOTAL_EXPECTED` (linea 111) = `320 - 39 + n_slots_final_essere`; actualizar el comentario explicativo del historial del total (lineas 64-94) anadiendo una linea DESPUES de la de Avere (lineas 88-94) que registre la conversion de Essere a slots: "-> <nuevo> (v1.6 Phase 23, ESS-01): Essere se convirtio al shape slot+variantes. La reagrupacion fundio 39 ejercicios payload por regla; el passato prossimo se partio en 4 slots por concordancia (D-23-03) + slot nuevo ser/estar (D-23-07) [+ otros slots nuevos si los hubo] por quorum cross-vendor = <count> slots reales. TOTAL = 320 - 39 + <count> = <nuevo>". Leer el conteo real del JSON, NO usar una estimacion (el net puede subir o bajar respecto a 39 segun los 4 slots del passato + slots nuevos). Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO numero.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de essere en ambos archivos == n REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/essere.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 320 - 39 + n_slots_final_essere (verificable aritmeticamente contra el conteo real; avere sigue contribuyendo 20, articoli 34, partitivos 19)
    - El reporter de run-validation-271 no emite warning `total != esperado` para essere
    - El comentario del historial del total en run-validation-271.mjs menciona la conversion de Essere a slots (incluido el split del passato en 4 slots y el slot ser/estar)
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Essere (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el n real de slots; TOTAL_EXPECTED recalculado (avere=20, articoli=34, partitivos=19 intactos); reporter PASS sin warnings de count; comentario del historial actualizado con la conversion de Essere.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido -> smoke parametrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

> Nota: esta fase edita hardcodes de count en tests/scripts de tooling de una app local single-user sin backend; no introduce nueva superficie de ataque. No se esperan amenazas HIGH.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Tampering | count desincronizado entre los 3 hardcodes -> gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leido del JSON (`data.exercises.length`), no estimaciones (leccion 19-03/20-03/22-03). El acceptance asserta que coinciden y que run-validation-271 PASS. El passato en 4 slots + slot ser/estar pueden subir el net -> leer, no asumir. |
| T-23-03 | Tampering | explanation/prompt de slot con HTML/markdown/smart-quotes pasa sin escanear | mitigate | El smoke YA bifurca por shape (heredado del piloto/Articoli/Partitivi/Avere): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots — la cobertura de seguridad de contenido NO se pierde al convertir Essere. |
| T-23-08 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 23-02 paso el quorum (status validated) y todo slot nuevo (ser/estar) lleva validation top-level (D-19-09) antes de integrarse -> la gate pasa solo si 23-02 cumplio. Si algo quedo disputed o sin validation top-level, la gate FALLA visible. |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v9 de Phase 21, integration, exercise-types, articoli/partitivos/avere de Phase 19/20/22)
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs essere content/exercises/essere.json` exits 0
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de essere.json
- (NO se corre ningun assert de snapshot — avere-only, no existe para Essere)
</verification>

<success_criteria>
- Los 3 hardcodes de count de Essere + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacia — verificado por el smoke shape-agnostic)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); logica del smoke NO tocada (ya shape-agnostic); avere=20, articoli=34 y partitivos=19 intactos
</success_criteria>

<output>
Create `.planning/phases/23-essere-a-slots-contenido/23-03-SUMMARY.md` when done
</output>
