---
phase: 42-fare-congiuntivo-4-slots-hom-grafas-disparador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/fare-congiuntivo.json
  - content/categories.json
  - tests/exercise-types.test.js
autonomous: false
requirements: [CONG-01, CONG-02]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- SC-5 / D-42-18 : el registro que hace existir la categoría ---
    - "`content/categories.json` contiene exactamente UNA entrada nueva con las 4 claves `id`, `name`, `order`, `origen`, valores `id: \"fare-congiuntivo\"`, `order: 16`, `origen: \"ia-quorum\"`, y las 15 entradas previas quedan byte-intactas (D-42-18, SC-5)."
    - "El slug es byte a byte `fare-congiuntivo` en los cuatro sitios que el contrato de Phase 40 vincula: nombre del fichero de contenido, `id` de `content/categories.json`, prefijo de los 5 ids de slot, y el elemento ya existente de `RESET_PREFIXES_V13` (`src/data/storage.js:1345`, que esta fase NO edita) — de modo que la categoría es una unidad de reset independiente de las otras 3 de `fare` (D-42-18, D-40-01, SC-5)."
    - "La categoría carga en boot y aparece en home, picker, Repaso y Examen por el camino genérico (`categoriesForDisplay` itera `content.categories`; `totalCount` se deriva de `categoryIds`), sin una sola línea de motor nueva: `git diff --stat src/screens/app.js src/domain/ src/data/` no produce salida (D-42-18, SC-5)."
    # --- edge: schema boundary (el orden de las dos ediciones) ---
    - "El fichero `content/exercises/fare-congiuntivo.json` nunca queda en disco sin su entrada en `categories.json`: `src/data/schema-validator.js:133-146` rechaza un `categoryIds` que referencia una categoría desconocida y `tests/domain.test.js` valida el bundle auto-descubierto exigiendo `result.errors` vacío, así que `node --test tests/*.test.js` termina con `fail 0` en cada commit de este plan (edge: schema boundary)."
    # --- CONG-01 / CONG-02 : el paradigma pedido, con homografía deliberada ---
    - "El slot `fare-congiuntivo-presente` tiene EXACTAMENTE 6 variantes y sus 6 keys (`options[correctIndex]`) son, en orden de persona io/tu/lui-lei/noi/voi/loro, `faccia`, `faccia`, `faccia`, `facciamo`, `facciate`, `facciano` — la key SE REPITE tres veces a propósito y la unicidad se resuelve por el sujeto explícito, no por la forma (CONG-01, D-42-05, SC-1)."
    - "El slot `fare-congiuntivo-imperfetto` tiene EXACTAMENTE 6 variantes y sus 6 keys son, en orden de persona, `facessi`, `facessi`, `facesse`, `facessimo`, `faceste`, `facessero` — con la key repetida en io y tu (CONG-02, D-42-05, SC-1)."
    - "El eje de variante de los dos slots es la persona, y lo sirve `pickVariantIndex` (`src/domain/session.js:232`, que solo lee `slot.variants.length`) sin código nuevo (D-42-01, D-42-18)."
    # --- D-42-06 : el gate HARD de esta fase ---
    - "En las 12 variantes de este plan el sujeto de la oración principal NUNCA coincide con el sujeto del hueco: o el disparador es impersonal (`Bisogna che`, `È necessario che`, `È importante che`, `Sembra che`, `Benché`, `Prima che`, `Purché`, `Nonostante`, `Basta che`, `Peccato che`) y no hay sujeto de principal que coincidir, o la principal lleva un sujeto explícito distinto del pronombre del hueco (D-42-06)."
    - "El `notes` declara el gate de no-correferencia ANTES de la primera variante, con su audit trail: en italiano estándar, cuando los dos sujetos coinciden se exige `di` + infinitivo y la construcción con `che` + subjuntivo es defectuosa, así que la variante de `io` escrita del modo obvio con `penso che` sale mal construida (D-42-06)."
    # --- D-42-18 : sujeto explícito, que es lo que hace resoluble la homógrafa ---
    - "Los 12 `prompt` contienen el hueco literal `___`, y el sujeto del hueco es un pronombre explícito (`io`, `tu`, `lui`, `lei`, `noi`, `voi`, `loro`) que coincide con la persona de la key de esa variante — es la exigencia literal de CONG-01 y CONG-02: sin él el ejercicio es irresoluble (D-42-18, CONG-01, CONG-02, SC-2)."
    # --- D-42-13 : 0-gloss del verbo, gloss léxico de conjunción ---
    - "Ninguno de los 12 `prompt` lleva gloss `(en español: ...)` sobre la forma verbal, ni la subcadena `espa` en ninguna capitalización: en una categoría cuyo ejercicio es elegir modo Y tiempo, y en la que el español también tiene subjuntivo, un gloss sobre el verbo regala las dos cosas a la vez (D-42-13, R1)."
    - "El único paréntesis admitido en un `prompt` es el gloss léxico de una conjunción por encima de nivel A1, y su contenido pertenece al conjunto cerrado `aunque`, `siempre que`, `antes de que`, `a pesar de que`; el paréntesis va inmediatamente detrás de la conjunción glosada y nunca junto a una forma de `fare` ni de `avere` (D-42-13)."
    - "El `notes` declara el 0-gloss del verbo Y su excepción léxica en el mismo párrafo, con las dos razones: el gloss del verbo filtra modo y tiempo, y el gloss de la conjunción no filtra nada porque «aunque» rige los dos modos en español; y advierte que un flag C5-leak de Gemini o DeepSeek sobre ese gloss es falso positivo de política y NO se «arregla» (D-42-13)."
    # --- heredado D-41-06 vía D-42-18 : SCOPE-GATE léxico ---
    - "El objeto de `fare` en las 12 variantes sale SOLO del conjunto literal cerrado `i compiti`, `un errore`, `il lavoro`, `una torta`, `il letto`, `tutto`, `una foto`; un escaneo por campo sobre `variants[].prompt` y `variants[].options[]` devuelve 0 coincidencias para los marcadores de perífrasis y modismos (D-42-18, D-41-06)."
    # --- D-42-11 : blacklist, la misma regla aplicada al contexto ---
    - "Un escaneo por campo con coincidencia EXACTA sobre `variants[].options[]` devuelve 0 coincidencias para las formas atestiguadas heredadas (`fo`, `fé`, `fenno`, `facea`, `fan`, `face`, `faci`, `fei`, `festi`, `femmo`, `feste`, `fero`, `feciono`, `fici`, `facisti`, `facette`, `facettero`, `facero`), para las trampas nuevas de esta categoría (`facci`, `facciam`, `facce`) y para las casillas de Phase 43 (la familia del condizionale y el imperativo apostrofado) — nunca por subcadena, porque `face` es prefijo de `facesse`, `faceste` y `faceva`, que sí son formas legítimas aquí (D-42-11)."
    - "El `notes` declara la blacklist con audit trail y con el criterio operativo explícito: lo prohibido es la distractora DEFENDIBLE como correcta, no la forma atestiguada en abstracto; bajo un disparador de subjuntivo `faccio` está atestiguada pero no es defendible, así que la distractora que cruza el modo es la regla aplicada correctamente y no su inversión. Cada forma va citada entre apóstrofes ASCII U+0027 (D-42-11)."
    - "El `notes` registra como vistas y deliberadas las cuatro homografías que esta categoría crea y que NO son bugs: `faccia` es también el sustantivo cara, `facciate` es a la vez la key de `voi` y el plural del sustantivo fachada, `abbiamo fatto` es a la vez congiuntivo passato de `noi` y passato prossimo de indicativo, y `faceste` es a la vez congiuntivo imperfetto de `voi` y passato remoto de indicativo (D-42-11)."
    # --- D-42-10 : el patrón fijo de distractoras de los dos slots simples ---
    - "En las 12 variantes de `presente` e `imperfetto` las 3 distractoras siguen el patrón fijo de D-42-10: exactamente una es el indicativo de ESA MISMA persona (`faccio`/`fai`/`fa`/`facciamo`/`fate`/`fanno` frente a la key del presente; `facevo`/`facevi`/`faceva`/`facevamo`/`facevate`/`facevano` frente a la del imperfetto), exactamente una es una forma REAL de subjuntivo de OTRA persona del mismo tiempo, y exactamente una es una raíz regularizada inexistente en italiano (D-42-10)."
    - "La distractora de indicativo del presente `noi` no colisiona con la key: `facciamo` es a la vez indicativo y congiuntivo de `noi`, así que la variante de `noi` usa la otra forma de indicativo disponible o sustituye ese eje por una segunda forma real de otra persona, y el `notes` lo declara — no se admite una variante con dos opciones idénticas ni con la key duplicada (D-42-10, edge: homograph boundary)."
    - "En cada una de las 12 variantes hay EXACTAMENTE una opción defendible: la de otra persona queda excluida por el pronombre sujeto explícito, la de indicativo queda excluida por el disparador de subjuntivo, y la regularizada no existe en italiano (D-42-06, D-42-10, D-42-11; edge: homograph/ambiguity boundary)."
    - statement: "Ninguna de las 12 variantes admite una segunda lectura del disparador o del marco que hiciera defendible una forma distinta de la key — en particular ninguna admite el indicativo por ser el disparador de los que el español construye con indicativo."
      verification: backstop
    # --- D-42-07 : qué diferencia a las variantes que comparten key ---
    - "Las tres variantes de `presente` que comparten la key `faccia` (io, tu, lui/lei) y las dos de `imperfetto` que comparten `facessi` (io, tu) no son el mismo ejercicio repetido: los disparadores de las 6 variantes de cada slot son distintos entre sí, el objeto literal es distinto entre las variantes que comparten key, y sus conjuntos de `options` son distintos dos a dos porque la distractora de indicativo es específica de la persona (D-42-07)."
    # --- D-42-14 : el error de interferencia, dosificado ---
    - "Las explanations de `presente` y de `imperfetto` llevan UNA línea de recordatorio del error de interferencia (`penso che` rige subjuntivo en italiano mientras «pienso que» rige indicativo en español) — recordatorio, no desarrollo: el desarrollo con el par explícito vive en la explanation del slot del disparador de 42-02, porque el motor sirve un slot por sesión y por presente e imperfetto se pasa muchas veces (D-42-14)."
    # --- D-42-15 : el hand-off a Phase 43, documentado y sin mencionarlo ---
    - "El `notes` registra el hand-off a Phase 43: `faccia`, `facciamo` y `facciano` son idénticas a 3 de las 5 formas del imperativo, y `fate` ya vive en `fare-indicativo`, así que 4 de las 5 ya están en el corpus y el magnet de Phase 43 es mayor que el `fa'`/`fai`/`fa` que INT-04 declara. Y ninguna de las explanations de este plan menciona el imperativo — sería adelantarse a otra fase (D-42-15)."
    # --- D-42-01 : la divergencia respecto al propio roadmap, escrita ---
    - "El `notes` declara la divergencia respecto a los números del roadmap («4 slots», «≈24 variantes») con su razón: son 5 slots y 30 variantes porque SC#4 exige un slot para el disparador con una variante de contraste cuya respuesta correcta es el indicativo, y esa variante no cabe en el slot de presente sin romper la enumeración de 6 personas de SC#1; y anota el knock-on para Phase 44 / INT-02: el milestone pasa de 21 slots y ≈107 variantes a 22 y ≈113 (D-42-01)."
    # --- D-42-18 : MC-only, 0-match, 0-word-buttons ---
    - "Los slots del fichero tienen `type: \"multiple-choice\"` con `options` de longitud 4 y sin duplicados dentro de una misma variante; no existe ningún slot `match` ni `word-buttons`, y el `notes` declara el 0-match (el pareo persona a forma es derivable por raíz, DESIGN RULE D-04) y el 0-word-buttons (lo examinado es elegir modo y tiempo, no ordenar palabras) como decisiones razonadas y no como omisiones (D-42-18)."
    # --- D-42-18 / D-42-03 : ids semánticos, un solo categoryId, espacio -300 libre ---
    - "Los ids de los slots de este plan son literalmente `fare-congiuntivo-presente` y `fare-congiuntivo-imperfetto`, los dos con `categoryIds: [\"fare-congiuntivo\"]` — exactamente un id, porque la variante de contraste del disparador no es un cruce y la lista de cruces de INT-03 queda cerrada —, y ningún id del fichero termina en sufijo numérico de 3 cifras: el espacio `fare-congiuntivo-300`+ queda libre para Phase 44 (D-42-18, D-42-03; edge: id-space boundary)."
    # --- D-42-04 : el estado del hand-off al quórum top-level ---
    - "Al cerrar este plan los slots llevan `validation` con `status: \"pending\"` y `passes: []`: el executor NO fabrica pases ni estampa `validated`, porque el quórum base canónico Opus+Sonnet vive en una pasada TOP-LEVEL posterior — el skill `gsd-validate-exercise` spawnea Task subagents y un `gsd-executor` no puede hacerlo (D-42-04, VAL-03)."
    - "`VAL_07_STRICT=1 node --test tests/*.test.js` falla EXACTAMENTE en el test de `content/exercises/fare-congiuntivo.json` nombrando sus slots `pending`, y ninguna otra categoría cambia de estado: ese rojo es el marcador visible del hand-off, porque el reporter `scripts/run-validation-271.mjs` no se pone rojo — queda CIEGO al fichero nuevo y sigue reportando su total anterior en PASS hasta Phase 44 / INT-02 (D-42-04, SC-5)."
    # --- D-42-17 : canon editorial ---
    - "La línea nueva de `CATEGORIES_WITH_EXPLANATIONS` (`tests/exercise-types.test.js`, tras la entrada de `fare-indicativo` de la línea 1293) con `expected: slotCountOf('content/exercises/fare-congiuntivo.json')` activa los sub-tests editoriales sobre el fichero nuevo y todos pasan: explanation no vacía por slot, cero smart-quotes, cero markdown, cero leak R1 en los prompts, cero cross-refs R2 en las explanations (D-42-17, SC-5)."
    - "Las explanations están en español acentuado RAE, plain text de un solo párrafo sin markdown ni backticks, con apóstrofes ASCII U+0027 al citar italiano y sin comillas tipográficas; un flag C4-accent del quórum sobre español sin tildes es bug REAL y se arregla poniendo los acentos, nunca con override (D-42-17)."
    # --- valla del motor ---
    - "`git status --porcelain` no lista ningún fichero fuera de los 3 de `files_modified`; en particular `src/`, `scripts/` y `tests/fixtures/` quedan sin tocar (Out of Scope literal del milestone)."

  artifacts:
    - "`content/exercises/fare-congiuntivo.json` — fichero NUEVO con exactamente 2 claves top-level (`notes`, `exercises`), el `notes` con las 12 declaraciones (divergencia de volumen D-42-01, no-correferencia D-42-06, 0-gloss del verbo con la excepción de conjunción D-42-13, 0-match, 0-word-buttons, blacklist ampliada con criterio de defendibilidad D-42-11, las cuatro homografías deliberadas, los tres patrones de distractoras, SCOPE-GATE del objeto literal, hand-off a Phase 43 D-42-15, dosificación del error de interferencia D-42-14, nota de escaneo por campo y nota de count-sync), y los 2 slots simples con 6 variantes cada uno."
    - "`content/categories.json` — 16 entradas; la nueva es `fare-congiuntivo`, `order: 16`, `origen: \"ia-quorum\"`, `name` con raya em U+2014 siguiendo el patrón descriptivo de las 15 existentes."
    - "`tests/exercise-types.test.js` — 1 línea nueva en `CATEGORIES_WITH_EXPLANATIONS` con `expected` dinámico vía `slotCountOf`, más un comentario de una línea que nombra la fase y los requisitos."

  key_links:
    - "`content/categories.json` → `src/data/schema-validator.js:133-146` (`knownCategoryIds`): sin la entrada `fare-congiuntivo`, cada slot emite `referencia a categoría desconocida` y `tests/domain.test.js` se pone rojo. Es la razón de que la entrada viva en la tarea tracer y no al final."
    - "`content/categories.json` → `categoriesForDisplay` (`src/screens/app.js`): el array de `content.categories` se mapea 1:1, así que la entrada es lo ÚNICO que hace aparecer la fila en home, en el picker, en Repaso y en Examen. `order` es documental; el orden del array define el display."
    - "`variants[]` → `pickVariantIndex` (`src/domain/session.js:232`): la longitud 6 del array es lo que produce «una persona distinta cada pasada». La función solo lee `slot.variants.length` y es axis-agnostic, lo que también hará gratis el slot de eje-disparador de 42-02."
    - "el pronombre sujeto explícito del `prompt` → la unicidad de la respuesta en las 5 variantes homógrafas de este plan (`faccia` io/tu/lui-lei, `facessi` io/tu): es el único desambiguador. Un prompt sin sujeto explícito convierte la variante en irresoluble y, por la cascada D-54, en un reset de la categoría entera."
    - "el disparador del `prompt` → la exclusión de la distractora de indicativo de D-42-10: si el disparador no exige subjuntivo sin ambigüedad, `faccio`/`facevo` pasan a ser defendibles y la variante queda injusta."
    - "`slotCountOf` (`tests/exercise-types.test.js:1268-1271`) → el `expected` de la línea nueva: al ser dinámico la línea es auto-consistente y no puede ponerse roja por conteo, que es lo que permite añadirla ya sin invadir INT-02."
    - "`validation.passes[]` → `deriveStatus` (`src/data/validation-state.js:32-43`): `validated` exige ≥2 pases `correcta` con `by` distintos y cero `incorrecta`; cualquier `incorrecta` da `disputed`. Escribir `status: \"validated\"` con `passes: []` produce un estado incoherente que el gate permanente de 42-02 detecta."

  prohibitions:
    - "MUST NOT dar de alta una variante en la que más de una opción sea una respuesta defendiblemente correcta para ese prompt: la cascada D-54 resetea la categoría entera con un solo fallo, así que un ejercicio injusto le enseña al autor que su respuesta correcta es incorrecta y le cuesta re-aprendizaje real. Es el daño exacto que esta herramienta existe para evitar."
    - "MUST NOT examinar vocabulario que el autor no ha estudiado sin glosarlo: fallar una variante por no conocer `benché` o `purché` — no por no saber el subjuntivo — resetea la categoría entera por la cascada D-54. Es la misma injusticia por otra puerta, y es la razón de ser del gloss léxico de conjunción."
    - "MUST NOT escribir `validation.status: \"validated\"` ni ninguna entrada de `passes[]` que no venga de un pase de modelo real: el audit trail es la única evidencia que el autor tiene de que una variante fue revisada, y un pase inventado la destruye sin dejar rastro."
    - "MUST NOT presentar una forma regional, dialectal, arcaica o de registro literario como si fuera la norma del italiano estándar, ni en un sentido ni en el otro: el autor no puede distinguirlas y la interiorizaría como regla."
    - "MUST NOT reducir el alcance de una decisión de 42-CONTEXT.md a una versión «v1», «simplificada», «estática por ahora» o «pendiente de cablear»: si algo no cabe, se parte la fase y se pide aprobación, no se entrega menos de lo decidido."
    - "MUST NOT renombrar el slug `fare-congiuntivo` ni ningún id de slot después de darlo de alta: los ids viven en el `exerciseStats` real del autor y en `RESET_PREFIXES_V13`, y un renombrado deja su progreso huérfano en silencio."
    - "MUST NOT introducir ninguna dependencia, `package.json`, paso de build ni proceso que haya que arrancar: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node (CLAUDE.md §Constraints)."
    - "MUST NOT tocar el motor v1.4 (`src/screens/app.js`, `src/domain/`, `src/data/`): si el contenido exigiera cambiar `session.js`, `applyResultToSession` o la cascada, el diseño del contenido está mal (Out of Scope literal del milestone)."
    - "MUST NOT autorar cruces multi-categoría ni usar el espacio de ids `fare-congiuntivo-300`+: son de Phase 44 / INT-03, y la lista de cruces está cerrada."
    - "MUST NOT sincronizar `CATEGORIES` de `scripts/run-validation-271.mjs` ni `REAL_CATEGORIES` de la fixture de integración ni `TOTAL_EXPECTED`: son el entregable literal de Phase 44 / INT-02."
    - "MUST NOT arreglar de paso ninguna discrepancia de conteo preexistente de `genero-numero` ni de `preposiciones` (D-35-08 / D-40-12)."
    - "MUST NOT introducir en `options` ninguna forma del condizionale ni del imperativo de `fare`, ni mencionar el imperativo en una explanation: son casillas declaradas de Phase 43 y adelantarlas colisionaría con su magnet (D-42-15, D-42-16)."
---

<objective>
Dar de alta la categoría `fare-congiuntivo` en el disco y en el registro, y autorar sus **2 tiempos
simples** — congiuntivo presente y congiuntivo imperfetto — como 2 slots `multiple-choice` de
**6 variantes cada uno** (12 variantes = 12 casillas persona x tiempo), naciendo directamente en
slot+variantes y NUNCA en legacy payload.

La primera tarea es un **tracer**: un solo slot (`fare-congiuntivo-presente`) + su `notes` completo +
la entrada de `content/categories.json` + la línea del smoke paramétrico, verificado de punta a punta
antes de que exista una segunda variante. Este slot contiene 3 de las 10 variantes homógrafas de la
fase (`faccia` io/tu/lui-lei), así que es exactamente donde la forma tiene que estar bien: si está
mal, está mal una vez y no cinco.

Cubre CONG-01 (congiuntivo presente en las 6 personas, con las tres primeras homógrafas y resueltas
por sujeto explícito) y CONG-02 (congiuntivo imperfetto en las 6, con `facessi` homógrafa en io y tu).

Purpose: el `notes` que se escribe aquí es el mecanismo anti-inercia de toda la fase. El fallo
sistemático de esta categoría no es que la autoría escriba una forma rara: es que escriba
`Penso che io faccia i compiti`, que es la frase natural y está **mal construida** — el italiano
exige `Penso di fare i compiti` cuando los dos sujetos coinciden. El gate de no-correferencia
(D-42-06) tiene que estar declarado ANTES de la primera variante, con el quórum como red y no como
mecanismo, porque la coincidencia de sujetos es un patrón sistemático y dejarlo al quórum se pagaría
diez veces.

Output: `content/exercises/fare-congiuntivo.json` con `notes` + 2 slots + 12 variantes en
`validation.status: "pending"`, `content/categories.json` con 16 entradas, y 1 línea nueva en
`CATEGORIES_WITH_EXPLANATIONS`. Ningún fichero de `src/` cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-CONTEXT.md
@.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-PATTERNS.md
</context>

## Frontera de este plan (y de la fase)

Lo que este plan NO hace, declarado para que la verificación no lo busque:

- **Cruces multi-categoría** (`fare-congiuntivo-300`+) → Phase 44, INT-03. El espacio de ids `-300`+
  se deja libre: no se pre-crea, no se reserva con placeholder, simplemente no se usa. El cruce
  congiuntivo ↔ indicativo **no existe** y no está en la lista de INT-03 (D-42-03).
- **Sync de counts**: `CATEGORIES` de `scripts/run-validation-271.mjs`, `REAL_CATEGORIES` de la
  fixture de integración, `TOTAL_EXPECTED` y la fórmula del baseline-guard → Phase 44, INT-02.
- **El motor v1.4.** `git diff src/screens/app.js src/domain/ src/data/` queda vacío al cierre.
- **Un eje de variante nuevo** — Out of Scope literal del milestone. El eje es uno solo: el índice de
  variante, que en estos dos slots significa la persona.
- **Perífrasis y modismos de `fare`** — SCOPE-GATE HARD heredado (D-41-06 vía D-42-18).
- **El condizionale y el imperativo de `fare`** — casillas declaradas de Phase 43. Ninguna forma de
  esos dos modos entra en el fichero, ni como key ni como opción, ni se menciona el imperativo en
  ninguna explanation (D-42-15, D-42-16).
- **Los 2 compuestos, el slot del disparador y el fichero de test de la categoría** — son 42-02.
- **El quórum base canónico Opus+Sonnet** — no corre dentro del executor (D-42-04). Ver §Hand-off.

## Correcciones y precisiones de plan-time (verificadas en el codebase el 2026-08-06)

Cinco puntos donde la letra de 42-CONTEXT.md o del roadmap necesita una precisión sobre hechos
comprobados. Ninguna cambia una decisión; todas evitan un gate insatisfacible o un rojo espurio.

**1. La entrada de `content/categories.json` va en la tarea tracer, no al final.**
`src/data/schema-validator.js:133-146` exige que todo valor de `categoryIds` referencie una categoría
CONOCIDA, y `tests/domain.test.js` auto-descubre `content/exercises/*.json` y valida el bundle
completo exigiendo `result.errors` vacío. En el instante en que el fichero de contenido existe en
disco sin su entrada, la suite se pone roja. La entrada no es cosmética: es prerequisito de schema
(D-42-18, confirmado en 42-PATTERNS.md §`content/categories.json`).

**2. La línea de `CATEGORIES_WITH_EXPLANATIONS` también va en el tracer.** Es 1 línea con `expected`
DINÁMICO (`slotCountOf`, `tests/exercise-types.test.js:1268-1271`), auto-consistente y por tanto
incapaz de ponerse roja por conteo. Sin ella el fichero nuevo no recibe NINGÚN sub-test editorial y
D-42-17 quedaría sin verificación automatizada durante las 12 primeras variantes. El append va tras
la línea 1293 (entrada de `fare-indicativo`) y ANTES del comentario de cobertura editorial de la 1294.
Los otros arrays de conteo siguen diferidos a Phase 44.

**3. El gloss léxico de conjunción NO dispara el regex de leak R1 del smoke.** Verificado leyendo
`tests/exercise-types.test.js:1373`: el patrón prohíbe `§N`, un paréntesis que abra con una palabra
de metalenguaje del conjunto cerrado (refuerzo, regla, combina, grupo, familia, cuerpo, animales,
lugares, locución, `D-NN`) y una raya seguida de metalenguaje. Las cuatro glosas admitidas de D-42-13
no están en ese conjunto, así que `Benché (aunque)` pasa el sub-test. No hace falta ninguna excepción
ni tocar el test existente.

**4. La distractora de indicativo del presente `noi` colisiona con la key.** `facciamo` es a la vez
indicativo presente y congiuntivo presente de `noi`: es la MISMA cadena. El patrón de D-42-10 pide
«el indicativo de esa misma persona» como distractora (a), y en `noi` eso duplicaría la key, lo que
además rompería la regla de `options` sin duplicados. Resolución de plan-time, dentro de la letra de
D-42-10 (que fija los tres EJES de error, no tres cadenas): en la variante de `noi` del presente el
eje (a) se sustituye por una **segunda forma real de subjuntivo de otra persona**, y el `notes`
declara la sustitución con su razón. El mismo chequeo se hace forma a forma en el resto: en el
imperfetto no hay colisión (`facessi` frente a `facevo`, etc.), y en el presente `voi` y `loro`
tampoco (`facciate` frente a `fate`, `facciano` frente a `fanno`).

**5. Baseline de la suite: 766 pass / 0 fail**, medido el 2026-08-06 con
`node --test tests/*.test.js` en árbol limpio. Ojo (`[[test_command_node_glob]]`): el glob es
obligatorio, el path desnudo `tests/` falla en Node 22.20. El marcador honesto del trabajo pendiente
de esta fase es `VAL_07_STRICT=1 node --test tests/*.test.js`, no el reporter: verificado en Phase 41
que omitir la categoría de los arrays de conteo no pone el reporter rojo, lo deja **ciego** y sigue
reportando su total anterior en PASS, que es peor porque parece verde.

## Artifacts this phase produces

Símbolos e identificadores **creados** por la fase 42; un pase de source-grounding debe excluirlos de
la verificación de drift porque no existen hoy en el codebase.

| Símbolo / identificador | Fichero | Tipo | Plan |
|---|---|---|---|
| `content/exercises/fare-congiuntivo.json` | (nuevo) | fichero de contenido, 2 claves top-level | 42-01 |
| `fare-congiuntivo` | `content/categories.json` | `id` de categoría (`order: 16`, `origen: "ia-quorum"`) | 42-01 |
| `Fare — congiuntivo (faccia/facessi/abbia fatto)` | `content/categories.json` | `name` de la entrada (raya em U+2014) | 42-01 |
| `fare-congiuntivo-presente` | `content/exercises/fare-congiuntivo.json` | id de slot MC, 6 variantes (eje: persona) | 42-01 |
| `fare-congiuntivo-imperfetto` | idem | id de slot MC, 6 variantes (eje: persona) | 42-01 |
| `fare-congiuntivo-passato` | idem | id de slot MC, 6 variantes (eje: persona del auxiliar) | 42-02 |
| `fare-congiuntivo-trapassato` | idem | id de slot MC, 6 variantes (eje: persona del auxiliar) | 42-02 |
| `fare-congiuntivo-disparador` | idem | id de slot MC, 6 variantes (eje: el DISPARADOR) | 42-02 |
| entrada `fare-congiuntivo` en `CATEGORIES_WITH_EXPLANATIONS` | `tests/exercise-types.test.js` | elemento de array de test | 42-01 |
| `tests/content-fare-congiuntivo.test.js` | (nuevo) | fichero de test de invariantes de la categoría | 42-02 |
| `describe` 1 `estructura y conteos (D-42-01, D-42-18)` | idem | bloque de test | 42-02 |
| `describe` 2 `paradigma completo, 30 keys con homografia deliberada (CONG-01..CONG-04, D-42-05)` | idem | bloque de test | 42-02 |
| `describe` 3 `sujeto explicito y no-correferencia (D-42-06, D-42-18)` | idem | bloque de test | 42-02 |
| `describe` 4 `0-gloss del verbo con gloss lexico de conjuncion (D-42-13)` | idem | bloque de test | 42-02 |
| `describe` 5 `SCOPE-GATE lexico del objeto literal (D-41-06 heredado)` | idem | bloque de test | 42-02 |
| `describe` 6 `blacklist de formas atestiguadas y defendibles (D-42-11)` | idem | bloque de test | 42-02 |
| `describe` 7 `distractoras de presente e imperfetto (D-42-10)` | idem | bloque de test | 42-02 |
| `describe` 8 `distractoras de passato y trapassato, cero indicativo (D-42-09, SC-3)` | idem | bloque de test | 42-02 |
| `describe` 9 `slot del disparador, 4 casillas modo x tiempo (D-42-12, D-42-16, SC-4)` | idem | bloque de test | 42-02 |
| `describe` 10 `disparadores y marcos distintos por variante (D-42-02, D-42-07)` | idem | bloque de test | 42-02 |
| `describe` 11 `canon editorial e higiene del JSON (D-42-17, T-42-01, T-42-02)` | idem | bloque de test | 42-02 |
| `describe` 12 `audit trail de validacion y ronda EXTRA (D-42-08, T-42-03)` | idem | bloque de test | 42-02 |
| `describe` 13 `registro de la categoria (D-42-18, SC-5)` | idem | bloque de test | 42-02 |

Espacio de ids reservado y **no usado** por la fase: `fare-congiuntivo-300` y siguientes (Phase 44,
INT-03). No se pre-crea ni se reserva con placeholder.

## Multi-source coverage audit

| Fuente | Item | Plan | Estado |
|--------|------|------|--------|
| GOAL | ROADMAP §Phase 42 Goal — subjuntivo completo de `fare` como categoría propia y unidad de reset separada del indicativo | 42-01 (simples) + 42-02 (compuestos + disparador) | COVERED |
| GOAL | SC-1 — presente en las 6 personas (`faccia`x3, `facciamo`, `facciate`, `facciano`) e imperfetto en las 6 | 42-01 T1 (presente) + T3 (imperfetto) | COVERED |
| GOAL | SC-2 — sujeto en la frase en TODAS las homógrafas + ronda EXTRA sobre ese bloque | 42-01 T1/T3 (5 de las 10) + 42-02 T1 (las otras 5) + §Hand-off (ronda EXTRA) | COVERED |
| GOAL | SC-3 — passato y trapassato por concordancia, distractoras del OTRO tiempo del subjuntivo y no de otro modo | 42-02 T1 | COVERED |
| GOAL | SC-4 — slot del DISPARADOR con al menos una variante de contraste en indicativo | 42-02 T2 | COVERED |
| GOAL | SC-5 — todas `validated` por quórum, canon editorial, sin leak R1, carga en boot, unidad de reset independiente | 42-01 T1 (registro + sub-tests) + T2 (checkpoint) + 42-02 T3 (gates) + pase top-level | COVERED |
| REQ | CONG-01 congiuntivo presente, homógrafas io/tu/lui-lei resueltas por sujeto explícito | 42-01 T1 | COVERED |
| REQ | CONG-02 congiuntivo imperfetto, `facessi` homógrafa io/tu | 42-01 T3 | COVERED |
| REQ | CONG-03 congiuntivo passato y trapassato, tiempo elegido por concordancia | 42-02 T1 | COVERED |
| REQ | CONG-04 el disparador del subjuntivo | 42-02 T2 | COVERED |
| RESEARCH | — v2.0 sin RESEARCH.md (`--skip-research` deliberado: el diseño FARE-X1 estaba cerrado antes de abrir el milestone). Los análogos los aportan 42-CONTEXT.md §canonical_refs y 42-PATTERNS.md | n/a | N/A por diseño |
| CONTEXT | D-42-01 5º slot dedicado al disparador, 5x6=30, divergencia del roadmap y knock-on de Phase 44 (costly) | 42-01 T1 (`notes`) + 42-02 T2 | COVERED |
| CONTEXT | D-42-02 eje de `passato` y `trapassato` = las 6 personas del auxiliar, con el marco de concordancia covariando | 42-02 T1 | COVERED |
| CONTEXT | D-42-03 `categoryIds` de 1; la variante de contraste no es un cruce | 42-01 T1 + 42-02 T2 | COVERED |
| CONTEXT | D-42-04 2 plans + quórum base en pasada TOP-LEVEL, VAL-03 inviolable | la estructura de los 2 plans + §Hand-off | COVERED |
| CONTEXT | D-42-05 el bloque homógrafo son 10 variantes, no 5 | 42-01 T1/T3 (5) + 42-02 T1 (5) + 42-02 T3 (gate) | COVERED |
| CONTEXT | D-42-06 gate HARD de no-correferencia en las 30 | 42-01 T1 (`notes` + 6) + T3 (6) + 42-02 T1/T2 + 42-02 T3 (gate) | COVERED |
| CONTEXT | D-42-07 qué diferencia a las variantes que comparten key | 42-01 T1/T3 + 42-02 T1 + 42-02 T3 | COVERED |
| CONTEXT | D-42-08 ronda EXTRA DeepSeek obligatoria sobre las 10 homógrafas | §Hand-off de los 2 plans + 42-02 T3 (gate condicionado) | COVERED |
| CONTEXT | D-42-09 `passato`/`trapassato`: cero indicativo, las 3 distractoras son subjuntivo de esa persona | 42-02 T1 | COVERED |
| CONTEXT | D-42-10 `presente`/`imperfetto`: patrón fijo de 3 ejes de error | 42-01 T1/T3 (+ la precisión 4 de §Correcciones) | COVERED |
| CONTEXT | D-42-11 blacklist ampliada con el criterio de defendibilidad-en-contexto | 42-01 T1 (`notes`) + todas las tareas de contenido + 42-02 T3 | COVERED |
| CONTEXT | D-42-12 slot del disparador: las 4 casillas reales modo x tiempo, ninguna inventada | 42-02 T2 | COVERED |
| CONTEXT | D-42-13 0-gloss del verbo + gloss léxico de la conjunción (reversible) | 42-01 T1 (`notes` + prompts) + T3 + 42-02 T1/T2 + 42-02 T3 (gate) | COVERED |
| CONTEXT | D-42-14 el error de interferencia `penso che` / «pienso que», dosificado por slot | 42-01 T1/T3 (1 línea) + 42-02 T2 (desarrollo) + 42-02 T1 (ausencia deliberada) | COVERED |
| CONTEXT | D-42-15 hand-off del magnet del imperativo a Phase 43, documentado y sin mencionarlo | 42-01 T1 (`notes`) + prohibición en los 2 plans | COVERED |
| CONTEXT | D-42-16 `se` hipotético con la principal en condizionale de un verbo que no es `fare` | 42-02 T2 | COVERED |
| CONTEXT | D-42-17 canon editorial locked (RAE acentuado, apóstrofes ASCII, plain text, sin leak R1) | 42-01 T1 (línea del smoke) + todas las tareas de contenido + 42-02 T3 | COVERED |
| CONTEXT | D-42-18 heredados: slug exacto, slot+variantes, sujeto explícito, MC-only, SCOPE-GATE, ids semánticos, entrada obligatoria en `categories.json` | 42-01 T1/T3 + 42-02 T1/T2 + 42-02 T3 | COVERED |

**0 items MISSING.** No se requiere split de fase. Los ítems de §Deferred de 42-CONTEXT.md quedan
fuera por decisión explícita y no cuentan como hueco.

## Hand-off al pase TOP-LEVEL de quórum (D-42-04, D-42-08)

Explícito porque es el punto donde algo puede parecer hecho sin estarlo. **Es un entregable visible de
la fase, no un supuesto.**

| Momento | `validation.status` | `validation.passes[]` | Cómo se comprueba |
|---|---|---|---|
| Al cerrar 42-01 | `"pending"` en los 2 slots simples | `[]` | `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los slots `pending` |
| Al cerrar 42-02 | `"pending"` en los 5 slots | `[]` | idem, con 5 slots |
| Tras el pase top-level | `"validated"` en los 5 | ≥2 pases `correcta` con `by` distintos en los 5; y en los 4 slots del paradigma (`presente`, `imperfetto`, `passato`, `trapassato`) al menos un `by` que empieza por `deepseek` | `VAL_07_STRICT=1 node --test tests/*.test.js` en verde |

El quórum base canónico (Opus + Sonnet vía el skill `gsd-validate-exercise`, que spawnea Task
subagents) **no está disponible dentro de un subagent `gsd-executor`**
(`[[executor_cannot_run_task_quorum]]`), así que ninguna tarea de estos plans lo ejecuta. Se corre en
top-level tras `execute-phase`, **un ejercicio por contexto, NUNCA batched (VAL-03)**.

Criterios de aceptación del pase top-level, para que no quede como intención:

1. Los 5 slots pasan de `pending` a `validated`, y `status === deriveStatus(passes)` en los 5.
2. **Ronda EXTRA obligatoria (D-42-08):** pase DeepSeek vía `scripts/validate-ai-pass.mjs` (claves en
   `.env`) sobre exactamente las **10 variantes homógrafas** — `faccia` io/tu/lui-lei del presente,
   `facessi` io/tu del imperfetto, `abbia fatto` io/tu/lui-lei del passato, `avessi fatto` io/tu del
   trapassato —, **además** del quórum base. El audit trail se agrega a nivel de slot, así que los 4
   slots del paradigma acaban con un `by` `deepseek-*`. El slot del disparador NO lleva ronda extra
   (16 de 30 sería demasiado para una fase de este tamaño).
3. En esa misma pasada, el pase Opus de cada slot registra en sus `concerns` la re-declaración local
   del 0-gloss del verbo y, donde aplique, del gloss léxico de conjunción — un pase `correcta` admite
   `concerns` declarativas (precedente literal en `content/exercises/riflessivi.json`).
4. **Falso positivo de política esperado:** Gemini y DeepSeek marcarán el gloss `Benché (aunque)` como
   C5-leak. NO se «arregla» (D-42-13, `[[gloss_es_desambiguacion_canon]]`): la base de aprobación es
   Claude Opus+Sonnet. En cambio, un flag **C4-accent** sobre español sin tildes es bug **REAL** y se
   arregla poniendo los acentos (`[[explanations_must_be_accented]]`).

## Assumptions (probe de edges sin SPEC — 4 filas `unclassified` surfaceadas, 0 descartes silenciosos)

No existe SPEC.md para esta fase, así que el probe determinista de edges corrió sobre CONG-01..CONG-04
y devolvió las 4 filas como `unclassified` (`coverage: {applicable: 4, resolved: 0, unresolved: 4}`).
Su clasificador funciona por palabras clave en inglés y el texto de los requisitos está en español.
Por protocolo una fila `unclassified` queda **`unresolved`** y **nunca** se auto-`backstop`ea ni se
auto-descarta: se surfacea aquí como **supuesto explícito marcado**.

| Requisito | Qué NO derivó el probe | Supuesto marcado que asume este plan |
|---|---|---|
| CONG-01 | ninguna categoría de edge (boundary / adjacency / encoding / ordering / empty / precision) | el edge real del presente es la **frontera de homografía**: `faccia` cubre io/tu/lui-lei, así que la unicidad depende por completo del sujeto explícito y del disparador; más la colisión `facciamo` indicativo == congiuntivo en `noi` (§Correcciones 4). Autorados a mano como `must_haves.truths` de este plan |
| CONG-02 | idem | el edge real del imperfetto es la misma frontera con `facessi` io/tu, más que ninguna distractora regularizada coincida con una forma real de otro tiempo (`facemmo`, `facesti`, `faceste` son passato remoto de indicativo). Autorado a mano como truth |
| CONG-03 | idem | tratado en 42-02: la frontera es que el marco de concordancia excluya limpiamente los DOS simples, o `faccia`/`facessi` serían defendibles (el trabajo fino que D-42-09 deja abierto) |
| CONG-04 | idem | tratado en 42-02: la frontera es que el disparador determine modo Y tiempo sin ambigüedad, incluida la variante de contraste cuya respuesta correcta es el indicativo |

**Igualdad de no-descarte-silencioso:** 4 filas surfaceadas por el probe == **0 autoradas como truths
desde el probe** + **4 surfaceadas aquí como supuestos marcados**. Los edges de esta fase se derivaron
a mano y se escribieron como `must_haves.truths` independientes del probe (schema boundary, count
boundary, id-space boundary, homograph/ambiguity boundary, blacklist absence), más **1 `backstop`** — la
unicidad de lectura del disparador y del marco, marcador estructurado con `verification: backstop`,
porque es un juicio lingüístico que ninguna aserción mecánica puede cerrar. Si el verificador no puede
confirmarlo con evidencia explícita debe abstenerse a `human_needed`, nunca dar pase silencioso.

## Nota de escaneo (no negociable, heredada de 41-01)

Todas las comprobaciones de **ausencia** de este plan van SIEMPRE **por campo** — `variants[].prompt` y
`variants[].options[]`, vía `node -e` sobre el JSON parseado — y **nunca** sobre el fichero completo ni
sobre `notes`. El `notes` de la categoría **nombra a propósito** cada forma de la blacklist y cada
perífrasis excluida, con su audit trail, así que un `grep` de fichero entero se auto-invalidaría. Por
la misma razón este plan no usa ni un solo grep negativo de fichero completo: los conjuntos de formas
prohibidas viven como arrays en criterios `node -e` con coincidencia **exacta**, nunca por subcadena
(`face` es prefijo de `facesse`, `faceste` y `faceva`, que son formas legítimas de esta categoría).

<!-- planner-discipline-allow: facci -->
<!-- planner-discipline-allow: facciam -->
<!-- planner-discipline-allow: facce -->
<!-- planner-discipline-allow: farei -->
<!-- planner-discipline-allow: fo -->
<!-- planner-discipline-allow: fan -->
<!-- planner-discipline-allow: face -->
<!-- planner-discipline-allow: espa -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — `fare-congiuntivo-presente` de punta a punta: fichero + `notes` + entrada de `categories.json` + línea del smoke</name>

  <files>content/exercises/fare-congiuntivo.json, content/categories.json, tests/exercise-types.test.js</files>

  <read_first>
    - `content/exercises/fare-indicativo.json` — el molde a clonar en forma: las 2 claves top-level, el
      orden literal de campos de un slot MC (`id`, `type`, `categoryIds`, `explanation`, `variants`,
      `validation`), los 3 campos de una variante (`prompt`, `options`, `correctIndex`, nada más), el
      shape de `validation.passes[]` (`by`, `date`, `verdict`, `concerns`) y sobre todo el **estilo de
      `notes`**: un único string en prosa, gigantesco, plain text sin markdown ni saltos de línea, en
      español acentuado RAE, con las secciones marcadas en MAYÚSCULAS dentro del propio texto y cada
      gate declarado con su «por qué» y su audit trail, incluidas las correcciones de autoría
      posteriores con su fecha.
    - `tests/content-fare-indicativo.test.js` — el idioma de los gates que 42-02 va a clonar: el
      **escaneo por campo** (construir `const campos = [v.prompt, ...v.options]` y filtrar), el patrón
      `assert.deepEqual(sucio, [], mensaje)` para que el diff liste las ofensoras, el escaneo **en
      positivo** sobre `notes` exigiendo el audit trail de cada forma de la blacklist entre apóstrofes
      ASCII, y la advertencia de escaneo de la cabecera. Leerlo AHORA condiciona cómo se escribe el
      `notes` de esta tarea: si una forma de la blacklist no va entre apóstrofes ASCII, el gate
      equivalente de 42-02 no la encontrará.
    - `content/categories.json` (19 líneas) — las 15 entradas actuales, las 4 claves exactas de una
      entrada, el patrón de `name` con raya em, y la alineación por columnas hecha a mano.
    - `src/data/schema-validator.js` líneas 133-146 (`categoryIds` con referencias conocidas), 148-174
      (`payload` XOR `variants`), 210-231 (`explanation` obligatoria a nivel de slot, `variants` no
      vacío) y 435-456 (`validateMultipleChoiceSurface`: `prompt` con `___`, `options` de 3 o 4 strings
      no vacíos, `correctIndex` entero en rango).
    - `tests/exercise-types.test.js` líneas 1265-1295 (`slotCountOf` + `CATEGORIES_WITH_EXPLANATIONS`,
      con la entrada de `fare-indicativo` en la 1293 como análogo literal y el comentario de cobertura
      cerrando en la 1294) y 1336-1405 (los sub-tests de smart-quotes, markdown, leak R1 y cross-refs
      R2 que la línea nueva activa; el regex de leak de la 1373 confirma que las glosas de conjunción
      de D-42-13 NO lo disparan).
    - `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-PATTERNS.md` — extractos
      verbatim del molde, del shape de `validation`, de la entrada de `categories.json`, y la tabla de
      las **tres desviaciones deliberadas** respecto del test análogo.
    - `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-CONTEXT.md` §decisions —
      D-42-01, D-42-03, D-42-05, D-42-06, D-42-07, D-42-10, D-42-11, D-42-13, D-42-14, D-42-15,
      D-42-17, D-42-18.
    - `.planning/REQUIREMENTS.md` línea 30 — el texto literal de CONG-01, incluida la frase «el
      pronombre sujeto (o un sujeto explícito) DEBE estar en la frase o el ejercicio es irresoluble».
  </read_first>

  <precondition>`node --test tests/*.test.js` reporta 766 pass / 0 fail en el árbol limpio ANTES de cualquier edición — baseline medido el 2026-08-06. Re-medirlo, no confiar en el número: si no da `fail 0`, parar y reportar en vez de seguir, porque el resto del plan mide «sin fails nuevos» contra él.</precondition>

  <reversibility rating="costly">Escribe el slug `fare-congiuntivo` como `id` de `categories.json` y los primeros ids semánticos de slot. El slug ya es elemento de `RESET_PREFIXES_V13` (`src/data/storage.js:1345`, Phase 40 shippeada), nombre del fichero y prefijo de los ids de cruce de Phase 44, así que renombrarlo después de que el autor haya fallado un slot deja estado huérfano en su `exerciseStats` real. No se inserta `checkpoint:decision`: la calificación es `costly`, no `one-way`, y las casillas del paradigma no tienen grados de libertad que aprobar — el slug lo cerró D-40-01/02.</reversibility>

  <action>
    Tres ediciones que van juntas porque separarlas deja la suite en rojo entre commits: el fichero de
    contenido no valida sin la entrada de `categories.json`, y sin la línea del smoke el fichero no
    recibe ningún sub-test editorial.

    **A. `content/categories.json` — append de 1 entrada (D-42-18)**

    Añadir coma al final de la línea 17 (`fare-indicativo`, order 15) y una línea 18 con las 4 claves
    exactas y en este orden: `id` = `fare-congiuntivo`, `name` = `Fare — congiuntivo (faccia/facessi/abbia fatto)`
    (raya em U+2014, igual que la entrada de `fare-indicativo`), `order` = 16, `origen` = `ia-quorum`.
    Respetar la alineación por columnas de las 15 filas existentes (no se pasa formateador sobre este
    fichero). `origen` solo acepta `ia-quorum` o `apuntes-profesora` (enum PROV-01,
    `tests/schema-validator-origen.test.js`): un typo da `ok:false`. `order` es documental; el orden del
    array define el display.

    **B. `content/exercises/fare-congiuntivo.json` — fichero nuevo con `notes` + 1 slot**

    Exactamente 2 claves top-level, `notes` y `exercises`. Sin `version`, sin `category`.

    `notes` es UN único string en prosa, plain text, sin markdown, sin backticks, sin saltos de línea,
    en español acentuado RAE y con apóstrofes ASCII U+0027. **Sin corchetes angulares en ningún punto
    del fichero** (usar «frente a» o « a » donde el análogo usaría una flecha), para que los escaneos de
    higiene queden limpios y sin excepciones. Instanciar el esqueleto del análogo (identidad y linaje ·
    la omisión en MAYÚSCULAS con su decision-id y su razonamiento · el cierre anti-inercia «es decisión,
    no omisión» · el gate etiquetado HARD con su frontera · la nota de count-sync) sobre estas DOCE
    declaraciones, todas escritas AHORA aunque cubran slots que autora 42-02:

    1. **Identidad, linaje y volumen.** Categoría `fare-congiuntivo` (order 16) nacida DIRECTAMENTE en
       slot+variantes, clon del patrón v1.7/v1.9 de `presente-regolare`, `modali`, `possessivi`,
       `riflessivi` y `fare-indicativo`, NUNCA legacy payload. **5 slots x 6 variantes = 30 variantes.**
       Los 4 primeros slots son las 4 casillas del subjuntivo de `fare` y su eje de variante es la
       persona; el 5º es el DISPARADOR y su eje es el disparador, no la persona. Un solo eje por slot en
       los cinco, así que `pickVariantIndex` los sirve sin una línea de motor nueva.
       **DIVERGENCIA DECLARADA respecto al roadmap (D-42-01):** el título de la fase dice «4 slots» y
       «≈24 variantes»; son 5 y 30 porque SC#4 exige un slot para el disparador con al menos una
       variante de contraste cuya respuesta correcta es el INDICATIVO, y esa variante no cabe en el slot
       de presente sin romper la enumeración de 6 personas que fija SC#1. Anotar el knock-on: el
       milestone pasa de 21 slots y ≈107 variantes a 22 slots y ≈113, dato que Phase 44 / INT-02 necesita.
    2. **GATE DE NO-CORREFERENCIA DE SUJETOS (HARD, D-42-06), con audit trail.** En italiano estándar,
       cuando el sujeto de la principal COINCIDE con el del subordinado, la construcción con `che` +
       subjuntivo es defectuosa: se exige `di` + infinitivo. Escribir la pareja explícita: la forma
       correcta es `Penso di fare i compiti` y la incorrecta `Penso che io faccia i compiti`. Y como
       `penso che` es el primer disparador que nombra CONG-04, la variante de `io` escrita del modo
       obvio sale mal construida. Declarar el gate: **el sujeto de la principal NUNCA coincide con el
       sujeto del hueco, en ninguna de las 30 variantes**, y se cumple de dos maneras — disparador
       impersonal (`Bisogna che`, `È necessario che`, `È importante che`, `Sembra che`, `Basta che`,
       `Peccato che`, `Benché`, `Prima che`, `Purché`, `Nonostante`) o sujeto explícito distinto en la
       principal (del tipo `Lui pensa che io faccia`). Cerrar con la razón de ser un gate y no un
       hallazgo del quórum: la coincidencia de sujetos es un patrón sistemático, no un fallo aislado,
       así que dejarlo al quórum se pagaría diez veces. El quórum es la red, no el mecanismo.
    3. **0-GLOSS DEL VERBO (D-42-13), con su excepción léxica declarada en el mismo párrafo.** Cero
       gloss de traducción al español sobre la forma verbal en las 30 variantes. Razón reforzada
       respecto a `fare-indicativo`: el español TAMBIÉN tiene subjuntivo, así que un gloss sobre el
       verbo regala modo Y tiempo a la vez — doble leak R1 en una categoría cuyo ejercicio es
       exactamente elegir modo y tiempo. **Excepción: SÍ hay gloss léxico sobre la conjunción** cuando
       está por encima del nivel A1 que el autor prepara, y solo con estas cuatro glosas: `benché` con
       «aunque», `purché` con «siempre que», `prima che` con «antes de que», `nonostante` con «a pesar
       de que». No filtra nada, porque «aunque» rige los dos modos en español, así que saber la
       traducción no dice qué rige el italiano: es el canon R7 aplicado al léxico, no a la respuesta.
       Razón de no ir a 0-gloss total: fallar por vocabulario resetea la categoría entera por la cascada
       D-54, que es justo el ejercicio injusto que esta herramienta existe para evitar. **Aviso para el
       quórum:** Gemini y DeepSeek marcarán este gloss como C5-leak; es falso positivo de política en
       este proyecto y NO se arregla en ninguna de las dos direcciones — la base de aprobación es Claude
       Opus+Sonnet. Cerrar con: el 0-gloss del verbo y el gloss de la conjunción son las dos decisión,
       no omisión ni descuido.
    4. **0-MATCH (D-42-18 / DESIGN RULE D-04).** Ningún slot `match`: el pareo de persona a forma es una
       asociación MECÁNICA derivable en cuanto se conoce la raíz, es decir arrastrar sin pensar, que es
       lo que D-04 y R3 prohíben. Cerrar con: es decisión, no omisión.
    5. **0-WORD-BUTTONS (D-42-18).** Ningún slot `word-buttons`: lo que se examina siempre es elegir
       modo y tiempo, nunca construir el orden de las palabras. Cerrar con: es decisión, no omisión.
    6. **BLACKLIST DE FORMAS ATESTIGUADAS Y DEFENDIBLES (D-42-11), con audit trail y criterio operativo.**
       Empezar por el criterio, porque es lo que evita que una pasada futura crea que la regla está
       invertida: lo que la regla ratificada por el autor prohíbe es la distractora **defendible como
       correcta**, y una forma atestiguada solo es defendible si el prompt la admite. Bajo un disparador
       de subjuntivo, `faccio` está atestiguada pero NO es defendible — el disparador la excluye sin
       ambigüedad —, así que la distractora que cruza el modo es la regla aplicada correctamente al
       CONTEXTO, no su inversión. Después la lista, cada forma entre apóstrofes ASCII U+0027 y con qué
       es y por qué no se usa: las heredadas arcaicas, poéticas y dialectales de Phase 41 (`fo`, `fé`,
       `fenno`, `facea`, `fan`, `face`, `faci`, `fei`, `festi`, `femmo`, `feste`, `fero`, `feciono`,
       `fici`, `facisti`, `facette`, `facettero`, `facero`); las trampas NUEVAS de esta categoría —
       `facci`, que no es un error de conjugación sino el imperativo `fa'` con el pronombre `ci`
       aglutinado, atestiguado y corriente, y por eso el calco más probable que la autoría generaría
       como distractora «obviamente mala»; `facciam`, truncamiento poético de `facciamo`; y `facce`,
       plural del sustantivo cara —; y las casillas de Phase 43, que se mantienen fuera de `options`: la
       familia del condizionale (`farei`, `faresti`, `farebbe`, `faremmo`, `fareste`, `farebbero`) y el
       imperativo apostrofado de 2ª singular.
    7. **HOMOGRAFÍAS VISTAS Y DELIBERADAS (D-42-11).** Cuatro, declaradas para que el quórum sepa que
       están vistas y no son bugs, y para que no se «arreglen»: `faccia` es a la vez la key de tres
       personas y el sustantivo cara; `facciate` es a la vez la key de `voi` y el plural del sustantivo
       fachada; `abbiamo fatto` es a la vez congiuntivo passato de `noi` y passato prossimo de
       indicativo — la MISMA cadena, así que no abre doble validez, solo doble análisis, y ya vive en
       `fare-indicativo`; y `faceste` es a la vez congiuntivo imperfetto de `voi` y passato remoto de
       indicativo de `voi`. Las cuatro son keys legítimas aquí, así que NO están en la blacklist de
       ausencia: la diferencia entre las dos listas es que una se comprueba por ausencia en `options` y
       la otra solo se documenta.
    8. **LOS TRES PATRONES DE DISTRACTORAS.** (a) `presente` e `imperfetto` (D-42-10): tres ejes de
       error reales — el indicativo de ESA MISMA persona, que es el error de interferencia del
       hispanohablante; una forma REAL de subjuntivo de OTRA persona, que solo se descarta leyendo el
       sujeto y es la trampa que las homógrafas exigen; y una raíz regularizada INEXISTENTE en italiano.
       Con la corrección de autoría de esta fase: en la variante de `noi` del presente el eje del
       indicativo se sustituye por una segunda forma real de otra persona, porque `facciamo` es la misma
       cadena en indicativo y en subjuntivo y duplicaría la key. (b) `passato` y `trapassato` (D-42-09):
       las 3 distractoras son formas de SUBJUNTIVO de esa misma persona — el otro compuesto más los dos
       simples — y **ningún indicativo**, porque SC#3 dice literalmente «el OTRO tiempo del subjuntivo,
       no otro modo» y la verificación lo va a leer al pie de la letra; el precio es que el marco de
       concordancia de cada prompt tiene que excluir limpiamente los dos simples, y ese es el trabajo
       fino de esos dos slots. (c) `disparador` (D-42-12): las 4 opciones son las 4 casillas reales de
       esa persona — congiuntivo presente, indicativo presente, congiuntivo imperfetto, indicativo
       imperfetto —, el autor decide modo Y tiempo, y **ninguna opción es forma inventada**.
    9. **SCOPE-GATE (HARD, heredado de D-41-06 vía D-42-18) del objeto literal.** El objeto de `fare` es
       SIEMPRE literal y transparente, del conjunto cerrado `i compiti`, `un errore`, `il lavoro`, `una
       torta`, `il letto`, `tutto`, `una foto`. Quedan FUERA las perífrasis y los modismos: son
       vocabulario y sintaxis, no paradigma, y tienen categoría propia si el autor los echa en falta.
       NINGUNA de las 30 variantes los cruza. Declarar la frontera y el efecto colateral buscado: sin
       perífrasis no queda léxico verbal que glosar, lo que refuerza el 0-gloss del verbo.
    10. **DOSIFICACIÓN DEL ERROR DE INTERFERENCIA (D-42-14).** `penso che` rige subjuntivo en italiano
        mientras «pienso que» rige INDICATIVO en español: el punto de interferencia más valioso de la
        categoría. La explanation del slot del disparador lo DESARROLLA con el par explícito y con la
        lista de disparadores italianos que exigen subjuntivo donde el español no; `presente` e
        `imperfetto` llevan UNA línea de recordatorio, porque el motor sirve un slot por sesión y por
        ahí se pasa muchas veces sin volver a leer la advertencia; y `passato` y `trapassato` NO lo
        repiten, porque su eje es la anterioridad y no el modo. Es dosificación deliberada, no
        inconsistencia editorial.
    11. **HAND-OFF A PHASE 43 (D-42-15).** `faccia`, `facciamo` y `facciano` son idénticas a 3 de las 5
        formas del imperativo de `fare`, y `fate` ya vive en `fare-indicativo`: tras esta fase **4 de
        las 5 formas del imperativo ya están en el corpus**, así que el magnet de Phase 43 es mayor que
        el que INT-04 declara. Registrarlo aquí, y dejar constancia de que las explanations de esta fase
        NO mencionan el imperativo a propósito — sería adelantarse a otra fase (precedente exacto: Phase
        41 hizo lo mismo con la forma de 3ª singular del indicativo).
    12. **Nota de escaneo por campo y nota de count-sync.** Escaneo: todas las comprobaciones de
        ausencia se hacen SIEMPRE por campo, sobre `prompt` y sobre `options`, y NUNCA sobre el fichero
        completo, porque este `notes` nombra a propósito cada forma de la blacklist y cada perífrasis
        excluida y un grep de fichero entero se auto-invalidaría; y siempre por coincidencia exacta,
        nunca por subcadena, porque la forma corta atestiguada de 3ª singular del passato remoto es
        prefijo de tres formas legítimas de esta categoría. Count-sync: la sincronización de los arrays
        del reporter y de la fixture de integración, más `TOTAL_EXPECTED` y la fórmula del
        baseline-guard, son de Phase 44 / INT-02; consecuencia verificada en Phase 41: mientras la
        categoría falte de esos arrays el reporter NO se pone rojo, queda CIEGO al fichero y sigue
        reportando su total anterior en PASS, que es peor porque parece verde. El marcador honesto del
        trabajo pendiente es `VAL_07_STRICT=1 node --test tests/*.test.js`, que sí ve el fichero por la
        entrada del smoke paramétrico.

    Después, `exercises` con UN solo elemento:

    - `id`: `fare-congiuntivo-presente`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-congiuntivo"]` — exactamente un id (D-42-03).
    - `explanation`: un párrafo largo en español acentuado, tono D-127 (3ª persona impersonal, regla,
      paradigma completo, paralelo italiano-español, y la trampa del hispanohablante al final). Debe
      citar las 6 formas `faccia`, `faccia`, `faccia`, `facciamo`, `facciate`, `facciano`; decir
      explícitamente que las tres primeras personas son la MISMA forma y que por eso el sujeto tiene que
      estar en la frase; explicar que el congiuntivo presente de `fare` usa la raíz `facc-` en las seis
      personas, sin la alternancia que el indicativo sí tiene; llevar UNA sola línea de recordatorio del
      error de interferencia de D-42-14 (que `penso che` pide subjuntivo en italiano y su equivalente
      español pide indicativo) sin desarrollarlo; y cerrar nombrando las tres trampas de sus
      distractoras (el indicativo de la misma persona, la forma real de otra persona y la raíz
      regularizada). **No menciona el imperativo** (D-42-15). Cero corchetes angulares, cero markdown,
      cero backticks, cero comillas tipográficas, apóstrofes ASCII, y ninguna referencia a otro
      ejercicio por id (el sub-test R2 lo escanea).
    - `variants`: 6 objetos con SOLO `prompt`, `options`, `correctIndex`. Una persona por variante en el
      orden `io`, `tu`, `lui/lei`, `noi`, `voi`, `loro`. Las 6 keys en ese orden son `faccia`, `faccia`,
      `faccia`, `facciamo`, `facciate`, `facciano` — **la key se repite tres veces a propósito**
      (D-42-05) y la unicidad la da el sujeto.
    - Cada `prompt`: un disparador de subjuntivo distinto en cada una de las 6 variantes (D-42-07),
      el sujeto del hueco como **pronombre explícito** de la persona de esa variante (D-42-18), el hueco
      literal `___`, y un objeto literal del conjunto cerrado de 7 — distinto entre las tres variantes
      que comparten la key `faccia`. **Gate HARD de no-correferencia (D-42-06):** el sujeto de la
      principal nunca coincide con el del hueco; se cumple con disparador impersonal o con un sujeto
      explícito distinto en la principal. Repertorio de disparadores de subjuntivo para este slot, de
      los que se eligen 6 distintos: `Bisogna che`, `È necessario che`, `È importante che`, `Sembra
      che`, `Basta che`, `Peccato che`, `Benché`, `Purché`, `Nonostante`, y las formas con sujeto
      explícito distinto del tipo `Lui pensa che`, `Mia madre vuole che`, `Il professore spera che`.
      Si se usa `benché`, `purché`, `prima che` o `nonostante`, va con su gloss léxico entre paréntesis
      del conjunto cerrado de cuatro (D-42-13) y el paréntesis va inmediatamente detrás de la
      conjunción, nunca junto a una forma verbal. Ninguna otra palabra en español dentro del prompt.
    - `options`: 4 strings sin duplicados. La key es la forma correcta de la persona del prompt. Las
      otras 3 siguen el patrón fijo de D-42-10: UNA es el indicativo presente de ESA MISMA persona
      (`faccio` para io, `fai` para tu, `fa` para lui/lei, `fate` para voi, `fanno` para loro); UNA es
      una forma REAL de congiuntivo presente de OTRA persona, elegida distinta en cada variante para que
      las tres que comparten key no tengan el mismo conjunto de opciones; y UNA es una raíz regularizada
      INEXISTENTE en italiano, del tipo de las que salen de tratar `fare` como un verbo regular o de
      escribir una sola c donde el paradigma pide dos. **Excepción obligada en `noi`** (§Correcciones 4):
      `facciamo` es la misma cadena en indicativo y en congiuntivo, así que ahí el eje del indicativo se
      sustituye por una segunda forma real de congiuntivo de otra persona, y ese cambio se declara en el
      `notes`. Regla dura para la distractora inexistente: antes de fijarla hay que comprobarla una a
      una — no puede ser forma atestiguada de `fare` en ningún modo ni tiempo, no puede estar en la
      blacklist (en particular quedan prohibidas la forma con `ci` aglutinado y el truncamiento
      poético), no puede ser palabra italiana homógrafa, y no puede diferir de una forma real solo por
      un acento, porque eso la convierte en un aparente error de tecleo en vez de en un error de
      conjugación.
    - `correctIndex`: entero en `[0,4)`, variado entre las 6 variantes (no las 6 en la misma posición).
    - `validation`: `{ "status": "pending", "passes": [] }`. No se fabrica ningún pase: el quórum base
      es la pasada top-level de D-42-04.

    **C. `tests/exercise-types.test.js` — 1 línea en `CATEGORIES_WITH_EXPLANATIONS`**

    Tras la entrada de `fare-indicativo` (línea 1293) y ANTES del comentario de cobertura editorial de
    la 1294: un comentario de una línea que nombre la fase y los requisitos (v2.0 Phase 42,
    CONG-01..CONG-04, `fare-congiuntivo`, 5 slots = 4 del paradigma + el disparador) y la entrada con
    `file: 'content/exercises/fare-congiuntivo.json'` y
    `expected: slotCountOf('content/exercises/fare-congiuntivo.json')` — dinámico, NUNCA número mágico
    (D-31-06). No tocar ningún otro array de conteo.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js && node -e "const fs=require('fs');const cats=JSON.parse(fs.readFileSync('content/categories.json','utf8')).categories;const e=cats.find(c=>c.id==='fare-congiuntivo');if(!e)throw new Error('falta la entrada de categories.json');if(e.order!==16||e.origen!=='ia-quorum')throw new Error('entrada mal formada: '+JSON.stringify(e));if(Object.keys(e).join(',')!=='id,name,order,origen')throw new Error('claves inesperadas: '+Object.keys(e));if(cats.length!==16)throw new Error('esperaba 16 categorias, hay '+cats.length);const d=JSON.parse(fs.readFileSync('content/exercises/fare-congiuntivo.json','utf8'));if(Object.keys(d).join(',')!=='notes,exercises')throw new Error('claves top-level inesperadas: '+Object.keys(d));const n=d.exercises.filter(x=>(x.categoryIds||[]).includes('fare-congiuntivo')).length;if(n===0)throw new Error('totalCount 0 -> examenEnabled false -> la fila no es jugable');console.log('SC-5 OK: fila order '+e.order+', totalCount '+n)"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con `pass` de al menos 772 (baseline 766 re-medido + los sub-tests editoriales que activa la línea nueva).
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; if(c.length!==16) process.exit(1); const e=c[15]; process.exit(e.id==='fare-congiuntivo'&&e.order===16&&e.origen==='ia-quorum'&&Object.keys(e).join(',')==='id,name,order,origen'?0:1)"` sale con código 0 — la entrada es la 16ª y última, con las 4 claves exactas.
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; process.exit(c[15].name.includes(String.fromCharCode(8212))?0:1)"` sale con código 0 — el `name` usa raya em U+2014 como las 15 anteriores.
    - `git diff content/categories.json | grep -c '^-'` devuelve `1` — la única línea eliminada es la 17 original (reescrita con coma); ninguna de las 15 entradas se altera de otro modo.
    - `grep -c "slotCountOf('content/exercises/fare-congiuntivo.json')" tests/exercise-types.test.js` devuelve `1`, y `grep -c 'fare-congiuntivo' tests/exercise-types.test.js` es `>= 2`.
    - `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/fare-congiuntivo.json','utf8')); const s=d.exercises[0]; process.exit(d.exercises.length===1&&s.id==='fare-congiuntivo-presente'&&s.type==='multiple-choice'&&s.categoryIds.length===1&&s.categoryIds[0]==='fare-congiuntivo'&&s.variants.length===6?0:1)"` sale con código 0.
    - `node -e` sobre `exercises[0]`: el array de `options[correctIndex]` de las 6 variantes es exactamente `["faccia","faccia","faccia","facciamo","facciate","facciano"]` en ese orden (D-42-05, CONG-01).
    - Escaneo `node -e` sobre los 6 `prompt`: los 6 contienen `___`; los 6 contienen el pronombre sujeto de su persona (`io`, `tu`, `lui` o `lei`, `noi`, `voi`, `loro`, en cualquier capitalización y como palabra suelta); los 6 disparadores son strings distintos entre sí; el objeto literal es distinto en las tres variantes cuya key es `faccia` (D-42-06, D-42-07, D-42-18).
    - Escaneo `node -e` sobre los 6 `prompt`: **0** contienen la subcadena `espa` en ninguna capitalización; y todo contenido entre paréntesis pertenece al conjunto cerrado `["aunque","siempre que","antes de que","a pesar de que"]` — un prompt sin paréntesis pasa trivialmente, un paréntesis con cualquier otro contenido falla (D-42-13).
    - Escaneo `node -e` sobre los 6 `prompt`: para cada paréntesis, el texto que lo precede termina en una de las conjunciones glosadas (`Benché`, `benché`, `Purché`, `purché`, `Prima che`, `prima che`, `Nonostante`, `nonostante`) seguida de un espacio, y en los 12 caracteres anteriores al paréntesis no aparece `facc`, `face`, `abbia` ni `aves` (D-42-13: el gloss es de la conjunción, nunca del verbo).
    - Escaneo `node -e` sobre `exercises[0].variants[].options[]` con coincidencia **EXACTA** (nunca subcadena): las 6 listas tienen longitud 4 y sin duplicados internos; **0** coincidencias con el conjunto heredado `["fo","fé","fenno","facea","fan","face","faci","fei","festi","femmo","feste","fero","feciono","fici","facisti","facette","facettero","facero"]`; **0** con las trampas nuevas `["facci","facciam","facce"]`; **0** con las casillas de Phase 43 `["farei","faresti","farebbe","faremmo","fareste","farebbero","fa'"]` (D-42-11).
    - Escaneo `node -e` sobre las 6 variantes: en cada una, exactamente 1 opción es la key; exactamente 1 pertenece al conjunto de las 6 formas del congiuntivo presente distinto de la key (la forma real de otra persona), salvo en la variante de `noi`, donde son 2 por la excepción de §Correcciones 4; y las opciones restantes no pertenecen a la unión de las formas reales de `fare` en ningún modo ni tiempo (D-42-10).
    - Escaneo `node -e` sobre las 6 variantes: los conjuntos de `options` de las tres variantes cuya key es `faccia` son distintos dos a dos (D-42-07).
    - Escaneo `node -e` sobre `prompt` y `options[]`: cada prompt contiene al menos un objeto del conjunto cerrado `["i compiti","un errore","il lavoro","una torta","il letto","tutto","una foto"]`, y hay **0** coincidencias para los marcadores de perífrasis `["colazione","spesa","freddo","farcel","far fare"]` (D-42-18, D-41-06).
    - Escaneo `node -e` sobre TODOS los strings del fichero (`notes`, `explanation`, `prompt`, `options`): **0** apariciones de los caracteres `<` y `>`, **0** de `&#`, **0** de `javascript:`, **0** de las comillas tipográficas de apertura y cierre, y **0** backticks (T-42-01, D-42-17).
    - Escaneo `node -e` recursivo sobre el objeto parseado: ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`, y `({}).polluted === undefined` tras el parse (T-42-02).
    - `node -e` sobre `exercises[0].explanation`: contiene al menos un carácter del conjunto `áéíóúñÁÉÍÓÚÑ`; contiene las 4 cadenas distintas del paradigma (`faccia`, `facciamo`, `facciate`, `facciano`); su longitud es `> 400` caracteres; y **no** contiene `imperativo` (D-42-15, D-42-17).
    - `node -e` sobre `notes`: longitud `> 4000` caracteres; contiene los 12 marcadores de decisión `D-42-01`, `D-42-03`, `D-42-05`, `D-42-06`, `D-42-07`, `D-42-09`, `D-42-10`, `D-42-11`, `D-42-12`, `D-42-13`, `D-42-14`, `D-42-15` (o su enunciado equivalente en prosa junto al id); contiene cada forma de la blacklist de ausencia entre apóstrofes ASCII, con el formato exacto `'forma'`, para que el gate positivo de 42-02 la encuentre; contiene las subcadenas `no omisión` al menos 3 veces, `HARD`, `22 slots`, `113`, y las cuatro homografías declaradas.
    - `node -e`: `exercises[0].validation.status === 'pending'` y `exercises[0].validation.passes.length === 0` (D-42-04).
    - `node -e`: ningún id del fichero coincide con el patrón de sufijo numérico de 3 cifras `/-\d{3}$/` — `fare-congiuntivo-300` y siguientes están libres (D-42-18).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla, y su salida nombra `content/exercises/fare-congiuntivo.json` con `fare-congiuntivo-presente` y `pending` — el marcador del hand-off existe y es legible.
    - `node scripts/run-validation-271.mjs` sigue reportando su total anterior en PASS — la ceguera documentada en §Correcciones 5, no un fallo.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ tests/fixtures/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    `content/exercises/fare-congiuntivo.json` existe con su `notes` completo (las 12 declaraciones, con
    el gate de no-correferencia y la blacklist escritos ANTES de la primera distractora) y un solo slot
    `fare-congiuntivo-presente` de 6 variantes en `pending`, cuyas 3 primeras keys son la misma forma y
    quedan resueltas por el sujeto explícito; `content/categories.json` tiene 16 entradas; la línea del
    smoke paramétrico está puesta y sus sub-tests editoriales pasan sobre el fichero nuevo;
    `node --test tests/*.test.js` sigue en `fail 0`; y la derivación de `categoriesForDisplay` (entrada
    presente + `totalCount > 0`) está comprobada por dato sin que `src/` haya cambiado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gate del tracer — la categoría se ve, se juega y la homógrafa es resoluble, antes de autorar 24 variantes más (SC-2, SC-5)</name>
  <what-built>
    `content/exercises/fare-congiuntivo.json` con el slot `fare-congiuntivo-presente` (6 variantes, una
    por persona, con la key `faccia` repetida en io, tu y lui/lei), la entrada `fare-congiuntivo` en
    `content/categories.json` (order 16, `origen: "ia-quorum"`) y la línea nueva del smoke paramétrico
    editorial. La suite completa está en `fail 0` y `src/` no se ha tocado: la categoría carga y se
    muestra por el camino genérico.

    Este checkpoint es el gate del tracer. Lo que se comprueba a ojo no es que compile — eso ya lo dicen
    los tests — sino las dos cosas que ningún test puede decidir: que la variante homógrafa sea
    **resoluble** leyendo la frase, y que el disparador excluya el indicativo **sin ambigüedad**. Si eso
    está mal, está mal una vez y no diez.
  </what-built>
  <how-to-verify>
    1. Arrancar el servidor local desde la raíz del repo: `npx serve` (o Live Server) y abrir la URL que
       imprime, normalmente `http://localhost:3000`.
    2. **Home:** en la tabla de categorías debe aparecer una fila nueva al final, con el nombre
       `Fare — congiuntivo (faccia/facessi/abbia fatto)`, estado `no-hecha`, racha `0 / 21 d`, y el
       botón `Examen` de esa fila HABILITADO (no gris, sin tooltip de «No hay ejercicios en esta
       categoría»). Deben seguir apareciendo las 15 filas anteriores sin cambios, incluida
       `Fare — indicativo`, que es una categoría SEPARADA: fallar una no debe marcar la otra.
    3. **Examen de la categoría:** pulsar `Examen` en esa fila. Debe presentar un multiple-choice con 4
       opciones, con el sujeto del hueco visible en el enunciado, y **sin ninguna traducción al español
       de la forma verbal**. Si aparece un paréntesis, su contenido tiene que ser la traducción de una
       CONJUNCIÓN (`aunque`, `siempre que`, `antes de que`, `a pesar de que`), nunca del verbo.
    4. **La prueba de la homógrafa (SC-2, lo más importante de este checkpoint):** entrar y salir del
       Examen hasta ver las variantes de `io`, de `tu` y de `lui/lei` — las tres tienen la misma
       respuesta correcta, `faccia`. En cada una, leer la frase e intentar responderla **tapando
       mentalmente el sujeto**: debe volverse imposible. Con el sujeto a la vista debe haber una sola
       respuesta. Si alguna se puede acertar sin leer el sujeto, o si dos opciones parecen igual de
       válidas, decirlo — es el fallo que esta fase existe para no cometer.
    5. **La prueba del sujeto de la principal (D-42-06):** en las 6 frases, comprobar que el sujeto de
       la oración principal NO es el mismo que el del hueco. Una frase del tipo «Yo pienso que yo
       haga…» está mal construida en italiano y hay que reportarla, aunque suene aceptable en español.
    6. **La prueba del disparador:** en cada frase, comprobar que la opción de indicativo (`faccio`,
       `fai`, `fa`, `fate`, `fanno`) queda descartada por el contexto sin dudar. Si en alguna el
       indicativo suena defendible, el disparador es demasiado débil.
    7. **Rotación de persona:** entrar y salir 4 o 5 veces; el sujeto del enunciado debe cambiar entre
       pasadas — eso es `pickVariantIndex` funcionando sin código nuevo.
    8. **Repaso:** volver a home, entrar en `Repaso` y comprobar que `fare-congiuntivo` aparece como
       opción seleccionable en el picker, separada de `fare-indicativo`.
    9. **Explicación:** dentro del ejercicio, contestar y pulsar `¿Por qué?` (o la tecla `e`). Debe
       leerse en español con acentos, en un solo párrafo, sin markdown ni asteriscos ni acentos
       faltantes; debe citar las formas del paradigma, decir que las tres primeras personas son la misma
       forma, llevar UNA línea sobre el error de «pienso que» + indicativo en español, y **no mencionar
       el imperativo**.
  </how-to-verify>
  <resume-signal>Escribe `aprobado` para desbloquear la autoría del imperfetto y del resto de la fase, o describe qué falla (el prompt concreto, la opción sospechosa, la frase con sujetos coincidentes) para corregir la forma ANTES de replicarla 24 veces.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Expansión — `fare-congiuntivo-imperfetto`, con `facessi` homógrafa en io y tu (6 variantes, CONG-02)</name>

  <files>content/exercises/fare-congiuntivo.json</files>

  <read_first>
    - `content/exercises/fare-congiuntivo.json` tal como quedó tras la Task 1 y las correcciones del
      checkpoint — el `notes` y el slot `fare-congiuntivo-presente` son el molde literal que este slot
      replica: mismo orden de campos, mismos 3 campos por variante, mismo `validation` en `pending`,
      mismo repertorio de disparadores y de objetos literales.
    - `content/exercises/fare-indicativo.json`, slot del imperfetto de indicativo — las 6 formas
      `facevo`, `facevi`, `faceva`, `facevamo`, `facevate`, `facevano` verbatim, que son la familia de
      distractoras de indicativo de este slot, y su explanation como referencia de tono.
    - `content/exercises/fare-indicativo.json`, slot del passato remoto — las 6 formas `feci`,
      `facesti`, `fece`, `facemmo`, `faceste`, `fecero`. **Se lee para NO inventarlas como distractoras
      malformadas:** `facesti`, `facemmo` y `faceste` son formas REALES del indicativo, y `faceste` es
      además la key de `voi` de este slot.
    - `src/data/schema-validator.js` líneas 435-456 — la superficie MC que cada variante debe cumplir.
    - `tests/exercise-types.test.js` línea 1373 — el regex de leak R1 que escanea TODOS los prompts;
      confirma que las 4 glosas de conjunción de D-42-13 no lo disparan, y qué palabras entre paréntesis
      sí lo harían.
    - `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-CONTEXT.md` §decisions —
      D-42-05, D-42-06, D-42-07, D-42-10, D-42-11, D-42-13, D-42-14, D-42-17, D-42-18.
    - `.planning/REQUIREMENTS.md` línea 31 — el texto literal de CONG-02, incluida «con la misma
      exigencia de sujeto explícito en `io`/`tu`».
  </read_first>

  <action>
    Añadir UN slot al array `exercises`, después de `fare-congiuntivo-presente`, con el molde exacto del
    tracer. `validation`: `{ "status": "pending", "passes": [] }`.

    **Slot `fare-congiuntivo-imperfetto` (CONG-02)**

    - `id`: `fare-congiuntivo-imperfetto`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-congiuntivo"]`.
    - Las 6 keys, una por variante y en orden de persona io/tu/lui-lei/noi/voi/loro: `facessi`,
      `facessi`, `facesse`, `facessimo`, `faceste`, `facessero`. **La key se repite en io y tu**
      (D-42-05): son 2 de las 10 variantes homógrafas de la fase, y la unicidad la da el sujeto
      explícito.
    - Disparadores: 6 distintos entre sí (D-42-07) y todos compatibles con el congiuntivo imperfetto, es
      decir con la principal en pasado o en condicional — del tipo `Bisognava che`, `Era necessario che`,
      `Non credevo che`, `Volevo che`, `Benché`, `Nonostante`, `Sarebbe meglio che`, o con sujeto
      explícito distinto (`Il professore voleva che`, `Mia madre sperava che`). **Gate HARD de
      no-correferencia (D-42-06)**: el sujeto de la principal nunca coincide con el del hueco. Si se usa
      `benché` o `nonostante`, va con su gloss léxico del conjunto cerrado (D-42-13), inmediatamente
      detrás de la conjunción y nunca junto a una forma verbal.
    - El objeto literal sale del conjunto cerrado de 7 y es distinto entre las dos variantes que
      comparten la key `facessi`.
    - Distractoras (D-42-10), patrón fijo de 4 opciones sin duplicados: UNA es el **imperfetto de
      indicativo de ESA MISMA persona** (`facevo`, `facevi`, `faceva`, `facevamo`, `facevate`,
      `facevano`) — el error de interferencia real y el contraste modo a modo que este slot examina;
      UNA es una **forma REAL de congiuntivo imperfetto de OTRA persona** del propio paradigma, elegida
      distinta en cada variante para que las dos que comparten key no tengan el mismo conjunto de
      opciones; y UNA es una **raíz regularizada INEXISTENTE**, del tipo de las que salen de conjugar
      `fare` como un verbo regular de primera conjugación o de importar la raíz contracta del infinitivo
      a las desinencias del imperfetto de subjuntivo.
    - **Comprobación obligatoria antes de fijar cada distractora inexistente**, forma a forma: que NO
      sea una forma real del passato remoto de indicativo (`facesti`, `facemmo`, `faceste` lo son y
      `faceste` es además una key de este slot), que no esté en la blacklist del `notes`, que no sea
      palabra italiana homógrafa, y que no difiera de una forma real solo por un acento.
    - `correctIndex`: entero en `[0,4)`, variado entre las 6 variantes.
    - `explanation`: párrafo largo en español acentuado, tono D-127. Debe citar las 5 cadenas distintas
      del paradigma (`facessi`, `facesse`, `facessimo`, `faceste`, `facessero`) señalando que io y tu
      comparten forma y que por eso el sujeto tiene que estar en la frase; explicar que el congiuntivo
      imperfetto se forma sobre la raíz del passato remoto con el infijo de subjuntivo, y que su
      disparador típico es una principal en pasado o una hipótesis; llevar UNA sola línea de
      recordatorio del error de interferencia de D-42-14 sin desarrollarlo; y cerrar nombrando las tres
      trampas de sus distractoras. **No menciona el imperativo** (D-42-15) ni referencia ningún
      ejercicio por id (sub-test R2).

    Reglas duras comunes, idénticas a las del tracer: hueco literal `___`, pronombre sujeto explícito de
    la persona de la variante, cero español en el prompt fuera de las 4 glosas de conjunción admitidas,
    cero corchetes angulares, cero backticks, cero comillas tipográficas, apóstrofes ASCII, y ninguna
    forma del condizionale ni del imperativo de `fare` en `options`.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 2`; los ids son exactamente `fare-congiuntivo-presente` y `fare-congiuntivo-imperfetto` en ese orden; los 2 tienen `type === 'multiple-choice'`, `categoryIds` de longitud 1 con valor `fare-congiuntivo`, y `variants.length === 6`; total 12 variantes.
    - `node -e`: el array de keys (`options[correctIndex]`) de `fare-congiuntivo-imperfetto` es exactamente `["facessi","facessi","facesse","facessimo","faceste","facessero"]` en ese orden (CONG-02, D-42-05).
    - `node -e` sobre las 6 variantes del imperfetto: en cada una, exactamente 1 opción pertenece al conjunto `["facevo","facevi","faceva","facevamo","facevate","facevano"]` y es la del **índice de persona de esa variante** — la distractora de indicativo es específica de la persona (D-42-10).
    - `node -e` sobre las 6 variantes del imperfetto: en cada una, exactamente 1 opción pertenece al conjunto de las 5 cadenas del congiuntivo imperfetto distinta de la key; y exactamente 1 no pertenece a la unión de las formas reales de `fare` (la regularizada inexistente) (D-42-10).
    - `node -e`: los conjuntos de `options` de las dos variantes cuya key es `facessi` son distintos entre sí, y sus objetos literales también (D-42-07).
    - `node -e` sobre `options[]` de los 2 slots, coincidencia **EXACTA**: **0** coincidencias con el conjunto heredado de 18 formas atestiguadas, **0** con `["facci","facciam","facce"]`, **0** con `["farei","faresti","farebbe","faremmo","fareste","farebbero","fa'"]`; ninguna opción de ningún slot contiene la subcadena `fatt` (los compuestos son de 42-02) (D-42-11).
    - Escaneo `node -e` sobre los 12 `prompt`: los 12 contienen `___`; los 12 contienen el pronombre sujeto de su persona como palabra suelta; los 6 disparadores de cada slot son distintos entre sí; **0** contienen `espa`; todo contenido entre paréntesis pertenece al conjunto cerrado de 4 glosas y va inmediatamente detrás de una de las conjunciones glosadas (D-42-06, D-42-13, D-42-18).
    - Escaneo `node -e` sobre `prompt` y `options[]` de los 2 slots: cada prompt contiene al menos un objeto del conjunto cerrado de 7; **0** coincidencias con `["colazione","spesa","freddo","farcel","far fare"]` (D-42-18).
    - Escaneo `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:`, comillas tipográficas y backticks; ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`.
    - `node -e` sobre la `explanation` del imperfetto: contiene al menos un carácter acentuado del español; contiene las 5 cadenas del paradigma; longitud `> 400`; **no** contiene `imperativo`; no coincide con el patrón de cross-ref R2.
    - `node -e`: los 2 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`; `correctIndex` no es el mismo valor en las 6 variantes de ningún slot.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`; el `expected` del smoke es dinámico, así que el conteo sigue solo.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 2 slots con `pending`.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ content/categories.json tests/` no produce ninguna línea de salida — esta tarea toca UN solo fichero.
  </acceptance_criteria>

  <done>
    El fichero tiene 2 slots y 12 variantes en `pending`; las keys del imperfetto son las 6 formas del
    congiuntivo imperfetto una por persona, con `facessi` repetida en io y tu y resuelta por el sujeto
    explícito; las 5 variantes homógrafas de este plan (`faccia` x3, `facessi` x2) cumplen el gate de
    no-correferencia y tienen una sola opción defendible; y la suite completa sigue en `fail 0` con el
    motor sin tocar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Honestamente acotadas: esto es una app web estática de un solo usuario, sin servidor, sin
autenticación, sin red en runtime y sin sincronización (CLAUDE.md §Constraints). El único dato nuevo
que entra es un fichero JSON escrito a mano por el propio autor. La superficie real de una fase de
**autoría de contenido** aquí es la **integridad de datos**, no el ataque.

| Boundary | Description |
|----------|-------------|
| `content/exercises/*.json` en disco → `fetch()` en boot → `JSON.parse` en `src/data/content-loader.js` | Contenido semi-confiable: lo escribe el autor (o una autoría asistida), no un tercero, pero pasa por `JSON.parse` y de ahí al estado de la app sin ninguna otra puerta. Un JSON malformado rompe `loadContent()` en boot y la app no arranca. |
| strings de contenido (`prompt`, `options[]`, `explanation`, `notes`) → render en el DOM por Alpine | La invariante anti-XSS del proyecto (T-02-01): el contenido se renderiza SIEMPRE como texto, nunca como HTML. Si un string llevara marcado, o el render cambiara a HTML, el contenido pasaría a ser código. |
| `content/categories.json` → `src/data/schema-validator.js` (`knownCategoryIds`) → `categoriesForDisplay` | Registro: una entrada mal formada rompe la validación del bundle completo, y una entrada ausente deja el fichero de contenido en estado inválido en disco. |
| slug del fichero y prefijo de los ids de slot → `RESET_PREFIXES_V13` (`src/data/storage.js:1345`) → `exerciseStats` del autor | Integridad del progreso: si el slug no coincide byte a byte con el elemento que Phase 40 ya dejó en `RESET_PREFIXES_V13`, el reset selectivo no alcanza la categoría y el progreso del autor queda huérfano en silencio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Tampering | strings de `content/exercises/fare-congiuntivo.json` renderizados en el DOM | medium | mitigate | Se preserva la invariante de render como texto (T-02-01): esta fase NO añade ni cambia una sola línea de render, y el contenido queda libre de marcado. Doble verificación automatizada: el sub-test `plain text (no markdown)` de `tests/exercise-types.test.js:1349-1363`, activado sobre el fichero nuevo por la línea de `CATEGORIES_WITH_EXPLANATIONS` de la Task 1; más un escaneo `node -e` en las dos tareas de contenido que exige 0 apariciones de los caracteres de corchete angular, de `&#`, del esquema `javascript:` y de backticks en TODOS los strings del fichero, incluido `notes`. El `notes` se redacta sin corchetes angulares a propósito, para que el escaneo no necesite excepciones. |
| T-42-02 | Tampering | `JSON.parse` del fichero de contenido en `content-loader.js` | medium | mitigate | Prototype-pollution por contenido: `JSON.parse` crea `__proto__` como own-property si el literal la declara. Mitigación de esta fase: el fichero declara ÚNICAMENTE el key set del schema (top-level `notes` y `exercises`; por slot `id`, `type`, `categoryIds`, `explanation`, `variants`, `validation`; por variante `prompt`, `options`, `correctIndex`), y cada tarea corre un escaneo recursivo `node -e` que exige que ninguna clave propia del objeto parseado se llame `__proto__`, `constructor` ni `prototype`, más `({}).polluted === undefined` tras el parse. El deep-clone defensivo del state vive en la cadena de migración y no se toca. |
| T-42-03 | Repudiation | `validation.passes[]` como audit trail del quórum | medium | mitigate | Un `status: "validated"` escrito a mano, o un pase con `by`/`date` inventados, forja la única evidencia que el autor tiene de que una variante fue revisada, y la falsificación es indetectable a ojo. Mitigación: el executor deja los slots en `status: "pending"` con `passes: []` y NO fabrica pases (criterio de aceptación en las dos tareas de contenido); el quórum real corre en la pasada top-level de D-42-04, un ejercicio por contexto (VAL-03); y el gate permanente de 42-02 exige `status === deriveStatus(passes)` (`src/data/validation-state.js:32-43`) más la ronda EXTRA `deepseek` condicionada en los 4 slots del paradigma — un `validated` con menos de 2 pases `correcta` de `by` distintos, con algún `incorrecta`, o sin la ronda extra, pone la suite roja. |
| T-42-04 | Tampering | entrada de `content/categories.json` | medium | mitigate | Es prerequisito de schema, no cosmética: sin ella, `schema-validator.js:133-146` emite `referencia a categoría desconocida` por cada slot y `tests/domain.test.js` se pone rojo, es decir el fichero de contenido no puede estar en disco solo. Un `origen` con typo o una clave extra hacen `ok:false` en el validador de procedencia (`tests/schema-validator-origen.test.js`). Verificado por criterio de aceptación: la entrada tiene exactamente las 4 claves `id`, `name`, `order`, `origen`, es la 16ª, `order: 16`, `origen: "ia-quorum"`, y la suite queda en `fail 0`. |
| T-42-05 | Tampering | coincidencia del slug con `RESET_PREFIXES_V13` | medium | mitigate | Si el slug del fichero, el `id` de `categories.json` o el prefijo de los ids de slot no son byte a byte `fare-congiuntivo`, el reset selectivo de v13 no alcanza la categoría y el progreso real del autor queda huérfano sin ningún error visible — el fallo silencioso más caro de esta fase. Mitigación: `src/data/storage.js:1345` ya contiene el string exacto (Phase 40 shippeada) y esta fase NO lo edita; el criterio de aceptación fija los 5 ids de slot literalmente y verifica el `id` de la entrada; y la prohibición de renombrar ids después del alta queda escrita en `must_haves.prohibitions`. |
| T-42-06 | Denial of Service | tamaño del bundle de contenido cargado en boot | low | accept | 30 variantes son del orden de 400 líneas de JSON; el loader ya trae 15 ficheros con 233 slots. El coste de `JSON.parse` a esta escala es despreciable y la app no tiene atacante remoto: sin servidor, sin red, sin multiusuario. |
| T-42-07 | Information Disclosure | el contenido del fichero | low | accept | Son conjugaciones del subjuntivo de un verbo italiano y explicaciones gramaticales. Cero PII, cero credenciales, cero secretos. El fichero no sale de la máquina del autor. |
| T-42-08 | Spoofing | — | n/a | n/a — no aplica | No existen identidades, sesiones, tokens ni cuentas en esta app: es local, monousuario y sin autenticación. No hay superficie de suplantación que evaluar en L1. |
| T-42-09 | Elevation of Privilege | — | n/a | n/a — no aplica | No hay roles, permisos ni frontera de privilegio: un único usuario con acceso total a su propio navegador y su propio disco. |
| T-42-SC | Tampering | instalaciones de npm / pip / cargo | n/a | n/a — gate no disparado | Esta fase no ejecuta ninguna instalación de gestor de paquetes ni añade dependencias: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node. El Package Legitimacy Gate no aplica y no se requiere tabla de auditoría. |

**ASVS L1, block on high.** No hay ninguna amenaza `high` en este plan, y no se inventa ninguna para
rellenar la tabla: en una app local sin servidor, sin red en runtime y sin entrada de terceros, la
superficie real de una fase de contenido son la invariante de render como texto, la higiene del key set
del JSON parseado, la integridad del audit trail de validación, el registro de la categoría como
prerequisito de schema y la coincidencia byte a byte del slug con el contrato de reset. Las cinco están
en `mitigate` con verificación automatizada. Las dos `accept` son `low` y su razón es estructural.

### Candidatos de prohibición canon descartados (breadcrumbs)

- **prototype-pollution** — canon de seguridad del proyecto; vive en `<threat_model>` T-42-02 y en
  `/gsd-secure-phase`, no se acuña como prohibición.
- **XSS / inyección de HTML** — canon; vive en T-42-01 (invariante de render como texto, T-02-01), no
  se acuña.
- **path-traversal** — sin acceso a sistema de ficheros en runtime: el contenido se sirve por HTTP
  local y el runtime es el navegador. Descartado.
- **GDPR / privacidad de datos personales** — el contenido son conjugaciones y el state es el progreso
  de estudio del propio autor en su máquina. Descartado.
- **fairness / sesgo genérico** — sin decisión automatizada sobre personas. Lo que SÍ se acuña son las
  variantes bespoke y específicas de esta fase, que ninguna herramienta canon detectaría: el ejercicio
  con dos respuestas defendibles, el examen de vocabulario no enseñado, y la forma regional presentada
  como norma.
</threat_model>

<verification>
Comprobaciones al terminar las 3 tareas de este plan:

1. `node --test tests/*.test.js` → código de salida 0, `fail 0`, `pass` mayor que el baseline
   re-medido en la precondición de Task 1 (766).
2. `node -e` sobre `content/exercises/fare-congiuntivo.json`: 2 slots, ids exactamente
   `fare-congiuntivo-presente` y `fare-congiuntivo-imperfetto`, 6 variantes cada uno, 12 en total, los
   2 en `type: "multiple-choice"` con `options` de 4 y `categoryIds` de 1.
3. `node -e` sobre `content/categories.json`: 16 entradas, la 16ª es `fare-congiuntivo` con `order: 16`
   y `origen: "ia-quorum"`, y exactamente las 4 claves del schema.
4. Los escaneos de ausencia, **todos por campo y por coincidencia exacta**, nunca sobre el fichero
   completo (el `notes` nombra a propósito las formas y las perífrasis excluidas, así que un grep de
   fichero completo se auto-invalidaría, y `face` es prefijo de tres formas legítimas aquí): blacklist
   heredada en `options`, trampas nuevas en `options`, casillas de Phase 43 en `options`, compuestos en
   `options`, perífrasis en `prompt` y `options`, español y paréntesis no autorizados en `prompt`.
5. Los escaneos en positivo: cada prompt con un objeto del conjunto cerrado, cada prompt con el
   pronombre de su persona, `notes` con las 12 declaraciones y con cada forma de la blacklist entre
   apóstrofes ASCII, cada explanation acentuada y sin mención del imperativo.
6. `VAL_07_STRICT=1 node --test tests/*.test.js` → falla nombrando los 2 slots `pending`. Ese rojo es
   el estado CORRECTO al cerrar este plan; lo cierra el pase top-level de quórum de §Hand-off.
7. `node scripts/run-validation-271.mjs` → sigue reportando su total anterior en PASS. Es la ceguera
   documentada en §Correcciones 5, no un fallo: el reporter no verá la categoría hasta Phase 44 / INT-02.
8. `git diff --quiet src/screens/app.js src/domain/ src/data/` → código de salida 0 (SC-5, Out of Scope
   del milestone).
9. `git status --porcelain` → exactamente los 3 ficheros de `files_modified`.
</verification>

<success_criteria>
- CONG-01: el autor es examinado sobre `faccia / faccia / faccia / facciamo / facciate / facciano`, con
  las tres primeras personas homógrafas y el sujeto explícito en la frase como único desambiguador, de
  modo que hay una sola respuesta correcta.
- CONG-02: el autor es examinado sobre `facessi / facessi / facesse / facessimo / faceste / facessero`,
  con la misma exigencia de sujeto explícito en io y tu.
- SC-1 (parte simple): los dos tiempos simples con las 6 personas enumeradas, una persona distinta por
  pasada vía `pickVariantIndex`, y distractoras que materializan los tres errores reales — el
  indicativo de la misma persona, la forma real de otra persona y la raíz regularizada inexistente.
- SC-2 (parte de este plan): 5 de las 10 variantes homógrafas de la fase quedan resueltas por sujeto
  explícito, con el gate de no-correferencia declarado en `notes` antes de escribirlas y comprobado a
  ojo en el checkpoint. Las otras 5 y la ronda EXTRA obligatoria son de 42-02 y de §Hand-off.
- SC-5 (parte de este plan): la categoría carga en boot y aparece en home, picker, Repaso y Examen como
  unidad de reset independiente de las otras 3 de `fare`, sin una línea de motor nueva — verificado por
  dato en Task 1 y a ojo en el checkpoint de Task 2.
- Estado de hand-off correcto: los 2 slots en `pending` con `passes: []`, y el rojo de
  `VAL_07_STRICT=1` como marcador visible de que el quórum base todavía no ha corrido.
</success_criteria>

<output>
Crear `.planning/phases/42-fare-congiuntivo-4-slots-hom-grafas-disparador/42-01-SUMMARY.md` al terminar.
</output>
