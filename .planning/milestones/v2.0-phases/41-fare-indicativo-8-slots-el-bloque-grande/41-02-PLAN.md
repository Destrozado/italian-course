---
phase: 41-fare-indicativo-8-slots-el-bloque-grande
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - content/exercises/fare-indicativo.json
  - tests/content-fare-indicativo.test.js
autonomous: true
requirements: [IND-05, IND-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- SC-2 / IND-05 : los 3 compuestos vivos ---
    - "El autor es examinado sobre los 3 compuestos vivos con `avere` + `fatto`: `ho fatto / hai fatto / ha fatto / abbiamo fatto / avete fatto / hanno fatto`, `avevo fatto / avevi fatto / aveva fatto / avevamo fatto / avevate fatto / avevano fatto` y `avrò fatto / avrai fatto / avrà fatto / avremo fatto / avrete fatto / avranno fatto` — una forma por variante, 6 variantes por slot (IND-05, SC-2)."
    - "En los 4 slots compuestos las 2 distractoras del auxiliar están en la MISMA persona que la key, así que lo único que distingue key de distractora es el TIEMPO DEL AUXILIAR: el ejercicio es exactamente elegir el tiempo del auxiliar según el marco temporal de la frase (D-41-10, IND-05, SC-2)."
    - "El participio es `fatto` invariable en las 24 variantes compuestas: ningún `options[]` contiene `fatta`, `fatti` ni `fatte` — la concordancia es el MAGNET de Phase 43 (INDEF-04) y no se adelanta aquí (D-41-10)."
    - "Ninguna opción de un slot compuesto es el tiempo simple correspondiente: cada opción es de dos palabras y la segunda es `fatto` o `fare`, invariante que excluye por construcción tanto la distractora de tiempo simple que D-41-10 rechazó explícitamente como el participio concordado (D-41-10)."
    # --- SC-3 / IND-06 : el trapassato remoto ---
    - "Las 6 variantes de `fare-indicativo-trapassato-remoto` van SIEMPRE dentro de una subordinada temporal con la principal en passato remoto: ninguna presenta la forma en frase suelta, y el verbo de la principal está en passato remoto y no es una forma de `fare` (D-41-03, IND-06, SC-3)."
    - "Los 3 conectores se reparten 2+2+2 entre las 6 variantes: `dopo che` en 2, `quando` en 2 y `appena` en 2, cada una con una persona distinta (D-41-03)."
    - "La explanation de `fare-indicativo-trapassato-remoto` dice EXPLÍCITAMENTE que fuera de ese marco la forma no se usa, y lo dice en una frase localizable, no como insinuación (D-41-04, SC-3)."
    - "Las 2 variantes con `quando` tienen lectura única por dos mecanismos verificables a la vez: la principal en passato remoto excluye las distractoras de auxiliar en presente y en futuro, y ninguna opción del slot lleva el auxiliar en imperfetto (`avevo fatto` y familia) ni es una forma simple, así que las lecturas alternativas que `quando` admitiría no están en la lista de opciones (D-41-03, SC-2; edge: single-reading boundary)."
    # --- D-41-01 / D-41-02 : el eje de variante, completo a 48 ---
    - "El fichero tiene EXACTAMENTE 8 slots y 48 variantes: 6 por slot, ni 5 ni 7 en ninguno, y las 6 de cada slot cubren las 6 personas sin repetir — 48 casillas persona×tiempo, ninguna sin examinar (D-41-01; edge: count boundary)."
    - "Cada una de las 48 variantes lleva su propio marco temporal y los 6 marcos de un mismo slot son distintos entre sí; en los 4 compuestos el marco es lo que fija el tiempo del auxiliar (D-41-02, SC-2)."
    - statement: "Ninguna de las 48 variantes admite una segunda lectura del marco temporal que hiciera correcta una forma de otro tiempo del paradigma de `fare`."
      verification: backstop
    # --- D-41-05 / D-41-06 / D-41-07 : los gates, sobre las 48 ---
    - "Ninguno de los 48 `prompt` contiene paréntesis `(` ni `)` ni la subcadena `espa` en ninguna capitalización: 0-gloss sobre toda la categoría, no solo sobre los simples (D-41-05)."
    - "Un escaneo sobre los 48 `prompt` y sus `options[]` devuelve 0 coincidencias para `colazione`, `spesa`, `freddo` y `farcel`, y el objeto de `fare` sale siempre del conjunto cerrado `i compiti`, `un errore`, `il lavoro`, `una torta`, `il letto`, `tutto`, `una foto` (D-41-06)."
    - "Los 48 `prompt` contienen el hueco literal `___` y un pronombre sujeto explícito de los 6, coherente con la persona de la forma correcta (D-41-07)."
    # --- D-41-08 : blacklist, sobre las 48 ---
    - "Un escaneo sobre los `options[]` de los 8 slots devuelve 0 coincidencias exactas con `fo`, `fé`, `fenno`, `facea` y `fan`, y 0 con las formas atestiguadas de otros modos de `fare` (`faccia`, `facciate`, `facciano`, `facessi`, `facesse`, `farei`, `farebbe`) ni con palabras italianas homógrafas (`facci`, `facce`, `fava`, `faro`) (D-41-08; edge: blacklist absence)."
    # --- D-41-11 : marcos disjuntos, en las DOS direcciones ---
    - "Los marcos de `fare-indicativo-passato-remoto` y de `fare-indicativo-passato-prossimo` son disjuntos en ambas direcciones: 0 marcos recientes (`stamattina`, `ieri`, `questa settimana`, `oggi`, `poco fa`, `questo mese`) en el slot remoto y 0 marcos remotos (`Nel 19`, `Quell'`, `anni fa`) en el slot reciente; y ninguno de los dos ofrece al otro como distractora — el slot remoto no tiene ninguna opción con `fatto` y el slot reciente ninguna con una forma simple de passato remoto (D-41-11; edge: disjoint-frame boundary, dos direcciones)."
    # --- D-41-12 / D-41-15 / SC-4 : quórum y hand-off ---
    - "Al cerrar este plan los 8 slots llevan `validation` con `status: \"pending\"` y `passes: []`: el executor no fabrica pases, porque el quórum base Opus+Sonnet no está disponible dentro de un subagent de ejecución (D-41-15, VAL-03)."
    - "`tests/content-fare-indicativo.test.js` importa `deriveStatus` de `src/data/validation-state.js` y exige `validation.status === deriveStatus(validation.passes)` en los 8 slots: un `validated` escrito a mano con `passes` incoherente pone la suite roja, así que el audit trail no se puede forjar en silencio (D-41-17, T-41-03)."
    - "El mismo test exige que, cuando `fare-indicativo-passato-remoto` o `fare-indicativo-trapassato-remoto` estén en `validated`, al menos uno de sus `by` case el prefijo `deepseek` — la ronda EXTRA de D-41-12 queda mecánicamente exigida sobre esos 12 variantes (D-41-12, SC-4)."
    - "Tras la pasada TOP-LEVEL de quórum los 8 slots quedan `validation.status: \"validated\"` con ≥2 pases `correcta` de `by` distintos y cero `incorrecta`, y `VAL_07_STRICT=1 node --test tests/*.test.js` termina en verde (SC-4). Mientras eso no ocurra, ese comando falla nombrando los slots `pending`, y ese rojo es el marcador honesto del hand-off — nunca un `validated` fabricado (D-41-15)."
    # --- D-41-13 / D-41-14 ---
    - "Los 8 slots tienen `type: \"multiple-choice\"` con `options` de longitud 4; el fichero no contiene ningún slot `match` ni `word-buttons`, y el `notes` declara ambas ausencias como decisiones razonadas (D-41-13)."
    - "Los ids de los 8 slots son literalmente `fare-indicativo-presente`, `fare-indicativo-imperfetto`, `fare-indicativo-passato-remoto`, `fare-indicativo-futuro-semplice`, `fare-indicativo-passato-prossimo`, `fare-indicativo-trapassato-prossimo`, `fare-indicativo-trapassato-remoto` y `fare-indicativo-futuro-anteriore`, y ningún id del fichero lleva sufijo numérico de 3 cifras: `fare-indicativo-300`+ queda libre para Phase 44 / INT-03 (D-41-14; edge: id-space boundary)."
    # --- D-41-17 / SC-4 : canon editorial ---
    - "Las 8 explanations están en español con acentos RAE (cada una contiene al menos un carácter acentuado), en plain text de un solo párrafo, con apóstrofes ASCII U+0027, sin markdown y sin comillas tipográficas; los 5 sub-tests editoriales de `CATEGORIES_WITH_EXPLANATIONS` pasan sobre el fichero (D-41-17, SC-4)."
    - "Ningún string del fichero (`notes`, `explanation`, `prompt`, `options`) contiene los caracteres `<` o `>`, ni `&#`, ni `javascript:`, y ninguna clave propia del objeto parseado se llama `__proto__`, `constructor` ni `prototype` (T-41-01, T-41-02)."
    # --- SC-5 / edge: schema boundary ---
    - "`node --test tests/*.test.js` termina con `fail 0` en cada commit de este plan: `tests/domain.test.js:322-341` valida el bundle auto-descubierto y exige `result.errors` vacío, así que cualquier violación del schema o del registro sale roja de inmediato (edge: schema boundary)."
    - "`git diff --stat src/screens/app.js src/domain/ src/data/` no produce salida al cierre de la fase: los 8 slots cargan y se muestran por el camino genérico, sin una línea de motor nueva (SC-5)."

  artifacts:
    - "`content/exercises/fare-indicativo.json` — completado a 8 slots y 48 variantes: los 4 compuestos añadidos con `validation` en `pending`."
    - "`tests/content-fare-indicativo.test.js` — fichero de test NUEVO con los invariantes permanentes de la categoría: conteos, paradigma de las 48 keys, eje de persona, 0-gloss, SCOPE-GATE léxico, blacklist, patrón de distractoras por familia de slot, marcos disjuntos en las dos direcciones, marco del trapassato remoto con reparto 2+2+2, espacio de ids `-300`+ libre, canon editorial, higiene del key set del JSON, y coherencia de `validation` con `deriveStatus` incluida la ronda extra de D-41-12."

  key_links:
    - "`variants[].options[]` de un slot compuesto → la invariante de dos palabras con `fatto` o `fare` como segunda: es lo que excluye simultáneamente la distractora de tiempo simple (rechazada por D-41-10) y el participio concordado (MAGNET de Phase 43). Si se relaja, las dos prohibiciones se caen a la vez."
    - "`validation.passes[]` → `deriveStatus` (`src/data/validation-state.js:31-42`) → el assert de `tests/content-fare-indicativo.test.js`: es el único punto que impide que un `status: \"validated\"` sin pases reales pase por bueno. `deriveStatus([])` devuelve `pending`, así que el assert de igualdad es verde también antes del quórum."
    - "El conector del prompt de `fare-indicativo-trapassato-remoto` → la unicidad de lectura: `dopo che` y `appena` fuerzan la anterioridad; `quando` no, y su lectura única depende de que la principal esté en passato remoto Y de que el auxiliar en imperfetto no esté entre las opciones. Los dos mecanismos son necesarios; quitar uno abre la doble validez."
    - "`slotCountOf` (`tests/exercise-types.test.js:1268-1271`) → el `expected` de la línea de `fare-indicativo`: al ser dinámico, pasar de 4 a 8 slots no requiere tocar ese array. Ninguno de los otros 2 arrays de conteo se toca (Phase 44 / INT-02)."

  prohibitions:
    - "MUST NOT dar de alta una variante en la que más de una opción sea una respuesta defendiblemente correcta para ese prompt: un ejercicio injusto le enseña al autor que su respuesta correcta es incorrecta, que es el daño exacto que esta herramienta existe para evitar."
    - "MUST NOT escribir `validation.status: \"validated\"` ni ninguna entrada de `passes[]` que no venga de un pase de modelo real: el audit trail es la única evidencia que el autor tiene de que una variante fue revisada, y un pase inventado la destruye sin dejar rastro."
    - "MUST NOT presentar el trapassato remoto — ni ninguna otra forma de baja frecuencia — como si fuera italiano hablado corriente: la explanation tiene que ser honesta sobre el registro, porque el autor no tiene otra fuente para calibrarlo y estudiaría de más una forma que casi no va a oír."
    - "MUST NOT presentar una forma regional, dialectal o de registro literario como si fuera la norma del italiano estándar, ni en un sentido ni en el otro: el autor no puede distinguirlas y la interiorizaría como regla."
    - "MUST NOT introducir ninguna dependencia, `package.json`, paso de build ni proceso que haya que arrancar: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node (CLAUDE.md §Constraints)."
    - "MUST NOT tocar el motor v1.4 (`src/screens/app.js`, `src/domain/`, `src/data/`): si el contenido exigiera cambiar `session.js`, `applyResultToSession` o la cascada, el diseño del contenido está mal (Out of Scope literal del milestone)."
    - "MUST NOT autorar cruces multi-categoría ni usar el espacio de ids `fare-indicativo-300`+: son de Phase 44 / INT-03."
    - "MUST NOT sincronizar `CATEGORIES` de `scripts/run-validation-271.mjs` ni `REAL_CATEGORIES` de `tests/fixtures/slot-variants-integration.test.js`, ni la fórmula del baseline-guard: son el entregable literal de Phase 44 / INT-02."
    - "MUST NOT arreglar de paso ninguna discrepancia de conteo preexistente de `genero-numero` ni de `preposiciones` (D-35-08 / D-40-12)."
    - "MUST NOT añadir un eje de variante nuevo (variar por tiempo dentro de un slot): el eje es uno solo, el índice de variante, que aquí significa la persona (Out of Scope literal del milestone)."
---

<objective>
Completar la categoría `fare-indicativo` con sus **4 tiempos compuestos** — passato prossimo,
trapassato prossimo, futuro anteriore y trapassato remoto — como 4 slots `multiple-choice` de 6
variantes cada uno (24 variantes más, hasta 48), y dejar los invariantes de toda la categoría
convertidos en un gate permanente de la suite.

Cubre IND-05 (los 3 compuestos vivos con `avere` + `fatto`, eligiendo el TIEMPO DEL AUXILIAR según el
marco) e IND-06 (el trapassato remoto DENTRO de su único marco vivo: subordinada temporal con
`dopo che` / `quando` / `appena` y la principal en passato remoto).

Purpose: los compuestos son donde el eje del ejercicio deja de ser la forma y pasa a ser el marco —
`fatto` no cambia nunca, así que lo único que el autor tiene que decidir es en qué tiempo va `avere`.
Y el trapassato remoto es el único requisito SINTÁCTICO de la fase: sin su marco la frase es
artificial, y SC-3 exige que la explanation lo diga con esas palabras.

El tercer entregable es el que hace que nada de esto se pueda deshacer por inercia: un fichero de
test que exige el 0-gloss, la ausencia de la blacklist, los marcos disjuntos y la coherencia del
audit trail de validación en las 48 variantes, para siempre.

Output: `content/exercises/fare-indicativo.json` con 8 slots y 48 variantes en
`validation.status: "pending"`, más `tests/content-fare-indicativo.test.js`. Ningún fichero de `src/`
cambia y ningún array de conteo diferido se toca.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md
@.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-PATTERNS.md
@.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-01-SUMMARY.md
</context>

## Frontera de este plan (y de la fase)

Idéntica a la de 41-01, repetida para que la verificación de este plan no la busque fuera:

- **Cruces multi-categoría** (`fare-indicativo-300`+, hacia `avere` / `modali` /
  `presente-regolare`) → Phase 44, INT-03. El espacio de ids `-300`+ se deja libre; no se pre-crea ni
  se reserva con placeholder.
- **Sync de counts**: `CATEGORIES` de `scripts/run-validation-271.mjs:174-189`, `REAL_CATEGORIES` de
  `tests/fixtures/slot-variants-integration.test.js:167-186` y la fórmula del baseline-guard → Phase 44,
  INT-02. La línea de `CATEGORIES_WITH_EXPLANATIONS` ya se puso en 41-01 y su `expected` es dinámico,
  así que pasar de 4 a 8 slots no la toca.
- **El motor v1.4.** `git diff src/screens/app.js src/domain/ src/data/` queda vacío al cierre.
- **Un eje de variante nuevo** (variar por tiempo dentro de un slot) — Out of Scope literal.
- **Perífrasis y modismos de `fare`** — Out of Scope; D-41-06 es el gate de autoría.
- **La concordancia de `fatto`** (`li ho fatti`) — es el MAGNET de Phase 43 / INDEF-04. Aquí `fatto` es
  invariable en las 24 variantes compuestas y no se ofrece concordado ni como distractora.
- **El contraste de uso passato remoto frente a passato prossimo** — rechazado por diseño en D-41-11: es
  registro y variación regional, no una casilla del paradigma. Si el autor lo echa en falta, es un slot
  nuevo, no un rediseño.
- **Las discrepancias de conteo VAL-06 preexistentes** de `genero-numero` y `preposiciones` — fuera de
  scope desde v1.9 (D-35-08 / D-40-12).
- **El quórum base canónico Opus+Sonnet** — no corre dentro del executor (D-41-15). Ver §Hand-off.

## Correcciones de plan-time heredadas de 41-01

Registradas en `41-01-PLAN.md` §Correcciones y verificadas en el codebase el 2026-08-03. Resumen de
las dos que afectan a este plan:

1. **La entrada de `content/categories.json` y la línea de `CATEGORIES_WITH_EXPLANATIONS` se hicieron en
   41-01, no aquí.** D-41-15 las había puesto entre paréntesis en 41-02, pero
   `src/data/schema-validator.js:133-141` exige que `categoryIds` referencie una categoría conocida y
   `tests/domain.test.js:322-341` valida el bundle auto-descubierto exigiendo `errors` vacío: el
   fichero de contenido no puede existir en disco sin su entrada. Este plan **no las vuelve a tocar**.
2. **El "rojo esperado" de los counts no es el del reporter.** Verificado ejecutando
   `node scripts/run-validation-271.mjs`: los dos lados del baseline-guard iteran el mismo array
   `CATEGORIES`, así que omitir `fare-indicativo` no dispara `process.exit(1)` ni pone rojo VAL-06 —
   el reporter queda **CIEGO** al fichero y sigue en PASS. El marcador honesto de esta fase es
   **`VAL_07_STRICT=1 node --test tests/*.test.js`** (`tests/exercise-types.test.js:1432-1448`).

Y una que se materializa en la Task 2 de este plan:

3. **D-41-04 dice "las 6 explanations"; el schema tiene UNA explanation por slot.**
   `src/data/schema-validator.js:211-214` la exige a nivel de slot y las variantes no llevan
   explanation propia (comentario 190-193). La exigencia se cumple, por tanto, con la única explanation
   de `fare-indicativo-trapassato-remoto`, que cubre sus 6 variantes y tiene que decir explícitamente
   que fuera del marco la forma no se usa. Además, para que la exigencia no se quede en la prosa, las
   6 variantes llevan el marco EN el prompt y el gate de la Task 3 lo verifica variante a variante.

## Artifacts this phase produces

Ver la tabla completa en `41-01-PLAN.md` §Artifacts this phase produces. Lo que añade este plan:

| Símbolo / identificador | Fichero | Tipo |
|---|---|---|
| `fare-indicativo-passato-prossimo` | `content/exercises/fare-indicativo.json` | id de slot MC, 6 variantes |
| `fare-indicativo-trapassato-prossimo` | idem | id de slot MC, 6 variantes |
| `fare-indicativo-futuro-anteriore` | idem | id de slot MC, 6 variantes |
| `fare-indicativo-trapassato-remoto` | idem | id de slot MC, 6 variantes |
| `tests/content-fare-indicativo.test.js` | (nuevo) | fichero de test de invariantes de la categoría |
| `CANON` | `tests/content-fare-indicativo.test.js` | constante de test: el mapa de los 8 slots a sus 6 formas |
| `SIMPLE_SLOTS` / `COMPOUND_SLOTS` | idem | constantes de test: los dos grupos con su patrón de distractoras |
| `BLACKLIST` / `OTHER_MOODS` / `PERIPHRASIS` | idem | constantes de test: los conjuntos de ausencia |
| `REMOTE_FRAMES` / `RECENT_FRAMES` | idem | constantes de test: los dos repertorios disjuntos de D-41-11 |

Espacio de ids reservado y **no usado** por esta fase: `fare-indicativo-300` y siguientes (Phase 44,
INT-03).

## Multi-source coverage audit

Auditoría completa de las 4 fuentes; la tabla es la misma en los dos plans y se repite aquí para que
cada PLAN.md sea auto-contenido.

| Fuente | Item | Plan | Estado |
|--------|------|------|--------|
| GOAL | ROADMAP §Phase 41 Goal — indicativo completo como UNA categoría en slot+variantes, persona como eje | 41-01 (simples) + 41-02 (compuestos) | COVERED |
| GOAL | SC-1 — 4 tiempos simples, persona distinta cada pasada, distractoras del error real | 41-01 T1, T3, T4 | COVERED |
| GOAL | SC-2 — 3 compuestos con `avere`, tiempo del auxiliar por el marco, sin doble lectura | 41-02 T1 + T3 (gate) | COVERED |
| GOAL | SC-3 — trapassato remoto siempre en su marco, explanation que dice que fuera no se usa | 41-02 T2 + T3 (gate) | COVERED |
| GOAL | SC-4 — todas `validated` por quórum + canon editorial + sin leak R1 | 41-02 T3 (coherencia de `deriveStatus` + ronda extra) + pase top-level | COVERED |
| GOAL | SC-5 — carga en boot y aparece en home/picker/Repaso/Examen sin línea de motor | 41-01 T1, T2 + 41-02 T3 (assert de la entrada y de `totalCount`) | COVERED |
| REQ | IND-01 presente (`facc-`/`fa-`) | 41-01 T1 | COVERED |
| REQ | IND-02 imperfetto (`fac-`) | 41-01 T3 | COVERED |
| REQ | IND-03 passato remoto (`fec-`/`fac-`) | 41-01 T4 | COVERED |
| REQ | IND-04 futuro semplice (`far-`) | 41-01 T3 | COVERED |
| REQ | IND-05 los 3 compuestos con `avere` | 41-02 T1 | COVERED |
| REQ | IND-06 trapassato remoto en su marco | 41-02 T2 | COVERED |
| RESEARCH | — v2.0 sin RESEARCH.md (`--skip-research` deliberado: el diseño FARE-X1 estaba cerrado). Los análogos los aportan 41-CONTEXT.md §canonical_refs y 41-PATTERNS.md | n/a | N/A por diseño |
| CONTEXT | D-41-01 48 variantes = 8×6, eje persona (costly) | 41-01 T1/T3/T4 + 41-02 T1/T2 + T3 | COVERED |
| CONTEXT | D-41-02 marco temporal propio en las 8 casillas | 41-01 T1/T3/T4 + 41-02 T1/T2 | COVERED |
| CONTEXT | D-41-03 trapassato remoto: subordinada + 3 conectores 2+2+2 + check de `quando` | 41-02 T2 + T3 | COVERED |
| CONTEXT | D-41-04 la explanation dice que fuera del marco no se usa | 41-02 T2 + T3 | COVERED |
| CONTEXT | D-41-05 0-gloss declarado (reversible) | 41-01 T1 (`notes`) + 41-02 T1/T2 + T3 (gate) | COVERED |
| CONTEXT | D-41-06 SCOPE-GATE HARD de perífrasis | 41-01 T1 (`notes`) + 41-02 T1/T2 + T3 (gate) | COVERED |
| CONTEXT | D-41-07 pronombre sujeto explícito en las 48 | 41-02 T1/T2 + T3 (gate) | COVERED |
| CONTEXT | D-41-08 blacklist con audit trail + mención en 2 explanations | 41-01 T1/T4 + 41-02 T3 (gate) | COVERED |
| CONTEXT | D-41-09 patrón fijo de distractoras de los simples | 41-01 T1/T3/T4 + 41-02 T3 (gate) | COVERED |
| CONTEXT | D-41-10 patrón fijo de distractoras de los compuestos | 41-02 T1/T2 + T3 (gate) | COVERED |
| CONTEXT | D-41-11 marcos disjuntos pr↔pp, sin contraste (reversible) | 41-02 T1 + T3 (gate, dos direcciones) | COVERED |
| CONTEXT | D-41-12 ronda EXTRA DeepSeek en los 12 variantes de pr + tr | §Hand-off + 41-02 T3 (assert condicional de `by`) | COVERED |
| CONTEXT | D-41-13 MC-only con 0-match Y 0-wb declarados | 41-01 T1 (`notes`) + 41-02 T3 (assert de `type`) | COVERED |
| CONTEXT | D-41-14 los 8 ids verbatim, `-300`+ libre (costly) | 41-02 T1/T2 + T3 (gate) | COVERED |
| CONTEXT | D-41-15 2 plans + validación top-level, VAL-03 inviolable | la estructura de los 2 plans + §Hand-off + T3 (coherencia) | COVERED |
| CONTEXT | D-41-16 entrada en `categories.json` (order 15, `origen`) | 41-01 T1 + 41-02 T3 (assert de regresión) | COVERED |
| CONTEXT | D-41-17 canon editorial heredado | 41-01 T1 (línea del smoke) + 41-02 T1/T2 + T3 (gate) | COVERED |

**0 items MISSING.** No se requiere split de fase.

## Hand-off al pase TOP-LEVEL de quórum (D-41-15)

| Momento | `validation.status` | `validation.passes[]` | Cómo se comprueba |
|---|---|---|---|
| Al cerrar 41-01 | `"pending"` en los 4 slots simples | `[]` | `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los slots `pending` |
| Al cerrar 41-02 | `"pending"` en los 8 slots | `[]` | idem, con 8 slots; y `tests/content-fare-indicativo.test.js` verde con `status === deriveStatus(passes)` |
| Tras el pase top-level | `"validated"` en los 8 | ≥2 pases `correcta` con `by` distintos; ≥4 pases con al menos uno `deepseek-*` en `fare-indicativo-passato-remoto` y `fare-indicativo-trapassato-remoto` | `VAL_07_STRICT=1 node --test tests/*.test.js` en verde y `tests/content-fare-indicativo.test.js` en verde |

El quórum base canónico (Opus + Sonnet vía el skill `gsd-validate-exercise`, que spawnea Task
subagents) **no está disponible dentro de un subagent `gsd-executor`**, así que ninguna tarea de estos
plans lo ejecuta. Se corre en top-level tras `execute-phase`, **un ejercicio por contexto, NUNCA
batched (VAL-03)**: 8 invocaciones, una por id de slot, en el orden en que aparecen en el fichero. La
ronda EXTRA DeepSeek de D-41-12 entra en esa misma pasada sobre `fare-indicativo-passato-remoto` y
`fare-indicativo-trapassato-remoto` (`scripts/validate-ai-pass.mjs`, claves en `.env`), y sobre las 2
variantes con `quando` del trapassato remoto debe pronunciarse explícitamente sobre la unicidad de
lectura del marco. Además, en esa pasada el pase Opus de cada slot debe registrar en sus `concerns` la
re-declaración local del 0-gloss, mirror literal de `content/exercises/riflessivi.json:245`, para que
la decisión quede también en el audit trail por slot y no solo en el `notes` global.

El gate de la Task 3 es lo que hace ese hand-off no-falsificable: mientras los slots estén `pending`
la suite está verde y `VAL_07_STRICT=1` roja; si alguien escribiera `validated` sin pases reales, la
suite se pondría roja. No hay estado en el que la categoría parezca validada sin estarlo.

## Assumptions (probe de edges sin SPEC — 6 filas `unclassified` surfaceadas, 0 descartes silenciosos)

No existe SPEC.md para esta fase, así que el probe determinista de edges corrió sobre IND-01..IND-06 y
devolvió las 6 filas como `unclassified`: su clasificador funciona por palabras clave en inglés y el
texto de los requisitos está en español. Por protocolo una fila `unclassified` queda `unresolved` y
**nunca** se auto-`backstop`ea ni se descarta — se surfacea aquí como supuesto explícito.

| Requisito | Qué NO derivó el probe | Supuesto que asume este plan |
|---|---|---|
| IND-01 | ninguna categoría de edge (boundary / adjacency / encoding / ordering / empty / precision) | tratado en 41-01 (frontera de homografía y ausencia de la blacklist) |
| IND-02 | idem | tratado en 41-01 (ninguna distractora de raíz es forma atestiguada de otro modo) |
| IND-03 | idem | tratado en 41-01 y cerrado aquí en la segunda dirección: la frontera de marcos disjuntos (D-41-11) se verifica también desde el lado del passato prossimo |
| IND-04 | idem | tratado en 41-01 (`facerò` como distractora y nunca key) |
| IND-05 | idem | los edges reales de los compuestos son la frontera del TIEMPO DEL AUXILIAR (las 2 distractoras en la misma persona que la key) y la invariabilidad del participio (ninguna opción concordada); autorados como truths |
| IND-06 | idem | los edges reales del trapassato remoto son la frontera sintáctica del marco (subordinada + principal en passato remoto, conectores 2+2+2) y la unicidad de lectura con `quando` (principal en passato remoto Y ninguna opción con auxiliar en imperfetto); autorados como truths |

Igualdad de no-descarte-silencioso: **6 filas surfaceadas por el probe == 6 supuestos declarados
aquí**, más los edges derivados a mano y escritos como `must_haves.truths` (count boundary, id-space
boundary, schema boundary, blacklist absence, disjoint-frame boundary en dos direcciones,
single-reading boundary) y **1 `backstop`** (la unicidad de lectura del marco en las 48 variantes,
marcador estructurado con `verification: backstop`, porque es un juicio lingüístico que ninguna
aserción mecánica puede cerrar; si el verificador no puede confirmarlo con evidencia explícita debe
abstenerse a `human_needed`, nunca dar pase silencioso — y el mecanismo que lo cierra de verdad es la
pasada top-level de quórum, con su ronda extra en los 12 variantes delicados).

<tasks>

<task type="auto">
  <name>Task 1: Los 3 compuestos vivos con `avere` — passato prossimo, trapassato prossimo y futuro anteriore (18 variantes, IND-05)</name>

  <files>content/exercises/fare-indicativo.json</files>

  <read_first>
    - `content/exercises/fare-indicativo.json` tal como quedó tras 41-01 — el `notes` (que ya declara
      los marcos disjuntos y el SCOPE-GATE que estos slots aplican) y los 4 slots simples son el molde
      literal: mismo orden de campos, mismos 3 campos por variante, `validation` en `pending`.
    - `content/exercises/riflessivi.json` líneas 186-233 — el precedente de slot compuesto MC: el molde
      de 4 opciones repetido variante a variante y la explanation que cierra nombrando sus dos trampas
      (línea 192).
    - `src/data/schema-validator.js` líneas 437-456 — la superficie MC: `prompt` con `___`, `options` de
      3 o 4 strings no vacíos, `correctIndex` entero en rango.
    - `tests/exercise-types.test.js` línea 1373 — el regex de leak R1 que escanea todos los prompts:
      los prompts de estos slots no llevan paréntesis ni guion largo, así que basta no introducirlos.
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md` §decisions —
      D-41-02, D-41-07, D-41-10 (patrón fijo de distractoras de los compuestos, con el rechazo
      explícito del tiempo simple como distractora), D-41-11, D-41-14, D-41-17, y §specifics (el punto
      de que `fatto` es invariable con `avere` en esta fase y que la concordancia es Phase 43).
    - `.planning/REQUIREMENTS.md` línea 25 — el texto literal de IND-05.
    - `.planning/ROADMAP.md` §Phase 41 SC-2 — la exigencia de que el autor elija el TIEMPO DEL AUXILIAR
      según el marco y de que ninguna variante admita dos lecturas del marco.
  </read_first>

  <precondition>`content/exercises/fare-indicativo.json` existe con 4 slots (`fare-indicativo-presente`, `-imperfetto`, `-passato-remoto`, `-futuro-semplice`) y 24 variantes, `content/categories.json` tiene la entrada `fare-indicativo` con `order: 15`, y `node --test tests/*.test.js` reporta `fail 0` — todo producido por el plan 41-01. Si falta cualquiera de las tres cosas, parar y reportar: sin la entrada de `categories.json` el schema rechaza cada slot nuevo y la suite se pone roja por un motivo que no es el contenido.</precondition>

  <action>
    Añadir TRES slots al array `exercises`, después de `fare-indicativo-futuro-semplice`, con el molde
    exacto de los slots simples. `validation` de los tres: `{ "status": "pending", "passes": [] }`.

    **Invariante estructural de los 3 slots (D-41-10):** cada `options[]` tiene 4 entradas y CADA UNA
    es de dos palabras cuya segunda palabra es `fatto` o `fare`. Eso excluye por construcción las dos
    cosas que D-41-10 y las §specifics prohíben: el tiempo simple correspondiente como distractora
    (rechazado explícitamente por subir el riesgo de doble validez) y el participio concordado, que es
    el MAGNET de Phase 43. Las 2 distractoras de auxiliar van SIEMPRE en la misma persona que la key:
    lo único que las distingue es el tiempo del auxiliar, que es exactamente lo que IND-05 examina.

    **Tercera distractora, malformada (D-41-10):** una de dos familias, a elegir por variante:
    (a) `essere` como auxiliar, en el mismo tiempo y persona que la key, con `fatto` sin flexionar
    (`sono fatto`, `ero fatto`, `sarò fatto`); es la sobre-generalización real del auxiliar desde los
    reflexivos y los verbos de movimiento; o (b) el auxiliar correcto seguido del infinitivo `fare` en
    lugar del participio (`ho fare`, `avevo fare`, `avrò fare`). **Regla dura:** la familia (a) solo se
    usa cuando el prompt lleva un objeto directo explícito, porque el objeto bloquea la lectura pasiva
    que haría gramatical a `sono fatto`; si el objeto no está en la frase, se usa la familia (b).
    Comprobar variante a variante antes de fijar la opción.

    **Slot `fare-indicativo-passato-prossimo` (IND-05)**

    - Las 6 keys, una por variante en orden de persona: `ho fatto`, `hai fatto`, `ha fatto`,
      `abbiamo fatto`, `avete fatto`, `hanno fatto`.
    - Familia de marco (D-41-11, SOLO reciente o conectado con el presente): un marco distinto por
      variante, del repertorio `stamattina`, `ieri`, `questa settimana`, `oggi`, `poco fa`,
      `questo mese`. **Ningún marco remoto:** nada de un año concreto, nada de un demostrativo de
      periodo con apóstrofe, nada de un cuantificador de lejanía — ese repertorio es exclusivo del
      slot de passato remoto de 41-01.
    - Las 2 distractoras de auxiliar: el imperfetto de `avere` (`avevo fatto` y familia) y el futuro de
      `avere` (`avrò fatto` y familia), siempre en la persona del prompt.
    - **Prohibición dura y específica (D-41-11):** ninguna opción puede ser una forma simple de passato
      remoto de `fare` (`feci`, `facesti`, `fece`, `facemmo`, `faceste`, `fecero`). La invariante de dos
      palabras ya lo impide; anotarlo de todos modos porque es la otra mitad de la simetría de D-41-11 y
      el gate de la Task 3 la comprueba en las dos direcciones.
    - Explanation: el compuesto se forma con `avere` conjugado más el participio `fatto`, que con
      `avere` es INVARIABLE; el tiempo lo lleva el auxiliar, no el participio; las 6 formas citadas; el
      marco reciente o conectado con el presente como lo que selecciona este tiempo; el paralelo con el
      castellano `he hecho`, que también deja el participio quieto; y el cierre nombrando las dos
      trampas (el auxiliar en el tiempo equivocado y la sobre-generalización de `essere`).

    **Slot `fare-indicativo-trapassato-prossimo` (IND-05)**

    - Las 6 keys: `avevo fatto`, `avevi fatto`, `aveva fatto`, `avevamo fatto`, `avevate fatto`,
      `avevano fatto`.
    - Familia de marco: anterioridad respecto a OTRO evento pasado. Cada uno de los 6 prompts contiene
      un segundo verbo en pasado (passato prossimo o imperfetto, de un verbo que NO sea `fare`) más el
      adverbio `già` delante del hueco o inmediatamente después, del tipo "cuando llegaron, yo ya había
      hecho los deberes". El marco tiene que hacer imposible el passato prossimo simple: la
      anterioridad respecto al segundo evento es lo que fuerza el trapassato.
    - Las 2 distractoras de auxiliar: el presente de `avere` (`ho fatto` y familia) y el futuro de
      `avere` (`avrò fatto` y familia), en la persona del prompt.
    - Explanation: el trapassato prossimo se forma con el imperfetto de `avere` más `fatto`, y expresa
      lo que ya estaba hecho ANTES de otro momento pasado; las 6 formas citadas; el paralelo con
      `había hecho`; el cierre nombrando las dos trampas.

    **Slot `fare-indicativo-futuro-anteriore` (IND-05)**

    - Las 6 keys: `avrò fatto`, `avrai fatto`, `avrà fatto`, `avremo fatto`, `avrete fatto`,
      `avranno fatto`. Ojo a la tilde: `avrò` y `avrà` llevan acento grave; `avrai`, `avremo`,
      `avrete`, `avranno` no.
    - Familia de marco: anterioridad respecto a un momento FUTURO. Cada uno de los 6 prompts contiene
      `quando` o `appena` con el otro verbo en futuro semplice (de un verbo que no sea `fare`) más
      `già`, del tipo "mañana, cuando vuelvas, yo ya habré hecho la tarta". El marco tiene que hacer
      imposible el futuro semplice simple.
    - Las 2 distractoras de auxiliar: el presente de `avere` (`ho fatto` y familia) y el imperfetto de
      `avere` (`avevo fatto` y familia), en la persona del prompt.
    - Explanation: el futuro anteriore se forma con el futuro de `avere` más `fatto`, y expresa lo que
      estará hecho ANTES de otro momento futuro; las 6 formas citadas; el paralelo con `habré hecho`; el
      cierre nombrando las dos trampas.

    Reglas duras comunes a los 3 slots: pronombre sujeto explícito en los 18 prompts (D-41-07), hueco
    literal `___`, objeto del conjunto cerrado de D-41-06, cero paréntesis y cero castellano en el
    prompt (D-41-05), 4 opciones sin duplicados dentro de la variante, `correctIndex` variado entre las
    6, cero corchetes angulares en cualquier string, explanations en español acentuado, plain text, un
    solo párrafo, apóstrofes ASCII y sin referencias a otro ejercicio por id.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 7`; los ids son, en orden, los 4 simples de 41-01 más `fare-indicativo-passato-prossimo`, `fare-indicativo-trapassato-prossimo` y `fare-indicativo-futuro-anteriore`; los 7 tienen `type === 'multiple-choice'` y `variants.length === 6`; total 42 variantes.
    - `node -e`: el conjunto de keys de `fare-indicativo-passato-prossimo` es exactamente `{ho fatto, hai fatto, ha fatto, abbiamo fatto, avete fatto, hanno fatto}`; el de `fare-indicativo-trapassato-prossimo` es exactamente `{avevo fatto, avevi fatto, aveva fatto, avevamo fatto, avevate fatto, avevano fatto}`; el de `fare-indicativo-futuro-anteriore` es exactamente `{avrò fatto, avrai fatto, avrà fatto, avremo fatto, avrete fatto, avranno fatto}`; sin repetidos en ninguno.
    - `node -e` sobre los `options[]` de los 3 slots nuevos: las 18 listas tienen longitud 4; **todas** las 72 opciones se parten en exactamente 2 palabras por el espacio, y la segunda palabra es `fatto` o `fare` (D-41-10 — excluye tiempo simple y participio concordado a la vez).
    - `node -e` sobre los 3 slots nuevos: por variante, exactamente 2 de las 4 opciones tienen como primera palabra una forma de `avere` en un tiempo distinto al de la key pero en la MISMA persona; y exactamente 1 opción es de la familia malformada (primera palabra forma de `essere`, o segunda palabra `fare`).
    - Escaneo `node -e` sobre los 6 `prompt` de `fare-indicativo-passato-prossimo`: **0** coincidencias con `Nel 19`, `Quell'`, `anni fa`, `Molti anni`, `Tanti anni` (D-41-11, dirección reciente→remoto); los 6 marcos son distintos y salen del repertorio reciente.
    - Escaneo `node -e` sobre los 6 `prompt` de `fare-indicativo-trapassato-prossimo`: los 6 contienen `già`, y los 6 contienen un segundo verbo en pasado distinto de `fare`.
    - Escaneo `node -e` sobre los 6 `prompt` de `fare-indicativo-futuro-anteriore`: los 6 contienen `già`, y los 6 contienen `quando` o `appena`.
    - Escaneo `node -e` sobre los 42 `prompt`: los 42 contienen `___`; los 42 contienen un pronombre sujeto de los 6; **0** contienen `(`, `)` ni `espa`.
    - Escaneo `node -e` sobre `prompt` y `options[]` de los 7 slots: **0** coincidencias con `colazione`, `spesa`, `freddo`, `farcel`; **0** opciones contienen `fatta`, `fatti` ni `fatte`.
    - Escaneo `node -e` sobre los `options[]` de los 7 slots: **0** coincidencias exactas con `["fo","fé","fenno","facea","fan"]`; **0** con `["faccia","facciate","facciano","facessi","facesse","farei","farebbe","facci","facce","fava","faro"]`.
    - Escaneo `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:` y de las comillas tipográficas; ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`.
    - `node -e`: las 3 explanations nuevas contienen sus 6 formas correctas como subcadenas, contienen la subcadena `fatto`, tienen longitud `> 400` caracteres cada una y al menos un carácter acentuado del español.
    - `node -e`: los 7 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`; `correctIndex` no es el mismo valor en las 6 variantes de ningún slot.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - `git status --porcelain` lista únicamente `content/exercises/fare-indicativo.json`; `git diff --stat src/ scripts/ content/categories.json tests/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    Los 3 compuestos vivos están autorados con 18 variantes en `pending`; sus keys son las 18 formas
    correctas de `avere` + `fatto`, una por casilla persona×tiempo; las 2 distractoras de auxiliar de
    cada variante están en la misma persona que la key, así que el ejercicio es elegir el tiempo del
    auxiliar; ninguna opción es un tiempo simple ni un participio concordado; los marcos del passato
    prossimo son exclusivamente recientes; y la suite completa sigue en `fail 0`.
  </done>
</task>

<task type="auto">
  <name>Task 2: `fare-indicativo-trapassato-remoto` — el único requisito sintáctico de la fase (6 variantes, IND-06, SC-3)</name>

  <files>content/exercises/fare-indicativo.json</files>

  <read_first>
    - `content/exercises/fare-indicativo.json` tal como quedó tras la Task 1 — molde, `notes` y los 3
      slots compuestos anteriores, cuyo patrón de distractoras este slot hereda con una desviación
      declarada (el auxiliar en imperfetto queda prohibido, ver `<action>`).
    - `content/exercises/riflessivi.json` líneas 186-268 — el slot MAGNET completo: el molde de 4
      opciones, la explanation que nombra las trampas y el shape de 4 pases que la ronda extra de
      D-41-12 dejará aquí en la pasada top-level.
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md` §decisions —
      D-41-03 (subordinada obligatoria, conectores 2+2+2, riesgo asumido de `quando`), D-41-04 (la
      explanation lo dice explícitamente), D-41-10, D-41-12 (ronda extra), y §specifics (el punto de
      que las 2 variantes con `quando` son donde SC-2/R7 puede fallar).
    - `.planning/REQUIREMENTS.md` línea 26 — el texto literal de IND-06, incluido "El marco es parte del
      ejercicio: sin él la frase es artificial".
    - `.planning/ROADMAP.md` §Phase 41 SC-3 — la formulación literal que la verificación va a comprobar.
  </read_first>

  <action>
    Añadir UN slot al array `exercises`, en última posición, con el molde de los compuestos.
    `validation`: `{ "status": "pending", "passes": [] }`.

    **Slot `fare-indicativo-trapassato-remoto` (IND-06)**

    - Las 6 keys, una por variante en orden de persona: `ebbi fatto`, `avesti fatto`, `ebbe fatto`,
      `avemmo fatto`, `aveste fatto`, `ebbero fatto`. El auxiliar es el passato remoto de `avere`, con
      su propia alternancia: `ebb-` en 1ª singular, 3ª singular y 3ª plural; `av-` en 2ª singular, 1ª
      plural y 2ª plural.
    - **Marco obligatorio en las 6 (D-41-03, SC-3):** el hueco vive SIEMPRE dentro de una subordinada
      temporal, y la oración principal lleva su verbo en passato remoto. Ninguna variante presenta la
      forma en frase suelta. El verbo de la principal NO es una forma de `fare` — un verbo de
      movimiento o de comunicación en passato remoto (`uscii`, `uscisti`, `uscì`, `uscimmo`, `usciste`,
      `uscirono`, `partì`, `telefonò`, `arrivarono`, `tornò`) mantiene el marco legible sin duplicar el
      verbo que se examina.
    - **Reparto 2+2+2 de los conectores (D-41-03), fijado aquí:**
      `dopo che` → las variantes de `io` (`ebbi fatto`) y de `noi` (`avemmo fatto`);
      `quando` → las variantes de `tu` (`avesti fatto`) y de `voi` (`aveste fatto`);
      `appena` → las variantes de `lui/lei` (`ebbe fatto`) y de `loro` (`ebbero fatto`).
    - **Las 2 variantes con `quando` — el punto exacto donde SC-2/R7 puede fallar.** `quando` es el más
      permisivo de los tres conectores: por sí solo admitiría también el imperfetto y el passato remoto
      simple. La lectura única se garantiza con DOS mecanismos, los dos obligatorios:
      (i) la principal está en passato remoto, lo que excluye cualquier opción con el auxiliar en
      presente o en futuro; y
      (ii) ninguna opción es una forma simple ni lleva el auxiliar en imperfetto, así que las lecturas
      alternativas que `quando` admitiría no están en la lista de opciones y no pueden competir.
      Verificar los dos mecanismos variante a variante antes de cerrar el slot, y anotarlo en el
      SUMMARY para que la pasada top-level de quórum se pronuncie explícitamente sobre esas dos.
    - **Distractoras (D-41-10, con una desviación declarada):** se mantiene la invariante de dos
      palabras con `fatto` o `fare` como segunda. Las 2 distractoras de auxiliar son el PRESENTE de
      `avere` (`ho fatto` y familia) y el FUTURO de `avere` (`avrò fatto` y familia), en la persona del
      prompt. **El imperfetto de `avere` (`avevo fatto` y familia) queda PROHIBIDO como distractora en
      este slot**, a diferencia de los otros tres: el trapassato prossimo es el competidor
      casi-sinónimo tras `quando` y `dopo che`, y ofrecerlo abriría exactamente la segunda lectura que
      SC-2 prohíbe. La tercera distractora es la malformada, con la misma regla de familias que la
      Task 1 (la familia de `essere` solo si el prompt lleva objeto directo explícito).
    - **Explanation (D-41-04, exigencia literal de SC-3):** además del paradigma de las 6 formas y del
      reparto `ebb-`/`av-` del auxiliar, tiene que contener una frase que diga EXPLÍCITAMENTE que fuera
      de ese marco — subordinada temporal con `dopo che`, `quando` o `appena` y la principal en passato
      remoto — esta forma NO se usa. No basta insinuarlo: SC-3 lo comprueba literalmente. Debe también
      ser honesta sobre el registro (es una forma prácticamente extinta en el habla, que vive en la
      narración escrita y solo en esta construcción), nombrar los 3 conectores, dar el paralelo con el
      castellano (donde `hube hecho` es igual de marginal y lo normal es el pretérito simple), y cerrar
      nombrando las dos trampas (el auxiliar en el tiempo equivocado y el compuesto mal construido).

    Reglas duras comunes: pronombre sujeto explícito en los 6 prompts, hueco literal `___`, objeto del
    conjunto cerrado de D-41-06, cero paréntesis y cero castellano en el prompt, 4 opciones sin
    duplicados, `correctIndex` variado, cero corchetes angulares.

    **Nota para el pase top-level (no es trabajo de esta tarea):** este slot es el segundo de los dos
    que llevan ronda EXTRA DeepSeek obligatoria (D-41-12), por ser el único requisito sintáctico de la
    fase y por el agravante de `quando`. Dejarlo anotado en el SUMMARY.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 8`; los 8 ids son exactamente los de D-41-14 en el orden presente, imperfetto, passato remoto, futuro semplice, passato prossimo, trapassato prossimo, futuro anteriore, trapassato remoto; los 8 tienen `type === 'multiple-choice'` y `variants.length === 6`; total 48 variantes.
    - `node -e`: el conjunto de keys de `fare-indicativo-trapassato-remoto` es exactamente `{ebbi fatto, avesti fatto, ebbe fatto, avemmo fatto, aveste fatto, ebbero fatto}`, sin repetidos, y cada key corresponde al pronombre sujeto de su propio prompt.
    - `node -e` sobre los 6 `prompt` de este slot: cada uno contiene exactamente uno de `dopo che`, `quando`, `appena`; el reparto es 2 / 2 / 2; `dopo che` cae en las variantes de `io` y `noi`, `quando` en las de `tu` y `voi`, `appena` en las de `lui/lei` y `loro` (D-41-03).
    - `node -e` sobre los 6 `prompt` de este slot: cada uno contiene, además del hueco, un segundo verbo en passato remoto que no es una forma de `fare` — verificado porque el prompt contiene una de las formas declaradas de la principal y **0** de `feci`, `facesti`, `fece`, `facemmo`, `faceste`, `fecero`.
    - `node -e` sobre los `options[]` de este slot: las 6 listas tienen longitud 4; las 24 opciones se parten en 2 palabras con `fatto` o `fare` como segunda; **0** opciones tienen como primera palabra una forma del imperfetto de `avere` (`avevo`, `avevi`, `aveva`, `avevamo`, `avevate`, `avevano`) — la desviación declarada de D-41-10 para este slot.
    - `node -e`: la `explanation` de `fare-indicativo-trapassato-remoto` contiene las 6 formas correctas, contiene los 3 conectores `dopo che`, `quando` y `appena`, contiene la subcadena `passato remoto`, y contiene una frase con la negación explícita del uso fuera del marco — verificable como la coocurrencia de `fuera de` con `no se usa` en la misma explanation (D-41-04, SC-3). Longitud `> 500` caracteres.
    - `node -e`: la `explanation` de `fare-indicativo-trapassato-remoto` contiene al menos un carácter acentuado del español y **0** apariciones de markdown, comillas tipográficas o corchetes angulares.
    - Escaneo `node -e` sobre los 48 `prompt`: los 48 contienen `___`; los 48 contienen un pronombre sujeto de los 6; **0** contienen `(`, `)` ni `espa`.
    - Escaneo `node -e` sobre `prompt` y `options[]` de los 8 slots: **0** coincidencias con `colazione`, `spesa`, `freddo`, `farcel`; **0** opciones contienen `fatta`, `fatti` ni `fatte`; **0** coincidencias exactas con la blacklist de 5 formas ni con las 11 formas de otros modos y homógrafas.
    - Escaneo `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:` y de las comillas tipográficas; ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`.
    - `node -e`: los 8 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`; ningún id lleva sufijo numérico de 3 cifras (`fare-indicativo-300`+ libre).
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 8 slots con `(pending)`.
    - `git status --porcelain` lista únicamente `content/exercises/fare-indicativo.json`.
  </acceptance_criteria>

  <done>
    Los 8 slots y las 48 variantes están autorados en `pending`; las 6 variantes del trapassato remoto
    viven dentro de una subordinada temporal con la principal en passato remoto, con los 3 conectores
    repartidos 2+2+2; ninguna opción del slot lleva el auxiliar en imperfetto, así que las 2 variantes
    con `quando` no ofrecen su competidor casi-sinónimo; la explanation dice con esas palabras que
    fuera del marco la forma no se usa; y la suite completa sigue en `fail 0`.
  </done>
</task>

<task type="auto">
  <name>Task 3: `tests/content-fare-indicativo.test.js` — convertir los 8 gates de la categoría en invariantes permanentes de la suite</name>

  <files>tests/content-fare-indicativo.test.js</files>

  <read_first>
    - `content/exercises/fare-indicativo.json` completo tal como quedó tras la Task 2 — el test se
      escribe contra el fichero real; los conjuntos de formas canónicas del test tienen que coincidir
      byte a byte con las keys del fichero.
    - `tests/schema-validator-origen.test.js` líneas 1-20 — el molde de cabecera y de imports de un
      fichero de test de este repo: banner de comentario que dice qué cubre y con qué comando se corre,
      `import { test, describe } from 'node:test'`, `import assert from 'node:assert/strict'`, e import
      relativo del módulo de `src/`.
    - `src/data/validation-state.js` líneas 22-42 — la firma y las reglas de `deriveStatus`, que el test
      importa: `incorrecta` es sticky y da `disputed`; `>= 2 correcta` con `by` distintos y cero
      `incorrecta` da `validated`; todo lo demás, incluido `passes: []`, da `pending`. Esa última rama
      es la que hace que el assert de igualdad sea verde también antes de la pasada de quórum.
    - `tests/exercise-types.test.js` líneas 1265-1293 y 1432-1448 — lo que el smoke paramétrico ya cubre
      (coverage de explanation, apóstrofes, markdown, leak R1, cross-refs R2, y el gate VAL-07 bajo
      `VAL_07_STRICT=1`). Este fichero NO duplica ninguno de esos 6: cubre lo que es específico de
      `fare-indicativo` y que ningún test existente mira.
    - `tests/domain.test.js` líneas 220-230 y 322-341 — lo que ya cubre el auto-descubrimiento y la
      validación de schema del bundle. Tampoco se duplica.
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md` §decisions —
      D-41-05 a D-41-14, que son los que este fichero convierte en aserciones, y §Claude's Discretion,
      que deja los nombres y la estructura de los tests a criterio propio siempre que se cubran la
      entrada del smoke, la validación de schema, la cobertura editorial y los sub-tests por categoría.
  </read_first>

  <action>
    Crear `tests/content-fare-indicativo.test.js`, cero dependencias, ejecutable con
    `node --test tests/content-fare-indicativo.test.js` y recogido por el glob de la suite. Cabecera de
    comentario en el estilo del repo: qué fase y qué requisitos cubre (v2.0 Phase 41, IND-01..IND-06),
    con qué comando se corre, y la advertencia de que los escaneos de ausencia van SIEMPRE por campo
    (`variants[].prompt`, `variants[].options[]`) y nunca sobre el fichero completo, porque el `notes`
    nombra a propósito las formas de la blacklist y las perífrasis excluidas y un escaneo de fichero
    completo se auto-invalidaría.

    Leer el JSON una vez en el top-level del módulo con `readFileSync` y una URL relativa
    (`new URL('../content/exercises/fare-indicativo.json', import.meta.url)`), y también
    `content/categories.json`. Importar `deriveStatus` de `../src/data/validation-state.js`.

    Declarar las constantes de datos del test, que son la especificación ejecutable de la categoría:

    - `CANON`: mapa de los 8 ids de slot a su array de 6 formas correctas, en orden de persona. Es la
      tabla completa del paradigma: presente, imperfetto, passato remoto, futuro semplice y los cuatro
      compuestos con `avere` + `fatto`.
    - `PRONOUNS`: los 6 pronombres sujeto, con las dos variantes de tercera singular.
    - `SIMPLE_SLOTS` y `COMPOUND_SLOTS`: los dos grupos de 4 ids, porque el patrón de distractoras es
      distinto en cada uno (D-41-09 frente a D-41-10).
    - `BLACKLIST`: las 5 formas atestiguadas de D-41-08.
    - `OTHER_MOODS`: las formas de congiuntivo, imperativo y condizionale de `fare` que pertenecen a las
      Phases 42 y 43, más las palabras italianas homógrafas que una autoría podría producir por
      descuido.
    - `PERIPHRASIS`: los marcadores léxicos del SCOPE-GATE de D-41-06.
    - `OBJECTS`: el conjunto cerrado de 7 objetos literales permitidos.
    - `REMOTE_FRAMES` y `RECENT_FRAMES`: los dos repertorios disjuntos de D-41-11.
    - `AVERE_IMPERFETTO`: las 6 formas del imperfetto de `avere`, prohibidas como primera palabra de una
      opción del slot de trapassato remoto.
    - `EXTRA_ROUND_SLOTS`: los 2 ids con ronda extra obligatoria (D-41-12).

    Organizar los tests en `describe` por decisión, para que un fallo diga qué decisión se rompió:

    1. **Estructura y conteos (D-41-01, D-41-13, D-41-14).** El fichero tiene exactamente 2 claves
       top-level; 8 slots; los 8 ids son los de `CANON` y en ese orden; los 8 son `multiple-choice`; no
       existe ningún slot de otro tipo; cada slot tiene exactamente 6 variantes; el total es 48; ningún
       id lleva sufijo numérico de 3 cifras (espacio de cruces libre); cada variante tiene exactamente
       las 3 claves `prompt`, `options`, `correctIndex`.
    2. **El paradigma completo (IND-01..IND-06).** Por slot, el conjunto de `options[correctIndex]` de
       sus 6 variantes es exactamente el array de `CANON`, sin repetidos y con las 6 formas presentes.
       Es la aserción que garantiza que no falta ninguna casilla persona×tiempo.
    3. **Eje de persona (D-41-07).** Cada prompt contiene `___`; cada prompt contiene exactamente un
       pronombre sujeto de `PRONOUNS`; y el pronombre de cada variante es coherente con la posición de
       su key en el array de `CANON` (la variante índice `k` lleva el pronombre de la persona `k`).
    4. **0-gloss (D-41-05).** Ningún prompt contiene `(` ni `)`; ningún prompt contiene `espa` en
       ninguna capitalización. Comentario en el test explicando por qué la ausencia de paréntesis es la
       forma más robusta de exigir el 0-gloss y por qué el 0-gloss aquí es decisión y no omisión, con
       el puntero a la declaración del `notes` — para que una pasada futura que quiera "arreglarlo" se
       encuentre el porqué en el propio test.
    5. **SCOPE-GATE léxico (D-41-06).** Ningún prompt ni opción contiene un marcador de `PERIPHRASIS`;
       cada prompt contiene al menos un objeto de `OBJECTS`.
    6. **Blacklist y formas de otros modos (D-41-08).** Ninguna opción de ningún slot coincide
       exactamente con una entrada de `BLACKLIST` ni de `OTHER_MOODS`; ninguna opción contiene un
       participio concordado (`fatta`, `fatti`, `fatte`). Y en positivo: el `notes` nombra las 5 formas
       de `BLACKLIST`, y las explanations de `fare-indicativo-presente` y de
       `fare-indicativo-passato-remoto` nombran las suyas.
    7. **Patrón de distractoras (D-41-09 y D-41-10).** Para los 4 slots simples: cada variante tiene 4
       opciones sin duplicados; exactamente 1 es la key; exactamente 1 es otra forma del mismo array de
       `CANON`; y exactamente 2 no pertenecen a la unión de los 48 valores de `CANON`, es decir son
       formas inexistentes. Para los 4 compuestos: cada opción se parte en 2 palabras y la segunda es
       `fatto` o `fare`; exactamente 1 es la key; exactamente 2 tienen la misma persona que la key con
       el auxiliar `avere` en otro tiempo; exactamente 1 es de la familia malformada. Y para
       `fare-indicativo-trapassato-remoto`, además: ninguna opción tiene como primera palabra una forma
       de `AVERE_IMPERFETTO`.
    8. **Marcos disjuntos, en las dos direcciones (D-41-11).** Ningún prompt de
       `fare-indicativo-passato-remoto` contiene un marcador de `RECENT_FRAMES`, y ninguna de sus
       opciones contiene `fatt`. Ningún prompt de `fare-indicativo-passato-prossimo` contiene un
       marcador de `REMOTE_FRAMES`, y ninguna de sus opciones es una forma simple de passato remoto.
       Además, los 6 marcos de cada slot son distintos entre sí, en los 8 slots (D-41-02).
    9. **Marco del trapassato remoto (D-41-03, D-41-04, SC-3).** Cada uno de los 6 prompts contiene
       exactamente uno de los 3 conectores; el reparto es 2 / 2 / 2; y la explanation del slot contiene
       los 3 conectores, la subcadena `passato remoto`, y la negación explícita del uso fuera del marco.
    10. **Canon editorial y higiene del contenido (D-41-17, T-41-01, T-41-02).** Ningún string del
        fichero (`notes`, las 8 explanations, los 48 prompts, las 192 opciones) contiene `<`, `>`, `&#`,
        `javascript:` ni comillas tipográficas; cada explanation contiene al menos un carácter acentuado
        del español y no está vacía; y un recorrido recursivo del objeto parseado no encuentra ninguna
        clave propia llamada `__proto__`, `constructor` ni `prototype`.
    11. **Coherencia del audit trail de validación (D-41-12, D-41-15, T-41-03).** Para los 8 slots,
        `validation` existe con las claves `status` y `passes`, `passes` es un array, y
        `status === deriveStatus(passes)`. Condicionalmente: si `status` es `validated`, entonces
        `passes` tiene al menos 2 entradas con `verdict: "correcta"`, al menos 2 valores distintos de
        `by`, y cero `incorrecta`. Y para los 2 ids de `EXTRA_ROUND_SLOTS`: si `status` es `validated`,
        al menos un `by` empieza por `deepseek`. Comentario en el test explicando que estas aserciones
        son verdes con `passes: []` (porque `deriveStatus([])` da `pending`) y que su función es impedir
        que un `validated` escrito a mano, sin pases reales, pase por bueno.
    12. **Registro de la categoría (D-41-16, SC-5).** `content/categories.json` contiene una entrada
        `fare-indicativo` con exactamente las 4 claves `id`, `name`, `order`, `origen`, con `order: 15`
        y `origen: "ia-quorum"`; es la última del array; y al menos un slot del fichero la referencia en
        `categoryIds`, de modo que la derivación de `categoriesForDisplay` da `totalCount > 0` y por
        tanto `examenEnabled` verdadero. Este test es la red de regresión del registro: si alguien
        reordena o reescribe `categories.json`, salta aquí.

    Los mensajes de aserción van en español y nombran la decisión y el id de slot o el índice de
    variante concretos, para que un fallo se localice sin abrir el JSON. No se duplica ninguna aserción
    que ya haga `tests/domain.test.js` (schema del bundle) ni el smoke paramétrico de
    `tests/exercise-types.test.js` (coverage de explanation, apóstrofes, markdown, leak R1, cross-refs
    R2, VAL-07 estricto).
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/content-fare-indicativo.test.js && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-indicativo.test.js` sale con código 0 y reporta `fail 0` con al menos 20 tests pasados.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - `grep -c "describe(" tests/content-fare-indicativo.test.js` es `>= 10` — los 12 bloques por decisión están presentes.
    - `grep -c "deriveStatus" tests/content-fare-indicativo.test.js` es `>= 3` (el import y al menos dos usos).
    - `grep -c "validation-state.js" tests/content-fare-indicativo.test.js` devuelve `1` — el test importa la función real, no reimplementa las reglas.
    - `grep -c "D-41-" tests/content-fare-indicativo.test.js` es `>= 10` — cada bloque cita la decisión que verifica.
    - `grep -c "deepseek" tests/content-fare-indicativo.test.js` es `>= 1` (D-41-12).
    - `grep -c "categories.json" tests/content-fare-indicativo.test.js` es `>= 1` (D-41-16 / SC-5).
    - Prueba de fail-first, sobre una copia temporal fuera del repo (nunca sobre el fichero real): mutar la copia en 4 sentidos y comprobar que el test se pone rojo en cada uno — (a) añadir un gloss entre paréntesis a un prompt, (b) sustituir una distractora por `fo`, (c) poner un marco reciente en una variante del passato remoto, (d) escribir `status: "validated"` dejando `passes: []`. Los 4 casos tienen que fallar; si alguno pasa, la aserción correspondiente no tiene dientes y hay que arreglarla.
    - `node -e` sobre el fichero de contenido: sigue con 8 slots, 48 variantes y `validation.status === 'pending'` en los 8 — esta tarea NO toca el contenido.
    - `git status --porcelain` lista únicamente `tests/content-fare-indicativo.test.js`; `git diff --stat src/ scripts/ content/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    `tests/content-fare-indicativo.test.js` existe, corre dentro del glob de la suite en verde, y
    convierte en aserciones permanentes los 8 gates de la categoría: conteos y paradigma completo, eje
    de persona, 0-gloss, SCOPE-GATE léxico, blacklist, patrón de distractoras por familia, marcos
    disjuntos en las dos direcciones, marco del trapassato remoto con su negación explícita, canon
    editorial e higiene del JSON, coherencia de `validation` con `deriveStatus` incluida la ronda extra,
    y el registro de la categoría. La prueba de fail-first demuestra que las cuatro aserciones más
    importantes tienen dientes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Honestamente acotadas: app web estática de un solo usuario, sin servidor, sin autenticación, sin red y
sin sincronización (CLAUDE.md §Constraints). El único dato nuevo es un fichero JSON escrito a mano por
el propio autor, más un fichero de test que no corre en el navegador.

| Boundary | Description |
|----------|-------------|
| `content/exercises/fare-indicativo.json` en disco → `fetch()` en boot → `JSON.parse` en `content-loader.js` | Contenido semi-confiable: lo escribe el autor (o una autoría asistida), no un tercero, pero pasa por `JSON.parse` y de ahí al estado de la app sin otra puerta. |
| strings de contenido (`prompt`, `options[]`, `explanation`, `notes`) → render en el DOM por Alpine | La invariante anti-XSS del proyecto (T-02-01): el contenido se renderiza SIEMPRE como texto (`x-text`), nunca como HTML. Si un string llevara marcado, o el render cambiara a HTML, el contenido pasaría a ser código. |
| `validation.passes[]` → `deriveStatus` → gate VAL-07 y reporter | Frontera de INTEGRIDAD, no de confidencialidad: es la única evidencia de que una variante fue revisada por quórum, y quien escribe el JSON puede escribir también su propio veredicto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | strings de `content/exercises/fare-indicativo.json` renderizados en el DOM | medium | mitigate | Se preserva la invariante `x-text`-only (T-02-01): esta fase no añade ni cambia una sola línea de render. Triple verificación automatizada: el sub-test `plain text (no markdown — D-126)` (`tests/exercise-types.test.js:1349-1363`) activado en 41-01; el escaneo `node -e` de cada tarea de contenido, que exige **0** apariciones de `<`, `>`, `&#` y `javascript:` en TODOS los strings del fichero; y el bloque 10 de `tests/content-fare-indicativo.test.js`, que convierte ese escaneo en un test permanente. El `notes` se redacta sin corchetes angulares a propósito, para que el escaneo no necesite excepciones. |
| T-41-02 | Tampering | `JSON.parse` del fichero de contenido en `content-loader.js` | medium | mitigate | Prototype-pollution por contenido: `JSON.parse` crea `__proto__` como own-property si el literal la declara. Mitigación: el fichero declara ÚNICAMENTE el key set del schema (top-level `notes` y `exercises`; por slot `id`, `type`, `categoryIds`, `explanation`, `variants`, `validation`; por variante `prompt`, `options`, `correctIndex`), verificado clave a clave en la Task 3 bloque 1; y el bloque 10 recorre recursivamente el objeto parseado exigiendo que ninguna clave propia se llame `__proto__`, `constructor` ni `prototype`. El deep-clone defensivo del state vive en la cadena de migración (T-40-01) y no se toca. |
| T-41-03 | Repudiation | `validation.passes[]` como audit trail del quórum | medium | mitigate | Un `status: "validated"` escrito a mano, o un pase con `by`/`date` inventados, forja la única evidencia que el autor tiene de que una variante fue revisada, y a ojo es indetectable. Mitigación en tres capas: el executor deja los 8 slots en `pending` con `passes: []` y no fabrica pases (criterio de aceptación en Tasks 1 y 2); el quórum real corre en la pasada top-level de D-41-15, un ejercicio por contexto (VAL-03); y el bloque 11 de `tests/content-fare-indicativo.test.js` importa `deriveStatus` de `src/data/validation-state.js` y exige `status === deriveStatus(passes)` más, condicionalmente a `validated`, ≥2 `correcta` con ≥2 `by` distintos, cero `incorrecta`, y al menos un `by` con prefijo `deepseek` en los 2 slots de ronda extra (D-41-12). La prueba de fail-first del criterio de aceptación demuestra que esa aserción tiene dientes. |
| T-41-04 | Tampering | entrada de `content/categories.json` (regresión) | low | mitigate | Esta fase ya no la escribe (fue 41-01), pero un reordenado o una reescritura posterior dejaría la categoría invisible o rompería el contrato de slugs de Phase 40. El bloque 12 de la Task 3 la convierte en test de regresión: 4 claves exactas, `order: 15`, `origen: "ia-quorum"`, última del array, y al menos un slot que la referencia. |
| T-41-05 | Denial of Service | tamaño del bundle de contenido cargado en boot | low | accept | 48 variantes son del orden de 600 líneas de JSON; el loader ya trae 14 ficheros con 225 slots. El coste de `JSON.parse` a esta escala es despreciable y la app no tiene atacante remoto: sin servidor, sin red, sin multiusuario. |
| T-41-06 | Information Disclosure | el contenido del fichero y del fichero de test | low | accept | Son conjugaciones de un verbo italiano, explicaciones gramaticales y aserciones sobre ellas. Cero PII, cero credenciales, cero secretos. Nada sale de la máquina del autor. |
| T-41-07 | Spoofing | — | n/a | n/a — no aplica | No existen identidades, sesiones, tokens ni cuentas en esta app: es local, monousuario y sin autenticación. No hay superficie de suplantación que evaluar en L1. |
| T-41-08 | Elevation of Privilege | — | n/a | n/a — no aplica | No hay roles, permisos ni frontera de privilegio: un único usuario con acceso total a su propio navegador y su propio disco. |
| T-41-SC | Tampering | instalaciones de npm / pip / cargo | n/a | n/a — gate no disparado | Esta fase no ejecuta ninguna instalación de gestor de paquetes ni añade dependencias: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node. El Package Legitimacy Gate no aplica y no se requiere tabla de auditoría en RESEARCH.md. |

**ASVS L1, block on high.** No hay ninguna amenaza `high` en este plan, y no se inventa ninguna para
rellenar la tabla. La superficie real de una fase de contenido en una app local sin servidor son tres
cosas: la invariante de render como texto, la higiene del key set del JSON parseado, y la integridad
del audit trail de validación — que en esta fase es la más interesante, porque el mismo agente que
escribe el contenido podría escribir su propio veredicto. Las cuatro amenazas `medium`/`low` en
`mitigate` tienen verificación automatizada; las tres `accept` son `low` y su razón es estructural.

### Candidatos de prohibición canon descartados (breadcrumbs)

- **prototype-pollution** — canon de seguridad del proyecto; vive en `<threat_model>` T-41-02 y en
  `/gsd-secure-phase`, no se acuña como prohibición.
- **XSS / inyección de HTML** — canon; vive en T-41-01 (invariante `x-text`-only T-02-01), no se acuña.
- **path-traversal** — sin acceso a sistema de ficheros en runtime: el contenido se sirve por HTTP
  local y el runtime es el navegador. Descartado.
- **GDPR / privacidad de datos personales** — el contenido son conjugaciones y el state es el progreso
  de estudio del propio autor en su máquina. Descartado.
- **fairness / sesgo genérico** — sin decisión automatizada sobre personas. Lo que SÍ se acuña son las
  dos variantes bespoke de esta fase: no mentir sobre el registro de una forma casi extinta
  (prohibición 3) y no presentar una variedad regional como norma (prohibición 4), que ninguna
  herramienta canon detectaría.
</threat_model>

<verification>
Comprobaciones al terminar las 3 tareas de este plan, que son también las de cierre de la fase:

1. `node --test tests/*.test.js` → código de salida 0, `fail 0`, con el fichero de test nuevo dentro
   del glob.
2. `node --test tests/content-fare-indicativo.test.js` corre aislado, en verde, con `fail 0` y al
   menos 20 tests pasados.
3. `node -e` sobre `content/exercises/fare-indicativo.json`: 8 slots con los ids exactos de D-41-14 en
   el orden declarado, 6 variantes cada uno, 48 en total, los 8 en `multiple-choice` con `options` de 4,
   y las 48 keys iguales al paradigma completo del indicativo de `fare`.
4. Los escaneos de ausencia, todos por campo y nunca sobre el fichero completo (el `notes` nombra a
   propósito las formas y las perífrasis excluidas): blacklist y formas de otros modos en `options`,
   perífrasis en `prompt` y `options`, paréntesis y castellano en `prompt`, marcos recientes en el
   passato remoto y marcos remotos en el passato prossimo, participio concordado en `options`, y
   auxiliar en imperfetto en las `options` del trapassato remoto.
5. `VAL_07_STRICT=1 node --test tests/*.test.js` → falla nombrando los 8 slots `pending`. Ese rojo es
   el estado CORRECTO al cerrar la ejecución de la fase; **la pasada TOP-LEVEL de quórum es lo que lo
   cierra** (§Hand-off): 8 invocaciones del skill `gsd-validate-exercise`, una por id de slot, NUNCA
   batched, más la ronda extra DeepSeek en `fare-indicativo-passato-remoto` y
   `fare-indicativo-trapassato-remoto`. Hasta entonces, SC-4 está abierto por diseño y a la vista.
6. `node scripts/run-validation-271.mjs` → sigue reportando su total anterior en PASS. Es la ceguera
   documentada en §Correcciones, no un fallo: el reporter no verá la categoría hasta Phase 44 / INT-02.
7. `git diff --stat src/screens/app.js src/domain/ src/data/` → salida vacía (SC-5).
8. `grep -c 'applyImmediateFailure(this.state' src/screens/app.js` → sigue dando `2` (invariante D-54
   del milestone, verificable por grep).
9. `git status --porcelain` → exactamente los 2 ficheros de `files_modified`.
10. Los 17 items de 41-CONTEXT.md (D-41-01 .. D-41-17) están presentes entre los dos plans: los que
    viven en el `notes` se comprueban leyéndolo (debe nombrar el 0-gloss, el 0-match, el
    0-word-buttons, las 5 formas de la blacklist con su audit trail, el SCOPE-GATE de perífrasis, los
    marcos disjuntos y la nota de count-sync apuntando a Phase 44 / INT-02).
</verification>

<success_criteria>
- IND-05: el autor es examinado sobre `ho fatto`, `avevo fatto` y `avrò fatto` en las 6 personas cada
  uno, eligiendo el TIEMPO DEL AUXILIAR según el marco temporal, con `fatto` invariable y con las 2
  distractoras de auxiliar en la misma persona que la key.
- IND-06: el autor es examinado sobre `ebbi fatto` y familia SIEMPRE dentro de subordinada temporal con
  `dopo che` / `quando` / `appena` (reparto 2+2+2) y la principal en passato remoto.
- SC-2: ninguna variante compuesta admite dos lecturas del marco — garantizado por la exclusión del
  tiempo simple y del auxiliar en imperfetto de las opciones, y confirmado en la ronda extra de quórum.
- SC-3: la explanation del trapassato remoto dice explícitamente que fuera de ese marco la forma no se
  usa, y es honesta sobre el registro.
- SC-4: canon editorial verificado por los sub-tests editoriales, y la coherencia de `validation` con
  `deriveStatus` convertida en gate permanente; el `validated` de los 8 slots lo cierra la pasada
  top-level de quórum, cuyo estado pendiente es visible en `VAL_07_STRICT=1` y nunca fabricado.
- SC-5: la categoría carga y aparece en home, picker, Repaso y Examen sin una línea de motor nueva,
  con la entrada de `categories.json` protegida por un test de regresión.
</success_criteria>

<output>
Crear `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-02-SUMMARY.md` al terminar.
</output>
