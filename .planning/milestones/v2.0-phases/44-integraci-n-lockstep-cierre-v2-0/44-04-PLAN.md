---
phase: 44-integraci-n-lockstep-cierre-v2-0
plan: 04
type: execute
wave: 3
depends_on: [44-02, 44-03]
files_modified:
  - tests/content-fare-indicativo.test.js
  - tests/content-fare-indefiniti.test.js
autonomous: true
gap_closure: true
gap_ids: [G-44-3-WR02, G-44-3-WR03]
requirements: [INT-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- G-44-3-WR02 : el gate deja de contradecir al gate de al lado ---
    - "El gate G1/G2 de `options` de los cruces ya no prohíbe la inicial `f-` a ciegas: RESTA las formas que el propio fichero autoriza. `ESSERE_FORMS` incluye `fui, fosti, fu, fummo, foste, furono` y el gate G1 autoriza EXPLÍCITAMENTE cualquier miembro de `ESSERE_FORMS` como distractora de auxiliar, así que la premisa escrita en el comentario —«en los pools de los cruces no hay ninguna palabra legítima con inicial f-»— es falsa y su contraprueba vive 700 líneas más arriba en el mismo fichero (G-44-3-WR02, D-44-02)."
    - "El predicado nuevo se DERIVA de `ESSERE_FORMS` y no de una lista escrita a mano: el día que la autoría añada a esa whitelist otra forma de `essere` con inicial `f-` (el congiuntivo `fossi`/`fosse`/`fossero`, por ejemplo), la resta la sigue sola. Una whitelist paralela escrita a mano sería la misma clase de duplicado que produjo el falso rojo (G-44-3-WR02)."
    - "El comentario que justificaba la prohibición queda REESCRITO diciendo la verdad —se prohíbe la inicial `f-` MENOS las formas de `essere` que la whitelist de auxiliares autoriza— porque un comentario que afirma una premisa que el fichero desmiente es la deuda que produjo el warning, y dejarlo intacto la deja intacta (G-44-3-WR02)."
    - "La alternación redundante desaparece: equivalía exactamente a la inicial simple porque el resultado solo se usaba vía `.test()`, y una alternación que finge cubrir tres casos cuando cubre uno es una afirmación falsa incrustada en el gate (G-44-3-WR02)."
    - "El mismo arreglo se aplica en `CRUCES_AJENOS` de `tests/content-fare-indefiniti.test.js`, que propagaba el mismo predicado a los cruces de las OTRAS tres categorías y habría roto una segunda suite con un mensaje igualmente falso (`la exclusion de CONJUGATE dejaria de ser inocua`), que no es lo que estaría pasando (G-44-3-WR02, D-44-02)."
    - "La whitelist de `tests/content-fare-indefiniti.test.js` NO puede derivar en silencio de la de `tests/content-fare-indicativo.test.js`: un source-assert lee el literal `ESSERE_FORMS` del fichero hermano, extrae sus formas con inicial `f-` y exige igualdad de conjunto. Si la extracción devuelve la lista vacía, el gate falla diciendo que perdió su fuente de verdad en vez de comparar dos conjuntos vacíos y pasar (G-44-3-WR02)."
    - "El gate de `CRUCES_AJENOS` deja de poder ser vacuo: se asserta que la lista de options de cruces ajenos NO está vacía. Hoy son las 24 options de `fare-indicativo-300` y `-301`; el día que un refactor deje de recolectarlas, el gate pasaría en verde sin haber mirado nada."
    # --- G-44-3-WR03 : el gloss de los cruces, que es el Core Value ---
    - "**`fare-indicativo-301` está FIJADO al 0-gloss y se queda ahí:** ninguno de sus 3 `prompt` contiene paréntesis ni menciona el español en ninguna capitalización. Su resolución documentada fue BORRAR los 3 glosses por leak C5 —el gloss conjugaba el verbo del hueco (`repasamos / comete / revisáis`)— y hasta ahora nada impedía reponerlos con la suite entera en verde. La asimetría que el `notes` declara «deliberada y no uniformable» pasa a tener assert (G-44-3-WR03, D-41-05, R1/C5)."
    - "**La frontera del gloss de `fare-indicativo-300` está codificada:** los 3 conservan su gloss —se exige que exista exactamente un paréntesis por prompt, o el gate sería vacuo— y el contenido de ese paréntesis no contiene ninguna forma del auxiliar castellano `haber`. El pase de Opus lo dejó escrito como condición de supervivencia del slot: los glosses usan el pretérito simple (`hice / hicimos / hicieron`) y con un `he hecho` o un `hemos hecho` el slot pasa a leak R1 inmediato, porque el hueco ES el auxiliar (G-44-3-WR03, R1)."
    - "**El gloss de `fare-indefiniti-300` no traduce el modal:** los 3 conservan su gloss y el contenido del paréntesis no contiene ninguna forma conjugada de los modales castellanos ni la perífrasis de obligación. Un gloss con `tengo que`, `puedo` o `quiero` entregaría el modal examinado, que es exactamente la casilla que el cruce pregunta (G-44-3-WR03, G3, D-44-04)."
    - "Los tres gates son por PARTICIÓN y con lista negra explícita, no un gate uniforme sobre los tres cruces: el 0-gloss rige en `-301` y NO en los otros dos, donde el gloss es legítimo porque glosa el complemento y no la casilla. Un gate uniforme sería falso en las dos direcciones."
    - "La lista negra del modal NO reutiliza la del auxiliar castellano, y viceversa: el gloss de la tercera variante de `fare-indefiniti-300` contiene legítimamente un compuesto con `haber` (`nadie nos lo ha pedido`), así que aplicarle ahí la lista de `fare-indicativo-300` produciría un falso rojo sobre contenido ya validado por quórum — la misma clase de error que WR-02 (G-44-3-WR03)."
    - "Las listas negras casan con FRONTERA DE PALABRA UNICODE y nunca por `includes` crudo: `mucha `, `fecha ` y `hace` contienen la forma del auxiliar como subcadena, y el gloss de la primera variante de `fare-indefiniti-300` contiene `no tengo elección`, que NO es la perífrasis de obligación. Un `includes` a pelo pondría rojo contenido legítimo, que es literalmente el bug que este plan cierra en su otra mitad."
    # --- la propiedad transversal ---
    - "Cada predicado nuevo es una función pura con goldens COMMITEADOS que incluyen el caso positivo, el negativo y al menos un guardia de falso positivo tomado del contenido real en disco, y cada gate real se ha VISTO ROJO por mutación de su propia entrada dentro del test —nunca editando contenido— con revert por `git checkout --` y verde posterior. Un gate que nunca se ha visto rojo no está verificado (CR-01, `[[gate_congela_literal_debe_anclar_disco]]`)."
    - "`content/` termina el plan BYTE-IDÉNTICO: `git status --porcelain content/` no imprime ninguna línea en ningún momento, y el diff del plan lista exactamente los dos ficheros de test. Si un assert nuevo pareciera exigir un cambio de contenido, el assert está mal y se reporta (decisión del autor en el test 3 del UAT: los tres son fixes SOLO de test)."
    - "`node --test tests/*.test.js` cierra con `fail 0` y con un número de tests estrictamente mayor que el medido al abrir el plan en la misma sesión, y `node scripts/run-validation-271.mjs` sigue en `Milestone gate PASS`: ningún gate existente se debilita ni se borra para que los nuevos entren (INT-03)."

  artifacts:
    - path: "tests/content-fare-indicativo.test.js"
      provides: "el predicado de `fare` derivado de la whitelist de auxiliares, y los dos gates de gloss de los cruces"
      contains: "`FORMAS_CON_F_AUTORIZADAS` derivado de `ESSERE_FORMS` con `pareceFare`, el comentario reescrito sin la premisa falsa, los goldens del predicado, el gate del 0-gloss de `fare-indicativo-301` (paréntesis y mención del español) y el gate de la frontera del gloss de `fare-indicativo-300` (existe el gloss + sin auxiliar compuesto castellano) con sus helpers `glosDe` / `auxHaberEn` y sus goldens"
    - path: "tests/content-fare-indefiniti.test.js"
      provides: "la misma resta en el gate de cruces ajenos, con su anclaje a la fuente de verdad, y el gate de gloss del cruce"
      contains: "`FORMAS_CON_F_AUTORIZADAS` local con el source-assert contra el literal `ESSERE_FORMS` del fichero hermano, la cláusula de no-vacuidad de `CRUCES_AJENOS`, y el gate del gloss de `fare-indefiniti-300` con `modalesEsEn` y sus goldens incluido el guardia de falso positivo de `no tengo elección`"

  key_links:
    - "`ESSERE_FORMS` → el gate G1 que autoriza sus miembros como distractora de auxiliar → el predicado de `fare`: es EL enlace que WR-02 encontró roto. Los dos gates viven en el mismo fichero y decían cosas contradictorias sobre la misma palabra; derivar el segundo del primero es lo que los reconcilia de forma permanente."
    - "el gloss ES del prompt → la unicidad de la respuesta: en `fare-indicativo-300` el hueco es el AUXILIAR, así que el auxiliar castellano en el gloss es el espejo exacto de la casilla; en `fare-indefiniti-300` el hueco es el MODAL, así que el modal castellano lo es. El gate de cada cruce mira su propio espejo y no el del vecino."
    - "el gloss del cruce → la cascada D-54: un gloss que regala la casilla no cuesta un ejercicio, cuesta las DOS categorías que el cruce nombra en `categoryIds` (el propio `fare-*` y `avere` / `presente-regolare` / `modali`). Es la razón de que el quórum tratara el leak C5 de `-301` como rechazo real y no como observación."
    - "el `notes` de los dos ficheros → estos gates: el `notes` ya DECLARA las tres fronteras (la asimetría del `-301`, el pretérito simple del `-300`, «el gloss NO traduce el modal»). Lo que faltaba no era la decisión, era el predicado mecánico que la hace incumplible. Los gates no inventan política: ejecutan la escrita."
    - "`CRUCES_AJENOS` → la exclusión de los cruces al construir `CONJUGATE`: el gate existe para que esa exclusión sea auditable y no un acto de fe. Si se queda vacuo o se pone rojo por un falso positivo, la auditoría deja de valer en las dos direcciones."

  prohibitions:
    - "MUST NOT tocar ningún fichero de `content/`, ni transitoriamente. La mutación de verificación se hace sobre la ENTRADA del gate dentro del propio fichero de test (inyectando el defecto en la expresión que el gate recorre), nunca sobre el JSON. Los tres cruces están `validated` por quórum Opus+Sonnet y su contenido no está en discusión."
    - "MUST NOT relajar, borrar ni re-apuntar a la baja ningún gate existente de los dos ficheros: este plan AÑADE cobertura y solo REESCRIBE el predicado que producía un diagnóstico falso. El 0-gloss de los slots base se queda donde está (`BASE_SLOTS`); lo que se añade es su contrapartida por partición en el bloque de cruces, que es lo que la cabecera de ese bloque ya promete."
    - "MUST NOT escribir un gate de gloss uniforme sobre los tres cruces: `fare-indicativo-301` va a 0-gloss y los otros dos conservan gloss por decisión escrita en su `notes`. Un gate uniforme o pondría rojo un gloss legítimo o dejaría reponer el que el quórum obligó a borrar."
    - "MUST NOT aplicar la lista negra del auxiliar castellano al gloss de `fare-indefiniti-300`: su tercera variante contiene legítimamente `nadie nos lo ha pedido`. La frontera de ese cruce es el MODAL, no el auxiliar. Confundirlas reproduce el bug de WR-02 en el mismo commit que lo arregla."
    - "MUST NOT comparar una forma castellana con `includes` crudo: `no tengo elección` no es la perífrasis de obligación y `mucha`/`fecha` no son el auxiliar. Toda comparación va con frontera de palabra unicode, que es la disciplina que `tests/content-fare-indefiniti.test.js` ya declara en mayúsculas en su cabecera."
    - "MUST NOT dejar un gate nuevo que pueda pasar sin haber mirado nada: cada uno lleva su cláusula de no-vacuidad derivada del disco (número de variantes recorridas, existencia del gloss, lista de options no vacía). Es la lección de CR-01 y la razón por la que este plan existe."
    - "MUST NOT cerrar un gate nuevo sin haberlo visto ROJO por mutación, con el mensaje de assert literal pegado en el SUMMARY. Un gate probado solo en verde es una afirmación, no una garantía."
    - "MUST NOT escribir en un assert ninguna cifra transcrita de un `notes`, de un SUMMARY, de este plan o del 44-REVIEW.md: toda cifra se DERIVA del disco al correr."
    - "MUST NOT ampliar el alcance a WR-04…WR-09 ni a IN-01…IN-03 — el autor los aceptó como deuda documentada. En particular NO se toca el conteo de pronombres de los cruces (WR-04), aunque su gate esté a pocas líneas del que se edita, ni se añade la tabla `PERSONA_DE_LA_KEY` de los blockers ya cerrados."
    - "MUST NOT importar un fichero de test desde otro fichero de test: bajo `node --test tests/*.test.js` eso registraría y correría sus `describe` una segunda vez y falsearía el conteo de la suite. El anclaje entre los dos ficheros va por source-assert sobre el TEXTO, que es el idiom que el proyecto ya usa."
    - "MUST NOT tocar `src/`, `scripts/`, `tests/count-arrays-lockstep.test.js` (propiedad del plan 44-03) ni añadir dependencia o paso de build: proyecto de dependencias cero (CLAUDE.md §Constraints)."
---

<objective>
Cerrar **G-44-3-WR02** y **G-44-3-WR03**, los dos gaps que viven en los mismos dos ficheros de test y por
eso van en el mismo plan. Los dos son fixes SOLO de test; el contenido de los 3 cruces está `validated` por
quórum y no se toca.

**WR-02 — el gate que contradice al gate de al lado.** El predicado que prohíbe la inicial `f-` en las
`options` de los cruces se apoya en una premisa que el propio fichero desmiente: `ESSERE_FORMS` incluye
`fui, fosti, fu, fummo, foste, furono` y el gate G1 autoriza EXPLÍCITAMENTE cualquier miembro de esa
whitelist como distractora de auxiliar. Si la autoría usa el passato remoto de `essere` —legítimo y ya en
la whitelist— el gate se pone rojo con el diagnóstico FALSO «mete una forma de fare en options», y arrastra
un tercer fichero: el mismo predicado se propaga a los cruces de las otras tres categorías y rompería la
suite de `fare-indefiniti` con el mensaje igualmente falso «la exclusion de CONJUGATE dejaria de ser
inocua». Se arregla RESTANDO las formas ya autorizadas, derivadas de la whitelist, en vez de prohibir la
inicial a ciegas.

**WR-03 — el gloss de los cruces, que es donde el quórum cazó el leak real.** El 0-gloss se re-apuntó a los
slots base durante 44-02, así que dejó de cubrir los cruces, y el bloque de cruces NO lo re-asserta en
ninguna forma pese a que su cabecera promete «RE-ASERTA los gates que siguen rigiendo sobre ellos». Tres
agujeros, todos sobre el defecto que esta misma fase tuvo que arreglar: `fare-indicativo-301` no está
fijado al 0-gloss (su resolución documentada fue BORRAR los 3 glosses por leak C5, y nada impide
reponerlos con la suite entera en verde); la frontera del gloss de `fare-indicativo-300` no está codificada
(el pase de Opus la dejó escrita como condición de supervivencia: pretérito simple sí, `he hecho` es leak
R1 inmediato); y `fare-indefiniti-300` declara en su `notes` que «el gloss NO traduce el modal» y tampoco
tiene gate.

Purpose: **es el gate que protege el Core Value.** Que no se cuele un ejercicio con dos respuestas
defendibles no es una preocupación abstracta en estos 3 slots: por la cascada D-54 un cruce injusto no
cuesta un ejercicio, cuesta las DOS categorías que nombra en `categoryIds`. Y la ambigüedad de
`fare-indicativo-301` vivía en el PROMPT (el gloss conjugaba el verbo del hueco), no en las `options`, así
que lo que la cerró fue borrar el gloss — y lo que impide que vuelva es un gate que inspeccione el
paréntesis del prompt.

Output: los dos ficheros de test con el predicado de `fare` derivado de su whitelist, los tres gates de
gloss por partición, los predicados nuevos como funciones puras con goldens committeados (incluidos los
guardias de falso positivo tomados del contenido real), y cada gate visto ROJO por mutación de su propia
entrada. Cero cambios en `content/`, `src/` y `scripts/`.

> **Por qué este plan NO va en paralelo con 44-03** (de ahí el `depends_on`): los dos verifican por
> MUTACIÓN, es decir dejan el árbol transitoriamente roto a propósito y corren la suite entera para
> observar el rojo. En paralelo, la suite de un plan vería el rojo transitorio del otro y el diagnóstico
> dejaría de significar nada. La serialización es del arnés de verificación, no de las dependencias de
> código: 44-03 toca un fichero de test distinto y no produce nada que este plan consuma.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-UAT.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-REVIEW.md
@.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Verificado contra el disco el 2026-08-12. Lee SOLO estas regiones; los dos ficheros suman 3.274 líneas y
leerlos enteros no aporta nada que no esté aquí.

**`tests/content-fare-indicativo.test.js`** (1.048 líneas):
- 95-123: `CROSS_PAIRS`, `BASE_SLOTS` / `CROSS_SLOTS`, `OBJECT_PRONOUN_RE`, y el comentario + el predicado
  de inicial `f-` con la premisa FALSA. Es lo que se reescribe.
- 218-223: `ESSERE_FORMS` — la fuente de verdad de la resta. **Está DESPUÉS del predicado, así que el
  `new Set(...)` derivado NO puede vivir en la línea 123: sería un `ReferenceError` de TDZ al evaluar el
  módulo.** El derivado va justo detrás de esta declaración.
- 344-368: el `describe` del 0-gloss, ya re-apuntado a los slots base vía `allVariants()`. Es el gate cuya
  contrapartida por partición falta.
- 769-953: el bloque 13 (cruces). Su cabecera promete re-asertar los gates que siguen rigiendo. Ahí viven
  `personaDelHueco`, el gate CR-02 de la key, y en 948-953 el gate G1/G2 que consume el predicado.

**`tests/content-fare-indefiniti.test.js`** (2.226 líneas):
- 101-137: `byId`, `CROSS_IDS`, `CROSS_PAIRS`, `BASE_SLOTS` / `CROSS_SLOTS`, `allVariants()`,
  `crossVariants()`.
- 139-166: `wordish` — el matcher con FRONTERA DE PALABRA UNICODE, con su comentario explicando por qué
  ningún cue de ese fichero se compara con `includes`. **Es el helper a reutilizar para las listas negras
  de gloss; el fichero de `fare-indicativo` no tiene equivalente y necesita uno.**
- 240-275: el comentario de la exclusión de los cruces al construir `CONJUGATE`, y `CRUCES_AJENOS`.
- 823-843: el 0-gloss de la categoría, re-apuntado a los slots base (mismo agujero que en el hermano).
- 980-992: el gate «EN POSITIVO» sobre `CRUCES_AJENOS` que propaga el predicado de inicial `f-`.
- 2068-2226: el bloque del cruce `-300`, con `COMPLEMENTOS_QUE_EXCLUYEN`, `MODAL_STEM_RE` y su
  `PRONOUN_RE` local. Ahí va el gate de gloss del modal.

**El contenido REAL de los 3 cruces** (leído del disco el 2026-08-12) — los gates nuevos tienen que quedar
VERDES sobre esto, y estos son los guardias de falso positivo:

`fare-indicativo-300` (hueco = auxiliar de `avere`; gloss en PRETÉRITO SIMPLE):
```
Stamattina io ___ fatto i compiti prima di uscire. (en español: esta mañana hice los deberes antes de salir)
Ieri noi ___ fatto una torta per il compleanno di Marco. (en español: ayer hicimos un pastel para el cumpleaños de Marco)
Il mese scorso loro ___ fatto tutto senza aiuto. (en español: el mes pasado hicieron todo sin ayuda)
```

`fare-indicativo-301` (hueco = presente regular; **CERO paréntesis, es el 0-gloss que el quórum impuso**):
```
Tu fai i compiti da solo, ma noi ___ tutto insieme.
Io faccio una foto senza problemi, ma lui ___ un errore ogni volta.
Noi facciamo il letto ogni mattina, e voi ___ il lavoro con calma.
```

`fare-indefiniti-300` (hueco = modal conjugado; el gloss glosa el COMPLEMENTO, nunca el modal):
```
Domani io ___ fare i compiti: è un obbligo della scuola, non ho scelta, altrimenti prendo un brutto voto. (en español: es una obligación del colegio, no tengo elección; si no, saco mala nota)
Qui tu ___ fare una foto, perché il museo dà il permesso. (en español: porque el museo da su autorización)
Sabato noi ___ fare una torta per il compleanno di Marco: è un desiderio nostro, nessuno ce lo ha chiesto. (en español: es un deseo nuestro, nadie nos lo ha pedido)
```

> **Los dos guardias de falso positivo que hay que respetar, o el plan rompe contenido validado:**
> el gloss del primer prompt de `fare-indefiniti-300` contiene `no tengo elección` —que NO es la perífrasis
> de obligación— y el del tercero contiene `nadie nos lo ha pedido` —un compuesto con `haber` perfectamente
> legítimo, porque la frontera de ESE cruce es el modal y no el auxiliar.

**Los fixes que el reviewer dejó escritos** (`44-REVIEW.md` §§ WR-02 y WR-03), punto de partida y no copia
literal: al de WR-02 le falta el anclaje de la whitelist del segundo fichero, y al de WR-03 le falta la
cláusula de existencia del gloss (sin ella, borrar el gloss deja el gate en verde sobre una cadena vacía) y
la frontera de palabra:

```js
// WR-02
const FORMAS_PERMITIDAS_CON_F = new Set(ESSERE_FORMS.filter((f) => /^f/i.test(f))); // fui, fosti, fu, fummo, foste, furono
const pareceFare = (w) => /^f/i.test(w) && !FORMAS_PERMITIDAS_CON_F.has(w.toLowerCase());

// WR-03
test('gloss de los cruces: puede glosar el complemento, NUNCA la casilla examinada (R1)', () => {
  for (const v of byId('fare-indicativo-301').variants) {
    assert.ok(!/[()]/.test(v.prompt), `C5-leak: el -301 recupero el gloss que el quorum obligo a borrar: "${v.prompt}"`);
  }
  const AUX_ES = ['he ', 'has ', 'ha ', 'hemos ', 'habéis ', 'han ', 'había', 'habré'];
  for (const v of byId('fare-indicativo-300').variants) {
    const gloss = (v.prompt.match(/\(([^)]*)\)/) || ['', ''])[1].toLowerCase();
    const sucio = AUX_ES.filter((a) => gloss.includes(a));
    assert.deepEqual(sucio, [], `R1: el gloss exhibe el auxiliar castellano espejo del hueco: ${sucio.join(', ')}`);
  }
});
```

> Ojo: `AUX_ES` con espacio final y `gloss.includes(...)` es un `includes` crudo — pondría rojo un gloss
> futuro con `mucha gente` o `la fecha de hoy`. La comparación va con frontera de palabra unicode, igual
> que `wordish` en el fichero hermano.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tarea 1 (tracer): el predicado de `fare` derivado de la whitelist de auxiliares, en los DOS ficheros (WR-02)</name>
  <files>tests/content-fare-indicativo.test.js, tests/content-fare-indefiniti.test.js</files>
  <behavior>
    Predicado puro `pareceFare(palabra)`, con goldens committeados en los dos ficheros:
    - `pareceFare('fatto')`, `pareceFare('faccio')`, `pareceFare('farò')` → `true` (sigue mordiendo).
    - `pareceFare('fui')`, `pareceFare('fosti')`, `pareceFare('fu')`, `pareceFare('fummo')`,
      `pareceFare('foste')`, `pareceFare('furono')` → `false` (las autoriza G1: eran el falso rojo).
    - `pareceFare('Fu')` → `false` (la resta no depende de la capitalización).
    - `pareceFare('ho')`, `pareceFare('abbiamo')`, `pareceFare('devo')` → `false`.
    - Todo miembro de la whitelist derivada pertenece a `ESSERE_FORMS`, y la whitelist NO está vacía.
    En `tests/content-fare-indefiniti.test.js`, además:
    - El source-assert del anclaje: las formas con inicial `f-` extraídas del literal `ESSERE_FORMS` del
      fichero hermano son EXACTAMENTE la whitelist local, y la extracción devuelve una lista no vacía.
    - `CRUCES_AJENOS` no está vacío (hoy son las options de los 2 cruces de `fare-indicativo`).
    Mutación: inyectando `'fatto'` en la lista que cada gate recorre, los dos gates se ponen ROJOS
    nombrando `fatto`; inyectando `'fui'`, los dos siguen VERDES.
  </behavior>
  <action>
    Paso 1 — `tests/content-fare-indicativo.test.js`. Sustituye el predicado de inicial de la región 114-123
    por un puntero: el comentario se REESCRIBE para decir la verdad (se prohíbe la inicial `f-` MENOS las
    formas de `essere` que la whitelist de auxiliares de este mismo fichero autoriza como distractora, y
    por eso el predicado vive detrás de esa whitelist), y la definición pasa a ir justo DESPUÉS de la
    declaración de `ESSERE_FORMS`. Ese traslado es obligatorio y no cosmético: derivar un `Set` de
    `ESSERE_FORMS` antes de su declaración es un `ReferenceError` de TDZ al evaluar el módulo, y el
    fichero entero dejaría de correr. Deja la inicial simple: la alternación de tres ramas equivalía
    exactamente a la simple porque el resultado solo se consume vía prueba booleana.

    Paso 2 — Escribe los goldens del predicado ANTES de cablearlo al gate real, en un `describe` propio
    colocado inmediatamente antes del bloque 13, que es su único consumidor. Incluye el caso positivo, los
    seis casos que producían el falso rojo, el de capitalización, la no-vacuidad de la whitelist derivada y
    la pertenencia de cada miembro a `ESSERE_FORMS`. Corre el fichero.

    Paso 3 — Cablea el gate G1/G2 de `options` de los cruces (región 948-953) al predicado nuevo y ajusta
    su mensaje para que siga siendo verdadero. Corre el fichero: verde.

    Paso 4 — `tests/content-fare-indefiniti.test.js`. Declara la whitelist local y su `pareceFare` junto a
    `CRUCES_AJENOS`, y añade en el mismo `describe` que aloja el gate «EN POSITIVO» (región 980-992) tres
    cosas: los mismos goldens del predicado; el source-assert que lee el TEXTO del fichero hermano,
    extrae el literal de la whitelist de auxiliares, filtra sus formas con inicial `f-` y exige igualdad de
    conjunto con la local, fallando de forma explícita si la extracción devuelve la lista vacía (perdió su
    fuente de verdad); y la cláusula de no-vacuidad de `CRUCES_AJENOS`. El anclaje va por source-assert y
    NUNCA por import: importar un fichero de test desde otro haría correr sus `describe` dos veces bajo el
    glob de la suite.

    Paso 5 — Cablea el gate «EN POSITIVO» al predicado local y reescribe su comentario, que hoy afirma la
    misma premisa falsa que el hermano. Corre los dos ficheros y la suite entera: verde.

    Paso 6 — COMMITEA antes de mutar (el revert va por `git checkout --`).

    Paso 7 — MUTACIÓN, sobre la ENTRADA del gate y nunca sobre contenido. En cada uno de los dos ficheros,
    inyecta temporalmente en la lista que el gate recorre una option sintética con la forma de `fare`
    (concatenando `'fatto'` a la lista de options que el bucle inspecciona) y comprueba que el gate se pone
    ROJO nombrando `fatto`. Después repite la inyección con `'fui'` y comprueba que el gate se queda VERDE:
    ese segundo paso es la prueba directa de que el falso rojo de WR-02 quedó cerrado, y es tan importante
    como el rojo. Revierte con `git checkout -- <fichero>` tras cada mutación, comprueba verde, y pega en
    el SUMMARY los dos mensajes rojos y la observación de los dos verdes.
  </action>
  <verify>
    <automated>node --test tests/content-fare-indicativo.test.js tests/content-fare-indefiniti.test.js 2>&amp;1 | tail -8</automated>
    <automated>node --test tests/*.test.js 2>&amp;1 | tail -8</automated>
    <automated>node -e "const fs=require('fs');for(const p of ['tests/content-fare-indicativo.test.js','tests/content-fare-indefiniti.test.js']){const s=fs.readFileSync(p,'utf8');console.log(p,/pareceFare/.test(s),/ESSERE_FORMS/.test(s))}"</automated>
    <automated>git status --porcelain content/ src/ scripts/ | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-indicativo.test.js` y `node --test tests/content-fare-indefiniti.test.js` cierran los dos con `fail 0`, y `node --test tests/*.test.js` también.
    - El número de tests de cada uno de los dos ficheros es estrictamente mayor que el medido antes de la tarea (valores previos registrados en el SUMMARY, medidos en la misma sesión).
    - La whitelist se DERIVA y no se escribe a mano en `tests/content-fare-indicativo.test.js`: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8');console.log(/new Set\(\s*ESSERE_FORMS/.test(s)||/ESSERE_FORMS\s*\.filter/.test(s))"` imprime `true`.
    - El predicado está declarado DESPUÉS de su fuente: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8');console.log(s.indexOf('const ESSERE_FORMS') < s.indexOf('pareceFare'))"` imprime `true` (si diera `false`, el módulo lanzaría `ReferenceError` de TDZ y ningún test correría).
    - El anclaje va por LECTURA de texto y no por import — comprobado en positivo, que es lo que hace el criterio auto-verificable: `node -e "const s=require('fs').readFileSync('tests/content-fare-indefiniti.test.js','utf8');console.log(/readFileSync\(\s*new URL\(\s*'\.\/content-fare-indicativo\.test\.js'/.test(s))"` imprime `true`. Y el conteo de tests de la suite completa crece en el número exacto de tests nuevos escritos (si el hermano se hubiera importado, crecería el doble en sus 33 suites y el criterio lo delataría).
    - MUTACIÓN de rojo: con `'fatto'` inyectado en la entrada del gate, cada uno de los dos ficheros sale con código distinto de 0 y su mensaje contiene `fatto`. Mensajes literales en el SUMMARY.
    - MUTACIÓN de verde (el cierre de WR-02): con `'fui'` inyectado en la entrada del gate, los dos ficheros siguen en `fail 0`. Observación registrada en el SUMMARY.
    - Tras los reverts, `git status --porcelain content/ src/ scripts/` no imprime ninguna línea.
  </acceptance_criteria>

  <done>Los dos ficheros derivan el predicado de `fare` de la whitelist de auxiliares en vez de prohibir la inicial a ciegas, el predicado está declarado detrás de su fuente (sin TDZ), los comentarios ya no afirman la premisa falsa, la whitelist del segundo fichero está anclada por source-assert al literal del primero, `CRUCES_AJENOS` no puede quedar vacuo, y el gate se ha visto ROJO con una forma de `fare` y VERDE con el passato remoto de `essere`.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: los dos gates de gloss de los cruces de `fare-indicativo` — 0-gloss del `-301` y frontera del `-300` (WR-03)</name>
  <files>tests/content-fare-indicativo.test.js</files>
  <behavior>
    Helpers puros con goldens committeados:
    - `glosDe('… ___ … (en español: ayer hicimos un pastel)')` → devuelve el contenido del paréntesis.
    - `glosDe('Tu fai i compiti da solo, ma noi ___ tutto insieme.')` → devuelve nulo (no hay gloss).
    - `auxHaberEn('esta mañana hice los deberes antes de salir')` → lista vacía.
    - `auxHaberEn('esta mañana he hecho los deberes')` → contiene la forma del auxiliar.
    - `auxHaberEn('ayer hemos hecho un pastel')` → contiene la forma del auxiliar.
    - Guardias de falso positivo: `auxHaberEn('mucha gente en la fecha de hoy')` y
      `auxHaberEn('hace tiempo que no')` → lista vacía. Es lo que distingue la frontera de palabra de un
      `includes` crudo.
    Gate A — `fare-indicativo-301` fijado al 0-gloss:
    - Ninguno de sus `prompt` contiene paréntesis, y ninguno menciona el español en ninguna
      capitalización (el gloss podría reponerse sin paréntesis).
    - Cláusula de no-vacuidad: el número de variantes recorridas es mayor que cero, derivado del disco.
    Gate B — `fare-indicativo-300` conserva gloss y respeta su frontera:
    - Cada `prompt` tiene EXACTAMENTE un paréntesis de apertura y `glosDe` devuelve contenido no vacío
      (sin esta cláusula, borrar el gloss dejaría el gate en verde sobre una cadena vacía).
    - `auxHaberEn(gloss)` es la lista vacía en las tres variantes.
    Mutaciones: añadiendo un paréntesis a los prompts que el gate A recorre, A se pone rojo; prefijando
    la forma del auxiliar al gloss que el gate B recorre, B se pone rojo; vaciando el gloss que B
    recorre, B se pone rojo por la cláusula de existencia y no por la lista negra.
  </behavior>
  <action>
    Paso 1 — Escribe los helpers y sus goldens ANTES de los gates, en un `describe` propio dentro del
    bloque 13 o inmediatamente antes: `glosDe(prompt)` devuelve el contenido del primer paréntesis o nulo,
    y `auxHaberEn(gloss)` devuelve las formas del auxiliar castellano `haber` presentes en el gloss. La
    lista negra de `haber` enumera las formas compuestas que pueden aparecer en un gloss de este slot
    (presente, imperfecto, futuro y pretérito de `haber`, en su ortografía acentuada RAE y también sin
    tilde por defensa), y cada comparación va con FRONTERA DE PALABRA UNICODE clonando el matcher del
    fichero hermano: el fichero de `fare-indicativo` no tiene ese helper y necesita uno. Los goldens
    incluyen obligatoriamente los dos guardias de falso positivo, que son lo único que distingue este gate
    del `includes` crudo que el reviewer esbozó.

    Paso 2 — Gate A, el 0-gloss de `fare-indicativo-301`. Va dentro del bloque 13, que es el que promete
    re-asertar los gates que siguen rigiendo sobre los cruces. Recorre las variantes del slot exigiendo
    ausencia de paréntesis y ausencia de mención del español, con una cláusula previa de no-vacuidad sobre
    el número de variantes leídas del disco. El mensaje de assert tiene que decir POR QUÉ está rojo: el
    `-301` recuperó el gloss que el quórum obligó a borrar por leak C5, porque el gloss conjugaba el verbo
    del hueco. Deja escrito en el comentario que esta asimetría es la que el `notes` declara deliberada y
    no uniformable, y que por eso el gate es por partición: el `-300` de al lado SÍ conserva gloss.

    Paso 3 — Gate B, la frontera del gloss de `fare-indicativo-300`. Dos cláusulas por variante y en este
    orden: primero que el gloss EXISTE —exactamente un paréntesis y contenido no vacío—, porque sin ella
    borrar el gloss dejaría la lista negra pasando sobre una cadena vacía; después que el gloss no exhibe
    ninguna forma del auxiliar castellano. El comentario explica la frontera tal como el pase de Opus la
    dejó escrita: los glosses usan el pretérito simple, y con un compuesto castellano el slot pasa a leak
    R1 inmediato porque el hueco ES el auxiliar italiano y el gloss sería su espejo.

    Paso 4 — Corre el fichero y la suite entera: verde. Deja constancia en el SUMMARY de que los 3 prompts
    del `-300` conservan su gloss en pretérito simple y los 3 del `-301` siguen sin paréntesis, medido del
    disco. COMMITEA antes de mutar.

    Paso 5 — MUTACIÓN, sobre la entrada de cada gate y NUNCA sobre el JSON de contenido. Para A: añade
    temporalmente un paréntesis a la cadena que el bucle inspecciona y comprueba el rojo con el mensaje del
    leak C5. Para B: prefija temporalmente la forma del auxiliar al gloss que el bucle inspecciona y
    comprueba el rojo nombrando la forma; después vacía temporalmente ese gloss y comprueba que el rojo
    llega por la cláusula de existencia y no por la lista negra. Revierte con `git checkout --` tras cada
    una, comprueba verde y pega los tres mensajes rojos en el SUMMARY.
  </action>
  <verify>
    <automated>node --test tests/content-fare-indicativo.test.js 2>&amp;1 | tail -8</automated>
    <automated>node --test tests/*.test.js 2>&amp;1 | tail -8</automated>
    <automated>node -e "const d=require('./content/exercises/fare-indicativo.json');const g=id=>d.exercises.find(e=>e.id===id).variants.map(v=>(v.prompt.match(/\(([^)]*)\)/)||[null,null])[1]);console.log('301:',JSON.stringify(g('fare-indicativo-301')));console.log('300:',JSON.stringify(g('fare-indicativo-300')))"</automated>
    <automated>git status --porcelain content/ src/ scripts/ | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-indicativo.test.js` y `node --test tests/*.test.js` cierran con `fail 0`, y el conteo del fichero es estrictamente mayor que el medido al empezar la tarea.
    - El gate A existe y nombra el slug completo: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8');console.log(s.split(\"'fare-indicativo-301'\").length-1)"` imprime un número mayor que antes de la tarea (registrar el previo).
    - Los helpers son puros y tienen goldens: la salida de `node --test tests/content-fare-indicativo.test.js` contiene líneas `ok` con los nombres de los goldens de `glosDe` y del auxiliar, incluidos los dos guardias de falso positivo.
    - El gate B tiene la cláusula de EXISTENCIA del gloss, no solo la lista negra: `node -e "const s=require('fs').readFileSync('tests/content-fare-indicativo.test.js','utf8');console.log(/glosDe/.test(s))"` imprime `true`, y el SUMMARY documenta la mutación de gloss vacío con su rojo.
    - La comparación NO es por `includes` crudo: los goldens `mucha gente en la fecha de hoy` y `hace tiempo que no` devuelven lista vacía y están committeados.
    - Estado del contenido, sin cambiarlo: el comando del tercer bloque de verify imprime `301: [null,null,null]` y tres glosses no nulos para `300`. Es la foto del disco al cerrar la tarea, fechada en el SUMMARY.
    - MUTACIONES: tres rojos observados (paréntesis en el `-301`; auxiliar en el gloss del `-300`; gloss vacío en el `-300`) con sus mensajes literales en el SUMMARY, y verde tras cada revert.
    - `git status --porcelain content/ src/ scripts/` no imprime ninguna línea.
  </acceptance_criteria>

  <done>El `-301` está fijado al 0-gloss con un gate que nombra el leak C5 en su mensaje, el `-300` tiene codificada su frontera con la cláusula de existencia del gloss por delante de la lista negra, los helpers son puros con goldens que incluyen los guardias de falso positivo, y los tres rojos se han observado por mutación de la entrada del gate sin tocar una línea de contenido.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: el gate de gloss de `fare-indefiniti-300` — el gloss no traduce el modal (WR-03)</name>
  <files>tests/content-fare-indefiniti.test.js</files>
  <behavior>
    Helper puro `modalesEsEn(gloss)` con goldens committeados:
    - `modalesEsEn('tengo que hacer los deberes')` → contiene la perífrasis de obligación.
    - `modalesEsEn('puedo hacer una foto')` → contiene la forma de permiso.
    - `modalesEsEn('queremos hacer un pastel')` → contiene la forma de voluntad.
    - `modalesEsEn('debemos hacer los deberes')` → contiene la forma de obligación.
    - Guardia de falso positivo tomado del contenido real: `modalesEsEn('es una obligación del colegio, no
      tengo elección; si no, saco mala nota')` → lista vacía. `no tengo elección` NO es la perífrasis.
    - Segundo guardia, también del contenido real: `modalesEsEn('es un deseo nuestro, nadie nos lo ha
      pedido')` → lista vacía. El compuesto con `haber` es legítimo aquí: la frontera de este cruce es el
      MODAL, no el auxiliar.
    Gate: cada variante de `fare-indefiniti-300` tiene EXACTAMENTE un gloss no vacío (cláusula de
    existencia, o el gate sería vacuo) y `modalesEsEn(gloss)` es la lista vacía en las tres.
    Mutaciones: prefijando la perífrasis de obligación al gloss que el gate recorre, se pone rojo
    nombrándola; vaciando el gloss, se pone rojo por la cláusula de existencia.
  </behavior>
  <action>
    Paso 1 — Escribe `modalesEsEn(gloss)` y sus goldens ANTES del gate, dentro del `describe` del cruce
    `-300` (región 2068-2226), junto a `COMPLEMENTOS_QUE_EXCLUYEN` y `MODAL_STEM_RE`, que son sus vecinos
    conceptuales: son las constantes del cruce y no de la categoría. La lista negra enumera las formas
    conjugadas castellanas de los tres modales que el cruce examina —obligación, permiso y voluntad— más la
    perífrasis de obligación con su auxiliar, en su ortografía acentuada RAE y también sin tilde por
    defensa. Cada comparación va con el `wordish` del propio fichero, que es la disciplina que su cabecera
    declara en mayúsculas: la perífrasis se compara como bigrama completo y NUNCA por su primera palabra
    suelta, porque el gloss real de la primera variante contiene `no tengo elección` y compararlo por la
    palabra suelta pondría rojo contenido ya validado por quórum. Los dos guardias de falso positivo
    tomados del contenido real son obligatorios en los goldens.

    Paso 2 — Añade el gate con las dos cláusulas en orden: primero que el gloss EXISTE (exactamente un
    paréntesis, contenido no vacío) con la no-vacuidad del número de variantes derivada del disco, después
    que no aparece ninguna forma de la lista negra. El mensaje de assert dice que el gloss entregaría el
    modal examinado, que es la casilla que el cruce pregunta y por la que la cascada D-54 resetea
    `fare-indefiniti` y `modali` a la vez. El comentario cita la frontera tal como el `notes` la declara
    —el gloss NO traduce el modal, glosa el complemento que desambigua la intención— y deja escrito por qué
    la lista negra de este cruce NO es la de `fare-indicativo-300`: aquí un compuesto con `haber` en el
    gloss es legítimo y allí es el leak.

    Paso 3 — Corre el fichero y la suite entera: verde. COMMITEA antes de mutar.

    Paso 4 — MUTACIÓN, sobre la entrada del gate y nunca sobre el JSON: prefija temporalmente la perífrasis
    de obligación al gloss que el bucle inspecciona y comprueba el rojo nombrándola; después vacía
    temporalmente ese gloss y comprueba que el rojo llega por la cláusula de existencia. Revierte con
    `git checkout --` tras cada una, comprueba verde y pega los dos mensajes rojos en el SUMMARY.

    Paso 5 — Cierre del plan: corre la suite entera y el reporter, comprueba que `git status --porcelain`
    no menciona `content/`, `src/` ni `scripts/`, y registra en el SUMMARY la tabla de los tres gaps
    cerrados con el conteo de tests de apertura y de cierre, ambos MEDIDOS en esta sesión.
  </action>
  <verify>
    <automated>node --test tests/content-fare-indefiniti.test.js 2>&amp;1 | tail -8</automated>
    <automated>node --test tests/*.test.js 2>&amp;1 | tail -8</automated>
    <automated>node -e "const d=require('./content/exercises/fare-indefiniti.json');console.log(JSON.stringify(d.exercises.find(e=>e.id==='fare-indefiniti-300').variants.map(v=>(v.prompt.match(/\(([^)]*)\)/)||[null,null])[1]),null,1))"</automated>
    <automated>node scripts/run-validation-271.mjs 2>&amp;1 | tail -4; echo "reporter-exit=$?"; git status --porcelain content/ src/ scripts/ | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-indefiniti.test.js` y `node --test tests/*.test.js` cierran con `fail 0`, y los dos conteos son estrictamente mayores que los medidos al abrir el plan (valores de apertura y cierre en el SUMMARY, medidos en la misma sesión, nunca transcritos).
    - El helper existe y está junto a las constantes del cruce: `node -e "const s=require('fs').readFileSync('tests/content-fare-indefiniti.test.js','utf8');console.log(/modalesEsEn/.test(s), s.indexOf('COMPLEMENTOS_QUE_EXCLUYEN') < s.indexOf('modalesEsEn'))"` imprime `true true`.
    - La comparación va con frontera de palabra: `node -e "const s=require('fs').readFileSync('tests/content-fare-indefiniti.test.js','utf8');const i=s.indexOf('modalesEsEn');console.log(/wordish/.test(s.slice(i-400,i+800)))"` imprime `true`.
    - Los dos guardias de falso positivo tomados del contenido real están COMMITEADOS como goldens y en verde: la salida del fichero contiene sus líneas `ok`.
    - Estado del contenido, sin cambiarlo: el comando del tercer bloque de verify imprime los tres glosses reales del cruce (los que hoy contienen `no tengo elección`, `da su autorización` y `nadie nos lo ha pedido`), foto fechada en el SUMMARY.
    - MUTACIONES: dos rojos observados (perífrasis de obligación inyectada; gloss vaciado) con sus mensajes literales en el SUMMARY, y verde tras cada revert.
    - `node scripts/run-validation-271.mjs` imprime su `Milestone gate PASS` y `git status --porcelain content/ src/ scripts/` no imprime ninguna línea.
  </acceptance_criteria>

  <done>`fare-indefiniti-300` tiene su gate de gloss por partición con la cláusula de existencia por delante de la lista negra del modal, la lista negra no confunde su frontera con la del cruce de `fare-indicativo`, los dos guardias de falso positivo tomados del contenido real están committeados en verde, los dos rojos se han observado por mutación, y los tres gaps del cierre (WR-01, WR-02, WR-03) quedan cerrados con `content/` byte-idéntico y el reporter en `Milestone gate PASS`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido autorado a mano → arnés de test | Los gates leen `content/exercises/*.json` del disco y asertan sobre `variants[].prompt` y `variants[].options[]`. El escaneo va POR CAMPO y nunca sobre el fichero completo (D-42-11): el `notes` nombra a propósito las formas prohibidas y un grep de fichero entero se auto-invalidaría. |
| `tests/content-fare-indicativo.test.js` → `tests/content-fare-indefiniti.test.js` | Anclaje de la whitelist por source-assert sobre TEXTO. Nunca por import: bajo el glob de la suite, importar un fichero de test desde otro correría sus `describe` dos veces. |
| árbol de git → mutación transitoria de verificación | Las mutaciones se hacen sobre la ENTRADA del gate dentro del fichero de test, jamás sobre `content/`. |
| (sin red, sin entrada de usuario, sin instalación de paquetes) | Web estática de dependencias cero, single-user local (CLAUDE.md §Constraints). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-04-01 | Tampering | gate de gloss sobre una cadena vacía | critical | mitigate | Si el gloss se borra, la lista negra pasa en verde sobre `''` y el gate certifica lo contrario de lo que dice: es el modo de fallo de CR-01 aplicado al Core Value. Cláusula de EXISTENCIA del gloss (exactamente un paréntesis, contenido no vacío) por DELANTE de la lista negra, más mutación de gloss vaciado exigiendo el rojo por esa cláusula. |
| T-44-04-02 | Tampering | lista negra por `includes` crudo | high | mitigate | `mucha `/`fecha ` contienen la forma del auxiliar y `no tengo elección` contiene la primera palabra de la perífrasis: un `includes` pondría rojo contenido validado y el arreglo siguiente sería relajar el gate. Frontera de palabra unicode obligatoria más goldens de falso positivo tomados del contenido REAL. |
| T-44-04-03 | Tampering | whitelist duplicada entre los dos ficheros | high | mitigate | Una whitelist paralela escrita a mano deriva en silencio y reproduce el falso rojo de WR-02 en el otro fichero. Source-assert de igualdad de conjunto contra el literal del hermano, con fallo explícito si la extracción devuelve lista vacía. |
| T-44-04-04 | Tampering | mutación transitoria del fichero de test | high | mitigate | Commit del gate ANTES de mutar, revert por `git checkout -- <fichero>`, y `git status --porcelain content/ src/ scripts/` vacío como criterio de aceptación. Prohibición explícita de mutar `content/` ni siquiera transitoriamente. |
| T-44-04-05 | Elevation of privilege | un gate nuevo que baja la barra de uno existente | high | mitigate | Los gates nuevos son ADITIVOS y por partición; el único predicado reescrito es el que emitía un diagnóstico falso. Criterio de aceptación: el conteo de tests de cada fichero crece y la suite entera queda en `fail 0`, con el reporter en `Milestone gate PASS`. |
| T-44-04-06 | Repudiation | gate cerrado sin evidencia de rojo | high | mitigate | Cada gate exige su mensaje de assert rojo literal pegado en el SUMMARY, más goldens committeados que sobreviven a la sesión. Un gate que nunca se ha visto rojo no está verificado. |
| T-44-04-SC | Tampering | npm/pip/cargo installs | high | accept | Este plan NO instala nada: proyecto de dependencias cero, runner nativo de Node. No hay tabla de legitimidad que consultar porque no hay paquete. Si una tarea futura introdujera un install, aplica el gate de legitimidad con checkpoint humano bloqueante. |
</threat_model>

<verification>
1. `node --test tests/content-fare-indicativo.test.js` y `node --test tests/content-fare-indefiniti.test.js` → `fail 0` los dos.
2. `node --test tests/*.test.js` → `fail 0`, con `pass` estrictamente mayor que el medido al abrir el plan en la misma sesión (apertura y cierre registrados en el SUMMARY).
3. `node scripts/run-validation-271.mjs` → `Milestone gate PASS`.
4. `git status --porcelain content/ src/ scripts/` → sin líneas. El diff del plan lista exactamente los dos ficheros de test.
5. Siete mutaciones documentadas con su mensaje de assert literal: `fatto` en los dos gates de options (2 rojos) y `fui` en los dos (2 verdes, el cierre de WR-02); paréntesis en el `-301`, auxiliar y gloss vacío en el `-300`, perífrasis y gloss vacío en `fare-indefiniti-300`.
6. Foto del disco fechada en el SUMMARY: los 3 prompts del `-301` sin paréntesis, los 3 del `-300` con gloss en pretérito simple, los 3 de `fare-indefiniti-300` con gloss que no traduce el modal — todo LEÍDO del disco, nunca transcrito.
</verification>

<success_criteria>
- G-44-3-WR02 cerrado: el gate de options resta las formas que la whitelist de auxiliares autoriza en vez de prohibir la inicial a ciegas, en los DOS ficheros, con la whitelist del segundo anclada por source-assert al literal del primero. Verificado en las dos direcciones: rojo con una forma de `fare`, verde con el passato remoto de `essere`.
- G-44-3-WR03 cerrado con TRES gates por partición: el `-301` fijado al 0-gloss, la frontera del gloss del `-300` codificada, y el gloss de `fare-indefiniti-300` sin el modal. Cada uno con cláusula de existencia o de no-vacuidad por delante, para que no pueda pasar sin haber mirado nada.
- Cada predicado nuevo es puro y tiene goldens COMMITEADOS con al menos un guardia de falso positivo tomado del contenido real: la prueba de que el gate muerde y de que no muerde de más sobrevive a la sesión.
- Ninguna cifra asertada está transcrita de prosa o de un `notes`: todas se derivan del disco al correr.
- Cero cambios en `content/`, `src/` y `scripts/`. Suite entera verde y reporter en `Milestone gate PASS`.
</success_criteria>

<output>
Create `.planning/phases/44-integraci-n-lockstep-cierre-v2-0/44-04-SUMMARY.md` when done
</output>
