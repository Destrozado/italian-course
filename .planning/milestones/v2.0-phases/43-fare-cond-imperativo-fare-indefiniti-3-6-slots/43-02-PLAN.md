---
phase: 43-fare-cond-imperativo-fare-indefiniti-3-6-slots
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - content/exercises/fare-indefiniti.json
  - content/categories.json
  - tests/exercise-types.test.js
  - tests/content-fare-indefiniti.test.js
  - .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md
  - .claude/skills/gsd-validate-exercise/SKILL.md
  - .claude/skills/gsd-validate-batch/SKILL.md
autonomous: false
requirements: [INDEF-01, INDEF-02, INDEF-03, INDEF-04]

estimate:
  tokens: 132000
  raw_tokens: 132000
  tasks: 5
  confidence: low

must_haves:
  truths:
    # --- SC-5 / D-43-22 : el registro que hace existir la segunda categoría ---
    - "`content/categories.json` contiene exactamente UNA entrada nueva más respecto al cierre de 43-01, con las 4 claves `id`, `name`, `order`, `origen`, valores `id: \"fare-indefiniti\"`, `order: 18`, `origen: \"ia-quorum\"`, y las 17 entradas previas quedan byte-intactas (D-43-22, SC-5)."
    - "El slug es byte a byte `fare-indefiniti` en los cuatro sitios que el contrato de Phase 40 vincula: nombre del fichero de contenido, `id` de `content/categories.json`, prefijo de los 6 ids de slot, y el elemento ya existente de `RESET_PREFIXES_V13` (`src/data/storage.js:1345`, que esta fase NO edita). Toda comprobación de prefijo declara el slug COMPLETO y nunca truncado, porque `fare-indicativo` y `fare-indefiniti` comparten `fare-ind` (D-40-01, D-40-03, D-43-22, SC-5)."
    - "Las DOS categorías de la fase cargan en boot como unidades de reset SEPARADAS: `fare-cond-imperativo` (order 17) y `fare-indefiniti` (order 18) son dos entradas distintas, dos prefijos distintos de `RESET_PREFIXES_V13` y dos filas distintas en home, picker, Repaso y Examen — no una fusión (D-43-01, SC-5)."
    - "`git diff --stat src/screens/app.js src/domain/ src/data/` no produce salida al cerrar el plan: los 6 slots tienen un solo eje de variante cada uno y `pickVariantIndex` (`src/domain/session.js:232`, `Pure` y axis-agnostic) sirve un slot de 2, de 3 y de 4 variantes sin una línea nueva (D-43-22, SC-5)."
    # --- edge: schema boundary ---
    - "El fichero `content/exercises/fare-indefiniti.json` nunca queda en disco sin su entrada en `categories.json`: `src/data/schema-validator.js` rechaza un `categoryIds` que referencia una categoría desconocida, así que `node --test tests/*.test.js` termina con `fail 0` en cada commit de este plan (edge: schema boundary)."
    # --- D-43-03 : el reparto desigual, declarado slot a slot ---
    - "El fichero tiene EXACTAMENTE 6 slots con 3, 3, 4, 2, 3 y 3 variantes — total **18** — y el `notes` justifica cada desviación del reparto uniforme por su nombre: `participio-presente` con **2** porque `facente` es fosilizado y sus contextos reales están contados, y forzar un tercero sería inventarlo, que es literalmente lo que INDEF-03 prohíbe; `participio-passato` con **4** porque tiene que alojar el par invariable-frente-a-concordado que SC-4 declara magnet más el uso invariable como forma léxica que INDEF-02 pide (D-43-03)."
    - "El slot `fare-indefiniti-participio-presente` con 2 variantes es legal: el mínimo del motor son 2 variantes por slot y `pickVariantIndex` las sirve sin cambio (D-43-03)."
    # --- SC-3 / INDEF-01 / INDEF-04 : el eje CONTEXTO, nunca la persona ---
    - "El eje de variante de los 6 slots es el CONTEXTO y NUNCA la persona: las formas son fijas, así que las variantes de un slot comparten key y lo que las diferencia es el encaje sintáctico. Ningún gate de este fichero comprueba pronombres sujeto, y esa ausencia va declarada como desviación en la cabecera de `tests/content-fare-indefiniti.test.js` (SC-3, D-43-12, nota de cabecera de REQUIREMENTS.md línea 8)."
    - "El `notes` declara la lista CERRADA de tipos de contexto sintáctico y qué tipo lleva cada una de las 18 variantes; dentro de cada slot los tipos son todos DISTINTOS, con la única excepción enumerada por id de `fare-indefiniti-participio-passato`, cuyo eje es la terminación por D-43-16 y cuyo sustituto es el reparto 2 invariables + 2 concordadas (D-43-12, D-43-16)."
    - "Las 3 keys de `fare-indefiniti-infinito-presente` son `fare`, `fare`, `fare` y las 3 de `fare-indefiniti-infinito-passato` son `aver fatto`, `aver fatto`, `aver fatto`: la decisión que el par de slots examina es la ANTERIORIDAD respecto a la principal, que es lo que INDEF-01 y SC-3 nombran (INDEF-01, SC-3)."
    - "Cada uno de los 3 prompts de `fare-indefiniti-infinito-passato` fuerza la anterioridad de forma comprobable: o la preposición `dopo` gobierna la cláusula del hueco, o la cláusula del hueco lleva un deíctico de pasado del conjunto cerrado `ieri`, `la settimana scorsa`, `l'anno scorso`, `stamattina`, `il mese scorso` — sin ese forzado el infinito simple sería igualmente defendible y la variante tendría dos respuestas (INDEF-01; edge: ambiguity boundary)."
    - "Las 3 keys de `fare-indefiniti-gerundio-presente` son `facendo` y las 3 de `fare-indefiniti-gerundio-passato` son `avendo fatto`, y una de las tres del gerundio presente usa el marco progresivo `stare + gerundio`, que INDEF-04 y SC-3 nombran los dos explícitamente y que por tanto se EXAMINA y no solo se menciona (INDEF-04, D-43-15, SC-3)."
    - "La explanation de `fare-indefiniti-gerundio-presente` desarrolla la interferencia real de `stare + gerundio`, que no es la formación sino el USO: el italiano lo restringe a la acción en curso ahora mismo mientras el español extiende «estar + gerundio» mucho más, así que el error del hispanohablante es usarlo de MÁS (D-43-15, INDEF-04)."
    # --- D-43-14 : el imperativo negativo entra como contexto del infinito ---
    - "Una de las 3 variantes de `fare-indefiniti-infinito-presente` es el imperativo negativo de 2ª singular, que en italiano no es la forma apostrofada negada sino `non` más infinitivo: el error se EXAMINA en vez de quedarse dicho en una explanation, sin romper las 5 variantes exactas del slot de imperativo de 43-01 y sin abrir un cruce técnico — `categoryIds` sigue siendo de longitud 1 (D-43-14, cruce conceptual y no técnico)."
    # --- D-43-13 : el pool cerrado, el gate más barato de la fase ---
    - "Las 4 `options` de las 18 variantes salen SIEMPRE del pool cerrado no personal `fare`, `aver fatto`, `fatto`, `fatta`, `fatti`, `fatte`, `facendo`, `avendo fatto`, `facente`, `facenti`: la unión de todas las opciones del fichero está contenida en ese conjunto, y por tanto NINGUNA forma conjugada de `fare` entra en `options` en esta categoría. Es un gate grep-verificable congelado en el fichero de test (D-43-13, SC-3)."
    - "El pool cubre la interferencia por construcción: el español dice «después de hacer» con infinitivo simple, así que `fare` es la distractora natural y pedagógicamente correcta de `aver fatto`, y no hace falta meter una conjugada de otra categoría para tener una distractora plausible (D-43-13)."
    # --- D-43-16 / INDEF-02 / SC-4 : el participio passato y su gate HARD ---
    - "Las 4 `options` de las 4 variantes de `fare-indefiniti-participio-passato` son SIEMPRE el mismo conjunto `fatto`, `fatta`, `fatti`, `fatte` (igualdad del conjunto ordenado en las 4), y sus keys son `fatto`, `fatto`, `fatti`, `fatte`: un solo eje, un solo pool, y el doble comportamiento que SC-4 pide sale del contraste entre variantes del mismo slot (D-43-16, INDEF-02, SC-4)."
    - "EXACTAMENTE 2 de las 4 variantes son invariables —compuesto con el auxiliar de posesión y el objeto POSPUESTO, sin ningún pronombre antepuesto— y EXACTAMENTE 2 llevan pronombre objeto directo antepuesto que obliga a la concordancia; el reparto va declarado en tabla y congelado por gate (D-43-16, INDEF-02, SC-4)."
    - "GATE HARD de pronombre: en los 4 prompts de ese slot hay **0** apariciones como palabra suelta de `mi`, `ti`, `ci`, `vi` y `ne` —con los cuatro primeros la concordancia es OPCIONAL en italiano y con `ne` tiene reglas propias, así que cualquiera de ellos abriría dos respuestas defendibles— y las 2 variantes concordadas usan `li` o `le`, nunca `lo` ni `la` antepuestos, porque con el singular el auxiliar se elide y la vocal elidida esconde el género (D-43-16; edge: ambiguity boundary)."
    - "La explanation de `fare-indefiniti-participio-passato` desarrolla el par de la interferencia: el español NO concuerda («las he hecho») y el italiano SÍ («le ho fatte») (D-43-16, INDEF-02, SC-4)."
    # --- D-43-17 : el CUARTO magnet ---
    - "La key de las 3 variantes de `fare-indefiniti-infinito-passato` es la forma elidida `aver fatto`, y la cadena `avere fatto` no aparece en NINGÚN `prompt` ni en NINGUNA `options[]` de las 18 variantes: las dos son correctas y atestiguadas, así que ofrecer la no elidida como distractora «incorrecta» produciría un ejercicio injusto, un `disputed` STICKY y el reset de la categoría entera por la cascada D-54 (D-43-17, INDEF-01)."
    - "El `notes` registra `aver fatto` frente a `avere fatto` como el CUARTO MAGNET del milestone, con su audit trail: INT-04 declara solo tres (imperativo `tu`, homógrafas de congiuntivo, `fatto` invariable-frente-a-concordado) y este cuarto estaba escondido a plena vista en la grafía literal `aver(e) fatto` de INDEF-01; Phase 44 tendrá que recogerlo al verificar INT-04 (D-43-17)."
    - "La explanation de `fare-indefiniti-infinito-passato` dice que las dos formas son correctas y que la elidida es la corriente — es la línea concreta del principio RECONOCER, NO PRODUCIR aplicada a este magnet (D-43-17, D-43-19)."
    # --- D-43-18 / INDEF-03 / SC-4 : facente y la excepción acotada ---
    - "El slot `fare-indefiniti-participio-presente` tiene 2 variantes cuyos contextos son los DOS compuestos declarados —el de sustitución en el cargo en singular y el de pertenencia en plural—, y ninguna de las dos es un contexto burocrático genérico construido: INDEF-03 dice literalmente «sin mentir sobre su uso real» (D-43-18, INDEF-03, SC-4)."
    - "El `notes` declara la EXCEPCIÓN ACOTADA al SCOPE-GATE léxico de D-41-06 con su audit trail y su frontera explícita: solo esos dos compuestos, solo en ese slot, y CERO perífrasis léxicas del verbo `fare` en ningún sitio de las dos categorías. Con la justificación de fondo escrita para que un re-pase futuro no lo «arregle»: D-41-06 vetaba perífrasis del VERBO `fare`, y `facente funzione` es un participio fosilizado que funciona como sustantivo o adjetivo, no una perífrasis verbal (D-43-18)."
    - "La exención al gate del objeto literal está enumerada POR ID DE SLOT en `tests/content-fare-indefiniti.test.js` y no como relajación global: el conjunto cerrado de objetos y el escaneo de perífrasis siguen mordiendo en las otras 16 variantes (D-43-18)."
    - "La explanation de `fare-indefiniti-participio-presente` lleva la NOTA DE REGISTRO explícita que INDEF-03 exige: avisa de que la forma es burocrática y fosilizada y de que vive sobre todo en esos compuestos, y de que entra para que el paradigma esté completo (INDEF-03, SC-4)."
    # --- D-43-19 + [[exception_belongs_in_validation_prompt]] : la excepción llega al subagent ---
    - "`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` contiene, escritas por este plan y EXACTAMENTE UNA VEZ en toda la fase, las dos excepciones nuevas: la de los dos compuestos fosilizados al criterio de naturalidad, y el principio RECONOCER NO PRODUCIR aplicado a las tres formas atestiguadas que el material nombra pero no ofrece (los dos imperativos vivos vetados de 43-01 y la forma no elidida del infinito compuesto). El subagent del quórum NO ve el `notes`: sin esta edición marcaría el compuesto fosilizado como violación y los magnets como sin resolver (`[[exception_belongs_in_validation_prompt]]`)."
    - "Las excepciones nuevas van insertadas ANTES de la línea de cierre del prompt (`Fin del prompt. A continuación se adjunta el ejercicio bajo evaluación.`), nunca después: todo lo que va detrás de esa línea el subagent lo lee como parte del ejercicio bajo evaluación y no como regla."
    - "El pase DeepSeek de la ronda EXTRA verá esas excepciones sin cambio de código: `scripts/validate-ai-pass.mjs:38-39` ya lee ese fichero por su ruta real. El skill `gsd-validate-exercise` en cambio apunta a una ruta que NO existe desde el archivado del milestone v1.1, y este plan la corrige — sin esa corrección la excepción escrita no llegaría al quórum base y la edición sería inútil."
    # --- D-43-02 / D-43-20 : el estado del hand-off ---
    - "Al cerrar este plan los 6 slots de `fare-indefiniti` llevan `validation` con `status: \"pending\"` y `passes: []`, igual que los 3 de 43-01: el executor NO fabrica pases ni estampa `validated`, porque el quórum base canónico Opus+Sonnet vive en una pasada TOP-LEVEL posterior (D-43-02, VAL-03)."
    - "`tests/content-fare-indefiniti.test.js` incluye el gate condicionado de ronda EXTRA con `EXTRA_ROUND_SLOTS = ['fare-indefiniti-participio-passato', 'fare-indefiniti-infinito-passato']`: si cualquiera de esos dos llega a `validated` sin un pase cuyo `by` empiece por `deepseek`, la suite se pone roja (D-43-20, SC-4)."
    - "`tests/content-fare-indefiniti.test.js` exige `status === deriveStatus(passes)` importando `deriveStatus` de `src/data/validation-state.js` (nunca reimplementándolo), de modo que un `validated` escrito a mano no puede pasar por bueno (D-43-21, VAL-03)."
    - "`VAL_07_STRICT=1 node --test tests/*.test.js` falla al cerrar el plan nombrando los 9 slots `pending` de las dos categorías nuevas, y `node scripts/run-validation-271.mjs` sigue reportando su total anterior en PASS: los dos son el estado ESPERADO hasta Phase 44 / INT-02, no fallos que arreglar (D-43-02, Out of Scope)."
    # --- D-43-21 / D-43-22 : canon editorial y heredados ---
    - "Los 6 slots tienen `type: \"multiple-choice\"` con `options` de 4 sin duplicados internos y `correctIndex` entero en `[0,4)` no constante dentro de ningún slot; no existe ningún slot `match` ni `word-buttons`, y el `notes` declara el 0-match y el 0-word-buttons como decisiones razonadas (D-43-22, DESIGN RULE D-04)."
    - "Ningún `prompt` de las 18 variantes glosa la forma verbal ni contiene la subcadena `espa` en ninguna capitalización (D-41-05, D-43-22)."
    - "Los ids de los 6 slots son literalmente `fare-indefiniti-infinito-presente`, `fare-indefiniti-infinito-passato`, `fare-indefiniti-participio-passato`, `fare-indefiniti-participio-presente`, `fare-indefiniti-gerundio-presente` y `fare-indefiniti-gerundio-passato`, los seis con `categoryIds: [\"fare-indefiniti\"]`, y ningún id termina en sufijo numérico de 3 cifras: el espacio `fare-indefiniti-300`+ queda libre para Phase 44 / INT-03 (D-41-14, D-40-07, D-43-22; edge: id-space boundary)."
    - "Las 6 explanations están en español acentuado RAE, plain text de un solo párrafo sin markdown ni backticks, con apóstrofes ASCII U+0027 al citar italiano y sin comillas ni apóstrofes tipográficos; un flag C4-accent del quórum sobre español sin tildes es bug REAL y se arregla poniendo los acentos, nunca con override (D-43-21)."
    - "El objeto de `fare` en las 16 variantes NO exentas sale solo del conjunto literal cerrado heredado de 7 más las dos inflexiones en plural declaradas por este plan (`le foto` y `le torte`, que las variantes de concordancia necesitan porque el conjunto heredado no tiene ningún femenino plural), con exactamente uno en la cláusula del hueco; y hay 0 coincidencias por campo para los marcadores de perífrasis y modismos en las 18 (D-41-06, D-43-22)."
    - "La línea nueva de `CATEGORIES_WITH_EXPLANATIONS` con `expected: slotCountOf('content/exercises/fare-indefiniti.json')` activa los sub-tests editoriales sobre el fichero nuevo y todos pasan; con ella el array queda con las 4 categorías de v2.0 y `tests/exercise-types.test.js` no vuelve a tocarse en esta fase (D-43-21, SC-5)."
    # --- valla ---
    - "`git status --porcelain` no lista ningún fichero fuera de los 7 de `files_modified`; en particular `src/`, `scripts/`, `content/exercises/fare-cond-imperativo.json` y `tests/content-fare-cond-imperativo.test.js` quedan sin tocar (Out of Scope del milestone y frontera con el plan 43-01)."

  artifacts:
    - "`content/exercises/fare-indefiniti.json` — fichero NUEVO con exactamente 2 claves top-level (`notes`, `exercises`), el `notes` con las declaraciones de esta categoría (identidad y volumen 6 slots / 18 variantes con el reparto desigual justificado slot a slot, lista CERRADA de tipos de contexto con su reparto por variante, pool CERRADO no personal, CUARTO MAGNET, GATE HARD de pronombre, EXCEPCIÓN ACOTADA al SCOPE-GATE, RECONOCER NO PRODUCIR, imperativo negativo como contexto, `stare + gerundio` como uso y no como formación, 0-gloss, 0-match, 0-word-buttons, nota de escaneo por campo y nota de count-sync), y 6 slots con 3, 3, 4, 2, 3 y 3 variantes."
    - "`content/categories.json` — 18 entradas; la nueva es `fare-indefiniti`, `order: 18`, `origen: \"ia-quorum\"`, `name` con raya em U+2014."
    - "`tests/exercise-types.test.js` — 1 línea nueva en `CATEGORIES_WITH_EXPLANATIONS` con `expected` dinámico vía `slotCountOf`, más un comentario de una línea que nombra la fase y los requisitos."
    - "`tests/content-fare-indefiniti.test.js` — fichero NUEVO de invariantes permanentes de la categoría, 13 bloques `describe`, con las desviaciones declaradas en la cabecera."
    - "`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` — una sección nueva de excepciones declaradas por categoría, insertada antes de la línea de cierre del prompt, más tres punteros de una línea desde los criterios C1, C2 y C3."
    - "`.claude/skills/gsd-validate-exercise/SKILL.md` y `.claude/skills/gsd-validate-batch/SKILL.md` — corrección de ruta (3 ocurrencias en total) del fichero de prompt de validación, que quedó obsoleta al archivarse el milestone v1.1. Solo ruta: ni una regla cambia."

  key_links:
    - "`content/categories.json` → `src/data/schema-validator.js` (`knownCategoryIds`): sin la entrada `fare-indefiniti`, cada slot emite `referencia a categoría desconocida` y la suite se pone roja. La entrada va en la tarea tracer, no al final."
    - "`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` → el subagent del quórum: es la ÚNICA fuente de reglas que ve (arranca con context window vacío, no ve `CLAUDE.md`, ni `.planning/`, ni el `notes` del fichero). Toda excepción que no esté ahí escrita produce el síntoma diagnóstico del patrón: el modelo marca un caso y aprueba otro idéntico."
    - "`.claude/skills/gsd-validate-exercise/SKILL.md` → la ruta de lectura del prompt: la ruta declarada hoy no existe en disco desde el archivado de v1.1. `scripts/validate-ai-pass.mjs:38-39` sí tiene la real, y por eso la ronda EXTRA DeepSeek funciona y el quórum base es el que está en riesgo."
    - "el pronombre objeto antepuesto del `prompt` → la unicidad de la respuesta en las 2 variantes de concordancia: es el único desambiguador entre las cuatro terminaciones. Con `mi`/`ti`/`ci`/`vi` la concordancia es opcional, con `ne` tiene reglas propias y con `lo`/`la` la elisión del auxiliar esconde el género — cualquiera de los tres casos produce dos respuestas defendibles y, por la cascada D-54, el reset de las 18 variantes."
    - "el marcador de anterioridad del `prompt` → la exclusión del infinito simple y del gerundio simple: sin él, `fare` es defendible frente a `aver fatto` y `facendo` frente a `avendo fatto`, porque la diferencia entre las dos parejas es exactamente la anterioridad."
    - "el pool cerrado de `options` → la frontera entre categorías: mientras ninguna conjugada entre en `options`, este fichero no puede meter casillas de `fare-indicativo`, `fare-congiuntivo` ni `fare-cond-imperativo` en el examen, que es lo que las tres fases anteriores evitaron entre sí."
    - "`validation.passes[]` → `deriveStatus` (`src/data/validation-state.js`): `validated` exige ≥2 pases `correcta` con `by` distintos y cero `incorrecta`; cualquier `incorrecta` da `disputed` STICKY."

  prohibitions:
    - "MUST NOT inventar un tercer contexto de `facente` para que el slot llegue a tres variantes: INDEF-03 dice literalmente «sin mentir sobre su uso real», y un contexto burocrático genérico construido —del tipo del sujeto que presenta una solicitud— es exactamente lo que el requisito prohíbe. Dos variantes honestas valen más que tres con una inventada, y el mínimo del motor son 2."
    - "MUST NOT «arreglar» en un re-pase futuro la excepción declarada de los dos compuestos fosilizados devolviéndola a una violación del SCOPE-GATE: la justificación está escrita en el `notes` y en el prompt de validación precisamente para eso. D-41-06 vetaba perífrasis del VERBO `fare`; un participio fosilizado que funciona como sustantivo o adjetivo no es una perífrasis verbal, y el requisito INDEF-03 lo exige."
    - "MUST NOT dar de alta una variante en la que más de una opción sea una respuesta defendiblemente correcta: el quórum la marca `disputed`, `disputed` es STICKY y la cascada D-54 resetea la categoría entera. En este fichero los tres vectores concretos son el pronombre de concordancia opcional, el marcador de anterioridad ausente y la forma no elidida del infinito compuesto ofrecida como distractora."
    - "MUST NOT ofrecer `avere fatto` como distractora «incorrecta» de `aver fatto`, ni al revés: las dos son correctas y atestiguadas, y `aver(e) fatto` es la grafía literal del propio requisito INDEF-01. Es el cuarto magnet de la fase y su tratamiento es el mismo que el del imperativo apostrofado: key para una, blacklist declarada para la otra, y la explanation diciendo que las dos valen."
    - "MUST NOT hacer override de un flag C4-accent del quórum sobre español sin tildes: en este proyecto ese flag es un bug REAL y se arregla poniendo los acentos. El override existe para adjudicaciones razonadas del autor, no para ahorrarse una tilde."
    - "MUST NOT escribir `validation.status: \"validated\"` ni ninguna entrada de `passes[]` que no venga de un pase de modelo real: el audit trail es la única evidencia que el autor tiene de que una variante fue revisada, y un pase inventado la destruye sin dejar rastro."
    - "MUST NOT batchear el quórum ni evaluar más de un ejercicio por contexto: VAL-03 es un invariante del proyecto y un contexto compartido contamina el juicio de la variante siguiente con la anterior."
    - "MUST NOT meter una forma conjugada de `fare` en `options` de esta categoría «para tener una distractora plausible»: metería casillas de otras tres categorías en el examen y bajaría el poder discriminante, porque una conjugada es descartable de un vistazo cuando el hueco pide una forma no personal."
    - "MUST NOT presentar una forma regional, dialectal, arcaica o de registro literario como si fuera la norma del italiano estándar, ni en un sentido ni en el otro: aquí el caso vivo es el participio presente, que es real pero burocrático, y la explanation tiene que decir exactamente eso."
    - "MUST NOT reducir el alcance de una decisión de 43-CONTEXT.md a una versión «v1», «simplificada», «estática por ahora» o «pendiente de cablear»: si algo no cabe, se parte la fase y se pide aprobación, no se entrega menos de lo decidido."
    - "MUST NOT renombrar el slug `fare-indefiniti` ni ningún id de slot después de darlo de alta, ni truncar el prefijo a la parte compartida con `fare-indicativo`: los ids viven en el `exerciseStats` real del autor y en `RESET_PREFIXES_V13`, y un renombrado o un prefijo truncado dejan su progreso huérfano en silencio."
    - "MUST NOT tocar el motor v1.4 (`src/screens/app.js`, `src/domain/`, `src/data/`) ni introducir dependencia, `package.json` o paso de build: el proyecto es web estática de dependencias cero (Out of Scope literal del milestone, CLAUDE.md §Constraints)."
    - "MUST NOT autorar cruces multi-categoría ni usar el espacio de ids `fare-indefiniti-300`+, ni sincronizar `CATEGORIES` de `scripts/run-validation-271.mjs`, `REAL_CATEGORIES` ni `TOTAL_EXPECTED`: son el entregable literal de Phase 44 / INT-02 e INT-03."
    - "MUST NOT cambiar ninguna REGLA de `09-VALIDATION-PROMPT.md` al añadir la sección de excepciones, ni tocar el guard anti prompt-injection, ni insertar contenido después de la línea de cierre del prompt: lo que va detrás de esa línea el subagent lo lee como ejercicio bajo evaluación, no como regla."
---

<objective>
Dar de alta la categoría `fare-indefiniti` en el disco y en el registro, y autorarla **de punta a punta**:
6 slots `multiple-choice` — infinito presente (3), infinito passato (3), participio passato (**4**),
participio presente (**2**), gerundio presente (3) y gerundio passato (3) = **18 variantes** —, más su
fichero de invariantes permanentes `tests/content-fare-indefiniti.test.js`, más la edición **única de toda
la fase** del prompt de validación por quórum.

En estos 6 slots las formas son FIJAS, así que el eje de variante deja de ser la persona y pasa a ser el
**CONTEXTO**: las variantes de un slot comparten key y lo que las diferencia es el encaje sintáctico.

La primera tarea es un **tracer**: un solo slot (`fare-indefiniti-infinito-presente`) + su `notes` completo
+ la entrada de `content/categories.json` con `order: 18` + la línea del smoke paramétrico, verificado de
punta a punta antes de que exista una segunda variante. Se elige ese slot porque es donde se estrena el
eje CONTEXTO de toda la categoría —si tres frases que comparten la misma key no examinan tres encajes
distintos, son el mismo ejercicio tres veces— y porque una de sus tres variantes es el imperativo negativo
de D-43-14, la casilla que 43-01 no podía alojar.

Cubre INDEF-01 (infinito presente y passato, con la elección por anterioridad), INDEF-02 (participio
passato como forma léxica e invariable con el auxiliar de posesión frente a la concordancia con pronombre
antepuesto), INDEF-03 (participio presente con nota de registro explícita) e INDEF-04 (gerundio presente y
passato, incluido `stare + gerundio`).

Purpose: esta categoría contiene el CUARTO magnet del milestone —`aver fatto` frente a `avere fatto`, que
INT-04 no declara y que estaba escondido en la grafía literal de INDEF-01— y el gate más sistemático de la
fase: la concordancia del participio con pronombre antepuesto es OBLIGATORIA solo con cuatro pronombres,
OPCIONAL con otros cuatro y con reglas propias con un noveno. Escribir una variante con el pronombre
equivocado produce dos respuestas defendibles por inercia, no por descuido. Y contiene el choque real entre
dos reglas del proyecto: INDEF-03 exige el compuesto fosilizado del participio presente y el SCOPE-GATE de
D-41-06 lo prohibiría — resuelto por escrito, en el `notes` y en el prompt de validación, porque el
subagent del quórum no ve el `notes`.

Output: `content/exercises/fare-indefiniti.json` con `notes` + 6 slots + 18 variantes en
`validation.status: "pending"`, `content/categories.json` con 18 entradas, 1 línea nueva en
`CATEGORIES_WITH_EXPLANATIONS`, `tests/content-fare-indefiniti.test.js` con 13 `describe`, y las dos
excepciones nuevas escritas en el prompt de validación con su ruta arreglada en los skills. Ningún fichero
de `src/` cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-CONTEXT.md
@.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-PATTERNS.md
@.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-01-SUMMARY.md
</context>

## Frontera de este plan (y de la fase)

- **La categoría `fare-cond-imperativo` entera** — es del plan 43-01, wave 1, ya cerrado. Este plan **no
  toca** `content/exercises/fare-cond-imperativo.json` ni `tests/content-fare-cond-imperativo.test.js`.
  Sí toca `content/categories.json` y `tests/exercise-types.test.js`, que es exactamente el punto de
  colisión por el que los dos plans van en waves distintas y no en paralelo (D-43-02).
- **Cruces multi-categoría** (`fare-indefiniti-300`+) → Phase 44, INT-03. El espacio de ids `-300`+ se
  deja libre: no se pre-crea, no se reserva con placeholder, simplemente no se usa. El «cruce» de D-43-14
  (el imperativo negativo dentro del infinito) es **conceptual y no técnico**: `categoryIds` sigue siendo
  de longitud 1.
- **Sync de counts** → Phase 44, INT-02. El rojo de `VAL_07_STRICT=1` y la ceguera del reporter al cerrar
  esta fase son el estado ESPERADO.
- **El motor v1.4.** `git diff src/screens/app.js src/domain/ src/data/` queda vacío al cierre.
- **Perífrasis y modismos de `fare`** — SCOPE-GATE HARD heredado de D-41-06, vigente con la ÚNICA
  excepción acotada de D-43-18, enumerada por id de slot.
- **Un slot propio de concordancia del participio** y **un slot propio para `stare + gerundio`** — los dos
  evaluados y descartados en D-43-16 y D-43-15 para no abrir una segunda divergencia de conteo del
  milestone. Están en §Deferred de 43-CONTEXT.md; no se planifican.
- **El quórum base canónico Opus+Sonnet y la ronda EXTRA DeepSeek** — no corren dentro del executor
  (D-43-02). Ver §Hand-off del plan 43-01, que vale para los 9 slots.

## Correcciones y precisiones de plan-time (verificadas en el codebase el 2026-08-07)

Cinco puntos donde la letra de 43-CONTEXT.md o de 43-PATTERNS.md necesita una precisión sobre hechos
comprobados. Ninguna cambia una decisión; todas evitan un gate insatisfacible o una variante con dos
lecturas.

**1. `docs/09-VALIDATION-PROMPT.md` NO existe.** El planning-context y 43-CONTEXT.md nombran esa ruta;
`docs/` contiene solo `DECOY-VALIDATION-PROMPT.md`, `SONG-VALIDATION-PROMPT.md` y `VALIDACION-QUORUM.md`.
El fichero real, y el que de verdad se envía al subagent, es
`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` (276 líneas),
que es la ruta que `scripts/validate-ai-pass.mjs:38-39` ya lee. Ese es el destino de la edición.

**2. El skill del quórum base apunta a una ruta que no existe.**
`.claude/skills/gsd-validate-exercise/SKILL.md` líneas 50 y 231, y
`.claude/skills/gsd-validate-batch/SKILL.md` línea 545, declaran
`.planning/phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md`; ese directorio no existe
desde que el milestone v1.1 se archivó a `.planning/milestones/v1.1-phases/`. Consecuencia directa para
esta fase: la excepción que se escriba llegaría a DeepSeek (que lee la ruta buena) pero no al quórum base
Opus+Sonnet, que es justo el que decide. La corrección de ruta es de 3 líneas, no cambia ninguna regla y
va en la misma tarea que la edición del prompt, porque sin ella la edición es inútil.

**3. La sección nueva va ANTES de la línea de cierre del prompt.** El fichero termina con
`*Fin del prompt. A continuación se adjunta el ejercicio bajo evaluación.*`, y el §6 anterior es el guard
anti prompt-injection que declara que todo lo que viene después es DATA a evaluar. Insertar reglas después
de esa línea las convertiría en contenido del ejercicio a ojos del subagent — el fallo más silencioso
posible en esta edición.

**4. El conjunto cerrado de objetos de D-41-06 no tiene ningún femenino plural, y las variantes de
concordancia lo necesitan.** Los 7 objetos heredados son `i compiti`, `un errore`, `il lavoro`,
`una torta`, `il letto`, `tutto` y `una foto`: el único plural es masculino. Pero D-43-16 exige una
variante con concordancia en femenino plural, cuyo antecedente tiene que ser un sintagma femenino plural.
Resolución de plan-time, dentro del espíritu del gate: el conjunto se amplía con las **inflexiones en
plural de dos de sus propios miembros** —`le foto` y `le torte`—, enumeradas y declaradas en el `notes`.
No se añade ningún objeto léxico nuevo: es el mismo conjunto cerrado, flexionado donde la sintaxis lo
exige.

**5. Con `lo` y `la` antepuestos el auxiliar se elide y el género se esconde.** D-43-16 declara el gate
como «solo `lo`/`la`/`li`/`le`», y es correcto frente a `mi`/`ti`/`ci`/`vi`/`ne`. Pero en la práctica el
singular produce la forma elidida del auxiliar, y esa vocal elidida no dice si el pronombre era masculino
o femenino: el autor tendría que deducir el género solo del antecedente, y un lector razonable podría
defender la forma invariable. Las 2 variantes de concordancia usan por tanto **`li` y `le`**, donde el
pronombre es visible y la terminación cambia de forma inequívoca. El gate escrito prohíbe explícitamente
los bigramas de pronombre singular antepuesto más auxiliar, elididos o no. No es una reducción del gate de
D-43-16: es su forma satisfacible.

## Artifacts this phase produces

La tabla completa de símbolos creados por la fase 43 —los 9 ids de slot, las 2 entradas de
`categories.json`, los 2 ficheros de contenido, los 2 ficheros de test y sus `describe`, y las 2 líneas
del smoke— está en `43-01-PLAN.md` §Artifacts this phase produces y **vale sin cambios para este plan**.
Lo que este plan añade a esa lista, y que 43-01 no podía producir:

| Símbolo / identificador | Fichero | Tipo | Plan |
|---|---|---|---|
| `describe` 1 `fare-indefiniti — estructura y conteos (D-43-03)` | `tests/content-fare-indefiniti.test.js` | bloque de test | 43-02 |
| `describe` 2 `fare-indefiniti — paradigma no personal, 18 keys con repeticion deliberada (INDEF-01..INDEF-04)` | idem | bloque de test | 43-02 |
| `describe` 3 `fare-indefiniti — tipos de contexto sintactico, conjunto cerrado y distintos por slot (D-43-12)` | idem | bloque de test | 43-02 |
| `describe` 4 `fare-indefiniti — 0-gloss del verbo (D-41-05, D-43-22)` | idem | bloque de test | 43-02 |
| `describe` 5 `fare-indefiniti — SCOPE-GATE lexico con la excepcion acotada del participio presente (D-41-06, D-43-18)` | idem | bloque de test | 43-02 |
| `describe` 6 `fare-indefiniti — blacklist de formas atestiguadas y defendibles (D-43-16, D-43-17, D-43-07)` | idem | bloque de test | 43-02 |
| `describe` 7 `fare-indefiniti — pool CERRADO de options, cero formas conjugadas (D-43-13, SC-3)` | idem | bloque de test | 43-02 |
| `describe` 8 `fare-indefiniti — participio passato, las 4 terminaciones y el gate HARD de pronombre (D-43-16, SC-4)` | idem | bloque de test | 43-02 |
| `describe` 9 `fare-indefiniti — infinito passato, el cuarto magnet, e imperativo negativo non fare (D-43-17, D-43-14)` | idem | bloque de test | 43-02 |
| `describe` 10 `fare-indefiniti — contextos, objetos y marco progresivo por variante (D-43-12, D-43-15)` | idem | bloque de test | 43-02 |
| `describe` 11 `fare-indefiniti — canon editorial e higiene del JSON (D-43-21, T-43-01, T-43-02)` | idem | bloque de test | 43-02 |
| `describe` 12 `fare-indefiniti — audit trail de validacion y ronda EXTRA (D-43-20, T-43-03)` | idem | bloque de test | 43-02 |
| `describe` 13 `fare-indefiniti — registro de la categoria (D-43-22, SC-5)` | idem | bloque de test | 43-02 |
| sección `7. Excepciones declaradas por categoria de contenido` | `09-VALIDATION-PROMPT.md` | sección de prompt de validación | 43-02 |

## Multi-source coverage audit

La auditoría multi-fuente completa de la fase (GOAL, REQ, RESEARCH, CONTEXT; 5 criterios de éxito, 7
requisitos, 22 decisiones) está en `43-01-PLAN.md` §Multi-source coverage audit, con **0 items MISSING** y
sin necesidad de split de fase. Es una sola tabla para los dos plans a propósito: la cobertura se audita
por fase, no por plan, y duplicarla invitaría a que las dos copias divergieran.

Los items cuyo dueño es **este** plan son: SC-3, SC-4, la parte de SC-5 correspondiente a `order: 18`,
INDEF-01, INDEF-02, INDEF-03, INDEF-04, y las decisiones D-43-03, D-43-12, D-43-13, D-43-14, D-43-15,
D-43-16, D-43-17, D-43-18, más la mitad de D-43-19 que vive en `09-VALIDATION-PROMPT.md` y la mitad de
D-43-20 correspondiente a sus dos slots de ronda EXTRA.

## Hand-off al pase TOP-LEVEL de quórum (D-43-02, D-43-20)

La tabla y los 6 criterios de aceptación del pase top-level están en `43-01-PLAN.md` §Hand-off y cubren
los 9 slots de las dos categorías. Se repiten aquí solo los tres puntos que este plan condiciona:

1. **La Task 5 de este plan es un PREREQUISITO del pase.** Sin la sección de excepciones escrita en
   `09-VALIDATION-PROMPT.md` y sin la ruta arreglada en el skill, el subagent del quórum base marcará el
   compuesto fosilizado del participio presente como violación del gate léxico y los magnets como sin
   resolver, y el síntoma será el diagnóstico clásico del patrón: el modelo marca un caso y aprueba otro
   idéntico (`[[exception_belongs_in_validation_prompt]]`).
2. **Ronda EXTRA obligatoria sobre 7 de las 18 variantes de esta categoría** (D-43-20): el slot
   `fare-indefiniti-participio-passato` completo (4) —SC-4 la exige literalmente sobre el par
   invariable-frente-a-concordado— y el slot `fare-indefiniti-infinito-passato` completo (3), por el
   cuarto magnet. Con las 5 del imperativo de 43-01 son las 12 de 35 que declara D-43-20. Pase DeepSeek
   vía `scripts/validate-ai-pass.mjs`, claves en `.env`, **un ejercicio por contexto, NUNCA batched**.
3. **DeepSeek y no Gemini**, por dos razones ya verificadas: es el estricto en acentos y concordancia y ya
   cazó bugs que los checkpoints human-verify aprobaron (`[[feedback_cross_vendor_catches_bugs]]`), y
   Gemini marca el gloss en español como leak, que es falso positivo de política en este proyecto
   (`[[gloss_es_desambiguacion_canon]]`). En cambio un flag **C4-accent** sobre español sin tildes es bug
   **REAL** y se arregla poniendo los acentos (`[[explanations_must_be_accented]]`).

## Assumptions (probe de edges sin SPEC)

Las 7 filas `unclassified` que devolvió el probe determinista, y el análisis de por qué ninguna se
auto-`backstop`ea ni se descarta, están en `43-01-PLAN.md` §Assumptions — una sola tabla para los 7
requisitos de la fase. Las **cuatro** cuyo dueño es este plan (INDEF-01, INDEF-02, INDEF-03, INDEF-04)
quedan surfaceadas allí como supuestos explícitos marcados y desarrolladas aquí como gates concretos:
el forzado de anterioridad (§Correcciones 5 del plan 43-01 no aplica; aquí es el conjunto cerrado de
deícticos de pasado y la preposición que gobierna el hueco), el gate HARD de pronombre con su precisión de
§Correcciones 5, la excepción enumerada por id de slot, y la exclusión del gerundio compuesto de las
opciones donde sería igualmente defendible.

**Igualdad de no-descarte-silencioso:** 7 filas devueltas == 0 autoradas como truths desde el probe + 7
surfaceadas como supuestos marcados en 43-01. Este plan añade **1 `backstop`** propio, en `must_haves`
del propio 43-01 no; aquí se declara donde vive: la unicidad de lectura de la variante causal del gerundio
passato, la única del fichero donde el gerundio simple queda entre las opciones y el adverbial de
anterioridad es lo único que lo excluye. Es juicio lingüístico, no aserción mecánica: si el verificador no
puede confirmarlo con evidencia explícita debe abstenerse a `human_needed`, nunca dar pase silencioso.

## Nota de escaneo (no negociable, heredada de 41-01, 42-01 y 43-01)

Todas las comprobaciones de **ausencia** de este plan van SIEMPRE **por campo** — `variants[].prompt` y
`variants[].options[]`, vía `node -e` sobre el JSON parseado — y **nunca** sobre el fichero completo, ni
sobre `notes`, ni sobre `explanation`. El `notes` nombra a propósito la forma no elidida del infinito
compuesto, los cinco pronombres prohibidos y las perífrasis excluidas; y las explanations **tienen que**
nombrar la forma no elidida (D-43-17) y el registro burocrático del participio presente (INDEF-03). Un
`grep` de fichero entero se auto-invalidaría y un escaneo que incluyera `explanation` pondría roja
precisamente la línea que el requisito pide. En `options` la coincidencia es **EXACTA**; en `prompt` va por
palabra Unicode. Ojo particular de esta categoría: `ne` y `ci` son subcadenas de decenas de palabras
italianas, así que su escaneo va por palabra suelta y acotado al slot del participio passato.

<!-- planner-discipline-allow: avere fatto -->
<!-- planner-discipline-allow: mi -->
<!-- planner-discipline-allow: ti -->
<!-- planner-discipline-allow: ci -->
<!-- planner-discipline-allow: vi -->
<!-- planner-discipline-allow: ne -->
<!-- planner-discipline-allow: lo -->
<!-- planner-discipline-allow: la -->
<!-- planner-discipline-allow: fo -->
<!-- planner-discipline-allow: fé -->
<!-- planner-discipline-allow: fallo -->
<!-- planner-discipline-allow: fammi -->
<!-- planner-discipline-allow: fatelo -->
<!-- planner-discipline-allow: facci -->
<!-- planner-discipline-allow: espa -->
<!-- planner-discipline-allow: facendo -->
<!-- planner-discipline-allow: avendo fatto -->
<!-- planner-discipline-allow: fare -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — `fare-indefiniti-infinito-presente` de punta a punta: fichero + `notes` completo + entrada `order: 18` + línea del smoke</name>

  <files>content/exercises/fare-indefiniti.json, content/categories.json, tests/exercise-types.test.js</files>

  <read_first>
    - `content/exercises/fare-congiuntivo.json` — el molde de FORMA a clonar byte a byte: 2 claves
      top-level, orden literal de campos de un slot MC, los 3 campos de una variante, el shape de
      `validation.passes[]`, y el **estilo de `notes`** (un único string en prosa, gigantesco, plain text
      sin markdown ni saltos de línea, español acentuado RAE, secciones en MAYÚSCULAS dentro del texto,
      cada gate con su «por qué» y su audit trail).
    - `content/exercises/fare-cond-imperativo.json` tal como lo dejó el plan 43-01 — el molde MÁS cercano,
      escrito hace horas contra las mismas decisiones: su `notes` ya instancia el principio RECONOCER NO
      PRODUCIR, la nota de escaneo por campo y la nota de count-sync, y este fichero tiene que ser su
      hermano en tono y en estructura. **No copiar sus formas**: ninguna conjugada entra aquí.
    - `content/exercises/riflessivi.json`, slot `riflessivi-pp-concordanza` — el análogo de CONTENIDO del
      slot de participio passato que autora la Task 3 (pool fijo de terminaciones, un cue de concordancia
      por prompt, distractoras que son la concordancia incorrecta), y el molde de tono de su `explanation`
      para la interferencia con el español. Leerlo AHORA, aunque su slot sea de la Task 3, porque fija el
      registro de todo el fichero.
    - `content/categories.json` (18 líneas de entradas tras 43-01, 17 entradas) — las 4 claves exactas, el
      patrón de `name` con raya em U+2014, y la entrada `fare-cond-imperativo` de `order: 17` como
      antecesora inmediata.
    - `src/data/schema-validator.js` — `knownCategoryIds` (por qué la entrada es prerequisito y no
      cosmética), el XOR de `payload` frente a `variants`, la `explanation` obligatoria a nivel de slot, y
      `validateMultipleChoiceSurface`.
    - `tests/exercise-types.test.js` líneas 1328-1364 — el comentario IN-03, el helper `slotCountOf`, y la
      entrada de `fare-cond-imperativo` que el plan 43-01 dejó como análogo literal y punto de inserción.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-PATTERNS.md` — §Trampas del
      clonado, la lista de marcadores del `notes` a clonar semánticamente, y la tabla de los 13 `describe`
      con su columna de `fare-indefiniti`.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-CONTEXT.md` §decisions —
      D-43-03, D-43-12, D-43-13, D-43-14, D-43-15, D-43-16, D-43-17, D-43-18, D-43-19, D-43-21, D-43-22.
    - `.planning/REQUIREMENTS.md` líneas 43-48 — la nota de cabecera del bloque INDEF («el eje de variante
      es el CONTEXTO, no la persona») y el texto literal de INDEF-01..INDEF-04, con atención a la grafía
      `aver(e) fatto` de INDEF-01 y al «con nota de registro explícita» de INDEF-03.
  </read_first>

  <precondition>El plan 43-01 está cerrado y commiteado: `content/categories.json` tiene 17 entradas con `fare-cond-imperativo` en `order: 17`, `tests/exercise-types.test.js` tiene su línea de smoke, y `node --test tests/*.test.js` reporta `fail 0` en el árbol limpio. Re-medir el `pass` de partida y anotarlo; si no da `fail 0`, parar y reportar. Usar el glob: el path desnudo `tests/` falla en Node 22.20.</precondition>

  <reversibility rating="costly">Escribe el slug `fare-indefiniti` como `id` de `categories.json`, fija `order: 18` y estrena los ids semánticos de los 6 slots. El slug ya es elemento de `RESET_PREFIXES_V13` y comparte el prefijo `fare-ind` con `fare-indicativo` (D-40-03), así que un renombrado o un prefijo truncado dejan progreso huérfano en silencio. No se inserta `checkpoint:decision`: 43-CONTEXT.md declara explícitamente que el mapa de ids no lleva checkpoint porque las 9 casillas están fijadas por FARE-X1 y el roadmap.</reversibility>

  <action>
    Tres ediciones que van juntas porque separarlas deja la suite en rojo entre commits.

    **A. `content/categories.json` — append de 1 entrada (D-43-22, INT-01 parcial)**

    Añadir coma al final de la línea de `fare-cond-imperativo` (order 17, la que dejó 43-01) y una línea
    más con las 4 claves exactas y en este orden: `id` = `fare-indefiniti`, `name` =
    `Fare — formas indefinidas (fare/fatto/facendo)` (raya em U+2014), `order` = 18, `origen` =
    `ia-quorum`. `origen` solo acepta `ia-quorum` o `apuntes-profesora` (enum PROV-01). `order` es
    documental; el orden del array define el display. Las 17 entradas previas quedan byte-intactas y los
    `order` siguen contiguos 1..18, que es lo que mantienen verdes los gates de registro de las tres
    categorías de `fare` ya existentes.

    **B. `content/exercises/fare-indefiniti.json` — fichero nuevo con `notes` + 1 slot**

    Exactamente 2 claves top-level, `notes` y `exercises`. Sin `version`, sin `category`.

    `notes` es UN único string en prosa, plain text, sin markdown, sin backticks, sin saltos de línea, en
    español acentuado RAE y con apóstrofes ASCII U+0027, **sin corchetes angulares en ningún punto del
    fichero**. Instanciar el esqueleto del análogo sobre estas TRECE declaraciones, todas escritas AHORA
    aunque cubran slots que autoran las Tasks 3 y 4:

    1. **Identidad, linaje y volumen.** Categoría `fare-indefiniti` (order 18) nacida DIRECTAMENTE en
       slot+variantes, clon del patrón v1.7/v1.9 y hermana de las otras tres de `fare`, NUNCA legacy
       payload. 6 slots y 18 variantes. Es la SEGUNDA categoría de la fase 43 y una unidad de reset
       INDEPENDIENTE de `fare-cond-imperativo`: van juntas en la fase por volumen de quórum, no por
       parentesco de contenido. Cerrar con el knock-on que Phase 44 e INT-02 necesitan: el milestone queda
       en 22 slots y 113 variantes y `TOTAL_EXPECTED` pasa de 225 a 247.
    2. **EJE = CONTEXTO, NUNCA PERSONA.** En estos 6 slots las formas son fijas, así que las variantes de
       un slot comparten key y el eje de variante es el encaje sintáctico. Declarar explícitamente que
       aquí NO aplica el gate de pronombre sujeto explícito de D-41-07 —que sí gobierna las 12 variantes
       de condizionale de la categoría hermana— porque el eje no es la persona, y que su sustituto es el
       gate de tipo de contexto del punto siguiente. Es la nota de cabecera literal de REQUIREMENTS.md.
    3. **REPARTO DESIGUAL DE VARIANTES, justificado slot a slot (D-43-03).** 3 · 3 · 4 · 2 · 3 · 3. El
       documento de diseño estima unas 3 por slot; aquí dos slots se desvían y cada desviación va con su
       porqué: el de participio presente lleva **2** porque la forma es fosilizada y sus contextos reales
       están contados, y forzar un tercero sería inventarlo, que es literalmente lo que INDEF-03 prohíbe
       cuando dice «sin mentir sobre su uso real»; el de participio passato lleva **4** porque tiene que
       alojar el par invariable-frente-a-concordado que SC-4 declara magnet y además el uso invariable
       como forma léxica que INDEF-02 pide. El mínimo del motor son 2 variantes por slot, así que el de 2
       es legal y `pickVariantIndex` lo sirve sin cambio.
    4. **CONJUNTO CERRADO DE TIPOS DE CONTEXTO SINTÁCTICO, con su reparto por variante (D-43-12).**
       Enumerar la lista cerrada y decir qué tipo lleva cada una de las 18 variantes. Los tipos son: tras
       preposición; tras modal, verbo o adjetivo que rige la preposición de infinitivo; sujeto o nominal;
       imperativo negativo de 2ª singular; subordinada implícita causal; subordinada implícita temporal;
       subordinada implícita modal; subordinada implícita concesiva; marco progresivo; compuesto
       lexicalizado en singular; compuesto lexicalizado en plural con concordancia; objeto pospuesto en
       compuesto con el auxiliar de posesión; pronombre objeto directo antepuesto con concordancia
       obligatoria. Declarar el gate: **dentro de cada slot los tipos son todos DISTINTOS**, con la única
       excepción ENUMERADA de `fare-indefiniti-participio-passato`, cuyo eje es la terminación por D-43-16
       y que por eso repite dos veces cada uno de sus dos tipos. Y la razón de que esto sea gate y no
       discreción de autoría: en un slot de forma fija, tres variantes que no examinan tres encajes
       distintos son el mismo ejercicio tres veces, y con 18 variantes dejarlo al quórum se paga muchas
       veces.
    5. **POOL CERRADO DE FORMAS NO PERSONALES, grep-verificable (D-43-13).** Las 4 `options` de cualquier
       variante de esta categoría salen SIEMPRE del paradigma no personal: el infinito simple, el infinito
       compuesto elidido, las cuatro terminaciones del participio pasado, el gerundio simple, el gerundio
       compuesto, y el participio presente en singular y en plural. **Ninguna forma conjugada entra en
       `options`** en esta categoría, y eso queda como gate en el fichero de test. La razón de fondo:
       meter una conjugada metería casillas de las otras tres categorías de `fare` en el examen —lo que
       las tres fases anteriores evitaron entre sí— y bajaría el poder discriminante, porque una conjugada
       es descartable de un vistazo cuando el hueco pide una forma no personal. Y la buena noticia: la
       interferencia ya está cubierta por el propio pool, porque el español dice «después de hacer» con
       infinitivo simple, así que el infinito simple es la distractora natural y pedagógicamente correcta
       del compuesto.
    6. **CUARTO MAGNET DEL MILESTONE (D-43-17), con audit trail.** El requisito INDEF-01 escribe el
       infinito compuesto con el paréntesis de la vocal opcional: las dos grafías son correctas y
       atestiguadas. Si una es la key y la otra entra como distractora «incorrecta», el ejercicio es
       injusto, el quórum lo marca `disputed`, `disputed` es STICKY y la cascada D-54 resetea la categoría
       entera. Resolución, mismo tratamiento exacto que el MAGNET del imperativo de la categoría hermana:
       la key es la ELIDIDA, que es la forma normal del infinito compuesto en italiano; la no elidida va a
       la blacklist declarada y no entra en `options` de ninguna variante; y la explanation dice que las
       dos son correctas y que la elidida es la corriente. Registrar que INT-04 declara solo TRES magnets
       y que este es el cuarto, y que **Phase 44 tendrá que recogerlo** al verificar INT-04 o su criterio
       de éxito describirá un estado que no es el real.
    7. **BLACKLIST DE FORMAS ATESTIGUADAS Y DEFENDIBLES, con audit trail.** Cada forma citada entre
       apóstrofes ASCII con el formato exacto de comilla simple, forma, comilla simple, para que el gate
       positivo del fichero de test la encuentre: la grafía no elidida del infinito compuesto (D-43-17),
       las cuatro formas con clítico heredadas de D-43-07, y las arcaicas heredadas de D-41-08 y D-42-11.
       Más el criterio operativo: lo prohibido es la distractora DEFENDIBLE como correcta en ese contexto,
       no la forma atestiguada en abstracto.
    8. **GATE HARD DE PRONOMBRE en el participio passato (D-43-16).** Con pronombre objeto directo
       antepuesto la concordancia del participio es OBLIGATORIA solo con los de 3ª persona; con los de 1ª
       y 2ª es OPCIONAL en italiano estándar y con el partitivo tiene reglas propias. Declarar la lista de
       los cinco pronombres PROHIBIDOS en los prompts de ese slot con su razón —cualquiera de ellos
       produciría dos respuestas defendibles— y la precisión de plan-time: las 2 variantes de concordancia
       usan los de PLURAL y no los de singular, porque con el singular el auxiliar se elide y la vocal
       elidida esconde el género, de modo que el autor tendría que deducirlo solo del antecedente. Es el
       análogo de un gate sistemático cerrado ANTES de escribir, no una red de quórum.
    9. **EXCEPCIÓN ACOTADA AL SCOPE-GATE (D-43-18), con audit trail y frontera explícita.** Choque real
       entre dos reglas del proyecto: INDEF-03 exige el compuesto fosilizado del participio presente y el
       SCOPE-GATE HARD de D-41-06 prohíbe las perífrasis y exige objeto literal transparente. Se resuelve
       a favor del requisito, con la excepción escrita: los DOS compuestos declarados entran en las 2
       variantes de ese slot, y de ningún otro. Escribir la frontera: solo esos dos compuestos, solo en
       ese slot, y CERO perífrasis léxicas del verbo en ningún sitio de las dos categorías de la fase. Y
       escribir la justificación de fondo para que un re-pase futuro no lo «arregle»: D-41-06 vetaba
       perífrasis del VERBO, y un participio fosilizado que funciona como sustantivo o adjetivo no es una
       perífrasis verbal. Anotar que la excepción está escrita también en el prompt de validación por
       quórum, porque el subagent no ve este `notes`.
   10. **RECONOCER, NO PRODUCIR (D-43-19), como principio de la fase, con su tercer caso.** Los otros dos
       viven en la categoría hermana (los dos imperativos vivos vetados como key); el de aquí es la grafía
       no elidida del infinito compuesto. Hay formas italianas atestiguadas que el material NOMBRA pero no
       pide producir, para que el autor las reconozca al leer y no las tome por errores. Es también la
       explicación de por qué están en la blacklist: un solo razonamiento en vez de tres coartadas
       separadas.
   11. **NOTA DE REGISTRO del participio presente (INDEF-03).** La forma es real pero burocrática y
       fosilizada y vive sobre todo en esos dos compuestos; entra para que el paradigma esté completo, sin
       mentir sobre su uso real. Declarar que la explanation del slot lo avisa, porque el requisito dice
       literalmente «la explanation avisa» y el `notes` es metadata que no se muestra en la interfaz.
   12. **EL IMPERATIVO NEGATIVO ENTRA COMO CONTEXTO DEL INFINITO (D-43-14).** El imperativo negativo de 2ª
       singular no es la forma apostrofada negada sino la negación más infinitivo, y es uno de los errores
       más comunes del hispanohablante. No cabía en el slot de imperativo de la categoría hermana porque
       SC-2 fija exactamente 5 variantes, así que entra aquí como una de las 3 del infinito presente: el
       eje de ese slot es el contexto, y el imperativo negativo es un contexto que exige el infinitivo de
       verdad, no uno inventado para rellenar. Declarar que es **cruce conceptual y no técnico**:
       `categoryIds` sigue siendo de longitud 1.
   13. **DECISIONES DE OMISIÓN, SCOPE-GATE, NOTA DE ESCANEO y NOTA DE COUNT-SYNC.** 0-MATCH y
       0-WORD-BUTTONS (DESIGN RULE D-04). 0-GLOSS del verbo (D-41-05). SCOPE-GATE HARD del objeto literal,
       conjunto cerrado de 7 **más las dos inflexiones en plural declaradas** —las variantes de
       concordancia necesitan un antecedente femenino plural y el conjunto heredado no tiene ninguno, así
       que se flexionan dos de sus propios miembros en vez de añadir léxico nuevo—, con exactamente uno en
       la cláusula del hueco y con la exención enumerada del slot de participio presente. `categoryIds` de
       1 en los 6 slots. Ningún id con sufijo numérico de 3 cifras. Los escaneos de ausencia van SIEMPRE
       por campo, sobre `prompt` y `options`, y NUNCA sobre el fichero completo ni sobre `notes` ni sobre
       `explanation`, porque este `notes` nombra a propósito las formas prohibidas y las explanations
       tienen que nombrar la no elidida y el registro burocrático. Y: los arrays hardcoded de conteo,
       `TOTAL_EXPECTED` y el baseline-guard son de Phase 44 / INT-02.

    **Slot 1 de 6: `fare-indefiniti-infinito-presente` (INDEF-01, D-43-14, SC-3)**

    - `id`: `fare-indefiniti-infinito-presente`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-indefiniti"]`.
    - **3 variantes**, las 3 con la misma key `fare`. Lo que las diferencia es el TIPO DE CONTEXTO, y los
      3 son distintos entre sí:
      · variante 1, tipo **imperativo negativo de 2ª singular** — prompt del tipo `Anna, non ___ un
        errore adesso!`, con la negación inmediatamente antes del hueco. Objeto: `un errore`.
        Distractoras del pool: las cuatro terminaciones no; usar el participio invariable, el gerundio
        simple y el infinito compuesto elidido — ninguna de las tres es un imperativo negativo válido.
      · variante 2, tipo **tras preposición** — prompt del tipo `Prima di ___ i compiti, io guardo un
        film.`, con la locución preposicional inmediatamente antes del hueco. Objeto: `i compiti`.
        Distractoras: el infinito compuesto elidido, el gerundio simple y el participio invariable. Es la
        variante que prepara el contraste con el slot de infinito passato de la Task 3.
      · variante 3, tipo **tras modal** — prompt del tipo `Io devo ___ il letto ogni mattina.`, con la
        forma modal inmediatamente antes del hueco. Objeto: `il letto`. Distractoras: el gerundio simple,
        el participio invariable y el participio presente singular. **El infinito compuesto elidido NO
        entra en las opciones de esta variante**: la forma modal con el infinito compuesto es gramatical
        en italiano con lectura epistémica, así que sería una segunda respuesta defendible. Declararlo en
        el `notes` junto al gate de tipos de contexto.
    - Los 3 prompts: hueco literal `___` exactamente una vez, cero español, cero paréntesis, un objeto del
      conjunto cerrado en la cláusula del hueco, y cero perífrasis y modismos del verbo.
    - `options`: 4 strings sin duplicados, **todas del pool cerrado no personal**, y los 3 conjuntos
      distintos dos a dos. `correctIndex` entero en `[0,4)`, no el mismo en las 3.
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal de 3ª persona,
      regla más ejemplo paralelo italiano-español. Debe explicar que el infinito simple es la forma no
      personal por defecto y enumerar los tres encajes que este slot examina; y **desarrollar el
      imperativo negativo** como la trampa concreta: en italiano la orden negativa de tú se construye con
      la negación más el INFINITIVO, no con la forma del imperativo afirmativo, que es justo donde el
      hispanohablante calca su propia lengua. No referencia ningún ejercicio por id y no menciona el
      participio presente ni las otras casillas, que son de las Tasks 3 y 4.
    - `validation`: `{ "status": "pending", "passes": [] }`. No se fabrica ningún pase.

    **C. `tests/exercise-types.test.js` — 1 línea en `CATEGORIES_WITH_EXPLANATIONS`**

    Tras la entrada de `fare-cond-imperativo` que dejó el plan 43-01 y ANTES del comentario de cobertura
    editorial que cierra el array: un comentario de una línea que nombre la fase y los requisitos (v2.0
    Phase 43, INDEF-01..INDEF-04, `fare-indefiniti`, 6 slots de formas no personales) y la entrada con
    `file: 'content/exercises/fare-indefiniti.json'` y
    `expected: slotCountOf('content/exercises/fare-indefiniti.json')` — dinámico, NUNCA número mágico. No
    tocar ningún otro array de conteo. Con esta línea el array queda cerrado para la fase: las 4
    categorías de v2.0 están dentro.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js && node -e "const fs=require('fs');const cats=JSON.parse(fs.readFileSync('content/categories.json','utf8')).categories;const e=cats.find(c=>c.id==='fare-indefiniti');if(!e)throw new Error('falta la entrada de categories.json');if(e.order!==18||e.origen!=='ia-quorum')throw new Error('entrada mal formada: '+JSON.stringify(e));if(Object.keys(e).join(',')!=='id,name,order,origen')throw new Error('claves inesperadas: '+Object.keys(e));if(cats.length!==18)throw new Error('esperaba 18 categorias, hay '+cats.length);if(!cats.every((c,i)=>c.order===i+1))throw new Error('orders no contiguos');const d=JSON.parse(fs.readFileSync('content/exercises/fare-indefiniti.json','utf8'));if(Object.keys(d).join(',')!=='notes,exercises')throw new Error('claves top-level inesperadas: '+Object.keys(d));const n=d.exercises.filter(x=>(x.categoryIds||[]).includes('fare-indefiniti')).length;if(n===0)throw new Error('totalCount 0 -> examenEnabled false -> la fila no es jugable');console.log('SC-5 OK: fila order '+e.order+', totalCount '+n)"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con `pass` mayor que el medido en la precondición.
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; if(c.length!==18) process.exit(1); const e=c[17]; process.exit(e.id==='fare-indefiniti'&&e.order===18&&e.origen==='ia-quorum'&&Object.keys(e).join(',')==='id,name,order,origen'?0:1)"` sale con código 0.
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; process.exit(c.every((x,i)=>x.order===i+1)&&c[17].name.includes(String.fromCharCode(8212))?0:1)"` sale con código 0 — `order` contiguos 1..18 e índice igual a `order - 1`, y raya em en el `name`; los gates de registro de `fare-indicativo`, `fare-congiuntivo` y `fare-cond-imperativo` siguen verdes.
    - `git diff content/categories.json | grep -c '^-'` devuelve `1` — la única línea eliminada es la de `order: 17` (reescrita con coma).
    - `grep -c "slotCountOf('content/exercises/fare-indefiniti.json')" tests/exercise-types.test.js` devuelve `1`, y `grep -c 'slotCountOf' tests/exercise-types.test.js` devuelve `9` (5 previas + `fare-indicativo` + `fare-congiuntivo` + `fare-cond-imperativo` + esta; re-contar sobre el fichero real y ajustar el número esperado a «el de antes más uno» si el conteo previo difiere).
    - `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/fare-indefiniti.json','utf8')); const s=d.exercises[0]; process.exit(d.exercises.length===1&&s.id==='fare-indefiniti-infinito-presente'&&s.type==='multiple-choice'&&s.categoryIds.length===1&&s.categoryIds[0]==='fare-indefiniti'&&s.variants.length===3?0:1)"` sale con código 0.
    - `node -e` sobre `exercises[0]`: el array de `options[correctIndex]` de las 3 variantes es exactamente `["fare","fare","fare"]` — la key se repite por diseño, y lo que diferencia a las variantes es el tipo de contexto (SC-3, D-43-12).
    - `node -e` sobre las 3 variantes: la unión de todas sus `options` está contenida en el pool cerrado `["fare","aver fatto","fatto","fatta","fatti","fatte","facendo","avendo fatto","facente","facenti"]`; los 3 conjuntos de `options` son distintos dos a dos; ninguna lista tiene duplicados internos; `correctIndex` toma al menos 2 valores distintos (D-43-13).
    - `node -e` sobre las `options` de la variante de tipo «tras modal»: **0** coincidencias exactas con `aver fatto` — la lectura epistémica de la forma modal con el infinito compuesto la haría defendible (D-43-13; edge: ambiguity boundary).
    - Escaneo `node -e` sobre los 3 `prompt`: los 3 cumplen `prompt.split('___').length === 2`; el de tipo imperativo negativo lleva la negación como palabra inmediatamente anterior al hueco; el de tipo preposición lleva la locución preposicional inmediatamente anterior al hueco; el de tipo modal lleva una forma modal inmediatamente anterior al hueco; y los 3 tipos declarados en la tabla son distintos entre sí (D-43-12, D-43-14).
    - Escaneo `node -e` sobre los 3 `prompt`: **0** contienen la subcadena `espa` en ninguna capitalización y **0** contienen paréntesis de apertura (D-41-05, D-43-22).
    - Escaneo `node -e` sobre `prompt` y `options[]` con coincidencia **EXACTA** en `options`: **0** coincidencias con `avere fatto`, **0** con las formas con clítico `["fallo","fammi","fatelo","facci"]`, **0** con las arcaicas heredadas, y **0** con la unión de las formas conjugadas de `fare` presentes en `fare-indicativo.json`, `fare-congiuntivo.json` y `fare-cond-imperativo.json` (D-43-13, D-43-17).
    - Escaneo `node -e` sobre `prompt` y `options[]`: cada prompt contiene exactamente **un** objeto del conjunto cerrado ampliado `["i compiti","un errore","il lavoro","una torta","il letto","tutto","una foto","le foto","le torte"]` en el segmento del hueco (cortando por `,;.:` y raya), y hay **0** coincidencias para los marcadores de perífrasis `["colazione","spesa","freddo","farcel","far fare","causativo"]` (D-41-06, §Correcciones 4).
    - Escaneo `node -e` sobre TODOS los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:`, comillas y apóstrofes tipográficos, y backticks (T-43-01, D-43-21).
    - Escaneo `node -e` recursivo sobre el objeto parseado: ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`, y `({}).polluted === undefined` tras el parse (T-43-02).
    - `node -e` sobre `exercises[0].explanation`: contiene al menos un carácter del conjunto `áéíóúñÁÉÍÓÚÑ`; contiene la subcadena `infinitivo`; desarrolla el imperativo negativo (contiene las subcadenas `negativ` y `orden` o equivalente comprobable); su longitud es `> 500`; y no coincide con el patrón de cross-ref R2 (D-43-14).
    - `node -e` sobre `notes`: longitud `> 6000` caracteres; contiene la grafía no elidida del infinito compuesto entre apóstrofes ASCII con el formato `'forma'`, más cada forma con clítico y cada arcaica de la blacklist con el mismo formato; contiene los marcadores literales en MAYÚSCULAS `EJE = CONTEXTO`, `REPARTO DESIGUAL`, `CONJUNTO CERRADO DE TIPOS DE CONTEXTO`, `POOL CERRADO`, `CUARTO MAGNET`, `GATE HARD DE PRONOMBRE`, `EXCEPCIÓN ACOTADA AL SCOPE-GATE`, `RECONOCER, NO PRODUCIR`, `NOTA DE REGISTRO`; contiene `22 slots`, `113` y `247`; y contiene los identificadores `D-43-03`, `D-43-12`, `D-43-13`, `D-43-14`, `D-43-15`, `D-43-16`, `D-43-17`, `D-43-18`, `D-43-19`, `D-43-22`.
    - `node -e`: `exercises[0].validation.status === 'pending'` y `passes.length === 0`; ningún id coincide con `/-\d{3}$/`; el `id` del slot empieza por el slug COMPLETO `fare-indefiniti-` (D-40-03, D-43-02).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando ahora **4** slots `pending` (los 3 de `fare-cond-imperativo` y el nuevo), y su salida cita `content/exercises/fare-indefiniti.json`.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ tests/fixtures/ content/exercises/fare-cond-imperativo.json tests/content-fare-cond-imperativo.test.js` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    `content/exercises/fare-indefiniti.json` existe con su `notes` completo (las 13 declaraciones, con el
    eje CONTEXTO, el reparto desigual justificado, la lista cerrada de tipos, el pool cerrado, el cuarto
    magnet, el gate HARD de pronombre y la excepción acotada al SCOPE-GATE escritos ANTES de la primera
    distractora) y un solo slot `fare-indefiniti-infinito-presente` de 3 variantes en `pending`, cuyas 3
    keys son la misma forma y cuyos 3 tipos de contexto son distintos; `content/categories.json` tiene 18
    entradas con `order` contiguos; la línea del smoke está puesta y sus sub-tests editoriales pasan;
    `node --test tests/*.test.js` sigue en `fail 0`; y la fila nueva es jugable por dato sin que `src/`
    haya cambiado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gate del tracer — la segunda categoría se ve y se juega, y tres frases con la misma respuesta no son el mismo ejercicio (SC-3, SC-5)</name>
  <what-built>
    `content/exercises/fare-indefiniti.json` con el slot `fare-indefiniti-infinito-presente` (3 variantes,
    las 3 con la misma key), la entrada `fare-indefiniti` en `content/categories.json` (order 18) y la
    línea nueva del smoke paramétrico. La suite completa está en `fail 0` y `src/` no se ha tocado.

    Este checkpoint es el gate del tracer de la segunda categoría. Lo que se comprueba a ojo son las dos
    cosas que ningún test puede decidir: que las tres frases que comparten respuesta examinen de verdad
    tres cosas distintas —si no, es el mismo ejercicio tres veces— y que en cada una la respuesta sea
    ÚNICA dentro del pool cerrado de formas no personales. Si eso está mal, está mal una vez y no
    dieciocho.
  </what-built>
  <how-to-verify>
    1. Arrancar el servidor local desde la raíz del repo: `npx serve` (o Live Server) y abrir la URL que
       imprime, normalmente `http://localhost:3000`.
    2. **Home:** deben aparecer **DOS** filas nuevas al final y no una: `Fare — condizionale e imperativo`
       (de la wave 1) y `Fare — formas indefinidas (fare/fatto/facendo)`, las dos con estado `no-hecha`,
       racha `0 / 21 d` y el botón `Examen` HABILITADO. Son dos unidades de reset SEPARADAS: comprobar que
       están en dos filas distintas y no fundidas (SC-5). Las 16 filas anteriores siguen sin cambios.
    3. **Examen de la categoría:** pulsar `Examen` en la fila nueva. Debe presentar un multiple-choice con
       4 opciones y **sin ninguna traducción al español** en el enunciado.
    4. **La prueba del eje CONTEXTO (SC-3, lo más importante de este checkpoint):** entrar y salir hasta
       ver las tres frases. Las tres tienen la MISMA respuesta correcta. La pregunta a responder es: ¿me
       están preguntando tres cosas distintas o la misma tres veces? Deben notarse tres encajes
       sintácticos claramente distintos — una orden negativa, una locución preposicional y un verbo modal.
       Si dos de ellas se sienten iguales, reportarlo: es el modo de fallo que este slot existe para
       cerrar.
    5. **La prueba de la unicidad:** en cada frase, mirar las cuatro opciones y preguntarse si alguna
       segunda opción «también podría valer». Todas son formas reales del verbo, así que la única defensa
       es el encaje: si en alguna hay dos que encajan, reportarla — es un `disputed` que resetearía la
       categoría entera.
    6. **La prueba del imperativo negativo:** en la frase que es una orden negativa, comprobar que suena a
       orden y no a otra cosa, y que la respuesta correcta es el infinitivo. Es la trampa que el español
       calca al revés.
    7. **Rotación de contexto:** entrar y salir 4 o 5 veces; la frase debe cambiar entre pasadas — eso es
       `pickVariantIndex` sirviendo un eje que no es la persona, sin código nuevo.
    8. **Repaso:** volver a home, entrar en `Repaso` y comprobar que `fare-indefiniti` aparece como opción
       seleccionable en el picker, separada de `fare-indicativo` (ojo: los dos nombres empiezan igual, así
       que comprobar que son dos entradas distintas y que la selección de una no marca la otra).
    9. **Explicación:** contestar y pulsar `¿Por qué?` (o la tecla `e`). Debe leerse en español con
       acentos, en un solo párrafo, sin markdown; debe explicar los tres encajes y desarrollar la trampa
       del imperativo negativo.
  </how-to-verify>
  <resume-signal>Escribe `aprobado` para desbloquear la autoría de los otros 5 slots, o describe qué falla (las dos frases que se sienten iguales, la opción que también valdría, el encaje que no se nota) para corregir la forma ANTES de replicarla quince veces.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Expansión A — `fare-indefiniti-infinito-passato` (3 variantes, el CUARTO MAGNET) y `fare-indefiniti-participio-passato` (4 variantes, el gate HARD de pronombre)</name>

  <files>content/exercises/fare-indefiniti.json</files>

  <read_first>
    - `content/exercises/fare-indefiniti.json` tal como quedó tras la Task 1 y las correcciones del
      checkpoint — el `notes` y el slot del infinito presente son el molde literal: mismo orden de campos,
      mismos 3 campos por variante, mismo `validation` en `pending`, mismo conjunto cerrado de objetos.
    - `content/exercises/riflessivi.json`, slot `riflessivi-pp-concordanza` — el análogo de CONTENIDO
      directo del slot de participio passato: su pool fijo de terminaciones en `options`, su cue de
      concordancia por prompt, sus distractoras que son la concordancia incorrecta, y su `explanation`
      como molde de tono para la interferencia con el español (la trampa de que en español el participio
      con el auxiliar no cambia). Copiar el patrón de ahí, no reinventarlo.
    - `content/exercises/fare-indicativo.json`, slot de los compuestos con el auxiliar de posesión — la
      confirmación por dato de que en esos cuatro compuestos el participio es INVARIABLE, que es
      exactamente el comportamiento que las 2 variantes invariables de este slot replican y el contraste
      del que sale el magnet.
    - `.planning/REQUIREMENTS.md` líneas 45-46 — el texto literal de INDEF-01, con la grafía
      `aver(e) fatto` que es el origen del cuarto magnet, y de INDEF-02, con «como forma léxica y su
      comportamiento con `avere` (invariable) frente a la concordancia cuando la rige un pronombre objeto».
    - `.planning/ROADMAP.md` §Phase 43, criterios de éxito 3 y 4 — la letra que la verificación comprueba,
      en particular «infinito presente vs passato elegido por anterioridad respecto a la principal» y «con
      ronda EXTRA de quórum sobre ese par».
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-CONTEXT.md` §decisions —
      D-43-12, D-43-13, D-43-16, D-43-17, D-43-19, D-43-20, D-43-21, D-43-22.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-PATTERNS.md`, la fila 8 de la
      tabla de los 13 `describe` — el gate que la Task 5 escribirá sobre estos dos slots.
    - `src/data/schema-validator.js` — la superficie MC que cada variante debe cumplir.
  </read_first>

  <reversibility rating="reversible">Fija la key elidida del infinito compuesto y con ella la blacklist de la no elidida. Invertirla es una edición local del slot más un re-pase de quórum de 3 variantes (D-43-17 lo califica así explícitamente). No se inserta `checkpoint:decision`.</reversibility>

  <action>
    Añadir DOS slots al array `exercises`, después del infinito presente y en este orden, con el molde
    exacto del tracer. Los dos con `validation`: `{ "status": "pending", "passes": [] }`. **Son los dos
    slots de ronda EXTRA de esta categoría (D-43-20), así que la exigencia de unicidad de lectura es
    máxima: cualquier duda se resuelve reescribiendo el prompt, nunca dejándola al quórum.**

    **Slot 2 de 6: `fare-indefiniti-infinito-passato` (INDEF-01, SC-3, el CUARTO MAGNET)**

    - `id`: `fare-indefiniti-infinito-passato`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-indefiniti"]`.
    - **3 variantes**, las 3 con la misma key: la forma ELIDIDA del infinito compuesto. La grafía no
      elidida **no aparece en ningún `prompt` ni en ninguna `options[]` de todo el fichero** — es el
      cuarto magnet y su tratamiento es key para una, blacklist declarada para la otra (D-43-17).
    - **Gate HARD de anterioridad**, que es lo que hace que la respuesta sea única frente al infinito
      simple: cada prompt fuerza la anterioridad de una de estas dos maneras, y los 3 tipos de contexto
      son distintos entre sí:
      · variante 1, tipo **tras preposición** — la preposición de posterioridad gobierna la cláusula del
        hueco, del tipo `Dopo ___ i compiti, io sono uscito con Anna.`. Objeto: `i compiti`. Es la
        variante que cierra el contraste con la variante 2 del slot anterior, y su distractora estrella es
        el infinito simple, que es el calco literal del español «después de hacer».
      · variante 2, tipo **tras verbo o adjetivo que rige la preposición de infinitivo, con deíctico de
        pasado** — del tipo `Maria è contenta di ___ il lavoro la settimana scorsa.`. Objeto: `il lavoro`.
        El deíctico de pasado en la cláusula del hueco es lo que hace agramatical el infinito simple.
      · variante 3, tipo **subordinada implícita causal, con deíctico de pasado** — del tipo
        `Per ___ un errore ieri, lui ha perso tutto.`. Objeto en la cláusula del hueco: `un errore` (el
        `tutto` de la segunda cláusula queda fuera del segmento del hueco al cortar por la coma, así que
        no cuenta doble). Sin el deíctico de pasado, la preposición causal admitiría la lectura de
        finalidad con el infinito simple.
      El conjunto cerrado de deícticos de pasado admitidos es `ieri`, `la settimana scorsa`,
      `l'anno scorso`, `stamattina`, `il mese scorso`. Declararlo en el `notes` junto al gate de tipos.
    - `options`: 4 strings sin duplicados, todas del pool cerrado. Las 3 distractoras de cada variante
      salen de: el infinito simple (obligatoria en las 3, porque es la interferencia que INDEF-01 nombra),
      el gerundio simple, el gerundio compuesto, y una terminación del participio pasado. **Comprobar
      variante a variante que ninguna de las tres es gramatical en ese encaje**: en particular, el
      gerundio compuesto tras la preposición de posterioridad y tras la preposición de infinitivo es
      agramatical, y por eso es distractora legítima y no una segunda respuesta.
    - `correctIndex`: entero en `[0,4)`, no el mismo en las 3.
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal. Debe explicar
      que el infinito compuesto marca ANTERIORIDAD respecto al verbo principal y que esa es la decisión
      que el slot examina; desarrollar la interferencia concreta —el español dice «después de hacer» con
      infinitivo SIMPLE donde el italiano exige el COMPUESTO—; y **decir explícitamente que la grafía no
      elidida también es correcta y que la elidida es la corriente**, que es la línea del principio
      RECONOCER NO PRODUCIR aplicada a este magnet (D-43-17, D-43-19, INDEF-01). No referencia ningún
      ejercicio por id.

    **Slot 3 de 6: `fare-indefiniti-participio-passato` (INDEF-02, SC-4, el gate HARD de pronombre)**

    - `id`: `fare-indefiniti-participio-passato`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-indefiniti"]`.
    - **4 variantes**. Eje ÚNICO: la TERMINACIÓN. Las 4 `options` de las 4 variantes son **siempre el
      mismo conjunto** de las cuatro terminaciones del participio pasado, con el orden variado entre
      variantes para que `correctIndex` no sea constante. Las 4 keys, en el orden de las variantes, son la
      invariable, la invariable, la de masculino plural y la de femenino plural.
    - **Reparto 2 + 2 declarado en tabla** (D-43-16, SC-4):
      · variante 1, tipo **objeto pospuesto en compuesto con el auxiliar de posesión** — del tipo
        `Ieri io ho ___ i compiti con Anna.`. Objeto masculino PLURAL pospuesto y sin pronombre
        antepuesto: el participio es INVARIABLE aunque el objeto sea plural, que es justo lo que hace
        valiosa la variante. Key: la invariable.
      · variante 2, tipo **objeto pospuesto en compuesto con el auxiliar de posesión** — del tipo
        `Maria ha ___ una torta per la festa.`. Objeto femenino SINGULAR pospuesto: el participio sigue
        siendo invariable. Key: la invariable. Es el «como forma léxica» que INDEF-02 pide.
      · variante 3, tipo **pronombre objeto directo antepuesto con concordancia obligatoria** — del tipo
        `I compiti? Marco li ha ___ ieri sera.`. Key: la de masculino plural.
      · variante 4, tipo **pronombre objeto directo antepuesto con concordancia obligatoria** — del tipo
        `Le foto? Noi le abbiamo ___ in montagna.`. Key: la de femenino plural.
      Los tipos de las variantes 1 y 2 coinciden, y los de las 3 y 4 también: **es la única excepción
      enumerada al gate de tipos distintos por slot** (D-43-12), porque D-43-16 fija el eje en la
      terminación y exige exactamente 2 + 2. Va declarada por id de slot en el `notes` y en la cabecera
      del fichero de test.
    - **GATE HARD de pronombre (D-43-16, §Correcciones 5), no negociable:**
      · **0** apariciones como palabra suelta de los pronombres de 1ª y 2ª persona y del partitivo en los
        4 prompts de este slot: con los primeros la concordancia es OPCIONAL en italiano estándar y con el
        partitivo tiene reglas propias, así que cualquiera de ellos abriría dos respuestas defendibles.
      · Las 2 variantes de concordancia usan pronombre de 3ª persona **PLURAL** (masculino en la 3,
        femenino en la 4), inmediatamente antes de una forma del auxiliar de posesión. **Prohibidos los de
        3ª persona singular antepuestos**, en cualquiera de sus formas incluida la elidida ante el
        auxiliar: la vocal elidida no dice el género y el autor tendría que deducirlo solo del
        antecedente, con lo que la forma invariable pasaría a ser defendible.
      · Las 2 variantes invariables **no llevan ningún pronombre objeto antepuesto**: su objeto va después
        del participio, que es lo que hace que la invariabilidad sea la única lectura.
    - Objeto literal: del conjunto cerrado ampliado de 9 (los 7 heredados más las dos inflexiones en
      plural declaradas en §Correcciones 4), exactamente uno en la cláusula del hueco de cada prompt. Es
      la variante 4 la que necesita la inflexión femenina plural, porque el conjunto heredado no tiene
      ningún femenino plural y sin él la concordancia femenina no se puede examinar.
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal, y es la más
      cargada de este plan porque tiene que hacer TRES cosas: (a) enunciar la regla del doble
      comportamiento —con el auxiliar de posesión y el objeto pospuesto el participio no cambia; con el
      pronombre objeto directo antepuesto la concordancia es obligatoria—; (b) **desarrollar el par
      explícito de la interferencia**, que es lo que SC-4 y el análogo de `riflessivi` piden: el español
      NO concuerda («las he hecho») y el italiano SÍ («le ho fatte»); y (c) enumerar las cuatro
      terminaciones y decir con qué antecedente va cada una. No referencia ningún ejercicio por id.

    Reglas duras comunes a los dos slots: hueco literal `___` exactamente una vez por prompt, todas las
    `options` del pool cerrado no personal, cero español en el prompt, cero paréntesis, cero corchetes
    angulares, cero backticks, cero comillas ni apóstrofes tipográficos, cero perífrasis y modismos del
    verbo, y la grafía no elidida del infinito compuesto ausente de todo `prompt` y de toda `options[]`.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 3`; los ids son exactamente `fare-indefiniti-infinito-presente`, `fare-indefiniti-infinito-passato` y `fare-indefiniti-participio-passato` en ese orden; `variants.length` es `3`, `3` y `4`; los 3 con `type === 'multiple-choice'` y `categoryIds` de longitud 1.
    - `node -e`: el array de keys de `fare-indefiniti-infinito-passato` es exactamente `["aver fatto","aver fatto","aver fatto"]` (INDEF-01, D-43-17).
    - `node -e`: el array de keys de `fare-indefiniti-participio-passato` es exactamente `["fatto","fatto","fatti","fatte"]` en ese orden (INDEF-02, D-43-16, SC-4).
    - `node -e` sobre las 4 variantes del participio passato: en las 4, `[...options].sort()` es exactamente `["fatta","fatte","fatti","fatto"]` — mismo pool en las 4, orden variado; y `correctIndex` toma al menos 2 valores distintos (D-43-16).
    - Escaneo `node -e` sobre `prompt` y `options[]` de TODO el fichero con coincidencia exacta en `options` y por palabra suelta en `prompt`: **0** apariciones de la cadena `avere fatto` (el cuarto magnet, D-43-17).
    - Escaneo `node -e` sobre los 4 `prompt` del participio passato, por palabra suelta e insensible a mayúsculas: **0** apariciones de `mi`, `ti`, `ci`, `vi` y `ne` (D-43-16, gate HARD).
    - Escaneo `node -e` sobre los 4 `prompt` del participio passato: **exactamente 2** contienen un bigrama del conjunto cerrado de concordancia `["li ha","le ha","li ho","le ho","li hai","le hai","li abbiamo","le abbiamo","li avete","le avete","li hanno","le hanno"]`, y **exactamente 2** no contienen ninguno; y **0** de los 4 contienen un bigrama del conjunto prohibido de singular antepuesto `["lo ha","la ha","lo ho","la ho","l'ho","l'ha","l'hai","l'abbiamo","l'avete","l'hanno"]` (D-43-16, §Correcciones 5).
    - `node -e`: las 2 variantes con bigrama de concordancia son exactamente aquellas cuya key no es la forma invariable, y las 2 sin bigrama son exactamente aquellas cuya key sí lo es — el reparto 2+2 es coherente con las keys y no solo con la tabla (D-43-16, SC-4).
    - Escaneo `node -e` sobre los 3 `prompt` de `fare-indefiniti-infinito-passato`: cada uno cumple **al menos una** de las dos condiciones de anterioridad — la preposición de posterioridad gobierna la cláusula del hueco (está en el segmento del hueco al cortar por `,;.:` y raya, delante del hueco), o la cláusula del hueco contiene un deíctico del conjunto cerrado `["ieri","la settimana scorsa","l'anno scorso","stamattina","il mese scorso"]`; y los 3 tipos de contexto declarados son distintos entre sí (INDEF-01, D-43-12).
    - `node -e` sobre las 3 variantes del infinito passato: en las 3, el infinito simple `fare` está entre las `options` (es la distractora del calco que INDEF-01 nombra); y la unión de todas las `options` del slot está contenida en el pool cerrado (D-43-13).
    - Escaneo `node -e` sobre las 10 variantes del fichero: la unión de todas las `options` está contenida en `["fare","aver fatto","fatto","fatta","fatti","fatte","facendo","avendo fatto","facente","facenti"]`; **0** opciones pertenecen a la unión de las formas conjugadas de `fare` presentes en las otras tres categorías; ninguna lista tiene duplicados internos (D-43-13, SC-3).
    - Escaneo `node -e` sobre los 10 `prompt`: los 10 cumplen `prompt.split('___').length === 2`; **0** contienen `espa`; **0** contienen paréntesis de apertura; cada uno contiene exactamente **un** objeto del conjunto cerrado ampliado de 9 en el segmento del hueco; **0** coincidencias con los marcadores de perífrasis.
    - Escaneo `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:`, comillas y apóstrofes tipográficos y backticks; ninguna clave propia peligrosa.
    - `node -e` sobre la `explanation` de `fare-indefiniti-infinito-passato`: contiene al menos un carácter acentuado del español; contiene la subcadena `anterioridad`; contiene la cadena `avere fatto` (escaneo POSITIVO — la explanation TIENE que nombrarla por D-43-17 y D-43-19, y por eso la blacklist nunca se escanea sobre `explanation`); su longitud es `> 600`; y no coincide con el patrón de cross-ref R2.
    - `node -e` sobre la `explanation` de `fare-indefiniti-participio-passato`: contiene al menos un carácter acentuado; contiene las 4 terminaciones; contiene el par explícito de la interferencia (la forma española invariable y la italiana concordada, las dos citadas); su longitud es `> 700`; y no coincide con el patrón de cross-ref R2 (D-43-16, SC-4).
    - `node -e`: los 3 slots tienen `validation.status === 'pending'` y `passes.length === 0`; ningún id coincide con `/-\d{3}$/`.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ content/categories.json tests/ docs/ .claude/` no produce ninguna línea de salida — esta tarea toca UN solo fichero.
  </acceptance_criteria>

  <done>
    El fichero tiene 3 slots y 10 variantes en `pending`. El infinito passato tiene la forma elidida como
    key en sus 3 variantes, la no elidida ausente de todo campo, y cada prompt fuerza la anterioridad de
    forma comprobable. El participio passato tiene 4 variantes con el mismo pool de las cuatro
    terminaciones, el reparto 2 invariables + 2 concordadas coherente con sus keys, cero pronombres de
    concordancia opcional o partitiva, cero pronombres singulares antepuestos, y una explanation que
    desarrolla el par «las he hecho» frente a la forma italiana concordada. La suite completa sigue en
    `fail 0` con el motor sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 4: Expansión B — `fare-indefiniti-participio-presente` (2 variantes, la excepción acotada), `fare-indefiniti-gerundio-presente` (3) y `fare-indefiniti-gerundio-passato` (3)</name>

  <files>content/exercises/fare-indefiniti.json</files>

  <read_first>
    - `content/exercises/fare-indefiniti.json` tal como quedó tras la Task 3 — los 3 slots ya escritos son
      el molde literal, y su `notes` ya declara la excepción acotada y la lista cerrada de tipos que estos
      tres slots consumen.
    - `.planning/REQUIREMENTS.md` líneas 47-48 — el texto literal de INDEF-03 («con nota de registro
      explícita… la explanation avisa de que es burocrático/fosilizado y vive sobre todo en compuestos…
      sin mentir sobre su uso real») y de INDEF-04 («incluido `stare + gerundio` como progresivo»).
    - `.planning/ROADMAP.md` §Phase 43, criterios de éxito 3 y 4 — «gerundio presente vs passato incluido
      `stare + gerundio` como progresivo» y «`facente` entra en el paradigma CON nota de registro
      explícita».
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-CONTEXT.md` §decisions —
      D-43-12, D-43-13, D-43-15, D-43-18, D-43-19, D-43-21, D-43-22; y §specifics, la entrada sobre
      `stare + gerundio` («el error no es formarlo, es usarlo de más»).
    - `.planning/phases/41-fare-indicativo-8-slots-el-bloque-grande/41-CONTEXT.md` D-41-06 — el texto
      literal del SCOPE-GATE HARD al que D-43-18 declara su excepción, para poder escribir la frontera con
      precisión y no de memoria.
    - `src/data/schema-validator.js` — la superficie MC que cada variante debe cumplir.
  </read_first>

  <reversibility rating="reversible">Fija los dos compuestos fosilizados del participio presente como excepción al SCOPE-GATE. Revertirlo es una edición local del slot más un re-pase de quórum de 2 variantes (D-43-18 lo califica así explícitamente). No se inserta `checkpoint:decision`.</reversibility>

  <action>
    Añadir TRES slots al array `exercises`, después del participio passato y en este orden, con el molde
    exacto de los anteriores. Los tres con `validation`: `{ "status": "pending", "passes": [] }`.

    **Slot 4 de 6: `fare-indefiniti-participio-presente` (INDEF-03, SC-4, D-43-18)**

    - `id`: `fare-indefiniti-participio-presente`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-indefiniti"]`.
    - **2 variantes**, no 3. El reparto está justificado en el `notes` y es el mínimo legal del motor: la
      forma es fosilizada y sus contextos reales están contados, y forzar un tercero sería inventarlo, que
      es literalmente lo que INDEF-03 prohíbe.
    - Las 2 keys son el participio presente en **singular** y en **plural**, y los 2 tipos de contexto son
      distintos entre sí:
      · variante 1, tipo **compuesto lexicalizado en singular** — el compuesto de sustitución en el cargo,
        del tipo `Il direttore ___ funzione ha firmato il documento stamattina.`, con el sustantivo del
        compuesto inmediatamente DESPUÉS del hueco. Key: el singular.
      · variante 2, tipo **compuesto lexicalizado en plural con concordancia** — el compuesto de
        pertenencia, del tipo `I paesi ___ parte dell'accordo devono rispettare le nuove regole.`, con el
        sustantivo del compuesto inmediatamente después del hueco. Key: el plural. La distractora estrella
        de esta variante es el singular, porque el error real aquí es no concordar el participio con el
        sintagma plural que lo rige.
    - **EXCEPCIÓN ACOTADA al SCOPE-GATE (D-43-18), enumerada por id de slot:** estas 2 variantes quedan
      exentas del requisito de llevar un objeto del conjunto cerrado de 9, y a cambio **tienen que**
      contener uno de los dos sustantivos del compuesto declarado inmediatamente después del hueco. El
      escaneo de perífrasis y modismos del verbo **sigue mordiendo** en estas dos variantes igual que en
      las otras 16: la exención es del objeto literal, no del gate de perífrasis. No inventar un tercer
      compuesto ni un contexto burocrático genérico construido.
    - `options`: 4 strings sin duplicados, todas del pool cerrado. Las 3 distractoras salen de: la otra
      forma del participio presente (singular frente a plural), el gerundio simple, el infinito simple y
      una terminación del participio pasado. Comprobar variante a variante que ninguna es gramatical en el
      encaje del compuesto.
    - `correctIndex`: entero en `[0,4)`, distinto entre las 2 variantes.
    - `explanation`: párrafo en español acentuado RAE, plain text, tono impersonal, y **lleva la NOTA DE
      REGISTRO que INDEF-03 exige literalmente**: decir que la forma es real pero de registro burocrático
      y administrativo, que está fosilizada, que vive sobre todo en los dos compuestos que el slot
      examina, y que entra en el material para que el paradigma esté completo — sin dar a entender que se
      use en la lengua corriente. Explicar además cómo se forma y que concuerda en número cuando el
      sintagma que la rige es plural. No referencia ningún ejercicio por id.

    **Slot 5 de 6: `fare-indefiniti-gerundio-presente` (INDEF-04, D-43-15, SC-3)**

    - `id`: `fare-indefiniti-gerundio-presente`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-indefiniti"]`.
    - **3 variantes**, las 3 con la misma key: el gerundio simple. Los 3 tipos de contexto son distintos:
      · variante 1, tipo **marco progresivo** (D-43-15, INDEF-04) — una forma del verbo de estado
        inmediatamente antes del hueco, del tipo `Adesso io sto ___ i compiti, ti chiamo dopo.`. Objeto:
        `i compiti`. Es la variante que hace que `stare + gerundio` se EXAMINE y no solo se mencione.
      · variante 2, tipo **subordinada implícita modal** — del tipo `Si impara l'italiano ___ un errore
        dopo l'altro.`. Objeto: `un errore`.
      · variante 3, tipo **subordinada implícita causal** — del tipo `___ tutto in fretta, lei ha rotto un
        piatto.`. Objeto en la cláusula del hueco: `tutto`.
    - **Gate de exclusión del gerundio compuesto**, declarado en el `notes`: el gerundio compuesto entra
      en las `options` **solo** en la variante del marco progresivo, donde el verbo de estado lo hace
      agramatical. En las variantes 2 y 3 **no entra**, porque en una subordinada implícita modal o causal
      el gerundio compuesto es igualmente gramatical con lectura de anterioridad y sería una segunda
      respuesta defendible. Sus distractoras salen del resto del pool: infinito simple, participio
      invariable, participio presente.
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal. Debe enumerar
      los tres encajes; y sobre todo **desarrollar la interferencia real de `stare + gerundio`, que no es
      la formación sino el USO**: el italiano lo restringe a la acción en curso ahora mismo, mientras el
      español extiende «estar + gerundio» mucho más, así que el error del hispanohablante es usarlo de
      MÁS, no construirlo mal (D-43-15). No referencia ningún ejercicio por id.

    **Slot 6 de 6: `fare-indefiniti-gerundio-passato` (INDEF-04, SC-3)**

    - `id`: `fare-indefiniti-gerundio-passato`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-indefiniti"]`.
    - **3 variantes**, las 3 con la misma key: el gerundio compuesto. Los 3 tipos de contexto son
      distintos, y cada prompt lleva un marcador de anterioridad o de concesión del conjunto cerrado
      declarado:
      · variante 1, tipo **subordinada implícita temporal** — con un adverbial de anterioridad explícito,
        del tipo `___ i compiti la sera prima, io ho potuto dormire fino a tardi.`. Objeto: `i compiti`.
        **Es la ÚNICA variante del slot cuyas `options` incluyen el gerundio simple**: el adverbial de
        anterioridad lo hace agramatical, porque el gerundio simple exige contemporaneidad con el verbo
        principal, y es justo donde el hispanohablante calca su propia lengua. Ver §Assumptions: es el
        `backstop` de este plan.
      · variante 2, tipo **subordinada implícita causal** — con adverbial de anterioridad, del tipo
        `___ il lavoro il giorno prima, lei era già libera.`. Objeto: `il lavoro`. **Sin gerundio simple
        entre las opciones**; sus distractoras salen del infinito simple, una terminación del participio
        pasado y el participio presente.
      · variante 3, tipo **subordinada implícita concesiva** — con la partícula concesiva inmediatamente
        antes del hueco, del tipo `Pur ___ una torta per tutti, lui non è stato ringraziato.`. Objeto:
        `una torta`. **Sin gerundio simple entre las opciones**: la partícula concesiva admite también el
        gerundio simple con lectura de contemporaneidad, así que dejarlo abriría dos respuestas.
    - Conjunto cerrado de marcadores admitidos en este slot: `la sera prima`, `il giorno prima`,
      `poco prima`, y la partícula concesiva. Declararlo en el `notes` junto al gate de tipos.
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal. Debe explicar
      que el gerundio compuesto marca ANTERIORIDAD respecto al verbo principal mientras el simple marca
      contemporaneidad, que esa es la decisión que el slot examina, y enumerar los tres encajes. No
      referencia ningún ejercicio por id.

    Reglas duras comunes a los tres slots: hueco literal `___` exactamente una vez por prompt, todas las
    `options` del pool cerrado no personal, cero español en el prompt, cero paréntesis, cero corchetes
    angulares, cero backticks, cero comillas ni apóstrofes tipográficos, cero perífrasis y modismos del
    verbo (también en las 2 variantes exentas), y la grafía no elidida del infinito compuesto ausente de
    todo `prompt` y de toda `options[]`.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 6`; los 6 ids en orden son exactamente `fare-indefiniti-infinito-presente`, `fare-indefiniti-infinito-passato`, `fare-indefiniti-participio-passato`, `fare-indefiniti-participio-presente`, `fare-indefiniti-gerundio-presente`, `fare-indefiniti-gerundio-passato`; `variants.length` es `3`, `3`, `4`, `2`, `3`, `3` y el total es **18** (D-43-03).
    - `node -e`: las keys de `fare-indefiniti-participio-presente` son exactamente `["facente","facenti"]`; las de `fare-indefiniti-gerundio-presente` son `["facendo","facendo","facendo"]`; las de `fare-indefiniti-gerundio-passato` son `["avendo fatto","avendo fatto","avendo fatto"]` (INDEF-03, INDEF-04).
    - `node -e` sobre las 2 variantes del participio presente: la primera contiene el literal `funzione` y la segunda el literal `parte`, en ambos casos como la palabra inmediatamente posterior al hueco; y ninguna otra variante del fichero contiene esos dos literales (D-43-18, exención enumerada por id de slot).
    - Escaneo `node -e` sobre `prompt` y `options[]` de las **18** variantes: **0** coincidencias para los marcadores de perífrasis `["colazione","spesa","freddo","farcel","far fare","causativo"]` — el gate de perífrasis muerde también en las 2 variantes exentas, porque la exención es del objeto literal y no de este gate (D-43-18).
    - Escaneo `node -e` sobre las **16** variantes NO exentas: cada prompt contiene exactamente **un** objeto del conjunto cerrado ampliado de 9 en el segmento del hueco. Las 2 variantes de `fare-indefiniti-participio-presente` están excluidas del recuento por id de slot y no por relajación global (D-43-18).
    - `node -e` sobre `fare-indefiniti-gerundio-presente`: la variante de marco progresivo lleva una forma del verbo de estado (`sto`, `stai`, `sta`, `stiamo`, `state`, `stanno`) como palabra inmediatamente anterior al hueco; **exactamente 1** de las 3 variantes tiene `avendo fatto` entre sus `options`, y es esa (D-43-15, INDEF-04).
    - `node -e` sobre `fare-indefiniti-gerundio-passato`: **exactamente 1** de las 3 variantes tiene `facendo` entre sus `options`, y es la que lleva un adverbial de anterioridad del conjunto cerrado `["la sera prima","il giorno prima","poco prima"]`; los 3 prompts contienen exactamente un marcador del conjunto cerrado ampliado con la partícula concesiva; y los 3 tipos de contexto declarados son distintos.
    - `node -e` sobre los 6 slots: dentro de cada slot los tipos de contexto declarados en la tabla son todos DISTINTOS, con la única excepción de `fare-indefiniti-participio-passato`, cuyo patrón declarado es 2 + 2 (D-43-12, D-43-16).
    - Escaneo `node -e` sobre las 18 variantes: la unión de todas las `options` está contenida en `["fare","aver fatto","fatto","fatta","fatti","fatte","facendo","avendo fatto","facente","facenti"]`; **0** opciones pertenecen a la unión de las formas conjugadas de `fare` de las otras tres categorías; **0** coincidencias exactas con `avere fatto`, con las formas con clítico ni con las arcaicas; ninguna lista tiene duplicados internos; y `correctIndex` toma al menos 2 valores distintos dentro de cada slot (D-43-13, D-43-17).
    - Escaneo `node -e` sobre los 18 `prompt`: los 18 cumplen `prompt.split('___').length === 2`; **0** contienen `espa`; **0** contienen paréntesis de apertura.
    - Escaneo `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:`, comillas y apóstrofes tipográficos y backticks; ninguna clave propia peligrosa; `({}).polluted === undefined` tras el parse.
    - `node -e` sobre la `explanation` de `fare-indefiniti-participio-presente`: contiene al menos un carácter acentuado; contiene las subcadenas `burocrátic` y `fosiliz` (la nota de registro que INDEF-03 exige literalmente); contiene los dos sustantivos de los compuestos; su longitud es `> 500`; y no coincide con el patrón de cross-ref R2 (INDEF-03, SC-4).
    - `node -e` sobre la `explanation` de `fare-indefiniti-gerundio-presente`: contiene al menos un carácter acentuado; contiene la subcadena `progresiv`; contiene una formulación del uso excesivo (la subcadena `de más` o `en exceso`); su longitud es `> 600`; y no coincide con el patrón de cross-ref R2 (D-43-15, INDEF-04).
    - `node -e` sobre la `explanation` de `fare-indefiniti-gerundio-passato`: contiene al menos un carácter acentuado; contiene las subcadenas `anterioridad` y `contemporane`; su longitud es `> 500` (INDEF-04).
    - `node -e`: las 6 explanations contienen al menos un carácter del conjunto `áéíóúñÁÉÍÓÚÑ` y ninguna está vacía; los 6 slots tienen `validation.status === 'pending'` y `passes.length === 0`; ningún id coincide con `/-\d{3}$/`.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ content/categories.json tests/ .claude/` no produce ninguna línea de salida — esta tarea toca UN solo fichero.
  </acceptance_criteria>

  <done>
    El fichero tiene los 6 slots y las 18 variantes en `pending`. El participio presente tiene 2 variantes
    con los dos compuestos declarados y una explanation con la nota de registro que INDEF-03 exige, sin
    ningún contexto inventado. El gerundio presente examina `stare + gerundio` y su explanation desarrolla
    que el error es usarlo de más. El gerundio passato fuerza la anterioridad en las 3 y solo deja el
    gerundio simple entre las opciones donde el adverbial lo hace agramatical. Ninguna opción del fichero
    sale del pool cerrado no personal, y la suite completa sigue en `fail 0` con el motor sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 5: `tests/content-fare-indefiniti.test.js` con 13 `describe`, y la edición ÚNICA de toda la fase del prompt de validación por quórum con su ruta arreglada</name>

  <files>tests/content-fare-indefiniti.test.js, .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md, .claude/skills/gsd-validate-exercise/SKILL.md, .claude/skills/gsd-validate-batch/SKILL.md</files>

  <read_first>
    - `tests/content-fare-congiuntivo.test.js` **completo** (1443 líneas, 13 `describe`) — el molde
      directo: la cabecera con la ADVERTENCIA DE ESCANEO y las «TRES DESVIACIONES DELIBERADAS» (1-70), el
      `wordish` (82-83), los helpers de cláusula `CORTE_FUERTE`, `CORTE_DE_CLAUSULA`, `segmentoDelHueco` y
      `gobiernaElHueco` (104-121), las constantes de escaneo (184-200), el `camposDe` y el escaneo por
      campo (802-818), el escaneo en positivo sobre `notes` (842-853), el gate del objeto literal
      (757-775), el gate de canon editorial con el `walk` recursivo (1215-1269), el gate de audit trail y
      ronda EXTRA (1309-1385) con su comentario de por qué es verde con `passes: []`, y el gate de
      registro (1391-1433) con los dos invariantes derivados de WR-05.
    - `tests/content-fare-cond-imperativo.test.js` tal como lo dejó el plan 43-01 — el hermano más cercano,
      escrito contra las mismas decisiones, con su propia cabecera de cuatro desviaciones. Reusar sus
      nombres de constantes y su estilo de mensaje para que los dos ficheros se lean como una pareja.
    - `content/exercises/fare-indefiniti.json` tal como quedó tras la Task 4 — los 6 slots, las 18
      variantes, los marcadores literales del `notes`, y las cadenas exactas. Los gates se escriben contra
      el contenido real, no contra el plan.
    - `src/data/validation-state.js` — la firma y el comportamiento de `deriveStatus(passes)`, incluido el
      camino de `by: "autor"` con `override: true`. Se importa del código real, **nunca se reimplementa**.
    - `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md`
      **completo** (276 líneas) — la estructura de secciones (1 rol, 2 R1-R7 verbatim, 3 mapping a C1-C5,
      4 contrato de output, 5 few-shot, 6 guard anti prompt-injection, y la línea de cierre), y sobre todo
      **cómo están redactadas las dos excepciones que ya existen** (el gloss léxico de conjunción en R1 y
      su repetición dentro de C5): son el molde exacto de tono y de nivel de detalle para las dos nuevas.
    - `.claude/skills/gsd-validate-exercise/SKILL.md` líneas 45-55 y 225-255 — el paso de lectura del
      prompt, la ruta declarada, y la nota de que el subagent arranca con context window vacío y NO ve
      `CLAUDE.md` ni `.planning/` ni el `notes`. Es el argumento entero de por qué esta edición existe.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-PATTERNS.md` §Trampas del
      clonado, §Excepción declarada, y la tabla de los 13 `describe` con su columna de `fare-indefiniti`.
  </read_first>

  <precondition>`.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` existe y tiene 276 líneas, y `.planning/phases/09-infraestructura-de-validaci-n/` NO existe. Verificarlo con `ls` antes de editar: si la primera ruta ha cambiado, parar y reportar en vez de crear un fichero nuevo en la ruta obsoleta, porque un segundo prompt de validación en disco es peor que uno desactualizado.</precondition>

  <action>
    Dos entregables independientes que van juntos porque los dos son prerequisito del mismo pase top-level
    de quórum.

    **A. `tests/content-fare-indefiniti.test.js` — 13 bloques `describe`**

    Clonar la estructura del análogo: imports con `node:test` / `node:assert/strict` / `node:fs`, carga
    del JSON y de `content/categories.json` con `new URL(..., import.meta.url)`, helpers `byId` / `keyOf`
    / `eachVariant` / `allVariants`, comentarios del test **sin acentos**, y el patrón
    `assert.deepEqual(sucio, [], mensaje)` con cada mensaje citando la decisión y la coordenada
    `${slot.id}#${k}`.

    **Cabecera: la ADVERTENCIA DE ESCANEO heredada y las CINCO DESVIACIONES DELIBERADAS de esta
    categoría:**

    1. **Conteo DESIGUAL de variantes por slot.** Tabla `EXPECTED_VARIANTS` con 3, 3, 4, 2 y 3, 3 en vez
       del `equal(variants.length, 6)` del análogo; total 18.
    2. **Keys REPETIDAS por diseño.** El `new Set(keys).size === n` del molde de Phase 41 **no se clona**:
       aquí cinco de los seis slots repiten su key a propósito porque la forma es fija. El sustituto de la
       unicidad es el gate de tipo de contexto distinto por variante, no un pronombre.
    3. **No hay gate de pronombre sujeto explícito.** El eje no es la persona (nota de cabecera de
       REQUIREMENTS.md línea 8), así que el gate de D-41-07 que sí gobierna la categoría hermana no aplica
       aquí. Declararlo para que un re-pase futuro no lo eche en falta como omisión.
    4. **Excepción ENUMERADA por id de slot al gate del objeto literal.** El gate del análogo es global
       sobre todas las variantes; aquí las 2 de `fare-indefiniti-participio-presente` quedan exentas y a
       cambio deben llevar su compuesto declarado. La exención va por id, nunca como relajación global, y
       el gate de perífrasis sigue mordiendo en las 18.
    5. **Excepción ENUMERADA al gate de tipos de contexto distintos.** `fare-indefiniti-participio-passato`
       repite dos veces cada uno de sus dos tipos porque su eje es la terminación (D-43-16); es la única
       excepción y va por id.

    **Constantes de escaneo a declarar** (re-derivadas para esta categoría): `EXPECTED_VARIANTS`; `CANON`
    (mapa de los 6 ids al array ordenado de sus keys, con las 18 cadenas literales); `POOL` (las 10 formas
    no personales, el universo cerrado de todas las `options`); `CONJUGATE` (la unión de las formas
    conjugadas de `fare` presentes en las otras tres categorías, leída de sus JSON o declarada literal, y
    usada como gate de ausencia); `BLACKLIST` (la grafía no elidida del infinito compuesto, las cuatro
    formas con clítico y las arcaicas heredadas); `CONTEXT_TYPES` (la lista cerrada de tipos) y
    `VARIANT_TABLE` (el tipo, el objeto y el cue literal de cada una de las 18 variantes); `CONCORD_CUES`
    y `CONCORD_PROHIBIDOS` (los bigramas de plural admitidos y los de singular prohibidos);
    `PRONOMBRES_PROHIBIDOS` (los cinco de concordancia opcional o partitiva); `DEITTICI_PASSATO`;
    `MARCADORES_GERUNDIO_PASSATO`; `COMPUESTOS_FACENTE` (los dos sustantivos); `PERIPHRASIS` (clonado
    literal más el causativo) y `OBJECTS` (los 7 heredados más las 2 inflexiones en plural declaradas);
    y `EXTRA_ROUND_SLOTS` con exactamente los ids de `fare-indefiniti-participio-passato` y
    `fare-indefiniti-infinito-passato`.

    **Los 13 `describe`, con el nombre exacto de la tabla de §Artifacts y lo que congela cada uno:**

    1. **estructura y conteos** — 2 claves top-level; los 6 ids en orden de disco; MC en los 6;
       `variants.length` por `EXPECTED_VARIANTS` y total 18; key set exacto de slot y de variante;
       `options` de 4 sin duplicados; `correctIndex` en rango y no constante por slot; `categoryIds` de 1
       con el slug COMPLETO; ningún id con sufijo numérico de 3 cifras.
    2. **paradigma no personal, 18 keys con repeticion deliberada** — `deepEqual(variants.map(keyOf),
       CANON[id])` ordenado en los 6; sin gate de unicidad, con el comentario de la desviación 2.
    3. **tipos de contexto sintactico** — cada variante tiene un tipo de `CONTEXT_TYPES` en
       `VARIANT_TABLE`; su cue literal está en el prompt en la posición que la tabla declara; y dentro de
       cada slot los tipos son todos distintos, con la excepción enumerada de `-participio-passato`, cuyo
       patrón se comprueba como 2 + 2.
    4. **0-gloss del verbo** — ningún prompt contiene la subcadena española ni ningún paréntesis; gate
       estricto, sin la excepción de conjunción del análogo.
    5. **SCOPE-GATE lexico con la excepcion acotada** — 0 coincidencias de `PERIPHRASIS` por campo en las
       18; exactamente un objeto de `OBJECTS` en el segmento del hueco de las 16 no exentas; y en las 2
       exentas, el sustantivo de `COMPUESTOS_FACENTE` inmediatamente después del hueco. El mensaje del
       assert de exención cita D-43-18 y su frontera.
    6. **blacklist de formas atestiguadas y defendibles** — escaneo por campo con `camposDe`, `option` por
       igualdad exacta y `prompt` por `wordish`; 0 coincidencias de `BLACKLIST` y de `CONJUGATE`; y el
       escaneo en POSITIVO sobre `notes` exigiendo cada forma de `BLACKLIST` entre apóstrofes ASCII y los
       marcadores literales en MAYÚSCULAS `EJE = CONTEXTO`, `POOL CERRADO`, `CUARTO MAGNET`,
       `GATE HARD DE PRONOMBRE`, `EXCEPCIÓN ACOTADA AL SCOPE-GATE`, `RECONOCER, NO PRODUCIR`,
       `NOTA DE REGISTRO`.
    7. **pool CERRADO de options, cero formas conjugadas** — la unión de todas las `options` de las 18
       contenida en `POOL`, con el mensaje que lista las ofensoras; y 0 intersección con `CONJUGATE`. Es
       el gate más barato de la fase y el que impide que casillas de las otras tres categorías entren en
       el examen.
    8. **participio passato, las 4 terminaciones y el gate HARD de pronombre** — en las 4 variantes,
       `[...options].sort()` igual al conjunto de las cuatro terminaciones; las keys por `CANON`;
       exactamente 2 prompts con un cue de `CONCORD_CUES` y exactamente 2 sin ninguno, coherentes con sus
       keys; 0 apariciones por palabra suelta de `PRONOMBRES_PROHIBIDOS` en los 4 prompts; y 0
       coincidencias de `CONCORD_PROHIBIDOS`.
    9. **infinito passato, el cuarto magnet, e imperativo negativo** — la key elidida en las 3 variantes;
       0 apariciones de la grafía no elidida en cualquier campo de las 18; el infinito simple presente en
       las `options` de las 3 (la distractora del calco); el gate de anterioridad (preposición que
       gobierna el hueco o deíctico de `DEITTICI_PASSATO` en la cláusula del hueco); y, en el slot de
       infinito presente, la variante de imperativo negativo declarada por tabla con la negación
       inmediatamente antes del hueco.
   10. **contextos, objetos y marco progresivo por variante** — la tabla declarativa completa; el verbo de
       estado inmediatamente antes del hueco en la variante progresiva; exactamente 1 variante del
       gerundio presente con el gerundio compuesto entre sus opciones y exactamente 1 del gerundio passato
       con el gerundio simple, las dos identificadas por tabla y no por conteo ciego.
   11. **canon editorial e higiene del JSON** — `walk` recursivo clonado literal; 0 marcadores de marcado
       y de esquema, 0 smart quotes ni apóstrofes tipográficos, 0 claves peligrosas; las 6 explanations no
       vacías y con acento RAE. Más, en POSITIVO: la del infinito passato con la grafía no elidida; la del
       participio passato con el par de la interferencia; la del participio presente con la nota de
       registro; la del gerundio presente con la formulación del uso excesivo; con sus longitudes mínimas.
   12. **audit trail de validacion y ronda EXTRA** — `status === deriveStatus(passes)` en los 6 con
       `deriveStatus` importado del código real; quórum si `validated` (≥2 `correcta`, ≥2 `by` distintos,
       0 `incorrecta` salvo override del autor con motivo escrito y con al menos una `correcta` de un `by`
       que no es el autor); y el gate condicionado sobre `EXTRA_ROUND_SLOTS`. Clonar también el comentario
       que explica por qué el bloque es verde con `passes: []`.
   13. **registro de la categoria** — la entrada `fare-indefiniti` con las 4 claves, `order: 18`,
       `origen: "ia-quorum"`, índice igual a `order - 1`, orders contiguos 1..n; y los 6 slots
       referenciando el slug COMPLETO y nunca la parte compartida con `fare-indicativo` (D-40-03).

    **B. La edición ÚNICA de toda la fase del prompt de validación por quórum**

    Es la que cierra `[[exception_belongs_in_validation_prompt]]`: el subagent del quórum arranca con
    context window vacío y NO ve el `notes`, así que sin esto marcará el compuesto fosilizado como
    violación y los magnets como sin resolver. **El plan 43-01 no tocó este fichero a propósito; esta es
    la única edición de la fase.**

    **B1. Corrección de ruta (3 líneas, ninguna regla cambia).** En
    `.claude/skills/gsd-validate-exercise/SKILL.md` (2 ocurrencias, líneas 50 y 231) y en
    `.claude/skills/gsd-validate-batch/SKILL.md` (1 ocurrencia, línea 545), sustituir la ruta obsoleta
    `.planning/phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` por la real
    `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md`, que es la
    que `scripts/validate-ai-pass.mjs` ya usa. Sin esta corrección la excepción escrita llegaría al pase
    DeepSeek pero no al quórum base Opus+Sonnet, que es el que decide, y la edición sería inútil.

    **B2. Sección nueva de excepciones.** Añadir en
    `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` una
    sección numerada 7, titulada como excepciones declaradas por categoría de contenido, **insertada
    inmediatamente después del final del guard anti prompt-injection de la sección 6 y ANTES de la línea
    de cierre del prompt** (§Correcciones 3: lo que va detrás de esa línea el subagent lo lee como
    ejercicio bajo evaluación, no como regla). Redactada con el mismo tono y nivel de detalle que las dos
    excepciones que ya existen para el gloss léxico de conjunción, y con estos tres contenidos:

    · **Excepción de naturalidad para el participio presente fosilizado (v2.0 Phase 43, categoría
      `fare-indefiniti`).** Los dos compuestos declarados son italiano real y atestiguado: son un
      participio presente fosilizado que funciona como sustantivo o adjetivo en registro administrativo,
      no una perífrasis verbal. La explanation del slot declara el registro explícitamente, que es lo que
      el requisito exige. **Marcarlos como poco naturales, como burocráticos fuera de nivel o como
      perífrasis prohibida es un falso positivo** y hace fallar el criterio de naturalidad sin motivo. Lo
      que SÍ es violación, y aquí no hay excepción, es una perífrasis o modismo del VERBO en cualquier
      otra variante.
    · **Principio RECONOCER, NO PRODUCIR (v2.0 Phase 43, las dos categorías).** El material de esta fase
      nombra en sus explanations tres formas italianas atestiguadas que deliberadamente NO ofrece entre
      las opciones: las dos formas alternativas del imperativo de 2ª singular en `fare-cond-imperativo`, y
      la grafía no elidida del infinito compuesto en `fare-indefiniti`. Su ausencia es una decisión de
      autoría declarada, no un olvido: ofrecerlas como distractora «incorrecta» daría por errónea una
      forma correcta. **Marcar como distractora plausible ausente cualquiera de esas tres es un falso
      positivo** del criterio de distractoras. Y al revés: su ausencia es precisamente lo que garantiza
      que hay UNA sola opción defendible, así que el criterio de opción única se cumple gracias a ellas y
      no a pesar de ellas.
    · **Recordatorio de que el gloss léxico de conjunción sigue vigente** y de que ninguna de estas
      excepciones nuevas relaja el criterio de leak: el gloss sobre la forma verbal o sobre la palabra del
      hueco sigue prohibido sin excepción.

    **B3. Tres punteros de una línea.** Dentro de los criterios de naturalidad, de opción única y de
    distractoras de la sección 3, añadir una frase que nombre la excepción correspondiente y remita a la
    sección 7. El subagent evalúa criterio a criterio y una sección al final del fichero puede pasarle
    desapercibida; los punteros son lo que la hace efectiva.

    **No tocar nada más de ese fichero**: ni R1-R7, ni el contrato de output, ni los few-shot, ni el guard
    anti prompt-injection, ni la línea de cierre.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/content-fare-indefiniti.test.js && node --test tests/*.test.js && node -e "const fs=require('fs');const P='.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md';const t=fs.readFileSync(P,'utf8');const cierre=t.indexOf('Fin del prompt');const sec=t.indexOf('## 7.');if(sec===-1)throw new Error('falta la seccion 7 de excepciones');if(cierre===-1)throw new Error('falta la linea de cierre del prompt');if(sec>cierre)throw new Error('la seccion 7 esta DESPUES de la linea de cierre: el subagent la leeria como ejercicio');for(const f of ['.claude/skills/gsd-validate-exercise/SKILL.md','.claude/skills/gsd-validate-batch/SKILL.md']){const s=fs.readFileSync(f,'utf8');if(s.includes('.planning/phases/09-infraestructura'))throw new Error('ruta obsoleta todavia presente en '+f);}console.log('excepcion escrita antes del cierre y rutas corregidas')"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-indefiniti.test.js` sale con código 0 y reporta `fail 0`.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con `pass` estrictamente mayor que el de la Task 4.
    - `grep -c '^describe(' tests/content-fare-indefiniti.test.js` devuelve `13`.
    - `grep -c "from '../src/data/validation-state.js'" tests/content-fare-indefiniti.test.js` devuelve `1` — `deriveStatus` se importa del código real y no se reimplementa.
    - `grep -c 'EXTRA_ROUND_SLOTS' tests/content-fare-indefiniti.test.js` es `>= 2`, y el array contiene exactamente `fare-indefiniti-participio-passato` y `fare-indefiniti-infinito-passato`, ni uno más ni uno menos (D-43-20).
    - `grep -c 'DESVIACIONES DELIBERADAS' tests/content-fare-indefiniti.test.js` devuelve `1`, y la cabecera enumera las cinco (conteo desigual, keys repetidas por diseño, ausencia de gate de pronombre sujeto, exención enumerada del objeto literal, exención enumerada del gate de tipos).
    - `grep -c 'ADVERTENCIA DE ESCANEO' tests/content-fare-indefiniti.test.js` devuelve `1`.
    - **Prueba negativa del pool cerrado:** con una copia temporal del JSON en la que una variante lleve una forma conjugada de `fare` entre sus `options`, el test falla nombrando D-43-13 y listando la ofensora; restaurada la copia, vuelve a pasar. Ejecutar y reportar el resultado; no dejar el fichero modificado.
    - **Prueba negativa del gate HARD de pronombre:** con una copia temporal en la que una variante del participio passato use un pronombre de concordancia opcional o el partitivo, el test falla nombrando D-43-16; y con otra en la que use el pronombre singular antepuesto con el auxiliar elidido, también falla; restauradas, vuelven a pasar.
    - **Prueba negativa del cuarto magnet:** con una copia temporal en la que la grafía no elidida del infinito compuesto aparezca en las `options` de una variante, el test falla nombrando D-43-17; restaurada, vuelve a pasar.
    - **Prueba negativa de la exención acotada:** con una copia temporal en la que una variante que NO es del participio presente contenga uno de los dos sustantivos del compuesto, el test falla nombrando D-43-18 — la exención está enumerada por id de slot y no es una relajación global; restaurada, vuelve a pasar.
    - **Prueba negativa del gate de audit trail:** con una copia temporal en la que un slot lleve `status: "validated"` y `passes: []`, el test falla; con `passes` de dos entradas `correcta` del mismo `by`, también falla; y con uno de los dos slots de ronda EXTRA en `validated` sin ningún `by` que empiece por `deepseek`, también falla. Restauradas, vuelven a pasar.
    - `node -e` sobre el prompt de validación: el fichero contiene una sección numerada 7 de excepciones declaradas; su posición en el fichero es ANTERIOR a la de la línea de cierre `Fin del prompt`; y el guard anti prompt-injection de la sección 6 sigue presente y sin cambios (§Correcciones 3).
    - `node -e` sobre el prompt de validación: la sección 7 contiene los dos sustantivos de los compuestos fosilizados, la subcadena `falso positivo` al menos dos veces, la subcadena `RECONOCER` y la mención de las tres formas atestiguadas que el material no ofrece; y los criterios de naturalidad, de opción única y de distractoras de la sección 3 contienen cada uno una referencia a la sección 7 (los tres punteros de B3).
    - `git diff --stat '.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md'` muestra **solo adiciones y cero borrados**: ninguna regla existente se altera, ni R1-R7, ni el contrato de output, ni los few-shot, ni el guard, ni la línea de cierre.
    - `grep -rc 'planning/phases/09-infraestructura' .claude/skills/` devuelve `0` en los dos ficheros de skill, y `grep -rc 'planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md' .claude/skills/gsd-validate-exercise/SKILL.md` devuelve `2` y en `gsd-validate-batch/SKILL.md` devuelve `1` (§Correcciones 2).
    - `test -f '.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md' && test ! -e '.planning/phases/09-infraestructura-de-validaci-n'` sale con código 0 — la ruta corregida existe y no se ha creado un segundo prompt en la obsoleta.
    - `git diff --stat .claude/skills/` muestra exactamente 3 líneas cambiadas en total, todas de ruta: ninguna regla, ningún paso, ningún modelo del skill cambia.
    - `node scripts/validate-ai-pass.mjs --dry-run` (con cualquier id de ejercicio existente) imprime el prompt e incluye la sección 7 — la ronda EXTRA DeepSeek verá la excepción. Si el script exige claves para el dry-run, comprobarlo en su lugar leyendo `PROMPT_PATH` y confirmando que apunta al fichero editado.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` sigue fallando solo por los 9 slots `pending` de las dos categorías nuevas, y **no** por ninguno de los gates nuevos.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ content/ tests/exercise-types.test.js tests/content-fare-cond-imperativo.test.js` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    `tests/content-fare-indefiniti.test.js` existe con 13 `describe` y congela como invariantes permanentes
    el reparto desigual (3/3/4/2/3/3 y 18 en total), el paradigma no personal con sus keys repetidas por
    diseño, la lista cerrada de tipos de contexto con su gate de distinción por slot y su única excepción
    enumerada, el 0-gloss, el SCOPE-GATE con la excepción acotada del participio presente enumerada por id,
    la blacklist por campo, el pool cerrado de opciones sin ninguna conjugada, el participio passato con
    sus cuatro terminaciones y su gate HARD de pronombre, el cuarto magnet, la tabla declarativa por
    variante, el canon editorial con los escaneos positivos de las explanations, el audit trail con
    `deriveStatus` real y la ronda EXTRA condicionada sobre sus dos slots, y el registro de la categoría.
    Las cinco desviaciones respecto del análogo están declaradas en la cabecera y las pruebas negativas
    demuestran que los gates muerden.

    Y el prompt de validación por quórum contiene, escritas exactamente una vez en toda la fase y antes de
    la línea de cierre, la excepción de naturalidad de los dos compuestos fosilizados y el principio
    RECONOCER NO PRODUCIR con sus tres formas, con punteros desde los tres criterios que las necesitan; y
    los dos skills apuntan a la ruta real del fichero, de modo que el quórum base Opus+Sonnet las verá.
  </done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

Honestamente acotadas: esto es una app web estática de un solo usuario, sin servidor, sin autenticación,
sin red en runtime y sin sincronización (CLAUDE.md §Constraints). El único dato nuevo que entra es un
fichero JSON escrito a mano por el propio autor. La superficie real de una fase de **autoría de
contenido** aquí es la **integridad de datos**, no el ataque. Este plan añade una frontera que 43-01 no
tenía: el fichero de reglas que se envía al subagent del quórum.

| Boundary | Description |
|----------|-------------|
| `content/exercises/*.json` en disco → `fetch()` en boot → `JSON.parse` en `src/data/content-loader.js` | Contenido semi-confiable: lo escribe el autor (o una autoría asistida), no un tercero, pero pasa por `JSON.parse` y de ahí al estado de la app sin ninguna otra puerta. Un JSON malformado rompe `loadContent()` en boot y la app no arranca. |
| strings de contenido (`prompt`, `options[]`, `explanation`, `notes`) → render en el DOM por Alpine | La invariante anti-XSS del proyecto (T-02-01): el contenido se renderiza SIEMPRE como texto, nunca como HTML. |
| `content/categories.json` → `src/data/schema-validator.js` (`knownCategoryIds`) → `categoriesForDisplay` | Registro: una entrada mal formada rompe la validación del bundle completo; una entrada ausente deja el fichero de contenido en estado inválido en disco; y un `order` no contiguo pone rojos los gates de registro de las tres categorías de `fare` ya existentes. |
| slug del fichero y prefijo de los ids de slot → `RESET_PREFIXES_V13` (`src/data/storage.js:1345`) → `exerciseStats` del autor en `localStorage` | Integridad del progreso, con el agravante específico de esta categoría: `fare-indicativo` y `fare-indefiniti` comparten el prefijo `fare-ind` (D-40-03), así que un prefijo truncado en cualquier comprobación cruza dos categorías que son unidades de reset distintas. |
| `09-VALIDATION-PROMPT.md` en disco → el prompt que el skill y el script envían al subagent del quórum | **Frontera nueva de este plan.** El subagent arranca con context window vacío: ese fichero es la ÚNICA fuente de reglas que ve. Su contenido gobierna el veredicto que acaba escrito en `validation.passes[]`, que es el audit trail del autor. Y el guard anti prompt-injection de su sección 6 marca la línea a partir de la cual todo es DATA a evaluar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Tampering | strings de `content/exercises/fare-indefiniti.json` renderizados en el DOM | medium | mitigate | Se preserva la invariante de render como texto (T-02-01): esta fase NO cambia una línea de render y el contenido queda libre de marcado. Doble verificación: el sub-test `plain text (no markdown)` de `tests/exercise-types.test.js`, activado por la línea de `CATEGORIES_WITH_EXPLANATIONS` de la Task 1; más un escaneo `node -e` en las tres tareas de contenido y el `describe` 11 de la Task 5, que exigen 0 apariciones de corchetes angulares, `&#`, `javascript:` y backticks en TODOS los strings, incluido `notes`. |
| T-43-02 | Tampering | `JSON.parse` del fichero de contenido en `content-loader.js` | medium | mitigate | Prototype-pollution por contenido: `JSON.parse` crea `__proto__` como own-property si el literal la declara. El fichero declara ÚNICAMENTE el key set del schema (2 claves top-level, 6 por slot, 3 por variante), congelado por el `describe` 1 de la Task 5; y cada tarea corre un escaneo recursivo que exige que ninguna clave propia se llame `__proto__`, `constructor` ni `prototype`, más `({}).polluted === undefined` tras el parse. |
| T-43-03 | Repudiation | `validation.passes[]` como audit trail del quórum | medium | mitigate | Un `validated` escrito a mano, o un pase con `by`/`date` inventados, forja la única evidencia de revisión que tiene el autor. El executor deja los 6 slots en `pending` con `passes: []` y NO fabrica pases; el quórum real corre en la pasada top-level (VAL-03, un ejercicio por contexto); y el `describe` 12 de la Task 5 exige `status === deriveStatus(passes)` con la función real, más la ronda EXTRA `deepseek` condicionada en los dos slots de D-43-20, con pruebas negativas que demuestran que el gate muerde. |
| T-43-04 | Tampering | entrada de `content/categories.json` y contigüidad de `order` | medium | mitigate | Prerequisito de schema, no cosmética. Riesgo específico del segundo append de la fase: romper la contigüidad de `order`, que los gates de registro de `fare-indicativo`, `fare-congiuntivo` y `fare-cond-imperativo` ya vigilan con `indexOf(cat) === cat.order - 1` y con la igualdad del mapa de orders a `1..n`. Verificado por criterio de aceptación: 4 claves exactas, 18ª entrada, `order: 18`, `origen: "ia-quorum"`, índice igual a `order - 1`, orders contiguos 1..18, y la suite en `fail 0`. |
| T-43-05 | Tampering | coincidencia del slug con `RESET_PREFIXES_V13` y colisión de prefijo `fare-ind` | high | mitigate | Si el slug no es byte a byte `fare-indefiniti`, el reset selectivo de v13 no alcanza la categoría y el progreso del autor queda huérfano sin ningún error visible: rompe el Core Value del proyecto sin emitir señal, y por eso `high`. Agravante propio de esta categoría: comparte `fare-ind` con `fare-indicativo`, así que una comprobación de prefijo truncada mezclaría dos unidades de reset distintas. Mitigación: `src/data/storage.js:1345` ya contiene el string exacto (Phase 40 shippeada) y esta fase NO lo edita; el criterio de aceptación fija los 6 ids literalmente y exige el prefijo COMPLETO; el `describe` 13 de la Task 5 lo congela; y la prohibición de renombrar o truncar queda escrita en `must_haves.prohibitions`. |
| T-43-06 | Tampering | `validation.status` de la categoría frente a la cascada D-54 | high | mitigate | La amenaza más cara de la fase: una variante con dos respuestas defendibles hace que el quórum emita `incorrecta`, `deriveStatus` la deja en `disputed`, `disputed` es STICKY y la cascada D-54 resetea las 18 variantes — el autor pierde progreso real por un defecto del material. Los cuatro vectores concretos de esta categoría son el pronombre de concordancia opcional o partitiva, el pronombre singular antepuesto con el auxiliar elidido, el marcador de anterioridad ausente en las parejas simple-frente-a-compuesto, y la grafía no elidida del infinito compuesto ofrecida como distractora. Mitigación: los cuatro van como gates HARD declarados en `notes` ANTES de escribir y congelados en los `describe` 5, 7, 8, 9 y 10 de la Task 5 con pruebas negativas, más el checkpoint humano de la Task 2 sobre la unicidad de lectura, más la ronda EXTRA DeepSeek sobre los 7 variantes de los dos slots más delicados. El quórum es la red, no el mecanismo. |
| T-43-07 | Tampering | contenido de `09-VALIDATION-PROMPT.md` y su posición respecto al guard | high | mitigate | Amenaza nueva de este plan y la razón de que la Task 5 tenga una precondición propia. Tres vectores: (a) insertar las excepciones DESPUÉS de la línea de cierre del prompt las convierte en DATA a evaluar a ojos del subagent, así que dejarían de ser reglas sin que nada falle visiblemente; (b) tocar el guard anti prompt-injection de la sección 6 abriría la puerta a que el propio contenido del ejercicio dirija al evaluador; (c) crear un segundo fichero de prompt en la ruta obsoleta dejaría dos fuentes de verdad divergentes, que es peor que una desactualizada. Mitigación: la precondición verifica por `ls` que la ruta real existe y la obsoleta no; el criterio de aceptación comprueba por índice de cadena que la sección nueva va ANTES de la línea de cierre; `git diff --stat` sobre el fichero debe mostrar solo adiciones y cero borrados; y el guard se comprueba presente y sin cambios. |
| T-43-08 | Repudiation | ruta obsoleta del prompt en los skills del quórum | medium | mitigate | Defecto preexistente descubierto en plan-time (§Correcciones 2): los dos skills apuntan a un directorio que no existe desde el archivado del milestone v1.1, mientras `scripts/validate-ai-pass.mjs` sí tiene la ruta real. Consecuencia para esta fase: la excepción escrita llegaría a DeepSeek pero no al quórum base Opus+Sonnet, que es el que decide, y el audit trail registraría pases emitidos con un cuerpo de reglas incompleto. Mitigación: corrección de ruta de 3 líneas en la misma tarea, verificada con un grep que exige 0 apariciones de la ruta obsoleta y el conteo exacto de la nueva, más `git diff --stat` que acota el cambio a 3 líneas de ruta y ninguna regla. |
| T-43-09 | Denial of Service | tamaño del bundle de contenido cargado en boot | low | accept | 18 variantes son del orden de 300 líneas de JSON; el loader ya trae 17 ficheros. El coste de `JSON.parse` a esta escala es despreciable y la app no tiene atacante remoto: sin servidor, sin red, sin multiusuario. |
| T-43-10 | Information Disclosure | el contenido del fichero | low | accept | Son formas no personales de un verbo italiano y explicaciones gramaticales. Cero PII, cero credenciales, cero secretos. El fichero no sale de la máquina del autor. |
| T-43-11 | Spoofing | — | n/a | n/a — no aplica | No existen identidades, sesiones, tokens ni cuentas: la app es local, monousuario y sin autenticación. |
| T-43-12 | Elevation of Privilege | — | n/a | n/a — no aplica | No hay roles, permisos ni frontera de privilegio: un único usuario con acceso total a su propio navegador y su propio disco. |
| T-43-SC | Tampering | instalaciones de npm / pip / cargo | n/a | n/a — gate no disparado | Esta fase no ejecuta ninguna instalación de gestor de paquetes ni añade dependencias: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node. El Package Legitimacy Gate no aplica y no se requiere tabla de auditoría. |

**ASVS L1, block on high.** Hay tres amenazas `high` en este plan y las tres están en `mitigate` con
verificación automatizada: T-43-05 (el slug byte a byte, con el agravante del prefijo compartido), T-43-06
(la doble validez, cuyo fallo dispara la cascada D-54) y T-43-07 (la posición y la integridad de la
edición del prompt de validación, cuyo fallo es completamente silencioso). No se inventa ninguna amenaza
para rellenar la tabla: en una app local sin servidor, sin red en runtime y sin entrada de terceros, la
superficie real es la integridad de los datos y de las reglas con las que esos datos se validan. Las dos
`accept` son `low` y su razón es estructural.

### Candidatos de prohibición canon descartados (breadcrumbs)

- **prototype-pollution** — canon de seguridad del proyecto; vive en T-43-02 y en `/gsd-secure-phase`, no
  se acuña como prohibición.
- **XSS / inyección de HTML** — canon; vive en T-43-01 (invariante de render como texto, T-02-01).
- **prompt-injection** — canon, y además ya mitigado en el propio fichero por el guard de su sección 6,
  que este plan tiene prohibido tocar. Vive en T-43-07, no se acuña por separado.
- **path-traversal** — sin acceso a sistema de ficheros en runtime: el contenido se sirve por HTTP local y
  el runtime es el navegador. Descartado.
- **GDPR / privacidad de datos personales** — el contenido son conjugaciones y el state es el progreso de
  estudio del propio autor en su máquina. Descartado.
- **fairness / sesgo genérico** — sin decisión automatizada sobre personas. Lo que SÍ se acuña son las
  variantes bespoke de esta fase, que ninguna herramienta canon detectaría: el contexto inventado para
  llenar un slot, la excepción declarada «arreglada» por un re-pase futuro, el ejercicio con dos
  respuestas defendibles, la forma atestiguada ofrecida como distractora «incorrecta», y el override de un
  flag de acentos.
</threat_model>
<verification>
Comprobaciones al terminar las 5 tareas de este plan — y, como es la wave 2, al terminar la FASE entera:

1. `node --test tests/*.test.js` → código de salida 0, `fail 0`, `pass` mayor que el medido en la
   precondición de la Task 1.
2. `node --test tests/content-fare-indefiniti.test.js` y `node --test tests/content-fare-cond-imperativo.test.js`
   → los dos con código de salida 0 y 13 `describe` cada uno.
3. `node -e` sobre `content/exercises/fare-indefiniti.json`: 6 slots con los ids exactos y 3, 3, 4, 2, 3, 3
   variantes, 18 en total, los 6 en `type: "multiple-choice"` con `options` de 4 y `categoryIds` de 1.
4. `node -e` sobre `content/categories.json`: 18 entradas, la 17ª `fare-cond-imperativo` y la 18ª
   `fare-indefiniti`, las dos con `origen: "ia-quorum"` y las 4 claves exactas, índices iguales a
   `order - 1` y orders contiguos 1..18. **Son DOS entradas distintas, no una fusión** (SC-5, D-43-01).
5. `node -e` de cierre de fase: los dos ficheros de contenido suman **9 slots y 35 variantes**, todos en
   `validation.status: "pending"` con `passes: []`.
6. Los escaneos de ausencia, **todos por campo y por coincidencia exacta en `options`**, nunca sobre el
   fichero completo ni sobre `notes` ni sobre `explanation`: pool cerrado y cero conjugadas en las 18,
   grafía no elidida del infinito compuesto en todo el fichero, pronombres prohibidos y bigramas de
   singular antepuesto en el slot de participio, blacklist y perífrasis en las 18, español y paréntesis en
   los 18 prompts.
7. Los escaneos en positivo: cada prompt no exento con un objeto del conjunto cerrado ampliado, cada
   variante con su cue de tipo de contexto, las 2 variantes exentas con su compuesto declarado, `notes`
   con cada forma de la blacklist entre apóstrofes ASCII y con los marcadores en MAYÚSCULAS, y las 6
   explanations con sus contenidos obligatorios (nota de registro, par de la interferencia de
   concordancia, grafía no elidida nombrada, uso excesivo del progresivo).
8. `node -e` sobre el prompt de validación: la sección 7 existe, está ANTES de la línea de cierre, contiene
   las dos excepciones nuevas, y el guard de la sección 6 sigue intacto. `git diff --stat` sobre ese
   fichero: solo adiciones, cero borrados.
9. `grep -rc 'planning/phases/09-infraestructura' .claude/skills/` → `0` en los dos ficheros; la ruta real
   presente 2 veces en el skill de ejercicio y 1 en el de batch.
10. `VAL_07_STRICT=1 node --test tests/*.test.js` → falla nombrando los **9** slots `pending` de las dos
    categorías. Ese rojo es el estado CORRECTO al cerrar la fase; lo cierra el pase top-level de quórum de
    §Hand-off, que además necesita que la Task 5 esté hecha.
11. `node scripts/run-validation-271.mjs` → sigue reportando su total anterior en PASS. Es la ceguera
    documentada, no un fallo: el reporter no verá las dos categorías hasta Phase 44 / INT-02.
12. `git diff --quiet src/screens/app.js src/domain/ src/data/` → código de salida 0. **Es el gate del
    motor byte-intacto de toda la fase 43**, no solo de este plan.
13. `git status --porcelain` → exactamente los 7 ficheros de `files_modified`. En particular
    `content/exercises/fare-cond-imperativo.json` y `tests/content-fare-cond-imperativo.test.js` NO están
    tocados: son del plan 43-01, ya cerrado.
</verification>

<success_criteria>
- INDEF-01: el autor es examinado sobre el infinito presente y el infinito compuesto elidido, eligiendo
  entre los dos por la ANTERIORIDAD respecto a la principal, con el infinito simple como distractora del
  calco español en las 3 variantes del compuesto, y con el cuarto magnet resuelto — la grafía no elidida
  es correcta, se nombra en la explanation y no entra en ninguna opción.
- INDEF-02: el autor es examinado sobre el participio pasado en su doble comportamiento — invariable con
  el auxiliar de posesión y el objeto pospuesto en 2 variantes, concordado con pronombre objeto directo
  antepuesto en las otras 2 —, con el mismo pool de las cuatro terminaciones en las 4 y con el gate HARD
  que deja fuera todo pronombre que haría la concordancia opcional o ambigua.
- INDEF-03: el participio presente entra en el paradigma con 2 variantes construidas sobre sus dos
  compuestos reales, sin ningún contexto inventado, y con la NOTA DE REGISTRO explícita en la explanation
  (burocrático, fosilizado, vive en compuestos) que el requisito exige literalmente.
- INDEF-04: el autor es examinado sobre el gerundio simple y el compuesto, incluido `stare + gerundio`
  como progresivo en una variante propia, con una explanation que desarrolla que la interferencia real es
  usarlo de MÁS y no formarlo mal.
- SC-3: los 6 slots con el eje de variante CONTEXTO y NUNCA la persona, unas 3 frases por slot, la lista
  cerrada de tipos declarada y distinta dentro de cada slot, y la elección por anterioridad tanto en el
  par de infinitos como en el par de gerundios.
- SC-4: el doble comportamiento del participio pasado examinado en un solo slot con un solo pool, con la
  ronda EXTRA ya escrita como gate condicionado y encargada en §Hand-off; y el participio presente con su
  nota de registro sin mentir sobre su uso real.
- SC-5: las DOS categorías de la fase cargan en boot como unidades de reset SEPARADAS —`order: 17` y
  `order: 18`, dos filas, dos prefijos de reset— y las 35 variantes quedan listas para el quórum, con
  canon editorial y sin leak R1; el motor sigue byte-intacto.
- Estado de hand-off correcto: los 9 slots en `pending` con `passes: []`, el rojo de `VAL_07_STRICT=1`
  como marcador visible, los gates condicionados de ronda EXTRA escritos en los dos ficheros de test, y
  **las excepciones de la fase ya escritas en el prompt de validación con la ruta arreglada**, que es lo
  que hace que el pase top-level pueda empezar.
</success_criteria>

<output>
Crear `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-02-SUMMARY.md` al terminar.
</output>