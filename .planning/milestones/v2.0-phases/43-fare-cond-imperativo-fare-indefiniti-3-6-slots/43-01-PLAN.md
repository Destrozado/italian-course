---
phase: 43-fare-cond-imperativo-fare-indefiniti-3-6-slots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/fare-cond-imperativo.json
  - content/categories.json
  - tests/exercise-types.test.js
  - tests/content-fare-cond-imperativo.test.js
autonomous: false
requirements: [CI-01, CI-02, CI-03]

estimate:
  tokens: 118000
  raw_tokens: 118000
  tasks: 4
  confidence: low

must_haves:
  truths:
    # --- SC-5 / D-43-22 : el registro que hace existir la categoría ---
    - "`content/categories.json` contiene exactamente UNA entrada nueva con las 4 claves `id`, `name`, `order`, `origen`, valores `id: \"fare-cond-imperativo\"`, `order: 17`, `origen: \"ia-quorum\"`, y las 16 entradas previas quedan byte-intactas (D-43-22, SC-5)."
    - "El slug es byte a byte `fare-cond-imperativo` en los cuatro sitios que el contrato de Phase 40 vincula: nombre del fichero de contenido, `id` de `content/categories.json`, prefijo de los 3 ids de slot, y el elemento ya existente de `RESET_PREFIXES_V13` (`src/data/storage.js:1345`, que esta fase NO edita) — de modo que la categoría es una unidad de reset independiente de las otras 3 de `fare` (D-40-01, D-43-22, SC-5)."
    - "La categoría carga en boot y aparece en home, picker, Repaso y Examen por el camino genérico (`categoriesForDisplay` itera `content.categories`; `pickVariantIndex` de `src/domain/session.js:232` solo lee `slot.variants.length` y es axis-agnostic), sin una sola línea de motor nueva: `git diff --stat src/screens/app.js src/domain/ src/data/` no produce salida (D-43-22, SC-5)."
    # --- edge: schema boundary (el orden de las dos ediciones) ---
    - "El fichero `content/exercises/fare-cond-imperativo.json` nunca queda en disco sin su entrada en `categories.json`: `src/data/schema-validator.js` rechaza un `categoryIds` que referencia una categoría desconocida y `tests/domain.test.js` valida el bundle auto-descubierto exigiendo `result.errors` vacío, así que `node --test tests/*.test.js` termina con `fail 0` en cada commit de este plan (edge: schema boundary)."
    # --- CI-01 : el condizionale presente en las 6 personas ---
    - "El slot `fare-cond-imperativo-cond-presente` tiene EXACTAMENTE 6 variantes y sus 6 keys (`options[correctIndex]`) son, en orden de persona io/tu/lui-lei/noi/voi/loro, `farei`, `faresti`, `farebbe`, `faremmo`, `fareste`, `farebbero` — las 6 sobre la raíz contracta `far-`, sin ninguna repetición (CI-01, SC-1)."
    - "En cada una de las 6 variantes del condizionale presente exactamente UNA distractora es el futuro semplice de ESA MISMA persona (`farò`/`farai`/`farà`/`faremo`/`farete`/`faranno`), exactamente UNA es una forma real de condizionale presente de OTRA persona, y exactamente UNA es una raíz regularizada inexistente sobre `facer-`: SC-1 pide «al menos una variante» que obligue a distinguir futuro de condizionale y aquí lo hacen las 6 (D-43-09, CI-01, SC-1)."
    - "Ninguno de los 6 `prompt` del condizionale presente contiene un deíctico temporal de futuro del conjunto cerrado `domani`, `dopodomani`, `stasera`, `presto`, `fra poco`, `la prossima settimana`, `l'anno prossimo`, `il mese prossimo` — con un marcador de futuro en la frase la distractora de futuro pasaría a ser defendible y la variante tendría dos respuestas correctas (D-43-09; edge: ambiguity boundary)."
    - "Cada uno de los 6 `prompt` del condizionale presente contiene exactamente un disparador de condizionale del conjunto cerrado declarado en `notes` (prótasis hipotética con congiuntivo imperfetto o locución de cortesía o de sustitución del tipo `Al posto tuo`), los 6 distintos entre sí, y cuando la prótasis lleva persona esa persona COINCIDE con el pronombre del hueco, nunca lo contradice (D-43-09, D-41-07)."
    # --- CI-02 : el condizionale passato y el futuro en el pasado ---
    - "El slot `fare-cond-imperativo-cond-passato` tiene EXACTAMENTE 6 variantes y sus 6 keys son, en orden de persona del auxiliar, `avrei fatto`, `avresti fatto`, `avrebbe fatto`, `avremmo fatto`, `avreste fatto`, `avrebbero fatto` (CI-02, SC-1)."
    - "En cada una de las 6 variantes del condizionale passato exactamente UNA distractora es el condizionale PRESENTE de esa misma persona — el calco español exacto, porque el español dice «dijo que haría» con condicional simple donde el italiano exige el compuesto —, exactamente UNA es el mismo participio con el auxiliar en otro tiempo (`avrà fatto`, `aveva fatto`, `avrò fatto`, `avevo fatto`, `avrebbe avuto fatto` no), y exactamente UNA es una forma mal construida por sobre-generalización del auxiliar o por participio ausente (D-43-10, CI-02, SC-1)."
    - "Al menos 2 de las 6 variantes del condizionale passato usan un marco de futuro-nel-passato del conjunto cerrado `ha detto che`, `mi ha promesso che`, `sapevo che`, `era sicuro che`, `aveva giurato che`, y ese marco GOBIERNA la cláusula del hueco (está en la cláusula del hueco o en la inmediatamente anterior de la misma frase), no solo aparece en el prompt (D-43-11, CI-02, SC-1)."
    - "Ninguno de los 6 `prompt` del condizionale passato contiene la palabra suelta `se`: el slot no usa prótasis condicional, porque bajo una prótasis con congiuntivo imperfetto la distractora de condizionale presente pasaría a ser defendible y la variante tendría dos lecturas — es el trabajo fino que D-43-10 deja abierto, resuelto como gate y no como red de quórum (D-43-10; edge: ambiguity boundary)."
    - "La explanation de `fare-cond-imperativo-cond-passato` desarrolla el par explícito `ha detto che avrebbe fatto` frente a «dijo que haría», señalando que el español lo resuelve con condicional SIMPLE y el italiano exige el COMPUESTO — la advertencia va donde el error muerde, no solo donde se explica (D-43-11, SC-1)."
    - "La explanation de `fare-cond-imperativo-cond-presente` lleva una línea propia sobre el par mínimo `faremo` frente a `faremmo`, que se diferencian en una sola consonante doble y son futuro y condizionale de la misma persona (D-43-09)."
    # --- CI-03 / SC-2 : el MAGNET del imperativo ---
    - "El slot `fare-cond-imperativo-imperativo` tiene EXACTAMENTE 5 variantes — no 6 — y sus 5 keys son, en orden tu/Lei/noi/voi/Loro, `fa'`, `faccia`, `facciamo`, `fate`, `facciano` (CI-03, SC-2)."
    - "El apóstrofe de la key `fa'` es U+0027 (ASCII) y no U+2019: aquí deja de ser canon cosmético porque es parte de la cadena que el autor tiene que elegir (D-43-21, D-43-04)."
    - "Ninguna de las cadenas `fai` ni `fa` aparece como opción en NINGUNA de las 17 variantes del fichero, y `fa'` aparece como opción en EXACTAMENTE una variante, donde es la key: se cumple al pie de la letra que ninguna de `fa'`/`fai`/`fa` es distractora «incorrecta» (D-43-04, SC-2)."
    - "Las 3 distractoras de cada una de las 5 variantes del imperativo son formas REALES del imperativo de `fare` de OTRO destinatario, tomadas del conjunto `faccia`, `facciamo`, `fate`, `facciano` — ninguna inventada, ninguna de otro modo, y nunca `fa'` —, de modo que el eje examinado es el REGISTRO y no la morfología (D-43-05, CI-03)."
    - "Cada uno de los 5 `prompt` del imperativo lleva exactamente un marcador de destinatario inequívoco del conjunto cerrado declarado (`Marco,` para el tú informal, `Signor Rossi,` para el usted formal, `Dai, noi due` para el exhortativo de nosotros, `Bambini,` para el vosotros informal, y `Signori,` junto al pronombre de cortesía `Loro` con L mayúscula para el ustedes formal) — el análogo funcional del pronombre sujeto explícito de D-41-07 (D-43-05, SC-2)."
    - statement: "En la variante del imperativo de Loro el pronombre de cortesía explícito excluye la lectura de vosotros, de modo que la distractora `fate` no es defendible; y en la variante de noi el sujeto inclusivo explícito excluye la misma lectura. Sin esos dos marcadores el vocativo de plural admitiría las dos formas y la variante tendría dos respuestas correctas."
      verification: backstop
    - "El `notes` declara la ausencia de `io` en el imperativo como estructural y no como olvido (no se dan órdenes a uno mismo), la explanation del slot cita el paradigma de 5 y dice por qué falta esa casilla, y un gate de test congela `variants.length === 5` en ese slot como invariante permanente — los tres sitios que SC-2 exige (D-43-08, CI-03, SC-2)."
    - "El `notes` declara con audit trail por qué la key de la 2ª singular es `fa'`: es la única de las cinco formas del imperativo que NO vive ya en el corpus (verificado 2026-08-07: `fai`, `fa` y `fate` son keys de `fare-indicativo`; `faccia`, `facciamo` y `facciano` lo son de `fare-congiuntivo`; `fa'` no aparece en ningún fichero), es la canónica de los manuales, y su apóstrofe de elisión es justo lo que hay que aprender (D-43-04, SC-2)."
    - "El `notes` declara la homografía con el congiuntivo y el indicativo con audit trail — `faccia`, `facciamo` y `facciano` son keys de `fare-congiuntivo` y `fate` lo es de `fare-indicativo-presente`, porque el imperativo formal italiano toma prestadas las formas del congiuntivo presente y el de `voi` coincide con el indicativo —, la explanation del imperativo lo ENSEÑA como el hecho gramatical que es, y NINGUNA variante pide decidir entre los dos modos: la homografía se declara y se enseña, nunca se contrasta (D-43-06)."
    - "El `notes` declara la blacklist de formas con clítico (`fallo`, `fammi`, `fatelo`, `facci`) con su audit trail, la explanation del imperativo las nombra como lo que son —imperativo apostrofado más clítico con duplicación consonántica— y ninguna entra en `options` de ninguna variante (D-43-07)."
    - "El `notes` declara el principio RECONOCER, NO PRODUCIR como regla de la fase: hay formas italianas atestiguadas que el material nombra pero no pide producir (aquí `fai` y `fa`, imperativos vivos vetados como key), para que el autor las reconozca al leer y no las tome por errores; y la explanation del imperativo lleva su línea concreta (D-43-19, SC-2)."
    # --- D-43-22 : heredados que este fichero tiene que cumplir ---
    - "Los 12 `prompt` de los dos slots de condizionale llevan el pronombre sujeto explícito de su persona (`io`, `tu`, `lui` o `lei`, `noi`, `voi`, `loro`), que es lo que hace resoluble la casilla cuando la distractora es una forma real de otra persona (D-41-07, D-43-22)."
    - "El objeto de `fare` en las 17 variantes sale SOLO del conjunto literal cerrado `i compiti`, `un errore`, `il lavoro`, `una torta`, `il letto`, `tutto`, `una foto`, y hay exactamente UNO en la cláusula del hueco de cada prompt; un escaneo por campo sobre `variants[].prompt` y `variants[].options[]` devuelve 0 coincidencias para los marcadores de perífrasis y modismos de `fare` (D-41-06, D-43-22, SCOPE-GATE HARD)."
    - "Ningún `prompt` de las 17 variantes glosa la forma verbal ni contiene la subcadena `espa` en ninguna capitalización: en esta categoría el candidato legítimo de gloss léxico es el vocativo o el marco, nunca la forma de `fare` (D-41-05, D-43-22)."
    - "Los 3 slots tienen `type: \"multiple-choice\"` con `options` de longitud 4 y sin duplicados dentro de una misma variante; no existe ningún slot `match` ni `word-buttons`, y el `notes` declara el 0-match (el pareo persona a forma es derivable por raíz, DESIGN RULE D-04) y el 0-word-buttons como decisiones razonadas y no como omisiones (D-43-22)."
    - "Los ids de los slots son literalmente `fare-cond-imperativo-cond-presente`, `fare-cond-imperativo-cond-passato` y `fare-cond-imperativo-imperativo`, los tres con `categoryIds: [\"fare-cond-imperativo\"]` — exactamente un id —, y ningún id del fichero termina en sufijo numérico de 3 cifras: el espacio `fare-cond-imperativo-300`+ queda libre para Phase 44 / INT-03, sin pre-crear ni reservar con placeholder (D-41-14, D-40-07, D-43-22; edge: id-space boundary)."
    - "`correctIndex` es un entero en `[0,4)` y NO es constante dentro de ningún slot — si lo fuera, el autor aprendería la posición en vez de la forma (D-43-22)."
    # --- D-43-02 / D-43-20 : el estado del hand-off al quórum top-level ---
    - "Al cerrar este plan los 3 slots llevan `validation` con `status: \"pending\"` y `passes: []`: el executor NO fabrica pases ni estampa `validated`, porque el quórum base canónico Opus+Sonnet vive en una pasada TOP-LEVEL posterior — el skill `gsd-validate-exercise` spawnea Task subagents y un `gsd-executor` no puede hacerlo (D-43-02, VAL-03)."
    - "`VAL_07_STRICT=1 node --test tests/*.test.js` falla EXACTAMENTE en el test de `content/exercises/fare-cond-imperativo.json` nombrando sus slots `pending`, y ninguna otra categoría cambia de estado: ese rojo es el marcador visible del hand-off, porque el reporter `scripts/run-validation-271.mjs` no se pone rojo — queda CIEGO al fichero nuevo y sigue reportando su total anterior en PASS hasta Phase 44 / INT-02 (D-43-02, Out of Scope)."
    - "`tests/content-fare-cond-imperativo.test.js` incluye el gate condicionado de ronda EXTRA con `EXTRA_ROUND_SLOTS = ['fare-cond-imperativo-imperativo']`: si ese slot llega a `validated` sin un pase cuyo `by` empiece por `deepseek`, la suite se pone roja — el condizionale passato NO lleva ronda extra, su riesgo se controla con el gate de marco y no con presupuesto de quórum (D-43-20)."
    - "`tests/content-fare-cond-imperativo.test.js` exige `status === deriveStatus(passes)` importando `deriveStatus` de `src/data/validation-state.js` (nunca reimplementándolo), de modo que un `validated` escrito a mano sin ≥2 pases `correcta` de `by` distintos y con cero `incorrecta` no puede pasar por bueno (D-43-21, VAL-03)."
    # --- D-43-21 : canon editorial ---
    - "La línea nueva de `CATEGORIES_WITH_EXPLANATIONS` (`tests/exercise-types.test.js`, tras la entrada de `fare-congiuntivo` de la línea 1360) con `expected: slotCountOf('content/exercises/fare-cond-imperativo.json')` activa los sub-tests editoriales sobre el fichero nuevo y todos pasan: explanation no vacía por slot, cero smart-quotes, cero markdown, cero leak R1 en los prompts, cero cross-refs R2 en las explanations (D-43-21, SC-5)."
    - "Las 3 explanations están en español acentuado RAE, plain text de un solo párrafo sin markdown ni backticks, con apóstrofes ASCII U+0027 al citar italiano y sin comillas tipográficas; un flag C4-accent del quórum sobre español sin tildes es bug REAL y se arregla poniendo los acentos, nunca con override (D-43-21)."
    # --- trampa del clonado nº1 y nº3 de 43-PATTERNS.md ---
    - "El gate de blacklist de `tests/content-fare-cond-imperativo.test.js` no pone roja la variante correcta: la entrada `fa` usa un matcher propio con lookahead negativo del apóstrofe ASCII, porque el `wordish` heredado hace match DENTRO de `fa'` (el apóstrofe cuenta como no-letra) — la desviación está declarada en la cabecera del fichero de test (43-PATTERNS.md §Trampas del clonado, trampa 1)."
    - "El escaneo en positivo del `notes` no clona ciegamente el idioma heredado de envolver la forma entre apóstrofes para la key apostrofada: `fa'` NO es elemento de `BLACKLIST` (es la key), y su presencia documentada en el `notes` se comprueba con el literal distintivo `la forma fa'`, declarado como desviación en la cabecera del fichero de test (43-PATTERNS.md §Trampas del clonado, trampa 3)."
    # --- valla del motor ---
    - "`git status --porcelain` no lista ningún fichero fuera de los 4 de `files_modified`; en particular `src/`, `scripts/`, `content/exercises/fare-indefiniti.json` y `tests/fixtures/` quedan sin tocar (Out of Scope literal del milestone y frontera con el plan 43-02)."

  artifacts:
    - "`content/exercises/fare-cond-imperativo.json` — fichero NUEVO con exactamente 2 claves top-level (`notes`, `exercises`), el `notes` con las declaraciones de esta categoría (identidad y volumen 3 slots x 6/6/5 = 17, ausencia de `io` estructural, MAGNET `fa'` con audit trail, blacklist de `fai`/`fa`/clíticos/arcaicas, homografía con congiuntivo e indicativo declarada y enseñada, RECONOCER NO PRODUCIR, gate de vocativo inequívoco, gate de exclusión del deíctico de futuro, gate de exclusión de la prótasis en el passato, patrones fijos de distractoras, SCOPE-GATE del objeto literal, 0-gloss, 0-match, 0-word-buttons, nota de escaneo por campo y nota de count-sync), y 3 slots con 6, 6 y 5 variantes."
    - "`content/categories.json` — 17 entradas; la nueva es `fare-cond-imperativo`, `order: 17`, `origen: \"ia-quorum\"`, `name` con raya em U+2014 siguiendo el patrón descriptivo de las 16 existentes."
    - "`tests/exercise-types.test.js` — 1 línea nueva en `CATEGORIES_WITH_EXPLANATIONS` con `expected` dinámico vía `slotCountOf`, más un comentario de una línea que nombra la fase y los requisitos."
    - "`tests/content-fare-cond-imperativo.test.js` — fichero NUEVO de invariantes permanentes de la categoría, 13 bloques `describe` clonados de `tests/content-fare-congiuntivo.test.js`, con las desviaciones declaradas en la cabecera."

  key_links:
    - "`content/categories.json` → `src/data/schema-validator.js` (`knownCategoryIds`): sin la entrada `fare-cond-imperativo`, cada slot emite `referencia a categoría desconocida` y `tests/domain.test.js` se pone rojo. Es la razón de que la entrada viva en la tarea tracer y no al final."
    - "`content/categories.json` → `categoriesForDisplay` (`src/screens/app.js`): el array de `content.categories` se mapea 1:1, así que la entrada es lo ÚNICO que hace aparecer la fila en home, en el picker, en Repaso y en Examen. `order` es documental; el orden del array define el display."
    - "`variants[]` → `pickVariantIndex` (`src/domain/session.js:232`): la longitud del array es lo que produce «una casilla distinta cada pasada». La función es `Pure` y axis-agnostic, y por eso un slot de 5 variantes funciona sin una línea de motor nueva — es la prueba de que `git diff src/` queda vacío."
    - "el marcador de destinatario del `prompt` → la unicidad de la respuesta en las 5 variantes del imperativo: con `fai` y `fa` vetadas, el único desambiguador entre `fa'`, `faccia`, `facciamo`, `fate` y `facciano` es a quién se le habla. Un prompt sin vocativo inequívoco convierte la variante en irresoluble y, por la cascada D-54, en un reset de la categoría entera."
    - "el marco temporal del `prompt` → la exclusión de la distractora de futuro en el condizionale presente y de la de condizionale presente en el passato: un deíctico de futuro en el primero, o una prótasis hipotética en el segundo, hacen defendible la distractora y abren la doble validez que produce `disputed` sticky."
    - "`slotCountOf` (`tests/exercise-types.test.js:1335-1336`) → el `expected` de la línea nueva: al ser dinámico la línea es auto-consistente y no puede ponerse roja por conteo, que es lo que permite añadirla ya sin invadir INT-02. El gate de conteo REAL (3 slots / 17 variantes) vive solo en `tests/content-fare-cond-imperativo.test.js`."
    - "`validation.passes[]` → `deriveStatus` (`src/data/validation-state.js`): `validated` exige ≥2 pases `correcta` con `by` distintos y cero `incorrecta`; cualquier `incorrecta` da `disputed` STICKY, y por la cascada D-54 un `disputed` en esta categoría es un reset de las 17 variantes. Escribir `status: \"validated\"` con `passes: []` produce un estado incoherente que el gate permanente de la Task 4 detecta."
    - "el slug `fare-cond-imperativo` → `RESET_PREFIXES_V13` (`src/data/storage.js:1345`, Phase 40 shippeada): una divergencia de una letra deja la categoría fuera del reset selectivo sin que ningún test lo diga. Y ojo a D-40-03: `fare-indicativo` y `fare-indefiniti` comparten el prefijo `fare-ind`, así que toda comprobación de prefijo declara el slug COMPLETO, nunca truncado."

  prohibitions:
    - "MUST NOT dar de alta una variante en la que más de una opción sea una respuesta defendiblemente correcta para ese prompt: el quórum la marca `disputed` y `disputed` es STICKY, así que por la cascada D-54 un solo ejercicio injusto resetea la categoría entera y le enseña al autor que su respuesta correcta es incorrecta. Es el daño exacto que esta herramienta existe para evitar."
    - "MUST NOT introducir `fai`, `fa` ni ninguna forma con clítico en `options` para «cubrir el error típico»: son formas atestiguadas y defendibles, y el criterio ratificado el 2026-08-03 las manda a la blacklist con audit trail, no al ejercicio. Ofrecerlas como distractora «incorrecta» viola SC-2 al pie de la letra."
    - "MUST NOT resolver el MAGNET de la 2ª singular eligiendo `fai` o `fa` como key para «no complicar»: el autor vería la misma respuesta en dos categorías con etiquetas distintas y `fa'` no se examinaría nunca, que es justo lo que la fase existe para arreglar."
    - "MUST NOT sacar una variante del imperativo para que salgan 6 personas, ni añadir una casilla de `io`, ni mezclar una variante con clítico: SC-2 fija EXACTAMENTE 5 y la ausencia de `io` es un hecho del italiano que hay que documentar, no un hueco que rellenar."
    - "MUST NOT pedir en ninguna variante que el autor decida entre imperativo y congiuntivo con las formas homógrafas: no hay marco que excluya limpiamente una de las dos lecturas, así que el contraste activo produce dos respuestas defendibles. La homografía se declara en `notes` y se enseña en la explanation; nunca se pregunta."
    - "MUST NOT escribir `validation.status: \"validated\"` ni ninguna entrada de `passes[]` que no venga de un pase de modelo real: el audit trail es la única evidencia que el autor tiene de que una variante fue revisada, y un pase inventado la destruye sin dejar rastro."
    - "MUST NOT hacer override de un flag C4-accent del quórum sobre español sin tildes: en este proyecto ese flag es un bug REAL y se arregla poniendo los acentos. El override existe para adjudicaciones razonadas del autor, no para ahorrarse una tilde."
    - "MUST NOT presentar una forma regional, dialectal, arcaica o de registro literario como si fuera la norma del italiano estándar, ni en un sentido ni en el otro: el autor no puede distinguirlas y la interiorizaría como regla. Vale también al revés: `fa'` es norma, no arcaísmo, y la explanation tiene que decirlo."
    - "MUST NOT examinar vocabulario que el autor no ha estudiado sin glosarlo: fallar una variante por no conocer una locución — no por no saber el condizionale — resetea la categoría entera por la cascada D-54. Es la misma injusticia por otra puerta."
    - "MUST NOT reducir el alcance de una decisión de 43-CONTEXT.md a una versión «v1», «simplificada», «estática por ahora» o «pendiente de cablear»: si algo no cabe, se parte la fase y se pide aprobación, no se entrega menos de lo decidido."
    - "MUST NOT renombrar el slug `fare-cond-imperativo` ni ningún id de slot después de darlo de alta: los ids viven en el `exerciseStats` real del autor y en `RESET_PREFIXES_V13`, y un renombrado deja su progreso huérfano en silencio."
    - "MUST NOT tocar el motor v1.4 (`src/screens/app.js`, `src/domain/`, `src/data/`) ni introducir dependencia, `package.json` o paso de build: el proyecto es web estática de dependencias cero y si el contenido exigiera cambiar el motor, el diseño del contenido está mal (Out of Scope literal del milestone, CLAUDE.md §Constraints)."
    - "MUST NOT autorar cruces multi-categoría ni usar el espacio de ids `fare-cond-imperativo-300`+, ni sincronizar `CATEGORIES` de `scripts/run-validation-271.mjs`, `REAL_CATEGORIES` ni `TOTAL_EXPECTED`: son el entregable literal de Phase 44 / INT-02 e INT-03, y el rojo/ciego de esta fase es el estado esperado."
    - "MUST NOT tocar `content/exercises/fare-indefiniti.json`, `tests/content-fare-indefiniti.test.js` ni `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md`: son propiedad exclusiva del plan 43-02 y duplicar la edición del prompt de validación rompería el «exactamente una vez»."
---

<objective>
Dar de alta la categoría `fare-cond-imperativo` en el disco y en el registro, y autorarla **de punta a
punta**: 3 slots `multiple-choice` — condizionale presente (6 variantes), condizionale passato (6) e
imperativo presente (**5**, no 6) = **17 variantes** —, más su fichero de invariantes permanentes
`tests/content-fare-cond-imperativo.test.js`.

La primera tarea es un **tracer**: un solo slot (`fare-cond-imperativo-cond-presente`) + su `notes`
completo + la entrada de `content/categories.json` con `order: 17` + la línea del smoke paramétrico,
verificado de punta a punta antes de que exista una segunda variante. Se elige ese slot porque es donde
vive el contraste que SC-1 nombra —futuro frente a condizionale sobre la misma raíz contracta `far-`— y
porque su gate más delicado (que ningún prompt lleve un deíctico de futuro, o la distractora pasa a ser
defendible) es el mismo patrón que gobierna los otros dos slots: si está mal, está mal una vez y no seis.

Cubre CI-01 (condizionale presente en las 6 personas con la raíz contracta), CI-02 (condizionale
passato, incluido el futuro en el pasado que el español resuelve de otra forma) y CI-03 (imperativo
presente con EXACTAMENTE 5 variantes y el MAGNET de la 2ª singular resuelto con audit trail).

Purpose: esta categoría contiene el MAGNET declarado del milestone. `fa'`, `fai` y `fa` están las tres
atestiguadas como imperativo de 2ª singular; `fai` y `fa` ya son keys de `fare-indicativo`. El fallo
sistemático no es escribir una forma rara: es ofrecer `fai` como distractora «incorrecta» —lo natural,
porque es el error típico— y con eso dar por incorrecta una forma que el italiano acepta. Ese ejercicio
es injusto, el quórum lo marca `disputed`, `disputed` es STICKY y la cascada D-54 resetea las 17
variantes. Por eso la blacklist, el vocativo y el paradigma de 5 van declarados en `notes` ANTES de la
primera variante, con el quórum como red y no como mecanismo.

Output: `content/exercises/fare-cond-imperativo.json` con `notes` + 3 slots + 17 variantes en
`validation.status: "pending"`, `content/categories.json` con 17 entradas, 1 línea nueva en
`CATEGORIES_WITH_EXPLANATIONS` y `tests/content-fare-cond-imperativo.test.js` con 13 `describe`.
Ningún fichero de `src/` cambia.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-CONTEXT.md
@.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-PATTERNS.md
</context>

## Frontera de este plan (y de la fase)

Lo que este plan NO hace, declarado para que la verificación no lo busque:

- **La categoría `fare-indefiniti` entera** — fichero de contenido, entrada `order: 18`, su línea del
  smoke y su fichero de test → **plan 43-02, wave 2**. Los dos plans escriben en `content/categories.json`
  y en el array del smoke, y por eso van en waves distintas y no en paralelo (D-43-02).
- **La edición de `09-VALIDATION-PROMPT.md`** (la excepción de `facente funzione` al SCOPE-GATE y el
  principio «reconocer, no producir») → **plan 43-02, Task 5**, exactamente una vez. Este plan **no**
  toca ese fichero, aunque `fa'` y la blacklist de `fai`/`fa` son suyas: la edición es una sola y llega
  antes de la pasada top-level de quórum, que corre cuando los dos plans han cerrado.
- **Cruces multi-categoría** (`fare-cond-imperativo-300`+) → Phase 44, INT-03. El espacio de ids `-300`+
  se deja libre: no se pre-crea, no se reserva con placeholder, simplemente no se usa.
- **Sync de counts**: `CATEGORIES` de `scripts/run-validation-271.mjs`, `REAL_CATEGORIES` de la fixture
  de integración, `TOTAL_EXPECTED` y la fórmula del baseline-guard → Phase 44, INT-02. **El rojo de
  `VAL_07_STRICT=1` y la ceguera del reporter al cerrar esta fase son el estado ESPERADO**, no un fallo
  que haya que arreglar de paso.
- **El motor v1.4.** `git diff src/screens/app.js src/domain/ src/data/` queda vacío al cierre. Los 3
  slots tienen un solo eje de variante cada uno, así que `pickVariantIndex` los sirve sin una línea nueva.
- **Un eje de variante nuevo** — Out of Scope literal del milestone.
- **Perífrasis y modismos de `fare`** — SCOPE-GATE HARD heredado de D-41-06. La excepción acotada de
  D-43-18 es de `fare-indefiniti` y **no aplica aquí**: en esta categoría el gate léxico muerde entero.
- **El quórum base canónico Opus+Sonnet y la ronda EXTRA DeepSeek** — no corren dentro del executor
  (D-43-02). Ver §Hand-off.

## Correcciones y precisiones de plan-time (verificadas en el codebase el 2026-08-07)

Seis puntos donde la letra de 43-CONTEXT.md necesita una precisión sobre hechos comprobados. Ninguna
cambia una decisión; todas evitan un gate insatisfacible, un rojo espurio o una variante con dos lecturas.

**1. Baseline de la suite: 842 pass / 0 fail**, medido el 2026-08-07 con `node --test tests/*.test.js`
en árbol limpio. Ojo (`[[test_command_node_glob]]`): el glob es obligatorio, el path desnudo `tests/`
falla en Node 22.20. El marcador honesto del trabajo pendiente de esta fase es
`VAL_07_STRICT=1 node --test tests/*.test.js`, no el reporter.

**2. `CATEGORIES_WITH_EXPLANATIONS` está en la línea 1338, no en la 1273.** Coordenadas verificadas:
helper `slotCountOf` en 1335-1336, apertura del array en 1338, entrada de `fare-indicativo` en 1358,
comentario + entrada de `fare-congiuntivo` en 1359-1360, comentario de cobertura editorial en 1361,
cierre en 1362. El append de este plan va **tras la línea 1360 y antes del comentario de la 1361**.
43-CONTEXT.md daba la coordenada de antes de la revisión IN-03 de Phase 42.

**3. El vocativo de D-43-05 se realiza como vocativo ITALIANO, no como etiqueta.** D-43-05 nombra los
cuatro destinatarios en español (`a un amico`, `a un signore`, `ai bambini`, `a dei clienti`) porque los
está caracterizando, no dictando la cadena literal del prompt: escribirlos tal cual en italiano metería
español en el enunciado y cambiaría el significado de la frase. Se realizan como los cinco marcadores
italianos del conjunto cerrado de la Task 3. Y son **cinco**, no cuatro: el imperativo de `noi` es
exhortativo y no tiene destinatario, así que su marcador es el sujeto inclusivo explícito, declarado
como desviación en `notes` y en la tabla del test.

**4. `Signori, ___ tutto` tiene DOS respuestas defendibles y hay que blindarlo.** En italiano moderno
`Signori, fate...` es corriente, así que un vocativo de cortesía en plural admite tanto `fate` (voi)
como `facciano` (Loro). La variante de `Loro` lleva por eso el **pronombre de cortesía explícito `Loro`
con L mayúscula** dentro del prompt, que exige la 3ª persona plural y excluye `fate`. Es el análogo
exacto de D-41-07 aplicado al imperativo formal, y es un gate, no una preferencia de redacción. Mismo
razonamiento en la variante de `noi`: `Dai, ___ una torta insieme!` admitiría `fate`, así que el sujeto
inclusivo explícito `noi due` es obligatorio.

**5. El deíctico de futuro es el gate del condizionale presente, y no estaba escrito.** D-43-09 fija el
futuro semplice como distractora de las 6 variantes, y eso es correcto y lo que SC-1 pide. Pero un
prompt con `domani` o `stasera` hace defendible ese futuro y produce la doble validez que D-43-10
declara como riesgo del OTRO slot. El gate espejo va escrito: **cero deícticos de futuro en los 6
prompts del condizionale presente**, con su conjunto cerrado, y cada prompt lleva un disparador de
condizionale del conjunto cerrado.

**6. La prótasis condicional queda fuera del condizionale passato.** El trabajo fino que D-43-10 deja
abierto —blindar que `farebbe` no sea defendible— tiene una solución grep-verificable que no cuesta
ninguna variante: el slot no usa prótasis. `Se avesse tempo, lui ___ il lavoro` admite `farebbe`; los
marcos de futuro-nel-passato, de acción no realizada y de rumor en pasado no lo admiten. Gate: **cero
apariciones de la palabra suelta `se` en los 6 prompts del condizionale passato**. Se prefirió esto al
matiz sintáctico (prótasis con congiuntivo trapassato sí, con imperfetto no) porque ese matiz no es
mecánicamente comprobable y este gate sí, y porque los tres marcos que quedan cubren de sobra lo que
CI-02 y SC-1 piden. El helper `gobiernaElHueco` del test análogo se reusa para el marco de
futuro-nel-passato de D-43-11.

## Artifacts this phase produces

Símbolos e identificadores **creados** por la fase 43; un pase de source-grounding debe excluirlos de la
verificación de drift porque no existen hoy en el codebase.

| Símbolo / identificador | Fichero | Tipo | Plan |
|---|---|---|---|
| `content/exercises/fare-cond-imperativo.json` | (nuevo) | fichero de contenido, 2 claves top-level | 43-01 |
| `content/exercises/fare-indefiniti.json` | (nuevo) | fichero de contenido, 2 claves top-level | 43-02 |
| `fare-cond-imperativo` | `content/categories.json` | `id` de categoría (`order: 17`, `origen: "ia-quorum"`) | 43-01 |
| `Fare — condizionale e imperativo (farei/avrei fatto/fa')` | `content/categories.json` | `name` de la entrada (raya em U+2014, apóstrofe ASCII) | 43-01 |
| `fare-indefiniti` | `content/categories.json` | `id` de categoría (`order: 18`, `origen: "ia-quorum"`) | 43-02 |
| `Fare — formas indefinidas (fare/fatto/facendo)` | `content/categories.json` | `name` de la entrada (raya em U+2014) | 43-02 |
| `fare-cond-imperativo-cond-presente` | `content/exercises/fare-cond-imperativo.json` | id de slot MC, 6 variantes (eje: persona) | 43-01 |
| `fare-cond-imperativo-cond-passato` | idem | id de slot MC, 6 variantes (eje: persona del auxiliar) | 43-01 |
| `fare-cond-imperativo-imperativo` | idem | id de slot MC, **5** variantes (eje: persona/registro) | 43-01 |
| `fare-indefiniti-infinito-presente` | `content/exercises/fare-indefiniti.json` | id de slot MC, 3 variantes (eje: contexto) | 43-02 |
| `fare-indefiniti-infinito-passato` | idem | id de slot MC, 3 variantes (eje: contexto) | 43-02 |
| `fare-indefiniti-participio-passato` | idem | id de slot MC, **4** variantes (eje: terminación) | 43-02 |
| `fare-indefiniti-participio-presente` | idem | id de slot MC, **2** variantes (eje: contexto) | 43-02 |
| `fare-indefiniti-gerundio-presente` | idem | id de slot MC, 3 variantes (eje: contexto) | 43-02 |
| `fare-indefiniti-gerundio-passato` | idem | id de slot MC, 3 variantes (eje: contexto) | 43-02 |
| entrada `fare-cond-imperativo` en `CATEGORIES_WITH_EXPLANATIONS` | `tests/exercise-types.test.js` | elemento de array de test | 43-01 |
| entrada `fare-indefiniti` en `CATEGORIES_WITH_EXPLANATIONS` | `tests/exercise-types.test.js` | elemento de array de test | 43-02 |
| `tests/content-fare-cond-imperativo.test.js` | (nuevo) | fichero de test de invariantes de la categoría | 43-01 |
| `describe` 1 `fare-cond-imperativo — estructura y conteos (D-43-01, D-43-03, D-43-08)` | idem | bloque de test | 43-01 |
| `describe` 2 `fare-cond-imperativo — paradigma completo, 17 keys sin repeticion (CI-01, CI-02, CI-03)` | idem | bloque de test | 43-01 |
| `describe` 3 `fare-cond-imperativo — pronombre explicito y vocativo inequivoco (D-41-07, D-43-05)` | idem | bloque de test | 43-01 |
| `describe` 4 `fare-cond-imperativo — 0-gloss del verbo (D-41-05, D-43-22)` | idem | bloque de test | 43-01 |
| `describe` 5 `fare-cond-imperativo — SCOPE-GATE lexico del objeto literal (D-41-06 heredado)` | idem | bloque de test | 43-01 |
| `describe` 6 `fare-cond-imperativo — blacklist de formas atestiguadas y defendibles (D-43-04, D-43-07)` | idem | bloque de test | 43-01 |
| `describe` 7 `fare-cond-imperativo — distractoras del condizionale presente, el futuro en las 6 (D-43-09, SC-1)` | idem | bloque de test | 43-01 |
| `describe` 8 `fare-cond-imperativo — distractoras del condizionale passato, el calco espanol (D-43-10, SC-1)` | idem | bloque de test | 43-01 |
| `describe` 9 `fare-cond-imperativo — slot del imperativo, 5 variantes y registro (D-43-04, D-43-05, D-43-08, SC-2)` | idem | bloque de test | 43-01 |
| `describe` 10 `fare-cond-imperativo — marcos, objetos y destinatarios por variante (D-43-10, D-43-11)` | idem | bloque de test | 43-01 |
| `describe` 11 `fare-cond-imperativo — canon editorial e higiene del JSON (D-43-21, T-43-01, T-43-02)` | idem | bloque de test | 43-01 |
| `describe` 12 `fare-cond-imperativo — audit trail de validacion y ronda EXTRA (D-43-20, T-43-03)` | idem | bloque de test | 43-01 |
| `describe` 13 `fare-cond-imperativo — registro de la categoria (D-43-22, SC-5)` | idem | bloque de test | 43-01 |
| `tests/content-fare-indefiniti.test.js` y sus 13 `describe` | (nuevo) | fichero de test de invariantes de la categoría | 43-02 |

Espacio de ids reservado y **no usado** por la fase: `fare-cond-imperativo-300` y `fare-indefiniti-300`
y siguientes (Phase 44, INT-03). No se pre-crean ni se reservan con placeholder.

## Multi-source coverage audit

| Fuente | Item | Plan | Estado |
|--------|------|------|--------|
| GOAL | ROADMAP §Phase 43 Goal — cerrar el paradigma con las dos categorías de cola, DOS unidades de reset y no una fusión | 43-01 (`fare-cond-imperativo`) + 43-02 (`fare-indefiniti`) | COVERED |
| GOAL | SC-1 — condizionale presente en 6 personas con raíz contracta, condizionale passato con el futuro en el pasado, explanation que señala la divergencia con el español, al menos una variante `farà` frente a `farebbe` | 43-01 T1 (presente, las 6) + T3 (passato) | COVERED |
| GOAL | SC-2 — imperativo con EXACTAMENTE 5 variantes, ausencia de `io` documentada, MAGNET resuelto con audit trail, ninguna de `fa'`/`fai`/`fa` como distractora incorrecta | 43-01 T3 (slot) + T1 (`notes`) + T4 (gates) | COVERED |
| GOAL | SC-3 — las 6 formas indefinidas con eje CONTEXTO, infinito por anterioridad, gerundio con `stare + gerundio` | 43-02 T1/T3/T4 | COVERED |
| GOAL | SC-4 — doble comportamiento de `fatto` con ronda EXTRA, y `facente` con nota de registro | 43-02 T3 (participio passato) + T4 (`facente`) + §Hand-off (ronda EXTRA) | COVERED |
| GOAL | SC-5 — las dos categorías cargan en boot como unidades de reset SEPARADAS y todas sus variantes quedan `validated` con canon editorial y sin leak R1 | 43-01 T1 (registro `order: 17`) + T2 (checkpoint) + 43-02 T1 (`order: 18`) + pase top-level | COVERED |
| REQ | CI-01 condizionale presente en las 6 personas, raíz contracta `far-` | 43-01 T1 | COVERED |
| REQ | CI-02 condizionale passato, futuro en el pasado `ha detto che avrebbe fatto` | 43-01 T3 | COVERED |
| REQ | CI-03 imperativo presente, 5 variantes, MAGNET de la 2ª singular | 43-01 T3 | COVERED |
| REQ | INDEF-01 infinito presente y passato, elección por anterioridad | 43-02 T1 + T3 | COVERED |
| REQ | INDEF-02 participio passato como forma léxica e invariable con `avere` frente a la concordancia | 43-02 T3 | COVERED |
| REQ | INDEF-03 participio presente `facente` con nota de registro explícita | 43-02 T4 | COVERED |
| REQ | INDEF-04 gerundio presente y passato, incluido `stare + gerundio` | 43-02 T4 | COVERED |
| RESEARCH | — v2.0 sin RESEARCH.md por diseño: el documento FARE-X1 cerró el research antes de abrir el milestone. Los análogos los aportan 43-CONTEXT.md §canonical_refs y 43-PATTERNS.md | n/a | N/A por diseño |
| CONTEXT | D-43-01 dos ficheros, dos categorías, 9 slots / 35 variantes (costly) | 43-01 (17) + 43-02 (18) | COVERED |
| CONTEXT | D-43-02 2 plans secuenciales, uno por categoría, tracer por plan, quórum en pasada TOP-LEVEL, VAL-03 inviolable (costly) | la estructura de los 2 plans + §Hand-off | COVERED |
| CONTEXT | D-43-03 reparto DESIGUAL de variantes en `fare-indefiniti`, justificado slot a slot (costly) | 43-02 T1 (`notes`) + T3 + T4 + T5 (tabla `EXPECTED_VARIANTS`) | COVERED |
| CONTEXT | D-43-04 la key de la 2ª singular es `fa'`; `fai` y `fa` a blacklist (costly) | 43-01 T1 (`notes`) + T3 (slot) + T4 (gate) | COVERED |
| CONTEXT | D-43-05 distractoras del imperativo = formas reales de OTRO registro + gate de vocativo inequívoco | 43-01 T3 + T4 | COVERED |
| CONTEXT | D-43-06 la homografía con congiuntivo e indicativo se declara y se enseña, nunca se contrasta | 43-01 T1 (`notes`) + T3 (explanation) + prohibición | COVERED |
| CONTEXT | D-43-07 formas con clítico a blacklist + mención en la explanation | 43-01 T1 (`notes`) + T3 + T4 | COVERED |
| CONTEXT | D-43-08 la ausencia de `io` es estructural, declarada en `notes`, en la explanation y en un gate de test | 43-01 T1 + T3 + T4 | COVERED |
| CONTEXT | D-43-09 patrón fijo de distractoras del condizionale presente, el futuro en las 6 | 43-01 T1 + T4 | COVERED |
| CONTEXT | D-43-10 distractoras del condizionale passato, el condizionale simple como calco español, con gate de unicidad de lectura | 43-01 T3 + T4 (+ §Correcciones 6) | COVERED |
| CONTEXT | D-43-11 el futuro en el pasado, ≥2 de 6 variantes con marco de futuro-nel-passato y explanation que señala la divergencia | 43-01 T3 + T4 | COVERED |
| CONTEXT | D-43-12 conjunto CERRADO de tipos de contexto sintáctico, declarado en `notes` | 43-02 T1 (`notes`) + T3 + T4 + T5 | COVERED |
| CONTEXT | D-43-13 pool CERRADO de formas no personales con gate grep-verificable, cero conjugadas | 43-02 T1 + T5 | COVERED |
| CONTEXT | D-43-14 `non fare` como contexto del infinito presente | 43-02 T1 | COVERED |
| CONTEXT | D-43-15 `stare + gerundio` como variante del gerundio presente, con la explanation del uso y no de la formación | 43-02 T4 | COVERED |
| CONTEXT | D-43-16 participio passato con eje TERMINACIÓN y gate HARD de pronombre (solo `lo`/`la`/`li`/`le`) | 43-02 T3 + T5 | COVERED |
| CONTEXT | D-43-17 `aver fatto` como CUARTO magnet, `avere fatto` a la blacklist (reversible) | 43-02 T1 (`notes`) + T3 + T5 | COVERED |
| CONTEXT | D-43-18 `facente`: excepción declarada y acotada al SCOPE-GATE (reversible) | 43-02 T4 + T5 (`notes`, test y `09-VALIDATION-PROMPT.md`) | COVERED |
| CONTEXT | D-43-19 principio declarado de RECONOCER, NO PRODUCIR | 43-01 T1 (`notes`) + T3 (explanation) + 43-02 T1 + T5 (`09-VALIDATION-PROMPT.md`) | COVERED |
| CONTEXT | D-43-20 ronda EXTRA DeepSeek sobre 12 de 35 variantes (imperativo 5 + participio passato 4 + infinito passato 3) | §Hand-off de los 2 plans + 43-01 T4 y 43-02 T5 (gates condicionados) | COVERED |
| CONTEXT | D-43-21 canon editorial locked (RAE acentuado, apóstrofe ASCII U+0027, plain text, sin leak R1, `validated` por quórum) | 43-01 T1/T3/T4 + 43-02 T1/T3/T4/T5 | COVERED |
| CONTEXT | D-43-22 heredados: slugs exactos, slot+variantes, MC-only, SCOPE-GATE, pronombre explícito, blacklist por campo, ids semánticos, entrada obligatoria en `categories.json`, 0-gloss | 43-01 T1/T3/T4 + 43-02 T1/T3/T4/T5 | COVERED |

**0 items MISSING.** No se requiere split de fase. Los ítems de §Deferred de 43-CONTEXT.md quedan fuera
por decisión explícita y no cuentan como hueco.

## Hand-off al pase TOP-LEVEL de quórum (D-43-02, D-43-20)

Explícito porque es el punto donde algo puede parecer hecho sin estarlo. **Es un entregable visible de la
fase, no un supuesto.** Ninguna tarea de este plan lo ejecuta.

| Momento | `validation.status` | `validation.passes[]` | Cómo se comprueba |
|---|---|---|---|
| Al cerrar 43-01 | `"pending"` en los 3 slots de `fare-cond-imperativo` | `[]` | `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los slots `pending` |
| Al cerrar 43-02 | `"pending"` en los 9 slots de las dos categorías | `[]` | idem, con 9 slots |
| Tras el pase top-level | `"validated"` en los 9 | ≥2 pases `correcta` con `by` distintos en los 9; y en los 3 slots de ronda EXTRA al menos un `by` que empieza por `deepseek` | `VAL_07_STRICT=1 node --test tests/*.test.js` en verde |

El quórum base canónico (Opus + Sonnet vía el skill `gsd-validate-exercise`, que spawnea Task subagents)
**no está disponible dentro de un subagent `gsd-executor`** (`[[executor_cannot_run_task_quorum]]`). Se
corre en top-level tras `execute-phase`, **un ejercicio por contexto, NUNCA batched (VAL-03)**.

Criterios de aceptación del pase top-level, para que no quede como intención:

1. Los 9 slots pasan de `pending` a `validated`, y `status === deriveStatus(passes)` en los 9.
2. **La edición de `09-VALIDATION-PROMPT.md` (43-02 Task 5) tiene que estar hecha ANTES del pase.** El
   subagent del quórum no ve el `notes`: sin la excepción escrita en el prompt, marcará `facente funzione`
   como violación y `fa'` / `aver fatto` como magnets sin resolver (`[[exception_belongs_in_validation_prompt]]`).
3. **Ronda EXTRA obligatoria (D-43-20):** pase DeepSeek vía `scripts/validate-ai-pass.mjs` (claves en
   `.env`) sobre exactamente **12 de las 35 variantes** — el slot de imperativo completo (5), el de
   `fare-indefiniti-participio-passato` completo (4) y el de `fare-indefiniti-infinito-passato` completo
   (3) —, **además** del quórum base. El audit trail se agrega a nivel de slot, así que esos 3 slots
   acaban con un `by` `deepseek-*`. El condizionale passato **no** lleva ronda extra: su riesgo es de
   redacción de marco, no de doble validez de forma, y se controla con el gate de §Correcciones 6.
4. En esa misma pasada, el pase Opus de los slots delicados registra en sus `concerns` la re-declaración
   local de sus gates (vocativo inequívoco, exclusión del deíctico de futuro, ausencia de prótasis en el
   passato) — un pase `correcta` admite `concerns` declarativas, con precedente literal en
   `content/exercises/riflessivi.json` → `riflessivi-pp-concordanza`.
5. `by` = el modelo **realmente resuelto** (`claude-opus-5`, `claude-sonnet-5`, `deepseek-reasoner`), no
   el ID pinneado de la skill (`[[top_level_quorum_mechanics]]`).
6. **Un flag C4-accent del quórum sobre español sin tildes es bug REAL y se arregla poniendo los
   acentos, nunca con override** (`[[explanations_must_be_accented]]`).

## Assumptions (probe de edges sin SPEC — 7 filas `unclassified` surfaceadas, 0 descartes silenciosos)

No existe SPEC.md para esta fase, así que el probe determinista de edges corrió sobre CI-01..CI-03 e
INDEF-01..INDEF-04 y devolvió las **7** filas como `unclassified`
(`coverage: {applicable: 7, resolved: 0, unresolved: 7, byVerification: {explicit: 0, backstop: 0}}`).
Su clasificador funciona por palabras clave en inglés (boundary / null / concurrency / ordering) y el
texto de los requisitos está en español y es de autoría de contenido. Por protocolo una fila
`unclassified` queda **`unresolved`** y **nunca** se auto-`backstop`ea ni se auto-descarta: se surfacea
aquí como **supuesto explícito marcado**.

| Requisito | Qué NO derivó el probe | Supuesto marcado que asume este plan |
|---|---|---|
| CI-01 | ninguna categoría de edge | el edge real es la **frontera de defendibilidad temporal**: con la distractora de futuro en las 6 variantes, cualquier deíctico de futuro en el prompt abre una segunda respuesta correcta. Autorado a mano como truth (§Correcciones 5) |
| CI-02 | idem | el edge real es la **frontera de lectura del marco**: sin un ancla de pasado que gobierne la cláusula del hueco, `farebbe` es defendible como hipotético presente. Autorado a mano como truth y como gate de ausencia de prótasis (§Correcciones 6) |
| CI-03 | idem | el edge real es la **frontera de registro**: un vocativo de plural admite `fate` y `facciano` a la vez, y el imperativo de `noi` sin sujeto inclusivo admite `fate`. Autorados a mano como truth explícita y como el único `backstop` de este plan (§Correcciones 3 y 4) |
| INDEF-01 | idem | tratado en 43-02: el edge es que el marco fuerce la anterioridad, o `fare` y `aver fatto` son las dos defendibles; más el cuarto magnet `aver fatto` frente a `avere fatto` |
| INDEF-02 | idem | tratado en 43-02: el edge es el pronombre antepuesto — con `mi`/`ti`/`ci`/`vi` la concordancia es OPCIONAL y con `ne` tiene reglas propias, así que solo `li` y `le` producen una única respuesta |
| INDEF-03 | idem | tratado en 43-02: el edge es el choque entre INDEF-03 y el SCOPE-GATE de D-41-06, resuelto como excepción enumerada por id de slot y escrita también en `09-VALIDATION-PROMPT.md` |
| INDEF-04 | idem | tratado en 43-02: el edge es que el marco progresivo exija `facendo` sin admitir el gerundio adverbial, y al revés |

**Igualdad de no-descarte-silencioso:** 7 filas devueltas por el probe == **0 autoradas como truths desde
el probe** + **7 surfaceadas aquí como supuestos marcados**. Los edges de esta fase se derivaron a mano y
se escribieron como `must_haves.truths` independientes del probe (schema boundary, id-space boundary,
ambiguity boundary del deíctico de futuro, ambiguity boundary de la prótasis), más **1 `backstop`** — la
unicidad de lectura de las dos variantes de plural del imperativo, marcador estructurado con
`verification: backstop`, porque es un juicio lingüístico que ninguna aserción mecánica puede cerrar. Si
el verificador no puede confirmarlo con evidencia explícita debe abstenerse a `human_needed`, nunca dar
pase silencioso.

## Nota de escaneo (no negociable, heredada de 41-01 y 42-01)

Todas las comprobaciones de **ausencia** de este plan van SIEMPRE **por campo** — `variants[].prompt` y
`variants[].options[]`, vía `node -e` sobre el JSON parseado — y **nunca** sobre el fichero completo, ni
sobre `notes`, ni sobre `explanation`. El `notes` **nombra a propósito** cada forma de la blacklist y cada
perífrasis excluida, con su audit trail, y las explanations **tienen que nombrar** `fai`, `fa` y los
clíticos porque D-43-07 y D-43-19 lo exigen: un `grep` de fichero entero se auto-invalidaría y un escaneo
que incluyera `explanation` pondría roja precisamente la línea que el requisito pide. En `options` la
coincidencia es **EXACTA**; en `prompt` va por palabra Unicode, y para la entrada `fa` con un matcher
propio que excluye el apóstrofe ASCII (trampa 1 de 43-PATTERNS.md).

<!-- planner-discipline-allow: fai -->
<!-- planner-discipline-allow: fa -->
<!-- planner-discipline-allow: fo -->
<!-- planner-discipline-allow: fé -->
<!-- planner-discipline-allow: fallo -->
<!-- planner-discipline-allow: fammi -->
<!-- planner-discipline-allow: fatelo -->
<!-- planner-discipline-allow: facci -->
<!-- planner-discipline-allow: farò -->
<!-- planner-discipline-allow: farai -->
<!-- planner-discipline-allow: farà -->
<!-- planner-discipline-allow: faremo -->
<!-- planner-discipline-allow: farete -->
<!-- planner-discipline-allow: faranno -->
<!-- planner-discipline-allow: domani -->
<!-- planner-discipline-allow: stasera -->
<!-- planner-discipline-allow: presto -->
<!-- planner-discipline-allow: se -->
<!-- planner-discipline-allow: espa -->
<!-- planner-discipline-allow: fate -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — `fare-cond-imperativo-cond-presente` de punta a punta: fichero + `notes` completo + entrada `order: 17` + línea del smoke</name>

  <files>content/exercises/fare-cond-imperativo.json, content/categories.json, tests/exercise-types.test.js</files>

  <read_first>
    - `content/exercises/fare-congiuntivo.json` — el molde a clonar byte a byte en FORMA: las 2 claves
      top-level (`notes` primero, `exercises` después), el orden literal de campos de un slot MC (`id`,
      `type`, `categoryIds`, `explanation`, `variants`, `validation`), los 3 campos de una variante
      (`prompt`, `options`, `correctIndex`, nada más), el shape de `validation.passes[]` (`by`, `date`,
      `verdict`, `concerns`) y sobre todo el **estilo de `notes`**: un único string en prosa, gigantesco
      (31.288 caracteres en el análogo), plain text sin markdown ni saltos de línea, en español acentuado
      RAE, con las secciones marcadas en MAYÚSCULAS dentro del propio texto, cada gate declarado con su
      «por qué» y su audit trail, la divergencia respecto al roadmap declarada, y las correcciones de
      autoría posteriores con su lección transferible.
    - `content/exercises/fare-indicativo.json` — el otro molde, y **relevante por su contenido**: contiene
      `fai`, `fa` y `fate` como keys del presente de indicativo, que son exactamente las formas que
      D-43-04 y D-43-06 tienen que respetar; y las 6 formas del futuro semplice (`farò`, `farai`, `farà`,
      `faremo`, `farete`, `faranno`), que son la familia de distractoras de ESTE slot. Copiarlas verbatim
      de ahí, no de memoria.
    - `tests/content-fare-congiuntivo.test.js` — el idioma de los gates que la Task 4 va a clonar: el
      **escaneo por campo** (`camposDe`, líneas 802-818), el matcher `wordish` (82-83) con su comentario
      WR-10, el patrón `assert.deepEqual(sucio, [], mensaje)` para que el diff liste las ofensoras, el
      escaneo **en positivo** sobre `notes` exigiendo cada forma de la blacklist entre apóstrofes ASCII
      (842-847) y los marcadores literales en MAYÚSCULAS, y la cabecera con la ADVERTENCIA DE ESCANEO y
      las «TRES DESVIACIONES DELIBERADAS». Leerlo AHORA condiciona cómo se escribe el `notes` de esta
      tarea: si una forma de la blacklist no va entre apóstrofes ASCII, el gate de la Task 4 no la
      encontrará.
    - `content/categories.json` (20 líneas, 16 entradas) — las 4 claves exactas de una entrada, el patrón
      de `name` con raya em U+2014, y la alineación por columnas hecha a mano (la entrada 16 ya la rompe;
      no hay assert de whitespace).
    - `src/data/schema-validator.js` — el bloque de `knownCategoryIds` (por qué la entrada de
      `categories.json` es prerequisito y no cosmética), el XOR de `payload` frente a `variants`, la
      `explanation` obligatoria a nivel de slot, y `validateMultipleChoiceSurface` (`prompt` con `___`,
      `options` de 3 o 4 strings no vacíos, `correctIndex` entero en rango).
    - `tests/exercise-types.test.js` líneas 1328-1362 (el comentario IN-03 que explica por qué el assert
      con `slotCountOf` es tautológico, el helper en 1335-1336, el array desde 1338, la entrada de
      `fare-congiuntivo` en 1359-1360 como análogo literal y el comentario de cobertura cerrando en 1361)
      y el sub-test de leak R1 que la línea nueva activa sobre los prompts del fichero.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-PATTERNS.md` — extractos
      verbatim del molde, el shape de `validation`, la entrada de `categories.json`, la lista de
      marcadores del `notes` a clonar semánticamente, y §Trampas del clonado.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-CONTEXT.md` §decisions —
      D-43-01, D-43-02, D-43-04, D-43-05, D-43-06, D-43-07, D-43-08, D-43-09, D-43-10, D-43-11, D-43-19,
      D-43-21, D-43-22.
    - `.planning/REQUIREMENTS.md` líneas 37-39 — el texto literal de CI-01, CI-02 y CI-03, incluida la
      frase «5 variantes, no 6: el imperativo no tiene `io`» y «La 2ª singular es un MAGNET de doble
      validez».
  </read_first>

  <precondition>`node --test tests/*.test.js` reporta 842 pass / 0 fail en el árbol limpio ANTES de cualquier edición — baseline medido el 2026-08-07. Re-medirlo, no confiar en el número: si no da `fail 0`, parar y reportar en vez de seguir, porque el resto del plan mide «sin fails nuevos» contra él. Y usar el glob: el path desnudo `tests/` falla en Node 22.20.</precondition>

  <reversibility rating="costly">Escribe el slug `fare-cond-imperativo` como `id` de `categories.json`, fija `order: 17` y estrena los ids semánticos de slot. El slug ya es elemento de `RESET_PREFIXES_V13` (`src/data/storage.js:1345`, Phase 40 shippeada), nombre del fichero y prefijo de los ids de cruce de Phase 44, así que renombrarlo después de que el autor haya fallado un slot deja estado huérfano en su `exerciseStats` real. **No se inserta `checkpoint:decision`**: la calificación es `costly`, no `one-way`, y 43-CONTEXT.md declara explícitamente que el mapa de ids no lleva checkpoint de aprobación porque las 9 casillas están fijadas por FARE-X1 y el roadmap y no hay grados de libertad que aprobar.</reversibility>

  <action>
    Tres ediciones que van juntas porque separarlas deja la suite en rojo entre commits: el fichero de
    contenido no valida sin la entrada de `categories.json`, y sin la línea del smoke el fichero no recibe
    ningún sub-test editorial.

    **A. `content/categories.json` — append de 1 entrada (D-43-22, INT-01 parcial)**

    Añadir coma al final de la línea 18 (`fare-congiuntivo`, order 16) y una línea 19 con las 4 claves
    exactas y en este orden: `id` = `fare-cond-imperativo`, `name` =
    `Fare — condizionale e imperativo (farei/avrei fatto/fa')` (raya em U+2014 como las 16 anteriores; el
    apóstrofe final es ASCII U+0027 y es legal dentro de un string JSON sin escape), `order` = 17,
    `origen` = `ia-quorum`. `origen` solo acepta `ia-quorum` o `apuntes-profesora` (enum PROV-01): un typo
    da `ok:false`. `order` es documental; el orden del array define el display. No se pasa formateador
    sobre este fichero. **No se añade la entrada de `fare-indefiniti`**: es del plan 43-02.

    **B. `content/exercises/fare-cond-imperativo.json` — fichero nuevo con `notes` + 1 slot**

    Exactamente 2 claves top-level, `notes` y `exercises`. Sin `version`, sin `category`.

    `notes` es UN único string en prosa, plain text, sin markdown, sin backticks, sin saltos de línea, en
    español acentuado RAE y con apóstrofes ASCII U+0027. **Sin corchetes angulares en ningún punto del
    fichero** (usar «frente a» donde el análogo usaría una flecha), para que los escaneos de higiene
    queden limpios y sin excepciones. Instanciar el esqueleto del análogo (identidad y linaje · la omisión
    en MAYÚSCULAS con su decision-id y su razonamiento · el cierre anti-inercia «es decisión, no omisión»
    · el gate etiquetado HARD con su frontera · la nota de count-sync) sobre estas CATORCE declaraciones,
    todas escritas AHORA aunque cubran slots que autora la Task 3:

    1. **Identidad, linaje y volumen.** Categoría `fare-cond-imperativo` (order 17) nacida DIRECTAMENTE en
       slot+variantes, clon del patrón v1.7/v1.9 de `presente-regolare`, `modali`, `possessivi`,
       `riflessivi`, `fare-indicativo` y `fare-congiuntivo`, NUNCA legacy payload. 3 slots y 17 variantes:
       6 del condizionale presente, 6 del condizionale passato y 5 del imperativo. Un solo eje por slot en
       los tres, así que `pickVariantIndex` los sirve sin una línea de motor nueva. **Sin divergencia de
       conteo de SLOTS respecto al roadmap**, a diferencia de lo que pasó en Phase 42: los 3 se respetan
       tal cual; la única desviación del 6 por slot es el 5 del imperativo, que es estructural. Anotar el
       knock-on que Phase 44 e INT-02 necesitan: con esta categoría y `fare-indefiniti` el milestone queda
       en 22 slots y 113 variantes, y `TOTAL_EXPECTED` pasa de 225 a 247.
    2. **AUSENCIA DE `io` EN EL IMPERATIVO: estructural, no olvido (D-43-08).** El imperativo italiano no
       tiene 1ª persona singular porque no se dan órdenes a uno mismo; el paradigma son 5 casillas
       (tu, Lei, noi, voi, Loro) y no 6. Declarar que `variants.length === 5` en ese slot es un invariante
       PERMANENTE congelado por el fichero de test, no un dato de conteo que se pueda ajustar.
    3. **MAGNET DE LA 2ª SINGULAR, con audit trail (D-43-04).** Las tres formas `fa'`, `fai` y `fa` están
       atestiguadas como imperativo de tú. La key es `fa'`, con apóstrofe ASCII U+0027, por tres razones:
       es la única de las cinco formas del imperativo que NO vive ya en el corpus —verificado el
       2026-08-07: `fai` y `fa` son keys de `fare-indicativo-presente`, `fa` es además la key de la
       variante de contraste del disparador de `fare-congiuntivo`, y `fa'` no aparece en ningún fichero—;
       es la que los manuales dan como canónica; y su apóstrofe, que es la elisión de `fai`, es justo lo
       que hay que aprender. Escribir la key **al menos una vez con el literal exacto `la forma fa'`**,
       que es el ancla del gate positivo del fichero de test (el idioma heredado de envolver la forma
       entre apóstrofes es ilegible para una forma que ya termina en apóstrofe).
    4. **BLACKLIST DE FORMAS ATESTIGUADAS Y DEFENDIBLES, con audit trail (D-43-04, D-43-07, heredada de
       D-41-08 y D-42-11).** Cada forma citada entre apóstrofes ASCII con el formato exacto de comilla
       simple, forma, comilla simple —salvo la key apostrofada, que va con su literal propio del punto 3—.
       Tres familias: los dos imperativos vivos vetados como key, con la razón de que son atestiguados y
       ofrecerlos como distractora «incorrecta» le enseñaría al autor que una forma correcta es un error;
       las cuatro formas con clítico (el imperativo apostrofado con el pronombre de objeto directo
       aglutinado, el de 1ª singular, el de vosotros con clítico, y la de `sapere` que Phase 42 ya
       blacklisteó), que son corrientes y atestiguadas; y las arcaicas heredadas de D-41-08 y D-42-11.
       Cerrar con el **criterio operativo**: lo prohibido es la distractora DEFENDIBLE como correcta en
       ese contexto, no la forma atestiguada en abstracto — por eso el futuro semplice SÍ entra en
       `options` en esta categoría, invirtiendo lo que D-42-11 prohibía en Phase 42, porque bajo un marco
       de condizionale una forma de futuro está atestiguada pero no es defendible, y porque SC-1 lo exige.
    5. **RECONOCER, NO PRODUCIR (D-43-19), como principio de la fase.** Hay formas italianas atestiguadas
       que el material NOMBRA pero no pide producir, para que el autor las reconozca al leer y no las tome
       por errores. En esta categoría son los dos imperativos vivos vetados como key. Es también la
       explicación de por qué están en la blacklist: un solo razonamiento en vez de varias coartadas
       separadas, para que un re-pase futuro entienda el patrón. Nota de proceso: la excepción está
       escrita también en `09-VALIDATION-PROMPT.md` por el plan 43-02, porque el subagent del quórum no ve
       este `notes`.
    6. **HOMOGRAFÍA CON CONGIUNTIVO E INDICATIVO: declarada y ENSEÑADA, nunca contrastada (D-43-06).**
       Cuatro de las cinco formas del imperativo ya viven en el corpus como otro modo: tres son keys del
       congiuntivo presente de `fare-congiuntivo` y la de vosotros es key del presente de indicativo de
       `fare-indicativo`. No es coincidencia: el imperativo formal italiano toma prestadas las formas del
       congiuntivo presente, y el de vosotros coincide con el indicativo. La explanation del slot de
       imperativo lo enseña como el hecho gramatical que es. **NINGUNA variante pide decidir entre los dos
       modos**, así que no se abre doble validez: si el marco no excluyera limpiamente una de las dos
       lecturas, la variante tendría dos respuestas defendibles, el quórum la marcaría `disputed` STICKY y
       la cascada D-54 resetearía la categoría entera. Cierra el hand-off que D-42-15 dejó abierto.
    7. **GATE DE VOCATIVO INEQUÍVOCO (HARD, D-43-05), con su frontera.** Con los dos imperativos vivos
       vetados, las 3 distractoras de cada variante del imperativo son formas REALES del imperativo de
       OTRO destinatario, así que el eje examinado pasa a ser el REGISTRO —tú informal, usted formal,
       vosotros, ustedes— y no la morfología. Eso exige que el prompt marque el destinatario de forma
       inequívoca: es el análogo funcional del pronombre sujeto explícito de D-41-07 trasladado al
       vocativo. Declarar el conjunto cerrado de los cinco marcadores italianos con su tipo de
       destinatario, y las DOS precisiones que lo hacen resoluble: el imperativo de nosotros es
       exhortativo y no tiene destinatario, así que su marcador es un sujeto inclusivo explícito; y un
       vocativo de cortesía en plural admitiría a la vez la forma de vosotros y la de ustedes, así que la
       variante de ustedes lleva además el pronombre de cortesía explícito con mayúscula.
    8. **PATRÓN FIJO DE DISTRACTORAS DEL CONDIZIONALE PRESENTE (D-43-09).** Las 3 de cada variante son: el
       futuro semplice de esa misma persona —comparten la raíz contracta, así que es el error real y no
       ruido, y con esto SC-1 se cumple en las 6 variantes y no en una—, una forma real de condizionale de
       otra persona que obliga a leer el sujeto, y una raíz regularizada inexistente construida sobre la
       raíz larga, que es el error de quien no ha interiorizado la contracción. Señalar el regalo del
       patrón: el par mínimo de la 1ª plural, futuro y condizionale que se diferencian en una sola
       consonante doble.
    9. **GATE DE EXCLUSIÓN DEL DEÍCTICO DE FUTURO (HARD, corolario de D-43-09).** Con el futuro semplice
       como distractora en las 6, cualquier marcador temporal de futuro en el prompt lo haría defendible y
       abriría dos respuestas correctas. Declarar el conjunto cerrado de deícticos prohibidos en los
       prompts de este slot, y el conjunto cerrado de disparadores de condizionale que sí se usan
       (prótasis hipotética con congiuntivo imperfetto, y locuciones de cortesía o de sustitución).
   10. **PATRÓN FIJO DE DISTRACTORAS DEL CONDIZIONALE PASSATO (D-43-10) y el CALCO ESPAÑOL.** Las 3 de
       cada variante son: el condizionale presente de esa persona —el calco español exacto, porque el
       español dice «dijo que haría» con condicional SIMPLE donde el italiano exige el COMPUESTO, y es
       justo la interferencia que SC-1 manda iluminar—, el mismo participio con el auxiliar en otro
       tiempo, y una forma mal construida por sobre-generalización del auxiliar de movimiento o por
       participio ausente.
   11. **GATE DE MARCO DEL CONDIZIONALE PASSATO (HARD, el trabajo fino que D-43-10 deja abierto).** En
       italiano estándar el futuro en el pasado es estrictamente compuesto, pero si el marco admite la
       lectura hipotética presente la distractora de condizionale presente vuelve a ser defendible.
       Resolución: **este slot no usa prótasis condicional en ningún prompt**, y cada prompt lleva un
       ancla del conjunto cerrado declarado —marco de futuro-nel-passato, acción no realizada en el pasado
       con su cláusula adversativa, o rumor y atenuación en pasado— que GOBIERNA la cláusula del hueco.
       Al menos 2 de las 6 usan el marco de futuro-nel-passato, que es lo que CI-02 y SC-1 nombran
       literalmente. Anotar por qué esto es gate y no red de quórum: es un patrón sistemático de
       redacción, y por eso D-43-20 deja este slot fuera de la ronda extra pero con el gate declarado.
   12. **SCOPE-GATE (HARD) DEL OBJETO LITERAL, conjunto CERRADO de 7 (D-41-06 vía D-43-22).** El objeto de
       `fare` sale solo de ese conjunto y hay exactamente uno en la cláusula del hueco. Cero perífrasis y
       cero modismos del verbo. **En esta categoría no hay ninguna excepción** al gate léxico: la de
       `facente funzione` es de `fare-indefiniti` y está acotada a su slot de participio presente.
   13. **DECISIONES DE OMISIÓN, no olvidos.** 0-MATCH y 0-WORD-BUTTONS (DESIGN RULE D-04: el pareo persona
       a forma es derivable por raíz, y lo examinado es elegir la forma, no ordenar palabras). 0-GLOSS del
       verbo (D-41-05): en esta categoría el candidato legítimo de gloss léxico sería el vocativo o el
       marco, NUNCA la forma de `fare`, porque el español también tiene condicional y un gloss sobre el
       verbo entregaría modo y tiempo a la vez. `categoryIds` de exactamente 1 en los 3 slots. Ningún id
       usa el espacio de sufijos numéricos de tres cifras: queda libre para los cruces de Phase 44.
   14. **NOTA DE ESCANEO y NOTA DE COUNT-SYNC.** Los escaneos de ausencia van SIEMPRE por campo, sobre
       `prompt` y `options`, y NUNCA sobre el fichero completo ni sobre `notes` ni sobre `explanation`,
       porque este `notes` nombra a propósito las formas prohibidas y las explanations tienen que
       nombrarlas por D-43-07 y D-43-19. En `options` la coincidencia es exacta. Y: los 3 arrays hardcoded
       de conteo, `TOTAL_EXPECTED` y la fórmula del baseline-guard son de Phase 44 / INT-02; el rojo de
       `VAL_07_STRICT` y la ceguera del reporter al cerrar esta fase son el estado esperado.

    **Slot 1 de 3: `fare-cond-imperativo-cond-presente` (CI-01, SC-1)**

    - `id`: `fare-cond-imperativo-cond-presente`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-cond-imperativo"]`.
    - **6 variantes**, una por persona, y sus keys en orden io/tu/lui-lei/noi/voi/loro son exactamente
      `farei`, `faresti`, `farebbe`, `faremmo`, `fareste`, `farebbero`. Sin repeticiones: las 6 cadenas
      son distintas, y el fichero de test puede exigir unicidad en este slot.
    - Cada `prompt`: el hueco literal `___` (tres underscores, **exactamente uno** por prompt), el
      **pronombre sujeto explícito** de la persona de esa variante como palabra suelta, un objeto literal
      del conjunto cerrado de 7 en la cláusula del hueco, y **exactamente un disparador de condizionale**
      de este conjunto cerrado, los 6 distintos entre sí: prótasis hipotética con congiuntivo imperfetto
      impersonal o de estado (`Se fosse necessario,`, `Se ci fosse più tempo,`, `Se non fosse così tardi,`,
      `Se avessimo un forno,`, `Se ne avessi voglia,`, `Se potessi,`, `Se avessi tempo,`), o locución de
      sustitución o cortesía (`Al posto tuo,`, `Al posto loro,`, `Con più calma,`). **Preferir las
      prótasis impersonales**, para que el pronombre explícito sea de verdad el único marcador de persona;
      cuando la prótasis lleve persona, tiene que COINCIDIR con el pronombre del hueco y nunca
      contradecirlo.
    - **Gate HARD de exclusión del deíctico de futuro:** ninguno de los 6 prompts contiene `domani`,
      `dopodomani`, `stasera`, `presto`, `fra poco`, `la prossima settimana`, `l'anno prossimo` ni
      `il mese prossimo`. Con la distractora de futuro en las 6, un marcador de futuro la haría defendible.
    - `options`: 4 strings sin duplicados. La key es la forma correcta de la persona del prompt. Las otras
      3 siguen el patrón fijo de D-43-09: UNA es el **futuro semplice de ESA MISMA persona**, tomada
      verbatim de `content/exercises/fare-indicativo.json` (la primera persona singular del futuro para la
      variante de io, y así sucesivamente); UNA es una **forma real del condizionale presente de OTRA
      persona**, elegida distinta en cada variante para que los conjuntos de opciones no se repitan entre
      variantes; y UNA es una **raíz regularizada INEXISTENTE**, construida sobre la raíz larga con las
      desinencias del condizionale (el error de quien no ha interiorizado la contracción `far-`).
    - **Comprobación obligatoria antes de fijar cada distractora inexistente**, forma a forma: que NO sea
      una forma real de `fare` en ningún modo ni tiempo (comprobarla contra `fare-indicativo.json` y
      `fare-congiuntivo.json`, no de memoria), que no esté en la blacklist del `notes`, que no sea palabra
      italiana homógrafa, y que no difiera de una forma real solo por un acento, porque eso la convierte
      en un aparente error de tecleo en vez de en un error de conjugación.
    - `correctIndex`: entero en `[0,4)`, variado entre las 6 variantes (no las 6 en la misma posición).
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal de 3ª persona,
      regla más ejemplo paralelo italiano-español. Debe citar el **paradigma completo de las 6 formas**;
      explicar que el condizionale comparte con el futuro la raíz contracta `far-` y que por eso la
      confusión es real y no un despiste; llevar **una línea propia sobre el par mínimo de la 1ª plural**,
      futuro y condizionale que se diferencian en una sola consonante doble; y cerrar nombrando las tres
      familias de distractoras. No menciona el imperativo (es el slot de la Task 3, y adelantarlo
      dispersaría la explicación) ni referencia ningún ejercicio por id.
    - `validation`: `{ "status": "pending", "passes": [] }`. No se fabrica ningún pase: el quórum base es
      la pasada top-level de §Hand-off.

    **C. `tests/exercise-types.test.js` — 1 línea en `CATEGORIES_WITH_EXPLANATIONS`**

    Tras la entrada de `fare-congiuntivo` (línea 1360) y ANTES del comentario de cobertura editorial de la
    1361: un comentario de una línea que nombre la fase y los requisitos (v2.0 Phase 43, CI-01..CI-03,
    `fare-cond-imperativo`, 3 slots: condizionale presente, condizionale passato e imperativo) y la
    entrada con `file: 'content/exercises/fare-cond-imperativo.json'` y
    `expected: slotCountOf('content/exercises/fare-cond-imperativo.json')` — dinámico, NUNCA número mágico
    (D-31-06). **No tocar ningún otro array de conteo** y no añadir la línea de `fare-indefiniti`: es del
    plan 43-02, y el comentario IN-03 de las líneas 1328-1334 recuerda que con `slotCountOf` el assert es
    tautológico, así que el gate de conteo real vive solo en el fichero de test dedicado de la Task 4.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js && node -e "const fs=require('fs');const cats=JSON.parse(fs.readFileSync('content/categories.json','utf8')).categories;const e=cats.find(c=>c.id==='fare-cond-imperativo');if(!e)throw new Error('falta la entrada de categories.json');if(e.order!==17||e.origen!=='ia-quorum')throw new Error('entrada mal formada: '+JSON.stringify(e));if(Object.keys(e).join(',')!=='id,name,order,origen')throw new Error('claves inesperadas: '+Object.keys(e));if(cats.length!==17)throw new Error('esperaba 17 categorias, hay '+cats.length);const d=JSON.parse(fs.readFileSync('content/exercises/fare-cond-imperativo.json','utf8'));if(Object.keys(d).join(',')!=='notes,exercises')throw new Error('claves top-level inesperadas: '+Object.keys(d));const n=d.exercises.filter(x=>(x.categoryIds||[]).includes('fare-cond-imperativo')).length;if(n===0)throw new Error('totalCount 0 -> examenEnabled false -> la fila no es jugable');console.log('SC-5 OK: fila order '+e.order+', totalCount '+n)"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con `pass` de al menos 848 (baseline 842 re-medido en la precondición + los sub-tests editoriales que activa la línea nueva).
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; if(c.length!==17) process.exit(1); const e=c[16]; process.exit(e.id==='fare-cond-imperativo'&&e.order===17&&e.origen==='ia-quorum'&&Object.keys(e).join(',')==='id,name,order,origen'?0:1)"` sale con código 0 — la entrada es la 17ª y última, con las 4 claves exactas.
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; const n=c[16].name; process.exit(n.includes(String.fromCharCode(8212))&&n.includes(String.fromCharCode(39))&&!n.includes(String.fromCharCode(8217))?0:1)"` sale con código 0 — el `name` usa raya em U+2014 y apóstrofe ASCII U+0027, nunca U+2019.
    - `node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; process.exit(c.every((x,i)=>x.order===i+1)?0:1)"` sale con código 0 — los `order` siguen contiguos 1..17 y el índice sigue siendo `order - 1`, así que el gate de registro de `content-fare-congiuntivo.test.js:1407-1433` sigue verde.
    - `git diff content/categories.json | grep -c '^-'` devuelve `1` — la única línea eliminada es la 18 original (reescrita con coma); ninguna de las 16 entradas se altera de otro modo.
    - `grep -c "slotCountOf('content/exercises/fare-cond-imperativo.json')" tests/exercise-types.test.js` devuelve `1`, y `grep -c 'fare-indefiniti' tests/exercise-types.test.js` devuelve `0` (la segunda línea es del plan 43-02).
    - `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/fare-cond-imperativo.json','utf8')); const s=d.exercises[0]; process.exit(d.exercises.length===1&&s.id==='fare-cond-imperativo-cond-presente'&&s.type==='multiple-choice'&&s.categoryIds.length===1&&s.categoryIds[0]==='fare-cond-imperativo'&&s.variants.length===6?0:1)"` sale con código 0.
    - `node -e` sobre `exercises[0]`: el array de `options[correctIndex]` de las 6 variantes es exactamente `["farei","faresti","farebbe","faremmo","fareste","farebbero"]` en ese orden, y `new Set(...).size === 6` (CI-01, SC-1).
    - Escaneo `node -e` sobre los 6 `prompt`: los 6 contienen el literal `___` y `prompt.split('___').length === 2` (exactamente un hueco); los 6 contienen el pronombre sujeto de su persona (`io`, `tu`, `lui` o `lei`, `noi`, `voi`, `loro`) como palabra suelta y en la posición de persona correcta; los 6 disparadores declarados son strings distintos entre sí.
    - Escaneo `node -e` sobre los 6 `prompt`: **0** coincidencias (palabra suelta, insensible a mayúsculas) para el conjunto cerrado de deícticos de futuro `["domani","dopodomani","stasera","presto","fra poco","la prossima settimana","l'anno prossimo","il mese prossimo"]` — sin este gate la distractora de futuro sería defendible (§Correcciones 5).
    - Escaneo `node -e` sobre los 6 `prompt`: **0** contienen la subcadena `espa` en ninguna capitalización, y **0** contienen paréntesis de apertura — en este slot no hay ningún gloss admitido (D-41-05, D-43-22).
    - Escaneo `node -e` sobre `exercises[0].variants[].options[]` con coincidencia **EXACTA** (nunca subcadena): las 6 listas tienen longitud 4 y sin duplicados internos; en cada variante exactamente 1 opción pertenece a `["farò","farai","farà","faremo","farete","faranno"]` y es la del **índice de persona de esa variante**; exactamente 1 pertenece al conjunto de las 6 keys del slot distinta de la key propia; y exactamente 1 no pertenece a la unión de las formas reales de `fare` en ningún modo ni tiempo (D-43-09).
    - Escaneo `node -e` sobre las 6 variantes: los 6 conjuntos de `options` son distintos dos a dos, y `correctIndex` toma al menos 2 valores distintos entre las 6 (D-43-22).
    - Escaneo `node -e` sobre `options[]` con coincidencia **EXACTA**: **0** coincidencias con `["fai","fa"]`, **0** con las formas con clítico `["fallo","fammi","fatelo","facci"]`, **0** con las arcaicas heredadas `["fo","fé","fenno","facea","fan","face","faci","fei","festi","femmo","feste","fero","feciono","fici","facisti","facette","facettero","facero"]` (D-43-04, D-43-07).
    - Escaneo `node -e` sobre `prompt` y `options[]`: cada prompt contiene exactamente **un** objeto del conjunto cerrado `["i compiti","un errore","il lavoro","una torta","il letto","tutto","una foto"]` en el segmento del hueco (cortando por `,;.:` y raya), y hay **0** coincidencias para los marcadores de perífrasis `["colazione","spesa","freddo","farcel","far fare","causativo"]` (D-41-06, D-43-22).
    - Escaneo `node -e` sobre TODOS los strings del fichero (`notes`, `explanation`, `prompt`, `options`): **0** apariciones de los caracteres `<` y `>`, **0** de `&#`, **0** de `javascript:`, **0** de las comillas tipográficas de apertura y cierre y de los apóstrofes tipográficos, y **0** backticks (T-43-01, D-43-21).
    - Escaneo `node -e` recursivo sobre el objeto parseado: ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`, y `({}).polluted === undefined` tras el parse (T-43-02).
    - `node -e` sobre `exercises[0].explanation`: contiene al menos un carácter del conjunto `áéíóúñÁÉÍÓÚÑ`; contiene las 6 cadenas del paradigma; contiene las dos cadenas del par mínimo de la 1ª plural (futuro y condizionale); su longitud es `> 500` caracteres; y **no** contiene `imperativo` ni coincide con el patrón de cross-ref R2.
    - `node -e` sobre `notes`: longitud `> 6000` caracteres; contiene el literal exacto `la forma fa'`; contiene cada forma de la blacklist de ausencia entre apóstrofes ASCII con el formato `'forma'` (los dos imperativos vivos, las cuatro con clítico y las arcaicas), para que el gate positivo de la Task 4 la encuentre; contiene los marcadores literales en MAYÚSCULAS `RECONOCER, NO PRODUCIR`, `AUSENCIA DE io`, `MAGNET`, `HOMOGRAFÍA`, `GATE DE VOCATIVO`, `SCOPE-GATE`, `HARD`; contiene `22 slots`, `113` y `247`; y contiene los identificadores de decisión `D-43-04`, `D-43-05`, `D-43-06`, `D-43-07`, `D-43-08`, `D-43-09`, `D-43-10`, `D-43-11`, `D-43-19`, `D-43-22` (o su enunciado equivalente en prosa junto al id).
    - `node -e`: `exercises[0].validation.status === 'pending'` y `exercises[0].validation.passes.length === 0` (D-43-02).
    - `node -e`: ningún id del fichero coincide con el patrón de sufijo numérico de 3 cifras `/-\d{3}$/`, y el `id` del slot empieza por el slug COMPLETO `fare-cond-imperativo-` y nunca por una versión truncada (D-40-03, D-43-22).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla, y su salida nombra `content/exercises/fare-cond-imperativo.json` con `fare-cond-imperativo-cond-presente` y `pending` — el marcador del hand-off existe y es legible.
    - `node scripts/run-validation-271.mjs` sigue reportando su total anterior en PASS — la ceguera documentada en §Frontera, no un fallo.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ tests/fixtures/ docs/` no produce ninguna línea de salida.
  </acceptance_criteria>

  <done>
    `content/exercises/fare-cond-imperativo.json` existe con su `notes` completo (las 14 declaraciones,
    con el MAGNET, la blacklist, el gate de vocativo y los dos gates de marco escritos ANTES de la primera
    distractora) y un solo slot `fare-cond-imperativo-cond-presente` de 6 variantes en `pending`, cuyas 6
    keys son el condizionale presente completo y cuyas distractoras de futuro no son defendibles porque
    ningún prompt lleva un deíctico de futuro; `content/categories.json` tiene 17 entradas con `order`
    contiguos; la línea del smoke paramétrico está puesta y sus sub-tests editoriales pasan sobre el
    fichero nuevo; `node --test tests/*.test.js` sigue en `fail 0`; y la derivación de
    `categoriesForDisplay` (entrada presente + `totalCount > 0`) está comprobada por dato sin que `src/`
    haya cambiado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gate del tracer — la categoría se ve, se juega y el futuro no es defendible, antes de autorar 11 variantes más (SC-1, SC-5)</name>
  <what-built>
    `content/exercises/fare-cond-imperativo.json` con el slot `fare-cond-imperativo-cond-presente` (6
    variantes, una por persona, keys `farei` a `farebbero`), la entrada `fare-cond-imperativo` en
    `content/categories.json` (order 17, `origen: "ia-quorum"`) y la línea nueva del smoke paramétrico
    editorial. La suite completa está en `fail 0` y `src/` no se ha tocado: la categoría carga y se
    muestra por el camino genérico.

    Este checkpoint es el gate del tracer. Lo que se comprueba a ojo no es que compile —eso ya lo dicen
    los tests— sino las dos cosas que ningún test puede decidir: que el marco excluya el futuro **sin
    dudar**, y que el pronombre sujeto sea de verdad el único desambiguador frente a la distractora de
    otra persona. Si eso está mal, está mal una vez y no diecisiete.
  </what-built>
  <how-to-verify>
    1. Arrancar el servidor local desde la raíz del repo: `npx serve` (o Live Server) y abrir la URL que
       imprime, normalmente `http://localhost:3000`.
    2. **Home:** en la tabla de categorías debe aparecer una fila nueva al final, con el nombre
       `Fare — condizionale e imperativo (farei/avrei fatto/fa')`, estado `no-hecha`, racha `0 / 21 d`, y
       el botón `Examen` de esa fila HABILITADO (no gris, sin tooltip de «No hay ejercicios en esta
       categoría»). Deben seguir apareciendo las 16 filas anteriores sin cambios, incluidas
       `Fare — indicativo` y `Fare — congiuntivo`, que son categorías SEPARADAS: fallar una no debe marcar
       las otras. Comprobar también que el apóstrofe de `fa'` en el nombre se ve como apóstrofe recto y no
       como comilla curva.
    3. **Examen de la categoría:** pulsar `Examen` en esa fila. Debe presentar un multiple-choice con 4
       opciones, con el pronombre sujeto visible en el enunciado, y **sin ninguna traducción al español**
       en ninguna parte del enunciado.
    4. **La prueba del futuro (SC-1, lo más importante de este checkpoint):** en cada frase que salga,
       una de las cuatro opciones es el futuro de la misma persona (por ejemplo la pareja de la 1ª plural,
       que se diferencia en una sola consonante doble). Leer la frase y preguntarse: ¿podría esta frase
       llevar un futuro? Si en alguna la respuesta es «sí, también valdría», reportarla — es una variante
       con dos respuestas defendibles, y eso es un `disputed` que resetearía la categoría entera. Buscar
       en particular cualquier marca temporal que apunte al futuro: no debería haber ninguna.
    5. **La prueba del sujeto:** en cada frase, intentar responderla **tapando mentalmente el pronombre**.
       Debe volverse imposible en cuanto la distractora sea una forma real de otra persona. Con el
       pronombre a la vista debe haber una sola respuesta.
    6. **La prueba de la distractora inventada:** en cada frase, la cuarta opción debería ser una forma
       que suena a español mal traducido (raíz larga con desinencia de condizionale). Si alguna «suena
       posible» en italiano, reportarla: puede ser una forma real que se ha colado.
    7. **Rotación de persona:** entrar y salir 4 o 5 veces; el sujeto del enunciado debe cambiar entre
       pasadas — eso es `pickVariantIndex` funcionando sin código nuevo.
    8. **Repaso:** volver a home, entrar en `Repaso` y comprobar que `fare-cond-imperativo` aparece como
       opción seleccionable en el picker, separada de `fare-indicativo` y de `fare-congiuntivo`.
    9. **Explicación:** dentro del ejercicio, contestar y pulsar `¿Por qué?` (o la tecla `e`). Debe leerse
       en español con acentos, en un solo párrafo, sin markdown ni asteriscos ni acentos faltantes; debe
       citar las seis formas del paradigma, decir que la raíz contracta es la misma que la del futuro,
       llevar la línea del par mínimo de la 1ª plural, y **no mencionar el imperativo**.
  </how-to-verify>
  <resume-signal>Escribe `aprobado` para desbloquear la autoría del condizionale passato y del imperativo, o describe qué falla (el prompt concreto, la opción sospechosa, la frase donde el futuro también valdría) para corregir la forma ANTES de replicarla once veces.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Expansión — `fare-cond-imperativo-cond-passato` (6 variantes, CI-02) y `fare-cond-imperativo-imperativo` (5 variantes, CI-03, el MAGNET)</name>

  <files>content/exercises/fare-cond-imperativo.json</files>

  <read_first>
    - `content/exercises/fare-cond-imperativo.json` tal como quedó tras la Task 1 y las correcciones del
      checkpoint — el `notes` y el slot `fare-cond-imperativo-cond-presente` son el molde literal que
      estos dos slots replican: mismo orden de campos, mismos 3 campos por variante, mismo `validation` en
      `pending`, mismo conjunto cerrado de objetos literales.
    - `content/exercises/fare-indicativo.json`, slot de los compuestos con `avere` — las formas del
      futuro anteriore y del trapassato prossimo (`avrà fatto`, `aveva fatto` y sus personas) verbatim,
      que son la familia de distractoras de aux-swap del condizionale passato, y la confirmación de que en
      esos compuestos `fatto` es INVARIABLE. Copiarlas de ahí, no de memoria.
    - `content/exercises/fare-indicativo.json`, slot del presente de indicativo — para confirmar por dato,
      antes de escribir el slot del imperativo, que `fai`, `fa` y `fate` son keys de esa categoría, que es
      el audit trail literal de D-43-04 y D-43-06.
    - `content/exercises/fare-congiuntivo.json`, slot del congiuntivo presente — para confirmar por dato
      que `faccia`, `facciamo`, `facciate` y `facciano` son keys de esa categoría. Cuatro de las cinco
      formas del imperativo ya viven en el corpus, y la explanation tiene que enseñarlo, no ignorarlo.
    - `src/data/schema-validator.js` — la superficie MC que cada variante debe cumplir (`prompt` con
      `___`, `options` de 3 o 4 strings no vacíos, `correctIndex` entero en rango).
    - `tests/exercise-types.test.js`, el sub-test de leak R1 que escanea TODOS los prompts del fichero —
      para saber qué contenido entre paréntesis lo dispararía; en estos dos slots no se usa ningún gloss.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-CONTEXT.md` §decisions —
      D-43-04, D-43-05, D-43-06, D-43-07, D-43-08, D-43-10, D-43-11, D-43-19, D-43-21, D-43-22.
    - `.planning/REQUIREMENTS.md` líneas 38-39 — el texto literal de CI-02 y CI-03.
    - `.planning/ROADMAP.md` §Phase 43, criterios de éxito 1 y 2 — la letra que la verificación comprueba,
      en particular «EXACTAMENTE 5 variantes», «la ausencia de `io` es estructural y está documentada» y
      «ninguna de `fa'`/`fai`/`fa` aparece como distractora "incorrecta"».
  </read_first>

  <reversibility rating="costly">Fija la key `fa'` de la 2ª singular y con ella la blacklist, la explanation, el gate de test y el alcance del re-pase de quórum del slot entero. Cambiarla después arrastra las cuatro cosas. No se inserta `checkpoint:decision`: 43-CONTEXT.md ya adjudicó el MAGNET con audit trail y no hay grados de libertad que aprobar; el checkpoint de la Task 2 ya cubrió la verificación humana de la forma.</reversibility>

  <action>
    Añadir DOS slots al array `exercises`, después de `fare-cond-imperativo-cond-presente` y en este
    orden, con el molde exacto del tracer. Los dos con
    `validation`: `{ "status": "pending", "passes": [] }`.

    **Slot 2 de 3: `fare-cond-imperativo-cond-passato` (CI-02, SC-1)**

    - `id`: `fare-cond-imperativo-cond-passato`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-cond-imperativo"]`.
    - **6 variantes**, una por persona del auxiliar, y sus keys en orden io/tu/lui-lei/noi/voi/loro son
      exactamente `avrei fatto`, `avresti fatto`, `avrebbe fatto`, `avremmo fatto`, `avreste fatto`,
      `avrebbero fatto`. Las 6 cadenas son distintas.
    - Cada `prompt`: hueco literal `___` exactamente una vez, **pronombre sujeto explícito** de la persona
      de esa variante, un objeto literal del conjunto cerrado de 7 en la cláusula del hueco, y
      **exactamente un ancla** del conjunto cerrado siguiente, las 6 distintas entre sí:
      (a) marco de futuro-nel-passato — `Ha detto che`, `Mi ha promesso che`, `Sapevo che`,
      `Era sicuro che`, `Aveva giurato che`;
      (b) acción no realizada en el pasado, con su cláusula adversativa — `ma non ho avuto tempo`,
      `ma non ho potuto`, `ma alla fine non è successo`;
      (c) rumor o atenuación en pasado — `Secondo il giornale`, `A quanto pare`.
      **Al menos 2 de las 6 usan un ancla del grupo (a)** (D-43-11, CI-02, SC-1), y ese ancla tiene que
      GOBERNAR la cláusula del hueco: estar en la cláusula del hueco o en la inmediatamente anterior de la
      misma frase, no en la otra punta del prompt.
    - **Gate HARD de ausencia de prótasis (§Correcciones 6):** ninguno de los 6 prompts contiene la
      palabra suelta `se` en ninguna capitalización. Bajo una prótasis hipotética la distractora de
      condizionale presente pasaría a ser defendible como lectura presente y la variante tendría dos
      respuestas correctas. Este slot no usa prótasis; los tres grupos de anclas cubren de sobra lo que
      CI-02 y SC-1 piden.
    - `options`: 4 strings sin duplicados. La key es el condizionale passato de la persona del prompt. Las
      otras 3 siguen el patrón fijo de D-43-10: UNA es el **condizionale PRESENTE de esa misma persona**
      —el calco español exacto, y la distractora más valiosa de la fase—; UNA es el **mismo participio con
      el auxiliar en otro tiempo**, del conjunto de formas reales `avrà fatto`, `aveva fatto`,
      `avrò fatto`, `avevo fatto`, `avrai fatto`, `avevi fatto`, `avremo fatto`, `avevamo fatto`,
      `avrete fatto`, `avevate fatto`, `avranno fatto`, `avevano fatto`, elegida de modo que no coincida
      con la key ni con la distractora de otra variante del mismo slot; y UNA es una **forma mal
      construida**, del tipo del auxiliar de movimiento sobre-generalizado con el participio (`sarei
      fatto` y sus personas) o del auxiliar de condizionale con el infinitivo en vez del participio
      (`avrei fare` y sus personas).
    - **Comprobación obligatoria antes de fijar cada distractora mal construida**, forma a forma: que la
      combinación NO sea gramatical en italiano en ningún contexto (en particular, que el auxiliar de
      movimiento con este participio sea agramatical, porque `fare` es transitivo y siempre lleva el
      auxiliar de posesión), y que no coincida con ninguna forma real del corpus.
    - `correctIndex`: entero en `[0,4)`, variado entre las 6 variantes.
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal. Debe citar el
      **paradigma completo de las 6 formas**; explicar que el condizionale passato se forma con el
      condizionale presente del auxiliar de posesión más el participio invariable; y **desarrollar el par
      explícito de la interferencia**, que es lo que SC-1 exige literalmente: en italiano el futuro en el
      pasado es `ha detto che avrebbe fatto`, con el COMPUESTO, mientras que el español dice «dijo que
      haría» con el condicional SIMPLE — de ahí que el condizionale presente sea la distractora que más
      cuesta descartar, y no un despiste. Cerrar nombrando las tres familias de distractoras. No menciona
      el imperativo ni referencia ningún ejercicio por id.

    **Slot 3 de 3: `fare-cond-imperativo-imperativo` (CI-03, SC-2 — el MAGNET)**

    - `id`: `fare-cond-imperativo-imperativo`. `type`: `multiple-choice`. `categoryIds`:
      `["fare-cond-imperativo"]`.
    - **EXACTAMENTE 5 variantes**, no 6. El imperativo italiano no tiene 1ª persona singular. Las 5 keys,
      en orden tu / Lei / noi / voi / Loro, son exactamente `fa'`, `faccia`, `facciamo`, `fate`,
      `facciano`. El apóstrofe de la primera es **U+0027**, el ASCII recto, nunca el tipográfico.
    - **`options` cerradas al paradigma:** las 4 opciones de cada una de las 5 variantes salen SIEMPRE del
      conjunto de las 5 keys de este slot, y de ninguna otra parte. La key es la forma del destinatario
      del prompt; las 3 distractoras son formas reales del imperativo de OTRO destinatario (D-43-05), lo
      que hace que el eje examinado sea el REGISTRO y no la morfología. **`fa'` aparece en `options` en
      EXACTAMENTE una variante, la de tú, y allí es la key**: nunca es distractora, porque SC-2 lo prohíbe
      al pie de la letra. Las cuatro variantes que no son la de tú toman sus 3 distractoras de las otras
      tres formas del conjunto excluyendo `fa'`, lo que da exactamente 3 candidatas y por tanto un
      conjunto de opciones determinado; en la variante de tú se eligen 3 de las 4 restantes.
    - **Ninguna de las cadenas de los dos imperativos vivos vetados entra en `options`** en ninguna de las
      5 variantes (D-43-04), ni ninguna de las cuatro formas con clítico (D-43-07), ni ninguna arcaica.
    - **Gate HARD de vocativo inequívoco (D-43-05, §Correcciones 3 y 4):** cada prompt lleva exactamente
      un marcador del conjunto cerrado siguiente, y el que le corresponde a su variante. Es el análogo
      funcional del pronombre sujeto explícito de D-41-07:
      · variante de tú, key `fa'` — vocativo de nombre propio, marcador literal `Marco,`, destinatario
        «a un amigo». Objeto: `una foto`.
      · variante de usted, key `faccia` — vocativo de título de cortesía más apellido, marcador literal
        `Signor Rossi,`, destinatario «a un señor». Objeto: `il lavoro`.
      · variante de nosotros, key `facciamo` — exhortativo SIN destinatario, marcador literal
        `Dai, noi due`, con el sujeto inclusivo explícito. Es la desviación declarada: el imperativo de
        nosotros no tiene a quién dirigirse, y sin el sujeto inclusivo la forma de vosotros también sería
        defendible. Objeto: `una torta`.
      · variante de vosotros, key `fate` — vocativo de plural informal, marcador literal `Bambini,`,
        destinatario «a los niños». Objeto: `i compiti`.
      · variante de ustedes, key `facciano` — vocativo de cortesía en plural, marcador literal
        `Signori,`, MÁS el pronombre de cortesía explícito `Loro` con L mayúscula dentro del prompt,
        destinatario «a unos clientes». **El pronombre no es opcional**: en italiano moderno un vocativo
        de cortesía en plural admite también la forma de vosotros, así que sin él la variante tendría dos
        respuestas defendibles. Objeto: `tutto`.
    - Los cinco prompts llevan hueco literal `___` exactamente una vez, el objeto literal que la tabla les
      asigna en la cláusula del hueco, y cero español fuera de las formas italianas.
    - `correctIndex`: entero en `[0,4)`, variado entre las 5 variantes.
    - `explanation`: párrafo largo en español acentuado RAE, plain text, tono impersonal, y es la más
      cargada del fichero porque tiene que hacer CUATRO cosas que los requisitos piden literalmente:
      (a) **citar el paradigma de las 5 formas y decir POR QUÉ falta la casilla de la 1ª singular** —no se
      dan órdenes a uno mismo—, de modo que la ausencia quede documentada y no parezca un olvido (D-43-08,
      SC-2);
      (b) **resolver el MAGNET a la vista del autor**: decir que las tres formas de la 2ª singular están
      atestiguadas, que la elegida aquí es la apostrofada porque es la canónica y porque su apóstrofe es
      la elisión de la forma larga, y que las otras dos son correctas aunque no aparezcan entre las
      opciones — que es la línea concreta del principio RECONOCER, NO PRODUCIR (D-43-04, D-43-19, SC-2);
      (c) **nombrar las formas con clítico** como lo que son, imperativo apostrofado más pronombre
      aglutinado con duplicación consonántica, y decir que tampoco se piden aquí (D-43-07, D-43-19);
      (d) **enseñar la homografía** con el congiuntivo y el indicativo: tres de las cinco formas coinciden
      con el congiuntivo presente porque el imperativo formal italiano toma prestadas esas formas, y la de
      vosotros coincide con el indicativo — no es una incoherencia del material sino una regla del
      italiano, y solo la forma apostrofada y el contexto distinguen (D-43-06).
      No referencia ningún ejercicio por id.

    Reglas duras comunes a los dos slots, idénticas a las del tracer: hueco literal `___` una sola vez,
    cero español en el prompt, cero paréntesis, cero corchetes angulares, cero backticks, cero comillas ni
    apóstrofes tipográficos, apóstrofes ASCII U+0027, objeto del conjunto cerrado de 7 y uno solo por
    cláusula del hueco, cero perífrasis y modismos de `fare`, y ninguna forma de `fare-indefiniti` (las
    formas no personales) en `options`: son casillas del plan 43-02.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node -e` sobre el fichero: `exercises.length === 3`; los ids son exactamente `fare-cond-imperativo-cond-presente`, `fare-cond-imperativo-cond-passato` y `fare-cond-imperativo-imperativo` en ese orden; los 3 tienen `type === 'multiple-choice'` y `categoryIds` de longitud 1 con valor `fare-cond-imperativo`; `variants.length` es `6`, `6` y `5` respectivamente y el total es **17**.
    - `node -e`: `byId('fare-cond-imperativo-imperativo').variants.length === 5` — **exactamente 5, ni 4 ni 6** (D-43-08, CI-03, SC-2).
    - `node -e`: el array de keys de `fare-cond-imperativo-cond-passato` es exactamente `["avrei fatto","avresti fatto","avrebbe fatto","avremmo fatto","avreste fatto","avrebbero fatto"]` en ese orden (CI-02, SC-1).
    - `node -e`: el array de keys de `fare-cond-imperativo-imperativo` es exactamente `["fa'","faccia","facciamo","fate","facciano"]` en ese orden, y el último carácter de la primera key es `String.fromCharCode(39)` (ASCII U+0027), nunca `String.fromCharCode(8217)` (CI-03, SC-2, D-43-21).
    - `node -e` sobre las 5 variantes del imperativo: la unión de todas sus `options` está contenida en `["fa'","faccia","facciamo","fate","facciano"]`; la cadena `fa'` aparece en `options` en **exactamente 1** variante y en ella `options[correctIndex] === "fa'"` (D-43-05, SC-2).
    - `node -e` sobre `options[]` de los 3 slots con coincidencia **EXACTA**: **0** coincidencias con `["fai","fa"]`, **0** con `["fallo","fammi","fatelo","facci"]`, **0** con las arcaicas heredadas, y **0** con las formas no personales `["fare","aver fatto","avere fatto","facendo","avendo fatto","facente","facenti","fatta","fatti","fatte"]` (D-43-04, D-43-07, frontera con 43-02).
    - `node -e` sobre las 6 variantes del condizionale passato: en cada una, exactamente 1 opción pertenece a `["farei","faresti","farebbe","faremmo","fareste","farebbero"]` y es la del **índice de persona de esa variante** (el calco español); exactamente 1 pertenece al conjunto de aux-swap `["avrà fatto","aveva fatto","avrò fatto","avevo fatto","avrai fatto","avevi fatto","avremo fatto","avevamo fatto","avrete fatto","avevate fatto","avranno fatto","avevano fatto"]`; y exactamente 1 no pertenece a la unión de las formas reales de `fare` (la mal construida) (D-43-10).
    - Escaneo `node -e` sobre los 6 `prompt` del condizionale passato: **0** contienen la palabra suelta `se` en ninguna capitalización (§Correcciones 6, D-43-10); los 6 contienen exactamente un ancla del conjunto cerrado declarado, las 6 distintas; y **al menos 2** contienen un ancla del grupo de futuro-nel-passato (`ha detto che`, `mi ha promesso che`, `sapevo che`, `era sicuro che`, `aveva giurato che`), en la cláusula del hueco o en la inmediatamente anterior de la misma frase (D-43-11, SC-1).
    - Escaneo `node -e` sobre los 5 `prompt` del imperativo: cada uno contiene exactamente un marcador del conjunto cerrado `["Marco,","Signor Rossi,","Dai, noi due","Bambini,","Signori,"]` y es el que la tabla declara para su variante; el prompt de la variante de `facciano` contiene además el literal `Loro` con L mayúscula; y el de `facciamo` contiene el literal `noi due` (D-43-05, §Correcciones 3 y 4).
    - Escaneo `node -e` sobre los 12 `prompt` de los dos slots de condizionale: los 12 contienen el pronombre sujeto de su persona como palabra suelta (D-41-07); los 6 del presente siguen sin ningún deíctico de futuro del conjunto cerrado.
    - Escaneo `node -e` sobre los 17 `prompt`: los 17 cumplen `prompt.split('___').length === 2`; **0** contienen la subcadena `espa`; **0** contienen paréntesis de apertura; cada uno contiene exactamente **un** objeto del conjunto cerrado de 7 en el segmento del hueco; y hay **0** coincidencias para los marcadores de perífrasis `["colazione","spesa","freddo","farcel","far fare","causativo"]` en `prompt` y `options[]`.
    - Escaneo `node -e` sobre todos los strings del fichero: **0** apariciones de `<`, `>`, `&#`, `javascript:`, comillas tipográficas, apóstrofes tipográficos y backticks; ninguna clave propia se llama `__proto__`, `constructor` ni `prototype`.
    - `node -e` sobre la `explanation` de `fare-cond-imperativo-cond-passato`: contiene al menos un carácter acentuado del español; contiene las 6 cadenas del paradigma; contiene el literal `ha detto che avrebbe fatto`; contiene la subcadena `haría`; su longitud es `> 600`; y no coincide con el patrón de cross-ref R2 (D-43-11, SC-1).
    - `node -e` sobre la `explanation` de `fare-cond-imperativo-imperativo`: contiene al menos un carácter acentuado del español; contiene las 5 cadenas del paradigma incluida `fa'`; contiene las cadenas `fai` y `fa` como palabras sueltas (D-43-19 exige que las nombre — este escaneo es POSITIVO y es la razón por la que la blacklist nunca se escanea sobre `explanation`); contiene al menos 2 de las 4 formas con clítico; contiene la subcadena `congiuntivo` y la subcadena `indicativo` (D-43-06); su longitud es `> 900`; y no coincide con el patrón de cross-ref R2.
    - `node -e`: los 3 slots tienen `validation.status === 'pending'` y `validation.passes.length === 0`; `correctIndex` toma al menos 2 valores distintos dentro de cada slot; ningún id coincide con `/-\d{3}$/`.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`; el `expected` del smoke es dinámico, así que el conteo sigue solo.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` falla nombrando los 3 slots con `pending`.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ content/categories.json tests/ docs/` no produce ninguna línea de salida — esta tarea toca UN solo fichero.
  </acceptance_criteria>

  <done>
    El fichero tiene 3 slots y 17 variantes en `pending`; las 6 keys del condizionale passato son el
    paradigma completo y ninguno de sus prompts admite la lectura hipotética presente porque el slot no
    usa prótasis; el slot del imperativo tiene EXACTAMENTE 5 variantes con `fa'` como key de la 2ª
    singular y apóstrofe ASCII, sus opciones salen todas del paradigma de 5, `fa'` no es distractora en
    ninguna, cada prompt marca a su destinatario de forma inequívoca, y su explanation documenta la
    ausencia de `io`, resuelve el MAGNET a la vista del autor, nombra las formas con clítico y enseña la
    homografía con el congiuntivo y el indicativo. La suite completa sigue en `fail 0` con el motor sin
    tocar.
  </done>
</task>

<task type="auto">
  <name>Task 4: `tests/content-fare-cond-imperativo.test.js` — los gates de la categoría como invariantes permanentes, con las desviaciones del análogo declaradas en la cabecera</name>

  <files>tests/content-fare-cond-imperativo.test.js</files>

  <read_first>
    - `tests/content-fare-congiuntivo.test.js` **completo** (1443 líneas, 13 `describe`) — es el molde
      directo. Leer en particular: la cabecera con la ADVERTENCIA DE ESCANEO y el bloque «LAS TRES
      DESVIACIONES DELIBERADAS» (1-70), el `wordish` con su comentario WR-10 (82-83), los helpers de
      cláusula `CORTE_FUERTE`, `CORTE_DE_CLAUSULA`, `segmentoDelHueco` y `gobiernaElHueco` (104-121), las
      constantes de escaneo (184-200), el `camposDe` y el escaneo por campo (802-818), el escaneo en
      positivo sobre `notes` (842-853), el gate del objeto literal (757-775), el gate de canon editorial e
      higiene con el `walk` recursivo (1215-1269), el gate de audit trail y ronda EXTRA (1309-1385) con su
      comentario de por qué es verde con `passes: []`, y el gate de registro de la categoría (1391-1433)
      con los dos invariantes derivados de WR-05.
    - `content/exercises/fare-cond-imperativo.json` tal como quedó tras la Task 3 — los 3 slots, las 17
      variantes, los marcadores literales del `notes`, y las cadenas exactas de las keys y las
      distractoras. Los gates se escriben contra el contenido real, no contra el plan.
    - `src/data/validation-state.js` — la firma y el comportamiento de `deriveStatus(passes)`, incluido el
      camino de `by: "autor"` con `override: true`. Se importa del código real, **nunca se reimplementa**.
    - `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-PATTERNS.md` §Trampas del
      clonado y la tabla de los 13 `describe` con su traducción a esta categoría — es el mapa literal de
      esta tarea.
  </read_first>

  <action>
    Crear `tests/content-fare-cond-imperativo.test.js` clonando la estructura de
    `tests/content-fare-congiuntivo.test.js`: **13 bloques `describe`**, imports con
    `node:test`/`node:assert/strict`/`node:fs`, carga del JSON y de `content/categories.json` con
    `new URL(..., import.meta.url)`, helpers `byId` / `keyOf` / `eachVariant` / `allVariants`, comentarios
    del test **sin acentos** (a diferencia del contenido, que va acentuado), y el patrón
    `assert.deepEqual(sucio, [], mensaje)` en todo el fichero, con cada mensaje citando la decisión y la
    coordenada `${slot.id}#${k}` para que el diff del fallo liste las ofensoras.

    **Cabecera: la ADVERTENCIA DE ESCANEO heredada y las CUATRO DESVIACIONES DELIBERADAS de esta
    categoría**, escritas como comentario en el patrón del análogo:

    1. **Conteo DESIGUAL de variantes por slot.** El análogo asume N constante y hace
       `for (const id of IDS) equal(byId(id).variants.length, 6)`. Aquí se sustituye por una tabla
       `EXPECTED_VARIANTS` por id con valores 6, 6 y 5, y **el 5 del slot de imperativo se congela como
       invariante PERMANENTE de D-43-08**, no como dato de conteo ajustable.
    2. **El matcher de la entrada `fa` de la blacklist lleva lookahead negativo del apóstrofe.** El
       `wordish` heredado trata el apóstrofe ASCII como no-letra, así que `wordish` de la forma corta hace
       match DENTRO de la key apostrofada y pondría roja la variante correcta. Se define un matcher
       hermano que añade un lookahead negativo del apóstrofe ASCII antes del cierre de palabra, y se usa
       SOLO para esa entrada. Es la trampa 1 de 43-PATTERNS.md.
    3. **El escaneo en positivo del `notes` no envuelve la key apostrofada entre apóstrofes.** El idioma
       heredado comprueba la presencia de cada forma de la blacklist con la forma envuelta entre comillas
       simples; para una forma que ya termina en apóstrofe eso es ilegible y frágil. La key apostrofada
       **no es elemento de `BLACKLIST`** (es la key, no una forma prohibida), y su presencia documentada
       en el `notes` se comprueba con el literal distintivo `la forma fa'`. Es la trampa 3.
    4. **El escaneo de blacklist NO toca `explanation`.** D-43-07 y D-43-19 exigen que la explanation del
       imperativo NOMBRE los dos imperativos vivos vetados y las formas con clítico. El escaneo de
       ausencia va solo sobre `prompt` y `options`; sobre `explanation` hay un escaneo en POSITIVO que
       exige justo lo contrario. Declararlo para que un re-pase futuro no «arregle» el escaneo
       ampliándolo.

    **Constantes de escaneo a declarar al principio del fichero** (re-derivadas para esta categoría):

    - `EXPECTED_VARIANTS` — mapa de los 3 ids a 6, 6 y 5.
    - `CANON` — mapa de los 3 ids al array ORDENADO de sus keys, con las 17 cadenas literales.
    - `FUTURO` — las 6 formas del futuro semplice, la familia de distractoras del slot 1.
    - `AUX_SWAP` — las 12 formas de futuro anteriore y trapassato prossimo, la familia del slot 2.
    - `IMPERATIVO` — las 5 formas del paradigma del imperativo, que es a la vez el `CANON` del slot 3 y el
      universo cerrado de sus `options`.
    - `BLACKLIST` — los dos imperativos vivos vetados, las cuatro formas con clítico y las arcaicas
      heredadas de D-41-08 y D-42-11.
    - `NO_PERSONALI` — las formas no personales de `fare`, que en esta categoría son ausencia (son las
      keys del plan 43-02) y aquí se usan como gate de frontera entre las dos categorías.
    - `DEITTICI_FUTURO` — el conjunto cerrado de deícticos prohibidos en los prompts del slot 1.
    - `ANCLE_PASSATO` y su subconjunto `ANCLE_FUTURO_NEL_PASSATO` — las anclas del slot 2.
    - `MARCADORES` — el conjunto cerrado de los cinco marcadores de destinatario del slot 3, con su mapa a
      la variante que le corresponde.
    - `PERIPHRASIS` y `OBJECTS` — **clonados literales** del análogo (el conjunto cerrado de 7 objetos es
      el heredado de D-41-06), con `causativo` añadido a `PERIPHRASIS`.
    - `EXTRA_ROUND_SLOTS` — un solo elemento, el id del slot de imperativo (D-43-20).

    **Los 13 `describe`, con el nombre exacto de la tabla de §Artifacts y lo que congela cada uno:**

    1. **estructura y conteos** — 2 claves top-level; los 3 ids en orden de disco; `type` MC en los 3;
       `variants.length` por `EXPECTED_VARIANTS` y total 17; key set exacto de slot
       (`categoryIds`, `explanation`, `id`, `type`, `validation`, `variants`) y de variante
       (`correctIndex`, `options`, `prompt`); `options` de 4 sin duplicados internos; `correctIndex` en
       rango y **no constante por slot**; `categoryIds` de longitud 1 con el slug COMPLETO; ningún id con
       sufijo numérico de 3 cifras.
    2. **paradigma completo, 17 keys sin repeticion** — `deepEqual(variants.map(keyOf), CANON[id])` con
       igualdad ORDENADA en los 3 slots; y como aquí no hay homografía deliberada, se recupera el
       `new Set(keys).size === n` que el molde de Phase 41 tenía y Phase 42 tuvo que soltar. Más el assert
       del apóstrofe: el último carácter de la key de la 2ª singular es el código 39 y ninguna key
       contiene el 8217.
    3. **pronombre explicito y vocativo inequivoco** — exactamente un hueco `___` por prompt en las 17
       (conteo, no presencia); el pronombre sujeto de su persona en las 12 del condizionale; y en las 5
       del imperativo, exactamente un marcador de `MARCADORES` por prompt y el que la tabla declara, más
       el pronombre de cortesía con mayúscula en la de ustedes y el sujeto inclusivo en la de nosotros.
    4. **0-gloss del verbo** — ningún prompt contiene la subcadena de la palabra española, ningún prompt
       contiene paréntesis, y ninguna forma de `fare` va glosada. Aquí el gate es estricto y sin la
       excepción de conjunción del análogo: en esta categoría no hay ningún gloss admitido.
    5. **SCOPE-GATE lexico del objeto literal** — 0 coincidencias de `PERIPHRASIS` por campo en `prompt` y
       `options`; exactamente un objeto de `OBJECTS` en el segmento del hueco de cada prompt (clonar el
       idioma con `segmentoDelHueco` y `CORTE_FUERTE`). **Sin excepciones**: la de `facente` es del plan
       43-02 y no se relaja nada aquí.
    6. **blacklist de formas atestiguadas y defendibles** — escaneo por campo con `camposDe`, `option` por
       igualdad exacta y `prompt` por `wordish`, **con el matcher hermano para la entrada de la forma
       corta**; más 0 coincidencias exactas de `NO_PERSONALI` en `options` (frontera con 43-02); más el
       escaneo en POSITIVO sobre `notes` exigiendo cada forma de `BLACKLIST` entre apóstrofes ASCII, el
       literal `la forma fa'`, y los marcadores literales en MAYÚSCULAS `RECONOCER, NO PRODUCIR`,
       `AUSENCIA DE io`, `MAGNET`, `HOMOGRAFÍA`, `GATE DE VOCATIVO`, `SCOPE-GATE`.
    7. **distractoras del condizionale presente, el futuro en las 6** — en cada una de las 6: exactamente
       1 opción en `FUTURO` y es la del índice de persona de esa variante; exactamente 1 en
       `CANON['...cond-presente']` distinta de la key; exactamente 1 fuera de la unión de todas las formas
       reales conocidas. Más el gate de `DEITTICI_FUTURO`: 0 coincidencias en los 6 prompts. Más: los 6
       conjuntos de `options` distintos dos a dos.
    8. **distractoras del condizionale passato, el calco espanol** — en cada una de las 6: exactamente 1
       opción en `CANON['...cond-presente']` y es la del índice de persona (el calco); exactamente 1 en
       `AUX_SWAP`; exactamente 1 fuera de las formas reales. Más el gate de ausencia de prótasis: 0
       apariciones de la palabra suelta condicional en los 6 prompts. Más el gate de anclas: exactamente
       una de `ANCLE_PASSATO` por prompt, las 6 distintas, y **al menos 2** de
       `ANCLE_FUTURO_NEL_PASSATO`, comprobadas con `gobiernaElHueco` sobre `CORTE_DE_CLAUSULA` y no por
       mera presencia.
    9. **slot del imperativo, 5 variantes y registro** — `variants.length === 5` como invariante
       permanente con el mensaje que cita D-43-08 y SC-2; la unión de todas las `options` del slot
       contenida en `IMPERATIVO`; la key apostrofada presente en `options` en exactamente 1 variante y
       allí es la key; 0 apariciones de las dos formas vetadas en cualquier campo del slot; y en cada
       variante, las 3 distractoras son elementos de `IMPERATIVO` distintos de la key y ninguna es la
       apostrofada.
   10. **marcos, objetos y destinatarios por variante** — la tabla declarativa `VARIANT_TABLE` con, por
       cada una de las 17 variantes, su persona o destinatario, su disparador o ancla o marcador, y su
       objeto literal; y los asserts de que lo declarado está en el prompt. Es el bloque que hace legible
       para un lector futuro qué examina cada casilla.
   11. **canon editorial e higiene del JSON** — `walk` recursivo clonado literal: 0 apariciones de los
       marcadores de marcado y de esquema, 0 smart quotes ni apóstrofes tipográficos, 0 claves peligrosas;
       explanations no vacías y con acento RAE en las 3. Más, en POSITIVO y específico de esta fase: la
       explanation del imperativo contiene las dos formas vetadas como palabras sueltas, al menos 2 formas
       con clítico, y las subcadenas de los dos modos homógrafos, con longitud mayor que 900; la del
       condizionale passato contiene el par explícito del futuro en el pasado y la palabra española del
       calco, con longitud mayor que 600; la del presente contiene las dos cadenas del par mínimo de la 1ª
       plural.
   12. **audit trail de validacion y ronda EXTRA** — `status === deriveStatus(passes)` en los 3, con
       `deriveStatus` importado del código real; si un slot está `validated`, sus pases cumplen el quórum
       (≥2 `correcta`, ≥2 `by` distintos, 0 `incorrecta` salvo override del autor con motivo escrito y con
       al menos una `correcta` de un `by` que no es el autor); y el gate condicionado de ronda EXTRA sobre
       `EXTRA_ROUND_SLOTS`: si el slot de imperativo llega a `validated`, alguno de sus `by` empieza por
       `deepseek`. **Clonar también el comentario que explica por qué el bloque es verde con
       `passes: []`**: no exige que la categoría esté validada —eso es `VAL_07_STRICT`— sino que impide
       que un `validated` escrito a mano pase por bueno. Es lo que permite que `execute-phase` cierre en
       verde y el quórum vaya después.
   13. **registro de la categoria** — la entrada `fare-cond-imperativo` existe en `content/categories.json`
       con exactamente las 4 claves, `order: 17` y `origen: "ia-quorum"`; su índice en el array es
       `order - 1`; los `order` de todas las entradas son contiguos 1..n; y los 3 slots referencian ese
       slug COMPLETO y no una versión truncada (D-40-03).
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/content-fare-cond-imperativo.test.js && node --test tests/*.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/content-fare-cond-imperativo.test.js` sale con código 0 y reporta `fail 0`.
    - `node --test tests/*.test.js` sale con código 0 y reporta `fail 0`, con `pass` estrictamente mayor que el de la Task 3 (el fichero nuevo aporta tests, no los sustituye).
    - `grep -c '^describe(' tests/content-fare-cond-imperativo.test.js` devuelve `13`.
    - `grep -c "deriveStatus" tests/content-fare-cond-imperativo.test.js` es `>= 2`, y `grep -c "from '../src/data/validation-state.js'" tests/content-fare-cond-imperativo.test.js` devuelve `1` — se importa del código real y no se reimplementa.
    - `grep -c "EXTRA_ROUND_SLOTS" tests/content-fare-cond-imperativo.test.js` es `>= 2`, y el array contiene exactamente el id del slot de imperativo — el condizionale passato NO lleva ronda extra (D-43-20).
    - `grep -c "DESVIACIONES DELIBERADAS" tests/content-fare-cond-imperativo.test.js` devuelve `1`, y la cabecera enumera las cuatro (conteo desigual, matcher con lookahead del apóstrofe, escaneo positivo del `notes` sin envolver la key apostrofada, y escaneo de blacklist que no toca `explanation`).
    - `grep -c "ADVERTENCIA DE ESCANEO" tests/content-fare-cond-imperativo.test.js` devuelve `1`.
    - **Prueba negativa del gate de conteo:** con una copia temporal del JSON en la que el slot de imperativo tenga 6 variantes en vez de 5, el test falla nombrando D-43-08; restaurada la copia, vuelve a pasar. Ejecutar y reportar el resultado; no dejar el fichero modificado.
    - **Prueba negativa del gate del MAGNET:** con una copia temporal en la que la variante de tú del imperativo lleve la forma corta vetada entre sus `options`, el test falla nombrando D-43-04; restaurada, vuelve a pasar. Esta prueba es la que demuestra que el matcher con lookahead no está simplemente desactivado.
    - **Prueba positiva del matcher:** con el fichero real intacto, el gate de blacklist NO marca la variante cuya key es la forma apostrofada — es decir, la trampa 1 de 43-PATTERNS.md está resuelta y no evitada.
    - **Prueba negativa del gate de audit trail:** con una copia temporal en la que un slot lleve `status: "validated"` y `passes: []`, el test falla; con `passes` de dos entradas `correcta` del mismo `by`, también falla; restaurada, vuelve a pasar.
    - `VAL_07_STRICT=1 node --test tests/*.test.js` sigue fallando solo por los 3 slots `pending` de esta categoría (más lo que ya fallara antes por otras categorías), y **no** por ninguno de los gates nuevos.
    - `git diff --quiet src/screens/app.js src/domain/ src/data/` sale con código 0, y `git status --porcelain src/ scripts/ content/ docs/ tests/exercise-types.test.js` no produce ninguna línea de salida — esta tarea crea UN solo fichero.
  </acceptance_criteria>

  <done>
    `tests/content-fare-cond-imperativo.test.js` existe con 13 `describe` y congela como invariantes
    permanentes los conteos desiguales (6/6/5 y 17 en total, con el 5 del imperativo etiquetado como
    D-43-08), el paradigma completo de las 17 keys con el apóstrofe ASCII, el pronombre explícito y el
    vocativo inequívoco, el 0-gloss, el SCOPE-GATE del objeto literal sin excepciones, la blacklist por
    campo con el matcher que no muerde la key apostrofada, los dos patrones fijos de distractoras con sus
    gates de marco, el slot de imperativo con su universo cerrado de opciones, la tabla declarativa por
    variante, el canon editorial con los escaneos positivos de las explanations, el audit trail con
    `deriveStatus` real y la ronda EXTRA condicionada, y el registro de la categoría. Las cuatro
    desviaciones respecto del análogo están declaradas en la cabecera, las pruebas negativas demuestran
    que los gates muerden, y la suite completa sigue en `fail 0`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Honestamente acotadas: esto es una app web estática de un solo usuario, sin servidor, sin autenticación,
sin red en runtime y sin sincronización (CLAUDE.md §Constraints). El único dato nuevo que entra es un
fichero JSON escrito a mano por el propio autor. La superficie real de una fase de **autoría de
contenido** aquí es la **integridad de datos**, no el ataque.

| Boundary | Description |
|----------|-------------|
| `content/exercises/*.json` en disco → `fetch()` en boot → `JSON.parse` en `src/data/content-loader.js` | Contenido semi-confiable: lo escribe el autor (o una autoría asistida), no un tercero, pero pasa por `JSON.parse` y de ahí al estado de la app sin ninguna otra puerta. Un JSON malformado rompe `loadContent()` en boot y la app no arranca. |
| strings de contenido (`prompt`, `options[]`, `explanation`, `notes`) → render en el DOM por Alpine | La invariante anti-XSS del proyecto (T-02-01): el contenido se renderiza SIEMPRE como texto, nunca como HTML. Si un string llevara marcado, o el render cambiara a HTML, el contenido pasaría a ser código. |
| `content/categories.json` → `src/data/schema-validator.js` (`knownCategoryIds`) → `categoriesForDisplay` | Registro: una entrada mal formada rompe la validación del bundle completo, y una entrada ausente deja el fichero de contenido en estado inválido en disco. |
| slug del fichero y prefijo de los ids de slot → `RESET_PREFIXES_V13` (`src/data/storage.js:1345`) → `exerciseStats` del autor en `localStorage` | Integridad del progreso: si el slug no coincide byte a byte con el elemento que Phase 40 ya dejó en `RESET_PREFIXES_V13`, el reset selectivo no alcanza la categoría y el progreso del autor queda huérfano en silencio. Agravante de esta fase: `fare-indicativo` y `fare-indefiniti` comparten el prefijo `fare-ind` (D-40-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Tampering | strings de `content/exercises/fare-cond-imperativo.json` renderizados en el DOM | medium | mitigate | Se preserva la invariante de render como texto (T-02-01): esta fase NO añade ni cambia una sola línea de render, y el contenido queda libre de marcado. Doble verificación automatizada: el sub-test `plain text (no markdown)` de `tests/exercise-types.test.js`, activado sobre el fichero nuevo por la línea de `CATEGORIES_WITH_EXPLANATIONS` de la Task 1; más un escaneo `node -e` en las tareas de contenido y el `describe` 11 de la Task 4, que exigen 0 apariciones de los caracteres de corchete angular, de `&#`, del esquema `javascript:` y de backticks en TODOS los strings del fichero, incluido `notes`. El `notes` se redacta sin corchetes angulares a propósito, para que el escaneo no necesite excepciones. |
| T-43-02 | Tampering | `JSON.parse` del fichero de contenido en `content-loader.js` | medium | mitigate | Prototype-pollution por contenido: `JSON.parse` crea `__proto__` como own-property si el literal la declara. Mitigación de esta fase: el fichero declara ÚNICAMENTE el key set del schema (top-level `notes` y `exercises`; por slot las 6 claves; por variante las 3), congelado por el `describe` 1 de la Task 4; y cada tarea corre un escaneo recursivo `node -e` que exige que ninguna clave propia del objeto parseado se llame `__proto__`, `constructor` ni `prototype`, más `({}).polluted === undefined` tras el parse. El deep-clone defensivo del state vive en la cadena de migración y no se toca. |
| T-43-03 | Repudiation | `validation.passes[]` como audit trail del quórum | medium | mitigate | Un `status: "validated"` escrito a mano, o un pase con `by`/`date` inventados, forja la única evidencia que el autor tiene de que una variante fue revisada, y la falsificación es indetectable a ojo. Mitigación: el executor deja los slots en `status: "pending"` con `passes: []` y NO fabrica pases (criterio de aceptación en las dos tareas de contenido); el quórum real corre en la pasada top-level de §Hand-off, un ejercicio por contexto (VAL-03); y el `describe` 12 de la Task 4 exige `status === deriveStatus(passes)` importando la función real, más la ronda EXTRA `deepseek` condicionada en el slot de imperativo, con pruebas negativas que demuestran que el gate muerde. |
| T-43-04 | Tampering | entrada de `content/categories.json` | medium | mitigate | Es prerequisito de schema, no cosmética: sin ella, `schema-validator.js` emite `referencia a categoría desconocida` por cada slot y `tests/domain.test.js` se pone rojo. Un `origen` con typo o una clave extra hacen `ok:false` en el validador de procedencia. Riesgo específico del append doble de esta fase: romper la contigüidad de `order`, que el gate de registro de `content-fare-congiuntivo.test.js:1407-1433` ya vigila. Verificado por criterio de aceptación: 4 claves exactas, es la 17ª, `order: 17`, `origen: "ia-quorum"`, índice igual a `order - 1`, orders contiguos 1..17, y la suite en `fail 0`. |
| T-43-05 | Tampering | coincidencia del slug con `RESET_PREFIXES_V13` y colisión de prefijo `fare-ind` | high | mitigate | Si el slug del fichero, el `id` de `categories.json` o el prefijo de los ids de slot no son byte a byte `fare-cond-imperativo`, el reset selectivo de v13 no alcanza la categoría y el progreso real del autor queda huérfano sin ningún error visible — el fallo silencioso más caro de esta fase, y por eso `high` y no `medium`: rompe el Core Value del proyecto (que el sistema te obligue a no olvidar) sin emitir ninguna señal. Agravante: `fare-indicativo` y `fare-indefiniti` comparten `fare-ind`, así que una comprobación de prefijo truncada daría falsos positivos entre categorías. Mitigación: `src/data/storage.js:1345` ya contiene el string exacto (Phase 40 shippeada) y esta fase NO lo edita; el criterio de aceptación fija los 3 ids de slot literalmente, verifica el `id` de la entrada y exige que el prefijo declarado sea el slug COMPLETO; el `describe` 13 de la Task 4 lo congela como invariante permanente; y la prohibición de renombrar ids después del alta queda escrita en `must_haves.prohibitions`. |
| T-43-06 | Tampering | `validation.status` de la categoría frente a la cascada D-54 | high | mitigate | Amenaza de integridad específica de esta fase, y la más cara: una variante con dos respuestas defendibles hace que el quórum emita `incorrecta`, `deriveStatus` la deja en `disputed`, `disputed` es STICKY, y la cascada D-54 resetea la categoría entera — el autor pierde progreso real por un defecto del material, no por no saber. Los tres vectores concretos aquí son el MAGNET de la 2ª singular (tres formas atestiguadas), el deíctico de futuro en el condizionale presente y la prótasis hipotética en el passato. Mitigación: los tres van como gates HARD declarados en `notes` ANTES de escribir y congelados en los `describe` 6, 7, 8 y 9 de la Task 4, más el checkpoint humano de la Task 2 sobre la unicidad de lectura, más la ronda EXTRA DeepSeek del slot de imperativo en el pase top-level. El quórum es la red, no el mecanismo. |
| T-43-07 | Denial of Service | tamaño del bundle de contenido cargado en boot | low | accept | 17 variantes son del orden de 300 líneas de JSON; el loader ya trae 16 ficheros con 238 slots. El coste de `JSON.parse` a esta escala es despreciable y la app no tiene atacante remoto: sin servidor, sin red, sin multiusuario. |
| T-43-08 | Information Disclosure | el contenido del fichero | low | accept | Son conjugaciones del condizionale y del imperativo de un verbo italiano y explicaciones gramaticales. Cero PII, cero credenciales, cero secretos. El fichero no sale de la máquina del autor. |
| T-43-09 | Spoofing | — | n/a | n/a — no aplica | No existen identidades, sesiones, tokens ni cuentas en esta app: es local, monousuario y sin autenticación. No hay superficie de suplantación que evaluar en L1. |
| T-43-10 | Elevation of Privilege | — | n/a | n/a — no aplica | No hay roles, permisos ni frontera de privilegio: un único usuario con acceso total a su propio navegador y su propio disco. |
| T-43-SC | Tampering | instalaciones de npm / pip / cargo | n/a | n/a — gate no disparado | Esta fase no ejecuta ninguna instalación de gestor de paquetes ni añade dependencias: el proyecto es web estática de dependencias cero y los tests corren con el runner nativo de Node. El Package Legitimacy Gate no aplica y no se requiere tabla de auditoría. |

**ASVS L1, block on high.** Hay dos amenazas `high` en este plan y las dos están en `mitigate` con
verificación automatizada más un checkpoint humano: T-43-05 (el slug byte a byte, cuyo fallo es silencioso
y huérfana el progreso real del autor) y T-43-06 (la doble validez, cuyo fallo dispara la cascada D-54 y
resetea la categoría entera). No se inventa ninguna amenaza para rellenar la tabla: en una app local sin
servidor, sin red en runtime y sin entrada de terceros, la superficie real de una fase de contenido son la
invariante de render como texto, la higiene del key set del JSON parseado, la integridad del audit trail
de validación, el registro de la categoría como prerequisito de schema, la coincidencia byte a byte del
slug y la unicidad de lectura de cada variante. Las dos `accept` son `low` y su razón es estructural.

### Candidatos de prohibición canon descartados (breadcrumbs)

- **prototype-pollution** — canon de seguridad del proyecto; vive en `<threat_model>` T-43-02 y en
  `/gsd-secure-phase`, no se acuña como prohibición.
- **XSS / inyección de HTML** — canon; vive en T-43-01 (invariante de render como texto, T-02-01), no se
  acuña.
- **path-traversal** — sin acceso a sistema de ficheros en runtime: el contenido se sirve por HTTP local y
  el runtime es el navegador. Descartado.
- **GDPR / privacidad de datos personales** — el contenido son conjugaciones y el state es el progreso de
  estudio del propio autor en su máquina. Descartado.
- **fairness / sesgo genérico** — sin decisión automatizada sobre personas. Lo que SÍ se acuña son las
  variantes bespoke y específicas de esta fase, que ninguna herramienta canon detectaría: el ejercicio con
  dos respuestas defendibles, la forma atestiguada ofrecida como distractora «incorrecta», el MAGNET
  resuelto por comodidad, el override de un flag de acentos, y el examen de vocabulario no enseñado.
</threat_model>

<verification>
Comprobaciones al terminar las 4 tareas de este plan:

1. `node --test tests/*.test.js` → código de salida 0, `fail 0`, `pass` mayor que el baseline re-medido en
   la precondición de Task 1 (842).
2. `node --test tests/content-fare-cond-imperativo.test.js` → código de salida 0, `fail 0`, 13 `describe`.
3. `node -e` sobre `content/exercises/fare-cond-imperativo.json`: 3 slots, ids exactamente
   `fare-cond-imperativo-cond-presente`, `fare-cond-imperativo-cond-passato` y
   `fare-cond-imperativo-imperativo`, con 6, 6 y 5 variantes, 17 en total, los 3 en
   `type: "multiple-choice"` con `options` de 4 y `categoryIds` de 1.
4. `node -e` sobre `content/categories.json`: 17 entradas, la 17ª es `fare-cond-imperativo` con
   `order: 17` y `origen: "ia-quorum"`, exactamente las 4 claves del schema, e índice igual a `order - 1`
   con los `order` contiguos 1..17.
5. Los escaneos de ausencia, **todos por campo y por coincidencia exacta en `options`**, nunca sobre el
   fichero completo, ni sobre `notes`, ni sobre `explanation`: blacklist en `options`, formas no
   personales en `options` (frontera con 43-02), perífrasis en `prompt` y `options`, deícticos de futuro
   en los prompts del slot 1, la palabra condicional en los prompts del slot 2, español y paréntesis en
   los 17 prompts.
6. Los escaneos en positivo: cada prompt con un objeto del conjunto cerrado, cada prompt de condizionale
   con el pronombre de su persona, cada prompt de imperativo con su marcador de destinatario, `notes` con
   cada forma de la blacklist entre apóstrofes ASCII y con el literal `la forma fa'` y los marcadores en
   MAYÚSCULAS, y las 3 explanations con sus contenidos obligatorios (paradigma, par mínimo, par del futuro
   en el pasado, formas vetadas nombradas, homografía enseñada).
7. `VAL_07_STRICT=1 node --test tests/*.test.js` → falla nombrando los 3 slots `pending`. Ese rojo es el
   estado CORRECTO al cerrar este plan; lo cierra el pase top-level de quórum de §Hand-off.
8. `node scripts/run-validation-271.mjs` → sigue reportando su total anterior en PASS. Es la ceguera
   documentada en §Frontera, no un fallo: el reporter no verá la categoría hasta Phase 44 / INT-02.
9. `git diff --quiet src/screens/app.js src/domain/ src/data/` → código de salida 0 (SC-5, Out of Scope del
   milestone).
10. `git status --porcelain` → exactamente los 4 ficheros de `files_modified`. En particular
    `content/exercises/fare-indefiniti.json`, `tests/content-fare-indefiniti.test.js` y
    `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` NO están
    tocados: son del plan 43-02.
</verification>

<success_criteria>
- CI-01: el autor es examinado sobre `farei / faresti / farebbe / faremmo / fareste / farebbero`, con la
  raíz contracta `far-` y con el futuro semplice de la misma persona como distractora en las 6 variantes.
- CI-02: el autor es examinado sobre `avrei fatto` en las 6 personas, incluido el futuro en el pasado
  (`ha detto che avrebbe fatto`), con el condizionale presente como distractora del calco español y una
  explanation que desarrolla el par explícito con «dijo que haría».
- CI-03: el autor es examinado sobre `fa' / faccia / facciamo / fate / facciano` en EXACTAMENTE 5
  variantes, con la key de la 2ª singular resuelta con audit trail, sin que ninguna de las tres formas
  atestiguadas aparezca como distractora «incorrecta», y con el registro como eje examinado.
- SC-1: los dos slots de condizionale con las 6 personas enumeradas, una persona distinta por pasada vía
  `pickVariantIndex`, el contraste futuro frente a condizionale en las 6 variantes del presente (SC-1 pide
  «al menos una»), el futuro en el pasado en al menos 2 del passato, y la explanation señalando que el
  español lo resuelve de otra forma.
- SC-2: 5 variantes exactas, la ausencia de `io` documentada en `notes`, en la explanation y en un gate de
  test permanente, y el MAGNET resuelto con audit trail — verificado por dato en la Task 3 y por gate en
  la Task 4.
- SC-5 (parte de este plan): la categoría carga en boot y aparece en home, picker, Repaso y Examen como
  unidad de reset independiente de las otras 3 de `fare`, sin una línea de motor nueva — verificado por
  dato en la Task 1 y a ojo en el checkpoint de la Task 2.
- Estado de hand-off correcto: los 3 slots en `pending` con `passes: []`, el rojo de `VAL_07_STRICT=1`
  como marcador visible de que el quórum base todavía no ha corrido, y el gate condicionado de ronda EXTRA
  ya escrito para cuando corra.
</success_criteria>

<output>
Crear `.planning/phases/43-fare-cond-imperativo-fare-indefiniti-3-6-slots/43-01-SUMMARY.md` al terminar.
</output>
