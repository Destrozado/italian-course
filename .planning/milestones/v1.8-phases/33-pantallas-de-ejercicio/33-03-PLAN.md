---
phase: 33-pantallas-de-ejercicio
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - index.html
  - app.css
autonomous: true
requirements: [EX-05]
must_haves:
  truths:
    - "The word bank shows pills on --ed-surface with --ed-border-soft, radius 12, keeping the ¹..⁹ key superscripts"
    - "Placed words stay as invisible placeholders (no reflow) — the .wb-placed visibility:hidden technique is preserved"
    - "The answer area shows a stable styled gap; post-grading it fills green-underline if correct / red-strikethrough if wrong (consistent with multiple-choice D-08)"
    - "On feedback, a feedback box shows the serif Italian title ¡Esatto! / Quasi… + the existing explanation (consistent with multiple-choice D-09)"
    - "The native assemble-then-check model is preserved: Comprobar (disabled until canCheck) → Siguiente"
  artifacts:
    - path: "index.html"
      provides: "Repainted word-buttons sub-template (bank + answer + feedback box + Comprobar/Siguiente)"
      contains: "wordButtonsAddWord(entry.idx)"
    - path: "app.css"
      provides: "Editoriale repaint of .wb-bank/.wb-answer/.wb-placed/.kbd-hint + wb feedback consistent with MC"
      contains: ".wb-bank"
  key_links:
    - from: "index.html bank button"
      to: "wordButtonsAddWord / bankWithKeys"
      via: "@click + x-for over bankWithKeys (unchanged)"
      pattern: "wordButtonsAddWord\\(entry.idx\\)"
    - from: "index.html Comprobar"
      to: "wordButtonsCheck / wordButtonsCanCheck"
      via: ":disabled + @click (unchanged)"
      pattern: "wordButtonsCheck"
---

<objective>
Repaint the word-buttons sub-template into the Editoriale language (EX-05), extrapolated consistently with multiple-choice, over the intact assemble-then-check mechanics.

Purpose: EX-05 is not in the handoff (D-12) — extrapolate: bank pills on `--ed-surface`, stable gaps with post-grading fill (D-08), green/red feedback box + serif Italian title (D-09), all consistent with the MC repaint from 33-02. The mechanics (`wordButtonsAddWord`/`RemoveWord`/`Check`, `bankWithKeys`, `.wb-placed` no-reflow technique, ¹..⁹ key superscripts) are preserved. Word-buttons KEEPS its native "Comprobar" → "Siguiente" model (assemble-then-check is its natural flow — NOT the same as MC's 1-step deviation).
Output: Repainted word-buttons sub-template (index.html ~485-549) + wb CSS appended to the Phase 33 section in app.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md
@.planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md
@.planning/phases/33-pantallas-de-ejercicio/33-02-SUMMARY.md

<interfaces>
<!-- Reuse from 33-01/33-02 + existing engine state — DO NOT re-derive. -->
Shared from 33-01/02: `.session-feedback`(`.correct`/`.wrong`), `.session-feedback-title`, gap-slot classes, tint tokens, `.session-explanation` repaint, `.session-why`.
Existing bindings to PRESERVE verbatim (index.html 485-549):
  - Bank: `<template x-for="(entry, idx) in bankWithKeys" :key="entry.idx">`, `@click="wordButtonsAddWord(entry.idx)"`, `:class="{ 'wb-placed': entry.placed }"`, `:disabled`, `:tabindex`, `:aria-hidden`, `:aria-label`, `<span x-text="entry.word"></span><sup class="kbd-hint" x-text="entry.key"></sup>`.
  - Answer: `.wb-answer` with `:class` toggles `wb-answer-empty` / `incorrecta`, `aria-live="polite"`, `x-for="(word, idx) in wordButtonsAnswer"`, `@click="wordButtonsRemoveWord(idx)"`, `x-text="word"`.
  - "Respuesta correcta" line `.wb-correct-answer` (518-521) — keep or fold into feedback box (planner's call, copy Spanish).
  - `.session-explanation` (529-531), `.session-why` (540-543), Comprobar `@click="wordButtonsCheck" :disabled="!wordButtonsCanCheck"` (535-538), Siguiente `@click="sessionAdvance"` (544-546).
bankWithKeys (app.js ~2704) returns `{word, idx, placed, key}` — reuse as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint the word-buttons sub-template markup (bank + answer + feedback box)</name>
  <files>index.html</files>
  <read_first>
    - index.html word-buttons sub-template lines 485-549 (bank 488-500, answer 503-515, wb-correct-answer 518-521, explanation 529-531, button-row 534-547).
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Word-buttons (EX-05, D-12), §Gap rendering & fill (D-08), §Copywriting (Comprobar/Siguiente existing; ¡Esatto!/Quasi…), §Conscious Deviations.
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §Word-buttons sub-template.
    - 33-02-SUMMARY.md — the exact `.session-feedback`/title pattern established for MC, to mirror it here.
  </read_first>
  <action>
    In `index.html` word-buttons sub-template (~485-549), repaint while keeping all mechanics:
    - Keep the `.wb-bank` `x-for` over `bankWithKeys`, `@click="wordButtonsAddWord(entry.idx)"`, the `wb-placed` `:class`, `:disabled`/`:tabindex`/`:aria-hidden`/`:aria-label`, and the `<span x-text="entry.word">` + `<sup class="kbd-hint" x-text="entry.key">` EXACTLY (the ¹..⁹ superscripts and no-reflow placeholder stay).
    - Keep the `.wb-answer` block with its `wb-answer-empty`/`incorrecta` `:class` toggles, `aria-live`, the `wordButtonsAnswer` `x-for`, `@click="wordButtonsRemoveWord(idx)"`, and `x-text="word"`. For the post-grading gap-fill (D-08), drive a green-underline (correct) / red-strikethrough (wrong) presentation on the assembled answer via a `sessionFeedback`-based class — consistent with the MC gap fill from 33-01/02.
    - Wrap the feedback area into the shared `.session-feedback` box (`.correct`/`.wrong` toggled on `sessionFeedback`) with the hardcoded serif title `¡Esatto!` (`x-show` correct) / `Quasi…` (`x-show` incorrect), then the EXISTING `.session-explanation` <p> and `.session-why` button (both preserved). The `.wb-correct-answer` line may be folded into the box or kept (planner's call; `<strong x-text="...answer.join(' ')">` preserved if kept; copy stays Spanish).
    - Keep the button-row Comprobar (`@click="wordButtonsCheck" :disabled="!wordButtonsCanCheck"`, label hardcoded) → Siguiente (`@click="sessionAdvance"`, label hardcoded). Word-buttons KEEPS its native Comprobar model (do not convert to 1-step).
    - x-text ONLY for data (word/answer/explanation); all titles/labels hardcoded. No x-html.
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8'); const keep=['wordButtonsAddWord(entry.idx)','wordButtonsRemoveWord(idx)','wordButtonsCheck','wordButtonsCanCheck','bankWithKeys','kbd-hint']; const okKeep=keep.every(k=>h.includes(k)); const okComprobar=h.includes('Comprobar')&&h.includes('Siguiente'); const noHtml=(h.match(/x-html/g)||[]).length===0; if(okKeep&&okComprobar&&noHtml){console.log('PASS')}else{console.error('FAIL keep='+okKeep+' comprobar='+okComprobar+' noHtml='+noHtml);process.exit(1)}"</automated>
    <human-check>Run `npx serve`, do a word-buttons exercise. Confirm: bank pills look Editoriale with ¹..⁹ superscripts; placing a word leaves a stable invisible gap (no reflow); Comprobar is disabled until the answer is complete; on check, a feedback box shows ¡Esatto!/Quasi… + explanation; the answer fills green-underline (correct) / red-strikethrough (wrong); Siguiente advances.</human-check>
  </verify>
  <acceptance_criteria>
    - Word-buttons sub-template keeps `wordButtonsAddWord(entry.idx)`, `wordButtonsRemoveWord(idx)`, `wordButtonsCheck`, `wordButtonsCanCheck`, the `bankWithKeys` `x-for`, and the `.kbd-hint` `<sup x-text="entry.key">` superscripts.
    - The `.wb-placed` class toggle remains (the no-reflow placeholder technique is intact).
    - A `.session-feedback` box with hardcoded `¡Esatto!`/`Quasi…` title + the preserved `.session-explanation` and `.session-why` is present.
    - Comprobar (`:disabled="!wordButtonsCanCheck"`) → Siguiente flow is preserved with hardcoded labels (native model, NOT converted to 1-step).
    - `grep -c 'x-html' index.html` = 0.
  </acceptance_criteria>
  <done>Word-buttons markup repainted, mechanics + Comprobar/Siguiente preserved, shared feedback box added; automated check prints PASS; human-check confirms the visuals.</done>
</task>

<task type="auto">
  <name>Task 2: Editoriale CSS for the word bank, answer area, placed/key states</name>
  <files>app.css</files>
  <read_first>
    - styles.css `.wb-bank` (192-202), `.wb-placed` (203-209), `.wb-answer` (210-223), `.wb-answer-empty::before` (224-230), `.wb-answer.incorrecta` (233-240), `.wb-correct-answer` (241-250), `.kbd-hint` (251-256).
    - app.css Phase 33 session section (append here) + the tint tokens + the MC option styling from 33-02 (mirror its surface/border/radius language).
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Word-buttons (bank `--ed-surface` + `--ed-border-soft` + radius `--ed-radius-ghost` 12; placed dim/disabled; answer stable gap + post-grading fill; feedback green/red mirroring MC), §Typography (`.kbd-hint` 0.65em relative, sans 500, `--ed-faint`), §Focus/disabled (Comprobar disabled uses `cta-disabled-bg` #dcd7cb / `cta-disabled-text` #a89f8c).
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §styles.css legacy wb states.
  </read_first>
  <action>
    In `app.css` (Phase 33 session section, after 33-02's MC rules — app.css wins by source order), ADD the word-buttons styling consuming only `--ed-*`:
    - `.wb-bank` pills: `--ed-surface` fill, `--ed-border-soft` border, radius `--ed-radius-ghost` (12px), serif consistent with MC options; flex-wrap layout preserved.
    - `.wb-placed`: KEEP `visibility: hidden` (the no-reflow placeholder — do NOT remove or change to display:none); just ensure surrounding repaint does not break the stable gap.
    - `.wb-answer` / `.wb-answer-empty::before`: stable styled gap; the empty placeholder italic hint repainted toward `--ed-faint`. Post-grading fill: green underline (`--ed-green` / correct) / red strikethrough (`--ed-red` / wrong) — reuse the gap-fill classes from 33-01 or mirror them, consistent with MC.
    - `.kbd-hint`: 0.65em relative, sans 500, `--ed-faint`.
    - Comprobar disabled state: `cta-disabled-bg` #dcd7cb fill + `cta-disabled-text` #a89f8c text (handoff values; isolate as local rule). Focus-visible 2px `--ed-green` outline offset 2px on bank/answer/Comprobar.
    - Override the legacy `.wb-answer.incorrecta` / `.wb-correct-answer` green/red so they consume the Editoriale tints rather than the old `--pico-*` greens. Transitions ~150-200ms. No Pico reintroduction, no palette redefinition.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app.css','utf8'); const ok=s.includes('.wb-bank')&&/visibility:\s*hidden/.test(s)&&s.includes('.kbd-hint')&&(s.includes('--ed-radius-ghost')||s.includes('12px')); if(ok){console.log('PASS')}else{console.error('FAIL ok='+ok);process.exit(1)}"</automated>
    <automated>node --test tests/*.test.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `app.css` Phase 33 section styles `.wb-bank` pills with `--ed-surface` + `--ed-border-soft` + radius 12 (`--ed-radius-ghost`), and `.kbd-hint` at 0.65em sans `--ed-faint`.
    - `.wb-placed { visibility: hidden }` is preserved (no-reflow technique intact).
    - The answer post-grading fill uses `--ed-green` underline (correct) / `--ed-red` strikethrough (wrong); legacy `--pico-*` greens no longer win.
    - Comprobar disabled uses #dcd7cb / #a89f8c; focus-visible green outline present; only `--ed-*` (plus the two literal disabled hexes) used; no new `--pico-*` definition.
    - `node --test tests/*.test.js` shows only the known preexisting `genero-numero` failure.
  </acceptance_criteria>
  <done>Word-buttons CSS appended, no-reflow placeholder + key superscripts preserved, feedback consistent with MC; tests show only the known preexisting failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exercise content (words/answer/explanation) → DOM | Hand-authored word/answer/explanation strings rendered in the word-buttons sub-template. Only untrusted-input surface (static, single-user, local, no backend/network/auth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Tampering (DOM injection) | word/answer/explanation rendering (index.html 485-549) | mitigate | `x-text` exclusive for `entry.word`, `word`, `payload.explanation`, `answer.join(' ')`; feedback titles, Comprobar/Siguiente labels, and key superscripts are hardcoded/derived strings via x-text, never x-html. Acceptance asserts `grep -c x-html` = 0. |
| T-33-06 | (no new surface) | CSS repaint + markup | accept | Pure presentation; no network/storage/privilege/auth surface added. `wordButtonsAddWord`/`RemoveWord`/`Check` mechanics unchanged. |

No server/auth surface in this architecture — none invented.
</threat_model>

<verification>
- `node --test tests/*.test.js` → only the known preexisting `genero-numero` failure.
- `grep -c 'x-html' index.html` → 0.
- Manual: Editoriale bank pills + ¹..⁹, stable no-reflow gaps, Comprobar disabled-until-canCheck → Siguiente, titled feedback box, green-underline/red-strikethrough fill.
</verification>

<success_criteria>
- EX-05 word-buttons extrapolated to Editoriale (bank, stable gaps, green/red feedback + Italian title) consistent with MC (D-08/D-09/D-12).
- Native assemble-then-check (Comprobar → Siguiente) preserved; mechanics + no-reflow placeholder + key superscripts intact.
- Engine untouched; anti-XSS x-text-only invariant preserved.
</success_criteria>

<output>
Create `.planning/phases/33-pantallas-de-ejercicio/33-03-SUMMARY.md` when done.
</output>
