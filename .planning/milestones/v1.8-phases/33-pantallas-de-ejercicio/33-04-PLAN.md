---
phase: 33-pantallas-de-ejercicio
plan: 04
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - index.html
  - app.css
autonomous: true
requirements: [EX-04]
must_haves:
  truths:
    - "Match keeps the per-pair flow: each pair validates instantly with a red flash + immediate D-61 cascade on first failure — NO match-all-then-Comprobar, NO disabled CTA"
    - "D-05: only the per-pair-compatible handoff visual states are applied — matched pairs show green-tint pills (green-tint-border, green-on-tint text) with a numeric badge (green circle, white number) replacing the ¹/ᵃ suffixes; the candidate '?'/dashed-border state is optional (planner discretion, omit if it does not fit the 2-column layout)"
    - "The selected/active pill shows a green 2.5px border + --ed-surface + green shadow"
    - "The per-pair failure flash is a single-iteration ≤300ms red animation (WCAG 2.3.1 safe)"
    - "An optional centered italic note 'N de M emparejadas' (12px Hanken italic, --ed-faint) derives from match state"
  artifacts:
    - path: "index.html"
      provides: "Repainted match sub-template (2-column pills + numeric badge + states + optional note)"
      contains: "matchSelectLeft(idx)"
    - path: "app.css"
      provides: "Editoriale repaint of .match-grid/.match-col/.match-selected/.match-consumed/.match-flash + numeric badge + match note"
      contains: ".match-selected"
  key_links:
    - from: "index.html match pill"
      to: "matchSelectLeft / matchPickRight (per-pair, unchanged)"
      via: "@click + :class + :disabled (unchanged)"
      pattern: "matchPickRight\\(idx\\)"
    - from: "app.css .match-consumed repaint"
      to: "--ed-green-tint tokens"
      via: "matched pill tint + badge"
      pattern: "--ed-green-tint"
---

<objective>
Repaint the match sub-template into the Editoriale per-pair states (EX-04) over the intact instant-validate cascade flow.

Purpose: EX-04 visual states (2-column pills, numeric badge per pair, green-tint matched, selected/candidate states, "N de M emparejadas" note). CONSCIOUS DEVIATION (D-03): match stays PER-PAIR — each pair validates instantly with a red flash + immediate D-61 cascade on first failure. We do NOT adopt the handoff "match-all-then-Comprobar" model, and there is NO disabled CTA (the EX-04 "CTA disabled until complete" sub-criterion is intentionally not met). A plan that "fixes" EX-04 back to the all-then-check model is WRONG. Per D-05, apply only the handoff states compatible with the 2-column per-pair layout.
Output: Repainted match sub-template (index.html ~582-635) + match CSS appended to the Phase 33 section in app.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md
@.planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md
@.planning/phases/33-pantallas-de-ejercicio/33-03-SUMMARY.md

<interfaces>
<!-- Reuse from 33-01 + existing engine state — DO NOT re-derive. -->
Shared from 33-01: `.session-feedback`/`.session-feedback-title`, tint tokens, `.session-explanation` repaint, `.session-why`, the `currentColor` dot technique (Phase 32 `.status-dot` app.css 546-552) for the green numeric badge.
Existing bindings to PRESERVE verbatim (index.html 582-635):
  - `.match-grid` / two `.match-col` (1fr 1fr layout).
  - Left: `x-for="(item, idx) in matchLeft" :key="'L_'+idx+'_'+item"`, `@click="matchSelectLeft(idx)"`, `:disabled="matchLeftIsConsumed(idx) || sessionFeedback !== null"`, `:class` toggles `match-selected`/`match-consumed`/`match-flash`, `:aria-label`, `<span x-text="item"><sup class="kbd-hint" x-text="idx<9?(idx+1):''">`.
  - Right: `x-for="(item, idx) in matchRight" :key="'R_'+idx+'_'+item"`, `@click="matchPickRight(idx)"`, `:disabled="matchRightIsConsumed(idx) || ..."`, `:class` toggles `match-consumed`/`match-flash`, `:aria-label`, `<span x-text="item"><sup class="kbd-hint" x-text="letterFor(idx)">`.
  - `.session-explanation` (623-625), `.session-why` (627-630), Siguiente `@click="sessionAdvance"` (631-633).
  Engine: matchSelectedLeftIdx, matchLeftIsConsumed/matchRightIsConsumed, matchFlashIdx{left,right}, matchHadFailure — read-only consumption only; do NOT modify match grading logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint the match sub-template markup (2-col pills + numeric badge + states + note)</name>
  <files>index.html</files>
  <read_first>
    - index.html match sub-template lines 582-635 (grid/cols 584-614, explanation 623-625, ¿Por qué? 627-630, Siguiente 631-633).
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Match (EX-04, PER-PAIR, D-03/D-05; numeric badge, selected, candidate-only-if-fits, failure flash, note 12px Hanken italic), §Conscious Deviations #2.
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §Match sub-template + §No Analog Found (numeric badge).
    - 33-03-SUMMARY.md — confirm the shared feedback/note patterns established so far.
  </read_first>
  <action>
    In `index.html` match sub-template (~582-635), repaint while keeping the per-pair flow and every binding:
    - Keep the `.match-grid` / `.match-col` 2-column structure, both `x-for`s with their `:key`s, `@click="matchSelectLeft(idx)"` / `@click="matchPickRight(idx)"`, the `:disabled` guards, the `:class` toggles (`match-selected`/`match-consumed`/`match-flash`), and the `:aria-label`s EXACTLY (D-03: NO match-all-then-Comprobar, NO disabled CTA).
    - Replace the `<sup class="kbd-hint">` numeric/alpha suffixes with the numeric badge per matched pair (D-05): a hardcoded badge element (green circle, white number) shown on matched (`.match-consumed`) pills. The badge number derives from the existing index expressions (`idx+1` left / pairing) — render via `x-text` on the derived number only; the circle styling is CSS. The candidate "?" / dashed-border state (other column) is OPTIONAL — apply ONLY if it fits the current 2-column layout without forcing it (D-05); if it does not fit cleanly, omit it and document the omission in the SUMMARY (it is a D-05 planner-discretion state, not a requirement).
    - Optionally ADD a centered italic note "N de M emparejadas" derived presentationally from match state (e.g. count of consumed pairs vs total) — `x-text` on the derived count only; if no clean read-only derivation exists without new engine state, omit and note it (the note is "optional" in UI-SPEC).
    - Keep the `.session-explanation`, `.session-why`, and Siguiente (`@click="sessionAdvance"`) — wrap the explanation in the shared `.session-feedback` box with the hardcoded `Quasi…` title on failure (match has no "correct feedback box" mid-flow; on the final-failure feedback the box + explanation apply, consistent with 33-01/02). x-text ONLY for data; labels/titles hardcoded. No x-html.
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8'); const keep=['matchSelectLeft(idx)','matchPickRight(idx)','matchLeftIsConsumed','matchRightIsConsumed','match-grid','match-selected','match-flash']; const okKeep=keep.every(k=>h.includes(k)); const noComprobarMatch=!/match[^\"]*Comprobar|Comprobar[^<]*match/i.test(h); const noHtml=(h.match(/x-html/g)||[]).length===0; if(okKeep&&noHtml){console.log('PASS')}else{console.error('FAIL keep='+okKeep+' noHtml='+noHtml);process.exit(1)}"</automated>
    <human-check>Run `npx serve`, do a match exercise. Confirm: two columns of serif pills; selecting a left pill shows a green border + shadow; a correct pair fixes both pills with green-tint + a numeric badge; a wrong pick flashes red once (~300ms) and triggers the cascade; the optional "N de M emparejadas" note (if added) reads correctly; NO Comprobar button exists; Siguiente only appears on the final-failure feedback.</human-check>
  </verify>
  <acceptance_criteria>
    - Match sub-template keeps `matchSelectLeft(idx)`, `matchPickRight(idx)`, `matchLeftIsConsumed`/`matchRightIsConsumed`, the `match-selected`/`match-consumed`/`match-flash` `:class` toggles, the 2-column `.match-grid`/`.match-col`, and the `:aria-label`s.
    - NO "Comprobar"/match-all CTA and NO disabled completion CTA exists in the match sub-template (D-03).
    - A numeric badge (number via `x-text` on a derived index, circle via CSS) marks matched pairs, replacing the `¹`/`ᵃ` suffix role.
    - The shared `.session-feedback` box + `Quasi…` title + preserved `.session-explanation`/`.session-why` apply on final-failure feedback.
    - `grep -c 'x-html' index.html` = 0.
  </acceptance_criteria>
  <done>Match markup repainted with per-pair flow intact, numeric badge + states, optional note; automated check prints PASS; human-check confirms per-pair behavior and visuals. Any omitted D-05 optional state (candidate "?"/note) noted in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Editoriale CSS for match pills, badge, selected/matched/flash states</name>
  <files>app.css</files>
  <read_first>
    - styles.css `.match-grid` (278-283), `.match-col` (284-288), `.match-selected` (293-296), `.match-consumed` (300-306), `@keyframes match-flash-red` (314-324), `.match-flash` (325-327).
    - app.css Phase 33 session section (append here) + tint tokens + the `.status-dot` currentColor technique (Phase 32, app.css 546-552) for the green badge circle.
    - .planning/phases/33-pantallas-de-ejercicio/33-UI-SPEC.md §Match (resting pill `--ed-surface` + `--ed-border-soft` + radius 13; matched green-tint + badge green ~20px Hanken 12/800 white; selected green 2.5px border + green shadow 0 6px 16px rgba(47,125,86,.18); candidate dashed green 2px if fits; flash red ≤300ms), §Spacing (match padding 14 verbatim, column gap 14 verbatim), §Typography (match note 12px Hanken italic --ed-faint; match prompt serif 17).
    - .planning/phases/33-pantallas-de-ejercicio/33-PATTERNS.md §styles.css legacy match states + §WCAG-safe single-flash animation.
  </read_first>
  <action>
    In `app.css` (Phase 33 session section, after 33-03's wb rules — app.css wins by source order), ADD the match styling consuming only `--ed-*`:
    - `.match-grid` / `.match-col`: keep 1fr 1fr; column gap 14px (`/* exception verbatim */`).
    - Resting pill: serif 17, `--ed-surface` fill, `--ed-border-soft` border, radius 13px (`/* exception verbatim */`), padding 14px (`/* exception verbatim */`).
    - Override `.match-selected` → green 2.5px border + `--ed-surface` fill + green shadow `0 6px 16px rgba(47,125,86,.18)` (`.18` alpha verbatim). Remove the old blue Pico outline.
    - Override `.match-consumed` → matched pill: `--ed-green-tint` fill + `--ed-green-tint-border` + `--ed-green-on-tint` text (replaces the old muted-grey). Numeric badge: green circle ~20px (build from the `.status-dot` currentColor technique), white number Hanken 12/800.
    - Repaint `@keyframes match-flash-red` / `.match-flash` toward `--ed-red`, keeping SINGLE iteration ≤300ms (`300ms ease-out 1` — WCAG 2.3.1 safe; do NOT change to loop).
    - Candidate pill (if Task 1 added it): dashed `--ed-green` 2px border. `.match-note` (if added): 12px Hanken italic, `--ed-faint`, centered. Focus-visible 2px `--ed-green` outline offset 2px. Transitions ~150-200ms. No Pico reintroduction, no palette redefinition.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app.css','utf8'); const ok=s.includes('.match-selected')&&s.includes('.match-consumed')&&s.includes('--ed-green-tint')&&/match-flash-red[\s\S]*ease-out 1/.test(s)&&!/match-flash-red[\s\S]{0,80}infinite/.test(s); if(ok){console.log('PASS')}else{console.error('FAIL ok='+ok);process.exit(1)}"</automated>
    <automated>node --test tests/*.test.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `app.css` Phase 33 section overrides `.match-selected` (green 2.5px border + `--ed-surface` + green shadow `.18` alpha), `.match-consumed` (`--ed-green-tint` matched pill + badge), and resting pill (serif 17, `--ed-surface`, `--ed-border-soft`, radius 13, padding 14).
    - The numeric badge is a green circle with a white Hanken 12/800 number.
    - `.match-flash` keeps a single-iteration (`... 1`, not infinite) ≤300ms red animation repainted toward `--ed-red`.
    - Verbatim off-grid values (radius 13, padding 14, column gap 14) marked `/* exception verbatim */`; only `--ed-*` tokens used; no new `--pico-*` definition.
    - `node --test tests/*.test.js` shows only the known preexisting `genero-numero` failure.
  </acceptance_criteria>
  <done>Match CSS appended (pills, badge, selected/matched/flash), single-flash WCAG-safe animation preserved; tests show only the known preexisting failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exercise content (match items/explanation) → DOM | Hand-authored match item + explanation strings rendered in the match sub-template. Only untrusted-input surface (static, single-user, local, no backend/network/auth). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Tampering (DOM injection) | match item/explanation rendering (index.html 582-635) | mitigate | `x-text` exclusive for `item` (both columns) and `payload.explanation`; the numeric badge number, the `Quasi…` title, the note, and `aria-label`s are derived/hardcoded strings via x-text/attributes, never x-html. Acceptance asserts `grep -c x-html` = 0. |
| T-33-08 | (no new surface) | CSS repaint + markup | accept | Pure presentation; no network/storage/privilege/auth surface added. The per-pair flow (`matchSelectLeft`/`matchPickRight`/D-61 cascade) is unchanged. |
| T-33-09 | (accessibility — non-security) | `.match-flash` red animation | mitigate | Keep single-iteration ≤300ms (WCAG 2.3.1 safe); do not convert to loop. Asserted in the automated check. |

No server/auth surface in this architecture — none invented.
</threat_model>

<verification>
- `node --test tests/*.test.js` → only the known preexisting `genero-numero` failure.
- `grep -c 'x-html' index.html` → 0.
- Manual: 2-col pills, selected green border+shadow, matched green-tint + numeric badge, single red flash + cascade, optional note, NO Comprobar/disabled CTA.
</verification>

<success_criteria>
- EX-04 visual states recreated where compatible (2-col pills, numeric badge, green-tint matched, selected/candidate, "N de M emparejadas" note) over the intact per-pair instant-validate + D-61 cascade flow (D-03/D-05).
- Engine untouched; anti-XSS x-text-only invariant preserved; flash stays WCAG-safe single iteration.
- Conscious deviation honored: NO match-all-then-Comprobar, NO disabled CTA.
</success_criteria>

<output>
Create `.planning/phases/33-pantallas-de-ejercicio/33-04-SUMMARY.md` when done.
</output>
