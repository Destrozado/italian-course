---
phase: 34-canciones-resultados-picker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/app.js
  - app.css
  - styles.css
  - tests/screen-canciones.test.js
autonomous: true
requirements: [SRP-01, SRP-03, SRP-04]

must_haves:
  truths:
    - "Song state pasada renders a green dot, fallada a red dot, no-hecha a neutral dot"
    - "songsForDisplay objects expose artist + titleDisplay + metaLabel derived from title split"
    - "A presentational score getter returns {correct, total, pct} from the answered-set snapshot"
    - "A presentational getter exposes the count of selected picker categories"
  artifacts:
    - path: "app.css"
      provides: "badge-pasada / badge-fallada song-state color classes + net-new tokens (--ed-green-dark, --ed-placeholder, ring track)"
      contains: "badge-pasada"
    - path: "src/screens/app.js"
      provides: "extended songsForDisplay (artist/titleDisplay/metaLabel + featured selection flag) + summaryScore getter + picker selection-count getter"
      contains: "summaryScore"
  key_links:
    - from: "songsForDisplay"
      to: "badge-${song.status}"
      via: "status field consumed by :class binding in canciones template"
      pattern: "badge-pasada|badge-fallada"
    - from: "summaryScore"
      to: "summarySessionResults"
      via: "read-only derive from answered snapshot (D-10)"
      pattern: "summarySessionResults"
---

<objective>
Lay the shared presentational groundwork that the four screen-repaint plans (34-02..34-05) consume: net-new CSS tokens, the two song-state badge classes, and the presentational getters/derivations. This plan owns the cross-cutting `app.js` getters and the token/badge CSS block so the per-screen plans can repaint markup without re-deriving data or redefining tokens.

Purpose: Honor D-05/D-06 (title→artist split), D-07 (song-state→color), D-09/D-10 (score X/Y), D-12 (picker selection count) and provide the net-new tokens the score ring + cover tiles need — all engine-untouched, presentation only.
Output: Extended `songsForDisplay`, new `summaryScore` getter, picker selection-count getter, `badge-pasada`/`badge-fallada` classes, and new `--ed-green-dark` / `--ed-placeholder` / ring-track tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-canciones-resultados-picker/34-CONTEXT.md
@.planning/phases/34-canciones-resultados-picker/34-UI-SPEC.md
@.planning/phases/34-canciones-resultados-picker/34-PATTERNS.md

<interfaces>
<!-- Extracted from src/screens/app.js — executor uses these directly, no exploration needed. -->

Existing `songsForDisplay` getter (app.js L3078-3093) returns objects:
  { id, title, phraseCount, status, statusLabel, vecesFallada }
  - status ∈ 'no-hecha' | 'pasada' | 'fallada' (song triad, NOT category triad)
  - title default = song.id when song.title missing

Existing snapshot used by summary (app.js):
  summarySessionResults  — Array<{exerciseId: string, correct: boolean, ...}> set in completeSession(), cleared in returnToHomeFromSummary(). (NOT the live sessionResults — same snapshot guard pattern as summaryDelta.)

Existing picker state/getters (app.js):
  pickerCheckedCategoryIds — array of selected category ids (L145)
  get pickerPoolSize() (L2978), get pickerStartLabel() (L2993) — reused verbatim downstream

Read-only derive idiom to mirror:
  get sessionProgressPercent() (L2616) — derives a percent from session state, no engine mutation

Existing badge classes (styles.css L62-64) — CATEGORY triad only:
  .badge-no-hecha { color: var(--ed-neutral-dot, #c4bcab); }
  .badge-hecha    { color: var(--ed-amber, #b9852f); }
  .badge-dominada { color: var(--ed-green, #2f7d56); }

Confirmed-present tokens (app.css :root): --ed-green #2f7d56, --ed-red #b5412e,
  --ed-neutral-dot #c4bcab, --ed-paper-elevated, --ed-radius-card/cta/feedback/pill,
  --ed-shadow-card/cta, --ed-green-tint*/--ed-red-tint*, --ed-faint/--ed-faint-2,
  --ed-font-mono/serif/sans, --ed-streak-track.
Confirmed-ABSENT (must be created here): --ed-green-dark (#296c4a, cover-tile default
  stripe per UI-SPEC §Color), --ed-placeholder (the "/Y" muted color in the score hero
  per UI-SPEC Copywriting), and the score-ring track literal #e6ddcd (handoff §5,
  distinct from --ed-streak-track — may live as a token or be inlined by the summary plan;
  define it here as --ed-ring-track for reuse).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend songsForDisplay + add summaryScore and picker selection-count getters</name>
  <files>src/screens/app.js, tests/screen-canciones.test.js</files>
  <read_first>
    - src/screens/app.js L3078-3093 (songsForDisplay — the getter to extend) and L3020+ (categoriesForDisplay split-name idiom to copy)
    - src/screens/app.js L297 (summarySessionResults snapshot) and L2616 (sessionProgressPercent read-only derive idiom)
    - src/screens/app.js L145 (pickerCheckedCategoryIds)
    - 34-PATTERNS.md §`songsForDisplay` (D-05/D-06 split spec) and §`summary` (summaryScore getter spec, D-09/D-10)
    - 34-CONTEXT.md D-05, D-06, D-10, D-12; 34-UI-SPEC.md Copywriting (song row meta, results hero)
    - tests/screen-home-editorial.test.js (source-assert test style to mirror)
  </read_first>
  <behavior>
    - songsForDisplay returns objects that ALSO carry: titleDisplay (title left of "—", trimmed), artist (right of "—", trimmed, '' when no "—"), metaLabel (`{artist} · {phraseCount} huecos` when artist present, else `{phraseCount} huecos`).
    - When a song.title has no "—", titleDisplay === full title and artist === ''.
    - songsForDisplay marks exactly one featured song = the FIRST song in list order whose status is 'no-hecha' or 'fallada' (D-01); when all are 'pasada', no song is featured (featured flag false on every row).
    - summaryScore returns { correct, total, pct } where total = summarySessionResults.length (answered count, D-10), correct = count of r.correct truthy, pct = total ? round(correct/total*100) : 0.
    - A getter exposes the selected-category count from pickerCheckedCategoryIds.length (D-12) for the "{N} categorías seleccionadas" copy.
    - Engine invariants intact: no change to startSong/completeSession/pickerToggleCategory/cascada; applyImmediateFailure(this.state call-sites count unchanged.
  </behavior>
  <action>
    Extend the `songsForDisplay` map (app.js L3081-3091) to derive title/artist via `(song.title ?? song.id).split('—').map(s => s.trim())` — same split idiom as `categoriesForDisplay` (which splits on '('), per D-05/D-06. Add `titleDisplay`, `artist`, `metaLabel` fields to the returned object; KEEP existing `id`, `title`, `phraseCount`, `status`, `statusLabel`, `vecesFallada` verbatim. Compute the featured song (D-01: first 'no-hecha'|'fallada' in iteration order) and tag each returned row with a boolean `featured` flag (only one true; all false when every status is 'pasada' per D-04). Add a new read-only getter `summaryScore` (mirror the `sessionProgressPercent` derive-from-session idiom at L2616) deriving `{correct, total, pct}` from `this.summarySessionResults` (the SNAPSHOT, not live `sessionResults` — D-10). Add a thin getter (e.g. `pickerSelectedCount`) returning `this.pickerCheckedCategoryIds.length` (D-12). Do NOT modify any engine method; this is presentation-only (per CONTEXT `<code_context>` and the v1.8 brownfield invariant). Extend `tests/screen-canciones.test.js` with source-asserts: songsForDisplay derives artist via `.split('—')`, returns `titleDisplay`/`artist`/`metaLabel`/`featured`; `summaryScore` reads `summarySessionResults` and computes `pct`; selection-count getter reads `pickerCheckedCategoryIds.length`; and a guard that `applyImmediateFailure(this.state` still has exactly 2 call-sites (cascada D-54 not regressed).
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js</automated>
  </verify>
  <done>songsForDisplay returns titleDisplay/artist/metaLabel/featured; summaryScore returns {correct,total,pct} from the snapshot; picker selection-count getter exists; new asserts pass; cascada call-site count unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add song-state badge classes + net-new tokens to app.css</name>
  <files>app.css, styles.css, tests/screen-canciones.test.js</files>
  <read_first>
    - app.css :root token block (L130-182) — confirm which --ed-* exist (--ed-green-dark and --ed-placeholder are ABSENT; ring track #e6ddcd is a handoff literal)
    - styles.css L62-64 (existing .badge-no-hecha/.badge-hecha/.badge-dominada — category triad)
    - 34-PATTERNS.md §Shared Patterns "Status color via currentColor + badge-*" (delta: add badge-pasada/badge-fallada) and §No Analog (ring track literal #e6ddcd, cover stripe --ed-green-dark #296c4a)
    - 34-UI-SPEC.md §Color (green/red reserved lists; neutral/muted), 34-CONTEXT.md D-07
  </read_first>
  <acceptance_criteria>
    - app.css `:root` defines `--ed-green-dark: #296c4a;`, `--ed-placeholder` (muted "/Y" color), and `--ed-ring-track: #e6ddcd;`.
    - app.css (bottom, in a new banner-commented block) defines `.badge-pasada { color: var(--ed-green); ... }` and `.badge-fallada { color: var(--ed-red); ... }` — the song triad, alongside (not replacing) the existing category triad. `.badge-no-hecha` (neutral) is reused as-is.
    - The new tokens carry `/* exception verbatim — handoff §X */` or decision annotations per the established banner-comment idiom; ring track #e6ddcd marked as a handoff §5 literal distinct from --ed-streak-track.
    - No `--pico-*` reference is introduced in any new rule; no `prefers-color-scheme` rule added (dark-mode off, D-32-03).
    - Engine untouched; this is CSS + token only.
  </acceptance_criteria>
  <action>
    Add `--ed-green-dark: #296c4a;` (cover-tile default stripe, UI-SPEC §Color), `--ed-placeholder` (the muted "/Y" color in the score hero per UI-SPEC Copywriting — pick a value from the muted family, e.g. `var(--ed-faint)` aliased or a literal consistent with the handoff), and `--ed-ring-track: #e6ddcd;` (handoff §5 literal, distinct from `--ed-streak-track`) to the `app.css :root` block. At the BOTTOM of `app.css` (wins by source order — the Phase 32-33 idiom), open a `═══` banner-commented block citing D-07 and add `.badge-pasada` (`color: var(--ed-green)`) and `.badge-fallada` (`color: var(--ed-red)`), mirroring the `font-weight`/structure of the existing category `badge-*` in styles.css. These feed `currentColor` of `.status-dot` / `.streak-bar-fill` exactly like the category triad. Do NOT touch styles.css badge rules (the category triad stays). Add source-asserts in `tests/screen-canciones.test.js`: app.css contains `--ed-green-dark`, `--ed-ring-track`, `.badge-pasada`, `.badge-fallada`; and a regression guard that no NEW `--pico-` token reference was introduced in the badge/token additions.
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js</automated>
  </verify>
  <done>app.css defines the 3 new tokens + badge-pasada/badge-fallada; category badge triad untouched; no --pico-* or dark-mode rules added; asserts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON content → DOM | Song titles / session results are author-authored data bound into the DOM. Single-user local tool: no network, no backend, no auth, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | data binding in templates that consume songsForDisplay/summaryScore | mitigate | All dynamic content rendered via `x-text` exclusively; NEVER `x-html`. This plan adds only getters + CSS — no new bindings — so the invariant is carried forward unchanged. Button/label text stays hardcoded in markup. |

No other threats apply (presentation-only, localStorage, single-user local).
</threat_model>

<verification>
- `node --test tests/screen-canciones.test.js` passes (new source-asserts green).
- `node --test tests/*.test.js` overall suite stays green except the single preexisting unrelated failure (genero-numero count).
- No `x-html` anywhere; no new `--pico-*` reference; no `prefers-color-scheme` rule.
</verification>

<success_criteria>
- songsForDisplay exposes titleDisplay/artist/metaLabel/featured (D-01/D-05/D-06).
- summaryScore returns {correct,total,pct} from the answered snapshot (D-09/D-10).
- Picker selection-count getter exists (D-12).
- app.css defines badge-pasada/badge-fallada (D-07) + --ed-green-dark/--ed-placeholder/--ed-ring-track.
- Engine logic unchanged (cascada call-site count intact).
</success_criteria>

<output>
Create `.planning/phases/34-canciones-resultados-picker/34-01-SUMMARY.md` when done.
</output>
