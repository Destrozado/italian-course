---
phase: 34-canciones-resultados-picker
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - index.html
  - app.css
  - tests/screen-canciones.test.js
autonomous: true
requirements: [SRP-01]

must_haves:
  truths:
    - "Canciones screen shows a featured Continuar/Empezar card for the first pending song (hidden when all songs pasada)"
    - "Each song row shows a tinted cover tile (serif initial), serif title, italic meta, and a state-colored status dot"
    - "The featured card progress bar is state-based (no numeric fraction)"
  artifacts:
    - path: "index.html"
      provides: "repainted canciones template (featured card + Editoriale list rows)"
      contains: "songsForDisplay"
    - path: "app.css"
      provides: "Editoriale CSS block for the canciones featured card + cover tiles + rows"
      contains: "huecos"
  key_links:
    - from: "canciones template row"
      to: "startSong(song.id)"
      via: "row @click reused verbatim"
      pattern: "startSong\\(song\\.id\\)"
    - from: "status dot"
      to: "badge-${song.status}"
      via: ":class binding feeds currentColor"
      pattern: "badge-\\$\\{song\\.status\\}"
---

<objective>
Repaint the Canciones list screen (SRP-01) in the Editoriale language: a featured "Continuar"/"Empezar" card for the first pending song, plus a list of hairline rows with tinted cover tiles (serif initial), serif title + italic meta, and a state-colored status dot — reusing the `songsForDisplay` derivations added in 34-01 and the existing `startSong` click handler.

Purpose: Deliver SRP-01 with real song-state data, honoring D-01 (featured = first pending), D-02 (state-based bar, no fraction), D-03 (CONTINUAR/EMPEZAR overline), D-04 (hide card when all pasada), D-05/D-06 (title/artist/meta), D-07 (state→dot color).
Output: Repainted `canciones` `<template>` interior + new app.css canciones block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-canciones-resultados-picker/34-CONTEXT.md
@.planning/phases/34-canciones-resultados-picker/34-UI-SPEC.md
@.planning/phases/34-canciones-resultados-picker/34-PATTERNS.md
@.planning/phases/34-canciones-resultados-picker/34-01-SUMMARY.md

<interfaces>
<!-- From 34-01 (this plan depends on it). songsForDisplay now returns: -->
  { id, title, titleDisplay, artist, metaLabel, phraseCount, status, statusLabel, vecesFallada, featured }
  - featured: exactly one row true (first 'no-hecha'|'fallada'); all false when every status is 'pasada'
  - metaLabel: `{artist} · {N} huecos` or `{N} huecos`
  - status ∈ 'no-hecha' | 'pasada' | 'fallada'; badge-pasada (green) / badge-fallada (red) / badge-no-hecha (neutral) exist in CSS

<!-- Existing markup to repaint — index.html L274-315 (canciones template). -->
  Current row: <td> badge + statusLabel, title via x-text, phraseCount, <button @click="startSong(song.id)">Jugar</button>
  Existing engine handler: startSong(song.id) — reuse verbatim, do NOT modify.

<!-- Reusable CSS classes (app.css, Phases 32-33). -->
  .home-cta / .home-cta-text / .home-cta-title / .home-cta-sub / .home-cta-arrow (L467-500) — elevated CTA pattern
  .home-overline (L447-455) — overline; .home-section-overline (L528) — section header
  .cat-name (L566, serif 18/600) / .cat-topic (L574, italic 12.5 --ed-faint)
  .status-dot (L558, 9px round, background:currentColor) — color from badge-*
  .streak-bar / .streak-bar-fill (L589-603) — currentColor fill bar
  .match-badge (L1254-1269) — solid color-block-with-glyph structure (anchor for cover tiles)
  Tokens: --ed-paper-elevated, --ed-radius-card (18), --ed-shadow-card, --ed-green,
          --ed-green-dark (#296c4a, NEW in 34-01), cover-tile radius literal 11.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint the canciones featured card (Continuar/Empezar)</name>
  <files>index.html, app.css, tests/screen-canciones.test.js</files>
  <read_first>
    - index.html L274-315 (canciones template to repaint)
    - 34-PATTERNS.md §`canciones` template — "Featured Continuar card" (copy .home-cta elevated pattern; surface --ed-paper-elevated radius 18; overline copies .home-overline; state-based bar copies .streak-bar)
    - 34-UI-SPEC.md §Interaction Contract (Canciones rows D-01/D-02/D-04) + Copywriting (featured overline CONTINUAR/EMPEZAR; "TODAS LAS CANCIONES" section overline)
    - 34-CONTEXT.md D-01, D-02, D-03, D-04; app.css L447-500 (.home-overline/.home-cta), L589-603 (.streak-bar)
  </read_first>
  <acceptance_criteria>
    - The featured card renders only for the row where `song.featured` is true, via `x-for`/`x-show` over songsForDisplay (or a thin filtered binding); when no song is featured (all pasada, D-04) the card block does not render.
    - Featured overline text is hardcoded per branch: `CONTINUAR` when the featured song status is `fallada`, `EMPEZAR` when `no-hecha` (D-03) — driven by `x-show`/`x-if` branches, NOT `x-html`.
    - The featured card surface uses `--ed-paper-elevated` + `--ed-radius-card` (18) + `--ed-shadow-card` (NOT the green CTA fill); the progress bar is state-based (`.streak-bar`-style, currentColor from `badge-${status}`) with NO numeric fraction text (D-02).
    - The card title uses serif (`.cat-name` or featured 19/600), artist in italic; CTA glyph "Continuar →" reuses the `.home-cta-arrow` `→` (aria-hidden) and the row's existing `@click="startSong(...)"`.
    - app.css canciones block sits at file bottom under a `═══` banner citing SRP-01/D-01..D-04, consumes only `--ed-*` tokens, no `--pico-*`.
  </acceptance_criteria>
  <action>
    Inside the `canciones` `<template>` (index.html L274-315), add a featured-card block before the list. Bind it to the single `song.featured === true` row from `songsForDisplay` (a filtered `x-for` or `x-show` per row). Build the card by copying the `.home-cta` elevated-CTA structure (app.css L467-500) but on a `--ed-paper-elevated` surface with `--ed-radius-card` + `--ed-shadow-card` (per PATTERNS delta). Overline: two `x-show` branches rendering the hardcoded strings `CONTINUAR` (status `fallada`) and `EMPEZAR` (status `no-hecha`) styled like `.home-overline` in green (D-03). Title = `song.titleDisplay` serif, artist = `song.artist` italic (both via `x-text`). State-based progress bar = a `.streak-bar`/`.streak-bar-fill` clone whose fill width is a fixed state-based value (e.g. full for pasada — though pasada never featured — partial otherwise) with NO fraction text (D-02). CTA = "Continuar →" using `.home-cta-arrow`; the whole card's `@click` reuses `startSong(song.id)`. Add the app.css block at file bottom (new `═══` banner citing SRP-01/D-01..D-04). T-02-01: all dynamic text via `x-text`, overline/CTA labels hardcoded. Extend `tests/screen-canciones.test.js` asserting: index.html contains the `CONTINUAR`/`EMPEZAR` hardcoded strings under `song.featured` gating, the featured card uses `--ed-paper-elevated`/`--ed-radius-card`, the bar has no fraction binding, and `startSong(song.id)` is preserved.
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js</automated>
  </verify>
  <done>Featured card renders for the pending song with CONTINUAR/EMPEZAR overline + state-based bar (no fraction) + serif title/italic artist + Continuar → CTA wired to startSong; hidden when all pasada; asserts pass.</done>
</task>

<task type="auto">
  <name>Task 2: Repaint the canciones list rows + tinted cover tiles</name>
  <files>index.html, app.css, tests/screen-canciones.test.js</files>
  <read_first>
    - index.html L274-315 (the `x-for song in songsForDisplay` table rows to convert to Editoriale hairline rows)
    - 34-PATTERNS.md §`canciones` template — status-dot REUSE VERBATIM, title/meta via .cat-name/.cat-topic, row CTA arrow .home-cta-arrow; §No Analog "Tinted cover tiles" (repeating-linear-gradient + serif initial, anchor to .match-badge structure, radius literal 11, default stripe --ed-green-dark)
    - 34-UI-SPEC.md §Spacing (list tile 46px LOCKED), §Color (cover-tile stripes), §Copywriting (row meta `{artista} · {N} huecos`, "Jugar →")
    - 34-CONTEXT.md D-05, D-06, D-07; app.css L558-581 (.status-dot/.cat-name/.cat-topic), L1254-1269 (.match-badge)
  </read_first>
  <acceptance_criteria>
    - The `<figure><table>` structure is replaced by Editoriale hairline rows (one per `songsForDisplay` row) under a `TODAS LAS CANCIONES` section overline (`.home-section-overline`).
    - Each row shows: a tinted cover tile 46px (LOCKED, D-16) with a serif initial (first letter of titleDisplay) using `repeating-linear-gradient` stripes + `--ed-green-dark` default stripe, radius literal 11; a `.status-dot` whose color comes from `:class="\`badge-${song.status}\`"` (D-07); serif `song.titleDisplay` via `.cat-name`; italic `song.metaLabel` via `.cat-topic`; and a "Jugar →" arrow (`.home-cta-arrow` `→`).
    - Row tap reuses `@click="startSong(song.id)"` verbatim; the `vecesFallada > 0` "fallada xN" affordance is preserved (x-show + x-text).
    - All dynamic text via `x-text`; no `x-html`; cover-tile initial derived via x-text from titleDisplay.
    - app.css adds the list-row + cover-tile rules to the same canciones block; 46px and radius 11 carry `/* exception verbatim — handoff §4 */` annotations.
  </acceptance_criteria>
  <action>
    Replace the `<figure><table>…</table></figure>` in the canciones template (index.html L278-310) with an Editoriale list: a `TODAS LAS CANCIONES` overline (`.home-section-overline`) followed by an `x-for="song in songsForDisplay"` of hairline rows. Each row: a 46px cover tile (new `.song-cover` class — anchor structure to `.match-badge` solid-block-with-glyph at app.css L1254-1269, but fill with `repeating-linear-gradient` stripes defaulting to `--ed-green-dark`, radius literal 11, centered serif initial via `x-text="song.titleDisplay.charAt(0)"`); a `.status-dot` with `:class="\`badge-${song.status}\`"` and `:aria-label="song.statusLabel"` (REUSE VERBATIM, D-07); `.cat-name` serif `x-text="song.titleDisplay"`; `.cat-topic` italic `x-text="song.metaLabel"` (D-05/D-06); preserve the `vecesFallada > 0` "fallada xN" small via x-show/x-text; a trailing "Jugar →" arrow using `.home-cta-arrow`. Keep `@click="startSong(song.id)"` on the row/button verbatim. Add the list-row + `.song-cover` CSS to the canciones app.css block (annotate 46px/radius-11 as handoff §4 exceptions). Keep the "← Volver al home" control. T-02-01: x-text only. Extend `tests/screen-canciones.test.js`: rows bind `badge-${song.status}`, title via `song.titleDisplay`, meta via `song.metaLabel`, cover initial via `song.titleDisplay.charAt`, `startSong(song.id)` preserved, `TODAS LAS CANCIONES` present, zero `x-html=` in index.html.
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js</automated>
  </verify>
  <done>List renders Editoriale rows with cover tiles + state dots + serif title/italic meta + Jugar → arrow under TODAS LAS CANCIONES; startSong preserved; fallada xN preserved; asserts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Song JSON → DOM | Song titles/meta bound into the canciones list. Single-user local tool; no network/backend/auth/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | canciones template data bindings (titleDisplay, metaLabel, cover initial, statusLabel) | mitigate | All dynamic content via `x-text` exclusively; NEVER `x-html`. CONTINUAR/EMPEZAR/Jugar/TODAS LAS CANCIONES are hardcoded literals in markup, not data-driven. |

No other threats apply (presentation-only, localStorage, single-user local).
</threat_model>

<verification>
- `node --test tests/screen-canciones.test.js` passes.
- `node --test tests/*.test.js` stays green except the single preexisting unrelated failure.
- Grep guard: `grep -c 'x-html=' index.html` returns 0.
- Visual: featured card + tinted tiles + state dots render on the Canciones screen (human-verifiable via `npx serve`).
</verification>

<success_criteria>
- Featured Continuar/Empezar card backs the first pending song; hidden when all pasada (D-01/D-03/D-04).
- State-based progress bar, no numeric fraction (D-02).
- List rows: tinted cover tile + serif title + italic `{artista} · {N} huecos` + state dot (D-05/D-06/D-07).
- startSong + fallada-xN preserved; no x-html.
</success_criteria>

<output>
Create `.planning/phases/34-canciones-resultados-picker/34-02-SUMMARY.md` when done.
</output>
