---
phase: 34-canciones-resultados-picker
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - index.html
  - app.css
  - tests/screen-canciones.test.js
autonomous: true
requirements: [SRP-04]

must_haves:
  truths:
    - "Picker shows Editoriale hairline rows: serif category name + italic subtitle + green tick when selected"
    - "Picker shows a counter of SELECTED categories (not exercise count)"
    - "Seleccionar/Quitar todo, Contrarreloj toggle, and the Empezar CTA keep their existing behavior"
  artifacts:
    - path: "index.html"
      provides: "repainted picker template (hairline rows + green CTA)"
      contains: "pickerToggleCategory"
    - path: "app.css"
      provides: "Editoriale CSS block for picker rows + tick + counter"
      contains: "categorías seleccionadas"
  key_links:
    - from: "picker row"
      to: "pickerToggleCategory(cat.id)"
      via: "@change reused verbatim"
      pattern: "pickerToggleCategory\\(cat\\.id\\)"
    - from: "Empezar CTA"
      to: "startSession"
      via: ".session-cta @click + pickerStartLabel"
      pattern: "pickerStartLabel"
---

<objective>
Repaint the Repaso/Examen picker (SRP-04) in the Editoriale language: hairline category rows (serif name + italic subtitle split on parentheses + green tick when selected, full-row clickable), a "{N} categorías seleccionadas" counter, and the green `.session-cta` Empezar button — reusing every existing picker handler and getter verbatim.

Purpose: Deliver SRP-04, honoring D-12 (selected-category counter), D-13 (tick rows), D-14 (paren-split subtitle), while keeping pickerToggleCategory / pickerSelectAll / pickerClearAll / pickerTimed / pickerStartLabel / startSession untouched.
Output: Repainted `picker` `<template>` interior + new app.css picker block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-canciones-resultados-picker/34-CONTEXT.md
@.planning/phases/34-canciones-resultados-picker/34-UI-SPEC.md
@.planning/phases/34-canciones-resultados-picker/34-PATTERNS.md
@.planning/phases/34-canciones-resultados-picker/34-01-SUMMARY.md

<interfaces>
<!-- Existing picker markup — index.html L320-373. -->
  bulk: <button @click="pickerSelectAll">Seleccionar todo</button> / <button @click="pickerClearAll">Quitar todo</button>
  rows: <fieldset><template x-for="cat in content.categories" :key="cat.id">
          <label><input type="checkbox" :value="cat.id"
                 :checked="pickerCheckedCategoryIds.includes(cat.id)"
                 @change="pickerToggleCategory(cat.id)"><span x-text="cat.name"></span></label>
  timed: <label class="picker-timed"><input type="checkbox" x-model="pickerTimed"><span>Contrarreloj …</span></label>
  CTA:   <button @click="startSession" :disabled="pickerPoolSize === 0" x-text="pickerStartLabel"></button>
  warning: x-show="pickerMode === 'test-completo' && pickerPoolSize > 0" .picker-warning
  header: <h2 x-text="pickerHeaderLabel">

<!-- Reuse VERBATIM (engine + getters, do NOT modify): -->
  pickerToggleCategory(catId), pickerSelectAll, pickerClearAll, startSession,
  pickerCheckedCategoryIds (array), pickerPoolSize, pickerStartLabel, pickerTimed, pickerHeaderLabel
  + (from 34-01) the picker selection-count getter (pickerSelectedCount = pickerCheckedCategoryIds.length)

<!-- Reusable CSS (app.css). -->
  .cat-name (serif 18/600) / .cat-topic (italic 12.5 --ed-faint) — category name + paren-split subtitle (D-32-02 idiom: cat.name.split('(')[0].trim())
  .session-cta (L856-876) + :disabled (L1172) — green CTA radius 16 + green shadow
  .picker-timed + input[role="switch"] (L338-368) — Contrarreloj switch (keep)
  .button-row (styles.css L91) — bulk-action layout
  --ed-green (tick + CTA), --ed-faint (subtitle)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repaint picker rows + tick + selection counter</name>
  <files>index.html, app.css, tests/screen-canciones.test.js</files>
  <read_first>
    - index.html L320-373 (picker template to repaint)
    - 34-PATTERNS.md §`picker` template — hairline rows COPY home .cat-name/.cat-topic paren-split, green ✓ when pickerCheckedCategoryIds.includes(cat.id), keep @change="pickerToggleCategory(cat.id)" verbatim; selection counter D-12
    - 34-UI-SPEC.md §Interaction Contract (Picker D-12/D-13/D-14) + §Copywriting (counter "{N} categorías seleccionadas", "Seleccionar todo"/"Quitar todo", "Contrarreloj ⏱")
    - 34-CONTEXT.md D-12, D-13, D-14; app.css L319-368 (custom checkbox + .picker-timed), L566-581 (.cat-name/.cat-topic)
  </read_first>
  <acceptance_criteria>
    - The `<fieldset>` of plain checkbox labels becomes Editoriale hairline rows: full-row clickable `<label>` with serif `cat.name.split('(')[0].trim()` via `.cat-name` + italic subtitle (paren content) via `.cat-topic` shown with `x-show="cat.name.includes('(')"` (D-14, identical to home D-32-02).
    - A green `✓` shows on the right when `pickerCheckedCategoryIds.includes(cat.id)` (D-13); `@change="pickerToggleCategory(cat.id)"` is preserved verbatim.
    - A counter renders "{N} categorías seleccionadas" / "1 categoría seleccionada" driven by the selection-count getter (D-12) — NOT the exercise count (which stays in pickerStartLabel/warning).
    - Bulk actions ("Seleccionar todo"/"Quitar todo" via pickerSelectAll/pickerClearAll), Contrarreloj `.picker-timed` switch (`x-model="pickerTimed"`), header (pickerHeaderLabel), and test-completo warning are all preserved.
    - All dynamic text via `x-text`; counter/subtitle data-driven via x-text; labels hardcoded; no `x-html`.
    - app.css picker block at file bottom, `--ed-*` only, no `--pico-*`.
  </acceptance_criteria>
  <action>
    Repaint the picker `<fieldset>` (index.html L339-349) into Editoriale hairline rows: full-row clickable `<label>` per `cat in content.categories`, serif title `x-text="cat.name.split('(')[0].trim()"` via `.cat-name`, italic subtitle via `.cat-topic` with `x-show="cat.name.includes('(')"` rendering the parenthetical (same split idiom as home D-32-02 / D-14), and a green `✓` glyph on the right gated by `x-show="pickerCheckedCategoryIds.includes(cat.id)"` (D-13). Keep the `<input type="checkbox">` `@change="pickerToggleCategory(cat.id)"` and `:checked` binding verbatim (visually hide the native box or restyle via the existing custom-checkbox CSS at app.css L319-334). Add a selection counter element rendering the count via the 34-01 selection-count getter with hardcoded suffix "categorías seleccionadas" (handle the "1 categoría" singular via two x-show branches or a getter-supplied label) — D-12. Preserve the `.button-row` bulk actions, `.picker-timed` Contrarreloj switch, `pickerHeaderLabel` header, `.picker-warning`, and the "← Volver al home" control. Add the picker CSS (tick, hairline row, counter) to a new bottom-of-`app.css` `═══` block citing SRP-04/D-12..D-14. T-02-01: x-text only. Extend `tests/screen-canciones.test.js`: picker rows use `cat.name.split('('` and `.cat-name`/`.cat-topic`, `pickerToggleCategory(cat.id)` preserved, the selection counter binds the count getter (not pickerPoolSize), the "categorías seleccionadas" literal is present, and `pickerTimed`/`pickerSelectAll`/`pickerClearAll` are preserved.
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js</automated>
  </verify>
  <done>Picker rows are Editoriale hairline rows with serif name + paren-split subtitle + green tick; counter shows selected-category count; all picker handlers/getters preserved; asserts pass.</done>
</task>

<task type="auto">
  <name>Task 2: Repaint the Empezar CTA to .session-cta</name>
  <files>index.html, app.css, tests/screen-canciones.test.js</files>
  <read_first>
    - index.html L359-368 (Empezar button + test-completo warning)
    - 34-PATTERNS.md §`picker` template — "Empezar CTA REUSE .session-cta" (L856-876 + disabled L1172); pickerStartLabel/pickerPoolSize reused verbatim
    - 34-UI-SPEC.md §Color (green reserved for picker Empezar CTA + green shadow) + §Copywriting (Primary CTA = reuse pickerStartLabel)
    - app.css L856-876 (.session-cta), L1172-1177 (:disabled)
  </read_first>
  <acceptance_criteria>
    - The Empezar `<button>` carries `class="session-cta"` and keeps `@click="startSession"`, `:disabled="pickerPoolSize === 0"`, and `x-text="pickerStartLabel"` verbatim.
    - The green `.session-cta` shadow (`--ed-shadow-cta`) and disabled state apply (reused, not redefined).
    - The test-completo warning (`.picker-warning`, x-show on pickerMode/pickerPoolSize) stays intact.
    - No `--pico-*`, no `x-html`.
  </acceptance_criteria>
  <action>
    Add `class="session-cta"` to the Empezar `<button>` (index.html L360-363), keeping `@click="startSession"`, `:disabled="pickerPoolSize === 0"`, and `x-text="pickerStartLabel"` exactly as-is (the green CTA + disabled styling already exist at app.css L856-876 / L1172 — reuse, do not redefine). Leave the `.picker-warning` test-completo notice untouched. If any minor spacing rule is needed (CTA margin in the picker context), add it to the picker app.css block — `--ed-*` only. Extend `tests/screen-canciones.test.js`: the Empezar button has `session-cta` class and preserves `startSession` + `pickerStartLabel` + `:disabled="pickerPoolSize === 0"`.
  </action>
  <verify>
    <automated>node --test tests/screen-canciones.test.js</automated>
  </verify>
  <done>Empezar uses .session-cta (green + shadow + disabled) with startSession/pickerStartLabel/pickerPoolSize bindings intact; warning preserved; asserts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Category JSON → DOM | Category names bound into picker rows. Single-user local tool; no network/backend/auth/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | picker row name/subtitle + counter + pickerStartLabel bindings | mitigate | All dynamic content via `x-text` exclusively; NEVER `x-html`. "categorías seleccionadas", "Seleccionar todo", "Quitar todo", "Contrarreloj ⏱", `✓` are hardcoded literals/glyphs in markup. |

No other threats apply (presentation-only, localStorage, single-user local).
</threat_model>

<verification>
- `node --test tests/screen-canciones.test.js` passes.
- `node --test tests/*.test.js` stays green except the single preexisting unrelated failure.
- Grep guard: `grep -c 'x-html=' index.html` returns 0.
- Visual: picker rows with green tick + counter + green Empezar render (human-verifiable via `npx serve`).
</verification>

<success_criteria>
- Picker rows: serif name + italic paren-split subtitle + green tick when selected, full-row clickable (D-13/D-14).
- Counter shows selected-category count, not exercise count (D-12).
- pickerToggleCategory/pickerSelectAll/pickerClearAll/pickerTimed/startSession/pickerStartLabel preserved.
- Empezar uses .session-cta; no x-html.
</success_criteria>

<output>
Create `.planning/phases/34-canciones-resultados-picker/34-03-SUMMARY.md` when done.
</output>
