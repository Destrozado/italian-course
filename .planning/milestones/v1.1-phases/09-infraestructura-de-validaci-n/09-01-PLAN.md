---
phase: 09-infraestructura-de-validaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/schema-validator.js
  - src/data/validation-state.js
  - tests/exercise-types.test.js
autonomous: true
requirements: [VAL-01, VAL-05, VAL-07]
tags: [validation, schema, node-test, hand-written-validator, italian-course]

must_haves:
  truths:
    - "Schema validator acepta los 271 ejercicios existentes sin cambios (campo `validation` ausente = aceptado)."
    - "Schema validator rechaza ejercicios con `validation.status` no whitelisteado, o con `passes[]` mal formado."
    - "`deriveStatus(passes[])` devuelve `validated` / `disputed` / `pending` según D-VAL-07 estricto + sticky disputed."
    - "Suite baseline 209/209 sigue verde tras los cambios — backward-compat trivial."
    - "Smoke test paramétrico VAL-07 existe y está OFF por defecto (sin env var); pasa baseline con los 271 sin campo `validation`."
  artifacts:
    - path: "src/data/schema-validator.js"
      provides: "validateValidationShape() — validador top-level del campo `validation`, mensajes en español (FOUND-04)"
      contains: "function validateValidationShape"
    - path: "src/data/validation-state.js"
      provides: "deriveStatus(passes) pure helper — sticky disputed según D-VAL-07"
      exports: ["deriveStatus"]
    - path: "tests/exercise-types.test.js"
      provides: "Suite nueva 'data/schema-validator — validation field (Phase 9)' + suite 'validation-state — deriveStatus (Phase 9)' + bloque paramétrico VAL-07 tras feature flag"
      contains: "deriveStatus"
  key_links:
    - from: "src/data/schema-validator.js loop por ejercicio (línea ~133)"
      to: "validateValidationShape(ex, file, push)"
      via: "invocación tras `validator(ex, file, push)`"
      pattern: "validateValidationShape\\(ex, file, push\\)"
    - from: "tests/exercise-types.test.js bloque VAL-07"
      to: "process.env.VAL_07_STRICT"
      via: "lectura top-level + {skip} option en describe()"
      pattern: "VAL_07_STRICT"
---

<objective>
Extender el schema validator hand-written (D-08) con la rama `validateValidationShape` (campo opcional top-level `validation`, back-compat con los 271 actuales), crear el módulo puro `validation-state.js` con `deriveStatus(passes[])` (sticky disputed D-VAL-07), y añadir los tests unitarios + el bloque paramétrico VAL-07 tras feature flag (OFF durante Phase 9).

Esta es la base sobre la que Plan 09-02 monta el VALIDATION-PROMPT + skill orquestador, y sobre la que Plan 09-03 ejecuta el piloto end-to-end.

Purpose: VAL-01 (schema soporta `validation` opcional con back-compat), VAL-05 (cada entry en `passes[]` enforced shape `{by, date, verdict, concerns?}`), VAL-07 (smoke test paramétrico tras feature flag).

Output:
- `validateValidationShape` extendido en `src/data/schema-validator.js`
- Nuevo módulo `src/data/validation-state.js` con `deriveStatus`
- 3 suites nuevas en `tests/exercise-types.test.js` (~25 tests añadidos)
- 209/209 baseline + nuevos tests verdes; el bloque VAL-07 paramétrico está SKIP (env var no activada)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md
@.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md
@.planning/phases/09-infraestructura-de-validaci-n/09-PATTERNS.md
@src/data/schema-validator.js
@tests/exercise-types.test.js

<interfaces>
<!-- Contratos clave que el executor reutilizará (extraídos del codebase). -->
<!-- D-08 dispatch + acumulador. Mensajes en español (FOUND-04). -->

From src/data/schema-validator.js:
```
export function validateContent({ categories, exercisesByFile }): { ok: boolean, errors: Array<{file, exerciseId?, reason}> }

// Convención interna de los validators existentes:
function validateMultipleChoicePayload(ex, file, push) // ~líneas 154-181
function validateWordButtonsPayload(ex, file, push)    // ~líneas 198-226
function validateMatchPayload(ex, file, push)          // ~líneas 244-282
//   push(file, ex.id, reason) — acumula errores; ninguna excepción.
//   Mensajes formato: '"campo.subcampo" debe ser X (encontrado: ${typeof ...})'
//   Patrón opcional con back-compat (línea ~176): if (ex.payload.explanation !== undefined) { ... }
```

From tests/exercise-types.test.js:
```
const CATEGORIES_WITH_EXPLANATIONS = [ {file, expected}, ... ]  // línea 966 — patrón D-144
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, resolve } from 'node:path';
import { validateContent } from '../src/data/schema-validator.js';
```

Shape exacto del campo `validation` (D-VAL-06):
```
{
  "validation": {
    "status": "pending" | "validated" | "disputed",
    "passes": [
      { "by": "claude-opus-4-7", "date": "2026-05-26", "verdict": "correcta"|"incorrecta", "concerns": ["[C5-leak] ..."] }
    ]
  }
}
```

Reglas D-VAL-07 que `deriveStatus(passes[])` operacionaliza:
- CUALQUIER `verdict === 'incorrecta'` → `disputed` (sticky, no self-heal).
- ≥2 entries `correcta` con `by` DISTINTOS (Set size ≥2) AND cero `incorrecta` → `validated`.
- Resto → `pending`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema validator extension — validateValidationShape + suite unitaria</name>
  <files>src/data/schema-validator.js, tests/exercise-types.test.js</files>
  <read_first>
    - @src/data/schema-validator.js (toda — current state del hand-written validator D-08, especialmente las funciones `validateMultipleChoicePayload` líneas 154-181 y `validateMatchPayload` líneas 244-282 cuyo patrón se replica)
    - @tests/exercise-types.test.js líneas 104-356 (suite `data/schema-validator — word-buttons payload` y siguientes — patrón de tests de validator)
    - @tests/exercise-types.test.js líneas 874-955 (suite `data/schema-validator — payload.explanation (Phase 7 D-116)` — analog exacto para campo opcional back-compat)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-PATTERNS.md §1 (líneas ~27-99 — pattern de extensión + whitelist + early-return en forEach)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md §Example 1 (líneas 374-434 — implementación literal copy-paste-base)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md §decisions D-VAL-05/06/07/08 (shape exacto + reglas estrictas)
  </read_first>
  <behavior>
    Tests requeridos (añadir bloque `describe('data/schema-validator — validation field (Phase 9 D-VAL-08)', ...)` tras línea 955 de tests/exercise-types.test.js):
    - Test 1: ejercicio SIN campo `validation` → `result.ok === true` (back-compat — los 271 actuales).
    - Test 2: `validation` = string/array/null/undefined → error `"validation" debe ser objeto si está presente`.
    - Test 3: `validation.status` = 'invalid-status' → error mencionando whitelist `pending|validated|disputed`.
    - Test 4: `validation.status` faltante → error.
    - Test 5: `validation.passes` no es array → error `"validation.passes" debe ser array`.
    - Test 6: `passes[0].by` no es string / vacío → error.
    - Test 7: `passes[0].date` no matchea `/^\d{4}-\d{2}-\d{2}$/` → error mencionando formato ISO.
    - Test 8: `passes[0].verdict` no está en `['correcta', 'incorrecta']` → error.
    - Test 9: `passes[0].concerns` presente pero no-array o contiene non-string → error.
    - Test 10: ejercicio happy-path con `validation: {status: 'validated', passes: [{by, date, verdict: 'correcta'}, {by, date, verdict: 'correcta'}]}` → `result.ok === true`.
    - Test 11: `passes[]` vacío (length 0) con `status: 'pending'` → válido (estado inicial legítimo).
    - Cada test invoca `validateContent({ categories, exercisesByFile })` con `categories` mínimo (1 cat 'avere') y `exercisesByFile` con 1 ejercicio multiple-choice válido + el campo `validation` bajo prueba.
  </behavior>
  <action>
    En `src/data/schema-validator.js`:
    1. Añadir función nueva `validateValidationShape(ex, file, push)` TRAS los 3 PAYLOAD_VALIDATORS existentes (no entra al dispatch table — es top-level, no payload). Patrón: estilo `validateMatchPayload` (early-return en forEach + acumulador push). Implementación de referencia en 09-RESEARCH.md §Example 1.
    2. Whitelist literales: `const VALID_STATUS = ['pending', 'validated', 'disputed']`; `const VALID_VERDICT = ['correcta', 'incorrecta']`; `const ISO_DATE = /^\d{4}-\d{2}-\d{2}$/`.
    3. Back-compat línea 1 de la función: `if (!('validation' in ex)) return;` (D-VAL-08 — los 271 sin el campo pasan limpio).
    4. Mensajes 100% en español (FOUND-04), formato `"validation.passes[${idx}].verdict" inválido (esperado: correcta|incorrecta)`.
    5. En el loop existente de `validateContent()` (línea ~133, tras `validator(ex, file, push);`), añadir: `validateValidationShape(ex, file, push);`. Por D-VAL-05 enforce shape de cada entry.
    6. Comentario de cabecera de la función citando D-VAL-08 + VAL-01 + back-compat.

    En `tests/exercise-types.test.js`:
    - Añadir el bloque `describe('data/schema-validator — validation field (Phase 9 D-VAL-08)', () => { ... })` tras línea 955 (TRAS la suite Phase 7 D-116 explanation — analog directo) y ANTES del bloque `CATEGORIES_WITH_EXPLANATIONS` (línea 966).
    - Reusar imports existentes (líneas 14-24); cero imports nuevos requeridos.
    - Cada test construye `categories` y `exercisesByFile` inline minimal.

    NO modificar las funciones existentes (`validateMultipleChoicePayload`, `validateWordButtonsPayload`, `validateMatchPayload`). NO añadir `validation` al PAYLOAD_VALIDATORS dispatch table — es top-level.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js 2>&1 | tail -30 && grep -q "function validateValidationShape" src/data/schema-validator.js && grep -q "validateValidationShape(ex, file, push)" src/data/schema-validator.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q "function validateValidationShape" src/data/schema-validator.js` → exit 0.
    - Source: `grep -q "validateValidationShape(ex, file, push)" src/data/schema-validator.js` → exit 0 (invocación en el loop).
    - Source: `grep -c "VALID_STATUS\|VALID_VERDICT\|ISO_DATE" src/data/schema-validator.js` ≥ 3.
    - Source: `grep -q "if (!('validation' in ex)) return" src/data/schema-validator.js` → exit 0 (back-compat).
    - Test: `node --test tests/exercise-types.test.js` exit 0, suite "data/schema-validator — validation field (Phase 9 D-VAL-08)" aparece con ≥11 tests pasando.
    - Behavior: `node --test tests/*.test.js` 2>&1 | grep -E "tests\s+[0-9]+" muestra >= 220 (baseline 209 + ≥11 nuevos).
    - Behavior: `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0 (los 271 sin `validation` siguen válidos).
    - Behavior: Mensajes en español — `grep -c "debe ser\|inválido" src/data/schema-validator.js | grep -v '^#'` muestra los validators existentes + los nuevos.
  </acceptance_criteria>
  <done>
    `validateValidationShape` existe en `src/data/schema-validator.js`, invocada en el loop, con back-compat trivial. La suite "data/schema-validator — validation field (Phase 9 D-VAL-08)" tiene ≥11 tests y todos pasan. Baseline 209 sigue verde. Mensajes en español.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure helper deriveStatus + suite de transiciones</name>
  <files>src/data/validation-state.js, tests/exercise-types.test.js</files>
  <read_first>
    - @src/data/schema-validator.js cabecera (líneas 1-20 — convención de módulo puro D-08: `// Pure validator — sin DOM, sin localStorage, sin fetch.`)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-PATTERNS.md §2 (líneas ~102-138 — shape módulo + defensive-null reads + Set para distinct by)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md §Example 2 (líneas 437-457 — implementación verbatim)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md §decisions D-VAL-07 (reglas estrictas + sticky disputed)
  </read_first>
  <behavior>
    Tests requeridos (suite nueva `describe('validation-state — deriveStatus (Phase 9 D-VAL-07)', ...)` en tests/exercise-types.test.js, TRAS la suite del Task 1):
    - Test 1: `deriveStatus(undefined)` → `'pending'` (defensive — passes ausente).
    - Test 2: `deriveStatus(null)` → `'pending'`.
    - Test 3: `deriveStatus([])` → `'pending'` (cero pases — estado inicial legítimo).
    - Test 4: `deriveStatus([{by: 'claude-opus-4-7', date: '2026-05-26', verdict: 'correcta'}])` → `'pending'` (solo 1 pase).
    - Test 5: `deriveStatus([{by: 'A', ..., verdict: 'correcta'}, {by: 'A', ..., verdict: 'correcta'}])` → `'pending'` (2 pases pero MISMO `by` — D-VAL-07 exige Set ≥2).
    - Test 6: `deriveStatus([{by: 'claude-opus-4-7', ..., verdict: 'correcta'}, {by: 'claude-sonnet-4-6', ..., verdict: 'correcta'}])` → `'validated'` (happy path: ≥2 correctas + by distintos).
    - Test 7: `deriveStatus([{by: 'A', verdict: 'incorrecta'}])` → `'disputed'` (cualquier incorrecta gana).
    - Test 8: `deriveStatus([{by: 'A', verdict: 'correcta'}, {by: 'B', verdict: 'correcta'}, {by: 'C', verdict: 'incorrecta'}])` → `'disputed'` (sticky — incorrecta NO se limpia con correctas posteriores).
    - Test 9: `deriveStatus([{by: 'A', verdict: 'correcta'}, {verdict: 'incorrecta'}])` → `'disputed'` (defensive — entry sin `by` no se cuenta como correcta pero sí dispara disputed por verdict).
    - Test 10: input no-array (string, number, object) → `'pending'`.
  </behavior>
  <action>
    Crear `src/data/validation-state.js` nuevo (~30 líneas, módulo ES puro D-08 estilo):

    1. Cabecera: `// src/data/validation-state.js — Phase 9 D-VAL-07.\n// Pure — sin DOM, sin localStorage, sin fetch. D-08 estilo.`
    2. Comentario de reglas D-VAL-07 antes de la función (sticky disputed; ≥2 correctas + by distintos para validated; cualquier incorrecta → disputed).
    3. Exportar named function `deriveStatus(passes)` con JSDoc completo: `@param {Array<{by:string, date:string, verdict:string, concerns?:string[]}>} passes` + `@returns {"pending" | "validated" | "disputed"}`.
    4. Implementación de RESEARCH.md §Example 2 verbatim:
       - `if (!Array.isArray(passes)) return 'pending';`
       - `const hasIncorrecta = passes.some(p => p?.verdict === 'incorrecta');`
       - `if (hasIncorrecta) return 'disputed';`
       - `const correctas = passes.filter(p => p?.verdict === 'correcta');`
       - `const distinctBy = new Set(correctas.map(p => p?.by).filter(Boolean));`
       - `if (correctas.length >= 2 && distinctBy.size >= 2) return 'validated';`
       - `return 'pending';`
    5. Defensive reads (`p?.verdict`) — JSON puede llegar parcial si subagent crashea mid-write (D-VAL-07 defensive).

    En `tests/exercise-types.test.js`:
    - Añadir `import { deriveStatus } from '../src/data/validation-state.js';` en el bloque de imports (línea ~24, tras `validateContent`).
    - Añadir suite `describe('validation-state — deriveStatus (Phase 9 D-VAL-07)', () => { ... })` tras la suite del Task 1.
    - Tests usan `assert.equal(deriveStatus(input), expected)` — cero infra paralela.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js 2>&1 | tail -20 && grep -q "export function deriveStatus" src/data/validation-state.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q "export function deriveStatus" src/data/validation-state.js` → exit 0.
    - Source: `grep -q "Pure — sin DOM" src/data/validation-state.js` → exit 0 (cabecera D-08 estilo).
    - Source: `grep -q "sticky" src/data/validation-state.js` → exit 0 (regla D-VAL-07 documentada).
    - Source: `grep -q "new Set" src/data/validation-state.js` → exit 0 (distinct by enforcement).
    - Test: `node --test tests/exercise-types.test.js` exit 0, suite "validation-state — deriveStatus" aparece con ≥10 tests pasando.
    - Behavior: el módulo es importable sin side-effects → `node -e "import('./src/data/validation-state.js').then(m => console.log(typeof m.deriveStatus === 'function'))"` imprime `true`.
    - Behavior: `node --test tests/*.test.js` exit 0; total tests ≥ 230 (baseline 209 + 11 schema + ≥10 deriveStatus).
  </acceptance_criteria>
  <done>
    `src/data/validation-state.js` existe con `deriveStatus(passes)` exportado, JSDoc completo, defensive reads, sticky disputed según D-VAL-07. La suite de 10 transiciones pasa. Importable desde Node sin side-effects.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bloque paramétrico VAL-07 tras feature flag (off durante Phase 9)</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - @tests/exercise-types.test.js líneas 966-1072 (bloque actual `CATEGORIES_WITH_EXPLANATIONS` — patrón D-144 a replicar)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-PATTERNS.md §3 (líneas ~141-228 — pattern paramétrico + bloque exacto a añadir)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md §Example 4 (líneas 500-535 — implementación literal del describe con `{skip}`)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md §decisions D-VAL-17/18 (feature flag implementación + reutilización array existente)
  </read_first>
  <action>
    En `tests/exercise-types.test.js`, AL FINAL del archivo (tras línea 1072 — cierre del bloque `CATEGORIES_WITH_EXPLANATIONS`):

    1. Añadir constante top-level: `const VAL_07_STRICT = process.env.VAL_07_STRICT === '1';`. Comentario de cabecera explicando: durante Phase 9 el flag está OFF (los 271 sin validar no rompen tests). Al cierre Phase 10 el autor invoca `VAL_07_STRICT=1 node --test tests/*.test.js` y el test debe pasar con 271/271 `validated`.

    2. Añadir `describe('VAL-07 — todos los ejercicios validated (Phase 10 close gate)', { skip: VAL_07_STRICT ? false : 'feature flag VAL_07_STRICT=1 no activado (esperado durante Phase 9)' }, () => { ... })`.

    3. Dentro del describe: `for (const { file, expected } of CATEGORIES_WITH_EXPLANATIONS) { test(\`${file} — todos los ejercicios con validation.status === "validated"\`, () => { ... }); }`.

    4. Cada test:
       - Re-declara `__filename`/`__dirname` con `fileURLToPath(import.meta.url)` + `dirname` (convención del archivo — los bloques `describe` no comparten scope).
       - Lee `JSON.parse(readFileSync(resolve(__dirname, '..', file), 'utf-8'))`.
       - Computa `notValidated = data.exercises.filter(ex => ex.validation?.status !== 'validated')`.
       - `assert.equal(notValidated.length, 0, \`${notValidated.length} ejercicios sin status "validated" en ${file}: ${notValidated.map(ex => \`${ex.id}(${ex.validation?.status ?? 'absent'})\`).join(', ')}\`)`.

    5. Mensaje en español (FOUND-04). Imports ya disponibles en líneas 14-19 (zero new imports).

    Constraint clave (D-VAL-17): tras este cambio, `node --test tests/*.test.js` SIN env var debe seguir verde (la suite está SKIP). Con `VAL_07_STRICT=1`, los 271 ejercicios sin `validation` ROMPEN los tests — comportamiento esperado y deseado para Phase 10 close.
  </action>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -10 && grep -q "VAL_07_STRICT" tests/exercise-types.test.js && grep -q "describe('VAL-07" tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js 2>&1 | grep -E "tests\s+|fail\s+[1-9]" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q "const VAL_07_STRICT = process.env.VAL_07_STRICT === '1'" tests/exercise-types.test.js` → exit 0.
    - Source: `grep -q "describe('VAL-07 — todos los ejercicios validated" tests/exercise-types.test.js` → exit 0.
    - Source: `grep -q "{ skip: VAL_07_STRICT" tests/exercise-types.test.js` → exit 0 (uso de `{skip}` option, no `t.skip()` runtime).
    - Behavior (OFF default): `node --test tests/*.test.js` exit 0 con todos los baseline tests + los nuevos verdes; el suite VAL-07 aparece como `# SKIP feature flag VAL_07_STRICT=1 no activado`.
    - Behavior (ON manual): `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exit !=0 con failures sobre los 7 archivos de categoría (esperado — los 271 no tienen `validation` aún). Esto confirma que el flag estricto FUNCIONARÁ al cierre de Phase 10.
    - Source: `grep -c "VAL_07_STRICT" tests/exercise-types.test.js` ≥ 2 (constante + uso en {skip}).
  </acceptance_criteria>
  <done>
    El bloque VAL-07 paramétrico existe al final de `tests/exercise-types.test.js`, está SKIP por defecto (env var sin setear), y activarse con `VAL_07_STRICT=1` produce failures sobre los 7 archivos sin `validation`. Baseline 209/209 sigue verde. El bloque reutiliza `CATEGORIES_WITH_EXPLANATIONS` (D-VAL-18).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author→JSON files | El autor edita JSON a mano; el schema validator es la línea de defensa contra typos / shapes inválidos. Confianza alta (single user) pero validación obligatoria. |
| test runner→env vars | `VAL_07_STRICT` se lee de `process.env` — heredado del proceso padre (shell del autor). Sin secret value, solo on/off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-T1 | Tampering | `passes[].verdict` editado a mano a un string fuera de whitelist (ej. "ok") | mitigate | `validateValidationShape` enforce whitelist `['correcta', 'incorrecta']` con mensaje en español. JSON con verdict inválido → schema validator falla en boot → banner Phase 1 muestra el error. |
| T-09-01-T2 | Tampering | `validation.status` mentido sin `passes[]` consistent (ej. `status: 'validated'` con `passes: []`) | accept | Single-user local; daño self-inflicted. Mejora opcional futura: añadir `deriveStatus(passes) === status` check en `validateValidationShape`. NO incluido en este plan (deferred — el smoke test VAL-07 al cierre Phase 10 captura `status !== 'validated'` que es el caso peligroso). |
| T-09-01-T3 | Tampering | `validation.passes[i].concerns` array con elementos no-string | mitigate | `validateValidationShape` enforce `Array.isArray(p.concerns) && p.concerns.every(c => typeof c === 'string')` con mensaje en español. |
| T-09-01-I1 | Information Disclosure | El campo `validation` se expone en runtime al alumno (UI) | accept | `validation` es metadata invisible al alumno: el motor de re-verificación + sampler + UI NO leen el campo. Solo schema-validator (boot check) + smoke test paramétrico + script piloto (Plan 03) lo tocan. Confirmado en RESEARCH §Architecture Diagram. |
| T-09-01-D1 | Denial of Service | Array `passes[]` con miles de entries hace `deriveStatus` lento | accept | Phase 10 espera ~2-3 entries por ejercicio (Opus + Sonnet + optionally autor override). Tope práctico <10. `deriveStatus` es O(n) con n trivial. |

No `high`-severity threats. Phase 9 plan 01 passes gate.
</threat_model>

<verification>
**Baseline preservation (CRÍTICO):**
- `node --test tests/*.test.js` SIN env var: TODOS los baseline 209 + los ≥21 nuevos pasan. Total ≥230.

**Backward-compat con los 271 ejercicios actuales:**
- `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0.
- `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exit 0.
- (Idem las 5 categorías restantes — los 271 actuales NO tienen `validation`, debe pasar limpio.)

**Smoke test paramétrico activado manualmente (sanity):**
- `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` debe FALLAR (los 271 aún no tienen `validation`). Esto demuestra que el smoke test funcionará al cierre de Phase 10.

**Cero `npm install`:**
- `git status -s package.json package-lock.json node_modules` → vacío (cero deps añadidas).

**Mensajes en español:**
- `grep -c "debe ser\|inválido\|esperado" src/data/schema-validator.js` ≥ 10 (FOUND-04 enforced).
</verification>

<success_criteria>
- `validateValidationShape` existe en `src/data/schema-validator.js` e invocada en el loop por ejercicio (per D-VAL-08, VAL-01).
- `src/data/validation-state.js` existe con `deriveStatus(passes)` puro, exportado, JSDoc completo, sticky disputed enforce (per D-VAL-07).
- 3 suites nuevas en `tests/exercise-types.test.js`: schema-validator validation field, deriveStatus transitions, VAL-07 paramétrico tras `VAL_07_STRICT` env var.
- Suite VAL-07 está SKIP por defecto y FALLA al activarse con `VAL_07_STRICT=1` sobre los 271 sin `validation` (per D-VAL-17 + VAL-07).
- 209 baseline + ≥21 nuevos tests verdes; `node --test tests/*.test.js` exit 0.
- Mensajes en español (FOUND-04). Cero deps nuevas. Backward-compat con los 271 ejercicios actuales.
</success_criteria>

<output>
Crear `.planning/phases/09-infraestructura-de-validaci-n/09-01-SUMMARY.md` cuando termine, documentando:
- Archivos creados/modificados con líneas añadidas
- Total tests pre/post (209 → ≥230)
- Comando exacto que activa el smoke test estricto (VAL_07_STRICT=1)
- Cualquier deferred encontrado durante ejecución
</output>
