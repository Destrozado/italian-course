---
phase: 09-infraestructura-de-validaci-n
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - scripts/run-validation-pilot.mjs
  - content/exercises/preposiciones.json
  - content/exercises/avere.json
  - tests/fixtures/validation-pilot-disputed.json
autonomous: false
requirements: [VAL-01, VAL-02, VAL-03, VAL-05]
tags: [validation, pilot, end-to-end, gate, italian-course]

must_haves:
  truths:
    - "E1 = `preposiciones-040` ha sido validado por Opus + Sonnet (2 entries en `passes[]` con `by` distintos, ambos `verdict: correcta`) y `validation.status === 'validated'`."
    - "E2 = `avere-001` (baseline) ha sido validado por Opus + Sonnet con el mismo patrón → `validated`."
    - "E3 = `pilot-disputed-c5-leak-001` (fixture en `tests/fixtures/`) ha sido validado por Opus + Sonnet con ≥1 `verdict: incorrecta` Y `concerns[]` con tag `[C5-` → `validation.status === 'disputed'`."
    - "Los 3 outputs JSON del subagent parsearon limpio en el PRIMER intento (cero retries en el piloto — gate D-VAL-15 #4)."
    - "`scripts/run-validation-pilot.mjs` corre, lee los 3 JSONs YA mutados, aplica `deriveStatus()`, e imprime tabla + exit 0 si los 4 gates de D-VAL-15 pasan."
    - "`node scripts/assert-avere-prefix-unchanged.mjs` exit 0 tras añadir `validation` a `avere-001` (relax Plan 02 funciona)."
    - "Cada ejercicio commiteado en 1 commit atómico (3 commits totales — granularidad D-VAL-04 + Pattern 2)."
  artifacts:
    - path: "scripts/run-validation-pilot.mjs"
      provides: "Reporte post-piloto — tabla colorizada (exercise, pass1.by/verdict, pass2.by/verdict, derived status, expected, PASS/FAIL) + gate D-VAL-15 enforcement"
      contains: "deriveStatus"
    - path: "content/exercises/preposiciones.json"
      provides: "preposiciones-040 con campo `validation` poblado (E1 happy path)"
      contains: "preposiciones-040"
    - path: "content/exercises/avere.json"
      provides: "avere-001 con campo `validation` poblado (E2 baseline)"
      contains: "avere-001"
    - path: "tests/fixtures/validation-pilot-disputed.json"
      provides: "pilot-disputed-c5-leak-001 con campo `validation` poblado (E3 disputed gate)"
      contains: "pilot-disputed-c5-leak-001"
  key_links:
    - from: "scripts/run-validation-pilot.mjs"
      to: "src/data/validation-state.js deriveStatus"
      via: "import named de helper puro"
      pattern: "import.*deriveStatus.*validation-state"
    - from: "content/exercises/{preposiciones,avere}.json passes[]"
      to: "subagent outputs Opus + Sonnet"
      via: "skill gsd-validate-exercise (Task() spawn × 2 serial)"
      pattern: "claude-opus-4-7|claude-sonnet-4-6"
---

<objective>
Ejecutar el piloto end-to-end de Phase 9 sobre los 3 ejercicios `preposiciones-040` (E1 happy), `avere-001` (E2 baseline), `pilot-disputed-c5-leak-001` (E3 fixture disputed), invocando el skill `gsd-validate-exercise` por cada uno (3 invocaciones del skill = 6 Task() spawns secuenciales = 2 pases Opus + Sonnet por ejercicio). Crear el reporter `scripts/run-validation-pilot.mjs` que valida el gate D-VAL-15 (4 must-haves observables). Cerrar el plan con un human-verify checkpoint donde el autor inspecciona la tabla y autoriza Phase 10 (o pide iteración del prompt si algún gate falla).

Purpose: validar end-to-end que la infraestructura Plan 09-01 + Plan 09-02 funciona sobre 3 ejercicios reales antes de invertir ~1.5-2M tokens en Phase 10. Cierra los Success Criteria #3 + #5 del ROADMAP §Phase 9.

Output:
- 3 ejercicios mutados in-place con `validation.passes[]` poblado (2 entries cada uno)
- `scripts/run-validation-pilot.mjs` ~50 LOC zero-deps
- 3 commits atómicos (1 por ejercicio, granularidad D-VAL-04)
- Reporte de gate D-VAL-15 verificable por humano
- Checkpoint final donde el autor autoriza Phase 10
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md
@.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md
@.planning/phases/09-infraestructura-de-validaci-n/09-PATTERNS.md
@.planning/phases/09-infraestructura-de-validaci-n/09-01-SUMMARY.md
@.planning/phases/09-infraestructura-de-validaci-n/09-02-SUMMARY.md
@.planning/phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@scripts/validate-content-fixture.mjs
@scripts/assert-avere-prefix-unchanged.mjs

<interfaces>
<!-- Convenciones reutilizadas en este plan. -->

Skill invocation (creado en Plan 09-02):
- Slash command `/gsd-validate-exercise <exercise-id>`
- O equivalente: invocar el skill `gsd-validate-exercise` con argument `<exercise-id>`
- Output: ejercicio JSON mutado in-place con `validation.passes[]` (2 entries) y commit atómico

Helper deriveStatus (creado en Plan 09-01):
- `import { deriveStatus } from '../src/data/validation-state.js';`
- `deriveStatus(passes)` → `'pending' | 'validated' | 'disputed'`
- Sticky disputed: cualquier `incorrecta` gana

Gate D-VAL-15 (4 must-haves):
1. E1 (preposiciones-040) status === "validated" con 2 entries `by` distintos, ambos `verdict: correcta`.
2. E2 (avere-001) status === "validated" igual que E1.
3. E3 (pilot-disputed-c5-leak-001) status === "disputed" con ≥1 entry `verdict: incorrecta` Y `concerns[]` contiene un string que matchea `/^\[C5-/`.
4. Los 3 outputs JSON parsearon limpio en el PRIMER intento, sin retries. (Verificable inspeccionando que ningún `concerns[]` contiene `[meta] parse failed`.)

CLI helper pattern (de `scripts/validate-content-fixture.mjs`):
- Shebang `#!/usr/bin/env node`
- Header docblock con uso + exit codes
- Imports zero-deps: `node:fs`, `node:url`, `node:path`
- `console.error` + `process.exit(1)` para errores en español
- `console.log` + `process.exit(0)` para success

ANSI colors zero-deps (RESEARCH §Recomendaciones #5):
- Verde: `\x1b[32m`, Rojo: `\x1b[31m`, Reset: `\x1b[0m`
- Aplicar al PASS/FAIL final de la tabla
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear scripts/run-validation-pilot.mjs (reporter + gate D-VAL-15)</name>
  <files>scripts/run-validation-pilot.mjs</files>
  <read_first>
    - @scripts/validate-content-fixture.mjs (COMPLETO — patrón CLI helper zero-deps en español que se replica)
    - @scripts/assert-avere-prefix-unchanged.mjs (COMPLETO — patrón de exit-codes + diff verbose en español)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md §Recomendaciones #5 (líneas ~779 — spec del pilot-report)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md §Architectural Responsibility Map (líneas ~17-27 — el reporter es post-processing, NO orquestador)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-PATTERNS.md §8 (líneas ~447-512 — shape específico run-validation-pilot.mjs)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md §decisions D-VAL-15 (4 must-haves del gate)
    - @src/data/validation-state.js (helper `deriveStatus` creado en Plan 09-01 — importable)
  </read_first>
  <action>
    Crear `scripts/run-validation-pilot.mjs` (~60 LOC, zero-deps, ES module estilo `validate-content-fixture.mjs`):

    Estructura del archivo:
    1. Shebang `#!/usr/bin/env node` + header docblock en español: justificación (gate D-VAL-15 del piloto Phase 9), uso (`node scripts/run-validation-pilot.mjs`), exit codes (0 = los 4 gates PASS; 1 = al menos 1 gate FAIL — el autor itera el prompt o ajusta antes de Phase 10).
    2. Imports zero-deps: `readFileSync` de `node:fs`, `fileURLToPath` de `node:url`, `dirname`, `resolve` de `node:path`.
    3. Import `deriveStatus` named de `../src/data/validation-state.js` (helper Plan 09-01).
    4. Constantes top-level con los 3 ejercicios del piloto:
       - `PILOT_EXERCISES`: array de 3 objetos con shape `{label, exerciseId, file, expectedStatus, expectedConcernPrefix?}`:
         - `{label: 'E1', exerciseId: 'preposiciones-040', file: 'content/exercises/preposiciones.json', expectedStatus: 'validated'}`
         - `{label: 'E2', exerciseId: 'avere-001', file: 'content/exercises/avere.json', expectedStatus: 'validated'}`
         - `{label: 'E3', exerciseId: 'pilot-disputed-c5-leak-001', file: 'tests/fixtures/validation-pilot-disputed.json', expectedStatus: 'disputed', expectedConcernPrefix: '[C5-'}`
    5. ANSI color helpers literales: `const GREEN = '\x1b[32m';`, `const RED = '\x1b[31m';`, `const RESET = '\x1b[0m';`.
    6. Función principal:
       a. Para cada `PILOT_EXERCISES[i]`: lee el JSON del `file`, busca el ejercicio por `id`, extrae `validation` (si ausente → marca como `MISSING`, gate FAIL).
       b. Aplica `deriveStatus(ex.validation.passes)` → `derivedStatus`.
       c. Compara `derivedStatus === expectedStatus` → boolean `statusOk`.
       d. Compara `ex.validation.status === derivedStatus` → boolean `consistencyOk` (defensa contra mutación manual D-VAL-07).
       e. Para E3: chequea que al menos 1 entry de `passes[]` tenga `verdict === 'incorrecta'` Y al menos un `concerns[i]` matchea prefix `[C5-` → boolean `concernsOk`.
       f. Chequea que NINGÚN `concerns[]` de ningún pase contenga el string `[meta] parse failed` (gate D-VAL-15 #4 — sin retries) → boolean `noRetriesOk`.
    7. Imprime tabla en español con columnas: `Label`, `Exercise ID`, `pass1.by/verdict`, `pass2.by/verdict`, `Derived status`, `Expected`, `Gate` (PASS verde / FAIL rojo). Usar `padEnd` para alineación, ASCII puro.
    8. Sección final "Gate D-VAL-15":
       - Imprime las 4 sub-gates con verde/rojo:
         - "1. E1 validated: PASS|FAIL"
         - "2. E2 validated: PASS|FAIL"
         - "3. E3 disputed + concerns [C5-...]: PASS|FAIL"
         - "4. Parsing limpio primer intento: PASS|FAIL"
    9. Exit code: 0 si los 4 gates PASS; 1 si CUALQUIERA FAIL. Mensaje final en español resumiendo: "Piloto PASS. Phase 10 autorizada." o "Piloto FAIL — itera Plan 09-01/09-02 antes de Phase 10. Detalle arriba.".

    Constraint clave: este script es POST-processing. NO invoca Task(), NO orquesta subagents — solo lee los JSONs que el skill ya mutó y aplica el gate. El skill `gsd-validate-exercise` (Plan 09-02) es quien hace los Task() spawn. El script solo verifica el resultado.

    Mensajes 100% en español (FOUND-04). Zero deps externas.
  </action>
  <verify>
    <automated>test -f scripts/run-validation-pilot.mjs && head -1 scripts/run-validation-pilot.mjs | grep -q "^#!/usr/bin/env node" && grep -q "import { deriveStatus }" scripts/run-validation-pilot.mjs && grep -q "preposiciones-040" scripts/run-validation-pilot.mjs && grep -q "avere-001" scripts/run-validation-pilot.mjs && grep -q "pilot-disputed-c5-leak-001" scripts/run-validation-pilot.mjs && grep -qE "D-VAL-15|gate" scripts/run-validation-pilot.mjs && grep -q "process.exit" scripts/run-validation-pilot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: archivo existe `scripts/run-validation-pilot.mjs` con shebang Node.
    - Source: `grep -q "import { deriveStatus }" scripts/run-validation-pilot.mjs` → exit 0 (reutiliza helper Plan 09-01).
    - Source: `grep -c "preposiciones-040\|avere-001\|pilot-disputed-c5-leak-001" scripts/run-validation-pilot.mjs` = 3 (los 3 ejercicios del piloto referenciados).
    - Source: `grep -qE "expectedStatus.*validated.*disputed|disputed.*validated" scripts/run-validation-pilot.mjs` (E1/E2 expected validated, E3 expected disputed).
    - Source: `grep -q "\[C5-" scripts/run-validation-pilot.mjs` (tag check para E3).
    - Source: `grep -q "parse failed" scripts/run-validation-pilot.mjs` (gate #4 verifica ausencia de retries).
    - Source: cero `npm install` mencionado — `! grep -q "npm install\|require('axios')\|@anthropic" scripts/run-validation-pilot.mjs`.
    - Behavior (pre-piloto): `node scripts/run-validation-pilot.mjs` exit 1 (los 3 ejercicios aún no tienen `validation` — gate FAIL esperado antes del Task 2).
    - Behavior (script funcional): el script imprime tabla y mensaje de gate en español sin throw — `node scripts/run-validation-pilot.mjs 2>&1 | head -30` muestra columnas "Label", "Exercise ID", "Gate", etc.
    - Source: mensajes en español — `grep -cE "Piloto|gate|PASS|FAIL|autorizada|itera" scripts/run-validation-pilot.mjs` ≥ 5.
  </acceptance_criteria>
  <done>
    `scripts/run-validation-pilot.mjs` existe, ~60 LOC, zero-deps. Importa `deriveStatus` de Plan 09-01. Define los 3 ejercicios del piloto con expectedStatus. Imprime tabla con ANSI colors + gate D-VAL-15 (4 sub-gates). Exit 0 si los 4 PASS, exit 1 si cualquiera FAIL. Mensajes en español.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ejecutar el piloto — 3 invocaciones del skill gsd-validate-exercise (3 commits atómicos)</name>
  <files>content/exercises/preposiciones.json, content/exercises/avere.json, tests/fixtures/validation-pilot-disputed.json</files>
  <read_first>
    - @.claude/skills/gsd-validate-exercise/SKILL.md (COMPLETO — el skill que el executor invoca; pasos 1-9 documentados)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md (COMPLETO — el prompt que el skill pasa al subagent)
    - @content/exercises/preposiciones.json líneas 471-482 (E1 actual sin `validation`)
    - @content/exercises/avere.json líneas 1-14 (E2 actual sin `validation`)
    - @tests/fixtures/validation-pilot-disputed.json (E3 actual sin `validation`)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md §decisions D-VAL-13 (los 3 ejercicios del piloto) + D-VAL-14 (E3 vive en tests/fixtures/) + D-VAL-15 (gate)
    - @.planning/phases/09-infraestructura-de-validaci-n/09-RESEARCH.md §Pattern 2 (commit granularity = 1 por ejercicio)
    - @scripts/assert-avere-prefix-unchanged.mjs (verificar tras E2 que sigue exit 0 — Pitfall 5)
  </read_first>
  <action>
    Ejecutar el piloto invocando el skill `gsd-validate-exercise` 3 veces (1 por ejercicio). Cada invocación produce 2 Task() spawns secuenciales (Opus + Sonnet) y termina con 1 commit atómico (D-VAL-04 + Pattern 2).

    **Subtask 2.1 — E1 = `preposiciones-040` (happy path real):**

    1. Invocar el skill: `gsd-validate-exercise preposiciones-040`.
    2. El skill resuelve a `content/exercises/preposiciones.json` index donde `id === 'preposiciones-040'`.
    3. Spawn Task #1 con `model: claude-opus-4-7` + VALIDATION-PROMPT.md + JSON de `preposiciones-040` SOLO.
    4. Aguarda Task #1 (serial), parsea JSON output.
    5. Spawn Task #2 con `model: claude-sonnet-4-6` + mismo prompt.
    6. Aguarda Task #2, parsea JSON output.
    7. Construye `validation.passes[]` = 2 entries `{by, date: today ISO, verdict, concerns: []}`.
    8. Aplica `deriveStatus(passes)` → expected `'validated'`.
    9. Mergea el campo `validation: {status, passes}` in-place al ejercicio en `content/exercises/preposiciones.json` (Edit tool — preservar todos los demás campos del ejercicio + de los 49 ejercicios vecinos en el archivo).
    10. `git add content/exercises/preposiciones.json` + `git commit -m "validate(preposiciones): preposiciones-040 → validated (Opus + Sonnet)\n\npasses[0]: claude-opus-4-7 — correcta\npasses[1]: claude-sonnet-4-6 — correcta\nconcerns: 0"`.

    **Subtask 2.2 — E2 = `avere-001` (baseline):**

    Análogo a 2.1 con:
    - Skill arg: `avere-001`
    - File: `content/exercises/avere.json` index 0 (primer ejercicio del archivo).
    - Expected: `validated`.
    - **Constraint Pitfall 5:** tras mergear `validation` a `avere-001`, EJECUTAR `node scripts/assert-avere-prefix-unchanged.mjs` y verificar exit 0. El relax del Plan 09-02 garantiza que `validation` está en la lista de campos aditivos excluidos del compare → el assert NO debe fallar. Si falla → STOP, revisar Plan 09-02 Task 3.2 (orden crítico).
    - Commit message: `validate(avere): avere-001 → validated (Opus + Sonnet)\n\npasses[0]: claude-opus-4-7 — correcta\npasses[1]: claude-sonnet-4-6 — correcta\nconcerns: 0`.

    **Subtask 2.3 — E3 = `pilot-disputed-c5-leak-001` (fixture disputed gate):**

    Análogo a 2.1 con:
    - Skill arg: `pilot-disputed-c5-leak-001`
    - File: `tests/fixtures/validation-pilot-disputed.json` index 0.
    - Expected: `disputed` con ≥1 entry `verdict: 'incorrecta'` Y `concerns[]` contiene un string que matchea `/^\[C5-/`.
    - El subagent (al evaluar el prompt `"Una casa, due ___ (refuerzo regla §1 fem -a→-e)."`) DEBE detectar el C5-leak (`(refuerzo regla §1 fem -a→-e)` viola R1 verbatim). Tag esperado en `concerns[]`: `"[C5-leak] el prompt contiene '(refuerzo regla §1 fem -a→-e)'"` o similar.
    - Commit message: `validate(pilot-fixture): pilot-disputed-c5-leak-001 → disputed (Opus + Sonnet, C5-leak detectado)\n\npasses[0]: claude-opus-4-7 — <verdict>\npasses[1]: claude-sonnet-4-6 — <verdict>\nconcerns: <count + ej. \"[C5-leak] ...\">`.

    **Constraint #4 del gate D-VAL-15 — parsing limpio primer intento:**

    Durante las 6 invocaciones Task() spawn (3 ejercicios × 2 pases), si CUALQUIER subagent devuelve JSON malformado y el skill ejerce el retry budget = 1, MARCAR como gate FAIL #4. Cómo: el skill añade un campo opcional `concerns[i]: "[meta] parse retry"` cuando el primer intento falla pero el 2º éxito. El reporter Task 1 detecta esto. Si esto ocurre en el piloto: STOP, ajusta el VALIDATION-PROMPT.md (Plan 09-02 Task 1) y re-ejecuta. NO sigas a Phase 10 con prompt malformado-prone.

    **Orden de subtasks:** 2.1, 2.2, 2.3 son INDEPENDIENTES (mutan archivos distintos) — pueden ejecutarse en cualquier orden, pero secuencialmente (NO paralelo, para mantener atomicidad de commits). Recomendado: en el orden 2.1 → 2.2 → 2.3 (preposiciones primero por ser el motivador, avere segundo porque ejerce el relax D-178/D-VAL-08, fixture último porque cierra el gate disputed).
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); const e=p.exercises.find(x=>x.id==='preposiciones-040'); if(!e.validation || e.validation.passes.length<2) process.exit(1); console.log('E1 passes:', e.validation.passes.length, 'status:', e.validation.status)" && node -e "const a=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); const e=a.exercises[0]; if(!e.validation || e.validation.passes.length<2) process.exit(1); console.log('E2 passes:', e.validation.passes.length, 'status:', e.validation.status)" && node -e "const f=JSON.parse(require('fs').readFileSync('tests/fixtures/validation-pilot-disputed.json','utf8')); const e=f.exercises[0]; if(!e.validation || e.validation.passes.length<2) process.exit(1); if(e.validation.status!=='disputed') process.exit(2); console.log('E3 status:', e.validation.status)" && node scripts/assert-avere-prefix-unchanged.mjs && node scripts/validate-content-fixture.mjs avere content/exercises/avere.json && node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json && node --test tests/exercise-types.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source (E1): `preposiciones-040` en `content/exercises/preposiciones.json` tiene campo `validation` con `passes.length === 2` y `by` distintos `claude-opus-4-7` + `claude-sonnet-4-6`. Verificable con `node -e` oneliner.
    - Source (E2): `avere-001` en `content/exercises/avere.json` tiene `validation.passes.length === 2` con `by` distintos.
    - Source (E3): `pilot-disputed-c5-leak-001` en `tests/fixtures/validation-pilot-disputed.json` tiene `validation.passes.length === 2`, `validation.status === 'disputed'`, y ≥1 entry con `verdict: 'incorrecta'` cuyo `concerns[]` contiene un string que matchea `/^\[C5-/`.
    - Behavior (E1+E2 status): `deriveStatus(e.validation.passes) === 'validated'` para ambos.
    - Behavior (E3 status): `deriveStatus(e.validation.passes) === 'disputed'` (sticky D-VAL-07).
    - Behavior (assert): `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 tras E2 (el relax Plan 09-02 funciona — `validation` excluido del compare).
    - Behavior (schema): `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0 (E2 valida shape). Mismo para preposiciones.
    - Behavior (tests): `node --test tests/*.test.js` (sin VAL_07_STRICT) sigue verde — el campo `validation` añadido cumple el shape del schema-validator.
    - Behavior (gate #4): cero entries en `validation.passes[*].concerns` con tag `[meta] parse failed` o `[meta] parse retry` en NINGUNO de los 3 ejercicios.
    - Source (commits): `git log --oneline -3` muestra los 3 commits atómicos con prefijo `validate(`.
    - Source (audit trail): cada `passes[i].date` matchea formato ISO `YYYY-MM-DD`; cada `passes[i].by` ∈ `{"claude-opus-4-7", "claude-sonnet-4-6"}`.
  </acceptance_criteria>
  <done>
    Los 3 ejercicios del piloto tienen `validation.passes[]` poblado con 2 entries cada uno (`by` distintos Opus + Sonnet, `date` ISO, `verdict` whitelist, `concerns[]` opcional). E1 + E2 son `validated`; E3 es `disputed` con concerns tagged `[C5-...]`. Los 3 outputs parsearon limpio en primer intento (cero retries). 3 commits atómicos en `git log`. `assert-avere-prefix-unchanged.mjs` exit 0. Schema validator acepta los 3. Suite baseline sigue verde.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate D-VAL-15 — autor revisa tabla del piloto y autoriza Phase 10 (o pide iteración)</name>
  <what-built>
    Plan 09-03 ha ejecutado el piloto end-to-end: 3 ejercicios validados por quórum Opus + Sonnet (6 invocaciones Task() en serie), con `validation.passes[]` poblado en cada uno y 3 commits atómicos. El reporter `scripts/run-validation-pilot.mjs` está listo para imprimir la tabla + gate.
  </what-built>
  <how-to-verify>
    El autor (en su shell) ejecuta:

    1. `node scripts/run-validation-pilot.mjs`

       Inspecciona la tabla impresa:
       - Columna `Label`: E1, E2, E3
       - Columna `Exercise ID`: preposiciones-040, avere-001, pilot-disputed-c5-leak-001
       - Columnas `pass1.by/verdict` y `pass2.by/verdict`: deben mostrar `claude-opus-4-7/correcta` y `claude-sonnet-4-6/correcta` para E1+E2, y para E3 al menos uno debe ser `incorrecta`.
       - Columna `Derived status`: `validated` para E1+E2, `disputed` para E3.
       - Columna `Gate`: 3× verde PASS.

    2. Inspecciona la sección "Gate D-VAL-15":
       - "1. E1 validated: PASS (verde)"
       - "2. E2 validated: PASS"
       - "3. E3 disputed + concerns [C5-...]: PASS"
       - "4. Parsing limpio primer intento: PASS"

    3. Si exit code = 0 + los 4 sub-gates en verde: **autoriza Phase 10** respondiendo con `approved` o `phase 10 go`.

    4. Si CUALQUIER sub-gate FAIL:
       - Lee la tabla — qué pase devolvió qué.
       - Lee `git log --oneline -3` para ver los commits.
       - Inspecciona los `concerns[]` de los `passes` que fallaron (debugging visible — el subagent emitió `[Cn-criterio]` tagged).
       - Decide:
         - Si el VALIDATION-PROMPT.md tiene un bug (ej. few-shot bias hacia FAIL → E2 sale disputed por sesgo) → vuelve a Plan 09-02 Task 1, ajusta el prompt, re-ejecuta este Plan 03 Task 2.
         - Si el SKILL.md tiene un bug (ej. retry budget no se respeta, model ID typo) → vuelve a Plan 09-02 Task 2.
         - Si el shape del JSON output del subagent no parsea (gate #4 FAIL) → ajusta la regex extractora o la sección 4 del prompt; re-ejecuta.

    5. Adicionalmente: inspecciona manualmente los 3 ejercicios mutados con `cat content/exercises/preposiciones.json | jq '.exercises[] | select(.id=="preposiciones-040") | .validation'` (idem para avere y fixture) — verifica que el JSON es legible, fechas ISO razonables, `by` y `verdict` consistentes con la tabla del reporter.

    Decisión del autor:
    - **`approved`** o **`phase 10 go`**: el gate D-VAL-15 está cleared. Plan 03 completa. El operador procede a `/clear` + `/gsd:plan-phase 10`.
    - **`iterate prompt`**: hay un sesgo del prompt que el piloto detecta — vuelve a Plan 09-02 Task 1 con un ajuste específico (autor describe qué cambiar). NO se cierra Phase 9.
    - **`iterate skill`**: hay un bug del orquestador (parsing, retry, commit) — vuelve a Plan 09-02 Task 2. NO se cierra Phase 9.
    - **`stop`**: el autor decide pausar — Phase 10 queda blocked pendiente.

    Mensajes en español (FOUND-04) en el output del reporter.
  </how-to-verify>
  <resume-signal>
    Responde con:
    - `approved` o `phase 10 go` → Plan 03 PASS, Phase 9 close, proceder a `/gsd:plan-phase 10`.
    - `iterate prompt: <descripción del ajuste>` → vuelta a Plan 09-02 Task 1 con feedback explícito.
    - `iterate skill: <descripción>` → vuelta a Plan 09-02 Task 2.
    - `stop` → pausar, Phase 10 blocked.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→content JSON files | El skill muta `content/exercises/*.json` in-place (E1, E2) y `tests/fixtures/*.json` (E3). El parent (executor) es single-threaded; cero race conditions cross-write. |
| subagent output→passes[] | El JSON parseado del subagent se inserta en `passes[]`. Si el subagent emite JSON con `verdict: "tampered"` (fuera de whitelist), `validateValidationShape` (Plan 09-01) lo rechaza en el siguiente boot — defensa en profundidad. |
| commit→git history | Los 3 commits atómicos quedan en el historial local. Pre-commit hooks (si existen) corren — el plan asume cero hooks que fallen sobre los archivos JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-T1 | Tampering | Subagent emite `verdict: "correcta"` cuando debería ser "incorrecta" en E3 (false positive — el C5-leak pasa desapercibido) | mitigate | Quórum ≥2 modelos: ambos Opus y Sonnet deben coincidir en `correcta` para que `deriveStatus` devuelva `validated`. Sticky disputed: si UNO solo dice `incorrecta`, gate FAIL #3 → autor itera. Adicionalmente: el fixture E3 es deliberadamente C5-leak literal — su detección es trivial para cualquier modelo competente; un false positive aquí indica problema sistémico (autor itera prompt antes de Phase 10). |
| T-09-03-T2 | Tampering | Race condition al escribir 3 archivos JSON in-place | accept | El executor (Claude Code parent) es single-threaded; las 3 invocaciones del skill son SECUENCIALES (D-VAL-04 + Task 2 orden explícito). Cero concurrent writes. |
| T-09-03-T3 | Tampering | Commit mensaje fabricado que no refleja los pases reales | mitigate | El reporter `run-validation-pilot.mjs` lee los JSONs YA mutados (no los commit messages). El gate inspecciona el CAMPO `validation.passes[]` real — un commit message engañoso no escapa al check programático. |
| T-09-03-D1 | Denial of Service | Una invocación Task() cuelga indefinidamente | accept | Phase 9 piloto = 3 ejercicios; latencia esperada ~30-60s por ejercicio (Opus + Sonnet en serie). Si CUALQUIER pase cuelga >5 min, el autor pulsa Ctrl-C → re-ejecuta el subtask específico (commits atómicos permiten resume idempotente). |
| T-09-03-T4 | Tampering | `assert-avere-prefix-unchanged.mjs` falla tras añadir `validation` a `avere-001` (relax Plan 09-02 mal aplicado) | mitigate | Task 2 Subtask 2.2 incluye verificación explícita post-mutación: `node scripts/assert-avere-prefix-unchanged.mjs` debe exit 0 ANTES de continuar. Si falla → STOP y vuelve al Plan 09-02 Task 3.2 antes de re-intentar. |
| T-09-03-I1 | Information Disclosure | Los `concerns[]` se commitean a git con razonamiento del subagent (chain-of-thought partial) | accept | Editorial-pedagogical content sin secretos. RESEARCH §Security Domain confirma. El autor revisa el commit mensaje. |
| T-09-03-SC | Tampering | npm/pip/cargo installs en el reporter | mitigate | `run-validation-pilot.mjs` es zero-deps por construcción (verificable con `! grep -q "npm install\|@anthropic" scripts/run-validation-pilot.mjs`). Cero packages. |

No `high`-severity threats. Phase 9 plan 03 passes gate.
</threat_model>

<verification>
**Gate D-VAL-15 (4 must-haves observables) — los 4 deben PASS para autorizar Phase 10:**

1. **E1 validated:** `node -e "const p=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); const e=p.exercises.find(x=>x.id==='preposiciones-040'); console.log(e.validation.status === 'validated' && e.validation.passes.length === 2 && new Set(e.validation.passes.map(p=>p.by)).size === 2 ? 'PASS' : 'FAIL')"` debe imprimir `PASS`.

2. **E2 validated:** mismo check sobre `avere-001` en `content/exercises/avere.json` (index 0). Adicionalmente: `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (relax funciona).

3. **E3 disputed con [C5-...]:** `node -e "const f=JSON.parse(require('fs').readFileSync('tests/fixtures/validation-pilot-disputed.json','utf8')); const e=f.exercises[0]; const hasIncorrecta = e.validation.passes.some(p => p.verdict === 'incorrecta'); const hasC5Tag = e.validation.passes.some(p => (p.concerns||[]).some(c => /^\[C5-/.test(c))); console.log(e.validation.status === 'disputed' && hasIncorrecta && hasC5Tag ? 'PASS' : 'FAIL')"` debe imprimir `PASS`.

4. **Parsing limpio primer intento:** `node -e "const files=['content/exercises/preposiciones.json','content/exercises/avere.json','tests/fixtures/validation-pilot-disputed.json']; const allConcerns=files.flatMap(f=>{const d=JSON.parse(require('fs').readFileSync(f,'utf8'));return d.exercises.flatMap(ex=>(ex.validation?.passes||[]).flatMap(p=>p.concerns||[]))}); const hasMetaFail = allConcerns.some(c=>/parse failed|parse retry/.test(c)); console.log(hasMetaFail ? 'FAIL' : 'PASS')"` debe imprimir `PASS`.

**Reporter ejecutable:**
- `node scripts/run-validation-pilot.mjs` exit 0 cuando los 4 gates pasan.

**Backward-compat:**
- `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exit 0 (el shape `validation` añadido pasa el schema Plan 09-01).
- `node scripts/validate-content-fixture.mjs avere content/exercises/avere.json` exit 0.
- `node --test tests/*.test.js` (sin VAL_07_STRICT): suite baseline + nuevos siguen verdes.

**Audit trail commits:**
- `git log --oneline -3` muestra: 3 commits con prefijo `validate(...)` y los exercise IDs en el subject.

**Cero dependencias nuevas:**
- `git status -s package.json package-lock.json` vacío.
</verification>

<success_criteria>
- 3 ejercicios del piloto (E1 preposiciones-040, E2 avere-001, E3 pilot-disputed-c5-leak-001) tienen `validation.passes[]` con 2 entries Opus + Sonnet (per D-VAL-13).
- E1 + E2 status `validated`, E3 status `disputed` con `concerns[]` tagged `[C5-...]` (per D-VAL-15 #1/#2/#3).
- Los 3 outputs JSON parsearon en primer intento — cero `[meta] parse failed/retry` en `concerns[]` (per D-VAL-15 #4).
- 3 commits atómicos con template `validate(<cat>): <id> → <status> (Opus + Sonnet)` (per D-VAL-04 + Pattern 2).
- `scripts/run-validation-pilot.mjs` corre, imprime tabla + gate en español, exit 0 si los 4 PASS.
- `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (relax Plan 09-02 funciona post-mutación).
- Baseline tests + nuevos del Plan 09-01 siguen verdes.
- Autor autoriza Phase 10 vía checkpoint Task 3 (`approved` o `phase 10 go`).
</success_criteria>

<output>
Crear `.planning/phases/09-infraestructura-de-validaci-n/09-03-SUMMARY.md` cuando termine, documentando:
- Tabla literal del piloto (3 filas — labels, IDs, pases, status, gates)
- Los 3 commit SHAs atómicos
- Decisión del autor en el checkpoint (approved / iterate prompt / iterate skill / stop)
- Si `approved`: confirmación de que Phase 10 está autorizada
- Si `iterate*`: descripción del ajuste pedido + qué Plan/Task se reabre
- Cualquier `concerns[]` poblado que sea interesante para Phase 10 (ej. tag-class más frecuente, para informar el orden de validación de las 7 categorías)
</output>
