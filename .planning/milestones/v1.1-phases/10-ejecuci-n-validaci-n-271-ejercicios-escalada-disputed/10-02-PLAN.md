---
phase: 10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run-validation-271.mjs
autonomous: true
requirements:
  - VAL-04
  - VAL-06
must_haves:
  truths:
    - "Existe `scripts/run-validation-271.mjs` ejecutable con `node` (sin `npm install`)."
    - "El reporter lee los 7 archivos `content/exercises/<cat>.json` (orden D-VAL-22) y aplica `deriveStatus` sobre cada `validation.passes[]`."
    - "El reporter computa 3 sub-gates: VAL-04 (≥2 distinct `by` con verdict correcta entre los validated), VAL-06 (271/271 status=validated), VAL-08 (cero disputed sin override path-B)."
    - "El reporter implementa `effectiveStatus(passes)` que relaja consistency check ante override path-B: si `deriveStatus === 'disputed'` PERO existe entry `{by:'autor', verdict:'correcta'}` → treat as validated (RESEARCH Open Q #1 opción c)."
    - "El reporter exit 0 cuando los 3 sub-gates PASS; exit 1 en cualquier otro caso."
    - "El reporter imprime tabla colorizada ANSI zero-deps (verde PASS / rojo FAIL / amarillo warn) coherente con `run-validation-pilot.mjs`."
    - "El reporter, en PASS, imprime literalmente el comando manual `VAL_07_STRICT=1 node --test tests/*.test.js` como siguiente paso (gesto consciente del autor, NO auto-flip)."
    - "Cero dependencias añadidas — el script es 100% Node builtins (`node:fs`, `node:path`, `node:url`) + import del módulo puro `validation-state.js`."
  artifacts:
    - path: "scripts/run-validation-271.mjs"
      provides: "Reporter milestone v1.1 — gate Phase 10 (VAL-04 + VAL-06 + VAL-08)"
      min_lines: 120
      contains:
        - "deriveStatus"
        - "effectiveStatus"
        - "VAL-04"
        - "VAL-06"
        - "VAL-08"
        - "preposiciones"
        - "avere"
        - "essere"
        - "genero-numero"
        - "profesiones"
        - "sustantivos-irregulares"
        - "verbos-movimiento"
        - "VAL_07_STRICT"
  key_links:
    - from: "scripts/run-validation-271.mjs"
      to: "src/data/validation-state.js"
      via: "import deriveStatus"
      pattern: "import.*deriveStatus.*validation-state"
    - from: "scripts/run-validation-271.mjs"
      to: "content/exercises/<cat>.json"
      via: "readFileSync + JSON.parse de los 7 archivos"
      pattern: "content/exercises"
---

<objective>
Crear `scripts/run-validation-271.mjs`, el reporter POST-processing puro del milestone v1.1 que verifica los 3 sub-gates VAL-04 + VAL-06 + VAL-08 sobre los 271 ejercicios. Es el gate del milestone: exit 0 = autor procede al smoke test estricto y `/gsd:complete-milestone v1.1`; exit 1 = volver al sub-skill batch para cerrar pendientes/disputed.

Purpose: VAL-04 (≥2 AIs distintos por ejercicio validated) + VAL-06 (271/271 status=validated) verificables sin abrir manualmente los 7 archivos. Sirve también como verificación intermedia tras cada categoría (el sub-skill batch lo recomienda al final del scope).

Output: `scripts/run-validation-271.mjs` (~120-180 LOC, zero-deps, ANSI colorizado, exit 0/1 por gate). Clon estructural de `run-validation-pilot.mjs` con scope diferente (7 categorías vs 3 IDs hardcoded) y 3 sub-gates vs 4 (el VAL-07 smoke test queda como paso manual separado per RESEARCH Q6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-PATTERNS.md
@scripts/run-validation-pilot.mjs
@src/data/validation-state.js

<interfaces>
<!-- Contratos clave. -->

From `src/data/validation-state.js`:
- `export function deriveStatus(passes): "pending" | "validated" | "disputed"`
- Sticky: cualquier `incorrecta` → `disputed`; ≥2 `correcta` con `by` distintos → `validated`.

From `scripts/run-validation-pilot.mjs` (TEMPLATE — clonar estructura):
- Línea 29-37: imports zero-deps (`node:fs`, `node:url`, `node:path`) + `deriveStatus`.
- Línea 39-48: ANSI helpers (`GREEN`, `RED`, `YELLOW`, `BOLD`, `RESET`, `ok`, `fail`, `warn`).
- Línea 51-71: array `PILOT_EXERCISES` (3 entries) — Phase 10 sustituye por `CATEGORIES` (7 entries).
- Línea 73-90: `loadExercise(file, exerciseId)` — devuelve `{found, ex}` o `{found, error}` defensive sin throws.
- Línea 92-105: `passSummary(p)` + `hasParseMeta(concerns)` helpers.
- Línea 191-249: tabla padded con colWidths + headerRow + ANSI markers.
- Línea 251-285: sub-gates aggregation + `subGates.push({id, label, pass})`.
- Línea 287-301: exit gate con mensaje sugerido en PASS / accionable en FAIL.

Datos esperados de cada `content/exercises/<cat>.json`:
- Shape: `{ exercises: [{id, type, categoryIds, payload, notes?, validation?: {status, passes:[{by,date,verdict,concerns?}]}}, ...] }`.
- Para los 271 ejercicios al cierre: `validation.status === "validated"` en todos.

Orden lockeado D-VAL-22 (con expected counts):
```javascript
const CATEGORIES = [
  { slug: 'preposiciones', file: 'content/exercises/preposiciones.json', expected: 50 },
  { slug: 'avere', file: 'content/exercises/avere.json', expected: 23 },
  { slug: 'essere', file: 'content/exercises/essere.json', expected: 39 },
  { slug: 'genero-numero', file: 'content/exercises/genero-numero.json', expected: 40 },
  { slug: 'profesiones', file: 'content/exercises/profesiones.json', expected: 51 },
  { slug: 'sustantivos-irregulares', file: 'content/exercises/sustantivos-irregulares.json', expected: 31 },
  { slug: 'verbos-movimiento', file: 'content/exercises/verbos-movimiento.json', expected: 37 }
];
// Total: 271.
```

Helper `effectiveStatus` (RESEARCH PATTERNS.md líneas 320-329 + Open Q #1 opción c):
```javascript
function effectiveStatus(passes) {
  const derived = deriveStatus(passes);
  if (derived !== 'disputed') return derived;
  const hasAuthorOverride = passes.some(
    (p) => p?.by === 'autor' && p?.verdict === 'correcta'
  );
  return hasAuthorOverride ? 'validated' : 'disputed';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear `scripts/run-validation-271.mjs` con 7 categorías y 3 sub-gates</name>
  <files>scripts/run-validation-271.mjs</files>
  <read_first>
    - `scripts/run-validation-pilot.mjs` (TEMPLATE estructural — clonar imports + ANSI helpers + `loadExercise` + tabla padded + sub-gates aggregation + exit gate)
    - `src/data/validation-state.js` (entender `deriveStatus` sticky D-VAL-07 para implementar `effectiveStatus` correctamente)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md` (Q5 esqueleto completo del reporter + Open Q #1 sobre relax path-B)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-PATTERNS.md` (sección `scripts/run-validation-271.mjs` con adaptaciones específicas + helper `effectiveStatus`)
    - `CLAUDE.md` (zero-deps invariant, FOUND-04 español)
  </read_first>
  <action>
    Crear `scripts/run-validation-271.mjs` con estructura clonada de `run-validation-pilot.mjs`, adaptando para 7 categorías y 3 sub-gates. NO incluir fenced code blocks aquí — las firmas concretas vienen de los read_first.

    **Header comment (1-30 líneas):**
    - Shebang `#!/usr/bin/env node`.
    - Comentario explicando: reporter del milestone v1.1 Phase 10, post-processing puro, NO invoca Task(), NO muta JSONs.
    - Lista los 3 sub-gates: VAL-04 (≥2 distinct `by` con verdict `correcta` en los validated), VAL-06 (271/271 con `status === "validated"`), VAL-08 (cero IDs `disputed` sin override path-B).
    - Exit codes: 0 = los 3 PASS, 1 = al menos 1 FAIL.
    - Justificación de NO shellear `node --test` (per RESEARCH Q5: separación de responsabilidades — el smoke test VAL-07 es paso manual separado).

    **Imports zero-deps:**
    - `readFileSync` de `node:fs`.
    - `fileURLToPath` de `node:url`.
    - `dirname`, `resolve` de `node:path`.
    - `deriveStatus` desde `../src/data/validation-state.js`.
    - Setup `__filename`, `__dirname`, `projectRoot` exactamente como `run-validation-pilot.mjs` líneas 35-37.

    **ANSI helpers zero-deps** (clonar literal de `run-validation-pilot.mjs` líneas 40-48): `GREEN`, `RED`, `YELLOW`, `BOLD`, `RESET`, `ok`, `fail`, `warn`.

    **Array `CATEGORIES`** (constante module-level, 7 entries en orden D-VAL-22 con campos `slug`, `file`, `expected`):
    - preposiciones / `content/exercises/preposiciones.json` / 50
    - avere / `content/exercises/avere.json` / 23
    - essere / `content/exercises/essere.json` / 39
    - genero-numero / `content/exercises/genero-numero.json` / 40
    - profesiones / `content/exercises/profesiones.json` / 51
    - sustantivos-irregulares / `content/exercises/sustantivos-irregulares.json` / 31
    - verbos-movimiento / `content/exercises/verbos-movimiento.json` / 37
    - Suma `expected` debe ser 271.

    **Helper `effectiveStatus(passes)`** (función pura zero-deps):
    - Llama `deriveStatus(passes)`.
    - Si el resultado NO es `"disputed"`, retorna el resultado.
    - Si es `"disputed"`, busca en `passes` una entry con `p?.by === "autor"` AND `p?.verdict === "correcta"`. Si la encuentra, retorna `"validated"`; si no, retorna `"disputed"`.
    - Razón: implementa relax path-B (RESEARCH Open Q #1 opción c) — el override del autor escribe `validation.status = "validated"` directamente, pero `deriveStatus` ve la incorrecta histórica y diría disputed; este helper lo trata correctamente como validated.

    **Helper defensive `loadCategory(file)`:**
    - Lee el archivo con try/catch JSON.parse.
    - Devuelve `{ok: true, exercises: data.exercises}` o `{ok: false, error: string}`.
    - NO throws.

    **Iteración por categoría** (loop sobre `CATEGORIES`):
    - Por cada `{slug, file, expected}`: `loadCategory` → para cada ejercicio del array:
      - Contadores: `validated`, `disputed`, `pending`, `missing` (ejercicio sin campo `validation` o `passes` no-array).
      - Lista `disputedIds` (push id si `effectiveStatus === "disputed"`).
      - Lista `missingMultiPassIds` (para VAL-04): si el ejercicio está `validated` pero `new Set(passes.filter(p => p?.verdict === "correcta").map(p => p?.by).filter(Boolean)).size < 2` → push id.
      - Lista `inconsistencyIds` (warning, no FAIL): si `validation.status !== effectiveStatus(validation.passes)` → push id (puede pasar si edición manual desincroniza el status escrito; flag para autor).
    - Acumular `perCategory[]` con `{slug, expected, total, validated, disputed, pending, missing, disputedIds, missingMultiPassIds, inconsistencyIds}`.

    **Tabla colorizada** (estilo `run-validation-pilot.mjs` líneas 191-249):
    - Header: `Milestone v1.1 — gate Phase 10 (VAL-04 + VAL-06 + VAL-08)` en BOLD.
    - colWidths para columnas: `Categoría` (24), `Total` (8), `Validated` (10), `Disputed` (9), `Pending` (8), `Missing` (8).
    - Header row + separator `-`.repeat(headerRow.length).
    - Por cada `perCategory[]` entry: una fila con valores padded.
    - Si `r.disputed > 0`: línea adicional `warn("→ Disputed IDs: " + r.disputedIds.join(", "))`.
    - Si `r.missing > 0`: línea adicional `warn("→ Missing validation: " + r.missing + " ejercicios")`.
    - Si `r.inconsistencyIds.length > 0`: línea adicional `warn("→ Inconsistencia status escrito vs derived: " + r.inconsistencyIds.join(", "))`.

    **Sub-gates** (clonar estructura `run-validation-pilot.mjs` líneas 251-285):
    - Computar `totalValidated`, `totalDisputed`, `totalPending`, `totalMissing`, `totalActual` (sumas sobre `perCategory`).
    - Header `Sub-gates:` en BOLD.
    - **VAL-06 sub-gate:** `val06Pass = totalValidated === 271 && totalActual === 271`. Imprimir `VAL-06 (271/271 validated): PASS|FAIL (X/271)`.
    - **VAL-08 sub-gate:** `val08Pass = totalDisputed === 0`. Imprimir `VAL-08 (cero disputed): PASS|FAIL (N disputed: id1, id2, ...)` con la lista flat de `disputedIds` de todas las categorías.
    - **VAL-04 sub-gate:** `val04Pass = sum(missingMultiPassIds) === 0`. Imprimir `VAL-04 (≥2 distinct AIs por validated): PASS|FAIL (N IDs sin ≥2 distinct by: id1, id2, ...)`.
    - `gatePass = val06Pass && val08Pass && val04Pass`.

    **Exit gate** (clonar `run-validation-pilot.mjs` líneas 287-301 con adaptaciones):
    - Si `gatePass`:
      - `console.log(ok(BOLD + "Milestone gate PASS." + RESET))`.
      - Línea en blanco.
      - "Siguiente paso (manual, gesto consciente del autor):" en BOLD.
      - Literal `  VAL_07_STRICT=1 node --test tests/*.test.js` (con dos espacios de indent).
      - "  → verifica smoke test paramétrico exit 0."
      - "  → si OK: /gsd:complete-milestone v1.1"
      - `process.exit(0)`.
    - Si NO `gatePass`:
      - `console.log(fail(BOLD + "Milestone gate FAIL — itera /gsd-validate-batch antes de cerrar." + RESET))`.
      - Línea en blanco.
      - "Acciones sugeridas según qué sub-gate falla:" en español.
      - Si `!val06Pass`: `  - VAL-06: ejecuta /gsd-validate-batch --all-pending para procesar los pendientes/missing.`
      - Si `!val08Pass`: `  - VAL-08: ejecuta /gsd-validate-batch <id1,id2,...> sobre los IDs disputed listados arriba.`
      - Si `!val04Pass`: `  - VAL-04: investiga manualmente los IDs sin ≥2 distinct by (probable corrupción del JSON o passes[] con by duplicado).`
      - `process.exit(1)`.

    **NO añadir** ningún `package.json`, ningún `npm install`, ningún spawn `Task()`, ningún shell-out a `node --test`. Reporter es 100% post-processing puro.
  </action>
  <verify>
    <automated>test -f scripts/run-validation-271.mjs &amp;&amp; head -5 scripts/run-validation-271.mjs | grep -q "node" &amp;&amp; grep -q "deriveStatus" scripts/run-validation-271.mjs &amp;&amp; grep -q "effectiveStatus" scripts/run-validation-271.mjs &amp;&amp; grep -q "VAL-04" scripts/run-validation-271.mjs &amp;&amp; grep -q "VAL-06" scripts/run-validation-271.mjs &amp;&amp; grep -q "VAL-08" scripts/run-validation-271.mjs &amp;&amp; grep -q "preposiciones" scripts/run-validation-271.mjs &amp;&amp; grep -q "avere" scripts/run-validation-271.mjs &amp;&amp; grep -q "essere" scripts/run-validation-271.mjs &amp;&amp; grep -q "genero-numero" scripts/run-validation-271.mjs &amp;&amp; grep -q "profesiones" scripts/run-validation-271.mjs &amp;&amp; grep -q "sustantivos-irregulares" scripts/run-validation-271.mjs &amp;&amp; grep -q "verbos-movimiento" scripts/run-validation-271.mjs &amp;&amp; grep -q "VAL_07_STRICT" scripts/run-validation-271.mjs &amp;&amp; grep -q 'by === .autor.' scripts/run-validation-271.mjs &amp;&amp; ! grep -q "child_process" scripts/run-validation-271.mjs &amp;&amp; ! grep -q "npm install" scripts/run-validation-271.mjs &amp;&amp; node -c scripts/run-validation-271.mjs 2&gt;/dev/null || node --check scripts/run-validation-271.mjs &amp;&amp; node scripts/run-validation-271.mjs; EXIT=$?; [ $EXIT -eq 0 ] || [ $EXIT -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `scripts/run-validation-271.mjs` existe, comienza con shebang `#!/usr/bin/env node`, y `node --check scripts/run-validation-271.mjs` pasa sin errores de sintaxis.
    - El reporter contiene un array module-level `CATEGORIES` con 7 entries; cada entry tiene `slug`, `file`, `expected`; los slugs incluyen los 7 valores D-VAL-22 en orden lockeado; la suma de `expected` es 271.
    - El reporter importa `deriveStatus` de `../src/data/validation-state.js`.
    - El reporter define una función `effectiveStatus(passes)` que retorna `"validated"` cuando `deriveStatus` da `"disputed"` y existe entry `by === "autor"` con `verdict === "correcta"`.
    - El reporter computa los 3 sub-gates VAL-04, VAL-06, VAL-08 (verificable: las cadenas `VAL-04`, `VAL-06`, `VAL-08` aparecen en el script).
    - El reporter imprime el comando literal `VAL_07_STRICT=1 node --test tests/*.test.js` en el path PASS.
    - El reporter es zero-deps: NO contiene `child_process`, NO contiene `npm install`, NO contiene `require('axios')` u otros imports externos.
    - Ejecución `node scripts/run-validation-271.mjs` en el estado actual (antes del batch) sale con exit 1 (gate FAIL — los 269 ejercicios pendientes están sin `validation` field), pero la ejecución NO crashea (la rama defensive `missing` lo maneja).
    - Tabla colorizada con headers en español (Categoría, Total, Validated, Disputed, Pending, Missing).
    - Mensajes en español: `Milestone gate PASS.` / `Milestone gate FAIL — itera /gsd-validate-batch antes de cerrar.` / `Acciones sugeridas según qué sub-gate falla:`.
  </acceptance_criteria>
  <done>
    `scripts/run-validation-271.mjs` ejecutable con `node`, cubre los 7 archivos de categoría en orden D-VAL-22, los 3 sub-gates VAL-04/VAL-06/VAL-08, el relax path-B vía `effectiveStatus`, tabla colorizada en español, mensaje de PASS con comando literal `VAL_07_STRICT=1 node --test tests/*.test.js`, zero-deps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk→reporter | El reporter lee los 7 JSONs de `content/exercises/*.json`. Si un JSON está corrupto (parse error), el helper `loadCategory` devuelve `{ok:false}` defensive — NO throws. |
| reporter→stdout | El reporter solo imprime; no muta archivos, no spawnea procesos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | reporter muta JSONs accidentalmente | mitigate | El reporter es POST-processing PURO — solo lee. Verify check: NO contiene `writeFileSync` ni `writeFile`. |
| T-10-02-02 | Denial of Service | JSON corrupto crashea el reporter | mitigate | Helper `loadCategory` con try/catch; devuelve `{ok:false, error}` y continúa. El gate falla por `missing > 0` (FAIL legítimo). |
| T-10-02-03 | Information Disclosure | reporter expone IDs disputed | accept | Datos públicos del autor sobre sus propios ejercicios. Single-user, local. |
| T-10-02-04 | Spoofing | entry malformada en `passes[]` | accept | `deriveStatus` y `effectiveStatus` usan defensive reads (`p?.by`, `p?.verdict`). Pase malformado se ignora — el ejercicio queda como `pending`, surface por `pending > 0`. |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | NO HAY package installs en este plan — el script es 100% Node builtins + import de módulo del proyecto. Zero-deps. |
</threat_model>

<verification>
1. `node --check scripts/run-validation-271.mjs` exit 0 (sintaxis válida).
2. `node scripts/run-validation-271.mjs` ejecuta sin crashear; en el estado pre-batch (los 269 sin `validation` field) sale con exit 1 — la rama defensive `missing` funciona.
3. La salida muestra tabla colorizada con 7 filas (una por categoría) y 6 columnas (Categoría/Total/Validated/Disputed/Pending/Missing).
4. La salida muestra 3 sub-gates etiquetados VAL-04, VAL-06, VAL-08.
5. En PASS, la salida contiene `VAL_07_STRICT=1 node --test tests/*.test.js` literal.
6. El script NO contiene `child_process`, NO contiene `writeFileSync`, NO contiene `npm install`.
</verification>

<success_criteria>
- `scripts/run-validation-271.mjs` exit 0 cuando los 271 ejercicios cumplen VAL-04 + VAL-06 + VAL-08 simultáneamente.
- Exit 1 en cualquier otro caso, con sub-gates indicando qué falla.
- Cero deps añadidas al proyecto (sin `package.json`).
</success_criteria>

<output>
Create `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-02-SUMMARY.md` when done.
</output>
</content>
</invoke>