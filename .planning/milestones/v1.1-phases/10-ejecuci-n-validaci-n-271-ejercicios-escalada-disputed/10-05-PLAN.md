---
phase: 10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed
plan: 05
type: execute
wave: 3
depends_on:
  - 10-04
files_modified:
  - .planning/STATE.md
autonomous: false
requirements:
  - VAL-04
  - VAL-06
  - VAL-08
must_haves:
  truths:
    - "El reporter `node scripts/run-validation-271.mjs` (Plan 10-02) exit 0 (los 3 sub-gates VAL-04 + VAL-06 + VAL-08 PASS)."
    - "El smoke test estricto `VAL_07_STRICT=1 node --test tests/*.test.js` exit 0 (todos los 271 ejercicios con `validation.status === \"validated\"` cuando el feature flag está activo — gate del milestone v1.1)."
    - "El gesto manual del autor (flip de `VAL_07_STRICT=1`) ocurrió conscientemente tras el reporter PASS, NO auto-flipeado por ningún script."
    - "Si ambos exit 0: el milestone v1.1 está cerrable via `/gsd:complete-milestone v1.1`."
    - "Si alguno falla: el autor itera el sub-skill batch (Plan 10-04) sobre los IDs problemáticos (resume idempotente) ANTES de cerrar."
    - "STATE.md actualizado con el cierre del Plan 10-05 + status milestone v1.1 close-ready."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Actualización final con milestone v1.1 ready to close"
      contains:
        - "Phase 10 SHIPPED"
  key_links:
    - from: "autor"
      to: "scripts/run-validation-271.mjs"
      via: "node scripts/run-validation-271.mjs — exit 0 espera"
      pattern: "run-validation-271"
    - from: "autor"
      to: "tests/exercise-types.test.js (bloque VAL-07 paramétrico)"
      via: "VAL_07_STRICT=1 node --test tests/*.test.js — exit 0 espera"
      pattern: "VAL_07_STRICT"
---

<objective>
Cerrar Phase 10 + milestone v1.1 ejecutando los dos gates de cierre: (1) el reporter `scripts/run-validation-271.mjs` (Plan 10-02) sobre los 7 archivos mutados por Plan 10-04, y (2) el smoke test paramétrico `VAL_07_STRICT=1 node --test tests/*.test.js` (Phase 9 D-VAL-17/18) con el feature flag activado manualmente por el autor como gesto consciente.

Purpose: VAL-04 + VAL-06 + VAL-08 verification — el reporter cubre los 3 sub-gates, el smoke test estricto previene regresión editorial futura. Sin estos dos exit 0, el milestone v1.1 no cierra.

Output: log del reporter + log del smoke test estricto en `.planning/STATE.md`, y autorización del autor para invocar `/gsd:complete-milestone v1.1`. Si alguno falla, rollback al estado validated parcial (los 7 JSONs ya están en disco; cero rollback de contenido), y el autor itera el sub-skill batch sobre los IDs problemáticos listados por el reporter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md
@README.md
@scripts/run-validation-271.mjs
@tests/exercise-types.test.js

<interfaces>
Dos comandos del autor (en orden):

1. `node scripts/run-validation-271.mjs`
   - Lee los 7 archivos `content/exercises/*.json`.
   - Imprime tabla colorizada por categoría (Total/Validated/Disputed/Pending/Missing).
   - Computa 3 sub-gates VAL-04 + VAL-06 + VAL-08.
   - Exit 0 si los 3 PASS; exit 1 con sub-gates accionables si alguno FAIL.
   - En PASS, imprime literalmente el comando del paso 2 como sugerencia.

2. `VAL_07_STRICT=1 node --test tests/*.test.js`
   - El bloque paramétrico Phase 9 VAL-07 (con `{skip: process.env.VAL_07_STRICT !== '1'}` option) ahora corre — verifica que TODOS los 271 ejercicios tienen `validation.status === "validated"`.
   - Exit 0 si los 271 cumplen; exit ≠ 0 con assertion failure si alguno no.
   - El env var `VAL_07_STRICT=1` se setea SOLO para este comando (no se persiste — el autor decide después si añadirlo a su shell rc).

Cierre del milestone:
- Si ambos exit 0: el autor invoca `/gsd:complete-milestone v1.1` (otro comando, fuera del scope de este plan).
- Si alguno falla: el autor regresa al sub-skill batch (Plan 10-04) con los IDs problemáticos, los resuelve con paths A/B/C, y re-ejecuta los dos gates de este plan.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Ejecutar reporter `scripts/run-validation-271.mjs` y verificar exit 0</name>
  <files>scripts/run-validation-271.mjs (read-only execution)</files>
  <read_first>
    - `scripts/run-validation-271.mjs` (entender qué imprime y qué sub-gates evalúa)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md` (Q5 esqueleto del reporter — sub-gates esperados)
    - STATE.md `## Phase 10 Progress ### Categorías cerradas` (verificar que las 7 categorías están cerradas pre-gate)
    - STATE.md `## Deferred-disputed` (verificar vacío — si hay deferred, este gate va a FAIL en VAL-08)
  </read_first>
  <what-built>
    Reporter `scripts/run-validation-271.mjs` (Plan 10-02) + los 7 archivos mutados por Plan 10-04.
  </what-built>
  <how-to-verify>
    1. El autor ejecuta en su terminal (o vía Bash tool desde Claude Code):
       
       `node scripts/run-validation-271.mjs`

    2. La salida debe mostrar:
       - Tabla con 7 filas, una por categoría, columnas Total/Validated/Disputed/Pending/Missing.
       - Cada fila con `Validated === Total`, `Disputed === 0`, `Pending === 0`, `Missing === 0`.
       - Sub-gates:
         - `VAL-06 (271/271 validated): PASS (271/271)` — verde.
         - `VAL-08 (cero disputed): PASS` — verde.
         - `VAL-04 (≥2 distinct AIs por validated): PASS` — verde.
       - Mensaje final: `Milestone gate PASS.` en verde + bold.
       - Sugerencia: `VAL_07_STRICT=1 node --test tests/*.test.js`.

    3. Si exit code != 0:
       - El autor regresa al Plan 10-04 con los IDs listados en los sub-gates FAIL.
       - Re-ejecuta `/gsd-validate-batch <id1,id2,...>` para procesarlos (paths A/B/C según el caso).
       - Re-ejecuta este task hasta exit 0.

    Verificación del autor antes de aprobar:
    - El comando exit 0.
    - La salida muestra "Milestone gate PASS." en pantalla.
    - Los counts por categoría son los esperados (preposiciones 50, avere 23, essere 39, genero-numero 40, profesiones 51, sustantivos-irregulares 31, verbos-movimiento 37 = 271 total).
  </how-to-verify>
  <action>
    El autor (o Claude Code via Bash tool) ejecuta `node scripts/run-validation-271.mjs` y observa la salida + exit code.

    Si exit 0: aprobar el task con `approved reporter` y proceder al Task 2.
    Si exit 1: el autor lee los sub-gates FAIL, identifica los IDs problemáticos, regresa al Plan 10-04 con `/gsd-validate-batch <id1,id2,...>` (resume idempotente sobre los IDs específicos), y re-itera este task tras resolverlos.

    Si los sub-gates FAIL incluyen:
    - **VAL-06 FAIL (X/271 con X<271):** hay ejercicios sin `validation field` o con `status !== "validated"`. El sub-skill batch los identifica con `/gsd-validate-batch --all-pending` (resume filtrará los ya-validated y procesará los pendientes/missing).
    - **VAL-08 FAIL:** hay IDs `disputed` no resueltos. El autor procesa cada ID con `/gsd-validate-batch <id1,id2,...>` y resuelve con paths A/B/C en la cola.
    - **VAL-04 FAIL:** hay IDs con `validated` pero `passes[]` sin ≥2 distinct `by`. Probable corrupción del JSON o passes[] con by duplicado. El autor investiga manualmente (típicamente abriendo el JSON del ID listado y verificando que los 2 pases originales no tienen el mismo `by`).
  </action>
  <verify>
    <automated>node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/run-validation-271.mjs` exit 0.
    - La salida contiene la cadena `Milestone gate PASS.` (verificable con `node scripts/run-validation-271.mjs | grep -q "Milestone gate PASS"`).
    - Los counts por categoría suman 271 totales y 271 validated.
    - El autor confirmó visualmente que la tabla está toda en verde (sin filas con disputed/pending/missing > 0).
  </acceptance_criteria>
  <resume-signal>El autor escribe `approved reporter` (exit 0) o `iterate <razón>` describiendo qué sub-gate falló y qué IDs tocará re-procesar.</resume-signal>
  <done>
    Reporter `scripts/run-validation-271.mjs` exit 0 con `Milestone gate PASS.` visible. Los 3 sub-gates VAL-04 + VAL-06 + VAL-08 PASS. Listo para Task 2 (smoke test estricto).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Flippear `VAL_07_STRICT=1` y ejecutar smoke test estricto `node --test`</name>
  <files>tests/exercise-types.test.js (read-only execution con env var activo)</files>
  <read_first>
    - `README.md` sección `## Validación editorial (milestone v1.1)` (instrucciones literales del comando — creadas por Plan 10-03)
    - `tests/exercise-types.test.js` (bloque paramétrico VAL-07 con `{skip: process.env.VAL_07_STRICT !== '1'}` option creado en Phase 9 Plan 09-01)
    - `.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md` D-VAL-17 (rationale del feature flag activación-al-cierre)
  </read_first>
  <what-built>
    Phase 9 Plan 09-01 creó el bloque paramétrico VAL-07 con feature flag env var. Phase 9/10 procesaron los 271 hasta validated. README documenta el comando.
  </what-built>
  <how-to-verify>
    1. El autor ejecuta en su terminal (Linux/macOS):
       
       `VAL_07_STRICT=1 node --test tests/*.test.js`

    2. La salida debe mostrar:
       - Los 254/254 tests baseline pasando.
       - Los nuevos sub-tests del bloque paramétrico VAL-07 (uno por categoría — 7 sub-tests, o 1 sub-test que itera las 7 según la implementación de Plan 09-01) corriendo (NO `skipped`).
       - `pass NNN` (un número ≥ 254) y exit code 0.

    3. Si exit code != 0:
       - El test imprime qué ejercicios fallan con `validation.status !== "validated"`.
       - El autor regresa al Task 1 + Plan 10-04 para resolver los IDs problemáticos.

    Verificación del autor antes de aprobar:
    - El comando exit 0 con el env var ACTIVO (`VAL_07_STRICT=1`).
    - La salida muestra los sub-tests VAL-07 ejecutándose (no skipped).
    - El comando sigue siendo exit 0 al re-ejecutarlo (estabilidad — no es flaky).

    Importante (per RESEARCH Q6 + D-VAL-17): el flip de `VAL_07_STRICT=1` es manual y NO se persiste automáticamente. El autor decide si añadirlo a `~/.bashrc` / `~/.zshrc` / `~/.config/fish/config.fish` para hacerlo permanente — pero ese gesto adicional es del autor, NO de este plan.
  </how-to-verify>
  <action>
    El autor (o Claude Code via Bash tool) ejecuta `VAL_07_STRICT=1 node --test tests/*.test.js` y observa la salida + exit code.

    Si exit 0: aprobar el task con `approved smoke-strict` y proceder al cierre del plan.
    Si exit ≠ 0: el autor lee los assertion failures (typically listings de IDs con status incorrecto), regresa al Plan 10-04 (o Task 1 de este plan), resuelve, y re-itera este task hasta exit 0.

    Caso edge: si el bloque paramétrico VAL-07 imprime `# skipped` (porque algún error de implementación dejó el bloque skipeado incluso con env var activo), el autor lo reporta como bug del Plan 09-01; el milestone NO cierra hasta que el test corra de verdad.
  </action>
  <verify>
    <automated>VAL_07_STRICT=1 node --test tests/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `VAL_07_STRICT=1 node --test tests/*.test.js` exit 0.
    - El run incluye los sub-tests VAL-07 ejecutándose (NO skipped). Verificable: la salida del comando NO contiene un `# skipped` justo en el sub-test paramétrico VAL-07 (debería aparecer un `ok` o `pass` para ese sub-test).
    - El run es estable: 2 ejecuciones consecutivas con env var activo dan ambas exit 0 (no flaky).
    - El env var `VAL_07_STRICT` NO fue añadido a ningún archivo del proyecto por este task (es exclusivamente del shell del autor — sin auto-write a `.env`, `.planning/config.json`, etc).
  </acceptance_criteria>
  <resume-signal>El autor escribe `approved smoke-strict` (exit 0 + tests VAL-07 ejecutados) o `iterate <razón>`.</resume-signal>
  <done>
    Smoke test estricto exit 0 con `VAL_07_STRICT=1` activo. Los 271 ejercicios verifican `validation.status === "validated"` en runtime de test. Milestone v1.1 listo para cierre via `/gsd:complete-milestone v1.1` (otro comando, fuera del scope).
  </done>
</task>

<task type="auto">
  <name>Task 3: Actualizar STATE.md con cierre Phase 10 + milestone v1.1 ready</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` (estado actual: ver Frontmatter + `## Current Position` + `## Phase 10 Progress` para actualizarlos)
    - `.planning/ROADMAP.md` (Phase 10 `**Plans:**` para marcar como complete tras este plan)
  </read_first>
  <action>
    Editar `.planning/STATE.md` con los siguientes updates puntuales:

    1. **Frontmatter:**
       - `last_updated:` → ISO timestamp actual (`date +%Y-%m-%dT%H:%M:%S.000Z`).
       - `last_activity:` → "Phase 10 SHIPPED — 271/271 ejercicios validated. Reporter exit 0 (VAL-04+VAL-06+VAL-08 PASS). Smoke test estricto VAL_07_STRICT=1 exit 0. Milestone v1.1 listo para cierre via /gsd:complete-milestone v1.1."
       - `status:` → "Phase 10 SHIPPED 2026-MM-DD — 271/271 validated. Milestone v1.1 close-ready."
       - `progress.completed_phases:` → incrementar a 2 (Phase 9 + Phase 10).
       - `progress.completed_plans:` → incrementar a 8 (Phase 9: 3 plans + Phase 10: 5 plans).
       - `progress.percent:` → recalcular como `(completed_phases / total_phases) * 100` o `(completed_plans / total_plans) * 100` según la convención usada en el archivo.

    2. **Section `## Current Position`:** actualizar el contenido para reflejar Phase 10 COMPLETE:
       - `Phase: 10 — SHIPPED 2026-MM-DD`
       - `Status: Milestone v1.1 close-ready — los 271 ejercicios validated por quórum Opus+Sonnet. Próximo paso: /gsd:complete-milestone v1.1.`

    3. **Section `## Phase 10 Progress`:** dejar el contenido tal cual (las 7 líneas de categorías cerradas, las 7 checklist marcadas `[x]`). Si Plan 10-04 las dejó sin marcar las `[x]`, marcarlas aquí.

    4. **Section `## Performance Metrics`:** añadir filas para Phase 10 al final de la tabla:
       - `| 10 | 10-01 | <duración> | 1 | 1 |` — sub-skill SKILL.md.
       - `| 10 | 10-02 | <duración> | 1 | 1 |` — reporter mjs.
       - `| 10 | 10-03 | <duración> | 2 | 2 |` — README + STATE.md scaffolding.
       - `| 10 | 10-04 | <duración> | 3 | 8 |` — batch execution (autonomous=false).
       - `| 10 | 10-05 | <duración> | 3 | 1 |` — milestone close gate.
       - Donde `<duración>` se rellena con el wall-clock real medido por el orchestrator de execute-phase (si el orchestrator no provee duración, dejar `?min`).

    5. **Section `## Active Todos`:** marcar como `[x]` los items relevantes:
       - `[x] /gsd:plan-phase 10`.
       - `[x] Ejecutar Phase 10 plans hasta 271/271 validated`.
       - El próximo item `[ ] /gsd:complete-milestone v1.1` queda sin marcar (el siguiente comando del autor).

    NO tocar los frontmatter campos no listados (`milestone`, `milestone_name`, `gsd_state_version`). NO tocar otras secciones del archivo (Project Reference, Accumulated Context, Session Continuity, Deferred-disputed — que debe seguir vacío si Plan 10-04 cerró todos los disputed).
  </action>
  <verify>
    <automated>grep -q "Phase 10 SHIPPED" .planning/STATE.md && grep -q "Milestone v1.1 close-ready" .planning/STATE.md && head -20 .planning/STATE.md | grep -q "milestone: v1.1"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter `last_updated` actualizado a ISO timestamp posterior al inicio de Phase 10.
    - STATE.md frontmatter `last_activity` menciona "Phase 10 SHIPPED" y "271/271 validated" y "VAL_07_STRICT=1 exit 0".
    - STATE.md frontmatter `status` menciona "Milestone v1.1 close-ready".
    - STATE.md frontmatter `progress.completed_phases` incrementado a 2.
    - STATE.md `## Current Position` actualizado con `Phase: 10 — SHIPPED <fecha>`.
    - STATE.md `## Active Todos` con los items relevantes marcados `[x]`.
    - STATE.md `## Performance Metrics` con 5 nuevas filas (una por plan 10-01..10-05).
    - El resto del archivo (Project Reference, Accumulated Context, Session Continuity, Phase 10 Progress, Deferred-disputed, Operator Next Steps) preservado sin alteraciones.
  </acceptance_criteria>
  <done>
    STATE.md refleja Phase 10 SHIPPED + milestone v1.1 close-ready. Frontmatter, progreso, performance metrics, y current position actualizados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor→Bash | El autor escribe y ejecuta `VAL_07_STRICT=1 node --test tests/*.test.js` y `node scripts/run-validation-271.mjs`. Cero injection surface — comandos fijos. |
| Bash→test suite | El test runner consume los 7 JSONs ya en disco. Cero auth, cero red. |
| STATE.md→git | El commit final del plan es contenido editorial; no contiene secretos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | autor olvida flipear VAL_07_STRICT=1 (corre sin env var) | mitigate | El comando literal copy-paste del README incluye el env var inline (`VAL_07_STRICT=1 node --test ...`); el README explica el rationale. Si el autor lo olvida, los sub-tests VAL-07 quedan skipped — `node --test` reportará `# skipped`, visible en la salida. Mitigación: el acceptance criterion del Task 2 explícitamente verifica que el sub-test paramétrico NO esté skipped. |
| T-10-05-02 | Repudiation | auto-flip de VAL_07_STRICT escondido al autor | mitigate | RESEARCH Q6 + D-VAL-17: el flip es MANUAL por diseño — ningún script lo escribe a `.env` o `.planning/config.json`. Auditable: si Plan 10-03 README accidentalmente incluyera auto-flip, sería bug.  |
| T-10-05-03 | Information Disclosure | reporter expone IDs en stdout | accept | Single-user, local. Datos editoriales del autor. |
| T-10-05-04 | Denial of Service | smoke test corre 254+ tests y tarda > minutos | accept | Phase 9 baseline 254/254 tests verdes en ~5-15s. Phase 10 añade <30 sub-tests adicionales (uno por categoría). Total esperado: < 20s. NO bloqueante. |
| T-10-05-SC | Tampering | npm/pip/cargo installs | accept | NO HAY package installs en este plan — solo `node --test` (Node builtin) y `node scripts/...`. Zero-deps. |
</threat_model>

<verification>
1. Task 1: `node scripts/run-validation-271.mjs` exit 0 con tabla colorizada PASS.
2. Task 2: `VAL_07_STRICT=1 node --test tests/*.test.js` exit 0 con sub-tests VAL-07 ejecutándose (NO skipped).
3. Task 3: STATE.md frontmatter + `## Current Position` actualizados.
4. Re-correr el reporter + smoke test cualquier número de veces debe seguir exit 0 (estabilidad).
5. `.planning/STATE.md ## Deferred-disputed` sigue vacío (si Plan 10-04 resolvió todos los disputed).
</verification>

<success_criteria>
- Los dos gates de cierre exit 0 simultáneamente (reporter + smoke test estricto).
- STATE.md refleja Phase 10 SHIPPED + milestone v1.1 ready.
- El autor puede invocar `/gsd:complete-milestone v1.1` como siguiente paso (fuera del scope de este plan).
- Cero deps añadidas; cero modificaciones a SKILL.md base Phase 9; cero modificaciones a content/*.json en este plan (todo el trabajo de mutación ocurrió en Plan 10-04).
</success_criteria>

<output>
Create `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-05-SUMMARY.md` when done.
</output>
</content>
