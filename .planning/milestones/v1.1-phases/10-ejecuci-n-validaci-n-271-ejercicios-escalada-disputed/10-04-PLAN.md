---
phase: 10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - content/exercises/preposiciones.json
  - content/exercises/avere.json
  - content/exercises/essere.json
  - content/exercises/genero-numero.json
  - content/exercises/profesiones.json
  - content/exercises/sustantivos-irregulares.json
  - content/exercises/verbos-movimiento.json
  - .planning/STATE.md
autonomous: false
requirements:
  - VAL-04
  - VAL-06
  - VAL-08
must_haves:
  truths:
    - "Los 269 ejercicios pendientes (271 totales − 2 piloto) tienen validation.passes[] poblado con ≥2 entries y validation.status === 'validated' (o disputed resuelto path-A/B/C → validated)."
    - "Los 3 motivadores históricos pendientes (preposiciones-031, -032, -047) están en validated (vía pase limpio Opus+Sonnet, o vía cola disputed D-VAL-25 con audit trail completo)."
    - "Cero ejercicios en estado disputed no resuelto al cierre del Plan (si el autor difirió alguno path-d, queda en STATE.md ## Deferred-disputed y será FAIL de VAL-08 en el reporter Plan 10-05)."
    - "Audit trail completo: git log --oneline muestra ~269 commits validate(<cat>): <id> → ... más commits adicionales por paths A/B/C de la cola disputed."
    - "El reconsider trigger D-VAL-21 se ejecutó al cerrar Preposiciones (banner + AskUserQuestion si dispute-rate <5% AND los 3 motivadores -031/-032/-047 validated sin concerns)."
    - "El pre-flight node scripts/assert-avere-prefix-unchanged.mjs se ejecutó antes de procesar avere; el assert al cierre de avere también se ejecutó."
    - "STATE.md ## Phase 10 Progress ### Categorías cerradas poblado con 7 líneas (una por categoría)."
  artifacts:
    - path: "content/exercises/preposiciones.json"
      provides: "50 ejercicios con validation field populado, status validated"
    - path: "content/exercises/avere.json"
      provides: "23 ejercicios con validation field populado, status validated"
    - path: "content/exercises/essere.json"
      provides: "39 ejercicios con validation field populado, status validated"
    - path: "content/exercises/genero-numero.json"
      provides: "40 ejercicios con validation field populado, status validated"
    - path: "content/exercises/profesiones.json"
      provides: "51 ejercicios con validation field populado, status validated"
    - path: "content/exercises/sustantivos-irregulares.json"
      provides: "31 ejercicios con validation field populado, status validated"
    - path: "content/exercises/verbos-movimiento.json"
      provides: "37 ejercicios con validation field populado, status validated"
  key_links:
    - from: "autor (Claude inline en main session)"
      to: ".claude/skills/gsd-validate-batch/SKILL.md"
      via: "/gsd-validate-batch <cat> por cada categoría D-VAL-22"
      pattern: "gsd-validate-batch"
    - from: ".claude/skills/gsd-validate-batch/SKILL.md"
      to: ".claude/skills/gsd-validate-exercise/SKILL.md"
      via: "Skill tool por cada ID pendiente"
      pattern: "gsd-validate-exercise"
    - from: ".claude/skills/gsd-validate-batch/SKILL.md"
      to: "scripts/assert-avere-prefix-unchanged.mjs"
      via: "pre-flight + cierre avere"
      pattern: "assert-avere-prefix"
---

<objective>
Ejecutar el sub-skill `gsd-validate-batch` (Plan 10-01) sobre las 7 categorías en el orden lockeado D-VAL-22 hasta que los 271 ejercicios tengan `validation.status === "validated"`. Procesar la cola disputed (D-VAL-24..26) tras cada categoría con AskUserQuestion (4 caminos D-VAL-25). Aplicar el reconsider trigger D-VAL-21 tras Preposiciones. Cubrir VAL-04 (≥2 distinct AIs por validated), VAL-06 (271/271 validated), VAL-08 (escalada disputed audit trail).

Purpose: el plan es la EJECUCIÓN del trabajo editorial principal del milestone v1.1. La infra de los Plans 10-01/10-02/10-03 está lista; este plan la ejerce sobre los 7 archivos de categoría. Es autonomous=false por construcción: cada categoría tiene un AskUserQuestion checkpoint final (Continuar/Pausar), y cada disputed surface un AskUserQuestion con 4 opciones para la decisión editorial del autor.

Output: los 7 archivos `content/exercises/<cat>.json` mutados in-place con `validation` field poblado para los 271, ~269+ commits atómicos en `git log`, STATE.md actualizado con progress por categoría + (opcional) `## Deferred-disputed` poblado si el autor difirió algún disputed path-d.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-PATTERNS.md
@.planning/phases/09-infraestructura-de-validaci-n/09-03-SUMMARY.md
@.claude/skills/gsd-validate-batch/SKILL.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@scripts/run-validation-271.mjs

<interfaces>
Invocaciones del autor (siempre desde main session, jamás dentro de Task — el batch requiere AskUserQuestion):
- /gsd-validate-batch preposiciones — procesa los 49 pendientes (50 total, -040 ya done en piloto).
- /gsd-validate-batch avere — procesa los 22 pendientes (23 total). Pre-flight + cierre AVERE assert.
- /gsd-validate-batch essere — procesa los 39.
- /gsd-validate-batch genero-numero — procesa los 40.
- /gsd-validate-batch profesiones — procesa los 51.
- /gsd-validate-batch sustantivos-irregulares — procesa los 31.
- /gsd-validate-batch verbos-movimiento — procesa los 37.

Alternativa equivalente (encadena con checkpoints intermedios):
- /gsd-validate-batch --all-pending — itera las 7 en orden D-VAL-22 con AskUserQuestion entre categorías.

Cola disputed por categoría (D-VAL-25):
- (a) Accept fix → reset passes + commit fix + re-validar.
- (b) Reject + override → append by:autor + status=validated direct + commit.
- (c) Rewrite manualmente → autor edita JSON + reset + commit rewrite-snapshot + re-validar.
- (d) Skip / defer → append a STATE.md ## Deferred-disputed.

Acceptance del milestone (verificable con Plan 10-02 reporter):
- node scripts/run-validation-271.mjs exit 0.

Resume idempotente (D-VAL-19): si el autor pausa o golpea session-limit, re-invocar /gsd-validate-batch <cat> reanuda desde el primer pendiente; el estado verdad vive en los JSONs.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Procesar categoría Preposiciones (49 pendientes) + reconsider trigger D-VAL-21</name>
  <files>content/exercises/preposiciones.json</files>
  <read_first>
    - `.claude/skills/gsd-validate-batch/SKILL.md` (instrucciones del bucle + cola disputed + reconsider trigger)
    - `.claude/skills/gsd-validate-exercise/SKILL.md` (lo que el batch invoca por cada ID)
    - `content/exercises/preposiciones.json` (estado actual — 50 ejercicios, 1 ya validated `-040`)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md` (D-VAL-21 reconsider trigger detalle: dispute-rate <5% AND los 3 motivadores -031/-032/-047 validated)
  </read_first>
  <what-built>
    Sub-skill `gsd-validate-batch` (Plan 10-01) + reporter `scripts/run-validation-271.mjs` (Plan 10-02). Listos para ejercer.
  </what-built>
  <how-to-verify>
    El autor ejecuta en su Claude Code main session (NO dentro de subagent):

    1. `/gsd-validate-batch preposiciones`
    2. El sub-skill itera los 49 pendientes invocando `gsd-validate-exercise <id>` por cada uno.
    3. Al cerrar la categoría, el sub-skill emite tabla resumen en español: `Categoría preposiciones cerrada: X/49 validated • Y disputed • Z pending`.
    4. Si dispute-rate < 5% AND los 3 motivadores históricos (`preposiciones-031`, `preposiciones-032`, `preposiciones-047`) salieron `validated` sin entrar a la cola disputed → banner amarillo D-VAL-21 + AskUserQuestion 2 opciones: "Continuar con avere" / "Pausar para reconsiderar".
    5. Si disputed > 0: el sub-skill procesa la cola con banner pretty-print + AskUserQuestion 4 opciones por cada uno (Accept fix / Reject + override / Rewrite manualmente / Skip).
    6. Al final, AskUserQuestion checkpoint: "Categoría preposiciones cerrada. ¿Continuar con avere?" → "Continuar" / "Pausar".

    Verificaciones manuales del autor antes de aprobar:
    - Abrir `content/exercises/preposiciones.json` y confirmar que los 50 ejercicios tienen `validation.status === "validated"` (o que los disputed-deferred path-d están en STATE.md).
    - `git log --oneline | grep 'validate(preposiciones)' | wc -l` debe ser >= 49 (más los commits adicionales path-A/B/C de la cola).
    - Los 3 motivadores (-031, -032, -047) deben estar validated (revisar su `validation.passes` — si saltaron a cola disputed, deben tener un commit `validate(preposiciones): preposiciones-NNN → validated POST-fix` o equivalente).
    - Si el banner D-VAL-21 disparó: el autor decidió conscientemente "Continuar con avere" o "Pausar para reconsiderar". Si "Pausar" → este Task NO se aprueba; el autor itera (probablemente añade Gemini ad-hoc o re-revisa Prompt) antes de continuar.

    Si dispute-rate >= 5%: el banner D-VAL-21 NO dispara (es el comportamiento esperado); el sub-skill ofrece la cola disputed normal.
  </how-to-verify>
  <action>
    El autor ejecuta `/gsd-validate-batch preposiciones` y responde a los AskUserQuestion del sub-skill según su criterio editorial. El sub-skill:

    1. Resuelve scope a `content/exercises/preposiciones.json`.
    2. Filtra los 49 pendientes (-040 ya validated en piloto).
    3. Itera invocando `gsd-validate-exercise <id>` por cada uno (1 commit atómico por ejercicio per D-VAL-04).
    4. Acumula `disputedQueue["preposiciones"]`.
    5. Al cierre: emite tabla resumen + computa dispute-rate.
    6. Si dispute-rate < 5% AND los 3 motivadores validated sin concerns: banner amarillo D-VAL-21 + AskUserQuestion 2 opciones.
    7. Si `disputedQueue["preposiciones"].length > 0`: procesa cola con banner D-VAL-26 + AskUserQuestion 4 opciones por cada uno. Las decisiones del autor mutan el JSON (paths a/b/c) o el STATE.md (path d).
    8. AskUserQuestion checkpoint final.

    NOTA crítica: el autor debe estar disponible al keyboard durante el procesamiento — la cola disputed y los checkpoints requieren respuestas a AskUserQuestion. El batch tolera pausas de hasta ~24h via resume idempotente, pero AskUserQuestion stale más de eso puede expirar (RESEARCH Open Q #4). Si el autor pausa una sesión: re-invocar `/gsd-validate-batch preposiciones` al día siguiente — el resume filtra ya-validated y arranca desde el primer pendiente.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); const total=d.exercises.length; const validated=d.exercises.filter(e=>e.validation && e.validation.status==='validated').length; const disputed=d.exercises.filter(e=>e.validation && e.validation.status==='disputed').length; console.log(JSON.stringify({total,validated,disputed})); process.exit(validated===total && disputed===0 ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/preposiciones.json` tiene los 50 ejercicios con `validation.status === "validated"` (o disputed-deferred path-d, registrado en STATE.md).
    - Los 3 motivadores históricos `preposiciones-031`, `preposiciones-032`, `preposiciones-047` están en `validated` (la asunción de trabajo en CONTEXT.md §deferred es que saldrán disputed en pase limpio y se resolverán vía path-A/B/C; si pasan validated directos, el reconsider trigger D-VAL-21 disparará).
    - `git log --oneline -50 | grep -c 'validate(preposiciones)'` retorna al menos 49 (más, si paths A/C produjeron commits adicionales).
    - El autor confirmó conscientemente la decisión del reconsider trigger D-VAL-21 (Continuar con avere / Pausar para reconsiderar) — si la rama "Pausar" se activó, este task NO se aprueba hasta que el autor re-itere y re-procese preposiciones con su decisión editorial.
    - STATE.md `## Phase 10 Progress ### Categorías cerradas` contiene una línea con formato `Phase 10 — Categoría preposiciones cerrada (<fecha>): X/Y validated, Z disputed resueltos, W deferred`.
    - Si hubo deferred (path-d): STATE.md `## Deferred-disputed` contiene las líneas correspondientes con razón.
    - El reporter `node scripts/run-validation-271.mjs` (Plan 10-02) muestra para Preposiciones: validated=50, disputed=0, pending=0, missing=0.
  </acceptance_criteria>
  <resume-signal>El autor escribe `approved preposiciones` (con la categoría cerrada al 100%) o describe issues encontrados durante la ejecución.</resume-signal>
  <done>
    Categoría Preposiciones cerrada: 50/50 validated (incluyendo los 3 motivadores históricos), 0 disputed sin resolver, cola path-A/B/C ejercitada (al menos uno de los caminos visto en uso real para los 4 motivadores), reconsider trigger D-VAL-21 evaluado y decisión autor capturada, STATE.md actualizado.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Procesar categoría Avere (22 pendientes) — pre-flight + cierre AVERE asserts</name>
  <files>content/exercises/avere.json</files>
  <read_first>
    - `.claude/skills/gsd-validate-batch/SKILL.md` (instrucciones avere-specific: pre-flight + cierre AVERE assert)
    - `content/exercises/avere.json` (estado actual — 23 ejercicios, -001 ya validated)
    - `scripts/assert-avere-prefix-unchanged.mjs` (header documenta el stripAdditive relax que ya acepta `validation` field; este script debe seguir exit 0)
  </read_first>
  <what-built>
    Sub-skill `gsd-validate-batch` con manejo específico de avere (pre-flight + cierre asserts).
  </what-built>
  <how-to-verify>
    1. Autor ejecuta `/gsd-validate-batch avere`.
    2. Pre-flight: el sub-skill corre `node scripts/assert-avere-prefix-unchanged.mjs` y verifica exit 0 ANTES de empezar. Si exit != 0: STOP con mensaje en español.
    3. Itera los 22 pendientes.
    4. Cola disputed (si la hay) con 4 caminos D-VAL-25.
    5. Cierre: re-ejecutar `node scripts/assert-avere-prefix-unchanged.mjs`. Si exit != 0 (raro — solo si path-A/C tocó el prefix blindado): banner amarillo + AskUserQuestion 3 opciones (regenerar snapshot / revertir último commit / pausar).
    6. AskUserQuestion checkpoint final.

    Verificaciones del autor:
    - `content/exercises/avere.json` tiene 23 ejercicios validated.
    - `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (final).
    - `git log --oneline | grep 'validate(avere)' | wc -l` >= 22.
  </how-to-verify>
  <action>
    Autor invoca `/gsd-validate-batch avere`. El sub-skill ejecuta pre-flight + bucle + cierre assert + cola disputed (si hay) + checkpoint final.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/avere.json','utf8')); const total=d.exercises.length; const validated=d.exercises.filter(e=>e.validation && e.validation.status==='validated').length; const disputed=d.exercises.filter(e=>e.validation && e.validation.status==='disputed').length; console.log(JSON.stringify({total,validated,disputed})); process.exit(validated===total && disputed===0 ? 0 : 1);" && node scripts/assert-avere-prefix-unchanged.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/avere.json` tiene los 23 ejercicios con `validation.status === "validated"`.
    - `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 (el invariante D-88 AVERE prefix sigue intacto tras las 22 mutaciones del validation field).
    - `git log --oneline | grep -c 'validate(avere)'` >= 22.
    - STATE.md `## Phase 10 Progress ### Categorías cerradas` contiene la línea de avere.
  </acceptance_criteria>
  <resume-signal>El autor escribe `approved avere` o describe issues (típicamente: AVERE assert falló post-mutación; el sub-skill ya ofreció las 3 opciones).</resume-signal>
  <done>
    Categoría Avere cerrada: 23/23 validated, 0 disputed sin resolver, AVERE prefix assert exit 0 tras las mutaciones, STATE.md actualizado.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Procesar las 5 categorías restantes (essere → genero-numero → profesiones → sustantivos-irregulares → verbos-movimiento)</name>
  <files>content/exercises/essere.json, content/exercises/genero-numero.json, content/exercises/profesiones.json, content/exercises/sustantivos-irregulares.json, content/exercises/verbos-movimiento.json</files>
  <read_first>
    - `.claude/skills/gsd-validate-batch/SKILL.md` (el flujo es idéntico a Tasks 1/2 pero sin pre-flight/cierre AVERE; sólo Paso 2.7 schema-validation defensive aplica)
    - Los 5 archivos de categoría arriba listados (estado actual de cada uno)
  </read_first>
  <what-built>
    Sub-skill `gsd-validate-batch` ya ejercitado en Preposiciones + Avere; ahora se ejerce sobre las 5 categorías restantes en orden D-VAL-22.
  </what-built>
  <how-to-verify>
    Por cada categoría (en orden D-VAL-22 #3..#7):
    1. Autor ejecuta `/gsd-validate-batch <slug>` (o equivalente).
    2. Sub-skill itera los pendientes.
    3. Cola disputed con 4 caminos.
    4. Schema-validation defensive (`node scripts/validate-content-fixture.mjs`) al cierre.
    5. AskUserQuestion checkpoint final.

    Total esperado: 39 + 40 + 51 + 31 + 37 = 198 ejercicios validated (+ commits adicionales por paths A/C de la cola).

    Verificaciones del autor:
    - Los 5 JSONs con `validation.status === "validated"` en todos sus ejercicios.
    - `node scripts/validate-content-fixture.mjs` exit 0 (schema OK).
    - STATE.md `## Phase 10 Progress ### Categorías cerradas` tiene 7 líneas en total (las de Tasks 1/2 + las 5 nuevas).
    - `git log --oneline | grep -c 'validate('` debería ser >= 271 (los 269 + algunos commits extra de path-A/B/C si hubo disputed resueltos).
  </how-to-verify>
  <action>
    Autor invoca el sub-skill batch 5 veces en orden D-VAL-22 #3 → #7, o ejecuta `/gsd-validate-batch --all-pending` desde el punto de retomada (el resume idempotente filtra preposiciones + avere ya validated). Por cada categoría, responde a los AskUserQuestion de la cola disputed y al checkpoint final.

    Pausas son seguras: el autor puede cerrar la sesión entre categorías; re-invocar `/gsd-validate-batch <cat>` reanuda desde el primer pendiente.

    Si el autor difiere algún disputed path-d en cualquier categoría: el ID queda registrado en STATE.md `## Deferred-disputed` y el reporter Plan 10-05 lo detectará como FAIL de VAL-08 — el milestone NO cierra hasta que el autor regrese y resuelva los deferred (path-A/B/C re-procesando los IDs específicos con `/gsd-validate-batch <id1,id2,...>`).
  </action>
  <verify>
    <automated>node scripts/check-phase10-categories.mjs essere genero-numero profesiones sustantivos-irregulares verbos-movimiento && node scripts/validate-content-fixture.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Los 5 archivos `content/exercises/essere.json`, `content/exercises/genero-numero.json`, `content/exercises/profesiones.json`, `content/exercises/sustantivos-irregulares.json`, `content/exercises/verbos-movimiento.json` tienen TODOS sus ejercicios con `validation.status === "validated"` (counts respectivos 39, 40, 51, 31, 37).
    - Suma total de ejercicios validated en las 5 categorías = 198.
    - `node scripts/validate-content-fixture.mjs` exit 0 (schema válido para los 7 archivos tras las mutaciones).
    - STATE.md `## Phase 10 Progress ### Categorías cerradas` contiene 7 líneas en total (preposiciones + avere de Tasks 1/2, más las 5 nuevas en orden D-VAL-22).
    - El reporter `node scripts/run-validation-271.mjs` (Plan 10-02) puede ejecutarse y NO crashea; muestra para las 5 categorías validated=expected, disputed=0, pending=0, missing=0.
    - Cero ejercicios en estado `disputed` o `pending` o `missing` al cierre de este Task (si hubo deferred path-d en algún disputed: queda en STATE.md, y este Task NO se aprueba hasta que el autor regrese y resuelva los deferred — el milestone NO cierra con deferred no resueltos).
    - NOTA: si el script `scripts/check-phase10-categories.mjs` no existe (se asume helper opcional), el ejecutor puede sustituir el verify automated por un node -e oneliner iterando las 5 categorías; el invariante material es: para cada categoría, validated===total Y disputed===0.
  </acceptance_criteria>
  <resume-signal>El autor escribe `approved all-5-cats` o describe issues / categorías parciales pendientes.</resume-signal>
  <done>
    Las 5 categorías restantes cerradas al 100% validated. STATE.md actualizado con 7 líneas en `## Phase 10 Progress ### Categorías cerradas`. Schema validator exit 0. Los 271 ejercicios totales están en estado validated (preparado para Plan 10-05 milestone gate).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor→AskUserQuestion | El autor responde con texto libre a la "razón del override" path-B y al rewrite path-C. El batch lo escribe al JSON; el shape validator acepta string en `concerns[]` sin enforcer contenido. |
| Task() Opus/Sonnet→sub-skill | Los subagents emiten JSON con verdict + concerns. El skill hijo (Phase 9) ya hardened el parseo con regex robusto + retry budget = 1. |
| sub-skill→content/*.json (Edit) | Las mutaciones del validation field y los paths A/C (reset, fix, rewrite-snapshot) van por Edit tool. Defensive: `validate-content-fixture.mjs` corre al cierre de cada categoría. |
| sub-skill→STATE.md (path-d) | Append idempotente; si el autor difiere el mismo ID dos veces (re-skip), STATE.md queda con dos líneas duplicadas — verificable pero no crítico (audit trail). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | Subagent Opus o Sonnet emite JSON malformado | mitigate | Skill Phase 9 retry budget = 1 + fallback a `verdict: null` + `concerns: ["[meta] parse failed twice"]`. El ejercicio queda en `pending` y el reporter lo surface como FAIL de VAL-06. |
| T-10-04-02 | Tampering | Edit tool del batch corrompe formatting del JSON (trailing comma, escape mal) | mitigate | `validate-content-fixture.mjs` al cierre de cada categoría detecta el bug; banner amarillo + Continuar/Pausar. Si el autor pausa, puede `git revert HEAD` el último commit. |
| T-10-04-03 | Repudiation | Override path-B sin razón explícita | mitigate | AskUserQuestion follow-up obligatorio "¿Razón del override?"; aunque el autor responda vacío, el concern queda con timestamp ISO + by:autor — audit trail no-vacío. |
| T-10-04-04 | Information Disclosure | Banner disputed expone contenido del ejercicio en stdout | accept | Datos públicos del autor sobre sus propios ejercicios. Single-user, local. |
| T-10-04-05 | Denial of Service | Session-limit Anthropic golpeado mid-batch | mitigate | Resume idempotente D-VAL-19: re-invocar `/gsd-validate-batch <cat>` reanuda desde el primer pendiente sin daño. Cero manifest paralelo que recuperar. |
| T-10-04-06 | Elevation of Privilege | path-d Skip permite descommittear los 271/271 | mitigate | VAL-06 enforce vía reporter Plan 10-05: deferred-disputed cuenta como FAIL de VAL-08. Milestone NO cierra hasta deferred-list vacía. |
| T-10-04-07 | Tampering | AVERE prefix mutation accidental path-A/C | mitigate | Pre-flight + cierre asserts (`assert-avere-prefix-unchanged.mjs`) detecta la mutación; AskUserQuestion 3 opciones (regenerar snapshot / revertir commit / pausar). |
| T-10-04-SC | Tampering | npm/pip/cargo installs durante el batch | accept | NO HAY package installs en este plan — todo via tools nativas + Node builtins. Zero-deps. |
</threat_model>

<verification>
1. Por cada categoría procesada: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/<cat>.json','utf8')); console.log(d.exercises.filter(e=>e.validation && e.validation.status==='validated').length, '/', d.exercises.length)"` devuelve `total/total` (todas validated).
2. `node scripts/assert-avere-prefix-unchanged.mjs` exit 0 al cierre.
3. `node scripts/validate-content-fixture.mjs` exit 0 al cierre.
4. `git log --oneline | grep -c 'validate('` >= 271 commits de validación (los 269 nuevos + 2 piloto).
5. STATE.md `## Phase 10 Progress ### Categorías cerradas` contiene 7 líneas.
6. STATE.md `## Deferred-disputed` vacío (si el autor resolvió todos los disputed) o con audit trail completo (si hay deferred).
7. El reporter `node scripts/run-validation-271.mjs` (Plan 10-02) muestra tabla con 7 filas y exit 0 (o 1 si hay deferred).
</verification>

<success_criteria>
- 271/271 ejercicios con `validation.status === "validated"` (acceptance condicional al cierre vía Plan 10-05 reporter exit 0).
- Audit trail completo en `git log` con ~269+ commits atómicos `validate(<cat>): <id> → ...`.
- Cero deps añadidas; cero scripts/skills modificados durante la ejecución (el batch solo CONSUME).
- El AVERE prefix invariant D-88 intacto.
</success_criteria>

<output>
Create `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-04-SUMMARY.md` when done.
</output>
</content>
