---
phase: 10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/STATE.md
autonomous: true
requirements:
  - VAL-06
must_haves:
  truths:
    - "README.md tiene una sección `## Validación editorial (milestone v1.1)` documentando la activación del smoke test estricto `VAL_07_STRICT=1`."
    - "La sección de README explica que el flip es manual (gesto consciente del autor) tras el reporter PASS — NO auto-flip."
    - "El comando literal `VAL_07_STRICT=1 node --test tests/*.test.js` aparece en README como bloque copy-paste."
    - "STATE.md tiene una nueva sección `## Phase 10 Progress` (o equivalente) preparada para que el sub-skill batch vaya appendeando líneas por cada categoría cerrada (Phase 10-04 ejercita)."
    - "STATE.md tiene una nueva sección `## Deferred-disputed` (vacía hasta que el sub-skill batch en path-d la pueble) — placeholder con el formato esperado documentado en comentario."
  artifacts:
    - path: "README.md"
      provides: "Documentación del smoke test estricto VAL_07_STRICT=1 al cierre del milestone"
      contains:
        - "Validación editorial"
        - "VAL_07_STRICT=1 node --test tests/*.test.js"
    - path: ".planning/STATE.md"
      provides: "Scaffolding para progress tracking + deferred-disputed log"
      contains:
        - "Phase 10 Progress"
        - "Deferred-disputed"
  key_links:
    - from: "README.md"
      to: "scripts/run-validation-271.mjs (Plan 10-02)"
      via: "README menciona que el reporter PASS imprime el comando del smoke test"
      pattern: "run-validation-271"
    - from: ".planning/STATE.md"
      to: "Phase 10 execution (Plan 10-04)"
      via: "secciones preparadas para que el sub-skill batch las puebla"
      pattern: "Phase 10 Progress"
---

<objective>
Preparar la documentación (README) y el scaffolding de estado (STATE.md) que el sub-skill batch (Plan 10-04) y el milestone-close (Plan 10-05) necesitan. Sin esto, la activación del smoke test VAL-07 al cierre queda undocumented y el autor podría olvidar el último paso del gate.

Purpose: VAL-06 documentation (la regla "271/271 validated antes de cerrar" + el comando manual para enforce vía smoke test). Single-source-of-truth en README para el comando `VAL_07_STRICT=1 node --test tests/*.test.js`. Placeholder en STATE.md para el progress per categoría y el log de deferred-disputed (path-d D-VAL-25).

Output: 
- README.md con nueva sección `## Validación editorial (milestone v1.1)`.
- STATE.md con secciones nuevas `## Phase 10 Progress` y `## Deferred-disputed` (con comentarios explicando el formato esperado para que el batch sepa cómo poblarlas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir sección `## Validación editorial (milestone v1.1)` al README.md</name>
  <files>README.md</files>
  <read_first>
    - `README.md` (estado actual: 88 líneas; entender el estilo y dónde insertar la nueva sección — al final, tras `## Estado del proyecto`)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md` (Q6 contenido sugerido para la sección, con la racionalización "manual flip, NO auto-write")
    - `.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md` (D-VAL-17/18 sobre el feature flag VAL-07)
  </read_first>
  <action>
    Añadir al final de `README.md` (tras la línea 88, después de la sección `## Estado del proyecto`) una nueva sección titulada exactamente `## Validación editorial (milestone v1.1)`.

    **Contenido obligatorio de la sección** (en español, FOUND-04):

    1. Párrafo introductorio (2-3 frases): qué es la validación editorial del milestone v1.1 (R1-R7 + quórum Opus+Sonnet), su outcome esperado (271/271 ejercicios con `validation.status === "validated"`), y por qué importa (cerrar el bug class de batched-curation que dio los 4 motivadores).

    2. Subsección `### Smoke test estricto al cierre del milestone`:
       - Explicación de que el smoke test paramétrico VAL-07 está tras un feature flag `VAL_07_STRICT=1` durante Phase 9/10 para no bloquear desarrollo mientras los 269 siguen sin validar.
       - Al cerrar Phase 10 (reporter `scripts/run-validation-271.mjs` exit 0), el autor activa MANUALMENTE el smoke test estricto:
       
       Bloque copy-paste literal:
       
       ```bash
       # Linux/macOS — gate del milestone v1.1
       VAL_07_STRICT=1 node --test tests/*.test.js
       ```
       
       - Frase explicando: "Una vez activo, cualquier ejercicio nuevo o modificado sin `validation.status === \"validated\"` rompe el test inmediatamente → previene regresión editorial."
       - Frase explicando: "El flip es MANUAL por diseño — no hay auto-flip al cerrar la 7ª categoría para que el gesto del autor sea consciente y deliberado (per RESEARCH Q6 + D-VAL-17). Si quieres persistirlo permanentemente, añade `export VAL_07_STRICT=1` a tu shell rc."

    3. Subsección `### Workflow editorial — comandos`:
       - Lista de los comandos del autor para Phase 10:
         - `/gsd-validate-exercise <id>` — valida 1 ejercicio (Phase 9 skill).
         - `/gsd-validate-batch <category> | --all-pending | <id1,id2,...>` — valida en bucle (Phase 10 sub-skill).
         - `node scripts/run-validation-271.mjs` — reporter del milestone gate (Phase 10 reporter).
       - 1 frase explicando: "El sub-skill batch ofrece cola disputed VAL-08 con 4 caminos terminales (Accept fix / Reject + override / Rewrite manualmente / Skip defer) y un checkpoint AskUserQuestion por cada categoría procesada."

    **NO incluir**:
    - Detalles del algoritmo `deriveStatus` (vive en el módulo).
    - Listado de los 8 D-VAL-XX (vive en el CONTEXT.md).
    - Comandos `npm install` o referencias a `package.json` (zero-deps invariant del proyecto).

    **Preservar el resto del README** intacto (88 líneas existentes sin cambios — solo append al final).
  </action>
  <verify>
    <automated>grep -q "## Validación editorial (milestone v1.1)" README.md &amp;&amp; grep -q "VAL_07_STRICT=1 node --test tests/\*.test.js" README.md &amp;&amp; grep -q "gsd-validate-batch" README.md &amp;&amp; grep -q "run-validation-271" README.md &amp;&amp; grep -q "Accept fix" README.md &amp;&amp; grep -q "Skip" README.md &amp;&amp; ! grep -q "npm install" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contiene la sección `## Validación editorial (milestone v1.1)` (verificable con grep exacto).
    - El comando literal `VAL_07_STRICT=1 node --test tests/*.test.js` aparece en el README dentro de un bloque copy-paste.
    - La sección menciona el sub-skill `gsd-validate-batch` y el reporter `scripts/run-validation-271.mjs`.
    - La sección menciona los 4 caminos de la cola disputed (al menos "Accept fix" y "Skip" aparecen).
    - El README NO contiene la cadena `npm install` (zero-deps invariant).
    - Las 88 líneas previas del README se preservan intactas (verificable: el header `# Italian Course — Ejercicios A1/A2` sigue en línea 1; la sección `## Estado del proyecto` sigue presente; el wc -l es mayor que 88).
    - El documento sigue siendo parseable Markdown (sin tabs/CR ni encoding broken).
  </acceptance_criteria>
  <done>
    README.md extendido con sección `## Validación editorial (milestone v1.1)`, smoke test manual documentado con bloque copy-paste literal, comandos del workflow editorial listados, zero-deps invariant respetado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffolding en STATE.md — secciones `## Phase 10 Progress` y `## Deferred-disputed`</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` (estado actual: ver donde añadir las nuevas secciones — antes de `## Operator Next Steps` que está al final del archivo)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md` (D-VAL-25 path-d → STATE.md `## Deferred-disputed`; D-VAL-23 → progress por categoría)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-PATTERNS.md` (sección `.planning/STATE.md` con el formato exacto de las líneas que el batch debe appendear)
  </read_first>
  <action>
    Editar `.planning/STATE.md` añadiendo dos secciones nuevas ANTES de la sección final `## Operator Next Steps`.

    **Sección 1 — `## Phase 10 Progress`:**

    - Header: `## Phase 10 Progress`.
    - Comentario HTML invisible explicando el formato (`<!-- El sub-skill gsd-validate-batch appendea aquí una línea por cada categoría cerrada, formato literal: "Phase 10 — Categoría <slug> cerrada (<ISO-fecha>): X/Y validated, Z disputed resueltos, W deferred" -->`).
    - Subsección `### Categorías (orden lockeado D-VAL-22):` con una checklist de las 7 categorías sin marcar (todas en `[ ]`), formato:
      - `- [ ] preposiciones — 49 pendientes (50 total, -040 ya validated piloto)`
      - `- [ ] avere — 22 pendientes (23 total, -001 ya validated piloto)`
      - `- [ ] essere — 39 pendientes (39 total)`
      - `- [ ] genero-numero — 40 pendientes (40 total)`
      - `- [ ] profesiones — 51 pendientes (51 total)`
      - `- [ ] sustantivos-irregulares — 31 pendientes (31 total)`
      - `- [ ] verbos-movimiento — 37 pendientes (37 total)`
    - Subsección `### Categorías cerradas:` vacía con comentario `<!-- el batch appendea aquí -->`.

    **Sección 2 — `## Deferred-disputed`:**

    - Header: `## Deferred-disputed`.
    - Comentario HTML invisible explicando el formato (`<!-- Cuando el autor responde "Skip (defer al final del milestone)" en la cola disputed D-VAL-25 path-d, el sub-skill appendea aquí una línea: "- <exercise-id> (<ISO-fecha>): deferred por autor — razón: \"<razón opcional>\"". VAL-06 impide cerrar el milestone con deferred no resueltos. El reporter scripts/run-validation-271.mjs los detectará como FAIL en VAL-08. -->`).
    - Después del comentario, una sola línea: `(vacío hasta que el autor difiera algún disputed en path-d)`.

    **NO tocar** el resto del archivo STATE.md (gsd_state_version frontmatter, Project Reference, Current Position, Performance Metrics, Accumulated Context, etc.). Sólo añadir las 2 secciones nuevas antes de `## Operator Next Steps`.

    También actualizar el frontmatter del archivo:
    - `last_updated:` → ISO timestamp actual (date `+%Y-%m-%dT%H:%M:%S.000Z`).
    - `last_activity:` → "Phase 10 PLAN.md set creado (10-01 sub-skill + 10-02 reporter + 10-03 README/STATE scaffolding + 10-04 ejecución batch + 10-05 milestone close). Ready `/gsd:execute-phase 10` Wave 1 paralelo."
    - `status:` → "Phase 10 planeada — 5 PLAN.md committed, esperando execute-phase para infra (Wave 1) + ejecución batch (Wave 2)."
    - `progress.total_plans:` → incrementar (3 actuales + 5 nuevos = 8).

    NOTA: el conteo `progress.completed_plans` queda en 3 (Phase 9 sigue siendo el único phase completo).
  </action>
  <verify>
    <automated>grep -q "^## Phase 10 Progress" .planning/STATE.md &amp;&amp; grep -q "^## Deferred-disputed" .planning/STATE.md &amp;&amp; grep -q "preposiciones — 49 pendientes" .planning/STATE.md &amp;&amp; grep -q "avere — 22 pendientes" .planning/STATE.md &amp;&amp; grep -q "essere — 39 pendientes" .planning/STATE.md &amp;&amp; grep -q "genero-numero — 40 pendientes" .planning/STATE.md &amp;&amp; grep -q "profesiones — 51 pendientes" .planning/STATE.md &amp;&amp; grep -q "sustantivos-irregulares — 31 pendientes" .planning/STATE.md &amp;&amp; grep -q "verbos-movimiento — 37 pendientes" .planning/STATE.md &amp;&amp; grep -q "D-VAL-25" .planning/STATE.md &amp;&amp; grep -q "deferred por autor" .planning/STATE.md &amp;&amp; head -20 .planning/STATE.md | grep -q "milestone: v1.1"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md tiene una sección con header exacto `## Phase 10 Progress`.
    - La checklist incluye los 7 slugs D-VAL-22 en orden lockeado.
    - Los counts pendientes son los correctos (preposiciones 49, avere 22, essere 39, genero-numero 40, profesiones 51, sustantivos-irregulares 31, verbos-movimiento 37).
    - STATE.md tiene una sección con header exacto `## Deferred-disputed`.
    - La sección `## Deferred-disputed` contiene un comentario HTML referenciando D-VAL-25 path-d y el formato literal de las líneas que el batch appendea.
    - El frontmatter del archivo conserva `milestone: v1.1` y `milestone_name: Validación editorial`.
    - El frontmatter `last_updated` y `last_activity` se actualizaron al estado post-plan-phase 10.
    - `progress.total_plans` incrementado para reflejar los 5 PLAN.md nuevos de Phase 10.
    - La sección final `## Operator Next Steps` sigue existiendo (NO se borra) y queda tras las dos nuevas secciones.
    - El resto del contenido del STATE.md (Project Reference, Current Position, Performance Metrics, Accumulated Context, Session Continuity, etc.) se preserva sin alteraciones de contenido (solo updates puntuales al frontmatter).
  </acceptance_criteria>
  <done>
    STATE.md extendido con las 2 secciones nuevas `## Phase 10 Progress` (checklist de las 7 categorías + comentario sobre formato de líneas a appendear) y `## Deferred-disputed` (vacío con comentario formato). Frontmatter actualizado con timestamp y status. Resto del archivo intacto.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README→autor | El README es leído por el autor (proyecto single-user). NO se renderiza desde un servidor; no hay XSS surface. |
| STATE.md→batch | El sub-skill batch (Plan 10-04) appendea líneas a STATE.md. El formato es Markdown; cualquier corrupción quedaría visible al autor al leer el archivo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | edición accidental de líneas pre-existentes del README/STATE | mitigate | Tasks usan Edit tool (NO Write) sobre archivos existentes — preserva contenido previo. Verify check grep'a líneas pre-existentes (`# Italian Course`, `milestone: v1.1`). |
| T-10-03-02 | Information Disclosure | README expone comando del workflow | accept | El comando es para el autor del proyecto (single-user). No hay secretos en el comando. |
| T-10-03-SC | Tampering | npm/pip/cargo installs | accept | NO HAY package installs en este plan — solo edición de docs. |
</threat_model>

<verification>
1. `grep -c "## Validación editorial (milestone v1.1)" README.md` retorna 1.
2. `grep -c "VAL_07_STRICT=1 node --test tests/\*.test.js" README.md` retorna ≥1.
3. `grep -c "^## Phase 10 Progress" .planning/STATE.md` retorna 1.
4. `grep -c "^## Deferred-disputed" .planning/STATE.md` retorna 1.
5. README.md sigue conteniendo `# Italian Course — Ejercicios A1/A2` en línea 1.
6. STATE.md frontmatter sigue conteniendo `milestone: v1.1` y `milestone_name: Validación editorial`.
</verification>

<success_criteria>
- README.md sirve como single-source-of-truth para el comando `VAL_07_STRICT=1 node --test tests/*.test.js`.
- STATE.md tiene las 2 secciones nuevas en su sitio correcto (antes de Operator Next Steps), con el formato que el batch (Plan 10-04) puede appendear sin re-leer el archivo entero.
- Cero deps añadidas; cero scripts/skills modificados (solo docs/state).
</success_criteria>

<output>
Create `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-03-SUMMARY.md` when done.
</output>
</content>
</invoke>