---
phase: 10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/gsd-validate-batch/SKILL.md
autonomous: true
requirements:
  - VAL-08
must_haves:
  truths:
    - "El skill `gsd-validate-batch` existe en `.claude/skills/gsd-validate-batch/SKILL.md` con frontmatter YAML válido (name, description, argument-hint, allowed-tools)."
    - "El frontmatter `allowed-tools` incluye `AskUserQuestion` y `Skill` (necesarios para D-VAL-26 banner y para invocar `gsd-validate-exercise`)."
    - "El frontmatter NO contiene `context: fork` (invariante arquitectónico RESEARCH Q1/Q2 — sin él la cola disputed no funcionaría)."
    - "El body documenta la garantía NUNCA batched D-VAL-20: el sub-skill ITERA pero el bucle inyecta UN id por invocación de `gsd-validate-exercise`."
    - "El body documenta el orden lockeado D-VAL-22: preposiciones → avere → essere → genero-numero → profesiones → sustantivos-irregulares → verbos-movimiento."
    - "El body documenta los 4 caminos D-VAL-25 (a Accept fix con BYPASS sticky vía reset passes[]; b Reject+override con append `by:autor` + status directo; c Rewrite manual + AskUserQuestion confirm + reset; d Skip/defer con append a STATE.md)."
    - "El body documenta el banner D-VAL-26 con 4 opciones literales en español + suggested-fix derivado determinísticamente del tag `[Cn-criterio]`."
    - "El body documenta el reconsider trigger D-VAL-21 al terminar la categoría preposiciones (dispute-rate <5% AND los 3 motivadores -031/-032/-047 validated → banner amarillo + AskUserQuestion)."
    - "El body documenta el pre-flight check `node scripts/assert-avere-prefix-unchanged.mjs` antes de entrar a la categoría avere (D-VAL-22 #2)."
    - "El body documenta el AVERE assert al cierre de la categoría avere (RESEARCH Q10)."
    - "El body documenta el resume idempotente D-VAL-19 (re-leer JSONs, filtrar `validation.status === \"validated\"`)."
  artifacts:
    - path: ".claude/skills/gsd-validate-batch/SKILL.md"
      provides: "Sub-skill orquestador batch inline en main session"
      min_lines: 250
      contains:
        - "name: gsd-validate-batch"
        - "AskUserQuestion"
        - "Skill"
        - "preposiciones"
        - "avere"
        - "essere"
        - "genero-numero"
        - "profesiones"
        - "sustantivos-irregulares"
        - "verbos-movimiento"
        - "Accept fix"
        - "Reject + override"
        - "Rewrite manualmente"
        - "Skip (defer al final del milestone)"
  key_links:
    - from: ".claude/skills/gsd-validate-batch/SKILL.md"
      to: ".claude/skills/gsd-validate-exercise/SKILL.md"
      via: "Skill tool invocation por cada ID pendiente del bucle"
      pattern: "gsd-validate-exercise"
    - from: ".claude/skills/gsd-validate-batch/SKILL.md"
      to: "content/exercises/<cat>.json"
      via: "Read tool para filtrar pendientes; Edit tool para BYPASS sticky paths a/c"
      pattern: "content/exercises"
    - from: ".claude/skills/gsd-validate-batch/SKILL.md"
      to: "src/data/validation-state.js"
      via: "Import deriveStatus para verificar transitions tras BYPASS"
      pattern: "deriveStatus"
---

<objective>
Crear el sub-skill `gsd-validate-batch` que orquesta inline en la sesión principal el batch de validación de las 7 categorías. El skill itera invocando `gsd-validate-exercise` (Phase 9, intocable) por cada ID pendiente, ofrece checkpoint por categoría (D-VAL-23), procesa la cola disputed al final de cada categoría (D-VAL-24..26) con AskUserQuestion + 4 caminos terminales (D-VAL-25), y documenta el reconsider trigger D-VAL-21 al cerrar Preposiciones.

Purpose: VAL-08 escalada UX inline — el autor decide accept/reject/rewrite/skip frente a un banner pretty-print con prompt + verdicts + concerns + suggested fix derivado del tag [Cn-criterio].

Output: `.claude/skills/gsd-validate-batch/SKILL.md` con frontmatter válido + body de ~250+ líneas siguiendo la estructura GSD canónica del skill Phase 9 (`<objective>`, `<critical_constraints>`, `<execution>` con pasos numerados, `<error_handling>` tabla, `<read_first_per_invocation>`, `<example_invocations>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md
@.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-PATTERNS.md
@.planning/phases/09-infraestructura-de-validaci-n/09-CONTEXT.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@src/data/validation-state.js

<interfaces>
<!-- Contratos clave que el sub-skill consume. Extraídos del codebase Phase 9 ya en producción. -->

From `.claude/skills/gsd-validate-exercise/SKILL.md` (frontmatter):
- `name: gsd-validate-exercise`
- `argument-hint: "<exercise-id> [--dry-run]"` (el batch invoca con UN id por iteración; NO añade flags)
- Invocación esperada desde el batch: `Skill(gsd-validate-exercise <exercise-id>)` o, equivalentemente, instrucción "invoca el skill `gsd-validate-exercise` con argumento `<id>`".
- Output: muta `validation.passes[]` y `validation.status` del ejercicio in-place, commitea atómicamente.

From `src/data/validation-state.js`:
- `export function deriveStatus(passes: Array<{by, date, verdict, concerns?}>): "pending" | "validated" | "disputed"`
- Sticky: cualquier `verdict: "incorrecta"` → `disputed` (NO se limpia con un correcta posterior).
- `validated`: ≥2 correctas con `by` DISTINTOS y cero incorrectas.

From `content/exercises/<cat>.json` (shape ejercicio):
- `{id: string, type: string, categoryIds: string[], payload: {...}, notes?: string, validation?: {status, passes: [...]}}`
- Tras el batch, `validation` poblado para los 271.

From `tests/fixtures/validation-pilot-disputed.json`:
- `pilot-disputed-c5-leak-001` (fixture, NO cuenta para los 271).
- Concerns reales con tag literal `[C5-leak] ...` confirman el formato del regex `/^\[(C[1-5]-[a-z_]+)\]/`.

Tag mapping verbatim D-VAL-26 (suggested-fix determinístico):
- `[C1-natural]` → "Reescribir el prompt para italiano más natural (sin propuesta concreta — revisión manual)."
- `[C2-una_opcion]` → "Reformular el prompt para forzar UNA opción (el schema multiple-choice no acepta múltiples correctIndex)."
- `[C3-distractoras]` → "Reemplazar distractoras con errores típicos del hispanohablante."
- `[C4-explanation]` → "Reescribir explanation enfocada al alumno (sin meta-staging, sin #NNN refs)."
- `[C5-leak]` → "Eliminar la frase/marca que contiene la regla del prompt."

Orden lockeado D-VAL-22 (con counts pendientes):
1. preposiciones — 49 pendientes (de 50; -040 ya validated en Phase 9 piloto)
2. avere — 22 pendientes (de 23; -001 ya validated)
3. essere — 39 pendientes
4. genero-numero — 40 pendientes
5. profesiones — 51 pendientes
6. sustantivos-irregulares — 31 pendientes
7. verbos-movimiento — 37 pendientes
Total pendientes: 269 ejercicios (271 totales − 2 piloto).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear `.claude/skills/gsd-validate-batch/SKILL.md` con frontmatter + secciones canónicas GSD</name>
  <files>.claude/skills/gsd-validate-batch/SKILL.md</files>
  <read_first>
    - `.claude/skills/gsd-validate-exercise/SKILL.md` (TEMPLATE estructural — copiar exacta cadencia de secciones: `<objective>`, `<critical_constraints>`, `<execution>` con Pasos 1-N numerados, `<error_handling>` tabla, `<read_first_per_invocation>`, `<example_invocations>`)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-CONTEXT.md` (decisiones D-VAL-19..26 verbatim)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-RESEARCH.md` (Q1 esqueleto del body con 4 pasos del bucle + Q3 path-C AskUserQuestion + Q4 BYPASS sticky paths a/c + Q9 suggested-fix regex)
    - `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-PATTERNS.md` (mapping verbatim de secciones del skill base a las del batch + Decision Tree para el Planner)
    - `src/data/validation-state.js` (entender deriveStatus sticky D-VAL-07 — para documentar BYPASS path-B en el body)
    - `CLAUDE.md` (FOUND-04 español, zero-deps invariant)
  </read_first>
  <action>
    Crear el archivo `.claude/skills/gsd-validate-batch/SKILL.md` con la siguiente composición (concrete identifiers, no fenced code in this action — el contenido del archivo viene de los read_first).

    **Frontmatter (YAML válido):**
    - `name: gsd-validate-batch`
    - `description:` literal en español describiendo: itera ejercicios pendientes de una categoría (o todas en orden D-VAL-22), procesa UNA categoría a la vez (D-VAL-23), emite tabla en español al final, ofrece cola disputed VAL-08 (D-VAL-24/25/26).
    - `argument-hint: "<category> | --all-pending | <id1,id2,...>"`
    - `disable-model-invocation: true` (sólo el autor con `/gsd-validate-batch <scope>`)
    - `allowed-tools:` lista YAML que incluya EXACTAMENTE estos 9 valores en orden: `Read`, `Write`, `Edit`, `Bash`, `Glob`, `Grep`, `Task`, `AskUserQuestion`, `Skill`.
    - **NO añadir `context: fork`** — invariante arquitectónico, su presencia rompe AskUserQuestion + nested Task (RESEARCH Q1/Q2). Documentar este NO en `<critical_constraints>`.

    **Sección `<objective>`:** 1-2 párrafos en español describiendo: orquestador inline para los 271 ejercicios, salida observable = los 7 archivos `content/exercises/*.json` con `validation.status === "validated"` salvo deferred path-d. Mencionar VAL-04 + VAL-06 + VAL-08 como requirements cubiertos.

    **Sección `<critical_constraints>`** — clonar la estructura bullet del skill Phase 9 (línea 21-41) y sustituir contenido por estos 8 constraints exactos (todos OBLIGATORIOS, redactados en español):
    1. "Sub-skill INLINE en main session — NUNCA `context: fork`" (RESEARCH Q1+Q2: AskUserQuestion no funciona en subagents spawned vía Task; tampoco se puede anidar Task→Task).
    2. "Sub-skill ITERA pero NUNCA compone N ejercicios en mismo Task() — el bucle inyecta UN id a cada invocación de `gsd-validate-exercise` (D-VAL-20). Garantía arquitectónica del bug class de los 4 motivadores."
    3. "Resume idempotente: re-leer JSONs y filtrar `validation.status === \"validated\"` antes de iterar (D-VAL-19). Estado verdad vive en los JSONs, NO en manifest paralelo. Crash / Ctrl-C / re-invoke al día siguiente → re-arranca limpio."
    4. "Una sola categoría por invocación; `--all-pending` encadena las 7 con AskUserQuestion intermedios (D-VAL-22 orden lockeado + D-VAL-23 checkpoint por categoría)."
    5. "Mensajes al autor en español (FOUND-04). Banners, tablas, cola disputed, suggested fix → castellano."
    6. "Zero-deps invariant: solo herramientas nativas (Read/Write/Edit/Bash/Glob/Grep/Task/AskUserQuestion/Skill) + Node builtins (`node:fs`, `node:path`, `node:url`). Cero `npm install`. Cero Ajv/Zod."
    7. "Commit granularity per D-VAL-04: 1 commit por ejercicio en el path normal (lo hace el skill hijo). Paths a/c añaden 1 commit propio del batch (fix-snapshot o rewrite-snapshot) ANTES del re-validate; path-b produce 1 commit propio (override + status directo)."
    8. "El skill base `gsd-validate-exercise` (Phase 9) es INTOCABLE. El batch NO modifica su SKILL.md, NO le pasa flags extra (su `argument-hint` es `<exercise-id> [--dry-run]` y eso se respeta)."

    **Sección `<execution>`** — pasos numerados con tools concretos, siguiendo el esqueleto de RESEARCH Q1 §execution:

    - **Paso 1 — Resolver scope:**
      - Si `<category>`: cargar `content/exercises/<category>.json` (vía Read tool).
      - Si `--all-pending`: iterar el orden lockeado D-VAL-22 (preposiciones → avere → essere → genero-numero → profesiones → sustantivos-irregulares → verbos-movimiento).
      - Si `<id1,id2,...>`: para cada ID, resolver su archivo de categoría con Glob + JSON.parse.
      - Validar que cualquier slug recibido es uno de los 7 ASCII conocidos; cualquier otro → exit con mensaje en español.

    - **Paso 2 — Para cada categoría en scope:**
      - **2.1 Pre-flight check** (solo si `cat === "avere"`): ejecutar `node scripts/assert-avere-prefix-unchanged.mjs` con Bash tool y verificar exit 0. Si exit != 0 → STOP batch con mensaje en español "El invariante D-88 AVERE prefix está roto pre-batch — investigar manualmente antes de continuar".
      - **2.2 Leer JSON** con Read tool, parsear con `node -e` oneliner (zero-deps) o JSON.parse directa.
      - **2.3 Filtrar pendientes:** `data.exercises.filter(ex => ex.validation?.status !== "validated")`.
      - **2.4 Si lista vacía:** imprimir "Categoría <cat> ya completa (X/X validated)" y pasar a la siguiente.
      - **2.5 Para cada ID pendiente:**
        - **2.5.1** Invocar el skill hijo via Skill tool: `gsd-validate-exercise <id>`. El skill hijo internamente hace todo (2 Task() Opus+Sonnet, parsea, deriveStatus, Edit JSON, commit atómico).
        - **2.5.2** Re-leer el JSON con Read tool para conocer el status resultante.
          - Si `status === "disputed"`: empujar a `disputedQueue[cat]`.
          - Si `status === "pending"` (parse-failed double): empujar a `pendingQueue[cat]` (NO bloquea — sigue iterando; el autor revisa al final).
          - Si `status === "validated"`: continúa.
      - **2.6 Pre-cola: AVERE assert al cierre** (solo si `cat === "avere"`): ejecutar `node scripts/assert-avere-prefix-unchanged.mjs` con Bash; si exit != 0 banner amarillo + AskUserQuestion 3 opciones: "Regenerar snapshot con `node scripts/snapshot-avere-prefix.mjs`", "Revertir último commit que tocó el prefix", "Pausar batch para revisar manualmente".
      - **2.7 Schema-validation defensive** (todas las categorías): ejecutar `node scripts/validate-content-fixture.mjs` con Bash; si exit != 0 banner amarillo "JSON corrompido por algún Edit — revisar `git diff HEAD~N`" y AskUserQuestion (Continuar / Pausar).
      - **2.8 Emitir tabla resumen en español** (formato literal):
        - `Categoría <cat> cerrada: X/Y validated • Z disputed • W pending`
        - Lista explícita de IDs disputed.
      - **2.9 Reconsider trigger D-VAL-21** (solo si `cat === "preposiciones"`):
        - Computar `dispute-rate = disputed_count / total_pending_in_category`.
        - Si `dispute-rate < 0.05` AND los 3 motivadores históricos (`preposiciones-031`, `preposiciones-032`, `preposiciones-047`) salieron `validated` (sin entrar a disputed): banner amarillo "Dispute-rate inusualmente bajo en la categoría con más bugs históricos. ¿Pausar y reconsiderar añadir Gemini antes de las 6 categorías restantes?" + AskUserQuestion 2 opciones: "Continuar con avere" / "Pausar para reconsiderar".
      - **2.10 Si disputedQueue[cat].length > 0:** procesar cola (Paso 3).
      - **2.11 AskUserQuestion checkpoint final de categoría:** "Categoría <cat> cerrada (X validated, Y disputed resueltos, Z pendientes/deferred). ¿Continuar con la siguiente categoría <next-cat>?" Opciones: "Continuar" / "Pausar".

    - **Paso 3 — Procesar cola disputed (D-VAL-24..26):**
      - Para cada ID en `disputedQueue[cat]` (secuencial 1-por-1, NO agrupado — D-VAL-26 deferred decidido secuencial):
        - **3.1 Imprimir banner pretty-print en español** (formato literal con separadores `━━━━━`), incluyendo:
          - Header: ID, categoría, posición (X/Y de la cola).
          - Prompt + Opciones (con índices [a]/[b]/[c]/[d]) + Correcta (índice + valor) + Explicación.
          - Verdict y Concerns de cada pase (Opus y Sonnet — con model ID literal).
          - Suggested fix derivado determinísticamente del PRIMER tag `[Cn-criterio]` en el concerns del PRIMER pase con `verdict: "incorrecta"`. Regex: `/^\[(C[1-5]-[a-z_]+)\]/`. Mapping verbatim D-VAL-26 (cinco bullets en el interfaces block del context).
          - Si ningún concern tiene tag parseable: "Sin suggested fix automatizable — revisa concerns manualmente."
        - **3.2 AskUserQuestion con 4 opciones literales en español** (texto EXACTO):
          - "Accept fix"
          - "Reject + override"
          - "Rewrite manualmente"
          - "Skip (defer al final del milestone)"
        - **3.3 Según respuesta:**
          - **(a) Accept fix:** (i) Edit tool aplica el fix al payload del ejercicio según el tag [Cn-criterio]. (ii) Edit tool resetea `validation: {status:"pending", passes:[]}` (BYPASS sticky D-VAL-07 — la responsabilidad del caller per Phase 9 SKILL.md Paso 8). (iii) `git commit -m "fix(<cat>): <id> — autor acepta sugerencia post-disputed ([Cn-...])"`. (iv) Invocar Skill: `gsd-validate-exercise <id>` (que añade 2 nuevas pases sobre array vacío y commitea su propio mensaje). (v) `git commit --amend -m "<prev-subject> — POST-fix (accept fix tras disputed)"` para añadir el sufijo POST-fix al commit del skill hijo.
          - **(b) Reject + override:** (i) AskUserQuestion follow-up: "¿Razón del override (una línea)?" — captura string. (ii) Edit tool APPEND al `passes[]` la entry `{by:"autor", date:"<ISO hoy>", verdict:"correcta", concerns:["[override] autor mantiene original tras revisión: <razón>"]}`. (iii) Edit tool: `validation.status = "validated"` DIRECTAMENTE (BYPASS deriveStatus — porque sticky daría disputed). (iv) `git commit -m "validate(<cat>): <id> → validated (override autor, post-disputed)"`. NO re-valida (el autor ES el oracle final).
          - **(c) Rewrite manualmente:** (i) Imprimir instrucciones: "Abre `content/exercises/<cat>.json` y edita el ejercicio <id>. Modifica `payload.prompt`, `options`, `correctIndex` o `explanation` a tu criterio. NO toques el campo `validation`." (ii) AskUserQuestion: "¿Has guardado los cambios?" con opciones "Sí, ya guardé y re-validar" / "No, cancelar (saltar al siguiente disputed)". (iii) Si "Sí": Edit tool resetea `validation: {status:"pending", passes:[]}`. (iv) `git commit -m "rewrite(<cat>): <id> — autor reescribe post-disputed"`. (v) Invocar Skill: `gsd-validate-exercise <id>`. (vi) `git commit --amend -m "<prev-subject> — POST-rewrite"`. Si la re-validación vuelve a salir disputed → el ID vuelve a `disputedQueue[cat]` para el siguiente ciclo (no se cierra hasta validated o skip/defer).
          - **(d) Skip / defer:** SIN mutación al JSON. Append a `.planning/STATE.md` sección `## Deferred-disputed` (crear si no existe) una línea: `- <id> (<ISO hoy>): deferred por autor — razón: \"<razón opcional>\"`. NOTA: VAL-06 impide cerrar el milestone con deferred → el autor lo verá en el reporter final.

    - **Paso 4 — Al terminar el scope:**
      - Imprimir tabla agregada en español: 7 filas (1 por categoría procesada en el scope), columnas: validated_count, disputed_resolved_count, disputed_deferred_count, pending_count, total.
      - Si todas las categorías procesadas son validated y deferred==0 y pending==0: sugerir literalmente: `node scripts/run-validation-271.mjs` (para gate final) seguido de `VAL_07_STRICT=1 node --test tests/*.test.js` (smoke test estricto manual — gesto consciente del autor).
      - Actualizar `.planning/STATE.md` con un append a `## Current Position`: `Phase 10 — Categoría <cat> cerrada (<fecha>): X/Y validated, Z disputed resueltos, W deferred`.

    **Sección `<error_handling>`** — tabla markdown 3 columnas `Caso | Acción | Mensaje al autor (español)`, cubriendo al menos:
    - Slug de categoría desconocido → exit 1 + lista de slugs válidos.
    - Skill hijo crashea sobre un ID → log + continuar con siguiente (NO bloquea batch).
    - Resultado del skill hijo es `pending` (parse-failed double) → empuja a pendingQueue, sigue iterando.
    - `assert-avere-prefix-unchanged.mjs` exit != 0 → STOP + 3 opciones.
    - `validate-content-fixture.mjs` exit != 0 post-categoría → banner amarillo + Continuar/Pausar.
    - Autor responde "Pausar" en cualquier checkpoint → exit limpio con mensaje "Re-invoca `/gsd-validate-batch <scope>` para continuar (resume idempotente garantizado)".

    **Sección `<workflow_justification_no_batched>`** — 1-2 párrafos en español replicando la justificación verbatim del skill Phase 9 (línea 211-225), adaptada al batch: "el sub-skill ITERA N veces pero el skill hijo `gsd-validate-exercise` que invoca SIGUE siendo single-exercise con context aislado por Task() spawn. El batch NO compone N ejercicios en mismo subagent — sería un regreso al bug class de los 4 motivadores."

    **Sección `<read_first_per_invocation>`** — lista numerada:
    1. El archivo JSON de la categoría del scope actual (e.g. `content/exercises/preposiciones.json`).
    2. `src/data/validation-state.js` (para entender el comportamiento de `deriveStatus` al verificar BYPASS).
    3. NO leer `VALIDATION-PROMPT.md` directamente — eso lo hace el skill hijo.

    **Sección `<example_invocations>`** — 4 bloques con `# Comando del autor:` + `# Lo que hace el skill:`:
    1. `/gsd-validate-batch preposiciones` — 49 pendientes de la primera categoría D-VAL-22.
    2. `/gsd-validate-batch --all-pending` — encadena las 7 con checkpoints intermedios.
    3. `/gsd-validate-batch preposiciones-031,preposiciones-032,preposiciones-047` — re-validar IDs específicos (caso path-A/C re-trigger).
    4. `/gsd-validate-batch avere` — incluye pre-flight AVERE assert + assert al cierre.

    **Sección `<gate_reminder>`** (opcional, replicando el skill Phase 9): 1 párrafo en español enlazando al reporter Plan 10-02 como gate del milestone.
  </action>
  <verify>
    <automated>test -f .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; head -20 .claude/skills/gsd-validate-batch/SKILL.md | grep -q "name: gsd-validate-batch" &amp;&amp; head -20 .claude/skills/gsd-validate-batch/SKILL.md | grep -q "argument-hint" &amp;&amp; grep -q "AskUserQuestion" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "Skill" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; ! grep -q "context: fork" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "preposiciones" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "avere" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "essere" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "genero-numero" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "profesiones" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "sustantivos-irregulares" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "verbos-movimiento" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "Accept fix" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "Reject + override" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "Rewrite manualmente" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "Skip (defer al final del milestone)" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "assert-avere-prefix-unchanged" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "deriveStatus" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "D-VAL-19" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "D-VAL-20" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "D-VAL-21" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "D-VAL-22" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "D-VAL-25" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; grep -q "D-VAL-26" .claude/skills/gsd-validate-batch/SKILL.md &amp;&amp; [ $(wc -l &lt; .claude/skills/gsd-validate-batch/SKILL.md) -ge 250 ]</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.claude/skills/gsd-validate-batch/SKILL.md` existe y tiene ≥250 líneas.
    - El frontmatter contiene `name: gsd-validate-batch`, `argument-hint: "<category> | --all-pending | <id1,id2,...>"`, `disable-model-invocation: true`, y `allowed-tools` con los 9 valores Read/Write/Edit/Bash/Glob/Grep/Task/AskUserQuestion/Skill.
    - El archivo NO contiene la cadena `context: fork` (invariante arquitectónico RESEARCH Q1/Q2).
    - El body referencia explícitamente las 8 decisiones D-VAL-19, D-VAL-20, D-VAL-21, D-VAL-22, D-VAL-23, D-VAL-24, D-VAL-25, D-VAL-26 (grep encuentra cada ID).
    - El body contiene los 4 caminos D-VAL-25 documentados como opciones literales en español: `Accept fix`, `Reject + override`, `Rewrite manualmente`, `Skip (defer al final del milestone)`.
    - El body contiene los 7 slugs de categoría D-VAL-22 (preposiciones, avere, essere, genero-numero, profesiones, sustantivos-irregulares, verbos-movimiento).
    - El body incluye el reconsider trigger D-VAL-21 referenciando los 3 motivadores históricos (`preposiciones-031`, `preposiciones-032`, `preposiciones-047`) y el threshold `5%` (o `<5%` o `0.05`).
    - El body incluye instrucciones para el pre-flight `node scripts/assert-avere-prefix-unchanged.mjs` antes de procesar avere.
    - El body incluye instrucciones para el assert AVERE al cierre de la categoría avere.
    - El body referencia `deriveStatus` (la función pura del módulo Phase 9) al menos una vez.
    - El skill base `.claude/skills/gsd-validate-exercise/SKILL.md` NO ha sido modificado (verificable con `git diff .claude/skills/gsd-validate-exercise/SKILL.md` → vacío).
    - El frontmatter YAML es válido: `node -e "console.log(JSON.parse(JSON.stringify({a:1})))"` no aplica aquí; en su lugar, el sub-skill debe poder ser invocable por Claude Code (verificable de facto por el grep de los campos requeridos `name:`, `description:`, `argument-hint:`, `allowed-tools:`).
  </acceptance_criteria>
  <done>
    El archivo `.claude/skills/gsd-validate-batch/SKILL.md` existe con frontmatter válido, body ≥250 líneas siguiendo la estructura GSD del skill Phase 9, todas las 8 decisiones D-VAL-19..26 documentadas, 4 caminos disputed implementados como instrucciones secuenciales con tools concretos, reconsider trigger D-VAL-21 encoded, pre-flight + cierre AVERE asserts encoded, y zero modificaciones al skill base Phase 9.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill→Task() (Opus, Sonnet) | El skill hijo `gsd-validate-exercise` (intocable) pasa el ejercicio + VALIDATION-PROMPT al subagent. Phase 9 ya hardened este boundary; Phase 10 no lo cruza directamente. |
| autor→AskUserQuestion | El autor responde con texto/opción libre al banner disputed. El sub-skill debe sanitizar la "razón del override" path-B antes de inyectarla a un string que llega al JSON. |
| sub-skill→Edit tool (JSON) | El sub-skill muta `content/exercises/*.json` en paths a/c (reset passes) y b (append override). Si el Edit corrupta el JSON, el smoke test paramétrico VAL-07 lo detectará al cierre. |
| sub-skill→Bash (assert scripts) | Ejecución de `assert-avere-prefix-unchanged.mjs` y `validate-content-fixture.mjs`. Son scripts del proyecto, NO command injection (zero-deps, sin user-input variable). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | sub-skill Edit tool sobre JSON | mitigate | Schema validator (`validate-content-fixture.mjs`) corre defensive POST-cada-categoría (Paso 2.7). Si Edit corrompe el JSON, banner amarillo + Continuar/Pausar antes de seguir. |
| T-10-01-02 | Repudiation | path-B override sin razón | mitigate | AskUserQuestion follow-up OBLIGATORIO ("¿Razón del override (una línea)?") — si el autor responde vacío, el concern queda con "[override] autor mantiene original tras revisión: (sin razón)" pero la entry queda registrada con timestamp ISO. Audit trail no-vacío. |
| T-10-01-03 | Information Disclosure | sub-skill expone passes[] originales en banner disputed | accept | Los pases con `verdict:"incorrecta"` y concerns son DATOS PÚBLICOS del autor sobre sus propios ejercicios (proyecto local, single-user). NO hay PII. |
| T-10-01-04 | Denial of Service | autor responde "Pausar" en bucle infinito | accept | El batch sale limpio; el autor re-invoca a su criterio. NO hay daño ni pérdida (resume idempotente D-VAL-19). |
| T-10-01-05 | Elevation of Privilege | `context: fork` añadido accidentalmente | mitigate | `<critical_constraints>` documenta explícitamente "NUNCA `context: fork`" + verify automatizado en el plan (`! grep -q "context: fork"`). |
| T-10-01-06 | Spoofing | model ID inventado en `passes[].by` por path-B (autor) | accept | El schema validator acepta `"autor"` como string válido (no enforça whitelist de model IDs). El reporter Plan 10-02 cuenta `"autor"` como una de las ≥2 distinct by, lo cual es semánticamente correcto (el autor ES el oracle final). |
| T-10-01-SC | Tampering | npm/pip/cargo installs | accept | NO HAY package installs en este plan — el sub-skill es 100% Markdown + tools nativas + Node builtins. Zero-deps invariant del proyecto. |
</threat_model>

<verification>
1. El archivo `.claude/skills/gsd-validate-batch/SKILL.md` existe y es legible.
2. Frontmatter contiene los 5 campos requeridos (`name`, `description`, `argument-hint`, `disable-model-invocation`, `allowed-tools`).
3. `allowed-tools` incluye los 9 valores requeridos (R/W/E/B/G/G/T + AskUserQuestion + Skill).
4. `context: fork` NO aparece en ninguna línea del archivo.
5. Las 8 decisiones D-VAL-19..26 aparecen referenciadas por ID en el body.
6. Los 7 slugs de categoría D-VAL-22 aparecen en orden lockeado en el body.
7. Los 4 textos literales del AskUserQuestion disputed (`Accept fix`, `Reject + override`, `Rewrite manualmente`, `Skip (defer al final del milestone)`) aparecen verbatim.
8. Las referencias a los 3 motivadores históricos preposiciones-031/-032/-047 + el threshold 5% aparecen en la sección del reconsider trigger.
9. `git diff .claude/skills/gsd-validate-exercise/SKILL.md` es vacío (el skill base Phase 9 sigue intocable).
10. El reporter Plan 10-02 (Wave 1 paralelo) NO depende de este archivo en tiempo de planning — solo en tiempo de ejecución del batch (Wave 2).
</verification>

<success_criteria>
- Sub-skill `gsd-validate-batch` cargable por Claude Code con frontmatter válido.
- Body completo cubre los 4 pasos del bucle + cola disputed + 4 caminos D-VAL-25 + reconsider trigger D-VAL-21 + pre-flight/cierre AVERE asserts.
- Zero-deps invariant respetado: el SKILL.md no instruye ningún `npm install` ni añade `package.json`.
- El skill base Phase 9 NO ha sido modificado.
</success_criteria>

<output>
Create `.planning/phases/10-ejecuci-n-validaci-n-271-ejercicios-escalada-disputed/10-01-SUMMARY.md` when done.
</output>
</content>
</invoke>