---
phase: 19-articoli-a-slots-contenido
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - content/exercises/articoli.json
  - .planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md
autonomous: false
requirements: [ART-02, ART-03]
must_haves:
  truths:
    - "Cada variante NUEVA y cada slot NUEVO autorado pasa el quórum cross-vendor R1-R7 (Gemini + DeepSeek vía validate-ai-pass + Claude Opus+Sonnet vía skill gsd-validate-exercise) — todos 'correcta', cero 'incorrecta', ANTES de entrar al slot"
    - "Existen slots NUEVOS para los huecos de disparador y / i+vocal en la serie lo/gli (lo yogurt / gli yogurt; lo iodio donde aplique), con artículo+sustantivo italiano verificado (R6, D-19-06)"
    - "Las celdas pobres (lo+ps, lo+gn, lo+x, uno+ps, uno+gn) quedan engordadas a >=2 variantes donde la regla admite reformulación natural (D-19-05); las excepciones idiomáticas no se inflan artificialmente"
    - "Las variantes nuevas se autoran SOLO donde la regla admite reformulación natural; ninguna lleva explanation propia (comparten la del slot)"
  artifacts:
    - path: "content/exercises/articoli.json"
      provides: "slots nuevos y/i+vocal + variantes nuevas validated integradas en variants[]"
      contains: "yogurt"
  key_links:
    - from: "cada variante/slot nuevo"
      to: "validation.passes[]"
      via: "quórum cross-vendor (>=4 pases by distintos, todos correcta)"
      pattern: "\"verdict\"\\s*:\\s*\"correcta\""
---

<objective>
Autorar variantes NUEVAS donde la regla admite reformulación natural (ART-02, D-19-05) priorizando engordar las celdas pobres (lo+ps, lo+gn, lo+x, uno+ps, uno+gn) a >=2 variantes, y añadir los slots NUEVOS para los huecos de disparador `y` / `i+vocal` en la serie lo/gli (D-19-06: lo yogurt / gli yogurt; lo iodio donde aplique). Cada superficie NUEVA pasa el quórum cross-vendor completo R1-R7 (D-17-07) ANTES de integrarse al slot — 1-por-1, fresh context, NUNCA batched, con revisión del autor por variante (D-85). Las superficies movidas intactas en 19-01 NO se re-validan.

Purpose: las variantes nuevas materializan el dolor anti-memorización (re-hacer un slot puede tocar una superficie distinta). Los huecos y/i+vocal cierran la serie de suoni speciali completa (completitud de la regla por encima de acotar el alcance, según los Specific Ideas del autor). El quórum cross-vendor es el cazador de bugs documentado (DeepSeek estricto en acentos, Opus indulgente; cazó 6 bugs en el piloto que el human-verify aprobó). R6 es crítico aquí: los semiconsonánticos y/i+vocal son terreno de error fácil — verificar el artículo+sustantivo italiano antes de meterlos.

Output: `articoli.json` con los slots nuevos y/i+vocal + las variantes nuevas validated integradas; cada superficie nueva con su quórum verde en `validation.passes[]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-articoli-a-slots-contenido/19-CONTEXT.md
@.planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@content/exercises/articoli.json

<interfaces>
<!-- Pipeline de quórum cross-vendor. Verificado en validate-ai-pass.mjs + skill (PATTERNS §Cross-vendor quorum pipeline). NO modificar estos scripts/skill. -->
Quórum por cada superficie NUEVA = 2 mitades, AMBAS deben dar "correcta":

Mitad 1 — externos (Gemini + DeepSeek):
  node scripts/validate-ai-pass.mjs <id> --model=gemini-2.5-flash --fallback=deepseek-chat --write
  node scripts/validate-ai-pass.mjs <id> --model=deepseek-chat --avoid=gemini-2.5-flash --write
  (--write muta validation.passes[] in-place con anchor "id":"<id>"; el `by` registrado es el modelo que respondió; >=2 by distintos para el quórum; status re-derivado por deriveStatus, NO hand-editar status)

Mitad 2 — Claude (skill gsd-validate-exercise, 1 spawn por superficie, secuencial Opus→Sonnet):
  /gsd-validate-exercise <id>
  (claude-opus-4-7 + claude-sonnet-4-6 literales; 1-por-1 fresh context NUNCA batched; 1 commit por ejercicio)

Gate D-17-07: el passes[] final de cada superficie nueva = >=4× "correcta" (gemini + deepseek + opus + sonnet, by distintos), CERO "incorrecta". deriveStatus → "validated"; cualquier "incorrecta" → "disputed" (sticky, bloquea, NO entra al slot).

Pitfall (PATTERNS): el VALIDATION-PROMPT es legacy-payload-céntrico. Validar cada superficie nueva como un EJERCICIO MULTIPLE-CHOICE LEGACY AISLADO (un id temporal con `payload:{prompt,options,correctIndex,explanation}` en content/exercises/articoli.json o tests/fixtures/*.json — el script escanea ambos) ANTES de moverla al slot. Tras pasar el quórum, mover la superficie al variants[] del slot (sin re-validación) y eliminar el id temporal legacy.

Alcance D-19-05 (priorizar celdas pobres, sin cuota rígida):
- Celdas pobres a engordar a >=2 variantes (reformular el MISMO sub-disparador con otro sustantivo del mismo disparador): lo+ps (otro sust. ps-), lo+gn (otro sust. gn-), lo+x (otro sust. x-), uno+ps, uno+gn. Verificar que el sustantivo italiano existe y dispara realmente esa serie.
- NO inflar slots cuya regla no admite reformulación natural (excepciones idiomáticas únicas). Pragmático como el piloto.

Slots NUEVOS de huecos (D-19-06, R6 crítico):
- `lo` + y / i+vocal (sing): lo yogurt, lo iodio (i+vocal) → slot(s) nuevo(s). VERIFICAR artículo+sustantivo (R6).
- `gli` + y / i+vocal (plural): gli yogurt → slot nuevo (D-19-02: gli su propio sub-disparador).
  categoryIds=["articoli"]; explanation de slot nueva (suono semiconsonantico y/i+vocal dispara la serie lo/gli).

Complemento de scan de acentos (MEMORY): DeepSeek estricto en acentos, Opus indulgente; antes del quórum, un scan ASCII/tildes (RAE en explanations españolas, apóstrofes U+0027, sin smart-quotes/markdown) evita gastar un pase en un fallo trivial.
Gloss ES (R7 canon): mantener "(en español: …)" donde desambigüe doble-validez; el C5-leak que marquen Gemini/DeepSeek sobre el gloss es falso-positivo de política (base de aprobación = Claude Opus+Sonnet).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proponer variantes nuevas (celdas pobres) + slots de huecos y/i+vocal</name>
  <files>.planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md</files>
  <read_first>
    - content/exercises/articoli.json (estado tras 19-01 — los slots ya reagrupados, para saber qué slots existen, cuáles son celdas pobres y qué admite variante nueva)
    - .planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md (las celdas pobres marcadas como candidatas en 19-01)
    - .planning/phases/19-articoli-a-slots-contenido/19-CONTEXT.md §decisions (D-19-05 alcance/celdas pobres, D-19-06 huecos y/i+vocal, D-17-07 quórum, R7 gloss)
    - .planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md §Cross-vendor quorum (R6 crítico para y/i+vocal, invocación, scan de acentos)
    - $HOME/.claude/projects/-home-vcompanyb-italian-course/memory/exercise_authoring_rules.md (R1-R6 si accesible) o la sección R1-R7 del 09-VALIDATION-PROMPT
  </read_first>
  <action>
    Proponer un set CONSERVADOR de superficies nuevas (D-19-05). (1) Para cada celda pobre marcada en 19-01 (lo+ps, lo+gn, lo+x, uno+ps, uno+gn): redactar 1 variante nueva (superficie `{prompt con ___, options 3-4, correctIndex}`) que reformule el MISMO sub-disparador con otro sustantivo italiano que realmente dispare esa serie. (2) Redactar los slots NUEVOS de huecos y/i+vocal (D-19-06): lo yogurt / gli yogurt (y semiconsonántica), lo iodio (i+vocal) donde aplique — con su explanation de slot nueva. VERIFICAR explícitamente (R6) que el artículo y el sustantivo italiano son correctos para cada hueco (el yogurt/lo yogurt; gli yogurt). Cumplir R1-R7 (sin leak de la respuesta en el prompt, sin refs #NNN en explanations, options con 3+ valores distintos, gloss ES donde desambigüe). Pasar un scan de acentos/ASCII (tildes RAE en explanations españolas, apóstrofes U+0027, sin smart-quotes ni markdown) sobre cada superficie/explanation nueva. Producir `.planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md`: lista de cada superficie nueva con su id temporal (para el quórum aislado), slot destino (existente o nuevo), prompt/options/correctIndex/explanation, y la justificación de por qué la regla admite reformulación / por qué el artículo italiano es correcto (R6). Reportar el conteo de superficies nuevas propuestas (driver del coste de quórum y del count final de 19-03).
  </action>
  <verify>
    <automated>test -f .planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md && grep -ci "yogurt\|iodio\|ps\|gn" .planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md</automated>
  </verify>
  <acceptance_criteria>
    - 19-VARIANTES-NUEVAS.md existe y lista cada superficie nueva con id temporal + slot destino + superficie + explanation
    - Los slots de huecos y/i+vocal están redactados (lo yogurt / gli yogurt como mínimo) con verificación R6 explícita del artículo+sustantivo italiano
    - Las celdas pobres (lo+ps, lo+gn, lo+x, uno+ps, uno+gn) tienen al menos una variante nueva propuesta cada una (engorde a >=2, D-19-05)
    - Ninguna superficie nueva se propone en una regla que no admite reformulación natural (alcance conservador D-19-05)
    - Cada explanation/prompt nueva pasa scan de acentos: sin smart-quotes (`grep -P '[\x{2018}\x{2019}\x{201C}\x{201D}]'` da 0), sin markdown markers
    - El conteo de superficies nuevas propuestas está reportado
  </acceptance_criteria>
  <done>
    19-VARIANTES-NUEVAS.md lista el set conservador de superficies nuevas (celdas pobres engordadas + slots de huecos y/i+vocal con R6 verificada) con superficies R1-R7-conformes y scan de acentos pasado; conteo reportado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El set propuesto de superficies nuevas (19-VARIANTES-NUEVAS.md) — variantes para engordar celdas pobres + los slots nuevos de huecos y/i+vocal, cumpliendo R1-R7 y con scan de acentos. AÚN NO validadas por quórum.</what-built>
  <how-to-verify>
    1. Abre `.planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md`.
    2. Revisa cada variante nueva de celda pobre: ¿el sustantivo dispara DE VERDAD ese sub-sonido (s+cons/z/gn/ps/x)? ¿la respuesta correcta es inequívoca (sin doble-validez R7)?
    3. Revisa los slots de huecos y/i+vocal: ¿`lo yogurt`/`gli yogurt`/`lo iodio` son el artículo correcto? (R6 — terreno de error fácil; los semiconsonánticos confunden).
    4. Confirma el alcance: ¿es conservador (no infla con variantes artificiales en reglas que no se reformulan)?
    5. Marca cualquier superficie a descartar/reformular antes de gastar tokens de quórum.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para enviar las superficies aprobadas al quórum cross-vendor, o lista las que descartar/reformular.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validar cada superficie nueva por quórum cross-vendor (1-por-1) e integrarla al slot</name>
  <files>content/exercises/articoli.json</files>
  <read_first>
    - .planning/phases/19-articoli-a-slots-contenido/19-VARIANTES-NUEVAS.md (las superficies aprobadas por el autor)
    - .claude/skills/gsd-validate-exercise/SKILL.md (constraints: 1-por-1, fresh context, NUNCA batched, model IDs literales claude-opus-4-7/claude-sonnet-4-6, 1 commit por ejercicio)
    - .planning/phases/19-articoli-a-slots-contenido/19-PATTERNS.md §Cross-vendor quorum pipeline (invocación exacta de validate-ai-pass.mjs + validar como legacy aislado + gate D-17-07 + R6/acentos/R7 gloss)
    - content/exercises/articoli.json (slots destino donde integrar las superficies tras pasar el quórum)
  </read_first>
  <action>
    Para CADA superficie nueva aprobada, ejecutar el ciclo completo de quórum 1-por-1 (NUNCA batched): (1) materializar la superficie como un ejercicio multiple-choice legacy aislado con un id temporal y `payload:{prompt,options,correctIndex,explanation}` en un archivo que `validate-ai-pass.mjs` escanee (content/exercises/articoli.json como entry temporal o tests/fixtures/*.json — el VALIDATION-PROMPT es legacy-payload-céntrico, así ve el shape que espera). (2) Mitad 1 externos: `node scripts/validate-ai-pass.mjs <id-temporal> --model=gemini-2.5-flash --fallback=deepseek-chat --write` y luego `--model=deepseek-chat --avoid=gemini-2.5-flash --write`. (3) Mitad 2 Claude: `/gsd-validate-exercise <id-temporal>` (skill, Opus→Sonnet secuencial, 1 spawn). (4) Verificar el gate D-17-07: el passes[] resultante tiene >=4× "correcta" (by distintos), CERO "incorrecta". Si alguna `incorrecta` → la superficie queda disputed y se resuelve (calidad > tokens; incluir gloss ES para desambiguar doble-validez R7; el C5-leak sobre el gloss es falso-positivo de política — NO override-atajo) o se descarta — NO entra al slot disputed. (5) Tras pasar el quórum, mover la superficie validada al variants[] del slot destino (existente, para celdas pobres) o crear el slot nuevo `{id, type:"multiple-choice", categoryIds:["articoli"], explanation, variants:[...]}` (para huecos y/i+vocal); eliminar los ids temporales legacy. Las variantes en variants[] NO llevan explanation ni validation propia (el slot las lleva). Hacer 1 commit por superficie validada. NUNCA validar más de una superficie en un mismo contexto/spawn.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` exits 0
    - Los slots de huecos existen: `grep -c "yogurt" content/exercises/articoli.json` >= 1 (lo yogurt / gli yogurt presentes)
    - Las celdas pobres engordadas tienen >=2 variantes (verificable contando variants[] de los slots lo+ps/lo+gn/lo+x/uno+ps/uno+gn en el JSON)
    - Cada superficie NUEVA pasó el quórum con >=4× "correcta" cero "incorrecta" ANTES de integrarse (registrado en el SUMMARY con el passes[] de cada una; el id temporal aislado se validó como legacy)
    - Ningún id temporal legacy queda en el JSON: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Las superficies existentes movidas en 19-01 NO se re-validaron (# invocaciones de quórum == # superficies nuevas)
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/articoli.json` da 0 matches
  </acceptance_criteria>
  <done>
    Todas las superficies nuevas aprobadas pasaron el quórum cross-vendor (>=4× correcta) e integradas a sus slots; slots de huecos y/i+vocal creados (R6 verificada); celdas pobres engordadas a >=2 variantes; validateContent verde; sin ids temporales residuales; 1 commit por superficie.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto del ejercicio → evaluador del quórum (LLM) | El contenido del ejercicio se pasa a Gemini/DeepSeek/Claude; un prompt podría intentar dirigir al evaluador |
| JSON de contenido editado a mano → loader/validator | superficies nuevas redactadas a mano cruzan a validateContent y al render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | superficie nueva con respuesta ambigua (doble-validez R7), artículo italiano incorrecto (R6, huecos y/i+vocal) o acento incorrecto entra al slot | mitigate | Quórum cross-vendor completo D-17-07 (DeepSeek estricto en acentos caza lo que Opus deja pasar); R6 verificada en Task 1 + checkpoint para los huecos semiconsonánticos; pre-scan de acentos; gate de >=4× "correcta" cero "incorrecta" antes de integrar; disputed bloquea. NUNCA batched. |
| T-19-06 | Tampering | prompt-injection en el quórum | mitigate | El VALIDATION-PROMPT trata el contenido del ejercicio como DATA, no instrucción; contenido autorado por el propio autor (single-user, sin input externo), riesgo bajo; el guard del prompt lo cubre. |
| T-19-03 | Tampering | explanation/prompt nuevos con HTML/markdown/smart-quotes | mitigate | Scan de acentos/ASCII en Task 1 (U+0027, sin markdown); el smoke de 19-03 re-asserta sobre variants[].prompt + explanation. |
| T-19-SC | Tampering | npm/pip/cargo installs; uso de claves .env (GEMINI/DEEPSEEK) | accept | No aplica install — zero-deps. `.env` solo se LEE (no se instala nada); claves del autor, single-user offline. |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` exits 0
- Cada superficie nueva: passes[] con >=4× "correcta", cero "incorrecta" (registrado en SUMMARY)
- Slots de huecos presentes (lo yogurt / gli yogurt); celdas pobres engordadas a >=2 variantes
- `node --test tests/*.test.js` — el count de Articoli sigue rojo a propósito hasta 19-03 (el conteo final ya es determinable tras este plan)
</verification>

<success_criteria>
- Variantes nuevas autoradas solo donde la regla admite reformulación (D-19-05, conservador, priorizando celdas pobres)
- Slots nuevos y/i+vocal creados con R6 verificada (D-19-06)
- Cada superficie nueva pasó el quórum cross-vendor R1-R7 (>=4× correcta) antes de entrar
- Superficies existentes movidas NO re-validadas
- validateContent verde; conteo final de slots determinable para 19-03
</success_criteria>

<output>
Create `.planning/phases/19-articoli-a-slots-contenido/19-02-SUMMARY.md` when done
</output>
