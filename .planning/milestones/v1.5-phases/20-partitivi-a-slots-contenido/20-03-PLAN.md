---
phase: 20-partitivi-a-slots-contenido
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - tests/exercise-types.test.js
  - tests/fixtures/slot-variants-integration.test.js
  - scripts/run-validation-271.mjs
autonomous: true
requirements: [PART-03]
must_haves:
  truths:
    - "Los 3 hardcodes de count de Partitivi (expected: 44 en exercise-types.test.js:1274, slot-variants-integration.test.js:175 y run-validation-271.mjs:91) estan sincronizados contra el conteo REAL final de slots leido del JSON (no una estimacion)"
    - "TOTAL_EXPECTED en run-validation-271.mjs esta recalculado = 348 - 44 + n_slots_final_partitivos"
    - "El smoke parametrico ya bifurca por shape (Array.isArray(ex.variants)) — heredado del piloto/Articoli; Partitivi convertido a slots pasa coverage/R1/R2/smart-quotes/markdown sin tocar la logica del test, solo el count"
    - "node --test tests/*.test.js, VAL_07_STRICT=1 node --test tests/*.test.js y node scripts/run-validation-271.mjs pasan verdes; la cobertura de explanations por slot preservada"
  artifacts:
    - path: "tests/exercise-types.test.js"
      provides: "count de Partitivi en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots"
      contains: "partitivos.json"
    - path: "tests/fixtures/slot-variants-integration.test.js"
      provides: "REAL_CATEGORIES expected de partitivos sincronizado al n de slots"
    - path: "scripts/run-validation-271.mjs"
      provides: "expected partitivos + TOTAL_EXPECTED recalculado"
      contains: "TOTAL_EXPECTED"
  key_links:
    - from: "los 3 hardcodes de count + TOTAL_EXPECTED"
      to: "data.exercises.length real de partitivos.json"
      via: "sincronizacion contra el conteo REAL del JSON, no estimacion"
      pattern: "partitivos"
---

<objective>
Re-sincronizar los 3 hardcodes de count de Partitivi (`expected: 44`) + `TOTAL_EXPECTED` contra el conteo REAL final de slots de `partitivos.json` leido del JSON tras 20-01/20-02 (NO una estimacion), cerrando PART-03 y el milestone v1.5. El smoke parametrico YA es shape-agnostic (la bifurcacion `Array.isArray(ex.variants)` se introdujo en el piloto Phase 17, se reuso en Articoli Phase 19, y esta en el codebase — verificado en exercise-types.test.js getExplanation/getPrompts), asi que Partitivi convertido a slots pasa coverage/R1/R2/smart-quotes/markdown/VAL_07 SIN tocar la logica del test: solo cambian los counts. El validator y el loader NO se tocan (ya aceptan variants[]).

Purpose: convertir Partitivi rompe los 3 counts hoy verdes (44 replicado en 3 archivos + TOTAL_EXPECTED=348 que incluye Articoli=34). PART-03 ES re-sincronizarlos contra el conteo real de slots + dejar el validator y el smoke verdes con la cobertura de explanations por slot preservada. Es el gate final de la fase y del milestone v1.5: estructura final pasa validator + smoke parametrico.

Output: los 3 test/script files con el count de Partitivi + TOTAL_EXPECTED sincronizados contra el conteo real de slots; suite completa verde + reporter de validacion PASS + smoke estricto verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md
@content/exercises/partitivos.json

<interfaces>
<!-- Los 3 lugares exactos a tocar. Verificado por grep en el codebase (2026-06-05). El smoke YA bifurca por shape (no requiere extension de logica, solo count). articoli ya esta en expected:34 — NO tocarlo. -->

1. tests/exercise-types.test.js:
   - linea 1274: `{ file: 'content/exercises/partitivos.json', expected: 44 }` en CATEGORIES_WITH_EXPLANATIONS <- cambiar 44 al n real de slots
   - linea 1273: `{ file: 'content/exercises/articoli.json', expected: 34 }` — YA sincronizado en 19-03, NO tocar
   - getExplanation = (ex) => Array.isArray(ex.variants) ? ex.explanation : ex.payload?.explanation — YA bifurcado por shape (heredado del piloto/Articoli, NO tocar)
   - getPrompts YA lee `ex.variants.map(v=>v.prompt)` vs `ex.payload?.prompt` — YA bifurcado (NO tocar)
   - bloque VAL_07_STRICT (lee `ex.validation?.status` top-level; hereda el `expected` del array compartido -> se sincroniza solo al arreglar el count arriba)

2. tests/fixtures/slot-variants-integration.test.js:175: `{ slug: 'partitivos', expected: 44 }` en REAL_CATEGORIES <- cambiar 44 al n real de slots (articoli en linea 174 ya en 34, NO tocar)

3. scripts/run-validation-271.mjs:
   - linea 91: `{ slug:'partitivos', file:'...', expected: 44 }` <- cambiar 44 al n real
   - linea 97: `const TOTAL_EXPECTED = 348;` <- recalcular = 348 - 44 + n_slots_final_partitivos
   - lineas 63-84: comentario explicativo del historial del total <- anadir la conversion de Partitivi a slots (precedente: el comentario narra cada cambio que movio el total; la conversion de Articoli en 19-03 esta documentada ahi como modelo a espejar)

El count `expected` de partitivos = n de SLOTS final (data.exercises.length tras 20-01+20-02) — sincronizar contra el JSON REAL, NO contra una estimacion. La fusion por forma REDUCE el count (44 -> ~18 tras reagrupar); los slots nuevos de huecos de del-formas de 20-02 lo SUBEN un poco. Net = leer del JSON.
Comando: `node -e "console.log(require('./content/exercises/partitivos.json').exercises.length)"` da el conteo real a usar en los 3 sitios.
Leccion 19-03: la estimacion fue ~25 y el real fue 34 — leer SIEMPRE del JSON, nunca estimar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sincronizar el count de Partitivi en exercise-types.test.js</name>
  <files>tests/exercise-types.test.js</files>
  <read_first>
    - tests/exercise-types.test.js (las lineas alrededor de 1265-1310 — CATEGORIES_WITH_EXPLANATIONS + getExplanation/getPrompts bifurcados + el assert de conteo; y el bloque VAL_07_STRICT)
    - content/exercises/partitivos.json (estado final tras 20-02 — para leer el conteo REAL de slots con `data.exercises.length`)
    - .planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md §tests/exercise-types.test.js (por que el count es el UNICO cambio: el test ya es shape-agnostic; los accessors getExplanation/getPrompts ya bifurcan)
    - .planning/phases/19-articoli-a-slots-contenido/19-03-PLAN.md (Task 1 analogo de Articoli — el mismo patron de sync de count contra el JSON real)
  </read_first>
  <action>
    En tests/exercise-types.test.js: leer el conteo REAL de slots de partitivos.json (`data.exercises.length` tras 20-02, via `node -e "console.log(require('./content/exercises/partitivos.json').exercises.length)"`) y actualizar SOLO el `expected` de la entry partitivos en CATEGORIES_WITH_EXPLANATIONS (linea 1274) a ese numero. NO tocar la entry de articoli (linea 1273, ya en 34). NO tocar la logica del loop ni los accessors getExplanation/getPrompts: ya bifurcan por `Array.isArray(ex.variants)` (heredado del piloto/Articoli) -> Partitivi slot pasa coverage/R1/R2/smart-quotes/markdown automaticamente. El bloque VAL_07_STRICT lee `ex.validation?.status` top-level -> no cambia salvo heredar el count sincronizado (toda variante/slot nuevo de 20-02 ya esta validated por el quorum con validation top-level D-19-09 -> la gate pasa). NO tocar los counts de las otras 8 categorias.
  </action>
  <verify>
    <automated>node --test tests/exercise-types.test.js && VAL_07_STRICT=1 node --test tests/exercise-types.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/exercise-types.test.js` exits 0
    - `VAL_07_STRICT=1 node --test tests/exercise-types.test.js` exits 0
    - El `expected` de partitivos en CATEGORIES_WITH_EXPLANATIONS == n REAL de slots: `node -e "const d=require('./content/exercises/partitivos.json'); console.log('real:', d.exercises.length)"` coincide con el valor escrito en linea 1274
    - Las otras 8 categorias siguen verdes con sus counts originales (articoli 34, preposiciones 49, genero-numero 40, avere 23, sustantivos-irregulares 31, verbos-movimiento 37, essere 39, profesiones 51) — no se tocaron
    - La logica del test (getExplanation/getPrompts/bifurcacion) NO se modifico: `grep -c "Array.isArray(ex.variants)" tests/exercise-types.test.js` >= 1 sin cambios
  </acceptance_criteria>
  <done>
    El count de Partitivi en CATEGORIES_WITH_EXPLANATIONS sincronizado al n real de slots; suite exercise-types + VAL_07_STRICT verdes; las 8 cats no-partitivos intactas (articoli sigue en 34); logica del smoke (ya shape-agnostic) sin tocar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar counts en slot-variants-integration.test.js + run-validation-271.mjs (TOTAL_EXPECTED)</name>
  <files>tests/fixtures/slot-variants-integration.test.js, scripts/run-validation-271.mjs</files>
  <read_first>
    - tests/fixtures/slot-variants-integration.test.js (las lineas alrededor de 166-202 — REAL_CATEGORIES + assert de conteo por slug)
    - scripts/run-validation-271.mjs (las lineas alrededor de 63-104 — comentario del historial del total + CATEGORIES array + TOTAL_EXPECTED + gates)
    - content/exercises/partitivos.json (conteo REAL final de slots)
    - .planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md §tests (GOTCHA: el test cuenta SLOTS, no variantes; fusion baja el count, huecos lo suben; los 3 hardcodes deben coincidir en el MISMO numero)
    - .planning/phases/19-articoli-a-slots-contenido/19-03-PLAN.md (Task 2 analogo de Articoli — mismo patron de 3 hardcodes + TOTAL_EXPECTED + comentario del historial)
  </read_first>
  <action>
    (1) En tests/fixtures/slot-variants-integration.test.js: cambiar `{ slug: 'partitivos', expected: 44 }` (linea 175) a `expected` = n REAL de slots. NO tocar la entry de articoli (linea 174, ya en 34). El resto del array y los asserts globales NO cambian (validateContent ya acepta variants[]). (2) En scripts/run-validation-271.mjs: cambiar `expected: 44` de la entry partitivos (linea 91) a n REAL de slots; recalcular `TOTAL_EXPECTED` (linea 97) = `348 - 44 + n_slots_final_partitivos`; actualizar el comentario explicativo del historial del total (lineas 63-84) registrando la conversion de Partitivi a slots (precedente: el comentario narra cada cambio historico que movio el total — el de Articoli de 19-03 esta ahi como modelo a espejar; anadir una linea analoga: "TOTAL = 348 - 44 + <count>"). Leer el conteo real del JSON, NO usar una estimacion. Los 3 hardcodes (este + el de Task 1) deben coincidir en el MISMO numero.
  </action>
  <verify>
    <automated>node --test tests/fixtures/slot-variants-integration.test.js && node scripts/run-validation-271.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/fixtures/slot-variants-integration.test.js` exits 0
    - `node scripts/run-validation-271.mjs` exits 0 (gate VAL-06 PASS: totalActual === TOTAL_EXPECTED)
    - El `expected` de partitivos en ambos archivos == n REAL de slots == el usado en Task 1 (los 3 hardcodes coinciden): verificable comparando contra `node -e "console.log(require('./content/exercises/partitivos.json').exercises.length)"`
    - `TOTAL_EXPECTED` == 348 - 44 + n_slots_final_partitivos (verificable aritmeticamente contra el conteo real; articoli sigue contribuyendo 34)
    - El reporter de run-validation-271 no emite warning `total != esperado` para partitivos
    - El comentario del historial del total en run-validation-271.mjs menciona la conversion de Partitivi a slots
  </acceptance_criteria>
  <done>
    Los 3 hardcodes de count de Partitivi (exercise-types de Task 1 + slot-variants-integration + run-validation-271 expected) coinciden en el n real de slots; TOTAL_EXPECTED recalculado (articoli=34 intacto); reporter PASS sin warnings de count; comentario del historial actualizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido -> smoke parametrico / reporter | El smoke y el reporter leen `data.exercises.length`; un count desincronizado debe fallar visible, no pasar silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | count desincronizado entre los 3 hardcodes -> gate falso-verde o falso-rojo | mitigate | Los 3 hardcodes + TOTAL_EXPECTED se sincronizan contra el MISMO conteo real leido del JSON (`data.exercises.length`), no estimaciones (leccion 19-03: estimo ~25, real 34). El acceptance criteria asserta que coinciden y que run-validation-271 PASS. |
| T-20-03 | Tampering | explanation/prompt de slot con HTML/markdown/smart-quotes pasa sin escanear | mitigate | El smoke YA bifurca por shape (heredado del piloto/Articoli): los escaneos smart-quotes/markdown/R1/R2 corren sobre `ex.explanation` + `ex.variants[].prompt` para slots — la cobertura de seguridad de contenido NO se pierde al convertir Partitivi. |
| T-20-08 | Tampering | nuevo slot/variante NO validated cuela en VAL_07_STRICT | mitigate | El gate VAL_07_STRICT lee `ex.validation?.status === 'validated'` top-level; toda superficie nueva de 20-02 paso el quorum (status validated) y todo slot nuevo lleva validation top-level (D-19-09) antes de integrarse -> la gate pasa solo si 20-02 cumplio. Si algo quedo disputed o sin validation top-level, la gate FALLA visible. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que auditar. |
</threat_model>

<verification>
- `node --test tests/*.test.js` exits 0 (suite COMPLETA verde — incluye storage/backup v8 de Phase 18, integration, exercise-types, articoli de Phase 19)
- `VAL_07_STRICT=1 node --test tests/*.test.js` exits 0
- `node scripts/run-validation-271.mjs` exits 0 (PASS)
- `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` exits 0
- Los 3 hardcodes + TOTAL_EXPECTED coinciden con `data.exercises.length` real de partitivos.json
</verification>

<success_criteria>
- Los 3 hardcodes de count de Partitivi + TOTAL_EXPECTED sincronizados contra el conteo REAL de slots
- Cobertura de explanations por slot preservada (todo slot con explanation no vacia — verificado por el smoke shape-agnostic)
- Suite completa + reporter + smoke estricto verdes
- Validator y loader NO tocados (ya aceptan variants[]); logica del smoke NO tocada (ya shape-agnostic); articoli=34 intacto
</success_criteria>

<output>
Create `.planning/phases/20-partitivi-a-slots-contenido/20-03-SUMMARY.md` when done
</output>
