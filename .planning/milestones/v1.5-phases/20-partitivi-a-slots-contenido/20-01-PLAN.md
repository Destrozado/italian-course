---
phase: 20-partitivi-a-slots-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/partitivos.json
  - .planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md
autonomous: false
requirements: [PART-01]
must_haves:
  truths:
    - "Los 44 ejercicios de Partitivi quedan reagrupados en slots por regla/forma: del-formas por forma (del/dello/della/dell'/dei/degli/delle) con un slot por sub-disparador donde la trampa difiere (dello z vs s-impura; degli s-impura vs vocal vs z, D-20-01) y delle invariable como un slot con variantes (D-17-01)"
    - "Las alternativas quedan en 3 slots: qualche, un po' di, y alcuni/alcune juntos como variantes de género en un solo slot (D-20-02)"
    - "La omisión en negativa queda en UN slot de contraste con caras afirmativa (034 del, 036 degli) y negativa '∅ sin partitivo' (035, 037) como variantes del mismo slot (D-20-03); la clasificación partitivo-vs-preposizione queda en UN slot con 040-044 como variantes, shape MC de 3 opciones (D-20-04)"
    - "Los pares contable/incontable (023/024 Ho comprato, 025/026 Ho preso) se absorben cada lado en su slot del-forma (D-20-05); los 2 match (038/039) quedan como slots-de-1 type:match (D-20-06)"
    - "Cada slot tiene una explanation top-level no vacia (merge elegir-la-mas-completa + injertar matices, D-17-05); ninguna variante lleva explanation propia; payload eliminado de todos los ejercicios; ids renumerados libremente (sin cruces, D-20-07)"
    - "Las superficies existentes movidas intactas a variants[] NO se re-validan; el JSON sigue pasando validateContent"
  artifacts:
    - path: "content/exercises/partitivos.json"
      provides: "Partitivi reagrupado a shape slot+variantes (payload eliminado, variants[]+explanation top-level por slot)"
      contains: "\"variants\""
    - path: ".planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md"
      provides: "Mapa id-fuente -> slot revisado por el autor (artefacto de auditoria); cubre los 44 ids 1:1"
  key_links:
    - from: "content/exercises/partitivos.json"
      to: "validateContent / normalizeExerciseToSlot"
      via: "shape slot+variantes (payload XOR variants[])"
      pattern: "\"variants\"\\s*:\\s*\\["
    - from: "el slot de omision en negativa (D-20-03)"
      to: "el valor de opcion '∅ / sin partitivo'"
      via: "U+2205 EMPTY SET preservado verbatim como option legitima"
      pattern: "∅"
---

<objective>
Reagrupar los 44 ejercicios reales de Partitivi en slots por regla/forma replicando el patron del plan 19-01 de Articoli, con las 4 reglas propias de Partitivi: del-formas por forma con split por sub-disparador donde la trampa difiere (D-20-01: dello z 005,006 vs s-impura 007; degli s-impura 017 vs vocal 018 vs z 019; delle invariable un slot con variantes D-17-01); alternativas en 3 slots, alcuni/alcune juntos por concordancia de genero (D-20-02); UN slot de contraste afirmativa/negativa con la respuesta "∅ sin partitivo" como skill propio (D-20-03); UN slot de clasificacion partitivo-vs-preposizione con las 5 frases 040-044 mezclando PARTITIVA y PREPOSITIVA, shape MC de 3 opciones (D-20-04). Los pares contable/incontable (023/024, 025/026) se absorben cada lado en su slot del-forma (D-20-05); los 2 match (038/039) quedan como slots-de-1 type:match (D-20-06). Producir la explanation de cada slot via elegir-la-mas-completa + injertar matices (D-17-05). Las superficies existentes se MUEVEN intactas a `variants[]` (cambio de contenedor -> NO re-validacion). Renumeracion de ids LIBRE — 0 cruces inter-categoria (D-20-07).

Este plan NO autora variantes nuevas (eso es 20-02) ni anade los slots de huecos de del-formas (dello+gn/ps/x, degli+gn/ps; eso es 20-02, D-19-06 arrastrado). NO toca los counts de tests/scripts (eso es 20-03, que sincroniza contra el conteo REAL final tras 20-02).

Purpose: es la columna vertebral de la fase — convierte el contenido real de Partitivi al modelo slot+variantes que el motor de Phase 16 ya consume, replicando exactamente el patron de Articoli (19-01) y el piloto (17-02). Es el segundo (y ultimo) caso del bloque Articulos de CONV-01. La diferencia clave vs Articoli: 0 cruces (ids libres, sin rango reservado) + 2 reglas estructurales propias (slot de contraste D-20-03 con la respuesta "∅", y slot de clasificacion D-20-04 con shape MC de 3 opciones).

Output: `partitivos.json` reescrito a slot+variantes (sin payload, con variants[]+explanation top-level por slot), validando verde; mapa de reagrupacion auditado por el autor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-partitivi-a-slots-contenido/20-CONTEXT.md
@.planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md
@content/exercises/partitivos.json
@content/exercises/articoli.json

<interfaces>
<!-- Shape de slot con variantes que enforza el validator. Verificado en schema-validator.js / content-loader.js (PATTERNS §Mechanical mapping). NO modificar loader ni validator: ya aceptan variants[] desde Phase 15. articoli.json (34 slots, todos validated) es la referencia AFTER exacta. -->
Shape autorado de un slot con variantes (verificado contra articoli-il-cons en 20-PATTERNS §AFTER):
- `id`: string id de slot (= exercise id). Esquema = Claude's Discretion (D-20-07): renumeracion LIBRE — Partitivi tiene 0 cruces, no hay rango reservado (D-19-04 NO aplica). Precedente fuerte: ids semanticos estilo Articoli (`partitivos-del-cons`, `partitivos-dello-z`, `partitivos-degli-vocal`, `partitivos-qualche`, `partitivos-negativa`, `partitivos-clasificacion`, `partitivos-match-incontable`). Unicidad obligatoria.
- `type`: "multiple-choice" o "match" a NIVEL de slot (42 superficies MC + 2 match)
- `categoryIds`: array no vacio a nivel de slot. SIEMPRE `["partitivos"]` (0 cruces -> ninguna entry de 2 ids, a diferencia de Articoli 300..305)
- `explanation`: string no vacio OBLIGATORIO a nivel de slot (D-15-02). Merge D-17-05: base = la explanation fuente mas completa/general del grupo + injertar los matices unicos de las descartadas (contrastes, pitfalls del hispanohablante: trampa de genero de `latte`, calco del plural espanol en `qualche` que es siempre singular, contraccion di+articolo) — NO reescritura total a regla generalizada. Explanations RULE-FIRST (lideran con la regla/disparador, no con un sustantivo concreto) por consistencia con el corpus tras D-19-07.
- `variants`: array NO vacio. Cada variante MC = objeto plano `{prompt (con ___), options (3-4 strings), correctIndex (en rango)}`. Cada variante match = `{prompt, pairs}` (array de 2-tuplas `[noun, forma]`, 3+ valores distintos en columna derecha R3, sin correctIndex). La variante NO lleva `explanation` ni `validation`.
- `validation`: top-level `{status, passes[]}` — se preserva del/los ejercicio(s) fuente.

Reglas hard del validator (verificadas, PATTERNS §GOTCHAS):
- `payload` XOR `variants[]` — nunca ambos, nunca ninguno. Al convertir se ELIMINA `payload`.
- `explanation` top-level string no vacio cuando hay `variants[]`.
- `variants[]` no vacio; superficie validada por el SURFACE_VALIDATOR del `type` (MC: prompt con `___`, 3-4 options, correctIndex en rango; match: pairs con 3+ valores distintos en derecha).
- Apostrofes ASCII U+0027 en `dell'` (el legacy ya los usa — preservar; el smoke FALLA con smart-quotes `'`).
- `∅` (U+2205 EMPTY SET) es un valor de opcion LEGITIMO en el slot de negativa (034-037) — NO es smart-quote y debe sobrevivir verbatim a la reescritura.

Mapa de reagrupacion PROPUESTO (verificado contra los `notes`/options de los 44 ids en 20-PATTERNS §Regrouping map; el planner lo deriva, el autor lo refina en el checkpoint):

DEL-FORMAS (D-20-01, 1 slot por forma + split por sub-disparador):
- `del` incontable masc + cons (sing): 001,002,003,004 + lado del de los pares D-01 (023, 025 si la respuesta es del — verificar correctIndex) -> 1 slot
- `dello` + z (sing): 005,006 -> 1 slot
- `dello` + s-impura (sing): 007 -> slot-de-1 (celda pobre -> candidata a variante en 20-02)
- `della` fem + cons (sing): 008,009,010 + lado della de pares D-01 si aplica -> 1 slot
- `dell'` + vocal elision (sing): 011,012,013 -> 1 slot
- `dei` contable masc pl + cons: 014,015,016 + lado dei de pares D-01 (024, 026 si aplica) -> 1 slot
- `degli` + s-impura (pl): 017 -> slot-de-1 (celda pobre)
- `degli` + vocal (pl): 018 -> slot-de-1 (celda pobre)
- `degli` + z (pl): 019 -> slot-de-1 (celda pobre)
- `delle` fem pl invariable (D-17-01 "el fem pl no cambia"): 020,021,022 -> 1 slot con variantes
- Pares contable/incontable D-01 (023,024 'Ho comprato'; 025,026 'Ho preso'): D-20-05 -> cada lado se absorbe en su slot del-forma segun su respuesta correcta (verificar correctIndex de cada uno: 023 del, 024 delle/dei segun option, 025 dello, 026 dei — comprobar en el JSON). NO crear slot dedicado.

ALTERNATIVAS (D-20-02, 3 slots):
- `qualche` (+siempre singular): 027,028,029 -> 1 slot
- `un po' di` (+solo incontable): 030,031 -> 1 slot
- `alcuni/alcune` (+solo plural contable, genero como variantes): 032,033 -> 1 slot (concordancia de genero = mismo slot, NO split D-20-01)

OMISION EN NEGATIVA (D-20-03, 1 slot de contraste):
- afirmativa (034 del, 036 degli -> USAR partitivo) + negativa (035, 037 -> "∅ / sin partitivo") como variantes del MISMO slot -> 1 slot

CLASIFICACION PARTITIVO-VS-PREPOSIZIONE (D-20-04, 1 slot de clasificacion):
- 040,041,042,043,044 como variantes mezclando PARTITIVA + PREPOSITIVA -> 1 slot. Shape MC de 3 opciones (partitivo / preposicion / articulo determinativo) — NO rellenar a 4.

MATCH (D-20-06, 2 slots-de-1 type:match):
- 038 (incontable sustantivo<->forma) -> slot-de-1 type:match
- 039 (contable plural sustantivo<->forma) -> slot-de-1 type:match

Este mapa propuesto da ~18 slots (sin contar variantes nuevas ni huecos de 20-02). El conteo final lo determina el autor en el checkpoint + 20-02. El conteo se REPORTA, NO se hardcodea en tests aqui (eso es 20-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Producir el mapa de reagrupacion auditable (id-fuente -> slot)</name>
  <files>.planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md</files>
  <read_first>
    - content/exercises/partitivos.json (integro — los 44 ejercicios con su campo `notes` que declara la regla/celda/disparador de cada uno, su `payload.explanation`, su `payload.correctIndex` (critico para los pares D-01 absorbidos D-20-05), y su `validation.passes[]`)
    - .planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md §content/exercises/partitivos.json (Before/After del shape legacy->slot, mechanical mapping table, Regrouping map, ID renumbering rules, GOTCHAS incluido el ∅ U+2205 y el shape MC de 3 opciones de clasificacion)
    - .planning/phases/20-partitivi-a-slots-contenido/20-CONTEXT.md §decisions (D-20-01..07, D-17-05 + D-19-07 arrastrados)
    - .planning/phases/19-articoli-a-slots-contenido/19-01-PLAN.md (el plan analogo de Articoli que esta fase replica) y .planning/phases/19-articoli-a-slots-contenido/19-REAGRUPACION-MAP.md (el artefacto analogo ya producido — formato de tabla a espejar)
  </read_first>
  <action>
    Crear `.planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md` como artefacto de auditoria: una tabla por cada slot resultante con columnas (slot-id propuesto, regla/forma/disparador, ids-fuente que entran como variantes, type [MC|match], categoryIds, explanation-base elegida + matices a injertar D-17-05, celda pobre candidata a variante nueva en 20-02?). Aplicar el mapa propuesto en <interfaces>: D-20-01 (del-formas por forma; split por sub-disparador en dello [z 005,006 / s-impura 007] y degli [s-impura 017 / vocal 018 / z 019]; delle invariable un slot con variantes D-17-01), D-20-02 (3 slots de alternativas con alcuni/alcune juntos por genero), D-20-03 (1 slot de contraste afirmativa/negativa con 034-037, la respuesta "∅" como skill propio), D-20-04 (1 slot de clasificacion con 040-044, shape MC de 3 opciones), D-20-05 (pares 023/024/025/026 absorbidos cada lado en su slot del-forma segun su correctIndex REAL leido del JSON), D-20-06 (038/039 slots-de-1 type:match). Para cada slot proponer un id semantico limpio (renumeracion LIBRE, 0 cruces, sin rango reservado — D-20-07). Para cada slot con varias variantes, comparar las explanations fuente y registrar cual es la base (la mas completa/general) y que matices unicos de las descartadas se injertan (incluir las pitfalls del hispanohablante: genero de `latte`, `qualche` siempre singular, contraccion di+articolo). Marcar explicitamente las celdas pobres (dello+s-impura 007, degli+s-impura 017, degli+vocal 018, degli+z 019) como candidatas a engordar en 20-02 (D-19-05) y anotar los huecos de del-formas a evaluar en 20-02 (dello+gn/ps/x, degli+gn/ps si existe sustantivo A1 natural, D-19-06). NO autorar variantes nuevas aqui. NO incluir los slots de huecos nuevos (van a 20-02). Reportar el conteo de slots resultante de SOLO esta reagrupacion (sin slots nuevos ni variantes nuevas).
  </action>
  <verify>
    <automated>test -f .planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md && grep -c "partitivos-" .planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - El archivo 20-REAGRUPACION-MAP.md existe con una tabla que cubre los 44 ids fuente (cada id 001-044 aparece exactamente una vez en alguna columna de ids-fuente)
    - Las del-formas estan por forma con split por sub-disparador donde difiere (D-20-01): dello+z (005,006) y dello+s-impura (007) son slots distintos; degli tiene un slot por sub-disparador (degli+s-impura 017, degli+vocal 018, degli+z 019); delle (020,021,022) es UN slot con variantes (D-17-01)
    - Las alternativas son 3 slots (D-20-02): qualche, un po' di, y alcuni/alcune juntos como variantes de genero en UN solo slot
    - La omision en negativa es UN slot de contraste (D-20-03) con 034,035,036,037 como variantes; la clasificacion es UN slot (D-20-04) con 040-044 como variantes, anotado como shape MC de 3 opciones
    - Los pares D-01 (023,024,025,026) estan absorbidos cada lado en su slot del-forma segun su correctIndex REAL del JSON (D-20-05), NO en un slot dedicado
    - 038 y 039 marcados como slots-de-1 type:match (D-20-06)
    - Todos los slots tienen categoryIds=["partitivos"] (0 cruces, ninguna entry de 2 ids); los ids propuestos son unicos y semanticos
    - Cada slot multi-variante registra explanation-base + matices a injertar (D-17-05, incluidas las pitfalls del hispanohablante); las celdas pobres estan marcadas como candidatas a 20-02 y los huecos de del-formas anotados
    - El conteo de slots de esta reagrupacion esta reportado (sin slots nuevos ni variantes nuevas)
  </acceptance_criteria>
  <done>
    20-REAGRUPACION-MAP.md cubre los 44 ids->slots con del-formas split por sub-disparador, delle invariable agrupada, 3 slots de alternativas (alcuni/e juntos), slot de contraste de negativa con "∅", slot de clasificacion de 3 opciones, pares D-01 absorbidos, 038/039 match slots-de-1, plan de explanation por slot multi-variante con pitfalls; ids semanticos libres (0 cruces); conteo de slots reportado.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Aprobar el mapa de reagrupacion de Partitivi (granularidad de slots, split por sub-disparador en dello/degli, slot de contraste de negativa, slot de clasificacion, merges de explanation) antes de reescribir el JSON</decision>
  <context>
    El mapa (20-REAGRUPACION-MAP.md) determina la estructura final de slots y por tanto el conteo que los tests de 20-03 sincronizan. Decisiones cargadas de esta fase: (1) el split por sub-disparador en dello/degli (D-20-01) — cada sub-sonido (z vs s-impura vs vocal) es una trampa distinta para el hispanohablante -> el motor re-verifica cada celda, a costa de mas slots (celdas pobres con 1 variante hasta engordarlas en 20-02); (2) la asimetria deliberada D-20-03 vs D-20-05 — la omision en negativa SI merece slot de contraste (la respuesta "∅ sin partitivo" es un skill propio que ninguna forma representa) pero el eje contable/incontable NO (ya esta representado por las formas distintas, sus pares se absorben); (3) el slot de clasificacion (D-20-04) mantiene las 5 frases mezcladas con shape MC de 3 opciones — separar por funcion romperia el ejercicio de clasificar. Ademas, las explanations mergeadas (D-17-05) requieren revision del autor (D-85) para no perder matices (pitfalls del hispanohablante: genero de `latte`, `qualche` siempre singular, contraccion di+articolo). Confirmar tambien que celdas pobres se engordaran y que huecos de del-formas se evaluaran en 20-02.
  </context>
  <options>
    <option id="split-por-subdisparador">
      <name>Split por sub-disparador en dello/degli + slot de contraste de negativa (D-20-01/D-20-03) — recomendado/locked</name>
      <pros>Coherente con D-20-01/D-20-03 (locked en CONTEXT): cada sub-sonido (z, s-impura, vocal) es una trampa distinta -> el motor re-verifica cada celda; la respuesta "∅" en negativa es un skill propio que merece su slot de contraste. Espeja la linea purista-por-forma del piloto/Articoli y maximiza el "no olvidar".</pros>
      <cons>Mas slots (celdas pobres dello+s-impura/degli+vocal/degli+z con 1 variante hasta engordarlas en 20-02).</cons>
    </option>
    <option id="del-formas-agrupadas">
      <name>Un slot unico por forma (dello sin split, degli sin split) y absorber la negativa en sus formas</name>
      <pros>Menos slots.</pros>
      <cons>Contradice D-20-01/D-20-03 (locked); mezcla trampas foneticas distintas en un slot y pierde el skill "∅" como contraste -> el motor no re-verifica cada celda. Descartado salvo override explicito del autor.</cons>
    </option>
  </options>
  <resume-signal>Confirma la granularidad (split por sub-disparador + slot de contraste de negativa recomendado), ajusta los slot-ids/merges de explanation y marca las celdas pobres a engordar + huecos de del-formas a evaluar en 20-02. Escribe "aprobado" cuando el mapa este listo para reescribir el JSON.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reescribir partitivos.json a slot+variantes segun el mapa aprobado</name>
  <files>content/exercises/partitivos.json</files>
  <read_first>
    - .planning/phases/20-partitivi-a-slots-contenido/20-REAGRUPACION-MAP.md (el mapa aprobado por el autor — fuente de verdad de la estructura)
    - content/exercises/partitivos.json (integro — superficies, explanations, correctIndex y validation.passes[] fuente a mover/mergear)
    - content/exercises/articoli.json (el output canonico de Articoli — shape multi-variante y slot-de-1 type:match reales, 34 slots todos validated top-level; la referencia AFTER exacta a espejar)
    - .planning/phases/20-partitivi-a-slots-contenido/20-PATTERNS.md §content/exercises/partitivos.json (Before/After MC + Before/After match 038 + el shape MC de 3 opciones de clasificacion 040 + el ∅ de negativa 035; mechanical mapping table; ID renumbering; GOTCHAS de apostrofes/∅/categoryIds/payload-XOR)
    - .planning/phases/19-articoli-a-slots-contenido/19-01-PLAN.md (Task 2 analogo)
  </read_first>
  <action>
    Reescribir `content/exercises/partitivos.json` segun el mapa aprobado. Por cada slot, emitir `{id, type, categoryIds, explanation, variants:[...], validation}` ELIMINANDO `payload` de cada ejercicio fuente y MOVIENDO su superficie intacta a `variants[]`: para MC `{prompt, options, correctIndex}`; para match (038/039) `{prompt, pairs}` (sin correctIndex). La `explanation` de cada slot multi-variante = la base elegida en el mapa + los matices injertados (D-17-05); para slots-de-1 la explanation existente sube a top-level tal cual; rule-first (D-19-07). Mover el texto intacto NO requiere re-validacion (cambio de contenedor); si al mergear/reformular cambia la SUPERFICIE de algun prompt/options existente, eso cuenta como variante nueva -> quorum en 20-02 (NO re-validar aqui; documentar en el mapa/SUMMARY cualquier superficie tocada). Todos los slots con categoryIds=["partitivos"] (0 cruces). Para el slot de negativa (D-20-03): preservar el valor de opcion `∅ / sin partitivo` (U+2205) verbatim en las variantes 035/037. Para el slot de clasificacion (D-20-04): conservar el shape MC de 3 opciones de 040-044 (NO rellenar a 4). Para los slots-de-1 match (038/039): conservar pairs con sus valores distintos en columna derecha (R3). Usar ids semanticos libres (renumeracion limpia, sin rango reservado — D-20-07). Preservar el campo `validation` top-level (de la fusion: combinar los passes[] o conservar el del ejercicio cuya superficie/explanation domina — registrar el criterio en el SUMMARY). Apostrofes ASCII U+0027 en dell' (preservar los del legacy). Aplicar un scan de acentos sobre las superficies/explanations MOVIDAS tambien (leccion Phase 19 WR-01/WR-02: el legacy puede arrastrar tildes ausentes `piu`->`piu`/`piu` o `e` sin acento que el quorum solo cubre para superficies nuevas) — corregir defectos legacy de acento encontrados (es correccion ortografica, no cambio de superficie). NO autorar variantes nuevas. NO anadir slots de huecos. NO tocar counts de tests/scripts.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` exits 0
    - Ningun ejercicio conserva payload: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); console.log('con payload:', d.exercises.filter(e=>e.payload).length)"` imprime `con payload: 0`
    - Todo slot con variants[] tiene explanation top-level no vacia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); console.log('slots sin explanation:', d.exercises.filter(e=>Array.isArray(e.variants)&&(!e.explanation||!e.explanation.trim())).length)"` imprime `0`
    - Ninguna variante lleva explanation propia: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); console.log('variantes con explanation:', d.exercises.flatMap(e=>e.variants||[]).filter(v=>v.explanation!==undefined).length)"` imprime `0`
    - Todos los slots tienen categoryIds=["partitivos"] (0 cruces): `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); console.log('cats no-partitivos:', d.exercises.filter(e=>!(e.categoryIds.length===1&&e.categoryIds[0]==='partitivos')).length)"` imprime `0`
    - Los 2 slots match existen: `node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); console.log('match slots:', d.exercises.filter(e=>e.type==='match').length)"` imprime `match slots: 2`
    - El slot de negativa conserva la opcion ∅: `grep -c "∅" content/exercises/partitivos.json` >= 2 (035 y 037 preservadas)
    - El slot de clasificacion tiene variantes de 3 opciones (no rellenadas a 4): verificable contando options de las variantes del slot de clasificacion en el JSON
    - Sin smart-quotes: `grep -nP '[\x{2018}\x{2019}\x{201C}\x{201D}]' content/exercises/partitivos.json` da 0 matches
  </acceptance_criteria>
  <done>
    partitivos.json reescrito a slot+variantes (sin payload, variants[]+explanation top-level por slot, variantes sin explanation propia; del-formas split por sub-disparador; delle invariable agrupada; 3 slots de alternativas con alcuni/e juntos; slot de contraste de negativa con ∅ preservado; slot de clasificacion de 3 opciones; pares D-01 absorbidos; 038/039 match slots-de-1; ids semanticos libres con categoryIds=["partitivos"]), validateContent verde, apostrofes ASCII + ∅ preservados, scan de acentos aplicado a superficies movidas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido editado a mano -> loader/validator al boot | El `partitivos.json` reescrito a mano cruza a validateContent y al render; explanation/prompt se renderizan como texto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering / data-integrity | reagrupacion que pierde/duplica una superficie fuente (especialmente un lado de un par D-01 absorbido, o una cara de la negativa) | mitigate | El mapa auditado (Task 1 + checkpoint) garantiza los 44 ids fuente mapeados 1:1; los pares D-01 se absorben segun su correctIndex REAL leido del JSON (no estimado); el acceptance criteria asserta con payload==0, slots match==2 y la opcion ∅ presente >=2. 0 cruces -> no hay riesgo de romper cobertura de otra categoria al renumerar. |
| T-20-02 | Tampering | reagrupacion pierde un matiz de explanation al mergear (D-17-05) o la opcion ∅ (U+2205) se corrompe | mitigate | Merge elegir-la-mas-completa + injertar matices (no reescritura), incluidas las pitfalls del hispanohablante; revision del autor en el checkpoint (D-85); acceptance asserta cobertura de explanation por slot (slots sin explanation == 0) y `grep -c "∅" >= 2`. El ∅ NO es smart-quote y debe sobrevivir. |
| T-20-03 | Tampering | explanation/prompt con HTML/markdown/smart-quotes o apostrofe no-ASCII; defecto de acento legacy arrastrado (Phase 19 WR-01/02) | mitigate | Acceptance asserta 0 smart-quotes; scan de acentos sobre superficies MOVIDAS tambien (no solo nuevas) para cazar tildes ausentes legacy; el smoke (20-03) re-asserta markdown/HTML/U+0027 sobre variants[].prompt + explanation. El legacy ya usa dell' U+0027 — preservar. |
| T-20-04 | Tampering | malformed JSON crashea el loader | mitigate | `validateContent` (existente, no modificado) rechaza con banner visible slot sin variantes / variante sin superficie valida / explanation ausente / clasificacion con options fuera de rango. El acceptance criteria corre el validador. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No aplica — zero-deps, sin packages que instalar/auditar (proyecto estatico offline). |
</threat_model>

<verification>
- `node scripts/validate-content-fixture.mjs partitivos content/exercises/partitivos.json` exits 0
- `node --test tests/*.test.js` — nota: el count de Partitivi (44 hardcoded en 3 sitios) FALLARA tras esta reescritura porque ahora hay menos slots; esto es ESPERADO y se arregla en 20-03. Documentar el rojo esperado en el SUMMARY; NO arreglar los counts aqui.
- El mapa 20-REAGRUPACION-MAP.md aprobado por el autor (checkpoint)
</verification>

<success_criteria>
- 44 ejercicios fuente reagrupados a slots por forma/sub-disparador; del-formas split por sub-disparador (D-20-01); delle invariable agrupada (D-17-01); 3 slots de alternativas con alcuni/e juntos (D-20-02); slot de contraste de negativa con ∅ (D-20-03); slot de clasificacion de 3 opciones (D-20-04); pares D-01 absorbidos (D-20-05); 038/039 match slots-de-1 (D-20-06); ids semanticos libres, categoryIds=["partitivos"] (D-20-07)
- Cada slot: explanation top-level no vacia (merge D-17-05, pitfalls injertadas), variantes sin explanation propia, payload eliminado
- validateContent verde; sin smart-quotes; ∅ y apostrofes ASCII preservados; scan de acentos aplicado a superficies movidas
- (Los counts de tests quedan rojos a proposito hasta 20-03)
</success_criteria>

<output>
Create `.planning/phases/20-partitivi-a-slots-contenido/20-01-SUMMARY.md` when done
</output>
