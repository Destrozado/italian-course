---
phase: 16-motor-de-examen-por-slots
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/screens/app.js
  - index.html
autonomous: false
requirements: [EXAM-01, EXAM-02, EXAM-03, EXAM-04, EXAM-05, EXAM-06]
must_haves:
  truths:
    - "Una sesión presenta como máximo 1 variante por slot (la fijada al construir); el render no re-sortea"
    - "Reanudar un Test completo muestra la MISMA variante que antes (variantIndices persistido en inFlightTest)"
    - "Un inFlightTest pre-existente sin variantIndices resuelve toda variante a índice 0 (back-compat, sin migración de schemaVersion)"
    - "Fallar la variante de un slot dispara la cascada D-54 reusando applyResultToSession; los call-sites de applyImmediateFailure siguen siendo exactamente 2"
    - "El recuento 'Ejercicios' del home y 'hecha'=N slots no cambian (slot id == exercise id); racha 21d + hecha→dominada intactas"
    - "El review de errores del summary muestra la variante exacta que se falló"
    - "La app arranca con las 9 categorías visibles como hoy, state v6 sin reset"
  artifacts:
    - path: "src/screens/app.js"
      provides: "sessionCurrentExercise slot-aware (synthetic payload) + variantIndices threaded en los 3 launch sites + advance + resume + inFlightTest + sessionResults"
      contains: "slotById"
    - path: "index.html"
      provides: "summary-errors resuelve la variante fallada via slotById + variantIndex"
      contains: "variantIndex"
  key_links:
    - from: "src/screens/app.js sessionCurrentExercise"
      to: "content.slotById[id].variants[variantIndex]"
      via: "getter slot-aware con synthetic .payload"
      pattern: "slotById\\?\\.\\[id\\]"
    - from: "src/screens/app.js applyResultToSession"
      to: "applyImmediateFailure (slotId)"
      via: "single call-site D-54 reusado"
      pattern: "applyImmediateFailure\\("
    - from: "src/screens/app.js persistInFlightTest"
      to: "state.inFlightTest.variantIndices"
      via: "array paralelo persistido"
      pattern: "variantIndices"
---

<objective>
Cablear la variante elegida por el sampler (Plan 01) a través de toda la pantalla: un getter `sessionCurrentExercise` slot-aware que resuelve `slotById[id].variants[variantIndex]` y lo re-envuelve en un `.payload` sintético (mismo truco que `songStart`/`songCurrentPhrase` de Phase 13), los 3 launch sites + advance + resume alimentando `slotById` y guardando `variantIndices`, la persistencia de la variante en `inFlightTest`, el registro de la variante en `sessionResults`, y la resolución de la variante fallada en el review de errores del summary. La cascada D-54 y la racha quedan intactas (operan por id de slot).

Purpose: EXAM-01 (1 variante por slot en sesión), EXAM-02 (recuento "Ejercicios"=slots sin cambio de copy), EXAM-03 (cascada D-54 por slotId reusando applyResultToSession, 2 call-sites), EXAM-04 (re-roll en resetSession toca variantes distintas), EXAM-05 (racha + hecha→dominada sobre "hecha" por slots), EXAM-06 (los 3 modos integran el muestreo por slot). Engine exercisable end-to-end con las 9 categorías legacy como slots de 1 variante.
Output: `src/screens/app.js` + `index.html` con el render slot-aware, el threading de variantIndices y la resolución de variante en el summary; checkpoint de booteo humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-motor-de-examen-por-slots/16-CONTEXT.md
@.planning/phases/16-motor-de-examen-por-slots/16-PATTERNS.md
@.planning/phases/15-modelo-de-datos-slot-variantes-schema-migraci-n/15-CONTEXT.md

<interfaces>
<!-- Return de buildSession/buildFullTest tras Plan 01 (16-01): -->
{ exerciseIds: string[], variantIndices: number[], actualSize: number }

<!-- slotById (content-loader.js:43-64): { id, type, categoryIds, explanation, variants:[ {flat surface} ] } -->
<!-- exerciseById (content-loader.js:149): ejercicios ORIGINALES con .payload — siguen existiendo intactos. -->

<!-- Getter analog a copiar — songCurrentPhrase (app.js:2253-2259) + synthetic payload de songStart (app.js:515-525):
     re-envuelve {id, type, categoryIds, payload:{...surface, explanation: slot.explanation}}
     para que TODOS los bindings .payload.* y initSubStateForExercise sobrevivan sin tocar. -->

<!-- Call-sites que leen result.exerciseIds + resuelven via exerciseById (a switchear a slotById/variant):
     startSession app.js:803-835 · _launchExamen app.js:417-455 · resetSession app.js:924-994
     sessionAdvance app.js:1522 · resumeInFlightTest app.js:1079,1106 -->
<!-- inFlightTest persist app.js:1051-1057 ; resume app.js:1090-1107 -->
<!-- sessionResults.push app.js:1459 ; applyResultToSession app.js:1457-1484 -->
<!-- applyImmediateFailure call-sites EXACTOS: app.js:1475 + app.js:1700 (match first-wrong) = 2. NO añadir un 3º. -->
<!-- summary-errors index.html:736-797 lee content.exerciseById[result.exerciseId].payload.* -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Getter sessionCurrentExercise slot-aware + variantIndices en los 3 launch sites + advance/resume</name>
  <files>src/screens/app.js</files>
  <read_first>
    - src/screens/app.js (sessionCurrentExercise :2230-2236; songCurrentPhrase analog :2253-2259; songStart synthetic payload :515-525; startSession :802-847; _launchExamen :409-460; resetSession :920-1006; sessionAdvance :1508-1531; resumeInFlightTest :1074-1110; initSubStateForExercise :1855-1888 que lee exercise.payload.*)
    - .planning/phases/16-motor-de-examen-por-slots/16-PATTERNS.md (§"variant resolution getter" líneas 120-183 — el blueprint exacto del getter; §"3 build/launch call-sites" líneas 187-208; §"Synthetic-payload re-wrap" líneas 269-271)
    - .planning/phases/16-motor-de-examen-por-slots/16-CONTEXT.md (D-16-08 fijar al construir + variantIndex; D-16-10 default 0; Claude's Discretion §"Resolución variante→superficie")
    - src/domain/session.js (estado tras Plan 01 — return con variantIndices)
    - src/data/content-loader.js (slotById :43-64,146-160 — shape de variante plana + explanation a nivel slot)
  </read_first>
  <behavior>
    - sessionCurrentExercise resuelve slotById[id].variants[variantIndex] y devuelve un objeto `{id, type, categoryIds, payload: {...surface, explanation: slot.explanation}}` — shape idéntico al legacy que initSubStateForExercise y los bindings .payload.* ya consumen.
    - Para una sesión legacy (slot de 1 variante) el getter devuelve exactamente la misma superficie que hoy (variantIndex 0).
    - Si no hay variantIndex para el cursor (resume legacy sin variantIndices, o defensivo) → variantIndex 0 (D-16-10).
    - Tras startSession/_launchExamen/resetSession, sessionCurrentExercise apunta a la variante fijada al construir; avanzar el cursor (sessionAdvance) no re-sortea la variante.
    - Re-hacer la sesión (resetSession) re-ejecuta buildSession/buildFullTest → puede fijar variantIndices distintos (EXAM-04).
  </behavior>
  <action>
    Introducir un campo de sesión `this.sessionVariantIndices = []` (array paralelo a `this.sessionExerciseIds`), inicializado donde se inicializan los demás sub-estados de sesión. Añadir un getter helper `sessionCurrentVariantIndex` que devuelve `this.sessionVariantIndices[this.sessionCursor] ?? 0` (D-16-10 default 0).

    Reescribir el getter `sessionCurrentExercise` (app.js:2230-2236) siguiendo el blueprint de PATTERNS.md líneas 165-181 y el analog `songCurrentPhrase`: resolver `const slot = this.content.slotById?.[id]`; `const vIdx = this.sessionCurrentVariantIndex`; `const surface = slot.variants?.[vIdx] ?? slot.variants?.[0] ?? {}`; devolver `{ id: slot.id, type: slot.type, categoryIds: slot.categoryIds, payload: { ...surface, explanation: slot.explanation } }`. Guards defensivos idénticos al getter actual (content null, cursor fuera de rango, id ausente, slot ausente → null). Este re-wrap hace que initSubStateForExercise y los bindings .payload.* sobrevivan sin tocar (NO editar initSubStateForExercise ni los renders de sesión).

    En los 3 launch sites — `startSession` (:803-835), `_launchExamen` (:417-455), `resetSession` (:924-994) — cambiar `Object.values(this.content.exerciseById)` por `Object.values(this.content.slotById)` al construir el pool, y tras recibir `result` guardar `this.sessionVariantIndices = result.variantIndices` junto a `this.sessionExerciseIds = result.exerciseIds`. Resolver el primer ejercicio a inicializar a través del getter (`this.initSubStateForExercise(this.sessionCurrentExercise)` con el cursor en 0) en lugar de `this.content.exerciseById[result.exerciseIds[0]]` — el getter ya re-envuelve la variante fijada. Mantener los 3 sitios en lockstep (idéntica secuencia).

    En `sessionAdvance` (:1522): reemplazar la resolución `const nextEx = this.content.exerciseById[...]` por `this.initSubStateForExercise(this.sessionCurrentExercise)` (el cursor ya está avanzado, el getter resuelve la variante fijada del nuevo slot). NO re-sortear la variante aquí.

    En `resumeInFlightTest` (:1106): inicializar el sub-estado vía el getter igual que arriba (cursor restaurado). La carga de `sessionVariantIndices` desde inFlightTest la hace Task 2; aquí basta que el getter caiga a 0 si el array está vacío (D-16-10). NO añadir un 3er call-site de applyImmediateFailure en ningún sitio.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*\*' src/screens/app.js | grep -v '^[[:space:]]*//' | grep -c "applyImmediateFailure("</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*\*' src/screens/app.js | grep -v '^[[:space:]]*//' | grep -c "applyImmediateFailure("` devuelve exactamente 2 (Pitfall #2 — los call-sites :1475 y :1700). NOTA: este grep excluye comentarios `//` Y líneas JSDoc `*`; el `grep -c` crudo devuelve 3 porque la línea :1645 es una mención en un bloque JSDoc, no un call-site. NO añadir un 3er call-site real.
    - `grep -c "Object.values(this.content.slotById)" src/screens/app.js` == 3 (los 3 launch sites alimentan slotById).
    - `grep -n "sessionVariantIndices" src/screens/app.js` aparece en: declaración del campo, los 3 launch sites, y el getter sessionCurrentVariantIndex.
    - `grep -n "slotById?.\[id\]" src/screens/app.js` confirma que sessionCurrentExercise resuelve contra slotById.
    - `node --test tests/` exits 0 (los screen tests no-DOM y domain tests no regresan).
  </acceptance_criteria>
  <done>
    sessionCurrentExercise resuelve la variante fijada y la re-envuelve en .payload sintético; los 3 launch sites + advance + resume alimentan slotById y guardan/leen variantIndices; cascada D-54 sigue con 2 call-sites; suite verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: variantIndices en inFlightTest (resume misma variante) + sessionResults + summary-errors</name>
  <files>src/screens/app.js, index.html</files>
  <read_first>
    - src/screens/app.js (persistInFlightTest :1039-1060; resumeInFlightTest :1074-1110 incl. stale-id check :1078-1079; sessionResults.push :1459; applyResultToSession :1457-1484)
    - index.html (summary-errors block :736-797 — lee content.exerciseById[result.exerciseId].payload.* por tipo; summarySessionResults filter :745; song summary analog :841-859 que resuelve contra songPhraseById)
    - .planning/phases/16-motor-de-examen-por-slots/16-PATTERNS.md (§"inFlightTest persistence" líneas 212-231 — array paralelo + default 0 sin bump; §"index.html render + summary-errors" líneas 234-244 — summary necesita la variante)
    - .planning/phases/16-motor-de-examen-por-slots/16-CONTEXT.md (D-16-09 persistir variante + review de errores la variante exacta; D-16-10 default 0 sin migración; Claude's Discretion §"¿Migración de inFlightTest?" — sin bump, default 0)
  </read_first>
  <behavior>
    - persistInFlightTest escribe `variantIndices: [...this.sessionVariantIndices]` paralelo a exerciseIds; reanudar un Test completo restaura la MISMA variante por slot.
    - Un inFlightTest pre-existente SIN variantIndices al reanudar → sessionVariantIndices se rellena con 0 para cada id restante (legacy = slot de 1, índice 0 correcto). Sin bump de schemaVersion (sigue v6).
    - sessionResults.push registra el variantIndex mostrado junto a {exerciseId, correct, userAnswer}.
    - El review de errores del summary muestra el prompt/respuesta/explicación de la VARIANTE concreta fallada (no la variante 0 por defecto cuando se falló otra).
    - El recuento "Ejercicios" del home y la lógica de "hecha" NO cambian (no se toca progress.js ni el copy del home).
  </behavior>
  <action>
    En `persistInFlightTest` (:1051-1057): añadir `variantIndices: [...this.sessionVariantIndices]` al objeto `inFlightTest`, paralelo a `exerciseIds`. En `resumeInFlightTest` (:1090-1107): restaurar `this.sessionVariantIndices = Array.isArray(ift.variantIndices) ? [...ift.variantIndices] : ift.exerciseIds.map(() => 0)` (D-16-10 back-compat blob legacy → todos 0). La validación stale-id (:1079) puede seguir contra `exerciseById` (ids idénticos) o cambiarse a `slotById` — mantener `exerciseById` para diff mínimo. NO bump de schemaVersion (D-16-09).

    En `applyResultToSession` (:1459): extender el push de `sessionResults` a `{ exerciseId: ex.id, correct, userAnswer, variantIndex: this.sessionCurrentVariantIndex }` para registrar qué variante se mostró (D-16-09). Verificar que `ex` que recibe applyResultToSession ya es el objeto re-envuelto del getter (lleva `.id`/`.categoryIds`) — la cascada D-54 sigue disparando por `slotId` sin cambio.

    En `index.html` summary-errors (:745-797): la resolución de la variante fallada debe usar `content.slotById[result.exerciseId].variants[result.variantIndex ?? 0]` en lugar de `content.exerciseById[result.exerciseId].payload.*`. Para minimizar el ripple del template, añadir en app.js un getter/helper `summaryVariantSurface(result)` que devuelva la superficie de la variante fallada re-envuelta como `{type, payload:{...surface, explanation}}` (mismo truco synthetic-payload), y que el `x-for result in summarySessionResults.filter(...)` consuma ese helper para prompt/options/correctIndex/answer/pairs/explanation por tipo. El filter de existencia (:745) debe seguir guardando contra slot ausente (`content.slotById[r.exerciseId]`). Mantener el copy y la estructura visual idénticos (EXAM-02 — sin cambio de UI). El bloque de song summary (:841-859) NO se toca (resuelve contra songPhraseById, fuera de scope).
  </action>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "variantIndices" src/screens/app.js` ≥ 2 (persistInFlightTest write + resumeInFlightTest restore).
    - `grep -n "variantIndex" src/screens/app.js` aparece en el push de sessionResults (:~1459).
    - `grep -n "ift.variantIndices" src/screens/app.js` confirma el fallback `.map(() => 0)` para blobs legacy.
    - `grep -n "slotById\[result.exerciseId\]\|summaryVariantSurface" index.html` confirma que el summary resuelve la variante (ya no `exerciseById[...].payload` para el path de slot).
    - `grep -c "CURRENT_SCHEMA_VERSION = 6\|schemaVersion.*6" src/data/storage.js` — el bump sigue en 6 (NO se incrementa; verificar que no aparece un `7`).
    - `node --test tests/` exits 0 (backup round-trip v6, data-storage, screen tests no regresan).
  </acceptance_criteria>
  <done>
    La variante fijada viaja en inFlightTest (resume = misma variante), se registra en sessionResults, y el review de errores muestra la variante exacta fallada; blobs legacy caen a índice 0 sin migración; schemaVersion sigue v6; suite verde.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint booteo — motor de slots end-to-end con las 9 categorías legacy</name>
  <what-built>
    El motor de examen ahora recorre slots: 1 variante elegida por slot al construir la sesión (Plan 01), resuelta vía getter slot-aware con .payload sintético, threaded por inFlightTest/sessionResults/summary (Plan 02). Las 9 categorías legacy funcionan como slots de 1 variante (back-compat). Cascada D-54 y racha intactas. schemaVersion sigue v6, sin reset de progreso.
  </what-built>
  <read_first>
    - .planning/phases/16-motor-de-examen-por-slots/16-CONTEXT.md (5 success criteria implícitos en EXAM-01..06)
    - .planning/ROADMAP.md (§Phase 16 — 5 success criteria)
    - CLAUDE.md (§Installation — `npx serve` / Live Server; localStorage NO en file://)
  </read_first>
  <how-to-verify>
    1. Arranca el server local (`npx serve` o VS Code Live Server) y abre http://localhost:PORT (NO file://, localStorage falla en file:// — ver CLAUDE.md).
    2. Home: confirma que las 9 categorías son visibles y la columna "Ejercicios" muestra el MISMO número que antes de esta fase (slots = ejercicios para legacy de 1 variante).
    3. Repaso 20 sobre 1-2 categorías: completa la sesión sin fallar → la categoría se marca "hecha"; el indicador "Ejercicio X / N" cuenta slots.
    4. Falla un ejercicio a propósito → confirma cascada D-54 inmediata (las categorías del slot se desmarcan al instante) y que la explicación pedagógica aparece igual que antes.
    5. Test completo de una categoría: avanza unos ejercicios, recarga la página (F5), pulsa "Reanudar" → confirma que reanuda EN EL MISMO PUNTO con el mismo ejercicio (misma variante).
    6. En la pantalla de resumen tras una sesión con fallos: confirma que "Errores cometidos" muestra el prompt/respuesta correcta/explicación del ejercicio exacto fallado.
    7. DevTools → Application → Local Storage: confirma `schemaVersion: 6` y que el progreso previo NO se reseteó.
  </how-to-verify>
  <acceptance_criteria>
    - Las 9 categorías cargan y son jugables; "Ejercicios" idéntico a pre-fase.
    - "hecha" se marca al pasar todos los slots; fallar desmarca (cascada D-54) al instante.
    - Reanudar un Test completo muestra el mismo ejercicio en el mismo punto.
    - El review de errores muestra el ejercicio exacto fallado.
    - localStorage schemaVersion 6, progreso preservado (sin reset).
    - Ningún banner de error de validación de contenido al arrancar.
  </acceptance_criteria>
  <resume-signal>Escribe "approved" o describe los problemas observados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor → JSON de contenido | El autor edita slots/variantes a mano; el schema-validator (Phase 15) ya valida la shape antes de llegar al motor. |
| state persistido (localStorage) → app | inFlightTest leído de localStorage puede ser un blob legacy (sin variantIndices) o manipulado por el autor en DevTools. |
| variante → DOM | La superficie de la variante (prompt/options/answer/explanation) se renderiza en la UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-XSS | Information disclosure / Injection | render de variante en sesión y summary | accept | Las superficies de variante se renderizan con `x-text` (textContent), NO `innerHTML` — igual que el render legacy de `.payload.*` (index.html:321,350,373,747). El re-wrap synthetic-payload no cambia el vector: sigue siendo `x-text`. App local single-user, contenido autorado por el propio usuario; sin entrada de terceros. |
| T-16-DATALOSS | Tampering | inFlightTest legacy sin variantIndices al reanudar | mitigate | Fallback `ift.exerciseIds.map(() => 0)` (D-16-10) — los slots legacy son de 1 variante, índice 0 correcto; jamás crash por índice ausente. Sin bump de schemaVersion (estado del autor preservado, D-16-09). |
| T-16-REGR | Tampering | cascada D-54 / "hecha" por slot | mitigate | Pitfall #2: applyImmediateFailure sigue con 2 call-sites (grep-verificado); progress.js no se reescribe (opera por id de slot == exercise id, D-16-07); racha/dominada intactas. |
| T-16-NULL | Denial of Service | getter resuelve slot/variante ausente | mitigate | Guards defensivos (content null, cursor fuera de rango, slot ausente → null; surface fallback `variants?.[0] ?? {}`) — espejo de songCurrentPhrase, evita TypeError en tick de unmount Alpine. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Cero instalaciones: stack estático, `node --test` nativo, sin paquetes nuevos. |
</threat_model>

<verification>
- `grep -v '^[[:space:]]*\*' src/screens/app.js | grep -v '^[[:space:]]*//' | grep -c "applyImmediateFailure("` == 2 (Pitfall #2 — excluye `//` y JSDoc `*`; el grep crudo da 3 por la mención JSDoc :1645).
- `grep -c "Object.values(this.content.slotById)" src/screens/app.js` == 3 (los 3 launch sites).
- `grep -n "variantIndices" src/screens/app.js` presente en persist + resume + los 3 launch sites.
- `node --test tests/` exits 0 (suite completa, ninguna regresión).
- storage.js sigue en `CURRENT_SCHEMA_VERSION = 6` (sin bump a 7).
- Checkpoint humano APPROVED: 9 categorías jugables, "Ejercicios" idéntico, cascada D-54 OK, resume misma variante, review de errores correcto, schemaVersion 6 sin reset, sin banner de validación.
</verification>

<success_criteria>
- EXAM-01: sesión presenta ≤1 variante por slot (fijada al construir, no re-sorteada en render).
- EXAM-02: "Ejercicios"=slots sin cambio de copy; "hecha"=N slots intacta.
- EXAM-03: fallar dispara cascada D-54 por slotId reusando applyResultToSession; 2 call-sites de applyImmediateFailure.
- EXAM-04: resetSession re-ejecuta el sampler → puede tocar variantes distintas.
- EXAM-05: racha 21d + hecha→dominada operan sobre "hecha" por slots sin cambio de mecánica.
- EXAM-06: Repaso 20 / Test completo / Modo Examen integran el muestreo por slot (slotById en los 3 launch sites).
</success_criteria>

<output>
Create `.planning/phases/16-motor-de-examen-por-slots/16-02-SUMMARY.md` when done
</output>
