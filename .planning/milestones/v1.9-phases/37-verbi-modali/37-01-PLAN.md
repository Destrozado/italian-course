---
phase: 37-verbi-modali
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/modali.json
  - content/categories.json
autonomous: false
requirements: [MODAL-01, MODAL-02]
must_haves:
  truths:
    - "El autor es examinado sobre el presente irregular de potere/volere/dovere en las 6 personas (posso/puoi/puo/possiamo/potete/possono y analogos) con distractoras plausibles de los 3 vectores D-37-03."
    - "El autor es examinado sobre la construccion modal + infinitivo (posso andare, voglio mangiare, devo studiare), con >=1 slot word-buttons donde el infinitivo invariable sigue al modal conjugado (SC#2 duro)."
    - "NINGUNA variante contiene passato prossimo modal (ho/sono/hai/sei + participio de modal); el scope-gate esta documentado OUT-OF-SCOPE en notes (SC#3, Pitfall 5)."
    - "El cruce modali-300 (categoryIds: modali+presente-regolare) existe como contenido, propaga la cascada D-54 y NO anade call-sites (siguen EXACTAMENTE 2)."
    - "modali es 0-match (D-04/D-37-02): person->form derivable por paradigma -> solo MC + word-buttons, sin slot match; el 0-match documentado en notes."
    - "La categoria modali queda registrada en categories.json con order 13 (append) sin romper el display del home."
  artifacts:
    - path: "content/exercises/modali.json"
      provides: "Categoria modali nacida en slot+variantes: presente irregular de los 3 modales (6 personas, 3 vectores de distractora), modal+infinitivo con >=1 word-buttons, cruce modali-300, todos validated; 0-match + scope-gate PP documentados en notes"
      contains: "\"id\": \"modali-"
    - path: "content/categories.json"
      provides: "Entrada modali order 13 (append al final del array, solo id/name/order, sin origen)"
      contains: "\"id\": \"modali\""
  key_links:
    - from: "content/exercises/modali.json modali-300"
      to: "cascada D-54 (applyResultToSession existente)"
      via: "categoryIds de 2 (modali+presente-regolare) -> el motor cascadea a ambas categorias sin call-site nuevo"
      pattern: "presente-regolare"
    - from: "content/exercises/modali.json"
      to: "content-loader.js loadContent()"
      via: "fetch generico por slug + validateContent (permisivo)"
      pattern: "modali"
    - from: "content/categories.json entry modali"
      to: "app.js categoriesForDisplay"
      via: "itera content.categories en orden de array -> fila nueva en el home sin codigo nuevo"
      pattern: "modali"
---

<objective>
Dar de alta la categoria `modali` (order 13) nacida DIRECTAMENTE en slot+variantes (clon del patron v1.7 `presente-regolare` + Phase 36), autorando por quorum cross-vendor R1-R7: el presente indicativo IRREGULAR de `potere/volere/dovere` en las 6 personas (con distractoras de los 3 vectores D-37-03), la construccion modal + infinitivo (`posso andare`, `voglio mangiare`, `devo studiare`) con >=1 slot word-buttons donde el infinitivo invariable sigue al modal conjugado, y el cruce multi-cat `modali-300` (`categoryIds: ["modali","presente-regolare"]`). Registrar la categoria en `categories.json` (order 13). Cubre MODAL-01 + MODAL-02.

Purpose: Es la 11a categoria de gramatica del proyecto. Sin estos slots el autor no re-verifica los modales; sin el word-buttons de posicion (SC#2) no se ejercita el patron estructural modal+infinitivo; sin el scope-gate documentado, el passato prossimo modal (pantano de doble-validez, PASSPROX-01) se colaria. La trampa real del hispanohablante es la regularizacion falsa (*poto/*volo/*dovo) y la contaminacion cross-verbo -> el core pedagogico son las distractoras.

Output: `content/exercises/modali.json` (slots del nucleo + word-buttons + cruce modali-300, todos `validation.status: validated`) y `content/categories.json` con la entrada order 13. El campo `origen`/PROV-01 y la sincronizacion de los 3 arrays de counts + TOTAL_EXPECTED son de Phase 39 (rojo esperado hasta entonces, patron v1.6/v1.7/Phase 36).

Este plan NO toca el motor v1.4 (D-54 = EXACTAMENTE 2 call-sites; sampler; slot-engine; schema-validator; migracion; backup). `git diff src/screens/app.js src/domain/progress.js` DEBE quedar vacio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-verbi-modali/37-CONTEXT.md
@.planning/phases/37-verbi-modali/37-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<slug_contract>
El slug es `modali` (contrato transversal INVIOLABLE de Phase 35, D-35-01/02): (a) `id` en categories.json, (b) fichero `content/exercises/modali.json`, (c) prefijo de TODOS los ids de ejercicio (`modali-*`) y del cruce (`modali-300`), (d) ya presente en `RESET_PREFIXES_V12` (storage.js:1168, verificado, sin colision `startsWith`). NO usar `verbi-modali` (eso es solo el nombre del directorio de la fase). TODOS los ids de slot DEBEN empezar por `modali-`.
</slug_contract>

<interfaces>
Shape de la categoria (analogo EXACTO `content/exercises/presente-regolare.json`, verificado):
- Top-level: `{ "notes": "...", "exercises": [ ...slots... ] }`. Sin categoryId top-level; pertenencia por-slot en `categoryIds:["modali"]`.
- Slot MC: `{ id, type:"multiple-choice", categoryIds:["modali"], explanation, variants:[{prompt,options,correctIndex}, ...>=2], validation }`.
- Slot word-buttons: `{ id, type:"word-buttons", categoryIds:["modali"], explanation, variants:[{prompt, answer:[tokens], distractors:[tokens]}, ...>=2], validation }`. Usa `answer:[tokens]`+`distractors:[tokens]` (NO options/correctIndex).
- Cruce: `{ id:"modali-300", type:"multiple-choice", categoryIds:["modali","presente-regolare"], explanation, variants:[...>=2], validation }`. La UNICA diferencia estructural vs slot normal: `categoryIds` con 2 slugs. El motor cascadea a ambas categorias automaticamente (D-54). CERO edits de motor. Colocar el cruce -300 al FINAL del array (convencion: cruces numerados -300+ van al tail).
- Bloque validation: `{ status:"validated", passes:[{by,date,verdict:"correcta",concerns:[]}, ... >=2 con >=2 by distintos] }`. `by` vistos en repo: `claude-opus-4-8`, `claude-sonnet-4-6`, `deepseek-chat`, `deepseek-reasoner`.
- modali es 0-MATCH (D-04/D-37-02): NO hay slot `type:"match"`. Person->form es derivable una vez conocido el paradigma irregular (mismo razonamiento que el 0-match de presente-regolare) -> solo MC + word-buttons.

Entrada categories.json (analogo linea 14 order 12): `{ "id":"modali", "name":"<display italiano>", "order":13 }` -- APPEND al final (despues de possessivi order 12). Solo id/name/order; NADA de origen (Phase 39). `order` es documental (content-loader NO re-ordena; categoriesForDisplay itera el array en orden de insercion).
</interfaces>

<canon_editorial>
LOCKED (aplica a CADA slot, prompt y note en modali.json):
- Explanations en espanol ACENTUADO RAE (a/e/i/o/u/n correctas). Un C4-accent flag del quorum = arreglar la tilde, NO override (Pitfall 10, memoria explanations_must_be_accented).
- Apostrofe ASCII U+0027, plain text (sin markdown), sin smart-quotes.
- Italianismos citados en ortografia italiana literal: `puo` LLEVA acento grave (`può`), `voglio`, `devo`, `possiamo`. Preservar el acento de `può` literalmente (es parte de la key Y un vector de distractora D-37-03).
- R1 NO-leak: el prompt = frase + hueco (+ gloss ES donde desambigua). NUNCA nombrar la forma objetivo, la persona, ni etiquetas como `(irregular)`/`(2a persona)`. La regla vive en `explanation` (mostrada tras fallo) + `notes` (autor-interno).
- Gloss `(en español: ...)` dentro del prompt donde una segunda lectura (persona/significado) es defendible: canon R7, NO es leak. Gemini/DeepSeek lo marcan C5 falso-positivo -> mantener (memoria gloss_es_desambiguacion_canon).
</canon_editorial>

<scope_gate>
HARD (SC#3, Pitfall 5): NINGUNA variante (prompt, options, answer, distractors) puede contener passato prossimo modal: ni `ho/sono/hai/sei/ha/abbiamo/avete/hanno/siamo/siete` + participio de modal (`dovuto`/`voluto`/`potuto`), ni el participio de modal aislado. Es scope creep + pantano de doble-validez, diferido a PASSPROX-01/MODAL-PP-01. Documentar OUT-OF-SCOPE en `notes` (mirror del 0-match de presente-regolare). Grep-verificable.
</scope_gate>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Aprobar el mapa de slots de modali antes de autorar</name>
  <read_first>
    - .planning/phases/37-verbi-modali/37-CONTEXT.md (D-37-02 guia+checkpoint; D-37-03 3 vectores; D-37-04 patron modal+infinitivo; scope-gate PP)
    - .planning/phases/37-verbi-modali/37-PATTERNS.md (ids semanticos propuestos: modali-potere/volere/dovere/infinito-wb; 0-match; word-buttons obligatorio SC#2)
    - .planning/research/FEATURES.md (lineas 31-32 presente irregular + modal+infinitivo; 107/118 match NOT justified -> MC + word-buttons; linea 80 hook modal->presente-regolare)
    - content/exercises/presente-regolare.json (molde: ids semanticos por regla, notes documentando 0-match, mezcla MC + -wb + cruces -300)
  </read_first>
  <decision>Aprobar el mapa de ids/tipo de modali antes de autorar (patron v1.6/v1.7/Phase 36 D-36-01/36-03). El nº final de slots, los ids exactos (prefijo `modali-`), y por-verbo-vs-consolidado son discrecion del autor dentro de la cobertura minima; D-37-02 fija 0-match (solo MC + word-buttons).</decision>
  <context>
    Cobertura MINIMA obligatoria (satisface MODAL-01/02 + SC#1/#2):
      1. Presente IRREGULAR de potere, volere, dovere en las 6 personas (posso/puoi/può/possiamo/potete/possono y los analogos voglio/vuoi/vuole/vogliamo/volete/vogliono, devo/devi/deve/dobbiamo/dovete/devono) con distractoras de los 3 vectores D-37-03: (a) regularizacion falsa (*poto/*volo/*dovo/*potiamo), (b) contaminacion cross-verbo (voglio/*volio, devo/*dovo, forma de un verbo aplicada a otro), (c) trampa del acento (può 3sg grave vs puoi 2sg). MC.
      2. Construccion modal + infinitivo (D-37-04, patron estructural NO 3 frases memorizadas): los 3 modales gobernando VARIOS infinitivos distintos (andare/mangiare/studiare/fare/leggere/...), con >=1 slot WORD-BUTTONS donde el infinitivo invariable sigue al modal conjugado (SC#2 duro).
    Mapa PROPUESTO (ids exactos = discrecion; el autor puede consolidar o partir):
      - `modali-potere` (MC) -> las 6 personas de potere como variantes contrastantes; distractoras 3 vectores incl. può/puoi.
      - `modali-volere` (MC) -> las 6 personas de volere; distractoras incl. voglio/*volio.
      - `modali-dovere` (MC) -> las 6 personas de dovere; distractoras incl. devo/*dovo.
      - `modali-infinito` (MC) -> modal conjugado + infinitivo (varios infinitivos), la forma modal como key.
      - `modali-infinito-wb` (word-buttons, OBLIGATORIO SC#2) -> el infinitivo invariable sigue al modal conjugado; banco con distractor de posicion/forma que fuerza el orden correcto.
      - `modali-300` (cruce, MC) -> modal conjugado + infinitivo de verbo REGULAR (enganche presente-regolare); al final del array.
    Alternativa (discrecion): consolidar los 3 verbos en 1-2 slots de conjugacion, o partir infinito/wb por modal. Debe cubrir siempre: los 3 modales x 6 personas, los 3 vectores de distractora, y >=1 word-buttons de posicion.
  </context>
  <options>
    <option id="approve-6slots">
      <name>Aprobar el mapa propuesto (3 slots de conjugacion por verbo + infinito MC + infinito-wb + cruce 300)</name>
      <pros>Un modal por slot (las 6 personas como variantes contrastantes) = maxima densidad de re-verificacion; espejo de como presente-regolare separa por regla (-are/-ere/-ire). word-buttons dedicado cumple SC#2. Cruce al tail.</pros>
      <cons>~6 slots; mas volumen de quorum. Si el autor prefiere menos, elegir consolidar.</cons>
    </option>
    <option id="approve-consolidated">
      <name>Consolidar conjugacion en 1-2 slots (los 3 modales mezclados por persona) + infinito MC + infinito-wb + cruce 300</name>
      <pros>Menos slots; la mezcla cross-verbo dentro del slot refuerza el vector (b) de contaminacion.</pros>
      <cons>Menos separacion pedagogica por verbo; la re-verificacion no aisla que modal se fallo.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - El autor ha aprobado un mapa que cubre: los 3 modales (potere/volere/dovere) en las 6 personas, los 3 vectores de distractora D-37-03, y >=1 slot word-buttons de posicion modal+infinitivo (SC#2).
    - El mapa incluye el cruce modali-300 (modali+presente-regolare) al final.
    - Todos los ids empiezan por `modali-`.
    - El mapa es 0-match (sin ningun `type:"match"`, D-04/D-37-02).
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" (opcion 6-slots), "consolidar" (opcion consolidada), o indica ajustes al mapa.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Autorar modali.json (nucleo + word-buttons + cruce modali-300, 1-por-1 por quorum)</name>
  <read_first>
    - content/exercises/presente-regolare.json (molde EXACTO: top-level notes+exercises; slot MC -are lineas 4-60; slot word-buttons presente-regolare-are-wb con answer[]+distractors[]; cruce presente-regolare-300 con categoryIds de 2 + convencion concerns[]; notes documentando el 0-match)
    - content/exercises/possessivi.json (twin fresco Phase 36: mismo canon/fechas; bloque validation simple y 3-by reforzado)
    - .planning/phases/37-verbi-modali/37-CONTEXT.md (D-37-01 cruce; D-37-03 3 vectores; D-37-04 patron; scope-gate PP HARD; canon editorial)
    - .planning/phases/37-verbi-modali/37-PATTERNS.md (shapes verbatim MC/word-buttons/cruce/validation; acento può; gloss R7)
    - .planning/research/PITFALLS.md (Pitfall 5 passato prossimo modal HARD scope-gate; Pitfall 8 R1 leak; Pitfall 9 gloss ES R7 falso-positivo mantener; Pitfall 10 acentos ES = bug real)
    - .planning/research/FEATURES.md (0-match confirmado; hook modal->presente-regolare para el cruce)
    - .claude/skills/gsd-validate-exercise (SKILL.md: quorum Opus+Sonnet, C1-C5, NUNCA batched, 1 subagent fresh por ejercicio)
    - scripts/validate-ai-pass.mjs (refuerzo cross-vendor Gemini/DeepSeek, --write, auto-fallback 429 -> deepseek-reasoner; claves en .env)
  </read_first>
  <action>
    Crear `content/exercises/modali.json` con top-level `{notes, exercises:[]}` copiando la estructura de presente-regolare.json. En `notes` (autor-interno, espanol acentuado, apostrofe ASCII, plain text) documentar DOS cosas, mirror de como presente-regolare documenta su 0-match: (1) modali es 0-match por D-04/D-37-02 (person->form derivable por paradigma irregular una vez conocido; un match io<->posso seria arrastrar sin pensar) -> solo MC + word-buttons; (2) el passato prossimo modal (ho dovuto / sono dovuto + participio, auxiliar prestado del infinitivo) queda HARD OUT-OF-SCOPE (pantano de doble-validez, diferido a PASSPROX-01/MODAL-PP-01) -> ninguna variante lo contiene.
    Autorar los slots del mapa aprobado (Task 1). Nucleo de conjugacion (MC, cada uno >=2 variantes): las 6 personas de potere/volere/dovere (posso/puoi/può/possiamo/potete/possono; voglio/vuoi/vuole/vogliamo/volete/vogliono; devo/devi/deve/dobbiamo/dovete/devono) con distractoras que ejercen los 3 vectores D-37-03 -> (a) regularizacion falsa (*poto/*volo/*dovo/*potiamo), (b) contaminacion cross-verbo (voglio/*volio, devo/*dovo, forma de un verbo en otro), (c) trampa del acento (`può` 3sg grave vs `puoi` 2sg). La explanation nombra la regla (irregular, raiz cambia). El gloss ES `(en español: ...)` desambigua la persona/verbo donde haga falta.
    Modal + infinitivo: >=1 slot MC (modal conjugado gobernando VARIOS infinitivos distintos, no solo andare/mangiare/studiare) + >=1 slot WORD-BUTTONS `modali-...-wb` (SC#2 DURO) donde el infinitivo INVARIABLE sigue al modal conjugado; el banco incluye un distractor de posicion o de forma (p.ej. una forma conjugada del verbo gobernado, o el orden invertido) que fuerza el orden correcto modal+infinitivo. Usar answer:[tokens]+distractors:[tokens].
    Cruce `modali-300` (MC, >=2 variantes, AL FINAL del array): `categoryIds: ["modali","presente-regolare"]` -- modal conjugado + infinitivo de un verbo REGULAR (enganche que FEATURES marca). Id estable con prefijo de la categoria primaria. CERO edits de motor: la cascada ya rutea por los 2 call-sites existentes para categoryIds de 2.
    SCOPE-GATE (verificar ANTES del quorum): grep mental por `dovuto`/`voluto`/`potuto` y por auxiliares + participio en TODO el JSON -> 0 ocurrencias. Preservar el acento `può` literalmente.
    Validar 1-por-1 via skill `gsd-validate-exercise` (base Opus+Sonnet, C1-C5, NUNCA batched, 1 subagent fresh por ejercicio). En el/los slot(s) con la trampa del acento (può/puoi) y en el cruce modali-300 anadir OBLIGATORIAMENTE una ronda EXTRA con `by` DeepSeek via validate-ai-pass.mjs (DeepSeek es estricto en acentos; caza può sin acento donde human-verify aprueba -- memoria cross-vendor). Resolver disputadas por REESCRITURA (ambas IAs correcta), nunca override-atajo; C4-accent en espanol = arreglar tilde; gloss R7 marcado C5 = falso-positivo, mantener. Bloque validation por variante/slot: >=2 passes / >=2 by distintos / verdict correcta; concerns[] con tags [D-37-01/03/04]/[R1-R7] y la verificacion del scope-gate.
    NO tocar el motor. NO anadir slot match (0-match). NO registrar en categories.json (Task 3). NO anadir origen ni sincronizar counts (Phase 39).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/exercises/modali.json'); const s=c.exercises; if(!s.every(e=>e.id.startsWith('modali-'))) throw new Error('id sin prefijo modali-'); if(s.some(e=>e.type==='match')) throw new Error('modali debe ser 0-match (D-04/D-37-02)'); if(!s.some(e=>e.type==='word-buttons')) throw new Error('falta slot word-buttons (SC#2)'); s.forEach(e=>{ if((e.variants||[]).length<2) throw new Error(e.id+' <2 variantes'); if(!e.validation||e.validation.status!=='validated') throw new Error(e.id+' no validated'); if(new Set(e.validation.passes.map(p=>p.by)).size<2) throw new Error(e.id+' <2 by'); if(!e.validation.passes.every(p=>p.verdict==='correcta')) throw new Error(e.id+' pass no correcta'); }); const x=s.find(e=>e.id==='modali-300'); if(!x) throw new Error('falta cruce modali-300'); if(JSON.stringify(x.categoryIds.slice().sort())!==JSON.stringify(['modali','presente-regolare'])) throw new Error('modali-300 categoryIds mal'); if(![...new Set(x.validation.passes.map(p=>p.by))].some(b=>/deepseek/i.test(b))) throw new Error('modali-300 sin pase DeepSeek'); console.log('OK modali:', s.length, 'slots')"</automated>
    <automated>node -e "const c=require('./content/exercises/modali.json'); const blob=JSON.stringify(c).toLowerCase(); ['dovuto','voluto','potuto'].forEach(pp=>{ if(blob.includes(pp)) throw new Error('SCOPE-GATE VIOLADO: participio de modal presente: '+pp); }); if(/\b(ho|hai|ha|abbiamo|avete|hanno|sono|sei|siamo|siete)\s+(dovuto|voluto|potuto)/.test(blob)) throw new Error('SCOPE-GATE VIOLADO: passato prossimo modal'); if(!c.notes||!/(passato prossimo|scope|out-of-scope|fuera de scope|passprox)/i.test(c.notes)) throw new Error('notes no documenta el scope-gate PP'); if(!/match/i.test(c.notes)) throw new Error('notes no documenta el 0-match'); console.log('OK scope-gate: 0 passato prossimo modal; notes documenta 0-match + scope-gate')"</automated>
    <automated>node -e "const c=require('./content/exercises/modali.json'); const blob=JSON.stringify(c); if(!blob.includes('può')) throw new Error('acento può ausente (D-37-03: parte de la key correcta)'); console.log('OK acento può preservado')"</automated>
    <automated>node scripts/validate-content-fixture.mjs modali content/exercises/modali.json</automated>
    <automated>grep -c "applyImmediateFailure(this.state" src/screens/app.js | grep -qx 2 && echo "D-54 OK (2 call-sites)"</automated>
    <automated>test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "engine diff vacio OK"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/modali.json` parsea como JSON; todos los ids empiezan por `modali-`.
    - NINGUN slot es `type:"match"` (0-match, D-04/D-37-02); existe >=1 slot `type:"word-buttons"` (SC#2).
    - Cada slot tiene `variants[].length >= 2`.
    - Los 3 modales (potere/volere/dovere) estan cubiertos en las 6 personas con distractoras de los 3 vectores D-37-03 (regularizacion falsa, contaminacion cross-verbo, trampa del acento può/puoi).
    - >=1 slot word-buttons ejerce la POSICION modal+infinitivo (infinitivo invariable tras modal conjugado) con distractor de posicion/forma en el banco.
    - `modali-300` existe con `categoryIds` == `["modali","presente-regolare"]`, al final del array, con >=2 variantes y pase DeepSeek.
    - SCOPE-GATE: NINGUNA variante contiene `dovuto`/`voluto`/`potuto` ni auxiliar + participio de modal; `notes` documenta el scope-gate PP OUT-OF-SCOPE.
    - `notes` documenta el 0-match (D-04/D-37-02).
    - El acento `può` esta preservado literalmente.
    - Cada slot: validation.status "validated", >=2 passes, >=2 by distintos, verdict correcta; pase DeepSeek en el slot del acento y en modali-300.
    - `node scripts/validate-content-fixture.mjs modali ...` sale 0.
    - `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2; `git diff src/screens/app.js src/domain/progress.js` vacio.
  </acceptance_criteria>
  <done>
    modali.json existe con el presente irregular de los 3 modales (6 personas, 3 vectores de distractora), el patron modal+infinitivo con >=1 word-buttons de posicion, y el cruce modali-300, todo autorado 1-por-1 por quorum y validated (pase DeepSeek en el acento y el cruce). Scope-gate PP verificado (0 ocurrencias) y documentado en notes junto al 0-match. Motor intacto. Listo para que Task 3 registre la categoria.
  </done>
</task>

<task type="auto">
  <name>Task 3: Registrar modali en categories.json (order 13)</name>
  <read_first>
    - content/categories.json (el archivo a MODIFICAR: 12 entradas, ultima possessivi order 12; APPEND modali order 13 al final)
    - .planning/phases/37-verbi-modali/37-PATTERNS.md (seccion categories.json lineas 130-149: append al final, order 13, solo id/name/order, NO origen; el order es documental)
    - content/exercises/possessivi.json (para el content-fixture validator de la nueva categoria)
    - .planning/research/PITFALLS.md (Pitfall 14 order duplicado; linea ~267 prefix-collision startsWith -- id `modali` ya en RESET_PREFIXES_V12 sin colision)
  </read_first>
  <action>
    Anadir al FINAL del array `categories` de `content/categories.json` la entrada `{ "id": "modali", "name": "Verbi modali (potere/volere/dovere)", "order": 13 }`, DESPUES de la entrada possessivi order 12. APPEND (Anti-Pattern 2: NO insertar a mitad; anadir coma a la linea de possessivi, luego insertar). Tres campos EXACTOS: `id` (= slug `modali`, matchea el nombre de fichero, el prefijo de ids de ejercicio, y RESET_PREFIXES_V12), `name` (display italiano; el estilo con parentesis-desambiguador espeja a dimostrativi/possessivi -- redaccion exacta a discrecion), `order` 13 (unico, siguiente libre tras 1-12). NO anadir `origen`/`source` (Phase 39, PROV-01).
    Los 3 arrays de counts hardcoded + TOTAL_EXPECTED + TOTAL_EXPECTED_BASELINE quedaran en rojo hasta Phase 39 (lockstep) -- rojo ESPERADO (patron v1.6/v1.7/Phase 36), NO es fallo de este plan. El planner opto por dejar el sync a Phase 39.
  </action>
  <verify>
    <automated>node -e "const c=require('./content/categories.json'); const cats=c.categories; const m=cats.find(x=>x.id==='modali'); const p=cats.find(x=>x.id==='possessivi'); if(!m) throw new Error('falta entrada modali'); if(!p) throw new Error('falta entrada possessivi (Phase 36)'); if(m.order!==13) throw new Error('modali order != 13'); if(p.order!==12) throw new Error('possessivi order != 12'); if(Object.keys(m).sort().join(',')!=='id,name,order') throw new Error('campos extra (origen?) o faltantes: '+Object.keys(m)); if(cats[cats.length-1].id!=='modali') throw new Error('modali no esta al final del array'); const orders=cats.map(x=>x.order); if(new Set(orders).size!==orders.length) throw new Error('order duplicado'); console.log('OK categories.json:', cats.length, 'entradas; possessivi 12, modali 13')"</automated>
    <automated>node scripts/validate-content-fixture.mjs modali content/exercises/modali.json && echo "modali valida contra categories.json"</automated>
  </verify>
  <acceptance_criteria>
    - `content/categories.json` parsea; contiene `{id:"modali", name, order:13}` al FINAL del array.
    - La entrada modali tiene EXACTAMENTE id/name/order (sin `origen`/`source`).
    - La entrada possessivi (order 12, Phase 36) sigue presente e intacta.
    - No hay `order` duplicado; las 13 entradas tienen orders unicos 1-13.
    - `node scripts/validate-content-fixture.mjs modali content/exercises/modali.json` sale 0.
  </acceptance_criteria>
  <done>
    modali registrada en categories.json (order 13, append). La categoria aparece en home/picker/Repaso/Examen genericamente (categoriesForDisplay itera el array). La sincronizacion de los 3 count arrays + TOTAL_EXPECTED + baseline queda para Phase 39 (rojo esperado).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Contenido JSON estatico + 1 entrada de indice; consumido por el content-loader validado existente. Sin codigo ejecutable nuevo, sin input de usuario, sin red, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | content/exercises/modali.json + content/categories.json (contenido nuevo) | accept | Sin superficie de ataque ejecutable: JSON estatico consumido por el content-loader ya validado; la unica preocupacion de integridad es JSON malformado, mitigada por el `validateContent` permisivo existente + el smoke `node --test tests/*.test.js` (readdir auto-cubre modali.json). Invariante anti-XSS `x-text`-only preservado. Cero codigo nuevo, cero input de usuario, cero red, cero auth. |
| T-37-SC | Tampering | scripts/validate-ai-pass.mjs (Gemini/DeepSeek, claves .env) | accept | Script existente ya en uso (v1.4-Phase 36); sin instalacion de paquetes nuevos (npm/pip/cargo); claves en .env fuera de git. Sin gate de legitimidad de paquetes: 0 installs en esta fase. |

No hay amenazas high/medium introducidas: content-only, motor intacto (D-54 = 2 call-sites), invariante anti-XSS `x-text` preservado. Threat register minimo por naturaleza (brownfield puro de contenido).
</threat_model>

<verification>
- MODAL-01 (presente irregular potere/volere/dovere, 6 personas, distractoras 3 vectores D-37-03) cubierto por los slots de conjugacion.
- MODAL-02 (modal + infinitivo) cubierto por el slot MC + el slot word-buttons de posicion (SC#2 duro).
- SC#3 scope-gate: 0 passato prossimo modal (grep dovuto/voluto/potuto + aux+PP == 0); documentado en notes.
- SC#4: modali carga en boot y aparece en home/picker/Repaso/Examen genericamente (registrada order 13).
- 0-match (D-04/D-37-02): sin slot match; documentado en notes.
- Cruce modali-300 (modali+presente-regolare) con categoryIds de 2, validated, pase DeepSeek.
- INT-04: variantes validadas 1-por-1 por quorum; ronda extra DeepSeek en el acento può/puoi y en el cruce.
- D-54: EXACTAMENTE 2 call-sites; git diff del motor vacio.
</verification>

<success_criteria>
- MODAL-01 + MODAL-02 ejercitables por los slots autorados de modali.
- Todas las variantes `validated` (>=2 by distintos), con pase DeepSeek en el magnet del acento y en el cruce.
- modali es 0-match (sin match) con >=1 word-buttons de posicion (SC#2).
- Scope-gate PP verificado (0 ocurrencias) y documentado en notes (SC#3).
- La categoria modali aparece en el home (registrada order 13).
- El cruce modali-300 propaga la cascada sin anadir call-sites (D-54 = 2).
- Motor v1.4 intacto (git diff vacio). Counts en rojo hasta Phase 39 = esperado, no es fallo.
</success_criteria>

<output>
Create `.planning/phases/37-verbi-modali/37-01-SUMMARY.md` when done
</output>
