---
phase: 35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/storage.js
  - src/data/backup.js
  - tests/data-storage.test.js
  - tests/backup.test.js
autonomous: true
requirements: [MIG-01, MIG-02]

must_haves:
  truths:
    - "Al arrancar sobre un state migrado a v12, las 4 categorías nuevas (dimostrativi/possessivi/modali/riflessivi) aparecen no-hecha con racha 0 (ninguna boota hecha/dominada por progreso espurio)"
    - "Las 10 categorías legacy + songProgress quedan byte-intactas tras migrate11to12 (fixture de no-regresión)"
    - "migrate11to12 y hydrateV12 son idempotentes y anti-prototype-pollution (deep-clone)"
    - "El backup hace round-trip en v12, migra un import v11→v12 con el reset aplicado, y rechaza wrappers > 12 con el mensaje existente"
    - "CURRENT_SCHEMA_VERSION === 12 espejado en storage.js Y backup.js"
    - "El motor de re-verificación NO se toca (0 cambios fuera de los 4 archivos declarados)"
  artifacts:
    - path: "src/data/storage.js"
      provides: "RESET_PREFIXES_V12, migrate11to12, hydrateV12, CURRENT_SCHEMA_VERSION=12, eslabón de cadena 11→12"
      contains: "RESET_PREFIXES_V12"
    - path: "src/data/backup.js"
      provides: "CURRENT_SCHEMA_VERSION=12 espejado + eslabón migrate11to12 + hydrateV12 terminal en parseBackupFile"
      contains: "migrate11to12"
    - path: "tests/data-storage.test.js"
      provides: "Bloque de tests v12 (reset selectivo, no-regresión legacy, idempotencia, anti-prototype-pollution, cadena)"
      contains: "migrate11to12"
    - path: "tests/backup.test.js"
      provides: "Bloque de tests round-trip v12 + import v11→v12 + reject > 12 (literal 13)"
      contains: "12"
  key_links:
    - from: "src/data/backup.js"
      to: "src/data/storage.js"
      via: "import { migrate11to12, hydrateV12 }"
      pattern: "migrate11to12.*hydrateV12"
    - from: "src/data/storage.js migrate() dispatcher"
      to: "hydrateV12"
      via: "cadena de migración terminal"
      pattern: "schemaVersion === 12.*hydrateV12"
    - from: "RESET_PREFIXES_V12"
      to: "categoryProgress / exerciseStats / inFlightTest"
      via: ".some(p => k.startsWith(p))"
      pattern: "RESET_PREFIXES_V12\\.some"
---

<objective>
Subir el `state` del proyecto a `schemaVersion 12` mediante un eslabón de migración `migrate11to12` + `hydrateV12` que resetea SELECTIVAMENTE por prefijo de id el progreso de las 4 categorías nuevas de v1.9 (`dimostrativi`, `possessivi`, `modali`, `riflessivi`), dejando el terreno limpio para que nazcan sin estado espurio en las Phases 36–38. Espejar el bump y la cadena en `backup.js`. Cubre MIG-01 + MIG-02.

Purpose: Va PRIMERA en el milestone (invariante v1.5/v1.6/v1.7 — la migración precede al contenido). Es un reset preventivo forward-compat (D-35-06): no-op sobre datos reales de hoy (los 4 slugs no existen aún), pero garantiza limpieza si un re-autorado futuro deja estado espurio.

Output: `storage.js` + `backup.js` con la cadena `11→12` y sus tests de migración/backup verdes.

Naturaleza: BROWNFIELD VERBATIM MIRROR del patrón `migrate10to11`/`hydrateV11`/`RESET_PREFIXES_V11` (v1.7, Phase 29). La mecánica está LOCKED por precedente (D-35-04/05) — NO se re-litiga; se copia la forma cambiando `10→11` a `11→12` y el array de prefijos a los 4 slugs. NO se toca el motor v1.4 (cascada D-54, sampler, slot-engine, schema-validator, screens).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-CONTEXT.md
@.planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-PATTERNS.md

<interfaces>
<!-- Contratos analog exactos (v1.7 Phase 29) que el executor DEBE espejar. Todos viven en los mismos 4 archivos, una versión abajo. Leer los analogs con read_first, NO explorar el codebase. -->

En src/data/storage.js:
- `const CURRENT_SCHEMA_VERSION = 11;` (línea 35) — fluye a blankState() (línea 70) automáticamente
- dispatcher `migrate()` cola (líneas 161-162): `if (s.schemaVersion === 10) s = migrate10to11(s);` seguido de `if (s.schemaVersion === 11) return hydrateV11(s);`
- `const RESET_PREFIXES_V11 = ['presente-regolare'];` (línea 1029) — con comentario de no-colisión startsWith (líneas 1018+)
- `export function migrate10to11(v10) { ... }` (líneas 1067-1106) — cuerpo de 3 pasos (deep-clone → delete/prune → invalidate) con la forma `.some(p => k.startsWith(p))` para pasos 2 y 3
- `export function hydrateV11(parsed) { ... }` (líneas 1131-1151) — usa el root guard `const p = (parsed && typeof parsed === 'object') ? parsed : {}`; NO repite poda

En src/data/backup.js:
- `import { ..., migrate10to11, hydrateV11 } from './storage.js';` (línea 26)
- `const CURRENT_SCHEMA_VERSION = 11;` (línea 52) — con doc comment arriba (líneas 28-51)
- guard de rechazo `if (state.schemaVersion > CURRENT_SCHEMA_VERSION)` con mensaje `/versión más nueva/` (líneas 122-127) — genérico, NO requiere cambio de código
- cadena en `parseBackupFile` cola (líneas 140-141): `if (migrated.schemaVersion === 10) migrated = migrate10to11(migrated);` seguido de `migrated = hydrateV11(migrated);` (hydrate UNCONDICIONAL, no return)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender la cadena de migración en storage.js (RESET_PREFIXES_V12, migrate11to12, hydrateV12, bump 11→12)</name>
  <files>src/data/storage.js</files>
  <read_first>
    - src/data/storage.js líneas 30-70 (CURRENT_SCHEMA_VERSION + blankState), 150-165 (dispatcher migrate), 1017-1106 (RESET_PREFIXES_V11 + migrate10to11), 1108-1151 (hydrateV11 + su comentario/root-guard)
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-PATTERNS.md (sección `src/data/storage.js` — Edits 1-5, con los excerpts y puntos de inserción exactos)
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-CONTEXT.md (D-35-01/03/04/05/06)
  </read_first>
  <behavior>
    - migrate11to12(state) sobre un state v11 con progreso en los 4 slugs nuevos + 10 legacy: borra categoryProgress de los 4 slugs, poda exerciseStats de claves que empiezan por los 4 prefijos, invalida inFlightTest si referencia un id de prefijo nuevo, y deja las 10 legacy + songProgress byte-idénticas; devuelve schemaVersion 12
    - migrate11to12 es idempotente: migrate11to12(migrate11to12(x)) deep-equal migrate11to12(x)
    - migrate11to12 es puro: no muta el input (los 4 slugs siguen presentes en el input tras la llamada)
    - anti-prototype-pollution: un fixture con clave `__proto__` en un sub-dict no contamina Object.prototype (({}).polluted === undefined)
    - hydrateV12(parsed) espeja hydrateV11: deep-clone, root-guard, NO repite poda; preserva un slug nuevo si viene presente (import directo v12)
    - blankState() devuelve schemaVersion 12
    - la cadena migrate() aplica 11→12 y termina en hydrateV12
  </behavior>
  <action>
    Cinco ediciones espejando el triplete `10→11` del MISMO archivo (mecánica LOCKED por D-35-04/05 — NO re-litigar):
    (1) Bump `CURRENT_SCHEMA_VERSION` de 11 a 12 (línea 35). blankState() NO necesita otro cambio (lee la constante).
    (2) Extender la cola del dispatcher `migrate()` (línea 162): cambiar la línea terminal `if (s.schemaVersion === 11) return hydrateV11(s);` por el eslabón `if (s.schemaVersion === 11) s = migrate11to12(s);` y añadir el nuevo terminal `if (s.schemaVersion === 12) return hydrateV12(s);`.
    (3) Añadir `const RESET_PREFIXES_V12 = ['dimostrativi', 'possessivi', 'modali', 'riflessivi'];` (mirror de RESET_PREFIXES_V11, línea 1029) con comentario espejo que explique la no-colisión `startsWith` (D-35-03): ningún slug existente empieza por un slug nuevo ni viceversa, y ningún slug nuevo es prefijo de otro.
    (4) Añadir `export function migrate11to12(v11)` como mirror verbatim de migrate10to11 (líneas 1067-1106): mismo cuerpo de 3 pasos con deep-clone `JSON.parse(JSON.stringify(...))` guardado por `typeof x === 'object' && x !== null`. Paso 1 = CUATRO deletes bracket-notation (`delete categoryProgress['dimostrativi']` etc., D-35-04). Pasos 2 (poda exerciseStats) y 3 (invalidar inFlightTest) copian la forma `.some(p => k.startsWith(p))` cambiando `RESET_PREFIXES_V11`→`RESET_PREFIXES_V12`. return con `schemaVersion: 12` y todos los `v10.`→`v11.`.
    (5) Añadir `export function hydrateV12(parsed)` como mirror verbatim de hydrateV11 (líneas 1131-1151): usar el root-guard `const p = (parsed && typeof parsed === 'object') ? parsed : {}`; NO repetir la poda (D-35-05); `schemaVersion: 12`.
    Usar la forma de array `.some(...)` (NO el hand-unrolled `delete a; delete b;` de migrate7to8), como hace migrate8to9/migrate10to11, porque el array tiene 4 elementos.
  </action>
  <verify>
    <automated>node --test tests/data-storage.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/storage.js` contiene `const RESET_PREFIXES_V12 = ['dimostrativi', 'possessivi', 'modali', 'riflessivi']`
    - `grep -c "CURRENT_SCHEMA_VERSION = 12" src/data/storage.js` === 1 (bump aplicado, no queda `= 11`)
    - `src/data/storage.js` exporta `migrate11to12` y `hydrateV12`
    - la cadena migrate() incluye `if (s.schemaVersion === 11) s = migrate11to12(s);` y `if (s.schemaVersion === 12) return hydrateV12(s);`
    - los 3 pasos de migrate11to12 usan `RESET_PREFIXES_V12.some(p => ...startsWith(p))` en la poda y la invalidación (no hardcode de un solo slug)
    - NOTA: los tests v12 de data-storage.test.js se escriben en Task 3 (mismo archivo distinto no aplica aquí); si `node --test tests/data-storage.test.js` falla porque los tests v11 aún asertan `=== 11`, es esperado hasta Task 3 — esta task se valida por las source-asserts anteriores + `node -e "import('./src/data/storage.js').then(m=>{const r=m.migrate11to12({schemaVersion:11,categoryProgress:{dimostrativi:{}, avere:{x:1}}}); if(r.schemaVersion!==12||r.categoryProgress.dimostrativi||!r.categoryProgress.avere) process.exit(1)})"` exit 0
  </acceptance_criteria>
  <done>storage.js sube a schemaVersion 12 con RESET_PREFIXES_V12 (4 slugs), migrate11to12 (reset selectivo idempotente anti-prototype-pollution) y hydrateV12 (mirror sin poda), eslabón de cadena aplicado.</done>
</task>

<task type="auto">
  <name>Task 2: Espejar el bump y la cadena en backup.js (CURRENT_SCHEMA_VERSION=12 + import + eslabón migrate11to12/hydrateV12)</name>
  <files>src/data/backup.js</files>
  <read_first>
    - src/data/backup.js líneas 26 (import), 28-63 (doc comment + CURRENT_SCHEMA_VERSION), 118-141 (guard de rechazo + cadena parseBackupFile)
    - src/data/storage.js líneas 161-162 (para confirmar los nombres exactos migrate11to12/hydrateV12 exportados en Task 1)
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-PATTERNS.md (sección `src/data/backup.js` — Edits 1-3)
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-CONTEXT.md (D-35-05/07)
  </read_first>
  <action>
    Tres ediciones espejando el bloque de versión del MISMO archivo (mantener LOCKSTEP con storage.js — nunca desincronizar):
    (1) Extender el import (línea 26): añadir `migrate11to12, hydrateV12` a la lista de nombres importados de `./storage.js` (dejar `hydrateV11` intacto aunque quede sin usar — inofensivo).
    (2) Bump `CURRENT_SCHEMA_VERSION` de 11 a 12 (línea 52). Extender el doc comment de arriba (líneas 28-51) con una frase de Phase 35 espejando la de Phase 29 (líneas 48-51): "11 a 12 — migrate11to12 resetea el progreso de las 4 categorías nuevas de v1.9".
    (3) Extender la cadena en `parseBackupFile` (líneas 140-141): añadir el eslabón `if (migrated.schemaVersion === 11) migrated = migrate11to12(migrated);` y cambiar el terminal `migrated = hydrateV11(migrated);` por `migrated = hydrateV12(migrated);`.
    El rechazo de `> 12` NO requiere cambio de código: el guard (líneas 122-127) es genérico contra CURRENT_SCHEMA_VERSION; el bump del paso 2 hace que rechace `> 12` automáticamente (solo cambia el literal del test, en Task 4).
  </action>
  <verify>
    <automated>node --test tests/backup.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CURRENT_SCHEMA_VERSION = 12" src/data/backup.js` === 1 (espejo de storage.js, sin `= 11` residual)
    - `src/data/backup.js` importa `migrate11to12` y `hydrateV12` de `./storage.js`
    - `parseBackupFile` contiene `if (migrated.schemaVersion === 11) migrated = migrate11to12(migrated);` y el terminal `migrated = hydrateV12(migrated);`
    - el guard de rechazo `> CURRENT_SCHEMA_VERSION` queda intacto (0 cambios de código en líneas 122-127)
    - NOTA: los tests v12 de backup.test.js se escriben en Task 4; validar por source-asserts + `node -e "import('./src/data/backup.js').then(m=>{const r=m.parseBackupFile(JSON.stringify({kind:'italian-course-backup',schemaVersion:13,state:{schemaVersion:13}})); if(r.ok!==false||!/versión más nueva/i.test(r.reason)) process.exit(1)})"` exit 0
  </acceptance_criteria>
  <done>backup.js espeja CURRENT_SCHEMA_VERSION=12, importa migrate11to12/hydrateV12, y la cadena de parseBackupFile aplica 11→12 terminando en hydrateV12; rechazo de > 12 automático por el guard genérico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bloque de tests v12 en data-storage.test.js (reset selectivo, no-regresión de las 10 legacy, idempotencia, anti-prototype-pollution, cadena) + bump asserts blankState 11→12</name>
  <files>tests/data-storage.test.js</files>
  <read_first>
    - tests/data-storage.test.js líneas 28 (import), 36-54 (bloque blankState v11), 1500-1783 (bloque `data/storage v11 — migrate10to11 …` completo: fixtures + los 15 tests)
    - src/data/storage.js líneas 1067-1151 (migrate11to12/hydrateV12 recién añadidos en Task 1) para confirmar la firma exacta
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-PATTERNS.md (sección `tests/data-storage.test.js` — la tabla de 15 tests a clonar + DIEZ_LEGACY/RESET_NEW)
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-CONTEXT.md (Claude's Discretion: checklist (a)-(d))
  </read_first>
  <behavior>
    - Clonar el bloque v11 (líneas 1500-1783) a un bloque v12 adaptando migrate10to11→migrate11to12, v10→v11, v11→v12, `presente-regolare`→los 4 slugs, NUEVE_REALES→DIEZ_LEGACY
    - DIEZ_LEGACY = ['avere','essere','verbos-movimiento','genero-numero','profesiones','sustantivos-irregulares','preposiciones','articoli','partitivos','presente-regolare']; RESET_NEW = ['dimostrativi','possessivi','modali','riflessivi']
    - Fixture v11WithNewFour(): los 4 slugs nuevos (a resetear) + las 10 legacy (a preservar) + songProgress
    - Cobertura (a): los 4 nuevos bootan no-hecha racha 0 tras reset por prefijo (borrado de categoryProgress + poda exerciseStats)
    - Cobertura (b) LOAD-BEARING: no-regresión — snapshot JSON de las 10 legacy + songProgress antes de migrar, deepEqual después (byte-idénticas); los 4 slugs ausentes de ambos dicts
    - Cobertura (c): idempotencia + pureza
    - Cobertura (d): anti-prototype-pollution (fixture `__proto__` con schemaVersion:11), hydrateV12 espejo de hydrateV11, cadena v10→v12 y end-to-end v8→v12
    - Bloque blankState: los 2 asserts `schemaVersion === 11` (líneas 39, 53) → 12
  </behavior>
  <action>
    Clonar el bloque `describe('data/storage v11 — migrate10to11 …')` (líneas 1500-1783) a un nuevo bloque v12/migrate11to12 aplicando los renames de la tabla de 35-PATTERNS.md. Adaptar cada uno de los 15 tests de esa tabla (borra categoryProgress deja legacy intactas · poda exerciseStats · invalida inFlightTest mixto · preserva inFlightTest solo-legacy · sin inFlightTest no crashea · idempotente · puro · anti-prototype-pollution · sub-dict corrupto→{} · hydrateV12 preserva+deep-clona · hydrateV12 normaliza sub-dicts ausentes · hydrateV12 anti-pollution · cadena v11→v12 · end-to-end v8→v12 vía `hydrateV12(migrate11to12(migrate10to11(migrate9to10(migrate8to9(v8)))))` · blankState()===12). El test de no-regresión (análogo línea 1730) es la cobertura (b) crítica: snapshot-before/deepEqual-after de las 10 legacy + songProgress byte-intact, y assert de los 4 slugs ausentes. Actualizar el bloque blankState v11 (líneas 36-54): 2 asserts `=== 11`→`12` + labels v11→v12. Añadir `migrate11to12, hydrateV12` al import (línea 28). Nombres de funciones de test / estructura de fixtures a discreción (Claude's Discretion) mientras cubran (a)-(d). Si algún assert `schemaVersion, 11` residual en el archivo (que aserte output migrado a CURRENT) rompe, buscar con grep y bumpearlo a 12 — NO tocar asserts que verifican versiones intermedias de la cadena.
  </action>
  <verify>
    <automated>node --test tests/data-storage.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/data-storage.test.js` exit 0 (0 fails nuevos)
    - `tests/data-storage.test.js` contiene un bloque describe con `migrate11to12` y las listas `DIEZ_LEGACY` (10 elementos incl. presente-regolare) + los 4 slugs nuevos
    - Existe un test de no-regresión que hace snapshot de las 10 legacy + songProgress y las deepEqual byte-idénticas tras migrate11to12, y asserta los 4 slugs nuevos ausentes de categoryProgress y exerciseStats
    - Existe test de idempotencia (migrate11to12∘migrate11to12), pureza (input no mutado), y anti-prototype-pollution (({}).polluted === undefined)
    - Existe test de cadena end-to-end v8→v12 que termina en schemaVersion 12
    - `grep -c "schemaVersion === 11" tests/data-storage.test.js` no incluye asserts de blankState (los 2 de blankState ahora === 12)
  </acceptance_criteria>
  <done>El bloque v12 de data-storage.test.js prueba (a) reset de los 4 nuevos, (b) las 10 legacy + songProgress byte-intactas, (c) idempotencia/pureza, (d) anti-prototype-pollution + cadena; blankState aserta 12; suite verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Bloque de tests v12 en backup.test.js (round-trip v12 + import v11→v12 + reject > 12) + bump asserts schemaVersion 11→12</name>
  <files>tests/backup.test.js</files>
  <read_first>
    - tests/backup.test.js líneas 85-95 (blankState v11 assert), 535-626 (bloque `data/backup v11 — round-trip + import v10→v11 …`), 668-679 (test reject-future)
    - src/data/backup.js líneas 140-141 (cadena parseBackupFile recién extendida en Task 2)
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-PATTERNS.md (sección `tests/backup.test.js` — la tabla de tests + la lista de líneas `r.state.schemaVersion, 11` a bumpear)
    - .planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-CONTEXT.md (D-35-07)
  </read_first>
  <behavior>
    - Clonar el bloque v11 (líneas 535-626) a un bloque v12 adaptando migrate10to11→migrate11to12, v10→v11, v11→v12, `presente-regolare`→un slug representativo (`dimostrativi`), y cada `r.state.schemaVersion, 11`→12
    - Fixture stateV12() en schemaVersion:12 con progreso en categorías legacy (incl. presente-regolare como preservada)
    - Round-trip v12: export→import sin "versión más nueva"; wrapper.schemaVersion 12; r.state.schemaVersion === 12; progreso legacy preservado (deepEqual)
    - Import cross-version v11→v12: stateV11In en schemaVersion:11 con un slug nuevo (dimostrativi) + una legacy → tras import, dimostrativi reseteada, legacy byte-intacta, r.state.schemaVersion === 12
    - Reject-future: wrapper schemaVersion 13 (state.schemaVersion 13) → r.ok false, r.reason match /versión más nueva/i
    - Todos los asserts de output-migrado-a-CURRENT (`r.state.schemaVersion, 11` / `s.schemaVersion, 11` / blankState) → 12
  </behavior>
  <action>
    Clonar el bloque `describe('data/backup v11 — round-trip + import v10→v11 …')` (líneas 535-626) a un bloque v12/import v11→v12 aplicando los renames de la tabla de 35-PATTERNS.md (3 tests: round-trip v12 sin "versión más nueva" · round-trip preserva progreso legacy · import v11→v12 con el slug nuevo reseteado y legacy byte-intacta, r.state.schemaVersion===12). Actualizar el test reject-future (líneas 668-679): cambiar el label a `> 12` y bumpear los DOS literales `schemaVersion: 12`→`13` (guard code sin cambios). Bumpear a 12 TODOS los asserts que verifican el output migrado a CURRENT: `r.state.schemaVersion, 11` en los bloques v7/v8/v9/v10 (líneas ~121, 155, 175, 198, 246, 294, 345, 386, 446, 488, 527 según PATTERNS) + el `blankState() … v11` (líneas 91-93, `s.schemaVersion, 11`→12). Buscar con grep `schemaVersion, 11` y `schemaVersion: 11` antes de finalizar; NO tocar los que representan la versión de ENTRADA de un fixture intermedio (solo los que asertan el output landeado en CURRENT). Estructura de fixtures/nombres a discreción.
  </action>
  <verify>
    <automated>node --test tests/backup.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/backup.test.js` exit 0 (0 fails nuevos)
    - Existe un bloque describe con round-trip v12 (`stateV12()` schemaVersion 12) que asserta `r.state.schemaVersion === 12` y que el import no reporta "versión más nueva"
    - Existe un test de import cross-version que parte de un wrapper schemaVersion:11 con un slug nuevo (dimostrativi) → tras parseBackupFile, el slug nuevo está reseteado, una categoría legacy queda byte-intacta, y r.state.schemaVersion === 12
    - El test reject-future usa `schemaVersion: 13` en wrapper y state, y asserta r.ok===false + r.reason /versión más nueva/i
    - `grep -c "schemaVersion, 11" tests/backup.test.js` no incluye asserts de output-migrado-a-CURRENT (todos bumpeados a 12)
  </acceptance_criteria>
  <done>El bloque v12 de backup.test.js prueba round-trip v12, import v11→v12 con reset del slug nuevo + legacy byte-intacta, y rechazo de wrapper 13; todos los asserts de output-CURRENT a 12; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → storage.js `migrate()` | State persistido (potencialmente manipulable vía DevTools) cruza a la cadena de migración/hidratación al boot |
| Archivo de backup JSON importado → backup.js `parseBackupFile` | JSON no confiable (elegido por el usuario, potencialmente editado/corrupto) cruza a la cadena de migración cross-version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | migrate11to12 / hydrateV12 (deep-clone de sub-dicts de state) | mitigate | Deep-clone defensivo `JSON.parse(JSON.stringify(x))` guardado por `typeof x === 'object' && x !== null` en cada sub-dict; objeto raíz siempre literal fresco `{ schemaVersion: 12, … }` (nunca asigna `__proto__` como prototipo). Bloquea prototype-pollution vía claves `__proto__`/`constructor` en el JSON de state o backup. Mirror de T-21-01/T-29-01. Test dedicado assert `({}).polluted === undefined` (Task 3). Severidad alta → BLOCK si el test de anti-pollution falla. |
| T-35-02 | Tampering / Elevation | parseBackupFile guard de versión (backup.js:122-127) | mitigate | Rechazar wrappers con `schemaVersion > CURRENT_SCHEMA_VERSION` (=12) con mensaje "versión más nueva" — no se hidrata una forma futura desconocida cuyo shape podría no cumplir invariantes. Guard genérico intacto; bump de la constante lo activa para `> 12`. Test dedicado con wrapper 13 (Task 4). |
| T-35-03 | Tampering | migrate11to12 (mutación del input) | mitigate | Todas las operaciones (delete/prune/invalidate) actúan sobre el CLONE, nunca sobre el input → garantiza pureza + idempotencia; un input compartido/reutilizado no se corrompe. Test de pureza + idempotencia (Task 3). |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No hay instalación de paquetes en esta fase (0 nuevas dependencias; el proyecto es vanilla + browser-native ES modules; tests con `node --test` nativo). N/A — no aplica el gate de legitimidad de paquetes. |

Nota: no se esperan mitigaciones nuevas más allá de espejar el patrón ya endurecido (D-35-04/05). La superficie es estrecha y self-contained; el motor v1.4 no se toca.
</threat_model>

<verification>
Suite completa (comando del proyecto — el path desnudo falla en Node 22.20, usar el glob):

```
node --test tests/*.test.js
```

Debe quedar verde salvo el fail preexistente AJENO conocido (`genero-numero` 13-vs-12, D-35-08 — fuera de scope, NO tocar). Sin fails NUEVOS introducidos por la migración.

Verificaciones de integridad del motor (brownfield — NO tocar):
```
git diff --stat src/screens/app.js src/domain/ src/data/schema*.js 2>/dev/null
grep -rn "applyImmediateFailure" src/ | grep -v "function applyImmediateFailure" | wc -l   # debe seguir en 2 call-sites
```
El `git diff` de motor/screens/schema debe estar vacío; solo cambian los 4 archivos declarados.
</verification>

<success_criteria>
1. `CURRENT_SCHEMA_VERSION === 12` en `storage.js` Y `backup.js` (espejado, sin `= 11` residual).
2. `RESET_PREFIXES_V12 = ['dimostrativi', 'possessivi', 'modali', 'riflessivi']` con comentario de no-colisión startsWith (D-35-03).
3. `migrate11to12` + `hydrateV12` presentes, exportados, idempotentes y anti-prototype-pollution; eslabón de cadena aplicado en el dispatcher de storage.js y en parseBackupFile de backup.js.
4. Fixture con progreso falso en los 4 slugs nuevos migra a categoryProgress SIN esas claves; las 10 legacy + songProgress byte-idénticas (no-regresión).
5. Backup: round-trip v12 OK, import v11→v12 aplica el reset, wrapper `schemaVersion: 13` rechazado con /versión más nueva/i.
6. `node --test tests/*.test.js` verde (solo el fail preexistente ajeno genero-numero); motor v1.4 NO tocado (git diff de screens/domain/schema vacío).
</success_criteria>

<output>
Create `.planning/phases/35-migraci-n-11-12-reset-selectivo-preventivo-de-las-4-categor-/35-01-SUMMARY.md` when done
</output>
