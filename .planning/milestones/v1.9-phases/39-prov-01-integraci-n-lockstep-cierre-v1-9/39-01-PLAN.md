---
phase: 39-prov-01-integraci-n-lockstep-cierre-v1-9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/schema-validator.js
  - content/categories.json
  - tests/schema-validator-origen.test.js
autonomous: true
requirements: [PROV-01, PROV-02, INT-01]
must_haves:
  truths:
    - "El schema-validator acepta una categoría con origen:\"ia-quorum\" y una con origen:\"apuntes-profesora\""
    - "El schema-validator acepta una categoría SIN campo origen (retrocompatible, absence = accepted)"
    - "El schema-validator RECHAZA una categoría con origen:\"typo\" (enum inválido, mensaje en español)"
    - "Exactamente las 4 cats nuevas (dimostrativi/possessivi/modali/riflessivi) tienen origen:\"ia-quorum\""
    - "Las 10 cats legacy quedan sin clave origen (ausente, no null)"
    - "content/categories.json parsea y valida con el schema extendido; orders 11-14 únicos"
  artifacts:
    - path: "src/data/schema-validator.js"
      provides: "Validación enum opcional de origen a nivel categoría"
      contains: "VALID_ORIGEN"
    - path: "content/categories.json"
      provides: "4 cats nuevas estampadas con origen"
      contains: "\"origen\": \"ia-quorum\""
    - path: "tests/schema-validator-origen.test.js"
      provides: "Test unitario del enum origen (acepta enum + ausencia; rechaza typo)"
  key_links:
    - from: "src/data/schema-validator.js"
      to: "content/categories.json"
      via: "for (const cat of categories) origen enum check"
      pattern: "VALID_ORIGEN"
---

<objective>
Añadir el campo OPCIONAL `origen` (enum `ia-quorum` | `apuntes-profesora`) al schema-validator a nivel categoría (PROV-01), estampar las 4 categorías nuevas con `origen: "ia-quorum"` dejando las 10 legacy AUSENTES (PROV-02), y verificar que las 4 entradas order 11-14 ya existen sin romper el display del home (INT-01).

Purpose: Cierra la marca de procedencia del milestone v1.9 con honestidad de provenance (legacy mixto = ausente) y refuerza el schema para cazar typos del enum.
Output: schema-validator.js con el enum opcional; categories.json estampado; un test unitario dedicado del enum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-RESEARCH.md

<interfaces>
<!-- Patrón canon a clonar (schema-validator.js:617-623 — validateValidationShape enum idiom): -->
<!-- optional field, absence returns early, enum via .includes(), mensaje en español via push(file, id, reason) -->

Category loop actual (src/data/schema-validator.js:78-90):
```js
const knownCategoryIds = new Set();
for (const cat of categories) {
  if (typeof cat?.id !== 'string' || !ID_SLUG_RE.test(cat.id)) { push(...); continue; }
  if (knownCategoryIds.has(cat.id)) { push('categories.json', cat.id, `id de categoría duplicado: "${cat.id}"`); }
  if (typeof cat.name !== 'string' || !cat.name.trim()) { push('categories.json', cat.id, `falta campo "name"...`); }
  knownCategoryIds.add(cat.id);
}
```

Enum idiom a clonar (src/data/schema-validator.js:621-623):
```js
const VALID_STATUS = ['pending', 'validated', 'disputed'];
if (!VALID_STATUS.includes(v.status)) { push(file, ex.id, `"validation.status" inválido: ${JSON.stringify(v.status)} (esperado: pending|validated|disputed)`); }
```

categories.json actual (14 cats; las 4 nuevas order 11-14 SIN origen todavía):
```
{ "id": "dimostrativi", ..., "order": 11 },
{ "id": "possessivi",   ..., "order": 12 },
{ "id": "modali",       ..., "order": 13 },
{ "id": "riflessivi",   ..., "order": 14 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir enum opcional origen al schema-validator + test unitario</name>
  <files>src/data/schema-validator.js, tests/schema-validator-origen.test.js</files>
  <read_first>
    - src/data/schema-validator.js (category loop lines 78-90 = punto de inserción; validateValidationShape lines 608-654 = patrón enum a clonar, específicamente 617-623)
    - .planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-RESEARCH.md (§"PROV-01 insertion pattern" lines 147-166; §"Don't Hand-Roll" line 214)
  </read_first>
  <behavior>
    - Categoría con origen:"ia-quorum" → validateContent ok:true (aceptada)
    - Categoría con origen:"apuntes-profesora" → ok:true (aceptada)
    - Categoría SIN clave origen → ok:true (retrocompatible, absence = accepted; es el caso de las 10 legacy)
    - Categoría con origen:"ia-quorumm" (typo) → ok:false, error mencionando "origen" y los valores esperados
    - Categoría con origen:"apuntes" (typo) → ok:false
  </behavior>
  <action>
    En src/data/schema-validator.js, dentro del loop `for (const cat of categories)` (~línea 78-90), DESPUÉS del check de `name` y ANTES de `knownCategoryIds.add(cat.id)`, añade un check de enum opcional per PROV-01 (RESEARCH §"PROV-01 insertion pattern"): declara `const VALID_ORIGEN = ['ia-quorum', 'apuntes-profesora'];` (dentro del loop o justo antes del loop como constante de módulo, tu discreción), y `if (cat.origen !== undefined) { if (!VALID_ORIGEN.includes(cat.origen)) { push('categories.json', cat.id, `"origen" inválido: ${JSON.stringify(cat.origen)} (esperado: ia-quorum|apuntes-profesora)`); } }`. Clon exacto del idiom de validateValidationShape (schema-validator.js:621-623): mensaje en español (FOUND-04), enum vía `.includes()`, `absence = accepted` por construcción (el `!== undefined` guard). NO toques ningún otro check del loop. NO renombres el campo a `source` (ARCHITECTURE.md dice source pero está SUPERSEDED por REQUIREMENTS/ROADMAP → usa origen, RESEARCH §Anti-Patterns line 204).

    Crea tests/schema-validator-origen.test.js importando `validateContent` de src/data/schema-validator.js (mira cómo lo importan tests/fixtures/slot-variants.test.js o tests/domain.test.js). Un `describe('schema-validator — origen enum (PROV-01)')` con tests que cubran los 5 casos del bloque <behavior>: construye objetos `{ categories: [{ id, name, origen }], exercisesByFile: {} }` mínimos y asserta `result.ok`. Usa `node:test` (`import { test, describe } from 'node:test'; import assert from 'node:assert'`).
  </action>
  <verify>
    <automated>node --test tests/schema-validator-origen.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VALID_ORIGEN" src/data/schema-validator.js` >= 1
    - `grep -c "source" src/data/schema-validator.js` no aumenta por este cambio (no se introduce el nombre source para procedencia)
    - `node --test tests/schema-validator-origen.test.js` exits 0
    - El test contiene un caso de ausencia (sin origen → ok:true) y un caso de typo (origen inválido → ok:false)
    - El mensaje de error del enum está en español y menciona ia-quorum|apuntes-profesora
  </acceptance_criteria>
  <done>El schema-validator valida el enum origen opcional (acepta enum válido + ausencia, rechaza typo con mensaje español); test unitario verde.</done>
</task>

<task type="auto">
  <name>Task 2: Estampar origen en las 4 cats nuevas + verificar INT-01</name>
  <files>content/categories.json</files>
  <read_first>
    - content/categories.json (14 cats; las 4 nuevas order 11-14 sin origen — líneas 13-16)
    - .planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-RESEARCH.md (§"PROV-02 stamp pattern" lines 168-178; §"Pitfall 5" line 258 — legacy NO se etiqueta)
    - .planning/REQUIREMENTS.md (PROV-02 line 47; line 88 decisión legacy-ausente)
  </read_first>
  <action>
    En content/categories.json añade `"origen": "ia-quorum"` a EXACTAMENTE las 4 entradas nuevas: dimostrativi (order 11), possessivi (order 12), modali (order 13), riflessivi (order 14) — RESEARCH §PROV-02 stamp pattern (líneas 173-176). Deja las 10 legacy (orders 1-10, líneas 3-12) EXACTAMENTE como están, SIN clave origen (procedencia mixta → ausente es la elección honesta, REQUIREMENTS line 88 / Pitfall 5). No añadas origen:null ni "" a ninguna — ausente, no null. INT-01 es verify-only: confirma que las 4 entradas ya están (append, orders 11-14 únicos) y que el JSON sigue parseando.
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')).categories; const nuevas=c.filter(x=>['dimostrativi','possessivi','modali','riflessivi'].includes(x.id)); const legacy=c.filter(x=>!['dimostrativi','possessivi','modali','riflessivi'].includes(x.id)); if(nuevas.length!==4||!nuevas.every(x=>x.origen==='ia-quorum'))process.exit(1); if(legacy.length!==10||legacy.some(x=>'origen' in x))process.exit(1); const orders=c.map(x=>x.order); if(new Set(orders).size!==14)process.exit(1); console.log('OK 4 estampadas, 10 legacy ausentes, 14 orders únicos');"</automated>
  </verify>
  <acceptance_criteria>
    - content/categories.json parsea sin error
    - Exactamente las 4 cats dimostrativi/possessivi/modali/riflessivi tienen origen:"ia-quorum"
    - Las otras 10 cats NO tienen la clave origen (`'origen' in cat` === false para las 10)
    - Los 14 orders (1-14) son únicos
    - `grep -c "\"origen\": \"ia-quorum\"" content/categories.json` == 4
    - Ninguna cat tiene origen:null ni origen:""
  </acceptance_criteria>
  <done>Las 4 cats nuevas estampadas con origen:"ia-quorum"; las 10 legacy quedan ausentes; INT-01 confirmado (order 11-14 únicos, JSON válido).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (ninguna nueva) | App estática single-user offline; sin red, sin auth, sin inputs de usuario nuevos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | schema-validator origen enum | accept | El enum solo hace el schema MÁS estricto (whitelist de 2 valores); no añade superficie. Contenido hand-authored por el autor. |

No hay dependencias nuevas (zero-deps), no hay instalaciones de package-manager → sin T-39-SC.
</threat_model>

<verification>
- `node --test tests/schema-validator-origen.test.js` exits 0
- content/categories.json: 4 estampadas + 10 legacy ausentes (script node del verify de Task 2 exits 0)
- Sin nuevos packages (zero-deps intacto): `git diff --name-only` no toca package.json/lock
</verification>

<success_criteria>
- PROV-01: schema-validator acepta origen opcional (enum + ausencia), rechaza typo — cubre SC-1
- PROV-02: 4 cats con ia-quorum, 10 legacy ausentes — cubre SC-2
- INT-01: 4 entradas order 11-14 únicas, JSON válido, no rompe display — cubre SC-2
</success_criteria>

<output>
Create `.planning/phases/39-prov-01-integraci-n-lockstep-cierre-v1-9/39-01-SUMMARY.md` when done
</output>
