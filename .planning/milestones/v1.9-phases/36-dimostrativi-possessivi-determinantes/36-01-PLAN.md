---
phase: 36-dimostrativi-possessivi-determinantes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/exercises/dimostrativi.json
autonomous: false
requirements: [DEMOS-01, DEMOS-02, DEMOS-03, DEMOS-04]
must_haves:
  truths:
    - "El autor es examinado sobre la concordancia de questo/questa/questi/queste (vicino) en genero y numero."
    - "El autor es examinado sobre la elision quest' ante vocal (quest'anno, quest'amica)."
    - "El autor es examinado sobre las formas tipo-articulo de quello (quel/quello/quell'/quei/quegli/quelle) segun el disparador fonetico, con un slot match sustantivo->forma analogo articoli-049."
    - "El autor es examinado sobre el colapso ES 3-vias->IT 2-vias (este/ese/aquel -> questo/quello) con anclas de distancia qui/la que fuerzan una sola lectura (sin doble-validez)."
    - "codesto queda documentado OUT-OF-SCOPE en notes y ausente de keys/distractoras."
  artifacts:
    - path: "content/exercises/dimostrativi.json"
      provides: "Categoria dimostrativi nacida en slot+variantes: slots core questo/quest'/quello MC/quello match/colapso ES, cada uno validated"
      contains: "\"id\": \"dimostrativi-"
  key_links:
    - from: "content/exercises/dimostrativi.json"
      to: "content-loader.js loadContent()"
      via: "fetch generico por slug + validateContent + normalizeExerciseToSlot"
      pattern: "dimostrativi"
    - from: "dimostrativi-match-quello variants[].pairs"
      to: "articoli-049 phonetic-trigger match template"
      via: "noun->quello-form pairing (no derivable por raiz, D-04)"
      pattern: "\"pairs\""
---

<objective>
Dar de alta la categoria `dimostrativi` (order 11) nacida DIRECTAMENTE en slot+variantes (clon exacto del patron v1.7 `presente-regolare`), autorando su NUCLEO por quorum cross-vendor R1-R7: concordancia de `questo`, elision `quest'`, formas tipo-articulo de `quello` (1 slot MC de variantes contrastantes + 1 slot `match` sustantivo->forma), y el colapso de calco ES 3-vias->IT 2-vias con anclas de distancia. Cubre DEMOS-01, DEMOS-02, DEMOS-03, DEMOS-04.

Purpose: Es la mitad "dimostrativi" del enganche con el articulo (`quello` espeja lo/gli). Sin estos slots el autor no puede re-verificar el determinante demostrativo, el trap #1 del hispanohablante (colapso 3->2 vias) queda sin ejercitar, y el magnet `quei/quegli` (INT-04) no se cubre.

Output: `content/exercises/dimostrativi.json` con los slots del nucleo autorados y `validation.status: validated`. El registro en `categories.json` y los diferenciadores A2 (`cio`, formas pronominales) + el cruce `dimostrativi-300` los hace el plan 36-02 (Wave 2).

Este plan NO toca el motor v1.4 (cascada D-54 = EXACTAMENTE 2 call-sites en `src/screens/app.js:1642` y `:1969`). `git diff src/screens/app.js src/domain/progress.js` DEBE quedar vacio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-CONTEXT.md
@.planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md

<slug_contract>
El slug es `dimostrativi` (contrato transversal de Phase 35: id en categories.json + nombre de fichero `content/exercises/dimostrativi.json` + prefijo de TODOS los ids de ejercicio + prefijo en `RESET_PREFIXES_V12` de storage.js:1168). TODOS los ids de slot DEBEN empezar por `dimostrativi-` (el reset de migracion matchea por `startsWith`).
</slug_contract>

<interfaces>
Shape de la categoria (analogo EXACTO `content/exercises/presente-regolare.json`, verificado 12 slots):
- Top-level: `{ "notes": "...", "exercises": [ ...slots... ] }`. NO hay `categoryId` top-level; la pertenencia es por-slot en `categoryIds[]`.
- Slot MC: `{ id, type:"multiple-choice", categoryIds:["dimostrativi"], explanation, variants:[{prompt,options,correctIndex}, ...>=2], validation }`.
- Slot match (analogo `articoli-049`): `{ id, type:"match", categoryIds:["dimostrativi"], explanation, variants:[{prompt:"Empareja...", pairs:[[noun,form],...] }], validation }`. Sin options/correctIndex.
- Slot word-buttons (si aporta): `variants:[{prompt, answer:[tokens ordenados], distractors:[...] }]`.
- Bloque validation: `{ status:"validated", passes:[{by,date,verdict:"correcta",concerns:[]}, ... >=2 con >=2 by distintos] }`.
</interfaces>

<canon_editorial>
LOCKED por memoria del proyecto (aplica a CADA slot):
- explanations en espanol ACENTUADO (RAE): a/e/i/o/u con tilde, n con virgulilla. Un flag C4-accent del quorum sobre espanol sin tildes es un bug REAL -> arreglar la tilde, NO override (Pitfall 10 / memoria explanations_must_be_accented).
- Apostrofe ASCII U+0027 unicamente; sin smart-quotes; plain text (sin markdown).
- Italianismos citados en ortografia italiana (`quegli`, `quell'albero`, `citta`).
- R1 no-leak: el prompt es la frase + el hueco. NADA de `(s impura -> quello)`, `(cerca)`, ni la forma-objetivo nombrada. La regla vive en `explanation` (post-fallo) y `notes` (autor-interno). Tags `(masc)`/`(fem)` solo si estructuralmente necesarios para desambiguar.
- El gloss `(en espanol: ...)` es CANON R7 (desambigua doble-validez), NO es leak. Gemini/DeepSeek pueden marcarlo como C5 falso-positivo -> mantener el gloss.
</canon_editorial>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Aprobar el mapa de slots del NUCLEO dimostrativi</name>
  <read_first>
    - .planning/research/FEATURES.md (Per-category slot summary linea ~103: DEMOS ~4 table-stakes; match justification linea ~114)
    - .planning/phases/36-dimostrativi-possessivi-determinantes/36-CONTEXT.md (D-36-01 granularidad de slots; scope-gate SC#2)
    - .planning/phases/36-dimostrativi-possessivi-determinantes/36-PATTERNS.md (sub-slot analogs table + articoli-049 shape lineas 103-138)
  </read_first>
  <decision>Aprobar el mapa de ids/tipo del nucleo dimostrativi antes de autorar (patron v1.6/v1.7 de aprobar el mapa de reagrupacion). Propuesta del planner (ids exactos = discrecion del executor con prefijo `dimostrativi-`, D-36-01 fija la granularidad):</decision>
  <context>
    D-36-01 (LOCKED): las 6 formas de `quello` van en 1 slot MC (variantes contrastantes por disparador fonetico) + 1 slot `match` (noun->forma, analogo articoli-049). NO el split fino por clase fonetica que hizo articoli en Phase 19. El magnet quei/quegli se cubre con variantes contrastantes + el match.
    Mapa propuesto para el NUCLEO (este plan; los A2 cio/pronominales + el cruce 300 los hace 36-02):
      - `dimostrativi-questo` (MC) -> concordancia questo/questa/questi/queste (genero/numero), vicino. DEMOS-01.
      - `dimostrativi-quest-elisione` (MC) -> elision `quest'` ante vocal (quest'anno, quest'amica). DEMOS-02.
      - `dimostrativi-quello` (MC) -> las 6 formas quel/quello/quell'/quei/quegli/quelle como variantes contrastantes por disparador fonetico. DEMOS-03. MAGNET #1.
      - `dimostrativi-match-quello` (match) -> noun->forma de quello, analogo articoli-049. DEMOS-03. MAGNET #1.
      - `dimostrativi-collasso-es` (MC o word-buttons) -> colapso ES 3-vias->IT 2-vias con anclas de distancia qui/la + gloss ES. DEMOS-04.
    Word-buttons vs MC en collasso-es = discrecion del executor (guia research: MC para produccion de forma; word-buttons donde el orden aporte).
  </context>
  <options>
    <option id="approve-as-is">
      <name>Aprobar el mapa propuesto (5 slots de nucleo)</name>
      <pros>Coherente con D-36-01, con presente-regolare (pocos slots densos), y con articoli-049 para el match.</pros>
      <cons>Ninguno conocido.</cons>
    </option>
    <option id="adjust">
      <name>Ajustar (renombrar ids, mover collasso-es a word-buttons, anadir/quitar un slot de nucleo)</name>
      <pros>El autor afina la granularidad a su gusto.</pros>
      <cons>Debe seguir cubriendo DEMOS-01..04 y respetar D-36-01 (quello = 1 MC + 1 match).</cons>
    </option>
  </options>
  <acceptance_criteria>
    - El autor ha aprobado un mapa que incluye: 1 slot questo-concordancia, 1 slot quest'-elision, 1 slot quello-MC, 1 slot quello-match, 1 slot colapso-ES.
    - Todos los ids acordados empiezan por `dimostrativi-`.
    - El mapa mantiene quello = 1 MC + 1 match (D-36-01); NO introduce el split fino por clase fonetica.
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" o indica los ajustes al mapa de slots.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Autorar los slots del nucleo dimostrativi (1-por-1 por quorum R1-R7)</name>
  <read_first>
    - content/exercises/presente-regolare.json (molde estructural: top-level notes+exercises, slot MC presente-regolare-are lineas 4-60, word-buttons -are-wb lineas 359-410, bloque validation lineas 43-59)
    - content/exercises/articoli.json (articoli-049 lineas 1326-1385: shape EXACTO del match noun->forma + la familia lo/gli/uno para verificar clases foneticas de quel/quello/quell'/quei/quegli/quelle)
    - .planning/research/PITFALLS.md (Pitfall 1 quei/quegli magnet; Pitfall 2 ES 3-vias + codesto OOS + anclas; Pitfall 8 R1 leak; Pitfall 9 R7 gloss; Pitfall 10 acentos)
    - .claude/skills/gsd-validate-exercise (SKILL.md: quorum Opus+Sonnet, C1-C5, NUNCA batched, 1 subagent fresh por ejercicio)
    - scripts/validate-ai-pass.mjs (refuerzo cross-vendor Gemini/DeepSeek, auto-fallback 429 -> deepseek-reasoner, --write; claves en .env)
  </read_first>
  <action>
    Crear `content/exercises/dimostrativi.json` con el top-level `{notes, exercises:[]}` copiando la estructura de presente-regolare.json. En `notes` (autor-interno, nunca mostrado): documentar (a) que `cio` sera slot lexico single-variant "sin autoria de variantes" (lo autora 36-02), patron PROF-01/SOST-01, y (b) `codesto` OUT-OF-SCOPE (near-arcaico/toscano-regional; refuerza el modelo ES 3-vias equivocado) -> NUNCA autorado como key ni distractora (SC#2, Pitfall 2). Espejo de como presente-regolare.json documenta su 0-match.
    Autorar los slots del mapa aprobado (Task 1), cada uno con >=2 variantes en los slots rule-rich (NUNCA payload, NUNCA variante solitaria en slot de regla; Anti-Pattern 4):
      - `dimostrativi-questo` (MC): variantes que ejercen las 4 formas questo/questa/questi/queste concordando con el sustantivo. Distractoras = otras formas reales de questo, solo una concuerda. Explanation nombra POR QUE las distractoras fallan.
      - `dimostrativi-quest-elisione` (MC): elision `quest'` ante vocal (quest'anno, quest'amica), contra la forma no-elidida como distractora. Apostrofe ASCII U+0027.
      - `dimostrativi-quello` (MC): las 6 formas quel/quello/quell'/quei/quegli/quelle como variantes CONTRASTANTES por disparador fonetico. MAGNET #1: pasar CADA noun por la tabla fonetica (R5 checklist item 4) ANTES de escribir la key -> quel (cons. simple, il->i), quello/quegli (s-impura/z/gn/ps/x/y/i+vocal), quell' (vocal sing), quei (il->plural), quelle (fem plural). Verificar quei-vs-quegli noun-por-noun (Pitfall 1).
      - `dimostrativi-match-quello` (match): `variants[0].pairs` = array de [noun, forma], >=3 valores derecha DISTINTOS (analogo articoli-049 que usa lo/gli/i). Mapear cada noun por la tabla fonetica: p.ej. quello studente / quei ragazzi / quegli zii / quegli amici / quell'albero / quelle case. Sin options/correctIndex.
      - `dimostrativi-collasso-es` (MC o word-buttons): colapso ES 3-vias->IT 2-vias. CADA fill-in lleva un ancla de distancia italiana (`qui`/`qua` fuerza questo; `la`/`li`/`in fondo` fuerza quello) MAS gloss `(en espanol: ...)` para que sobreviva UNA sola lectura (Pitfall 2, mismo shape que R7 `Sono ___ Roma di nascita`). Un gloss "(en espanol: ese)" SIN ancla italiana es trap de doble-validez -> prohibido. codesto ausente de keys y distractoras.
    Validar 1-por-1: por cada slot, 1 subagent FRESH via skill `gsd-validate-exercise` (base canonica Opus+Sonnet, C1-C5, NUNCA batched). En dimostrativi-quello Y dimostrativi-match-quello (MAGNET #1, INT-04) anadir OBLIGATORIAMENTE una ronda EXTRA con un `by` DeepSeek via `scripts/validate-ai-pass.mjs` (precedente v1.2: DeepSeek cazo 8 bugs de articoli que human-verify aprobo). Resolver disputadas por REESCRITURA (ambas IAs -> correcta), NUNCA por override-atajo; un flag C4-accent sobre espanol sin tildes = arreglar la tilde. El gloss R7 marcado C5 por Gemini/DeepSeek = falso-positivo de politica, mantener.
    Escribir el bloque `validation` de cada slot con >=2 passes, >=2 `by` distintos, ambos verdict "correcta". Usar el array `concerns[]` para dejar notas de verificacion tag [D-36-01]/[R1-R7] (convencion de presente-regolare-300).
    NO tocar el motor. NO registrar en categories.json (lo hace 36-02). NO autorar cio/pronominales/cruce 300 (lo hace 36-02).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/exercises/dimostrativi.json'); const s=c.exercises; const ids=s.map(e=>e.id); if(!ids.every(i=>i.startsWith('dimostrativi-'))) throw new Error('id sin prefijo dimostrativi-'); const m=s.find(e=>e.type==='match'); if(!m) throw new Error('falta el slot match de quello'); const rt=m.variants[0].pairs.map(p=>p[1]); if(new Set(rt).size<3) throw new Error('match <3 valores derecha distintos'); s.filter(e=>/^dimostrativi-(questo|quest-elisione|quello|collasso)/.test(e.id)).forEach(e=>{ const v=e.variants||[]; if(v.length<2) throw new Error(e.id+' rule-slot con <2 variantes'); }); s.forEach(e=>{ if(e.validation.status!=='validated') throw new Error(e.id+' no validated'); const by=new Set(e.validation.passes.map(p=>p.by)); if(by.size<2) throw new Error(e.id+' <2 by distintos'); if(!e.validation.passes.every(p=>p.verdict==='correcta')) throw new Error(e.id+' pass no correcta'); }); const magnet=s.filter(e=>/quello/.test(e.id)); magnet.forEach(e=>{ if(![...new Set(e.validation.passes.map(p=>p.by))].some(b=>/deepseek/i.test(b))) throw new Error(e.id+' MAGNET sin pase DeepSeek'); }); const blob=JSON.stringify(c).toLowerCase(); if(blob.includes('codesto')) throw new Error('codesto presente (OOS)'); console.log('OK dimostrativi nucleo:', ids.length, 'slots')"</automated>
    <automated>node scripts/validate-content-fixture.mjs dimostrativi content/exercises/dimostrativi.json</automated>
    <automated>grep -c "applyImmediateFailure(this.state" src/screens/app.js | grep -qx 2 && echo "D-54 OK: 2 call-sites"</automated>
    <automated>test -z "$(git diff --stat src/screens/app.js src/domain/progress.js)" && echo "engine diff vacio OK"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/dimostrativi.json` parsea como JSON valido.
    - Todos los ids empiezan por `dimostrativi-`.
    - Cada slot rule-rich (questo, quest-elisione, quello, collasso) tiene `variants[].length >= 2`.
    - Existe 1 slot `type:"match"` (dimostrativi-match-quello) con `pairs` de >=3 valores de forma DISTINTOS (analogo articoli-049).
    - Cada slot: `validation.status === "validated"`, >=2 passes, >=2 `by` distintos, todos `verdict:"correcta"`.
    - MAGNET #1: los slots que matchean `quello` incluyen un pase con `by` DeepSeek (INT-04). Las keys quei/quegli verificadas noun-por-noun contra la tabla fonetica (dejado en concerns[]).
    - Cada fill-in del colapso ES lleva ancla de distancia italiana + gloss `(en espanol: ...)` (una sola lectura).
    - `codesto` ausente de todo el archivo (keys, distractoras, prompts).
    - `notes` documenta cio single-variant (futuro 36-02) + codesto OUT-OF-SCOPE.
    - `node scripts/validate-content-fixture.mjs dimostrativi content/exercises/dimostrativi.json` sale 0.
    - `grep -c "applyImmediateFailure(this.state" src/screens/app.js` == 2 (D-54 intacto).
    - `git diff src/screens/app.js src/domain/progress.js` vacio.
  </acceptance_criteria>
  <done>
    dimostrativi.json existe con los slots del nucleo (questo, quest'-elision, quello MC, quello match, colapso ES) autorados y validados 1-por-1 por quorum, con pase DeepSeek en los slots quello (magnet). Motor intacto. Listo para que 36-02 anada los diferenciadores A2, registre la categoria y autore el cruce 300.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna nueva | Contenido JSON estatico cargado por el loader validado existente; sin input de usuario, sin red, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | content/exercises/dimostrativi.json (contenido nuevo) | accept | No introduce superficie de ataque: JSON estatico via loader validado (schema-validator permisivo pero validado); el invariante anti-XSS es render `x-text`-only (ya en su sitio, no tocado). Sin input de usuario, sin red, sin auth. |
| T-36-SC | Tampering | validate-ai-pass.mjs (llamadas a Gemini/DeepSeek con claves .env) | accept | Script existente ya en uso (v1.2-v1.7); no se instala ningun paquete nuevo en este plan; claves en .env fuera de git. |

No hay amenazas high/medium introducidas: content-only, engine untouched, invariante anti-XSS `x-text` preservado.
</threat_model>

<verification>
- `content/exercises/dimostrativi.json` parsea y valida via content-fixture validator.
- Los 5 slots del nucleo cubren DEMOS-01 (questo), DEMOS-02 (quest'), DEMOS-03 (quello MC + match), DEMOS-04 (colapso ES).
- MAGNET #1 (quei/quegli): pase DeepSeek presente en los slots quello.
- D-54: exactamente 2 call-sites de applyImmediateFailure; git diff del motor vacio.
- codesto ausente; notes documenta cio + codesto OOS.
</verification>

<success_criteria>
- DEMOS-01, DEMOS-02, DEMOS-03, DEMOS-04 ejercitables por el nucleo autorado de dimostrativi.
- Todas las variantes nuevas `validated` (>=2 by distintos), con pase DeepSeek en el magnet quello.
- Colapso ES sin doble-validez (ancla de distancia + gloss en cada fill-in).
- Motor v1.4 intacto (2 call-sites, diff vacio).
</success_criteria>

<output>
Create `.planning/phases/36-dimostrativi-possessivi-determinantes/36-01-SUMMARY.md` when done
</output>
