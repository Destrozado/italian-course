---
phase: 11-articoli
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - content/exercises/articoli.json
autonomous: false
requirements: [ART-03, ART-04, ART-05, ART-07]
must_haves:
  truths:
    - "Existe content/exercises/articoli.json con un bloque de ejercicios multiple-choice (columna vertebral de la categoría, grading por índice — D-05) que cubren cada forma del artículo DETERMINATIVO en su disparador fonético, incluyendo los básicos il/la/l' + plurales i/gli/le (D-01, D-02)"
    - "Existen ejercicios multiple-choice que cubren cada forma del artículo INDETERMINATIVO (un/uno/una/un') con su disparador (D-04)"
    - "Las trampas canónicas D-04 (lo zio, uno studente, lo psicologo, l'amico/l'amica, un'amica vs un amico) tienen cada una ≥1 ejercicio"
    - "El uso de word-buttons es mínimo o nulo en este bloque base — montar el sintagma completo solapa con génnumero, así que la columna vertebral es multiple-choice (D-07)"
    - "Cada ejercicio creado tiene una explanation pedagógica curada en payload (español acentuado, italianismos preservados, apóstrofes ASCII U+0027, plain text sin markdown) y queda en validation.status pending para la validación por quórum posterior (D-15)"
  artifacts:
    - path: "content/exercises/articoli.json"
      provides: "Archivo de ejercicios de la categoría articoli con la base multiple-choice det+indet y sus explanations"
      contains: "\"id\": \"articoli-001\""
  key_links:
    - from: "content/exercises/articoli.json"
      to: "src/data/schema-validator.js"
      via: "cada ejercicio valida contra validateMultipleChoicePayload (prompt con ___, options 3-4 strings, correctIndex en rango)"
      pattern: "\"type\": \"multiple-choice\""
    - from: "content/exercises/articoli.json"
      to: ".planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md"
      via: "cada celda del temario tiene ≥1 ejercicio en este archivo"
      pattern: "articoli-0"
---

<objective>
Crear `content/exercises/articoli.json` y autorizar el **bloque base de ejercicios multiple-choice** que cubre el sistema completo de artículos: determinativi (`il/lo/l'/la` + plurales `i/gli/le`) e indeterminativi (`un/uno/una/un'`), cada forma en su disparador fonético, con todas las trampas canónicas D-04. Cada ejercicio lleva una **explanation pedagógica curada** (D-85, canon editorial heredado).

Purpose: Es la columna vertebral de la categoría (D-05): multiple-choice rápido, grading por índice, la dificultad real en el disparador fonético. Cubre ART-03 (determinativi), ART-04 (indeterminativi), ART-05 (trampas) y ART-07 (explanations) para el grueso de los ejercicios.

Output: `content/exercises/articoli.json` con el envelope `{ "exercises": [...] }` y ~30-38 ejercicios `articoli-001..` de tipo `multiple-choice`, cada uno con `validation.status: "pending"` y `passes: []` (la validación por quórum la hace el plan 11-05). El bloque `match` y los bridges los añade el plan 11-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-articoli/11-CONTEXT.md
@.planning/phases/11-articoli/11-PATTERNS.md
@.planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear articoli.json y autorizar el bloque DETERMINATIVO multiple-choice (il/lo/l'/la + plurales i/gli/le)</name>
  <read_first>
    - .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md — la fuente de verdad de qué celdas determinativas cubrir (cada celda → ≥1 ejercicio)
    - .planning/phases/11-articoli/11-PATTERNS.md §"(a) Base multiple-choice exercise" — la shape exacta del ejercicio (id/type/categoryIds/payload con prompt+options+correctIndex+explanation/notes/validation), líneas 35-80
    - content/exercises/avere.json (líneas 3-38) — ejemplo real de ejercicio multiple-choice con bloque validation completo
    - src/data/schema-validator.js (validateMultipleChoicePayload, líneas 159-186) — constraints enforced: prompt debe contener "___", options array de 3 o 4 strings no vacíos, correctIndex entero en rango [0, options.length)
  </read_first>
  <action>
    Crear el archivo nuevo `content/exercises/articoli.json` con el envelope raíz `{ "exercises": [ ... ] }` (una sola clave top-level `exercises` con un array; sin metadata de categoría — eso vive en categories.json).

    Autorizar el bloque de ejercicios DETERMINATIVOS, ids consecutivos empezando en `articoli-001`. Todos `type: "multiple-choice"`, `categoryIds: ["articoli"]`. Cobertura obligatoria (≥1 ejercicio por celda del temario, varios contextos léxicos en los disparadores frecuentes — densidad D-12):
    - masc sing `il` ante consonante simple (il libro, il ragazzo, il cane, il tavolo).
    - masc sing `lo` ante s+consonante (lo studente, lo studio, lo spagnolo), z (lo zio, lo zaino, lo zucchero), ps (lo psicologo), gn (lo gnomo). Trampas D-04 obligatorias: `lo zio`, `lo psicologo`.
    - masc sing `l'` ante vocal / elisión (l'amico, l'occhio, l'uomo, l'orso). Trampa D-04: `l'amico`.
    - fem sing `la` ante consonante / s+cualquier-cosa (la casa, la zia, la studentessa, la spagnola).
    - fem sing `l'` ante vocal / elisión (l'amica, l'opera, l'isola). Trampa D-04: `l'amica`.
    - masc plural `i` (i libri, i ragazzi) vs `gli` (gli studenti, gli zii, gli psicologi, gli amici, gli occhi). Trampa D-04 plural: `gli gnocchi`.
    - fem plural `le` (le case, le amiche, le studentesse) — siempre `le`, sin variación.

    Para cada ejercicio: `payload.prompt` lleva el sustantivo disparador y el hueco `___` (ej. "Ho visto ___ studente." — el hueco literal "___" es OBLIGATORIO o el validador falla). `payload.options` = 3 o 4 formas de artículo plausibles (la correcta + distractoras que son las OTRAS formas plausibles ante ese disparador, p.ej. `["il","lo","l'","la"]` para masc sing; `["i","gli","le","l'"]` para plurales). La dificultad real es el disparador fonético, no la raíz (D-05). `payload.correctIndex` selecciona la forma correcta (grading POR ÍNDICE). Apóstrofes en options/prompt ASCII U+0027 (`l'`).

    Campo `notes` (hermano top-level de payload, NO renderizado): rationale breve referenciando la celda del temario y la decisión aplicada (ej. "Disparador z→lo (D-04 trampa). Distractoras il/l'/la = errores típicos del hispanohablante.").

    Campo `validation`: escribir `{ "status": "pending", "passes": [] }` en CADA ejercicio. NO inventar pases — la validación por quórum la ejecuta el plan 11-05 (interactivo).

    Estrategia de distractoras (Claude's discretion, CONTEXT): las otras formas de artículo plausibles ante el mismo disparador, priorizando el error típico del hispanohablante (usar `il` donde italiano pide `lo` por z-/s+cons-, como en español). Cada ejercicio debe tener UNA sola opción correcta inequívoca (regla C2 del canon de validación).

    NO escribir el bloque `match` ni los bridges en este plan (van en 11-03). NO tocar categories.json, run-validation-271.mjs ni tests/exercise-types.test.js (van en 11-04).
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); const mc=d.exercises.filter(e=>e.type==='multiple-choice'); if(mc.length<20){console.error('Esperaba >=20 mc determinativi, hay '+mc.length);process.exit(1)} const noExpl=d.exercises.filter(e=>typeof e.payload.explanation!=='string'||!e.payload.explanation.trim()); if(noExpl.length){console.error('Sin explanation: '+noExpl.map(e=>e.id).join(','));process.exit(1)} console.log('OK '+d.exercises.length+' ejercicios, todos con explanation')"</automated>
  </verify>
  <acceptance_criteria>
    - `content/exercises/articoli.json` parsea como JSON y tiene la clave top-level `exercises` (array).
    - `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` sale exit 0 (todos los multiple-choice cumplen: prompt con `___`, options 3-4 strings, correctIndex en rango).
    - Existen ≥20 ejercicios `multiple-choice` que cubren `il/lo/l'/la/i/gli/le` con sus disparadores; cada forma determinativa del temario tiene ≥1 ejercicio.
    - Las trampas determinativas D-04 (`lo zio`, `lo psicologo`, `gli gnocchi`, `l'amico`/`l'amica`) aparecen cada una en ≥1 prompt.
    - Cada ejercicio tiene `validation: {status:"pending", passes:[]}`.
    - Cero comillas tipográficas en el archivo (apóstrofes ASCII U+0027).
  </acceptance_criteria>
  <done>El bloque determinativo multiple-choice cubre cada forma `il/lo/l'/la/i/gli/le` en su disparador, con trampas D-04 y explanations curadas, validando contra el schema.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Añadir el bloque INDETERMINATIVO multiple-choice (un/uno/una/un') con contraste de espejo</name>
  <read_first>
    - .planning/phases/11-articoli/11-TEMARIO-ARTICOLI.md §Indeterminativi — celdas a cubrir (un/uno ante consonante/vocal vs s+cons/z/ps/gn; una/un' fem)
    - .planning/phases/11-articoli/11-PATTERNS.md §"(a) Base multiple-choice" y §"Explanation canon" (líneas 272-275) — canon de explanation aplicado
    - content/exercises/articoli.json — el archivo ya creado en Task 1 (para APPEND, conservando el envelope y los ids ya usados)
  </read_first>
  <action>
    APPEND al array `exercises` de `content/exercises/articoli.json` (sin reescribir lo de Task 1) el bloque INDETERMINATIVO, continuando la numeración consecutiva de ids (ej. si Task 1 terminó en articoli-0XX, seguir en articoli-0(XX+1)). Todos `type: "multiple-choice"`, `categoryIds: ["articoli"]`. ~10-15 ejercicios (reparto det/indet, CONTEXT). Cobertura obligatoria:
    - masc `un` ante consonante simple Y ante vocal — SIN apóstrofo en masc (un libro, un ragazzo, un amico, un occhio, un uomo). Trampa D-04: `un amico` (contraste con `un'amica`).
    - masc `uno` ante s+consonante, z, ps, gn (uno studente, uno zaino, uno psicologo, uno gnomo). Trampas D-04: `uno studente`, `uno zaino`.
    - fem `una` ante consonante (una casa, una zia, una ragazza).
    - fem `un'` ante vocal / elisión — CON apóstrofo (un'amica, un'opera, un'ora). Trampa D-04: `un'amica`.

    Crear al menos UN ejercicio de contraste que explote el espejo pedagógico (`<specifics>`): el mismo disparador dispara `il→lo→l'` (det) y `un→uno→un'` (indet). Ejemplo de contraste indet: prompt con `studente` y options `["un","uno","una","un'"]` → correcta `uno`; otro con `amico` masc → options incluyen `un` y `un'` para forzar la distinción "masc NO lleva apóstrofo" vs "fem SÍ" (`un amico` vs `un'amica`). La explanation debe dejar EXPLÍCITO que el apóstrofo en indeterminativo es SOLO femenino (`un'`), nunca masculino (`un`).

    Cada ejercicio: `validation: {status:"pending", passes:[]}`, `notes` con rationale + celda del temario, explanation curada (canon: español acentuado, italianismos preservados, apóstrofes ASCII U+0027, plain text SIN markdown — sin `**`/`__`/`##`/backtick — y SIN referencias `#NNN` a otros ejercicios, regla de memoria).

    Distractoras: las otras formas indeterminativas plausibles; el error típico del hispanohablante es poner apóstrofo en masc (`un'amico` ✗) o usar `un` donde va `uno` (`un studente` ✗) — usarlos como distractoras.
  </action>
  <verify>
    <automated>node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json && node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/articoli.json','utf8')); const ex=d.exercises; const hasUnApostrofe=ex.some(e=>JSON.stringify(e.payload).includes(\"un'\")); const hasUnoStudente=ex.some(e=>/studente/.test(e.payload.prompt)&&e.payload.options.includes('uno')); if(!hasUnApostrofe){console.error(\"Falta forma un' (fem elidido)\");process.exit(1)} if(!hasUnoStudente){console.error('Falta ejercicio uno+studente');process.exit(1)} const pend=ex.filter(e=>e.validation&&e.validation.status==='pending'&&Array.isArray(e.validation.passes)&&e.validation.passes.length===0); if(pend.length!==ex.length){console.error('No todos pending/passes[]: '+(ex.length-pend.length));process.exit(1)} console.log('OK indet, '+ex.length+' totales, todos pending')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` sigue exit 0 tras el append.
    - Existen ejercicios para `un/uno/una/un'` cubriendo cada disparador del temario indeterminativo.
    - Las trampas indeterminativas D-04 (`uno studente`, `uno zaino`, `un'amica` con apóstrofo, `un amico` sin apóstrofo) aparecen.
    - Al menos un ejercicio explota el contraste masc-sin-apóstrofo (`un`) vs fem-con-apóstrofo (`un'`).
    - TODOS los ejercicios del archivo (det+indet) tienen `validation:{status:"pending",passes:[]}`.
    - Cada explanation es plain text sin tokens markdown y sin referencias `#NNN`.
  </acceptance_criteria>
  <done>El bloque indeterminativo cubre `un/uno/una/un'` con trampas D-04 y el contraste de apóstrofo masc/fem, explanations curadas, validando contra el schema.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — el autor revisa por bloques las explanations del bloque base (D-85)</name>
  <what-built>
    El bloque base multiple-choice de Articoli (~30-38 ejercicios determinativi + indeterminativi) en `content/exercises/articoli.json`, cada uno con prompt+opciones+respuesta correcta y una explanation pedagógica curada (canon: español acentuado, italianismos, apóstrofes ASCII, plain text). Todos en `validation.status: "pending"` (la validación por quórum vendrá después).
  </what-built>
  <how-to-verify>
    1. Revisa las explanations por bloques (patrón D-85 — revisión humana por lote): abre `content/exercises/articoli.json` y lee las explanations de los ejercicios determinativos primero, luego los indeterminativos.
    2. Para cada explanation comprueba el canon: ¿español con acentos correctos? ¿términos italianos preservados (`lo`, `gli`, `un'`)? ¿apóstrofes ASCII (no tipográficos)? ¿plain text sin markdown? ¿sin referencias a "#NNN" de otros ejercicios?
    3. Comprueba que cada explanation enseña la REGLA del disparador fonético al alumno (por qué `lo` y no `il` ante z-/s+cons-), no solo el dato.
    4. Verifica las trampas que más te cuestan: busca `lo zio`, `uno studente`, `lo psicologo`, `gli gnocchi`, `l'amico`/`l'amica`, y el contraste `un'amica` (apóstrofo) vs `un amico` (sin apóstrofo).
    5. Comprueba que la respuesta correcta (correctIndex) es inequívoca en cada ejercicio y que las distractoras son errores plausibles del hispanohablante.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" para fijar el bloque base, o indica qué explanations reescribir, qué ejercicios reformular, o qué celdas del temario aún no están cubiertas.</resume-signal>
</task>

</tasks>

<verification>
- `content/exercises/articoli.json` existe, parsea, y `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` sale exit 0.
- Cobertura ART-03 (det) + ART-04 (indet) + ART-05 (trampas D-04) en el bloque multiple-choice base; cada celda del temario con ≥1 ejercicio.
- Cada ejercicio tiene explanation curada (ART-07) y `validation.status: "pending"`.
- El commit de articoli.json es POSTERIOR en git al commit del temario (11-01).
</verification>

<success_criteria>
- El grueso de ART-03/04/05 cubierto con multiple-choice; explanations curadas (ART-07) aprobadas por el autor.
- Archivo listo para que 11-03 añada el bloque match + bridges, y 11-05 valide por quórum.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON de contenido ↔ DOM de la app | Las explanations y prompts se renderizan al usuario. La superficie es content-injection/XSS si el contenido se interpretara como HTML/markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering / Injection | `payload.explanation` y `payload.prompt` renderizados al usuario | mitigate | Mitigado project-wide por la decisión LOCKED T-02-01: el contenido se renderiza vía `x-text` exclusivo (nunca x-html), así que tokens markdown/HTML se muestran literales, no se interpretan. El test `tests/exercise-types.test.js` (plain-text assertion) refuerza que las explanations no contienen `**`/`__`/`##`/backtick. Autor único, contenido editado a mano → sin entrada no confiable. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). No aplica audit de legitimidad. |

Sin amenazas HIGH: contenido educativo estático, renderizado por `x-text` (anti-XSS por construcción), un solo autor de confianza.
</threat_model>

<output>
Create `.planning/phases/11-articoli/11-02-SUMMARY.md` when done
</output>
