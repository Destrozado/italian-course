---
phase: 11-articoli
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - content/categories.json
  - scripts/run-validation-271.mjs
  - tests/exercise-types.test.js
autonomous: true
requirements: [ART-01]
must_haves:
  truths:
    - "La categoria articoli existe en content/categories.json con order 8 y la app la surfacea como 8a fila del home + boton Examen (sin codigo UI, via categoriesForDisplay)"
    - "Los TRES numeros de conteo estan en lockstep: articoli.json length == expected en CATEGORIES de run-validation-271.mjs == expected en CATEGORIES_WITH_EXPLANATIONS de exercise-types.test.js"
    - "El conteo N propagado a los 3 sitios es el numero final denso (~45-55) de la categoria (D-12), determinado por la longitud real de articoli.json segun el temario (D-13) — no un numero inventado"
    - "TOTAL_EXPECTED en run-validation-271.mjs subio de 272 a 272+N de forma consistente con la suma de expected"
  artifacts:
    - path: "content/categories.json"
      provides: "Entry de la categoria articoli (id/name/order) que auto-surfacea el home row + Examen"
      contains: "articoli"
    - path: "scripts/run-validation-271.mjs"
      provides: "Entry articoli en CATEGORIES[] + TOTAL_EXPECTED bumpeado a 272+N"
      contains: "slug: 'articoli'"
    - path: "tests/exercise-types.test.js"
      provides: "Entry articoli en CATEGORIES_WITH_EXPLANATIONS con su cuenta exacta"
      contains: "content/exercises/articoli.json"
  key_links:
    - from: "content/categories.json"
      to: "src/screens/app.js (categoriesForDisplay ~2141)"
      via: "anadir entry auto-surfacea fila home + Examen (examenEnabled = totalCount > 0), sin codigo UI"
      pattern: "id.*articoli"
    - from: "scripts/run-validation-271.mjs (CATEGORIES + TOTAL_EXPECTED)"
      to: "content/exercises/articoli.json (length)"
      via: "los 3 numeros en lockstep; el reporter falla si la suma de expected != TOTAL_EXPECTED o != ejercicios en disco"
      pattern: "TOTAL_EXPECTED"
---

<objective>
Cablear la categoria `articoli` en los TRES puntos de integracion que un alta de categoria requiere (sin tocar engine ni UI), con los tres numeros de conteo en LOCKSTEP: (1) entry en `content/categories.json`, (2) entry en `CATEGORIES[]` + bump de `TOTAL_EXPECTED` en `scripts/run-validation-271.mjs`, (3) entry en `CATEGORIES_WITH_EXPLANATIONS` en `tests/exercise-types.test.js`. Es la tarea BLOCKING de integracion: los 3 conteos deben coincidir con la longitud real de `content/exercises/articoli.json`.

Purpose: anadir la entry a categories.json auto-surfacea la 8a fila del home + boton Examen via `categoriesForDisplay` (cero codigo UI) — cubre ART-01. Los conteos en lockstep son condicion para que el reporter y el smoke test puedan pasar (el reporter falla si la suma de `expected` != `TOTAL_EXPECTED` o != ejercicios en disco).

Output: los 3 archivos editados con el conteo final N de articoli.json (leido del archivo, NO inventado) identico en los 3 sitios, y `TOTAL_EXPECTED` = 272 + N. Tras este plan: el content-fixture y el coverage test de articoli pasan; el reporter dara exit distinto de 0 SOLO por status (los nuevos ejercicios estan en `pending` hasta 11-05) — eso es esperado y lo cierra 11-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-articoli/11-PATTERNS.md

<interfaces>
Shapes exactas de los 3 puntos de integracion. Extraidas del repo. El ejecutor usa estas directamente, sin exploracion.

content/categories.json (array actual: 7 entries; anadir articoli como 8a, anadiendo coma tras la linea de profesiones). La entry nueva: id "articoli", name "Articoli (artículos)", order 8. Respetar el alineamiento de columnas del archivo (patron parentetico de las demas categorias).

scripts/run-validation-271.mjs — array CATEGORIES (lineas 68-76). Orden documentado en el comentario linea 63: "riesgo-first (preposiciones) + alfabetico resto". articoli es alfabeticamente primero del resto, asi que la entry nueva va JUSTO tras la linea de preposiciones: slug 'articoli', file 'content/exercises/articoli.json', expected N. Y TOTAL_EXPECTED (linea 78) pasa de 272 a 272+N (escribir el numero resuelto, p.ej. si N=50 escribir 322). El comentario lineas 64-67 explica de donde sale el 272: actualizarlo para reflejar 272 + N (articoli).

tests/exercise-types.test.js — array CATEGORIES_WITH_EXPLANATIONS (lineas 1265-1274). Anadir 1 linea: file 'content/exercises/articoli.json', expected N. (El orden dentro de este array no importa; anadir al final antes del cierre del array.)

El valor N es identico en los 3 sitios y es la longitud real de articoli.json.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver el conteo N de articoli.json y anadir la entry a categories.json</name>
  <read_first>
    - content/categories.json — el array completo actual (7 entries) para insertar la 8a conservando el formato alineado
    - .planning/phases/11-articoli/11-PATTERNS.md seccion "content/categories.json" (lineas 181-204) — insercion exacta y el dato de que esto auto-surfacea el home row sin codigo UI
    - .planning/phases/11-articoli/11-03-SUMMARY.md — el conteo final N reportado por el plan 11-03 (cross-check con el archivo real)
  </read_first>
  <action>
    Primero, resolver el conteo REAL N de articoli.json (NO inventar) leyendo el archivo y contando exercises (node oneliner: JSON.parse del archivo, .exercises.length). Cross-checar con el N reportado en 11-03-SUMMARY.md; si difieren, el archivo manda: usar la longitud real y anotarlo en el SUMMARY de este plan. Registrar N porque las Tasks 2 y 3 lo reutilizan identico.

    Editar `content/categories.json`: anadir la coma final a la linea de profesiones y anadir como ULTIMO elemento del array la entry articoli con id "articoli", name "Articoli (artículos)" (con acento en articulos), order 8. Respetar el alineamiento de columnas del archivo. NO tocar las otras 7 entries ni sus order.

    NO escribir codigo UI: la entry auto-surfacea la fila del home + boton Examen via categoriesForDisplay (src/screens/app.js ~2141, examenEnabled = totalCount > 0). Confirmado en PATTERNS.md; no requiere edicion de app.js.
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('content/categories.json','utf8')); const a=c.categories.find(x=>x.id==='articoli'); if(!a){console.error('Falta entry articoli');process.exit(1)} if(a.order!==8){console.error('order != 8');process.exit(1)} if(c.categories.length!==8){console.error('Esperaba 8 categorias');process.exit(1)} console.log('OK articoli order 8')" && node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json</automated>
  </verify>
  <acceptance_criteria>
    - El conteo N de articoli.json queda resuelto leyendo el archivo real y registrado en el SUMMARY.
    - content/categories.json parsea y contiene exactamente 8 categorias.
    - La entry articoli tiene id "articoli", order 8, name "Articoli (artículos)".
    - Las otras 7 entries quedan intactas (mismos id/order).
    - `node scripts/validate-content-fixture.mjs articoli content/exercises/articoli.json` sigue exit 0 (la nueva categoria no rompe la validacion de contenido).
  </acceptance_criteria>
  <done>La categoria articoli existe en categories.json con order 8; la app la surfacea como 8a fila + Examen sin codigo UI; N resuelto del archivo real.</done>
</task>

<task type="auto">
  <name>Task 2: Anadir articoli a CATEGORIES[] y bumpear TOTAL_EXPECTED en run-validation-271.mjs</name>
  <read_first>
    - scripts/run-validation-271.mjs (lineas 63-78) — el comentario del orden (riesgo-first + alfabetico), el array CATEGORIES (68-76) y TOTAL_EXPECTED (78). El valor actual de TOTAL_EXPECTED es 272 y la suma de expected de las 7 entries es 272.
    - .planning/phases/11-articoli/11-PATTERNS.md seccion "scripts/run-validation-271.mjs" (lineas 208-231) — las 2 ediciones (CATEGORIES + TOTAL_EXPECTED) y la regla de lockstep
  </read_first>
  <action>
    Usar el N resuelto en Task 1 (la longitud real de articoli.json). Editar scripts/run-validation-271.mjs:

    Edicion 1 — array CATEGORIES (lineas 68-76): insertar JUSTO tras la linea de preposiciones (orden alfabetico del resto) la entry: slug 'articoli', file 'content/exercises/articoli.json', expected N. Respetar el alineamiento de columnas del array.

    Edicion 2 — TOTAL_EXPECTED (linea 78): cambiar el literal de 272 a 272+N ya resuelto como numero entero (p.ej. si N=50, escribir 322). NO dejar una expresion "272 + N"; escribir el numero calculado.

    Edicion 3 — comentario lineas 64-67: actualizar la explicacion para que mencione que el total ahora incluye los N ejercicios de articoli (8a categoria, v1.2 Phase 11), no solo los 272 de v1.0/v1.1.

    El reporter exige que: suma de expected de CATEGORIES == TOTAL_EXPECTED, y que ambos == ejercicios encontrados en disco. Por eso N debe ser identico al de Task 1 y al de Task 3.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('scripts/run-validation-271.mjs','utf8'); const N=JSON.parse(fs.readFileSync('content/exercises/articoli.json','utf8')).exercises.length; if(!/slug:\s*'articoli'/.test(src)){console.error('Falta entry articoli en CATEGORIES');process.exit(1)} const m=src.match(/expected:\s*(\d+)[^\n]*content\/exercises\/articoli\.json|articoli\.json[^\n]*expected:\s*(\d+)/); const te=src.match(/TOTAL_EXPECTED\s*=\s*(\d+)/); if(!te){console.error('No encuentro TOTAL_EXPECTED');process.exit(1)} if(Number(te[1])!==272+N){console.error('TOTAL_EXPECTED='+te[1]+' esperado '+(272+N));process.exit(1)} console.log('OK TOTAL_EXPECTED='+te[1]+' (272+'+N+')')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run-validation-271.mjs contiene una entry con slug 'articoli' y file 'content/exercises/articoli.json' en CATEGORIES.
    - El expected de esa entry == N (longitud real de articoli.json).
    - TOTAL_EXPECTED == 272 + N (numero entero literal, no expresion).
    - El comentario 64-67 menciona la 8a categoria articoli.
    - La suma de expected de las 8 entries == TOTAL_EXPECTED (consistencia interna del reporter).
  </acceptance_criteria>
  <done>El reporter conoce la categoria articoli con su cuenta exacta y TOTAL_EXPECTED esta en lockstep (272+N).</done>
</task>

<task type="auto">
  <name>Task 3: Anadir articoli a CATEGORIES_WITH_EXPLANATIONS en exercise-types.test.js</name>
  <read_first>
    - tests/exercise-types.test.js (lineas 1255-1300) — el comentario de los 3 invariantes editoriales (coverage exacto, apostrofes ASCII, plain text sin markdown), el array CATEGORIES_WITH_EXPLANATIONS (1265-1274) y como se consume en el describe (1277-1300)
    - .planning/phases/11-articoli/11-PATTERNS.md seccion "tests/exercise-types.test.js" (lineas 235-252) — la insercion de 1 linea y la regla de lockstep
  </read_first>
  <action>
    Usar el N resuelto en Task 1. Editar tests/exercise-types.test.js: anadir al array CATEGORIES_WITH_EXPLANATIONS (lineas 1265-1274) una linea nueva con file 'content/exercises/articoli.json', expected N, antes del cierre `]` del array. Respetar el estilo de las lineas existentes. (El orden dentro del array no importa funcionalmente; el comentario de cierre linea 1273 sobre "271/271 cobertura v1.0" puede dejarse o actualizarse a la nueva cuenta — preferible actualizar para que no quede enganoso.)

    Este array se consume por 2 describe (lineas 1277 y 1400) que verifican 3 invariantes por cada categoria: (1) data.exercises.length === expected Y toda explanation no vacia; (2) apostrofes ASCII U+0027 (sin smart quotes); (3) plain text sin markers markdown. Con N correcto y las explanations curadas de 11-02/11-03, los 3 invariantes pasan para articoli.

    NO tocar la logica de los describe ni los otros entries del array.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('tests/exercise-types.test.js','utf8'); const N=JSON.parse(fs.readFileSync('content/exercises/articoli.json','utf8')).exercises.length; const re=new RegExp(\"content/exercises/articoli\\\\.json'\\\\s*,\\\\s*expected:\\\\s*(\\\\d+)\"); const m=src.match(re); if(!m){console.error('Falta entry articoli en CATEGORIES_WITH_EXPLANATIONS o formato inesperado');process.exit(1)} if(Number(m[1])!==N){console.error('expected='+m[1]+' != N='+N);process.exit(1)} console.log('OK CATEGORIES_WITH_EXPLANATIONS articoli expected='+N)" && node --test tests/exercise-types.test.js 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - tests/exercise-types.test.js contiene una entry con file 'content/exercises/articoli.json' y expected N en CATEGORIES_WITH_EXPLANATIONS.
    - El expected == N (identico a Task 1 y Task 2).
    - `node --test tests/exercise-types.test.js` pasa para los invariantes editoriales de articoli (coverage exacto + apostrofes ASCII + plain text sin markdown) — los 3 numeros estan en lockstep.
  </acceptance_criteria>
  <done>El smoke test parametrico cubre articoli.json con su cuenta exacta; los 3 numeros (categories/reporter/test) en lockstep verificado.</done>
</task>

</tasks>

<verification>
- Los TRES numeros en lockstep: longitud real de articoli.json == expected en run-validation-271.mjs CATEGORIES == expected en exercise-types.test.js CATEGORIES_WITH_EXPLANATIONS; y TOTAL_EXPECTED == 272 + N.
- content/categories.json tiene 8 categorias; articoli order 8; la app surfacea la fila + Examen sin codigo UI.
- `node --test tests/exercise-types.test.js` pasa los invariantes editoriales de articoli.
- NOTA esperada: `node scripts/run-validation-271.mjs` dara exit distinto de 0 en este punto SOLO porque los ejercicios de articoli estan en `pending` (no validados aun). Eso es correcto: el gate verde lo cierra el plan 11-05 tras la validacion por quorum. NO intentar arreglar el status en este plan.
</verification>

<success_criteria>
- ART-01 cubierto: categoria articoli existe en categories.json y carga como 8a en la app.
- Los 3 puntos de integracion cableados con conteos en lockstep; el smoke test de coverage editorial verde para articoli.
- El unico item pendiente para exit 0 del reporter es el status validated (plan 11-05).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| categories.json ↔ runtime de la app | La entry de categoria se lee al boot y mapea a la UI via categoriesForDisplay. id es slug ASCII validado por el schema. |
| constantes de conteo ↔ reporter/test | Numeros de configuracion en scripts de build/test, no entran al DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | content/categories.json (id/name renderizado en home) | mitigate | El name se renderiza via x-text (T-02-01 LOCKED), sin interpretacion de HTML/markdown. id es slug ASCII validado por ID_SLUG_RE en schema-validator. Autor unico, edicion a mano. |
| T-11-04b | Tampering | constantes de conteo en reporter/test | accept | Son numeros de configuracion en scripts Node de validacion; no entran al navegador. Un valor erroneo solo hace fallar el gate (fail-closed), no introduce ejecucion. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). |

Sin amenazas HIGH: edits de configuracion/registro; el unico valor renderizado (name de categoria) pasa por x-text.
</threat_model>

<output>
Create `.planning/phases/11-articoli/11-04-SUMMARY.md` when done (INCLUIR el valor N usado en los 3 sitios)
</output>
