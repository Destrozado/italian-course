---
phase: 12-partitivos
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified:
  - .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md
  - content/exercises/partitivos.json
autonomous: false
requirements: [PART-07]
must_haves:
  truths:
    - "Todos los ejercicios de partitivos.json tienen validation.status === validated con >=2 by DISTINTOS en passes[] (validación por quórum cross-vendor), pool = DeepSeek Pro + Opus 4.7 (override del autor de D-10, 2026-05-28; fallback DeepSeek Flash + Opus 4.7) — D-15"
    - "Cada entry de passes[] lleva el model-id COMPLETO con tier (el id de DeepSeek Pro resuelto en ejecución, p.ej. deepseek-v4-pro; claude-opus-4-7), nunca una clave genérica de vendor (deepseek / claude) — el invariante >=2 by distintos no se puede burlar con dos tiers del mismo modelo (D-12)"
    - "El canon ortográfico de acentos (D-135) figura como REGLA EXPLÍCITA en el prompt de validación (09-VALIDATION-PROMPT.md, sección C4 explanation) ANTES de correr la validación, no como scan posterior ni a discreción de la IA — ambas IAs verifican que las explanations cumplen español acentuado + italianismos preservados (D-11)"
    - "El reporter node scripts/run-validation-271.mjs sale exit 0 incluyendo todos los ejercicios nuevos de partitivos (328+N validated)"
    - "El smoke test estricto VAL_07_STRICT=1 node --test tests/*.test.js sigue verde con las constantes de conteo actualizadas (328+N)"
  artifacts:
    - path: ".planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md"
      provides: "El prompt de validación compartido con la regla de acentos D-135 explícita bajo el criterio C4 (explanation), garantizada ANTES de validar (D-11)"
      contains: "acentu"
    - path: "content/exercises/partitivos.json"
      provides: "Todos los ejercicios de partitivos con validation.status validated y passes[] de >=2 modelos distintos cross-vendor (DeepSeek Pro + Opus 4.7; fallback Flash + Opus)"
      contains: "validated"
  key_links:
    - from: ".planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md (sección C4)"
      to: "scripts/validate-ai-pass.mjs (composed = basePrompt + ejercicio) y el pase Opus (Task)"
      via: "ambos pases del quórum reciben el MISMO prompt; con la regla D-135 en C4, las dos IAs evalúan acentos como criterio binario, no a discreción"
      pattern: "acentu|D-135|RAE"
    - from: "content/exercises/partitivos.json (validation.passes)"
      to: "scripts/run-validation-271.mjs (sub-gates VAL-04/VAL-06/VAL-08)"
      via: "deriveStatus exige >=2 correcta con by distintos; el reporter verifica status validated por ejercicio y el conteo 328+N"
      pattern: "\"status\": \"validated\""
---

<objective>
Validar por QUÓRUM CROSS-VENDOR (≥2 IAs distintas de vendors diferentes) todos los ejercicios nuevos de `content/exercises/partitivos.json`, llevándolos de `validation.status: "pending"` a `"validated"` con ≥2 `by` distintos en `passes[]`, y cerrar el GATE VERDE de la fase: `node scripts/run-validation-271.mjs` exit 0 + `VAL_07_STRICT=1 node --test tests/*.test.js` verde. Antes de validar, garantizar que la regla de acentos D-135 está explícita en el prompt de validación (D-11).

Purpose: PART-07 — la definición de "done" editorial del proyecto exige quórum multi-IA por ejercicio (heredado de v1.1, R1-R7 → C1-C5). Cierra el último criterio de éxito del ROADMAP (reporter exit 0 + smoke estricto verde). El pool de Phase 12 es **DeepSeek Pro + Opus 4.7**: cross-vendor real (DeepSeek + Anthropic), conserva la diversidad de vendor que cazó 8 bugs en Articoli (Phase 11). Estructuralmente calca el mecanismo probado en Phase 11 (DeepSeek + Opus), con Pro en vez de Flash. Distinto del pool de v1.1 (Opus+Sonnet).

NOTA DE AUDITORÍA — OVERRIDE AUTORIZADO DE D-10 (2026-05-28): D-10 fijaba el pool en DeepSeek Flash + Sonnet 4.6. El autor del proyecto instruyó en runtime (2026-05-28) correr el quórum con **DeepSeek Pro + Opus 4.7**. Es un override DELIBERADO y autorizado por el autor: PRESERVA el principio real de D-10 (cross-vendor DeepSeek + Anthropic) y sube ambos tiers (coherente con la memoria documentada "calidad > tokens"). D-12 sigue vigente (≥2 by DISTINTOS con tier completo). La restricción previa de D-10 "NO usar Opus" queda SUPERSEDIDA: Opus es el validador Claude elegido para Phase 12.

Output: `content/exercises/partitivos.json` con todos los ejercicios en `status: "validated"`, cada uno con ≥2 `by` distintos (el id de DeepSeek Pro + `claude-opus-4-7`) y model-id con tier (D-12), audit trail en git, y ambos comandos del gate en verde (reporter 328+N validated; smoke estricto verde). El prompt 09-VALIDATION-PROMPT.md con la regla de acentos D-135 explícita bajo C4 (D-11).

IMPORTANTE — este plan es NO AUTÓNOMO y se ejecuta INTERACTIVAMENTE por el operador (el autor) en la sesión principal, NO por un subagente autónomo. Razón arquitectónica documentada en la skill `gsd-validate-batch`: usa `AskUserQuestion` (checkpoints por categoría + cola disputed con 4 caminos), y `AskUserQuestion` NO está disponible en subagentes spawneados vía Task() (D-VAL-19/D-VAL-20, anthropics/claude-code#18721). Las tareas de validación de abajo son guiones para que el OPERADOR ejecute la validación y verifique el gate. La Task 1 (pre-flight de acentos) SÍ es automatizable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-partitivos/12-CONTEXT.md
@.claude/skills/gsd-validate-batch/SKILL.md
@.claude/skills/gsd-validate-exercise/SKILL.md
@docs/VALIDACION-QUORUM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight (automatizable) — asegurar la regla de acentos D-135 como REGLA EXPLÍCITA en el prompt de validación (C4), ANTES de validar (D-11)</name>
  <read_first>
    - .planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md — el prompt COMPARTIDO de validación. Foco: la sección "### C4 explanation" (alrededor de la línea 134) y la tabla de mapping R1-R7→C1-C5 (línea ~117, donde C4 mapea hoy SOLO a "R2 + R4"). HOY no hay ninguna regla de acentos bajo C4. Ese es el hueco que cierra D-11.
    - .planning/phases/12-partitivos/12-CONTEXT.md §D-11 y §canonical_refs — D-11 exige el canon de acentos como regla explícita del prompt; canonical_refs dice "El canon de acentos (D-135) debe figurar como regla explícita aquí/en el prompt".
    - scripts/validate-ai-pass.mjs (líneas 37-38 y 108-111) — confirma que ESTE es el prompt que se compone con cada ejercicio (`composed = basePrompt + ejercicio`); el pase DeepSeek lo lee de PROMPT_PATH. El pase Opus recibe el MISMO prompt. Por eso meter la regla aquí la aplica a AMBOS pases.
  </read_first>
  <action>
    Editar `.planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md` para que el criterio C4 (explanation) incluya, de forma EXPLÍCITA, la regla ortográfica de acentos D-135 — convirtiéndola en un criterio binario que AMBAS IAs evalúan, NO un scan posterior ni algo "a discreción de la IA" (D-11). Hacerlo ANTES de lanzar cualquier pase de validación (esta tarea precede al checkpoint de validación de la Task 2).

    Concretamente, bajo la sección "### C4 explanation" (la prosa que hoy describe qué buscar en C4: enfocada al alumno, sin #NNN, sin meta-staging), AÑADIR un párrafo/viñeta nueva con la regla de acentos D-135. Texto de la regla a garantizar (parafraseable pero el SENTIDO debe quedar literal e inequívoco):

    "Ortografía de acentos (D-135): la explanation debe usar ortografía española acentuada correcta según la RAE — vocales acentuadas á/é/í/ó/ú y la ñ donde correspondan (ej. 'también', 'práctica', 'pequeño'). A la vez, los italianismos citados (palabras italianas mencionadas como tales: dello, perché, è, città, un po') DEBEN preservar su ortografía italiana original — no se castellanizan ni se les quitan/añaden tildes a la italiana. Una explanation con español sin acentuar (p.ej. 'tambien', 'practica') o con un italianismo mal escrito viola C4."

    Si quieres reforzar la trazabilidad, actualiza también la fila de C4 en la tabla de mapping (sección 3, ~línea 117) para que refleje que C4 cubre "R2 + R4 + D-135 (acentos)" en vez de solo "R2 + R4". (Opcional pero recomendado; el verify se ancla en la prosa de C4, no en la tabla.)

    REGLAS de la edición:
    - NO toques las secciones R1-R7 verbatim, ni el contrato de output (sección 4), ni los few-shot (sección 5), ni el guard anti-injection (sección 6). El cambio es ADITIVO y acotado a C4 (y opcionalmente la fila C4 de la tabla).
    - Mantén el estilo del documento: prosa plana, sin romper el formato markdown existente de la sección.
    - Es la lección de Phase 11 (memoria feedback_cross_vendor_catches_bugs.md): DeepSeek fue estricto en acentos donde Opus fue indulgente; metiendo el acento en las REGLAS, el criterio queda fijado para AMBOS validadores y no depende del temperamento de cada modelo.
  </action>
  <verify>
    <automated>grep -nE "acentu|á/é/í/ó/ú|D-135|RAE" ".planning/milestones/v1.1-phases/09-infraestructura-de-validaci-n/09-VALIDATION-PROMPT.md" && echo "OK: regla de acentos D-135 presente en el prompt de validación (C4)"</automated>
  </verify>
  <acceptance_criteria>
    - `09-VALIDATION-PROMPT.md` contiene, bajo la descripción del criterio C4, una regla EXPLÍCITA de acentos D-135: español acentuado RAE (á/é/í/ó/ú + ñ) requerido en explanations Y preservación de la ortografía italiana en italianismos citados.
    - `grep -nE "acentu|á/é/í/ó/ú|D-135|RAE"` sobre el prompt encuentra ≥1 match (exit 0).
    - El resto del prompt (R1-R7 verbatim, contrato de output, few-shot, guard anti-injection) queda intacto; el cambio es aditivo y acotado a C4.
    - Esta tarea queda COMPLETA antes de iniciar el checkpoint de validación de la Task 2 (D-11: la regla existe ANTES de validar, no se "advierte" durante).
  </acceptance_criteria>
  <done>El prompt de validación compartido tiene la regla de acentos D-135 explícita bajo C4; ambos pases del quórum (DeepSeek Pro y Opus) la aplicarán como criterio binario. D-11 garantizado como pre-condición de ejecución, no como paso advisory.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Checkpoint (operador) — validar partitivos por quórum cross-vendor DeepSeek Pro + Opus 4.7 (interactivo, no autónomo)</name>
  <what-built>
    Hasta aquí: el temario, todos los ejercicios de partitivos.json con explanations curadas y validation.status "pending", los 3 puntos de integración cableados (categories.json order 9 + reporter TOTAL_EXPECTED 328+N + test) con conteos en lockstep (12-01..12-04), Y el prompt de validación con la regla de acentos D-135 explícita bajo C4 (Task 1 de este plan). Lo que FALTA es la validación por quórum: flipar cada ejercicio de "pending" a "validated" con ≥2 by distintos (pool cross-vendor DeepSeek Pro + Opus 4.7).
  </what-built>
  <how-to-verify>
    Esta es una acción del operador (NO automatizable por subagente, porque el flujo usa AskUserQuestion). Pasos:

    1. Asegúrate de estar en la sesión principal de Claude Code (no en un subagente). Confirma que `.env` tiene `DEEPSEEK_API_KEY` (lo añadió Phase 11; `.env.example` lo documenta).

    2. POOL para Phase 12 (override autorizado de D-10, 2026-05-28): **DeepSeek Pro + Opus 4.7** — cross-vendor (DeepSeek + Anthropic). Estructuralmente calca el pool probado en Phase 11 (DeepSeek + Opus), con Pro en vez de Flash. El invariante es ≥2 by DISTINTOS, y cada by DEBE llevar el tier completo (D-12): el id de DeepSeek Pro (resuelto en ejecución, p.ej. `deepseek-v4-pro`) y `claude-opus-4-7` — NUNCA una clave genérica `deepseek` o `claude`. (La restricción previa "NO usar Opus" de D-10 queda SUPERSEDIDA — Opus es el validador Claude de Phase 12.)
       - Pase DeepSeek Pro: vía `node scripts/validate-ai-pass.mjs <id> --model=<deepseek-pro-id> --write` (el script rutea cualquier id `deepseek-*` a api.deepseek.com; auto-fallback en 429; el `by` registrado es SIEMPRE el modelo que respondió de verdad). Confirma con el API de DeepSeek el id EXACTO que expone su tier Pro (p.ej. `deepseek-v4-pro`); el `by` recogerá lo que de verdad respondió. Ver docs/VALIDACION-QUORUM.md.
       - Pase Opus 4.7: es el pase Claude, `by`=`claude-opus-4-7`. MECANISMO ELEGIDO (para obtener exactamente Pro + Opus, 2 pases, sin un Sonnet de más): correr el pase Opus como un `Task(model: "opus")` directo, pasándole el MISMO prompt compuesto que alimenta validate-ai-pass.mjs (basePrompt 09-VALIDATION-PROMPT.md + el ejercicio JSON), parsear su bloque JSON de verdict y escribirlo en passes[] con `by: "claude-opus-4-7"`. Razón: invocar la skill completa `gsd-validate-exercise` dispara DOS pases Claude (Opus Y Sonnet) — un Task directo evita el Sonnet sobrante. FALLBACK aceptable si el Task directo no es práctico: usar la skill y aceptar un 3er pase Sonnet inofensivo (`claude-sonnet-4-6`) — el invariante ≥2 by DISTINTOS sigue cumpliéndose (Pro + Opus + Sonnet = 3 distintos). Documenta en el SUMMARY qué mecanismo se usó.
       - FALLBACK DE POOL (si el API de DeepSeek NO expone tier Pro): usar **DeepSeek Flash (`deepseek-v4-flash`) + Opus 4.7** — exactamente el pool probado de Phase 11. Sigue siendo cross-vendor y cumple D-12. Documenta en el SUMMARY si se cayó al fallback.

    3. ACENTOS YA GARANTIZADOS COMO REGLA DEL PROMPT (D-11): la Task 1 de este plan ya metió la regla de acentos D-135 (español acentuado á/é/í/ó/ú + ñ RAE; italianismos preservados) como criterio explícito C4 del prompt de validación. NO hace falta "advertir" al operador que la añada — es una pre-condición ya cumplida. Si por lo que sea Task 1 no se ejecutó, PARA y ejecútala antes de validar (D-11 exige la regla ANTES de validar, no durante).

    4. Lanza la validación apuntando a la categoría nueva. NOTA CRÍTICA (feedback de Phase 11): el slug `partitivos` NO está en la lista lockeada D-VAL-22 de la skill gsd-validate-batch (que enumera solo los 7 slugs de v1.1) — igual que pasó con `articoli`. Por eso:
       - Intenta primero: /gsd-validate-batch partitivos
       - Si la skill RECHAZA `partitivos` como scope no reconocido (lo más probable, dado el precedente de articoli), usa el MODO CSV de IDs: pasa la lista de ids partitivos-001..partitivos-0NN separados por coma (la skill los resuelve por archivo). En Phase 11 funcionó el modo CSV; documenta en el SUMMARY cuál de los dos modos funcionó.
       - Alternativa equivalente: correr el pase DeepSeek Pro con scripts/validate-ai-pass.mjs por id y el pase Opus por Task(model:"opus") por id, ejercicio a ejercicio (1-por-1, NUNCA batched — D-VAL-20).

    5. El flujo itera 1-por-1 (un pase fresh-context por ejercicio, NUNCA batched — D-VAL-20): por cada ejercicio corre el pase DeepSeek Pro y el pase Opus, parsea verdicts, escribe passes[] con los by CON tier (el id de DeepSeek Pro + `claude-opus-4-7`), deriva status y commitea. Verás un checkpoint AskUserQuestion al cierre y, si hay disputed, la cola con 4 caminos (accept fix / reject+override / rewrite / skip).

    6. Resuelve cada disputed con el criterio de la memoria feedback_disputed_resolution.md: calidad > tokens, NO override-atajo; ambas IAs deben acabar dando "correcta" (salvo override consciente camino b). Incluye traducción española en el prompt si hay doble-validez que desambiguar (esto es especialmente relevante en Partitivos: las alternativas qualche/un po' di/alcuni son casi sinónimos — la restricción gramatical de D-03 debe garantizar UNA correcta; si una IA marca doble-validez, revisar que el ejercicio fuerza la restricción).

    7. Objetivo: 0 disputed sin resolver y 0 deferred (la sección ## Deferred-disputed de STATE.md vacía o ausente). PART-07 / VAL-06 impiden cerrar con deferred.
  </how-to-verify>
  <resume-signal>Escribe "validación completa" cuando todos los ejercicios de partitivos.json estén en status "validated" (≥2 by distintos con tier — el id de DeepSeek Pro + claude-opus-4-7, o el fallback Flash+Opus — 0 disputed, 0 deferred), o describe qué disputed quedan y por qué.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verificar quórum por ejercicio (≥2 by distintos CON tier) y cerrar el gate verde</name>
  <read_first>
    - content/exercises/partitivos.json — comprobar que todos los ejercicios tienen validation.status "validated" y passes[] con ≥2 by DISTINTOS, cada by con tier completo (el id de DeepSeek Pro / claude-opus-4-7; o el fallback deepseek-v4-flash / claude-opus-4-7)
    - scripts/run-validation-271.mjs (líneas 80-110 y 290-310) — el helper effectiveStatus y los sub-gates VAL-04/VAL-06/VAL-08 que el reporter verifica, y el chequeo totalValidated/totalActual === TOTAL_EXPECTED (328+N)
    - src/data/validation-state.js — deriveStatus: "validated" exige ≥2 correcta con by DISTINTOS y cero incorrecta (sticky disputed)
  </read_first>
  <action>
    Esta tarea verifica el resultado de la validación interactiva (la acción del checkpoint anterior) y cierra el gate. Es la parte automatizable del plan (junto con Task 1).

    1. Verificar quórum por ejercicio: cada ejercicio de partitivos.json debe tener validation.status === "validated" y passes[] con al menos 2 entries de `by` DISTINTOS, todas verdict "correcta" (o un override consciente by:"autor" "correcta" si hubo camino b — el reporter lo acepta vía effectiveStatus). NINGÚN ejercicio en "pending" ni "disputed". VERIFICAR D-12: los `by` distintos llevan el tier completo (esperados: el id de DeepSeek Pro resuelto en ejecución —p.ej. `deepseek-v4-pro`— y `claude-opus-4-7`; o si se cayó al fallback de pool, `deepseek-v4-flash` y `claude-opus-4-7`); NINGUNA entry con clave genérica de vendor (`deepseek`/`claude` a secas). El pool esperado es cross-vendor (DeepSeek + Anthropic), no dos tiers del mismo vendor.

    2. Ejecutar el reporter del gate: node scripts/run-validation-271.mjs — debe salir exit 0, con los sub-gates VAL-06 (todos validated), VAL-08 (cero disputed) y VAL-04 (≥2 by distintos) en PASS, e incluir el conteo de partitivos (TOTAL_EXPECTED = 328+N, totalValidated == totalActual == 328+N).

    3. Ejecutar el smoke test estricto: VAL_07_STRICT=1 node --test tests/*.test.js — debe salir exit 0 (todos los tests verdes, incluyendo los 3 invariantes editoriales de partitivos del plan 12-04 y cualquier check VAL_07 estricto).

    Si el reporter o el smoke fallan: NO es trabajo de re-autoría; lo normal es (a) algún ejercicio quedó en pending/disputed (volver al checkpoint anterior con los ids concretos), (b) un by sin tier o un solo by distinto (volver a validar ese ejercicio asegurando los 2 vendors), o (c) un conteo se desincronizó (revisar lockstep de 12-04: TOTAL_EXPECTED debe ser 328+N y las 3 cuentas idénticas). Diagnosticar con el output del reporter (lista por categoría) y remediar en el plan correspondiente, no parcheando aquí.

    NO modificar el contenido pedagógico de los ejercicios en esta tarea (prompts/options/explanations ya aprobados en 12-02/12-03). Esta tarea SOLO verifica el estado de validación y el gate.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('content/exercises/partitivos.json','utf8')); const bad=d.exercises.filter(e=>!e.validation||e.validation.status!=='validated'); if(bad.length){console.error('No validados: '+bad.map(e=>e.id+':'+(e.validation&&e.validation.status)).join(', '));process.exit(1)} const lowQuorum=d.exercises.filter(e=>{const by=new Set((e.validation.passes||[]).filter(p=>p&&p.verdict==='correcta').map(p=>p.by)); return by.size<2}); if(lowQuorum.length){console.error('Quorum <2 by distintos: '+lowQuorum.map(e=>e.id).join(', '));process.exit(1)} const genericBy=d.exercises.filter(e=>(e.validation.passes||[]).some(p=>p&&['deepseek','claude','gemini','openai'].includes(p.by))); if(genericBy.length){console.error('by generico sin tier (viola D-12): '+genericBy.map(e=>e.id).join(', '));process.exit(1)} console.log('OK '+d.exercises.length+' ejercicios validated con quorum >=2 by distintos CON tier (esperados: id DeepSeek Pro + claude-opus-4-7; fallback deepseek-v4-flash + claude-opus-4-7)')" && node scripts/run-validation-271.mjs && VAL_07_STRICT=1 node --test tests/*.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TODOS los ejercicios de partitivos.json tienen validation.status === "validated".
    - Cada ejercicio tiene ≥2 `by` DISTINTOS con verdict "correcta" en passes[] (quórum VAL-04), o un override consciente by:"autor" que el reporter acepta.
    - Los `by` distintos llevan tier completo (esperados: el id de DeepSeek Pro —p.ej. `deepseek-v4-pro`— + `claude-opus-4-7`; o el fallback `deepseek-v4-flash` + `claude-opus-4-7`); NINGUNA entry usa clave genérica de vendor (`deepseek`/`claude`/`gemini`) — D-12 verificado.
    - `node scripts/run-validation-271.mjs` sale exit 0 con VAL-06/VAL-08/VAL-04 en PASS y el conteo de partitivos incluido (TOTAL_EXPECTED = 328+N).
    - `VAL_07_STRICT=1 node --test tests/*.test.js` sale exit 0 (smoke estricto verde).
    - La sección ## Deferred-disputed de .planning/STATE.md está vacía o ausente (0 deferred).
  </acceptance_criteria>
  <done>Todos los ejercicios de partitivos validados por quórum cross-vendor (DeepSeek Pro + Opus 4.7, o fallback Flash + Opus; by con tier); reporter exit 0 (328+N); smoke estricto verde. Phase 12 lista para verifier.</done>
</task>

</tasks>

<verification>
- PART-07 cubierto: 100% de partitivos.json en status "validated" con ≥2 by distintos CON tier (DeepSeek Pro + claude-opus-4-7; fallback deepseek-v4-flash + claude-opus-4-7; override autorizado de D-10 / D-12).
- El canon de acentos (D-135) figura como regla explícita del prompt de validación (D-11), garantizado por la Task 1 ANTES de validar, no como scan posterior.
- Gate verde: `node scripts/run-validation-271.mjs` exit 0 (incluye partitivos, 328+N) + `VAL_07_STRICT=1 node --test tests/*.test.js` exit 0.
- 0 disputed, 0 deferred.
</verification>

<success_criteria>
- PART-07 satisfecho; los 5 criterios de éxito del ROADMAP Phase 12 quedan cubiertos en conjunto con los planes 12-01..12-04.
- El gate del milestone v1.2 (para Phase 12) en verde; lista para `/gsd:verify-phase 12` o el cierre que corresponda, y tras él `/gsd:complete-milestone v1.2`.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation.passes ↔ gate del reporter | El status validated controla si el gate pasa; lo escribe el flujo de quórum (pase DeepSeek Pro vía validate-ai-pass.mjs + pase Opus vía Task(model:"opus") aislado por ejercicio). |
| validate-ai-pass.mjs ↔ proveedor externo (DeepSeek) | El script llama a api.deepseek.com con la API key de .env; el by registrado es el modelo que respondió de verdad (auto-fallback en 429). |
| 09-VALIDATION-PROMPT.md ↔ ambos pases del quórum | El prompt compartido define los criterios C1-C5; con D-135 explícita en C4 (Task 1), el criterio de acentos se aplica a AMBAS IAs por igual y no a discreción de cada modelo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing / Repudiation | passes[].by (identidad del validador) | mitigate | El by registra el model-id COMPLETO con tier (el id de DeepSeek Pro —p.ej. deepseek-v4-pro— / claude-opus-4-7; fallback deepseek-v4-flash / claude-opus-4-7), nunca una clave genérica de vendor (D-12) — así el invariante ≥2 by distintos no se burla con dos tiers del mismo modelo bajo una clave. deriveStatus exige ≥2 by DISTINTOS, evita auto-aprobación con un solo validador. Override del autor queda registrado con by:"autor" + razón + timestamp ISO. El <verify> de Task 3 rechaza by genéricos. |
| T-12-05b | Tampering | validation.status escrito a mano (bypass del quórum) | accept | El reporter (effectiveStatus + sub-gates) es la verificación fail-closed: un status "validated" sin ≥2 by distintos y sin override del autor sería detectado por VAL-04. Autor único es el oracle final por diseño. |
| T-12-05c | Information Disclosure | API key de DeepSeek en .env | mitigate | .env está git-ignored (precedente Phase 11; .env.example documenta las variables sin valores). validate-ai-pass.mjs lee la clave solo de .env/process.env, no la imprime ni la commitea. Solo se envía el ejercicio + prompt de validación al proveedor (contenido educativo público, sin PII). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (zero-deps, CLAUDE.md). validate-ai-pass.mjs usa solo https/fs nativos; el pase Opus usa Task nativo. No aplica audit de legitimidad de paquetes. |

Sin amenazas HIGH: la validación refuerza la integridad editorial; el gate es fail-closed; el quórum cross-vendor (vendors distintos: DeepSeek + Anthropic) evita aprobación unilateral y puntos ciegos compartidos; la regla de acentos en el prompt iguala el criterio entre ambas IAs; la API key vive solo en .env git-ignored.
</threat_model>

<output>
Create `.planning/phases/12-partitivos/12-05-SUMMARY.md` when done (INCLUIR: que la regla de acentos D-135 se metió en el prompt C4 antes de validar (D-11); qué modo de scope funcionó (slug 'partitivos' o lista CSV de ids — feedback para futuras categorías); que el pool fue cross-vendor DeepSeek Pro + Opus 4.7 con tier —o el fallback Flash+Opus si se usó— anotando que es un override autorizado de D-10 (2026-05-28); qué mecanismo se usó para el pase Opus (Task directo model:opus, o skill con Sonnet extra); número de disputed resueltos y bugs de autoría capturados; resultado del gate reporter 328+N + smoke estricto)
</output>
