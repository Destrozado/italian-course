---
phase: 04-backup-robusto-contenido-completo
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - content/categories.json
  - content/exercises/preposiciones.json
  - content/exercises/verbos-movimiento.json
  - content/exercises/sustantivos-irregulares.json
  - content/exercises/genero-numero.json
  - content/exercises/profesiones.json
  - scripts/validate-content-fixture.mjs
autonomous: false
requirements: [SEED-01]
tags: [content, italian, pdf-transcription, multiple-choice, word-buttons]

must_haves:
  truths:
    - "content/categories.json lista 6 categorías (avere + 5 nuevas) en el orden Avere → Preposiciones → Verbos de movimiento → Sustantivos irregulares → Género y número → Profesiones"
    - "Los 3 archivos de exercises pendientes (sustantivos-irregulares.json, genero-numero.json, profesiones.json) se crean OBLIGATORIAMENTE como placeholders `{\"exercises\":[]}` en este plan — content-loader.js lanza al primer 404 (confirmado en líneas 81-83), así que SIN estos placeholders el boot fallaría con banner CONT-05 (B-1 checker fix)"
    - "content/exercises/preposiciones.json contiene ≥10 ejercicios validados por el schema validator existente, todos category-ID 'preposiciones' (sin multi-cat en este plan)"
    - "content/exercises/verbos-movimiento.json contiene ≥10 ejercicios validados (mayoría multiple-choice + al menos 1-2 word-buttons si el PDF lo sugiere)"
    - "Existe scripts/validate-content-fixture.mjs como helper único reusable que invoca validateContent({categories, exercisesByFile}) con la firma REAL (B-4 checker fix: planes anteriores invocaban validateContent({slug:c},cats) que falla silenciosamente con TypeError)"
    - "Al arrancar la app con `npx serve` tras este plan, la home muestra las 6 categorías (Preposiciones + Verbos de movimiento con ejercicios reales; las 3 restantes con count=0 hasta plan 04-03) sin que el schema validator dispare banner de error"
    - "El autor puede lanzar un Repaso de la categoría Preposiciones (sola o combinada) y completar la sesión usando exclusivamente los ejercicios transcritos del PDF"
    - "El autor puede lanzar un Repaso de Verbos de movimiento y completar la sesión"
    - "El autor ha revisado pedagógicamente cada categoría ANTES de su commit (checkpoint humano per D-85)"
  artifacts:
    - path: "content/categories.json"
      provides: "Registro maestro de 6 categorías (Avere existente + 5 nuevas) en orden establecido en RESEARCH §B"
      contains: "preposiciones"
    - path: "scripts/validate-content-fixture.mjs"
      provides: "Helper node-pure que llama validateContent con la firma real {categories, exercisesByFile}. Args: <slug> <path>. Exit 0 si válido, exit 1 + error en español si inválido. Reutilizable desde planes 04-02/04-03/04-04 (B-4 fix)."
      contains: "validateContent"
    - path: "content/exercises/preposiciones.json"
      provides: "≥10 multi-choice ejercicios de preposiciones simples (a/in/da/per) y articolate (basados en preposizioni_italiano.pdf). IDs preposiciones-001..NNN. Cero multi-cat en este plan (los cruces llegan en 04-04 avere-300+)."
      contains: "preposiciones-001"
    - path: "content/exercises/verbos-movimiento.json"
      provides: "≥10 ejercicios mezcla multi-choice (conjugación essere/avere + huecos andare/venire) + 1-2 word-buttons si una frase del PDF lo invita. IDs verbos-movimiento-001..NNN. Cero multi-cat en este plan."
      contains: "verbos-movimiento-001"
    - path: "content/exercises/sustantivos-irregulares.json"
      provides: "Placeholder vacío `{\"exercises\":[]}` para evitar 404 en content-loader (boot crash). Contenido real llega en plan 04-03 Task 1."
      contains: "exercises"
    - path: "content/exercises/genero-numero.json"
      provides: "Placeholder vacío `{\"exercises\":[]}`. Contenido real en plan 04-03 Task 2."
      contains: "exercises"
    - path: "content/exercises/profesiones.json"
      provides: "Placeholder vacío `{\"exercises\":[]}`. Contenido real en plan 04-03 Task 3."
      contains: "exercises"
  decisions_covered:
    - "D-85: Claude lee `material-profesora/preposizioni_italiano.pdf` y `Clase_Italiano_Verbos_Movimiento.pdf` con la Read tool y propone JSON candidato; el autor revisa pedagógicamente vía checkpoint:human-verify por categoría antes del commit (Tasks 2 y 3)"
    - "D-86: tipo natural por PDF aplicado — preposiciones.json y verbos-movimiento.json son mayoría `multiple-choice` (huecos de preposición / conjugación de verbi di movimento) con algún `word-buttons` ocasional; los tipos `match` se reservan para Plan 04-03 (Género/Sustantivos)"
    - "D-87: cruces semánticos identificados durante transcripción (e.g., `Vado a Roma` = Verbos Movimiento + Preposiciones); SEED-02 cumplido parcialmente en este plan, completado en 04-03/04-04"
  key_links:
    - from: "content/categories.json"
      to: "content/exercises/{preposiciones,verbos-movimiento,sustantivos-irregulares,genero-numero,profesiones}.json"
      via: "category id slug ASCII coincide con nombre de archivo per content-loader.js iteration; los 3 placeholders están presentes para que el boot no falle (B-1)"
      pattern: "\"id\": \"preposiciones\"|\"id\": \"verbos-movimiento\""
    - from: "ejercicio.categoryIds[]"
      to: "categories.json category.id"
      via: "schema validator existente (CONT-04)"
      pattern: "\"categoryIds\": \\[\""
    - from: "ejercicio.payload"
      to: "schema-validator.js PAYLOAD_VALIDATORS dispatch table"
      via: "type === 'multiple-choice' | 'word-buttons'"
      pattern: "\"type\": \"(multiple-choice|word-buttons)\""
    - from: "scripts/validate-content-fixture.mjs"
      to: "src/data/schema-validator.js validateContent({categories, exercisesByFile})"
      via: "ESM import + readFileSync, sin DOM ni fetch"
      pattern: "validateContent\\(\\{"
---

<objective>
Primer slice de contenido real: registrar las 6 categorías en `categories.json` (Avere + 5 nuevas), crear los 3 placeholders OBLIGATORIOS (sustantivos-irregulares.json, genero-numero.json, profesiones.json) para evitar boot crash, y transcribir los 2 PDFs más simples a JSON validado por el schema existente. **Cada categoría con contenido real es un commit independiente** tras revisión pedagógica del autor (checkpoint humano per D-85). Tras este plan, el autor puede practicar Preposiciones y Verbos de movimiento en sesiones reales además de Avere.

**Purpose:** Validar el flujo "Claude lee PDF → propone JSON → autor revisa → commit" con las 2 categorías más fáciles ANTES de atacar las complejas (Sustantivos Irregulares, Género/Número, Profesiones en 04-03). Bajo riesgo, máxima validación del pipeline. **Crear también el helper `scripts/validate-content-fixture.mjs` que centraliza la invocación del schema validator con la firma REAL (B-4 fix), reusable desde planes 04-02/04-03/04-04.**

**Output:** 1 archivo `categories.json` con 6 entradas + 2 archivos nuevos `content/exercises/{preposiciones,verbos-movimiento}.json` cada uno con ≥10 ejercicios validados + 3 archivos placeholder + 1 helper script. Cero cambios en código de runtime (D-85 + RESEARCH confirman que la dispatch table del schema validator de Phase 3 cierra los 3 tipos sin extension). Cubre parcialmente SEED-01 (2 de 5 PDFs nuevos transcritos; los 3 restantes en 04-03; avere multi-cat en 04-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md
@.planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md
@.planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md

@content/categories.json
@content/exercises/avere.json
@src/data/schema-validator.js
@src/data/content-loader.js
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF source → Claude transcription | Material profesora PDFs son fuente de verdad pedagógica. Claude transcribe; el riesgo es "Claude inventa italiano malo". Mitigado por revisión humana per D-85 antes de cada commit. |
| Transcribed JSON → schema validator | JSON entra a `validateContent({categories, exercisesByFile})` con dispatch table cerrada Phase 3. Si malformado, banner visible CONT-05. La firma REAL se invoca a través del helper único `scripts/validate-content-fixture.mjs` (B-4 mitigation). |
| Transcribed JSON → user-facing UI | Strings italianos se renderean directamente; NFC normalize on load (CONT-06) cubre acentos copiados con composiciones distintas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.02-01 | Tampering | Italian language incorrectness in transcribed JSON (Claude inventa / malinterpreta) | medium | mitigate | Author review checkpoint per category (D-85). Cada categoría es un commit independiente. Schema validator (Phase 3 dispatch table) bloquea malformación estructural via helper `scripts/validate-content-fixture.mjs`. NFC normalize (CONT-06) cubre acentos. |
| T-04.02-02 | Tampering | Apostrophe inconsistency (`'` U+0027 vs `'` U+2019) | low | mitigate | RESEARCH §D landmine documentado. Acción: usar SIEMPRE `'` U+0027. Verificación: `grep -P "[\\x{2019}]" content/exercises/*.json` debe retornar exit 1 (sin matches) antes del commit. |
| T-04.02-03 | Information Disclosure | n/a — contenido pedagógico público | n/a | accept | Material profesora es de libre uso del autor para uso personal. |
| T-04.02-04 | Denial of Service | JSON malformado bloquea boot de la app (CONT-05 banner) | low | mitigate | Schema validator existente bloquea malformación y muestra banner. Iteración: si validation falla, el autor edita el JSON. Pipeline `npx serve` → reload → ver categorías es el smoke test natural. **Adicional B-1:** los 3 placeholders previenen el caso "404 → CONT-05" antes incluso de ejecutar el validator. |
| T-04.02-05 | Tampering | Falso positivo de validación por firma incorrecta (B-4 prior issue) | medium | mitigate | El helper `scripts/validate-content-fixture.mjs` invoca `validateContent({categories, exercisesByFile})` con la firma REAL extraída de src/data/schema-validator.js:50. Reemplaza la invocación errónea `validateContent({slug:c},cats)` que en planes anteriores fallaba silenciosamente con TypeError → smoke check fails-open. |
| T-04.02-SC | Tampering | npm/pip/cargo installs | n/a | mitigate | Plan 04-02 instala CERO paquetes externos. Solo contenido JSON + helper node script. Sin Package Legitimacy Audit. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Crear scripts/validate-content-fixture.mjs + actualizar categories.json a 6 entradas + 3 placeholders OBLIGATORIOS</name>
  <files>scripts/validate-content-fixture.mjs, content/categories.json, content/exercises/sustantivos-irregulares.json, content/exercises/genero-numero.json, content/exercises/profesiones.json</files>
  <read_first>
    - content/categories.json (full read — estado actual con solo Avere)
    - src/data/schema-validator.js (LECTURA CRÍTICA líneas 1-110 — firma `validateContent({categories, exercisesByFile})` en línea 50; `categories` espera el ARRAY ya destructurado, NO el objeto raw del JSON; `exercisesByFile` es `Record<string, Array<object>>`. Esta es la firma REAL que el helper debe invocar — NO inventar argumentos.)
    - src/data/content-loader.js (CONFIRMADO líneas 81-83: `if (!res.ok) throw new Error("No se pudo cargar...")` → el primer 404 lanza y bloquea el boot; PROHIBIDO depender de "tolerancia"; los 3 placeholders son OBLIGATORIOS en este task per B-1 checker fix)
    - src/screens/app.js (búsqueda grep `categoriesForDisplay` — confirmar que el field `order` se usa para sortear la tabla home; `main.js` deriva `categoryIds` del array completo de categories.json)
    - main.js (full read — confirmar que categoryRegistry pasado a `loadContent` proviene de las 6 categorías de categories.json)
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (sección `content/categories.json` líneas 622-638 — shape literal recomendado)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-85, D-86, D-87 — locks)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§B Recommended Transcription Order — justifica el orden de carga)
  </read_first>
  <action>
    Cuatro sub-acciones en este task. Las 4 son obligatorias.

    **Sub-acción A — Crear `scripts/validate-content-fixture.mjs` (NEW):**

    Pure node ESM script, sin imports externos (solo `node:fs`, `node:path`, `node:url`, y el módulo del proyecto `src/data/schema-validator.js`). Mensajes en español (FOUND-04). Comportamiento exacto:

    1. Args parsing: `node scripts/validate-content-fixture.mjs <slug> <path>`. Si missing args → exit 1 + mensaje `Uso: node scripts/validate-content-fixture.mjs <slug> <path-al-json>`.
    2. Cargar `content/categories.json` via `readFileSync` + `JSON.parse`. Si falla → exit 1 + `Error al leer content/categories.json: <message>`.
    3. Cargar el archivo de fixture (`<path>`) via `readFileSync` + `JSON.parse`. Si falla → exit 1 + `Error al leer <path>: <message>`.
    4. Extraer `categories: array` de `categoriesRaw.categories` (si no existe → exit 1 + `categories.json: falta campo "categories"`).
    5. Extraer `exercises: array` de `fixtureRaw.exercises` (si no array → tratar como `[]`).
    6. Construir `exercisesByFile = { [path]: exercises }` (clave = path literal del CLI arg, valor = array de ejercicios).
    7. Importar `validateContent` desde `../src/data/schema-validator.js` (path relativo desde `scripts/`). Usar `import` dinámico (`await import(...)`) o estático según la posición del top-level await; recomendado top-level `import { validateContent } from '../src/data/schema-validator.js';` ya que ESM lo permite.
    8. Llamar `const result = validateContent({ categories, exercisesByFile });`.
    9. Si `result.ok === true` → `console.log('OK validación', exercises.length, 'ejercicios en', path);` exit 0.
    10. Si `result.ok === false` → recorrer `result.errors[]`, para cada uno imprimir `[${err.file}] ${err.exerciseId ?? '(global)'}: ${err.reason}`. Exit 1.

    **Convenciones obligatorias:**
    - Shebang `#!/usr/bin/env node` en línea 1 (no necesario para invocación con `node script.mjs` pero útil si se hace ejecutable).
    - Header JSDoc con bloque "// Helper único reutilizable por planes 04-02/04-03/04-04 — invoca validateContent({categories, exercisesByFile}) con la firma REAL de src/data/schema-validator.js:50. Reemplaza invocaciones erróneas `validateContent({slug:c},cats)` que fallaban silenciosamente con TypeError (B-4 checker fix). FOUND-04: mensajes en español."
    - El `<slug>` arg se ACEPTA pero NO se usa para la invocación de `validateContent` (la firma real no toma slug). Sólo se usa en el mensaje OK (`'OK validación', N, 'ejercicios en', path` o variante similar). Esto facilita uso "self-documenting" desde los planes: `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json`.
    - Argumentos: `process.argv[2]` = slug, `process.argv[3]` = path.

    **Sub-acción B — Actualizar `content/categories.json` a 6 entradas:**

    Estructura literal (preservar formato del archivo existente):

    ```json
    {
      "categories": [
        { "id": "avere",                   "name": "Avere (auxiliar)",        "order": 1 },
        { "id": "preposiciones",           "name": "Preposiciones",           "order": 2 },
        { "id": "verbos-movimiento",       "name": "Verbos de movimiento",    "order": 3 },
        { "id": "sustantivos-irregulares", "name": "Sustantivos irregulares", "order": 4 },
        { "id": "genero-numero",           "name": "Género y número",         "order": 5 },
        { "id": "profesiones",             "name": "Profesiones",             "order": 6 }
      ]
    }
    ```

    Decisiones:
    - **Orden:** Avere primero (D-87 + CONTEXT.md `<specifics>`: Avere at order 1). Las 5 nuevas siguen el orden de transcripción de RESEARCH §B.
    - **IDs slug ASCII:** todos cumplen `ID_SLUG_RE = /^[a-z0-9][a-z0-9-]*$/`.
    - **Nombres humanos:** español. Sin emojis.
    - **NO ID rename del existente:** "avere" permanece exactamente como está.

    **Sub-acción C — Crear los 3 placeholders OBLIGATORIAMENTE (B-1 checker fix):**

    El plan anterior tenía esto como condicional ("ejecutar grep…si lanza al primer 404"). El checker confirmó (líneas 81-83 de content-loader.js): el loader LANZA al primer 404 y `main.js` deriva categoryIds del array completo de categories.json. Por tanto, listar 6 categorías SIN crear los 3 archivos de exercises hace que el boot falle con banner CONT-05. **Crear los 3 archivos placeholder en este task SIN ejecutar grep condicional:**

    - `content/exercises/sustantivos-irregulares.json` → contenido EXACTO: `{"exercises":[]}\n`
    - `content/exercises/genero-numero.json` → contenido EXACTO: `{"exercises":[]}\n`
    - `content/exercises/profesiones.json` → contenido EXACTO: `{"exercises":[]}\n`

    Estos archivos son valid JSON, valid contra el schema (array vacío permitido — no hay min count enforced en validateContent), y permiten que el loader complete las 6 fetches sin 404. **Los planes 04-03 Task 1/2/3 sobrescribirán estos archivos con contenido real tras revisión humana.**

    **Sub-acción D — Verificación combinada:**

    Tras crear los 4 archivos del bloque + el helper script, ejecutar verificaciones automáticas (ver `<verify>` block). El helper se auto-prueba contra los placeholders: invocar `node scripts/validate-content-fixture.mjs sustantivos-irregulares content/exercises/sustantivos-irregulares.json` debe retornar exit 0 con mensaje OK.
  </action>
  <verify>
    <automated>node -e "const c = JSON.parse(require('fs').readFileSync('content/categories.json','utf8')); if(c.categories.length !== 6) {console.error('expected 6 categories, got', c.categories.length); process.exit(1)}; const ids = c.categories.map(x=>x.id); ['avere','preposiciones','verbos-movimiento','sustantivos-irregulares','genero-numero','profesiones'].forEach(id => {if(!ids.includes(id)) {console.error('missing id', id); process.exit(1)}}); const slug = /^[a-z0-9][a-z0-9-]*$/; c.categories.forEach(x => {if(!slug.test(x.id)) {console.error('bad slug', x.id); process.exit(1)}}); const orders = c.categories.map(x=>x.order).sort((a,b)=>a-b); if(JSON.stringify(orders) !== JSON.stringify([1,2,3,4,5,6])) {console.error('orders gap or duplicate'); process.exit(1)}; console.log('OK 6 categorias con slugs ASCII validos y order 1..6');" && ls content/exercises/sustantivos-irregulares.json content/exercises/genero-numero.json content/exercises/profesiones.json && node scripts/validate-content-fixture.mjs sustantivos-irregulares content/exercises/sustantivos-irregulares.json && node --test tests/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - **Source: `content/categories.json` parsea sin errores** (incluido en el primer `node -e` del verify block).
    - **Source: 6 entradas exactas** en el array `categories` (verify block).
    - **Source: cada `id` cumple `^[a-z0-9][a-z0-9-]*$`** (verify block).
    - **Source: campo `order` 1..6 sin gaps ni duplicados** (verify block).
    - **Source: `grep -c '"id": "avere"' content/categories.json`** = 1 (Avere preservado).
    - **Source: los 3 placeholders existen** — `ls content/exercises/{sustantivos-irregulares,genero-numero,profesiones}.json` retorna 3 paths (B-1 fix verificado explícitamente).
    - **Source: cada placeholder es `{"exercises":[]}` válido** — `node -e "['sustantivos-irregulares','genero-numero','profesiones'].forEach(s=>{const c=JSON.parse(require('fs').readFileSync('content/exercises/'+s+'.json','utf8')); if(!Array.isArray(c.exercises)||c.exercises.length!==0) process.exit(1)})"` exit 0.
    - **Source: file exists `scripts/validate-content-fixture.mjs`** y contiene `validateContent` invocación con `{ categories, exercisesByFile }` literal — `grep -c 'validateContent({' scripts/validate-content-fixture.mjs` ≥ 1 y `grep -c 'exercisesByFile' scripts/validate-content-fixture.mjs` ≥ 1.
    - **Behavior: el helper valida los placeholders sin error** — `node scripts/validate-content-fixture.mjs sustantivos-irregulares content/exercises/sustantivos-irregulares.json` exit 0 (incluido en verify block).
    - **Behavior: `node --test tests/*.test.js` exits 0** (los 21 tests nuevos Plan 04-01 + 105 baseline + tests existentes siguen verdes — categories.json + placeholders + helper script no rompen ningún test puro).
  </acceptance_criteria>
  <done>
    `content/categories.json` lista 6 categorías ASCII-slug en orden Avere → Preposiciones → Verbos de movimiento → Sustantivos irregulares → Género y número → Profesiones. Los 3 placeholders OBLIGATORIOS creados (`{"exercises":[]}`). Helper `scripts/validate-content-fixture.mjs` operativo con la firma REAL `validateContent({categories, exercisesByFile})`. Tests verdes. App montable con `npx serve` sin banner CONT-05 (verificable manualmente; UAT en 04-04).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Transcribir Preposiciones a JSON + checkpoint humano de revisión pedagógica</name>
  <files>content/exercises/preposiciones.json</files>
  <read_first>
    - material-profesora/preposizioni_italiano.pdf (Read tool — Claude Code multimodal; PDF corto ~2.7 KB, 1 página; contiene 8 preposiciones simples + tabla articolate 6×6)
    - content/exercises/avere.json (full read — shape literal de los 3 tipos: multi-choice líneas 4-12, word-buttons líneas 137-145, match líneas 158-170; PATTERNS líneas 643-706 detalla los 3 shapes)
    - src/data/schema-validator.js (lines 1-110 — entender qué valida exactamente PAYLOAD_VALIDATORS por tipo; multi-choice payload requiere {prompt:string, options:string[]∈3..4, correctIndex:number 0..3})
    - src/data/content-loader.js (full read — entender NFC normalize on load CONT-06)
    - scripts/validate-content-fixture.mjs (CREADO EN TASK 1 — el helper que valida fixtures con la firma real)
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (sección `content/exercises/{5 new}.json` líneas 643-706 — shapes literales)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§D Italian-language Landmines + §E Distractoras Strategy + §B Recommended Order — preposiciones es primer PDF)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-85, D-86 Preposiciones → mayoría multi-choice "Vado ___ Roma")
  </read_first>
  <what-built>
    Claude:
    1. Lee `material-profesora/preposizioni_italiano.pdf` con la Read tool.
    2. Extrae las 8 preposiciones simples (`di, a, da, in, su, con, per, tra/fra`) + tabla articolate (`del/dello/della/dei/degli/delle`, `al/allo/alla/...`, etc.) según el PDF.
    3. Construye ≥10 ejercicios `multiple-choice` (más realista, apuntar 12-15 según RESEARCH) con shape literal del schema. Cero multi-cat en este task (los cruces llegan en 04-04).
    4. Cada ejercicio: `id` `preposiciones-001..NNN` (range 001-099 multi-choice per convención avere.json), `type: "multiple-choice"`, `categoryIds: ["preposiciones"]`, `payload: {prompt: string, options: string[3..4], correctIndex: number}`, opcional `notes` con referencia al PDF.
    5. **Distractoras pedagógicamente útiles** (RESEARCH §E): otras preposiciones del mismo grupo (e.g., para "Vado ___ Roma" correct `a`, distractoras `in`/`da`/`per` — todas son preposiciones plausibles). Evitar distractoras absurdas.
    6. **Posición correcta variada** (no siempre la 2ª opción).
    7. **Apostrofes consistentes** `'` U+0027 (no `'` U+2019) per RESEARCH §D.
    8. **Capitalización**: prompts con mayúscula inicial; `options[]` en minúscula (excepto nombres propios "Roma", "Italia").
    9. Sobrescribe `content/exercises/preposiciones.json` (que era placeholder o no existía) con shape literal `{"exercises": [ ... ]}`.
    10. Ejecuta verificaciones automáticas (ver `<acceptance_criteria>`).

    Después Claude **PAUSA** para checkpoint humano:
    El autor:
    1. Abre `content/exercises/preposiciones.json` en su editor.
    2. Lee los ejercicios uno por uno comparando contra `material-profesora/preposizioni_italiano.pdf`.
    3. Valida pedagógicamente: ¿el italiano es correcto? ¿las distractoras tienen sentido? ¿cubre las preposiciones del PDF de forma representativa?
    4. **Si encuentra errores:** edita el JSON directamente o pide a Claude ediciones específicas.
    5. **Si aprueba:** confirma para que Claude haga el commit `content: add preposiciones exercises (Phase 4 SEED-01)`.
    6. Opcionalmente: `npx serve` + reload + ver "Preposiciones" en la home + lanzar un Repaso de Preposiciones (categoría sola) y completar la sesión.
  </what-built>
  <how-to-verify>
    1. **Pre-checkpoint (Claude antes de pausar):**
       - `node -e "const c = JSON.parse(require('fs').readFileSync('content/exercises/preposiciones.json','utf8')); if (c.exercises.length < 10) {console.error('expected >=10 exercises, got', c.exercises.length); process.exit(1)}; console.log('OK', c.exercises.length, 'ejercicios')"` exit 0.
       - `grep -P "[\\x{2019}]" content/exercises/preposiciones.json` retorna exit 1 (no apóstrofes tipográficos `'`).
       - `node --test tests/*.test.js` exits 0 (no regresión).
       - **Schema validation via helper único (B-4 fix):** `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exit 0. Este helper invoca `validateContent({categories, exercisesByFile})` con la firma REAL — NO usar el inline `node -e` con `validateContent({slug:c},cats)` que falla silenciosamente.
    2. **Author review (pausa para humano):**
       - Abre el archivo en el editor.
       - Verifica los 10+ ejercicios uno por uno contra `material-profesora/preposizioni_italiano.pdf`.
       - **Checklist pedagógico:**
          - [ ] Cada preposición simple del PDF (`di/a/da/in/su/con/per/tra/fra`) aparece en al menos 1 ejercicio como respuesta correcta.
          - [ ] Las preposizioni articolate (`del/al/dal/nel/sul/...`) están cubiertas si el PDF las desarrolla — al menos 3-4 ejercicios articolate.
          - [ ] Los prompts son frases italianas plausibles A1 (no inventadas raras).
          - [ ] Las distractoras son preposiciones plausibles del mismo grupo (no "xyz" ni "blah").
          - [ ] No hay typos italianos visibles.
       - **Opcional pero recomendado:** `npx serve` → `http://localhost:3000` → Repaso → marcar solo "Preposiciones" → lanzar → completar la sesión → confirmar que cada ejercicio se renderea y se grading correctamente.
  </how-to-verify>
  <acceptance_criteria>
    - **Source: file exists `content/exercises/preposiciones.json`** y `node -e "JSON.parse(...)"` exit 0.
    - **Source: ≥10 ejercicios** en el array `exercises`.
    - **Source: cada ejercicio tiene `id` matching `^preposiciones-\\d{3}$`** (range 001-099 para multi-choice).
    - **Source: cada ejercicio `type === "multiple-choice"`** (D-86 — preposiciones mayoría multi-choice).
    - **Source: cada ejercicio `categoryIds === ["preposiciones"]`** (sin multi-cat en este plan — los cruces llegan en 04-04).
    - **Source: `grep -P "[\\x{2019}]" content/exercises/preposiciones.json`** exit 1 (sin apóstrofes tipográficos).
    - **Source: distintas preposiciones cubiertas — `grep -oE '\"correctIndex\": [0-9]' content/exercises/preposiciones.json | sort -u | wc -l`** ≥ 3 (al menos 3 valores distintos de correctIndex → posición correcta variada per RESEARCH §E).
    - **Behavior: schema validation via helper único passes** — `node scripts/validate-content-fixture.mjs preposiciones content/exercises/preposiciones.json` exit 0 (B-4 fix).
    - **Behavior: `node --test tests/*.test.js` exits 0** sin regresiones.
    - **Human checkpoint signal:** el autor confirma "approved" o describe ediciones necesarias.
  </acceptance_criteria>
  <resume-signal>
    Type `approved` para confirmar revisión + commit.
    Type `edit: <descripción>` para pedir cambios a Claude.
    Type `manual` si has editado el JSON tú mismo y quieres que Claude commitee el estado actual.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Transcribir Verbos de movimiento a JSON + checkpoint humano de revisión pedagógica</name>
  <files>content/exercises/verbos-movimiento.json</files>
  <read_first>
    - material-profesora/Clase_Italiano_Verbos_Movimiento.pdf (Read tool — PDF mediano ~24 KB, 3 páginas; contiene conjugación essere/avere + lista de verbos de movimiento andare/venire/uscire/entrare/etc.)
    - content/exercises/avere.json (multi-choice + word-buttons shape — usar como modelo)
    - content/exercises/preposiciones.json (CREADO EN TASK 2 — leer para reutilizar patrón de IDs y notes)
    - src/data/schema-validator.js (lines 1-110 — word-buttons payload requiere {prompt:string, answer:string[≥1], distractors?:string[]})
    - scripts/validate-content-fixture.mjs (CREADO EN TASK 1 — helper para validación)
    - .planning/phases/04-backup-robusto-contenido-completo/04-PATTERNS.md (sección `content/exercises/{5 new}.json` líneas 643-706)
    - .planning/phases/04-backup-robusto-contenido-completo/04-RESEARCH.md (§D Italian-language Landmines + §F Word-buttons strategy + §B verbos-mov es PDF 2 — material claro conjugación essere/avere ya en formato multi-choice + frases para word-buttons)
    - .planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md (D-86 — verbos-movimiento mayoría multi-choice + algún word-buttons "si surge una frase clara")
  </read_first>
  <what-built>
    Claude:
    1. Lee `material-profesora/Clase_Italiano_Verbos_Movimiento.pdf`.
    2. Extrae:
       - Conjugación essere/avere (passato prossimo): `Lui è andato`, `Lei è andata`, `Loro sono andati`.
       - Verbos de movimiento clave: `andare`, `venire`, `arrivare`, `partire`, `entrare`, `uscire`, `salire`, `scendere`, etc.
       - Ejercicios del PDF si los hay (los blanks "______" sugieren multi-choice o word-buttons).
    3. Construye ≥10 ejercicios (apuntar 12-15) distribuidos:
       - **Mayoría multi-choice** (ids `verbos-movimiento-001..099`): conjugaciones essere/avere, huecos andare/venire según contexto.
       - **1-2 word-buttons** (ids `verbos-movimiento-100..199`) si hay frases claras 3-6 tokens. Ejemplo válido del PDF: `"Lei è andata al cinema"` (5 tokens). RESEARCH §F caps span 3-6 tokens.
    4. Cada ejercicio: shape literal del schema Phase 3:
       - multi-choice: `{prompt, options[3-4], correctIndex}`.
       - word-buttons: `{prompt: string en español, answer: string[≥1] tokens italianos, distractors?: string[1-2]}`. **Sin punto final en answer tokens** (Pitfall RESEARCH §F).
    5. Cero multi-cat en este task (los cruces Verbos+Preposiciones llegan en 04-04 avere-3XX o un cruce dedicado).
    6. Convenciones de RESEARCH §D, §E, §F.
    7. Sobrescribe `content/exercises/verbos-movimiento.json` (era placeholder o no existía).
    8. Pre-checkpoint verificaciones automáticas.

    Después Claude **PAUSA** para checkpoint humano:
    El autor revisa:
    - Conjugaciones essere/avere correctas (passato prossimo: con essere para verbos de movimiento, concordancia género/número).
    - `Lui è andato` vs `Lei è andata` (concordancia A1 explícita en el PDF).
    - Para word-buttons: que la frase tenga sentido y los tokens sean los esperados.
    - Distractoras válidas.

    Si aprueba: commit `content: add verbos-movimiento exercises (Phase 4 SEED-01)`.
  </what-built>
  <how-to-verify>
    1. **Pre-checkpoint (Claude):**
       - `node -e "const c = JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); if (c.exercises.length < 10) process.exit(1); console.log('OK', c.exercises.length)"` exit 0.
       - `grep -P "[\\x{2019}]" content/exercises/verbos-movimiento.json` exit 1.
       - **Schema validation via helper (B-4 fix):** `node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json` exit 0.
       - `node --test tests/*.test.js` exits 0.
       - **Type mix check:** `grep -c '"type": "multiple-choice"' content/exercises/verbos-movimiento.json` ≥ 8 y `grep -c '"type": "word-buttons"' content/exercises/verbos-movimiento.json` ≥ 0 (idealmente ≥1-2 según D-86). Si Claude no encuentra frase clara para word-buttons, puede ser 0 — documentar en notes.
       - **Word-buttons sin punto final (W-6 fix, robust node-based check):** ejecutar el siguiente smoke (no grep, parsea JSON real):
         ```
         node -e "const c=JSON.parse(require('fs').readFileSync('content/exercises/verbos-movimiento.json','utf8')); c.exercises.filter(e=>e.type==='word-buttons').forEach(e=>{e.payload.answer.forEach(t=>{if(typeof t==='string' && t.endsWith('.')){console.error('Token con punto final:',e.id,t);process.exit(1)}})});"
         ```
         Exit 0 esperado. Si algún token de answer termina en '.', el script falla con el id del ejercicio y el token.
    2. **Author review:**
       - Abre el JSON, lee cada ejercicio contra el PDF.
       - **Checklist:**
          - [ ] Conjugación essere/avere correcta (especialmente passato prossimo).
          - [ ] Concordancia género en participio pasado (`andato`/`andata`/`andati`/`andate`).
          - [ ] Verbos de movimiento del PDF aparecen (al menos 5 de los 10+ típicos: andare/venire/arrivare/partire/entrare/uscire/salire/scendere/tornare).
          - [ ] Ningún ejercicio mezcla con preposiciones a propósito (eso vendrá en 04-04 como multi-cat).
          - [ ] word-buttons: si hay alguno, los `answer[]` tokens son correctos y SIN PUNTO FINAL.
       - Opcional: `npx serve` → Repaso → solo "Verbos de movimiento" → completar sesión.
  </how-to-verify>
  <acceptance_criteria>
    - **Source: file exists `content/exercises/verbos-movimiento.json`** y JSON.parse exit 0.
    - **Source: ≥10 ejercicios** en el array.
    - **Source: cada `id` matching `^verbos-movimiento-\\d{3}$`** (range 001-099 multi-choice; 100-199 word-buttons).
    - **Source: cada `categoryIds === ["verbos-movimiento"]`** (sin multi-cat aquí).
    - **Source: tipos solo multi-choice o word-buttons** (`grep -c '"type":' content/exercises/verbos-movimiento.json` == total ejercicios; cada uno match `(multiple-choice|word-buttons)`).
    - **Source: mayoría multi-choice (D-86) — count(multi-choice) ≥ count(word-buttons)**.
    - **Source: `grep -P "[\\x{2019}]" content/exercises/verbos-movimiento.json`** exit 1 (sin apóstrofes tipográficos).
    - **Behavior: word-buttons NO incluyen punto final en `answer[]`** — node-based smoke W-6 (ver `<how-to-verify>` paso 1) exit 0.
    - **Behavior: schema validation via helper único passes** — `node scripts/validate-content-fixture.mjs verbos-movimiento content/exercises/verbos-movimiento.json` exit 0.
    - **Behavior: `node --test tests/*.test.js` exits 0**.
    - **Human checkpoint signal:** "approved" tras revisión.
  </acceptance_criteria>
  <resume-signal>
    Type `approved` para confirmar + commit.
    Type `edit: <descripción>` para ajustes.
    Type `manual` si has editado y quieres commitear el estado actual.
  </resume-signal>
</task>

</tasks>

<verification>
- **W-NEW-1 nota de scope honesta:** este plan contiene varios `node -e "..."` inline en `<verify>`/`<acceptance_criteria>`/`<how-to-verify>` (líneas ~197, 206, 253, 269, 329, 336). Son intencionalmente inline porque son checks triviales one-off (count ≥ 10, shape `{exercises:[]}`, no-period en tokens) que bash maneja correctamente — las apóstrofes son simples, sin anidación, y los scripts se ejecutan vía Bash directamente. El patrón "todos los smoke checks usan helper scripts" del B-2 fix (anclado en 04-04) aplica EXCLUSIVAMENTE a los checks complejos relacionados con `avere.json` (snapshot D-88, multi-cat cross). NO refactorizar las inline aquí — sería ruido sin beneficio. Si en el futuro un check requiere apóstrofes anidados o lógica > 2 líneas, promoverlo a `scripts/`.
- Task 1 completable autónomamente (categories.json + 3 placeholders OBLIGATORIOS + helper script — todo en 1 commit `content: register 6 categories + placeholders + validate-content helper`).
- Task 2 y 3 cada uno produce 1 archivo JSON nuevo (sobreescribe el placeholder) + 1 commit tras human-verify.
- Total commits esperados: 3 (categories+placeholders+helper / preposiciones / verbos-movimiento).
- Tests baseline 126 (105 Phase 1-3 + ≥21 Phase 4) siguen verdes.
- App arranca con `npx serve` sin banner de error CONT-05 (verificable por autor antes del último checkpoint; UAT integral en 04-04 Task 2).
</verification>

<success_criteria>
- `content/categories.json` lista 6 categorías ASCII-slug validables en orden ROADMAP.
- Los 3 placeholders `{"exercises":[]}` existen (B-1 fix).
- `scripts/validate-content-fixture.mjs` existe con la firma REAL de validateContent (B-4 fix).
- `content/exercises/preposiciones.json` ≥10 multi-choice exercises validados, autor-aprobados.
- `content/exercises/verbos-movimiento.json` ≥10 ejercicios (mezcla multi-choice + word-buttons), autor-aprobados.
- `npx serve` + reload muestra "Preposiciones" y "Verbos de movimiento" en la home, sin banner CONT-05 de error.
- Autor puede lanzar Repaso de Preposiciones (sola) o Verbos de movimiento (sola) y completar la sesión usando solo los nuevos ejercicios.
- SEED-01 cubierto 2/5 (3 categorías restantes en 04-03; avere extension en 04-04).
- Cero multi-cat en este plan — SEED-02 cubierto en 04-04.
</success_criteria>

<output>
Crear `.planning/phases/04-backup-robusto-contenido-completo/04-02-SUMMARY.md` al finalizar:
- 6 categorías registradas + 3 placeholders creados (B-1 fix anclado) + helper script creado (B-4 fix anclado).
- Counts de ejercicios por categoría nueva.
- Commits per categoría (hashes).
- Iteraciones del autor por categoría (cambios pedidos, si los hubo).
- Open items para Plan 04-03: las 3 categorías pendientes (sustantivos-irregulares, genero-numero, profesiones) sobreescribirán sus placeholders.
- Open items para Plan 04-04: avere multi-cat extension + smoke test multi-cat real + UAT integral.
</output>
</content>
</invoke>