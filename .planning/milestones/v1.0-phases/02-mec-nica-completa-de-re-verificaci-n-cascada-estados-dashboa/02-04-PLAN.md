---
phase: 02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/screens/app.js
  - index.html
  - styles.css
  - tests/domain-progress.test.js
autonomous: false
mvp_mode: true
mvp_slice_kind: ui-vertical-slice-close
requirements:
  - SESSION-07
  - SESSION-08
  - SESSION-09
  - DOMAIN-10
user_setup: []

must_haves:
  truths:
    - "Al completar una sesión (Repaso o Test completo), aparece una pantalla completa de resumen con encabezado `Sesión terminada · X/N correctos`, lista de categorías tocadas (estado antes→después, racha antes→después, ejercicios pendientes para hecha si sigue no-hecha), y un único botón `Volver al home`"
    - "El resumen es factústico (tono neutral): texto plano, color rojo sutil en flecha de regresión (hecha → no-hecha), verde sutil en promoción (no-hecha → hecha), sin emojis decorativos"
    - "Una sesión Repaso abandonada cerrando el tab mid-sesión NO afecta al state (verificable: closing tab mid-sesión + recarga → tabla home idéntica antes y después)"
    - "Una sesión Test completo escribe `state.inFlightTest` tras CADA respuesta (verificable: responder 3 ejercicios + inspeccionar localStorage → ver `inFlightTest.cursor=3`, `inFlightTest.answers.length=3`)"
    - "Al recargar mid-Test completo, la home muestra un banner persistente arriba con `⚠ Tienes un Test completo a medias (X/N ejercicios). [Reanudar] [Descartar]`"
    - "Click `Reanudar` lleva directo a la pantalla session con el cursor y answers restaurados; click `Descartar` muestra confirmación inline `¿Descartar el test? Los aciertos hasta ahora no se guardarán.` y luego limpia `inFlightTest`"
    - "Click `Test completo` en home cuando ya hay in-flight pendiente muestra confirmación `Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?` (D-44)"
    - "Al completar un Test completo, `inFlightTest` se borra (paso 6 del pseudocódigo de applySessionResult ya lo hacía; verificable en summary)"
    - "Suite de smoke tests verde incluye simulación de ≥30 días con la combinación: cascada multi-cat + promoción + regresión + racha guard + sampler edges (DOMAIN-10 completo)"
    - "Resume validation de in-flight stale: si `inFlightTest.exerciseIds` contiene IDs que ya no existen en content (autor borró un ejercicio), `Reanudar` muestra confirm para descartar en lugar de crashear (Pitfall #5)"
  artifacts:
    - path: "src/screens/app.js"
      provides: "Extensión: summaryDelta computado en completeSession + persistInFlightTest + resumeInFlightTest + clearInFlightTest + discardInFlightTestWithConfirm + computeSummaryDelta helper"
      contains: "summaryDelta"
    - path: "index.html"
      provides: "Template summary completo con lista de delta + banner in-flight test en home + integración confirmDialog en D-43/D-44"
      contains: "summaryDelta"
    - path: "tests/domain-progress.test.js"
      provides: "Suite ≥30 días simulados completa cubriendo combinaciones D-53.1+D-53.2+D-53.3 en un solo escenario integrado (DOMAIN-10)"
      contains: "30 días"
  key_links:
    - from: "src/screens/app.js (completeSession)"
      to: "summaryDelta"
      via: "computeSummaryDelta(before, newState, sessionResult, content) helper antes de currentScreen='summary'"
      pattern: "summaryDelta"
    - from: "src/screens/app.js (sessionSelectOption)"
      to: "saveState"
      via: "persistInFlightTest invocado al final del select cuando mode='test-completo'"
      pattern: "persistInFlightTest"
    - from: "index.html (home template)"
      to: "src/screens/app.js (state.inFlightTest)"
      via: "<div x-show=\"state.inFlightTest\"> banner arriba de los botones grandes"
      pattern: "state.inFlightTest"
---

<objective>
**Cerrar el bucle** "te obliga a no olvidar" completo: añadir la pantalla de resumen pantalla-completa con delta factústico por categoría (SESSION-07), implementar persistencia in-flight del Test completo con banner reanudar/descartar (SESSION-09), confirmar el descarte de Repaso abandonado (SESSION-08 — el comportamiento de "cerrar tab descarta" ya funciona en Plan 02-03; aquí se documenta y verifica), y cerrar la suite de smoke tests del dominio con un escenario integrado ≥30 días (DOMAIN-10).

Tras este plan, los 6 success criteria de la Phase 2 (ROADMAP.md) están todos verdes y el motor de re-verificación es observable end-to-end.

Purpose: Plan 02-03 dejó la app navegable con appShell unificado pero **sin resumen** (`completeSession` iba directo a home) y **sin persistence in-flight** (Test completo en Plan 02-03 se descartaba al recargar). Este plan añade ambas piezas — son cosméticamente importantes pero arquitecturalmente sencillas porque el dominio ya está completo.

Output:
- `src/screens/app.js` ampliado: `completeSession` ahora computa `summaryDelta` y navega a `summary`; nuevos métodos `persistInFlightTest`, `resumeInFlightTest`, `clearInFlightTest`, `discardInFlightTestWithConfirm`; helper `computeSummaryDelta` privado; integración del banner in-flight en `init()` (no automático — solo muestra el banner en home).
- `index.html`: template `summary` ahora muestra la lista de delta + botón único; banner in-flight test añadido al template `home` arriba de los botones grandes; el botón `Test completo` ahora usa `requestConfirmIfInFlight()` antes de openPicker (D-44).
- `styles.css`: estilos del summary delta (flechas de color sutil) + banner in-flight.
- `tests/domain-progress.test.js`: añadir 1 test integrado ≥30 días que combina cascada multi-cat + promoción + regresión + racha guard en una sola simulación (DOMAIN-10 final).

**Slice vertical observable:** el autor lanza `npx serve`, completa una sesión Repaso → ve resumen con delta → vuelve a home → arranca Test completo → responde 3 ejercicios → cierra el tab → recarga → ve banner in-flight → click Reanudar → continúa donde lo dejó → completa → ve resumen Test completo con cascada/promoción.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-01-SUMMARY.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-02-SUMMARY.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-03-SUMMARY.md

<!-- Estado tras Plan 02-03 — base de este plan -->
@src/screens/app.js
@index.html
@styles.css
@src/main.js
@src/domain/progress.js
@src/domain/session.js
@src/data/storage.js
@tests/domain-progress.test.js

<interfaces>
<!-- Contratos heredados que este plan usa -->

```js
// src/domain/progress.js (Plan 02-01) — applySessionResult ya borra inFlightTest en paso 6
applySessionResult(state, sessionResult, content, today): newState  // newState.inFlightTest === undefined siempre
```

```js
// src/data/storage.js (Plan 02-01)
saveState(state): void  // persiste el blob completo, JSON.stringify elide inFlightTest=undefined
```

Shape de `inFlightTest` (D-41):
```ts
{
  categoryIds: string[],
  exerciseIds: string[],
  cursor: number,
  answers: Array<{exerciseId: string, correct: boolean}>,
  startedAt: number  // epoch ms — informativo, NO usado para staleness en v1
}
```

Shape de `summaryDelta` (estructura derivada en `computeSummaryDelta` helper privado):
```ts
Array<{
  categoryId: string,
  categoryName: string,
  statusBefore: 'no-hecha' | 'hecha' | 'dominada',
  statusAfter: 'no-hecha' | 'hecha' | 'dominada',
  streakBefore: number,
  streakAfter: number,
  pendingForHecha: number,         // total - len(clearedExerciseIds), solo relevante si statusAfter='no-hecha'
  failed: boolean,                  // true si hubo fallo en esta categoría
  failureReason: string | null      // ej. "falló avere-007 (multi: avere + género)"
  isPromotion: boolean,             // statusBefore !== 'hecha'/'dominada' && statusAfter ∈ {'hecha','dominada'}
  isRegression: boolean             // statusBefore ∈ {'hecha','dominada'} && statusAfter === 'no-hecha'
}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: completeSession con summaryDelta + template summary completo</name>
  <files>src/screens/app.js, index.html, styles.css</files>

  <read_first>
    - src/screens/app.js (estado tras Plan 02-03 — `completeSession` actual va directo a `currentScreen='home'`; este plan lo cambia)
    - index.html (estado tras Plan 02-03 — template summary es un stub minimal; este plan lo expande)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md sección "D-37" (resumen pantalla completa, tono neutro factúal)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Code Examples — Patrón canónico de computar `summaryDelta`" (código canónico íntegro)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Performance de Alpine" subsección "Getters NO están cacheados" (justifica computar summaryDelta una vez, NO usar getter)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md sección "Patrón canónico de computar summaryDelta"
  </read_first>

  <action>
    Modificar `src/screens/app.js`, `index.html` y `styles.css` para añadir el resumen pantalla completa.

    **A) `src/screens/app.js`:**

    1. **Reescribir `completeSession()`** (actualmente en Plan 02-03 va directo a home). Nuevo cuerpo derivado del código canónico de 02-RESEARCH.md "Patrón canónico de computar summaryDelta":
       - `const sessionResult = {answers: this.sessionResults}`.
       - `const today = todayLocal()`.
       - **Snapshot ANTES de aplicar**: `const before = JSON.parse(JSON.stringify(this.state.categoryProgress ?? {}))` (deep clone — el shape es pequeño y necesitamos preservar identidad por categoría para el delta).
       - `const newState = applySessionResult(this.state, sessionResult, this.content, today)`.
       - `saveState(newState)`.
       - `this.state = newState`.
       - **Computar `summaryDelta`** invocando `computeSummaryDelta(before, newState, sessionResult, this.content)` (helper privado al fondo del archivo).
       - `this.summaryDelta = result.delta`.
       - `this.summaryHeaderLabel = result.headerLabel` (formato `\`Sesión terminada · ${correctCount}/${total} correctos\``).
       - `this.currentScreen = 'summary'`. (Plan 02-03 iba a `'home'`; ahora pasa por summary.)

    2. **Helper privado `computeSummaryDelta(before, newState, sessionResult, content)`** al fondo del archivo (función `function`, no método). Devuelve `{delta: Array<...>, headerLabel: string}`:
       - Computar `practicedCatIds` y `failedCatIds` usando `Set` flatMap sobre `sessionResult.answers` (mismo patrón que `applySessionResult`).
       - Para cada `catId` en `practicedCatIds`, construir un objeto `delta` con: `categoryId`, `categoryName` (de `content.categories.find(c => c.id === catId).name`), `statusBefore` (de `before[catId]?.status ?? 'no-hecha'`), `statusAfter` (de `newState.categoryProgress[catId].status`), `streakBefore` (de `before[catId]?.streakDays ?? 0`), `streakAfter` (de `newState.categoryProgress[catId].streakDays`), `totalInCat` (count de ejercicios en content que incluyen catId), `pendingForHecha` (solo si statusAfter === 'no-hecha': `totalInCat - newState.categoryProgress[catId].clearedExerciseIds.length`; else `0`), `failed` (`failedCatIds.has(catId)`), `failureReason` (si failed: construir string formato `\`falló (cascada multi: ${[...failedCatIds].join(' + ')})\`` o `'falló'` si solo 1 categoría afectada; null si no failed), `isPromotion` (statusBefore in `{'no-hecha'}` Y statusAfter in `{'hecha','dominada'}`), `isRegression` (statusBefore in `{'hecha','dominada'}` Y statusAfter === `'no-hecha'`).
       - Ordenar `delta` por: primero las regresiones (más relevante para el autor), después las promociones, después las neutrales. Sub-orden alfabético por `categoryName` para determinismo.
       - `headerLabel` = `\`Sesión terminada · ${sessionResult.answers.filter(a => a.correct).length}/${sessionResult.answers.length} correctos\``.
       - Devolver `{delta, headerLabel}`.

    3. **Añadir método `returnToHomeFromSummary()`** que `currentScreen = 'home'` + `this.resetSession()` (limpia los `session*`). El template summary lo llama al pulsar el botón único.

    **B) `index.html` — Template `summary` (reemplaza el stub de Plan 02-03):**

    ```html
    <template x-if="currentScreen === 'summary'">
      <article>
        <header x-text="summaryHeaderLabel"></header>

        <ul class="summary-delta">
          <template x-for="entry in summaryDelta" :key="entry.categoryId">
            <li>
              <strong x-text="entry.categoryName"></strong>:
              <span x-text="entry.statusBefore"></span>
              <span class="delta-arrow"
                    :class="{ 'delta-regression': entry.isRegression, 'delta-promotion': entry.isPromotion }">→</span>
              <span x-text="entry.statusAfter"></span>
              <template x-if="entry.failed">
                <span class="delta-reason" x-text="' · ' + entry.failureReason"></span>
              </template>
              <template x-if="entry.streakBefore !== entry.streakAfter">
                <span class="delta-streak"
                      x-text="' · racha ' + entry.streakBefore + ' → ' + entry.streakAfter"></span>
              </template>
              <template x-if="entry.statusAfter === 'no-hecha' && entry.pendingForHecha > 0">
                <span class="delta-pending"
                      x-text="' · ' + entry.pendingForHecha + ' ejercicios para volver a hecha'"></span>
              </template>
            </li>
          </template>
        </ul>

        <button @click="returnToHomeFromSummary">Volver al home</button>
      </article>
    </template>
    ```

    Aspectos clave:
    - Un solo elemento root `<article>` (Pitfall #1).
    - Lista `<ul class="summary-delta">` con `<li>` por entrada. La flecha tiene clase condicional para color sutil rojo (regresión) o verde (promoción).
    - **NO emojis decorativos** (D-37 tono neutro). El badge de estado se muestra como texto plano (`x-text="entry.statusBefore"`) — el badge visual con glifo Unicode vive solo en la tabla home (D-31 explícito: "el resumen menciona el cambio una vez, sin fanfare").
    - Un único botón al pie: `Volver al home`. No "Repetir esta sesión" (descartado en Deferred Ideas).

    **C) `styles.css` — estilos del summary:**

    Apendear:

    ```css
    /*
     * Resumen pantalla completa (D-37, SESSION-07).
     * Tono neutral factúal: colores SUTILES en flechas de promoción/regresión.
     * Sin fanfare, sin emojis, sin badges grandes — el badge visual vive en la tabla home.
     */
    .summary-delta { list-style: none; padding-left: 0; }
    .summary-delta li { padding: 0.5rem 0; border-bottom: 1px solid var(--pico-muted-border-color, #e0e0e0); }
    .summary-delta li:last-child { border-bottom: none; }
    .delta-arrow { margin: 0 0.3rem; font-weight: 600; }
    .delta-regression { color: var(--pico-color-red-500, #d32f2f); }
    .delta-promotion { color: var(--pico-color-green-500, #2e7d32); }
    .delta-reason,
    .delta-streak,
    .delta-pending { color: var(--pico-muted-color, #6c757d); font-size: 0.9em; }
    ```

    Mensajes en español. NO añadir librerías.
  </action>

  <verify>
    <automated>
      cd /home/vcompanyb/italian-course &amp;&amp; { grep -q 'computeSummaryDelta' src/screens/app.js &amp;&amp; grep -q 'returnToHomeFromSummary' src/screens/app.js &amp;&amp; grep -q 'summary-delta' index.html &amp;&amp; grep -q 'delta-arrow' index.html &amp;&amp; grep -q 'isPromotion' src/screens/app.js &amp;&amp; grep -q 'isRegression' src/screens/app.js &amp;&amp; grep -q '.delta-regression' styles.css &amp;&amp; grep -q '.delta-promotion' styles.css &amp;&amp; node --test tests/*.test.js 2&gt;&amp;1 | tail -3 &amp;&amp; echo OK; } || echo FAIL
    </automated>
  </verify>

  <acceptance_criteria>
    - El comando arriba imprime `OK` (todas las grep + tests verdes).
    - `grep -n "this.currentScreen = 'summary'" src/screens/app.js` devuelve match (completeSession ya no va a home directamente).
    - `grep -n "this.summaryDelta = " src/screens/app.js` devuelve ≥1 match (summaryDelta se computa en completeSession).
    - `grep -n "function computeSummaryDelta" src/screens/app.js` devuelve match (helper privado al fondo).
    - `grep -nE "JSON.parse\\(JSON.stringify\\(this.state.categoryProgress" src/screens/app.js` devuelve match (deep snapshot antes de aplicar).
    - `grep -c "x-text=" index.html` devuelve más matches que en Plan 02-03 (los nuevos x-text del summary).
    - `node --test tests/*.test.js` exits 0 (la suite del dominio NO se rompe — este task no toca progress.js).
  </acceptance_criteria>

  <done>
    `completeSession` ahora computa `summaryDelta` y navega a `summary`. Template summary muestra lista con delta + estilos sutiles. NO se ha tocado lógica de in-flight Test completo todavía (Task 2 lo añade).
  </done>
</task>

<task type="auto">
  <name>Task 2: inFlightTest persistence + banner home + confirmaciones D-43/D-44</name>
  <files>src/screens/app.js, index.html, styles.css</files>

  <read_first>
    - src/screens/app.js (estado tras Task 1 — `completeSession` ya funciona con summaryDelta)
    - index.html (estado tras Task 1 — template summary completo; este task añade el banner in-flight al template home)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md secciones "D-41", "D-42", "D-43", "D-44", "D-45" (in-flight test shape, escritura per-answer, banner, conflicto al pulsar Test completo nuevo, limpieza al completar)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Persistencia in-flight Test completo (HIGH)" íntegra (cuándo escribir, atomicity, validación stale)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Code Examples — Patrón canónico de banner in-flight" (snippet HTML + JS)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Common Pitfalls" #5 (inFlightTest stale) y #9 (limpiar al completar)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md sección "Confirmación inline pattern" (los 3 usos D-27/D-43/D-44)
    - src/domain/progress.js (estado tras Plan 02-01/02 — `applySessionResult` paso 6 ya borra `inFlightTest`; verificar)
  </read_first>

  <action>
    Añadir persistencia in-flight del Test completo, banner en home, y confirmaciones D-43/D-44.

    **A) `src/screens/app.js` — nuevos métodos + extender `sessionSelectOption` + extender `openPicker`:**

    1. **Método `persistInFlightTest()`** (D-42 — escritura tras CADA respuesta del Test completo):
       - Construir el shape per D-41:
         ```
         this.state = {
           ...this.state,
           inFlightTest: {
             categoryIds: this.pickerCheckedCategoryIds.length > 0 ? [...this.pickerCheckedCategoryIds] : (this.state.inFlightTest?.categoryIds ?? []),
             exerciseIds: this.sessionExerciseIds,
             cursor: this.sessionCursor,
             answers: [...this.sessionResults],
             startedAt: this.state.inFlightTest?.startedAt ?? Date.now()
           }
         };
         saveState(this.state);
         ```
       - **Nota sobre `categoryIds`:** cuando el usuario REANUDA un test, `this.pickerCheckedCategoryIds` puede estar vacío (el picker no fue tocado). Por eso preservar `inFlightTest.categoryIds` previo si existe.
       - **Nota sobre `startedAt`:** se preserva el original si ya existe; solo se setea por primera vez en `startSession` cuando mode='test-completo'.

    2. **Extender `sessionSelectOption(idx)`** — al final del método, añadir:
       ```
       if (this.sessionMode === 'test-completo') {
         this.persistInFlightTest();
       }
       ```
       Esto va DESPUÉS del push a `sessionResults` y ANTES (o después; no importa el orden lógico) del `setTimeout` de auto-advance.

       **Importante:** La research D-42 dice "tras cada respuesta". Llamar también desde `sessionAdvance` puede ser redundante porque `cursor` cambia tras advance — pero la atomic write por respuesta cubre el caso. **Decisión:** llamar `persistInFlightTest()` SOLO desde `sessionSelectOption` (después del push del answer) y también desde `sessionAdvance` antes del `cursor += 1` para capturar el cursor correcto post-advance. Concretamente, en `sessionAdvance`, tras `this.sessionCursor += 1`, antes del check `if (sessionDone)`:
       ```
       if (this.sessionMode === 'test-completo' && !this.sessionDone) {
         this.persistInFlightTest();  // cursor actualizado
       }
       ```
       Si `sessionDone === true`, NO escribir aquí — `completeSession` correrá y `applySessionResult` borrará `inFlightTest` (paso 6 ya implementado en Plan 02-01).

    3. **Extender `startSession()`** — cuando mode='test-completo', tras setear sub-estado pero antes de `currentScreen='session'`:
       ```
       if (this.pickerMode === 'test-completo') {
         this.persistInFlightTest();  // escritura inicial del inFlightTest
       }
       ```

    4. **Método `resumeInFlightTest()`** (D-43, "Reanudar"):
       - Validar staleness (Pitfall #5): `const ift = this.state.inFlightTest;`. `const validIds = ift.exerciseIds.filter(eid => this.content.exerciseById[eid] !== undefined);`. Si `validIds.length === 0 || validIds.length <= ift.cursor`:
         ```
         this.requestConfirm({
           message: 'El test que tenías a medias ya no existe en el contenido. ¿Descartarlo?',
           confirmLabel: 'Descartar',
           cancelLabel: 'Cancelar',
           onConfirm: () => this.clearInFlightTest()
         });
         return;
         ```
       - Si válido: restaurar sub-estado session:
         - `this.sessionMode = 'test-completo'`.
         - `this.sessionExerciseIds = ift.exerciseIds`.
         - `this.sessionCursor = ift.cursor`.
         - `this.sessionResults = [...ift.answers]`.
         - `this.sessionSelectedIndex = null`.
         - `this.sessionFeedback = null`.
         - **Importante:** también restaurar `this.pickerCheckedCategoryIds = [...ift.categoryIds]` (útil si futuro restart del test).
       - `this.currentScreen = 'session'`.
       - **NO escribir** (`saveState` no es necesario aquí — el estado del blob ya tiene el inFlightTest correcto; solo navegamos).

    5. **Método `clearInFlightTest()`** (helper limpio, NO requiere confirmación porque ya pasó por D-43 o D-44):
       ```
       this.state = { ...this.state, inFlightTest: undefined };
       saveState(this.state);
       ```
       JSON.stringify elide `undefined` automáticamente (verificar opcionalmente con DevTools).

    6. **Método `discardInFlightTestWithConfirm()`** (D-43, botón "Descartar" del banner):
       ```
       this.requestConfirm({
         message: '¿Descartar el test? Los aciertos hasta ahora no se guardarán.',
         confirmLabel: 'Descartar',
         cancelLabel: 'Cancelar',
         onConfirm: () => this.clearInFlightTest()
       });
       ```

    7. **Extender `openPicker(mode)` para D-44** (conflicto al pulsar Test completo con in-flight pendiente):
       - Al entrar al método, ANTES de cambiar `currentScreen`:
         ```
         if (mode === 'test-completo' && this.state.inFlightTest) {
           this.requestConfirm({
             message: 'Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?',
             confirmLabel: 'Descartar y empezar',
             cancelLabel: 'Cancelar',
             onConfirm: () => {
               this.clearInFlightTest();
               // tras limpiar, abre el picker normalmente
               this.pickerMode = mode;
               this.pickerCheckedCategoryIds = [];
               this.currentScreen = 'picker';
             }
           });
           return;
         }
         ```
       - Si no hay in-flight pendiente: el flujo normal de Plan 02-03 (setear pickerMode, vaciar checks, currentScreen='picker').
       - Para mode='repaso': sin cambios.

    **B) `index.html` — banner in-flight test en template `home`:**

    Añadir DENTRO del `<template x-if="currentScreen === 'home'">`, DENTRO del `<article>`, ENCIMA de los botones grandes (per el "orden vertical sugerido" en CONTEXT.md "Specific Ideas"):

    ```html
    <div x-show="state.inFlightTest" role="alert" class="inflight-banner">
      ⚠ Tienes un Test completo a medias
      (<span x-text="state.inFlightTest?.cursor || 0"></span>
      / <span x-text="state.inFlightTest?.exerciseIds?.length || 0"></span> ejercicios).
      <div role="group" style="margin-top:0.5rem;">
        <button @click="resumeInFlightTest">Reanudar</button>
        <button class="secondary" @click="discardInFlightTestWithConfirm">Descartar</button>
      </div>
    </div>
    ```

    El orden vertical dentro del `<article>` del home tras esta inserción:
    1. (Opcional) `<header>` con título.
    2. **Banner in-flight (`x-show`)**.
    3. Botones grandes (Repaso 20 / Test completo).
    4. Tabla densa.

    **C) `styles.css` — estilo del banner in-flight:**

    Apendear:

    ```css
    /*
     * Banner persistente in-flight Test (D-43).
     * Color amarillo sutil para distinguirlo de la tabla pero sin alarmismo.
     */
    .inflight-banner {
      border: 1px solid var(--pico-color-amber-500, #f59e0b);
      background: rgba(245, 158, 11, 0.08);
      padding: 0.75rem 1rem;
      border-radius: var(--pico-border-radius, 0.25rem);
      margin-bottom: 1rem;
    }
    ```

    Mensajes en español. Layer purity preservada (app.js NO importa nada nuevo).
  </action>

  <verify>
    <automated>
      cd /home/vcompanyb/italian-course &amp;&amp; { grep -q 'persistInFlightTest' src/screens/app.js &amp;&amp; grep -q 'resumeInFlightTest' src/screens/app.js &amp;&amp; grep -q 'clearInFlightTest' src/screens/app.js &amp;&amp; grep -q 'discardInFlightTestWithConfirm' src/screens/app.js &amp;&amp; grep -q 'inflight-banner' index.html &amp;&amp; grep -q 'inflight-banner' styles.css &amp;&amp; grep -q "x-show=\"state.inFlightTest\"" index.html &amp;&amp; grep -q "'Ya hay un Test completo en curso'" src/screens/app.js &amp;&amp; node --test tests/*.test.js 2&gt;&amp;1 | tail -3 &amp;&amp; echo OK; } || echo FAIL
    </automated>
  </verify>

  <acceptance_criteria>
    - El comando arriba imprime `OK`.
    - `grep -nE "^[[:space:]]+persistInFlightTest\\(\\)" src/screens/app.js` devuelve match (método como propiedad del objeto Alpine).
    - `grep -n "this.sessionMode === 'test-completo'" src/screens/app.js` devuelve ≥2 matches (en sessionSelectOption Y sessionAdvance).
    - `grep -n "if (mode === 'test-completo' && this.state.inFlightTest)" src/screens/app.js` devuelve match (D-44 conflict check).
    - `grep -n "this.content.exerciseById\\[eid\\] !== undefined" src/screens/app.js` devuelve match (Pitfall #5 stale validation).
    - `grep -n "@click=\"resumeInFlightTest\"" index.html` devuelve match.
    - `grep -n "@click=\"discardInFlightTestWithConfirm\"" index.html` devuelve match.
    - `node --test tests/*.test.js` exits 0 (la suite del dominio NO se rompe).
  </acceptance_criteria>

  <done>
    Persistencia in-flight Test completo funciona. Banner home reactivo a `state.inFlightTest`. Reanudar/Descartar funcionales con validación stale. Conflicto D-44 al pulsar Test completo con in-flight pendiente. NO se ha tocado el dominio.
  </done>
</task>

<task type="auto">
  <name>Task 3: Smoke test ≥30 días integrado (DOMAIN-10)</name>
  <files>tests/domain-progress.test.js</files>

  <read_first>
    - tests/domain-progress.test.js (estado tras Plan 02-02 — ya tiene tests D-53.1, D-53.2, D-53.3 split en describe blocks)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md sección "D-53" (suite obligatoria smoke tests)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Testing strategy — Patrón de tests parametrizados (D-53 simulación 30 días)" (código canónico del bucle 30 días)
    - tests/util/test-helpers.js (helpers `blankStateV2`, `makeContent`, `setupHechaState`)
  </read_first>

  <behavior>
    Añadir UN test integrado `describe('domain — smoke test integrado 30 días (DOMAIN-10)', ...)` al final de `tests/domain-progress.test.js`. El test simula un escenario realista de uso durante 30+ días combinando todos los aspectos del dominio en una sola simulación:

    **Escenario:**
    1. **Días 1-7:** El autor practica `avere` y `genero` sin fallar nunca. Cada día acierta todos los ejercicios de ambas categorías. Verificar:
       - Día 1: ambas pasan de no-hecha a hecha con streakDays=1.
       - Día 2-7: streakDays incrementa 1 por día.
       - Estado al final del día 7: avere.streakDays=7, genero.streakDays=7, ambas hecha.

    2. **Día 8:** Sesión con ejercicio multi-cat `[avere, genero]` fallado tras aciertos previos en la misma sesión. Verificar cascada multi-cat:
       - avere.status='no-hecha', streakDays=0, clearedExerciseIds=[].
       - genero.status='no-hecha', streakDays=0, clearedExerciseIds=[].
       - exerciseStats sigue acumulando (DOMAIN-09 monotónico — verificar timesShown y timesFailed crecieron).

    3. **Días 9-29 (21 días continuos):** El autor recupera. Cada día acierta todos los ejercicios de `avere` (solo avere, no genero) sin fallar. Verificar:
       - Día 9: avere promociona de nuevo a hecha (streakDays=1, becameHechaAt='día 9').
       - Día 9-29: streakDays incrementa cada día.
       - Día 29 (que es el día 21 desde la nueva promoción): avere.streakDays=21, **status='dominada'**, becameDominadaAt='día 29'.

    4. **Día 30:** Sesión con fallo en `avere`. Verificar regresión total desde dominada:
       - avere.status='no-hecha', streakDays=0, clearedExerciseIds=[], becameHechaAt=undefined, becameDominadaAt=undefined.
       - genero NO se toca (no estaba en esta sesión), sigue `no-hecha` con streakDays=0 desde el día 8.

    5. **Día 30 (segunda sesión el MISMO día):** 5 sesiones más el mismo día tocando `avere` sin fallar. Verificar racha guard:
       - Tras la primera sesión exitosa post-fallo: streakDays=1.
       - Tras las siguientes 5 sesiones del mismo día con `lastSuccessDate === today`: streakDays sigue siendo 1 (NO incrementa 5 veces).

    Total: ≥35 iteraciones de applySessionResult con ISO dates sintéticos `'2026-06-01'` a `'2026-06-30'`. La aserción final verifica el invariante combinado: dominio funciona end-to-end durante un escenario complejo y realista.

    **Bonus aserción de pureza:** al final del escenario, verificar que el `content` original NO se mutó (`content.exerciseById` sigue siendo `===` referencialmente intacto, sin nuevos campos añadidos por sorpresa).
  </behavior>

  <action>
    Añadir al final de `tests/domain-progress.test.js` un `describe` nuevo:

    ```js
    describe('domain — smoke test integrado 30 días (DOMAIN-10)', () => {
      test('escenario completo: cascada multi-cat + promoción + dominada + regresión + racha guard', () => {
        // ... implementación derivada del <behavior> arriba ...
      });
    });
    ```

    Implementación:

    1. **Setup:** `let state = blankStateV2();`. `const content = makeContent([...])` con ejercicios:
       - `a1, a2, a3` con `categoryIds: ['avere']`.
       - `g1, g2` con `categoryIds: ['genero']`.
       - `m1` con `categoryIds: ['avere', 'genero']` (el multi-cat).
       - Total: 6 ejercicios, 2 categorías. avere tiene 4 ejercicios (a1, a2, a3, m1); genero tiene 3 (g1, g2, m1).

    2. **Helper local** dentro del test (no se reusa fuera, no exportar):
       ```js
       function isoDay(n) { return `2026-06-${String(n).padStart(2, '0')}`; }
       function sessionAnswers(exerciseIds, correctMap = {}) {
         return exerciseIds.map(id => ({ exerciseId: id, correct: correctMap[id] !== false }));
       }
       ```
       `correctMap` permite especificar fallos selectivos: `{m1: false}` significa "todo correct excepto m1".

    3. **Días 1-7 — bucle de promoción + 7 días racha:**
       ```js
       for (let day = 1; day <= 7; day++) {
         state = applySessionResult(state, { answers: sessionAnswers(['a1','a2','a3','m1','g1','g2']) }, content, isoDay(day));
       }
       assert.equal(state.categoryProgress.avere.status, 'hecha');
       assert.equal(state.categoryProgress.avere.streakDays, 7);
       assert.equal(state.categoryProgress.genero.status, 'hecha');
       assert.equal(state.categoryProgress.genero.streakDays, 7);
       ```

    4. **Día 8 — cascada multi-cat:** sesión con `m1` fallado tras aciertos en a1/g1:
       ```js
       state = applySessionResult(state, { answers: [
         { exerciseId: 'a1', correct: true },
         { exerciseId: 'g1', correct: true },
         { exerciseId: 'm1', correct: false }
       ]}, content, isoDay(8));
       assert.equal(state.categoryProgress.avere.status, 'no-hecha');
       assert.equal(state.categoryProgress.avere.streakDays, 0);
       assert.deepEqual(state.categoryProgress.avere.clearedExerciseIds, []);
       assert.equal(state.categoryProgress.genero.status, 'no-hecha');
       assert.equal(state.categoryProgress.genero.streakDays, 0);
       ```

    5. **Días 9-29 — recuperación + 21 días racha continua sobre avere:** 21 días consecutivos sin tocar genero, todos los ejercicios de avere acertados:
       ```js
       for (let day = 9; day <= 29; day++) {
         state = applySessionResult(state, { answers: sessionAnswers(['a1','a2','a3','m1']) }, content, isoDay(day));
       }
       assert.equal(state.categoryProgress.avere.status, 'dominada');
       assert.equal(state.categoryProgress.avere.streakDays, 21);
       assert.equal(state.categoryProgress.avere.becameDominadaAt, isoDay(29));
       ```

    6. **Día 30 — regresión desde dominada por fallo:**
       ```js
       state = applySessionResult(state, { answers: [
         { exerciseId: 'a1', correct: false }
       ]}, content, isoDay(30));
       assert.equal(state.categoryProgress.avere.status, 'no-hecha');
       assert.equal(state.categoryProgress.avere.streakDays, 0);
       assert.equal(state.categoryProgress.avere.becameDominadaAt, undefined);
       assert.equal(state.categoryProgress.avere.becameHechaAt, undefined);
       ```

    7. **Día 30 (mismo día, sesión 2-6) — racha guard `lastSuccessDate !== today`:** completar correctamente avere (cubriendo todos los ejercicios), luego 5 sesiones más con 1 acierto cada una (cubriendo m1 una vez por sesión); verificar racha guard:
       ```js
       // Sesión 2: completa avere → promociona a hecha con streak=1
       state = applySessionResult(state, { answers: sessionAnswers(['a1','a2','a3','m1']) }, content, isoDay(30));
       assert.equal(state.categoryProgress.avere.status, 'hecha');
       assert.equal(state.categoryProgress.avere.streakDays, 1);

       // Sesiones 3-7: mismo día, tocan avere sin fallar
       for (let i = 0; i < 5; i++) {
         state = applySessionResult(state, { answers: [{ exerciseId: 'a1', correct: true }] }, content, isoDay(30));
       }
       // Racha guard: streakDays SIGUE siendo 1 (no incrementa 5 veces el mismo día)
       assert.equal(state.categoryProgress.avere.streakDays, 1);
       assert.equal(state.categoryProgress.avere.lastSuccessDate, isoDay(30));
       ```

    8. **Aserción final de pureza/monotónico:**
       ```js
       // exerciseStats es monotónico — un acierto durante 7+1+21+1+5 = 35 iteraciones sobre a1 implica timesShown ≥ 35.
       assert.ok(state.exerciseStats.a1.timesShown >= 30, `a1.timesShown=${state.exerciseStats.a1.timesShown}, esperado ≥30`);
       // dailyLog tiene entrada para cada día tocado (al menos 30 fechas distintas en isoDay range)
       assert.ok(Object.keys(state.dailyLog).length >= 9);  // días 1-7 + 8 + 9-29 + 30 = al menos 9 entradas únicas
       ```

    Total: 1 test integrado que añade ~10 assertions clave cubriendo TODOS los aspectos del dominio en una sola simulación realista.

    **NO modificar tests previos** (Plans 02-01 y 02-02 ya tienen tests unitarios por aspecto; este es complementario).

    Mensajes en español en los mensajes de error de assert (en lo posible). Comentarios en código en español o spanglish (consistente con el resto de la suite).
  </action>

  <verify>
    <automated>
      cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/*.test.js 2&gt;&amp;1 | tail -10
    </automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/*.test.js` exits 0 con el test nuevo "escenario completo: cascada multi-cat + promoción + dominada + regresión + racha guard" pasando.
    - `grep -n "smoke test integrado 30 días" tests/domain-progress.test.js` devuelve match.
    - `grep -n "becameDominadaAt" tests/domain-progress.test.js` devuelve ≥2 matches (la simulación verifica promoción Y regresión de dominada).
    - El total de tests en la suite es ≥33 (32 de Plans 02-01/02 + 1 nuevo integrado).
    - El test integrado completa en <500ms (simulación pura sin I/O — debe ser rápido).
  </acceptance_criteria>

  <done>
    DOMAIN-10 completo: la suite tiene 1 test integrado que cubre el escenario realista de 30 días combinando todos los aspectos del dominio. Tests verdes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT final Phase 2 — los 6 success criteria verdes</name>

  <what-built>
    El motor completo de re-verificación operativo end-to-end:
    - Home dashboard con tabla densa + banner in-flight test reactivo.
    - Picker compartido con dos modos (Repaso 20 / Test completo).
    - Sesión multiple-choice que aplica cascada + promociones al terminar.
    - Resumen pantalla completa con delta factústico por categoría tocada.
    - Persistencia in-flight Test completo con Reanudar/Descartar.
    - Confirmaciones inline en los 3 puntos (D-27/D-43/D-44).
    - Boot regression (DOMAIN-06) cuando se añaden ejercicios al JSON.
    - Suite de smoke tests verde con escenario integrado ≥30 días.
  </what-built>

  <how-to-verify>
    Arrancar `npx serve` y http://localhost:3000. Verificar los 6 success criteria del ROADMAP.md §Phase 2:

    **CRITERIO 1 — Home dashboard completo (SESSION-01):**
    1. La pantalla home muestra TODAS las categorías de `categories.json` (al menos avere).
    2. Cada fila tiene 5 columnas: Estado (badge Unicode + color), Categoría, Racha (`N d` o `21+ d`), Ejercicios (total), Última vez (`hoy`/`ayer`/`hace N d`/`—`).
    3. Los badges visualmente distintos: `●` gris para no-hecha, `✓` verde para hecha, `★` ámbar para dominada.
    4. Encima de la tabla: dos botones grandes lado a lado (`Repaso 20` y `Test completo`).

    **CRITERIO 2 — Picker con dos modos (SESSION-02 + SESSION-03):**
    1. Click `Repaso 20` → picker con cabecera `Repaso de 20 ejercicios`, checkboxes vacíos por defecto, botón disabled cuando 0 marcadas.
    2. Marcar avere → label del botón muestra `Empezar repaso (N ejercicios — todos los disponibles)` cuando N<20.
    3. Click `Test completo` desde home → picker con cabecera `Test completo` + aviso inline `⚠ Test completo — N ejercicios sin tope. Se puede reanudar si lo cierras a medias.`
    4. Botones `Seleccionar todo` / `Quitar todo` funcionan.

    **CRITERIO 3 — Cascada + promoción visibles tras una sesión:**
    1. Lanzar un Repaso → completar TODOS los ejercicios SIN fallar → al terminar aparece resumen con `avere: no-hecha → hecha` (flecha verde sutil) y `racha 0 → 1`.
    2. Volver al home → la fila de avere ahora tiene badge `✓` verde, Racha `1 d`, Última vez `hoy`.
    3. Lanzar otro Repaso → fallar ≥1 ejercicio → resumen muestra `avere: hecha → no-hecha` (flecha roja sutil), `racha 1 → 0`, mensaje `falló` con la cascada documentada.
    4. Volver al home → avere otra vez `● no-hecha`, Racha `0 d`.

    **CRITERIO 4 — DOMAIN-06 regresión (manual):**
    1. Si avere está en `hecha`, editar `content/exercises/avere.json` añadiendo un nuevo ejercicio (ej. `avere-nuevo-001` con id único). Validar el JSON con el formato existente.
    2. Recargar la página → home muestra avere ahora con `●` gris (no-hecha), Racha `0`, pero columna Ejercicios incrementada (12+1).
    3. Inspeccionar localStorage → `categoryProgress.avere.clearedExerciseIds` sigue conteniendo los IDs viejos (no se vació — D-40).

    **CRITERIO 5 — Abandono Repaso descarta + Test completo reanuda (SESSION-08, SESSION-09):**
    1. Lanzar Repaso → responder 2 ejercicios → cerrar el tab (Ctrl+W).
    2. Recargar http://localhost:3000 → home idéntica antes y después (los 2 ejercicios respondidos NO se persistieron porque era Repaso).
    3. Lanzar Test completo → marcar avere → click Empezar.
    4. Responder 3 ejercicios. Inspeccionar localStorage → `inFlightTest.cursor=3`, `inFlightTest.answers.length=3`.
    5. Cerrar el tab. Recargar → en el home aparece el banner amarillo `⚠ Tienes un Test completo a medias (3/N ejercicios). [Reanudar] [Descartar]`.
    6. Click `Reanudar` → continúa en la sesión exactamente en el ejercicio 4.
    7. Completar el Test completo → ve resumen → vuelve a home → banner ya NO aparece (inFlightTest borrado al completar).
    8. Iniciar otro Test completo a mitad → click otra vez `Test completo` en home → confirmación `Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?` (D-44).
    9. Mid-Test completo → click `Descartar` en el banner del home (volver al home primero) → confirmación inline `¿Descartar el test? Los aciertos hasta ahora no se guardarán.` → click `Descartar` → banner desaparece, inFlightTest vacío.

    **CRITERIO 6 — Smoke tests verde (DOMAIN-10):**
    1. En la terminal: `node --test tests/*.test.js`.
    2. Verificar exit code 0 y mensaje de cobertura con ≥30 tests pasando, incluyendo el test "smoke test integrado 30 días".

    **CRITERIO BONUS — Confirmación inline mid-Repaso (D-27, ya validado en Plan 02-03):**
    1. Mid-Repaso, click `← Volver al home` → confirmación inline `¿Descartar esta sesión de repaso? Tus respuestas no se guardarán.` con botones `Descartar` / `Continuar`. Click `Descartar` → vuelve a home, state intacto.

    **Si TODOS los 6 + 1 bonus pasan:** reportar "approved 7/7" o "approved 6/6 + bonus". Si falla algo, describir el comportamiento observado.
  </how-to-verify>

  <resume-signal>Type "approved" si todos los 6 criterios + bonus pasan, o describe los problemas (con ID del criterio 1-6).</resume-signal>
</task>

</tasks>

<verification>
  - **Verificación automática:** `node --test tests/*.test.js` exits 0 con ≥33 tests verdes (incluye el integrado 30 días).
  - **Verificación browser (UAT):** 6 success criteria del ROADMAP Phase 2 todos verdes, validados por el autor en el Task 4 checkpoint.
  - **Verificación de no-regresión:** el flujo Phase 1 (multiple-choice con feedback verde/rojo, auto-avance 600ms) sigue intacto — verificable implícitamente en CRITERIO 3 step 1.
</verification>

<success_criteria>
  Los 6 success criteria de la Phase 2 del ROADMAP.md están verdes:
  1. Home dashboard con tabla completa y todas sus columnas (SESSION-01).
  2. Picker con dos modos, checkboxes, contador dinámico, aviso Test completo (SESSION-02/03).
  3. Cascada multi-cat + promociones observables, resumen con delta antes→después (DOMAIN-04/05/07/08, SESSION-07).
  4. Regresión por ejercicio nuevo añadido al JSON (DOMAIN-06).
  5. Abandono Repaso descarta state; Test completo reanudable con banner; conflicto al lanzar nuevo Test completo confirmado (SESSION-08/09).
  6. Suite smoke tests verde con ≥30 días simulados cubriendo todos los aspectos (DOMAIN-10).
</success_criteria>

<output>
Crear `.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-04-SUMMARY.md` al terminar. Documentar:
- summaryDelta + computeSummaryDelta helper + template summary completo.
- persistInFlightTest implementación (cuándo escribe, integración en sessionSelectOption y sessionAdvance).
- Banner in-flight test reactivo + métodos resumeInFlightTest/discardInFlightTestWithConfirm + validación stale.
- Confirmaciones D-43/D-44 integradas con el helper requestConfirm de Plan 02-03.
- Test integrado 30 días DOMAIN-10.
- Resultados del UAT 6/6 + bonus.
- Lista final de tests verdes (esperado ≥33).
- Phase 2 cerrada: motor "te obliga a no olvidar" operativo end-to-end.
</output>
