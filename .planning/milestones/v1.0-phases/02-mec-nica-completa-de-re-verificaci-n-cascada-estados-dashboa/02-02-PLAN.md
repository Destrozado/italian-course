---
phase: 02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/domain/session.js
  - src/domain/progress.js
  - tests/domain-session.test.js
autonomous: true
mvp_mode: true
mvp_slice_kind: domain-foundation
requirements:
  - DOMAIN-03
  - DOMAIN-06
  - DOMAIN-10
user_setup: []

must_haves:
  truths:
    - "`buildSession(categoryIds, allExercises, state, requestedSize, mode, rng)` mantiene la firma de Phase 1 pero añade una GUARANTEE phase antes del FILL: para cada categoría seleccionada, garantiza ≥1 ejercicio si hay candidates disponibles"
    - "`buildSession` con 2 categorías y 1 ejercicio multi-cat que las cubre ambas → el ejercicio se elige una sola vez y el `alreadyCovered` chequeo evita duplicación"
    - "`buildSession` con targetSize=20 y 25 categorías marcadas (oversubscription) → la GUARANTEE phase corta cuando alcanza targetSize, las categorías sobrantes se ignoran silenciosamente (D-51)"
    - "`buildFullTest(categoryIds, allExercises, rng)` devuelve TODOS los ejercicios del pool filtrado por las categorías, ordenados por Fisher-Yates con el RNG inyectado, sin tope ni weighted sampling"
    - "`buildFullTest` con seed determinista produce el mismo orden repetidamente"
    - "Ratio estadístico weighted sampling: con 1000 iteraciones seeded, un ejercicio timesShown=0 vs timesShown=100 produce ratio cold/hot ≈ 11 (weight cap=10 → ratios teóricos 1 vs 1/11; tolerancia ±3 sobre 1000 iters)"
    - "`applyNewExerciseRegression(state, content)` (export NUEVA en `progress.js`) detecta ejercicios añadidos al JSON: categorías `hecha`/`dominada` cuyo `clearedExerciseIds` no cubre algún ejercicio del content regresan a `no-hecha` con `streakDays=0`, `becameHechaAt=undefined`, `becameDominadaAt=undefined`; `clearedExerciseIds` se PRESERVA (D-40 explícito)"
    - "`applyNewExerciseRegression` es PURA: input no se muta; categorías `no-hecha` no se tocan; categorías cuyo content no cambió no se tocan (idempotente cuando no hay regresión)"
  artifacts:
    - path: "src/domain/session.js"
      provides: "buildSession con GUARANTEE phase + buildFullTest (nueva export) + weightedPickOne (helper privado factorizado)"
      contains: "buildFullTest"
    - path: "src/domain/progress.js"
      provides: "applyNewExerciseRegression (nueva export) — DOMAIN-06 evaluada al boot"
      contains: "applyNewExerciseRegression"
    - path: "tests/domain-session.test.js"
      provides: "≥6 tests sampler edges (GUARANTEE phase + multi-cat coverage + oversubscription + buildFullTest determinism + statistical ratio + DOMAIN-06 regression)"
  key_links:
    - from: "src/domain/session.js"
      to: "src/domain/progress.js"
      via: "weightedPickOne usa state.exerciseStats[ex.id].timesShown vía exerciseWeight (preservado de Phase 1)"
      pattern: "exerciseWeight"
    - from: "tests/domain-session.test.js"
      to: "tests/util/test-helpers.js"
      via: "import blankStateV2, makeContent, seededLcg"
      pattern: "import.*test-helpers"
---

<objective>
Completar el **motor del dominio** añadiendo dos piezas pendientes que viven en la capa pura:

1. **`buildSession` con GUARANTEE phase** (D-49) — Phase 1 era FILL-only; ahora antes de la FILL hay una pasada que garantiza ≥1 ejercicio por categoría seleccionada (set-cover greedy). Esto materializa el comportamiento "cada categoría es practicada al menos una vez por sesión" que el dashboard de la Phase 2 hace visible.

2. **`buildFullTest` nueva export** (D-50) — modo "Test completo" del picker. Sin tope, sin weighted sampling, solo Fisher-Yates determinista sobre todos los ejercicios del pool filtrado.

3. **`applyNewExerciseRegression` nueva export en `progress.js`** (D-40, DOMAIN-06) — invocada UNA SOLA VEZ en boot por `main.js` (Plan 02-03 hace la integración). Detecta categorías `hecha`/`dominada` cuyo `clearedExerciseIds` no cubre algún ejercicio actual del content (porque el autor añadió ejercicios al JSON) y las regresa a `no-hecha` con `clearedExerciseIds` PRESERVADO (D-40 explícito — los aciertos previos siguen contando para futuras sesiones).

Purpose: completar la suite de funciones puras que Plan 02-03 (UI) consumirá. Sin este plan, el picker "Test completo" no tiene sampler, y el banner "Categoría X ha vuelto a no-hecha porque añadiste un ejercicio nuevo" no funciona.

Output:
- `src/domain/session.js` ampliado con GUARANTEE phase + `buildFullTest` + `weightedPickOne` privado.
- `src/domain/progress.js` ampliado con `applyNewExerciseRegression` (función SEPARADA — Pitfall #10 documentado: NO va dentro de `applySessionResult`).
- `tests/domain-session.test.js` nuevo con ≥6 tests cubriendo D-53.4 (sampler edges + DOMAIN-06 regression).

**Slice vertical observable:** al final del plan, `node --test tests/*.test.js` pasa con ≥32 tests verdes. Un REPL podría hacer `buildFullTest(['avere','genero'], allExercises, seededLcg(42))` y obtener un orden determinista.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-01-SUMMARY.md

<!-- Phase 1 archivos que este plan extiende -->
@src/domain/session.js
@src/domain/progress.js
@tests/domain.test.js
@tests/util/seeded-rng.js
@tests/util/test-helpers.js

<interfaces>
<!-- Contrato actual Phase 1 (a EXTENDER, no romper firma) -->

```js
// src/domain/session.js — actual Phase 1
const WEIGHT_CAP = 10;
export function exerciseWeight(timesShown) { ... }
export function buildSession(categoryIds, allExercises, state, requestedSize, mode='repaso', rng=Math.random) {
  // Phase 1: FILL-only weighted random sin reemplazo + Fisher-Yates final
}
```

Nuevas exports tras este plan:
```js
// src/domain/session.js
export function buildFullTest(categoryIds, allExercises, rng=Math.random)
  : { exerciseIds: string[], actualSize: number };

// src/domain/progress.js
export function applyNewExerciseRegression(state, content)
  : newState;  // categorías hecha/dominada regresan a no-hecha si content tiene ejercicios fuera de cleared
```

Firma de helper privado factorizado:
```js
function weightedPickOne(candidates, state, rng): exerciseObj
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildSession con GUARANTEE phase + buildFullTest + weightedPickOne helper</name>
  <files>src/domain/session.js, tests/domain-session.test.js</files>

  <read_first>
    - src/domain/session.js (líneas 1-96 íntegras — Phase 1 actual: `WEIGHT_CAP=10`, `exerciseWeight`, `buildSession` FILL-only con weighted random sin reemplazo + Fisher-Yates final)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md secciones "D-49", "D-50", "D-51" (GUARANTEE phase + buildFullTest + oversubscription)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Sampler GUARANTEE + FILL (HIGH)" íntegra (código canónico + edge cases tabla)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md sección "src/domain/session.js (DOMAIN, pure, transform) — MODIFICADO"
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Testing strategy — Patrón para sampler tests (D-53.4)" (statistical ratio test)
    - tests/domain.test.js líneas 50-90 (tests Phase 1 sobre buildSession que esta task DEBE preservar verdes con la firma extendida)
    - tests/util/seeded-rng.js (RNG determinista para tests)
    - tests/util/test-helpers.js (creado en Plan 02-01 Task 1)
  </read_first>

  <behavior>
    Tests obligatorios en `tests/domain-session.test.js`. Mínimo **6 tests** repartidos en 2 `describe`:

    **Grupo D-53.4a — GUARANTEE phase (`buildSession`):**
    - Test: 2 categorías `['avere','genero']` + 1 solo ejercicio multi-cat `m1` con `categoryIds:['avere','genero']` + pool=1 → `buildSession` devuelve `exerciseIds=['m1']`, `actualSize=1`. La GUARANTEE para `avere` elige `m1`; la GUARANTEE para `genero` ve `alreadyCovered=true` y skip. NO se duplica.
    - Test: 3 categorías `['avere','genero','verbos']` + pool con 1 ejercicio por categoría (3 ejercicios totales únicos por categoría) + targetSize=20 → la GUARANTEE phase llena 3 ejercicios (uno por categoría); el FILL phase no añade más (pool exhausto); `actualSize=3`.
    - Test: oversubscription — 25 categorías con 1 ejercicio cada una + targetSize=20 → `buildSession` devuelve `actualSize=20` (no 25). La GUARANTEE phase corta cuando alcanza target. 5 categorías quedan sin cubrir silenciosamente (D-51).
    - Test: weighted statistical ratio — pool con 2 ejercicios `cold` (timesShown=0) y `hot` (timesShown=100), ambos en categoría `avere`, requestedSize=1 → ejecutar 1000 iteraciones con `seededLcg(1000+i)` produce ratio `counts.cold / counts.hot` entre 8 y 14 (esperado ~11, weight cap=10 → ratios teóricos 1/1 vs 1/11). Aceptar tolerancia amplia por el efecto del Fisher-Yates final (no afecta a la prob pero garantiza determinismo).

    **Grupo D-53.4b — `buildFullTest`:**
    - Test: 2 categorías + pool con 12 ejercicios distribuidos entre ellas → `buildFullTest` devuelve `exerciseIds.length === 12`, `actualSize === 12`, todos los IDs únicos, mismo seed produce mismo orden (determinismo).
    - Test: pool vacío (categoría sin ejercicios) → `buildFullTest` devuelve `{exerciseIds:[], actualSize:0}` sin throw.

    **Bonus (opcional pero recomendado, no contado en mínimo):**
    - Test: `buildSession` con `categoryIds:[]` (lista vacía) → devuelve `{exerciseIds:[], actualSize:0}` (pool vacío por filtrado, defensivo).

    Los tests Phase 1 existentes sobre `buildSession` en `tests/domain.test.js` líneas 50-90 deben seguir verdes (NO requieren cambio — la firma `buildSession` no cambia, solo el cuerpo crece). Si algún test Phase 1 falla tras añadir GUARANTEE phase, INVESTIGAR: probablemente el orden de outputs cambió porque la GUARANTEE se ejecuta antes de FILL. Caso típico: el test `'buildSession filters by categoryIds'` (línea 80) llama con `['avere']` + pool mixto y espera 2 ejercicios. Con GUARANTEE phase, el primer pick es la GUARANTEE para `avere`, después FILL — el `actualSize` final sigue siendo 2 (no cambia). Si rompe por orden, ajustar el assert a verificar SET de IDs en lugar de orden.
  </behavior>

  <action>
    Extender `src/domain/session.js` siguiendo el código canónico de 02-RESEARCH.md sección "Sampler GUARANTEE + FILL (HIGH)".

    1. **Extraer helper privado `weightedPickOne(candidates, state, rng)`:** factorizar el bucle "computar weights → totalWeight → random pick" que actualmente vive inline en `buildSession`. Función pura, no exportada. Mismo algoritmo del Phase 1 (líneas 67-83 actuales) con safety por float-rounding al final. La factorización es necesaria porque GUARANTEE phase y FILL phase ambas la usan.

    2. **Reescribir `buildSession`** preservando la firma `(categoryIds, allExercises, state, requestedSize, mode='repaso', rng=Math.random)`. El cuerpo nuevo tiene 4 pasos en este orden:
       - Paso 1: Pool por solapamiento de categoryIds (preservar del Phase 1).
       - Paso 2: **GUARANTEE phase** (D-49) — bucle por `for (const cat of categoryIds)`: si `session.some(ex => ex.categoryIds.includes(cat))` continuar (alreadyCovered chequeo); si no, `candidates = pool.filter(ex => ex.categoryIds.includes(cat) && !session.includes(ex))`; si candidates vacío continuar (D-51 silent skip); si `session.length >= targetSize` break (oversubscription protection); pick con `weightedPickOne(candidates, state, rng)`; push a session.
       - Paso 3: **FILL phase** (existente Phase 1, refactorizada para usar `weightedPickOne`): bucle `while (session.length < targetSize && remaining.length > 0)` con `pool.filter(ex => !session.includes(ex))` como `remaining`.
       - Paso 4: **Fisher-Yates final** con el mismo RNG (preservar del Phase 1 — los picks de GUARANTEE no deben quedar todos al inicio).

       Devolver `{exerciseIds: session.map(ex => ex.id), actualSize: session.length}`.

    3. **Añadir export `buildFullTest(categoryIds, allExercises, rng=Math.random)`** (D-50). Cuerpo simple:
       - Pool por solapamiento categoryIds (mismo filtro que buildSession).
       - Fisher-Yates sobre el pool entero usando el RNG.
       - Devolver `{exerciseIds: shuffled.map(ex => ex.id), actualSize: shuffled.length}`.
       - Sin tope, sin weighted, sin guarantee phase (todos los ejercicios entran, orden random determinista).

    4. **Header del archivo:** actualizar comentarios y JSDoc:
       - Comentario superior: cambiar "Phase 1 scope: solo FILL phase. La GUARANTEE phase llega en Phase 2..." por "Phase 2: añadida GUARANTEE phase (D-49) + buildFullTest (D-50)".
       - JSDoc de `buildSession`: documentar el nuevo paso 2 (GUARANTEE) con referencia a D-49.
       - JSDoc de `buildFullTest`: nuevo, similar al de `buildSession` pero con `requestedSize` ausente y nota "sin tope, sin weighted".
       - Sección "Decisiones aplicadas": añadir D-49, D-50, D-51.

    5. **Crear `tests/domain-session.test.js`** con los ≥6 tests del `<behavior>`. Imports:
       ```
       import { test, describe } from 'node:test';
       import assert from 'node:assert/strict';
       import { buildSession, buildFullTest, exerciseWeight } from '../src/domain/session.js';
       import { seededLcg } from './util/seeded-rng.js';
       ```
       (NO necesita `test-helpers.js` aquí porque el sampler tiene shape de exercises explícito en cada test.)

       Para el statistical ratio test: usar `seededLcg(1000 + i)` como en RESEARCH.md "Patrón para sampler tests" — cada iteración con seed distinto para evitar correlación entre picks. Aceptar tolerancia `ratio > 8 && ratio < 14` (más amplia que la documentada en RESEARCH.md ±5% porque 1000 iteraciones con seeds distintos tienen varianza).

       Para los tests de oversubscription: generar 25 categorías con `Array.from({length:25}, (_,i) => 'cat'+i)` y 25 ejercicios `{id:'e'+i, categoryIds:['cat'+i]}`.

    **Importante para Phase 1 compatibility:** tras extender `buildSession`, ejecutar `node --test tests/domain.test.js` y verificar que los 3 tests sobre buildSession siguen verdes. Si alguno falla por cambio de orden post-GUARANTEE, ajustar el assert del test viejo en `tests/domain.test.js` (línea ~86) para verificar SET de IDs en lugar de orden — esto es legítimo porque el comportamiento documentado es "los IDs únicos de las categorías seleccionadas", no un orden específico. NO mover esos tests a `domain-session.test.js` — son Phase 1 y se quedan donde están por traceability.

    Mensajes en español (FOUND-04). Layer purity intacta: `src/domain/session.js` sigue sin imports.
  </action>

  <verify>
    <automated>
      cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/*.test.js 2&gt;&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "^export function buildFullTest" src/domain/session.js` devuelve match.
    - `grep -nE "^function weightedPickOne" src/domain/session.js` devuelve match (helper privado factorizado).
    - `grep -nE "alreadyCovered" src/domain/session.js` devuelve ≥1 match (chequeo GUARANTEE).
    - `node --test tests/domain-session.test.js` exits 0 con ≥6 tests pasando.
    - `node --test tests/domain.test.js` exits 0 (los 3 tests Phase 1 sobre buildSession siguen verdes — la firma se preservó).
    - `node --test tests/*.test.js` exits 0 (suite completa).
    - El test "weighted statistical ratio" pasa con tolerancia documentada (ratio entre 8 y 14).
    - El test "buildFullTest pool 12 ejercicios" verifica `actualSize === 12` Y `new Set(exerciseIds).size === 12` (unicidad).
    - El test "oversubscription 25 cats targetSize 20" verifica `actualSize === 20` (la GUARANTEE phase cortó).
    - Header de `src/domain/session.js` contiene literal "D-49" y "D-50" (grep).
  </acceptance_criteria>

  <done>
    Sampler completo: GUARANTEE phase funciona, oversubscription se gestiona silenciosa, `buildFullTest` es determinista, weighted ratio estadísticamente correcto. Tests Phase 1 verdes (firma `buildSession` preservada).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyNewExerciseRegression (DOMAIN-06)</name>
  <files>src/domain/progress.js, tests/domain-progress.test.js</files>

  <read_first>
    - src/domain/progress.js (estado tras Plan 02-01 Task 2 — `applySessionResult` extendido + helpers privados `blankCategoryProgress`, `buildExercisesByCategory`, `uniqueStrings`)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md sección "D-40" (regresión por ejercicio nuevo)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Common Pitfalls" #10 (DOMAIN-06 en boot, NO dentro de applySessionResult) — incluye código canónico de `applyNewExerciseRegression`
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md sección "src/domain/progress.js — MODIFICADO" subsección "Nueva export `applyNewExerciseRegression`"
    - tests/util/test-helpers.js (creado en Plan 02-01 — `blankStateV2`, `makeContent`, `setupHechaState`)
  </read_first>

  <behavior>
    Tests obligatorios añadidos al `describe('domain/progress — DOMAIN-06 regression', ...)` en `tests/domain-progress.test.js` (creado en Plan 02-01 Task 2). Mínimo **4 tests**:

    - Test: estado v2 con `categoryProgress.avere = {status:'hecha', clearedExerciseIds:['a1','a2']}` + content con ejercicios `[a1, a2, a3_nuevo]` → `applyNewExerciseRegression` devuelve nuevo state con `avere.status='no-hecha'`, `streakDays=0`, `becameHechaAt=undefined`, `becameDominadaAt=undefined`, **`clearedExerciseIds=['a1','a2']` preservado** (D-40 explícito).
    - Test: estado v2 con `categoryProgress.avere = {status:'dominada', streakDays:25, becameDominadaAt:'2026-05-01', clearedExerciseIds:['a1','a2']}` + content con ejercicios `[a1, a2, a3_nuevo]` → regresión a `no-hecha`, `becameDominadaAt=undefined`, `clearedExerciseIds=['a1','a2']` preservado.
    - Test: categoría `no-hecha` con cleared parcial + content con ejercicio nuevo → NO se toca (la regresión solo aplica a `hecha`/`dominada`). Verificar `state === result` por shallow equality (el spread-clone interno crea un nuevo objeto, pero `categoryProgress[catId]` debería ser el MISMO objeto referencia si no hubo cambios — verificable con `assert.strictEqual(result.categoryProgress.avere, before.categoryProgress.avere)` SI el código preserva la identidad cuando no hay cambio; alternativa más laxa: verificar todos los campos iguales con `deepEqual`).
    - Test: idempotencia — categoría `hecha` con `clearedExerciseIds=['a1','a2','a3']` cubriendo TODOS los ejercicios del content `[a1,a2,a3]` → `applyNewExerciseRegression` NO la regresa (no hay ejercicio nuevo); el state output es funcionalmente igual al input.
    - **Test pureza (bonus):** input state NO se muta tras `applyNewExerciseRegression` (`assert.deepEqual(stateAntes, stateOriginalClonado)`).
  </behavior>

  <action>
    Añadir a `src/domain/progress.js` la export nueva `applyNewExerciseRegression(state, content)` derivada del código canónico de 02-RESEARCH.md "Common Pitfalls #10".

    **CRÍTICO:** esta función va SEPARADA de `applySessionResult` (Pitfall #10). NO se integra dentro de `applySessionResult`. La distinción semántica:
    - `applySessionResult` corre al **terminar una sesión** con `sessionResult.answers`.
    - `applyNewExerciseRegression` corre al **boot** del app (Plan 02-03 hace la integración en `main.js`), después de `loadState` + `loadContent`, antes de resolver `appDataReady`. Solo entonces, una vez por carga.

    Cuerpo de la función (algoritmo):

    1. Construir `exercisesByCategory` reusando el helper privado `buildExercisesByCategory(content)` que ya existe en el módulo (creado en Plan 02-01 Task 2).
    2. Spread-clone del state + sub-objeto `categoryProgress` (patrón "Spread-clone defensivo" de 02-PATTERNS.md).
    3. Bucle `for (const [catId, cat] of Object.entries(next.categoryProgress))`:
       - Si `cat.status !== 'hecha' && cat.status !== 'dominada'`: continue (solo regresa estados promocionados).
       - `allInCat = exercisesByCategory[catId] ?? []`.
       - `hasNewExercise = allInCat.some(eid => !cat.clearedExerciseIds.includes(eid))`.
       - Si `hasNewExercise`: `next.categoryProgress[catId] = { ...cat, status:'no-hecha', streakDays:0, becameHechaAt:undefined, becameDominadaAt:undefined }`. **NO vaciar `clearedExerciseIds`** (D-40 explícito y crítico — sin esto, el usuario perdería todo el trabajo al añadir un solo ejercicio). `lastPracticedDate` y `lastSuccessDate` se preservan (no se mencionan en el destructuring de override).
    4. Devolver `next`.

    JSDoc explícito:
    - Documentar la firma `(state, content) → newState`.
    - Documentar que la función es PURA (no muta input).
    - Documentar EXPLÍCITAMENTE: "Esta función NO va dentro de `applySessionResult`. Se invoca UNA VEZ al boot (`main.js`), tras `loadState` y `loadContent`. Ver Pitfall #10 en 02-RESEARCH.md."
    - Documentar que `clearedExerciseIds` se PRESERVA por D-40 — los aciertos previos siguen contando para futuras sesiones que cubran el ejercicio nuevo.

    Header del archivo: actualizar sección "Decisiones aplicadas" añadiendo D-40 (boot regression).

    Añadir los 4 tests del `<behavior>` al archivo `tests/domain-progress.test.js` existente (creado en Plan 02-01 Task 2) bajo un nuevo `describe('domain/progress — DOMAIN-06 applyNewExerciseRegression (D-53.3b)', ...)`. Reusar `blankStateV2`, `makeContent`, `setupHechaState` de `tests/util/test-helpers.js`.

    NO modificar `tests/domain.test.js`. NO modificar `src/main.js` (la integración boot va en Plan 02-03 Task 3 — este plan deja la función pura disponible).
  </action>

  <verify>
    <automated>
      cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/*.test.js 2&gt;&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "^export function applyNewExerciseRegression" src/domain/progress.js` devuelve match con firma `(state, content)`.
    - `grep -nE "clearedExerciseIds.*NO se vacía|preserved|preservado|D-40" src/domain/progress.js` devuelve ≥1 match (documentación inline crítica).
    - `node --test tests/domain-progress.test.js` exits 0 con los tests anteriores (Plan 02-01) + ≥4 tests nuevos pasando (totalizando ≥16 tests en el archivo).
    - `node --test tests/*.test.js` exits 0 (suite completa).
    - Test "regresión hecha→no-hecha con cleared preservado" verifica explícitamente `assert.deepEqual(result.categoryProgress.avere.clearedExerciseIds, ['a1','a2'])`.
    - Test "regresión dominada→no-hecha con becameDominadaAt borrado" verifica `assert.equal(result.categoryProgress.avere.becameDominadaAt, undefined)`.
    - `grep -nE "applyNewExerciseRegression" src/domain/progress.js` muestra que la función está DEFINIDA SEPARADA, NO dentro de `applySessionResult` (verificar grepeando "function applyNewExerciseRegression" como declaración top-level export, no nested).
    - Header de `src/domain/progress.js` contiene literal "D-40" (grep).
  </acceptance_criteria>

  <done>
    DOMAIN-06 implementado como función separada exportada en `progress.js`. Tests verdes. La función queda lista para que Plan 02-03 la invoque en `main.js` al boot. NO se ha tocado UI ni `main.js`.
  </done>
</task>

</tasks>

<verification>
  - **Comando único de verificación:** `node --test tests/*.test.js` exits 0 con ≥32 tests pasando (14 Phase 1 que sobreviven + 6 data-storage + 12+ domain-progress Plan 02-01 + 4 domain-progress DOMAIN-06 + 6+ domain-session).
  - **Sin browser, sin UAT:** este plan es 100% domain puro. Validación 100% automatizada.
  - **Determinismo statistical test:** el test de ratio weighted sampling debe pasar reproducible — si flakea, ampliar tolerancia (no es una métrica científica, es defensive check del weight cap).
</verification>

<success_criteria>
  - Sampler completo: GUARANTEE phase cubre cada categoría seleccionada con ≥1 ejercicio cuando hay candidates; `buildFullTest` es determinista con seed.
  - DOMAIN-06 implementado como `applyNewExerciseRegression` función exportada separada; documentación explícita de "NO va dentro de applySessionResult" (Pitfall #10).
  - `clearedExerciseIds` se PRESERVA cuando categoría regresa por ejercicio nuevo (D-40 invariante crítico verificado por test).
  - Tests verdes: ≥32 tests pasando en `node --test tests/*.test.js`.
  - Layer purity preservada: ningún módulo del dominio importa de `screens/`, `data/storage.js`, `main.js`.
  - Phase 1 tests sobre `buildSession` siguen verdes (firma preservada — solo el cuerpo creció).
</success_criteria>

<output>
Crear `.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-02-SUMMARY.md` al terminar. Documentar:
- GUARANTEE phase implementada + cómo coexiste con FILL phase de Phase 1.
- `buildFullTest` nueva export — caso de uso "Test completo" del picker que Plan 02-03 invocará.
- `applyNewExerciseRegression` como función SEPARADA — Plan 02-03 la invocará en `main.js` boot (snippet de integración mostrado para el ejecutor del Plan 02-03).
- Lista de tests añadidos.
- Total de tests verdes en la suite.
</output>
