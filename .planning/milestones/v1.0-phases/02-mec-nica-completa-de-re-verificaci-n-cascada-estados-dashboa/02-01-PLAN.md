---
phase: 02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/storage.js
  - src/domain/progress.js
  - tests/domain-progress.test.js
  - tests/util/test-helpers.js
autonomous: true
mvp_mode: true
mvp_slice_kind: domain-foundation
requirements:
  - DOMAIN-04
  - DOMAIN-05
  - DOMAIN-07
  - DOMAIN-08
  - DOMAIN-10
user_setup: []

must_haves:
  truths:
    - "`tests/domain-progress.test.js` corre con `node --test` y todos los tests pasan (≥10 tests nuevos cubriendo cascada, promociones, racha guard, simulación 21 días)"
    - "`applySessionResult(state, sessionResult, content, today)` con firma extendida es PURA (no muta input, devuelve nuevo state)"
    - "Categoría `no-hecha` que cubre todos sus ejercicios sin fallar en una sesión promociona a `hecha` con `streakDays = 1`, `becameHechaAt = today`, `lastSuccessDate = today`"
    - "Categoría `hecha` tocada sin fallar en 21 días consecutivos distintos promociona a `dominada` (test simulación 21 días verde)"
    - "Categoría con cualquier fallo en la sesión resetea: `status='no-hecha'`, `clearedExerciseIds=[]`, `streakDays=0`, `becameHechaAt=undefined`, `becameDominadaAt=undefined`"
    - "Cascada multi-categoría: ejercicio fallado con `categoryIds=['avere','genero']` resetea AMBAS categorías"
    - "`exerciseStats` sigue siendo monotónico (DOMAIN-09 preservado): timesShown/timesCorrect/timesFailed nunca decrecen"
    - "`lastSuccessDate` guard: 5 sesiones el mismo día tocando una categoría sin fallar incrementan `streakDays` UNA SOLA VEZ"
    - "`storage.js` migra automáticamente schemaVersion 1 → 2 al cargar: añade `categoryProgress: {}`, `dailyLog: {}`, preserva `exerciseStats`"
    - "`blankState()` devuelve shape v2 completo: `{schemaVersion: 2, exerciseStats: {}, categoryProgress: {}, dailyLog: {}}`"
  artifacts:
    - path: "src/data/storage.js"
      provides: "Migración v1→v2 + blankState v2 + hydrateV2 defensivo"
      contains: "migrate1to2"
    - path: "src/domain/progress.js"
      provides: "applySessionResult con firma (state, sessionResult, content, today); cascada fail-wins; promoción no-hecha→hecha→dominada; racha con lastSuccessDate guard"
      contains: "applySessionResult"
      min_lines: 100
    - path: "tests/domain-progress.test.js"
      provides: "≥10 tests cubriendo D-53.1 (cascada multi-cat), D-53.2 (promoción y regresión), D-53.3 (racha guard)"
      contains: "applySessionResult"
    - path: "tests/util/test-helpers.js"
      provides: "Helpers compartidos: blankStateV2(), makeContent(exerciseSpecs), setupHechaState()"
  key_links:
    - from: "src/domain/progress.js"
      to: "src/data/storage.js"
      via: "blankState v2 + applySessionResult escriben el mismo shape v2"
      pattern: "schemaVersion.*2"
    - from: "tests/domain-progress.test.js"
      to: "src/domain/progress.js"
      via: "import applySessionResult"
      pattern: "import.*applySessionResult.*progress"
---

<objective>
Establecer el **motor puro del dominio** que materializa la mecánica completa de re-verificación. Sin UI, sin storage I/O en este plan (más allá de actualizar blankState v2 + migración). Phase 1 deja `applySessionResult` reducido a "actualiza contadores"; este plan lo convierte en la máquina de estados completa (cascada fail-wins + promociones + racha 21 días + dailyLog), con tests exhaustivos que simulan ≥21 días continuos.

Purpose: Si el dominio es correcto, las pantallas (Plans 02-03/04) son render condicional sobre un state confiable. Si el dominio falla, la UI pinta estados inválidos durante días sin que se note. Por eso **el dominio va primero, con tests verdes**.

Output:
- `src/domain/progress.js` con `applySessionResult` extendido (firma rota intencionalmente: añade `content` y `today` por D-52) y nuevos helpers (`blankCategoryProgress`, `buildExercisesByCategory`, `uniqueStrings`).
- `src/data/storage.js` con `blankState v2` + rama `migrate1to2` + `hydrateV2` defensivo.
- `tests/domain-progress.test.js` con suite split desde `domain.test.js` (los 3 tests de Phase 1 sobre `applySessionResult` se actualizan a la nueva firma) cubriendo D-53.1, D-53.2, D-53.3.
- `tests/util/test-helpers.js` con helpers compartidos.

**Slice vertical observable:** al final del plan, `node --test tests/*.test.js` pasa con ~25-28 tests verdes; un usuario puede `console.log(applySessionResult(blankState(), {answers:[...]}, content, '2026-05-23'))` en un REPL y ver la cascada funcionar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md
@.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md

<!-- Phase 1 archivos que este plan extiende -->
@src/domain/progress.js
@src/data/storage.js
@tests/domain.test.js
@tests/util/seeded-rng.js

<interfaces>
<!-- Contratos clave heredados de Phase 1 que este plan respeta -->

Phase 1 actual (a EXTENDER, NO eliminar firma):

```js
// src/domain/progress.js
export function applySessionResult(state, sessionResult) { ... }
// Phase 2 cambia a:
export function applySessionResult(state, sessionResult, content, today) { ... }
```

```js
// src/data/storage.js — actual Phase 1
const CURRENT_SCHEMA_VERSION = 1;
export function blankState() { return { schemaVersion: 1, exerciseStats: {} }; }
export function loadState() { ... }   // wrapper try/catch → migrate(parsed)
export function saveState(state) { ... }
function migrate(parsed) { ... }      // EXTENDER con migrate1to2 + hydrateV2
```

Shape v2 final (D-41/D-46):
```ts
{
  schemaVersion: 2,
  exerciseStats: Record<exerciseId, {timesShown:number, timesCorrect:number, timesFailed:number}>,
  categoryProgress: Record<categoryId, {
    status: 'no-hecha' | 'hecha' | 'dominada',
    clearedExerciseIds: string[],
    streakDays: number,
    lastPracticedDate?: string,        // ISO YYYY-MM-DD
    lastSuccessDate?: string,
    becameHechaAt?: string,
    becameDominadaAt?: string
  }>,
  dailyLog: Record<isoDate, {date:string, categoriesPracticed:string[], categoriesWithFailure:string[]}>,
  inFlightTest?: { ... }               // poblado en Plan 02-03
}
```

`content` shape (lo que Plan 01-01 produce desde `loadContent`):
```ts
{ categories: Array<{id, name, order}>, exerciseById: Record<exerciseId, {id, type, categoryIds, payload}> }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Storage v2 — blankState + migrate1to2 + hydrateV2</name>
  <files>src/data/storage.js, tests/util/test-helpers.js, tests/data-storage.test.js</files>

  <read_first>
    - src/data/storage.js (líneas 1-109 íntegras — estado actual Phase 1 con `CURRENT_SCHEMA_VERSION = 1`, `blankState`, `loadState`, `saveState`, `migrate`)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md secciones "D-41", "D-46", "D-47" (migración 1→2, shape v2, hydratación lazy)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Migración schemaVersion 1 → 2 (HIGH)" — código canónico de `migrate1to2` y `hydrateV2`
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md sección "src/data/storage.js (DATA, request-response) — MODIFICADO" (deltas concretos)
    - tests/util/seeded-rng.js (estilo de helpers test, sin cambios)
  </read_first>

  <behavior>
    - Test 1: `blankState()` devuelve `{schemaVersion: 2, exerciseStats: {}, categoryProgress: {}, dailyLog: {}}` (clave `inFlightTest` omitida — undefined).
    - Test 2: `migrate1to2({schemaVersion: 1, exerciseStats: {a1: {timesShown: 5, timesCorrect: 3, timesFailed: 2}}})` produce shape v2 con `exerciseStats` preservado íntegro y `categoryProgress: {}`, `dailyLog: {}`.
    - Test 3: `migrate1to2({schemaVersion: 1, exerciseStats: null})` (input defensivo malformado) produce shape v2 con `exerciseStats: {}` (no crashea, defensivo).
    - Test 4: `hydrateV2({schemaVersion: 2, exerciseStats: {a1:{timesShown:1}}, categoryProgress: {avere:{status:'hecha'}}, dailyLog: {'2026-05-23':{...}}, inFlightTest: {cursor: 5, ...}})` preserva los 4 sub-objetos íntegros sin reset.
    - Test 5: `hydrateV2({schemaVersion: 2, categoryProgress: null})` (campo malformado) defaults a `categoryProgress: {}` sin crashear (defensivo en el espíritu de Phase 1).
    - Test 6: La constante `CURRENT_SCHEMA_VERSION` exportada (o detectable vía blankState) es `2`, no `1`.
  </behavior>

  <action>
    Extender `src/data/storage.js` siguiendo el patrón "schemaVersion-based migrate(parsed) en cadena" documentado en 02-PATTERNS.md "Shared Patterns":

    1. Cambiar `CURRENT_SCHEMA_VERSION` de `1` a `2`. La constante `KEY` se mantiene en `'italianCourse.v1'` — la KEY de localStorage NO cambia (esto es deliberado por D-41; el nombre histórico se conserva para no perder estado existente).

    2. Actualizar `blankState()` para devolver el shape v2 completo: `{schemaVersion: 2, exerciseStats: {}, categoryProgress: {}, dailyLog: {}}`. NO añadir `inFlightTest` (se omite undefined; JSON.stringify lo elide al persistir).

    3. Refactorizar la función privada `migrate(parsed)` para enrutar por versión exactamente como el código canónico de 02-RESEARCH.md "Migración schemaVersion 1 → 2 (HIGH)":
       - Branch `schemaVersion === 2` → `hydrateV2(parsed)` (función nueva, defensiva como en Phase 1)
       - Branch `schemaVersion === 1` → `migrate1to2(parsed)` (función nueva, conserva `exerciseStats`)
       - Default (desconocido) → `console.warn` + `blankState()` (preservar branch defensivo de Phase 1)

    4. Implementar `migrate1to2(v1)`: spread shallow del shape v2 con `exerciseStats` preservado vía type-guard `(typeof v1.exerciseStats === 'object' && v1.exerciseStats !== null) ? v1.exerciseStats : {}`. `categoryProgress: {}` y `dailyLog: {}` se inicializan vacíos — la nota documentada en 02-RESEARCH.md "Hidratación retroactiva" justifica que NO intentamos reconstruir estado v1 (sería imposible).

    5. Implementar `hydrateV2(parsed)`: type-guard cada sub-objeto del shape v2 con el mismo patrón defensivo de Phase 1 (`(typeof X === 'object' && X !== null) ? X : {}`). `inFlightTest` permitido como undefined u objeto literal sin type-check estricto (validación profunda es responsabilidad del consumidor).

    6. Header del archivo: actualizar la sección "Decisiones aplicadas" añadiendo referencias D-41, D-46, D-47 y la nota "inFlightTest vive en el mismo blob que el resto del state, NO en una key separada" (per 02-PATTERNS.md).

    7. Crear `tests/util/test-helpers.js` con helpers compartidos entre suites (este archivo lo extenderán también las Tasks 2 y 3):
       - `blankStateV2()` — clon literal del shape v2 esperado (para tests que no importan `storage.js` por su dependencia de localStorage).
       - `makeContent(exerciseSpecs)` — toma `Array<{id, categoryIds, type?, payload?}>` y devuelve `{categories, exerciseById}` para inyectar a `applySessionResult`. Defaults razonables para `type` (`'multiple-choice'`) y `payload` (objeto vacío). `categories` se deriva inferida del set único de IDs vistos en `categoryIds`.

    8. Crear `tests/data-storage.test.js` con los 6 tests del bloque `<behavior>` arriba usando `node:test` y `node:assert/strict`. Este archivo importa `migrate1to2`, `hydrateV2` y `blankState` desde `storage.js`. **Importante:** dado que `storage.js` toca `localStorage` (que no existe en Node), los tests SOLO importan funciones puras (`migrate1to2`, `hydrateV2`, `blankState`). Para que sean importables, márcalos como `export` (actualmente son privadas — promover a `export` con marca de "test-only export"). El header del archivo aclara que estas exports son por testabilidad y NO se usan desde el resto del código de producción.

    Mensajes en español (FOUND-04). NO añadir librerías. NO instalar nada. NO romper la firma de `loadState`/`saveState`.
  </action>

  <verify>
    <automated>
      cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/data-storage.test.js 2&gt;&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "CURRENT_SCHEMA_VERSION = 2" src/data/storage.js` devuelve match exacto en una línea.
    - `grep -nE "^export (function|const) (blankState|migrate1to2|hydrateV2)" src/data/storage.js` devuelve 3 matches: `blankState`, `migrate1to2`, `hydrateV2`.
    - `grep -n "categoryProgress" src/data/storage.js | grep -v "^[[:space:]]*//"` devuelve ≥3 matches (en blankState, migrate1to2, hydrateV2).
    - `node --test tests/data-storage.test.js` exits 0 con 6 tests pasando.
    - `node --test tests/*.test.js` exits 0 (verifica que NO se rompe la suite Phase 1 — el plan 02-01 NO toca tests/domain.test.js todavía; Task 3 los actualiza).
    - Header de `src/data/storage.js` contiene referencias literales "D-41", "D-46", "D-47" (grep -n).
    - File `tests/util/test-helpers.js` existe con `export function blankStateV2` y `export function makeContent` (grep).
  </acceptance_criteria>

  <done>
    Storage v2 funciona. `blankState()` produce v2. `migrate1to2` y `hydrateV2` están exportados y testeados. `loadState`/`saveState` siguen funcionando con el shape ampliado (no se han tocado estructuralmente). Tests verdes. NO se ha tocado todavía `src/domain/progress.js`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applySessionResult extendido — cascada + promociones + racha guard + dailyLog</name>
  <files>src/domain/progress.js, tests/domain-progress.test.js, tests/util/test-helpers.js</files>

  <read_first>
    - src/domain/progress.js (líneas 1-47 íntegras — `applySessionResult` Phase 1 con spread-clone defensivo)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-CONTEXT.md secciones "D-38", "D-39", "D-47", "D-48", "D-52", "D-53"
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Algoritmo applySessionResult (HIGH)" íntegra (firma, pseudocódigo canónico, invariantes críticos, 12 casos límite, "Por qué NO se vacía clearedExerciseIds")
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Testing strategy con `node --test` (HIGH)" (patrón `today` inyectado, simulación 21 días, tests parametrizados)
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-PATTERNS.md sección "src/domain/progress.js (DOMAIN, pure, state machine) — MODIFICADO"
    - .planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-RESEARCH.md sección "Common Pitfalls" #2 (firma rota), #10 (DOMAIN-06 en boot, NO dentro de applySessionResult)
    - src/data/storage.js (ya extendido en Task 1) — para confirmar el shape v2 que `applySessionResult` debe respetar
    - tests/util/test-helpers.js (creado en Task 1) — para reusar `blankStateV2`, `makeContent`
    - tests/domain.test.js líneas 96-128 (los 2 tests Phase 1 sobre applySessionResult que esta task DEBE actualizar a la nueva firma para no romper la suite)
  </read_first>

  <behavior>
    Tests obligatorios en `tests/domain-progress.test.js` (siguen el split documentado en 02-RESEARCH.md "Testing strategy"). Mínimo **12 tests** repartidos en 3 grupos `describe`:

    **Grupo D-53.1 — Cascada multi-cat (3 tests):**
    - Test: ejercicio `[avere, genero]` fallado tras acierto previo `[avere]` en la MISMA sesión → `clearedExerciseIds[avere]=[]` Y `clearedExerciseIds[genero]=[]` (aciertos previos descartados); `streakDays=0` ambas.
    - Test: sesión con 1 fallo `[avere]` y 5 aciertos `[avere]` → avere resetea por fail-wins absoluto (los 5 aciertos se descartan).
    - Test: ejercicio fallado `[avere]` NO afecta a `[genero]` ajena que se acertó en la misma sesión (genero sigue acumulando aciertos en `clearedExerciseIds`).

    **Grupo D-53.2 — Promoción y regresión completas (4 tests):**
    - Test: categoría `no-hecha` con pool 3 ejercicios, sesión acierta los 3 sin fallar → promoción a `hecha`, `becameHechaAt=today`, `streakDays=1`, `lastSuccessDate=today`, `clearedExerciseIds.length===3` (NO se vacía tras promocionar — D-39 paso 4c).
    - Test: secuencia de 3 sesiones acumulando aciertos progresivamente → promoción a `hecha` SOLO en la sesión que cubre el último ejercicio (no antes).
    - Test: **simulación 21 días** — empezar en `no-hecha`, día 1 promociona a `hecha` con streak=1; días 2..21 incrementan streak (un día por iteración con ISO string distinto); en día 21 promociona a `dominada` con `becameDominadaAt='2026-06-21'` y `streakDays=21`.
    - Test: tras `dominada` con streak=25, sesión con fallo → regresión total a `no-hecha`: `streakDays=0`, `becameDominadaAt=undefined`, `becameHechaAt=undefined`, `clearedExerciseIds=[]`.

    **Grupo D-53.3 — Racha guard + edge cases (≥5 tests):**
    - Test: 5 sesiones el MISMO día (`today` constante) tocando una categoría `hecha` sin fallar → `streakDays` incrementa UNA SOLA VEZ (guard `lastSuccessDate !== today`).
    - Test: `applySessionResult` con `answers: []` (sesión vacía) → estado devuelto es funcionalmente equivalente al input (NO crashea, no muta).
    - Test: `applySessionResult` con ejercicio cuyo `categoryIds` está vacío `[]` → `exerciseStats` se actualiza igualmente, `categoryProgress` no se toca para categorías inexistentes.
    - Test: `dailyLog[today]` se popula correctamente con `{date: today, categoriesPracticed: [...], categoriesWithFailure: [...]}` tras una sesión (D-48 shape).
    - Test: invocaciones múltiples el mismo día acumulan en `dailyLog[today]` (idempotencia con uniqueStrings) — segundo call con misma categoría no duplica en `categoriesPracticed`.
    - Test: pureza — input `state` NO se muta tras `applySessionResult` (`assert.deepEqual(stateAntes, stateOriginalClonado)`).
    - Test: `exerciseStats` monotónico — un fallo NO decrementa `timesCorrect`; solo incrementa `timesShown` y `timesFailed` (preservar DOMAIN-09 de Phase 1).

    **Actualización Phase 1 tests:** los 2 tests existentes en `tests/domain.test.js` líneas 96-128 (sobre `applySessionResult`) DEBEN actualizarse a la nueva firma `(state, sessionResult, content, today)` o moverse a `tests/domain-progress.test.js`. Decisión: **moverlos** al nuevo archivo (split documentado en RESEARCH.md). El `tests/domain.test.js` mantiene los tests de `dates`, `session`, `schema-validator`, `multiple-choice`, `registry`.
  </behavior>

  <action>
    Reescribir `src/domain/progress.js` derivando línea por línea del pseudocódigo canónico de 02-RESEARCH.md "Algoritmo applySessionResult (HIGH)". La firma cambia de `applySessionResult(state, sessionResult)` (Phase 1) a `applySessionResult(state, sessionResult, content, today)` por D-52.

    Estructura interna obligatoria (los 6 pasos del pseudocódigo, en este orden):

    1. **Paso 0 — Pre-cómputo:** invocar helper privado nuevo `buildExercisesByCategory(content)` que devuelve `Record<categoryId, exerciseId[]>` iterando `Object.values(content.exerciseById)`.

    2. **Paso 1 — Spread-clone defensivo:** clonar shallow `state` + sub-objetos `exerciseStats`, `categoryProgress`, `dailyLog`. Patrón calcado del Phase 1 actual (líneas 27-30 de `progress.js`).

    3. **Paso 2 — Conjuntos auxiliares:** computar `failedExerciseIds`, `correctExerciseIds`, `failedCategoryIds` (Set), `practicedCategoryIds` (Set) usando `content.exerciseById[eid]?.categoryIds`. Defensivo con `?? []` cuando exerciseId no exista.

    4. **Paso 3 — `exerciseStats` monotónico:** bucle por `answers` actualizando `timesShown`, `timesCorrect`, `timesFailed`. Este paso CORRE SIEMPRE, antes y al margen del branch fail/correct de la categoría (preservar DOMAIN-09 — invariante crítico #3 de RESEARCH.md).

    5. **Paso 4 — Bucle por `practicedCategoryIds`** con dos ramas:
       - **Branch FAIL-WINS** (si `failedCategoryIds.has(catId)`): reset total — `status='no-hecha'`, `clearedExerciseIds=[]`, `streakDays=0`, `becameHechaAt=undefined`, `becameDominadaAt=undefined`. `lastSuccessDate` se preserva (decisión documentada en pseudocódigo comentario).
       - **Branch PROMOTION** (sin fallo): añadir aciertos a `clearedExerciseIds` con `uniqueStrings`, computar `isCovered` (`allInCat.every(id => cat.clearedExerciseIds.includes(id))`), aplicar la lógica de promoción de D-38 según los 3 casos A/B/C del pseudocódigo:
         - Case A: `status==='no-hecha' && isCovered` → promociona a `hecha`, `streakDays=1`, `becameHechaAt=today`, `lastSuccessDate=today`.
         - Case B: `status==='hecha'||'dominada'` y `lastSuccessDate !== today` → `streakDays += 1`, `lastSuccessDate = today`. Luego, si `streakDays >= 21 && status !== 'dominada'` → promociona a `dominada`, `becameDominadaAt = today`.
         - Edge: `status==='no-hecha' && !isCovered` (sin fallo, no cubierta) → solo añade a cleared, NO incrementa streak ni promociona (per D-38 línea 1).

       Setear `lastPracticedDate = today` SIEMPRE (paso 4d, para todas las tocadas en fail o no).

    6. **Paso 5 — `dailyLog[today]`:** merge con entrada previa si existe (`categoriesPracticed` y `categoriesWithFailure` se acumulan con `uniqueStrings`, D-48 shape).

    7. **Paso 6 — Limpiar `inFlightTest`:** `delete next.inFlightTest` (Pitfall #9 — el caller asume idempotencia y NO debería relancer la cascada con el blob viejo).

    Helpers privados a añadir en el mismo archivo (NO exportar a menos que se reusen en otro módulo del dominio):
    - `blankCategoryProgress()` — devuelve el shape exacto de D-47.
    - `buildExercisesByCategory(content)` — derivación pura desde `content.exerciseById`.
    - `uniqueStrings(arr)` — `[...new Set(arr)]`.

    Header del archivo: actualizar JSDoc + sección "Decisiones aplicadas" con D-38, D-39, D-47, D-48, D-52. **Documentar EXPLÍCITAMENTE en el JSDoc de `applySessionResult`** que la firma rompe Phase 1 intencionalmente (Pitfall #2 de RESEARCH.md) y que `content`/`today` son obligatorios; el caller en `src/screens/app.js` (Plan 02-03) los suministra.

    **No incluir `applyNewExerciseRegression` aquí** — esa export es Plan 02-02 (DOMAIN-06 corre solo en boot, no en `applySessionResult`; Pitfall #10).

    Crear `tests/domain-progress.test.js` con los ≥12 tests del bloque `<behavior>` arriba. Import pattern de 02-PATTERNS.md (sección "tests/domain.test.js — MODIFICADO"):

    ```
    import { test, describe } from 'node:test';
    import assert from 'node:assert/strict';
    import { applySessionResult } from '../src/domain/progress.js';
    import { blankStateV2, makeContent } from './util/test-helpers.js';
    ```

    **Importante para el test "simulación 21 días":** generar las 21 fechas ISO de forma determinista (`'2026-06-01'`, `'2026-06-02'`, ..., `'2026-06-21'`) con `String(day+1).padStart(2, '0')`, NO usar `new Date()` mock global. Inyectar `today` como string ISO al `applySessionResult` (D-52, el patrón canónico).

    Para "regresión tras fallo desde dominada": construir manualmente un state inicial con `categoryProgress.avere = {status:'dominada', streakDays:25, becameDominadaAt:'2026-05-01', ...}` (no es necesario simular los 25 días previos — el invariante a probar es la transición, no el camino).

    Mover los 2 tests `applySessionResult` de `tests/domain.test.js` líneas 96-128 a `tests/domain-progress.test.js`, actualizándolos a la nueva firma: el `before` del test ahora es `blankStateV2()` (no `{schemaVersion:1, exerciseStats:{}}`), y la llamada pasa `content` y `today`. Eliminar el `describe('domain/progress', ...)` íntegro del antiguo `tests/domain.test.js` (el archivo Phase 1 deja de tener el bloque progress).

    Helper en `tests/util/test-helpers.js` a añadir si no existe ya:
    - `setupHechaState(catId, exerciseIds)` — devuelve state v2 con `categoryProgress[catId].status='hecha'`, `clearedExerciseIds=exerciseIds`, `streakDays=5`. Útil para tests que parten de "ya está hecha". Documentar con JSDoc.
  </action>

  <verify>
    <automated>
      cd /home/vcompanyb/italian-course &amp;&amp; node --test tests/*.test.js 2&gt;&amp;1 | tail -30
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE "^export function applySessionResult" src/domain/progress.js` devuelve match con 4 parámetros (`state, sessionResult, content, today`).
    - `grep -cE "^(function|const) (blankCategoryProgress|buildExercisesByCategory|uniqueStrings)" src/domain/progress.js` devuelve 3 (helpers privados definidos).
    - `grep -nE "delete next\.inFlightTest" src/domain/progress.js` devuelve ≥1 match (paso 6 del pseudocódigo).
    - `grep -nE "lastSuccessDate !== today" src/domain/progress.js` devuelve ≥1 match (streak guard implementado).
    - `grep -cnE "streakDays.*21" src/domain/progress.js` devuelve ≥1 match (promoción a dominada).
    - `node --test tests/domain-progress.test.js` exits 0 con ≥12 tests pasando.
    - `node --test tests/*.test.js` exits 0 (full suite — ningún test Phase 1 roto; los 2 tests viejos de progress ahora viven en domain-progress.test.js).
    - El test "21 días simulados promocionan a dominada" pasa con `state.categoryProgress.avere.becameDominadaAt === '2026-06-21'` y `streakDays === 21`.
    - El test "5 sesiones mismo día → streak +=1 una vez" pasa.
    - `grep -nE "applySessionResult" tests/domain.test.js` devuelve 0 matches (los tests viejos se movieron — preservar isolation por archivo de la dependency graph).
  </acceptance_criteria>

  <done>
    `applySessionResult` es la máquina de estados completa: cascada fail-wins, promociones no-hecha→hecha→dominada, racha 21 días con `lastSuccessDate` guard, `dailyLog` poblado, `inFlightTest` limpiado. Todos los tests verdes incluyendo los 12+ nuevos. La suite Phase 1 sigue verde tras mover los 2 tests viejos. NO se ha tocado la UI ni `main.js`.
  </done>
</task>

</tasks>

<verification>
  - **Comando único de verificación:** `node --test tests/*.test.js` exits 0 con ≥25 tests pasando (14 Phase 1 que sobreviven en `domain.test.js` + 6 nuevos en `data-storage.test.js` + 12+ nuevos en `domain-progress.test.js`).
  - **Sin browser, sin UAT, sin localStorage funcional:** este plan NO toca UI. La validación es 100% automatizada vía `node --test`.
  - **Determinismo:** cualquier reejecución de la suite produce el mismo resultado verde (cero flakiness, sin Date.now() directo en el dominio — `today` siempre se inyecta).
</verification>

<success_criteria>
  - Dominio puro v2 funciona: cascada multi-categoría, promociones encadenadas, racha 21 días simulada, regresión tras fallo, dailyLog poblado.
  - Migración storage v1→v2 transparente: un state v1 cargado se convierte automáticamente a v2 con `exerciseStats` preservado.
  - Tests verdes: ≥25 tests pasando en `node --test tests/*.test.js`.
  - Layer purity preservada: ningún módulo del dominio importa de `screens/`, `data/storage.js`, `main.js`, `document`, `localStorage`, `fetch`.
  - Firma rota de `applySessionResult` documentada en JSDoc y SUMMARY (Pitfall #2 de RESEARCH.md mitigado).
  - Helpers de test reusables en `tests/util/test-helpers.js` para Plans 02-02 y 02-04.
</success_criteria>

<output>
Crear `.planning/phases/02-mec-nica-completa-de-re-verificaci-n-cascada-estados-dashboa/02-01-SUMMARY.md` al terminar. Documentar:
- Cambio de firma `applySessionResult` (rompe Phase 1, ahora exige `content` y `today`).
- Shape v2 final del state y la cadena de migración.
- Lista de tests nuevos por grupo D-53.
- Cómo Plan 02-02 (sampler) y Plan 02-03 (UI) consumirán estos contratos.
</output>
