---
phase: 07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/schema-validator.js
  - index.html
  - styles.css
  - content/exercises/preposiciones.json
  - tests/exercise-types.test.js
requirements: [EXPL-01, EXPL-02, EXPL-03]
autonomous: false

must_haves:
  truths:
    - "Al fallar un ejercicio con `payload.explanation`, el autor ve un párrafo italic muted bajo la línea 'Respuesta correcta:' durante el feedback rojo de la sesión (los 3 tipos: multi-choice / word-buttons / match)."
    - "Al terminar una sesión con ≥1 fallo sobre un ejercicio con explanation, el autor ve la misma explanation bajo la fila correspondiente en la sección 'Errores cometidos' del summary."
    - "Si un ejercicio NO tiene explanation, no se renderiza ningún placeholder ni mensaje en sesión ni en summary (graceful degradation — D-121)."
    - "El schema validator acepta `payload.explanation` ausente (back-compat con los 271 ejercicios actuales), acepta string no vacío, y rechaza no-string o string vacío con error visible en banner CONT-05."
    - "El boot post-Plan-07-01 carga las 7 categorías sin errores en el banner (back-compat: ningún ejercicio existente queda en mal estado por la nueva regla)."
    - "Los apóstrofes en explanations son ASCII U+0027 (CONT-06); el texto se renderiza con `x-text` exclusivo (T-02-01 — markdown literal no se interpreta)."
  artifacts:
    - path: src/data/schema-validator.js
      provides: "3 reglas if-explanation-then-string-no-vacío añadidas dentro de validateMultipleChoicePayload, validateWordButtonsPayload, validateMatchPayload"
      contains: "payload.explanation"
    - path: index.html
      provides: "3 bloques `<p class=\"session-explanation\">` en sub-templates multi-choice/word-buttons/match + 1 bloque `<p class=\"summary-error-explanation\">` en summary-errors"
      contains: "session-explanation"
    - path: styles.css
      provides: "2 reglas CSS `.session-explanation` y `.summary-error-explanation` (italic, muted, font-size ~0.9em, márgenes 4px múltiplos)"
      contains: ".session-explanation"
    - path: content/exercises/preposiciones.json
      provides: "2 seed explanations en ejercicios reales (preposiciones-001 'di Roma' + preposiciones-006 'sul tavolo' — uno tema simple + uno articolata, los 2 más representativos del PDF)"
      contains: "\"explanation\":"
    - path: tests/exercise-types.test.js
      provides: "3 sub-tests dentro de los describe blocks existentes (multi-choice / word-buttons / match) verificando explanation válida/no-string/ausente. Mínimo 9 tests nuevos (3 reglas × 3 tipos) o un bloque describe común parametrizado."
      contains: "explanation"
  key_links:
    - from: "src/data/schema-validator.js validateMultipleChoicePayload"
      to: "ex.payload.explanation"
      via: "if (payload.explanation !== undefined) check string && trim"
      pattern: "payload\\.explanation !== undefined"
    - from: "index.html sub-template session multi-choice (línea ~275)"
      to: "sessionCurrentExercise.payload.explanation"
      via: "x-show + x-text Alpine bindings"
      pattern: "session-explanation"
    - from: "index.html summary-errors section (línea ~538)"
      to: "content.exerciseById[result.exerciseId].payload.explanation"
      via: "x-show optional chaining + x-text"
      pattern: "summary-error-explanation"
    - from: "styles.css"
      to: "--pico-muted-color"
      via: "var() reuse — cero tokens nuevos"
      pattern: "pico-muted-color"
---

<objective>
Entregar la infraestructura completa para mostrar explicaciones pedagógicas opcionales al fallar un ejercicio, en sesión (inline durante feedback rojo) y en summary ("Errores cometidos"). Vertical slice end-to-end con 2 seeds reales en Preposiciones para que el UAT humano sea visible y verificable. Las 48 explanations restantes se entregan en Plan 07-02.

Purpose: cerrar el dolor capturado en discuss-phase — el autor consulta Gemini cada vez que falla preposiciones (4 ejemplos: sulle, da lui, dalle, sui). Tras este plan, una explanation curada vive junto al ejercicio y aparece destilada cuando se necesita (post-error).

Output:
- Schema validator extendido con la regla opcional `payload.explanation: string` en los 3 tipos.
- Sub-templates de sesión renderizan la explanation durante feedback rojo (los 3 tipos: multi-choice, word-buttons, match).
- Summary-errors extendida con un `<p>` por error cuando el ejercicio tiene explanation.
- 2 reglas CSS muted/italic alineadas con el UI-SPEC §Spacing/Color/Typography Phase 6 (sin tokens nuevos).
- 2 explanations seed en preposiciones-001 y preposiciones-006 para que el UAT humano vea contenido real.
- Tests schema validator parametrizados sobre los 3 tipos.
- UAT humano de fallo deliberado sobre los 2 seeds: ver explanation inline + en summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-CONTEXT.md
@.planning/phases/06-polish-ux-post-sesion-reiniciar-y-review-errores/06-CONTEXT.md
@.planning/phases/06-polish-ux-post-sesion-reiniciar-y-review-errores/06-UI-SPEC.md

@src/data/schema-validator.js
@index.html
@styles.css
@content/exercises/preposiciones.json
@tests/exercise-types.test.js

<interfaces>
<!-- Identifiers que el executor necesita sin explorar el codebase. -->

From src/data/schema-validator.js (líneas 28-35):
- `PAYLOAD_VALIDATORS = { 'multiple-choice': validateMultipleChoicePayload, 'word-buttons': validateWordButtonsPayload, 'match': validateMatchPayload }` — dispatch table cerrada (Phase 3 D-65)
- `validateContent({ categories, exercisesByFile }) → { ok, errors: [{file, exerciseId, reason}] }` — entry point
- `push(file, exerciseId, reason)` — callback para acumular errores SIN throw (D-08)
- Cada validateXxxPayload firma: `(ex, file, push) → void` (no return; usa push para reportar)

From src/data/schema-validator.js (líneas 154-254):
- validateMultipleChoicePayload destructura `{ prompt, options, correctIndex }` desde `ex.payload`
- validateWordButtonsPayload destructura `{ prompt, answer, distractors }`
- validateMatchPayload destructura `{ prompt, pairs }`
- Todos tienen el mismo patrón: bloques `if (!check) push(...)` sin early return (D-08)

From index.html (líneas 247-413, post-Phase 6):
- Sub-template multi-choice: `<template x-if="sessionCurrentExercise.type === 'multiple-choice'">` línea 259, `<p x-show="sessionFeedback === 'incorrect'">Respuesta correcta: <strong>...</strong></p>` línea 275-278, `<button x-show="sessionFeedback === 'incorrect'" @click="sessionAdvance">Siguiente</button>` línea 279.
- Sub-template word-buttons: `<template x-if="sessionCurrentExercise.type === 'word-buttons'">` línea 296, `<p x-show="sessionFeedback === 'incorrect'" class="wb-correct-answer">Respuesta correcta: <strong>...</strong></p>` línea 326-329, botón Siguiente dentro de `.button-row` línea 337-339.
- Sub-template match: `<template x-if="sessionCurrentExercise.type === 'match'">` línea 375, `<button type="button" x-show="sessionFeedback === 'incorrect'" @click="sessionAdvance">Siguiente</button>` línea 408-410. NO hay línea "Respuesta correcta:" en match — la captura visual del fallo es el match-flash rojo. La explanation va justo antes del botón Siguiente.
- Summary section summary-errors: `<template x-if="summarySessionResults.some(r => !r.correct && content.exerciseById[r.exerciseId])">` línea 529, `<template x-for="result in summarySessionResults.filter(r => !r.correct && content.exerciseById[r.exerciseId])" :key="result.exerciseId">` línea 538, `<li>` con `<strong x-text="...prompt"></strong>` + 3 sub-templates por type (multi-choice 542-547, word-buttons 549-554, match 556-570).

From src/screens/app.js (sin cambios estructurales):
- `sessionFeedback`: null | 'correct' | 'incorrect' (estado reactivo Alpine)
- `sessionCurrentExercise`: getter Alpine que devuelve `content.exerciseById[sessionExerciseIds[sessionCursor]]` (puede ser null)
- `summarySessionResults`: array snapshot Phase 6 CR-02 fix — set en `completeSession()`, limpiado en `returnToHomeFromSummary()`. Shape: `[{exerciseId, correct, userAnswer}]`
- `content.exerciseById[exerciseId]`: lookup en memoria del ejercicio completo (boot loaded)

From styles.css (líneas 372-414, Phase 6):
- `.summary-errors` (margin-top 1.5rem)
- `.summary-errors h3` (margin-bottom 0.5rem)
- `.summary-errors li` (display flex column, gap 0.25rem, padding 0.5rem 0, border-bottom muted)
- `.summary-errors .user-answer` (background-color var(--pico-color-red-500), white, padding 0 0.25rem)
- Pico tokens disponibles sin import: `--pico-muted-color`, `--pico-muted-border-color`, `--pico-border-radius`

From tests/exercise-types.test.js (estructura existente):
- `import { validateContent } from '../src/data/schema-validator.js';` (línea 22)
- describe blocks por tipo: `'data/schema-validator — word-buttons payload'` (línea 102), `'data/schema-validator — match payload'` (línea 477), multi-choice tests viven antes en el archivo
- Patrón test: construir `{ categories, exercisesByFile }` mínimo, llamar `validateContent`, assertear `result.ok === true/false` y filtrar `result.errors.some(e => /pattern/.test(e.reason))`
</interfaces>

<reference_text>
<!-- Ejemplos canónicos del tono que Claude debe usar para las 2 seed explanations. -->

Tono 3ª persona impersonal, 1 frase de regla + 1 frase de ejemplo paralelo (D-127). Longitud ~250-400 chars / 50-80 palabras (D-128).

Apóstrofes ASCII U+0027 (CONT-06 / D-129) — nunca smart quotes `'`.

Plain text (T-02-01) — sin markdown, sin negritas, sin listas, sin headers.

Ejemplos canónicos del autor (destilados de Gemini, sirven como template tonal):

1. `"Su significa 'sobre' o 'encima de' con contacto físico. Los libros se colocan encima de la superficie de la mesa. Por eso Su + Le (las) = Sulle."`

2. `"En italiano, cuando una acción la realiza un agente en una frase pasiva, la preposición obligatoria es Da. Significa 'por' en el sentido de autoría. Il libro è scritto da lui = El libro está escrito por él."`

3. `"Cuando te desplazas saliendo del lugar de alguien (su casa, por ejemplo), usas la preposición Da combinada con el artículo. Da + Le = Dalle. Vengo dalle mie zie."`
</reference_text>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender PAYLOAD_VALIDATORS con regla `explanation if present must be non-empty string` (D-116) + tests parametrizados</name>
  <files>src/data/schema-validator.js, tests/exercise-types.test.js</files>
  <read_first>
    - `src/data/schema-validator.js` líneas 1-254 ENTERAS (entender el patrón D-08 acumulación de errores + las 3 validateXxxPayload existentes — multi-choice 154-171, word-buttons 188-208, match 226-254). NO releer parcialmente — una sola pasada captura los 3 patrones.
    - `tests/exercise-types.test.js` líneas 1-100 (imports + describe block multi-choice) y 100-200 (describe block word-buttons) — entender el patrón de assertion (`result.ok`, `result.errors.some(e => /regex/.test(e.reason))`).
    - `tests/exercise-types.test.js` líneas 477-700 (describe block match) para mantener el orden de tests por tipo.
  </read_first>
  <action>
    Implementar la regla schema D-116 en las 3 funciones validateXxxPayload de `src/data/schema-validator.js`. Patrón EXACTO para los 3 tipos (multi-choice, word-buttons, match), añadido al FINAL de cada función (después de las validaciones existentes, antes del cierre `}`):

      if (ex.payload.explanation !== undefined) {
        if (typeof ex.payload.explanation !== 'string' || !ex.payload.explanation.trim()) {
          push(file, ex.id, '"payload.explanation" debe ser string no vacío si está presente');
        }
      }

    Justificación de patrón:
    - `!== undefined` (no `!= null`) — distingue ausente (válido, back-compat con 271 ejercicios) de presente-con-null (inválido).
    - `!explanation.trim()` rechaza `""` y `"   "` (whitespace puro).
    - Mensaje en español (FOUND-04) coherente con los mensajes de otros checks del mismo validator.
    - NO usa early return — sigue el invariante D-08 (acumular todos los errores en una pasada).
    - NO valida longitud (D-116/D-128: el límite 250-400 chars es recomendación editorial, no constraint).
    - Cero refactor del patrón dispatch table — añadir un bloque if al final de cada función.

    Implementar tests en `tests/exercise-types.test.js`. AÑADIR un nuevo describe block compartido al FINAL del archivo (después del último describe existente):

      describe('data/schema-validator — payload.explanation (Phase 7 D-116)', () => {
        // Parametrizado sobre los 3 tipos para evitar duplicación.
        const validCategories = [{ id: 'avere', name: 'Avere', order: 1 }];
        const validPayloadByType = {
          'multiple-choice': { prompt: 'Hello ___ world', options: ['a','b','c'], correctIndex: 0 },
          'word-buttons':    { prompt: 'Hello world', answer: ['a','b'] },
          'match':           { prompt: 'Match', pairs: [['a','x'],['b','y']] }
        };

        for (const type of ['multiple-choice', 'word-buttons', 'match']) {
          test(`${type}: accepts exercise WITHOUT explanation (back-compat)`, () => {
            const result = validateContent({
              categories: validCategories,
              exercisesByFile: {
                'avere.json': [{ id: `expl-${type}-omitted`, type, categoryIds: ['avere'], payload: validPayloadByType[type] }]
              }
            });
            assert.equal(result.ok, true, JSON.stringify(result.errors));
          });

          test(`${type}: accepts exercise WITH explanation string`, () => {
            const result = validateContent({
              categories: validCategories,
              exercisesByFile: {
                'avere.json': [{
                  id: `expl-${type}-string`, type, categoryIds: ['avere'],
                  payload: { ...validPayloadByType[type], explanation: 'Esta es una regla didactica.' }
                }]
              }
            });
            assert.equal(result.ok, true, JSON.stringify(result.errors));
          });

          test(`${type}: rejects explanation non-string (number)`, () => {
            const result = validateContent({
              categories: validCategories,
              exercisesByFile: {
                'avere.json': [{
                  id: `expl-${type}-num`, type, categoryIds: ['avere'],
                  payload: { ...validPayloadByType[type], explanation: 42 }
                }]
              }
            });
            assert.equal(result.ok, false);
            assert.ok(
              result.errors.some(e => /explanation/.test(e.reason) && e.exerciseId === `expl-${type}-num`),
              `Esperaba error sobre explanation; errores: ${JSON.stringify(result.errors)}`
            );
          });

          test(`${type}: rejects explanation empty string`, () => {
            const result = validateContent({
              categories: validCategories,
              exercisesByFile: {
                'avere.json': [{
                  id: `expl-${type}-empty`, type, categoryIds: ['avere'],
                  payload: { ...validPayloadByType[type], explanation: '   ' }
                }]
              }
            });
            assert.equal(result.ok, false);
            assert.ok(
              result.errors.some(e => /explanation/.test(e.reason) && e.exerciseId === `expl-${type}-empty`),
              `Esperaba error sobre explanation vacío; errores: ${JSON.stringify(result.errors)}`
            );
          });
        }
      });

    Esto añade 12 tests nuevos (4 sub-cases × 3 tipos) — cubre back-compat (sin explanation), happy path (string), y los 2 caminos de rechazo (no-string + vacío). Implementa EXPL-01.

    NO crear `tests/schema-explanation.test.js` separado — el patrón existente del proyecto agrupa tests de validator dentro de `tests/exercise-types.test.js` (verificado por los describe blocks existentes 'data/schema-validator — ...'). Mantener convención.
  </action>
  <acceptance_criteria>
    - `grep -n "payload.explanation" src/data/schema-validator.js | grep -c "if (ex.payload.explanation !== undefined)"` devuelve `3` (una ocurrencia por validateXxxPayload).
    - `grep -n "explanation" src/data/schema-validator.js | grep -c "debe ser string no vacío si está presente"` devuelve `3` (mensaje en español coherente).
    - `grep -c "describe.*explanation" tests/exercise-types.test.js` devuelve ≥ 1.
    - `node --test tests/exercise-types.test.js` sale con exit code 0 y muestra ≥12 nuevos `pass` con `explanation` en el nombre.
    - `node --test tests/*.test.js` mantiene baseline 166/166 verdes + 12 nuevos = 178/178 (o el delta exacto que arroje el runner).
    - Validar back-compat sobre contenido real: `node scripts/validate-content-fixture.mjs` (si existe el helper de Phase 4) exit 0 — los 271 ejercicios existentes siguen pasando porque ninguno tiene `explanation` aún.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/exercise-types.test.js 2>&1 | tail -20</automated>
    <automated>grep -c "if (ex.payload.explanation !== undefined)" src/data/schema-validator.js</automated>
    <automated>node --test tests/*.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
    Schema validator extendido (3 bloques nuevos), 12 tests nuevos verdes, baseline 166 tests sigue verde. Commit: `feat(07-01): extend payload validators with optional explanation rule (EXPL-01)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render UI — 3 sub-templates session + summary-errors + 2 CSS + 2 seed explanations (D-117/D-118/D-119/D-120/D-121)</name>
  <files>index.html, styles.css, content/exercises/preposiciones.json</files>
  <read_first>
    - `index.html` líneas 247-441 (toda la pantalla session post-Phase 6 incluyendo los 3 sub-templates) — entender dónde añadir el `<p class="session-explanation">` en cada uno.
    - `index.html` líneas 443-580 (toda la pantalla summary post-Phase 6 incluyendo summary-errors con dispatch por type) — entender dónde añadir el `<p class="summary-error-explanation">` dentro del `<li>`.
    - `styles.css` líneas 1-90 (head + `.correcta`/`.incorrecta` baseline) y 140-220 (summary-delta + wb-answer.incorrecta) y 360-415 (summary-errors completa Phase 6) — extraer el tono visual ya establecido.
    - `content/exercises/preposiciones.json` líneas 1-80 (ejercicios 001-006) — captar el shape `payload` actual sin explanation y el tono de los `notes` existentes.
    - `material-profesora/Preposizioni.pdf` (opcional, solo si Claude necesita validar la regla de 'di Roma' o 'sul tavolo' antes de redactar las explanations).
  </read_first>
  <action>
    Per D-118, AÑADIR en cada uno de los 3 sub-templates de `index.html` el bloque siguiente:

      <p x-show="sessionFeedback === 'incorrect' && sessionCurrentExercise.payload.explanation"
         class="session-explanation"
         x-text="sessionCurrentExercise.payload.explanation"></p>

    Posicionamiento exacto por tipo:
    - **Multi-choice** (línea ~278, dentro del `<template x-if="...type === 'multiple-choice'">`): justo después de `<p x-show="sessionFeedback === 'incorrect'">Respuesta correcta: <strong>...</strong></p>` y ANTES del `<button x-show="sessionFeedback === 'incorrect'" @click="sessionAdvance">Siguiente</button>`.
    - **Word-buttons** (línea ~329, dentro del `<template x-if="...type === 'word-buttons'">`): justo después de `<p x-show="sessionFeedback === 'incorrect'" class="wb-correct-answer">Respuesta correcta: <strong>...</strong></p>` y ANTES de `<div class="button-row">`.
    - **Match** (línea ~407, dentro del `<template x-if="...type === 'match'">`): justo después de `</div>` que cierra `.match-grid` y ANTES del `<button type="button" x-show="sessionFeedback === 'incorrect'" @click="sessionAdvance">Siguiente</button>`. NO hay línea "Respuesta correcta:" en match (la captura visual es match-flash rojo) — la explanation va sola entre el grid y el botón Siguiente.

    Cláusulas obligatorias del bloque:
    - `x-text` exclusivo (T-02-01 anti-XSS — D-126). Prohibido `x-html`.
    - `x-show` con DOBLE guard: `sessionFeedback === 'incorrect'` AND `sessionCurrentExercise.payload.explanation` truthy. Esto evita renderizar el bloque cuando el ejercicio no tiene explanation (graceful degradation D-121: sin placeholder, sin mensaje "sin explicación").
    - Clase `session-explanation` (no reutilizar `.incorrecta` rojo — la explicación es didáctica, no error D-120).
    - No `aria-live` (la captura aria-live ya la cubre `<p>Respuesta correcta:`).

    Per D-119, AÑADIR en `index.html` summary-errors dentro de CADA UNO de los 3 sub-templates de la fila `<li>` (multi-choice línea ~542-547, word-buttons línea ~549-554, match línea ~556-570), justo ANTES del `</div>` que cierra el `<div>` del sub-template:

      <p x-show="content.exerciseById[result.exerciseId]?.payload?.explanation"
         class="summary-error-explanation"
         x-text="content.exerciseById[result.exerciseId].payload.explanation"></p>

    Justificación:
    - Optional chaining `?.payload?.explanation` defensivo. CR-01 fix Phase 6 ya garantiza que el filter exterior excluye exerciseIds stale, pero el chaining añade defensa-en-profundidad coherente con el patrón existente.
    - `x-text` exclusivo (T-02-01).
    - 3 ocurrencias necesarias (una por sub-template `x-if="...type === 'X'"`) porque cada sub-template ya tiene su propio `<div>` con prompt + tu-respuesta + respuesta-correcta. La explanation va al final, antes de cerrar ese `<div>`.

    Per D-120, AÑADIR en `styles.css` 2 reglas nuevas al FINAL del archivo (después de `.summary-errors .user-answer` línea 408-414). Posición coherente con el orden temático de Phase 6 → Phase 7:

      /*
       * Phase 7 plan 01 — D-120: Explicaciones pedagógicas (.session-explanation +
       * .summary-error-explanation).
       *
       * Tono didactico: muted gris (--pico-muted-color), italic, font-size 0.9em.
       * NO rojo .incorrecta — la explicación es enseñanza, no error.
       *
       * Spacing: multiples 4px (UI-SPEC Phase 6 §Spacing): margin-top 0.5rem (8px)
       * separa del bloque "Respuesta correcta" anterior; margin-bottom 0.5rem (8px)
       * separa del boton Siguiente. Sin tokens nuevos, sin overrides de fuente.
       */
      .session-explanation,
      .summary-error-explanation {
        color: var(--pico-muted-color);
        font-style: italic;
        font-size: 0.9em;
        margin-top: 0.5rem;
        margin-bottom: 0.5rem;
      }

    Reglas compartidas en un único selector porque ambas son visualmente idénticas (D-120: coherencia cross-context). El diferenciador solo es nombre de clase para que el autor pueda customizarlas independientemente en el futuro si lo necesita.

    Per D-122/D-123, AÑADIR seed explanations en `content/exercises/preposiciones.json`:

    1. **preposiciones-001** (`"Sono ___ Roma."` → `"di"`): agregar campo `"explanation"` al final del payload (tras `correctIndex`), siguiendo el tono D-127 (3ª impersonal + regla + ejemplo paralelo). Ejemplo de redacción candidata (Claude refina, autor revisa en checkpoint):

       `"La preposicion Di indica origen o procedencia (de donde viene alguien o algo). Sono di Roma significa 'soy de Roma'. Va sin articulo cuando se nombra una ciudad directamente."`

    2. **preposiciones-006** (`"Il gatto è ___ tavolo."` → `"sul"`): explanation candidata:

       `"Su significa 'sobre' o 'encima de' con contacto fisico. Se combina con el articulo definido para formar la preposicion articolata: Su + Il (la mesa, masc sing) = Sul. Il gatto e sul tavolo = El gato esta sobre la mesa."`

    Constraints obligatorios:
    - Apóstrofes ASCII U+0027 (CONT-06 / D-129) — nunca smart quotes `'`. Si la redacción incluye contracciones italianas como `un'amica` o `c'è`, usar `'` ASCII.
    - Plain text (T-02-01 / D-126) — sin markdown (`**`, `__`, `*`, `_`), sin listas (`-`, `*`), sin headers (`#`), sin backticks.
    - Longitud ~250-400 chars cada una (D-128) — recomendación, no enforce.
    - NO modificar `notes` existente — `notes` es comentario interno del autor (D-deferred "Mantener nombres distintos"), `explanation` es user-facing en feedback rojo + summary. Coexisten.
    - NO cambiar el orden de los 50 ejercicios, NO añadir ejercicios, NO modificar `prompt`/`options`/`correctIndex`/`categoryIds`. Solo añadir el campo `explanation` en los 2 ejercicios.

    NO añadir explanations a los 48 ejercicios restantes en este plan — eso es Plan 07-02.

    Per D-134 (preparación, NO ejecución en este plan): NO tocar `.planning/PROJECT.md` aún. La reapertura Out of Scope se ejecuta en Plan 07-02 Task 4 (el último plan de la fase).
  </action>
  <acceptance_criteria>
    - `grep -c 'class="session-explanation"' index.html` devuelve `3` (una por sub-template session).
    - `grep -c 'class="summary-error-explanation"' index.html` devuelve `3` (una por sub-template summary-errors).
    - `grep -c 'sessionFeedback === '\''incorrect'\'' && sessionCurrentExercise.payload.explanation' index.html` devuelve `3` (doble guard).
    - `grep -c 'content.exerciseById\[result.exerciseId\]?.payload?.explanation' index.html` devuelve `3` (optional chaining defensivo).
    - `grep -c '\.session-explanation\|\.summary-error-explanation' styles.css` devuelve ≥ 2 (selectores nuevos).
    - `grep -c 'var(--pico-muted-color)' styles.css` aumenta (reuso de token Pico, sin nuevos).
    - `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); xs=d['exercises']; print(sum(1 for x in xs if x['payload'].get('explanation')), '/', len(xs))"` devuelve `2 / 50`.
    - El JSON sigue parseable: `python3 -c "import json; json.load(open('content/exercises/preposiciones.json'))"` exit 0.
    - Validator pasa sobre el contenido real: `node --test tests/*.test.js` exit 0 (los 12 tests de Task 1 + los 166 baseline + cualquier smoke test paramétrico).
    - Apóstrofes ASCII solamente en las 2 explanations nuevas: `python3 -c "import json,sys; d=json.load(open('content/exercises/preposiciones.json')); errors=[x['id'] for x in d['exercises'] if x['payload'].get('explanation') and any(c in x['payload']['explanation'] for c in ['‘','’','“','”'])]; print('SMART_QUOTES_FOUND:', errors); sys.exit(1 if errors else 0)"` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'class="session-explanation"' index.html</automated>
    <automated>grep -c 'class="summary-error-explanation"' index.html</automated>
    <automated>python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print('with_explanation:', sum(1 for x in d['exercises'] if x['payload'].get('explanation')), '/', len(d['exercises']))"</automated>
    <automated>node --test tests/*.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
    3 bloques `<p class="session-explanation">` añadidos a los 3 sub-templates session, 3 bloques `<p class="summary-error-explanation">` añadidos en summary-errors, 2 reglas CSS muted/italic añadidas, 2 seed explanations curadas en preposiciones-001 + preposiciones-006. Schema validator sigue verde sobre el contenido real. Commit: `feat(07-01): render explanation in session + summary-errors + 2 seed preposiciones (EXPL-02 EXPL-03)`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT humano vertical slice — fallar preposiciones-001 y preposiciones-006 + ver explanation inline + en summary</name>
  <files>index.html, styles.css, content/exercises/preposiciones.json, src/data/schema-validator.js</files>
  <action>Checkpoint humano (no auto). El autor ejecuta los 6 UATs descritos en <how-to-verify>. El executor espera "approved 07-01" o "issues: ..." y, tras approval, crea `07-01-SUMMARY.md` capturando los 6 UATs PASS + capturas de los 2 commits previos. Sin approval, el executor abre patch tasks contextuales para los issues reportados antes de cerrar el plan.</action>
  <verify><human-check>UATs 1-6 (ver <how-to-verify>) ejecutados en navegador con `npx serve` corriendo. Resume signal: "approved 07-01".</human-check></verify>
  <done>Los 6 UATs visualmente confirmados PASS por el autor. 07-01-SUMMARY.md creado con observaciones. Plan 07-01 cerrado y listo para 07-02.</done>
  <what-built>
    - Schema validator extendido con la regla `payload.explanation: string` opcional (3 tipos), 12 tests nuevos verdes.
    - 3 sub-templates session renderizan `<p class="session-explanation">` durante feedback rojo si el ejercicio tiene explanation.
    - Summary-errors extendida con `<p class="summary-error-explanation">` por error.
    - 2 reglas CSS muted/italic sin tokens nuevos.
    - 2 seed explanations curadas en preposiciones-001 ("Sono ___ Roma" → "di") y preposiciones-006 ("Il gatto è ___ tavolo" → "sul").
    - Los 49 ejercicios sin explanation siguen funcionando como antes (graceful degradation D-121).
  </what-built>
  <how-to-verify>
    Pre-requisito: `npx serve` corriendo, abrir `http://localhost:3000` en el browser (Chrome o Firefox, target desktop).

    **UAT-1 — Render inline en feedback rojo (multi-choice):**
    1. Desde home, abrir picker → seleccionar SOLO "Preposiciones" → "Repaso de 20" → "Empezar".
    2. Si el primer ejercicio de la sesión NO es preposiciones-001 ("Sono ___ Roma"), pulsar "Reiniciar ejercicios" (botón Phase 6) hasta que aparezca, o avanzar pulsando opciones correctas hasta encontrarlo (los tests prepoposiciones tienen 50 ejercicios, puede caer rápido o tarde — paciencia).
    3. Al llegar a preposiciones-001: pulsar UNA opción INCORRECTA (e.g., "a", "da", o "in" — no "di"). Esperado:
       - El botón clickado se pone rojo (.incorrecta).
       - Aparece `<p>Respuesta correcta: <strong>di</strong></p>`.
       - DEBAJO aparece un `<p>` en GRIS, ITÁLICA, font-size ligeramente más pequeño que el body, con el texto de la explanation seed.
       - DEBAJO del párrafo italic muted, aparece el botón "Siguiente".
       - El orden visual de arriba a abajo: opciones → Respuesta correcta → explanation italic → Siguiente.
    4. NO pulsar Siguiente todavía. **Foto mental** del layout.
    5. Pulsar "Reiniciar ejercicios" para volver a la misma sesión.

    **UAT-2 — Graceful degradation en feedback rojo (ejercicio SIN explanation):**
    1. Avanzar (acertando o reiniciando) hasta caer en preposiciones-002 ("Vado ___ casa") o cualquier ejercicio que NO sea preposiciones-001 ni preposiciones-006.
    2. Pulsar opción INCORRECTA. Esperado:
       - Botón rojo + "Respuesta correcta: <strong>a</strong>" + botón Siguiente.
       - **NO aparece ningún párrafo italic muted bajo "Respuesta correcta"** (graceful degradation D-121: sin placeholder, sin mensaje "sin explicación").

    **UAT-3 — Render en summary (Errores cometidos):**
    1. Continuar la sesión (puede ser nueva tras "Reiniciar" o seguir la actual).
    2. Asegurarse de que al menos UNO de los fallos sea sobre preposiciones-001 o preposiciones-006 (fallar deliberadamente cuando aparezca).
    3. Completar la sesión (puede acertar el resto o fallar todos los demás — irrelevante para UAT).
    4. En la pantalla de resumen, scroll hasta "Errores cometidos". Esperado:
       - Por cada ejercicio fallado con explanation, la fila `<li>` muestra:
         - `<strong>` con el prompt.
         - "Tu respuesta: <span class='user-answer'>" en rojo + lo que clickaste.
         - "Respuesta correcta: <strong>" con la opción correcta.
         - DEBAJO un `<p>` GRIS ITÁLICO con la explanation. Mismo estilo visual que UAT-1.
       - Para ejercicios fallados SIN explanation (e.g., preposiciones-002), la fila se ve igual que antes de Phase 7 (sin el bloque italic).

    **UAT-4 — Boot sin errores (back-compat):**
    1. Refrescar `http://localhost:3000` (F5).
    2. Esperado: la home carga las 7 categorías sin banner CONT-05 de error (cero ejercicio rompe el schema porque `explanation` es opcional y los 271 ejercicios actuales no lo tienen, excepto los 2 seed que sí tienen string válido).
    3. DevTools → Console: cero errores en boot.

    **UAT-5 — Tono visual didáctico (no error):**
    1. Comparar visualmente la explanation italic muted con el rojo `.incorrecta` y el verde `<strong>`. Esperado: la explanation NO es rojo (D-120 — la explicación es enseñanza, no error). NO destaca como botón fallido. Es prosa secundaria didáctica.

    **UAT-6 — Plain text sin markdown (T-02-01 anti-XSS):**
    1. Inspeccionar el texto de la explanation renderizado. Esperado: si la explanation en JSON contiene `Su + Il = Sul` o similar, se ve LITERAL en pantalla. Si por accidente contuviera `**Sul**`, los asteriscos se ven literalmente (no negrita). Confirma T-02-01.

    **Adicional defensivo (DevTools):**
    1. En Application → Local Storage → http://localhost:3000 → `italianCourse.v1`: confirmar que `schemaVersion` sigue siendo `4` (Phase 6) — D-116 garantiza cero migración.

    Si todos los UATs PASS:
      - Confirmar al executor que se puede continuar al Plan 07-02.
      - El executor crea `07-01-SUMMARY.md` capturando: 12 tests nuevos verdes, 2 seed explanations curadas + revisadas por el autor, 3 sub-templates + summary actualizados, layout visual UAT 6/6 PASS, vertical slice cerrada — el flow "fallar → ver explanation inline + en summary" funciona end-to-end.

    Si CUALQUIER UAT falla:
      - Describir lo que viste exactamente (DOM dump del fragmento problemático + screenshot mental).
      - El executor abre patch task para el fix antes de commit final.
  </how-to-verify>
  <resume-signal>Type "approved 07-01" si los 6 UATs pasan. Type "issues: ..." con descripción si algo falla (e.g., "italic no aparece", "explanation aparece en aciertos también", "smart quotes en preposiciones-001", "JSON parse falla").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/exercises/*.json → boot loader → DOM via x-text | El JSON viene del filesystem local (single-user, sin red) pero se renderiza en el browser; `payload.explanation` puede contener cualquier string. |
| boot loader → schema validator | El validator decide si el ejercicio es válido antes de exponerlo a la UI; explanation no-string o `null` no debe llegar al DOM. |
| Alpine `x-text` directive → DOM textContent | Alpine garantiza escape de HTML (innerHTML de propiedades string es seguro). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering (XSS via JSON content) | `<p class="session-explanation" x-text="...">` y `<p class="summary-error-explanation" x-text="...">` (6 ocurrencias en index.html) | mitigate | `x-text` exclusivo (T-02-01 invariante Phase 1). Prohibido `x-html` (Task 2 acceptance criteria grep verifica que no se usa). Si una explanation contiene `<script>alert(1)</script>`, Alpine lo renderiza como text plano (textContent), no como markup. |
| T-07-02 | Input Validation (JSON injection) | `validateXxxPayload` en schema-validator.js | mitigate | Task 1 añade `typeof === 'string' && .trim()` check. Rechaza `null`, `number`, `array`, `object`, `string vacío`. Banner CONT-05 visible en boot si la regla falla. 12 tests cubren los 4 casos (back-compat ausente, válido, no-string, vacío) sobre los 3 tipos. |
| T-07-03 | Information Disclosure | Banner CONT-05 con el ID del ejercicio que rompe la regla | accept | El "leak" del exerciseId al banner es trivial — single-user local, sin observers externos. El threat surface aceptado por arquitectura. |
| T-07-04 | Tampering (smart quotes / encoding issues) | content/exercises/preposiciones.json explanations añadidas en Task 2 | mitigate | CONT-06 NFC normalize en boot (Phase 1 invariante). Apóstrofes ASCII U+0027 enforced via acceptance criteria de Task 2 (smart-quote scan). Si en el futuro un autor pega smart quotes desde un editor que las auto-convierte, NFC normalize NO los convierte a ASCII — solo normaliza forma de unicode. Mitigación: revisar manualmente al copy-paste; el assertion script alerta. |
| T-07-05 | Denial of Service (explanation extremadamente larga rompe UI layout) | `.session-explanation` y `.summary-error-explanation` CSS | accept | Sin enforce de longitud en schema (D-116/D-128). Riesgo aceptado porque (a) el contenido lo escribe el autor manualmente, (b) Pico flex column del `<li>` envuelve naturalmente texto largo, (c) el font-size 0.9em + italic no causa overflow horizontal en desktop target. Si emerge dolor real con explanations > 600 chars, refinar manualmente. |
| T-07-06 | Repudiation (cambio del PROJECT.md Out of Scope sin trail) | `.planning/PROJECT.md` Out of Scope entry | mitigate | D-134 explícitamente requiere AUDIT TRAIL: mover la entrada a `### Validated` con nota "Validado en Phase 7: ..." + añadir Key Decision con razón del pivote post-uso-real. Ejecutado en Plan 07-02 Task 4 (no en este plan). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Phase 7 NO añade dependencias nuevas. Cero installs. No aplica el package legitimacy gate. |

</threat_model>

<verification>
## Plan-level verification

- [ ] `node --test tests/*.test.js` exit 0 con baseline + 12 tests nuevos verdes.
- [ ] `grep -c "if (ex.payload.explanation !== undefined)" src/data/schema-validator.js` = 3.
- [ ] `grep -c 'class="session-explanation"' index.html` = 3.
- [ ] `grep -c 'class="summary-error-explanation"' index.html` = 3.
- [ ] `grep -c "x-html" index.html` = 0 (T-02-01 invariante).
- [ ] `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print(sum(1 for x in d['exercises'] if x['payload'].get('explanation')))"` = 2.
- [ ] `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); errs=[x['id'] for x in d['exercises'] if x['payload'].get('explanation') and any(c in x['payload']['explanation'] for c in ['‘','’','“','”'])]; assert not errs, errs"` exit 0 (apóstrofes ASCII).
- [ ] UAT humano 6/6 PASS (Task 3 checkpoint).
</verification>

<success_criteria>
- Schema validator extendido con la regla `payload.explanation: string` opcional en los 3 tipos (EXPL-01).
- 3 bloques `<p class="session-explanation">` renderizando durante feedback rojo en los 3 sub-templates session, con `x-text` exclusivo y doble guard (EXPL-02).
- 3 bloques `<p class="summary-error-explanation">` renderizando en summary-errors bajo cada `<li>` cuando el ejercicio tiene explanation (EXPL-03).
- 2 reglas CSS muted/italic alineadas con UI-SPEC Phase 6 §Spacing/Color/Typography (sin tokens nuevos).
- 2 seed explanations en preposiciones-001 + preposiciones-006 con apóstrofes ASCII + plain text.
- 12 tests nuevos verdes (4 sub-cases × 3 tipos) + baseline 166 mantiene verde.
- UAT humano vertical slice 6/6 PASS — fallar los 2 seeds muestra explanation inline + en summary.
- Boot sin banner CONT-05 (back-compat con 269 ejercicios sin explanation + 2 con).
- Cero migración schemaVersion (sigue 4) — D-130/D-131/D-132 preservados.
- Cero modificación al motor de re-verificación (cascada D-54, sampler, exerciseStats, racha intactos).
</success_criteria>

<output>
Create `.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-01-SUMMARY.md` cuando done, documentando:
- Los 3 bloques modificados en schema-validator.js + el patrón si-explanation-then-string-no-vacío.
- Los 6 bloques `<p>` añadidos a index.html (3 session + 3 summary) con coordenadas de línea finales.
- Las 2 reglas CSS añadidas con razón visual + spacing 4px múltiplos.
- Las 2 seed explanations curadas (texto literal final tras revisión humana en Task 3).
- Resultado UAT humano 6/6 con observaciones del autor.
- Tests delta: 166 → 178 verdes.
- Cero deuda técnica añadida — vertical slice cerrada limpia.
- Próximo: Plan 07-02 entrega las 48 explanations restantes + smoke coverage test + reapertura PROJECT.md Out of Scope.
</output>
