---
phase: 07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - content/exercises/preposiciones.json
  - tests/exercise-types.test.js
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
requirements: [EXPL-04, EXPL-05]
autonomous: false

must_haves:
  truths:
    - "Las 50 entries de `content/exercises/preposiciones.json` tienen `payload.explanation: string` no vacío (las 2 seed de Plan 07-01 + las 48 nuevas)."
    - "Cada explanation sigue el tono D-127: 3ª persona impersonal, 1 frase de regla + 1 frase de ejemplo paralelo. Longitud ~250-400 chars (D-128 recomendación)."
    - "Todas las explanations usan apóstrofes ASCII U+0027 (CONT-06 / D-129) — cero smart quotes."
    - "Todas las explanations son plain text (T-02-01 / D-126) — sin markdown, sin negritas, sin listas, sin headers, sin backticks."
    - "El boot post-Plan-07-02 carga las 7 categorías sin banner CONT-05 — el schema validator acepta las 50 explanations string no vacío."
    - "Al fallar cualquiera de los 50 ejercicios de Preposiciones durante una sesión real, el autor ve la explanation curada inline + en summary."
    - "Existe un smoke test paramétrico que verifica que `preposiciones.json` tiene 50/50 explanations no vacías (defensa contra futuras eliminaciones accidentales)."
    - "PROJECT.md Out of Scope `Explicaciones pedagógicas` ha sido movido a `### Validated` con audit trail del pivote post-uso-real (D-134)."
    - "REQUIREMENTS.md tiene una sección nueva `### Explicaciones pedagógicas (EXPL) — Phase 7` con EXPL-01..EXPL-05 mapeados y traceability actualizada."
  artifacts:
    - path: content/exercises/preposiciones.json
      provides: "50 explanations completas en payload.explanation de cada uno de los 50 ejercicios"
      contains: "\"explanation\":"
    - path: tests/exercise-types.test.js
      provides: "1 smoke test paramétrico nuevo que carga content/exercises/preposiciones.json desde disk + assertea coverage 50/50"
      contains: "Preposiciones explanation coverage"
    - path: .planning/PROJECT.md
      provides: "Entrada 'Explicaciones pedagógicas' movida de Out of Scope a Validated + Key Decision añadida documentando el pivote post-uso-real"
      contains: "Validado en Phase 7"
    - path: .planning/REQUIREMENTS.md
      provides: "Nueva sección Explicaciones pedagógicas (EXPL) con EXPL-01..05 + traceability tabla actualizada"
      contains: "EXPL-01"
  key_links:
    - from: "content/exercises/preposiciones.json"
      to: "schema-validator validateMultipleChoicePayload"
      via: "boot loadContent → validateContent"
      pattern: "explanation"
    - from: "tests/exercise-types.test.js Preposiciones coverage block"
      to: "readFileSync content/exercises/preposiciones.json"
      via: "smoke test paramétrico (patrón Phase 5 multi-cat)"
      pattern: "preposiciones.json"
    - from: ".planning/PROJECT.md Validated"
      to: ".planning/REQUIREMENTS.md EXPL-01..05"
      via: "trazabilidad cross-archivo, ID prefixed"
      pattern: "EXPL-0"
---

<objective>
Completar el seed editorial de explanations para Preposiciones — 48 explanations curadas + revisadas en 3 batches de ~16 cada uno — y cerrar la fase con el audit trail del pivote (PROJECT.md Out of Scope → Validated, REQUIREMENTS.md sección nueva con EXPL-01..05).

Purpose: tras Plan 07-01 el flow funciona end-to-end con 2 seeds. Plan 07-02 entrega volumen pedagógico real (50/50 ejercicios de Preposiciones cubiertos) usando el patrón D-85 (Claude propone + autor revisa por bloque + commit incremental) ya validado en Phase 4 SEED-01 (5 categorías de 0) y Phase 5 Essere (39 ejercicios de 0).

Output:
- 48 explanations adicionales en preposiciones-002..005 + preposiciones-007..050 (preposiciones-001 y 006 ya hechos en Plan 07-01).
- 3 commits secuenciales por batch revisado (batch A 16 explanations / batch B 16 / batch C 16, números aproximados — el executor refina si el contenido se distribuye distinto).
- 1 smoke test paramétrico (`Preposiciones explanation coverage`) que verifica 50/50 al cargar el archivo desde disco.
- PROJECT.md reapertura Out of Scope ejecutada (D-134): entrada movida a Validated + Key Decision añadida.
- REQUIREMENTS.md sección nueva con EXPL-01..05 + traceability.
- Roadmap actualizado: Phase 7 Goal solidificado + Plans listadas con check post-completion.
- UAT humano final post-batch C: lanzar Repaso 20 solo Preposiciones, fallar 5+ ejercicios deliberadamente, ver explanation curada en cada uno + en summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-CONTEXT.md
@.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-01-SUMMARY.md
@.planning/phases/04-backup-robusto-contenido-completo/04-CONTEXT.md
@.planning/phases/05-essere-categoria-fundamental-que-faltaba/05-CONTEXT.md

@content/exercises/preposiciones.json
@tests/exercise-types.test.js

<interfaces>
<!-- Identifiers que el executor necesita sin explorar. -->

From content/exercises/preposiciones.json (50 ejercicios pre-Plan 07-02):
- Todos son `type: "multiple-choice"` con shape `{prompt: string-con-___, options: [3-4 strings], correctIndex: 0..3}`.
- 8 simples (preposiciones-001..005, 007, 047, 048): di/a/da/in/con/su/per — preposiciones base.
- 30 articolate (preposiciones-006, 008..046 mezclados): combinaciones preposición + artículo definido (del, dello, della, dell', dei, degli, delle, al/allo/alla/all'/ai/agli/alle, dal/dallo/dalla/dall'/dai/dagli/dalle, nel/nello/nella/nell'/nei/negli/nelle, sul/sullo/sulla/sull'/sui/sugli/sulle).
- 10 casos particulares + 2 excepciones (fra eufónico #049, con instrumental #050).
- Algunos tienen campo `notes` (autor-internal) — NO modificar, COEXISTE con explanation.

From content/exercises/preposiciones.json post-Plan 07-01:
- preposiciones-001 ya tiene `payload.explanation` (di / origen).
- preposiciones-006 ya tiene `payload.explanation` (sul / contacto físico).
- Los otros 48 NO tienen explanation aún — target de este plan.

From tests/exercise-types.test.js (estructura post-Plan 07-01):
- Nuevo describe block `'data/schema-validator — payload.explanation (Phase 7 D-116)'` con 12 tests parametrizados (de Plan 07-01 Task 1).
- Patrón smoke test paramétrico Phase 5 (referencia): carga `readFileSync('content/exercises/X.json')` + parsea + assertea propiedades sobre el shape.

From src/data/schema-validator.js (post-Plan 07-01):
- Las 3 validateXxxPayload tienen la regla `if (ex.payload.explanation !== undefined) check string && trim`.
- `validateContent` corre sobre todos los ejercicios al boot.

From .planning/PROJECT.md (estructura líneas 92-105):
- `### Validated` → contenido validado.
- `### Active` → en construcción.
- `### Out of Scope` → boundaries explícitos. Línea actual: "Explicaciones pedagógicas / mostrar la regla al fallar o acertar — solo bien/mal por velocidad; la teoría está en los PDFs".
- `## Key Decisions` (línea 122) → tabla `| Decision | Rationale | Outcome |`.

From .planning/REQUIREMENTS.md (estructura líneas 6-77):
- `## v1 Requirements` con subsecciones `### Foundation (FOUND)`, `### Content & Schema (CONT)`, `### Exercise Types (EXTYPE)`, ..., `### Polish UX post-sesión (UX) — Phase 6`.
- `## Traceability` (línea 119) con tabla `| Requirement | Phase | Status |`.
- Patrón para nueva sección: `### Explicaciones pedagógicas (EXPL) — Phase 7`.
</interfaces>

<canonical_tone_examples>
<!-- Los 4 ejemplos canónicos del autor (CONTEXT.md líneas 105-110) — bibliografía tonal para los 48 nuevos. -->

1. SU contacto físico (template para preposiciones-006 ya seedeado, sirve de referencia para sui/sugli/sulle/sull'/sullo/sulla restantes):
   `"Su significa 'sobre' o 'encima de' con contacto físico. Los libros se colocan encima de la superficie de la mesa. Por eso Su + Le (las) = Sulle."`

2. DA agente en pasiva:
   `"En italiano, cuando una acción la realiza un agente en una frase pasiva, la preposición obligatoria es Da. Significa 'por' en el sentido de autoría. Il libro è scritto da lui = El libro está escrito por él."`

3. DA + artículo (movimiento desde persona):
   `"Cuando te desplazas saliendo del lugar de alguien (su casa, por ejemplo), usas la preposición Da combinada con el artículo. Da + Le = Dalle. Vengo dalle mie zie."`

4. SU superficie vertical = horizontal:
   `"Su no solo significa estar encima en horizontal sino también pegado a superficies verticales. Los cuadros se cuelgan en los muros = contacto físico con la pared. Por eso Su + I (los, masc plural) = Sui."`

Tono mental: 3ª impersonal + regla + ejemplo paralelo italiano-español. NO 2ª persona conversacional. NO listas con bullets.
</canonical_tone_examples>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Batch A — Claude propone ~16 explanations (preposiciones-002..017) + autor revisa frase por frase + commit</name>
  <files>content/exercises/preposiciones.json</files>
  <read_first>
    - `content/exercises/preposiciones.json` ENTERO (50 ejercicios) — leer una vez para tener TODOS los prompts/options/correctIndex/notes en contexto al redactar el batch A. Sin esto, las explicaciones se aíslan del PDF de origen y pueden contradecir las `notes` existentes.
    - `material-profesora/Preposizioni.pdf` (opcional, si Claude necesita validar la regla canónica del PDF para una preposición concreta — e.g., el cuadro articolate ID-A).
    - `.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-01-SUMMARY.md` para ver el tono exacto que el autor APROBÓ en las 2 seeds de Plan 07-01 (esas son la referencia canónica de tono — NO los 4 ejemplos canónicos del context, esos son destilados pre-revisión).
  </read_first>
  <action>
    Per D-122/D-123/D-127, redactar 16 explanations nuevas para los ejercicios preposiciones-002..005 + preposiciones-007..017 (saltando 006 ya seedeado en Plan 07-01).

    Coherencia con cada ejercicio:
    - Leer el `prompt`, `options`, `correctIndex` y `notes` (si existe) del ejercicio.
    - La explanation DEBE responder a "por qué la opción correcta es esa, no las otras". Cuando `notes` ya da la pista pedagógica (e.g., "in vs a para países vs ciudades"), destilar esa info al estilo D-127 (3ª impersonal + regla + ejemplo paralelo).
    - NO copiar literalmente la `notes` — `notes` es taquigrafía interna; `explanation` es prosa didáctica destilada.

    Constraints DUROS:
    - Apóstrofes ASCII U+0027 (CONT-06 / D-129). Cero smart quotes `'` `'`.
    - Plain text (T-02-01 / D-126). Cero markdown (`**`, `__`, `*`, `_`, `#`, backticks), cero listas, cero headers.
    - Longitud objetivo 250-400 chars por explanation (D-128). Si una explanation excede 400 chars con buena razón pedagógica, aceptable; si excede 500 chars, considerar refinar.
    - Estructura mental D-127: 1 frase de regla + 1 frase de ejemplo paralelo italiano-español. Variedad léxica entre las 16 explanations — evitar empezar siempre con "La preposición X significa..." (el autor leerá las 16 juntas, repetición = fricción).
    - NO modificar `prompt`, `options`, `correctIndex`, `categoryIds`, `notes`, `id`, `type`, ni ningún campo existente. Solo AÑADIR el campo `explanation` al payload.

    Distribución temática esperada en el batch A (preposiciones-002..017):
    - **Simples + casos idiomáticos** (002 "a casa" — excepción a/in con casa, 003 "da Milano" — provenienza, 004 "in Italia" — paesi/regioni con in vs ciudades con a, 005 "con amici" — compagnia, 007 "per imparare" — scopo/finalidad).
    - **Articolate primeras 9 entries** (008..016 — depende del orden del JSON; el executor lee y cubre lo que toque).
    - **17 — sigue el patrón natural del orden del archivo.**

    El executor REDACTA las 16 explanations en una propuesta visible al autor ANTES de modificar el archivo. Patrón:

      ```
      Propuesta batch A (16 explanations):

      preposiciones-002 ("Vado ___ casa." → "a"):
      "Con la palabra casa se usa la preposicion A (no In) como excepcion idiomatica del italiano A1: Vado a casa significa 'voy a mi propia casa'. La regla general usa In con paesi/regioni pero casa lleva A como locucion fija."

      preposiciones-003 ("Vengo ___ Milano." → "da"):
      "..."

      [...16 propuestas]

      Esperando review del autor antes de commit.
      ```

    El autor revisa frase por frase, marca las que aprueba sin cambio, las que requieren refinar (con feedback concreto), y las que pide reescribir desde cero. El executor itera hasta que las 16 estén APPROVED.

    Tras aprobación, el executor MODIFICA `content/exercises/preposiciones.json` añadiendo el campo `explanation` en los 16 ejercicios. Verifica:
    - JSON parseable: `python3 -c "import json; json.load(open('content/exercises/preposiciones.json'))"`.
    - 18/50 ejercicios con explanation tras este batch (2 seed + 16 nuevos).
    - Schema validator pasa: `node --test tests/exercise-types.test.js`.

    Commit: `feat(07-02): batch A — 16 explanations preposiciones (preposiciones-002..017 — author-reviewed) (EXPL-04 parcial)`.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print(sum(1 for x in d['exercises'] if x['payload'].get('explanation')))"` devuelve `18` (2 de Plan 07-01 + 16 de batch A).
    - `python3 -c "import json,sys; d=json.load(open('content/exercises/preposiciones.json')); errs=[x['id'] for x in d['exercises'] if x['payload'].get('explanation') and any(c in x['payload']['explanation'] for c in ['‘','’','“','”'])]; sys.exit(0 if not errs else 1)"` exit 0.
    - `python3 -c "import json,sys; d=json.load(open('content/exercises/preposiciones.json')); errs=[x['id'] for x in d['exercises'] if x['payload'].get('explanation') and any(s in x['payload']['explanation'] for s in ['**','__','##','\\\`'])]; sys.exit(0 if not errs else 1)"` exit 0 (cero markdown).
    - `node --test tests/exercise-types.test.js` exit 0.
    - El autor confirmó APPROVED las 16 explanations (resume-signal "approved batch A").
  </acceptance_criteria>
  <how-to-verify>
    Tras la propuesta del executor (16 explanations en chat), el autor responde:
    - "approved" para confirmar las 16 sin cambios.
    - "refine: preposiciones-NNN — feedback breve" para pedir un refine concreto. Repetir hasta APPROVED.
    - Una vez aprobadas, el executor ESCRIBE el JSON + commitea, y muestra el output de `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print('coverage:', sum(1 for x in d['exercises'] if x['payload'].get('explanation')), '/ 50')"` para confirmar 18/50.
  </how-to-verify>
  <resume-signal>Type "approved batch A" cuando las 16 explanations propuestas estén OK y el commit hecho.</resume-signal>
  <verify><human-check>Propuesta visible al autor; refine iterativo hasta APPROVED; commit tras "approved batch A".</human-check></verify>
  <done>Las 16 explanations del batch A curadas + revisadas + commiteadas; coverage acumulado de payload.explanation crece como esperado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Batch B — Claude propone ~16 explanations (preposiciones-018..033) + autor revisa + commit</name>
  <files>content/exercises/preposiciones.json</files>
  <read_first>
    - Estado actual de `content/exercises/preposiciones.json` (re-leer los 16 ejercicios del batch A ya commiteados — para mantener variedad estilística vs los ya hechos).
    - `tests/exercise-types.test.js` (sin cambios — solo confirmar que el schema validator sigue verde tras batch A).
  </read_first>
  <action>
    Idéntico patrón a Task 1, aplicado a los siguientes 16 ejercicios del JSON (probablemente preposiciones-018..033 — el executor verifica el orden real del archivo y cubre los 16 que tocan).

    Constraints idénticos: apóstrofes ASCII, plain text, longitud 250-400, tono D-127, NO modificar campos existentes excepto añadir `explanation`.

    **Énfasis en variedad estilística** dado que batch A ya cubrió las primeras 16 — el autor leerá las 32 juntas eventualmente al fallar ejercicios random durante uso real, evitar empezar con la misma frase estructural en 5+ explanations.

    Tras aprobación, ESCRIBE y commitea: `feat(07-02): batch B — 16 explanations preposiciones (preposiciones-018..033 — author-reviewed) (EXPL-04 parcial)`.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print(sum(1 for x in d['exercises'] if x['payload'].get('explanation')))"` devuelve `34` (18 acumulado + 16 nuevos).
    - Apóstrofes ASCII + plain text verification idéntica a Task 1 acceptance criteria.
    - `node --test tests/exercise-types.test.js` exit 0.
    - El autor confirmó APPROVED.
  </acceptance_criteria>
  <how-to-verify>
    Mismo patrón que Task 1 — propuesta visible, refine iterativo, ESCRIBE + commit tras APPROVED, mostrar coverage 34/50.
  </how-to-verify>
  <resume-signal>Type "approved batch B" cuando las 16 explanations estén OK y el commit hecho.</resume-signal>
  <verify><human-check>Propuesta visible al autor; refine iterativo hasta APPROVED; commit tras "approved batch B".</human-check></verify>
  <done>Las 16 explanations del batch B curadas + revisadas + commiteadas; coverage acumulado de payload.explanation crece como esperado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Batch C — Claude propone ~16 explanations finales (preposiciones-034..050) + autor revisa + commit</name>
  <files>content/exercises/preposiciones.json</files>
  <read_first>
    - Estado actual de `content/exercises/preposiciones.json` post-batch B (34/50 cubiertos).
    - Las 2 excepciones especiales preposiciones-049 (fra eufónico) y preposiciones-050 (con instrumental) — sus `notes` documentan que son casos fuera del PDF aprobados por el autor en Phase 4. Las explanations deben honrar ese matiz (e.g., "Esta forma fra es una variante eufonica de tra que se usa antes de palabras que empiezan por consonante doble 't' o 'tr'... Es una excepcion idiomatica del italiano").
  </read_first>
  <action>
    Idéntico patrón a Task 1+2, aplicado a los 16 ejercicios restantes (preposiciones-034..050 — 17 entries pero solo 16 nuevos porque uno de ellos puede ser preposiciones-006 ya cubierto en Plan 07-01; el executor verifica y cubre los que toquen para llegar a 50/50 EXACTOS).

    Edge case: si tras batch C el coverage no llega exactamente a 50/50, el executor detecta el ejercicio que falta y propone su explanation antes del commit. La cifra exacta puede variar 1±1 (preposiciones-006 ya tiene explanation pre-existente — verificar idx no se duplica).

    Tras aprobación, ESCRIBE y commitea: `feat(07-02): batch C — explanations finales preposiciones (preposiciones-034..050 — author-reviewed) — 50/50 coverage (EXPL-04 done)`.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); xs=d['exercises']; cov=sum(1 for x in xs if x['payload'].get('explanation')); print(f'{cov}/{len(xs)}'); assert cov == len(xs), f'{cov}/{len(xs)} - missing'"` exit 0 con output `50/50`.
    - Apóstrofes ASCII + plain text verification idéntica.
    - `node --test tests/exercise-types.test.js` exit 0.
    - El autor confirmó APPROVED.
  </acceptance_criteria>
  <how-to-verify>
    Mismo patrón. Tras commit, mostrar coverage 50/50 + confirmar al autor que el seed editorial de Preposiciones está completo.
  </how-to-verify>
  <resume-signal>Type "approved batch C" cuando las explanations finales estén OK, el commit hecho, y el coverage 50/50 confirmado.</resume-signal>
  <verify><human-check>Propuesta visible al autor; refine iterativo hasta APPROVED; commit tras "approved batch C".</human-check></verify>
  <done>Las 16 explanations del batch C curadas + revisadas + commiteadas; coverage acumulado de payload.explanation crece como esperado.</done>
</task>

<task type="auto">
  <name>Task 4: Smoke test paramétrico de coverage Preposiciones + reapertura PROJECT.md Out of Scope (D-134) + REQUIREMENTS.md EXPL-01..05 + ROADMAP.md finalización</name>
  <files>tests/exercise-types.test.js, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `tests/exercise-types.test.js` líneas finales (donde añadir el nuevo smoke test, después del describe Phase 7 ya añadido en Plan 07-01).
    - `.planning/PROJECT.md` líneas 13-160 ENTERAS — secciones Validated, Active, Out of Scope, Key Decisions. NO modificar Constraints, Conventions, Architecture. NO releer parcialmente.
    - `.planning/REQUIREMENTS.md` líneas 1-180 ENTERAS — sección v1 Requirements (8 subsecciones existentes) + Traceability tabla. NO modificar Out of Scope ni v2.
    - `.planning/ROADMAP.md` líneas 167-180 (sección Phase 7 backlog actual con Goal `[To be planned]` y Plans `[ ] TBD`) — reemplazar con la versión finalizada.
  </read_first>
  <action>
    **Sub-step 4a — Smoke test paramétrico Preposiciones coverage:**

    AÑADIR al FINAL de `tests/exercise-types.test.js` (después del describe Phase 7 D-116 del Plan 07-01):

      // ────────────────────────────────────────────────────────────────────────────
      // Phase 7 Plan 07-02 — Preposiciones explanation coverage smoke test (EXPL-04)
      // ────────────────────────────────────────────────────────────────────────────
      // Verifica que las 50 entries de content/exercises/preposiciones.json tienen
      // payload.explanation: string no vacio. Defensa contra eliminaciones accidentales
      // futuras + sanity check de que Plan 07-02 entrego el seed completo.
      //
      // Si el autor anyade un 51er ejercicio sin explanation, este test FALLA y le
      // recuerda completar el seed editorial. Si el autor mueve un ejercicio a otra
      // categoria, el test sigue valido (cuenta solo las entries de este archivo).

      import { readFileSync } from 'node:fs';
      import { fileURLToPath } from 'node:url';
      import { dirname, resolve } from 'node:path';

      describe('content/preposiciones.json — explanation coverage (Phase 7 EXPL-04)', () => {
        test('all 50 exercises have payload.explanation: string no vacío', () => {
          const __filename = fileURLToPath(import.meta.url);
          const __dirname = dirname(__filename);
          const path = resolve(__dirname, '../content/exercises/preposiciones.json');
          const data = JSON.parse(readFileSync(path, 'utf-8'));
          const exercises = data.exercises;

          assert.equal(exercises.length, 50, `Esperaba 50 ejercicios, encontre ${exercises.length}`);

          const missing = exercises.filter(ex =>
            typeof ex.payload?.explanation !== 'string' || !ex.payload.explanation.trim()
          );

          assert.equal(
            missing.length,
            0,
            `Ejercicios sin explanation valida: ${missing.map(ex => ex.id).join(', ')}`
          );
        });

        test('explanations use ASCII apostrophes only (CONT-06 / D-129)', () => {
          const __filename = fileURLToPath(import.meta.url);
          const __dirname = dirname(__filename);
          const path = resolve(__dirname, '../content/exercises/preposiciones.json');
          const data = JSON.parse(readFileSync(path, 'utf-8'));

          const smartQuotePattern = /[‘’“”]/;
          const violations = data.exercises
            .filter(ex => smartQuotePattern.test(ex.payload?.explanation || ''))
            .map(ex => ex.id);

          assert.equal(violations.length, 0, `Smart quotes encontradas en: ${violations.join(', ')}`);
        });

        test('explanations are plain text (no markdown markers — D-126)', () => {
          const __filename = fileURLToPath(import.meta.url);
          const __dirname = dirname(__filename);
          const path = resolve(__dirname, '../content/exercises/preposiciones.json');
          const data = JSON.parse(readFileSync(path, 'utf-8'));

          // Tokens de markdown que rompen el render plain-text: ** (bold), __ (bold),
          // ## (header), backticks (inline code). Asteriscos sueltos en prosa son OK
          // (pueden aparecer como puntuacion). Solo doblados o headers prefijo.
          const mdPattern = /(\*\*|__|##|`)/;
          const violations = data.exercises
            .filter(ex => mdPattern.test(ex.payload?.explanation || ''))
            .map(ex => ex.id);

          assert.equal(violations.length, 0, `Markdown markers encontradas en: ${violations.join(', ')}`);
        });
      });

    Justificación de patrón:
    - 3 tests nuevos sobre las 3 invariantes editoriales (coverage 50/50, apóstrofes ASCII, plain text).
    - Lee del filesystem real (no fixture in-memory) — el test corre tras los commits y verifica el output real del seed editorial.
    - Patrón coherente con los smoke tests Phase 4 multi-cat y Phase 5 paramétricos que ya leen `content/exercises/*.json` desde disco.

    **Sub-step 4b — Reapertura PROJECT.md (D-134):**

    EDITAR `.planning/PROJECT.md`:

    1. En `### Out of Scope` (línea ~101), REMOVER la entrada:
       `- Explicaciones pedagógicas / mostrar la regla al fallar o acertar — solo bien/mal por velocidad; la teoría está en los PDFs`

    2. En `### Validated` (línea ~13, después de la última entrada Phase 6), AÑADIR:

       `**Phase 7 — Explicaciones pedagógicas al fallar (2026-05-25):**`
       `- ✓ Campo opcional payload.explanation en schema (3 tipos) con regla "if present, must be non-empty string" (D-116 / EXPL-01).`
       `- ✓ Render inline durante feedback rojo en los 3 sub-templates session screen (D-118 / EXPL-02) — x-text exclusivo (T-02-01).`
       `- ✓ Render en summary-errors bajo cada fila de fallo (D-119 / EXPL-03) — lectura desde content.exerciseById con optional chaining defensivo.`
       `- ✓ 50/50 explanations curadas en content/exercises/preposiciones.json — pattern D-85 Claude propone + autor revisa por bloque (3 batches × 16), tono D-127 3a impersonal regla + ejemplo paralelo, longitud ~250-400 chars, apóstrofes ASCII U+0027, plain text sin markdown (EXPL-04).`
       `- ✓ Pivote post-uso-real documentado: la decisión Phase 1 "Solo bien/mal por velocidad; la teoría está en los PDFs" fue revisada porque tras 271 ejercicios el autor pedía explicaciones a Gemini cada fallo de Preposiciones — fricción real superó la decisión inicial (EXPL-05 / D-134).`

    3. En `## Key Decisions` (tabla líneas 122+), AÑADIR fila nueva al final:

       `| 2026-05-25 | Reabrir Out of Scope "Explicaciones pedagógicas al fallar/acertar" — limitado a fallos, opcional por ejercicio, seed en Preposiciones | Tras 271 ejercicios el autor consultaba Gemini cada fallo de Preposiciones; la teoría en PDFs no era accesible mid-sesión; explanation destilada inline cierra el loop pedagógico sin romper la velocidad del flow (solo se muestra al fallar) | ✓ Validado Phase 7 (50 explanations curadas + render inline + summary; campo opcional retro-compatible con 221 ejercicios sin explanation) |`

    **Sub-step 4c — REQUIREMENTS.md nueva sección + traceability:**

    EDITAR `.planning/REQUIREMENTS.md`:

    1. AÑADIR nueva subsección DESPUÉS de `### Polish UX post-sesión (UX) — Phase 6` (línea ~72):

       `### Explicaciones pedagógicas (EXPL) — Phase 7`
       ``
       `- [x] **EXPL-01**: El schema validator acepta `payload.explanation: string` como campo opcional uniforme en los 3 tipos de ejercicio (multi-choice / word-buttons / match). Si presente, debe ser string no vacío (whitespace puro rechazado). Si ausente, el ejercicio sigue siendo válido (back-compat con 221 ejercicios pre-Phase 7). Cero migración schemaVersion (sigue 4 — explanation es contenido, no state).`
       `- [x] **EXPL-02**: Durante una sesión, si un ejercicio fallado tiene `payload.explanation`, el feedback rojo muestra un párrafo italic muted (`.session-explanation`) bajo la línea "Respuesta correcta:" y antes del botón Siguiente, en los 3 sub-templates (multi-choice / word-buttons / match). Render vía `x-text` exclusivo (T-02-01 anti-XSS). Si el ejercicio no tiene explanation, no se renderiza placeholder (D-121 graceful degradation).`
       `- [x] **EXPL-03**: Al final de sesión, la sección "Errores cometidos" del summary muestra la `explanation` (cuando existe) bajo la línea "Respuesta correcta" de cada `<li>` con la misma clase visual `.summary-error-explanation` (italic muted). Lectura desde `content.exerciseById` con optional chaining defensivo.`
       `- [x] **EXPL-04**: Las 50 entries de `content/exercises/preposiciones.json` tienen `payload.explanation` curada por Claude + revisada por el autor en 3 batches secuenciales (patrón D-85). Tono D-127 (3a impersonal + regla + ejemplo paralelo), longitud ~250-400 chars, apóstrofes ASCII, plain text. Smoke test paramétrico verifica 50/50 + apóstrofes ASCII + cero markdown.`
       `- [x] **EXPL-05**: PROJECT.md `## Out of Scope` reabrió la entrada "Explicaciones pedagógicas / mostrar la regla al fallar o acertar" → movida a `### Validated` con audit trail del pivote post-uso-real. Key Decisions tabla añadida con la razón (271 ejercicios mostraron fricción no anticipada; Gemini consultado cada fallo de Preposiciones). Las otras 6 categorías quedan opcionales para retro-rellenar en fases incrementales futuras (Phase 7.x).`

    2. EXTENDER la tabla `## Traceability` con 5 filas nuevas al final (antes del block "Coverage:"):

       `| EXPL-01 | Fase 7 | Complete (Plan 07-01) |`
       `| EXPL-02 | Fase 7 | Complete (Plan 07-01) |`
       `| EXPL-03 | Fase 7 | Complete (Plan 07-01) |`
       `| EXPL-04 | Fase 7 | Complete (Plan 07-02) |`
       `| EXPL-05 | Fase 7 | Complete (Plan 07-02) |`

    3. ACTUALIZAR el bloque "Coverage:" al final del archivo:
       - "v1 requirements: **48** total (43 originales + 5 EXPL Phase 7)"
       - "Mapped to phases: 48 (100%)"
       - "Unmapped: 0"
       - "Completed: 48/48 (100%)"

    4. ACTUALIZAR la nota final del archivo: "Last updated: 2026-05-25 after Phase 7 completion (EXPL-01..05 cerrados — 50 explanations preposiciones curadas + render inline + summary). Milestone v1.0 ampliado con explicaciones pedagógicas opcionales."

    **Sub-step 4d — ROADMAP.md Phase 7 finalizado:**

    EDITAR `.planning/ROADMAP.md`:

    1. En el array Phases (líneas 8-16), REEMPLAZAR la línea Phase 7 (si existe) o AÑADIR:
       `- [x] **Phase 7: Explicaciones pedagógicas al fallar — campo explanation por ejercicio + render en Errores cometidos** — Tras 271 ejercicios el autor pedía explicaciones a Gemini cada fallo de Preposiciones. Phase 7 añade `payload.explanation` opcional + render inline durante feedback rojo + render en summary-errors + 50 explanations curadas para Preposiciones como seed. Las otras 6 categorías quedan opcionales para fases incrementales futuras. (completed 2026-05-25)`

    2. REEMPLAZAR la sección Phase 7 (líneas ~167-175 backlog actual `Goal: [To be planned]`) con la versión finalizada:

       `### Phase 7: Explicaciones pedagógicas al fallar — campo explanation por ejercicio + render en Errores cometidos`
       `**Goal**: El autor ve, al fallar un ejercicio, una explicación pedagógica destilada (1 frase de regla + 1 frase de ejemplo paralelo) inline durante el feedback rojo y agregada en la sección "Errores cometidos" del summary. Las 50 entries de Preposiciones (la categoría que motivó la fase porque era donde el autor consultaba Gemini cada fallo) están cubiertas tras Plan 07-02; las otras 6 categorías quedan opcionales para fases incrementales si emerge necesidad.`
       `**Mode:** standard`
       `**Depends on**: Phase 6`
       `**Requirements**: EXPL-01, EXPL-02, EXPL-03, EXPL-04, EXPL-05`
       `**Success Criteria** (qué tiene que ser CIERTO):`
       `  1. content/exercises/preposiciones.json tiene 50/50 entries con `payload.explanation` no vacío, validado por schema + smoke test paramétrico.`
       `  2. Al fallar cualquiera de los 50 ejercicios de Preposiciones durante sesión real, el autor ve la explanation italic muted bajo "Respuesta correcta" inline + replicada en summary "Errores cometidos".`
       `  3. Los 221 ejercicios de las otras 6 categorías siguen funcionando sin cambios (graceful degradation — `<p class="session-explanation">` no se renderiza cuando explanation está ausente).`
       `  4. Cero migración schemaVersion (sigue 4 — explanation es contenido en `content/`, no state).`
       `  5. T-02-01 anti-XSS preservado: todas las explanations se renderizan via `x-text` exclusivo; markdown literal (asteriscos, doble subrayado) no se interpreta.`
       `  6. UAT humano: fallar deliberadamente 5+ ejercicios de Preposiciones en Repaso 20 + ver explanation curada en cada uno + repaso agregado en summary.`
       `**Plans**: 2 plans`
       `- [x] 07-01-PLAN.md — Schema validator extension + 3 sub-templates session + summary-errors extension + 2 reglas CSS muted/italic + 2 seed explanations (preposiciones-001/006) + 12 tests parametrizados (EXPL-01/02/03).`
       `- [x] 07-02-PLAN.md — Batch A 16 + batch B 16 + batch C 16 explanations Preposiciones (patrón D-85 Claude propone + autor revisa por bloque) + smoke test coverage 50/50 + reapertura PROJECT.md Out of Scope (D-134) + REQUIREMENTS.md EXPL-01..05 (EXPL-04/05).`

    3. ACTUALIZAR la tabla `## Progress` con una fila nueva Phase 7:
       `| 7. Explicaciones pedagógicas al fallar | 2/2 | Complete    | 2026-05-25 |`

    4. ACTUALIZAR el bloque `## Coverage Summary`:
       - "v1 requirements: 48 total (43 originales + 5 EXPL Phase 7)"
       - "Mapped to phases: 48 (100%)"

    Ejecutar tests + smoke + commit final:
    - `node --test tests/*.test.js` exit 0 (baseline 178 post-Plan-07-01 + 3 nuevos smoke = 181/181).
    - Commit: `feat(07-02): smoke coverage + PROJECT.md Out-of-Scope reapertura + REQUIREMENTS.md EXPL-01..05 + ROADMAP.md Phase 7 finalizada (EXPL-05)`.
  </action>
  <acceptance_criteria>
    - `grep -c "Preposiciones explanation coverage" tests/exercise-types.test.js` ≥ 1.
    - `node --test tests/exercise-types.test.js` exit 0 con ≥3 nuevos sub-tests en el describe Preposiciones coverage.
    - `node --test tests/*.test.js` exit 0 (baseline + 12 Plan 07-01 + 3 Plan 07-02 smoke).
    - `grep -c "Explicaciones pedagógicas" .planning/PROJECT.md` ≥ 2 (uno en Validated, uno en Key Decisions).
    - `grep -c "Explicaciones pedagógicas" .planning/PROJECT.md | head -1` MUST be ≥ 2 — verifica que la entrada NO sigue en Out of Scope (debería estar en Validated + Key Decisions).
    - `grep -A1 "### Out of Scope" .planning/PROJECT.md | grep -i "explicaciones pedag"` returns nothing (entrada removida de Out of Scope).
    - `grep -c "EXPL-01\|EXPL-02\|EXPL-03\|EXPL-04\|EXPL-05" .planning/REQUIREMENTS.md` ≥ 10 (5 IDs × ≥2 ocurrencias cada — sección + traceability).
    - `grep -c "EXPL-0" .planning/ROADMAP.md` ≥ 5 (Phase 7 requirements line).
    - `grep -c "Phase 7.*[Cc]omplete" .planning/ROADMAP.md` ≥ 1.
    - `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print('final:', sum(1 for x in d['exercises'] if x['payload'].get('explanation')), '/ 50')"` returns `final: 50 / 50`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/*.test.js 2>&1 | tail -10</automated>
    <automated>python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print(sum(1 for x in d['exercises'] if x['payload'].get('explanation')), '/', len(d['exercises']))"</automated>
    <automated>grep -c "EXPL-0" .planning/REQUIREMENTS.md</automated>
    <automated>grep -A2 "### Out of Scope" .planning/PROJECT.md | head -5</automated>
  </verify>
  <done>
    Smoke test paramétrico Preposiciones coverage añadido + verde, PROJECT.md Out-of-Scope movido a Validated + Key Decision añadida, REQUIREMENTS.md sección EXPL-01..05 + traceability extendida, ROADMAP.md Phase 7 finalizada con Goal + Plans + Success Criteria. Commit final hecho.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT humano final — Repaso 20 solo Preposiciones, fallar 5+ ejercicios, verificar explanation en sesión + summary</name>
  <files>content/exercises/preposiciones.json, tests/exercise-types.test.js, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Checkpoint humano (no auto). El autor ejecuta los 7 UATs descritos en <how-to-verify>. El executor espera "approved 07-02" o "issues: ..." y, tras approval, crea `07-02-SUMMARY.md` capturando los 7 UATs PASS + referencias a los 4 commits previos (batches A/B/C + smoke+docs). Sin approval, el executor abre patch tasks antes de cerrar el plan.</action>
  <verify><human-check>UATs 1-7 (ver <how-to-verify>) ejecutados en navegador con `npx serve` corriendo + sanity checks de PROJECT.md/REQUIREMENTS.md/ROADMAP.md actualizados. Resume signal: "approved 07-02".</human-check></verify>
  <done>Los 7 UATs visualmente confirmados PASS. 07-02-SUMMARY.md creado. Phase 7 cierra: 50/50 explanations curadas + render inline + summary + audit trail PROJECT.md + EXPL-01..05 en REQUIREMENTS.md.</done>
  <what-built>
    - 48 explanations curadas adicionales en `content/exercises/preposiciones.json` (3 batches × 16) — total 50/50 con la coverage de Plan 07-01.
    - Smoke test paramétrico `Preposiciones explanation coverage` con 3 sub-tests (coverage 50/50, apóstrofes ASCII, plain text no-markdown).
    - PROJECT.md `## Out of Scope` reabrió "Explicaciones pedagógicas" → movida a `### Validated` + Key Decision añadida con audit trail.
    - REQUIREMENTS.md sección nueva `### Explicaciones pedagógicas (EXPL) — Phase 7` con EXPL-01..05 + traceability tabla extendida.
    - ROADMAP.md Phase 7 finalizada con Goal + 6 Success Criteria + 2 Plans listadas.
  </what-built>
  <how-to-verify>
    Pre-requisito: `npx serve` corriendo, `http://localhost:3000` accesible.

    **UAT-1 — Repaso 20 solo Preposiciones, fallar 5+ deliberadamente:**
    1. Home → picker → seleccionar SOLO "Preposiciones" → "Repaso de 20" → "Empezar".
    2. Durante la sesión, fallar deliberadamente AL MENOS 5 ejercicios (clickar opción incorrecta en multi-choice).
    3. En CADA fallo: confirmar que aparece el `<p class="session-explanation">` italic muted bajo "Respuesta correcta" + antes del botón Siguiente.
    4. Leer la explanation curada de cada fallo: ¿tono D-127 (3ª impersonal + regla + ejemplo paralelo)? ¿plain text (no markdown literal)? ¿coherente con el `prompt` y la respuesta correcta?
    5. Completar la sesión.

    **UAT-2 — Summary "Errores cometidos" muestra las 5+ explanations:**
    1. Pantalla summary: scroll hasta "Errores cometidos".
    2. Para cada `<li>` de error, confirmar:
       - `<strong>` con prompt.
       - "Tu respuesta: <span class='user-answer'>" rojo + opción clickada.
       - "Respuesta correcta: <strong>" + opción correcta.
       - DEBAJO un `<p class="summary-error-explanation">` italic muted con la MISMA explanation que viste en sesión.
    3. Verificar que el estilo visual es coherente con el de sesión (italic muted, font-size más pequeño, sin rojo).

    **UAT-3 — Cobertura completa Preposiciones (50/50):**
    1. Si la sesión solo tocó 20 ejercicios random, repetir Repaso 20 hasta haber visto ≥30 ejercicios distintos.
    2. Comprobar (visualmente o vía DevTools en `content.exerciseById`) que todos los ejercicios fallados muestran explanation — cero "fallar y no ver bloque italic" (señal de explanation faltante).

    **UAT-4 — Otras 6 categorías sin cambios (graceful degradation):**
    1. Home → picker → seleccionar SOLO "Avere" (o cualquiera de las 6 sin explanations) → Repaso 20 → Empezar.
    2. Fallar 2-3 ejercicios.
    3. Esperado: NO aparece bloque italic muted (graceful degradation D-121 — los 221 ejercicios sin explanation no muestran placeholder).
    4. Summary: la sección "Errores cometidos" se ve idéntica a pre-Phase 7 (sin bloque explanation extra).

    **UAT-5 — Tests verdes:**
    1. En terminal: `node --test tests/*.test.js` exit 0 con baseline + 12 + 3 = ~181 verdes.

    **UAT-6 — Backup roundtrip sin afectación (BACK-04/-05):**
    1. Backup → Exportar progreso → guardar archivo.
    2. Abrir el archivo .json descargado en un editor.
    3. Esperado: el state exportado NO contiene las explanations (las explanations viven en content/, no en state — D-132 layer purity). El archivo de backup tiene exactamente el mismo shape que pre-Phase 7.
    4. Backup → Importar progreso → mismo archivo recién exportado → confirmar.
    5. Esperado: state restaurado idéntico, banner verde "Importado correctamente", schemaVersion sigue 4.

    **UAT-7 — Audit trail PROJECT.md visible:**
    1. Abrir `.planning/PROJECT.md` en editor.
    2. Confirmar que en `### Validated` aparece la sección "Phase 7 — Explicaciones pedagógicas al fallar" con sus 5 checkboxes.
    3. Confirmar que en `### Out of Scope` ya NO aparece la línea "Explicaciones pedagógicas / mostrar la regla al fallar o acertar".
    4. Confirmar que en `## Key Decisions` aparece la fila nueva con razón del pivote.

    Si todos los UATs PASS:
      - Confirmar al executor.
      - El executor crea `07-02-SUMMARY.md` capturando: las 50 explanations curadas (referencia a los 3 commits batch), el smoke test paramétrico, la reapertura PROJECT.md, la sección REQUIREMENTS.md EXPL-01..05, el ROADMAP.md finalizado, los tests verdes (~181), UAT 7/7 PASS por el autor. Phase 7 cierra.

    Si CUALQUIER UAT falla:
      - Describir el síntoma exactamente.
      - El executor abre patch task para el fix.
  </how-to-verify>
  <resume-signal>Type "approved 07-02" cuando los 7 UATs pasen. Type "issues: ..." con descripción si algo falla (e.g., "explanation X tiene smart quote", "PROJECT.md sigue teniendo la línea en Out of Scope", "tests rojos N").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author-curated content (50 explanations) → JSON file → boot loader → DOM | Single-user manual editing; threat surface idéntico a Phase 4 SEED-01 (autor escribe contenido pedagógico desde conocimiento + revisión). |
| Markdown markers en explanation (literal) → x-text → DOM | Si por accidente una explanation queda con `**bold**`, los asteriscos se ven literales (T-02-01 garantiza textContent escape). |
| .planning/PROJECT.md edits (Out of Scope removal + Validated addition + Key Decision) → git commit → audit trail | El pivote del producto debe quedar trazable; cero información destructiva. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering (smart quote contamination) | 48 nuevas explanations añadidas en batches A/B/C | mitigate | Smoke test (Task 4) `smartQuotePattern: /[‘’“”]/` corre tras los 3 commits; si una explanation tiene smart quote la batch fall PESoffline. Verificación humana en cada checkpoint también. |
| T-07-09 | Tampering (markdown contamination — bold/headers) | 48 nuevas explanations | mitigate | Smoke test (Task 4) `mdPattern: /(\*\*|__|##|`)/` rechaza tokens markdown obvios. Asteriscos sueltos en prosa siguen pasando (puntuación válida). |
| T-07-10 | Repudiation (cambio del PROJECT.md sin trail) | Out of Scope removal + Validated addition + Key Decision | mitigate | D-134 requiere AUDIT TRAIL explícito (3 sub-edits coordinados: remove + add + key decision con fecha). El commit final lleva mensaje descriptivo del pivote. |
| T-07-11 | Information Integrity (explanation pedagógicamente incorrecta) | Las 48 explanations nuevas | mitigate | Patrón D-85 (Claude propone + autor revisa por batch en checkpoint) — el autor ES el oracle pedagógico. Tests no validan corrección semántica (D-deferred — fuera de scope). Refine iterativo en cada batch garantiza approval frase por frase. |
| T-07-12 | Denial of Service (explanation extremadamente larga rompe layout) | Las 50 explanations | accept | Idéntico a T-07-05 (Plan 07-01) — el autor controla la longitud, layout flex envuelve naturalmente. Si una explanation supera 600 chars, refinar manualmente. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Cero dependencias nuevas en Phase 7. Plan 07-02 no introduce installs. |

</threat_model>

<verification>
## Plan-level verification

- [ ] `python3 -c "import json; d=json.load(open('content/exercises/preposiciones.json')); print(sum(1 for x in d['exercises'] if x['payload'].get('explanation')))"` devuelve `50`.
- [ ] `node --test tests/exercise-types.test.js` exit 0 (baseline + Plan 07-01 + 3 nuevos smoke = ≥15 nuevos en este describe + el baseline).
- [ ] `node --test tests/*.test.js` exit 0 globalmente.
- [ ] Cero smart quotes en explanations (smoke test paramétrico Task 4 sub-test 2).
- [ ] Cero markdown markers en explanations (smoke test paramétrico Task 4 sub-test 3).
- [ ] PROJECT.md Out-of-Scope línea "Explicaciones pedagógicas" REMOVIDA.
- [ ] PROJECT.md Validated tiene sección Phase 7 con ≥5 checkboxes.
- [ ] PROJECT.md Key Decisions tabla tiene fila nueva con fecha 2026-05-25 + razón del pivote.
- [ ] REQUIREMENTS.md sección `### Explicaciones pedagógicas (EXPL) — Phase 7` con EXPL-01..05.
- [ ] REQUIREMENTS.md Traceability tabla tiene 5 filas nuevas (EXPL-01..05).
- [ ] ROADMAP.md Phase 7 finalizada (sin más `[To be planned]`); 2 Plans listadas con check `[x]`.
- [ ] UAT humano 7/7 PASS (Task 5 checkpoint).
</verification>

<success_criteria>
- 50/50 explanations en preposiciones.json (las 2 de Plan 07-01 + 48 nuevas) — EXPL-04 cerrado.
- 3 commits secuenciales por batch revisado (batch A + batch B + batch C) — patrón D-85/D-123 ejecutado.
- Smoke test paramétrico `Preposiciones explanation coverage` con 3 sub-tests (50/50, ASCII apóstrofes, no markdown) — defensa contra regresiones editoriales futuras.
- PROJECT.md Out of Scope reapertura ejecutada (D-134): entrada movida a Validated + Key Decision con audit trail del pivote post-uso-real — EXPL-05 cerrado.
- REQUIREMENTS.md sección Phase 7 nueva con EXPL-01..05 + traceability extendida.
- ROADMAP.md Phase 7 finalizada con Goal + 6 Success Criteria + 2 Plans (07-01 + 07-02) en check.
- UAT humano 7/7 PASS — fallar 5+ ejercicios reales de Preposiciones muestra explanation curada inline + en summary.
- Cero migración schemaVersion (sigue 4 — D-132).
- Cero modificación al motor de re-verificación (D-130/D-131 preservados).
- Cero modificación a `notes` existente (coexistencia con explanation — D-deferred).
- Cero modificación a `content/exercises/avere.json` (D-133 D-88 APPEND-ONLY preservado).
- 178 + 3 = 181 tests verdes (baseline + Plan 07-01 + Plan 07-02 smoke).
</success_criteria>

<output>
Create `.planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/07-02-SUMMARY.md` cuando done, documentando:
- Las 50 explanations curadas (referencia a 3 commits batch + lista de IDs cubiertos por batch).
- El smoke test paramétrico añadido y sus 3 sub-tests.
- La reapertura PROJECT.md Out of Scope ejecutada con extracto de las 3 modificaciones (entry removal + Validated section + Key Decision row).
- La sección REQUIREMENTS.md Phase 7 + traceability.
- La actualización ROADMAP.md Phase 7 con success criteria finales.
- Resultado UAT humano 7/7 con observaciones del autor (especialmente cualquier explanation que se refinó iterativamente en checkpoint).
- Tests delta: ~178 → ~181 verdes.
- Phase 7 cierra. Milestone v1.0 extendido con explicaciones pedagógicas opcionales (Preposiciones cubierta como seed; 6 categorías restantes opcionales para fases incrementales 7.1, 7.2, ... si emerge dolor).
- Próximo: verifier pass de Phase 7 → `phase.complete` → considerar `/gsd:complete-milestone v1.0-expl` o similar tag del milestone extendido.
</output>
