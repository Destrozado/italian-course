---
phase: 08-modo-examen-por-categoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/app.js
  - index.html
  - tests/screen-examen.test.js
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - EXAM-01
  - EXAM-02
  - EXAM-03
  - EXAM-04
  - EXAM-05

must_haves:
  truths:
    - "El autor ve un botón 'Examen' (etiqueta plana D-185) en cada fila de la tabla home — 6ª columna nueva al final de cada fila (D-184, NO en el picker)."
    - "Click en 'Examen' de una categoría con ejercicios > 0 y sin Test completo activo → lanza directamente una sesión Test completo de SOLO esa categoría (1 click, salta picker — D-181 + D-186 lanzamiento directo sin confirmación previa). sessionMode='test-completo' literal (D-189). Sin atajos teclado (D-188 sin atajos v1)."
    - "Click en 'Examen' con state.inFlightTest activo → aparece el panel .confirm-inline (6ª call-site D-44) con copy 'Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?'; Descartar y empezar descarta el inFlightTest previo y arranca el Examen; Cancelar deja state intacto."
    - "Categoría con 0 ejercicios → botón Examen disabled (opacity Pico default) + tooltip 'No hay ejercicios en esta categoría' vía :title nativo."
    - "Categoría en estado 'hecha' o 'dominada' → botón Examen enabled normal (D-187 — estado de la cat NO oculta el botón)."
    - "Examen abandonado (cerrar tab mid-sesión) → banner home reanudar muestra copy genérica 'Tienes un Test completo a medias' y Reanudar reconstruye con buildFullTest([catId]) sobre las MISMAS categoryIds persistidas (D-183 copy genérica + D-182 slot único compartido)."
    - "Examen completado sin fallar → DOMAIN-04 promueve la cat a 'hecha' automáticamente; Examen con un fallo → cascada D-54 + cat vuelve a 'no-hecha' (D-190 promoción aplica igual que cualquier sesión, cero cambio al motor)."
    - "Summary tras Examen renderiza delta de la 1 categoría examinada sin cambios al template summary (D-191 cero modificación al summary render)."
    - "Cero migración schemaVersion (sigue 4 — D-192). Phase 8 es UX puro: cero campos nuevos en state, cero cambios a storage.js, cero cambios al schema validator, cero cambios al motor de re-verificación."
  artifacts:
    - path: "src/screens/app.js"
      provides: "startExamen(categoryId) handler + _launchExamen(catId) helper privado + 2 campos derivados en categoriesForDisplay"
      contains: "startExamen("
    - path: "src/screens/app.js"
      provides: "Helper privado _launchExamen(catId) con el bloque de buildFullTest + reset sub-estado + persistInFlightTest"
      contains: "_launchExamen("
    - path: "src/screens/app.js"
      provides: "Campo examenEnabled en cada item de categoriesForDisplay"
      contains: "examenEnabled"
    - path: "index.html"
      provides: "6ª columna Examen en la tabla home con <th> + <td> + <button class='secondary outline'>"
      contains: "<th scope=\"col\">Examen</th>"
    - path: "index.html"
      provides: "Botón Examen por fila con bindings :disabled + :title + @click"
      contains: "startExamen(cat.id)"
    - path: "tests/screen-examen.test.js"
      provides: "Smoke tests presence-check del handler startExamen (7 sub-tests mínimos)"
      contains: "describe"
  key_links:
    - from: "index.html (botón Examen del tbody x-for)"
      to: "src/screens/app.js (handler startExamen)"
      via: "@click=\"startExamen(cat.id)\""
      pattern: "@click=\"startExamen\\("
    - from: "src/screens/app.js (startExamen — path conflict)"
      to: "src/screens/app.js (requestConfirm + _launchExamen)"
      via: "requestConfirm({message:'Ya hay un Test completo en curso...', onConfirm: () => { this.clearInFlightTest(); this._launchExamen(catId); }})"
      pattern: "Ya hay un Test completo en curso"
    - from: "src/screens/app.js (_launchExamen — path normal)"
      to: "src/domain/session.js (buildFullTest)"
      via: "buildFullTest([catId], allExercises)"
      pattern: "buildFullTest\\(\\["
    - from: "src/screens/app.js (_launchExamen)"
      to: "src/screens/app.js (persistInFlightTest D-182 slot único)"
      via: "this.pickerCheckedCategoryIds = [catId]; this.persistInFlightTest();"
      pattern: "persistInFlightTest\\(\\)"
    - from: "index.html (botón Examen :disabled binding)"
      to: "src/screens/app.js (categoriesForDisplay.examenEnabled)"
      via: ":disabled=\"!cat.examenEnabled\""
      pattern: "examenEnabled"
---

<objective>
Entregar el feature completo "Modo Examen por categoría" (Phase 8) en un único plan atómico: handler `startExamen(categoryId)` + helper privado `_launchExamen(catId)` + 2 campos derivados en `categoriesForDisplay` + 6ª columna `Examen` en la tabla home + 7 smoke tests presence-check + audit trail consolidado (PROJECT.md + REQUIREMENTS.md EXAM-01..05 + ROADMAP.md Phase 8 finalizada + STATE.md baseline 199 → 199+N).

Purpose: Resolver el dolor canónico "5-6 Repasos para validar dominio de una sola categoría" con un click directo desde la tabla home. Examen es semánticamente Test completo de 1 cat (D-189) — reutiliza `buildFullTest` (D-50), slot único `inFlightTest` (D-182), y el patrón conflict D-44 como 6ª call-site del helper `requestConfirm`. Cero migración schemaVersion (D-192), cero CSS nuevo (UI-SPEC §CSS additions), cero módulos nuevos.

Output:
- Handler `startExamen(categoryId)` + helper `_launchExamen(catId)` en `src/screens/app.js`.
- 2 campos derivados (`examenEnabled`, `examenTooltip`) en `categoriesForDisplay`.
- 6ª columna en `index.html` `<th>` + `<td>` con botón `class="secondary outline"`.
- 7 smoke tests presence-check en `tests/screen-examen.test.js` (199 → 206 tests verdes).
- Audit trail consolidado: PROJECT.md `### Validated` con Phase 8 + REQUIREMENTS.md sección `### Modo Examen por categoría (EXAM)` con EXAM-01..05 + ROADMAP.md Phase 8 finalizada + traceability tabla.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-CONTEXT.md
@.planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-UI-SPEC.md
@.planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos clave que el ejecutor reusa SIN modificar. Extraídos de las
     fuentes de verdad listadas arriba. Mirar las líneas indicadas en
     PATTERNS.md para los excerpts completos. -->

From src/domain/session.js (líneas 173-188):
```
export function buildFullTest(categoryIds, allExercises, rng = Math.random): { exerciseIds: string[], actualSize: number }
// Pure. Devuelve TODOS los ejercicios cuyos categoryIds intersectan con el parámetro.
// Fisher-Yates con rng inyectable. Sin tope.
```

From src/screens/app.js (líneas 388-396 — helper sin cambios):
```
requestConfirm({ message: string, confirmLabel: string, cancelLabel?: string, onConfirm: () => void, onCancel?: () => void }): void
// Setea this.confirmDialog. Una sola call-site nueva en Phase 8 (la 6ª).
```

From src/screens/app.js (líneas 625-646 — sin cambios):
```
persistInFlightTest(): void
// Lee this.pickerCheckedCategoryIds (fallback prev?.categoryIds) + this.sessionExerciseIds/Cursor/Results.
// CRÍTICO Phase 8: setear this.pickerCheckedCategoryIds = [catId] ANTES de invocarlo (pitfall PATTERNS.md §1 Analog 2).
```

From src/screens/app.js (líneas 708-711):
```
clearInFlightTest(): void
// Limpia state.inFlightTest a null. Se invoca dentro del onConfirm del 6º requestConfirm.
```

From src/screens/app.js (líneas 339-362 — set canónico de reset):
```
resetSession(): void
// Pattern S-2 cancelaciones + reset de TODOS los sub-estados sesión + word-buttons + match + Phase 6 matchFirstWrongPair.
// El bloque equivalente está duplicado en restartRepaso líneas 535-584 (precedente D-104 Phase 6).
```

From src/screens/app.js (línea 60):
```
import { buildSession, buildFullTest, fisherYates } from '../domain/session.js';
// buildFullTest YA está importado. NO modificar el import block.
```

</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar startExamen + _launchExamen + extender categoriesForDisplay</name>
  <files>src/screens/app.js</files>

  <read_first>
    - src/screens/app.js (archivo completo OBLIGATORIO — verás el estado actual de openPicker líneas 271-290, requestConfirm líneas 388-396, resetSession líneas 339-362, startSession líneas 439-484, restartRepaso líneas 531-594, persistInFlightTest líneas 625-646, clearInFlightTest líneas 708-711, categoriesForDisplay líneas 1949-1977)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-CONTEXT.md (D-181..D-192 + Claude's Discretion)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-UI-SPEC.md (Component Inventory + Interaction State Diagram + Naming del handler + Decisiones de Claude's Discretion resueltas)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-PATTERNS.md (Pattern Assignments §1 Analog 1-5 + Shared Patterns S-A/S-B/S-C/S-D + Key cross-cutting decisions surfaced for planner)
    - src/domain/session.js (líneas 173-188 — confirmar firma de buildFullTest, no modificar)
  </read_first>

  <behavior>
    - Test 1: el archivo src/screens/app.js contiene el identificador `startExamen(` exactamente 1 vez como definición de método (al menos 1 occurrence total en el archivo, contemplando que los tests del Task 3 referenciarán el patrón).
    - Test 2: el archivo contiene `_launchExamen(` como helper privado (al menos 1 occurrence).
    - Test 3: dentro de la ventana de 3000 chars que arranca en `startExamen(`, aparece `this.state.inFlightTest` (chequeo conflict) ANTES de cualquier mención de `buildFullTest` o `_launchExamen`.
    - Test 4: dentro de esa ventana, aparece la copy literal `'Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?'`.
    - Test 5: dentro del cuerpo de `_launchExamen(`, aparece `buildFullTest([` (Examen es 1-cat — passing array literal con un elemento).
    - Test 6: dentro del cuerpo de `_launchExamen(`, aparece `this.sessionMode = 'test-completo'` (D-189 literal — NO `this.pickerMode`).
    - Test 7: dentro del cuerpo de `_launchExamen(`, aparece `this.pickerCheckedCategoryIds = [` (setear ANTES de persistInFlightTest — pitfall PATTERNS.md §1 Analog 2).
    - Test 8: dentro del cuerpo de `_launchExamen(`, aparece `this.persistInFlightTest()` (D-182 slot único — siempre persiste, no condicional).
    - Test 9: dentro del cuerpo de `_launchExamen(`, aparece `this.currentScreen = 'session'` (transición final).
    - Test 10: `categoriesForDisplay` getter incluye `examenEnabled` y `examenTooltip` en el objeto retornado por el `.map()`.
  </behavior>

  <action>
    Modificar `src/screens/app.js` con cuatro cambios atómicos. NO crear archivos nuevos en este task. NO tocar el import block (línea 60 ya tiene `buildFullTest`).

    **Cambio 1 — Insertar el método `startExamen(categoryId)` en el factory `appShell`, posición lógica: junto a `openPicker` (después de la línea 290, antes de `requestReturnToHome` línea 307).** Cuerpo del método:
    - Comment JSDoc breve explicando: Phase 8 D-181..D-192, lanza Test completo de 1 cat directo (salta picker), conflict D-44 reusa requestConfirm 6ª call-site.
    - Guard `if (this.state.inFlightTest) {...}` ANTES de cualquier mutación. Dentro del guard: invocar `this.requestConfirm({...})` con:
      - `message: 'Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?'` (literal idéntico a openPicker línea 275 — coherencia textual D-183 ya que el banner reanudar habla genéricamente de "Test completo").
      - `confirmLabel: 'Descartar y empezar'` (decisión del planner: coherencia con openPicker D-44 línea 276 — UI-SPEC línea 222 sugería `'Continuar'` pero el patrón unificado de la call-site D-44 más semánticamente similar usa `'Descartar y empezar'`; lockear esta opción para no homogeneizar las 6 call-sites en este plan — capturado como deferred si UAT lo pide).
      - `cancelLabel: 'Cancelar'`.
      - `onConfirm: () => { this.clearInFlightTest(); this._launchExamen(categoryId); }`.
    - Después del guard: invocar `this._launchExamen(categoryId)` directamente.
    - `return` implícito.

    **Cambio 2 — Insertar el helper privado `_launchExamen(catId)` justo después de `startExamen` (o en posición adyacente lógica antes de `requestReturnToHome`).** Cuerpo del helper (sigue el patrón de `restartRepaso` líneas 531-594 — superset completo de reset, NO el subset corto de `startSession`):
    - Comment JSDoc breve: helper privado invocado por startExamen (directo) y por requestConfirm.onConfirm (post-conflict). Cuerpo del lanzamiento puro — buildFullTest + reset sub-estado + persistInFlightTest + transición.
    - **Pattern S-2 cancelaciones defensivas:** `this.cancelAutoAdvance();` `this.cancelMatchFlash();` (set completo de restartRepaso — Examen puede venir de mid-match si el path conflict cancela un Test previo a medias).
    - Construir `const allExercises = Object.values(this.content.exerciseById);`.
    - Invocar `const result = buildFullTest([catId], allExercises);` (Fisher-Yates aleatorio default rng — sin seed determinista en runtime, igual que `startSession`).
    - `this.sessionMode = 'test-completo';` (D-189 literal — NO `this.pickerMode`).
    - **CRÍTICO — setear `this.pickerCheckedCategoryIds = [catId];` AHORA, ANTES de `persistInFlightTest()`.** Pitfall PATTERNS.md §1 Analog 2: `persistInFlightTest` lee `this.pickerCheckedCategoryIds` con fallback a `prev?.categoryIds`. Si no lo seteamos, el reanudar usaría las cats del Test anterior.
    - `this.sessionExerciseIds = result.exerciseIds;` `this.sessionCursor = 0;` `this.sessionResults = [];` `this.sessionSelectedIndex = null;` `this.sessionFeedback = null;`.
    - Reset sub-estados word-buttons: `this.wordButtonsBank = [];` `this.wordButtonsAnswer = [];`.
    - Reset sub-estados match (superset completo del precedente Phase 6 D-104 línea 564-584): `this.matchLeft = [];` `this.matchRight = [];` `this.matchSelectedLeftIdx = null;` `this.matchPairsConsumed = [];` `this.matchHadFailure = false;` `this.matchFirstWrongPair = null;` `this.matchFlashIdx = null;` `this.matchFlashHandle = null;`.
    - Inicializar sub-estado del primer ejercicio si hay pool: `if (result.exerciseIds.length > 0) { const firstEx = this.content.exerciseById[result.exerciseIds[0]]; this.initSubStateForExercise(firstEx); }`.
    - Persistir: `this.persistInFlightTest();` (D-182 slot único — SIEMPRE persiste, no condicional como startSession).
    - Transición: `this.currentScreen = 'session';`.

    **Cambio 3 — Extender `categoriesForDisplay` (líneas 1949-1977).** Dentro del `.map(cat => {...})`:
    - Después de la línea que calcula `totalCount`, derivar antes del `return`: `const examenEnabled = (exercisesByCat[cat.id] ?? []).length > 0;` (preferir reusar `exercisesByCat` ya construido — no recomputar). Refactor: si quieres mantener la línea `totalCount` actual, simplemente añadir: `const examenEnabled = totalCount > 0;` justo después de la línea actual `totalCount: (exercisesByCat[cat.id] ?? []).length,` no es viable porque `totalCount` está dentro del object literal — extraer a variable `const totalCount = (exercisesByCat[cat.id] ?? []).length;` ANTES del return + reusar la variable en el return.
    - Añadir 2 propiedades al return:
      - `examenEnabled` (booleano, derivado como arriba).
      - `examenTooltip: examenEnabled ? '' : 'No hay ejercicios en esta categoría'` (UI-SPEC línea 138-141 — texto plano factual sin signo de exclamación).
    - Comment inline arriba del bloque `.map()` o junto a los 2 nuevos campos: `// Phase 8 D-184/D-187: examenEnabled binding para botón Examen disabled state.`.

    **Cambio 4 — JSDoc del getter `categoriesForDisplay` actualizado.** Línea 1943-1944 actualmente dice "totalCount, lastPracticedLabel". Actualizar la lista en el comment para incluir `examenEnabled` y `examenTooltip` — coherencia documental.

    **NO tocar:**
    - El import block (línea 60).
    - `startSession`, `restartRepaso`, `persistInFlightTest`, `clearInFlightTest`, `resetSession`, `requestConfirm`, `openPicker`.
    - El bloque de `exercisesByCat` (líneas 1952-1957) — reusar la estructura ya construida.
    - Schema validator, render UI session/summary, motor de re-verificación.

    Si Pico secondary outline o el reset block es ambiguo: leer UI-SPEC §Component Inventory + §Estados visuales por componente.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --check src/screens/app.js && grep -c "startExamen(" src/screens/app.js | awk '$1 &gt;= 1' && grep -c "_launchExamen(" src/screens/app.js | awk '$1 &gt;= 1' && grep -q "examenEnabled" src/screens/app.js && grep -q "examenTooltip" src/screens/app.js && grep -q "'Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?'" src/screens/app.js && node --test tests/ 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Source: `node --check src/screens/app.js` exit 0 (sintaxis válida).
    - Source: `grep -c 'startExamen(' src/screens/app.js` devuelve ≥ 1 (definición del método).
    - Source: `grep -c '_launchExamen(' src/screens/app.js` devuelve ≥ 2 (definición + 1+ call-sites — al menos onConfirm + path normal).
    - Source: `grep -q 'examenEnabled' src/screens/app.js` exit 0.
    - Source: `grep -q 'examenTooltip' src/screens/app.js` exit 0.
    - Source: `grep -q "'No hay ejercicios en esta categoría'" src/screens/app.js` exit 0 (tooltip copy literal).
    - Source: `grep -q "'Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?'" src/screens/app.js` exit 0 (D-44 6ª call-site copy literal).
    - Source: `grep -q "'Descartar y empezar'" src/screens/app.js` exit 0 (confirmLabel lockeado).
    - Source: dentro de una ventana de 3000 chars desde `startExamen(`, la cadena `this.state.inFlightTest` aparece ANTES de `buildFullTest(` ni `_launchExamen(` (chequeo conflict primero — verificable inspeccionando manualmente el archivo o vía un script ad-hoc del Task 3).
    - Source: dentro del cuerpo de `_launchExamen(`, aparecen en orden lógico (no estricto): `cancelAutoAdvance()`, `cancelMatchFlash()`, `buildFullTest([`, `this.sessionMode = 'test-completo'`, `this.pickerCheckedCategoryIds = [`, `this.persistInFlightTest()`, `this.currentScreen = 'session'`.
    - Behavior: `cd /home/vcompanyb/italian-course && node --test tests/` mantiene 199 tests verdes (baseline preservado — sin nuevos tests aún, llegan en Task 3).
    - Source: `categoriesForDisplay` getter mantiene firma + invariantes existentes (sigue siendo getter, devuelve array de objetos con los 8 campos previos + 2 nuevos).
  </acceptance_criteria>

  <done>
    `src/screens/app.js` tiene `startExamen(categoryId)` + `_launchExamen(catId)` + `examenEnabled` + `examenTooltip` añadidos. `node --check` pasa, `node --test tests/` mantiene 199 tests verdes (sin regresiones). Copy literal D-44 + confirmLabel 'Descartar y empezar' + tooltip 'No hay ejercicios en esta categoría' verificados por grep. JSDoc del getter actualizado. NO tocó schema validator, motor de re-verificación, ni handlers de session/summary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insertar 6ª columna Examen en la tabla home (index.html)</name>
  <files>index.html</files>

  <read_first>
    - index.html (archivo completo OBLIGATORIO — verás la estructura actual líneas 140-180 con la tabla home `<thead>` 5 columnas + `<tbody><template x-for="cat in categoriesForDisplay">` con 5 `<td>` por fila)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-UI-SPEC.md (§Component Inventory + §Visual Wireframes + §Copywriting Contract — botón Examen + §Color §Decisión clave densidad visual)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-PATTERNS.md (Pattern Assignments §2 — código exacto a insertar)
    - src/screens/app.js (mirar el resultado del Task 1 — confirmar que categoriesForDisplay expone examenEnabled + examenTooltip; mirar handler startExamen para confirmar la firma `startExamen(categoryId)`)
  </read_first>

  <action>
    Modificar `index.html` con tres cambios atómicos en la sección `<template x-if="currentScreen === 'home'">` (líneas ~85-180):

    **Cambio 1 — Actualizar el comment de la tabla densa.** Línea 150 actual: `<!-- Tabla densa (D-29) con 5 columnas exactas. -->`. Reemplazar por: `<!-- Tabla densa (D-29) con 6 columnas exactas — 6ª columna Examen añadida en Phase 8 (D-184). -->` para coherencia documental (pitfall: dejar "5 columnas" cuando hay 6 deja un trap textual).

    **Cambio 2 — Insertar el nuevo `<th scope="col">Examen</th>` como 6º hermano dentro del `<tr>` del `<thead>` (líneas 153-160).** Después del `<th scope="col">Última vez</th>` actual (línea 159), añadir:
    `<th scope="col">Examen</th>`
    Con `scope="col"` OBLIGATORIO (a11y — coherencia con los 5 existentes; UI-SPEC §ARIA / semantic HTML).

    **Cambio 3 — Insertar el nuevo `<td>` con botón como 6ª `<td>` al final de cada `<tr>` dentro del `<template x-for>` (líneas 164-174).** Después del `<td x-text="cat.lastPracticedLabel"></td>` actual (línea 173), añadir:
    ```
    <td>
      <button type="button"
              class="secondary outline"
              :disabled="!cat.examenEnabled"
              :title="cat.examenTooltip"
              @click="startExamen(cat.id)">Examen</button>
    </td>
    ```
    Detalles obligatorios:
    - `type="button"` explícito (defensivo — consistencia con el resto de botones del archivo, evita auto-submit si la tabla quedara dentro de un `<form>`).
    - `class="secondary outline"` (UI-SPEC línea 115 lockea — nivel 3 jerarquía visual; NO `class="secondary"` solo, NO sin class).
    - `:disabled="!cat.examenEnabled"` binding reactivo (Alpine re-renderiza cuando `categoriesForDisplay` recompute).
    - `:title="cat.examenTooltip"` (tooltip nativo del navegador — UI-SPEC §Color §Estado disabled — `:title` escapa por defecto, T-02-01 invariante preservado).
    - `@click="startExamen(cat.id)"` (handler del Task 1, firma 1-cat).
    - Texto del botón: `Examen` LITERAL HTML (NO `x-text`, NO interpolado — D-185 + T-02-01 invariante).

    **NO tocar:**
    - El comment `<!-- Tabla densa (D-29) ... -->` antes que el de Cambio 1.
    - Los 5 `<th>` existentes (Estado/Categoría/Racha/Ejercicios/Última vez).
    - Los 5 `<td>` existentes dentro del `<template x-for>`.
    - La sección picker, session, summary, backup, banners.
    - El header del archivo, los SRI hashes, el script Alpine, los `<head>` resources.

    **NO añadir:**
    - `<small>`, `<kbd>`, `<span>`, ni wrappers extra dentro del `<td>`.
    - `aria-label` extra al botón (UI-SPEC línea 510: el texto del botón es la etiqueta canónica — coherencia con Backup/Reanudar/Descartar).
    - `class="home-table"` ni clases CSS nuevas a la tabla (UI-SPEC §CSS additions lockea CERO CSS nuevo).
    - `style="..."` inline, `tabindex` explícito, ni `id` al botón.

    Si la estructura de la tabla `<thead>/<tbody>` no es exactamente como dicta PATTERNS.md §2 Analog 1: leer el archivo completo de nuevo y localizar el bloque `currentScreen === 'home'`. La tabla home es la ÚNICA tabla del archivo con `categoriesForDisplay`.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && grep -q '&lt;th scope="col"&gt;Examen&lt;/th&gt;' index.html && grep -q 'startExamen(cat.id)' index.html && grep -q 'class="secondary outline"' index.html && grep -q ':disabled="!cat.examenEnabled"' index.html && grep -q ':title="cat.examenTooltip"' index.html &amp;&amp; grep -q '6 columnas exactas' index.html &amp;&amp; node --test tests/ 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -c '&lt;th scope="col"&gt;Examen&lt;/th&gt;' index.html` devuelve 1 (un solo header nuevo).
    - Source: `grep -c 'startExamen(cat.id)' index.html` devuelve 1 (un solo handler binding).
    - Source: `grep -c 'class="secondary outline"' index.html` devuelve ≥ 1 (botón Examen presente; otros botones del archivo NO usan `secondary outline` actualmente — esta sería la primera ocurrencia).
    - Source: `grep -q ':disabled="!cat.examenEnabled"' index.html` exit 0.
    - Source: `grep -q ':title="cat.examenTooltip"' index.html` exit 0.
    - Source: `grep -q '@click="startExamen(cat.id)"' index.html` exit 0 (binding handler completo).
    - Source: `grep -q 'type="button"' index.html` exit 0 (consistencia defensiva).
    - Source: `grep -q '6 columnas exactas' index.html` exit 0 (comment actualizado).
    - Source: el `<td>` con el botón Examen está dentro del `<template x-for="cat in categoriesForDisplay">` (verificable por inspección manual: el `<button>` aparece DESPUÉS de `<td x-text="cat.lastPracticedLabel"></td>` y ANTES del cierre `</tr></template>`).
    - Source: la tabla home tiene exactamente 6 `<th>` dentro del `<thead><tr>` (verificable: `grep -c '&lt;th scope="col"&gt;' index.html` devuelve 6 — los 5 existentes + el nuevo).
    - Source: el texto del botón es la cadena `Examen` LITERAL (no interpolada — `grep -q '&gt;Examen&lt;/button&gt;' index.html` exit 0).
    - Behavior: `cd /home/vcompanyb/italian-course && node --test tests/` mantiene 199 tests verdes (los tests no leen index.html así que no hay regresión esperada; el smoke test de Task 3 llega después).
    - T-02-01 invariante: CERO `x-html` añadidos (`grep -c 'x-html' index.html` devuelve 0 — verificable; el archivo no debe haber introducido x-html en este task).
  </acceptance_criteria>

  <done>
    `index.html` tiene la 6ª columna `Examen` insertada con `<th>` + `<td>` + `<button class="secondary outline" :disabled :title @click="startExamen(cat.id)">Examen</button>`. El comment de la tabla actualizado a "6 columnas exactas". `node --test tests/` mantiene 199 tests verdes. Cero CSS nuevo añadido. Cero `x-html` introducido (T-02-01 preservado). Cero modificación al resto de la pantalla home, picker, session, summary, backup.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear tests/screen-examen.test.js con 7 smoke tests presence-check</name>
  <files>tests/screen-examen.test.js</files>

  <read_first>
    - src/screens/app.js (verificar el resultado del Task 1 — confirmar nombres de los identificadores `startExamen`, `_launchExamen`, copy literales)
    - index.html (verificar el resultado del Task 2 — confirmar binding `@click="startExamen(cat.id)"` + clase del botón)
    - tests/exercise-types.test.js (líneas 736-793 OBLIGATORIO — patrón canónico presence-check con readFileSync + windowed slice; lo replicaremos aquí)
    - tests/screen-multi-choice-shuffle.test.js (estructura general de tests/screen-*.test.js como referencia visual)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-PATTERNS.md (Pattern Assignments §3 — Analog 1 + 7 tests sugeridos)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-UI-SPEC.md (§Test plan sugerido al planner — 5 tests sugeridos, expandidos a 7 aquí)
  </read_first>

  <behavior>
    - 7 sub-tests dentro de un `describe('appShell.startExamen — Phase 8 D-181..D-192', ...)` que verifican presence-check contra `src/screens/app.js` e `index.html` por lectura textual (sin instanciar Alpine — coherente con el patrón documentado en `tests/exercise-types.test.js:739-754`).
    - Cada test usa `readFileSync(new URL('../src/screens/app.js', import.meta.url), 'utf8')` o `'../index.html'` para leer el source.
    - Tests independientes (sin shared state mutable entre tests; describe-level setup vía const literales).
    - Mensajes de error de los asserts son explícitos — explican QUÉ pattern faltaba y POR QUÉ es relevante (D-181 / D-184 / D-44 / D-189 etc.).
  </behavior>

  <action>
    Crear `tests/screen-examen.test.js` (archivo nuevo — opción A de PATTERNS.md §3, preferida) con la siguiente estructura:

    **Imports (idénticos a `tests/exercise-types.test.js:14-19`):**
    - `import { test, describe } from 'node:test';`
    - `import assert from 'node:assert/strict';`
    - `import { readFileSync } from 'node:fs';`

    **Setup const a nivel describe (legible y reusable entre tests):**
    - `const appJsPath = new URL('../src/screens/app.js', import.meta.url);`
    - `const indexHtmlPath = new URL('../index.html', import.meta.url);`
    - Construir `const appSrc = readFileSync(appJsPath, 'utf8');` y `const indexSrc = readFileSync(indexHtmlPath, 'utf8');` dentro del describe (no en module scope — coherente con el patrón).
    - Calcular `const startExamenIdx = appSrc.indexOf('startExamen(');` y `const launchExamenIdx = appSrc.indexOf('_launchExamen(');` para slicing.
    - Window de inspección: `const startExamenWindow = appSrc.slice(startExamenIdx, startExamenIdx + 3000);` y `const launchExamenWindow = appSrc.slice(launchExamenIdx, launchExamenIdx + 3000);`.

    **Los 7 sub-tests (un `test('...', () => { ... })` por bloque):**

    1. `test('startExamen está definido en src/screens/app.js (Phase 8 D-181)', ...)`:
       - `assert.ok(startExamenIdx > -1, 'startExamen debe estar definido en src/screens/app.js — Phase 8 D-181/EXAM-02 entry point');`.
       - `assert.match(startExamenWindow, /startExamen\(categoryId\)/, 'startExamen debe tener firma startExamen(categoryId) — UI-SPEC línea 596-606 lockea el naming + 1-cat por diseño D-181');`.

    2. `test('_launchExamen helper privado existe y es invocado al menos 2 veces (path directo + onConfirm)', ...)`:
       - `assert.ok(launchExamenIdx > -1, '_launchExamen debe existir como helper privado');`.
       - `const callsToLaunchExamen = (appSrc.match(/\._launchExamen\(|this\._launchExamen\(/g) || []).length;`.
       - `assert.ok(callsToLaunchExamen >= 2, '_launchExamen debe tener ≥2 call-sites (directo + onConfirm post-clearInFlightTest) — PATTERNS.md §1 Analog 1');`.

    3. `test('startExamen chequea state.inFlightTest ANTES de buildFullTest (D-44 conflict)', ...)`:
       - `const inFlightIdx = startExamenWindow.indexOf('this.state.inFlightTest');`.
       - `const launchInWindow = startExamenWindow.indexOf('_launchExamen(');`.
       - `assert.ok(inFlightIdx > -1, 'startExamen debe chequear this.state.inFlightTest');`.
       - `assert.ok(inFlightIdx < launchInWindow || launchInWindow === -1, 'el chequeo de inFlightTest debe aparecer ANTES de cualquier invocación a _launchExamen — D-44 conflict pattern');`.

    4. `test('startExamen invoca requestConfirm con copy literal D-44 6ª call-site (D-183 + UI-SPEC línea 221)', ...)`:
       - `assert.match(startExamenWindow, /requestConfirm\(\{/, 'startExamen debe invocar requestConfirm (6ª call-site)');`.
       - `assert.ok(startExamenWindow.includes('Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?'), 'el message del requestConfirm debe ser literal idéntico al openPicker D-44 línea 275 (D-183 hereda copy genérica)');`.
       - `assert.ok(startExamenWindow.includes("'Descartar y empezar'") || startExamenWindow.includes('"Descartar y empezar"'), 'confirmLabel debe ser "Descartar y empezar" — planner lockea coherencia con openPicker D-44 línea 276 (UI-SPEC sugería "Continuar" — opcion deferida)');`.

    5. `test('_launchExamen invoca buildFullTest con un array de 1 categoryId (D-181 + D-189)', ...)`:
       - `assert.match(launchExamenWindow, /buildFullTest\(\[/, '_launchExamen debe invocar buildFullTest con array literal — Examen es 1-cat por diseño D-181');`.
       - `assert.match(launchExamenWindow, /this\.sessionMode\s*=\s*['"]test-completo['"]/, 'sessionMode debe setearse a "test-completo" literal — D-189 (NO this.pickerMode)');`.

    6. `test('_launchExamen setea pickerCheckedCategoryIds ANTES de persistInFlightTest (pitfall PATTERNS.md §1 Analog 2)', ...)`:
       - `const pickerCheckedIdx = launchExamenWindow.indexOf('this.pickerCheckedCategoryIds = [');`.
       - `const persistIdx = launchExamenWindow.indexOf('this.persistInFlightTest()');`.
       - `assert.ok(pickerCheckedIdx > -1, '_launchExamen debe setear this.pickerCheckedCategoryIds = [catId] — pitfall del fallback prev?.categoryIds en persistInFlightTest');`.
       - `assert.ok(persistIdx > -1, '_launchExamen debe invocar this.persistInFlightTest() — D-182 slot único compartido, SIEMPRE persiste');`.
       - `assert.ok(pickerCheckedIdx < persistIdx, 'pickerCheckedCategoryIds debe setearse ANTES de persistInFlightTest — el fallback prev?.categoryIds capturaría las cats del Test anterior');`.

    7. `test('index.html tiene 6ª columna Examen con botón secondary outline y binding al handler (D-184 + UI-SPEC línea 115)', ...)`:
       - `assert.ok(indexSrc.includes('<th scope="col">Examen</th>'), 'index.html debe tener <th scope="col">Examen</th> nuevo — UI-SPEC línea 184');`.
       - `assert.ok(indexSrc.includes('class="secondary outline"'), 'botón Examen debe tener class="secondary outline" — UI-SPEC línea 115 (nivel 3 jerarquía visual)');`.
       - `assert.ok(indexSrc.includes('startExamen(cat.id)'), 'index.html debe tener @click="startExamen(cat.id)" binding al handler');`.
       - `assert.ok(indexSrc.includes(':disabled="!cat.examenEnabled"'), 'el botón Examen debe enlazar :disabled a cat.examenEnabled (D-187 disabled cat-0)');`.
       - `assert.ok(indexSrc.includes(':title="cat.examenTooltip"'), 'el botón Examen debe enlazar :title a cat.examenTooltip (D-187 tooltip cat-0)');`.

    **NO incluir:**
    - Tests que instancian `appShell` o invocan handlers a runtime (el factory NO es trivialmente instanciable bajo node sin Alpine — `tests/exercise-types.test.js:739-754` lo documenta).
    - Tests de comportamiento de `buildFullTest` (ya cubierto por `tests/domain-session.test.js:134-178` para categoryIds 1-cat).
    - Tests de DOMAIN-04 promoción a `hecha` tras Examen sin fallos (ya cubierto por tests existentes del dominio — D-190 explícito: cero test nuevo necesario).
    - Tests de la copy genérica del banner reanudar (D-183 — sin cambios al banner; ya cubierto por tests Phase 2/4 de inFlightTest).

    **Cierre del archivo:** ningún export (es un test file).

    Si Node arroja errores de sintaxis al ejecutar el archivo nuevo: revisar imports, `import.meta.url`, comillas (apóstrofes ASCII U+0027 estrictos — coherencia con CONT-06 / D-129).
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --check tests/screen-examen.test.js &amp;&amp; node --test tests/screen-examen.test.js 2>&amp;1 | tail -15 &amp;&amp; node --test tests/ 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Source: `tests/screen-examen.test.js` existe y `node --check tests/screen-examen.test.js` exit 0 (sintaxis válida).
    - Behavior: `node --test tests/screen-examen.test.js` muestra ≥ 7 sub-tests verdes con 0 fallos.
    - Behavior: `cd /home/vcompanyb/italian-course && node --test tests/` (suite completa) devuelve `pass ≥ 206` (199 baseline + 7 nuevos mínimo), `fail 0`.
    - Source: el archivo tiene exactamente 1 `describe('appShell.startExamen — Phase 8 D-181..D-192', ...)` block.
    - Source: el archivo tiene ≥ 7 `test('...', ...)` blocks dentro del describe (verificable: `grep -c "^  test\\|^    test\\|^test\\|^\\s\\+test(" tests/screen-examen.test.js` ≥ 7 — ajustar el regex a la indentación que uses).
    - Source: usa apóstrofes ASCII U+0027 estrictos (no smart quotes — `grep -c '[’‘”“]' tests/screen-examen.test.js` debe ser 0).
    - Source: cero invocaciones a `appShell(` o factory instantiation (el patrón es presence-check, no behavioural — `grep -c 'appShell(' tests/screen-examen.test.js` debe ser 0).
    - CLI: el archivo no tiene tests skipped — todos los 7 corren (`grep -c "{ skip:" tests/screen-examen.test.js` debe ser 0, salvo que un test requiera skip condicional con razón documentada, en cuyo caso DEBE incluir la razón en el skip string).
  </acceptance_criteria>

  <done>
    `tests/screen-examen.test.js` existe con 7 smoke tests presence-check verdes. La suite completa `node --test tests/` reporta ≥ 206 tests verdes (199 baseline + 7 nuevos). Cero tests skipped. Cero factory instantiation. El patrón presence-check sigue el precedente `tests/exercise-types.test.js:736-793` (windowed slice + readFileSync). El gap "comportamiento runtime end-to-end" lo cierra UAT humano post-merge — coherente con el trade-off documentado en Phase 3+.
  </done>
</task>

<task type="auto">
  <name>Task 4: Audit trail consolidado (PROJECT.md + REQUIREMENTS.md + ROADMAP.md + STATE.md + commit final)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-01-SUMMARY.md</files>

  <read_first>
    - .planning/PROJECT.md (verás la sección `### Validated` con las entries de Phase 1..7.2 — patrón a continuar)
    - .planning/REQUIREMENTS.md (verás la subsección Phase 7.2 EXPL-09..14 + la tabla Traceability completa hasta línea 213 — patrón canónico a continuar)
    - .planning/ROADMAP.md (verás §Phase 8 actualmente con `**Goal:** [To be planned]` + `**Requirements**: TBD` + `**Plans:** 0 plans`; verás también la tabla Progress línea 219-229)
    - .planning/STATE.md (verás "Tests dominio + UI smoke | 145 verdes ..." en la tabla Performance Metrics — actualizar a 199+N donde N = tests añadidos en Task 3)
    - .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-CONTEXT.md (D-181..D-192 — copy de las decisiones a destilar en el SUMMARY)
    - .planning/phases/07-explicaciones-pedag-gicas-al-fallar-campo-explanation-por-ej/ (ejemplo del shape de un SUMMARY de fase completa — opcional, solo si necesitas referencia visual)
    - src/screens/app.js + index.html + tests/screen-examen.test.js (verificar el estado final post-Task 1-3 — necesario para componer el SUMMARY con commits + métricas accurate)
  </read_first>

  <action>
    Ejecutar 5 actualizaciones documentales en orden + 1 commit final atómico:

    **Actualización 1 — `.planning/PROJECT.md`:**
    - Añadir nueva entrada al final de `### Validated` (después de la última entry Phase 7.2):
      ```
      - **Phase 8 — Modo Examen por categoría (2026-05-25):** botón "Examen" en cada fila de la tabla home (6ª columna nueva) que arranca un Test completo de SOLO esa categoría con 1 click directo (D-181). Reutiliza `buildFullTest([catId])` (D-50), slot único `inFlightTest` (D-182), patrón D-44 6ª call-site `requestConfirm` (copy literal idéntica a openPicker — D-183). Disabled + tooltip "No hay ejercicios en esta categoría" cuando `totalCount === 0` (D-187); cats `hecha`/`dominada` siguen enabled normales. Cero migración schemaVersion (D-192), cero CSS nuevo, cero módulos nuevos. Resuelve el dolor "5-6 Repasos para validar dominio de una sola categoría" capturado durante uso real post-Phase 7.2.
      ```
    - Añadir fila a la tabla Key Decisions (después de la última fila Phase 7.2):
      ```
      | 2026-05-25 | Phase 8 — 6ª call-site `requestConfirm` con copy literal D-44 idéntica + confirmLabel `Descartar y empezar` lockeado | Patrón unificado de Phase 2 D-44 — coherencia textual entre las 6 call-sites del helper. UI-SPEC línea 222 sugería `Continuar` para coherencia con D-27; planner lockeó `Descartar y empezar` por proximidad semántica con openPicker (mismo message, mismo intent). Homogeneización de las 6 call-sites diferida (out of scope Phase 8). |
      ```

    **Actualización 2 — `.planning/REQUIREMENTS.md`:**
    - Añadir nueva sección antes de `## v2 Requirements` (línea 112 aprox):
      ```
      ### Modo Examen por categoría (EXAM) — Phase 8

      - [x] **EXAM-01**: La tabla home muestra una 6ª columna `Examen` con `<th scope="col">Examen</th>` + un botón `<button class="secondary outline">Examen</button>` por fila. Etiqueta plana `Examen` (D-185), sin paréntesis con número de ejercicios, sin glifo direccional. Validado: Plan 08-01 Task 2 (index.html) + Task 3 (smoke test 7).
      - [x] **EXAM-02**: Click en `Examen` de una categoría con `examenEnabled === true` y `state.inFlightTest === null` → invoca `_launchExamen(catId)` que arranca `buildFullTest([catId], allExercises)` + `sessionMode='test-completo'` + `persistInFlightTest()` + `currentScreen='session'`. Sin pasar por picker (D-181). Validado: Plan 08-01 Task 1 (handler) + Task 3 (smoke tests 1, 5).
      - [x] **EXAM-03**: Click en `Examen` con `state.inFlightTest !== null` → invoca `requestConfirm` 6ª call-site con copy literal `Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?` + confirmLabel `Descartar y empezar` + cancelLabel `Cancelar` (D-44 patrón clonado idéntico, coherencia textual con openPicker D-44 línea 275-276). `onConfirm` ejecuta `clearInFlightTest()` + `_launchExamen(catId)`. Validado: Plan 08-01 Task 1 + Task 3 (smoke tests 3, 4).
      - [x] **EXAM-04**: Persistencia `inFlightTest` funciona igual que Test completo regular — slot único compartido (D-182). El banner home reanudar mantiene copy genérica `Tienes un Test completo a medias` (D-183). Reanudar reconstruye con `buildFullTest` sobre las MISMAS `categoryIds` persistidas (`['avere']` en el ejemplo). Cero migración schemaVersion (sigue 4 — D-192). Validado: Plan 08-01 Task 1 (`pickerCheckedCategoryIds = [catId]` ANTES de `persistInFlightTest()` — pitfall PATTERNS.md §1 Analog 2 resuelto) + Task 3 (smoke test 6).
      - [x] **EXAM-05**: Botón disabled (Pico `:disabled` opacity ~0.5 + cursor not-allowed) + tooltip nativo `No hay ejercicios en esta categoría` cuando `cat.totalCount === 0` (D-187). Categorías en estado `hecha` o `dominada` siguen con el botón enabled normal (D-187 — el autor quiere poder re-examinar para reconfirmar dominio). Derivado del computed `categoriesForDisplay.examenEnabled = (exercisesByCat[cat.id] ?? []).length > 0`. Validado: Plan 08-01 Task 1 (computed extension) + Task 2 (bindings `:disabled` + `:title` en el botón) + Task 3 (smoke test 7).
      ```
    - Añadir las 5 filas a la tabla Traceability (después de EXPL-14 línea 213 aprox):
      ```
      | EXAM-01 | Fase 8 | Complete (Plan 08-01) |
      | EXAM-02 | Fase 8 | Complete (Plan 08-01) |
      | EXAM-03 | Fase 8 | Complete (Plan 08-01) |
      | EXAM-04 | Fase 8 | Complete (Plan 08-01) |
      | EXAM-05 | Fase 8 | Complete (Plan 08-01) |
      ```
    - Actualizar el bloque Coverage (final del archivo, alrededor de línea 215):
      ```
      **Coverage:**
      - v1 requirements: **62** total (57 post-Phase 7.2 + 5 EXAM-01..05 Phase 8)
      - Mapped to phases: 62 (100%)
      - Unmapped: 0
      - **Completed: 62/62 (100%)** tras Phase 8 cierre. [...]
      ```
    - Actualizar el footer del archivo (`*Last updated:*`) con `2026-05-25 after Phase 8 completion (EXAM-01..05 cerrados — botón Examen por categoría en home + handler startExamen + helper _launchExamen + 6ª columna tabla + 7 smoke tests presence-check). 62/62 v1 requirements complete; milestone v1.0 ampliado con feature Examen.`.

    **Actualización 3 — `.planning/ROADMAP.md`:**
    - Localizar la línea `- [ ] **Phase 8: Modo Examen por categoría — ...**` en la sección "Phases" (línea ~6 — aparece como entrada pendiente con `[ ]` checkbox). **Marcarla como `[x]`** y añadirle `(completed 2026-05-25)` al final.
    - Localizar la sección `### Phase 8: Modo Examen por categoría — ...` en la Backlog (línea 287-295 aprox) — esa sección actualmente dice `**Goal:** [To be planned]` + `**Requirements**: TBD` + `**Plans:** 0 plans`. **Moverla** desde la sección Backlog a la sección "## Phase Details" (justo después de §Phase 7.2 línea 217). Actualizar contenido:
      ```
      ### Phase 8: Modo Examen por categoría

      **Goal**: El autor puede examinar una categoría individual con 1 click desde la tabla home (botón `Examen` por fila) que arranca un Test completo de SOLO esa categoría — sin pasar por el picker. Reutiliza `buildFullTest`, slot único `inFlightTest`, y el patrón D-44 6ª call-site del helper `requestConfirm` para conflict con Test completo activo. Cero migración schemaVersion. Resuelve el dolor canónico "5-6 Repasos para validar dominio de una sola categoría" capturado post-Phase 7.2.
      **Mode:** standard
      **Depends on**: Phase 7.2
      **Requirements**: EXAM-01, EXAM-02, EXAM-03, EXAM-04, EXAM-05
      **Success Criteria** (qué tiene que ser CIERTO):

        1. La tabla home tiene una 6ª columna `Examen` al final de cada fila con un `<button class="secondary outline">Examen</button>` (D-184/D-185).
        2. Click `Examen` en categoría con ejercicios > 0 y sin Test completo activo → arranca directamente sesión Test completo de SOLO esa categoría (1 click, salta picker — D-181/D-189).
        3. Click `Examen` con `state.inFlightTest !== null` → aparece confirmación inline 6ª call-site D-44 con copy `Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?` (D-183 — copy genérica heredada del banner reanudar).
        4. Categoría con 0 ejercicios → botón disabled + tooltip `No hay ejercicios en esta categoría` (D-187). Categoría `hecha`/`dominada` → botón enabled normal.
        5. Persistencia `inFlightTest` funciona igual que Test completo regular — slot único compartido (D-182). El banner reanudar mantiene copy genérica. Reanudar usa las MISMAS `categoryIds` persistidas.
        6. Cero migración schemaVersion (sigue 4 — D-192). Cero CSS nuevo (UI-SPEC §CSS additions). Cero módulos nuevos.

      **Plans**: 1 plan

      - [x] 08-01-PLAN.md — Handler `startExamen(categoryId)` + helper `_launchExamen(catId)` + extensión computed `categoriesForDisplay` (examenEnabled + examenTooltip) + 6ª columna tabla home + 7 smoke tests presence-check + audit trail consolidado. **completado 2026-05-25** (tests count 199 → 206+, EXAM-01..05 cerrados, milestone v1.0 sigue listo + Modo Examen incremental).
      ```
    - Eliminar la entrada original duplicada de la sección Backlog (sección `### Phase 8: ...` antigua líneas 287-295).
    - Actualizar la tabla Progress (línea 219-229 aprox) — añadir fila:
      ```
      | 8. Modo Examen por categoría | 1/1 | Complete   | 2026-05-25 |
      ```
    - Actualizar el bloque Coverage Summary (alrededor de línea 233):
      ```
      - **v1 requirements:** **62** total (57 Phase 1..7.2 + 5 EXAM-01..05 Phase 8)
      - **Mapped to phases:** 62 (100%)
      ```
    - Extender el diagrama Dependency Graph para incluir Phase 8 después de Phase 7.
    - Actualizar el footer `*Last updated:*` con `2026-05-25 after Phase 8 completion (1 plan ejecutado: 08-01 botón Examen por categoría). 206+/206+ tests verdes (199 baseline + 7 sub-tests presence-check del nuevo screen-examen.test.js). EXAM-01..05 cerrados; milestone v1.0 ampliado con Modo Examen incremental.`.

    **Actualización 4 — `.planning/STATE.md`:**
    - Localizar la tabla Performance Metrics (línea 34-46 aprox). Actualizar la línea "Tests dominio + UI smoke" — el valor actual dice "145 verdes ..." (stale post-Phase 5). **Corregir** al baseline post-Phase 7.2: `199 verdes (post-Phase 7.2) → 206+ verdes (post-Phase 8 — +7 smoke tests presence-check del screen-examen.test.js)`. Si el número exacto post-Task 3 difiere de 206, usar el número real reportado por `node --test tests/ 2>&1 | grep 'pass ' | tail -1`.
    - Actualizar la línea "Fases completadas" con el nuevo conteo (Phase 1-7.2 + Phase 8 = 8 fases completas).
    - Actualizar la sección "Roadmap Evolution" — añadir entrada:
      ```
      - Phase 8 completada (2026-05-25): Modo Examen por categoría implementado. 1 plan ejecutado (08-01). Botón `Examen` en cada fila de la tabla home con `class="secondary outline"` que arranca Test completo de 1 cat directo. Handler `startExamen(categoryId)` + helper `_launchExamen(catId)` + 2 campos derivados en `categoriesForDisplay`. 6ª call-site del helper `requestConfirm` (D-44 patrón clonado). Tests 199 → 206+ verdes (+7 smoke tests presence-check en `tests/screen-examen.test.js`).
      ```
    - Actualizar `Current Position` / `Current Focus` con `Phase 8 — Modo Examen por categoría (completed 2026-05-25 — pending verifier)`.
    - Actualizar el footer `*Last updated:*` correspondiente.

    **Actualización 5 — Crear `.planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-01-SUMMARY.md`:**
    - Estructura mínima:
      - Frontmatter YAML con `phase: 08`, `plan: 01`, `completed: 2026-05-25`, `status: complete`, `tests_pass: 206+` (sustituir por el número real), `commits: [hashes — completar en commit]`.
      - Sección `## Executive Summary` con 3-5 líneas explicando: feature entregado (Examen 1-click desde home), D-181..D-192 honored, 6ª call-site D-44 + helper privado `_launchExamen`, 7 smoke tests presence-check, cero migración, EXAM-01..05 cerrados.
      - Sección `## What Was Built` con 4 sub-secciones: (1) Handler + helper en `src/screens/app.js`, (2) 6ª columna `Examen` en `index.html`, (3) 7 smoke tests en `tests/screen-examen.test.js`, (4) Audit trail consolidado (PROJECT.md + REQUIREMENTS.md + ROADMAP.md + STATE.md).
      - Sección `## Key Decisions` listando las 3 decisiones del planner sobre PATTERNS.md surfaced:
        - `_launchExamen(catId)` extraído como helper privado (2 call-sites — directo + onConfirm — extracción wins on readability).
        - `confirmLabel = 'Descartar y empezar'` lockeado (coherencia con openPicker D-44; UI-SPEC sugería `'Continuar'` — homogeneización deferida).
        - Test file nuevo `tests/screen-examen.test.js` (Opción A PATTERNS.md §3, per-feature consistency).
      - Sección `## Pitfalls Avoided` listando: `pickerCheckedCategoryIds = [catId]` ANTES de `persistInFlightTest` (PATTERNS.md §1 Analog 2 sutil), reset superset completo (no subset de `startSession` — cancelMatchFlash necesario porque el path conflict D-44 puede venir de mid-match), texto `Examen` literal HTML (NO interpolado — T-02-01 invariante).
      - Sección `## Tests Added` con la lista de los 7 sub-tests y el archivo.
      - Sección `## Files Modified / Created` listando los 7 paths (src/screens/app.js, index.html, tests/screen-examen.test.js, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-.../08-01-SUMMARY.md).
      - Sección `## Captured for Future Phase` listando los items deferred del CONTEXT.md `<deferred>` (Examen multi-cat, atajos de teclado, copy especializada en banner reanudar, etc.) + uno nuevo capturado por el planner: "Homogeneizar las 6 call-sites del helper `requestConfirm` con confirmLabel unificado (`'Continuar'` o `'Descartar y empezar'` — actualmente mezclado entre call-sites)".
      - Sección `## Next Steps` con: "Phase 8 verifier pass → milestone v1.0 ampliado close (62/62 v1 requirements complete + Modo Examen incremental)".

    **Commit final atómico** (al terminar TODAS las actualizaciones):
    - `cd /home/vcompanyb/italian-course && git add src/screens/app.js index.html tests/screen-examen.test.js .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-01-SUMMARY.md`.
    - `git commit -m "feat(08-01): Phase 8 Modo Examen por categoría — botón Examen por fila home + handler startExamen + helper _launchExamen + 6ª columna tabla + 7 smoke tests + audit trail EXAM-01..05"` (mensaje sin emojis decorativos, en español o español/inglés mezclado según convención del repo — verificar `git log --oneline -10` antes de commitear para imitar el estilo existente).

    **NO commitear durante Task 1, Task 2, Task 3** (audit trail consolidado en commit único final — coherencia con el patrón Phase 7.2-05 Task 3 ATÓMICA).

    Si `node --test tests/` deja de ser ≥ 206 verdes durante este task (i.e., algún rewrite de PROJECT.md/REQUIREMENTS.md rompe algo): NO commitear; reportar el fallo.
  </action>

  <verify>
    <automated>cd /home/vcompanyb/italian-course && node --test tests/ 2>&amp;1 | tail -5 &amp;&amp; grep -q 'EXAM-01' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'EXAM-05' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Phase 8 — Modo Examen' .planning/PROJECT.md &amp;&amp; grep -q '8. Modo Examen' .planning/ROADMAP.md &amp;&amp; test -f .planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-01-SUMMARY.md &amp;&amp; git log --oneline -1 | grep -q '08-01'</automated>
  </verify>

  <acceptance_criteria>
    - Behavior: `cd /home/vcompanyb/italian-course && node --test tests/` mantiene ≥ 206 tests verdes (199 baseline + 7 nuevos del Task 3), `fail 0`.
    - Source: `grep -q 'Phase 8 — Modo Examen por categoría' .planning/PROJECT.md` exit 0 (entrada Validated añadida).
    - Source: `grep -c 'EXAM-0[1-5]' .planning/REQUIREMENTS.md` devuelve ≥ 10 (5 IDs × ≥2 ocurrencias cada uno: una en la sub-section EXAM + una en la tabla Traceability).
    - Source: `grep -q '62 total' .planning/REQUIREMENTS.md || grep -q '62/62' .planning/REQUIREMENTS.md` exit 0 (Coverage actualizada).
    - Source: `grep -q '\[x\] \*\*Phase 8:' .planning/ROADMAP.md` exit 0 (checkbox de la sección Phases marcado).
    - Source: `.planning/ROADMAP.md` ya NO tiene `**Goal:** [To be planned]` para Phase 8 — `grep -B5 -A5 'Phase 8' .planning/ROADMAP.md | grep '[To be planned]'` devuelve 0 líneas (la sección Backlog antigua eliminada o reemplazada).
    - Source: `grep -q '8. Modo Examen por categoría' .planning/ROADMAP.md` exit 0 (entrada en tabla Progress).
    - Source: `grep -q '08-01-PLAN.md' .planning/ROADMAP.md` exit 0 (plan listado).
    - Source: `.planning/STATE.md` actualizado — `grep -q 'Phase 8' .planning/STATE.md` exit 0 + tests count actualizado.
    - Source: archivo `.planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-01-SUMMARY.md` existe con frontmatter YAML válido + las 8 secciones listadas en el action.
    - CLI: `git log --oneline -1` muestra un commit reciente con prefijo `feat(08-01)` o similar (commit final atómico ejecutado).
    - CLI: `git status` muestra working tree clean (sin uncommitted changes — todos los 8 archivos del plan están en el commit).
    - Source: cero emojis decorativos en cualquiera de los archivos documentales modificados (`grep -c '🎯\|📝\|✅\|🚀' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/08-.../08-01-SUMMARY.md` devuelve 0 — coherencia con CLAUDE.md / project conventions, salvo emojis preexistentes en el archivo antes de este plan).
  </acceptance_criteria>

  <done>
    Phase 8 cerrada documentalmente. PROJECT.md `### Validated` extendido con entrada Phase 8 + Key Decisions ampliada con la decisión del confirmLabel. REQUIREMENTS.md tiene la nueva sub-section EXAM-01..05 + 5 filas Traceability + Coverage actualizado a 62/62 (100%). ROADMAP.md Phase 8 movida de Backlog a Phase Details con goal + success criteria + plan listado + checkbox marcado + tabla Progress extendida + Coverage Summary actualizado + Dependency Graph extendido. STATE.md actualizado con tests count + roadmap evolution entry + current position. SUMMARY.md creado con 8 secciones requeridas. Commit final atómico ejecutado con los 8 archivos. `node --test tests/` ≥ 206 verdes preservado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→DOM (click handlers) | El autor pulsa el botón Examen — único punto de entrada nuevo de Phase 8. Trigger sincronico de mutaciones de estado Alpine. |
| JSON content → DOM (categoriesForDisplay) | El campo `cat.name` (categoría) viene del JSON validado por schema validator; el botón Examen NO interpola ningún contenido del JSON — solo lee `cat.id` (slug ASCII) y `cat.examenEnabled`/`cat.examenTooltip` que son derivados internos. |
| localStorage → state | `state.inFlightTest` (chequeado al click) viene de localStorage; el chequeo es `!== null`, no se interpola contenido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Texto del botón `Examen` en `index.html` | mitigate | Texto LITERAL HTML hardcoded, NO `x-text`, NO interpolado desde JSON. Cero superficie de injection. Reforzado por smoke test (Task 3 sub-test 7 — `grep -q '>Examen</button>' index.html`). |
| T-08-02 | Tampering | Tooltip `:title="cat.examenTooltip"` | accept | El valor de `cat.examenTooltip` es controlado por el computed `categoriesForDisplay` (literal: cadena vacía o `'No hay ejercicios en esta categoría'`). NO viene del JSON. El navegador escapa `title` por defecto. Cero superficie XSS. |
| T-08-03 | Tampering | Handler `startExamen(categoryId)` recibe `cat.id` | accept | `cat.id` es slug ASCII validado por schema validator (CONT-02). El handler lo pasa LITERAL a `buildFullTest([catId])` que filtra por igualdad estricta — no SQL injection, no path traversal aplicable a la app estática. |
| T-08-04 | Information Disclosure | `state.inFlightTest` chequeado al click | accept | Toda la app es single-user local FOUND-04. No hay leakage entre usuarios. |
| T-08-05 | Denial of Service | `buildFullTest([catId])` sobre una cat con 51 ejercicios (max actual — Profesiones) | accept | Pool máximo = 51 ejercicios; `buildFullTest` es O(n log n) con Fisher-Yates. Resuelve en <1ms — UI-SPEC línea 581 lo confirma. No requiere loading state. |
| T-08-06 | Spoofing / Repudiation | n/a | n/a | App single-user FOUND-04 — sin login, sin sesiones, sin audit log. Fuera de surface. |
| T-08-XSS | XSS preservation | Refuerzo T-02-01 invariante | mitigate | Cero `x-html` añadidos (Task 2 acceptance criterion explícito). Cero interpolación de contenido JSON en strings nuevos. Verificado por `grep -c 'x-html' index.html` debe ser 0 (asumiendo baseline 0 pre-plan). |

**Resumen seguridad Phase 8:** **superficie de threat MUY reducida**. Todos los textos nuevos (botón label `Examen`, tooltip `No hay ejercicios en esta categoría`, header `<th>Examen`, confirm message `Ya hay un Test completo en curso. ¿Descartarlo y empezar uno nuevo?`) son **hardcoded LITERAL** en `src/screens/app.js` e `index.html`. Cero superficie XSS nueva. T-02-01 anti-XSS invariante reforzado por construcción.

**Package legitimacy gate (npm/pip/cargo):** N/A — Phase 8 introduce 0 dependencias externas nuevas, 0 npm installs, 0 CDN packages nuevos. Stack queda 100% locked al estado post-Phase 7.2 (Alpine 3.15.12 + Pico 2.1.1 SRI-pinned desde Phase 1).
</threat_model>

<verification>
## Phase-level verification

Tras ejecutar los 4 tasks atómicamente:

1. **Smoke baseline preserved:** `cd /home/vcompanyb/italian-course && node --test tests/ 2>&1 | tail -5` reporta ≥ 206 tests verdes (199 baseline + 7 nuevos), `fail 0`.

2. **Source artifacts present:** todos los identificadores listados en `must_haves.artifacts` verificables por grep.

3. **Key links wired:** todos los patrones `key_links.pattern` matchean en los archivos correctos.

4. **Audit trail consolidado:** PROJECT.md + REQUIREMENTS.md + ROADMAP.md + STATE.md + 08-01-SUMMARY.md actualizados; cero `[To be planned]` o `TBD` residual para Phase 8.

5. **Threat model honored:** `grep -c 'x-html' index.html` igual al baseline pre-plan (0 nuevos); cero interpolación de JSON content en strings nuevos.

6. **Git history clean:** commit atómico final con los 8 archivos del plan; `git status` muestra working tree clean.

7. **UAT humano post-merge** (opcional pero recomendado — coherente con el patrón Phase 4+/5/6/7):
   - El autor arranca `npx serve`, abre `http://localhost:3000`.
   - Verifica visualmente: la tabla home tiene 6 columnas; la 6ª es `Examen`; cada fila tiene un botón outline gris muted.
   - Click `Examen` en una cat con ejercicios > 0 (e.g., Avere) sin Test completo activo → arranca sesión con `Ejercicio 1 / 23` (Avere tiene 23).
   - Click `Examen` con Test completo activo previo → aparece confirm inline top-right; Cancelar deja state; Continuar arranca el Examen nuevo descartando el previo.
   - (Opcional) Crear manualmente una cat vacía en `content/categories.json` con `id: 'test-vacia'` y verificar que el botón aparece disabled + tooltip al hover.
   - Verificar reanudar: arranca Examen, cierra tab a mitad, reabre — banner home reanudar visible con copy genérica → Reanudar → sesión continúa con los mismos ejercicios.
</verification>

<success_criteria>
**Plan 08-01 está completo cuando:**

- [ ] `src/screens/app.js` tiene `startExamen(categoryId)` + `_launchExamen(catId)` definidos + `categoriesForDisplay` extendido con `examenEnabled` + `examenTooltip`.
- [ ] `index.html` tiene 6ª columna `Examen` con `<th>` + `<td>` + `<button class="secondary outline">` con bindings `:disabled` + `:title` + `@click="startExamen(cat.id)"`.
- [ ] `tests/screen-examen.test.js` existe con ≥ 7 smoke tests presence-check verdes.
- [ ] `node --test tests/` reporta ≥ 206 verdes (199 baseline + 7 nuevos), `fail 0`.
- [ ] `.planning/PROJECT.md` `### Validated` extendido + Key Decisions ampliada.
- [ ] `.planning/REQUIREMENTS.md` tiene sub-section EXAM-01..05 + Traceability extendida + Coverage 62/62 (100%).
- [ ] `.planning/ROADMAP.md` Phase 8 movida de Backlog a Phase Details con checkbox marcado + plan listado.
- [ ] `.planning/STATE.md` actualizado con tests count + roadmap evolution + current position.
- [ ] `.planning/phases/08-.../08-01-SUMMARY.md` creado con 8 secciones.
- [ ] Commit final atómico ejecutado con los 8 archivos.
- [ ] Threat model T-02-01 anti-XSS invariante preservado (cero `x-html`, cero interpolación nueva).
- [ ] Cero CSS nuevo añadido (UI-SPEC §CSS additions lockea CERO).
- [ ] Cero migración schemaVersion (sigue 4 — D-192).
- [ ] Cero módulos nuevos creados.
</success_criteria>

<output>
Crear `.planning/phases/08-modo-examen-por-categor-a-bot-n-examen-al-lado-de-cada-categ/08-01-SUMMARY.md` al completar el plan (parte del Task 4).
</output>
